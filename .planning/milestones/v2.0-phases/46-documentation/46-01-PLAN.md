---
phase: 46-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/getting-started.md
  - docs/core-concepts.md
  - docs/actions-and-flow.md
  - docs/dice-and-scoring.md
  - docs/ui-components.md
  - docs/custom-ui-guide.md
  - docs/component-showcase.md
  - docs/ai-system.md
  - docs/common-pitfalls.md
  - docs/element-enrichment.md
  - docs/llm-overview.md
  - docs/README.md
autonomous: true

must_haves:
  truths:
    - "All documentation uses boardsmith imports instead of @boardsmith/* imports"
    - "No grep results for @boardsmith/ in docs/ directory"
    - "Code examples remain syntactically valid after import updates"
  artifacts:
    - path: "docs/getting-started.md"
      provides: "Entry point documentation with correct imports"
      contains: "from 'boardsmith'"
    - path: "docs/ui-components.md"
      provides: "UI documentation with boardsmith/ui imports"
      contains: "from 'boardsmith/ui'"
    - path: "docs/ai-system.md"
      provides: "AI documentation with boardsmith/ai imports"
      contains: "from 'boardsmith/ai'"
  key_links:
    - from: "docs/*.md"
      to: "package.json exports"
      via: "import paths match subpath exports"
      pattern: "from 'boardsmith(/[a-z-]+)?'"
---

<objective>
Update all existing documentation to use the new `boardsmith` import paths.

Purpose: After the v2.0 monorepo collapse, all `@boardsmith/*` packages are now subpath exports of the single `boardsmith` package. Documentation must reflect this change so users can copy-paste working code.

Output: All 12 docs/*.md files updated with correct import paths.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-documentation/46-CONTEXT.md
@.planning/phases/46-documentation/46-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all @boardsmith/* imports in documentation</name>
  <files>
    docs/getting-started.md
    docs/core-concepts.md
    docs/actions-and-flow.md
    docs/dice-and-scoring.md
    docs/ui-components.md
    docs/custom-ui-guide.md
    docs/component-showcase.md
    docs/ai-system.md
    docs/common-pitfalls.md
    docs/element-enrichment.md
    docs/llm-overview.md
    docs/README.md
  </files>
  <action>
    Read each documentation file and perform these import path replacements:

    | Old Pattern | New Pattern |
    |-------------|-------------|
    | `from '@boardsmith/engine'` | `from 'boardsmith'` |
    | `from '@boardsmith/ui'` | `from 'boardsmith/ui'` |
    | `from '@boardsmith/session'` | `from 'boardsmith/session'` |
    | `from '@boardsmith/testing'` | `from 'boardsmith/testing'` |
    | `from '@boardsmith/ai'` | `from 'boardsmith/ai'` |
    | `from '@boardsmith/ai-trainer'` | `from 'boardsmith/ai-trainer'` |
    | `from '@boardsmith/client'` | `from 'boardsmith/client'` |
    | `from '@boardsmith/server'` | `from 'boardsmith/server'` |
    | `from '@boardsmith/runtime'` | `from 'boardsmith/runtime'` |
    | `from '@boardsmith/worker'` | `from 'boardsmith/worker'` |
    | `from '@boardsmith/eslint-plugin'` | `from 'boardsmith/eslint-plugin'` |
    | `'@boardsmith/ui/animation/drag-drop.css'` | `'boardsmith/ui/animation/drag-drop.css'` |
    | `'@boardsmith/ui/animation/card-flip.css'` | `'boardsmith/ui/animation/card-flip.css'` |

    Also update any text references (not just code):
    - `@boardsmith/engine` -> `boardsmith` in prose
    - `@boardsmith/*` packages -> `boardsmith` subpath exports

    IMPORTANT: Only change import paths. Do NOT modify the surrounding code or explanatory text beyond updating package names.
  </action>
  <verify>
    Run: `grep -r "@boardsmith/" docs/ | grep -v "\.planning" | wc -l`
    Expected: 0 (no old import patterns remaining)

    Run: `grep -r "from 'boardsmith" docs/ | head -5`
    Expected: Shows updated imports using new pattern
  </verify>
  <done>
    - All 86 @boardsmith/* occurrences replaced with boardsmith equivalents
    - grep "@boardsmith/" docs/ returns 0 results
    - Code examples use correct subpath imports
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify code examples remain syntactically valid</name>
  <files>
    docs/getting-started.md
    docs/ui-components.md
    docs/actions-and-flow.md
  </files>
  <action>
    Review the updated documentation files to ensure code examples are syntactically valid:

    1. Check that all import statements have correct syntax (quotes match, semicolons if present)
    2. Check that multi-line imports remain properly formatted
    3. Check that destructured imports (e.g., `{ Card as BaseCard }`) are preserved correctly

    If any code block looks malformed after the import update, fix it.

    Focus on the files with the most code examples:
    - getting-started.md (introductory examples)
    - ui-components.md (30 imports - highest count)
    - actions-and-flow.md (flow system examples)
  </action>
  <verify>
    Manually review 3-5 code blocks in each file to confirm:
    - Import statements are complete
    - No partial substitutions (e.g., `from 'boardsmith/ui'/ui'`)
    - TypeScript syntax is valid
  </verify>
  <done>
    - No malformed code blocks in documentation
    - Import syntax is correct in all examples
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Run: `grep -r "@boardsmith/" docs/`
   Expected: No results (all old imports updated)

2. Run: `grep -c "from 'boardsmith" docs/*.md | grep -v ":0"`
   Expected: Multiple files show updated imports

3. Spot-check getting-started.md imports use `from 'boardsmith'` for engine types
</verification>

<success_criteria>
- [ ] All 86 @boardsmith/* imports replaced with boardsmith equivalents
- [ ] grep "@boardsmith/" docs/ returns 0 results
- [ ] Code examples remain syntactically valid
- [ ] DOC-01 and DOC-02 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/46-documentation/46-01-SUMMARY.md`
</output>
