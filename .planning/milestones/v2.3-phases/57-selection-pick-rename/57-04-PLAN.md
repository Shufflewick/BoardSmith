---
phase: 57-selection-pick-rename
plan: 04
type: execute
wave: 4
depends_on: ["57-01", "57-02", "57-03"]
files_modified:
  - docs/core-concepts.md
  - docs/custom-ui-guide.md
  - docs/actions-and-flow.md
  - docs/common-pitfalls.md
  - docs/api/session.md
  - docs/api/ui.md
  - docs/api/index.md
  - docs/getting-started.md
  - docs/ui-components.md
  - docs/component-showcase.md
  - docs/element-enrichment.md
  - docs/ai-system.md
  - docs/architecture.md
  - docs/llm-overview.md
  - src/cli/slash-command/instructions.md
  - src/cli/slash-command/aspects/square-grid.md
  - src/cli/slash-command/aspects/playing-cards.md
  - src/cli/slash-command/generate-ai-instructions.md
autonomous: true

must_haves:
  truths:
    - "Documentation uses pick terminology for user-facing concepts"
    - "Code examples in docs show currentPick, pickStep, fetchPickChoices"
    - "CLI templates generate code using pick terminology"
    - "New projects from boardsmith init use pick APIs"
  artifacts:
    - path: "docs/custom-ui-guide.md"
      provides: "Updated custom UI documentation"
      contains: "currentPick"
    - path: "docs/api/ui.md"
      provides: "Updated API reference"
      contains: "PickMetadata"
    - path: "src/cli/slash-command/instructions.md"
      provides: "Updated CLI templates"
      contains: "currentPick"
  key_links:
    - from: "docs/custom-ui-guide.md"
      to: "code examples"
      via: "code blocks"
      pattern: "currentPick|pickStep"
    - from: "src/cli/slash-command/instructions.md"
      to: "generated code"
      via: "template"
      pattern: "currentPick|pickStep"
---

<objective>
Update documentation and CLI templates to use pick terminology consistently.

Purpose: Ensure new users and generated code use the correct, standardized terminology.
Output: All docs and templates reference pick instead of selection for user-facing concepts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-selection-pick-rename/57-RESEARCH.md
@.planning/phases/57-selection-pick-rename/57-01-SUMMARY.md
@.planning/phases/57-selection-pick-rename/57-02-SUMMARY.md
@.planning/phases/57-selection-pick-rename/57-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update core documentation</name>
  <files>docs/core-concepts.md, docs/custom-ui-guide.md, docs/actions-and-flow.md, docs/common-pitfalls.md, docs/getting-started.md</files>
  <action>
Update user-facing documentation to use pick terminology:

In docs/core-concepts.md:
- Replace "selection" with "pick" when referring to player choices
- Keep "Selection" when referring to internal types (Selection, ChoiceSelection, etc.)
- Update any code examples to use currentPick, pickStep, etc.

In docs/custom-ui-guide.md:
- Update all code examples showing useActionController
- Replace currentSelection -> currentPick
- Replace selectionStep -> pickStep
- Replace fetchSelectionChoices -> fetchPickChoices
- Update explanatory text to use "pick" terminology

In docs/actions-and-flow.md:
- Update action selection explanations to use "pick" terminology
- Update code examples

In docs/common-pitfalls.md:
- Update any selection-related pitfalls to use pick terminology

In docs/getting-started.md:
- Update any introductory examples using selection APIs
  </action>
  <verify>
- `grep -n "currentSelection" docs/core-concepts.md docs/custom-ui-guide.md docs/actions-and-flow.md` shows no matches
- `grep -n "currentPick" docs/custom-ui-guide.md` shows updated examples
- Documentation still renders correctly (no broken formatting)
  </verify>
  <done>Core documentation updated to use pick terminology</done>
</task>

<task type="auto">
  <name>Task 2: Update API reference documentation</name>
  <files>docs/api/session.md, docs/api/ui.md, docs/api/index.md, docs/api/ai.md, docs/api/eslint-plugin.md, docs/ui-components.md, docs/component-showcase.md, docs/element-enrichment.md, docs/ai-system.md, docs/architecture.md, docs/llm-overview.md</files>
  <action>
Update API documentation:

In docs/api/session.md:
- Update type references: SelectionMetadata -> PickMetadata
- Update SelectionHandler -> PickHandler
- Update getSelectionChoices -> getPickChoices
- Add migration note about deprecated aliases

In docs/api/ui.md:
- Update composable documentation
- currentSelection -> currentPick
- selectionStep -> pickStep
- fetchSelectionChoices -> fetchPickChoices
- injectSelectionStepFn -> injectPickStepFn

In docs/api/index.md:
- Update any overview references

In other docs (ai.md, eslint-plugin.md, ui-components.md, etc.):
- Search for "selection" and update where appropriate
- Keep internal type references (Selection type union) as-is

In docs/llm-overview.md:
- Update any LLM-facing explanations of the API
  </action>
  <verify>
- `grep -rn "SelectionMetadata" docs/api/` shows only deprecation notes
- `grep -rn "PickMetadata" docs/api/` shows updated references
- API documentation accurately reflects renamed APIs
  </verify>
  <done>API reference documentation updated with pick terminology</done>
</task>

<task type="auto">
  <name>Task 3: Update CLI templates</name>
  <files>src/cli/slash-command/instructions.md, src/cli/slash-command/aspects/square-grid.md, src/cli/slash-command/aspects/playing-cards.md, src/cli/slash-command/generate-ai-instructions.md</files>
  <action>
Update CLI slash command templates (these generate code for new projects):

In src/cli/slash-command/instructions.md:
- Update all code templates to use currentPick instead of currentSelection
- Update templates to use pickStep instead of selectionStep
- Update templates to use fetchPickChoices instead of fetchSelectionChoices
- Update explanatory text to use "pick" terminology

In src/cli/slash-command/aspects/square-grid.md:
- Update grid-based game templates to use pick terminology
- Update any currentSelection usage in grid UI examples

In src/cli/slash-command/aspects/playing-cards.md:
- Update card game templates to use pick terminology
- Update chooseElement examples if they show selection handling

In src/cli/slash-command/generate-ai-instructions.md:
- Update AI generation instructions to use pick terminology

Test the templates by verifying the file contents are correct.
  </action>
  <verify>
- `grep -n "currentSelection" src/cli/slash-command/` shows no matches
- `grep -n "currentPick" src/cli/slash-command/instructions.md` shows updated templates
- Templates contain valid TypeScript syntax (no broken code blocks)
  </verify>
  <done>CLI templates updated to generate code using pick terminology</done>
</task>

</tasks>

<verification>
- `grep -rn "currentSelection" docs/` shows no matches (except deprecation notes)
- `grep -rn "selectionStep" docs/` shows no matches (except deprecation notes)
- `grep -rn "currentSelection" src/cli/slash-command/` shows no matches
- All documentation renders correctly
- CLI templates contain valid syntax
</verification>

<success_criteria>
- All docs use "pick" terminology for user-facing concepts
- Code examples show currentPick, pickStep, fetchPickChoices
- API docs mention deprecation aliases for migration
- CLI templates generate code with pick terminology
- No broken formatting or syntax errors in docs/templates
</success_criteria>

<output>
After completion, create `.planning/phases/57-selection-pick-rename/57-04-SUMMARY.md`
</output>
