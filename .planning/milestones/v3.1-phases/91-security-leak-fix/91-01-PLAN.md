---
phase: 91-security-leak-fix
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/engine/element/image-leak.test.ts
autonomous: true
requirements: [SEC-01, SEC-02]

must_haves:
  truths:
    - "A new test asserts a hidden deck card's serialized $images contains no face key"
    - "The same test asserts $images.back IS kept (renderer can still draw a back)"
    - "A non-owner viewing an owner-only hand receives no $images.face on any child"
    - "A count-only container's hidden representation leaks no $image / $images.face"
    - "A regression-guard walks the whole per-player view and asserts no $images.face or $image survives on any __hidden element"
    - "The test FAILS (red) against current unfixed game.ts, proving the leak"
  artifacts:
    - path: "src/engine/element/image-leak.test.ts"
      provides: "SEC-01/SEC-02 leak proof + regression guard for toJSONForPlayer redaction"
      min_lines: 90
      contains: "toJSONForPlayer"
  key_links:
    - from: "src/engine/element/image-leak.test.ts"
      to: "Game.toJSONForPlayer"
      via: "direct call on a game with contentsHidden() deck + contentsVisibleToOwner() hand"
      pattern: "toJSONForPlayer"
---

<objective>
Write a focused, currently-FAILING test that proves the `$images.face` information leak in `toJSONForPlayer`. This is the Prove-Before-Fix step (per CLAUDE.md "Prove Before Fix"): the test documents the bug in red before any fix exists, then Plan 02 turns it green.

Purpose: Lock in an executable proof of SEC-01 (face refs leak to unauthorized viewers) and SEC-02 (the blanket `$`-whitelist passes value-bearing keys). The test is the regression contract that prevents the leak from ever returning.
Output: `src/engine/element/image-leak.test.ts`, committed in a red (failing) state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-security-leak-fix/91-CONTEXT.md
@.planning/phases/91-security-leak-fix/91-RESEARCH.md

<interfaces>
<!-- Extracted from the codebase — executor uses these directly, no exploration needed. -->

From src/engine/element/types.ts:
```typescript
export type ImageRef =
  | string
  | { sprite: string; x: number; y: number; width: number; height: number };

export interface ElementJSON {
  className: string;
  // id, name optional on hidden children
  attributes: Record<string, unknown>;   // $-prefixed system attrs live here, plus __hidden
  children?: ElementJSON[];
  childCount?: number;
}
```

From src/engine/element/game.ts:
```typescript
toJSONForPlayer(player: P | number | null): ElementJSON
```

From src/engine/element/game-element.ts:
```typescript
$image?: ImageRef;                       // single-sided (identity-bearing)
$images?: Record<string, ImageRef>;      // multi-sided, e.g. { face, back }
isVisibleTo(player: Player | number): boolean
```

Existing test scaffold to model on (fixtures, imports, beforeEach shape):
src/engine/element/deck-hand-visibility.test.ts

Zone-visibility helpers used by the fixtures (do NOT re-implement):
`deck.contentsHidden()`, `hand.contentsVisibleToOwner()` — from the engine element index.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create image-leak.test.ts with SEC-01 face-leak assertions (RED)</name>
  <files>src/engine/element/image-leak.test.ts</files>
  <read_first>
    - src/engine/element/deck-hand-visibility.test.ts (test scaffold: imports, game construction, contentsHidden/contentsVisibleToOwner usage, player seats)
    - .planning/phases/91-security-leak-fix/91-RESEARCH.md ("Test Fixture Pattern", "Assertion Pattern (SEC-01)", Pitfalls 1 + 4)
    - .planning/phases/91-security-leak-fix/91-CONTEXT.md (the three leaking branches and the safe fully-hidden branch)
    - src/engine/element/game.ts:2195-2320 (toJSONForPlayer — the system under test; note the three branches at ~2205-2218, ~2244-2258, ~2265-2287)
  </read_first>
  <action>
    Create `src/engine/element/image-leak.test.ts`. Build a Go-Fish-style fixture using engine primitives only (no external game import): a `TestCard extends Card` with `rank`/`suit`, a `Deck` named `draw-pile` set to `contentsHidden()` holding a card created with `$images: { face: '/cards/AS.svg', back: '/cards/back.svg' }`, and a `Hand` named `hand-p1` owned by player 1 set to `contentsVisibleToOwner()` holding a card created with `$images: { face: '/cards/KH.svg', back: '/cards/back.svg' }`. Serialize the view of a player who can see NEITHER (player 2) via `game.toJSONForPlayer(2)`.
    Add SEC-01 assertions per the research "Assertion Pattern": the hidden deck child has `attributes.__hidden === true`; its `$images.face` is `undefined`; its `$images.back === '/cards/back.svg'` (back MUST survive so the renderer can draw a back — Pitfall 2); its `$type` survives. Add the matching owner-only-hand assertion (Pitfall 4): player 2 (non-owner) receives no `$images.face` on any `hand-p1` child but still gets `$images.back`. Add a count-only-container assertion (Pitfall 1): a `count-only` zone/container carrying `$images: { face, back }` does not leak `face` to a viewer who cannot see it.
    Do NOT assert against the fully-hidden placeholder branch (it already emits only `{ __hidden: true }` — do not regress it, but it is not the leak under test). Run the test; it MUST fail (red) because the current filter copies `$images` verbatim. No fenced implementation here — follow the research patterns.
  </action>
  <verify>
    <automated>npx vitest run src/engine/element/image-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/engine/element/image-leak.test.ts` exists and is picked up by vitest.
    - The command above EXITS NON-ZERO (red), and the failing assertion is specifically a surviving `$images.face` on a `__hidden` element (the leak), not a fixture/compile error.
    - Test covers all three leaking surfaces: `contentsHidden()` deck child, `contentsVisibleToOwner()` hand child (non-owner viewer), and a `count-only` container.
    - At least one assertion confirms `$images.back` is expected to survive (so Plan 02 cannot "fix" by dropping all images).
    - No `eslint-disable` added; no real game package imported (engine-only fixture).
  </acceptance_criteria>
  <done>Running image-leak.test.ts fails on a surviving `$images.face` for a hidden-to-viewer element across deck, owner-only hand, and count-only container; back-preservation expectation present.</done>
</task>

<task type="auto">
  <name>Task 2: Add SEC-02 allowlist + fail-safe + $image-drop assertions and a recursive regression guard (RED)</name>
  <files>src/engine/element/image-leak.test.ts</files>
  <read_first>
    - src/engine/element/image-leak.test.ts (the file created in Task 1)
    - .planning/phases/91-security-leak-fix/91-RESEARCH.md ("Complete $-Key Catalog", "Regression Guard (SEC-02)", Pitfall 3)
    - src/engine/element/game-element.ts:64-76 ($image / $images declarations)
  </read_first>
  <action>
    Extend `image-leak.test.ts`. Add SEC-02 coverage: create a hidden element carrying a safe layout key (e.g. `$type` and one of `$layout`/`$direction`) AND an unknown/future `$`-key (e.g. `$secretValue: 'leak-me'`) AND a single-sided `$image: '/cards/AS.svg'`. Assert that on the hidden representation: the allowlisted layout key(s) survive; the unknown `$secretValue` is `undefined` (fail-safe drop); and `$image` is `undefined` (single-sided images always carry identity — Pitfall 3, drop unconditionally). Implement the research "Regression Guard": a recursive walker (`collectAllHiddenAttrs`) over `game.toJSONForPlayer(2)` that gathers every attributes object where `__hidden` is true, then asserts for EACH that `$images.face` is `undefined` and `$image` is `undefined`. This guards every current and future hidden surface in one sweep. Run; still red (no fix yet).
  </action>
  <verify>
    <automated>npx vitest run src/engine/element/image-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts an unknown `$`-key (`$secretValue`) is dropped on hidden elements (fail-safe / SEC-02).
    - Test asserts a known safe layout key (`$type` plus at least one of `$layout`/`$direction`) survives on hidden elements.
    - Test asserts `$image` is absent on every hidden element (SEC-02 / Pitfall 3).
    - A recursive `collectAllHiddenAttrs`-style guard asserts no `$images.face` and no `$image` on ANY `__hidden` attributes object in the player-2 view.
    - The command EXITS NON-ZERO (red) for leak reasons (face/$image/unknown-key surviving), not compile errors.
  </acceptance_criteria>
  <done>image-leak.test.ts now also fails on surviving unknown `$`-keys and surviving `$image`, and a recursive guard sweeps the whole player view; all failures are leak assertions, confirming the bug end-to-end before the fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine state → per-player payload | `toJSONForPlayer` is the single filter deciding what each seat (and spectators) may learn; everything past it is untrusted-viewer territory |
| game-authored `$`-attrs → serializer | Game authors set `$image`/`$images`/custom `$`-keys on elements; the serializer must not assume any `$`-key is safe to broadcast |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01 | Information Disclosure | `toJSONForPlayer` hidden/owner-only/count-only branches leaking `$images.face`/`$image` | mitigate | THIS plan proves the leak with a failing test (deck, owner-only hand, count-only container) before any fix; Plan 02 closes it |
| T-91-02 | Information Disclosure | future value-bearing `$`-key inheriting the blanket `$` whitelist (fail-open) | mitigate | Test asserts an unknown `$secretValue` key is dropped on hidden elements, locking the fail-safe-drop contract for Plan 02 |
| T-91-03 | Tampering (regression) | the already-safe fully-hidden branch (`__hidden` only) | accept/guard | Test deliberately does not modify that branch's contract; recursive guard still tolerates `{ __hidden: true }` so a correct Plan 02 cannot regress it |
| T-91-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed this phase (engine-only TS + existing vitest) |
</threat_model>

<verification>
- `npx vitest run src/engine/element/image-leak.test.ts` runs and FAILS, with every failure being a leak assertion (surviving `$images.face`, `$image`, or unknown `$`-key on a `__hidden` element).
- The test file imports only engine primitives (Card/Deck/Hand/Game), no external game package.
- `npx vitest run src/engine/element/` still passes for all OTHER existing tests (the new file is the only red one).
</verification>

<success_criteria>
A committed, red `image-leak.test.ts` that proves SEC-01 (face/back handling across deck, owner-only hand, count-only container) and SEC-02 (allowlist survival, unknown-key fail-safe drop, `$image` drop) including a recursive whole-view regression guard. The red state is the proof; Plan 02 makes it green.
</success_criteria>

<output>
Create `.planning/phases/91-security-leak-fix/91-01-SUMMARY.md` when done. Record: the test fails red, the exact leak assertions that fail, and confirmation the rest of `src/engine/element/` stays green.
</output>
