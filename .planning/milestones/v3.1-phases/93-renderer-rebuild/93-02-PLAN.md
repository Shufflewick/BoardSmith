---
phase: 93-renderer-rebuild
plan: 02
type: execute
wave: 2
depends_on: ["93-01"]
files_modified:
  - src/ui/components/auto-ui/renderers/CardRenderer.vue
  - src/ui/components/auto-ui/renderers/HandRenderer.vue
  - src/ui/components/auto-ui/renderers/DeckRenderer.vue
autonomous: true
requirements: [RENDER-01]

must_haves:
  truths:
    - "A card with $images.face renders an <img> face-up; a hidden/face-down card renders $images.back or the default back gradient — never the face image"
    - "A hand renders its cards via ElementRenderer, shows a name + (N) count, and supports fan/overlap/two-row layout"
    - "A deck renders a stacked visual (up to 3 offset cards) with a 20px bold name + count, and shows italic 'Empty' when childCount is 0"
    - "Each renderer reflects board interaction state (is-selectable/is-selected/action-selectable/is-board-highlighted/is-board-selected) via tryUseBoardInteraction without modifying the substrate"
  artifacts:
    - path: "src/ui/components/auto-ui/renderers/CardRenderer.vue"
      provides: "Card visual + board-interaction state + face/back image resolution"
      min_lines: 60
    - path: "src/ui/components/auto-ui/renderers/HandRenderer.vue"
      provides: "Hand container with two-row/fan/overlap layout, children via ElementRenderer"
      min_lines: 60
    - path: "src/ui/components/auto-ui/renderers/DeckRenderer.vue"
      provides: "Deck stack visual + empty state + count"
      min_lines: 40
  key_links:
    - from: "src/ui/components/auto-ui/renderers/HandRenderer.vue"
      to: "src/ui/components/auto-ui/renderers/ElementRenderer.vue"
      via: "renders each child card via ElementRenderer"
      pattern: "ElementRenderer"
    - from: "src/ui/components/auto-ui/renderers/CardRenderer.vue"
      to: "src/ui/composables/useBoardInteraction.ts"
      via: "tryUseBoardInteraction inject (read-only — substrate unchanged)"
      pattern: "tryUseBoardInteraction"
---

<objective>
Extract the card / hand / deck rendering out of the doomed AutoElement.vue into three focused per-element renderer components, preserving every visual and board-interaction behavior verbatim while consuming (never modifying) the interaction substrate. These are registered as built-ins in plan 93-06.

Purpose: RENDER-01 — replace the monolithic v-if cascade with discrete renderers. This plan owns the card-family slice.
Output: CardRenderer.vue, HandRenderer.vue, DeckRenderer.vue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-renderer-rebuild/93-PATTERNS.md
@.planning/phases/93-renderer-rebuild/93-UI-SPEC.md
@.planning/phases/93-renderer-rebuild/93-RESEARCH.md

<interfaces>
<!-- Inject keys provided by AutoRenderer.vue (plan 93-06). Inject with EXACTLY these keys: -->
playerSeat: inject<number>('playerSeat', 0)
selectableElements: inject<Ref<Set<number>>>('selectableElements')
selectedElements: inject<Ref<Set<number>>>('selectedElements')
defaultBackImage: inject<Ref<ImageInfo | null>>('defaultBackImage', ref(null))

<!-- Board interaction API (read-only — DO NOT modify the composable). From useBoardInteraction.ts line 432: -->
tryUseBoardInteraction(): returns undefined outside GameShell. Methods used:
  .isHighlighted({id,name}), .isSelected({id,name}), .isSelectableElement({id,name}),
  .isDraggedElement({id,name}), .isDisabledElement(...), .triggerElementSelect(...),
  .startDrag(...), .endDrag(...), .isDragging
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CardRenderer.vue (card visual + board state + face/back resolution)</name>
  <files>src/ui/components/auto-ui/renderers/CardRenderer.vue</files>
  <read_first>
    - src/ui/components/auto-ui/AutoElement.vue (lines 18-22 imports; 46-55 + 70 inject + tryUseBoardInteraction; 153-243 board-state computeds; 215-220 elementRef object; 264-292 currentCardImage / cardImages; card section template + scoped styles)
    - .planning/phases/93-renderer-rebuild/93-PATTERNS.md (section "renderers/CardRenderer.vue" — every computed and CSS pattern; Shared Patterns: Board Interaction Inject, Data Attribute Pattern)
    - .planning/phases/93-renderer-rebuild/93-UI-SPEC.md (section "CardRenderer" — all four baselines + action-selectable pulse + back default gradient)
    - src/ui/composables/useBoardInteraction.ts (tryUseBoardInteraction signature only — do NOT edit)
    - src/ui/composables/dragImage.ts (setTransformAwareDragImage — do NOT edit)
  </read_first>
  <action>
    Create `renderers/CardRenderer.vue` with props `{ element: GameElement; depth: number }`. Inject `playerSeat`, `defaultBackImage` with the exact keys above; call `const boardInteraction = tryUseBoardInteraction()` (always tryUse, never useBoardInteraction directly). Port the board-state computeds verbatim from AutoElement.vue 153-243 (`isBoardHighlighted`, `isBoardSelected`, `isActionSelectable`, `isDragged`, etc.) — each with the `if (!boardInteraction) return false` defensive guard. Port `cardImages` + `currentCardImage` (AutoElement 264-292): face when not hidden, back when `element.__hidden`; support string URL and `{sprite,x,y,width,height}` descriptor. Bind the class list (`is-selectable`/`is-selected`/`action-selectable`/`is-board-highlighted`/`is-board-selected`/`is-hidden`) and `:data-element-id` + `:data-animatable="true"` per the Data Attribute Pattern. Implement all four visual baselines from UI-SPEC §CardRenderer (image / sprite / no-image white face with name label / default back gradient) and the green action-selectable pulse. Wire drag handlers calling the existing boardInteraction methods + `setTransformAwareDragImage` (same calls as AutoElement). DO NOT modify any composable. Scoped `<style>` only; consume drag-drop.css CSS vars (`--bs-*`), never redefine them. Use `.js` import extensions.
  </action>
  <verify>
    <automated>npx eslint src/ui/components/auto-ui/renderers/CardRenderer.vue && npx vitest run src/ui/</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint src/ui/components/auto-ui/renderers/CardRenderer.vue` reports no errors
    - `grep -c "tryUseBoardInteraction" src/ui/components/auto-ui/renderers/CardRenderer.vue` returns >=1
    - `grep -c "v-html" src/ui/components/auto-ui/renderers/CardRenderer.vue` returns 0 (no v-html — XSS guard)
    - `grep -c "data-animatable" src/ui/components/auto-ui/renderers/CardRenderer.vue` returns >=1 (FLIP/fly hook present)
    - Face image guarded by hidden state: `grep -n "__hidden" src/ui/components/auto-ui/renderers/CardRenderer.vue` shows the back/face branch keys on hidden
    - `npx vitest run src/ui/` stays GREEN (substrate + helper tests unchanged)
  </acceptance_criteria>
  <done>Card renders all four baselines, reflects board state, never leaks the face image when hidden, lints clean.</done>
</task>

<task type="auto">
  <name>Task 2: HandRenderer.vue (two-row/fan/overlap, children via ElementRenderer)</name>
  <files>src/ui/components/auto-ui/renderers/HandRenderer.vue</files>
  <read_first>
    - src/ui/components/auto-ui/AutoElement.vue (lines 409-453: the two-row hand sorting / backRowCards / frontRowCards / sortedHandCards logic — COPY VERBATIM; hand section template + styles)
    - .planning/phases/93-renderer-rebuild/93-PATTERNS.md (section "renderers/HandRenderer.vue" — CRITICAL verbatim-copy note; isFan/fanAngle)
    - .planning/phases/93-renderer-rebuild/93-UI-SPEC.md (section "HandRenderer" — container, header, fan/overlap/two-row, action-selectable whole-hand state)
    - src/ui/components/auto-ui/renderers/ElementRenderer.vue (children dispatch — render cards through this, never AutoElement)
  </read_first>
  <action>
    Create `renderers/HandRenderer.vue` with props `{ element: GameElement; depth: number }`. CRITICAL: copy the two-row hand layout logic (sortedHandCards / backRowCards / frontRowCards) VERBATIM from AutoElement.vue lines 409-453 — do not re-implement (named Phase 92 carry-forward; Pitfall 6). Inject `playerSeat`/`selectableElements`/`selectedElements`/`defaultBackImage` with the exact keys; `tryUseBoardInteraction()` for the whole-hand action-selectable state. Compute `isFan = element.attributes?.$fan === true` and `fanAngle = element.attributes?.$fanAngle ?? 30`. Render each card child via `ElementRenderer` (`:element="child" :depth="depth+1"`), never via AutoElement. Header: element name left, `(N)` count right (13px #888). Implement container baseline, fan rotation around `transform-origin: center bottom`, overlap `margin-right: -10px`, and two-row `margin-bottom: -50px; z-index: 1` per UI-SPEC. Whole-hand action-selectable green border/bg + hover lift. Scoped styles only; `.js` imports.
  </action>
  <verify>
    <automated>npx eslint src/ui/components/auto-ui/renderers/HandRenderer.vue && npx vitest run src/ui/</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint src/ui/components/auto-ui/renderers/HandRenderer.vue` reports no errors
    - `grep -c "ElementRenderer" src/ui/components/auto-ui/renderers/HandRenderer.vue` returns >=1 (children dispatch through registry)
    - `grep -c "AutoElement" src/ui/components/auto-ui/renderers/HandRenderer.vue` returns 0
    - `grep -Ec "backRow|frontRow|sortedHand" src/ui/components/auto-ui/renderers/HandRenderer.vue` returns >=1 (two-row logic carried forward)
    - `npx vitest run src/ui/` stays GREEN
  </acceptance_criteria>
  <done>Hand renders children via ElementRenderer with fan/overlap/two-row layout and whole-hand action state; lints clean.</done>
</task>

<task type="auto">
  <name>Task 3: DeckRenderer.vue (stack visual + empty state + count)</name>
  <files>src/ui/components/auto-ui/renderers/DeckRenderer.vue</files>
  <read_first>
    - src/ui/components/auto-ui/AutoElement.vue (deck section: stack rendering, childCount display, deck-label styles)
    - .planning/phases/93-renderer-rebuild/93-PATTERNS.md (section "renderers/DeckRenderer.vue" — stackCards slice(0,3), offset CSS, empty state)
    - .planning/phases/93-renderer-rebuild/93-UI-SPEC.md (section "DeckRenderer" — container, header, stack visual, empty state, action-selectable)
    - src/ui/components/auto-ui/renderers/ElementRenderer.vue
  </read_first>
  <action>
    Create `renderers/DeckRenderer.vue` with props `{ element: GameElement; depth: number }`. Inject the standard keys + `tryUseBoardInteraction()`. Compute `stackCards = (element.children ?? []).slice(0,3)` mapped with a `stackIndex`; render each via ElementRenderer inside a 60×84px relative container with offset `top: calc(var(--stack-index,0) * -2px); left: calc(var(--stack-index,0) * 1px)`. Header: name at 20px bold #fff, count at 13px #aaa. Empty state: when `childCount`/children length is 0 render italic `Empty` (#666, padding 20px) — the exact copy from UI-SPEC. Apply the green-pulse action-selectable treatment to the container. Scoped styles; `.js` imports.
  </action>
  <verify>
    <automated>npx eslint src/ui/components/auto-ui/renderers/DeckRenderer.vue && npx vitest run src/ui/</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint src/ui/components/auto-ui/renderers/DeckRenderer.vue` reports no errors
    - `grep -c "Empty" src/ui/components/auto-ui/renderers/DeckRenderer.vue` returns >=1 (empty-state copy present)
    - `grep -c "ElementRenderer" src/ui/components/auto-ui/renderers/DeckRenderer.vue` returns >=1
    - `npx vitest run src/ui/` stays GREEN
  </acceptance_criteria>
  <done>Deck renders an offset 3-card stack, count, and italic Empty state; lints clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine gameView → renderer DOM | Game-authored element names, labels, and $images URLs (already visibility-filtered by toJSONForPlayer) are rendered into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-03 | Tampering (XSS) | CardRenderer/HandRenderer/DeckRenderer text | mitigate | All author-provided strings rendered via Vue text interpolation `{{ }}` only; `v-html` is forbidden (acceptance criterion greps for 0 occurrences) |
| T-93-04 | Information Disclosure | CardRenderer face image | mitigate | currentCardImage selects `$images.back` (not face) when `element.__hidden` — same visibility guard as AutoElement.vue; the face URL redaction itself is enforced upstream by Phase 91 SEC-01 |
| T-93-05 | Tampering (URL injection) | $images src binding | mitigate | Images rendered via `<img :src>` / `background-image` only; never injected into a `<style>` tag or `v-html` |
| T-93-06 | Tampering | interaction substrate | accept | This plan only CALLS substrate composables (read-only); no substrate file is modified — the substrate test suite staying green (verified in 93-06) is the proof |
</threat_model>

<verification>
- `npx eslint src/ui/components/auto-ui/renderers/CardRenderer.vue src/ui/components/auto-ui/renderers/HandRenderer.vue src/ui/components/auto-ui/renderers/DeckRenderer.vue` — clean
- `npx vitest run src/ui/` — green (no substrate/helper regression)
- `git diff --stat src/ui/composables/` — shows ZERO changes (substrate untouched)
</verification>

<success_criteria>
- RENDER-01 (card-family slice): card/hand/deck render as discrete registry-ready components reproducing AutoElement visuals and board state
- No v-html anywhere; hidden cards never expose face image
- Children always dispatch through ElementRenderer; no AutoElement recursion
- No substrate composable modified
</success_criteria>

<output>
Create `.planning/phases/93-renderer-rebuild/93-02-SUMMARY.md` when done.
</output>
