---
phase: 94-interaction-presentation-playability
plan: 06
type: execute
wave: 5
depends_on: [94-05]
files_modified: []
autonomous: false
requirements: [INTERACT-01, INTERACT-02, INTERACT-03, PRESENT-01, PRESENT-02, PRESENT-03]
must_haves:
  truths:
    - "A complete game of Hex is played start-to-finish in the browser using only the auto-UI"
    - "A complete game of Go Fish is played start-to-finish in the browser using only the auto-UI"
    - "A complete game of Checkers (incl. multi-jump + king promotion) is played start-to-finish in the browser using only the auto-UI"
    - "During Go Fish, an opponent's face-down cards never display overlay art (PRESENT-02 confirmed in-browser)"
    - "The footer ActionPanel is absent (not merely hidden) when all active choices are board-anchored"
  artifacts: []
  key_links:
    - from: "board click"
      to: "action dispatch + multi-ref highlight"
      via: "auto-UI renderers → useBoardInteraction → session"
      pattern: "human-verified"
---

<objective>
Prove the milestone playability gate: Hex, Go Fish, and Checkers are each playable start-to-finish
in the browser using ONLY the auto-UI (no custom UI code in `#game-board`). This is human-verified
browser testing per the project rule "verify behavior by running the application" (Phase 92 precedent).

Purpose: Playability is the credibility test for the whole v3.1 auto-UI effort — criteria 6/7/8.
The gate also confirms the PRESENT-02 security invariant in the live browser (hidden cards show no art).
Output: Three human-verified playthroughs + the PRESENT-02 in-browser confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-interaction-presentation-playability/94-UI-SPEC.md

<interfaces>
<!-- The minimum interaction surface per game is the "Playability Acceptance Contract" in 94-UI-SPEC.md.
     Each game already slots only <AutoUI> after 94-05. Dev server: `boardsmith dev` in the game dir.
     The user has the Claude Code Chrome extension available for browser testing. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Regression gate — full suite + game builds green before human testing</name>
  <files>(none — verification only)</files>
  <read_first>
    - 94-RESEARCH.md "Validation Architecture → Sampling Rate" (phase gate = full suite green)
  </read_first>
  <action>
    Run the full BoardSmith test suite and confirm each gate game builds against the current local
    BoardSmith. Do NOT start any long-running dev server in this task. If anything fails, stop and report
    — do not advance to the human checkpoints with a broken build.
  </action>
  <verify>
    <automated>set -o pipefail; npm run test 2>&1 | tail -10 && for g in hex go-fish checkers; do echo "== $g =="; (cd ~/BoardSmithGames/$g && npm run build 2>&1 | tail -3) || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` reports a green suite (no failures).
    - Each of hex, go-fish, checkers completes `npm run build` without type errors.
  </acceptance_criteria>
  <done>The full suite is green and all three games build — human playtesting can proceed on a known-good build.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Playability Gate — Hex</name>
  <files>(none — human browser verification)</files>
  <read_first>
    - 94-UI-SPEC.md "Hex — Playability Acceptance"
  </read_first>
  <what-built>
    Hex now renders through AutoUI only (split-screen removed), with board-centric click interaction
    (D-03), the suppressible/auto-absent footer (D-02), and the hex presentation overlay (D-04).
    `placeStone` is an element pick → the footer should be auto-absent during play.
  </what-built>
  <action>
    Human-verify a full Hex playthrough in the browser. Start the dev server, play start-to-finish using
    only board clicks, then stop the server. Steps are in how-to-verify. This task changes no files.
  </action>
  <how-to-verify>
    1. Start the dev server: `cd ~/BoardSmithGames/hex && boardsmith dev` (use `--ai` for a solo opponent if needed).
    2. Open the game in the browser (Claude Code Chrome extension).
    3. Confirm only the auto-UI board is shown (no "Custom UI / Auto-Generated" split panels).
    4. Confirm empty hex cells show the green selectable state; click an empty cell to place a stone.
    5. Confirm the footer ActionPanel is ABSENT (no footer chrome) during placement — board occupies the space.
    6. Play to a win; confirm win detection / game-over surfaces via the normal flow.
    7. Stop the dev server when done (do not leave it running).
  </how-to-verify>
  <verify>
    <automated>echo "Human-verified browser gate — automated regression covered by Task 0 (npm run test green)"</automated>
    <human-check>A full game of Hex (place stones → win) completes via board clicks only; footer absent during placement; selectable cells show the green affordance; no custom UI code in #game-board.</human-check>
  </verify>
  <acceptance_criteria>
    - A full game of Hex (place stones → win) completes using only board clicks; no custom UI code in `#game-board`.
    - The footer is absent during placement (criterion 1/2 confirmed visually); selectable cells show the green affordance.
  </acceptance_criteria>
  <done>Hex is human-confirmed playable start-to-finish via auto-UI only.</done>
  <resume-signal>Type "approved" if Hex plays start-to-finish via auto-UI; otherwise describe the failure (which interaction/state broke).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Playability Gate — Go Fish (incl. PRESENT-02 in-browser)</name>
  <files>(none — human browser verification)</files>
  <read_first>
    - 94-UI-SPEC.md "Go Fish — Playability Acceptance" + "Presentation Overlay Contract → Security Guard — PRESENT-02"
  </read_first>
  <what-built>
    Go Fish renders through AutoUI only with the refs-migrated `ask` action and the go-fish overlay
    (PlayerHand label). Opponent hands are visibility-filtered server-side (Phase 91) and must show NO
    overlay art for face-down cards (PRESENT-02). Hand zones are board-anchored (`target` role).
  </what-built>
  <action>
    Human-verify a full Go Fish playthrough in the browser AND the PRESENT-02 security invariant. Start
    the dev server, play start-to-finish, perform the SECURITY CHECK on opponent face-down cards, then
    stop the server. Steps are in how-to-verify. This task changes no files.
  </action>
  <how-to-verify>
    1. Start the dev server: `cd ~/BoardSmithGames/go-fish && boardsmith dev` (use `--ai` if needed).
    2. Open in the browser; confirm only the auto-UI is shown (no split-screen).
    3. Deal: confirm your hand shows card faces and the opponent's cards show NO face art (backs/placeholder only).
    4. Ask: click an opponent's hand zone (board-anchored target → zone highlights); choose a rank; draw as prompted.
    5. SECURITY CHECK (PRESENT-02): confirm at no point does an opponent's face-down card display its face image
       or stats — only the back/placeholder. Inspect the DOM/network if needed to confirm no face image URL is present.
    6. Continue to collect books and reach game end.
    7. Stop the dev server when done.
  </how-to-verify>
  <verify>
    <automated>echo "Human-verified browser gate — PRESENT-02 unit guard covered by 94-04 presentation.test.ts; regression by Task 0"</automated>
    <human-check>A full game of Go Fish (deal → ask → draw → collect → end) completes via auto-UI only AND opponent face-down cards never show face art/stats in DOM or network.</human-check>
  </verify>
  <acceptance_criteria>
    - A full game of Go Fish (deal → ask → draw → collect sets → end) completes using only the auto-UI.
    - Opponent face-down cards never show overlay art (image/stats) — the PRESENT-02 invariant holds in the live browser.
    - The `ask` flow works: hand-zone selection is board-anchored; the rank step is reachable.
  </acceptance_criteria>
  <done>Go Fish is human-confirmed playable start-to-finish via auto-UI only with no hidden-card art leak.</done>
  <resume-signal>Type "approved" if Go Fish plays start-to-finish AND no hidden-card art leaks; otherwise describe the failure (gameplay break or any visible leak).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Playability Gate — Checkers (incl. multi-jump highlight)</name>
  <files>(none — human browser verification)</files>
  <read_first>
    - 94-UI-SPEC.md "Checkers — Playability Acceptance"
  </read_first>
  <what-built>
    Checkers renders through AutoUI only with refs-migrated `move` (source/target) and multi-jump
    highlight refs (intermediate captured squares as role:'highlight'), plus the checkers overlay
    (piece/square label suppression, optional King label). Both picks are board-anchored → footer absent.
  </what-built>
  <action>
    Human-verify a full Checkers playthrough in the browser, including a multi-jump with simultaneous
    highlighting and a king promotion. Start the dev server, play start-to-finish, then stop the server.
    Steps are in how-to-verify. This task changes no files.
  </action>
  <how-to-verify>
    1. Start the dev server: `cd ~/BoardSmithGames/checkers && boardsmith dev` (use `--ai` if needed).
    2. Open in the browser; confirm only the auto-UI is shown (no split-screen).
    3. Select a piece (click → selected/source state); click a destination square (move).
    4. Perform a single capture, then set up and perform a MULTI-JUMP: confirm ALL squares of the jump
       (source + final destination + each intermediate captured square) highlight SIMULTANEOUSLY (criterion 3).
    5. Promote a piece to King and confirm the King indicator (label 'K' or overlay treatment).
    6. Confirm the footer ActionPanel is absent throughout play (both picks board-anchored).
    7. Play to a win; stop the dev server when done.
  </how-to-verify>
  <verify>
    <automated>echo "Human-verified browser gate — multi-jump refs covered by 94-05 build; regression by Task 0"</automated>
    <human-check>A full game of Checkers (move → single capture → multi-jump → king promotion → win) completes via auto-UI only; multi-jump highlights all source/target/intermediate squares at once; footer absent throughout.</human-check>
  </verify>
  <acceptance_criteria>
    - A full game of Checkers (move → single capture → multi-jump → king promotion → win) completes using only the auto-UI.
    - During a multi-jump, all relevant source/target/intermediate squares highlight at the same time (criterion 3 confirmed).
    - The footer is absent throughout (criterion 1/2); king promotion is visible.
  </acceptance_criteria>
  <done>Checkers is human-confirmed playable start-to-finish via auto-UI only with simultaneous multi-jump highlighting.</done>
  <resume-signal>Type "approved" if Checkers plays start-to-finish via auto-UI with simultaneous multi-jump highlighting; otherwise describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-filtered gameView → browser render | the live browser is the final check that hidden-element data never reaches the unauthorized viewer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-06-01 | Information Disclosure | Go Fish opponent's face-down card displays overlay art in the live browser (PRESENT-02 regression) | mitigate | Task 2 SECURITY CHECK explicitly verifies no face image/stats render and no face URL appears in DOM/network; blocking human gate — cannot auto-approve |
| T-94-06-02 | Denial of Service | a left-running dev server holds the port across gates | mitigate | each gate explicitly stops the dev server when done (project rule: never leave a started server running) |
| T-94-06-SC | Tampering | npm/pip/cargo installs | n/a | Phase installs zero packages — no package-manager tasks in this plan |
</threat_model>

<verification>
- Task 0 automated regression gate green (full suite + 3 game builds).
- Three blocking human-verify checkpoints approved.
- PRESENT-02 confirmed in the live browser (no hidden-card art in Go Fish).
</verification>

<success_criteria>
- ROADMAP criteria 6, 7, 8 (Hex, Go Fish, Checkers each playable start-to-finish in the browser via auto-UI only) — all proven.
- ROADMAP criterion 5 reaffirmed in-browser (hidden-element overlay data never reaches the unauthorized viewer).
</success_criteria>

<output>
Create `.planning/phases/94-interaction-presentation-playability/94-06-SUMMARY.md` when done.
</output>
