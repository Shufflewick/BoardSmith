---
phase: 94.1-auto-ui-interaction-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - src/ui/components/auto-ui/ActionPanel.smoke.test.ts
autonomous: false
requirements: [R1]
must_haves:
  truths:
    - "A real Vue SFC from src/ui/components/auto-ui can be mounted in a vitest run and asserted against rendered DOM"
    - "The new component test runs under jsdom without changing the global node environment for existing tests"
    - "The full existing suite stays green after the dependency + config changes"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vue SFC transform plugin registered so .vue imports compile in tests"
      contains: "plugin-vue"
    - path: "src/ui/components/auto-ui/ActionPanel.smoke.test.ts"
      provides: "Proof the component-test harness mounts a real renderer in jsdom"
      min_lines: 15
    - path: "package.json"
      provides: "@vue/test-utils, jsdom, @vitejs/plugin-vue as devDependencies"
      contains: "@vue/test-utils"
  key_links:
    - from: "src/ui/components/auto-ui/ActionPanel.smoke.test.ts"
      to: "src/ui/components/auto-ui/ActionPanel.vue"
      via: "mount() import of the real SFC"
      pattern: "mount\\("
---

<objective>
Stand up Vue component/interaction test infrastructure (locked decision D-01). This is the
structural mechanism that prevents the Phase-94 non-convergence: controller-unit tests passed
while the browser failed because no test ever mounted a real Vue component. NOTHING else in this
phase may land before this plan — every subsequent fix is gated on "covered by a component test
that reproduces the browser failure."

Purpose: Make it possible to mount real `src/ui/components/auto-ui` SFCs in vitest and assert on
rendered DOM, so Bugs A/B/C are caught automatically instead of by slow manual browser runs.
Output: Three approved devDependencies installed, vitest configured to transform `.vue` SFCs under
jsdom, and a smoke test that proves a real renderer mounts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94.1-auto-ui-interaction-completion/94.1-CONTEXT.md
@.planning/phases/94.1-auto-ui-interaction-completion/94.1-RESEARCH.md
@.planning/phases/94.1-auto-ui-interaction-completion/94.1-VALIDATION.md

<critical_note>
RESEARCH.md names only `@vue/test-utils` + `jsdom`. That is INCOMPLETE. The repo ships `.vue`
source uncompiled (games compile via their own vite); vitest currently has `environment: 'node'`
and NO Vue SFC plugin. Mounting an SFC in vitest therefore ALSO requires `@vitejs/plugin-vue`
(verified absent from package.json and node_modules). All THREE packages must be in the approval
checkpoint. `vite ^5.4.0` is already a devDependency, so `@vitejs/plugin-vue@^5` is the matching major.
</critical_note>

<interfaces>
Current vitest.config.ts (verified): `test.globals: true`, `test.environment: 'node'`,
`test.include: ['src/**/*.test.ts']`, an `exclude` list of external-game-dependent tests, and a
`resolve.alias` block mapping `@boardsmith/*` to `src/*/index.ts`. There is NO `plugins` array and
NO Vue transform today.

`package.json`: `"test": "vitest run"`. Installed: `vitest ^2.1.0`, `vue ^3.5.26`, `vite ^5.4.0`.
ABSENT: `@vue/test-utils`, `jsdom`, `@vitejs/plugin-vue`.

`injectActionController()` (src/ui/composables/useActionController.ts:1722) throws
"injectActionController() must be called inside a GameShell context" when no controller is provided
via Vue `inject`. Component tests that mount ActionPanel must `provide` the controller through
`mount(Comp, { global: { provide: { ... } } })` using the same injection key the composable reads.
The exact injection-key symbol must be read from `injectActionController()` at test-writing time
(do not guess it).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Dependency approval + legitimacy gate (3 packages)</name>
  <read_first>
    - ./CLAUDE.md ("Don't add dependencies without discussing")
    - /Users/jtsmith/BoardSmith/CLAUDE.md
    - .planning/phases/94.1-auto-ui-interaction-completion/94.1-RESEARCH.md (Package Legitimacy Audit — both packages tagged [ASSUMED], slopcheck unavailable)
    - /Users/jtsmith/BoardSmith/package.json (confirm vite ^5.4.0, vue ^3.5.26, vitest ^2.1.0)
  </read_first>
  <what-built>
    NOTHING is installed yet. This is the mandatory dependency-discussion gate required by CLAUDE.md
    ("Don't add dependencies without discussing") AND the package-legitimacy gate (RESEARCH tagged
    both runtime packages [ASSUMED] because slopcheck was unavailable). The executor presents the
    three devDependencies and their verified provenance for explicit approval before any install runs.
  </what-built>
  <how-to-verify>
    1. Confirm each package on its registry page (do NOT install first):
       - @vue/test-utils → https://www.npmjs.com/package/@vue/test-utils (official Vue core team; registry version 2.4.11; peerDependency vue 3.x)
       - jsdom → https://www.npmjs.com/package/jsdom (foundational DOM impl; registry latest 29.1.1)
       - @vitejs/plugin-vue → https://www.npmjs.com/package/@vitejs/plugin-vue (official Vite Vue SFC transform; pick the major matching vite ^5 — i.e. @vitejs/plugin-vue@^5)
    2. Confirm rationale: @vue/test-utils mounts SFCs; jsdom is the DOM env (chosen over happy-dom for SVG class/attribute support used by HexBoardRenderer); @vitejs/plugin-vue compiles `.vue` SFCs so vitest can import them (required, not optional — see critical_note).
    3. Approve all three as devDependencies, or direct substitutions (e.g. happy-dom instead of jsdom).
  </how-to-verify>
  <verify>
    <automated>MISSING — human approval gate; no automated check. Install task (Task 2) is blocked until this is approved.</automated>
  </verify>
  <acceptance_criteria>
    - User has explicitly approved (or amended) the three devDependencies before any `npm install` runs.
    - Each package was confirmed on its npm registry page (legitimacy gate satisfied; [ASSUMED] cleared by human verification).
    - This checkpoint is NOT auto-approved regardless of any auto_advance setting (legitimacy gate).
  </acceptance_criteria>
  <resume-signal>Type "approved" (or name substitutions) to proceed with install.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install deps, register Vue SFC transform under jsdom, prove mount works</name>
  <read_first>
    - /Users/jtsmith/BoardSmith/vitest.config.ts (the file being edited — has no `plugins` array today)
    - /Users/jtsmith/BoardSmith/package.json (the "test": "vitest run" script + devDependencies)
    - src/ui/components/auto-ui/ActionPanel.vue (the SFC the smoke test will mount; confirm it imports `injectActionController`)
    - src/ui/composables/useActionController.ts:1700-1730 (injectActionController + the injection key it reads)
  </read_first>
  <action>
    1. Install the three approved devDependencies via npm (`npm install --save-dev @vue/test-utils jsdom @vitejs/plugin-vue@^5`). Commit the resulting package-lock.json change.
    2. Edit vitest.config.ts: import `vue` from `@vitejs/plugin-vue` and add `plugins: [vue()]` to the
       defineConfig object (top level, sibling of `test` and `resolve`). KEEP `test.environment: 'node'`
       as the global default — do NOT switch the whole suite to jsdom. Component tests opt into jsdom
       per-file via a `// @vitest-environment jsdom` docblock at the top of the file. Leave the existing
       `resolve.alias` block and `exclude` list untouched.
    3. Create src/ui/components/auto-ui/ActionPanel.smoke.test.ts as the harness proof. First line is
       the `// @vitest-environment jsdom` pragma. Mount the REAL ActionPanel.vue with `mount()` from
       `@vue/test-utils`, supplying a minimal controller via `global.provide` keyed by the injection key
       read from `injectActionController()` (read the actual symbol — do not invent it). The minimal
       controller only needs the reactive refs ActionPanel reads on first render (e.g. currentPick,
       currentChoices, validElements, currentArgs as empty/null refs) so the component mounts without
       throwing. Assert `wrapper.exists()` is true and the component produced DOM. This test exists ONLY
       to prove the harness compiles+mounts an SFC; it does not test interaction behavior.
    4. Do NOT add a global config-level jsdom default and do NOT remove any existing exclude entries.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/auto-ui/ActionPanel.smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/components/auto-ui/ActionPanel.smoke.test.ts` passes (proves SFC transform + jsdom mount work end-to-end).
    - vitest.config.ts contains `plugins: [vue()]` and STILL has `test.environment: 'node'` (grep both: `grep -c "plugins" vitest.config.ts` ≥ 1 and `grep -c "environment: 'node'" vitest.config.ts` ≥ 1).
    - The smoke test file's first line is `// @vitest-environment jsdom`.
    - Full existing suite still green: `npm test` matches the e46d450 baseline (892 pass / 1 known pre-existing `followUp+skip+auto-execute` fail) plus the new smoke test passing — no NEW failures introduced.
    - package.json devDependencies include `@vue/test-utils`, `jsdom`, and `@vitejs/plugin-vue`.
  </acceptance_criteria>
  <done>The component-test harness is real: a genuine auto-UI SFC mounts in jsdom inside `vitest run`, the global node environment is preserved for existing tests, and the full suite is green at the e46d450 baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → npm registry | Three new devDependencies are pulled from npm (supply-chain surface) |

This plan adds NO network, auth, persistence, or untrusted-input surface to the product. The only
new surface is the supply-chain trust of three test-only devDependencies.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.1-SC | Tampering | npm installs (@vue/test-utils, jsdom, @vitejs/plugin-vue) | mitigate | All three are [ASSUMED] (slopcheck unavailable); Task 1 is a blocking-human legitimacy checkpoint verifying each on its npm registry page before install. Never auto-approved. |
| T-94.1-01 | Information disclosure | PRESENT-02 face-down-card property | accept (no-regress note) | This plan does not touch rendering; the Go Fish opponent-hand visibility property (only back.svg, anonymized ids) is exercised/guarded in later plans (94.1-04). No regression risk here. |
</threat_model>

<verification>
- `npm test` green at the e46d450 baseline + the new smoke test passing.
- vitest still defaults to node env globally; only the smoke file opts into jsdom.
</verification>

<success_criteria>
- Three approved devDependencies installed.
- vitest transforms `.vue` SFCs and a real auto-UI component mounts under jsdom in `vitest run`.
- No new failures in the full suite.
</success_criteria>

<output>
Create `.planning/phases/94.1-auto-ui-interaction-completion/94.1-01-SUMMARY.md` when done.
</output>
