---
phase: 94.1-auto-ui-interaction-completion
plan: 05
type: execute
wave: 5
depends_on: ["94.1-02", "94.1-03", "94.1-04"]
files_modified: []
autonomous: false
requirements: [R2, R3, R4, R5]
must_haves:
  truths:
    - "The full test suite is green (e46d450 baseline + all new component/integration tests) before the browser run"
    - "Checkers is playable start-to-finish via the auto-UI in the browser (move, single capture, multi-jump, king promotion)"
    - "Go Fish is playable start-to-finish via the auto-UI in the browser (deal, ask, draw, collect sets, end)"
    - "Hex is still playable via single board click (regression confirmed in the browser)"
    - "No dev server is left running after verification"
  artifacts: []
  key_links:
    - from: "component/integration test suite"
      to: "browser playthrough"
      via: "tests are the primary gate; the browser run is the final confirmation per CLAUDE.md"
      pattern: "npm test"
---

<objective>
Final playability gate: confirm in the BROWSER that all three gate games are playable start-to-finish
via the auto-UI, on top of the green component/integration suite. Per CLAUDE.md the component tests are
the primary gate (they are what was missing in Phase 94); the browser run is the FINAL confirmation, not
a substitute. This plan closes the phase goal: Hex + Go Fish + Checkers playable end-to-end via the
auto-UI, proven by tests AND verified in the browser.

Purpose: The structural reason this phase exists is that Phase-94 fixes passed unit tests but failed in
the browser. This gate verifies the new component-test bar actually translates to a playable browser
experience.
Output: A human-verified playthrough record for all three games and a confirmed-green full suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/94.1-auto-ui-interaction-completion/94.1-CONTEXT.md
@.planning/phases/94.1-auto-ui-interaction-completion/94.1-RESEARCH.md

<repro_notes>
From CONTEXT.md (manual repro):
- Games symlink `node_modules/boardsmith → local source`. UI changes hot-reload; RULES changes require a
  dev-server restart (the dev host loads rules into its Node process at startup). Checkers/Go Fish fixes in
  this phase are in the UI layer (src/ui/*), so they hot-reload — but restart if in doubt.
- Checkers: `cd ~/BoardSmithGames/checkers && npx boardsmith dev --players 2`, open localhost:5173,
  "Take seat" (WAIT for load — clicks race page load), "Follow active seat", click a row-6 red piece →
  destinations must now populate (not "No options available") and be playable; complete a move, a single
  capture, a multi-jump (source+target+captured highlighted), and a king promotion.
- Go Fish: `cd ~/BoardSmithGames/go-fish && npx boardsmith dev --players 2`; on your turn the ask must
  surface a footer with target/rank choices; complete deal → ask → draw → collect a set → end game. The
  opponent's hand must show only card backs (PRESENT-02).
- Hex: `cd ~/BoardSmithGames/hex && npx boardsmith dev --players 2`; place a stone via a single hex click
  (regression guard) and play to a win.
- The user has the Claude Code Chrome extension enabled — use it for browser verification.
- NEVER leave a dev server running: kill it before returning.
</repro_notes>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Browser playthrough gate — Checkers, Go Fish, Hex</name>
  <read_first>
    - .planning/phases/94.1-auto-ui-interaction-completion/94.1-CONTEXT.md (Repro section — exact steps per game)
    - .planning/phases/94.1-auto-ui-interaction-completion/94.1-02-SUMMARY.md
    - .planning/phases/94.1-auto-ui-interaction-completion/94.1-03-SUMMARY.md
    - .planning/phases/94.1-auto-ui-interaction-completion/94.1-04-SUMMARY.md
  </read_first>
  <what-built>
    Plans 01-04 stood up the component-test harness and fixed Bug C (reactive choice population), D-02
    (footer fallback), Bug A (Checkers destination population + multi-jump highlight), and Bug B (Go Fish
    ask), each proven by a mounted component/integration test. Before this checkpoint the executor MUST
    confirm the full suite is green (`npm test` at the e46d450 baseline + all new tests passing), then start
    each game's dev server (via the Chrome extension) for the human playthrough, and KILL each dev server
    immediately after that game's verification — never leave one running.
  </what-built>
  <how-to-verify>
    Precondition (executor runs first, automated): `npm test` is green — 892 baseline + new component/
    integration tests pass, only the known pre-existing `followUp+skip+auto-execute` fail remains. If any
    NEW failure exists, STOP and do not proceed to the browser.

    Then verify each game in the browser (Chrome extension), per CONTEXT repro steps:
    1. Checkers (`npx boardsmith dev --players 2`, localhost:5173, Take seat → Follow active seat):
       - Click a row-6 red piece → destination cells populate (NOT "No options available") and are clickable.
       - Complete a normal move, a single capture, a multi-jump (source+target+captured all highlighted
         simultaneously), and a king promotion. Game playable start-to-finish via auto-UI only.
       - Kill the Checkers dev server.
    2. Go Fish (`npx boardsmith dev --players 2`):
       - On your turn the ask surfaces a footer with target/rank choices; complete deal → ask → draw →
         collect a set → end game.
       - Opponent's hand shows only card backs (no face leak — PRESENT-02).
       - Kill the Go Fish dev server.
    3. Hex (`npx boardsmith dev --players 2`):
       - Place a stone via a single hex click (regression guard); play to a win.
       - Kill the Hex dev server.
    Confirm NO dev server remains running after all three.
  </how-to-verify>
  <verify>
    <automated>npm test</automated>
    <human-check>All three games played start-to-finish via the auto-UI in the browser per the steps above; opponent hand shows only backs in Go Fish; all dev servers killed.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm test` is green at the e46d450 baseline + all new tests, with NO new failures, before the browser run.
    - Checkers: move, single capture, multi-jump (all refs highlighted), and king promotion all completed via the auto-UI in the browser.
    - Go Fish: full turn cycle (deal/ask/draw/collect/end) completed via the auto-UI; opponent hand renders only card backs (PRESENT-02 confirmed in browser).
    - Hex: stone placed via single board click; game played to a win (regression confirmed).
    - No dev server left running (verified by the executor before returning).
  </acceptance_criteria>
  <resume-signal>Type "approved" if all three games played to completion via the auto-UI, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner player ↔ opponent hidden hand (Go Fish) | Browser run must confirm no opponent face card is rendered (PRESENT-02) |
| developer machine ↔ local dev server | Dev servers are started for verification and must be killed afterward (no lingering processes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.1-05 | Information disclosure | Go Fish opponent hand in the live browser | mitigate | The playthrough explicitly checks the opponent's hand shows only card backs — a live re-confirmation of PRESENT-02 on top of the component guard (T-94.1-04). |
| T-94.1-DS | Denial of service (self) | lingering local dev server | mitigate | The checkpoint mandates killing each dev server immediately after its game; the executor confirms none remain before returning (CLAUDE.md "never leave a dev server running"). |
</threat_model>

<verification>
- `npm test` green at baseline + new tests before the browser run.
- Human-verified playthrough of Checkers, Go Fish, Hex via the auto-UI.
- No dev server left running.
</verification>

<success_criteria>
- All three gate games playable start-to-finish via the auto-UI, confirmed in the browser.
- Full suite green; PRESENT-02 visibility confirmed live.
</success_criteria>

<output>
Create `.planning/phases/94.1-auto-ui-interaction-completion/94.1-05-SUMMARY.md` when done.
</output>
