---
phase: 95-ship-reframe
plan: 03
type: execute
wave: 2
depends_on: ["95-01-ship-reframe"]
files_modified:
  - src/cli/lib/treeshake-bundle.test.ts
autonomous: true
requirements: [SHIP-02]
must_haves:
  truths:
    - "A production build of a game whose boardsmith.json \"ui\" points at a custom UI contains NO AutoUI/AutoRenderer symbols"
    - "A production build of a game with \"ui\":\"auto\" DOES contain AutoUI symbols (positive control)"
    - "Tree-shaking is proven by a real build + bundle grep, not a code-review claim, and uses no rollupOptions.input / no runtime registry"
  artifacts:
    - path: "src/cli/lib/treeshake-bundle.test.ts"
      provides: "Integration test: scaffold a fixture game, run the real build, grep dist for AutoRenderer"
      contains: "AutoRenderer"
  key_links:
    - from: "generated App.vue single static import (Plan 01)"
      to: "dist/ui bundle contents"
      via: "Vite/Rollup native tree-shaking"
      pattern: "AutoRenderer"
---

<objective>
Prove SHIP-02 with an automated bundle assertion: a fixture game whose chosen UI is a custom component produces a production bundle with NO AutoUI/AutoRenderer symbols, while a "ui":"auto" game does include them. This is the load-bearing tree-shaking guarantee — it must be a real build + grep, not inspection.

Purpose: D-02 chose a config field over a pure import convention; this is only acceptable IF the config resolves to a single static import so ordinary tree-shaking drops the unselected UI. This plan verifies that empirically and guards against the GameTable re-export landmine and any future regression.
Output: An integration test that scaffolds, builds, and greps the real bundle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-ship-reframe/95-RESEARCH.md
@.planning/phases/95-ship-reframe/95-01-PLAN.md

<interfaces>
<!-- Build pipeline confirmed by RESEARCH code trace. -->
boardsmith build → src/cli/commands/build.ts buildCommand():
  viteBuild({ root: cwd }) → dist/ui/  (entry: game index.html → src/main.ts → src/ui/index.ts → App.vue)
  Rollup tree-shakes from that entry — only components reachable from App.vue's static import survive.
generateScaffoldFiles(config) (src/cli/lib/project-scaffold.ts L471) returns the full file set for a fixture.
AutoUI/AutoRenderer are exported from boardsmith/ui (src/ui/index.ts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tree-shaking proof — custom-UI fixture build excludes AutoRenderer</name>
  <read_first>
    - src/cli/commands/build.ts (buildCommand / viteBuild invocation — how the UI bundle is produced)
    - src/cli/lib/project-scaffold.ts (generateScaffoldFiles + generateAppVue branch on config.ui, post-Plan-01)
    - src/cli/lib/sandbox-scan.test.ts (mkdtempSync/rmSync temp-dir + beforeEach/afterEach cleanup pattern)
    - .planning/phases/95-ship-reframe/95-RESEARCH.md (Build Pipeline + tree-shaking proof command, lines 96–120, 366–373)
  </read_first>
  <action>Create src/cli/lib/treeshake-bundle.test.ts as a vitest integration test with an extended timeout (build is heavyweight). Set up a fixture game project so that boardsmith/ui resolves to this repo's workspace (place the fixture under a repo-relative temp location and/or symlink node_modules, or reuse the existing repo's resolution — do NOT vendor a fake AutoUI; the test must build against the real boardsmith/ui). Generate the scaffold files via generateScaffoldFiles with `ui` set to a custom relative path (e.g. "./ui/components/GameTable.vue") so generateAppVue emits a single import of the custom component and NO AutoUI import. Run the real production UI build (invoke the same path as `boardsmith build` for the UI bundle — call build.ts's command or viteBuild on the fixture root). After the build, read every JS file under the fixture's dist/ui and assert none contains `AutoRenderer` or the AutoUI component identifier. Clean up the temp dir in afterEach. If workspace resolution makes a full CLI build impractical, drive viteBuild directly with the same config build.ts uses — but the bundle MUST be produced from a real Rollup tree-shake, not a stub.</action>
  <verify>
    <automated>npm test -- --run src/cli/lib/treeshake-bundle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The test performs a real production build of a fixture whose App.vue imports only a custom UI (no AutoUI import).
    - The test reads the actual dist/ui JS output and asserts `grep`-equivalent: `AutoRenderer` absent AND the AutoUI symbol absent.
    - No mock/stub substitutes for the real bundle; no rollupOptions.input manipulation is introduced.
    - Test passes.
  </acceptance_criteria>
  <done>An automated test proves a custom-UI production bundle contains no AutoUI/AutoRenderer symbols.</done>
</task>

<task type="auto">
  <name>Task 2: Positive control — "ui":"auto" fixture build includes AutoRenderer</name>
  <read_first>
    - src/cli/lib/treeshake-bundle.test.ts (the harness from Task 1)
    - src/cli/lib/project-scaffold.ts (generateAppVue auto branch)
  </read_first>
  <action>Add a second case to treeshake-bundle.test.ts that scaffolds a fixture with `ui: "auto"` (generateAppVue emits the AutoUI import), runs the same real build, and asserts the dist/ui bundle DOES contain `AutoRenderer` (or the AutoUI render symbol). This positive control proves the negative case in Task 1 is a genuine tree-shake drop, not a build that silently emits nothing. Reuse the Task 1 build+grep helper.</action>
  <verify>
    <automated>npm test -- --run src/cli/lib/treeshake-bundle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The auto-UI fixture build's dist/ui bundle contains `AutoRenderer`/AutoUI symbols (positive control passes).
    - Both cases share the same build+grep helper.
    - Test passes.
  </acceptance_criteria>
  <done>The positive control confirms the tree-shaking assertion is meaningful (AutoUI present when selected, absent when not).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only; builds a local fixture, no external input, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-03 | Tampering | fixture build resolving boardsmith/ui | accept | Test builds against the real repo workspace only; temp dir cleaned in afterEach. No package installs in this phase (no T-95-SC). |
</threat_model>

<verification>
- `npm test -- --run src/cli/lib/treeshake-bundle.test.ts` green.
- Negative case: custom-UI bundle has no AutoRenderer. Positive case: auto-UI bundle has AutoRenderer.
- Full suite `npm test` green after merge.
</verification>

<success_criteria>
- SHIP-02 proven automatically: removing the AutoUI static import (selecting a custom UI) drops AutoUI/AutoRenderer from the production bundle via ordinary tree-shaking.
</success_criteria>

<output>
Create `.planning/phases/95-ship-reframe/95-03-SUMMARY.md` when done
</output>
