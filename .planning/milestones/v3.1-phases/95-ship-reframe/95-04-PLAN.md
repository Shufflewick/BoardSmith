---
phase: 95-ship-reframe
plan: 04
type: execute
wave: 2
depends_on: ["95-01-ship-reframe"]
files_modified: []
autonomous: false
requirements: [SHIP-03]
must_haves:
  truths:
    - "boardsmith dev on a freshly scaffolded game opens a single UI (auto-UI) with no split-screen"
    - "The freshly scaffolded game is playable in the browser via the auto-UI with zero custom UI code"
  artifacts: []
  key_links:
    - from: "single-UI App.vue (Plan 01)"
      to: "boardsmith dev iframe (/__boardsmith/play)"
      via: "dev server serves the game's own App.vue — no DevHost change needed"
      pattern: "boardsmith dev"
---

<objective>
Confirm end-to-end that a freshly scaffolded game opens single-UI and is playable in the browser via `boardsmith dev` (SHIP-03 success criteria 3 + 4). This is the manual gate that RESEARCH/VALIDATION flagged as the only non-automatable check — the dev host serves the game's own App.vue, so the single-UI scaffold from Plan 01 should yield a single-UI dev experience with no changes to dev.ts/DevHost.vue.

Purpose: Per CLAUDE.md, verify behavior by running the application — confirm the scaffold change actually renders and plays in the browser, not just that generators emit the right strings.
Output: Human confirmation that the scaffold-to-play loop works single-UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-ship-reframe/95-RESEARCH.md
@.planning/phases/95-ship-reframe/95-01-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Browser smoke-test — fresh scaffold opens single-UI and plays</name>
  <read_first>
    - .planning/phases/95-ship-reframe/95-01-PLAN.md (the single-UI scaffold this verifies)
    - .planning/phases/95-ship-reframe/95-RESEARCH.md (dev pipeline trace, lines 80–94 — dev serves the game's own App.vue)
    - /Users/jtsmith/CLAUDE.md (use the Claude Code Chrome extension for browser testing; never leave a dev server running)
  </read_first>
  <action>Scaffold a throwaway game via the repo's scaffold entry, run `boardsmith dev`, and drive a browser smoke-test using the Claude Code Chrome extension. Confirm a single auto-UI renders (no split-screen), play a few turns with the scaffold's placeholder action, confirm boardsmith.json has "ui":"auto" and the custom-UI stub exists but is unmounted, then stop the dev server and delete the throwaway game. This is a verification action only — no production code is modified.</action>
  <what-built>
    Plan 01 replaced the split-screen scaffold with a single-UI App.vue (AutoUI by default, driven by boardsmith.json "ui"). Because boardsmith dev serves the game's own App.vue through the /__boardsmith/play iframe, a fresh scaffold should open directly in the auto-UI with no split-screen and be immediately playable.
  </what-built>
  <how-to-verify>
    1. Scaffold a throwaway game into a temp dir: `boardsmith init smoke-95` (or the repo's scaffold entry) and `cd` into it.
    2. Run `boardsmith dev` and open the dev host in the browser (use the Claude Code Chrome extension per CLAUDE.md).
    3. Confirm: exactly ONE UI renders (the auto-UI) — NO side-by-side "Custom UI" / "Auto-Generated UI" panels, no board-comparison grid.
    4. Play a few turns with the scaffold's placeholder action — confirm the auto-UI renders state and accepts interaction (the game is playable with zero custom UI code).
    5. Open boardsmith.json and confirm it contains `"ui": "auto"`; confirm the custom-UI stub (GameTable.vue) exists but is NOT mounted.
    6. Stop the dev server (do not leave it running) and delete the throwaway game.
  </how-to-verify>
  <verify>
    <human-check>Single auto-UI renders and plays in the browser; no split-screen; boardsmith.json has "ui":"auto"; dev server stopped and throwaway game deleted.</human-check>
  </verify>
  <acceptance_criteria>
    - Exactly one UI (auto-UI) renders in `boardsmith dev` — no split-screen / no "Auto-Generated UI" panel.
    - The game is playable via the auto-UI with zero custom UI code.
    - boardsmith.json contains "ui":"auto"; the custom-UI stub exists but is not mounted.
    - Dev server stopped and throwaway game removed afterward.
  </acceptance_criteria>
  <done>Human confirms single-UI render + playability for a freshly scaffolded game.</done>
  <resume-signal>Type "approved" if single-UI renders and plays, or describe what rendered (e.g. split-screen still present, blank board, interaction broken).</resume-signal>
</task>

</tasks>

<verification>
- Manual: single auto-UI renders in `boardsmith dev`; game is playable; no split-screen; boardsmith.json has "ui":"auto".
- Dev server stopped and throwaway game removed afterward.
</verification>

<success_criteria>
- A freshly scaffolded game opens single-UI in the browser and is playable via the auto-UI with no custom code (SHIP-03 criteria 3 & 4).
</success_criteria>

<output>
Create `.planning/phases/95-ship-reframe/95-04-SUMMARY.md` when done
</output>
