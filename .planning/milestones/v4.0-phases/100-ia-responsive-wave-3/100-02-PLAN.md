---
phase: 100-ia-responsive-wave-3
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/components/auto-ui/renderers/GridBoardRenderer.vue
  - src/ui/components/auto-ui/renderers/HexBoardRenderer.vue
  - src/ui/components/auto-ui/archetypes/GridBoardTemplate.vue
  - src/ui/components/auto-ui/renderers/GridBoardRenderer.fluid.test.ts
autonomous: true
requirements: [IA-05]

must_haves:
  truths:
    - "Grid cells size from a computed --cell clamp driven by the board's cols/rows, not a fixed 50px"
    - "Grid column/corner/row labels track the same --cell token so they stay aligned with cells"
    - "The hex board scales to its container (no max-height:80vh / overflow:hidden viewport clamp)"
    - "The grid archetype establishes its own container-type:size so cqw/cqh resolve regardless of the shell"
  artifacts:
    - path: "src/ui/components/auto-ui/renderers/GridBoardRenderer.vue"
      provides: "Fluid --cell clamp grid cells + label widths"
      contains: "--cell"
    - path: "src/ui/components/auto-ui/archetypes/GridBoardTemplate.vue"
      provides: "container-type:size board area + fluid hand-strip max-height"
      contains: "container-type"
  key_links:
    - from: "GridBoardRenderer .grid-cell"
      to: "--cols / --rows custom props"
      via: ":style binding from gridResult cols/rows"
      pattern: "--cols"
---

<objective>
Retire the fixed-pixel board sizing on the grid + hex auto-UI renderers and the grid archetype (IA-05). Grid cells become `--cell: clamp(28px, min(calc(100cqw/var(--cols)), calc(100cqh/var(--rows))), 96px)` with `--cols`/`--rows` set from the renderer's gridResult; labels track `--cell`; the hex SVG scales to its container instead of clamping to 80vh; the grid archetype carries its own `container-type:size` so container-query units resolve robustly even independent of GameShell's boardregion.

Purpose: The board fits its container by construction so the zoom slider is no longer the fit strategy (IA-05). This is the regression-critical renderer subset — call it out for Phase 103.
Output: Updated GridBoardRenderer.vue, HexBoardRenderer.vue, GridBoardTemplate.vue + new fluid test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/100-ia-responsive-wave-3/100-CONTEXT.md
@.planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md
@planning/mockups/boardsmith-chrome.html

<interfaces>
<!-- gridResult exposes cols/rows in GridBoardRenderer; confirm exact field names while reading. -->
- GridBoardRenderer renders .board-label (col), .board-label.corner, .board-row-labels .board-label, and .grid-cell.
- The mockup's reference clamp: `--c: clamp(46px, min(11cqw,12cqh), 86px)` (boardsmith-chrome.html:93). Phase target clamp uses cols/rows denominators per RESEARCH IA-05.
- HexBoardRenderer computes viewBox from hexToPixel; .hex-board-svg already has max-width:100%; height:auto at lines 269-270.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fluid --cell clamp for grid cells + aligned labels</name>
  <files>src/ui/components/auto-ui/renderers/GridBoardRenderer.vue, src/ui/components/auto-ui/renderers/GridBoardRenderer.fluid.test.ts</files>
  <read_first>
    - src/ui/components/auto-ui/renderers/GridBoardRenderer.vue lines 160-200 (template: labels + grid-cell), 240-300 (CSS: .board-label, .board-label.corner, .board-row-labels, .grid-cell)
    - planning/mockups/boardsmith-chrome.html lines 92-100 (.board / .cell --c clamp)
    - .planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md "Pattern 2" + "Pitfall 3" + Fixed-Pixel Inventory rows for GridBoardRenderer
  </read_first>
  <behavior>
    - The grid container exposes `--cols` and `--rows` custom properties bound from gridResult's column/row counts.
    - `.grid-cell` width and height both resolve to `var(--cell)` where `--cell` is the clamp expression.
    - `.board-label` (column) width resolves to `var(--cell)`; `.board-label.corner` and row-label widths track the cell token (corner narrower per RESEARCH).
  </behavior>
  <action>
    On the grid wrapper element add a `:style` binding setting `--cols` and `--rows` from the renderer's gridResult column/row counts (read the exact field names — likely gridResult.cols / gridResult.rows). Define `--cell: clamp(28px, min(calc(100cqw / var(--cols)), calc(100cqh / var(--rows))), 96px)` on that wrapper (or :root of the renderer). Replace `.grid-cell { width:50px; height:50px }` (lines 289-290) with `width:var(--cell); height:var(--cell)`. Replace `.board-label { width:50px; height:20px }` (249-250) with `width:var(--cell); height:auto`. Replace `.board-label.corner { width:20px }` (261) and `.board-row-labels .board-label { width:20px; height:50px }` (276-277) so the corner/row label widths are derived from `--cell` (e.g. `width:calc(var(--cell) * 0.4)` for corner; row-label height `var(--cell)`). Tokens/calc only — no new hex.
    Create GridBoardRenderer.fluid.test.ts: mount GridBoardRenderer with a known cols/rows board and assert the rendered grid wrapper carries the `--cols`/`--rows` custom properties and that `.grid-cell` CSS uses `var(--cell)` (assert via the component's style string / computed inline style, not a pixel value). Mirror an existing renderer test's mounting setup.
  </action>
  <verify>
    <automated>npm run test -- src/ui/components/auto-ui/renderers/GridBoardRenderer.fluid.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "width:\s*50px|height:\s*50px" src/ui/components/auto-ui/renderers/GridBoardRenderer.vue` returns nothing
    - `grep -c "var(--cell)" src/ui/components/auto-ui/renderers/GridBoardRenderer.vue` ≥ 3 (cell w/h + label)
    - `grep -c -- "--cols" src/ui/components/auto-ui/renderers/GridBoardRenderer.vue` ≥ 1 and `-- "--rows"` ≥ 1
    - `npm run test -- src/ui/components/auto-ui/renderers/GridBoardRenderer.fluid.test.ts` passes
  </acceptance_criteria>
  <done>Grid cells and labels are fluid via a cols/rows-driven --cell clamp; the fluid test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Hex board scales to its container</name>
  <files>src/ui/components/auto-ui/renderers/HexBoardRenderer.vue</files>
  <read_first>
    - src/ui/components/auto-ui/renderers/HexBoardRenderer.vue lines 188-200 (.hex-board-container + svg), 249-270 (.hex-board-container CSS incl max-height:80vh; overflow:hidden, and the svg max-width:100%; height:auto)
    - .planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md "Pitfall 7"
  </read_first>
  <action>
    Remove `overflow:hidden` and `max-height:80vh` from `.hex-board-container` (lines 256-257). Let the SVG drive its size from the computed viewBox while constraining it to the container with `max-width:100%; max-height:100%; height:auto; width:auto` (the svg already has max-width:100%; height:auto — add max-height:100% so it cannot exceed the container's height). Ensure `.hex-board-container` itself can shrink (`min-height:0; max-width:100%`). Do NOT introduce viewport units. Tokens/values only — no new hex colors.
  </action>
  <verify>
    <automated>npm run lint:css && npm run test -- src/ui/components/auto-ui 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "80vh|overflow:\s*hidden" src/ui/components/auto-ui/renderers/HexBoardRenderer.vue` returns nothing
    - The `.hex-board-svg` rule carries `max-height:100%` (or equivalent) alongside the existing max-width:100%
    - `npm run lint:css` exits 0
  </acceptance_criteria>
  <done>The hex board scales to fit its container with no viewport-relative clamp or hard overflow clip.</done>
</task>

<task type="auto">
  <name>Task 3: Grid archetype owns container-type + fluid hand strip</name>
  <files>src/ui/components/auto-ui/archetypes/GridBoardTemplate.vue</files>
  <read_first>
    - src/ui/components/auto-ui/archetypes/GridBoardTemplate.vue lines 80-136 (template grid areas + .grid-board-template__hand CSS incl max-height:30vh at 130)
    - .planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md "Assumption A3" (intermediate container-type on the archetype wrapper) + Fixed-Pixel Inventory GridBoardTemplate row
  </read_first>
  <action>
    Add `container-type:size` to the archetype's board/focal area wrapper (the grid-area that hosts the board renderer) so the renderer's cqw/cqh units resolve against the archetype even when this archetype is not inside GameShell's boardregion (robustness per Assumption A3). Ensure that wrapper has a definite block size (the existing `height:100%` grid at line 105 plus `min-height:0` on the board area). Replace `.grid-board-template__hand { max-height:30vh }` (line 130) with `max-height:clamp(96px, 22vh, 180px)`. Tokens/calc only.
  </action>
  <verify>
    <automated>npm run lint:css && npm run test -- src/ui/components/auto-ui 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "container-type" src/ui/components/auto-ui/archetypes/GridBoardTemplate.vue` ≥ 1
    - `grep -E "max-height:\s*30vh" src/ui/components/auto-ui/archetypes/GridBoardTemplate.vue` returns nothing; the hand strip uses `clamp(96px, 22vh, 180px)`
    - `npm run lint:css` exits 0
  </acceptance_criteria>
  <done>The grid archetype establishes its own container-query context and the docked hand strip uses a fluid clamp.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Pure CSS/template renderer changes; no inbound data, no new boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-02-01 | Tampering | --cols/--rows from game data | accept | values are integer counts from engine-built gridResult (already trusted render path); CSS clamp floors/ceils the result |
| T-100-02-SC | Tampering | npm/pip/cargo installs | accept | no package installs; CSS-only change |
</threat_model>

<verification>
- `npm run test` full suite green
- `npm run lint:css` green
- Phase 103 regression note: GridBoardRenderer, HexBoardRenderer, GridBoardTemplate changed (fluid sizing) — re-check against all 9 games + MERC.
</verification>

<success_criteria>
IA-05 (grid + hex subset): board cells/labels and hex SVG fit their container via container queries and clamps; no 50px/20px/80vh/30vh fixed values remain in these files; fluid test passes; lint:css green.
</success_criteria>

<output>
Create `.planning/phases/100-ia-responsive-wave-3/100-02-SUMMARY.md` when done. List the three changed renderer/archetype files explicitly as the Phase 103 regression surface for fluid sizing.
</output>
