---
phase: 101-accessibility-wcag-wave-4
plan: 05
type: execute
wave: 2
depends_on: [101-01]
files_modified:
  - src/ui/components/GameShell.vue
  - src/ui/components/GameShell.live-region.test.ts
  - src/ui/components/GameHeader.vue
  - src/ui/components/GameHistory.vue
  - src/ui/components/Toast.vue
  - src/ui/components/Toast.a11y.test.ts
autonomous: true
requirements: [A11Y-02, A11Y-03, A11Y-06, A11Y-08, A11Y-10]

must_haves:
  truths:
    - "Turn changes, opponent turns, connection changes, and game-over each write to a live region a screen reader announces"
    - "GameHistory is a role=log aria-live=polite region"
    - "Every interactive element shows a visible focus-visible ring; no outline:none survives without a ring replacement"
    - "prefers-reduced-motion: reduce halts pulse/slide/toast animation globally"
    - "A skip link jumps to #main and a visually-hidden h1 names the play view"
    - "When all picks are board-anchored the footer still shows the prompt; only the action buttons are suppressed"
    - "Toasts are role=status (role=alert for errors) with a real dismiss button"
  artifacts:
    - path: "src/ui/components/GameShell.vue"
      provides: "dual live regions, global focus-ring + reduced-motion + .vh/.sr-skip blocks, skip link, h1, footer parity gate"
      contains: "aria-live"
    - path: "src/ui/components/Toast.vue"
      provides: "role=status/alert toast with dismiss <button aria-label='Dismiss'>"
      contains: "aria-label=\"Dismiss\""
    - path: "src/ui/components/Toast.a11y.test.ts"
      provides: "jsdom assertions for toast role + dismiss button"
  key_links:
    - from: "src/ui/components/GameShell.vue"
      to: "live region refs"
      via: "watchers on isMyTurn/connectionStatus/flowState.complete"
      pattern: "aria-live"
    - from: "src/ui/components/GameShell.vue"
      to: "#main"
      via: "skip link href"
      pattern: "sr-skip"
---

<objective>
Make GameShell the single owner of every shell-level a11y primitive: dual live regions (A11Y-03), the global focus-visible ring + GameHeader outline deletion (A11Y-06), the global reduced-motion block (A11Y-08), the skip link + visually-hidden h1 (A11Y-10), and the footer parity gate that keeps the prompt visible (A11Y-02). Also make GameHistory a log region (A11Y-03) and Toast an accessible status/alert with a dismiss button (A11Y-10).

Purpose: These are global, non-scoped concerns that MUST live in one place to avoid divergence. Live regions and global CSS blocks belong to the shell.
Output: GameShell live regions + global blocks + skip link + h1 + footer gate; GameHeader outline removed; GameHistory log role; accessible Toast + test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md
@planning/mockups/boardsmith-chrome.html

<interfaces>
Anchors [VERIFIED 101-RESEARCH.md]:
  GameShell.vue — <main id="main">:1487, one aria-live="polite" span:1592, footer suppression via allCurrentChoicesAnchored:1596; signals isMyTurn, connectionStatus, state?.flowState?.complete, winnerSeats
  GameHeader.vue — outline:none at :185 (.zoom-slider rule) -> DELETE
  GameHistory.vue — message list v-for of processedMessages -> add role="log" aria-live="polite" aria-relevant="additions"
  Toast.vue — bare <div> per toast, @click="remove(toast.id)", no role/button; useToast.ts already has setTimeout auto-dismiss + remove(id)
Mockup: focus ring line 59; skip link 282-283; h1 284; dual live regions 285-286 (#live role=status polite, #alive role=alert assertive); .vh visually-hidden class.
Tokens: focus ring = "outline:none; box-shadow:0 0 0 2px var(--bsg-bg), 0 0 0 4px var(--bsg-accent); border-radius:var(--bsg-r-sm)". Reduced-motion block per 101-RESEARCH.md:517-525.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dual live regions + announce wiring + GameHistory log role + jsdom behavioral test</name>
  <files>src/ui/components/GameShell.vue, src/ui/components/GameShell.live-region.test.ts, src/ui/components/GameHistory.vue</files>
  <read_first>
    - src/ui/components/GameShell.vue (template root, aria-live span :1592, signals isMyTurn/connectionStatus/state.flowState.complete/winnerSeats)
    - src/ui/components/GameHistory.vue (processedMessages v-for container)
    - planning/mockups/boardsmith-chrome.html:285-286 (#live / #alive)
    - .planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md A11Y-03 (lines 441-456), Pitfall 2 (lines 613-617)
    - any existing GameShell test (mount harness / required props + injection) to mirror
  </read_first>
  <action>
    In GameShell.vue add two unconditionally-mounted (never v-if) visually-hidden paragraphs at the template root: `<p class="vh" role="status" aria-live="polite">{{ politeMessage }}</p>` and `<p class="vh" role="alert" aria-live="assertive">{{ assertiveMessage }}</p>`. Back them with two refs and `watch(..., { immediate: false })` (never write at mount — research Pitfall 2): isMyTurn toggle -> "Your turn"; opponent turn (from awaitingPlayers/awaitingPlayerNames) -> "<name> is playing…"; connectionStatus change -> "Reconnecting…"/"Reconnected"; state.flowState.complete -> assertive "Game over — <winner> wins". Also emit an `announce` postMessage alongside each write so a future host can relay (host relay deferred). Keep the existing :1592 polite span or fold it into the new region — do not leave two competing polite regions. In GameHistory.vue add `role="log" aria-live="polite" aria-relevant="additions"` to the processedMessages list container. Then write `GameShell.live-region.test.ts` (`// @vitest-environment jsdom`) that mounts GameShell with a minimal/mocked game state, toggles isMyTurn to true, flushes reactivity, and asserts the polite region's text becomes "Your turn" (proves the watcher fires with immediate:false — not on mount). If mounting full GameShell is impractical, extract the announce-message derivation into a tiny pure helper and unit-test that helper's mapping (isMyTurn/connection/game-over → text) instead — the behavioral mapping MUST have an automated assertion, not grep-only.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/GameShell.live-region.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "role=\"status\"" src/ui/components/GameShell.vue` returns >= 1
    - `grep -c "role=\"alert\"" src/ui/components/GameShell.vue` returns >= 1
    - `grep -c "role=\"log\"" src/ui/components/GameHistory.vue` returns 1
    - `grep -c "announce" src/ui/components/GameShell.vue` returns >= 1 (postMessage emit)
    - Live region writes occur in watchers with immediate:false, not on mount
    - GameShell.live-region.test.ts: toggling isMyTurn (or the extracted helper) yields polite text "Your turn"; the region is empty at mount (no write on mount)
    - `npx vitest run src/ui/components/GameShell.live-region.test.ts` exits 0
  </acceptance_criteria>
  <done>Dual live regions mounted + wired to turn/connection/game-over; announce postMessage emitted; GameHistory is role=log; behavioral live-region test green.</done>
</task>

<task type="auto">
  <name>Task 2: Global focus ring + reduced-motion + skip link + h1 + footer gate + GameHeader outline delete</name>
  <files>src/ui/components/GameShell.vue, src/ui/components/GameHeader.vue</files>
  <read_first>
    - src/ui/components/GameShell.vue (non-scoped style block; <main id="main">:1487; footer v-if allCurrentChoicesAnchored:1596)
    - src/ui/components/GameHeader.vue (outline:none .zoom-slider :185)
    - planning/mockups/boardsmith-chrome.html (focus ring :59, skip link :282, h1 :284, .vh, .sr-skip)
    - .planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md A11Y-06 (lines 476-493), A11Y-08 (lines 515-525), A11Y-02 footer (lines 437-439), A11Y-10 skip/h1 (lines 589-601)
  </read_first>
  <action>
    Add a NON-scoped `<style>` block to GameShell.vue (global) containing: (1) the `:focus-visible` ring `outline: none; box-shadow: 0 0 0 2px var(--bsg-bg), 0 0 0 4px var(--bsg-accent); border-radius: var(--bsg-r-sm);`; (2) the reduced-motion block `@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration: .01ms !important; animation-iteration-count: 1 !important; transition-duration: .01ms !important; } }`; (3) the `.vh` visually-hidden utility (NOT display:none — must stay in the a11y tree); (4) the `.sr-skip` class (visually hidden until :focus). Add the skip link `<a class="sr-skip" href="#main">Skip to game board</a>` and a visually-hidden `<h1 class="vh">BoardSmith — game board</h1>` at the template root (before #main at :1487). Change the footer parity gate at :1596 so the footer is NOT fully suppressed while picks are pending: when `allCurrentChoicesAnchored` is true, suppress only the action BUTTONS, leaving the prompt and the (plan 06) anchored-choices list visible. In GameHeader.vue DELETE the `outline: none` rule on `.zoom-slider` (:185) — the global ring now covers it. All colors via `var(--bsg-*)` only (no hex — lint:css gate).
  </action>
  <verify>
    <automated>npm run lint:css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ":focus-visible" src/ui/components/GameShell.vue` returns >= 1
    - `grep -c "prefers-reduced-motion" src/ui/components/GameShell.vue` returns >= 1
    - `grep -c "sr-skip" src/ui/components/GameShell.vue` returns >= 1
    - `grep -c "<h1" src/ui/components/GameShell.vue` returns >= 1
    - `grep -c "outline: none" src/ui/components/GameHeader.vue` returns 0
    - The footer template no longer fully suppresses on allCurrentChoicesAnchored (prompt stays)
    - `npm run lint:css` exits 0
  </acceptance_criteria>
  <done>Global ring + reduced-motion + .vh/.sr-skip + skip link + h1 added; footer keeps the prompt; GameHeader outline:none gone; lint:css green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Accessible Toast (role + dismiss button) + jsdom test</name>
  <files>src/ui/components/Toast.vue, src/ui/components/Toast.a11y.test.ts</files>
  <read_first>
    - src/ui/components/Toast.vue (bare div per toast, @click remove, slide transitions :69-88)
    - src/ui/composables/useToast.ts (setTimeout auto-dismiss :26, remove(id))
    - .planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md A11Y-10 Toast (lines 575-587)
  </read_first>
  <behavior>
    - A non-error toast renders role="status"; an error-type toast renders role="alert".
    - Each toast contains a real <button aria-label="Dismiss"> whose click calls remove(toast.id); the decorative ✕ glyph is aria-hidden.
    - Clicking the toast body no longer dismisses (only the button does).
  </behavior>
  <action>
    In Toast.vue set `:role="toast.type === 'error' ? 'alert' : 'status'"` on each toast element. Remove the `@click="remove(toast.id)"` from the outer toast body. Add a real `<button aria-label="Dismiss" @click.stop="remove(toast.id)"><span aria-hidden="true">✕</span></button>`. Auto-timeout already exists in useToast (do not re-implement). Ensure the dismiss button meets the target floor (min 44px) and uses `var(--bsg-*)` colors only. Write `Toast.a11y.test.ts` (`// @vitest-environment jsdom`) first asserting role mapping and the dismiss button calling remove.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/Toast.a11y.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "aria-label=\"Dismiss\"" src/ui/components/Toast.vue` returns 1
    - Toast.a11y.test.ts: error toast has role="alert", info/success has role="status"
    - Toast.a11y.test.ts: dismiss button click calls remove(id)
    - `npx vitest run src/ui/components/Toast.a11y.test.ts` exits 0
  </acceptance_criteria>
  <done>Toasts are role=status/alert with a keyboard-accessible dismiss button; body-click dismissal removed; test green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/ui/components/Toast.a11y.test.ts` green; full `npm test` stays green.
- `grep -c ":focus-visible" src/ui/components/GameShell.vue` >= 1; `grep -c "outline: none" src/ui/components/GameHeader.vue` == 0.
- `npm run lint:css` exits 0.
</verification>

<success_criteria>
- Live regions announce turn/connection/game-over; GameHistory is role=log.
- Global focus ring + reduced-motion + skip link + h1 present; footer keeps the prompt visible.
- Toasts are accessible with a dismiss button.
</success_criteria>

<output>
Create `.planning/phases/101-accessibility-wcag-wave-4/101-05-SUMMARY.md` when done.
</output>
