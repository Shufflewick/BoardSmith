---
phase: 101-accessibility-wcag-wave-4
plan: 06
type: execute
wave: 2
depends_on: [101-01]
files_modified:
  - src/ui/components/auto-ui/action-panel-helpers.ts
  - src/ui/components/auto-ui/action-panel-helpers.test.ts
  - src/ui/components/auto-ui/ActionPanel.vue
autonomous: true
requirements: [A11Y-02, A11Y-06, A11Y-09]

must_haves:
  truths:
    - "When every pick is board-anchored, the action panel renders those choices as a focusable secondary list of buttons — never a dead end"
    - "Activating a secondary-list button triggers the same selection as clicking the board element"
    - "The number/text input focus ring is visible (no outline:none)"
    - "The cancel and clear-selection controls meet a >= 44px target floor"
  artifacts:
    - path: "src/ui/components/auto-ui/action-panel-helpers.ts"
      provides: "splitAnchoredChoices returning { primary, anchored } — never drops anchored"
      contains: "anchored"
    - path: "src/ui/components/auto-ui/action-panel-helpers.test.ts"
      provides: "unit test proving anchored choices are returned, not dropped"
    - path: "src/ui/components/auto-ui/ActionPanel.vue"
      provides: "secondary <ul role=list> of <button> for anchored choices; no outline:none; 44px targets"
      contains: "role=\"list\""
  key_links:
    - from: "src/ui/components/auto-ui/ActionPanel.vue"
      to: "src/ui/components/auto-ui/action-panel-helpers.ts"
      via: "splitAnchoredChoices consumption"
      pattern: "splitAnchoredChoices|filterAnchoredChoices"
    - from: "ActionPanel secondary buttons"
      to: "triggerElementSelect"
      via: "notation activation"
      pattern: "triggerElementSelect"
---

<objective>
Close the second Critical finding: board-anchored choices must always be reachable through a focusable secondary list in the action panel instead of being silently dropped. Also delete the ActionPanel `outline:none` (A11Y-06) and bump its undersized controls to the target floor (A11Y-09).

Purpose: A11Y-02 (parity, Critical) is the screen-reader/keyboard escape hatch — when every pick is anchored to the board, the panel still offers operable controls.
Output: `splitAnchoredChoices` helper + test; ActionPanel secondary list; outline:none removed; cancel/clear targets >= 44px.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md
@src/ui/components/auto-ui/action-panel-helpers.ts

<interfaces>
Current filterAnchoredChoices [VERIFIED action-panel-helpers.ts:37-52] DROPS anchored choices:
```typescript
export function filterAnchoredChoices(choices: ChoiceWithRefs[], pickType: string|undefined): ChoiceWithRefs[]
// returns choices.filter(c => !isNotationAnchored(c))  <-- the dead end
```
Anchors: ActionPanel.vue imports at :29, calls at :255; outline:none at :1369; .cancel-btn :1179 (padding 2px 6px); .clear-selection-btn :1260 (padding 0 4px).
Note: no action-panel-helpers.test.ts exists yet (confirmed) — create it. The GameShell footer parity gate (:1596) is handled in plan 101-05.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: splitAnchoredChoices helper + unit test</name>
  <files>src/ui/components/auto-ui/action-panel-helpers.ts, src/ui/components/auto-ui/action-panel-helpers.test.ts</files>
  <read_first>
    - src/ui/components/auto-ui/action-panel-helpers.ts (filterAnchoredChoices :37-52, ChoiceWithRefs type, isNotationAnchored)
    - .planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md A11Y-02 (lines 432-439) + Pitfall 5 (lines 631-635)
  </read_first>
  <behavior>
    - For a non-'choice' pickType, all choices are primary, anchored empty.
    - For 'choice' pickType with some notation-anchored choices: primary = non-anchored, anchored = the notation-anchored ones (NEVER dropped).
    - For 'choice' pickType with no anchored choices: all primary, anchored empty.
  </behavior>
  <action>
    Replace the dropping `filterAnchoredChoices` with `splitAnchoredChoices(choices, pickType): { primary: ChoiceWithRefs[]; anchored: ChoiceWithRefs[] }` that partitions instead of filtering — anchored choices go into `anchored`, never discarded (No Backward Compatibility: delete the old function, update the single call site in Task 2; do not keep a shim). Create `action-panel-helpers.test.ts` (this file does not exist) first (RED) covering the three behaviors above.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/auto-ui/action-panel-helpers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "splitAnchoredChoices" src/ui/components/auto-ui/action-panel-helpers.ts` returns >= 1
    - `grep -c "filterAnchoredChoices" src/ui/components/auto-ui/action-panel-helpers.ts` returns 0 (old dead-end gone)
    - test asserts anchored choices appear in the returned `anchored` array (length > 0), not dropped
    - `npx vitest run src/ui/components/auto-ui/action-panel-helpers.test.ts` exits 0
  </acceptance_criteria>
  <done>splitAnchoredChoices partitions choices without dropping any; test green; old function removed.</done>
</task>

<task type="auto">
  <name>Task 2: ActionPanel secondary anchored list + render consumption</name>
  <files>src/ui/components/auto-ui/ActionPanel.vue</files>
  <read_first>
    - src/ui/components/auto-ui/ActionPanel.vue (import :29, call :255 consuming the helper)
    - src/ui/components/auto-ui/action-panel-helpers.ts (splitAnchoredChoices shape from Task 1)
    - src/ui/composables/useBoardInteraction.ts (triggerElementSelect)
  </read_first>
  <action>
    Update the import (:29) and call site (:255) to consume `splitAnchoredChoices`. Render the `primary` choices as today. Render the `anchored` choices as a secondary focusable `<ul role="list">` where each item is a `<button>` (real button, keyboard-operable, with the global focus ring) whose `@click` calls `boardInteraction.triggerElementSelect` on that choice's notation — same selection path as clicking the board element (parity). Label each button from the choice's display text + notation. The list stays visible whenever picks are pending so keyboard/SR users always have an operable control. Use `var(--bsg-*)` colors only.
  </action>
  <verify>
    <automated>grep -c "splitAnchoredChoices" src/ui/components/auto-ui/ActionPanel.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "splitAnchoredChoices" src/ui/components/auto-ui/ActionPanel.vue` returns >= 1
    - `grep -c "role=\"list\"" src/ui/components/auto-ui/ActionPanel.vue` returns >= 1
    - The anchored buttons call triggerElementSelect (grep finds it in the secondary-list handler)
    - `npm test` ActionPanel-related suites stay green
  </acceptance_criteria>
  <done>Anchored choices render as a focusable secondary <ul role=list> of buttons wired to triggerElementSelect.</done>
</task>

<task type="auto">
  <name>Task 3: Remove ActionPanel outline:none + 44px targets</name>
  <files>src/ui/components/auto-ui/ActionPanel.vue</files>
  <read_first>
    - src/ui/components/auto-ui/ActionPanel.vue (outline:none :1369; .cancel-btn :1179; .clear-selection-btn :1260)
    - .planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md A11Y-06 (lines 478-480), A11Y-09 targets (lines 560-563)
  </read_first>
  <action>
    DELETE the `outline: none` from the `.number-input input:focus, .text-input input:focus` rule (:1369) — keep the `border-color: var(--bsg-accent)`; the global :focus-visible ring (plan 05) now provides the focus indicator. Add `min-height: 44px` to `.cancel-btn` (:1179) and `min-height: 44px; min-width: 44px` to `.clear-selection-btn` (:1260). No hex literals.
  </action>
  <verify>
    <automated>grep -c "outline: none" src/ui/components/auto-ui/ActionPanel.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "outline: none" src/ui/components/auto-ui/ActionPanel.vue` returns 0
    - `.cancel-btn` block contains `min-height: 44px`
    - `.clear-selection-btn` block contains `min-height: 44px` and `min-width: 44px`
    - `npm run lint:css` exits 0
  </acceptance_criteria>
  <done>No outline:none in ActionPanel; cancel/clear controls meet the 44px floor.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/ui/components/auto-ui/action-panel-helpers.test.ts` green; `npm test` ActionPanel suites green.
- `grep -c "outline: none" src/ui/components/auto-ui/ActionPanel.vue` == 0; `grep -c "filterAnchoredChoices" src/ui/components/auto-ui/action-panel-helpers.ts` == 0.
- `npm run lint:css` exits 0.
</verification>

<success_criteria>
- Board-anchored choices are always reachable via a focusable secondary button list (Critical finding #2 genuinely closed in the panel).
- ActionPanel focus is visible; cancel/clear meet target floor.
</success_criteria>

<output>
Create `.planning/phases/101-accessibility-wcag-wave-4/101-06-SUMMARY.md` when done.
</output>
