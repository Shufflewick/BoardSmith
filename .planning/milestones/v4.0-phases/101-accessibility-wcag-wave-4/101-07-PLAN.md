---
phase: 101-accessibility-wcag-wave-4
plan: 07
type: execute
wave: 2
depends_on: [101-01]
files_modified:
  - src/ui/components/auto-ui/BoardLegend.vue
  - src/ui/components/auto-ui/AutoUI.vue
  - src/ui/components/PlayersPanel.vue
autonomous: true
requirements: [A11Y-04, A11Y-05, A11Y-08]

must_haves:
  truths:
    - "A legend names each interaction state (selectable/selected/drop-target/disabled/active) with a swatch AND a text label + non-color cue"
    - "Each player seat is distinguishable without color via a letter-holding shape, so the Sam/Sally initial collision is resolved by shape/ring variant"
    - "The active player's seat carries aria-current"
    - "Under prefers-reduced-motion the active-player breathe becomes a static high-contrast border instead of disappearing"
  artifacts:
    - path: "src/ui/components/auto-ui/BoardLegend.vue"
      provides: "legend listing the 5 interaction states with swatch + label"
      min_lines: 25
    - path: "src/ui/components/PlayersPanel.vue"
      provides: "shape-based seat identity + aria-current + reduced-motion static border"
      contains: "aria-current"
  key_links:
    - from: "src/ui/components/auto-ui/AutoUI.vue"
      to: "src/ui/components/auto-ui/BoardLegend.vue"
      via: "legend mount in board region"
      pattern: "BoardLegend"
---

<objective>
Add the non-color identity layer: a `BoardLegend` component (mirroring the mockup's legend) mounted in the board region, plus shape-based player seat identity and `aria-current` on the active seat in PlayersPanel, and the reduced-motion static-border replacement for the active-player breathe.

Purpose: A11Y-05 (non-color cues + legend) + A11Y-04 (aria-current) + A11Y-08 (reduced-motion static border for the active seat).
Output: New BoardLegend.vue mounted in AutoUI; PlayersPanel shape identity + aria-current + reduced-motion border.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md
@planning/mockups/boardsmith-chrome.html

<interfaces>
Anchors [VERIFIED 101-RESEARCH.md]:
  PlayersPanel.vue — role=list/listitem already present; active player listitem ~:127 (isPlayerActive); breathe animation :273 with @media reduced-motion {animation:none} at :282 (no static border replacement yet)
  AutoUI.vue — board region mount point for legend (absolute bottom-left per mockup)
Mockup: legend section lines 416-425; shape classes .sh-circle/.sh-square/.sh-hexagon (clip-path) lines for shape-based identity.
A11Y-08 active seat: add `@media (prefers-reduced-motion: reduce) { .player.active { border: 2px solid var(--bsg-accent); } }`.
All colors via var(--bsg-*) only (lint:css color-no-hex on .vue).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BoardLegend.vue and mount it in AutoUI</name>
  <files>src/ui/components/auto-ui/BoardLegend.vue, src/ui/components/auto-ui/AutoUI.vue</files>
  <read_first>
    - planning/mockups/boardsmith-chrome.html lines 416-425 (legend markup + swatches)
    - src/ui/components/auto-ui/AutoUI.vue (board region structure / mount point)
    - .planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md A11Y-05 (lines 466-474)
  </read_first>
  <action>
    Create `src/ui/components/auto-ui/BoardLegend.vue` as a small presentational component listing the five interaction states — selectable, selected, drop target, disabled, active player — each row showing the SAME non-color cue used on the board (dashed/solid/dotted border, ring/check icon, hatch for disabled) plus a text label, mirroring the mockup legend (:416-425). Use `var(--bsg-*)` tokens only (the hatch uses `repeating-linear-gradient` over `var(--bsg-cell)`). Mark it up as a labelled list/group so a screen reader can read it. Mount `<BoardLegend />` inside AutoUI's board region (absolute-positioned bottom-left per the mockup), without overlapping the action dock or turn zone.
  </action>
  <verify>
    <automated>grep -c "BoardLegend" src/ui/components/auto-ui/AutoUI.vue</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/components/auto-ui/BoardLegend.vue` exists and names all 5 states with a swatch + text label
    - `grep -c "BoardLegend" src/ui/components/auto-ui/AutoUI.vue` returns >= 1 (import + mount)
    - `grep -cE "#[0-9a-fA-F]{3,6}" src/ui/components/auto-ui/BoardLegend.vue` returns 0 (no hex)
    - `npm run lint:css` exits 0
  </acceptance_criteria>
  <done>BoardLegend names all 5 states with non-color cues + labels and is mounted in the board region.</done>
</task>

<task type="auto">
  <name>Task 2: PlayersPanel shape identity + aria-current + reduced-motion static border</name>
  <files>src/ui/components/PlayersPanel.vue</files>
  <read_first>
    - src/ui/components/PlayersPanel.vue (active listitem ~:127 isPlayerActive; breathe :273; @media reduced-motion :282)
    - planning/mockups/boardsmith-chrome.html (shape classes .sh-circle/.sh-square/.sh-hexagon clip-path identity)
    - .planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md A11Y-04 PlayersPanel (lines 461-462), A11Y-05 shapes (lines 472), A11Y-08 active seat (lines 539)
  </read_first>
  <action>
    Add a letter-holding SHAPE per seat (clip-path shapes per the mockup: circle/square/hexagon/diamond/triangle/pentagon for seats 1-6) so seats are distinguishable without color — this resolves the Sam/Sally same-initial collision via a distinct shape (or ring variant). Add `:aria-current="isPlayerActive ? 'true' : undefined"` to the active player's listitem (~:127). Extend the existing `@media (prefers-reduced-motion: reduce)` block (:282) so that in addition to `animation: none`, the active seat gets a static high-contrast border `border: 2px solid var(--bsg-accent);` (the turn cue must remain visible, not vanish, under reduced motion). Tokens only — no hex.
  </action>
  <verify>
    <automated>grep -c "aria-current" src/ui/components/PlayersPanel.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "aria-current" src/ui/components/PlayersPanel.vue` returns >= 1
    - `grep -c "clip-path" src/ui/components/PlayersPanel.vue` returns >= 1 (shape identity)
    - The reduced-motion block sets a static `border` on `.player.active` (not just animation:none)
    - `npm run lint:css` exits 0
  </acceptance_criteria>
  <done>Seats carry distinct shapes + aria-current on active; reduced-motion shows a static accent border on the active seat.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/ui/components/PlayersPanel*` (if any) and `npm test` stay green.
- `grep -c BoardLegend src/ui/components/auto-ui/AutoUI.vue` >= 1; `grep -c aria-current src/ui/components/PlayersPanel.vue` >= 1.
- `npm run lint:css` exits 0.
</verification>

<success_criteria>
- Legend present naming all 5 states with non-color cues + labels.
- Seats distinguishable by shape; active seat has aria-current and a static reduced-motion border.
</success_criteria>

<output>
Create `.planning/phases/101-accessibility-wcag-wave-4/101-07-SUMMARY.md` when done.
</output>
