---
phase: 102-material-dev-debug-wave-5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/components/DebugPanel.vue
  - src/ui/components/DebugPanel.shortcut.test.ts
  - src/ui/components/DebugPanel.tabs.test.ts
autonomous: true
requirements: [DEV-01]
must_haves:
  truths:
    - "Typing 'd' inside an input, textarea, select, or contenteditable element never opens the debug panel"
    - "Ctrl+D or Cmd+D toggles the debug panel and the browser bookmark dialog does not appear"
    - "Keyboard users can move between the 6 debug tabs with Arrow/Home/End keys"
    - "The DebugPanel monospace data uses the Slate --bsg-mono token, not hard-coded Monaco/Menlo"
    - "Restarting the game from the Controls tab requires two clicks, not a native window.confirm dialog"
  artifacts:
    - path: "src/ui/components/DebugPanel.vue"
      provides: "Slate-reskinned debug panel with ARIA tabs + guarded shortcut"
      contains: "role=\"tablist\""
    - path: "src/ui/components/DebugPanel.shortcut.test.ts"
      provides: "Coverage for the D-shortcut modifier + input-focus + contenteditable guards"
    - path: "src/ui/components/DebugPanel.tabs.test.ts"
      provides: "Coverage for tablist/tab/tabpanel ARIA roles and arrow-key nav"
  key_links:
    - from: "src/ui/components/DebugPanel.vue handleKeyDown"
      to: "togglePanel"
      via: "guarded keydown"
      pattern: "ctrlKey|metaKey"
    - from: "DebugPanel tab buttons"
      to: "tab panels"
      via: "aria-controls / aria-labelledby"
      pattern: "aria-controls"
---

<objective>
Bring `DebugPanel.vue` up to the Slate standard and fix its accessibility deficits (DEV-01): a real `<button aria-expanded>` toggle (already present — keep), a `D` shortcut gated behind Ctrl/Cmd AND an input/contenteditable guard, the ARIA tabs pattern on its 6 tabbed sections, Slate-token reskin (mono font, accent active indicator, flex height), a phone bottom-sheet dock, and a two-click in-panel restart confirm replacing the native `window.confirm`.

Purpose: DebugPanel is the dev's primary inspection surface; it must be keyboard-navigable, must not hijack typing, and must match the Slate chrome shipped in Phases 98-101.
Output: Reskinned, ARIA-correct DebugPanel + two new unit test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-material-dev-debug-wave-5/102-CONTEXT.md
@.planning/phases/102-material-dev-debug-wave-5/102-RESEARCH.md

<interfaces>
DebugPanel is mounted INSIDE the GameShell iframe (GameShell.vue:1704), so GameShell's
non-scoped :focus-visible ring and @media(prefers-reduced-motion) CSS already apply —
do NOT duplicate those rules here.

Slate tokens available (src/ui/theme.ts): --bsg-mono ('JetBrains Mono'…), --bsg-accent,
--bsg-accent-2, --bsg-surface, --bsg-surface-2, --bsg-ink, --bsg-ink-2, --bsg-ink-3,
--bsg-line, --bsg-line-2, --bsg-field, --bsg-r-sm, --bsg-danger.

Current shortcut handler: DebugPanel.vue handleKeyDown lines 317-326 (guards only
HTMLInputElement/HTMLTextAreaElement, fires on bare 'd').
Current tab markup: lines 1207-1245 — 6 plain <button>s, no ARIA. Tab ids in order:
state, elements, decks, actions, history, controls. activeTab ref drives v-if panels.
Current restart confirm: restartGame() lines 443-447 uses confirm(); emits 'restart-game'.
Monospace literals to replace: lines 2164, 2175 (and any others in DebugPanel scoped
style only — 'Monaco', 'Menlo', monospace → var(--bsg-mono)).
Height constraint to remove: .state-display max-height: calc(100vh - 280px) (line 2157).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard the D shortcut and apply the ARIA tabs pattern</name>
  <files>src/ui/components/DebugPanel.vue, src/ui/components/DebugPanel.shortcut.test.ts, src/ui/components/DebugPanel.tabs.test.ts</files>
  <read_first>
    - src/ui/components/DebugPanel.vue (handleKeyDown lines 317-326; togglePanel lines 311-314; tab markup lines 1207-1245; tab panel v-if blocks immediately after; hint text line 1198; Controls-tab kbd label around lines 1941-1943)
    - src/ui/components/Toast.a11y.test.ts (reference for @vue/test-utils mount + jsdom setup pattern used in this repo)
  </read_first>
  <behavior>
    - Test (shortcut): a bare 'd' keydown with no modifier does NOT call togglePanel (panel stays closed).
    - Test (shortcut): a 'd' keydown while e.target is an HTMLInputElement / HTMLTextAreaElement / HTMLSelectElement does NOT toggle.
    - Test (shortcut): a 'd' keydown while e.target.isContentEditable is true does NOT toggle.
    - Test (shortcut): Ctrl+D (ctrlKey) and Cmd+D (metaKey) DO toggle the panel and call preventDefault on the event.
    - Test (tabs): the tab container has role="tablist"; each of the 6 tab buttons has role="tab", an id, aria-controls, and aria-selected reflecting activeTab; the active tab has tabindex 0 and inactive tabs tabindex -1.
    - Test (tabs): each tab panel has role="tabpanel", an id, and aria-labelledby pointing at its tab.
    - Test (tabs): firing ArrowRight on the active tab advances activeTab to the next tab id (wrapping from controls to state); Home selects state, End selects controls.
  </behavior>
  <action>
    Update handleKeyDown so it returns early when e.target is an HTMLInputElement, HTMLTextAreaElement, or HTMLSelectElement, and also returns early when (e.target as HTMLElement).isContentEditable is true. Only toggle when (e.ctrlKey || e.metaKey) and the key is 'd' or 'D'; in that branch call e.preventDefault() first (to suppress the browser Add-Bookmark action) then togglePanel(). Update the hint text at line 1198 from "(Press D to toggle)" to "(Ctrl/Cmd+D to toggle)" and update the Controls-tab kbd label (around 1941-1943) to show Ctrl/Cmd+D.

    Convert the 6-button tab strip to the ARIA tabs pattern. Define a tabs descriptor array (id + label, in order state, elements, decks, actions, history, controls) and render with v-for. Give the container role="tablist" and aria-label="Debug sections". Each button gets role="tab", :id="`debug-tab-${tab.id}`", :aria-selected="activeTab === tab.id", :aria-controls="`debug-panel-${tab.id}`", :tabindex="activeTab === tab.id ? 0 : -1", @click selecting the tab, and @keydown="handleTabKeydown". Preserve the existing decks badge. Add :id and role="tabpanel" + :aria-labelledby="`debug-tab-${tab.id}`" to each of the 6 tab-content panel divs (keep their existing v-if="activeTab === '…'"). Add handleTabKeydown that handles ArrowLeft/ArrowRight (wrap), Home, End: it preventDefaults, computes the next tab id from the ordered id list, sets activeTab, and on nextTick focuses document.getElementById(`debug-tab-${activeTab}`). Per pitfall #2, inactive tabs use tabindex -1 so Tab key exits the tablist.

    Create DebugPanel.shortcut.test.ts and DebugPanel.tabs.test.ts using @vitest-environment jsdom and @vue/test-utils mount, asserting the behaviors above. Mount with the props DebugPanel requires (state, playerSeat, playerCount, gameId — see defineProps); a minimal state object is sufficient.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/DebugPanel.shortcut.test.ts src/ui/components/DebugPanel.tabs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both new test files pass.
    - Source assertion: `grep -n 'ctrlKey' src/ui/components/DebugPanel.vue` and `grep -n 'isContentEditable' src/ui/components/DebugPanel.vue` both return a hit inside handleKeyDown.
    - Source assertion: `grep -c 'role="tab"' src/ui/components/DebugPanel.vue` is at least 1 and `role="tablist"` and `role="tabpanel"` both present.
  </acceptance_criteria>
  <done>Bare 'd' and in-field 'd' never toggle; Ctrl/Cmd+D toggles with preventDefault; all 6 tabs carry ARIA roles with working arrow-key nav; both test files green.</done>
</task>

<task type="auto">
  <name>Task 2: Slate reskin, flex height, phone bottom-sheet, two-click restart</name>
  <files>src/ui/components/DebugPanel.vue</files>
  <read_first>
    - src/ui/components/DebugPanel.vue (scoped style: monospace literals lines 2164/2175 and any other 'Monaco','Menlo' in the DebugPanel scoped block; .state-display max-height line 2157; tab CSS lines 2072-2099; .debug-drawer / .debug-content / .tab-content layout rules; restartGame lines 443-447 and its template button in the Controls tab)
    - src/ui/theme.ts lines 100-160 (token names: --bsg-mono, --bsg-accent, surfaces, radii)
  </read_first>
  <action>
    Replace every `'Monaco', 'Menlo', monospace` font-family in the DebugPanel scoped style with `var(--bsg-mono)`. Remove the `.state-display { max-height: calc(100vh - 280px) }` constraint (line 2157) and instead give the scrolling container its height from flex: ensure `.debug-content` and `.tab-content` (and `.state-display`) use `min-height: 0` and `overflow: auto` so the panel scrolls within the drawer without the viewport-calc hack. Add an active-tab indicator: the `role="tab"` button with aria-selected true gets `border-bottom: 2px solid var(--bsg-accent)` (replace any existing active color rule that relied on a different treatment); ensure inactive tabs use --bsg-ink-3 and active uses --bsg-ink. Do not introduce any raw hex — `npm run lint:css` must stay green.

    Add a phone bottom-sheet dock: inside the DebugPanel scoped style add `@media (max-width: 639px)` that re-docks `.debug-drawer.open` to `left: 0; right: 0; bottom: 0; top: auto; width: 100%; height: 60dvh; border-radius: var(--bsg-r-sm) var(--bsg-r-sm) 0 0` (overriding the right-edge drawer geometry) so on phones it slides up from the bottom instead of in from the side.

    Replace the native confirm in restartGame() with a two-click in-panel confirm. Add a `restartConfirming = ref(false)` and a timer ref; the Controls-tab restart button calls a handler that, on first click, sets restartConfirming true and starts a 5s timeout that resets it, and on second click clears the timer, resets the flag, and emits 'restart-game'. The button label reads "Restart game" normally and "Confirm restart?" while restartConfirming; style it as a quiet/danger-outline control (use --bsg-danger for text/border on the confirming state, no accent fill). Remove the `confirm(...)` call entirely. Leave the unrelated rewindToAction() native confirm untouched (out of scope per research).
  </action>
  <verify>
    <automated>npm run lint:css && npx vitest run src/ui/components/DebugPanel.shortcut.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint:css` exits 0 (empty ignore list, no new hex).
    - Source assertion: `grep -n "Monaco" src/ui/components/DebugPanel.vue` returns nothing inside the scoped style for the panel data blocks (replaced by var(--bsg-mono)).
    - Source assertion: `grep -n "calc(100vh - 280px)" src/ui/components/DebugPanel.vue` no longer returns the .state-display rule (line ~2157 removed); note the unrelated duplicate at ~2517 is a different selector and may remain only if it is not the debug state-display.
    - Source assertion: `grep -n "restartConfirming" src/ui/components/DebugPanel.vue` returns hits and `confirm(` is gone from restartGame.
  </acceptance_criteria>
  <done>Panel uses --bsg-mono, accent-underlined active tab, flex-driven scroll height, a phone bottom sheet, and a two-click restart confirm; lint:css green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard → DebugPanel | global keydown listener; only a guarded Ctrl/Cmd+D should act |
| dev → game restart | restart discards all game progress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Denial of Service | DebugPanel global keydown | mitigate | Guard so typing 'd' in inputs/contenteditable never opens the panel; require Ctrl/Cmd modifier |
| T-102-02 | Tampering | restartGame() | mitigate | Two-click confirm replaces native dialog; no single-click data loss |
| T-102-03 | Information Disclosure | DebugPanel render | accept | Panel is gated by debugMode && platformMode && isDevBuild in GameShell; never ships in production embeds |
</threat_model>

<verification>
- `npx vitest run src/ui/components/DebugPanel.shortcut.test.ts src/ui/components/DebugPanel.tabs.test.ts` green.
- `npm run lint:css` green.
- Full suite unaffected: `npm run test` stays at baseline + 2 new files.
</verification>

<success_criteria>
DEV-01 satisfied: guarded shortcut, ARIA tabs, Slate reskin, phone bottom sheet, two-click restart; lint:css green; new tests green.
</success_criteria>

<output>
Create `.planning/phases/102-material-dev-debug-wave-5/102-01-SUMMARY.md` when done
</output>
