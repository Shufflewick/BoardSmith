---
phase: 102-material-dev-debug-wave-5
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/dev-host/DevHost.vue
  - src/cli/dev-host/DevHost.seats.test.ts
autonomous: true
requirements: [DEV-02, DEV-03, DEV-04]
must_haves:
  truths:
    - "Once the dev holds a seat the chrome bar is collapsed to a slim pull-tab by default"
    - "Collapsed/expanded chrome state survives a page reload (localStorage)"
    - "The seat badge is a working switcher: choosing another seat leaves the current seat and takes the target"
    - "A presence strip shows every seat with its name, connected/away dot, and an AI marker"
    - "A tucked-away Table setup panel displays aiSeats, aiLevel, playerCount, gameOptions, and playerOptions read-only"
    - "Below 640px the chrome controls collapse to icons with a … overflow"
  artifacts:
    - path: "src/cli/dev-host/DevHost.vue"
      provides: "Collapsible chrome + seat switcher + presence strip + table setup"
      contains: "boardsmith:dev-chrome-open"
    - path: "src/cli/dev-host/DevHost.seats.test.ts"
      provides: "Coverage for presence strip + seat switcher"
  key_links:
    - from: "DevHost chromeOpen ref"
      to: "localStorage"
      via: "load on mount / persist on toggle"
      pattern: "boardsmith:dev-chrome-open"
    - from: "seat switcher click"
      to: "leaveSeat + takeSeat"
      via: "WS leave then join"
      pattern: "takeSeat"
    - from: "Table setup panel"
      to: "cfg.aiSeats/aiLevel/playerCount/gameOptions/playerOptions"
      via: "read-only render"
      pattern: "cfg.gameOptions"
---

<objective>
Restore the dev's god-mode capability and tidy the dev-server chrome (DEV-02/03/04) — all in `DevHost.vue`: auto-collapse the in-game bar to a slim, localStorage-persisted pull-tab (default-collapsed once seated, icon-only below 640px), turn the dead static seat badge into a working seat switcher, add a presence strip (connected / AI / away per seat), and add a tucked-away read-only "Table setup" panel that finally surfaces the injected config the host already ships.

Purpose: The DevHost chrome was recolored in Phase 99 but its UX deficits remain — dead `leaveSeat()` code, a static badge, and config data plumbed to the browser but rendered nowhere.
Output: A collapsible, seat-switching, presence-aware DevHost with a Table setup panel + one new test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-material-dev-debug-wave-5/102-CONTEXT.md
@.planning/phases/102-material-dev-debug-wave-5/102-RESEARCH.md

<interfaces>
DevHost.vue is at src/cli/dev-host/ — OUTSIDE src/ui/**, so it is NOT covered by
`npm run lint:css`. Use --bsg-* tokens anyway for theme consistency.

Existing state (DevHost.vue): seats ref<SeatInfo[]> (line 43) populated by WS 'lobby';
SeatInfo = { seat:number, clientId:string|null, name:string, color?:string, connected:boolean }.
mySeat ref<number|null> (44). followActive ref (46). takeSeat(seat) (192-195) sends
{type:'join', seat, name, color}. leaveSeat() (196-199) sends {type:'leave'} and nulls
mySeat — currently UNREFERENCED in the in-game template (dead). seatLabel(seat) (210-213).
canTake(seat) (56-58).

cfg: DevHostConfig (config-types.ts) — has gameType, displayName, minPlayers, maxPlayers,
playerCount, aiSeats:number[] (1-indexed), aiLevel:string, gameOptions:DevOptionDef[],
playerOptions:DevOptionDef[], colorPalette, gameUrl. DevHost currently references only
displayName, colorPalette, gameUrl, gameType. AI seat detection: a seat n is AI when
cfg.aiSeats includes n (and/or seat has no clientId).

DevOptionDef = { id, type, label?, default?, choices?, min?, max?, [key]:unknown }.

In-game chrome template: lines 308-339 (<header class="dev-chrome">). Duplicate game
title to DROP: line 312 <strong>{{ cfg.displayName }}</strong> (the iframe GameShell
already renders the game title). Seat badge to replace: line 313.

GameShell's focus-visible/reduced-motion CSS does NOT reach the DevHost outer page
(pitfall #6) — add a local :focus-visible rule in DevHost scoped style for new controls.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapsible chrome with localStorage pull-tab + icon-only phone layout</name>
  <files>src/cli/dev-host/DevHost.vue</files>
  <read_first>
    - src/cli/dev-host/DevHost.vue (script: refs around lines 42-53, onMounted 215-218; template in-game chrome 308-339; scoped style .dev-chrome 519-606)
  </read_first>
  <action>
    Add `const chromeOpen = ref(true)` plus a CHROME_KEY = 'boardsmith:dev-chrome-open'. On mount, read CHROME_KEY from localStorage and set chromeOpen accordingly (string 'false' → false). Add a `toggleChrome()` that flips chromeOpen and persists it to localStorage. Add a watcher (or logic in the existing mySeat/init handling) so that when mySeat transitions from null to a number for the first time AND there is no stored preference, chromeOpen defaults to false (collapsed once seated) — but a stored explicit preference always wins (pit of success: respect the user's last choice).

    In the in-game template, wrap the existing bar contents (the dev-chrome__bar div) in `v-show="chromeOpen"`. Add a slim pull-tab trigger element (a real <button> with aria-expanded bound to chromeOpen and an aria-label like "Toggle dev chrome") that is always visible; clicking it calls toggleChrome(). When collapsed, only the pull-tab shows. Delete the duplicate game title at line 312 (cfg.displayName) — the iframe already renders it; keep the seat badge slot for Task 2.

    Add `@media (max-width: 639px)` scoped CSS that switches the bar-actions controls to icon-only (hide the text labels, keep accessible names via aria-label/title) and moves secondary controls (e.g. the UI switcher + follow toggle) into a `…` overflow using a native <details>/<summary> disclosure. Add a local `:focus-visible` ring rule in DevHost scoped style (box-shadow using --bsg-bg + --bsg-accent, matching the Phase-101 ring) since the outer page does not inherit GameShell's ring.
  </action>
  <verify>
    <automated>npx vitest run src/cli/dev-host/DevHost.seats.test.ts 2>/dev/null; npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -i devhost || echo "no devhost type errors"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "boardsmith:dev-chrome-open" src/cli/dev-host/DevHost.vue` returns a hit; `grep -n "chromeOpen" src/cli/dev-host/DevHost.vue` shows it bound in template (v-show + pull-tab aria-expanded).
    - Source assertion: the line-312 `<strong>{{ cfg.displayName }}</strong>` inside dev-chrome is gone.
    - Source assertion: `grep -n "max-width: 639px" src/cli/dev-host/DevHost.vue` returns a hit.
    - DevHost still type-checks (no new vue-tsc errors referencing DevHost).
  </acceptance_criteria>
  <done>Chrome collapses to a persisted pull-tab, default-collapsed once seated, icon-only with a … overflow under 640px, duplicate title removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seat switcher + presence strip</name>
  <files>src/cli/dev-host/DevHost.vue, src/cli/dev-host/DevHost.seats.test.ts</files>
  <read_first>
    - src/cli/dev-host/DevHost.vue (takeSeat 192-195, leaveSeat 196-199, seatLabel 210-213, canTake 56-58, seats/mySeat refs, badge line 313, followActive toggle 326-334)
    - src/cli/dev-host/multiplayer-host.test.ts (reference for WS-message-shape testing patterns in this dir; the new test will mock wsSend, not a live socket)
  </read_first>
  <behavior>
    - Test: given a seats array, the presence strip renders one pill per seat showing the seat name (or "Seat N" when open), a connected/away status, and an AI marker for seats in cfg.aiSeats.
    - Test: invoking the switch-seat handler for a target seat results in a 'leave' WS message followed by a 'join' message for the target seat (assert on a mocked wsSend call order).
    - Test: switching seats while followActive is true disables follow (emits {type:'follow', enabled:false}) OR is blocked — assert the chosen rule (auto-disable follow on manual switch).
  </behavior>
  <action>
    Replace the static seat badge (line 313) with a real seat-switcher: a <button> (aria-haspopup, aria-expanded) showing "Seat {{ mySeat }}" that toggles a small menu/popover listing all seats. Each menu item is a <button> for a target seat; clicking it calls a new `switchSeat(target)` that, when target !== mySeat, calls leaveSeat() then takeSeat(target). Per the follow-mode integration: if followActive is true when the user manually switches, first send toggleFollow() to disable follow (auto-disable on manual switch — document this in a comment). Use the existing canTake()/seatLabel() helpers; do not let the user switch into a seat actively held by another connected client.

    Add a presence strip: render `seats` as a row of pills, each showing seatLabel(seat), a status dot (--bsg-ok when connected, --bsg-away when not), and an "AI" badge when cfg.aiSeats.includes(seat.seat) (or seat.clientId is null). Reuse the existing seat-card dot color tokens. Place the strip in the chrome bar (visible when chromeOpen).

    Create DevHost.seats.test.ts (@vitest-environment jsdom, @vue/test-utils mount) that mounts DevHost with a config prop fixture (include aiSeats) and a mocked WebSocket / spied wsSend so the message-order assertions in <behavior> work. If wsSend is module-internal, drive it by stubbing the global WebSocket so send() is observable; assert the serialized JSON frames.
  </action>
  <verify>
    <automated>npx vitest run src/cli/dev-host/DevHost.seats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New test file passes.
    - Source assertion: `grep -n "switchSeat" src/cli/dev-host/DevHost.vue` returns hits and the handler calls both leaveSeat and takeSeat.
    - Source assertion: the presence strip references cfg.aiSeats for the AI marker.
    - leaveSeat() is no longer dead code (referenced from switchSeat).
  </acceptance_criteria>
  <done>The badge is a working switcher (leave→take, follow auto-disabled on manual switch); a presence strip shows connected/AI/away per seat; test green.</done>
</task>

<task type="auto">
  <name>Task 3: Read-only Table setup panel</name>
  <files>src/cli/dev-host/DevHost.vue</files>
  <read_first>
    - src/cli/dev-host/DevHost.vue (template in-game chrome 308-339; scoped style)
    - src/cli/dev-host/config-types.ts (DevHostConfig fields + DevOptionDef shape)
  </read_first>
  <action>
    Add a `tableSetupOpen = ref(false)` and a tucked-away "Table setup" disclosure in the chrome (a <button>/<details> labeled "Table setup", tucked into the … overflow or a secondary row so it does not crowd the bar). When open, render a read-only summary using a <dl> structure: Players = cfg.playerCount; AI seats = cfg.aiSeats joined (e.g. "2, 3" or "none"); AI level = cfg.aiLevel; then a Game options group listing each cfg.gameOptions entry as `label (or id): default`; and a Player options group listing each cfg.playerOptions entry the same way. For options with choices, show the default's matching choice label when available, else the raw default. This is display-only — no inputs, no editing (DEV-04 scope: read-only). Use --bsg-ink-3 for labels, --bsg-ink for values, --bsg-surface/--bsg-line for the panel.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -i devhost || echo "no devhost type errors"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "cfg.gameOptions" src/cli/dev-host/DevHost.vue` and `grep -n "cfg.playerOptions" src/cli/dev-host/DevHost.vue` and `grep -n "cfg.aiSeats" src/cli/dev-host/DevHost.vue` and `grep -n "cfg.aiLevel" src/cli/dev-host/DevHost.vue` and `grep -n "cfg.playerCount" src/cli/dev-host/DevHost.vue` all return hits in template.
    - The panel contains no <input>/<select> for these fields (read-only).
    - DevHost type-checks clean.
  </acceptance_criteria>
  <done>A tucked-away Table setup panel surfaces aiSeats/aiLevel/playerCount/gameOptions/playerOptions read-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DevHost → Node host (WS) | seat join/leave/follow frames cross to the authoritative host |
| dev → localStorage | chrome-open preference persisted client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-06 | Tampering | switchSeat WS frames | accept | Host (multiplayer-host.ts) remains authoritative — rejects taking a seat held by a connected client; UI uses canTake() as a courtesy guard only |
| T-102-07 | Information Disclosure | Table setup panel | accept | Surfaces only config the dev already passed in; dev-only chrome, never shipped to players |
</threat_model>

<verification>
- `npx vitest run src/cli/dev-host/DevHost.seats.test.ts` green.
- DevHost type-checks clean (vue-tsc).
- `npm run test` stays at baseline + 1 new file.
</verification>

<success_criteria>
DEV-02/03/04 satisfied: persisted collapsible pull-tab chrome (icon-only <640px), working seat switcher, presence strip, read-only Table setup panel.
</success_criteria>

<output>
Create `.planning/phases/102-material-dev-debug-wave-5/102-03-SUMMARY.md` when done
</output>
