---
phase: 102-material-dev-debug-wave-5
plan: 05
type: execute
wave: 2
depends_on: ["102-03"]
files_modified:
  - src/cli/dev-host/DevHost.vue
  - src/cli/dev-host/DevHost.restart.test.ts
  - src/cli/dev-host/host.html
autonomous: true
requirements: [DEV-07, DEV-08]
must_haves:
  truths:
    - "Clicking the dev New game button once shows a confirm state and does NOT restart"
    - "A second click within the window restarts (broadcasts restart to all seats)"
    - "After the restart's game_state arrives, a broadcast toast tells all seats a restart happened"
    - "The New game button uses neutral/ghost styling, not an accent CTA fill"
    - "The dev/standalone chrome shows a low-opacity SVG noise + screen vignette instead of a bare flat background, with no background-attachment:fixed"
  artifacts:
    - path: "src/cli/dev-host/DevHost.vue"
      provides: "Two-click restart confirm + broadcast toast + material layer"
      contains: "pendingRestart"
    - path: "src/cli/dev-host/DevHost.restart.test.ts"
      provides: "Coverage for two-click confirm + broadcast toast"
  key_links:
    - from: "New game button"
      to: "newGame() restart WS"
      via: "two-click handler"
      pattern: "restartConfirming"
    - from: "game_state after restart"
      to: "toast.info"
      via: "pendingRestart flag"
      pattern: "pendingRestart"
    - from: "dev-grain / dev-vignette layers"
      to: ".dev-host"
      via: "fixed decorative children"
      pattern: "dev-grain"
---

<objective>
Honor `always_confirm_destructive` for the dev "New game" action and apply the Slate material layer (DEV-07/08), both in `DevHost.vue` (+ `host.html`): a two-click confirm with neutral styling and a broadcast toast so every seat learns a restart happened, plus a low-opacity SVG noise + screen vignette decorative layer that replaces the bare flat background in the dev/standalone chrome only.

Purpose: A one-click "New game" silently wipes all seats' progress; and the flat dark background reads as TV static at scale. Both are the last polish items before the cross-repo verification gate.
Output: Guarded restart with broadcast toast + Slate material layer + one new test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-material-dev-debug-wave-5/102-CONTEXT.md
@.planning/phases/102-material-dev-debug-wave-5/102-RESEARCH.md
@.planning/phases/102-material-dev-debug-wave-5/102-03-SUMMARY.md

<interfaces>
DevHost.vue is OUTSIDE src/ui/** — not covered by lint:css. Use --bsg-* tokens anyway.

newGame() lines 200-202: wsSend({ type:'restart' }) — immediate, no confirm. Triggered by
the New game button at line 335 (class btn btn--start, already ghost-ish: transparent bg,
--bsg-ink-2 text). onHostMessage 'game_state' case lines 119-127 (where restart's fresh
state arrives for all clients).

Toast reuse (Phase 101, do NOT reinvent): Toast.vue at src/ui/components/Toast.vue
(<Teleport to="body">, role=status + dismiss); useToast at src/ui/composables/useToast.ts
exposing { toasts, show, remove, success, error, info, warning }. PITFALL #4: Toast.vue must
be added to DevHost's template or toasts never render (DevHost is the outer page, not inside
GameShell). Import paths from src/cli/dev-host/: '../../ui/components/Toast.vue' and
'../../ui/composables/useToast.js'.

Broadcast-toast mechanic (research-recommended, no host change): add pendingRestart ref;
newGame() sets it true before sending restart; in onHostMessage 'game_state', if
pendingRestart is true, clear it and toast.info('Game restarted'). Avoid touching
multiplayer-host.ts (keeps scope + test surface small — Open Question #3).

host.html (src/cli/dev-host/host.html): currently minimal (no background styling). DevHost
.dev-host currently `background: var(--bsg-bg)` (line 360).

Material layer (research §DEV-08 + Code Examples): two fixed pointer-events:none children of
.dev-host — .dev-grain (opacity ~0.07, SVG feTurbulence fractalNoise data URI, URL-encoded
per pitfall #7) and .dev-vignette (radial-gradient transparent center → color-mix(in srgb,
var(--bsg-bg) 70%, transparent) edge). Neutral/graphite, subtle. Do NOT add
background-attachment:fixed. No raw hex (use color-mix with --bsg-bg, not rgba(0,0,0)).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Two-click restart confirm + broadcast toast</name>
  <files>src/cli/dev-host/DevHost.vue, src/cli/dev-host/DevHost.restart.test.ts</files>
  <read_first>
    - src/cli/dev-host/DevHost.vue (newGame 200-202, New game button 335, onHostMessage game_state 119-127, btn--start style 497-511, onMounted/onUnmounted)
    - src/ui/composables/useToast.ts (full API)
    - src/ui/components/Toast.vue (Teleport + how it consumes useToast)
  </read_first>
  <behavior>
    - Test (two-click): a single click on New game sets restartConfirming true and does NOT send a restart WS frame; a second click within the window sends { type:'restart' } and resets restartConfirming.
    - Test (auto-cancel): after a single click, advancing fake timers past the confirm window resets restartConfirming to false without sending restart.
    - Test (broadcast toast): with pendingRestart set (post-restart), receiving an onHostMessage 'game_state' frame calls toast.info (assert the useToast show/info spy fired) and clears pendingRestart.
  </behavior>
  <action>
    Import useToast and Toast.vue; call `const toast = useToast()` in setup and add `<Toast />` to the DevHost template (pitfall #4 — required for rendering). Add `const restartConfirming = ref(false)` and a `restartConfirmTimer` plus `const pendingRestart = ref(false)`.

    Add `handleNewGameClick()`: if !restartConfirming, set it true and start a ~5s setTimeout that resets it; else clear the timer, reset restartConfirming, and call newGame(). Bind the New game button to handleNewGameClick; its label reads "New game" normally and "Confirm restart?" while restartConfirming. Update newGame() to set pendingRestart.value = true before wsSend({ type:'restart' }).

    In onHostMessage 'game_state', if pendingRestart.value is true, set it false and call toast.info('Game restarted'). Ensure the confirm timer is cleared on unmount.

    Confirm the button styling stays neutral/ghost (btn--start: transparent bg, --bsg-ink-2 text, no accent fill); add a subtle danger-tinted border only while restartConfirming if desired, but never an accent CTA fill.

    Create DevHost.restart.test.ts (@vitest-environment jsdom, @vue/test-utils mount, vi.useFakeTimers) covering the three behaviors; stub the WebSocket so wsSend frames are observable and spy on useToast.
  </action>
  <verify>
    <automated>npx vitest run src/cli/dev-host/DevHost.restart.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New test passes (single click sends nothing; second click sends restart; game_state fires toast).
    - Source assertion: `grep -n "restartConfirming" src/cli/dev-host/DevHost.vue` and `grep -n "pendingRestart" src/cli/dev-host/DevHost.vue` and `grep -n "useToast" src/cli/dev-host/DevHost.vue` all return hits; `<Toast` is present in the template.
    - The New game button has no accent fill (btn--start remains ghost).
  </acceptance_criteria>
  <done>New game requires two clicks (with auto-cancel), restart broadcasts a toast to all seats, and the button stays neutral; test green.</done>
</task>

<task type="auto">
  <name>Task 2: Slate material layer (SVG noise + vignette)</name>
  <files>src/cli/dev-host/DevHost.vue, src/cli/dev-host/host.html</files>
  <read_first>
    - src/cli/dev-host/DevHost.vue (.dev-host root template line 229 + scoped style .dev-host 355-363)
    - src/cli/dev-host/host.html (full file)
  </read_first>
  <action>
    Add two fixed decorative layers as direct children at the top of the .dev-host template (before the existing conditional content): a `<div class="dev-grain" aria-hidden="true">` and a `<div class="dev-vignette" aria-hidden="true">`. Both must be position:fixed; inset:0; pointer-events:none; low z-index (0 and 1) so they sit behind all chrome/content (give the real content a higher stacking context if needed, e.g. position:relative; z-index:2 on the chrome/stage wrappers).

    .dev-grain: opacity ~0.07; background-image a URL-encoded SVG feTurbulence fractalNoise data URI (encode # as %23, < as %3C, > as %3E, % as %25 per pitfall #7). Neutral/graphite — the turbulence itself is grayscale, do not tint it warm. .dev-vignette: a radial-gradient from transparent (~35% center) to color-mix(in srgb, var(--bsg-bg) 70%, transparent) at the edge — no rgba/hex literal. Do NOT set background-attachment:fixed anywhere. Keep it subtle — the board (iframe) stays the hero.

    In host.html, leave the background to DevHost (the layers live in DevHost); only ensure the existing reset (html,body,#app height:100%) does not clip the fixed layers. Add a local @media (prefers-reduced-motion) is not needed (the grain is static), but ensure no animation is introduced.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -i devhost || echo "no devhost type errors"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "dev-grain" src/cli/dev-host/DevHost.vue` and `grep -n "dev-vignette" src/cli/dev-host/DevHost.vue` return hits in both template and style.
    - Source assertion: `grep -n "feTurbulence" src/cli/dev-host/DevHost.vue` returns a hit; `grep -n "background-attachment: fixed" src/cli/dev-host/DevHost.vue` returns nothing.
    - Source assertion: the vignette uses color-mix with --bsg-bg (no rgba(0,0,0)/hex).
    - DevHost type-checks clean.
  </acceptance_criteria>
  <done>The dev/standalone chrome shows a subtle neutral SVG noise + vignette behind the board, token-clean, with no background-attachment:fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → all seats (WS restart) | restart wipes the game for every connected player |
| SVG data URI → CSS | embedded SVG rendered as a background image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-10 | Tampering | New game restart | mitigate | Two-click confirm with auto-cancel prevents accidental all-seat wipe; broadcast toast makes the action visible to every seat |
| T-102-11 | Tampering | SVG feTurbulence data URI | accept | Static, hard-coded grayscale noise; no user/network input flows into the data URI |
</threat_model>

<verification>
- `npx vitest run src/cli/dev-host/DevHost.restart.test.ts` green.
- DevHost type-checks clean.
- `npm run test` stays at baseline + 1 new file.
</verification>

<success_criteria>
DEV-07/08 satisfied: two-click neutral restart confirm with broadcast toast, and a subtle token-clean Slate material layer in dev/standalone chrome with no background-attachment:fixed.
</success_criteria>

<output>
Create `.planning/phases/102-material-dev-debug-wave-5/102-05-SUMMARY.md` when done
</output>
