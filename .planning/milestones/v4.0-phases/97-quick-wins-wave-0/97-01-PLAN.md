---
phase: 97-quick-wins-wave-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/components/GameShell.vue
  - src/ui/components/auto-ui/ActionPanel.vue
  - src/ui/components/HamburgerMenu.vue
  - src/ui/components/GameHeader.vue
  - src/cli/dev-host/host.html
  - src/cli/lib/project-scaffold.ts
  - src/ui/utils/notifyError.ts
  - src/ui/components/HamburgerMenu.test.ts
  - src/ui/components/auto-ui/ActionPanel.test.ts
autonomous: true
requirements: [QUICK-01, QUICK-02, QUICK-03, QUICK-04, QUICK-05]

must_haves:
  truths:
    - "A rejected move or failed action surfaces a visible toast with the actionable result.error string (no alert(), no silent console.error swallow)"
    - "After a failed action the action panel re-enables so the player can retry"
    - "A screen reader announces the hamburger and every X close/cancel/clear control by name"
    - "The hamburger button reports expanded/collapsed state and points to the drawer element"
    - "No dead Settings/Help menu items and no engine branding (BS chip, BoardSmith Dev Mode) appear in player-facing chrome"
    - "The mobile shell uses 100dvh with a 100vh fallback and no fixed/sticky chrome edge falls under the notch or home indicator"
  artifacts:
    - path: "src/ui/components/GameShell.vue"
      provides: "toast.error replacing alert() and alongside player-facing console.error; 100dvh fallbacks; safe-area insets on sticky action bar"
      contains: "100dvh"
    - path: "src/ui/components/auto-ui/ActionPanel.vue"
      provides: "useToast import + toast.error on rejected fill/execute; aria-label on cancel and clear-selection buttons"
      contains: "useToast"
    - path: "src/ui/components/HamburgerMenu.vue"
      provides: "aria-label/aria-expanded/aria-controls on hamburger; aria-label on close; defaultItems reduced to new-game+leave; BS chip and drawer-footer removed; 100dvh fallback"
      contains: "aria-controls"
    - path: "src/ui/components/GameHeader.vue"
      provides: "safe-area-inset padding on the topmost header"
      contains: "safe-area-inset-top"
    - path: "src/cli/dev-host/host.html"
      provides: "viewport-fit=cover on the dev-host viewport meta"
      contains: "viewport-fit=cover"
    - path: "src/cli/lib/project-scaffold.ts"
      provides: "viewport-fit=cover in the generateIndexHtml() viewport meta"
      contains: "viewport-fit=cover"
    - path: "src/ui/components/HamburgerMenu.test.ts"
      provides: "Component tests for hamburger/close aria attributes and aria-expanded state"
      min_lines: 30
  key_links:
    - from: "src/ui/components/auto-ui/ActionPanel.vue"
      to: "src/ui/composables/useToast.ts"
      via: "import { useToast } from '../../composables/useToast'"
      pattern: "useToast"
    - from: "src/ui/components/HamburgerMenu.vue hamburger button"
      to: "menu-drawer element id"
      via: "aria-controls matches id=hamburger-menu-drawer"
      pattern: "hamburger-menu-drawer"
    - from: "env(safe-area-inset-*) in scoped CSS"
      to: "HTML viewport meta"
      via: "viewport-fit=cover enables non-zero inset values"
      pattern: "viewport-fit=cover"
---

<objective>
Land the five isolated, token-free quick wins from the redesign audit (Wave 0): visible error feedback, accessible names on icon-only controls, dead-weight removal, and mobile viewport (dvh + safe-area) fixes.

Purpose: Bank credibility and de-risk later waves with surgical, low-risk fixes that need no part of the Slate token system.
Output: Four edited Vue SFCs, two edited HTML/scaffold files, one optional helper, and component tests covering the testable a11y/error behaviors.

Hard constraints (from CLAUDE.md + planning context):
- This repo ONLY. Do NOT touch ShufflewickPub host files (GameFrame.vue, [sessionId].vue, host main.css, nuxt config).
- NO `--bsg-*` tokens (Phase 98). NO dialog focus-trap/Escape (Phase 101). NO header removal / Leave/New-Game relocation (Phase 100). NO dev-server branding cleanup beyond deleting the BS chip + "BoardSmith Dev Mode" string (Phase 102).
- NO fallbacks/hacks/dummy data. Error text surfaced to the player MUST be the actionable `result.error` string (or an actionable fallback) — never a generic "something went wrong" and never a raw stack trace / internal path.
- Reuse the EXISTING toast (`useToast()` → `toast.error(message)`). Do NOT add a dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-quick-wins-wave-0/97-RESEARCH.md
@.planning/phases/97-quick-wins-wave-0/97-PATTERNS.md
@.planning/phases/97-quick-wins-wave-0/97-VALIDATION.md

<interfaces>
<!-- Verified from source — executor uses these directly, no codebase exploration needed. -->

useToast — src/ui/composables/useToast.ts (module singleton; Toast.vue already mounted in GameShell):
  const toast = useToast();
  toast.error(message: string, duration = 4000)   // red toast

Import path by depth:
  src/ui/components/*.vue           -> '../composables/useToast'   (GameShell already imports it, line 33)
  src/ui/components/auto-ui/*.vue   -> '../../composables/useToast' (ActionPanel must ADD this import)

useActionController — src/ui/composables/useActionController.ts (VERIFIED this plan):
  execute() resets isExecuting=false in finally (lines 850, 908) on BOTH success and failure.
  fill() early-returns on {valid:false} WITHOUT entering the executing state.
  => Panel re-enables after a toast-surfaced error with NO manual reset. (Confirm the wizard-meta
     branch of execute() past line 910 also resets isExecuting before relying on this — Prove Before Fix.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QUICK-01 — visible error feedback (toast.error)</name>
  <files>src/ui/components/GameShell.vue, src/ui/components/auto-ui/ActionPanel.vue, src/ui/utils/notifyError.ts</files>
  <read_first>
    - 97-RESEARCH.md "QUICK-01 — Verified Call Sites" (exact file:line table for every alert/console.error)
    - 97-PATTERNS.md "GameShell.vue (QUICK-01...)" and "ActionPanel.vue (QUICK-01...)" (before/after per call site, import path convention, three error-message variants)
    - src/ui/components/GameShell.vue (the call sites; re-verify line numbers — RESEARCH notes 0-2 line drift)
    - src/ui/components/auto-ui/ActionPanel.vue (lines ~16 imports, ~36 setup, 675, 730, 733)
    - src/ui/composables/useActionController.ts (confirm isExecuting reset on the execute() wizard-meta path before relying on auto re-enable)
  </read_first>
  <behavior>
    - When actionController.fill() returns {valid:false}, ActionPanel calls toast.error(result.error || 'Selection failed.')
    - When actionController.execute() returns {success:false, error}, ActionPanel calls toast.error(result.error)
    - When execute() throws, ActionPanel calls toast.error(err instanceof Error ? err.message : 'Failed to execute action.')
    - After any of the above, isExecuting is false (panel re-enabled) — verified via useActionController, no manual reset added unless the wizard-meta branch fails to reset
  </behavior>
  <action>
    In GameShell.vue: replace the five alert() calls (audit anchors ~755, 826, 908, 939, 943) with toast.error using the in-scope value — `result.error || '<actionable fallback>'` where a result is in scope (the lobby-join at ~939), `err instanceof Error ? err.message : '<actionable fallback>'` inside catch blocks (~755, ~908, ~943), and a plain actionable string for the game-code validation (~826). Add toast.error alongside (do NOT delete) the player-facing console.error calls at the undo paths (~482, ~495, ~499), setReady (~984), and restartGame (~1176), using result.error where available else the err.message variant. Do NOT add toast to the out-of-scope infrastructure console.error calls enumerated in RESEARCH ("Out-of-scope console.error calls" table: 365, 383, 410, 549, 803, 956, 1116, and the 1003-1105 block that already toasts).
    In ActionPanel.vue: ADD `import { useToast } from '../../composables/useToast'` and `const toast = useToast()` in <script setup>. Add toast.error alongside the console.error at setSelectionValue (~675, before the early return) and at executeAction (~730 success-false branch and ~733 catch). Keep every existing console.error for diagnostics.
    DRY (Claude's Discretion — helper preferred): extract a single `notifyError` at src/ui/utils/notifyError.ts that wraps `toast.error(result?.error || fallback)` and reuse it at the call sites. Inlining is acceptable only if it reads cleaner; either way the surfaced text MUST be the actionable result.error / err.message / validation string — never a generic message and never a raw stack trace or internal path (CLAUDE.md error-handling rule).
  </action>
  <verify>
    <automated>test -z "$(grep -rn 'alert(' src/ui/components/GameShell.vue)" && grep -q "useToast" src/ui/components/auto-ui/ActionPanel.vue && npx vitest run src/ui</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "alert(" src/ui/components/GameShell.vue` returns nothing (zero alert() calls remain)
    - ActionPanel.vue contains `import { useToast }` from '../../composables/useToast' and `const toast = useToast()`
    - ActionPanel.vue setSelectionValue, executeAction success-false branch, and executeAction catch each call toast.error with an actionable message
    - Out-of-scope console.error sites listed in RESEARCH remain console.error-only (no new toast)
    - `npx vitest run src/ui` passes
  </acceptance_criteria>
  <done>Every player-facing rejection in GameShell.vue and ActionPanel.vue surfaces a visible toast carrying the actionable error; no alert() remains; panel re-enables after failure.</done>
</task>

<task type="auto">
  <name>Task 2: QUICK-02 — accessible names on icon-only controls</name>
  <files>src/ui/components/HamburgerMenu.vue, src/ui/components/auto-ui/ActionPanel.vue</files>
  <read_first>
    - 97-RESEARCH.md "QUICK-02 — Verified Accessible-Name Gaps" (hamburger ~70, drawer ~81, close ~87, ActionPanel cancel ~805, clear ~813; note GameHeader needs NO change)
    - 97-PATTERNS.md "HamburgerMenu.vue (QUICK-02...)" and "ActionPanel.vue (QUICK-02...)" + "aria-hidden on Unicode glyph spans"
    - src/ui/components/HamburgerMenu.vue (re-verify anchors)
    - src/ui/components/auto-ui/ActionPanel.vue (re-verify anchors ~804, ~812)
  </read_first>
  <action>
    HamburgerMenu.vue hamburger button (~70): add `aria-label="Open menu"`, `:aria-expanded="isOpen"`, and `aria-controls="hamburger-menu-drawer"`. The three `.bar` spans are CSS-drawn (no text content) — do NOT add aria-hidden to them. On the menu-drawer element (~81) add `id="hamburger-menu-drawer"` so it matches aria-controls exactly. Close button (~87): add `aria-label="Close menu"` and wrap its `X` text in `<span aria-hidden="true">X</span>`.
    ActionPanel.vue cancel button (~804): add `aria-label="Cancel action"` and wrap its `✕` glyph in `<span aria-hidden="true">✕</span>`. clear-selection button (~813): add a dynamic `:aria-label="`Clear ${key}`"` (the `key` selection name is in scope) and wrap its `✕` glyph in `<span aria-hidden="true">✕</span>`.
    Do NOT add focus-trap, Escape handling, or role="dialog" — those are Phase 101.
  </action>
  <verify>
    <automated>grep -q 'aria-controls="hamburger-menu-drawer"' src/ui/components/HamburgerMenu.vue && grep -q 'id="hamburger-menu-drawer"' src/ui/components/HamburgerMenu.vue && grep -q 'aria-label="Cancel action"' src/ui/components/auto-ui/ActionPanel.vue && npx vitest run src/ui</automated>
  </verify>
  <acceptance_criteria>
    - HamburgerMenu hamburger button has `aria-label`, `:aria-expanded="isOpen"`, `aria-controls="hamburger-menu-drawer"`
    - menu-drawer element has `id="hamburger-menu-drawer"` (exact match to aria-controls)
    - HamburgerMenu close button has `aria-label="Close menu"` with the `X` inside an `aria-hidden="true"` span
    - ActionPanel cancel button has `aria-label="Cancel action"`; clear button has a `:aria-label` referencing `key`; both glyphs wrapped in `aria-hidden="true"` spans
    - `.bar` spans remain WITHOUT aria-hidden; no GameHeader.vue change for this requirement
  </acceptance_criteria>
  <done>Every icon-only control in the in-repo chrome exposes an accessible name; the hamburger reports expanded/controls state; decorative glyphs are aria-hidden.</done>
</task>

<task type="auto">
  <name>Task 3: QUICK-03 — remove dead menu items and engine branding</name>
  <files>src/ui/components/HamburgerMenu.vue</files>
  <read_first>
    - 97-RESEARCH.md "QUICK-03 — Verified Dead-Weight Locations" (defaultItems 55-62, BS chip 83-86 + CSS 214-225, drawer-footer 127-130 + CSS 347-355; confirms handleMenuItemClick only acts on leave/new-game)
    - 97-PATTERNS.md "HamburgerMenu.vue (QUICK-03...)" (exact before/after array and template)
    - src/ui/components/HamburgerMenu.vue (re-verify anchors before editing)
  </read_first>
  <action>
    Reduce `defaultItems` to exactly two entries — `{ id: 'new-game', ... }` and `{ id: 'leave', ... }` — removing `divider-1`, `settings`, `help`, and `divider-2` together (removing settings/help alone would orphan the two dividers and leave a stray separator). Remove the `<span class="logo-icon">BS</span>` from the `.logo` block (keep `logo-text`) and delete the now-dead `.logo-icon` CSS block. Remove the entire `.drawer-footer` div (the "BoardSmith Dev Mode" version line) and delete the now-dead `.drawer-footer` and `.version` CSS blocks. Deletion only — do NOT relocate Leave/New-Game (that is Phase 100).
  </action>
  <verify>
    <automated>test "$(grep -cE "id: '(settings|help|divider-1|divider-2)'" src/ui/components/HamburgerMenu.vue)" = "0" && test -z "$(grep -n 'logo-icon\|BoardSmith Dev Mode\|drawer-footer' src/ui/components/HamburgerMenu.vue)"</automated>
  </verify>
  <acceptance_criteria>
    - `defaultItems` contains only `new-game` and `leave` (no settings, help, divider-1, divider-2)
    - No `logo-icon` span or CSS block remains
    - No "BoardSmith Dev Mode" string and no `drawer-footer`/`version` template or CSS remains
    - `grep -n "logo-icon\|BoardSmith Dev Mode\|drawer-footer" src/ui/components/HamburgerMenu.vue` returns nothing
  </acceptance_criteria>
  <done>Player-facing chrome shows no dead Settings/Help items, no BS chip, and no BoardSmith Dev Mode branding; no orphaned dividers or dead CSS remain.</done>
</task>

<task type="auto">
  <name>Task 4: QUICK-04 + QUICK-05 — 100dvh fallback and safe-area insets</name>
  <files>src/ui/components/GameShell.vue, src/ui/components/HamburgerMenu.vue, src/ui/components/GameHeader.vue, src/cli/dev-host/host.html, src/cli/lib/project-scaffold.ts</files>
  <read_first>
    - 97-RESEARCH.md "QUICK-04 — Verified 100vh Locations" (GameShell 1441/1450/1482, HamburgerMenu 192) and "QUICK-05 — Verified Sticky/Fixed Chrome Edges + viewport-fit" (action bar 1552 + desktop override 1565, GameHeader 97 + desktop 275, host.html:5, project-scaffold.ts:200)
    - 97-PATTERNS.md "dvh fallback order (critical)" and "env() safe-area-inset with max()"
    - src/ui/components/GameShell.vue, HamburgerMenu.vue, GameHeader.vue (re-verify CSS anchors)
    - src/cli/dev-host/host.html, src/cli/lib/project-scaffold.ts (the viewport meta lines)
  </read_first>
  <action>
    QUICK-04: For each of the four 100vh declarations (GameShell ~1441 min-height, ~1450 height, ~1482 min-height; HamburgerMenu ~192 height), keep the existing `100vh` line and add an identical-property `100dvh` line IMMEDIATELY AFTER it (the 100vh fallback MUST precede 100dvh — reversed order breaks the fallback). Annotate the 100vh line as the fallback.
    QUICK-05 safe-area: On the sticky `.game-shell__action-bar` (~1552), replace the combined `padding: 12px 15px` with per-axis padding using `max(<existing>, env(safe-area-inset-*))` for bottom/left/right (top keeps its plain value — the bar does not touch the top edge); apply the same to the `@media (min-width: 768px)` override (~1565) with its `16px`/`20px` values. On `.game-header` (~97, the topmost element) replace `padding: 10px 12px` with per-axis padding applying `max(10px, env(safe-area-inset-top))` for top and `max(12px, env(safe-area-inset-left/right))` for left/right; apply the same to its desktop override (~275, `12px 20px`). Do NOT add insets to the menu-drawer (out of scope per RESEARCH).
    viewport-fit: append `, viewport-fit=cover` to the viewport meta `content` value in BOTH host.html (line 5) and the generateIndexHtml() template string in project-scaffold.ts (line 200) — env() insets evaluate to 0 without it.
  </action>
  <verify>
    <automated>test "$(grep -A1 '100vh' src/ui/components/GameShell.vue | grep -c '100dvh')" = "3" && grep -A1 '100vh' src/ui/components/HamburgerMenu.vue | grep -q '100dvh' && grep -q 'env(safe-area-inset-bottom)' src/ui/components/GameShell.vue && grep -q 'env(safe-area-inset-top)' src/ui/components/GameHeader.vue && grep -q 'viewport-fit=cover' src/cli/dev-host/host.html && grep -q 'viewport-fit=cover' src/cli/lib/project-scaffold.ts && npx vitest run src/ui</automated>
  </verify>
  <acceptance_criteria>
    - Each of the four 100vh declarations is immediately followed by an identical-property 100dvh line (fallback order: 100vh before 100dvh)
    - `.game-shell__action-bar` (both base and 768px override) uses `max(<existing>, env(safe-area-inset-bottom/left/right))`
    - `.game-header` (both base and 768px override) uses `max(<existing>, env(safe-area-inset-top/left/right))`
    - Both host.html:5 and project-scaffold.ts generateIndexHtml() viewport meta include `viewport-fit=cover`
    - menu-drawer received the dvh fallback but NO safe-area insets
  </acceptance_criteria>
  <done>The shell uses 100dvh with a 100vh fallback everywhere, sticky/topmost chrome respects safe-area insets, and viewport-fit=cover is set in both engine-controlled HTML files so the insets are non-zero.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Tests — a11y + toast component coverage</name>
  <files>src/ui/components/HamburgerMenu.test.ts, src/ui/components/auto-ui/ActionPanel.test.ts</files>
  <read_first>
    - 97-PATTERNS.md "HamburgerMenu.test.ts (NEW — QUICK-02)" (full harness template to copy/adapt) and "ActionPanel.smoke.test.ts" analog
    - 97-VALIDATION.md "Wave 0 Requirements" and "Phase Requirements -> Test Map"
    - src/ui/components/auto-ui/ActionPanel.smoke.test.ts and ActionPanel.test.ts (existing harness + how useToast / actionController are mounted/mocked)
  </read_first>
  <behavior>
    - HamburgerMenu: hamburger button has a truthy aria-label, aria-expanded="false" when closed, aria-controls="hamburger-menu-drawer"
    - HamburgerMenu: after clicking the hamburger, aria-expanded="true" and an element with id hamburger-menu-drawer exists
    - HamburgerMenu: close button has a truthy aria-label
    - ActionPanel: cancel button has aria-label="Cancel action"
    - ActionPanel: when actionController.fill() resolves {valid:false, error} the mocked toast.error is called with that error; when execute() resolves {success:false, error} toast.error is called with that error
  </behavior>
  <action>
    Create src/ui/components/HamburgerMenu.test.ts using the `// @vitest-environment jsdom` harness from PATTERNS.md, asserting the hamburger aria-label/aria-expanded(false->true)/aria-controls, the drawer id after open, and the close-button aria-label. Extend src/ui/components/auto-ui/ActionPanel.test.ts: add an assertion that the cancel button exposes `aria-label="Cancel action"`, and add tests that mock `useToast` and the action controller so that a rejected `fill()` and a rejected `execute()` each invoke `toast.error` with the controller's `error` string. Follow the existing mocking style in the ActionPanel test files — do NOT introduce a new toast mechanism.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/HamburgerMenu.test.ts src/ui/components/auto-ui/ActionPanel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/components/HamburgerMenu.test.ts exists and asserts aria-label + aria-expanded(false then true) + aria-controls + drawer id + close-button aria-label
    - ActionPanel.test.ts asserts cancel aria-label and that toast.error fires on rejected fill() and rejected execute()
    - `npx vitest run src/ui/components/HamburgerMenu.test.ts src/ui/components/auto-ui/ActionPanel.test.ts` passes
  </acceptance_criteria>
  <done>The testable a11y and error behaviors (hamburger/close aria, cancel aria-label, toast.error on rejection) have passing automated coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine/session → UI | Action results (`result.error`, caught `Error`) cross into player-facing toast/log surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Information disclosure | toast.error message strings (catch blocks in GameShell/ActionPanel) | mitigate | Surface only the actionable `result.error` or a clean `err.message`/generic actionable fallback — never a raw stack trace, line number, or internal path (CLAUDE.md error-handling rule). result.error strings are already player-facing. |
| T-97-02 | Tampering | viewport meta / scaffold template edit | accept | Static string append (`viewport-fit=cover`); no user input, no new attack surface |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH "Package Legitimacy Audit": not applicable). No supply-chain checkpoint required. |

Per RESEARCH Security Domain: no new input validation, auth, or session surface; the change is strictly additive (same engine error strings, now in a toast instead of a modal).
</threat_model>

<verification>
- `npm test` (full suite) green before `/gsd:verify-work`.
- Source assertions: zero `alert(` in GameShell.vue; `viewport-fit=cover` in both HTML files; each `100vh` immediately followed by `100dvh`; no `logo-icon`/`BoardSmith Dev Mode`/`drawer-footer` in HamburgerMenu.vue; `aria-controls`/`id` `hamburger-menu-drawer` match.
- Component tests: HamburgerMenu aria + ActionPanel cancel aria + toast.error-on-rejection pass.
- Manual (optional, not gating): on a real mobile browser the sticky action bar stays clear of the home indicator and the header clears the notch — non-visual fixes per orchestrator; spot-check only.
</verification>

<success_criteria>
1. A rejected move or failed action surfaces a visible `toast.error(result.error)`; no `alert()` and no silent player-facing `console.error` swallow remain; the panel re-enables (QUICK-01).
2. The hamburger and every X close/cancel/clear control are announced by name; the hamburger has `aria-expanded`/`aria-controls`; decorative glyphs are `aria-hidden` (QUICK-02).
3. No dead Settings/Help items and no engine branding (BS chip, BoardSmith Dev Mode) appear in player-facing chrome (QUICK-03).
4. The shell uses `100dvh` with a `100vh` fallback (QUICK-04) and fixed/sticky/topmost chrome edges respect `env(safe-area-inset-*)` with `viewport-fit=cover` set in both engine HTML files (QUICK-05).
5. `npm test` is green.
</success_criteria>

<output>
Create `.planning/phases/97-quick-wins-wave-0/97-01-SUMMARY.md` when done.
</output>
