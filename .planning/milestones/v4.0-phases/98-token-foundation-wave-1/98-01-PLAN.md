---
phase: 98-token-foundation-wave-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/theme.ts
  - src/ui/index.ts
  - src/ui/theme.test.ts
autonomous: true
requirements: [TOKEN-02, TOKEN-03, TOKEN-04, TOKEN-05]
must_haves:
  truths:
    - "theme.ts emits the full Slate dark token set as the default (graphite --bsg-bg #121417, layered surfaces, ink ramp, teal --bsg-accent #1fb8a6)"
    - "A complete Slate light token set exists and OS prefers-color-scheme selects it without host intervention"
    - "applyTheme({'--bsg-accent':'#abc'}) updates the resolved inline value on the document element"
    - "applyTheme can force a color scheme (light/dark/auto) so a host can override OS preference"
    - "A single --bsg-seat-1..6 token set is emitted from one SEAT_PALETTE source of truth"
    - "Interaction, drag/drop, and motion tokens are registered so the Phase 99 sweep only references them"
    - "The dead light-blue ThemeConfig defaults and color-math helpers are deleted"
  artifacts:
    - path: "src/ui/theme.ts"
      provides: "Slate token emission (themeCSS), applyTheme sole knob, SEAT_PALETTE"
      contains: "--bsg-bg"
      min_lines: 120
    - path: "src/ui/theme.test.ts"
      provides: "Unit tests for Slate defaults + applyTheme override + scheme forcing"
      exports: []
  key_links:
    - from: "src/ui/index.ts"
      to: "src/ui/theme.ts"
      via: "re-export of applyTheme/themeCSS/SEAT_PALETTE"
      pattern: "from './theme.js'"
---

<objective>
Rewrite `src/ui/theme.ts` from a dead light-blue color-only stub into the single Slate `--bsg-*` token engine: it emits the full pinned token set (color dark+light, spacing, type, radius, shadow, motion, interaction, drag/drop, seat), follows OS light/dark by default, and exposes `applyTheme()` as the sole host-overridable theming knob.

Purpose: This is the keystone every later visual wave (99, 100, 102) consumes. It is SAFE IN ISOLATION — `var(--bsg-*)` currently has zero real consumers, so emitting Slate defaults changes nothing visible; the chrome/renderers keep their hardcoded neon literals until the Phase 99 sweep.

Output: New `theme.ts` token engine, updated `index.ts` public exports, and `theme.test.ts` proving the defaults and the override knob.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-token-foundation-wave-1/98-CONTEXT.md

<interfaces>
<!-- Current theme.ts public surface (to be replaced). Executor: read the file. -->
Current exports from src/ui/theme.ts (DELETE the dead ones):
- applyTheme(config?: ThemeConfig): void        // KEEP name, REWRITE signature + body
- getTheme(): Required<ThemeConfig>              // DELETE (dead, light-blue scheme)
- themeCSS: string                               // KEEP name, REPLACE contents with full Slate token CSS
- interface ThemeConfig                          // DELETE (6-color light-blue shape)
- lighten/darken/hexToRgb/rgbToHex helpers       // DELETE (dead)

Current re-export block in src/ui/index.ts (lines ~276-281):
  export { applyTheme, getTheme, themeCSS, type ThemeConfig } from './theme.js';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite theme.ts as the Slate --bsg-* token engine</name>
  <read_first>
    - src/ui/theme.ts (the file being rewritten — read in full)
    - .planning/phases/98-token-foundation-wave-1/98-CONTEXT.md (the PINNED values — `<decisions>` color table + non-color/type/motion/interaction blocks)
    - src/ui/index.ts (lines ~272-285, the theming re-export block)
  </read_first>
  <behavior>
    - themeCSS string contains `--bsg-bg: #121417` and `--bsg-accent: #1fb8a6` in the `:root` (default dark) block
    - themeCSS string contains a light override (`--bsg-bg: #f3f2ef`, `--bsg-accent: #0d9488`) gated on `prefers-color-scheme: light` AND on `html[data-theme="light"]`, and a `html[data-theme="dark"]` block re-asserting dark so a host can force dark on a light OS
    - themeCSS string contains `--bsg-seat-1` through `--bsg-seat-6`, emitted from the exported SEAT_PALETTE array
    - themeCSS string contains the non-color tokens verbatim from CONTEXT (radius --bsg-r-sm..pill, spacing --bsg-s1..s6, --bsg-font, --bsg-mono, --bsg-rail, --bsg-side, the type scale --bsg-text-xs..2xl + line heights, motion --bsg-dur-* + --bsg-ease)
    - themeCSS string contains the interaction tokens (--bsg-selectable, --bsg-selected, --bsg-droptarget, --bsg-droptarget-hover, --bsg-ring, --bsg-elevation) derived via color-mix on --bsg-accent
    - themeCSS string contains the renamed drag tokens registered with defaults: --bsg-draggable-cursor:grab, --bsg-dragging-cursor:grabbing, --bsg-dragging-opacity:.5, --bsg-dragging-scale:.95, --bsg-droptarget-border, --bsg-droptarget-shadow, --bsg-droptarget-hover-border, --bsg-droptarget-hover-shadow, --bsg-droptarget-hover-scale:1.02, --bsg-drag-transition: all var(--bsg-dur-base) var(--bsg-ease)
    - No `#4a90d9` / `#f5f5f5` / other light-blue default strings remain anywhere in theme.ts
  </behavior>
  <action>
    Replace the entire body of `theme.ts`. DELETE `ThemeConfig`, `getTheme`, `defaultTheme`, and the `lighten`/`darken`/`hexToRgb`/`rgbToHex` helpers (all dead — No Backward Compatibility, no shims).

    Build a single `themeCSS` constant containing four CSS blocks: (1) `:root` with the DARK color tokens plus ALL non-color tokens (spacing, radius, type scale, fonts, motion, interaction, drag/drop, seat); (2) `@media (prefers-color-scheme: light) { :root:not([data-theme]) { ...light color tokens... } }`; (3) `html[data-theme="light"] { ...light color tokens... }`; (4) `html[data-theme="dark"] { ...dark color tokens... }`. Copy the color values VERBATIM from 98-CONTEXT.md `<decisions>` (do not re-derive). Load-bearing anchors: dark `--bsg-bg:#121417`, `--bsg-accent:#1fb8a6`, `--bsg-accent-2:#5fe6d6`, `--bsg-accent-ink:#04211d`, `--bsg-ink:#e8ebef`; light `--bsg-bg:#f3f2ef`, `--bsg-accent:#0d9488`, `--bsg-ink:#191e24`. Only COLOR tokens differ between light/dark; spacing/type/radius/motion/interaction/drag/seat are theme-independent and live once in `:root`.

    Interaction tokens per CONTEXT: `--bsg-selectable` = color-mix(in srgb, var(--bsg-accent) 14%, transparent); `--bsg-selected` = var(--bsg-accent); `--bsg-droptarget` = color-mix(in srgb, var(--bsg-accent) 12%, transparent); `--bsg-droptarget-hover` = color-mix(in srgb, var(--bsg-accent) 24%, transparent); `--bsg-ring` = 0 0 0 2px var(--bsg-accent); `--bsg-elevation` = var(--bsg-shadow-sm). Register the drag-token defaults named in `<behavior>` here (these replace the neon `--bs-*` definitions that Plan 98-03 deletes from drag-drop.css; the color ones use color-mix on accent, the cursor/opacity/scale/transition ones keep their non-color values).

    Define and export `const SEAT_PALETTE: readonly string[]` = a muted, ~30%-desaturated, colorblind-aware 6-color set on the graphite ground (executor's discretion per CONTEXT; suggested harmonious Slate set: desaturated red #b5544e, slate-blue #5a7fa8, sage #6f9e74, amber #c79a4b, plum #9a6f9b, teal #4fa39a). Emit `--bsg-seat-1..6` into `:root` from this array (single source — do NOT scatter the values). NOTE for the SUMMARY: CONTEXT mentioned PieceRenderer/DebugPanel as importers, but verification shows their hardcoded reds/blues are JSON value-type highlighting, not seat colors — the only real seat cycle is HexBoardRenderer's PLAYER_COLOR_CYCLE (handled in Plan 98-03).

    Emit `--bsg-font:'Hanken Grotesk', system-ui, sans-serif` and `--bsg-mono:'JetBrains Mono', ui-monospace, monospace` as family tokens only — do NOT add @font-face or webfont loading (that asset work is the visual wave's, not this phase's).

    Rewrite `applyTheme(overrides?: Record<string, string>, options?: { scheme?: 'light' | 'dark' | 'auto' }): void` as the SOLE theming knob. It must: (a) idempotently inject `themeCSS` into `document.head` once as `<style id="bsg-tokens">` (guard on existing id) so the base defaults + media query are present; (b) when `options.scheme` is 'light'/'dark', set `data-theme` on `document.documentElement`, and when 'auto' remove the attribute (restoring OS-following); (c) for each entry in `overrides`, write it via `root.style.setProperty(key, value)` ONLY when the key matches `/^--bsg-[a-z0-9-]+$/` and the value is a string — silently ignore any other key (this is the host→iframe injection guard; it must not write arbitrary CSS properties). Guard the whole function for non-DOM (SSR) by early-returning when `typeof document === 'undefined'`.

    Update `themeCSS` export to be this new full token string. Update `src/ui/index.ts` theming re-export block: export `applyTheme`, `themeCSS`, and `SEAT_PALETTE`; DELETE the `getTheme` and `type ThemeConfig` exports.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx tsc -p tsconfig.json --noEmit 2>&1 | grep -E 'theme\.ts|index\.ts' || echo "no type errors in theme/index"; grep -c -- '--bsg-bg' src/ui/theme.ts; grep -E '4a90d9|getTheme|ThemeConfig|interface ThemeConfig' src/ui/theme.ts && echo "FAIL dead code remains" || echo "OK dead code gone"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -- '--bsg-bg: #121417' src/ui/theme.ts` matches; `grep -- '--bsg-accent: #1fb8a6' src/ui/theme.ts` matches
    - `grep -- '--bsg-bg: #f3f2ef' src/ui/theme.ts` matches and sits under a `prefers-color-scheme: light` / `data-theme="light"` selector
    - `grep -cE -- '--bsg-seat-[1-6]' src/ui/theme.ts` == 6; `SEAT_PALETTE` is exported with exactly 6 entries
    - `grep -E '4a90d9|getTheme|hexToRgb|interface ThemeConfig' src/ui/theme.ts` returns nothing
    - applyTheme rejects non-`--bsg-` keys (verified by Task 2 test)
    - `src/ui/index.ts` no longer exports `getTheme` or `ThemeConfig`; it exports `applyTheme`, `themeCSS`, `SEAT_PALETTE`
  </acceptance_criteria>
  <done>theme.ts emits the full Slate token set (dark default + OS-following light + forced data-theme), seat tokens from one SEAT_PALETTE, interaction/drag/motion tokens registered, and applyTheme is a guarded sole knob; index.ts exports match; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for Slate defaults + applyTheme override knob</name>
  <read_first>
    - src/ui/theme.ts (the just-rewritten engine)
    - src/ui/composables/useBoardInteraction.test.ts (an existing vitest+jsdom test in this package, for setup/style conventions)
  </read_first>
  <behavior>
    - Test: themeCSS contains the Slate dark defaults (`--bsg-bg: #121417`, `--bsg-accent: #1fb8a6`) and a light override (`--bsg-bg: #f3f2ef`)
    - Test: themeCSS contains all six `--bsg-seat-1..6` declarations and SEAT_PALETTE has length 6
    - Test: applyTheme({'--bsg-accent':'#abc'}) makes `document.documentElement.style.getPropertyValue('--bsg-accent')` === '#abc'
    - Test: applyTheme ignores a non-bsg key — applyTheme({'color':'red','--evil':'x'}) leaves those properties unset on the root element
    - Test: applyTheme(undefined, {scheme:'light'}) sets `document.documentElement` `data-theme` to 'light'; applyTheme(undefined,{scheme:'auto'}) removes it
    - Test: applyTheme injects exactly one `<style id="bsg-tokens">` even when called twice (idempotent)
  </behavior>
  <action>
    Create `src/ui/theme.test.ts` using vitest (jsdom environment — confirm via the existing test's header/config). Assert the `<behavior>` cases above against the real exports. Use STRING assertions for themeCSS contents (jsdom does not resolve `color-mix` or media queries via getComputedStyle, so do not assert resolved computed values — assert the emitted declaration strings and the inline setProperty results instead). Reset `document.documentElement.style` and remove any injected `#bsg-tokens` style between tests so cases are independent.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/ui/theme.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/theme.test.ts` passes with all six behavior cases green
    - The applyTheme-override test specifically asserts the `#abc` resolved inline value (TOKEN-05 core proof)
    - The non-bsg-key test asserts arbitrary CSS properties are NOT written (injection guard proof)
  </acceptance_criteria>
  <done>theme.test.ts is green and proves Slate defaults are emitted, the override knob writes only `--bsg-*` keys, scheme forcing toggles data-theme, and injection is idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host page → iframe (postMessage `init.theme`) | Untrusted parent supplies a token override map consumed by applyTheme |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering/Injection | applyTheme(overrides) | mitigate | Accept only keys matching `^--bsg-[a-z0-9-]+$` with string values; write via setProperty (never innerHTML); ignore all other keys so a malicious host cannot set arbitrary CSS properties or inject markup |
| T-98-02 | DoS | applyTheme base injection | accept | Idempotent single `<style id="bsg-tokens">` guard prevents unbounded style growth on repeated calls; low value target |
</threat_model>

<verification>
- `npx vitest run src/ui/theme.test.ts` green.
- `npx tsc -p tsconfig.json --noEmit` shows no new errors in theme.ts / index.ts.
- `grep -rn 'getTheme' src/ui` returns no references (export removed and no consumers).
</verification>

<success_criteria>
- theme.ts is the single source emitting the full Slate `--bsg-*` token set (TOKEN-02, TOKEN-03), with a complete OS-following light set (TOKEN-04) and applyTheme as the sole override knob (TOKEN-05).
- Dead light-blue defaults and color-math helpers are deleted.
- Tests prove defaults emission and the override knob.
</success_criteria>

<output>
Create `.planning/phases/98-token-foundation-wave-1/98-01-SUMMARY.md` when done.
</output>
