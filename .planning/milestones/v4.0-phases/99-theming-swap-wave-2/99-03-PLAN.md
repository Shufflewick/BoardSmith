---
phase: 99-theming-swap-wave-2
plan: 03
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - src/ui/components/auto-ui/renderers/DeckRenderer.vue
  - src/ui/components/auto-ui/renderers/DieRenderer.vue
  - src/ui/components/auto-ui/renderers/PieceRenderer.vue
autonomous: true
requirements: [THEME-01, THEME-02]
must_haves:
  truths:
    - "Deck, die, and piece interaction states render in Slate accent tokens"
    - "No colored glow box-shadows remain on these renderers"
    - "Selection uses outline, never border; bg and ink are both tokenized"
  artifacts:
    - path: "src/ui/components/auto-ui/renderers/PieceRenderer.vue"
      provides: "tokenized piece with neutral elevation"
      contains: "var(--bsg-"
  key_links:
    - from: "these renderers"
      to: "theme.ts"
      via: "var(--bsg-selectable|selected|droptarget|accent|shadow-sm)"
      pattern: "--bsg-(selectable|selected|droptarget|accent)"
---

<objective>
Atomically sweep DeckRenderer, DieRenderer, and PieceRenderer to Slate interaction tokens and neutral elevation.

Purpose: THEME-01 (interaction tokens, no glow), THEME-02 (outline never border). Grouped because all three are small, glow-heavy element renderers with the same interaction language.
Output: Three renderers fully tokenized, no neon, no colored glow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theming-swap-wave-2/99-CONTEXT.md
@src/ui/theme.ts
@planning/mockups/boardsmith-chrome.html
@src/ui/components/auto-ui/renderers/DeckRenderer.vue
@src/ui/components/auto-ui/renderers/DieRenderer.vue
@src/ui/components/auto-ui/renderers/PieceRenderer.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep DeckRenderer.vue + DieRenderer.vue</name>
  <files>src/ui/components/auto-ui/renderers/DeckRenderer.vue, src/ui/components/auto-ui/renderers/DieRenderer.vue</files>
  <read_first>
    - src/ui/components/auto-ui/renderers/DeckRenderer.vue (<style>; glow box-shadow ~192, deck/zone heading colors, interaction states, `rgba(46,204,113…)`)
    - src/ui/components/auto-ui/renderers/DieRenderer.vue (<style>; `rgba(46,204,113…)` + the single hex)
    - src/ui/theme.ts (accent, accent-2, selectable, selected, droptarget, ring, shadow-sm, elevation, surface*, ink*, line*)
    - planning/mockups/boardsmith-chrome.html (dashed selectable / solid selected / dotted drop)
  </read_first>
  <action>
    For BOTH files, single atomic pass per rule (bg token ⇒ ink token in the same edit):
    - Interaction states: selectable dashed `var(--bsg-accent)` + `var(--bsg-selectable)`; selected solid `var(--bsg-accent)` outline + `var(--bsg-selected)` + small scale + `var(--bsg-ring)`; drop dotted `var(--bsg-accent-2)` + `var(--bsg-droptarget)`. Always `outline`, never `border`.
    - Remove colored glow box-shadow (Deck ~192) → `var(--bsg-shadow-sm)`/`var(--bsg-elevation)`.
    - Deck/zone heading text → `var(--bsg-ink)` (body) — headings may use `var(--bsg-display)` font with a faint text-shadow if they are titles.
    - Surfaces → `var(--bsg-surface*)`; hairlines → `var(--bsg-line)`/`var(--bsg-line-2)`; `color:#fff` → `var(--bsg-ink)`. Replace every `#00d9ff`/`#00ff88`/`rgba(46,204,113…)` and any other hex.
  </action>
  <acceptance_criteria>
    - Neither file has hex literals in <style>; no `#00d9ff`/`#00ff88`/`rgba(46,204,113`.
    - No colored glow box-shadow; selection uses outline only.
    - Every backgrounded surface also tokenizes ink.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/auto-ui/renderers/DeckRenderer.vue src/ui/components/auto-ui/renderers/DieRenderer.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -rnE '#00d9ff|#00ff88|rgba\(46, ?204, ?113' src/ui/components/auto-ui/renderers/DeckRenderer.vue src/ui/components/auto-ui/renderers/DieRenderer.vue && echo OK</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Sweep PieceRenderer.vue</name>
  <files>src/ui/components/auto-ui/renderers/PieceRenderer.vue</files>
  <read_first>
    - src/ui/components/auto-ui/renderers/PieceRenderer.vue (<style>; glow box-shadow ~242, interaction states, `rgba(46,204,113…)`)
    - src/ui/theme.ts (same tokens)
    - planning/mockups/boardsmith-chrome.html (interaction language)
  </read_first>
  <action>
    Atomic sweep: interaction states to `var(--bsg-selectable|selected|droptarget)` with dashed/solid/dotted accent outlines (never border); remove the colored glow box-shadow (~242) → `var(--bsg-shadow-sm)`/`var(--bsg-elevation)` + optional `var(--bsg-ring)`; piece fill/surfaces → tokens; `color:#fff` → `var(--bsg-ink)`; hairlines → `var(--bsg-line*)`. Replace all neon + any hex.
  </action>
  <acceptance_criteria>
    - No hex in <style>; no `#00d9ff`/`#00ff88`/`rgba(46,204,113`.
    - No colored glow; selection outline-only; bg+ink both tokenized.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/auto-ui/renderers/PieceRenderer.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -nE '#00d9ff|#00ff88|rgba\(46, ?204, ?113' src/ui/components/auto-ui/renderers/PieceRenderer.vue && echo OK</automated>
  </verify>
</task>

</tasks>

<verification>
- All three files pass inline stylelint color-no-hex; no neon; no colored glow; outline-only selection.
- `npx vitest run src/ui/components/auto-ui` stays green.
</verification>

<success_criteria>
Deck, die, and piece renderers are fully Slate with neutral elevation and tokenized interaction states.
</success_criteria>

<output>
Create `.planning/phases/99-theming-swap-wave-2/99-03-SUMMARY.md` when done.
</output>
