---
phase: 99-theming-swap-wave-2
plan: 04
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - src/ui/components/auto-ui/renderers/GridBoardRenderer.vue
  - src/ui/components/auto-ui/renderers/HexBoardRenderer.vue
  - src/ui/components/auto-ui/renderers/SpaceRenderer.vue
  - src/ui/components/auto-ui/renderers/ElementRenderer.vue
autonomous: true
requirements: [THEME-01, THEME-02]
must_haves:
  truths:
    - "Grid cells, hex <g> nodes, and spaces express selection/drop via accent tokens"
    - "Hex <g> interaction inherits the same dashed/solid/dotted Slate language"
    - "Selection uses outline, never border; bg and ink both tokenized"
  artifacts:
    - path: "src/ui/components/auto-ui/renderers/HexBoardRenderer.vue"
      provides: "tokenized hex <g> interaction states"
      contains: "var(--bsg-"
  key_links:
    - from: "board renderers"
      to: "theme.ts"
      via: "var(--bsg-selectable|selected|droptarget|cell|cell-line)"
      pattern: "--bsg-(selectable|selected|droptarget|cell)"
---

<objective>
Atomically sweep the board-topology renderers — GridBoard, HexBoard (including the hex `<g>` nodes), and Space — to Slate interaction tokens. Confirm ElementRenderer carries no literals (it currently has zero hex/rgba) and leave it clean.

Purpose: THEME-01, THEME-02. The hex `<g>` is called out explicitly in the phase scope.
Output: Board renderers fully tokenized; ElementRenderer confirmed clean so it passes the Wave 4 gate when the renderers glob is removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theming-swap-wave-2/99-CONTEXT.md
@src/ui/theme.ts
@planning/mockups/boardsmith-chrome.html
@src/ui/components/auto-ui/renderers/GridBoardRenderer.vue
@src/ui/components/auto-ui/renderers/HexBoardRenderer.vue
@src/ui/components/auto-ui/renderers/SpaceRenderer.vue
@src/ui/components/auto-ui/renderers/ElementRenderer.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep GridBoardRenderer.vue + SpaceRenderer.vue</name>
  <files>src/ui/components/auto-ui/renderers/GridBoardRenderer.vue, src/ui/components/auto-ui/renderers/SpaceRenderer.vue</files>
  <read_first>
    - src/ui/components/auto-ui/renderers/GridBoardRenderer.vue (<style>; cell interaction states, `rgba(46,204,113…)`, the 4 hex)
    - src/ui/components/auto-ui/renderers/SpaceRenderer.vue (<style>; interaction states, the 2 hex, `rgba(46,204,113…)`)
    - src/ui/theme.ts (accent, accent-2, selectable, selected, droptarget, ring, cell, cell-line, surface*, ink*, line*)
    - planning/mockups/boardsmith-chrome.html (.cell.selectable/.selected/.drop)
  </read_first>
  <action>
    Atomic pass per rule for both files: cell/space background → `var(--bsg-cell)` with `var(--bsg-cell-line)` gridlines; selectable → dashed `var(--bsg-accent)` outline + `var(--bsg-selectable)`; selected → solid `var(--bsg-accent)` outline + `var(--bsg-selected)` + small scale + `var(--bsg-ring)`; drop → dotted `var(--bsg-accent-2)` + `var(--bsg-droptarget)`. Always `outline`, never `border`. `color:#fff` → `var(--bsg-ink)`; hairlines → `var(--bsg-line*)`. Replace every neon + hex.
  </action>
  <acceptance_criteria>
    - Neither file has hex in <style>; no `#00d9ff`/`#00ff88`/`rgba(46,204,113`.
    - Selection outline-only; cells use `var(--bsg-cell)`/`var(--bsg-cell-line)`; bg+ink tokenized together.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/auto-ui/renderers/GridBoardRenderer.vue src/ui/components/auto-ui/renderers/SpaceRenderer.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -rnE '#00d9ff|#00ff88|rgba\(46, ?204, ?113' src/ui/components/auto-ui/renderers/GridBoardRenderer.vue src/ui/components/auto-ui/renderers/SpaceRenderer.vue && echo OK</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Sweep HexBoardRenderer.vue (incl. hex &lt;g&gt;) + confirm ElementRenderer.vue clean</name>
  <files>src/ui/components/auto-ui/renderers/HexBoardRenderer.vue, src/ui/components/auto-ui/renderers/ElementRenderer.vue</files>
  <read_first>
    - src/ui/components/auto-ui/renderers/HexBoardRenderer.vue (<style> AND the SVG `<g>`/polygon fill/stroke attrs in the template; 3 hex + many rgba; interaction classes applied to the hex group)
    - src/ui/components/auto-ui/renderers/ElementRenderer.vue (verify zero color literals — currently 0 hex / 0 rgba)
    - src/ui/theme.ts; planning/mockups/boardsmith-chrome.html
  </read_first>
  <action>
    HexBoardRenderer atomic sweep: the hex `<g>`/polygon selection/drop visuals → `var(--bsg-selectable|selected|droptarget)` and accent outline/stroke (dashed selectable / solid selected + ring / dotted drop). If fill/stroke are set via inline SVG attributes referencing literals, move them to CSS classes using `var(--bsg-*)` (SVG `fill`/`stroke` accept `var()` in CSS). Always outline-style ring, never layout-shifting border. Cell fills → `var(--bsg-cell)`/`var(--bsg-cell-line)`; `color:#fff` → `var(--bsg-ink)`; hairlines → `var(--bsg-line*)`. Replace every neon + hex (including the 3 hex and the `rgba(46,204,113…)`).
    ElementRenderer: confirm it has no color literals; make NO change if clean (it must simply pass the Wave 4 gate once the renderers glob is removed from ignoreFiles).
  </action>
  <acceptance_criteria>
    - HexBoardRenderer has no hex in <style>/template color attrs; no `#00d9ff`/`#00ff88`/`rgba(46,204,113`; hex `<g>` interaction expresses accent tokens with outline-style (non-border) selection.
    - ElementRenderer remains literal-free (passes color-no-hex).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/auto-ui/renderers/HexBoardRenderer.vue src/ui/components/auto-ui/renderers/ElementRenderer.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -nE '#00d9ff|#00ff88|rgba\(46, ?204, ?113' src/ui/components/auto-ui/renderers/HexBoardRenderer.vue && echo OK</automated>
  </verify>
</task>

</tasks>

<verification>
- All four files pass inline stylelint color-no-hex; no neon; hex <g> tokenized; outline-only selection.
- `npx vitest run src/ui/components/auto-ui` stays green.
</verification>

<success_criteria>
Board renderers (grid/hex/space) are fully Slate with tokenized interaction states; ElementRenderer confirmed clean.
</success_criteria>

<output>
Create `.planning/phases/99-theming-swap-wave-2/99-04-SUMMARY.md` when done.
</output>
