---
phase: 99-theming-swap-wave-2
plan: 06
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - src/ui/components/GameHeader.vue
  - src/ui/components/PlayersPanel.vue
autonomous: true
requirements: [THEME-04, THEME-06]
must_haves:
  truths:
    - "The GameHeader logo/title is solid display type, not gradient clip-text"
    - "The active-player cue is a calm accent dot with opacity-only breathe and a token border"
    - "The scaling neon halo / pulse-glow keyframes are deleted"
    - "Breathe animation is guarded by prefers-reduced-motion: reduce"
  artifacts:
    - path: "src/ui/components/GameHeader.vue"
      provides: "solid display title, no background-clip:text"
    - path: "src/ui/components/PlayersPanel.vue"
      provides: "calm active-player cue, opacity-only breathe"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "PlayersPanel.vue active card"
      to: "theme.ts accent token"
      via: "var(--bsg-accent) border + dot"
      pattern: "--bsg-accent"
---

<objective>
Sweep GameHeader (delete the gradient clip-text title; solid display type) and PlayersPanel (calm Slate active-player cue replacing the scaling neon halo).

Purpose: THEME-04 (titles/clip-text), THEME-06 (active-player cue). Paired as the two "type + player cue" chrome files.
Output: Solid display title; calm accent-dot cue with opacity-only, motion-safe breathe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theming-swap-wave-2/99-CONTEXT.md
@src/ui/theme.ts
@src/ui/components/GameHeader.vue
@src/ui/components/PlayersPanel.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete clip-text + tokenize GameHeader.vue</name>
  <files>src/ui/components/GameHeader.vue</files>
  <read_first>
    - src/ui/components/GameHeader.vue (the gradient clip-text block ~115-122 `-webkit-background-clip: text; -webkit-text-fill-color: transparent;`; `.logo-text`/`h1`; header surfaces; 14 hex incl. `#00d9ff`)
    - src/ui/theme.ts (display, font, accent, ink*, surface*, line*)
  </read_first>
  <action>
    Atomic sweep:
    - DELETE the gradient clip-text block. Replace with solid `color: var(--bsg-ink)` (or `var(--bsg-accent)` for the brandmark) in `var(--bsg-display)` font + faint `text-shadow`. No `background-clip:text`/`-webkit-text-fill-color: transparent` survives.
    - Body text → `var(--bsg-font)`/`var(--bsg-ink)`; the `#00d9ff` info-accent → `var(--bsg-accent)`; header surfaces → `var(--bsg-surface)` + `var(--bsg-line*)`; `color:#fff` → `var(--bsg-ink)`. Replace all 14 hex.
    Atomic rule: any surface that gets a token background also gets a token ink in the same edit.
  </action>
  <acceptance_criteria>
    - No `background-clip`/`text-fill-color: transparent` remains; title is solid `var(--bsg-display)`/token color with text-shadow.
    - No hex in <style>; no `#00d9ff`; surfaces tokenized; bg+ink together.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/GameHeader.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -nE 'background-clip|text-fill-color: ?transparent|#00d9ff' src/ui/components/GameHeader.vue && echo OK</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Calm active-player cue in PlayersPanel.vue</name>
  <files>src/ui/components/PlayersPanel.vue</files>
  <read_first>
    - src/ui/components/PlayersPanel.vue (`animation: pulse-glow 1.5s ... infinite` ~106; `@keyframes pulse-glow` ~110-117 with `transform: scale(1)`→`scale(1.15)`; active-card border ~72-75/101-119; 3 hex incl. neon)
    - src/ui/theme.ts (accent, ink*, surface*, line*)
  </read_first>
  <action>
    Atomic sweep:
    - DELETE the `pulse-glow` keyframes and the scaling halo. Replace the active-player dot with a calm `var(--bsg-accent)` dot whose breathe animates OPACITY ONLY (low amplitude, no `transform: scale`). The active card border → `var(--bsg-accent)` token border.
    - Wrap the breathe animation in `@media (prefers-reduced-motion: reduce) { … animation: none; }` so it is motion-safe (the global reduced-motion block is Phase 101 — this specific one is required now).
    - Surfaces → `var(--bsg-surface)` + `var(--bsg-line*)`; `color:#fff` → `var(--bsg-ink)`. Replace all neon + 3 hex.
  </action>
  <acceptance_criteria>
    - No `pulse-glow` and no `transform: scale` in the active-player animation; breathe is opacity-only.
    - A `@media (prefers-reduced-motion: reduce)` guard disables the breathe.
    - Active card border + dot use `var(--bsg-accent)`; no hex in <style>; bg+ink tokenized together.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/PlayersPanel.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -n 'pulse-glow' src/ui/components/PlayersPanel.vue && grep -q 'prefers-reduced-motion' src/ui/components/PlayersPanel.vue && echo OK</automated>
  </verify>
</task>

</tasks>

<verification>
- Both pass inline color-no-hex; clip-text gone; pulse-glow gone; reduced-motion guard present.
- `npx vitest run src/ui/components` stays green.
</verification>

<success_criteria>
Header title is solid display type; the active-player cue is a calm, motion-safe accent dot with a token border.
</success_criteria>

<output>
Create `.planning/phases/99-theming-swap-wave-2/99-06-SUMMARY.md` when done.
</output>
