---
phase: 99-theming-swap-wave-2
plan: 07
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - src/ui/components/auto-ui/ActionPanel.vue
autonomous: true
requirements: [THEME-03, THEME-07]
must_haves:
  truths:
    - "The primary action button is the Slate teal plate: token fill, dark-ink label, neutral elevation, no glow"
    - "Action dock surfaces are tokenized (bg AND ink)"
    - "No neon literal or colored glow remains in the action dock"
  artifacts:
    - path: "src/ui/components/auto-ui/ActionPanel.vue"
      provides: "teal primary button + tokenized action dock"
      contains: "var(--bsg-accent)"
  key_links:
    - from: "primary action button"
      to: "theme.ts"
      via: "background var(--bsg-accent) + color var(--bsg-accent-ink)"
      pattern: "--bsg-accent-ink"
---

<objective>
Sweep ActionPanel — the action dock — to Slate, with the primary action button as a teal plate. This is the single most literal-dense chrome file (36 hex / 33 rgba) so it gets its own plan.

Purpose: THEME-03 (teal primary button), THEME-07 (surfaces). The action dock is a KEY surface the both-theme contrast test (99-14) asserts on.
Output: ActionPanel fully tokenized; primary button is the Slate teal plate with neutral elevation and no glow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theming-swap-wave-2/99-CONTEXT.md
@src/ui/theme.ts
@planning/mockups/boardsmith-chrome.html
@src/ui/components/auto-ui/ActionPanel.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Primary teal button + tokenize action-dock surfaces (atomic)</name>
  <files>src/ui/components/auto-ui/ActionPanel.vue</files>
  <read_first>
    - src/ui/components/auto-ui/ActionPanel.vue (the primary action button rule ~1103-1118; all 36 hex incl. `#00d9ff`/`#00ff88`; 33 rgba glass/glow; secondary/disabled button states; the dock surface)
    - src/ui/theme.ts (accent, accent-ink, accent-2, shadow-sm, elevation, ring, surface*, ink*, line*, field, selectable, selected)
    - planning/mockups/boardsmith-chrome.html (primary button = teal plate, subtle hover lift, no glow)
  </read_first>
  <action>
    Single atomic sweep — for every rule whose background becomes a token, tokenize its text/ink in the same edit:
    - Primary action button (~1103-1118): `background: var(--bsg-accent); color: var(--bsg-accent-ink); box-shadow: var(--bsg-shadow-sm);` — NO colored glow box-shadow. Hover: subtle lift (`transform: translateY(-1px)` + slightly stronger neutral shadow), no neon glow.
    - Secondary/disabled buttons → `var(--bsg-surface*)`/`var(--bsg-field)` fills with `var(--bsg-ink)`/`var(--bsg-ink-2)` labels and `var(--bsg-line*)` borders.
    - Dock surface → `var(--bsg-surface)` + `var(--bsg-line*)` hairlines, replacing `rgba(0,0,0,x)` glass and `rgba(255,255,255,.1)` borders.
    - `#00d9ff` info-accent → `var(--bsg-accent)`; `#00ff88`/`rgba(46,204,113…)` → accent tokens; every `color:#fff` → `var(--bsg-ink)`.
    Replace ALL 36 hex. Remove every colored glow box-shadow → neutral `var(--bsg-shadow-sm)`/`var(--bsg-elevation)`.
  </action>
  <acceptance_criteria>
    - Primary button uses `background: var(--bsg-accent)` + `color: var(--bsg-accent-ink)` + `var(--bsg-shadow-sm)` and has NO colored glow.
    - No hex in <style>; no `#00d9ff`/`#00ff88`/`rgba(46,204,113`; no `color:#fff`.
    - Every backgrounded surface tokenizes its ink in the same rule (no invisible-text).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/auto-ui/ActionPanel.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -nE '#00d9ff|#00ff88|rgba\(46, ?204, ?113|color: ?#fff' src/ui/components/auto-ui/ActionPanel.vue && grep -q 'var(--bsg-accent-ink)' src/ui/components/auto-ui/ActionPanel.vue && echo OK</automated>
  </verify>
</task>

</tasks>

<verification>
- ActionPanel passes inline color-no-hex; primary button is the teal plate; no glow.
- `npx vitest run src/ui/components/auto-ui/ActionPanel.test.ts src/ui/components/auto-ui/ActionPanel.smoke.test.ts src/ui/components/auto-ui/ActionPanel.interaction.test.ts` stays green (update any test asserting an old literal).
</verification>

<success_criteria>
The action dock is fully Slate and its primary button is the teal plate — a KEY surface ready for the both-theme contrast test.
</success_criteria>

<output>
Create `.planning/phases/99-theming-swap-wave-2/99-07-SUMMARY.md` when done.
</output>
