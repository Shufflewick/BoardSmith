---
phase: 99-theming-swap-wave-2
plan: 09
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - src/ui/components/helpers/Button.vue
  - src/ui/components/helpers/FlyingCardsOverlay.vue
  - src/ui/components/helpers/ZoomPreviewOverlay.vue
autonomous: true
requirements: [THEME-07]
must_haves:
  truths:
    - "The shared Button helper renders all variants in Slate tokens (bg AND ink)"
    - "Flying-cards and zoom-preview overlays use tokenized surfaces and neutral shadows"
    - "No neon literal or colored glow remains in the helpers"
  artifacts:
    - path: "src/ui/components/helpers/Button.vue"
      provides: "tokenized button variants"
      contains: "var(--bsg-"
  key_links:
    - from: "helpers/*"
      to: "theme.ts"
      via: "var(--bsg-surface|ink|accent|shadow-sm)"
      pattern: "--bsg-(surface|ink|accent|shadow)"
---

<objective>
Sweep the three helper/overlay components to Slate.

Purpose: THEME-07 (surfaces). Grouped as the `helpers/` ignoreFiles entries.
Output: All three tokenized, no neon, neutral shadows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theming-swap-wave-2/99-CONTEXT.md
@src/ui/theme.ts
@src/ui/components/helpers/Button.vue
@src/ui/components/helpers/FlyingCardsOverlay.vue
@src/ui/components/helpers/ZoomPreviewOverlay.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep helpers/Button.vue (all variants)</name>
  <files>src/ui/components/helpers/Button.vue</files>
  <read_first>
    - src/ui/components/helpers/Button.vue (<style>; primary/secondary/danger/ghost variants; 15 hex incl. any neon)
    - src/ui/theme.ts (accent, accent-ink, accent-2, danger, surface*, field, ink*, line*, shadow-sm)
  </read_first>
  <action>
    Atomic sweep per variant rule (bg token ⇒ ink token in same edit): primary → `var(--bsg-accent)` fill + `var(--bsg-accent-ink)` label + `var(--bsg-shadow-sm)` (no glow); secondary/ghost → `var(--bsg-surface*)`/`var(--bsg-field)` + `var(--bsg-ink)`/`var(--bsg-line*)`; danger → `var(--bsg-danger)` + appropriate ink. Replace all 15 hex + any neon; `color:#fff` → `var(--bsg-ink)`/`var(--bsg-accent-ink)`.
  </action>
  <acceptance_criteria>
    - No hex in <style>; primary uses accent + accent-ink; no colored glow; bg+ink tokenized together for every variant.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/helpers/Button.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -nE '#00d9ff|#00ff88|rgba\(46, ?204, ?113' src/ui/components/helpers/Button.vue && echo OK</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Sweep FlyingCardsOverlay.vue + ZoomPreviewOverlay.vue</name>
  <files>src/ui/components/helpers/FlyingCardsOverlay.vue, src/ui/components/helpers/ZoomPreviewOverlay.vue</files>
  <read_first>
    - src/ui/components/helpers/FlyingCardsOverlay.vue (<style>; overlay card surfaces + shadows; 15 hex)
    - src/ui/components/helpers/ZoomPreviewOverlay.vue (<style>; preview surface + scrim + shadow; 7 hex)
    - src/ui/theme.ts (surface*, ink*, line*, shadow, shadow-sm, card-back)
  </read_first>
  <action>
    Atomic sweep for both: overlay/preview card surfaces → `var(--bsg-surface*)` (use `var(--bsg-card-back)` for any default card-back rendered in flight) + `var(--bsg-line*)`; scrims `rgba(0,0,0,x)` → keep neutral but prefer `var(--bsg-shadow*)` for elevation; `color:#fff` → `var(--bsg-ink)`. Replace all hex. bg+ink tokenized together.
  </action>
  <acceptance_criteria>
    - Neither file has hex in <style>; no neon; surfaces tokenized; bg+ink together.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/helpers/FlyingCardsOverlay.vue src/ui/components/helpers/ZoomPreviewOverlay.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -rnE '#00d9ff|#00ff88|rgba\(46, ?204, ?113' src/ui/components/helpers/FlyingCardsOverlay.vue src/ui/components/helpers/ZoomPreviewOverlay.vue && echo OK</automated>
  </verify>
</task>

</tasks>

<verification>
- All three pass inline color-no-hex; no neon; neutral shadows.
- `npx vitest run src/ui/components` stays green.
</verification>

<success_criteria>
The shared Button and the two overlays are fully Slate.
</success_criteria>

<output>
Create `.planning/phases/99-theming-swap-wave-2/99-09-SUMMARY.md` when done.
</output>
