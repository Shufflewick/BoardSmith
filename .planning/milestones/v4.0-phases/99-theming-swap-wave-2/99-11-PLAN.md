---
phase: 99-theming-swap-wave-2
plan: 11
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - src/ui/components/WaitingRoom.vue
autonomous: true
requirements: [THEME-07]
must_haves:
  truths:
    - "WaitingRoom carries no raw color literal — all 75 hex / 53 rgba are tokens"
    - "Every swept surface tokenizes bg AND ink together (no invisible-text)"
    - "No neon literal or colored glow remains"
  artifacts:
    - path: "src/ui/components/WaitingRoom.vue"
      provides: "fully tokenized waiting-room surface"
      contains: "var(--bsg-"
  key_links:
    - from: "WaitingRoom.vue"
      to: "theme.ts"
      via: "var(--bsg-surface|ink|accent|line|seat)"
      pattern: "--bsg-(surface|ink|accent|line|seat)"
---

<objective>
Sweep WaitingRoom — the second most literal-dense file (75 hex / 53 rgba) — to Slate in its own plan.

Purpose: THEME-07 (surfaces). Its size warrants isolation so the atomic bg+ink discipline is maintained across a large file.
Output: WaitingRoom fully tokenized, no neon, no colored glow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theming-swap-wave-2/99-CONTEXT.md
@src/ui/theme.ts
@src/ui/components/WaitingRoom.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokenize WaitingRoom.vue panels + headings (atomic)</name>
  <files>src/ui/components/WaitingRoom.vue</files>
  <read_first>
    - src/ui/components/WaitingRoom.vue (full <style>; panels, player slots, seat colors, headings, status pills; 75 hex incl. `#00d9ff`/`#00ff88`, 53 rgba glass/glow/`rgba(46,204,113…)`)
    - src/ui/theme.ts (surface*, ink*, accent, accent-2, line*, field, seat-1..6, shadow, shadow-sm, display, font)
  </read_first>
  <action>
    Work through the <style> rule-by-rule in a single atomic sweep — whenever a rule's background becomes a token, its text/ink in the SAME rule becomes a token:
    - Panels/cards → `var(--bsg-surface)`/`var(--bsg-surface-2)` + `var(--bsg-line)`/`var(--bsg-line-2)` hairlines, replacing `rgba(0,0,0,x)` glass and `rgba(255,255,255,.1)` white borders.
    - Player-slot seat colors → `var(--bsg-seat-1..6)` (do NOT inline seat hex — they live in theme.ts SEAT_PALETTE).
    - Headings → `var(--bsg-display)` font + `var(--bsg-ink)` + faint text-shadow; body → `var(--bsg-font)`/`var(--bsg-ink)`/`var(--bsg-ink-2)`.
    - Status pills/badges → token fills + token ink; `#00d9ff`/`#00ff88`/`rgba(46,204,113…)` → `var(--bsg-accent)`/`var(--bsg-ok)`.
    - Every `color:#fff` → `var(--bsg-ink)`. Remove every colored glow box-shadow → `var(--bsg-shadow*)`. Replace ALL 75 hex.
  </action>
  <acceptance_criteria>
    - No hex anywhere in <style>; no `#00d9ff`/`#00ff88`/`rgba(46,204,113`; no `color:#fff`.
    - Seat colors reference `var(--bsg-seat-*)` (no inline seat hex).
    - Every backgrounded surface tokenizes its ink in the same rule; no colored glow remains.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/WaitingRoom.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -nE '#00d9ff|#00ff88|rgba\(46, ?204, ?113|color: ?#fff' src/ui/components/WaitingRoom.vue && echo OK</automated>
  </verify>
</task>

</tasks>

<verification>
- WaitingRoom passes inline color-no-hex; no neon; no colored glow; seat colors via tokens.
- `npx vitest run src/ui/components` stays green.
</verification>

<success_criteria>
WaitingRoom is fully Slate with atomic bg+ink tokenization throughout.
</success_criteria>

<output>
Create `.planning/phases/99-theming-swap-wave-2/99-11-SUMMARY.md` when done.
</output>
