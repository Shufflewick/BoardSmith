---
phase: 99-theming-swap-wave-2
plan: 13
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - src/cli/dev-host/DevHost.vue
autonomous: true
requirements: [THEME-08]
must_haves:
  truths:
    - "DevHost indigo/neon literals are replaced with Slate --bsg-* tokens (bg AND ink)"
    - "The emerald 'New game' button is demoted to a quiet neutral outline"
    - "No neon literal remains; the two-click confirm is NOT added (Phase 102)"
  artifacts:
    - path: "src/cli/dev-host/DevHost.vue"
      provides: "Slate-recolored dev chrome, quiet New-game button"
      contains: "var(--bsg-"
  key_links:
    - from: "DevHost.vue"
      to: "theme.ts"
      via: "var(--bsg-surface|ink|accent|line)"
      pattern: "--bsg-(surface|ink|accent|line)"
---

<objective>
Recolor the in-repo dev chrome (DevHost.vue, 27 hex) from indigo/neon to Slate, and demote the emerald "New game" button to a quiet neutral outline.

Purpose: THEME-08 (DevHost recolor). Note: DevHost lives under `src/cli/dev-host/`, so `lint:css` (`src/ui/**`) does NOT cover it and it is NOT in the stylelint ignoreFiles list — the Wave 4 gate's residual-neon grep covers it instead.
Output: DevHost fully Slate; the two-click confirm itself is deferred to Phase 102 (DEV-07) — do NOT add it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theming-swap-wave-2/99-CONTEXT.md
@src/ui/theme.ts
@src/cli/dev-host/DevHost.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Slate-recolor DevHost.vue + demote New-game button</name>
  <files>src/cli/dev-host/DevHost.vue</files>
  <read_first>
    - src/cli/dev-host/DevHost.vue (<style> + inline color bindings; indigo/neon literals ~353-359, 413, 493-497, 505-508, 517, 542-550; the emerald "New game" button; 27 hex, 11 rgba)
    - src/ui/theme.ts (surface*, ink*, accent, accent-2, accent-ink, line*, field, danger, shadow-sm)
    - .planning/phases/99-theming-swap-wave-2/99-CONTEXT.md (THEME-08 decisions: demote New game to quiet outline; confirm is Phase 102)
  </read_first>
  <action>
    Atomic sweep (bg token ⇒ ink token in same rule):
    - Replace every indigo/neon literal with Slate tokens: dev chrome bars/panels → `var(--bsg-surface)`/`var(--bsg-surface-2)` + `var(--bsg-line*)`; accents (the indigo/neon highlights) → `var(--bsg-accent)`/`var(--bsg-accent-2)`; `color:#fff` → `var(--bsg-ink)`.
    - Demote the emerald "New game" button: from a filled emerald CTA to a QUIET neutral outline — `background: transparent; color: var(--bsg-ink-2); border: 1px solid var(--bsg-line-2);` with a subtle hover. Do NOT change its click behavior and do NOT add a two-click confirm (Phase 102 / DEV-07).
    - Replace all 27 hex. If the dev token base isn't applied in the dev host, confirm DevHost mounts under a tree where the `--bsg-*` tokens resolve (applyTheme base style); reference tokens regardless — they are the contract.
  </action>
  <acceptance_criteria>
    - No hex in <style>/inline color bindings; no `#00d9ff`/`#00ff88`/`rgba(46,204,113`; no indigo literal.
    - "New game" button is a quiet outline (transparent bg + token border + muted ink), NOT a filled emerald CTA, and its click handler is unchanged (no confirm step added).
    - Every backgrounded surface tokenizes its ink in the same rule.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/cli/dev-host/DevHost.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -nE '#00d9ff|#00ff88|rgba\(46, ?204, ?113|color: ?#fff' src/cli/dev-host/DevHost.vue && echo OK</automated>
  </verify>
</task>

</tasks>

<verification>
- DevHost passes inline color-no-hex; no neon/indigo; New-game button demoted; no confirm added.
- `npm run build` (or the CLI build/typecheck) stays green.
</verification>

<success_criteria>
The dev chrome is Slate and the New-game button is a quiet outline, with the confirm flow left for Phase 102.
</success_criteria>

<output>
Create `.planning/phases/99-theming-swap-wave-2/99-13-SUMMARY.md` when done.
</output>
