---
phase: 99-theming-swap-wave-2
plan: 14
type: tdd
wave: 3
depends_on: ["99-02", "99-03", "99-04", "99-05", "99-06", "99-07", "99-08", "99-09", "99-10", "99-11", "99-12", "99-13"]
files_modified:
  - src/ui/theme.contrast.test.ts
autonomous: true
requirements: [THEME-01, THEME-03, THEME-07]
must_haves:
  truths:
    - "An automated test asserts text-vs-background contrast on the KEY surfaces in BOTH dark and light schemes"
    - "The test catches the invisible-text trap (white text on light bg) at the token level"
    - "The test guards the atomic constraint: each key-surface component pairs a bg token with an ink token"
  artifacts:
    - path: "src/ui/theme.contrast.test.ts"
      provides: "both-theme WCAG contrast assertion + atomic-pairing source guard"
      contains: "data-theme"
  key_links:
    - from: "theme.contrast.test.ts"
      to: "theme.ts token sets"
      via: "parse DARK + LIGHT token blocks, compute WCAG contrast"
      pattern: "bsg-(bg|surface|ink|accent|accent-ink)"
---

<objective>
Create the dedicated both-theme contrast test — correctness rule #2. It resolves text vs background for the KEY surfaces (board, action dock, sidebar, dev chrome) and asserts they are NOT near-equal, in BOTH dark (default) and light schemes.

Purpose: This is the automated guard against the invisible-text trap. jsdom does NOT resolve `var()`/`color-mix` cascade from stylesheets, so the test computes WCAG contrast on the resolved hex parsed directly from `theme.ts`'s dark/light token blocks (single source of truth), AND adds a source-regex guard that each key-surface component pairs a bg token with an ink token.
Output: `src/ui/theme.contrast.test.ts`, green, run in CI alongside the suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theming-swap-wave-2/99-CONTEXT.md
@src/ui/theme.ts
@src/ui/theme.test.ts
@src/ui/components/GameShell.theme.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Token-level both-theme WCAG contrast assertion</name>
  <files>src/ui/theme.contrast.test.ts</files>
  <read_first>
    - src/ui/theme.ts (exported `themeCSS`; the `:root` dark block and the `html[data-theme="light"]` block; the four key pairs are all solid hex: dark bg #121417 / surface #1c2026 / ink #e8ebef / accent #1fb8a6 / accent-ink #04211d; light bg #f3f2ef / surface #ffffff / ink #191e24 / accent #0d9488 / accent-ink #ffffff)
    - src/ui/components/GameShell.theme.test.ts (the `// @vitest-environment jsdom` pragma + applyTheme usage pattern)
  </read_first>
  <behavior>
    - For BOTH schemes (dark = `:root`, light = `html[data-theme="light"]`), the (background, ink) token pairs for the KEY surfaces clear a WCAG contrast threshold:
      - board / sidebar surface pair: (--bsg-surface, --bsg-ink) ≥ 4.5:1
      - app background pair: (--bsg-bg, --bsg-ink) ≥ 4.5:1
      - primary action button (action dock) pair: (--bsg-accent, --bsg-accent-ink) ≥ 3:1
    - A deliberately swapped pair (e.g. ink-on-ink) would FAIL — assert the helper returns < 1.5 for an identical pair so the test is proven to discriminate.
  </behavior>
  <action>
    Create `src/ui/theme.contrast.test.ts` with `// @vitest-environment jsdom` (or node — computation is pure). Import `themeCSS` from `../ui/theme.js`. Parse the dark `:root { … }` block and the `html[data-theme="light"] { … }` block out of `themeCSS` via regex; extract the hex value of `--bsg-bg`, `--bsg-surface`, `--bsg-ink`, `--bsg-accent`, `--bsg-accent-ink` per scheme. Implement `relativeLuminance(hex)` and `contrastRatio(a, b)` per the WCAG 2.x formula. Assert each KEY pair clears its threshold in BOTH schemes (table-driven: iterate `['dark','light']`). Add a discrimination check: `contrastRatio(ink, ink) < 1.5`. Do NOT hardcode the hex values in the test — read them from `themeCSS` so the test tracks theme.ts.
  </action>
  <acceptance_criteria>
    - Test imports `themeCSS` and parses BOTH scheme blocks (no hardcoded token hex).
    - All key-pair assertions pass in dark AND light; the identical-pair discrimination check passes.
    - `npx vitest run src/ui/theme.contrast.test.ts` is green.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/ui/theme.contrast.test.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atomic-pairing source guard for key-surface components</name>
  <files>src/ui/theme.contrast.test.ts</files>
  <read_first>
    - the four KEY-surface component files swept earlier: src/ui/components/auto-ui/ActionPanel.vue (action dock), src/ui/components/GameHistory.vue (sidebar log), one board renderer src/ui/components/auto-ui/renderers/GridBoardRenderer.vue (board), src/cli/dev-host/DevHost.vue (dev chrome)
    - src/ui/theme.ts (token namespace)
  </read_first>
  <behavior>
    - For each key-surface file, its <style> contains NO raw hex color literal (reads file, regex-asserts zero `#[0-9a-fA-F]{3,8}` in color context) AND references BOTH a background token (`var(--bsg-…)` used as a background/surface) and an ink token (`var(--bsg-ink…)` or `var(--bsg-accent-ink)`).
    - This proves no key surface was left half-swept (bg tokenized but ink raw, or reverse).
  </behavior>
  <action>
    Add a second describe block to `theme.contrast.test.ts`. Read each of the four key-surface files via `fs.readFileSync`. Assert: (a) no neon literal (`#00d9ff`/`#00ff88`/`rgba(46,204,113`) and no hex remains in the file; (b) the file references at least one `--bsg-ink`/`--bsg-accent-ink` token AND at least one surface/bg/accent token — i.e. both ends of the bg↔ink pair are tokenized. Keep the file list small and explicit (the four KEY surfaces named above); this is a representative atomic-constraint guard, not a full-repo scan (the Wave 4 lint gate is the exhaustive check).
  </action>
  <acceptance_criteria>
    - For each of the four key-surface files: zero hex, references both a surface/bg/accent token and an ink token.
    - `npx vitest run src/ui/theme.contrast.test.ts` is green.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/ui/theme.contrast.test.ts</automated>
  </verify>
</task>

</tasks>

<verification>
- `npx vitest run src/ui/theme.contrast.test.ts` passes; both-scheme contrast + atomic-pairing guards green.
- Full suite `npm run test` stays green (969+ tests).
</verification>

<success_criteria>
The invisible-text trap is locked out by an automated both-theme contrast test plus an atomic-pairing source guard on the four key surfaces.
</success_criteria>

<output>
Create `.planning/phases/99-theming-swap-wave-2/99-14-SUMMARY.md` when done.
</output>
