---
phase: 106-predicate-triggers-ci-verifiable-authoring
plan: 04
type: execute
wave: 2
depends_on: ["106-01"]
files_modified:
  - src/testing/simulate-tutorial.ts
  - src/testing/tutorial-assertions.ts
  - src/testing/simulate-tutorial.test.ts
  - src/testing/index.ts
autonomous: true
requirements: [TUT-04]
must_haves:
  truths:
    - "An author can express a complete tutorial as a scripted scenario and run it as a normal Vitest test via simulateTutorial(testGame, tutorialDef, { seat, scenario, seed })"
    - "simulateTutorial reuses TestGame and the SAME engine autoAdvanceTutorial pump the server uses (no second evaluator)"
    - "The DSL FAILS on rule drift: (a) a scripted action illegal under the active gate throws, (b) an advanceWhen that does not fire when expected throws, (c) non-completion throws"
    - "assertTutorialStep / assertTutorialCompletes are exported from boardsmith/testing"
    - "A pinned seed makes the run reproducible"
  artifacts:
    - path: "src/testing/simulate-tutorial.ts"
      provides: "simulateTutorial driver"
      contains: "export function simulateTutorial"
    - path: "src/testing/tutorial-assertions.ts"
      provides: "assertTutorialStep, assertTutorialCompletes"
      contains: "export function assertTutorialCompletes"
    - path: "src/testing/index.ts"
      provides: "public boardsmith/testing exports"
      contains: "simulateTutorial"
  key_links:
    - from: "src/testing/simulate-tutorial.ts"
      to: "src/engine/tutorial/progress.ts"
      via: "autoAdvanceTutorial + validateTutorialDefinition + initialProgress"
      pattern: "autoAdvanceTutorial"
    - from: "src/testing/simulate-tutorial.ts"
      to: "src/engine/element/game.ts"
      via: "getTutorialDisabledActions for gate-legality assertion"
      pattern: "getTutorialDisabledActions"
---

<objective>
Deliver the CI-verifiable authoring DSL (TUT-04): author a tutorial as a test that fails when rules drift.

Purpose: CONTEXT Area 2 — add `simulateTutorial(testGame, tutorialDef, { seat, scenario, seed })` plus `assertTutorialStep`/`assertTutorialCompletes` to `src/testing` (exported via `boardsmith/testing`), REUSING `TestGame`. Failure-on-drift is the whole point: the DSL asserts each scripted action is legal under the active step gate (`game.getTutorialDisabledActions`), each advanceWhen fires when expected (via the SAME engine `autoAdvanceTutorial` the server runs), and the tutorial completes.

Output: the driver, the assertions, their exports, and tests proving the DSL throws on illegal-action drift, non-firing-predicate drift, and non-completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-predicate-triggers-ci-verifiable-authoring/106-CONTEXT.md

<interfaces>
src/testing/test-game.ts:
  TestGame.create(GameClass, { playerCount, playerNames?, seed?, autoStart? }) → TestGame  (seed at :72)
  testGame.game: G (public) — set `testGame.game.tutorialDefinition = def` to attach a tutorial
  testGame.doAction(seat, actionName, args) → ActionExecutionResult { success, error?, ... }
  testGame.getPlayerView(seat), testGame.isComplete()

src/engine/element/game.ts:
  game.getTutorialDisabledActions(seat): Record<actionName, reason>  — DSL asserts the scripted action is NOT a key (gate-legality)
  game.tutorialDefinition?: TutorialDefinition (public, settable)
  game.tutorialProgress: Map<seat, TutorialProgress>

src/engine/tutorial/progress.ts (Plan 01):
  initialProgress(def), autoAdvanceTutorial(game, seat): { advanced, finalStepId }, validateTutorialDefinition(def)
src/engine/tutorial/gate.ts: getActiveStep(game, seat) → resolve current stepId for assertions

src/testing/index.ts: public barrel — add the new exports alongside existing TestGame / simulateAction / assertions.
src/testing/assertions.ts:64+ — existing assert* style (throw with actionable message) to mirror.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: simulateTutorial driver + tutorial assertions</name>
  <read_first>
    - src/testing/test-game.ts:50-228 (TestGame API surface, seed threading)
    - src/testing/assertions.ts:1-135 (assert* pattern + error-message style)
    - src/testing/simulate-action.ts (existing scripted-action helper conventions)
    - src/engine/element/game.ts:900-947 (getTutorialDisabledActions)
    - src/engine/tutorial/progress.ts (autoAdvanceTutorial, initialProgress, validateTutorialDefinition)
    - src/engine/tutorial/gate.ts:65-74 (getActiveStep)
  </read_first>
  <files>src/testing/simulate-tutorial.ts, src/testing/tutorial-assertions.ts</files>
  <behavior>
    - Scenario shape: an ordered list of scripted moves `{ seat?, action, args?, expectStep? }`. seat defaults to the top-level `seat`.
    - simulateTutorial(testGame, tutorialDef, { seat, scenario, seed }): attaches tutorialDef to testGame.game, runs validateTutorialDefinition (MR-03 surfaces here too), sets initialProgress for `seat`, runs the engine auto-advance once at start (parity with server start-time evaluation), then for each scripted move:
        1. gate-legality: assert `move.action` is NOT a key of game.getTutorialDisabledActions(move.seat) — else throw an actionable Error naming the step + the disabled reason (this is the "taught action gated out" drift failure).
        2. perform: testGame.doAction(move.seat, move.action, move.args); assert result.success — else throw including result.error.
        3. auto-advance: run autoAdvanceTutorial for every running-tutorial seat (mirrors server post-action hook).
        4. expectation: if move.expectStep is set, assert the active stepId for `seat` === expectStep via getActiveStep — else throw "expected advance to '<x>' did not fire" (this is the "predicate did not fire" drift failure).
    - Returns a result: { completed: boolean; finalStepId: string|null; stepsVisited: string[] }.
    - seed: the run is reproducible. If testGame was created without a seed, simulateTutorial uses the provided `seed` to (re)create a deterministic TestGame from the same GameClass; document the precedence. Pit-of-Success: same seed → identical run.
    - assertTutorialStep(testGame|result, seat, expectedStepId) and assertTutorialCompletes(result) throw actionable errors on mismatch, matching the existing assert* style.
  </behavior>
  <action>
    Create `simulate-tutorial.ts` (driver + Scenario/result types) and `tutorial-assertions.ts` (the two asserts). Reuse the engine pump and gate helpers — do NOT write a second predicate evaluator or a second step-transition. Errors must be actionable per CLAUDE.md (name the step id, the action, and what drifted). Keep the driver UI-free and synchronous over the runner. Decide the seed precedence (caller-seeded TestGame wins; else apply `seed`) and document it in the JSDoc.
  </action>
  <verify>
    <automated>npx vitest run src/testing/simulate-tutorial.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function simulateTutorial" src/testing/simulate-tutorial.ts` matches.
    - `grep -nE "export function (assertTutorialStep|assertTutorialCompletes)" src/testing/tutorial-assertions.ts` shows both.
    - `grep -n "getTutorialDisabledActions" src/testing/simulate-tutorial.ts` matches (gate-legality check present).
    - `grep -nE "autoAdvanceTutorial" src/testing/simulate-tutorial.ts` matches (reuses engine pump, no second evaluator).
  </acceptance_criteria>
  <done>simulateTutorial drives a TestGame through a scripted tutorial, asserting gate-legality, predicate firing, and completion via shared engine machinery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DSL tests (drift FAILS) + public exports</name>
  <read_first>
    - src/testing/index.ts (export blocks)
    - src/session/tutorial-controller.test.ts:31-89 (in-test Game subclass for the fixture)
  </read_first>
  <files>src/testing/simulate-tutorial.test.ts, src/testing/index.ts</files>
  <behavior>
    - Happy path: a multi-step scripted tutorial over an in-test game completes (result.completed === true, expectStep assertions all pass).
    - Drift A (gate): a scenario whose scripted action is excluded by the active gate makes simulateTutorial throw, and the message names the step + disabled reason.
    - Drift B (predicate): a scenario whose move sets expectStep to a step that the advanceWhen does NOT actually advance to makes simulateTutorial throw "expected advance ... did not fire".
    - Non-completion: a scenario that ends before the last step → assertTutorialCompletes throws.
  </behavior>
  <action>
    Add `simulate-tutorial.test.ts` with a minimal in-test Game subclass (move/pass actions, simple flow — mirror tutorial-controller.test.ts) and raw labeled advanceWhen predicates (independent of Plan 02 helpers). Assert the happy path passes and that each drift case throws (`expect(() => simulateTutorial(...)).toThrow(/.../)`). Export `simulateTutorial`, its Scenario/result types, and `assertTutorialStep`/`assertTutorialCompletes` from `src/testing/index.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/testing</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "simulateTutorial|assertTutorialStep|assertTutorialCompletes" src/testing/index.ts` shows the exports.
    - The drift-A and drift-B tests each assert `toThrow` and pass.
    - `npx vitest run src/testing` is green.
  </acceptance_criteria>
  <done>The DSL is exported from boardsmith/testing and its own tests prove it throws on gate drift, predicate drift, and non-completion.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/testing` green.
- simulateTutorial reuses TestGame + the engine autoAdvanceTutorial pump + getTutorialDisabledActions — single source of truth, no second evaluator.
- Drift in any of the three dimensions throws.
</verification>

<success_criteria>
- TUT-04: a complete tutorial is expressible as a scripted CI test that fails on rule drift, with reproducible seed.
</success_criteria>

<output>
Create `.planning/phases/106-predicate-triggers-ci-verifiable-authoring/106-04-SUMMARY.md` when done.
</output>
