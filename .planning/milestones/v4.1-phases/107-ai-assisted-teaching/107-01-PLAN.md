---
phase: 107-ai-assisted-teaching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/mcts-bot.ts
  - src/ai/types.ts
  - src/ai/index.ts
  - src/ai/mcts-stats.test.ts
autonomous: true
requirements: [AI-01, AI-03]
must_haves:
  truths:
    - "playWithStats() returns the recommended move AND a non-empty per-candidate stats array"
    - "play() behaves identically to before (single-move path, parallel honored)"
    - "Stats reuse the existing MCTS search — no new search algorithm, weights, or training"
  artifacts:
    - path: "src/ai/mcts-bot.ts"
      provides: "private runSearch() + public playWithStats()"
      contains: "playWithStats"
    - path: "src/ai/types.ts"
      provides: "BotMoveStats interface + hintTargetFromMove on bot AI config"
      contains: "BotMoveStats"
    - path: "src/ai/mcts-stats.test.ts"
      provides: "unit + checkers-integration coverage for playWithStats"
      contains: "playWithStats"
  key_links:
    - from: "src/ai/mcts-bot.ts playWithStats()"
      to: "root.children before lines 292-293 cleanup"
      via: "runSearch() returns { move, root } before searchGame/rootSnapshot null"
      pattern: "runSearch"
    - from: "src/ai/index.ts"
      to: "BotMoveStats"
      via: "re-export"
      pattern: "BotMoveStats"
---

<objective>
Expose the existing MCTS bot's internal per-candidate evaluation so the session layer can drive the move hint (AI-01) and the evaluation heatmap (AI-03) — with NO new training, weights, or models.

Purpose: `playWithStats()` is the single shared data source for both the hint and the heatmap. It must read the root node's children (visits + value) BEFORE the post-search cleanup discards the tree (`mcts-bot.ts:292-293`).
Output: A private `runSearch()` extraction shared by `play()` and `playWithStats()`, a `BotMoveStats` type, an optional `hintTargetFromMove` hook on the bot AI config, and a new test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-ai-assisted-teaching/107-RESEARCH.md
@.planning/phases/107-ai-assisted-teaching/107-PATTERNS.md

<interfaces>
From src/ai/types.ts:
- `BotMove { action: string; args: Record<string, unknown> }` (line 44)
- `MCTSNode { parent; parentMove: BotMove | null; children: MCTSNode[]; visits; value; isProven; isDisproven; ... }` (lines 58-87)
- Bot AI config interface fields end with `moveOrdering?` (line 174) and `uctConstant?` (line 193) — insert `hintTargetFromMove?` between them.

From src/ai/mcts-bot.ts:
- `async play(): Promise<BotMove>` (line 93) dispatches to `playParallel()` (parallel>1) or `playSingle()` (line 97).
- `private async playSingle()` (line 174): the `root` MCTSNode is a local; cleanup nulls `this.searchGame`/`this.rootSnapshot` at lines 292-293; best-child selection (PNS proven-win fast path + most-visits fallback) is lines 296-341, returning `best.parentMove!`.
- `playParallel()` (line 107) internally calls `playSingle()` via a parallel:1 sub-bot (line 165).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BotMoveStats type + hintTargetFromMove hook</name>
  <files>src/ai/types.ts, src/ai/index.ts</files>
  <read_first>
    - src/ai/types.ts (BotMove at 41-49 for JSDoc-per-field style; bot AI config moveOrdering line 174 / uctConstant line 193 insertion point; MCTSNode 58-87)
    - src/ai/index.ts (existing type re-exports)
    - 107-PATTERNS.md section "src/ai/types.ts — extend"
  </read_first>
  <behavior>
    - `BotMoveStats` is importable from `boardsmith` ai surface and has exactly: `move: BotMove`, `visits: number`, `value: number` (value normalized [0,1]).
    - The bot AI config type accepts an optional `hintTargetFromMove?: (move: BotMove) => ElementRef | undefined` without breaking existing configs.
  </behavior>
  <action>
    Add an exported `BotMoveStats` interface immediately after `BotMove` in src/ai/types.ts, matching the JSDoc-per-field style: `move: BotMove` (the candidate), `visits: number` (visit count), `value: number` (normalized win-rate [0,1], 1.0 = certain win). Add an optional `hintTargetFromMove?: (move: BotMove) => ElementRef | undefined` field on the bot AI config interface, inserted between `moveOrdering?` (line 174) and `uctConstant?` (line 193), with a JSDoc note that it maps a chosen move to its target element for hint/heatmap anchoring and defaults to common destination arg-name extraction at the session layer. Import `ElementRef` from the engine types if not already imported. Re-export `BotMoveStats` from src/ai/index.ts alongside the existing `BotMove` export.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "ai/types|ai/index" || echo "no type errors in ai/types|index"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "BotMoveStats" src/ai/index.ts` returns a re-export line.
    - `hintTargetFromMove` appears in src/ai/types.ts on the bot AI config interface, between moveOrdering and uctConstant.
    - `npm run test` still green (no signature regression).
  </acceptance_criteria>
  <done>BotMoveStats exported from the ai surface; hintTargetFromMove optional hook added; existing tests unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract runSearch() and add playWithStats()</name>
  <files>src/ai/mcts-bot.ts, src/ai/mcts-stats.test.ts</files>
  <read_first>
    - src/ai/mcts-bot.ts (play() 93-98, playParallel() 107-166, playSingle() 174-341 including cleanup 292-293 and best-child selection 296-341)
    - 107-PATTERNS.md section "src/ai/mcts-bot.ts — extend"
    - 107-RESEARCH.md Pattern 1 + Pitfall 1 + Pitfall 2
  </read_first>
  <behavior>
    - `playWithStats()` returns `{ move, stats }` where `stats.length` equals the number of root children evaluated and matches each `parentMove`; `value` = `child.value / child.visits` (0 when visits===0).
    - `play()` returns the same move it returned before the refactor for a fixed-seed deterministic game.
    - `playWithStats()` ignores `config.parallel` and always runs a single search (parallel ensembles lose per-child stats).
  </behavior>
  <action>
    TDD-first (RED): begin by creating a minimal `src/ai/mcts-stats.test.ts` stub holding the failing in-repo TestGame tests for this task's behavior (playWithStats returns move + non-empty stats; value within [0,1]; play() move unchanged pre/post refactor). Run it RED before implementing, then make it GREEN with the refactor below. Task 3 expands this same file with full coverage + the checkers integration test — do not duplicate the file.
    Extract the body of `playSingle()` (lines 174-341) into a new `private async runSearch(): Promise<{ move: BotMove; root: MCTSNode }>`. Move the `this.searchGame = null; this.rootSnapshot = null;` cleanup (lines 292-293) and the best-child selection block (296-341) INSIDE runSearch, but capture and return the `root` reference together with the selected move — the root must remain referenced by the returned object so callers can read `root.children` AFTER cleanup nulls the bot's own snapshot fields (cleanup nulls instance fields, not the local root). Rewrite `play()` to: if `config.parallel > 1` return `playParallel()`; else `return (await this.runSearch()).move`. Update `playParallel()`'s internal single-search call (line 165) to use `runSearch()` if needed (preserve current behavior). Add `async playWithStats(): Promise<{ move: BotMove; stats: BotMoveStats[] }>` that always calls `runSearch()` (never parallel), maps `root.children` to `BotMoveStats` ({ move: child.parentMove!, visits: child.visits, value: child.visits > 0 ? child.value / child.visits : 0 }), and returns `{ move, stats }`. Document on the method that `parallel` config is intentionally ignored. Import `BotMoveStats` from './types.js'.
  </action>
  <verify>
    <automated>npx vitest run src/ai/mcts-stats.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "private async runSearch" src/ai/mcts-bot.ts` returns 1.
    - `grep -c "async playWithStats" src/ai/mcts-bot.ts` returns 1.
    - Test asserting `stats.length > 0` for a 3-choice TestGame passes.
    - Test asserting `play()` move equality pre/post refactor passes (or full suite green if covered elsewhere).
  </acceptance_criteria>
  <done>runSearch() shared by play()/playWithStats(); stats captured before tree discard; parallel forced off for stats.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test coverage — unit (TestGame) + checkers integration</name>
  <files>src/ai/mcts-stats.test.ts</files>
  <read_first>
    - src/ai/mcts-restore.test.ts (TestGame construction, MCTSBot constructor invocation, describe/it/expect structure)
    - vitest.config.ts (exclusion list — checkers-rules-dependent files are excluded)
    - 107-PATTERNS.md section "src/ai/mcts-stats.test.ts — new test file"
    - 107-RESEARCH.md "Environment Availability" note on excluded checkers tests
  </read_first>
  <behavior>
    - Unit (included): TestGame with a 3+ choice action → `playWithStats()` returns a move and `stats` with one entry per candidate, each `value` in [0,1], `visits >= 0`.
    - Unit: `playWithStats()` on a bot configured with `parallel: 2` still returns non-empty stats (proves parallel is forced off).
    - Integration (excluded file region): the symlinked checkers bot returns stats for each candidate checkers move with a resolvable target arg.
  </behavior>
  <action>
    Create src/ai/mcts-stats.test.ts following the mcts-restore.test.ts structure. Include in-repo TestGame unit tests (no external dependency, runs in default vitest): assert playWithStats returns move + non-empty stats, every value within [0,1], stats count matches candidate count, and that a `parallel: 2` bot still yields non-empty stats. Add the checkers-bot integration test guarded by the project's vitest exclusion convention (mirror how mcts-bot.test.ts is excluded in vitest.config.ts) so CI without the symlink does not fail — assert the checkers bot's stats include the recommended move and that the recommended move's `to` arg is present. Do NOT use dummy/fallback data; if the symlink is required, gate via the existing exclusion mechanism rather than stubbing.
  </action>
  <verify>
    <automated>npx vitest run src/ai/mcts-stats.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File exists; included unit tests pass under `npx vitest run src/ai`.
    - At least one assertion is `expect(stats.length).toBeGreaterThan(0)`.
    - At least one assertion bounds value: `toBeGreaterThanOrEqual(0)` and `toBeLessThanOrEqual(1)`.
  </acceptance_criteria>
  <done>playWithStats covered by deterministic in-repo unit tests; checkers integration present and correctly excluded when symlink absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot search → returned stats | Internal compute; values flow outward to session/UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01 | Denial of Service | playWithStats() repeated calls | accept | Rate-limiting/concurrency guards live at the session layer (Plan 02/04); the bot method itself is a pure compute call |
| T-107-02 | Information Disclosure | stats expose internal evaluation | accept | Checkers is perfect-information (no hidden state). Imperfect-info gating documented as a Phase 109+ gap in RESEARCH.md security domain |
| T-107-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs ZERO new packages (verified: RESEARCH Package Legitimacy Audit — none) |
</threat_model>

<verification>
- `npx vitest run src/ai` green.
- `playWithStats()` exported and consumable from `boardsmith` ai surface.
- No new dependencies added (`git diff package.json` empty).
</verification>

<success_criteria>
- runSearch() extraction: play() and playWithStats() share one search body; stats captured before tree discard.
- BotMoveStats + hintTargetFromMove exported.
- Unit + checkers-integration tests cover AI-01/AI-03 stats source.
</success_criteria>

<output>
Create `.planning/phases/107-ai-assisted-teaching/107-01-SUMMARY.md` when done.
</output>
