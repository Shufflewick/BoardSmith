---
phase: 109-checkers-tutorial-content
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/session/stateless-ops.ts            # BoardSmith
  - src/session/stateless-ops.test.ts       # BoardSmith
  - src/session/types.ts                    # BoardSmith
  - src/session/utils.ts                    # BoardSmith
  - src/session/build-player-state.test.ts  # BoardSmith
  - src/cli/dev-host/bridge.ts              # BoardSmith
  - src/ui/components/ControlsMenu.vue      # BoardSmith
  - src/ui/components/GameShell.vue         # BoardSmith
autonomous: true
requirements: [CHK-04]
must_haves:
  truths:
    - "A start-tutorial request routed through the platformRequest/bridge chain (re)starts the tutorial from step 0 for the requesting seat"
    - "PlayerGameState carries hasTutorial: true whenever the game has a tutorial definition"
    - "ControlsMenu shows a 'Start tutorial' item only when hasTutorial is true, in production GameShell AND the boardsmith dev host"
  artifacts:
    - path: "src/session/stateless-ops.ts"
      provides: "startTutorial Op variant + executeOp case (snapshot mutation of tutorialProgress)"
      contains: "startTutorial"
    - path: "src/session/types.ts"
      provides: "PlayerGameState.hasTutorial field"
      contains: "hasTutorial"
    - path: "src/cli/dev-host/bridge.ts"
      provides: "start-tutorial wire op → startTutorial Op translation + shapeResult"
      contains: "start-tutorial"
    - path: "src/ui/components/ControlsMenu.vue"
      provides: "Tutorial group + Start tutorial menu item gated on hasTutorial"
      contains: "start-tutorial"
  key_links:
    - from: "src/ui/components/GameShell.vue"
      to: "platformRequest('start-tutorial')"
      via: "handleTeachingAction start-tutorial case"
      pattern: "platformRequest\\('start-tutorial'"
    - from: "src/cli/dev-host/bridge.ts"
      to: "stateless-ops executeOp"
      via: "translateOp start-tutorial → { type: 'startTutorial', player: seat }"
      pattern: "type: 'startTutorial'"
    - from: "src/session/utils.ts"
      to: "PlayerGameState.hasTutorial"
      via: "buildPlayerState reads runner.game.tutorialDefinition"
      pattern: "hasTutorial = true"
---

<objective>
Build the tutorial launch surface. `GameSession.startTutorial(seat)` exists but nothing calls it. This plan adds a stateless `start-tutorial` platform op (mutating only the serialized `tutorialProgress` snapshot — so the dev bridge picks it up for free), broadcasts a `hasTutorial` signal in `PlayerGameState`, and surfaces a "Start tutorial" item in `ControlsMenu` wired through `GameShell`'s `platformRequest` chain — working in production GameShell AND the `boardsmith dev` host.

Purpose: Delivers CHK-04's launch requirement as reusable substrate (benefits the future cribbage tutorial too).
Output: `startTutorial` stateless Op + executeOp case, `hasTutorial` field + buildPlayerState wiring, bridge translateOp/shapeResult cases, ControlsMenu item, GameShell handler + computed, with session unit tests.

Scope guard: hint/heatmap/demo bridge ops are OUT of scope (stateful AI layer — deferred to Phase 110). Implement ONLY `start-tutorial`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-checkers-tutorial-content/109-RESEARCH.md
@.planning/phases/109-checkers-tutorial-content/109-PATTERNS.md

<interfaces>
<!-- Verified wire path + seams. Executor uses these directly. -->

Wire path (RESEARCH, verified through 5 files):
  GameShell.handleTeachingAction('start-tutorial')
    → platformRequest('start-tutorial', { seat })
    → DevHost forwards WS server_request
    → MultiplayerHost.handleServerRequest → session.handleServerRequest
    → bridge.translateOp('start-tutorial', seat, payload) → { type:'startTutorial', player:seat }
    → host.handleOp → stateless-ops.executeOp case 'startTutorial'
    → runner.game.tutorialProgress.set(seat, initialProgress(def)) → broadcast

Existing analogs to copy exactly:
- stateless-ops.ts: Op union lines 29-59; executeOp switch line ~651; mirror the `undo`/`cancelAction` variants. startTutorial MUTATES state — do NOT add to READ_ONLY_OP_TYPES.
- bridge.ts: WireOp union lines 21-37; translateOp switch 92-165; shapeResult 172-213; mirror the `undo`/`cancel_action` cases (shape: { success, error }).
- utils.ts: buildPlayerState ~lines 345-462; the tutorial projection block 451-462. hasTutorial applies to ALL seats (no playerPosition>0 guard).
- ControlsMenu.vue: Teaching group lines 258-301 gated on `showHint !== undefined`. NEW Tutorial group uses `v-if="hasTutorial"` (boolean gate, not tri-state).
- GameShell.vue: handleTeachingAction 711-750; showHintProp computed 694-696 (mirror for hasTutorialProp); ControlsMenu usage 1909-1924.

Restart is already clean/idempotent: TutorialController.start() overwrites progress unconditionally; validateTutorialDefinition throws on empty steps (MR-03 already resolved — RESEARCH).
validateTutorialDefinition + initialProgress live in src/engine/tutorial/progress.js.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: startTutorial stateless op + hasTutorial signal + bridge routing</name>
  <files>src/session/stateless-ops.ts, src/session/types.ts, src/session/utils.ts, src/cli/dev-host/bridge.ts, src/session/stateless-ops.test.ts, src/session/build-player-state.test.ts (all BoardSmith)</files>
  <read_first>
    - src/session/stateless-ops.ts (Op union 29-59, executeOp switch ~651, errorResult helper, how a mutating op returns its snapshot)
    - src/session/types.ts (PlayerGameState optional fields ~401-485, the tutorial?/disabledActions? block as style analog)
    - src/session/utils.ts (buildPlayerState tutorial projection block 451-462)
    - src/cli/dev-host/bridge.ts (WireOp union 21-37, translateOp 92-165, shapeResult 172-213, the `undo`/`cancel_action` cases)
    - src/session/stateless-ops.test.ts + src/session/build-player-state.test.ts (existing op + field test structure)
    - PATTERNS.md sections for stateless-ops.ts, types.ts, utils.ts, bridge.ts
  </read_first>
  <action>
    stateless-ops.ts: add `| { type: 'startTutorial'; player: number }` to the Op union; add an `executeOp` switch case `startTutorial` that reads `runner.game.tutorialDefinition` (error via errorResult 'No tutorial definition on this game.' when absent), calls `validateTutorialDefinition(def)` then `runner.game.tutorialProgress.set(op.player, initialProgress(def))` (import both from `../engine/tutorial/progress.js`), and falls through to the standard snapshot-build/broadcast result path. Do NOT add it to READ_ONLY_OP_TYPES.
    types.ts: add an optional `hasTutorial?: boolean` field to `PlayerGameState` with JSDoc mirroring the existing optional-field style (set when the game has a tutorial definition; omitted otherwise to keep the wire shape lean).
    utils.ts: in `buildPlayerState`, before the tutorial projection block, set `state.hasTutorial = true` when `runner.game.tutorialDefinition` is defined (no seat guard — the menu shows for any connected player).
    bridge.ts: add `'start-tutorial'` to the WireOp union; add a translateOp case returning `{ type: 'startTutorial', player: seat }`; add a shapeResult case returning `{ success: result.success, error: result.error }` (same shape as `undo`).
    stateless-ops.test.ts: add a case proving `{ type:'startTutorial', player:1 }` on a game WITH a tutorial definition succeeds and the resulting snapshot carries tutorial progress for seat 1; add a case proving it fails with an error containing 'No tutorial definition' when the game has none.
    build-player-state.test.ts: add a case asserting `state.hasTutorial === true` when `runner.game.tutorialDefinition` is set, and that `hasTutorial` is absent/undefined when it is not.
  </action>
  <verify>
    <automated>npx vitest run src/session/stateless-ops.test.ts src/session/build-player-state.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `startTutorial` Op variant + executeOp case exist (grep: `grep -c startTutorial src/session/stateless-ops.ts` ≥ 2) and the op is NOT in READ_ONLY_OP_TYPES.
    - `start-tutorial` wire op translates to `{ type:'startTutorial', player:seat }` and shapeResult returns `{ success, error }` (grep: `grep -c 'start-tutorial' src/cli/dev-host/bridge.ts` ≥ 2).
    - `PlayerGameState.hasTutorial` exists and `buildPlayerState` sets it from `runner.game.tutorialDefinition`.
    - New tests pass: startTutorial succeeds with a tutorial def, fails cleanly without one, and hasTutorial reflects definition presence.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ControlsMenu "Start tutorial" item + GameShell launch wiring</name>
  <files>src/ui/components/ControlsMenu.vue, src/ui/components/GameShell.vue (both BoardSmith)</files>
  <read_first>
    - src/ui/components/ControlsMenu.vue (defineProps 44-48, teaching-action emit type line 62, Teaching group template 258-301)
    - src/ui/components/GameShell.vue (handleTeachingAction 711-750, showHintProp computed 694-696, ControlsMenu usage 1909-1924, platformRequest)
    - PATTERNS.md sections for ControlsMenu.vue and GameShell.vue + "platformRequest / teaching-action emit" shared pattern
  </read_first>
  <action>
    ControlsMenu.vue: add an optional `hasTutorial?: boolean` prop (JSDoc: renders the Tutorial group when true); extend the `teaching-action` emit union to include `'start-tutorial'`; add a new Tutorial group template block (separator + 'Tutorial' grouplabel + a 'Start tutorial' menuitem button) gated on `v-if="hasTutorial"`, whose click emits `'teaching-action','start-tutorial'` then calls `close()`. Mirror the Teaching group's button markup/classes/roles.
    GameShell.vue: extend the `handleTeachingAction` parameter union with `'start-tutorial'`; add an else-if branch that calls `await platformRequest('start-tutorial', { seat: playerSeat.value })` inside try/catch with a `toast.error('Failed to start tutorial.')` on failure (mirror the heatmap-toggle branch). Add a `hasTutorialProp` computed mirroring `showHintProp` reading `state.value?.state?.hasTutorial ?? false`. Bind `:has-tutorial="hasTutorialProp"` on the `<ControlsMenu>` element. The launch must work in BOTH production GameShell and the boardsmith dev host (the platformRequest/bridge chain handles both — no separate dev path).
  </action>
  <verify>
    <automated>npx vitest run src/ui</automated>
  </verify>
  <acceptance_criteria>
    - ControlsMenu emits `'start-tutorial'` from a Tutorial-group item gated on `hasTutorial` (grep: `grep -c 'start-tutorial' src/ui/components/ControlsMenu.vue` ≥ 1).
    - GameShell routes `start-tutorial` through `platformRequest` and binds `has-tutorial` (grep: `grep -c \"platformRequest('start-tutorial'\" src/ui/components/GameShell.vue` ≥ 1 and `grep -c 'has-tutorial' src/ui/components/GameShell.vue` ≥ 1).
    - `npx vitest run src/ui` green (no UI regressions). hint/heatmap/demo bridge ops are NOT touched.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→session (start-tutorial op) | Seat-scoped request; seat lookup enforced upstream by MultiplayerHost.handleServerRequest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-02 | Elevation of Privilege | start-tutorial op writing another seat's progress | mitigate | Op carries `player: seat` resolved from the authenticated seat at the bridge; no client-supplied seat override |
| T-109-03 | Denial of Service | repeated start-tutorial (restart spam) | accept | start() is idempotent — overwrites progress to step 0; no resource accumulation |
| T-109-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (audit N/A) |

start-tutorial inherits the existing seat-based session auth model (RESEARCH Security Domain); no new auth surface.
</threat_model>

<verification>
- `npx vitest run src/session src/ui` green.
- start-tutorial op (re)starts the tutorial via a pure snapshot mutation; bridge routes it; ControlsMenu gates the item on hasTutorial.
- Scope honored: only start-tutorial wired; hint/heatmap/demo deferred to Phase 110.
</verification>

<success_criteria>
- A player can launch the tutorial from the ControlsMenu in production GameShell and the boardsmith dev host (CHK-04 substrate).
- hasTutorial broadcasts whenever a tutorial definition is present.
- BoardSmith session + UI suites green.
</success_criteria>

<output>
Create `.planning/phases/109-checkers-tutorial-content/109-02-SUMMARY.md` when done.
Commit in the BoardSmith repo (cwd `/Users/jtsmith/BoardSmith`).
</output>
