---
phase: 109-checkers-tutorial-content
plan: 03
type: execute
wave: 2
depends_on: ["109-01", "109-02"]
files_modified:
  - ~/BoardSmithGames/checkers/src/rules/game.ts          # checkers repo
  - ~/BoardSmithGames/checkers/src/rules/tutorial.ts      # checkers repo (NEW)
  - ~/BoardSmithGames/checkers/src/rules/index.ts         # checkers repo
  - ~/BoardSmithGames/checkers/tests/tutorial-preset.test.ts  # checkers repo (NEW, Wave 0 scaffold)
autonomous: true
requirements: [CHK-01, CHK-02, CHK-03, CHK-04]
must_haves:
  truths:
    - "A tutorialSetup option places a deterministic minimal position that forces an immediate mandatory capture for seat-1, followed by a multi-jump continuation"
    - "CHECKERS_TUTORIAL teaches: mandatory-capture tip (advanceWhen via playerHasCaptures), two-step piece+destination move (per-selection gate), multi-jump continuation (continuingPiece), and end-turn confirmation"
    - "gameDefinition.tutorial === CHECKERS_TUTORIAL so the game advertises a tutorial (drives hasTutorial / the Start tutorial launch item)"
  artifacts:
    - path: "~/BoardSmithGames/checkers/src/rules/game.ts"
      provides: "tutorialSetup option + placeTutorialPieces() deterministic position"
      contains: "tutorialSetup"
    - path: "~/BoardSmithGames/checkers/src/rules/tutorial.ts"
      provides: "CHECKERS_TUTORIAL TutorialDefinition (capture-tip, execute-capture, multi-jump-continue, confirm-turn)"
      contains: "CHECKERS_TUTORIAL"
    - path: "~/BoardSmithGames/checkers/src/rules/index.ts"
      provides: "tutorial: CHECKERS_TUTORIAL on gameDefinition"
      contains: "tutorial"
  key_links:
    - from: "~/BoardSmithGames/checkers/src/rules/tutorial.ts"
      to: "per-selection gate (Plan 01)"
      via: "gate.selections piece by {id}, destination by {toNotation}"
      pattern: "selections"
    - from: "~/BoardSmithGames/checkers/src/rules/tutorial.ts"
      to: "game.playerHasCaptures / game.continuingPiece"
      via: "custom advanceWhen predicates"
      pattern: "playerHasCaptures|continuingPiece"
---

<objective>
Author the checkers tutorial content against the Plan 01/02 substrate. Add a deterministic `tutorialSetup` board position that forces an immediate mandatory capture then a multi-jump, author `CHECKERS_TUTORIAL` (mandatory-capture tip → two-step gated move → multi-jump continuation → end-turn), and register it on `gameDefinition`.

Purpose: Delivers CHK-01 (two-step move via per-selection gate + annotations), CHK-02 (mandatory-capture tip via predicate), CHK-03 (multi-jump continuation), and the content half of CHK-04 (registration drives the launch item).
Output: `tutorialSetup` option + `placeTutorialPieces()`, `src/rules/tutorial.ts`, `gameDefinition.tutorial`, and a preset-verification test.

CROSS-REPO: ALL files in this plan live in `~/BoardSmithGames/checkers` (a SEPARATE git repo). Run and commit IN THAT REPO. The boardsmith dep is a symlink to this repo's `src/` — new Plan 01/02 exports resolve live with NO rebuild/re-vendor (RESEARCH verified).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/109-checkers-tutorial-content/109-RESEARCH.md
@.planning/phases/109-checkers-tutorial-content/109-PATTERNS.md

<interfaces>
<!-- Checkers seams verified by scout/research. Executor uses directly. -->

Action selections (src/rules/actions.ts): move = chooseElement('piece', CheckerPiece) then chooseFrom('destination', DestinationChoice). Selection names: 'piece', 'destination'.
DestinationChoice fields: { pieceId, fromNotation, toNotation, isCapture, becomesKing, capturedNotations }.
game.ts hooks: playerHasCaptures(player): boolean (src/rules/game.ts:208); continuingPiece (set during multi-jump, null when chain ends); hasMovedThisTurn.
PITFALL — CheckerPiece has NO .notation (only Square does). Gate the 'piece' selection by { id }, NEVER { notation }.
PITFALL — do NOT use whenForced('move') for mandatory capture (move is always available → fires prematurely). Use custom advanceWhen reading playerHasCaptures.
PITFALL — endTurn deadlock: a move-only gate blocks endTurn. Design the preset capture-first (no intro non-capture move) and add an explicit { gate:{action:'endTurn'} } confirm step after the jump chain.
PITFALL — advanceWhen firing before the beat is shown: simulateTutorial pumps once at start, so design so captures are NOT forced from the very first frame OR the capture-tip beat is the intended first step (RESEARCH Pitfall 4 — re-evaluate during authoring; preset must still force the capture for seat-1's first action).

Substrate from this phase: TutorialGateAllowList.selections (Plan 01), imported via TutorialDefinition type from 'boardsmith'. afterFirstTurn/afterTurns/whenForced exported from 'boardsmith'.
Pattern file for full step shapes: tutorial.ts section in 109-PATTERNS.md; analog DEMO_TUTORIAL in BoardSmith src/testing/tutorial-ci-demo.test.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tutorialSetup option + deterministic placeTutorialPieces position</name>
  <files>~/BoardSmithGames/checkers/src/rules/game.ts, ~/BoardSmithGames/checkers/tests/tutorial-preset.test.ts (NEW) (checkers repo)</files>
  <read_first>
    - ~/BoardSmithGames/checkers/src/rules/game.ts (CheckersOptions interface ~9-12, constructor, placePieces() ~97-115, the create-piece pattern ~105-107, playerHasCaptures ~208, getValidMoves, continuingPiece)
    - ~/BoardSmithGames/checkers/tests/game.test.ts (import + TestGame usage conventions for this repo)
    - 109-PATTERNS.md section "src/rules/game.ts (checkers)" and the Critical Pitfalls table
  </read_first>
  <action>
    Working dir: `cd ~/BoardSmithGames/checkers`. Add an optional `tutorialSetup?: boolean` to `CheckersOptions` (JSDoc: replaces the 24-piece opening with a minimal deterministic position forcing a capture + multi-jump for seat-1; used by the CI tutorial). Thread `options.tutorialSetup ?? false` from the constructor into `placePieces`; give `placePieces(tutorialSetup = false)` an early branch delegating to a new private `placeTutorialPieces()` when true. Implement `placeTutorialPieces()` to place a minimal (~2-3 piece) position where seat-1 has exactly one piece positioned to make a MANDATORY capture whose landing square sets up a second jump (multi-jump continuation), seat-2 has the two pieces in the jump path PLUS at least one extra piece so seat-2 can make a legal move when the flow reaches its turn. Use the same create-piece pattern as `placePieces()`. Derive the exact squares yourself from the board coordinate system (notation = colLetter + (8-row), row 0 = rank 8) and `getValidMoves` — commit them as named constants so the position is stable. Do NOT hard-code piece IDs into the tutorial; the position is what must be deterministic.
    Create `tests/tutorial-preset.test.ts`: with `TestGame.create(CheckersGame, { playerCount:2, seed:'checkers-tutorial-pinned', tutorialSetup:true })`, assert (a) seat-1's `getValidMoves` contains at least one capture move (`captures.length > 0`); (b) `playerHasCaptures(seat-1 player)` is true; (c) after executing that first capture, `game.continuingPiece !== null` (a multi-jump continuation exists). This proves the preset deterministically forces capture→multi-jump.
  </action>
  <verify>
    <automated>cd ~/BoardSmithGames/checkers && npx vitest run tests/tutorial-preset.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `CheckersOptions` has `tutorialSetup?: boolean` and the constructor routes it into piece placement (grep in checkers repo: `grep -c tutorialSetup src/rules/game.ts` ≥ 2).
    - `tests/tutorial-preset.test.ts` passes, proving: seat-1 has a forced capture, `playerHasCaptures` is true, and executing the capture leaves `continuingPiece !== null` (multi-jump).
    - The existing `npx vitest run` (full checkers suite) stays green — the standard opening is unchanged when `tutorialSetup` is absent/false.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CHECKERS_TUTORIAL definition + gameDefinition registration</name>
  <files>~/BoardSmithGames/checkers/src/rules/tutorial.ts (NEW), ~/BoardSmithGames/checkers/src/rules/index.ts, ~/BoardSmithGames/checkers/tests/tutorial-preset.test.ts (checkers repo)</files>
  <read_first>
    - ~/BoardSmithGames/checkers/src/rules/index.ts (gameDefinition shape ~15-44, ai/presets fields)
    - ~/BoardSmithGames/checkers/src/rules/game.ts (the constants/IDs exported by Task 1; playerHasCaptures, continuingPiece, hasMovedThisTurn)
    - BoardSmith src/testing/tutorial-ci-demo.test.ts (DEMO_TUTORIAL shape, advanceWhen predicate style)
    - 109-PATTERNS.md section "src/rules/tutorial.ts (NEW)" + "advanceWhen predicate style" + "Tutorial gate allow-list structure"
  </read_first>
  <action>
    Working dir: `cd ~/BoardSmithGames/checkers`. Create `src/rules/tutorial.ts` exporting `CHECKERS_TUTORIAL` (type `TutorialDefinition` from 'boardsmith'). Author four steps:
    1. `capture-tip` (CHK-02): `gate:{action:'move'}`, a content annotation explaining captures are mandatory (targeting the move action), and a custom `advanceWhen` whose predicate casts `ctx.game` to CheckersGame, resolves the seat's player, and returns `game.playerHasCaptures(player)`. Do NOT use whenForced.
    2. `execute-capture` (CHK-01): `suppressAutoFill: true`; `gate:{ action:'move', selections:{ piece:{ id: <tutorial piece id> }, destination:{ toNotation: <capture square> } } }`; two content annotations (highlight the piece by `{id}` ref, highlight the capture square by `{notation}` ref); `advanceWhen` returns `continuingPiece !== null` (capture executed, more jumps exist).
    3. `multi-jump-continue` (CHK-03): `gate:{ action:'move', selections:{ destination:{ toNotation: <second-jump square> } } }`, content "the turn continues while more jumps exist", `advanceWhen` returns `continuingPiece === null` (chain complete).
    4. `confirm-turn`: `gate:{action:'endTurn'}`, content prompting End Turn, `advanceWhen` returns `!hasMovedThisTurn`.
    Resolve the tutorial piece id / capture square / second-jump square from the deterministic preset — prefer a factory `createCheckersTutorial(game)` looking up `game.getPlayerPieces()` + `getValidMoves()` (avoids circular refs and hard-coded ids), OR export named constants from `game.ts` set by `placeTutorialPieces()`. Use `{id}` for the piece gate (CheckerPiece has no notation). Match the advanceWhen predicate style in PATTERNS (human-readable label keys, `(ctx): boolean =>`, no state mutation).
    In `src/rules/index.ts`, import `CHECKERS_TUTORIAL` (or the factory) and add `tutorial: CHECKERS_TUTORIAL` to `gameDefinition` after `presets`.
    Extend `tests/tutorial-preset.test.ts` with a registration smoke assertion: `gameDefinition.tutorial` is defined, its `steps` length is ≥ 4, and the step ids include `capture-tip`, `execute-capture`, `multi-jump-continue`, `confirm-turn`.
  </action>
  <verify>
    <automated>cd ~/BoardSmithGames/checkers && npx vitest run tests/tutorial-preset.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `CHECKERS_TUTORIAL` exists with the four steps; the piece gate uses `{id}` (NOT notation) and the capture/jump gates use `{toNotation}` (grep in checkers repo: `grep -c 'playerHasCaptures' src/rules/tutorial.ts` ≥ 1 and `grep -c 'continuingPiece' src/rules/tutorial.ts` ≥ 2).
    - `capture-tip` uses a custom `advanceWhen` (no `whenForced` in tutorial.ts: `grep -c whenForced src/rules/tutorial.ts` returns 0).
    - `gameDefinition.tutorial` is set (grep: `grep -c 'tutorial' src/rules/index.ts` ≥ 1) and the registration smoke test passes.
    - Full checkers suite (`npx vitest run`) green. NOTE: the full step-transition walkthrough + green→red proof is Plan 04 — this plan only proves the preset + registration.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author→game content | Tutorial definition is trusted authoring data in the game package; no end-user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-04 | Tampering | non-deterministic preset (piece ids/squares drift) | mitigate | Pinned seed + named-constant position; preset test asserts forced capture + multi-jump so drift fails CI |
| T-109-SC | Tampering | npm/pip/cargo installs | accept | No package installs (symlinked boardsmith dep; audit N/A) |

Content-only authoring; no network/auth surface (RESEARCH Security Domain).
</threat_model>

<verification>
- `cd ~/BoardSmithGames/checkers && npx vitest run` green.
- Preset deterministically forces capture→multi-jump; CHECKERS_TUTORIAL authored with per-selection gates + custom predicates; registered on gameDefinition.
- Cross-repo resolution confirmed: Plan 01/02 exports import from 'boardsmith' with no rebuild.
</verification>

<success_criteria>
- The checkers game advertises a tutorial teaching the two-step move, mandatory capture, and multi-jump continuation (CHK-01/02/03 content + CHK-04 registration).
- Preset + registration are verified by an automated test in the checkers repo.
</success_criteria>

<output>
Create `.planning/phases/109-checkers-tutorial-content/109-03-SUMMARY.md` in the BoardSmith repo (`.planning/` tracking stays in BoardSmith).
Commit the CODE changes IN THE CHECKERS REPO (`cd ~/BoardSmithGames/checkers && git add/commit`). Keep the two commit streams separate.
</output>
