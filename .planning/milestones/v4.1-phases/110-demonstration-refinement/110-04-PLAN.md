---
phase: 110-demonstration-refinement
plan: 04
type: execute
wave: 3
depends_on: [110-02]
files_modified:
  - ~/BoardSmithGames/checkers/src/rules/index.ts            # CHECKERS REPO — commit in that repo
  - ~/BoardSmithGames/checkers/src/rules/index.test.ts       # CHECKERS REPO (or nearest rules test file)
autonomous: true
requirements: [DEMO-01]

cross_repo:
  working_dir: ~/BoardSmithGames/checkers
  commit_repo: ~/BoardSmithGames/checkers
  note: "This is the ONLY task outside BoardSmith. .planning/ stays in BoardSmith; the code change + its commit happen IN the checkers repo. node_modules/boardsmith there is a symlink to this repo, so the Plan 02 hint op is already live."

must_haves:
  truths:
    - "The checkers hint highlights the destination square on the board (not just a floating bubble), because gameDefinition.ai.hintTargetFromMove resolves the destination's toNotation"
  artifacts:
    - path: "~/BoardSmithGames/checkers/src/rules/index.ts"
      provides: "hintTargetFromMove added to gameDefinition.ai"
      contains: "hintTargetFromMove"
  key_links:
    - from: "gameDefinition.ai.hintTargetFromMove"
      to: "move.args.destination.toNotation"
      via: "{ notation: toNotation }"
      pattern: "hintTargetFromMove"
---

<objective>
Add `hintTargetFromMove` to checkers' `gameDefinition.ai` so the AI hint highlights the destination board square. The checkers `destination` arg is an object `{ pieceId, fromNotation, toNotation }`, so the default `DEST_ARGS` fallback in the hint op (which only matches a plain string/number) returns no target — the hint would show a bubble with no board highlight (RESEARCH Pitfall 4). This is a small cross-repo change in `~/BoardSmithGames/checkers`.

Purpose: Makes the AI move hint genuinely useful for the live demonstration — the suggested destination square lights up. Without it the hint still works but is just a floating "Suggested move" bubble.
Output: checkers `gameDefinition.ai.hintTargetFromMove` + a checkers-repo unit test, committed in the checkers repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-demonstration-refinement/110-RESEARCH.md
@.planning/phases/110-demonstration-refinement/110-PATTERNS.md

<interfaces>
<!-- From PATTERNS.md / RESEARCH.md Pattern 8. -->

checkers gameDefinition.ai currently (index.ts:17-47): { objectives: getCheckersObjectives } — no hintTargetFromMove.
CheckersMove destination object (actions.ts:39-41,128-131): { pieceId: number; fromNotation: string; toNotation: string }.
hintTargetFromMove signature (boardsmith AIConfig, ai/types.ts:201): (move) => ElementRef | undefined.
Target shape for a board cell: { notation: string }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hintTargetFromMove to checkers gameDefinition.ai (CHECKERS REPO)</name>
  <files>~/BoardSmithGames/checkers/src/rules/index.ts, ~/BoardSmithGames/checkers/src/rules/index.test.ts</files>
  <read_first>
    - ~/BoardSmithGames/checkers/src/rules/index.ts (gameDefinition + .ai block, lines ~17-47)
    - ~/BoardSmithGames/checkers/src/rules/actions.ts (createMoveAction destination arg shape, ~119-155)
    - PATTERNS.md: checkers/src/rules/index.ts section (Pattern 8) + RESEARCH Pitfall 4
  </read_first>
  <action>
    Working directory MUST be `~/BoardSmithGames/checkers` (cd there for all shell commands). Add `hintTargetFromMove` to `gameDefinition.ai` alongside the existing `objectives`: a function `(move) => { const dest = move.args.destination as { toNotation?: string } | undefined; return dest?.toNotation ? { notation: dest.toNotation } : undefined; }`. Return `undefined` when there is no destination (fail-safe — the hint falls back to a bubble, never crashes).

    Add a unit test (in the rules test file — `index.test.ts` or the nearest existing rules-test) asserting: given a move with `args.destination.toNotation = 'd6'` (or a real checkers notation), `gameDefinition.ai.hintTargetFromMove(move)` returns `{ notation: 'd6' }`; given a move with no destination, it returns `undefined`.

    Commit IN the checkers repo (`cd ~/BoardSmithGames/checkers && git add … && git commit`). Do NOT commit this change to BoardSmith. The BoardSmith-side wiring (GameDefinitionLike.ai + dev.ts threading + the hint op honoring hintTargetFromMove) already landed in Plan 02, and node_modules/boardsmith is symlinked, so this change is live immediately.
  </action>
  <verify>
    <automated>cd ~/BoardSmithGames/checkers && npx vitest run src/rules</automated>
  </verify>
  <acceptance_criteria>
    - Source: gameDefinition.ai.hintTargetFromMove exists in the checkers repo.
    - Behavior: returns { notation: toNotation } for a move with a destination; undefined otherwise (no crash).
    - The change is committed in `~/BoardSmithGames/checkers` (NOT BoardSmith); `cd ~/BoardSmithGames/checkers && npx vitest run` green.
  </acceptance_criteria>
  <done>Checkers hint resolves the destination square; checkers suite green; committed in the checkers repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| move.args.destination → hintTargetFromMove | Engine-produced move object; trusted, but shape-checked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-09 | Tampering | hintTargetFromMove destination access | mitigate | Optional-chained `dest?.toNotation`; returns undefined (safe bubble fallback) when shape unexpected — never throws |
</threat_model>

<verification>
- `cd ~/BoardSmithGames/checkers && npx vitest run src/rules` green
- The commit lives in the checkers repo, not BoardSmith
</verification>

<success_criteria>
The checkers AI hint highlights the suggested destination square on the board.
</success_criteria>

<output>
Create `.planning/phases/110-demonstration-refinement/110-04-SUMMARY.md` in BoardSmith when done (the SUMMARY stays in BoardSmith even though the code change is cross-repo).
</output>
