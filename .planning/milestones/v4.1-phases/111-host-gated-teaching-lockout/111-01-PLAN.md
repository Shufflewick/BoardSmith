---
phase: 111-host-gated-teaching-lockout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/session/game-session.ts
  - src/session/types.ts
  - src/session/teaching.test.ts
autonomous: true
requirements: [LOCK-01]
must_haves:
  truths:
    - "A GameSession created with teachingDisabled:true rejects hint, heatmap, demo, and startTutorial requests fail-loud with an actionable error"
    - "A GameSession created with teachingDisabled:true (or absent) reflects state.teachingDisabled into every broadcast player state"
    - "Default (no flag / false) leaves all four teaching methods fully functional"
  artifacts:
    - path: "src/session/game-session.ts"
      provides: "teachingDisabled config threading + fail-loud guards on requestHint/setHeatmapVisible/startDemo/startTutorial + broadcast reflection"
      contains: "teachingDisabled"
    - path: "src/session/types.ts"
      provides: "PlayerGameState.teachingDisabled optional field"
      contains: "teachingDisabled"
  key_links:
    - from: "GameSessionOptions.teachingDisabled"
      to: "GameSession broadcast state.teachingDisabled"
      via: "private field set in constructor, injected post-buildPlayerState"
      pattern: "teachingDisabled"
---

<objective>
Make the production `GameSession` the server-side source of truth for the host teaching lockout (LOCK-01): a single session-level `teachingDisabled` boolean, threaded from session creation, that (a) rejects the four assist ops fail-loud and (b) reflects into every broadcast so all clients hide the affordances consistently.

Purpose: This is the anti-cheat control. Client hiding is defense-in-depth; the server MUST refuse a crafted op even when the UI is bypassed (criterion 3). Action help is NEVER gated (D-06).
Output: `teachingDisabled` config field + four fail-loud guards + broadcast reflection in `GameSession`, plus the `PlayerGameState.teachingDisabled` type field that downstream client gating reads.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-host-gated-teaching-lockout/111-CONTEXT.md

<interfaces>
<!-- Existing seams the executor edits. Extracted from the codebase — no exploration needed. -->

GameSessionOptions (src/session/game-session.ts:68) carries config fields (gameType, aiConfig, gameOptions, displayName...). `teachingDisabled?: boolean` is the new dedicated field — NOT inside gameOptions (D-01: prevents collision with a game that names its own option `teachingDisabled`).

GameSession.create (game-session.ts:477) destructures options and forwards to the private constructor (game-session.ts:256). Follow how `displayName`/`aiConfig` thread: option → create() destructure → constructor param → private field.

Existing fail-loud op-error pattern (requestHint, game-session.ts:952):
  throw new Error(`Cannot hint: seat ${seat} is not awaiting input`);
The four methods to guard:
  - requestHint(seat)            game-session.ts:952
  - setHeatmapVisible(seat,v)    game-session.ts:1041
  - startDemo(options?)          game-session.ts:1144
  - startTutorial(seat)          game-session.ts:1886
NOTE: do NOT guard exitTutorial/advanceTutorial/skipTutorial — exiting is always safe (D-06).

Broadcast injection seam (game-session.ts:1967-1978): transient teaching fields are injected AFTER buildPlayerState() to keep that function pure:
  const hint = this.#hint.get(effectivePosition);
  if (hint) state.hint = hint;
  ...
  if (this.#demoMode) state.isDemoRunning = true;
Inject `state.teachingDisabled = this.#teachingDisabled` at this same seam (D-03). Inject the boolean unconditionally (both true and false) so reconnecting clients always read the authoritative value.

PlayerGameState (src/session/types.ts:401) already declares optional transient teaching fields: `hint?` (457), `heatmap?` (465), `narration?` (473), `isDemoRunning?` (484), `hasTutorial?` (493). Add `teachingDisabled?: boolean` alongside them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread teachingDisabled config + fail-loud guards on the four assist methods</name>
  <read_first>
    - src/session/game-session.ts (the file being edited — read the GameSessionOptions interface ~68, create() ~477, constructor ~256, and the four methods at 952/1041/1144/1886)
    - src/session/types.ts (PlayerGameState ~401 — add the type field)
  </read_first>
  <behavior>
    - Test: a session created with `teachingDisabled: true` → `requestHint(seat)` rejects with an actionable error (e.g. "Teaching features are disabled for this session.").
    - Test: same session → `setHeatmapVisible(seat, true)` rejects fail-loud.
    - Test: same session → `startDemo()` rejects fail-loud (throws; does not swap the AI controller).
    - Test: same session → `startTutorial(seat)` rejects fail-loud.
    - Test: `exitTutorial(seat)` is NOT blocked when teachingDisabled (exiting is always safe — D-06).
    - Test: default session (no flag) — all four methods behave exactly as before (no rejection).
  </behavior>
  <action>
    Add `teachingDisabled?: boolean` to `GameSessionOptions`. Destructure it in `create()` and thread it to the private constructor as a new trailing param (mirror how `botAIConfig` threads), storing it as a private field `#teachingDisabled = teachingDisabled ?? false`. Add `teachingDisabled?: boolean` to `PlayerGameState` in types.ts next to the other transient teaching fields.

    In each of requestHint, setHeatmapVisible, startDemo, startTutorial: at the top of the method body (before any state mutation or AI work), if `this.#teachingDisabled` is true, throw `new Error('Teaching features are disabled for this session.')`. Match the existing fail-loud throw style (requestHint:955). For setHeatmapVisible guard BEFORE the `if (!visible)` early-return so a crafted `visible:false` is also rejected. For startDemo guard BEFORE the `#demoMode` idempotency check and BEFORE saving/replacing the AI controller. Do NOT add a guard to exitTutorial/advanceTutorial/skipTutorial.

    Use the literal message string above so it is discoverable as the single lockout error.
  </action>
  <verify>
    <automated>npm test -- src/session/teaching.test.ts</automated>
  </verify>
  <done>The four methods reject when teachingDisabled, exitTutorial does not, default behavior unchanged, eslint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Reflect teachingDisabled into broadcast + cross-layer trace test</name>
  <read_first>
    - src/session/game-session.ts (broadcast seam 1967-1978 — the post-buildPlayerState injection block)
    - src/session/teaching.test.ts (analog: how existing tests assert injected broadcast fields like hint/isDemoRunning)
  </read_first>
  <action>
    At the broadcast injection seam (game-session.ts:1971-1978, where `state.hint`/`state.isDemoRunning` are injected after buildPlayerState), add `state.teachingDisabled = this.#teachingDisabled;` injected unconditionally (set the boolean for both true and false) so reconnecting clients and second windows always read the authoritative value (criterion 4). Do not modify buildPlayerState (keep it pure — D-03).
  </action>
  <verify>
    <automated>npm test -- src/session/teaching.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Trace test (config→session→broadcast): a session created with `teachingDisabled: true` produces broadcast/getState player state with `state.teachingDisabled === true` for every seat; a default session yields `state.teachingDisabled === false`. (One test per CLAUDE.md cross-layer rule.)
    - The four guards from Task 1 still reject; default session still functional.
    - `npm test -- src/session/teaching.test.ts` green; `npx eslint src/session/game-session.ts src/session/types.ts` clean.
  </acceptance_criteria>
  <done>state.teachingDisabled is present in every broadcast player state and reflects the session config; trace test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GameSession op call | A player may craft `hint`/`heatmapToggle`/`demoStart`/`startTutorial` requests directly, bypassing the hidden UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Tampering | requestHint/setHeatmapVisible/startDemo/startTutorial | mitigate | Fail-loud guard at method entry rejects the op when teachingDisabled, before any state mutation — the server is the real anti-cheat control (criterion 3) |
| T-111-02 | Elevation of Privilege | client gating relies on local state | mitigate | teachingDisabled reflected into broadcast (single source of truth); server enforcement is independent of any client honoring it |
| T-111-03 | Information Disclosure | exitTutorial over-blocking | accept | exitTutorial/advance/skip are intentionally NOT gated — exiting teaching is always safe (D-06); blocking would trap a player |

No package-manager installs in this plan — supply-chain (T-111-SC) is N/A.
</threat_model>

<verification>
- `npm test -- src/session/teaching.test.ts` passes (new guard + trace tests + unchanged-default).
- Existing Phase 107–110 session suites stay green: `npm test -- src/session/` (criterion 5 — default behavior unchanged).
- `npx eslint src/session/game-session.ts src/session/types.ts` clean.
</verification>

<success_criteria>
- Production `GameSession` rejects the four assist ops fail-loud when `teachingDisabled` (criterion 3, production path).
- `state.teachingDisabled` is reflected into every broadcast (criterion 4).
- Default (no flag) behavior is unchanged (criterion 5).
- `PlayerGameState.teachingDisabled` typed field exists for downstream client gating.
</success_criteria>

<output>
Create `.planning/phases/111-host-gated-teaching-lockout/111-01-SUMMARY.md` when done.
</output>
