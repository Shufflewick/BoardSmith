---
phase: 111-host-gated-teaching-lockout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/session/stateless-ops.ts
  - src/session/snapshot-session-host.ts
  - src/cli/dev-host/bridge.ts
  - src/session/stateless-ops.test.ts
  - src/session/snapshot-session-host.test.ts
  - src/cli/dev-host/bridge.test.ts
autonomous: true
requirements: [LOCK-01]
must_haves:
  truths:
    - "executeOp rejects hint, heatmapToggle, and startTutorial fail-loud when teachingDisabled is threaded via its gameOptions input"
    - "SnapshotSessionHost.handleOp rejects demoStart fail-loud when its teachingDisabled adapter is set"
    - "SnapshotSessionHost reflects state.teachingDisabled into every broadcast player view"
    - "Default (teachingDisabled absent/false) leaves all dev-host ops functional"
  artifacts:
    - path: "src/session/stateless-ops.ts"
      provides: "fail-loud guards in handleHint/handleHeatmapToggle/startTutorial reading gameOptions.teachingDisabled"
      contains: "teachingDisabled"
    - path: "src/session/snapshot-session-host.ts"
      provides: "demoStart guard + state.teachingDisabled broadcast injection + teachingDisabled adapter"
      contains: "teachingDisabled"
    - path: "src/cli/dev-host/bridge.ts"
      provides: "DevSessionOptions.teachingDisabled threaded into SnapshotSessionHost"
      contains: "teachingDisabled"
  key_links:
    - from: "DevSessionOptions.teachingDisabled"
      to: "SnapshotSessionHost demoStart guard + broadcast state.teachingDisabled"
      via: "adapters field"
      pattern: "teachingDisabled"
    - from: "gameOptions.teachingDisabled"
      to: "executeOp handleHint/handleHeatmapToggle/startTutorial rejection"
      via: "pure-function input"
      pattern: "teachingDisabled"
---

<objective>
Enforce the host teaching lockout (LOCK-01) in the dev-host server path — the second of the two mandatory enforcement paths (criterion 3). The dev-host stack is `bridge.ts → SnapshotSessionHost.handleOp → pure executeOp`; the lockout must reject the four assist ops in BOTH the pure executor (hint/heatmapToggle/startTutorial) and the host lifecycle layer (demoStart), and reflect the flag into the dev-host broadcast.

Purpose: A crafted op against the dev host (or any SnapshotSessionHost embedder) must be refused even when the UI is hidden. The dev host is also what makes the lockout browser-demonstrable (Plan 04).
Output: `teachingDisabled` threaded into the pure `executeOp` inputs + the host adapters; fail-loud guards on hint/heatmapToggle/startTutorial/demoStart; `state.teachingDisabled` injected at the dev-host broadcast seam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-host-gated-teaching-lockout/111-CONTEXT.md

<interfaces>
<!-- Existing seams the executor edits. Extracted from the codebase — no exploration needed. -->

executeOp is PURE (stateless-ops.ts:944): `executeOp(def, gameOptions, snapshot, pendingState, op)`. `gameOptions` is `{ playerCount: number; [key: string]: unknown }` — an open bag. teachingDisabled rides in this bag (no signature change). Each handler already receives gameOptions:
  - handleHint(def, gameOptions, snap, op)            stateless-ops.ts:531
  - handleHeatmapToggle(def, gameOptions, snap, op)   stateless-ops.ts:594
  - startTutorial case (inline)                       stateless-ops.ts:1014-1038
The dispatch switch is at stateless-ops.ts:967. exitTutorial (case 1039) MUST stay functional — do NOT guard it (D-06). Note demoStart/demoStop never reach executeOp — they hit the fallback at 1063 (handled in the host).

Existing executeOp error pattern (fail-loud, returns not throws): `return errorResult('No tutorial definition on this game.', 'protocol');` (stateless-ops.ts:1016). Use the same `errorResult(message, 'protocol')` shape with message 'Teaching features are disabled for this session.'

SnapshotSessionHost (snapshot-session-host.ts):
  - constructor(private readonly adapters: SnapshotSessionAdapters) — line 91. Adapters interface declared at line 13 (executeOp, broadcast, aiSeats...). Add `teachingDisabled?: boolean`.
  - handleOp (line 166) intercepts demoStart (line 171) and demoStop (197) BEFORE delegating to executeOp. Guard demoStart here: if `this.adapters.teachingDisabled`, throw `new Error('Teaching features are disabled for this session.')` before starting runDemoLoop. Do NOT guard demoStop (stopping is safe).
  - mergeTransientState (line 103) is the dev-host broadcast injection seam — it spreads transient fields into each seat's `state` Record (state.hint, state.isDemoRunning, state.hasAIPlayers...). Inject `state.teachingDisabled = this.adapters.teachingDisabled ?? false` here for every view (set unconditionally — criterion 4). NOTE: the early `if (!hasTransient) return playerViews;` short-circuit (line 108) must NOT skip teachingDisabled injection — include `teachingDisabled` in the hasTransient condition (or always inject) so a lockout-only session still reflects the flag.

bridge.ts: DevSessionOptions (line 53) declares playerCount/aiSeats/executeOp/postGameState/postServerResponse. Add `teachingDisabled?: boolean`. createDevSession (line 268) constructs `new SnapshotSessionHost({ playerCount, aiSeats, executeOp, broadcast })` — pass `teachingDisabled: opts.teachingDisabled` through.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard hint/heatmapToggle/startTutorial in the pure executeOp</name>
  <read_first>
    - src/session/stateless-ops.ts (handleHint ~531, handleHeatmapToggle ~594, startTutorial case ~1014, exitTutorial case ~1039, errorResult usage)
    - src/session/stateless-ops.test.ts (analog: how ops are invoked with a gameOptions bag and how errorResult responses are asserted)
  </read_first>
  <action>
    In handleHint and handleHeatmapToggle, at the top of each (before seat-range validation / any runner work), if `gameOptions.teachingDisabled` is truthy, `return errorResult('Teaching features are disabled for this session.', 'protocol')`. Add the same guard at the top of the `startTutorial` case in the executeOp switch, before the `def.tutorial` check. Do NOT touch the `exitTutorial` case (D-06). gameOptions is already in scope in each handler/case. Read teachingDisabled off the gameOptions bag (it is set by the dev.ts closure in Plan 04; tests pass it directly).
  </action>
  <verify>
    <automated>npm test -- src/session/stateless-ops.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - With `gameOptions.teachingDisabled: true`: executeOp for op types hint, heatmapToggle, and startTutorial each returns `{ success: false }` with the lockout message; exitTutorial still succeeds.
    - With teachingDisabled absent/false: all three ops behave exactly as before.
    - `npm test -- src/session/stateless-ops.test.ts` green; `npx eslint src/session/stateless-ops.ts` clean.
  </acceptance_criteria>
  <done>Three executeOp ops reject fail-loud under teachingDisabled via the gameOptions input; exitTutorial unaffected; defaults unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Guard demoStart + reflect teachingDisabled in SnapshotSessionHost + thread through bridge</name>
  <read_first>
    - src/session/snapshot-session-host.ts (SnapshotSessionAdapters ~13, constructor ~91, mergeTransientState ~103, handleOp demoStart ~171 / demoStop ~197)
    - src/cli/dev-host/bridge.ts (DevSessionOptions ~53, createDevSession ~268 SnapshotSessionHost construction)
    - src/session/snapshot-session-host.test.ts and src/cli/dev-host/bridge.test.ts (analogs for host op invocation + broadcast view assertions)
  </read_first>
  <action>
    Add `teachingDisabled?: boolean` to SnapshotSessionAdapters and to DevSessionOptions; pass it through createDevSession into the SnapshotSessionHost adapters bag. In handleOp's demoStart branch, before launching runDemoLoop, if `this.adapters.teachingDisabled` throw `new Error('Teaching features are disabled for this session.')` (handleOp callers already convert a throw into a `{ success:false, error }` response — bridge.ts:304). Do NOT guard demoStop. In mergeTransientState, inject `state.teachingDisabled = this.adapters.teachingDisabled ?? false` for every seat view, and ensure the `hasTransient` short-circuit does not skip it (include teachingDisabled in the condition or inject before the short-circuit).
  </action>
  <verify>
    <automated>npm test -- src/session/snapshot-session-host.test.ts src/cli/dev-host/bridge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - With a host/adapters built with `teachingDisabled: true`: `handleOp(seat, { type:'demoStart' })` rejects fail-loud; `demoStop` is unaffected.
    - Every broadcast player view carries `state.teachingDisabled` reflecting the adapter value, even for a lockout-only session with no other transient state (criterion 4).
    - Default (teachingDisabled absent): demoStart works and views show `state.teachingDisabled === false`.
    - `npm test -- src/session/snapshot-session-host.test.ts src/cli/dev-host/bridge.test.ts` green; `npx eslint src/session/snapshot-session-host.ts src/cli/dev-host/bridge.ts` clean.
  </acceptance_criteria>
  <done>demoStart rejects under lockout; teachingDisabled reflected into dev-host broadcast; bridge threads the flag; defaults unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iframe seat → bridge → SnapshotSessionHost → executeOp | A dev-host player may craft an assist op directly, bypassing the hidden UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-04 | Tampering | executeOp handleHint/handleHeatmapToggle/startTutorial | mitigate | Fail-loud `errorResult` at handler entry when gameOptions.teachingDisabled; pure function refuses regardless of caller |
| T-111-05 | Tampering | SnapshotSessionHost.handleOp demoStart | mitigate | Throw before launching runDemoLoop when adapters.teachingDisabled — no demo loop / timer ever starts |
| T-111-06 | Elevation of Privilege | dev-host client trusts local init | mitigate | mergeTransientState reflects teachingDisabled into broadcast as the authoritative source for every connected seat |

No package-manager installs in this plan — supply-chain (T-111-SC) is N/A.
</threat_model>

<verification>
- `npm test -- src/session/stateless-ops.test.ts src/session/snapshot-session-host.test.ts src/cli/dev-host/bridge.test.ts` passes.
- Existing dev-host + stateless-ops suites stay green (criterion 5).
- `npx eslint src/session/stateless-ops.ts src/session/snapshot-session-host.ts src/cli/dev-host/bridge.ts` clean.
</verification>

<success_criteria>
- Dev-host path rejects all four assist ops fail-loud under teachingDisabled (criterion 3, dev-host path).
- `state.teachingDisabled` reflected into dev-host broadcast (criterion 4).
- Default behavior unchanged (criterion 5).
- DevSessionOptions exposes `teachingDisabled` for the CLI wiring in Plan 04.
</success_criteria>

<output>
Create `.planning/phases/111-host-gated-teaching-lockout/111-02-SUMMARY.md` when done.
</output>
