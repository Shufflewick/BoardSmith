---
phase: 111-host-gated-teaching-lockout
plan: 04
type: execute
wave: 2
depends_on: ["111-02"]
files_modified:
  - src/cli/cli.ts
  - src/cli/commands/dev.ts
  - src/cli/dev-host/multiplayer-host.ts
  - src/cli/dev-host/config-types.ts
  - src/cli/dev-host/DevHost.vue
  - src/cli/dev-host/multiplayer-host.test.ts
autonomous: true
requirements: [LOCK-01]
must_haves:
  truths:
    - "boardsmith dev --lock-teaching creates a dev-host session with teachingDisabled:true"
    - "A crafted hint/heatmap/startTutorial op routed through the running dev host is rejected fail-loud when --lock-teaching is set"
    - "A crafted demoStart op is rejected fail-loud when --lock-teaching is set"
    - "DevHost delivers teachingDisabled to each iframe via the init postMessage and the broadcast reflects it"
    - "Without --lock-teaching, the dev host behaves exactly as before"
  artifacts:
    - path: "src/cli/cli.ts"
      provides: "--lock-teaching dev command option"
      contains: "lock-teaching"
    - path: "src/cli/commands/dev.ts"
      provides: "parse lockTeaching → thread teachingDisabled into MultiplayerHost + DevHostConfig"
      contains: "teachingDisabled"
    - path: "src/cli/dev-host/multiplayer-host.ts"
      provides: "teachingDisabled threaded into executeOp gameOptions + createDevSession"
      contains: "teachingDisabled"
    - path: "src/cli/dev-host/config-types.ts"
      provides: "DevHostConfig.teachingDisabled"
      contains: "teachingDisabled"
    - path: "src/cli/dev-host/DevHost.vue"
      provides: "init postMessage carries teachingDisabled"
      contains: "teachingDisabled"
  key_links:
    - from: "--lock-teaching CLI flag"
      to: "executeOp gameOptions.teachingDisabled + createDevSession teachingDisabled + iframe init"
      via: "DevOptions.lockTeaching → MultiplayerHostOptions.teachingDisabled / DevHostConfig.teachingDisabled"
      pattern: "teachingDisabled"
---

<objective>
Wire the dev-host exercise path for the lockout (LOCK-01, D-04): a `boardsmith dev --lock-teaching` flag (mirroring `--ai`) that creates the dev-host session with `teachingDisabled:true`. This makes the enforcement (Plan 02) and gating (Plan 03) browser-demonstrable end-to-end, and exercises the dev-host server path with a real running host.

Purpose: One CLI flag drives the whole dev-host stack — server enforcement + broadcast reflection + iframe init delivery — from a single source.
Output: `--lock-teaching` option; `teachingDisabled` threaded from the CLI into the executeOp gameOptions bag, the SnapshotSessionHost adapters, the DevHostConfig, and the iframe init postMessage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-host-gated-teaching-lockout/111-CONTEXT.md

<interfaces>
<!-- Existing seams the executor edits. Extracted from the codebase — no exploration needed. -->

cli.ts dev command (28-38): `.option('--ai <players...>', ...)` is the sibling. Add `.option('--lock-teaching', 'Disable AI hint, move-quality heatmap, AI-vs-AI demo, and tutorial (action help stays enabled)')` — a boolean flag (no value). Commander exposes it as `options.lockTeaching`.

dev.ts DevOptions interface (43): add `lockTeaching?: boolean`. devCommand (339) parses options into locals (aiPlayers/aiLevel...). Derive `const teachingDisabled = options.lockTeaching === true` ONCE — single source for the two consumers below.
  - MultiplayerHost construction (dev.ts:582): add `teachingDisabled` to the options object.
  - buildDevConfig (dev.ts:311) returns DevHostConfig — add `teachingDisabled` to its args + return object.

multiplayer-host.ts:
  - MultiplayerHostOptions (57): add `teachingDisabled?: boolean`.
  - In startGame (~415-426): `baseOptions = { playerCount }` is the gameOptions bag for NON-start ops — add `teachingDisabled: this.opts.teachingDisabled` so executeOp's hint/heatmapToggle/startTutorial guards (Plan 02) see it. Also add `teachingDisabled` to `startGameOptions` so a startTutorial crafted at start is covered. Pass `teachingDisabled: this.opts.teachingDisabled` into `createDevSession({...})` (line 419) so the SnapshotSessionHost demoStart guard + broadcast injection (Plan 02) are armed.

config-types.ts DevHostConfig (20): add `teachingDisabled?: boolean` next to aiSeats. This config is consumed by the browser DevHost.vue.

DevHost.vue init frames: `postToGame({ type: 'init', seat: msg.seat })` at lines 154, 201, 224. Add `teachingDisabled: cfg.teachingDisabled === true` to each init payload (the host config is available as `cfg`/the injected dev config — match how aiSeats/seat are read). This is the first-render delivery GameShell consumes (Plan 03 init handler); the broadcast remains the authoritative source.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: --lock-teaching flag + thread teachingDisabled through the dev-host server</name>
  <read_first>
    - src/cli/cli.ts (dev command options 28-38)
    - src/cli/commands/dev.ts (DevOptions 43, devCommand 339, MultiplayerHost construction 582)
    - src/cli/dev-host/multiplayer-host.ts (MultiplayerHostOptions 57, startGame baseOptions/createDevSession 415-426)
    - src/cli/dev-host/multiplayer-host.test.ts (analog: end-to-end op dispatch through the host with a fake executeOp)
  </read_first>
  <action>
    Add the `--lock-teaching` boolean option to the dev command in cli.ts. Add `lockTeaching?: boolean` to DevOptions; in devCommand derive `const teachingDisabled = options.lockTeaching === true` once and pass it into the MultiplayerHost options object. Add `teachingDisabled?: boolean` to MultiplayerHostOptions. In startGame, add `teachingDisabled: this.opts.teachingDisabled` to BOTH `baseOptions` and `startGameOptions` (so the executeOp gameOptions bag carries it for non-start and start ops), and pass `teachingDisabled: this.opts.teachingDisabled` into `createDevSession({...})`. Print a one-line notice when the flag is set (mirror the `--ai` seat notice at dev.ts:664) so the operator sees the lockout is active.
  </action>
  <verify>
    <automated>npm test -- src/cli/dev-host/multiplayer-host.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - End-to-end host test: a MultiplayerHost built with `teachingDisabled: true`, dispatching a `server_request` for hint / heatmap-toggle / start-tutorial / demo-start, returns a `server_response` with `success:false` and the lockout message (the gameOptions bag + createDevSession adapter carry the flag to the Plan 02 guards). Use the real executeOp/SnapshotSessionHost, not a stub, so the cross-layer wiring is exercised.
    - The same host WITHOUT teachingDisabled processes those ops normally.
    - `npm test -- src/cli/dev-host/multiplayer-host.test.ts` green; `npx eslint src/cli/cli.ts src/cli/commands/dev.ts src/cli/dev-host/multiplayer-host.ts` clean.
  </acceptance_criteria>
  <done>--lock-teaching threads teachingDisabled into the executeOp gameOptions and the dev session; crafted ops rejected end-to-end through the running host.</done>
</task>

<task type="auto">
  <name>Task 2: Deliver teachingDisabled to the iframe via DevHostConfig + init postMessage</name>
  <read_first>
    - src/cli/dev-host/config-types.ts (DevHostConfig 20)
    - src/cli/commands/dev.ts (buildDevConfig 311-337)
    - src/cli/dev-host/DevHost.vue (init frames 154, 201, 224; how cfg is injected/read)
  </read_first>
  <action>
    Add `teachingDisabled?: boolean` to DevHostConfig. In buildDevConfig, accept teachingDisabled in its args and include it in the returned config; pass the devCommand-level `teachingDisabled` const into buildDevConfig at its call site. In DevHost.vue, add `teachingDisabled: cfg.teachingDisabled === true` to each `postToGame({ type: 'init', ... })` payload (lines 154/201/224) so every (re)init carries the flag. Do not change the broadcast path — broadcast already carries state.teachingDisabled (Plan 02) and is the authoritative source; this init delivery only covers first render (criterion 1).
  </action>
  <verify>
    <automated>npm test -- src/cli/dev-host/</automated>
  </verify>
  <acceptance_criteria>
    - DevHostConfig and buildDevConfig carry teachingDisabled from the CLI flag.
    - DevHost.vue init frames include `teachingDisabled` (assert in an existing or new DevHost test, or by reading the emitted init payload shape).
    - Default (no flag) config/init unchanged.
    - `npm test -- src/cli/dev-host/` green; `npx eslint src/cli/dev-host/config-types.ts src/cli/dev-host/DevHost.vue src/cli/commands/dev.ts` clean.
  </acceptance_criteria>
  <done>The dev host delivers teachingDisabled to the iframe init for first-render gating; defaults unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI operator → dev host → iframe seats | The --lock-teaching flag is the host policy input; dev-host seats are untrusted players who may craft ops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-10 | Tampering | dev-host op dispatch under --lock-teaching | mitigate | teachingDisabled threaded into the executeOp gameOptions bag (non-start + start) AND the SnapshotSessionHost adapters so all four Plan 02 guards fire on the real running host |
| T-111-11 | Spoofing | iframe trusts init flag only | accept | init delivery is first-render only; the authoritative gate is the broadcast flag (Plan 02 mergeTransientState) — covered by Plan 03 broadcast-preferred computed |

No package-manager installs in this plan — supply-chain (T-111-SC) is N/A.
</threat_model>

<verification>
- `npm test -- src/cli/dev-host/` passes (end-to-end host rejection + config/init delivery).
- Existing dev-host suites stay green (criterion 5).
- `npx eslint src/cli/cli.ts src/cli/commands/dev.ts src/cli/dev-host/multiplayer-host.ts src/cli/dev-host/config-types.ts src/cli/dev-host/DevHost.vue` clean.
</verification>

<success_criteria>
- `boardsmith dev --lock-teaching` arms the dev-host lockout end-to-end (D-04).
- Crafted ops rejected fail-loud through the real host (criterion 3, exercised live).
- teachingDisabled delivered to the iframe via init + reflected via broadcast (criteria 1, 4).
- Default behavior unchanged (criterion 5).
</success_criteria>

<output>
Create `.planning/phases/111-host-gated-teaching-lockout/111-04-SUMMARY.md` when done.
</output>
