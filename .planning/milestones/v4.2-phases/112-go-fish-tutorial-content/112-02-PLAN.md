---
phase: 112-go-fish-tutorial-content
plan: 02
type: execute
wave: 2
depends_on: ["112-01"]
files_modified:
  - /Users/jtsmith/BoardSmithGames/go-fish/src/rules/game.ts
  - /Users/jtsmith/BoardSmithGames/go-fish/src/rules/tutorial.ts
  - /Users/jtsmith/BoardSmithGames/go-fish/src/rules/index.ts
  - /Users/jtsmith/BoardSmithGames/go-fish/tests/tutorial-preset.test.ts
autonomous: true
requirements: [GFT-01, GFT-02, GFT-03, GFT-04, GFT-05]
must_haves:
  truths:
    - "A deterministic tutorial preset deals named cards: learner holds sevens + one queen, opponent holds the fourth seven"
    - "The ask-for-rank step is gated to rank '7' and opponent seat 2, with a card-name overlay anchor in the learner's hand"
    - "Four advanceWhen predicates (read game state only) cover ask-hit, turn-continuation, go-fish-draw, and book-formed beats"
    - "gameDefinition carries tutorial: GO_FISH_TUTORIAL so the ControlsMenu Start-tutorial entry lights up in GameShell and dev host"
  artifacts:
    - path: /Users/jtsmith/BoardSmithGames/go-fish/src/rules/game.ts
      provides: "resetToTutorialPreset() + tutorialSetup constructor option"
      contains: "resetToTutorialPreset"
    - path: /Users/jtsmith/BoardSmithGames/go-fish/src/rules/tutorial.ts
      provides: "GO_FISH_TUTORIAL TutorialDefinition with four beat steps"
      contains: "GO_FISH_TUTORIAL"
    - path: /Users/jtsmith/BoardSmithGames/go-fish/src/rules/index.ts
      provides: "tutorial wiring on gameDefinition + GO_FISH_TUTORIAL export"
      contains: "tutorial: GO_FISH_TUTORIAL"
    - path: /Users/jtsmith/BoardSmithGames/go-fish/tests/tutorial-preset.test.ts
      provides: "Deterministic preset deal + pond draw-order verification"
      contains: "tutorialSetup"
  key_links:
    - from: /Users/jtsmith/BoardSmithGames/go-fish/src/rules/tutorial.ts
      to: GoFishGame.resetToTutorialPreset
      via: "TutorialDefinition.setup()"
      pattern: "resetToTutorialPreset"
    - from: /Users/jtsmith/BoardSmithGames/go-fish/src/rules/index.ts
      to: GO_FISH_TUTORIAL
      via: "gameDefinition.tutorial attach"
      pattern: "tutorial: GO_FISH_TUTORIAL"
    - from: /Users/jtsmith/BoardSmithGames/go-fish/src/rules/tutorial.ts
      to: "card named '7H'"
      via: "step content target ref { name }"
      pattern: "name: '7"
---

<objective>
Author the go-fish tutorial content cross-repo in `~/BoardSmithGames/go-fish` (node_modules/boardsmith is symlinked to this repo, so the Wave 1 matcher fix is live — no re-vendor). Three artifacts: (1) a deterministic `resetToTutorialPreset()` on `GoFishGame` plus a `tutorialSetup` constructor path; (2) `GO_FISH_TUTORIAL: TutorialDefinition` with four teaching beats; (3) one-line wiring on `gameDefinition` that lights up the v4.1 launch surface.

Purpose: Prove the v4.1 tutorial substrate generalizes from a grid board (checkers) to a hidden-information card game. This is the parity path: checkers anchors overlays on board-square `notation`; go-fish anchors on card `name`. Four beats — ask-for-rank gating (GFT-01), Go-Fish-draw tip (GFT-02), forming a book (GFT-03), turn continuation (GFT-04) — authored declaratively, launchable in both hosts (GFT-05).

Output: `game.ts` (preset + option), `tutorial.ts` (new), `index.ts` (wiring + export), `tutorial-preset.test.ts` (deal + pond-order proof). BoardSmith vitest stays green throughout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/112-go-fish-tutorial-content/112-CONTEXT.md
@.planning/phases/112-go-fish-tutorial-content/112-RESEARCH.md

<interfaces>
<!-- go-fish substrate the executor builds against. Verified by research; do NOT re-derive. -->
<!-- Card naming: GoFishGame.createDeck() names cards `${rank}${suit}` → '7H','7D','7C','7S','QH'. Stable, collision-free. -->
<!-- bookCount: serializable number = 0 on GoFishPlayer (elements.ts); incremented ONLY by checkForBooks(). -->
<!-- ask action (actions.ts): chooseFrom('target', game.playerChoices({excludeSelf:true})) → { value:seat, display }[]; chooseFrom('rank', getPlayerRanks()) → string[]. -->
<!-- flow.ts line 77: extraTurn read from ctx.lastActionResult?.data?.extraTurn to loop/end turn. -->
<!-- Accessors on GoFishGame: getPlayer(seat), getPlayerHand(player), getPlayerBooks(player), getPlayerRanks(player), pond (a Deck space, setOrder('stacking')). -->

<!-- CRITICAL: TutorialGateContext = { game, seat } ONLY. There is NO lastActionResult in advanceWhen. -->
<!-- ALL advanceWhen predicates MUST read game state (hand count, pond count, bookCount). Do NOT reference ctx.lastActionResult. -->

<!-- Matcher (live from Wave 1): gate.selections rank uses { value: '7' } (primitive); target uses { value: 2 } (object field equality). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic tutorial preset on GoFishGame</name>
  <files>/Users/jtsmith/BoardSmithGames/go-fish/src/rules/game.ts, /Users/jtsmith/BoardSmithGames/go-fish/tests/tutorial-preset.test.ts</files>
  <read_first>
    - /Users/jtsmith/BoardSmithGames/go-fish/src/rules/game.ts (the file under change: constructor, createDeck(), shuffle/deal, checkForBooks(), getPlayerHand/getPlayerBooks/getPlayerRanks, pond)
    - /Users/jtsmith/BoardSmithGames/go-fish/src/rules/elements.ts (Card has id/rank/suit; GoFishPlayer.bookCount; Hand/Books spaces)
    - /Users/jtsmith/BoardSmithGames/checkers/src/rules/game.ts (lines ~139-173: resetToTutorialPreset() / placeTutorialPieces() — the template to mirror)
    - /Users/jtsmith/BoardSmithGames/checkers/tests/tutorial-preset.test.ts (the preset-verification test template to mirror)
    - .planning/phases/112-go-fish-tutorial-content/112-RESEARCH.md (Q3 preset design — Option A preferred, Option B fallback; constructor passthrough)
  </read_first>
  <action>
    Add a `tutorialSetup?: boolean` field to `GoFishOptions` (game.ts) and branch the constructor: when `options.tutorialSetup` is true, call `resetToTutorialPreset()` instead of `pond.shuffle()` + `dealCards()`. Implement `resetToTutorialPreset()` to deal scripted NAMED cards via `this.first(Card, '<name>')!.putInto(...)` (mirror checkers' `placeTutorialPieces()`): first return any hand/book cards to the pond and zero each player's bookCount, then deal Option A — learner (seat 1) hand = ['7H','7D','QH'], opponent (seat 2) hand = ['7S'], and put '7C' into the pond LAST so it is the next-drawn card. Then write `tests/tutorial-preset.test.ts` (mirror checkers') that creates `TestGame.create(GoFishGame, { playerCount: 2, playerNames: ['Learner','Opponent'], seed: 'go-fish-tutorial-pinned', tutorialSetup: true })` and asserts: learner hand contains exactly 7H/7D/QH, opponent hand contains 7S, and a single `pond.drawTo(tmpHand, 1, Card)` returns the card named '7C'. If the draw-order assertion fails (pond stacking does not surface '7C' first — RESEARCH Pitfall 5 / Assumption A1), FALL BACK to Option B: learner hand = ['7H','7D','7C','QH'], opponent = ['7S'], no pond-order dependency; update the preset test's expectations to Option B accordingly and record the choice in the SUMMARY. Pick exactly one option and make the preset test deterministic under the pinned seed. Do NOT use dummy data or a non-deterministic shuffle in the preset (CLAUDE.md: no fallbacks/hacks).
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmithGames/go-fish && npm test -- tutorial-preset</automated>
  </verify>
  <acceptance_criteria>
    - `game.ts` GoFishOptions has `tutorialSetup?: boolean`; constructor calls `resetToTutorialPreset()` only when set, else shuffles+deals
    - `resetToTutorialPreset()` deals named cards by `first(Card, name)` and zeroes bookCount; no random shuffle in this path
    - `tests/tutorial-preset.test.ts` proves the learner/opponent hands AND the first pond draw identity (Option A: '7C'; or Option B documented if fallback taken)
    - `cd /Users/jtsmith/BoardSmithGames/go-fish && npm test -- tutorial-preset` passes
    - SUMMARY records whether Option A or Option B was used and why
  </acceptance_criteria>
  <done>Deterministic named-card preset deals reproducibly under the pinned seed and the preset test (incl. pond draw order) is green.</done>
</task>

<task type="auto">
  <name>Task 2: GO_FISH_TUTORIAL definition + gameDefinition wiring</name>
  <files>/Users/jtsmith/BoardSmithGames/go-fish/src/rules/tutorial.ts, /Users/jtsmith/BoardSmithGames/go-fish/src/rules/index.ts</files>
  <read_first>
    - /Users/jtsmith/BoardSmithGames/go-fish/src/rules/index.ts (current gameDefinition shape — no tutorial field yet; add export + attach)
    - /Users/jtsmith/BoardSmithGames/go-fish/src/rules/actions.ts (ask action: selection names 'target' and 'rank', value shapes)
    - /Users/jtsmith/BoardSmithGames/checkers/src/rules/tutorial.ts (CHECKERS_TUTORIAL — the full template: setup, steps, gate.selections, suppressAutoFill, content target ref, advanceWhen)
    - /Users/jtsmith/BoardSmithGames/checkers/src/rules/index.ts (how CHECKERS_TUTORIAL is exported and attached to gameDefinition.tutorial)
    - .planning/phases/112-go-fish-tutorial-content/112-RESEARCH.md (Q4 beat→predicate table, Q5 launch wiring, Code Examples skeleton, Pitfalls 1-4)
  </read_first>
  <action>
    Create `src/rules/tutorial.ts` exporting `GO_FISH_TUTORIAL: TutorialDefinition` with `setup: (game) => (game as GoFishGame).resetToTutorialPreset()` and four steps (ids: `ask-for-rank`, `turn-continuation`, `go-fish-tip`, `book-formed`). Step `ask-for-rank`: set `suppressAutoFill: true` (RESEARCH Pitfall 3), `gate: { action: 'ask', selections: { target: { value: 2 }, rank: { value: '7' } } }` (target is object field-equality; rank is the primitive `{ value }` form enabled by Wave 1), and a `content` entry whose `target: { kind: 'element', ref: { name: '7H' } }` anchors the overlay to the learner's held seven with `placement: 'top'` and text pointing at the opponent. Define `const TUTORIAL_PRESET_POND_SIZE` matching the chosen preset (Option A: 48; Option B: 47 — match Task 1's actual remaining pond size). Author the four `advanceWhen` predicates reading GAME STATE ONLY (never ctx.lastActionResult, RESEARCH Pitfall 1) per the Q4 table: ask-hit via learner hand count grew, turn-continuation/go-fish-tip via `pond.count(Card) < TUTORIAL_PRESET_POND_SIZE`, book-formed via `(getPlayer(seat) as GoFishPlayer).bookCount > 0`. If Option B was chosen in Task 1, use the Option-B step ordering/conditions from RESEARCH Q4 so all four ids still land in stepsVisited. Then edit `src/rules/index.ts`: `import { GO_FISH_TUTORIAL } from './tutorial.js'`, add `tutorial: GO_FISH_TUTORIAL` to `gameDefinition`, and re-export `GO_FISH_TUTORIAL` (and ensure `GoFishGame` is exported) for the Wave 3 test import. No code fences belong in the action — name the ids, selection matchers, anchor name, and predicate state reads exactly as above.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmithGames/go-fish && npx tsc --noEmit && npm test -- tutorial-preset</automated>
  </verify>
  <acceptance_criteria>
    - `src/rules/tutorial.ts` exports `GO_FISH_TUTORIAL` with exactly four steps whose ids are `ask-for-rank`, `turn-continuation`, `go-fish-tip`, `book-formed`
    - `ask-for-rank` has `suppressAutoFill: true`, `gate.selections.rank = { value: '7' }`, `gate.selections.target = { value: 2 }`, and a content target `ref: { name: '7H' }` (or the actual learner seven name) with `placement: 'top'`
    - No advanceWhen predicate references `ctx.lastActionResult` (grep: `grep -n "lastActionResult" src/rules/tutorial.ts` returns nothing)
    - `src/rules/index.ts` contains `tutorial: GO_FISH_TUTORIAL` on gameDefinition and re-exports both `GO_FISH_TUTORIAL` and `GoFishGame`
    - `npx tsc --noEmit` passes and `npm test -- tutorial-preset` still green
  </acceptance_criteria>
  <done>GO_FISH_TUTORIAL authored with four beats, gated ask, card-name overlay anchor, game-state-only predicates, and attached to gameDefinition; typecheck and preset test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Tutorial content is authored TypeScript constants compiled into the trusted game bundle; predicate functions run in the engine's synchronous trusted path. No untrusted input crosses a new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-02 | Tampering | advanceWhen predicates reading game state | accept | Predicates are read-only on `game` (TutorialGateContext is read-only by contract); no mutation, no external input. Low risk. |
| T-112-SC | Tampering | npm/pip/cargo installs | accept | No package installs (boardsmith is symlinked, already present in go-fish package.json); nothing to verify. |
</threat_model>

<verification>
- `cd /Users/jtsmith/BoardSmithGames/go-fish && npm test -- tutorial-preset` — preset deal + pond order green.
- `cd /Users/jtsmith/BoardSmithGames/go-fish && npx tsc --noEmit` — tutorial.ts + index.ts typecheck clean.
- `cd /Users/jtsmith/BoardSmith && npm test` — BoardSmith suite stays green (symlink sanity; no src change here).
</verification>

<success_criteria>
- Deterministic named-card preset with verified pond draw order (or documented Option-B fallback).
- GO_FISH_TUTORIAL with four beats, gated ask-for-rank, card-name overlay anchor, game-state-only predicates.
- `tutorial: GO_FISH_TUTORIAL` on gameDefinition — launch surface wired for GameShell + dev host (GFT-05; visually confirmed in 112-04).
</success_criteria>

<output>
Create `.planning/phases/112-go-fish-tutorial-content/112-02-SUMMARY.md` when done. Record Option A vs B and the exact `TUTORIAL_PRESET_POND_SIZE`.
</output>
