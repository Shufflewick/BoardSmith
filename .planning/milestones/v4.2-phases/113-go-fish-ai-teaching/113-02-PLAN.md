---
phase: 113-go-fish-ai-teaching
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "~/BoardSmithGames/go-fish/tests/demo.test.ts"
autonomous: true
requirements: [GFAI-02]
must_haves:
  truths:
    - "An AI-vs-AI go-fish demo narrates each move (announcement text present) BEFORE that move executes"
    - "The narrated move's args are the SAME args that execute (no narrate/execute mismatch)"
    - "demoStop halts the demo with no orphaned state: demoRunning=false, narration cleared, no pending timers"
    - "The demo is fully substrate-driven — no new go-fish runtime code or timers are added"
  artifacts:
    - path: "~/BoardSmithGames/go-fish/tests/demo.test.ts"
      provides: "AI-vs-AI narrated demo integration test exercising narrate-before-execute + clean stop"
      contains: "demoStart"
  key_links:
    - from: "SnapshotSessionHost demoStart/demoStop"
      to: "executeOp(goFishGameDefinition, ...)"
      via: "boardsmith/session exports (runDemoLoop narrate→delay→execute)"
      pattern: "demoStart"
---

<objective>
Deliver GFAI-02 (narrated AI-vs-AI demo) for go-fish by proving the v4.1 demo substrate drives a real go-fish game correctly: each move is announced in the narration before it executes, and stopping mid-demo leaves no orphaned state. This is substrate-driven — `runDemoLoop` in `SnapshotSessionHost` (aiSuggest preview → narrate/broadcast → delay → execute-same-args, with `demoAbort` checked before AND after the delay) is game-agnostic and go-fish's MCTS is already attached. No new go-fish runtime code or timers.

Purpose: Prove parity for a card game with an honest integration test, not a vacuous assertion. The test wires `SnapshotSessionHost` to go-fish's real `gameDefinition` via the exported `executeOp`, starts the demo, and asserts the narrate-before-execute ordering and a clean `demoStop` (`demoRunning=false`, narration cleared, `vi.getTimerCount() === 0`) — the same correctness contract as v4.1's demo + the CLAUDE.md no-timer-leak / no-orphaned-state hard rule.

Output: `~/BoardSmithGames/go-fish/tests/demo.test.ts`. No go-fish `src/` changes (the optional per-game `narrateMove` hook is added ONLY if the substrate exposes it as a one-liner AND default narration is unreadable — otherwise keep the engine-derived default).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-go-fish-ai-teaching/113-RESEARCH.md
@.planning/phases/113-go-fish-ai-teaching/113-CONTEXT.md
@.planning/phases/113-go-fish-ai-teaching/113-VALIDATION.md

<interfaces>
<!-- Substrate the test drives — all from boardsmith/session (symlinked, no re-vendor). -->

From boardsmith/session (src/session/snapshot-session-host.ts + stateless-ops.ts):
```typescript
class SnapshotSessionHost {
  constructor(adapters: SnapshotSessionAdapters);
  start(): Promise<...>;
  handleOp(seat: number, op: Op): Promise<...>;   // op: { type:'demoStart', delay?:number } | { type:'demoStop' }
  // observable demo state broadcast per-seat: state.isDemoRunning, state.narration?.text
}
interface SnapshotSessionAdapters {
  playerCount: number;
  executeOp: (snapshot, pendingState, op) => Promise<OpResult>;
  broadcast: (views, meta) => void;     // capture views to assert narration ordering
  aiSeats: Array<{ seat: number; level?: string }>;
}
// executeOp(gameDef, options, snapshot, pendingState, op) — real op execution for a gameDefinition
import { executeOp, type GameDefinitionLike, type Op, type OpResult } from 'boardsmith/session';
import { SnapshotSessionHost, type SnapshotSessionAdapters } from 'boardsmith/session';
```

runDemoLoop ordering (the contract under test), per iteration:
  1. aiSuggest preview (read-only) → suggestedAction/suggestedArgs/aiPlayer
  2. demoAbort check (after async suggest)
  3. narrationText = buildNarration(...) → broadcastCurrent()   ← announcement BEFORE execute
  4. delay (demoPaceOrPause); demoAbort checked again after
  5. execute the SAME suggestedArgs via the 'action' op
  demoStop sets demoAbort and clears narration immediately; finally block broadcasts demoRunning=false. No timer survives stop.

go-fish gameDefinition: `import { gameDefinition } from '../src/rules/index.js'` — gameType 'go-fish', `ai.objectives` attached, 2..6 players. The ask BotMove is `{ action:'ask', args:{ target, rank } }`.

Substrate test reference (mirror its harness design): BoardSmith `src/session/snapshot-session-host.test.ts` — `describe('demo loop (demoStart / demoStop / runDemoLoop)')`, `makeDemoAdapters` (stubs aiSuggest to a deterministic canned move, real executeOp for the 'action' op), `broadcastState(broadcastLog, idx, seat)` helper, and the timer-leak assertion `vi.getTimerCount() === 0`. DESIGN NOTE there: stub aiSuggest (avoid MCTS setImmediate vs fake-timers conflict) while running the REAL 'action' op — this still exercises narrate-before-execute on a genuine go-fish move.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: go-fish AI-vs-AI narrated demo integration test (narrate-before-execute + clean stop)</name>
  <files>~/BoardSmithGames/go-fish/tests/demo.test.ts</files>
  <read_first>
    - /Users/jtsmith/BoardSmith/src/session/snapshot-session-host.test.ts (the `demo loop` describe block ~:993: `makeDemoAdapters`, `broadcastState`, the narrate-before-execute assertions, and the `vi.getTimerCount() === 0` timer-leak check — mirror this harness)
    - /Users/jtsmith/BoardSmith/src/session/snapshot-session-host.ts (runDemoLoop ~:436 — the narrate→delay→execute ordering and demoAbort-before-and-after-delay)
    - /Users/jtsmith/BoardSmith/src/session/stateless-ops.ts (executeOp + GameDefinitionLike signature; the aiSuggest and 'action' op shapes)
    - ~/BoardSmithGames/go-fish/src/rules/index.ts (gameDefinition to feed executeOp; gameType 'go-fish')
    - ~/BoardSmithGames/go-fish/src/rules/actions.ts (the ask move args `{ target, rank }` the demo narrates/executes)
    - .planning/phases/113-go-fish-ai-teaching/113-RESEARCH.md (Validation Architecture — GFAI-02 integration; Pitfall 3 demo-abort)
  </read_first>
  <action>Create `tests/demo.test.ts`. Import `SnapshotSessionHost`, `executeOp`, types `Op`/`OpResult`/`GameDefinitionLike` from `boardsmith/session`, and `gameDefinition` from `../src/rules/index.js`. Build adapters mirroring the substrate test's `makeDemoAdapters`: delegate the real `'action'` op (and `start`) to `executeOp(gameDefinition, options, snap, pend, op)` so genuine go-fish state advances, and stub the `'aiSuggest'` op to return a deterministic legal go-fish ask (`suggestedAction:'ask'`, `suggestedArgs:{ target, rank }`, `aiPlayer`) — this avoids MCTS's `setImmediate` conflicting with `vi.useFakeTimers()` while still narrating + executing a REAL go-fish ask. Capture broadcasts into a log via the `broadcast` adapter. Use a 2-player go-fish setup with `aiSeats` for both seats and a tutorial-style or default preset whose seat-1 hand makes the canned ask legal (so the real 'action' op succeeds). Assert: (1) the broadcast carrying `state.narration.text` for the ask appears at a STRICTLY EARLIER index than the broadcast reflecting the executed move's state change (narrate-before-execute); (2) the narrated args equal the executed args (no mismatch — both come from the same `suggestedArgs`); (3) after `handleOp(seat, { type:'demoStop' })` + draining timers, the latest broadcast has `state.isDemoRunning===false`, `state.narration` undefined, and `vi.getTimerCount() === 0` (no orphaned timer/state). Add a top-of-file comment documenting that the demo is substrate-driven (no go-fish runtime code/timers). Do NOT add fake fallbacks — if the canned ask cannot be made legal against the chosen preset, fix the preset/hand setup, do not stub the action op.</action>
  <verify>
    <automated>cd ~/BoardSmithGames/go-fish && npm test -- demo</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- demo` (go-fish) passes
    - The test asserts a narration broadcast index STRICTLY LESS THAN the executed-state broadcast index (genuine narrate-before-execute, not a tautology)
    - The test asserts narrated args === executed args
    - The test asserts post-demoStop: `isDemoRunning===false`, narration cleared, and `vi.getTimerCount() === 0`
    - No go-fish `src/` files modified (test-only); if a per-game `narrateMove` hook is added it is a one-liner justified in the SUMMARY
    - go-fish full suite green (`cd ~/BoardSmithGames/go-fish && npm test`)
  </acceptance_criteria>
  <done>An honest integration test drives the go-fish AI-vs-AI demo through the real substrate, proving each ask is announced before it executes and that stopping mid-demo leaves demoRunning=false, narration cleared, and zero pending timers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only plan. The demo ops (`demoStart`/`demoStop`) are host-lifecycle ops in the v4.1 substrate, already gated by `teachingDisabled`. No new code path or untrusted input is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-03 | Denial of service | runDemoLoop timer cleanup | mitigate | Test asserts `vi.getTimerCount() === 0` after demoStop — enforces the CLAUDE.md no-timer-leak rule for the go-fish demo path. |
| T-113-04 | Repudiation/Integrity | narrate vs execute | mitigate | Test asserts narrated args === executed args (same `suggestedArgs`), preventing a narration that lies about the move. |
| T-113-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies; `SnapshotSessionHost`/`executeOp` are existing `boardsmith/session` exports via the symlink. |
</threat_model>

<verification>
- `cd ~/BoardSmithGames/go-fish && npm test -- demo` green.
- `cd ~/BoardSmithGames/go-fish && npm test` full suite green.
- `cd /Users/jtsmith/BoardSmith && npm test` stays green (no BoardSmith `src/` changes expected).
</verification>

<success_criteria>
- GFAI-02 proven by an honest integration test: narrate-before-execute on a real go-fish ask + clean demoStop (no orphaned state/timers).
- Demo is substrate-driven; no new go-fish runtime timers; any optional `narrateMove` hook is a justified one-liner.
</success_criteria>

<output>
Create `.planning/phases/113-go-fish-ai-teaching/113-02-SUMMARY.md` when done.
</output>
