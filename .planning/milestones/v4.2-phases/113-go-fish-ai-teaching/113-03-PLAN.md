---
phase: 113-go-fish-ai-teaching
plan: 03
type: execute
wave: 2
depends_on: ["113-01", "113-02"]
files_modified: []
autonomous: false
requirements: [GFAI-01, GFAI-02]
must_haves:
  truths:
    - "On the learner's turn, Get a hint highlights the learner's cards of the suggested rank with a ring — on the rank card-group in hand, NOT a board cell and NOT a floating bubble"
    - "Watch AI demo announces each ask in the narration card BEFORE it executes"
    - "Stopping the demo mid-game leaves a clean board (no orphaned narration, no stuck demo state)"
    - "The hint clears on the next player action (R-06, substrate-owned)"
    - "The boardsmith dev server is killed after verification — never left running"
  artifacts: []
  key_links:
    - from: "ControlsMenu Get a hint / Watch AI demo"
      to: "card-group [data-bs-el-name=<rank>] ring + narration card"
      via: "v4.1 hint/demo substrate + 113-01 hook/anchor + 113-02 substrate demo"
      pattern: "data-bs-el-name"
---

<objective>
Visually confirm GFAI-01 and GFAI-02 in the browser, the CLAUDE.md hard rule that features are verified by running the app, not just passing tests. The automation landed in 113-01 (hint hook + rank-group anchor) and is substrate-driven for the demo (113-02 proved the loop). This checkpoint proves the highest-parity item of the milestone: the hint ring lands on the learner's RANK CARDS in a custom card UI (not a board cell, not floating), and a narrated AI-vs-AI demo announces each ask before it executes.

Purpose: The unit/integration tests prove logic and DOM-anchor agreement; only the browser proves the ring actually resolves to the card-group rect and that the narration card reads correctly during a live demo. If the ring does NOT anchor to the rank cards, that is a flagged finding (likely a go-fish custom-UI anchor gap like Phase 112, or — only if proven — a BoardSmith substrate gap to fix in BoardSmith with a test). No go-fish workaround.

Output: Human-verified confirmation (or a logged finding). No code changes in the happy path. The dev server MUST be killed afterward.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-go-fish-ai-teaching/113-VALIDATION.md
@.planning/phases/113-go-fish-ai-teaching/113-RESEARCH.md
@.planning/phases/112-go-fish-tutorial-content/112-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Browser verify — hint ring on rank cards + narrated AI-vs-AI demo</name>
  <files>(verification only — no files modified; automation done in 113-01 / substrate-driven for 113-02)</files>
  <read_first>
    - .planning/phases/113-go-fish-ai-teaching/113-VALIDATION.md (Manual-Only Verifications — the exact dev-host steps)
    - .planning/phases/113-go-fish-ai-teaching/113-RESEARCH.md (Pitfall 1 rank-group anchor; Pitfall 4 hint only meaningful on the learner's turn)
    - .planning/phases/112-go-fish-tutorial-content/112-04-SUMMARY.md (the custom-UI anchorAttrs gap pattern — what a missing-anchor failure looks like and how it was fixed in go-fish, not BoardSmith)
  </read_first>
  <what-built>
    113-01 added `getGoFishHintTarget` (ask → `{ name: rank }`), wired it into `gameDefinition.ai.hintTargetFromMove`, and bound `anchorAttrs({ name: rank })` on the learner's rank `card-group` div (emits `data-bs-el-name="<rank>"`). The v4.1 substrate (`ControlsMenu` "Get a hint" / "Watch AI demo", `game-session.requestHint`, `HintOverlay`, `runDemoLoop` narration) lights up automatically because `gameDefinition.ai` is present. 113-02 proved the demo loop narrates-before-executes and stops cleanly. This checkpoint confirms both live in the dev host.
  </what-built>
  <how-to-verify>
    1. Run `cd /Users/jtsmith/BoardSmithGames/go-fish && npx boardsmith dev` (serves on http://localhost:5173).
    2. Open the game in the browser (use the Claude Code Chrome extension per CLAUDE.md).
    3. HINT (GFAI-01): Make it the learner seat's turn (use "Follow active seat" so you drive whichever seat is awaiting input). Open ControlsMenu and click "Get a hint". Confirm a ring highlights the learner's cards of the suggested rank in their OWN hand (the rank card-group), NOT a board cell and NOT a floating unanchored bubble. Note the suggested rank in the hint bubble and confirm the ring sits on that rank's cards.
    4. R-06: Perform the next action (e.g. complete the suggested ask). Confirm the hint ring clears on that action.
    5. DEMO (GFAI-02): Click "Watch AI demo". Confirm each ask is announced in the narration card (e.g. "Player N: ask …") BEFORE the move executes — narration appears, then the cards move. Let a few moves play.
    6. STOP: Click "Stop demo" mid-game. Confirm the board returns to a clean state — no lingering narration card, no stuck "demo running" indicator.
    7. KILL the dev server (Ctrl-C) before finishing — never leave `boardsmith dev` running (CLAUDE.md hard rule + project memory).
    If the hint ring does NOT anchor to the rank cards: STOP and report it. First suspect a go-fish custom-UI anchor gap (as in Phase 112). Only if you PROVE the anchor is correct and the substrate still fails to resolve it, log a BoardSmith substrate-gap finding to fix in BoardSmith with a test — never fake the anchor in go-fish.
  </how-to-verify>
  <acceptance_criteria>
    - Hint ring visibly anchors to the learner's rank cards in hand (not a board cell, not floating) — GFAI-01
    - Hint clears on the next action — R-06
    - Narrated AI-vs-AI demo announces each ask before it executes — GFAI-02
    - Stopping the demo mid-game leaves a clean board (no orphaned narration / demo state)
    - Dev server killed after verification (port 5173 free)
    - Any anchoring failure is recorded as a finding (go-fish custom-UI gap, or proven BoardSmith substrate gap) — not worked around
  </acceptance_criteria>
  <resume-signal>Type "approved" if the hint ring lands on the rank cards, the hint clears on the next action, and the narrated demo announces each ask before executing and stops clean; otherwise describe what you saw (especially any anchoring or narration-ordering failure).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only; no code change in the happy path. Teaching ops remain substrate-gated (`teachingDisabled`, `requestHint` fail-loud off-turn). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-05 | Denial of service | leftover dev server | mitigate | Acceptance criteria require killing `boardsmith dev` (port 5173 free) — enforces the CLAUDE.md no-orphaned-process rule. |
| T-113-SC | Tampering | npm/pip/cargo installs | mitigate | No installs; verification only. |
</threat_model>

<verification>
- Human confirms hint ring on rank cards + R-06 clear + narrated demo announce-before-execute + clean stop in the `boardsmith dev` host.
- Dev server killed (port 5173 free) before the plan completes.
</verification>

<success_criteria>
- GFAI-01 and GFAI-02 visually confirmed in the browser; the card-game `anchorAttrs` parity (ring on cards, not a board cell) is proven.
- No code changes in the happy path; any gap is flagged + fixed in the correct repo (go-fish custom UI vs proven BoardSmith substrate), never worked around.
</success_criteria>

<output>
Create `.planning/phases/113-go-fish-ai-teaching/113-03-SUMMARY.md` when done.
</output>
