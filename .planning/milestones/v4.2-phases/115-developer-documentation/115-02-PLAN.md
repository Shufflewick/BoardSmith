---
phase: 115-developer-documentation
plan: 02
type: execute
wave: 2
depends_on: [115-01]
files_modified:
  - docs/teaching-and-tutorials.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-04]

must_haves:
  truths:
    - "Every API name, type, method, and flag cited in the guide exists in the live source (BoardSmith src/, checkers, go-fish) with matching signatures — no invented API, no drift"
    - "Every code excerpt in the guide matches its real source file"
    - "The guide demonstrably covers DOC-01, DOC-02, DOC-03, and DOC-04"
    - "docs/README.md links the guide"
    - "BoardSmith npm test stays green (docs-only change, no src/ regressions)"
  artifacts:
    - path: "docs/teaching-and-tutorials.md"
      provides: "Verified, drift-free teaching & tutorials guide"
      contains: "anchorAttrs"
  key_links:
    - from: "docs/teaching-and-tutorials.md cited symbols"
      to: "live source (src/, checkers, go-fish)"
      via: "per-claim doc-verifier accuracy check"
      pattern: "(TutorialDefinition|simulateTutorial|teachingDisabled|anchorAttrs)"
---

<objective>
Verify the accuracy and coverage of `docs/teaching-and-tutorials.md` produced by Plan 01. Read the guide alongside every source file it cites and confirm — per claim — that every named symbol/type/method/flag and every code excerpt exists in the live source with matching signatures. Fix any mismatch in the guide (docs only). Confirm DOC-01..04 coverage, the README link, and that the BoardSmith suite stays green.

Purpose: Drift is the #1 risk for documentation. This plan is the factual-accuracy gate that guarantees the guide does not invent API or carry drifted signatures — the integrity guard for the milestone's capstone doc.

Output: A verified docs/teaching-and-tutorials.md with any mismatches corrected; a per-claim PASS/MISMATCH record in the SUMMARY. NO `src/` or game-code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/115-developer-documentation/115-CONTEXT.md
@.planning/phases/115-developer-documentation/115-RESEARCH.md
@.planning/phases/115-developer-documentation/115-VALIDATION.md

# The artifact under verification:
@docs/teaching-and-tutorials.md
@docs/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-claim accuracy verification against live source; fix any drift</name>
  <files>docs/teaching-and-tutorials.md</files>
  <read_first>
    - docs/teaching-and-tutorials.md (the guide under verification — extract every cited symbol/type/method/flag and every code excerpt)
    - src/engine/tutorial/types.ts (TutorialDefinition, TutorialStep, TutorialGateAllowList, Annotation, AnnotationTarget)
    - src/engine/tutorial/gate.ts (selectionMatchesValue, getGateReasonForValue)
    - src/engine/tutorial/predicates.ts (advanceWhen predicate forms)
    - src/session/game-session.ts (startTutorial, exitTutorial, requestHint, setHeatmapVisible, demoStart/demoStop, teachingDisabled + thrown message)
    - src/ui/composables/useBoardInteraction.ts (anchorAttrs and the data-bs-el-* attribute names)
    - src/ui/components/helpers/{overlay-utils.ts,TutorialOverlay.vue,HintOverlay.vue,ActionHelpPopover.vue}
    - src/engine/action/{types.ts,action-builder.ts} (ActionDefinition.help, .help() builder, ActionMetadata.help)
    - src/testing/{simulate-tutorial.ts,tutorial-assertions.ts,index.ts,tutorial-ci-demo.test.ts} (simulateTutorial, assertTutorialCompletes, stepsVisited, TestGame.create tutorialSetup)
    - src/cli/commands/dev.ts, src/cli/cli.ts, src/cli/dev-host/config-types.ts (--lock-teaching)
    - ~/BoardSmithGames/checkers/src/rules/{tutorial.ts,actions.ts} and ~/BoardSmithGames/checkers/tests/tutorial.test.ts
    - ~/BoardSmithGames/go-fish/src/rules/{tutorial.ts,actions.ts,ai.ts} and ~/BoardSmithGames/go-fish/src/ui/components/GameTable.vue and ~/BoardSmithGames/go-fish/tests/{tutorial.test.ts,demo.test.ts,action-help.test.ts,host-lockout.test.ts}
  </read_first>
  <action>
    Extract every code-referencing claim from docs/teaching-and-tutorials.md: each named symbol (TutorialDefinition, TutorialStep, TutorialGateAllowList, AnnotationTarget, selectionMatchesValue, startTutorial, exitTutorial, requestHint, hintTargetFromMove, getGoFishHintTarget, setHeatmapVisible, demoStart, demoStop, teachingDisabled, anchorAttrs, buildSelector, .help, ActionMetadata.help, simulateTutorial, assertTutorialCompletes, stepsVisited, CHECKERS_TUTORIAL, GO_FISH_TUTORIAL, --lock-teaching), each cited type/method/flag signature, each fenced code excerpt, and each file reference. For each, open the corresponding live source file (listed in read_first) and confirm the symbol exists, the signature matches, the excerpt is faithful, and the file path is correct. Record a per-claim PASS/MISMATCH list. For every MISMATCH, correct the guide to match the live source (rename a drifted symbol, fix a signature, repair an excerpt, fix a file reference). Verify the exact teachingDisabled thrown message string ("Teaching features are disabled for this session.") and the data-bs-el-* attribute names match useBoardInteraction.ts exactly. Do NOT modify any src/ or game source — only the markdown guide. If a cited symbol genuinely does not exist anywhere (true invented API), remove or replace the claim with the real equivalent from the source.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && for s in TutorialDefinition selectionMatchesValue startTutorial requestHint setHeatmapVisible teachingDisabled simulateTutorial assertTutorialCompletes; do grep -rq "$s" src/ || { echo "MISSING IN SRC: $s"; exit 1; }; done && grep -rq "anchorAttrs" src/ui/composables/useBoardInteraction.ts && grep -rq "data-bs-el" src/ui/composables/useBoardInteraction.ts && grep -q "CHECKERS_TUTORIAL" ~/BoardSmithGames/checkers/src/rules/tutorial.ts && grep -q "GO_FISH_TUTORIAL" ~/BoardSmithGames/go-fish/src/rules/tutorial.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - A per-claim PASS/MISMATCH record exists in the SUMMARY covering every cited symbol/excerpt/file-reference.
    - Every symbol the guide cites from BoardSmith resolves in src/ (the verify gate proves the core set; the SUMMARY proves the full set).
    - anchorAttrs and data-bs-el-* are confirmed in src/ui/composables/useBoardInteraction.ts.
    - CHECKERS_TUTORIAL resolves in the checkers source and GO_FISH_TUTORIAL in the go-fish source.
    - The teachingDisabled thrown-message string in the guide matches game-session.ts exactly.
    - Any mismatch found is corrected in the guide; no src/ or game-code changes were made.
  </acceptance_criteria>
  <done>Every cited claim verified against live source; all mismatches corrected; per-claim record in SUMMARY; verify gate passes.</done>
</task>

<task type="auto">
  <name>Task 2: Coverage + README-link + no-regression confirmation</name>
  <files>docs/teaching-and-tutorials.md</files>
  <read_first>
    - docs/teaching-and-tutorials.md (confirm DOC-01..04 concept coverage)
    - docs/README.md (confirm the guide link)
    - .planning/phases/115-developer-documentation/115-VALIDATION.md (the coverage/accuracy sign-off checklist)
  </read_first>
  <action>
    Confirm DOC-01..04 coverage with concept greps against docs/teaching-and-tutorials.md: DOC-01 (TutorialDefinition, lifecycle start/advance/skip/exit, gating, AnnotationTarget cell/piece/card/panel/action); DOC-02 (predicate triggers / advanceWhen, simulateTutorial, assertTutorialCompletes, the green->red rule-break demo); DOC-03 (requestHint, narrated demo, heatmap labeled board-only, teachingDisabled, .help()); DOC-04 (CHECKERS_TUTORIAL, GO_FISH_TUTORIAL, anchorAttrs, notation vs name parity, data-bs-el). Confirm docs/README.md links ./teaching-and-tutorials.md. Run the BoardSmith suite to confirm no regressions (docs-only change — must stay green). If any coverage grep fails, the guide is missing a required concept — flag it back (Plan 01 must add it); do not invent content here beyond what the live source supports.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && grep -q "teaching-and-tutorials.md" docs/README.md && for c in TutorialDefinition advanceWhen simulateTutorial assertTutorialCompletes requestHint heatmap board-only teachingDisabled "\.help(" anchorAttrs CHECKERS_TUTORIAL GO_FISH_TUTORIAL data-bs-el; do grep -qi "$c" docs/teaching-and-tutorials.md || { echo "MISSING CONCEPT: $c"; exit 1; }; done && npm test && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All DOC-01..04 concept greps pass against docs/teaching-and-tutorials.md.
    - docs/README.md links ./teaching-and-tutorials.md.
    - `npm test` is green (no src/ regressions; docs-only change).
    - 115-VALIDATION.md sign-off boxes are satisfiable (coverage + accuracy + README link + suite green).
  </acceptance_criteria>
  <done>Coverage greps pass, README link present, BoardSmith suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Verification of a documentation artifact: no executable code, no external input, no package installs, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-02 | Tampering (doc drift) | cited API excerpts | mitigate | This plan IS the mitigation: per-claim accuracy verification against live source; mismatches corrected before sign-off. |
| T-115-03 | Information Disclosure | docs/teaching-and-tutorials.md | accept | Verification confirms only public API surface is cited; no secrets or internals introduced during fixes. |
</threat_model>

<verification>
- Per-claim accuracy record covers every cited symbol/excerpt/file-reference; mismatches fixed.
- DOC-01..04 coverage greps pass.
- docs/README.md links the guide.
- BoardSmith `npm test` green (no src/ changes).
</verification>

<success_criteria>
- The guide is drift-free: every cited symbol/type/method/flag/excerpt verified against live source.
- DOC-01..04 all covered; README links the guide; BoardSmith suite green.
</success_criteria>

<output>
Create `.planning/phases/115-developer-documentation/115-02-SUMMARY.md` when done, including the per-claim PASS/MISMATCH record.
</output>
