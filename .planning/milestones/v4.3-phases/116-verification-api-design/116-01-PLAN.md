---
phase: 116-verification-api-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v4.3-API-DESIGN.md
autonomous: true
requirements: [DSGN-01, DSGN-02]
must_haves:
  truths:
    - "A reader can see, for every scouted friction claim, a verdict (confirmed/false/partial) with resolvable file:line evidence and an exists-vs-build note"
    - "A reader can find full signatures, return shapes, serialization format, and owning module for every IN INTRO and TEST surface"
    - "The three load-bearing citations (getPlayerView, StateHistory, actionCompletedTick) are spot-checked and confirmed against live src/"
  artifacts:
    - path: ".planning/v4.3-API-DESIGN.md"
      provides: "Part 1 Verdicts Table + Part 2 INTRO/TEST API spec"
      contains: "## Part 1: Verification Verdicts"
  key_links:
    - from: ".planning/v4.3-API-DESIGN.md verdicts"
      to: "src/ file:line citations"
      via: "every verdict row cites a resolvable file:line"
      pattern: "snapshot.ts:194"
---

<objective>
Create `.planning/v4.3-API-DESIGN.md` and author its first two sections: the DSGN-01 verification verdicts table (consolidated from 116-RESEARCH.md, with the most load-bearing citations spot-checked against live `src/`), and the DSGN-02 API specification for the INTRO (action-space introspection) and TEST (test ergonomics) surfaces.

Purpose: Establish ground truth and lock the introspection + test-ergonomics contract that Phases 117–120 implement against.
Output: `.planning/v4.3-API-DESIGN.md` with Part 1 (verdicts) and the INTRO + TEST portion of Part 2 (API spec).
</objective>

<security_and_uispec>
Security threat model and UI-SPEC are N/A for this phase: it ships a markdown design document and changes no production code or UI. No trust boundaries are crossed, no packages are installed.
</security_and_uispec>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/116-verification-api-design/116-CONTEXT.md
@.planning/phases/116-verification-api-design/116-RESEARCH.md
@.planning/phases/116-verification-api-design/116-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spot-check load-bearing citations and author Part 1 Verdicts Table</name>
  <files>.planning/v4.3-API-DESIGN.md</files>
  <read_first>
    - .planning/phases/116-verification-api-design/116-RESEARCH.md (the "## Verification Findings" section is the source material)
    - src/engine/utils/snapshot.ts (confirm createPlayerView ~line 194 and PlayerStateView ~line 97)
    - src/session/state-history.ts (confirm the five methods at ~lines 110, 152, 202, 255, 337)
    - src/ui/composables/useActionController.ts (confirm actionCompletedTick at ~line 186 and the return-value export ~line 1811)
    - src/engine/flow/builders.ts (confirm loop() devWarn at ~lines 85-96)
    - src/session/utils.ts (confirm buildActionMetadata ~line 35 and buildSingleActionMetadata ~line 91)
  </read_first>
  <action>
    Create `.planning/v4.3-API-DESIGN.md`. Add a document header (title "BoardSmith v4.3 — API Design & Verification" + a one-line statement that this is the locked planning contract for Phases 117–121).

    Spot-check the FIVE load-bearing citations listed in read_first by opening each file and confirming the cited symbol exists near the cited line. If any citation is off by more than a few lines or the symbol is absent, correct the line number in the verdicts table and add a note "(citation corrected during spot-check)".

    Then write a "## Part 1: Verification Verdicts" section as a markdown table with one row per scouted claim. Columns: Claim ID | Claim | Verdict (CONFIRMED / FALSE / PARTIAL) | Evidence (file:line) | Exists-vs-Build. Include rows for: the three named claims (getPlayerView/INTRO-05, checkpoint-rewind/INTRO-F1, action-resolved-signal/DEV-03), and the remaining friction claims (INTRO-01..04, TEST-01..05, DEV-01, DEV-02, DEV-04, PIT-01..04 — DEV-03 is already covered by the named-claim row above, so do not write a second DEV-03 row). That is 19 rows total. Each row's verdict and evidence MUST match 116-RESEARCH.md's findings (DEV-04 verdict is FALSE — a documentation/proof gap, no source archaeology needed); do not re-derive from scratch — consolidate. Below the table, add a short "Spot-check log" noting which citations were verified and whether any were corrected.
  </action>
  <verify>
    <automated>test -f .planning/v4.3-API-DESIGN.md && grep -q "## Part 1: Verification Verdicts" .planning/v4.3-API-DESIGN.md && grep -q "snapshot.ts:194" .planning/v4.3-API-DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v4.3-API-DESIGN.md` exists with a "## Part 1: Verification Verdicts" heading
    - The verdicts table has a row for getPlayerView citing `snapshot.ts:194` and a row for each of INTRO-01..04, TEST-01..05, DEV-01..04, PIT-01..04, plus the checkpoint/rewind and action-signal named claims (19 rows total)
    - Every row carries a verdict value of exactly CONFIRMED, FALSE, or PARTIAL, a file:line evidence cell, and an exists-vs-build note
    - A "Spot-check log" subsection records the five citations checked
  </acceptance_criteria>
  <done>Part 1 verdicts table is complete, evidence-cited, and spot-checked.</done>
</task>

<task type="auto">
  <name>Task 2: Author Part 2 API spec for INTRO and TEST surfaces</name>
  <files>.planning/v4.3-API-DESIGN.md</files>
  <read_first>
    - .planning/phases/116-verification-api-design/116-RESEARCH.md (the "## API Design Blueprint", "## Key API Signatures", "## Don't Hand-Roll", and "## Open Questions" sections)
    - .planning/REQUIREMENTS.md (INTRO-01..05, TEST-01..05 descriptions and success criteria)
  </read_first>
  <action>
    Append a "## Part 2: API Specification" heading, then a "### INTRO — Action-Space Introspection" subsection and a "### TEST — Test Ergonomics" subsection.

    For each surface (INTRO-01, INTRO-02, INTRO-03, INTRO-04, INTRO-05, TEST-01, TEST-02, TEST-03, TEST-04, TEST-05) write a sub-entry containing: (a) exact public name, (b) full TypeScript signature, (c) return shape (interface definitions where the return is structured), (d) serialization format where the value crosses a process boundary, (e) the single owning module (engine | session | runtime | testing) from the Architectural Responsibility Map, and (f) a one-line disposition (EXPOSE/DOCUMENT existing vs. BUILD new) consistent with the Part 1 verdict.

    Resolve these two open design decisions explicitly under a clearly-labeled note in the relevant entries (do NOT decide silently):
    - INTRO-01 arg-template placeholder format (null vs sentinel vs empty) — state the chosen convention and rationale.
    - INTRO-03 wire-format vs in-process args — state that in-process element-object args suffice for TestGame/headless use and the `{__elementRef}` wire format is required only for cross-process agents; specify which the public API emits.

    Honor the Don't-Hand-Roll constraints: getActionSpace MUST be specified as wrapping `buildActionMetadata` + `availableActionsForSeat` (not a parallel validator); enumerateLegalMoves MUST be specified as an extraction of the MCTSBot enumeration core; INTRO-05 is DOCUMENT-existing (`createPlayerView`/`runner.getPlayerView`). Note the TEST-03 caveat that the trace should wire through `game.debugActionAvailability()`, not the possibly-stale `traceAction()` (RESEARCH Assumption A1).
  </action>
  <verify>
    <automated>grep -q "## Part 2: API Specification" .planning/v4.3-API-DESIGN.md && grep -q "### INTRO" .planning/v4.3-API-DESIGN.md && grep -q "### TEST" .planning/v4.3-API-DESIGN.md && grep -c "Owning module" .planning/v4.3-API-DESIGN.md | awk '$1>=10{exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - "## Part 2: API Specification" with "### INTRO" and "### TEST" subsections exist
    - Each of INTRO-01..05 and TEST-01..05 has a sub-entry with a full TypeScript signature, a return shape, an owning module, and an expose-vs-build disposition
    - The INTRO-01 arg-template format decision and INTRO-03 wire-vs-in-process decision are each stated explicitly with rationale
    - getActionSpace is specified as wrapping buildActionMetadata (no parallel validator); INTRO-05 is marked DOCUMENT-existing
  </acceptance_criteria>
  <done>INTRO and TEST API specs are complete with signatures, return shapes, ownership, and the two design decisions resolved.</done>
</task>

</tasks>

<verification>
- `.planning/v4.3-API-DESIGN.md` exists with Part 1 and the INTRO+TEST portion of Part 2.
- `git diff --stat src/` shows no changes (this plan touches only the design doc).
- Every verdict citation resolves to real code.
</verification>

<success_criteria>
- Part 1 verdicts table covers all 19 claims with verdict + file:line + exists-vs-build.
- INTRO-01..05 and TEST-01..05 fully specified with the two open design decisions resolved.
- No `src/` modification.
</success_criteria>

<output>
Create `.planning/phases/116-verification-api-design/116-01-SUMMARY.md` when done.
</output>
