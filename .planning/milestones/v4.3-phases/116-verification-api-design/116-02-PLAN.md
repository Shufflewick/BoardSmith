---
phase: 116-verification-api-design
plan: 02
type: execute
wave: 2
depends_on: ["116-01"]
files_modified:
  - .planning/v4.3-API-DESIGN.md
autonomous: true
requirements: [DSGN-02, DSGN-03]
must_haves:
  truths:
    - "A reader can find full signatures, return shapes, serialization, and owning module for every IN DEV and PIT surface"
    - "The DEV-01 attribute-unification direction is decided explicitly (align AutoUI to data-bs-el-id, not the reverse)"
    - "Every Future Requirement is marked IN or DEFERRED with a rationale, and INTRO-F1 is recorded as promoted for sign-off"
  artifacts:
    - path: ".planning/v4.3-API-DESIGN.md"
      provides: "Part 2 DEV/PIT API spec + Part 3 speculative-scope disposition"
      contains: "## Part 3: Speculative Scope Disposition"
  key_links:
    - from: ".planning/v4.3-API-DESIGN.md DEV-01 entry"
      to: "anchorAttrs single-source-of-truth (useBoardInteraction.ts:408)"
      via: "decision aligns AutoUI to data-bs-el-id"
      pattern: "data-bs-el-id"
---

<objective>
Complete `.planning/v4.3-API-DESIGN.md`: author the DEV (dev-host devtools bridge) and PIT (authoring guards) portions of Part 2, then author Part 3 (DSGN-03 speculative-scope disposition).

Purpose: Lock the devtools-bridge and authoring-guard contracts (Phases implementing DEV/PIT build against these), and record the IN/DEFERRED scope decisions including the INTRO-F1 promotion.
Output: Parts 2 (DEV+PIT) and 3 of the design doc.
</objective>

<security_and_uispec>
Security threat model and UI-SPEC are N/A: markdown design document only, no production code or UI changes.
</security_and_uispec>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/116-verification-api-design/116-RESEARCH.md
@.planning/phases/116-verification-api-design/116-CONTEXT.md
@.planning/v4.3-API-DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Part 2 API spec for DEV and PIT surfaces</name>
  <files>.planning/v4.3-API-DESIGN.md</files>
  <read_first>
    - .planning/phases/116-verification-api-design/116-RESEARCH.md (DEV-01..04 and PIT-01..04 findings, the DEV-02 BoardsmithDevtools draft shape, the "## Don't Hand-Roll" and "## Common Pitfalls" sections, Open Question 2 and 4)
    - .planning/REQUIREMENTS.md (DEV-01..04, PIT-01..04 descriptions)
    - src/ui/composables/useBoardInteraction.ts (confirm anchorAttrs ~line 408 and the single-source-of-truth comment ~line 403)
  </read_first>
  <action>
    Under "## Part 2: API Specification", append a "### DEV — Dev-Host Devtools Bridge" subsection and a "### PIT — Authoring Pit-of-Success Guards" subsection.

    For each surface (DEV-01, DEV-02, DEV-03, DEV-04, PIT-01, PIT-02, PIT-03, PIT-04) write a sub-entry with: exact name(s), full signature or shape, return/event payload shape, serialization/DOM-event/postMessage format where it crosses the iframe→outer-page boundary, the single owning module (ui | cli | engine | eslint-plugin), and an expose-vs-build disposition matching the Part 1 verdict.

    Resolve the DEV-01 attribute-unification decision explicitly: align AutoUI to emit `data-bs-el-id` (the declared single source of truth in `anchorAttrs`), NOT the reverse. State the rationale and reference the Common-Pitfall #2 warning sign. DEV-02: specify the `window.__BOARDSMITH_DEVTOOLS` interface (use the BoardsmithDevtools draft from research as the basis) and the iframe→outer-page postMessage bridge. DEV-03: specify the `CustomEvent` (name, detail payload with success/failure + action name) dispatched on `window` when `actionCompletedTick` increments. DEV-04: document the end-to-end agent loop tying INTRO + DEV surfaces together.

    PIT entries: PIT-01 is a BREAKING change — specify that `loop()` without `maxIterations` throws at construction (remove the devWarn path, do not add an opt-in alongside it, per Common-Pitfall #4 and the No-Backward-Compat hard rule). PIT-02/PIT-03 specify validation at `startFlow()` traversal time (note the construction-time-static-analysis limitation from Open Question 4). PIT-04 specifies the two new ESLint rules (`no-element-identity-comparison`, `no-element-array-state`) and their owning module.
  </action>
  <verify>
    <automated>grep -q "### DEV" .planning/v4.3-API-DESIGN.md && grep -q "### PIT" .planning/v4.3-API-DESIGN.md && grep -q "data-bs-el-id" .planning/v4.3-API-DESIGN.md && grep -q "__BOARDSMITH_DEVTOOLS" .planning/v4.3-API-DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - "### DEV" and "### PIT" subsections exist under Part 2
    - Each of DEV-01..04 and PIT-01..04 has a sub-entry with a signature/shape, payload/return shape, owning module, and expose-vs-build disposition
    - The DEV-01 decision explicitly aligns AutoUI to `data-bs-el-id` with rationale
    - PIT-01 is specified as a breaking construction-time throw with the devWarn path removed
  </acceptance_criteria>
  <done>DEV and PIT API specs complete with the DEV-01 attribute decision resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Author Part 3 Speculative Scope Disposition</name>
  <files>.planning/v4.3-API-DESIGN.md</files>
  <read_first>
    - .planning/phases/116-verification-api-design/116-RESEARCH.md (the "## Speculative Scope Disposition" section)
    - .planning/phases/116-verification-api-design/116-CONTEXT.md (the "Speculative Scope (IN vs DEFERRED)" decision — DEFAULT DEFER)
    - .planning/REQUIREMENTS.md (Future Requirements list: INTRO-F1, INTRO-F2, TEST-F1, DEV-F1, DEV-F2, PIT-F1)
  </read_first>
  <action>
    Append a "## Part 3: Speculative Scope Disposition" section with a table: Future Requirement | IN / DEFERRED | Rationale. Cover all six Future Requirements (INTRO-F1, INTRO-F2, TEST-F1, DEV-F1, DEV-F2, PIT-F1). Apply the CONTEXT.md default-defer policy: mark INTRO-F1 (checkpoint/rewind) as IN/PROMOTED — its rationale must state it is a trivial expose-not-build (five methods already public on GameSession; gap is exporting UndoResult/ElementDiff types) and flag it for explicit user sign-off at the approval gate. Mark the other five DEFERRED, each with the rationale from the research disposition table.

    Below the table, add a short "Approval gate" note stating the doc is not complete until the user approves it (this is fulfilled in plan 116-03).
  </action>
  <verify>
    <automated>grep -q "## Part 3: Speculative Scope Disposition" .planning/v4.3-API-DESIGN.md && grep -q "INTRO-F1" .planning/v4.3-API-DESIGN.md && grep -q "INTRO-F2" .planning/v4.3-API-DESIGN.md && grep -q "PIT-F1" .planning/v4.3-API-DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - "## Part 3: Speculative Scope Disposition" table covers all six Future Requirements
    - Each is marked IN or DEFERRED with a rationale
    - INTRO-F1 is marked IN/PROMOTED with the trivial-expose-not-build rationale and flagged for sign-off
    - The other five (INTRO-F2, TEST-F1, DEV-F1, DEV-F2, PIT-F1) are DEFERRED
  </acceptance_criteria>
  <done>Part 3 disposition complete; design doc is fully authored and ready for approval.</done>
</task>

</tasks>

<verification>
- `.planning/v4.3-API-DESIGN.md` now contains all three parts.
- `git diff --stat src/` shows no changes.
</verification>

<success_criteria>
- DEV-01..04 and PIT-01..04 fully specified with the DEV-01 decision resolved.
- All six Future Requirements dispositioned; INTRO-F1 promoted, others deferred.
- No `src/` modification.
</success_criteria>

<output>
Create `.planning/phases/116-verification-api-design/116-02-SUMMARY.md` when done.
</output>
