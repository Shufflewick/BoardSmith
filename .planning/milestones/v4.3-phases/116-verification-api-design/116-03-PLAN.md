---
phase: 116-verification-api-design
plan: 03
type: execute
wave: 3
depends_on: ["116-02"]
files_modified: []
autonomous: false
requirements: [DSGN-02]
must_haves:
  truths:
    - "No production code changed during this phase (git diff src/ is empty)"
    - "The BoardSmith test suite is still green"
    - "The phase is not complete until the user explicitly approves .planning/v4.3-API-DESIGN.md"
  artifacts:
    - path: ".planning/v4.3-API-DESIGN.md"
      provides: "The locked, human-approved API-design contract for Phases 117–121"
      contains: "## Part 3: Speculative Scope Disposition"
  key_links:
    - from: "approval gate"
      to: "Phase 117 implementation"
      via: "user approval unlocks downstream phases"
      pattern: "approved"
---

<objective>
Verify the phase invariants (no production code touched, suite green, every verdict citation resolves) and run the keystone human approval gate for `.planning/v4.3-API-DESIGN.md`. The phase is not complete until the user approves the doc.

Purpose: This is the milestone's keystone control point — Phase 117 implementation must not begin until the locked surface is reviewed and approved.
Output: A green no-regression check and an explicit user approval (or revision requests).
</objective>

<security_and_uispec>
Security threat model and UI-SPEC are N/A: no code or UI changes; this plan verifies a documentation artifact and collects approval.
</security_and_uispec>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-verification-api-design/116-VALIDATION.md
@.planning/v4.3-API-DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: No-regression and evidence-integrity verification</name>
  <files></files>
  <read_first>
    - .planning/v4.3-API-DESIGN.md (the completed design doc — all three parts)
    - .planning/phases/116-verification-api-design/116-VALIDATION.md (the validation contract)
  </read_first>
  <action>
    Confirm the phase changed no production code: run `git diff --stat -- src/` and confirm it is empty. Run `npm test` and confirm the suite is green (no new failures attributable to this phase). Re-resolve a sample of the Part 1 verdict citations: pick 3 cited `file:line` references from the verdicts table and confirm each points to the symbol the verdict describes. If any citation does not resolve, record the discrepancy and stop before the approval gate so it can be corrected.
  </action>
  <verify>
    <automated>test -z "$(git diff --stat -- src/)" && echo "src clean" && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat -- src/` produces no output (no production code changed)
    - `npm test` completes with no new failures
    - Three sampled verdict citations from Part 1 each resolve to the described symbol
  </acceptance_criteria>
  <done>Phase invariants verified: no src/ changes, suite green, citations resolve.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human approval gate for the API-design doc</name>
  <what-built>
    The complete `.planning/v4.3-API-DESIGN.md` design contract: Part 1 verification verdicts (19 claims with file:line evidence), Part 2 full API specification for all 17 IN surfaces (INTRO/TEST/DEV/PIT) with signatures, return shapes, serialization, and module ownership, and Part 3 speculative-scope disposition (INTRO-F1 promoted, five others deferred). No production code was changed.
  </what-built>
  <how-to-verify>
    1. Open `.planning/v4.3-API-DESIGN.md`.
    2. Confirm Part 1 verdicts match your understanding of what already exists vs. needs building (spot-check 2–3 cited file:line references).
    3. Confirm Part 2 signatures, return shapes, serialization, and ownership are the surface you want Phases 117–121 to implement — in particular the three design decisions: INTRO-01 arg-template format, INTRO-03 wire-vs-in-process args, and DEV-01 attribute unification (AutoUI aligned to `data-bs-el-id`).
    4. Confirm Part 3: approve or reject the INTRO-F1 (checkpoint/rewind) promotion and the five deferrals.
  </how-to-verify>
  <resume-signal>Type "approved" to lock the design and unlock Phase 117, or describe the changes you want (triggers a revision pass).</resume-signal>
</task>

</tasks>

<verification>
- `git diff --stat -- src/` empty; `npm test` green.
- User has explicitly approved `.planning/v4.3-API-DESIGN.md`.
</verification>

<success_criteria>
- No production code changed in Phase 116.
- The design doc is human-approved (the keystone milestone gate).
</success_criteria>

<output>
Create `.planning/phases/116-verification-api-design/116-03-SUMMARY.md` when done, recording the approval outcome.
</output>
