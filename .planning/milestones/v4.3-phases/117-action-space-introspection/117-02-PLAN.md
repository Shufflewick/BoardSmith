---
phase: 117-action-space-introspection
plan: 02
type: execute
wave: 2
depends_on: ["117-01"]
files_modified:
  - src/engine/utils/enumerate-moves.ts
  - src/engine/utils/enumerate-moves.test.ts
  - src/ai/mcts-bot.ts
  - src/engine/index.ts
autonomous: true
requirements: [INTRO-04]
must_haves:
  truths:
    - "A caller can enumerate every concrete legal move (action + fully-resolved args) for a seat from current state"
    - "Default enumeration is full; maxPerAction opt-in truncates per action"
    - "Enumeration core returns in-process element objects (NOT serialized IDs)"
    - "The three in-vitest MCTS tests stay green after extraction (behavior preserved)"
  artifacts:
    - path: "src/engine/utils/enumerate-moves.ts"
      provides: "enumerateLegalMoves(game, seat, options?) + extracted pure combinatorics helpers"
      exports: ["enumerateLegalMoves"]
      min_lines: 60
    - path: "src/engine/utils/enumerate-moves.test.ts"
      provides: "parity + full-enumeration + maxPerAction coverage"
  key_links:
    - from: "src/ai/mcts-bot.ts"
      to: "src/engine/utils/enumerate-moves.ts"
      via: "delegation from enumerateSelectionsInternal to enumerateSelectionsCore"
      pattern: "enumerateSelectionsCore"
    - from: "src/engine/index.ts"
      to: "src/engine/utils/enumerate-moves.ts"
      via: "barrel export"
      pattern: "enumerateLegalMoves"
---

<objective>
Implement INTRO-04: extract the legal-move enumeration core out of `MCTSBot`'s private methods into a new public engine utility `enumerateLegalMoves(game, seat, options?)`, and rewire the bot to delegate to it. The bot keeps its seeded-RNG sampling and its wire serialization; only the pure combinatorics move into the shared utility.

Purpose: This is the single highest-risk task in Phase 117. The enumeration logic handles `dependsOn`, `multiSelect`, `optional`, and recursive selection combinations — it must not be duplicated (drift risk) and the bot's behavior must be preserved exactly.
Output: New `src/engine/utils/enumerate-moves.ts` (+ test), modified `src/ai/mcts-bot.ts` delegating to it, barrel export from `src/engine/index.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-action-space-introspection/117-RESEARCH.md
@.planning/phases/117-action-space-introspection/117-VALIDATION.md
@.planning/v4.3-API-DESIGN.md
@src/ai/mcts-bot.ts

<interfaces>
<!-- Public signature to build (design doc INTRO-04, lines 216-227) -->
enumerateLegalMoves(game: Game, seat: number, options?: { maxPerAction?: number })
  : Array<{ action: string; args: Record<string, unknown> }>
// args are in-process element objects (D-07 default = full enumeration; maxPerAction opt-in)

<!-- Methods to EXTRACT from MCTSBot into enumerate-moves.ts (mcts-bot.ts) -->
enumerateSelectionsInternal(game, actionDef, player, noSampling)  :924  -> enumerateSelectionsCore(game, actionDef, player, options?)
enumerateSelectionsRecursive(...)                                 :941  -> folded into core
parseMultiSelect(multiSelect)                                     :1016 -> parseMultiSelect (pure)
generateCombinations(choices, min, max, selection)               :1028 -> generateCombinations(choices, min, max)
combinationsOfSize(choices, size, current, startIndex, results)  :1057 -> combinationsOfSize(...)

<!-- Stays in MCTSBot (bot-specific — DO NOT extract) -->
sampleChoices(choices, maxCount)  :1141  (uses this.rng — seeded)
serializeArgs(args, selections)   :1115  (bot wire format)
getChoicesForSelection            :1082  (thin wrapper -> inline game.getSelectionChoices in core)

<!-- Reachable game-public APIs the core uses -->
game.getSelectionChoices(actionName, selectionName, player, args): AnnotatedChoice<unknown>[]  // game.ts:934
game.getAction(name): ActionDefinition | undefined  // game.ts:875
availableActionsForSeat(flowState, seat): string[]  // engine barrel, src/engine/index.ts:175
game.getPlayer(seat): P | undefined  // game.ts:1749
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the enumerate-moves test (parity + full enumeration + maxPerAction)</name>
  <files>src/engine/utils/enumerate-moves.test.ts</files>
  <read_first>
    - src/ai/mcts-bot.ts:818-1011 (enumerateMoves / enumerateAllMoves / enumerateSelectionsInternal / Recursive — the exact behavior to preserve)
    - src/engine/utils/snapshot.test.ts (analog: how engine util tests construct a real Game with registered actions and a flow)
    - src/ai/mcts-restore.test.ts (analog: how in-vitest AI tests set up a game with >=2 moves; MEMORY.md notes MCTS tests need multiple moves)
    - .planning/phases/117-action-space-introspection/117-VALIDATION.md (INTRO-04 Parity Test section)
    - .planning/phases/117-action-space-introspection/117-RESEARCH.md (Target 4 — recursive structure, serialization invariant)
  </read_first>
  <action>
    Create src/engine/utils/enumerate-moves.test.ts covering: (1) full enumeration — a game with >=2 available moves returns every concrete move (action + fully-resolved args); (2) the returned args contain in-process element objects, NOT numeric IDs (assert that an element-typed arg is an object/GameElement, e.g. has expected element fields, not a bare number — this guards the "no serialization in core" invariant); (3) `maxPerAction` truncates the per-action move count when provided; (4) parity — capture the bot's enumeration for the identical game state and assert the sorted set of {action, args-as-ids} equals enumerateLegalMoves' results after applying the same ID projection (use a deterministic comparison: normalize element args to their element ids for set-equality). This is the RED step — the test imports `enumerateLegalMoves` which does not exist yet, so it must fail to compile/run until Task 2.
  </action>
  <verify>
    <automated>npx vitest run src/engine/utils/enumerate-moves.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File exists and references `enumerateLegalMoves` from the engine utils
    - Test currently FAILS (function not implemented yet) — RED confirmed
    - Test includes an explicit assertion that element-typed args are objects, not numeric IDs
    - Test includes a parity assertion against the bot's enumeration on identical state
  </acceptance_criteria>
  <done>RED test file in place asserting full enumeration, in-process objects, maxPerAction, and bot parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract enumeration core to enumerate-moves.ts and rewire MCTSBot to delegate</name>
  <files>src/engine/utils/enumerate-moves.ts, src/ai/mcts-bot.ts, src/engine/index.ts</files>
  <behavior>
    - enumerateLegalMoves(game, seat) returns the full set of legal {action, args} with in-process element objects
    - enumerateLegalMoves(game, seat, { maxPerAction: n }) returns at most n moves per action
    - enumerateSelectionsCore terminal case returns [{ ...currentArgs }] (plain copy, NO serializeArgs)
    - MCTSBot.enumerateSelectionsInternal delegates to enumerateSelectionsCore, then applies its own serializeArgs + sampleChoices afterward
    - The three in-vitest MCTS tests still pass (behavior preserved)
  </behavior>
  <read_first>
    - src/ai/mcts-bot.ts:818-1156 (the full enumeration + sampling + serialization hierarchy to split)
    - src/engine/utils/serializer.ts:9,27 (do NOT serialize in core; bot still uses its own serializeArgs)
    - src/engine/index.ts:175,211 (barrel pattern — where availableActionsForSeat/createPlayerView are exported; add enumerateLegalMoves alongside)
    - .planning/phases/117-action-space-introspection/117-RESEARCH.md (Pattern: INTRO-04 Extraction; Pitfall 2 — no serialization in core; Pitfall 4 — bot sampling stays in bot)
    - .planning/v4.3-API-DESIGN.md (INTRO-04 lines 210-247 — signature, D-07 full-enumeration default)
  </read_first>
  <action>
    Create src/engine/utils/enumerate-moves.ts exporting `enumerateLegalMoves(game, seat, options?)` plus the extracted pure helpers `enumerateSelectionsCore(game, actionDef, player, options?)`, `parseMultiSelect`, `generateCombinations`, `combinationsOfSize`. `enumerateLegalMoves` resolves the flow state via game.getFlowState(), gets action names via availableActionsForSeat, resolves the Player via game.getPlayer(seat), looks up each ActionDefinition via game.getAction(name), and calls enumerateSelectionsCore for each. The core inlines the former getChoicesForSelection by calling game.getSelectionChoices(actionName, selectionName, player, args) and filtering `disabled === false`. CRITICAL per research Pitfall 2: the recursive terminal case (formerly `return [this.serializeArgs(currentArgs, actionDef.selections)]`) must instead return `[{ ...currentArgs }]` with NO serialization — the core returns element objects. Apply `maxPerAction` truncation only when provided (D-07: default unlimited / full enumeration). Export enumerateLegalMoves from the engine barrel src/engine/index.ts. Then in src/ai/mcts-bot.ts, rewire `enumerateSelectionsInternal` to call `enumerateSelectionsCore(game, actionDef, player)`, apply `serializeArgs` to each returned arg-set (restoring the bot's prior wire-ID behavior), and apply `sampleChoices`/the 20-cap only on the non-noSampling path. Delete the now-duplicated private combinatorics (`parseMultiSelect`, `generateCombinations`, `combinationsOfSize`, `enumerateSelectionsRecursive`, `getChoicesForSelection`) from the bot, importing the shared helpers where the bot still needs them. Keep `sampleChoices`, `sampleMovesWithPreserved`, and `serializeArgs` in the bot. Do NOT import session into engine and do NOT call evaluateCondition — the core uses only game-public APIs.
  </action>
  <verify>
    <automated>npx vitest run src/engine/utils/enumerate-moves.test.ts src/ai/mcts-stats.test.ts src/ai/mcts-restore.test.ts src/ai/mcts-clone-options.test.ts 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `enumerateLegalMoves` exported from src/engine/index.ts (grep confirms)
    - `npx vitest run src/engine/utils/enumerate-moves.test.ts` exits 0 (GREEN — parity + in-process objects + maxPerAction)
    - REGRESSION GATE: `npx vitest run src/ai/mcts-stats.test.ts src/ai/mcts-restore.test.ts src/ai/mcts-clone-options.test.ts` all green
    - `grep -n "serializeArgs" src/engine/utils/enumerate-moves.ts` returns nothing (core never serializes)
    - MCTSBot no longer contains private `enumerateSelectionsRecursive`/`generateCombinations`/`combinationsOfSize` duplicates (delegates instead)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>enumerateLegalMoves returns full in-process legal moves; bot delegates and the three MCTS tests stay green; no serialization in the core.</done>
</task>

</tasks>

<threat_model>
The only access-control concern is that enumeration must be scoped to `seat`: `enumerateLegalMoves` derives action names solely from `availableActionsForSeat(flowState, seat)` and resolves choices via `game.getSelectionChoices(..., player, ...)`, so it cannot surface another seat's actions. No new trust boundary, no untrusted input, no network/auth/secrets surface. No package installs (RESEARCH.md confirms no new packages).
</threat_model>

<verification>
- `npx vitest run src/engine/ src/ai/` green
- `npx tsc --noEmit` clean
- Per-wave merge: `npx vitest run src/engine/ src/session/ src/ai/ src/runtime/`
</verification>

<success_criteria>
A caller can call `enumerateLegalMoves(game, seat)` and receive every concrete legal move with in-process element-object args, optionally capped by `maxPerAction`. The MCTS bot delegates to the same core and its three in-vitest tests remain green, proving behavior was preserved with no logic duplication.
</success_criteria>

<output>
Create `.planning/phases/117-action-space-introspection/117-02-SUMMARY.md` when done.
</output>
