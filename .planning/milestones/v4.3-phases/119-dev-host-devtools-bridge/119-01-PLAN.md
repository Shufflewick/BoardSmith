---
phase: 119-dev-host-devtools-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/components/auto-ui/renderers/renderers.devtools-attrs.test.ts
autonomous: true
requirements: [DEV-01]

must_haves:
  truths:
    - "All four AutoUI renderers emit data-bs-el-id on their mounted root, matching String(element.id)"
    - "data-element-id (FLIP alias) remains present on the same root"
    - "The single-source guard (no inline data-bs-el-* literals in renderer files) still passes"
  artifacts:
    - path: "src/ui/components/auto-ui/renderers/renderers.devtools-attrs.test.ts"
      provides: "DOM-mount parity test for data-bs-el-id across PieceRenderer, SpaceRenderer, DieRenderer, CardRenderer"
      contains: "data-bs-el-id"
  key_links:
    - from: "renderer template v-bind=\"selectableAttrs\""
      to: "anchorAttrs(identity())"
      via: "useSelectable.attrs"
      pattern: "data-bs-el-id"
---

<objective>
Prove DEV-01 with a new DOM-mount parity test asserting every AutoUI renderer exposes a stable `data-bs-el-id` selector (matching the element id) plus the `data-element-id` FLIP alias — in the AutoUI path, matching what custom UI already emits.

Purpose: DEV-01's attribute emission is ALREADY implemented via `v-bind="selectableAttrs"` → `useSelectable.attrs` → `anchorAttrs(identity())`. The deliverable is verification, NOT a code change. Adding inline `:data-bs-el-id` literals to renderers would BREAK the single-source guard at `anchorAttrs.test.ts:174`.
Output: `renderers.devtools-attrs.test.ts` (new), single-source guard stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-dev-host-devtools-bridge/119-RESEARCH.md
@.planning/phases/119-dev-host-devtools-bridge/119-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DOM-mount parity test for data-bs-el-id across all four AutoUI renderers</name>
  <files>src/ui/components/auto-ui/renderers/renderers.devtools-attrs.test.ts</files>
  <read_first>
    - src/ui/components/auto-ui/renderers/CardRenderer.a11y.test.ts — the mount pattern: `defineComponent` wrapper that calls `provideBoardInteraction()` in setup so the renderer's `tryUseBoardInteraction()` inject resolves to a spy-backed mock (makeSelectableMock). Reuse this exact wrapper + mock shape.
    - src/ui/composables/useSelectable.ts (lines ~41-58) — `attrs` computed spreads `anchorAttrs(identity())`; confirms data-bs-el-id origin.
    - src/ui/composables/useBoardInteraction.ts (line ~408) — `anchorAttrs({ id, notation?, name? })` returns `data-bs-el-id`, `data-bs-el-notation`, `data-bs-el-name`.
    - src/ui/composables/anchorAttrs.test.ts (line ~174) — the negative single-source guard that MUST stay green; do NOT add inline data-bs-el-* literals to any renderer .vue file.
    - src/ui/components/auto-ui/renderers/PieceRenderer.vue, SpaceRenderer.vue, DieRenderer.vue, CardRenderer.vue — inspect each for required props and the root element carrying `v-bind="selectableAttrs"` + `:data-element-id`.
  </read_first>
  <action>Create the test file with `// @vitest-environment jsdom` at the top. Mount each of PieceRenderer, SpaceRenderer, DieRenderer, and CardRenderer through the CardRenderer.a11y.test.ts wrapper-component pattern (a wrapper that calls provideBoardInteraction with the makeSelectableMock). For each renderer, pass a mock element whose id is a fixed number (e.g. 42) and the minimal props that renderer requires. Assert the mounted root element returns `String(42)` from `getAttribute('data-bs-el-id')`, and that `getAttribute('data-element-id')` is non-null (FLIP alias preserved). Where the element carries a notation/name, also assert `data-bs-el-notation`/`data-bs-el-name` is present. Do NOT modify any renderer .vue file. Do NOT add any `data-bs-el-*` string literal to a .vue file.</action>
  <verify>
    <automated>npx vitest run src/ui/components/auto-ui/renderers/renderers.devtools-attrs.test.ts src/ui/composables/anchorAttrs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/components/auto-ui/renderers/renderers.devtools-attrs.test.ts` exits 0 with assertions for all four renderers (PieceRenderer, SpaceRenderer, DieRenderer, CardRenderer).
    - Each renderer case asserts `root.getAttribute('data-bs-el-id') === String(element.id)` AND `root.getAttribute('data-element-id')` is non-null.
    - `npx vitest run src/ui/composables/anchorAttrs.test.ts` still exits 0 (single-source guard green — proves no inline literals were added).
    - `git diff --name-only` shows only the new test file changed — zero renderer .vue files modified.
    - `npx vue-tsc --noEmit` (or the project's tsc check) reports no new errors from the test file.
  </acceptance_criteria>
  <done>New parity test proves data-bs-el-id + data-element-id on all four AutoUI renderer roots; single-source guard remains green; no renderer source touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundary is crossed by this plan. It adds a test-only file; no runtime code, no network, no user input, no new attribute emission (attributes already emitted by existing code).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01 | Information Disclosure | data-bs-el-id in production DOM | accept | Element ids are non-secret ordinals already emitted in custom UI today; no PII, no change in exposure. Attribute presence is unchanged by this plan. |
</threat_model>

<verification>
- `npx vitest run src/ui/components/auto-ui/renderers/` green.
- `npx vitest run src/ui/composables/anchorAttrs.test.ts` green (guard intact).
</verification>

<success_criteria>
DEV-01 verified: `[data-bs-el-id="<id>"]` resolves the same element in AutoUI as in custom UI, proven by a DOM-mount test across all four renderers, with the FLIP alias intact and the single-source guard still passing.
</success_criteria>

<output>
Create `.planning/phases/119-dev-host-devtools-bridge/119-01-SUMMARY.md` when done
</output>
