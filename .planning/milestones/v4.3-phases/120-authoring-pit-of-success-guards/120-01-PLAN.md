---
phase: 120-authoring-pit-of-success-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/flow/builders.ts
  - src/engine/flow/builders.test.ts
  - src/engine/flow/engine.test.ts
  - src/engine/utils/dev-state.test.ts
autonomous: true
requirements: [PIT-01]
must_haves:
  truths:
    - "loop({ do }) without maxIterations throws at construction with an actionable message"
    - "loop({ maxIterations: N, ... }) still constructs a valid 'loop' FlowNode"
    - "The full test suite stays green after the throw lands (all 6 pre-existing call sites fixed)"
  artifacts:
    - path: "src/engine/flow/builders.ts"
      provides: "loop() unconditional throw when maxIterations is undefined; devWarn('loop-no-max:*') path removed"
      contains: "throw new Error"
    - path: "src/engine/flow/builders.test.ts"
      provides: "Dedicated PIT-01 unit coverage (throw + happy-path construction)"
  key_links:
    - from: "src/engine/flow/builders.ts loop()"
      to: "throw new Error"
      via: "config.maxIterations === undefined guard"
      pattern: "maxIterations === undefined"
---

<objective>
PIT-01: Make `loop()` fail fast at game construction when `maxIterations` is omitted, replacing the silent dev-only `devWarn` with an unconditional `throw`. Fix the 6 in-repo test call sites that construct `loop({...})` without `maxIterations` so the suite stays green.

Purpose: A missing `maxIterations` is currently silent in production and only surfaces as a surprise runtime iteration-cap throw. This converts it into an actionable authoring-time error.
Output: Modified `builders.ts`, new `builders.test.ts`, and 6 patched test call sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-authoring-pit-of-success-guards/120-CONTEXT.md
@.planning/phases/120-authoring-pit-of-success-guards/120-RESEARCH.md
@.planning/phases/120-authoring-pit-of-success-guards/120-VALIDATION.md

<interfaces>
From src/engine/flow/builders.ts — loop() signature (verified 2026-07-01):
  export function loop(config: { name?: string; while?: (context: FlowContext) => boolean; maxIterations?: number; do: FlowNode }): FlowNode
  Current lines 85–96 hold the `if (config.maxIterations === undefined) { devWarn('loop-no-max:${loopName}', ...) }` block. Returns `{ type: 'loop', config: {...} }`.

Secondary safety net (LEAVE UNTOUCHED): src/engine/flow/engine.ts — `DEFAULT_MAX_ITERATIONS = 10000` declared line ~30, consumed line 1032 (`config.maxIterations ?? DEFAULT_MAX_ITERATIONS`), enforced line 1045 (`if (iteration >= maxIterations) throw ...`). This runtime cap remains as a secondary net; the design doc's stale "engine.ts:1045 = DEFAULT_MAX_ITERATIONS" citation is wrong — the constant is at line ~30.

`devWarn` import (`import { devWarn } from '../../utils/dev.js'`) is used by other builders — do NOT remove the import; only remove the loop() call site to it. Verify no unused-import lint error results after removal.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace loop() devWarn with an unconditional construction-time throw (RED then GREEN)</name>
  <files>src/engine/flow/builders.ts, src/engine/flow/builders.test.ts</files>
  <read_first>
    - src/engine/flow/builders.ts (lines 55–120, the loop() function)
    - .planning/v4.3-API-DESIGN.md (PIT-01 section — exact error-text intent)
    - src/utils/dev.ts (confirm devWarn is used by other builders before deciding on the import)
  </read_first>
  <behavior>
    - Test: `loop({ do: someLeafNode })` (no maxIterations) throws an Error whose message contains "maxIterations", the example `loop({ maxIterations: 100`, and mentions the silent 10000 cap risk.
    - Test: `loop({ maxIterations: 10, do: someLeafNode })` returns a node with `type === 'loop'` and `config.maxIterations === 10` — no throw.
    - Test: `loop({ maxIterations: 5, while: () => false, do: someLeafNode })` preserves `config.while` and `config.name`.
  </behavior>
  <action>
    Create src/engine/flow/builders.test.ts first with the three behaviors above (RED — the throw case fails against current devWarn code). Use a trivial leaf node for `do` (e.g. an `actionStep({ actions: ['noop'] })` or `execute(...)` builder from the same module). Then edit loop() in builders.ts: delete the entire `if (config.maxIterations === undefined) { const loopName = ...; devWarn('loop-no-max:${loopName}', ...) }` block and replace it with `if (config.maxIterations === undefined) throw new Error(...)`. The message MUST be actionable: state that loop() requires maxIterations, show the corrective form `loop({ maxIterations: 100, ... })`, and explain the silent 10000-iteration default-cap risk. Use an early throw (guard clause) before the return statement. Keep the `import { devWarn }` line — confirm via grep that devWarn is still referenced elsewhere in builders.ts; if it is NOT, remove only the now-unused import to avoid an unused-import lint error.
  </action>
  <verify>
    <automated>npx vitest run src/engine/flow/builders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - builders.test.ts exists and passes: the no-maxIterations case asserts a throw with a message matching /maxIterations/ and /100/; the happy-path cases assert `type === 'loop'` with no throw.
    - No `devWarn('loop-no-max:` string remains anywhere in builders.ts (grep returns nothing).
    - `npm run lint src/engine/flow/builders.ts` reports no new unused-import error for devWarn.
  </acceptance_criteria>
  <done>loop() throws with an actionable message when maxIterations is undefined; happy-path construction unchanged; dedicated test file green.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the 6 pre-existing loop({...}) call sites lacking maxIterations</name>
  <files>src/engine/flow/engine.test.ts, src/engine/utils/dev-state.test.ts</files>
  <read_first>
    - src/engine/flow/engine.test.ts (lines around 50, 174, 196, 1030, 1465)
    - src/engine/utils/dev-state.test.ts (line around 76)
    - .planning/phases/120-authoring-pit-of-success-guards/120-RESEARCH.md (Common Pitfalls #1 — the exact 6 call-site list)
  </read_first>
  <action>
    Add an explicit `maxIterations` to each of the 6 real `loop({...})` construction sites so they no longer throw under the Task 1 change: src/engine/utils/dev-state.test.ts:76 and src/engine/flow/engine.test.ts lines 50, 174, 196, 1030, 1465. Choose a bound that does not alter each test's intent — a small value such as `maxIterations: 10` is appropriate for short-lived test flows (engine.test.ts:50 only asserts `node.type === 'loop'`, so any positive value is fine). Do NOT modify JSDoc `@example` `loop({...})` occurrences — those are comments, not executable call sites. After editing, grep the whole `src/` tree for executable `loop({` calls lacking `maxIterations` to confirm none remain outside doc comments.
  </action>
  <verify>
    <automated>npx vitest run src/engine/flow/engine.test.ts src/engine/utils/dev-state.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 6 named call sites carry an explicit `maxIterations`.
    - `npx vitest run src/engine/flow src/engine/utils` is fully green.
    - `npm test` (full suite) shows no new construction-time failures attributable to the PIT-01 throw.
  </acceptance_criteria>
  <done>All in-repo executable loop() sites pass maxIterations; suite green after the PIT-01 throw lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game author → engine | Flow definition authored by a developer; validated at construction time. No end-user/runtime input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | Tampering | loop() error message | accept | Developer-facing message; no PII, no runtime attack surface. |
| T-120-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (RESEARCH: "installs no new packages"); no legitimacy gate required. |
</threat_model>

<verification>
- `npx vitest run src/engine/flow/builders.test.ts` green.
- `npx vitest run src/engine/flow src/engine/utils` green.
- `grep -rn "loop({" src/ | grep -v maxIterations` returns only JSDoc `@example` lines.
</verification>

<success_criteria>
loop() without maxIterations throws an actionable construction-time error; the devWarn path is gone; the full suite stays green.
</success_criteria>

<output>
Create `.planning/phases/120-authoring-pit-of-success-guards/120-01-SUMMARY.md` when done.
</output>
