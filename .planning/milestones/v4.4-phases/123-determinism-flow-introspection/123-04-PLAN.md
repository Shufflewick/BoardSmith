---
phase: 123-determinism-flow-introspection
plan: 04
type: execute
wave: 2
depends_on: ["123-01"]
files_modified:
  - src/session/game-session.ts
  - src/session/types.ts
  - src/session/game-session.test.ts
  - src/session/stateless-ops.ts
  - src/session/stateless-ops.test.ts
  - src/cli/dev-host/bridge.ts
  - src/ui/components/DebugPanel.vue
  - src/ui/components/GameShell.vue
  - src/ui/components/GameShell.devtools.ts
  - src/ui/global.d.ts
  - src/cli/dev-host/DevHost.vue
autonomous: false
requirements: [FLOW-01, FLOW-03]
must_haves:
  truths:
    - "The session broadcast carries a serializable flow-debug snapshot (phase/step/path/awaiting + a precomputed description string) for every connected client"
    - "The session broadcast carries the connected seat's OWN pending-action snapshot only — never another seat's accumulated args"
    - "window.__BOARDSMITH_DEVTOOLS.getFlowDebugInfo() and getPendingAction() return the flow position and pending action in the dev host, matching the engine's values"
    - "A `debug:flow-state` dev-host WS op returns the same serialized FlowDebugInfo structure (plus the requesting seat's own pending action), reusing the existing debug:* op family — per the FLOW-01 locked CONTEXT decision"
    - "The visual DebugPanel displays the readable flow position (description string) sourced from the debug:flow-state op"
    - "The devtools additions stay inside the existing isDevBuild / import.meta.env.DEV guards and are dead-code-eliminated in production"
  artifacts:
    - path: "src/session/game-session.ts"
      provides: "Broadcast injection of serialized flowDebugInfo + own-seat pendingAction"
      contains: "getFlowDebugInfo"
    - path: "src/session/stateless-ops.ts"
      provides: "debugFlowState op + handler returning serialized FlowDebugInfo + own-seat pendingAction"
      contains: "debugFlowState"
    - path: "src/cli/dev-host/bridge.ts"
      provides: "debug:flow-state WireOp + translateOp case + shapeResult case"
      contains: "debug:flow-state"
    - path: "src/ui/components/DebugPanel.vue"
      provides: "Visual readable flow-position display sourced from debug:flow-state"
      contains: "debug:flow-state"
    - path: "src/ui/components/GameShell.devtools.ts"
      provides: "flowDebugInfo + pendingAction fields on the devtools payload"
      contains: "flowDebugInfo"
    - path: "src/cli/dev-host/DevHost.vue"
      provides: "getFlowDebugInfo / getPendingAction window bridge getters"
      contains: "getFlowDebugInfo"
  key_links:
    - from: "src/session/game-session.ts"
      to: "runner.game.getFlowDebugInfo() + this.getPendingAction(seat)"
      via: "broadcast injects serialized flow-debug + own-seat pending action"
      pattern: "getFlowDebugInfo|getPendingAction"
    - from: "src/cli/dev-host/bridge.ts"
      to: "src/session/stateless-ops.ts debugFlowState handler"
      via: "debug:flow-state WireOp → translateOp → executeOp → shapeResult"
      pattern: "debug:flow-state|debugFlowState"
    - from: "src/ui/components/DebugPanel.vue"
      to: "debug:flow-state op result"
      via: "debugRequest('debug:flow-state') renders the readable description"
      pattern: "debug:flow-state"
    - from: "src/ui/components/GameShell.vue"
      to: "buildDevtoolsPayload"
      via: "forwards flowDebugInfo/pendingAction from received state to the devtools payload"
      pattern: "flowDebugInfo"
    - from: "src/cli/dev-host/DevHost.vue"
      to: "devtoolsSnapshot"
      via: "window bridge getters read the two new snapshot fields"
      pattern: "__BOARDSMITH_DEVTOOLS"
---

<objective>
Complete FLOW-01/03 introspection parity in the browser (v4.3 hard rule) via BOTH channels the CONTEXT decisions lock:

1. The `__BOARDSMITH_DEVTOOLS` window bridge (FLOW-03 decision: "Pending-action state also surfaced via the existing __BOARDSMITH_DEVTOOLS bridge").
2. The existing dev-host `debug:*` WS op family (FLOW-01 decision: "Also exposed via the existing dev-host debug:* WS op family (reuses the same structure)"), surfaced in the visual DebugPanel.

GameShell is a client over HTTP/WS — it holds no engine Game — so the flow-debug and pending-action data must travel through the session broadcast / the debug op path to the client, then into the bridge and panel. Crucially, per-seat pending-action data must be perspective-safe: each connected seat receives only its OWN pending action, never another seat's accumulated args (the flagged hidden-info leak threat for this phase).

Purpose: An agent/developer can read the current flow position and the acting seat's pending action from the dev host bridge AND from the visual debug panel, matching engine values, without a Node terminal.
Output: Serializable flow-debug + own-seat pending-action in the broadcast; a `debug:flow-state` op returning the same structure; the devtools window getters; the visual DebugPanel readable flow position; browser-verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-determinism-flow-introspection/123-CONTEXT.md
@.planning/phases/123-determinism-flow-introspection/123-RESEARCH.md
@.planning/phases/123-determinism-flow-introspection/123-PATTERNS.md
@.planning/phases/123-determinism-flow-introspection/123-01-SUMMARY.md

<interfaces>
<!-- Extracted contracts the executor needs. -->

FROM PLAN 01 (available):
- Game.getFlowDebugInfo(): FlowDebugInfo — { phase?, step?, path, awaiting, describe() }. describe() is a METHOD and does NOT survive serialization; anything sent over the wire must send a plain object with a precomputed `description` string.

SESSION BROADCAST (server side, src/session/game-session.ts):
- broadcast() (line 1983) loops over connected sessions; for each it computes `effectivePosition` (spectator -> 0, else session.playerSeat) and builds per-seat `state` via the free function buildPlayerState(this.#runner, playerNames, effectivePosition, {...}).
- Transient, never-serialized fields are INJECTED after buildPlayerState (hint at 2003, heatmap 2005, narration 2007, isDemoRunning 2010, teachingDisabled 2014) — mirror THIS injection pattern; keep buildPlayerState pure.
- StateUpdate (line 2016) already carries top-level `flowState` (public, non-seat-sensitive).
- GameSession.getPendingAction(playerPosition): PendingActionState | undefined already EXISTS (line 1885, delegates to PendingActionManager). Use `this.getPendingAction(effectivePosition)` — the OWN seat only.
- runner.game.getFlowDebugInfo() gives the flow-debug object; call describe() server-side into a string.

PlayerGameState (src/session/types.ts:401) is the per-seat `state` shape (has `phase`, `customDebug?`, `tutorial?`, etc.). Add the two new optional fields here. Define/export a plain-object type `SerializedFlowDebugInfo` here (reused by the debug op + devtools + bridge).

DEBUG OP FAMILY (dev-host WS ops — the FLOW-01 locked "debug:* WS op family" channel):
- WireOp union + translateOp + shapeResult live in src/cli/dev-host/bridge.ts (WireOp at line 21; translateOp switch at 111; shapeResult switch at 209). Existing read-only debug ops: `debug:history` (returns actionHistory), `debug:state-at` (returns state), `debug:action-traces` (returns traces + flowContext). Follow the `debug:history` / `debug:action-traces` handler pattern EXACTLY.
- Op union + OpResult + READ_ONLY_OP_TYPES + handlers + dispatch live in src/session/stateless-ops.ts: Op union (`debugHistory` at 55, `debugActionTraces` at 58), READ_ONLY_OP_TYPES set (83-91), OpResult debug fields (actionHistory/traces/flowContext at 124-128), handlers (handleDebugActionTraces at 872 is the closest analog — it builds a runner from snapshot via runnerFromSnapshot, reads flow state, and returns extra fields alongside stateEnvelope), dispatch switch (989-996).
- runnerFromSnapshot(snapshot, def) rebuilds a GameRunner from the snapshot; `runner.game` is the engine Game exposing getFlowDebugInfo(). executeOp receives `pendingState` (the requesting seat's persisted pending state) — this is naturally seat-scoped, so it is the perspective-safe source for the requesting seat's own pending action in the debug op.

DEBUGPANEL (visual dev panel, src/ui/components/DebugPanel.vue):
- debugRequest(op, payload) helper at line 255 issues a debug op over the bridge and returns the result. Example usage: `debug:action-traces` at 551 (stores traces + flowContext), `debug:history` at 1077.
- Existing flow-context box at 1718-1732 renders flowContext.currentPhase/currentPlayer (sourced from the action-traces op). Add a readable flow-position line here (or a sibling box) sourced from the new `debug:flow-state` op's `description`.
- FlowContext interface at 297.

DEVTOOLS BRIDGE (client side):
- GameShell.devtools.ts: DevtoolsStateMessage (interface, line 17), DevtoolsParams (interface, line 36), buildDevtoolsPayload (line 51) returns the postMessage payload. Add the two fields to all three (additive).
- GameShell.vue: the devtools watch/post block is guarded by `if (isDevBuild)` (line 702); it calls maybePostDevtoolsUpdate with a params object (716-726). `state` = received broadcast state; read the new fields from state and pass them through. isDevBuild = import.meta.env.DEV (line 175).
- global.d.ts: BoardsmithDevtools interface (lines 4-13) — four existing getters return `... | null`. Add getFlowDebugInfo/getPendingAction with the same `| null` convention.
- DevHost.vue: window.__BOARDSMITH_DEVTOOLS assigned at line 373 inside `if (import.meta.env.DEV)`, getters read `devtoolsSnapshot.value?.<field>`. Received payload handled at the `boardsmith:devtools-state-update` branch (line 240). Add two getters mirroring getBoardInteractionState (line 379).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Broadcast serialized flow-debug + own-seat pending-action (perspective-safe)</name>
  <files>src/session/game-session.ts, src/session/types.ts, src/session/game-session.test.ts</files>
  <read_first>
    - src/session/game-session.ts:1983-2030 (broadcast loop + the hint/heatmap/narration injection pattern to mirror)
    - src/session/game-session.ts:1885-1886 (getPendingAction — own-seat source)
    - src/session/types.ts:401-440+ (PlayerGameState fields; add optional fields following the existing doc-comment style)
    - src/engine/action/types.ts:137 (PendingActionState shape being surfaced)
    - src/engine/flow/types.ts (FlowDebugInfo from Plan 01 — describe() is a method, must be serialized to a string)
  </read_first>
  <behavior>
    - After a broadcast, each connected seat's state carries flowDebugInfo as a plain serializable object { phase?, step?, path, awaiting, description } where description equals the engine's describe() output
    - Each seat's state carries pendingAction equal to that seat's OWN pending action snapshot (or omitted/undefined when none)
    - Seat N's broadcast NEVER contains seat M's pending action (M != N) — verified by an explicit multi-seat test
    - Spectators (effectivePosition 0) receive flowDebugInfo but no other seat's pending action
  </behavior>
  <action>
    In src/session/types.ts, add two optional fields to PlayerGameState with doc comments matching the existing style: `flowDebugInfo?` typed as a serializable snapshot (define/export a plain-object type `SerializedFlowDebugInfo` with `phase?: string; step?: string; path: number[]; awaiting: { currentPlayer?: number; awaitingPlayers?: number[] }; description: string`), and `pendingAction?: PendingActionState` (the own-seat snapshot). Import PendingActionState via its public engine path. `SerializedFlowDebugInfo` is the single shared serialized shape reused by the debug op (Task 3), the devtools payload (Task 2), and the bridge.

    In src/session/game-session.ts broadcast() (after buildPlayerState, in the same injection block as hint/heatmap at ~2003-2014), inject: (a) `state.flowDebugInfo` = a serialized snapshot built from `this.#runner.game.getFlowDebugInfo()` — spread its structured fields and set `description` to `info.describe()` (do NOT put the method on the wire); (b) `state.pendingAction` = `this.getPendingAction(effectivePosition)` — the OWN seat only, never a loop over all seats. Because getFlowDebugInfo() is flow-position data (public), computing it once outside the loop and reusing is fine; pendingAction MUST be per-seat inside the loop keyed on effectivePosition.

    In src/session/game-session.test.ts, add a test that connects two seats with a multi-step action pending for seat 1, triggers a broadcast, and asserts: seat 1's broadcast state.pendingAction reflects seat 1's pending action, AND seat 2's broadcast state.pendingAction does NOT contain seat 1's accumulated args (perspective isolation). Assert both seats receive flowDebugInfo.description as a non-empty string.
  </action>
  <verify>
    <automated>npx vitest run src/session/game-session.test.ts -t "pendingAction" --silent && npx vitest run src/session/game-session.test.ts -t "flowDebugInfo" --silent</automated>
  </verify>
  <acceptance_criteria>
    - PlayerGameState has flowDebugInfo? and pendingAction? optional fields (grep both in src/session/types.ts)
    - SerializedFlowDebugInfo is exported from src/session/types.ts
    - broadcast() injects pendingAction using effectivePosition (own seat), NOT a per-seat loop over all seats
    - `grep -n "getFlowDebugInfo" src/session/game-session.ts` shows the serialized-snapshot build with a description string (method not sent on the wire)
    - game-session.test.ts asserts seat 2's broadcast does NOT contain seat 1's pending-action args (perspective isolation)
    - both vitest runs pass
  </acceptance_criteria>
  <done>Broadcast carries serialized flow-debug (all seats) + own-seat pending action (perspective-isolated), proven by a multi-seat leak test.</done>
</task>

<task type="auto">
  <name>Task 2: Wire flow-debug + pending-action through the devtools bridge (client)</name>
  <files>src/ui/components/GameShell.vue, src/ui/components/GameShell.devtools.ts, src/ui/global.d.ts, src/cli/dev-host/DevHost.vue</files>
  <read_first>
    - src/ui/components/GameShell.devtools.ts:17-67 (DevtoolsStateMessage, DevtoolsParams, buildDevtoolsPayload — additive-field pattern; the isDevBuild guard comment at 6-8)
    - src/ui/components/GameShell.vue:700-731 (devtools watch/post block, maybePostDevtoolsUpdate params)
    - src/ui/global.d.ts:4-13 (BoardsmithDevtools interface, `| null` return convention)
    - src/cli/dev-host/DevHost.vue:240 (devtools-state-update handler), :370-382 (window.__BOARDSMITH_DEVTOOLS getters, all inside import.meta.env.DEV)
    - src/session/types.ts (SerializedFlowDebugInfo + PlayerGameState.flowDebugInfo/pendingAction from Task 1)
  </read_first>
  <action>
    In src/ui/components/GameShell.devtools.ts, add `flowDebugInfo` and `pendingAction` fields (typed to match the serialized session types) to both DevtoolsStateMessage (17) and DevtoolsParams (36), and pass them through in buildDevtoolsPayload's returned object (51-67). Keep everything additive; do not restructure the payload.

    In src/ui/components/GameShell.vue, inside the existing `if (isDevBuild)` watch block (702-731), read `flowDebugInfo` and `pendingAction` from the received broadcast state (`state.value?.state`) and include them in the params object passed to maybePostDevtoolsUpdate (alongside seat/state/availableActions/actionMetadata/boardInteraction). Do not add a new watch; extend the existing params object.

    In src/ui/global.d.ts, add `getFlowDebugInfo(seat?: number): <SerializedFlowDebugInfo> | null;` and `getPendingAction(seat?: number): <PendingActionState-plain> | null;` to the BoardsmithDevtools interface, matching the existing `| null` return convention (never undefined).

    In src/cli/dev-host/DevHost.vue, add two getters to the window.__BOARDSMITH_DEVTOOLS assignment (373-381) inside the existing `if (import.meta.env.DEV)` guard: `getFlowDebugInfo: (_seat?: number) => (devtoolsSnapshot.value?.flowDebugInfo as ...) ?? null` and `getPendingAction: (_seat?: number) => (devtoolsSnapshot.value?.pendingAction as ...) ?? null`, mirroring getBoardInteractionState (379). Ensure the devtools-state-update handler (240) already stores the full payload into devtoolsSnapshot (the two new fields ride along automatically if it stores the whole message; if it cherry-picks fields, add the two new ones).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "GameShell|DevHost|global.d.ts" | grep -c error | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "flowDebugInfo" src/ui/components/GameShell.devtools.ts` returns >= 2 (both interfaces + return)
    - `grep -c "flowDebugInfo\|pendingAction" src/ui/components/GameShell.vue` returns >= 2 (forwarded in params)
    - global.d.ts BoardsmithDevtools has getFlowDebugInfo and getPendingAction returning `| null`
    - DevHost.vue adds both getters inside the `import.meta.env.DEV` guard
    - tsc reports no new errors in the touched UI files
  </acceptance_criteria>
  <done>Flow-debug + pending-action ride the devtools payload and are exposed as two production-guarded window getters; typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add debug:flow-state WS op + surface readable flow position in the visual DebugPanel (FLOW-01 locked decision)</name>
  <files>src/session/stateless-ops.ts, src/session/stateless-ops.test.ts, src/cli/dev-host/bridge.ts, src/ui/components/DebugPanel.vue</files>
  <read_first>
    - src/session/stateless-ops.ts:55-62 (Op union debug entries), :83-91 (READ_ONLY_OP_TYPES), :123-128 (OpResult debug fields), :872-902 (handleDebugActionTraces — the closest analog: runnerFromSnapshot → read flow → return extra fields + stateEnvelope), :989-996 (dispatch switch)
    - src/cli/dev-host/bridge.ts:21-51 (WireOp union), :163-179 (translateOp debug cases), :241-254 (shapeResult debug cases)
    - src/session/types.ts (SerializedFlowDebugInfo from Task 1 — reuse this exact shape)
    - src/ui/components/DebugPanel.vue:255 (debugRequest helper), :543-559 (fetchActionTraces pattern to mirror), :1718-1732 (flow-context box render)
    - src/engine/flow/types.ts (FlowDebugInfo — describe() must be serialized to a string, never sent as a method)
  </read_first>
  <action>
    This task delivers the FLOW-01 locked CONTEXT decision: "Also exposed via the existing dev-host debug:* WS op family (reuses the same structure)." It is the debug:* channel, distinct from Task 2's window-bridge channel.

    In src/session/stateless-ops.ts: (a) add `| { type: 'debugFlowState'; player: number }` to the Op union alongside the other debug ops (~55-62); (b) add `'debugFlowState'` to READ_ONLY_OP_TYPES (83-91) — it reports without mutating; (c) add `flowDebugInfo?: SerializedFlowDebugInfo` (import the shared type from src/session/types.ts) to OpResult's debug fields (~123-128); (d) add `handleDebugFlowState(def, gameOptions, snapshot, pendingState, op)` mirroring handleDebugActionTraces (872): validate op.player is in range (return errorResult on out-of-range, matching handleDebugActionTraces), build the runner via runnerFromSnapshot, build the serialized snapshot from `runner.game.getFlowDebugInfo()` (spread structured fields + `description: info.describe()`), and return `{ success: true, ...stateEnvelope(runner, gameOptions.playerCount), flowDebugInfo, pendingAction }` where pendingAction is the requesting seat's OWN pending action derived from the passed-in `pendingState` (naturally seat-scoped — do NOT read any other seat's pending state); (e) add `case 'debugFlowState': return handleDebugFlowState(...)` to the dispatch switch (~989-996). Note executeOp already receives `pendingState` (the requesting seat's) — thread it into the handler.

    In src/cli/dev-host/bridge.ts: (a) add `| 'debug:flow-state'` to the WireOp union alongside the other debug: entries (~43-51); (b) add a translateOp case (~163) returning `{ type: 'debugFlowState', player: (payload.player as number) ?? seat }` mirroring debug:action-traces (178); (c) add a shapeResult case (~241) returning `{ success: result.success, error: result.error, flowDebugInfo: result.flowDebugInfo, pendingAction: result.pendingAction }` mirroring the debug:action-traces shaping (248-254).

    In src/ui/components/DebugPanel.vue: add a `fetchFlowState()` function mirroring fetchActionTraces (543) that calls `debugRequest('debug:flow-state', { player: props.playerSeat })`, stores the returned `flowDebugInfo` in a new ref, and refresh it on the same triggers as action-traces (the actions-tab watch / state-change watch at 1052-1062). Render the readable `description` string in (or next to) the existing flow-context box (~1718-1732) as a "Flow position" line so the visual panel shows the human-readable flow position. Keep it dev-only (the whole panel is dev-only) — no production exposure.

    In src/session/stateless-ops.test.ts, add a test that runs `executeOp(def, gameOptions, snapshot, pendingState, { type: 'debugFlowState', player: 1 })` and asserts the result has `flowDebugInfo.description` as a non-empty string and the structured fields (path array, awaiting) present. Add an out-of-range seat case asserting an error result (no throw). If a pendingState fixture is available, assert the returned pendingAction reflects the passed-in seat's state (perspective-scoped).
  </action>
  <verify>
    <automated>npx vitest run src/session/stateless-ops.test.ts -t "debugFlowState" --silent && npx vitest run src/session/stateless-ops.test.ts -t "flow-state" --silent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "debug:flow-state" src/cli/dev-host/bridge.ts` returns >= 3 (WireOp union + translateOp case + shapeResult case)
    - `grep -n "debugFlowState" src/session/stateless-ops.ts` shows the Op-union entry, READ_ONLY_OP_TYPES membership, the handler, and the dispatch case
    - OpResult has `flowDebugInfo?: SerializedFlowDebugInfo` (grep in stateless-ops.ts) — the SAME shared type as the broadcast (no divergent structure)
    - handleDebugFlowState derives pendingAction from the passed-in pendingState only (own seat) — never reads other seats' pending state
    - `grep -c "debug:flow-state" src/ui/components/DebugPanel.vue` returns >= 1 (fetchFlowState issues the op) and the panel renders the readable `description`
    - stateless-ops.test.ts asserts flowDebugInfo.description is a non-empty string and out-of-range seat returns an error result
    - both vitest runs pass
  </acceptance_criteria>
  <done>The debug:* WS op family exposes flow position (same serialized structure as the broadcast + own-seat pending action), and the visual DebugPanel displays the readable flow position — the FLOW-01 locked debug:* channel delivered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Browser-verify devtools + DebugPanel flow/pending-action parity</name>
  <action>Pause for human verification per the how-to-verify steps below — no code changes in this task. This confirms BOTH introspection channels (the __BOARDSMITH_DEVTOOLS window bridge AND the visual DebugPanel debug:flow-state op) work end-to-end in a real browser (the manual-only verification recorded in 123-VALIDATION.md).</action>
  <what-built>FLOW-01/03 introspection parity via two channels: (1) the session broadcasts a serialized flow-debug snapshot (all seats) and each seat's own pending action (perspective-isolated), exposed as window.__BOARDSMITH_DEVTOOLS.getFlowDebugInfo() / getPendingAction(); (2) a debug:flow-state WS op returns the same structure and the visual DebugPanel renders the readable flow position.</what-built>
  <how-to-verify>
    1. From the go-fish game repo, start the dev host: `cd ~/BoardSmithGames/go-fish && npx boardsmith dev` (serves on :5173).
    2. Open the game in the browser (use the Claude Code Chrome extension). Start a game and take a turn so a flow position is active.
    3. In the GameShell iframe console, evaluate `window.__BOARDSMITH_DEVTOOLS.getFlowDebugInfo()` — confirm it returns an object with phase/step/path/awaiting and a `description` string matching the visible game position.
    4. Begin (but do not complete) a multi-step action if the game has one, then evaluate `window.__BOARDSMITH_DEVTOOLS.getPendingAction()` — confirm it reflects the in-progress selection for your seat.
    5. Open the visual Debug panel (toggle from the Dev header) and confirm the readable flow position (the description string, e.g. "phase X → step Y, waiting on seat N") is displayed in the flow-context area — sourced from the debug:flow-state op.
    6. Confirm both getters return null (not errors) when no flow/pending action applies.
    7. Kill the dev server when done (do not leave :5173 running).
  </how-to-verify>
  <resume-signal>Type "approved" once both the window getters AND the visual DebugPanel show correct values in the browser, or describe what was wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (GameSession) → client broadcast | Per-seat state crosses to each connected client; a seat must never receive another seat's hidden info |
| dev-host bridge (debug:* WS ops) → client | Debug ops carry a seat/player param; a debug op must not leak another seat's pending action |
| client → __BOARDSMITH_DEVTOOLS (dev only) | Devtools bridge exposes introspection to the browser console |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-07 | Information Disclosure | broadcast pendingAction field | mitigate | pendingAction is injected per-seat using effectivePosition (the connected seat), NEVER a loop exposing all seats. A dedicated multi-seat test asserts seat 2's broadcast contains no part of seat 1's pending-action args. This is the phase's primary flagged threat. |
| T-123-08 | Information Disclosure | flowDebugInfo field | accept | Flow position (phase/step/path/awaiting/description) is public game structure, not per-seat hidden info; safe to broadcast to all seats and spectators. |
| T-123-09 | Information Disclosure | __BOARDSMITH_DEVTOOLS getters | mitigate | Both new getters live inside the existing import.meta.env.DEV / isDevBuild guards and are dead-code-eliminated in production builds — never exposed in a production bundle (RESEARCH.md Security Domain). |
| T-123-10 | Information Disclosure | debug:flow-state op pendingAction | mitigate | The debug op derives pendingAction ONLY from the passed-in `pendingState` (the requesting seat's own persisted state threaded by the host), never by reading another seat's pending state. flowDebugInfo is public flow structure. The DebugPanel is a dev-only surface (mirrors the existing debug:* ops, all dev-only). Consistent with the broadcast's per-seat isolation (T-123-07). |
| T-123-SC | Tampering | npm/pip/cargo installs | accept | No package installs (internal TypeScript/Vue only). |
</threat_model>

<verification>
- `npx vitest run src/session/game-session.test.ts --silent` green, including the perspective-isolation test
- `npx vitest run src/session/stateless-ops.test.ts --silent` green, including the debug:flow-state cases
- `npm test` full suite green
- tsc clean on touched UI files
- Browser checkpoint: both the devtools getters AND the visual DebugPanel show correct values in the dev host (manual, per VALIDATION.md)
</verification>

<success_criteria>
- FLOW-01/03 introspection parity achieved via BOTH locked channels: getFlowDebugInfo()/getPendingAction() (window bridge) AND debug:flow-state op + visual DebugPanel (debug:* WS family)
- flowDebugInfo uses one shared serialized structure across broadcast, debug op, and devtools (no divergent shapes)
- pendingAction is perspective-isolated (own seat only) on both channels — proven by test, verified in browser
- Devtools additions are production-guarded and dead-code-eliminated
</success_criteria>

<output>
Create `.planning/phases/123-determinism-flow-introspection/123-04-SUMMARY.md` when done
</output>
