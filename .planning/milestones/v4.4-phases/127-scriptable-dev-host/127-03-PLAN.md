---
phase: 127-scriptable-dev-host
plan: 03
type: execute
wave: 2
depends_on: [127-01]
files_modified:
  - src/client/dev-host-client.ts
  - src/client/index.ts
  - src/cli/dev-host/dev-host.integration.test.ts
autonomous: true
requirements: [DRIVE-01, DRIVE-02, DRIVE-03]
must_haves:
  truths:
    - "A typed dev-host protocol client (createDevHostClient) speaks the dev host's hello/join/server_request/getState/getLobby/debugToggle/uiSwitch wire protocol from Node, using globalThis.WebSocket (or an injectable override)"
    - "createDevHostClient is exported from boardsmith/client alongside GameConnection (siblings, not one bilingual class)"
    - "A Node integration test spins up a real WebSocketServer({port:0}) wired to MultiplayerHost in-process (mirroring dev.ts) and drives the full agent flow: connect → hello → join → getLobby → getState → perform an action → debugToggle → uiSwitch"
    - "The integration test is the phase's literal acceptance proof — every DRIVE requirement exercised in one browserless flow"
  artifacts:
    - path: "src/client/dev-host-client.ts"
      provides: "createDevHostClient — typed dev-host WS protocol client with promise-based requestId correlation"
      exports: ["createDevHostClient"]
    - path: "src/cli/dev-host/dev-host.integration.test.ts"
      provides: "real-ws-server Node integration test — the DRIVE-01..03 acceptance proof"
      contains: "WebSocketServer"
    - path: "src/client/index.ts"
      provides: "createDevHostClient export"
      contains: "createDevHostClient"
  key_links:
    - from: "src/cli/dev-host/dev-host.integration.test.ts"
      to: "MultiplayerHost.handleMessage"
      via: "WebSocketServer({port:0}) connection → JSON.parse → handleMessage (dev.ts wiring, minus Vite httpServer)"
      pattern: "new WebSocketServer"
    - from: "src/client/dev-host-client.ts"
      to: "getState/getLobby requestId echo"
      via: "promise-based request/response correlation on requestId"
      pattern: "requestId"
---

<objective>
Deliver the utility agents actually need to script `boardsmith dev`, and prove the whole phase end-to-end:
1. `createDevHostClient` (DRIVE-02 amendment): a small typed Node/browser client speaking the dev host's real `hello`/`join`/`server_request`/`getState`/`getLobby`/`debugToggle`/`uiSwitch` protocol, exported from `boardsmith/client`.
2. A Node integration test (the phase's literal acceptance proof) that hosts a real `ws` server wired to `MultiplayerHost` in-process and drives the full browserless agent flow exercising DRIVE-01/02/03.

Purpose: The integration test IS the acceptance test for the phase promise ("drivable by a scripted, non-browser client"). The dev-host client is the reusable surface agents script against.
Output: `src/client/dev-host-client.ts`, its export from `index.ts`, and `src/cli/dev-host/dev-host.integration.test.ts`.

Scope fence (RESEARCH Pitfall 1 / anti-pattern): this client is a SEPARATE sibling of GameConnection — it must NOT be folded into GameConnection (which speaks the incompatible production protocol).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/127-scriptable-dev-host/127-CONTEXT.md
@.planning/phases/127-scriptable-dev-host/127-PATTERNS.md
@.planning/phases/127-scriptable-dev-host/127-01-SUMMARY.md

<interfaces>
<!-- The dev-host wire protocol createDevHostClient must speak (from Plan 01 + existing multiplayer-host.ts). -->

Outbound (client → host): `{type:'hello'}`, `{type:'join', seat, name?, color?}`, `{type:'leave'}`,
`{type:'restart'}`, `{type:'server_request', requestId, op, payload}`, `{type:'follow', enabled}`,
`{type:'getState', requestId?}`, `{type:'getLobby', requestId?}`, `{type:'debugToggle'}`, `{type:'uiSwitch', name}`

Inbound (host → client): `{type:'lobby', phase, seats, minPlayers, playerCount, requestId?}`,
`{type:'joined', seat}`, `{type:'error', message}`, `{type:'init', seat}`,
`{type:'game_state', view, isComplete, winners, requestId?}`, `{type:'server_response', requestId, result}`,
`{type:'follow', enabled, seat}`, `{type:'debugToggle'}`, `{type:'uiSwitch', name}`

<!-- Production wiring to replicate in the test — src/cli/commands/dev.ts:516-583, minus Vite httpServer. -->
`new WebSocketServer({ port: 0 })` standalone; on connection, JSON.parse each message; the first
`hello` registers the socket in a `clients: Map<string, WebSocket>` keyed by clientId and calls
`mpHost.handleMessage(clientId, {type:'hello'})`; subsequent messages forward to `handleMessage`;
close → `clients.delete` + `mpHost.disconnect(clientId)`. MultiplayerHost is constructed with
`{ playerCount, minPlayers, executeOp, send }` where `send(clientId, message)` writes to the mapped socket.

<!-- Correlation idea to borrow (different protocol): GameConnection.action() pendingActions Map + timeout, src/client/game-connection.ts:41-48, 117-153. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: createDevHostClient dev-host protocol client</name>
  <files>src/client/dev-host-client.ts, src/client/index.ts</files>
  <read_first>
    - src/cli/dev-host/DevHost.vue:105-137 (ws/wsSend/connect block — the same wire protocol, Vue-reactive; adapt to a plain class/closure)
    - src/client/game-connection.ts:41-48, 117-153 (pendingActions Map + timeout correlation idea to borrow — NOT the protocol)
    - src/client/index.ts:35-44 (export surface; GameConnection already exported line 38)
    - 127-PATTERNS.md "dev-host protocol client helper" (No Analog Found) + "src/client/index.ts export surface"; 127-RESEARCH.md anti-pattern "Building a second GameConnection-shaped class"
  </read_first>
  <action>
    Create `src/client/dev-host-client.ts` exporting `createDevHostClient(url, opts?)`. Use `opts.wsImplementation ?? globalThis.WebSocket` with the same fail-loud actionable guard style as Plan 02 (name Node >=22.4 + the override). Provide a typed async surface over the dev-host protocol: `hello()`, `join(seat, opts?)`, `getState(): Promise<GameStateReply>`, `getLobby(): Promise<LobbyReply>`, `serverRequest(op, payload): Promise<result>`, `debugToggle()`, `uiSwitch(name)`, `close()`. For getState/getLobby/serverRequest, generate a `requestId`, send, and resolve the returned promise when the matching `requestId` echoes back (a pendingRequests Map keyed by requestId with a timeout that rejects with an actionable message — borrow the shape from GameConnection.action(), not its protocol). Fire-and-forget ops (debugToggle/uiSwitch/join/leave) need no correlation. Keep it a plain closure/class; do NOT extend or reuse GameConnection.

    Export from index.ts: `export { createDevHostClient } from './dev-host-client.js';` alongside GameConnection (siblings).
  </action>
  <verify>
    <automated>npx vitest run src/cli/dev-host/dev-host.integration.test.ts</automated>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "dev-host-client" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `createDevHostClient` is exported from src/client/index.ts (grep confirms)
    - getState/getLobby resolve via requestId correlation with a timeout-reject fallback
    - No import of or extension of GameConnection in dev-host-client.ts (grep confirms separation)
  </acceptance_criteria>
  <done>createDevHostClient speaks the dev-host protocol with promise correlation and is exported from boardsmith/client.</done>
</task>

<task type="auto">
  <name>Task 2: Node integration test — full browserless agent flow</name>
  <files>src/cli/dev-host/dev-host.integration.test.ts</files>
  <read_first>
    - src/cli/commands/dev.ts:516-583 (WebSocketServer + MultiplayerHost wiring, hello-first handshake, clients Map) — replicate minus the Vite httpServer/noServer upgrade routing; use `new WebSocketServer({ port: 0 })` standalone
    - src/cli/dev-host/multiplayer-host.test.ts:1-40 (how a minimal PassGame + executeOp are constructed for the host)
    - src/cli/dev-host/dev-host.integration.test.ts's sibling createDevHostClient (Task 1) — the driving client
    - 127-PATTERNS.md "integration.test.ts" section (lines 293-327); 127-RESEARCH.md Validation Architecture + Don't Hand-Roll (reuse ws WebSocketServer, not a custom transport)
  </read_first>
  <action>
    Create `src/cli/dev-host/dev-host.integration.test.ts` (default Node vitest environment). In `beforeAll`/setup: build a minimal PassGame gameDefinition + real `executeOp` (mirror multiplayer-host.test.ts), construct `MultiplayerHost`, and stand up `new WebSocketServer({ port: 0 })` wired exactly as dev.ts does (connection → JSON.parse → hello registers clientId in a clients Map → forward to handleMessage; close → disconnect). Read the assigned port from `wss.address()`.

    Drive the full agent flow with `createDevHostClient` over a REAL socket (globalThis.WebSocket, Node 22.4+): connect → hello → getLobby (assert lobby payload in lobby phase, requestId correlated) → join a seat → (start the game via the host's normal path so phase becomes 'playing') → getState (assert it returns THIS seat's view with requestId echoed, and includes the Phase 123/126 enrichments if present on the view) → perform an action via serverRequest (assert server_response) → debugToggle + uiSwitch (assert they are accepted without error; with a second connected client, assert the relay is received). Tear down the server and all sockets in `afterAll` (never leave a server running).

    This test is the phase acceptance proof — it must exercise DRIVE-01 (getState/getLobby), DRIVE-02 (browserless Node client over real WS), and DRIVE-03 (debugToggle/uiSwitch) in one flow.
  </action>
  <verify>
    <automated>npx vitest run src/cli/dev-host/dev-host.integration.test.ts</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - Test connects over a real ws socket (no browser, no jsdom) to a WebSocketServer({port:0}) wired to MultiplayerHost
    - getLobby succeeds in lobby phase; getState after join+start returns the caller's own seat view with requestId echoed
    - An action performed via serverRequest yields a server_response; debugToggle/uiSwitch are accepted and (with a 2nd client) relayed
    - Server + sockets are closed in afterAll (no leaked handles); `npm test` fully green
  </acceptance_criteria>
  <done>The browserless Node integration test drives connect→join→getLobby→getState→action→debugToggle→uiSwitch against a real in-process dev-host WS server; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| createDevHostClient (Node script) → MultiplayerHost (real WS) | Scripted agent input crosses the same untrusted, unauthenticated boundary as a browser tab (pre-existing loopback dev-tool model) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-07 | Information Disclosure | integration test getState assertion | mitigate | Test asserts getState returns ONLY the caller's own seat view (Plan 01's server-tracked seat resolution) — regression guard against a cross-seat leak (V4 Access Control) |
| T-127-08 | Denial of Service | leaked WS server/sockets after test | mitigate | afterAll closes the WebSocketServer and all client sockets — no lingering listeners/ports (CLAUDE.md: never leave a server running) |
| T-127-06 | Tampering | injected wsImplementation | accept | Same as Plan 02: opt-in developer escape hatch, no privilege boundary crossed |
| T-127-SC | Tampering | npm installs | accept | Zero new dependencies — `ws` (server) already present, `globalThis.WebSocket` (client) native |
</threat_model>

<verification>
- `npx vitest run src/cli/dev-host/dev-host.integration.test.ts` green (the acceptance proof)
- `npm test` fully green
- `grep -n "GameConnection" src/client/dev-host-client.ts` returns nothing (client stays a separate sibling)
- No dev server / WS server left running after the test (afterAll teardown asserted)
</verification>

<success_criteria>
- createDevHostClient exported from boardsmith/client, speaks the dev-host protocol with requestId correlation
- Browserless Node integration test exercises DRIVE-01/02/03 end-to-end against a real in-process dev-host WS server
- Zero new dependencies; GameConnection and dev-host-client remain separate
</success_criteria>

<output>
Create `.planning/phases/127-scriptable-dev-host/127-03-SUMMARY.md` when done
</output>
