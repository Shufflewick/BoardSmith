---
phase: 128-animation-drag-drop-test-story
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/composables/useBoardInteraction.ts
  - src/ui/composables/anchorAttrs.test.ts
  - src/ui/composables/useDragDrop.test.ts
autonomous: true
requirements: [ANIM-02, ANIM-03]

must_haves:
  truths:
    - "A custom board that renders a selectable element without any anchor identity gets a dev-only, once-per-type console warning"
    - "The warning fires from the single source of truth (anchorAttrs) so both useSelectable and useSelectableGrid callers are covered"
    - "useDragDrop has direct unit tests at both the boardInteraction API level and the jsdom drag-event-shaped-object level"
    - "useDragDrop's existing degrade-gracefully (warnNoProvider) behavior is unchanged — no throw added"
  artifacts:
    - path: "src/ui/composables/useBoardInteraction.ts"
      provides: "anchorAttrs() dev-warning on empty anchor result (ANIM-03 deferred v4.1 item)"
      contains: "anchorattrs-missing"
    - path: "src/ui/composables/anchorAttrs.test.ts"
      provides: "Extended coverage for the anchorAttrs dev-warning (new describe block)"
    - path: "src/ui/composables/useDragDrop.test.ts"
      provides: "Direct unit tests for useDragDrop (ANIM-02)"
  key_links:
    - from: "src/ui/composables/useSelectable.ts"
      to: "src/ui/composables/useBoardInteraction.ts"
      via: "anchorAttrs() call — warning fires here for empty refs"
      pattern: "anchorAttrs"
---

<objective>
Close the deferred v4.1 `anchorAttrs` dev-warning (ANIM-03) and give `useDragDrop` its first direct unit tests (ANIM-02). Both are independent of the trace module (no test-mode branch), so this plan runs in Wave 1 in parallel with 128-01.

Purpose: ANIM-03 (custom-board anchor warning) + ANIM-02 (useDragDrop coverage).
Output: warning inside `anchorAttrs()`, extended `anchorAttrs.test.ts`, new `useDragDrop.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/128-animation-drag-drop-test-story/128-CONTEXT.md
@.planning/phases/128-animation-drag-drop-test-story/128-RESEARCH.md
@.planning/phases/128-animation-drag-drop-test-story/128-PATTERNS.md

<interfaces>
<!-- anchorAttrs is the single source of truth (useBoardInteraction.ts:396-414). -->
```typescript
export interface ElementRef { id?: number|string; notation?: string; name?: string; className?: string }
export function anchorAttrs(ref: ElementRef): Record<string, string>;
// Emits data-bs-el-id / data-bs-el-notation / data-bs-el-name. Empty {} when id+notation+name all undefined.
```
<!-- devWarn dedups by key, dev-only (src/utils/dev.ts) -->
```typescript
export function devWarn(key: string, message: string): void; // fires once per key, dev-mode only
export function _clearShownWarnings(): void;                  // test reset
```
</interfaces>

@src/utils/dev.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fire a once-per-type dev-warning from anchorAttrs() on empty anchor result</name>
  <files>src/ui/composables/useBoardInteraction.ts, src/ui/composables/anchorAttrs.test.ts</files>
  <read_first>
    - src/ui/composables/useBoardInteraction.ts:396-414 — `anchorAttrs()` implementation (the single source of truth; the empty-result branch is the warning site)
    - src/ui/composables/useBoardInteraction.ts:371-382 — `triggerChoiceSelect` devWarn with STATIC key: the CORRECT pattern to copy
    - src/ui/composables/useDragDropTargets.ts:214-234 — `warnNoTargets` per-instance key: the WRONG pattern (negative example, do not imitate for this warning)
    - src/ui/composables/anchorAttrs.test.ts (full) — existing describe blocks for anchorAttrs / useSelectable.attrs / useSelectableGrid; EXTEND this file, do not duplicate (PATTERNS "Note on registration-warning test coverage")
    - src/ui/composables/useSelectable.ts:41-70, 152 — proves both useSelectable.attrs and useSelectableGrid.cellAttrs route through anchorAttrs(), so warning here covers both callers automatically
    - src/utils/dev.ts — devWarn (dedup-by-key, dev-only) + _clearShownWarnings reset
  </read_first>
  <action>
    In `anchorAttrs()` (useBoardInteraction.ts:396-414), after building the attrs record, detect the empty result (no `data-bs-el-*` keys were added, i.e. `ref.id`, `ref.notation`, and `ref.name` are all undefined) and call `devWarn` with a TYPE-LEVEL dedup key: `` `anchorattrs-missing-${ref.className ?? ref.name ?? 'unknown'}` `` (per RESEARCH Pitfall 5 — key by type, NEVER by id, or a 50-card board emits 50 warnings). The message must be actionable, matching the multi-line what/why/how style of `warnNoTargets`: name that a selectable/renderable element produced no anchor attributes, explain that custom boards must spread `anchorAttrs(ref)` (or `v-bind="attrs"` from `useSelectable`) onto each element root so FLIP/flying-element animations and drag-drop can find it, and that without it animations silently no-op and traces cannot identify the element. Import `devWarn` from `../../utils/dev.js` if not already imported. Do NOT throw — this is a dev warning, not a fail-loud throw (the throw path is for animation helpers in plans 03-06). Then EXTEND `anchorAttrs.test.ts` with a new `describe('anchorAttrs dev-warning', ...)` block.
  </action>
  <verify>
    <automated>npx vitest run src/ui/composables/anchorAttrs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: calling `anchorAttrs({})` (empty ref) once triggers exactly one `console.warn` (spy) whose message contains "anchorAttrs"; a second call with a same-typed empty ref does NOT warn again (dedup) — reset via `_clearShownWarnings()` in beforeEach
    - Test: `anchorAttrs({ id: 5 })` produces `data-bs-el-id` and emits NO warning
    - `grep -q "anchorattrs-missing" src/ui/composables/useBoardInteraction.ts` succeeds
    - The warning site calls `devWarn` (dev-only gate) and does not `throw`
  </acceptance_criteria>
  <done>anchorAttrs() emits a dev-only, once-per-type warning on empty anchor result; extended tests green; no throw added.</done>
</task>

<task type="auto">
  <name>Task 2: Direct unit tests for useDragDrop (API-level + jsdom drag-event-shaped object)</name>
  <files>src/ui/composables/useDragDrop.test.ts</files>
  <read_first>
    - src/ui/composables/useDragDrop.ts:93-107 — `warnNoProvider` doc-comment + devWarn: the deliberate degrade-gracefully design that must NOT be changed (RESEARCH Pitfall 4)
    - src/ui/composables/useDragDrop.ts:212-266 — `dragProps`/`dropProps` handler shapes the fake-event object must satisfy (reads `e.dataTransfer?.setData`, `.effectAllowed`, `.dropEffect`, `e.preventDefault()`, `e.currentTarget`, `e.relatedTarget`)
    - src/ui/components/auto-ui/renderers/drag-keyboard-parity.test.ts:108-134 — API-level driving pattern: createBoardInteraction → startDrag/setDropTargets/triggerDrop → assert onDrop callback
    - src/ui/components/helpers/HintOverlay.test.ts:28-45 — matchMedia + ResizeObserver `vi.stubGlobal` stubs to place BEFORE composable imports (RESEARCH Pitfall 1)
  </read_first>
  <action>
    Create `src/ui/composables/useDragDrop.test.ts` with `// @vitest-environment jsdom` pragma. Place the `vi.stubGlobal('matchMedia', ...)` and `vi.stubGlobal('ResizeObserver', ...)` stubs (copy verbatim from HintOverlay.test.ts:28-45) as the FIRST statements, ABOVE the static composable imports (Pitfall 1 — the composables transitively read matchMedia at module load). Cover two levels per CONTEXT.md: (1) API-level parity path — build a board interaction via `createBoardInteraction`, drive `startDrag`/`setDropTargets`/`triggerDrop` and assert the `onDrop` callback fires with the right target id, `isDragging` transitions, and `dropTargets` resets (mirror drag-keyboard-parity.test.ts:108-134); (2) event-level path — call `dragProps(ref).onDragstart(fakeEvent)` and `dropProps(target).onDragover(fakeEvent)`/`.onDrop(fakeEvent)` directly with a hand-built plain object shaped like the subset the handlers read (`{ dataTransfer: { setData: vi.fn(), getData: vi.fn(), effectAllowed: '', dropEffect: '' }, preventDefault: vi.fn(), currentTarget: el, relatedTarget: null }`) — NOT `element.dispatchEvent(new DragEvent(...))`, since jsdom lacks the DragEvent/DataTransfer constructors (RESEARCH probe). Assert the handlers call `dataTransfer.setData`/set `effectAllowed` and call `preventDefault` on dragover. Also add one test proving the degrade-gracefully path: `useDragDrop` with no provider emits the `useDragDrop-no-provider` devWarn (spy) and does NOT throw. Do NOT modify `useDragDrop.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/ui/composables/useDragDrop.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/composables/useDragDrop.test.ts` passes
    - Test file drives BOTH the `boardInteraction.startDrag()/triggerDrop()` API path AND a directly-invoked `dragProps(ref).onDragstart(fakeEvent)` handler
    - A test asserts the no-provider path warns (devWarn) and does not throw
    - `git diff --stat src/ui/composables/useDragDrop.ts` shows no change (test-only per Pitfall 4)
  </acceptance_criteria>
  <done>useDragDrop has passing direct unit tests at both levels; source composable unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| custom board → animation/drag pipeline | A misconfigured element (no anchor) crosses into animation/drag lookups |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-03 | Denial of Service | anchorAttrs warning path | mitigate | Warning uses `devWarn` (dev-only gate); in production it is a no-op, so a live game never crashes or spams console on a cosmetic anchor gap — the throw path is reserved for the animation helpers (plans 03-06) |
| T-128-04 | Tampering | useDragDrop existing behavior | accept | Plan is test-only for useDragDrop; the deliberate warn-not-throw degrade-gracefully design (Pitfall 4) is preserved — a test asserts it stays a devWarn |
| T-128-SC | Tampering | package installs | accept | No package installs this phase |
</threat_model>

<verification>
- `npx vitest run src/ui/composables/anchorAttrs.test.ts src/ui/composables/useDragDrop.test.ts` green
- useDragDrop.ts unchanged (git diff)
- warning uses a type-level dedup key, not per-id
</verification>

<success_criteria>
Custom boards missing `anchorAttrs` get a dev-time once-per-type warning from the single source of truth; useDragDrop has direct unit tests at both the API and event levels without altering its graceful-degradation design.
</success_criteria>

<output>
Create `.planning/phases/128-animation-drag-drop-test-story/128-02-SUMMARY.md` when done.
</output>
