---
phase: 128-animation-drag-drop-test-story
plan: 03
type: execute
wave: 2
depends_on: [128-01]
files_modified:
  - src/ui/composables/useFLIP.ts
  - src/ui/composables/useFLIP.test.ts
autonomous: true
requirements: [ANIM-01, ANIM-02, ANIM-03]

must_haves:
  truths:
    - "In test mode, useFLIP.animate() resolves instantly and records one 'flip' trace per moved element instead of running WAAPI"
    - "The test-mode branch sits ABOVE the prefersReducedMotion branch and is never merged with it"
    - "When a captured element lacks its id/anchor attribute at first resolution, useFLIP throws (dev) / console.error+skips (prod) with an actionable message"
    - "useFLIP has passing direct unit tests covering test-mode, the mocked-WAAPI/RAF real path, and the throw path"
  artifacts:
    - path: "src/ui/composables/useFLIP.ts"
      provides: "Test-mode trace branch + fail-loud anchor throw"
      contains: "isAnimationTestModeEnabled"
    - path: "src/ui/composables/useFLIP.test.ts"
      provides: "Direct unit tests (test-mode + real path + throw)"
  key_links:
    - from: "src/ui/composables/useFLIP.ts"
      to: "src/ui/composables/useAnimationTestMode.ts"
      via: "isAnimationTestModeEnabled() + recordTrace() in animate()"
      pattern: "recordTrace"
---

<objective>
Add the test-mode trace branch and fail-loud anchor throw to `useFLIP`, and give it its first direct unit tests.

Purpose: ANIM-01/02/03 for useFLIP.
Output: modified `useFLIP.ts` + new `useFLIP.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-animation-drag-drop-test-story/128-CONTEXT.md
@.planning/phases/128-animation-drag-drop-test-story/128-RESEARCH.md
@.planning/phases/128-animation-drag-drop-test-story/128-PATTERNS.md

<interfaces>
<!-- From 128-01 (dependency). -->
```typescript
export interface AnimationTrace { kind: 'flip'|'fly'|'element'|'action'; element: string|null; from: string|undefined; to: string|undefined; meta?: Record<string, unknown>; }
export function isAnimationTestModeEnabled(): boolean;
export function recordTrace(entry: AnimationTrace): void;
```
<!-- isDevMode() gate for the fail-loud throw (src/utils/dev.ts) -->
```typescript
export function isDevMode(): boolean;
```
</interfaces>

@src/utils/dev.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add test-mode trace branch + fail-loud anchor throw to useFLIP</name>
  <files>src/ui/composables/useFLIP.ts</files>
  <read_first>
    - src/ui/composables/useFLIP.ts:56-60 — import block + prefersReducedMotion re-export (add the test-mode import alongside)
    - src/ui/composables/useFLIP.ts:242-250 — `animate()` prefersReducedMotion branch: insert the test-mode branch ABOVE it (PATTERNS "insertion point")
    - src/ui/composables/useFLIP.ts:221-236 — `capture()` querySelectorAll silent-empty: the FIRST-resolution throw site
    - src/ui/composables/useFLIP.ts:264-269 — per-element `if (!id) return;` / `if (!oldRect) return;` silent skips: the two most surgical throw sites
    - src/ui/composables/useFLIP.ts:146-154 — `getElementId(el)` identity priority (data-card-id → data-piece-id → data-element-id → id): the trace `element` value + throw message references this attribute set
    - src/ui/composables/useFlyingElements.ts:390-401 — `normalizeRect`'s existing throw: the fail-loud WORDING template to match
    - src/ui/composables/useDragDropTargets.ts:214-234 — actionable multi-line what/why/how diagnostic style
    - src/utils/dev.ts — `isDevMode()` gate to reuse (do NOT duplicate a fresh import.meta.env check)
  </read_first>
  <behavior>
    - Test mode ON: animate() records one AnimationTrace{kind:'flip', element:getElementId(el), from:containerName, to:containerName, meta:{deltaX,deltaY}} per moved element, clears captured positions, and returns WITHOUT calling el.animate()
    - Test-mode branch is checked BEFORE the prefersReducedMotion branch (independent flags)
    - First-resolution missing anchor: an element the caller asked to animate that has no id (getElementId returns null) triggers the fail-loud path — throw in dev (isDevMode true), console.error + skip in prod
    - The per-frame / already-captured re-check is NOT a throw site (only first resolution)
  </behavior>
  <action>
    Import `isAnimationTestModeEnabled`, `recordTrace` from `./useAnimationTestMode.js` and `isDevMode` from `../../utils/dev.js` in useFLIP.ts (alongside the existing imports at lines 56-60). In `animate()` (line ~242), insert a test-mode branch ABOVE the `prefersReducedMotion` check: if `isAnimationTestModeEnabled()`, iterate the handlers' captured elements, `recordTrace` one `{kind:'flip'}` entry per element (element = `getElementId(el)`, from/to = the container's own identity/name available to the handler — use the container's `data-*` id via getElementId on the container element, or the handler's selector string as a stable label; meta carries `deltaX`/`deltaY` when computable, else omit), clear `handler.positions`, and `return`. For the fail-loud throw: at the FIRST resolution site (`capture()` at 221-236 producing an empty NodeList for a selector the caller registered, AND the per-element `if (!id) return;` at line 265), when an element that was explicitly asked to animate lacks its anchor id, build an actionable message naming the composable (`useFLIP`), the selector/attribute set searched (`data-card-id`/`data-piece-id`/`data-element-id`/`id`), and how to fix (spread `anchorAttrs(ref)` / bind `useSelectable` attrs). Gate with `isDevMode()`: `if (isDevMode()) throw new Error(msg); else console.error(msg);` then skip that element. Leave the `if (!oldRect) return;` per-frame/re-check path (line 268) as a silent skip (legitimate: element captured but not re-found is a transient render state, per RESEARCH Pitfall 3). Match `normalizeRect`'s wording style.
  </action>
  <verify>
    <automated>grep -q "isAnimationTestModeEnabled" src/ui/composables/useFLIP.ts && grep -q "isDevMode" src/ui/composables/useFLIP.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Test-mode branch textually precedes the `prefersReducedMotion` branch in `animate()`
    - Fail-loud site uses `isDevMode()` (not a fresh `import.meta.env` check) and throws in dev / console.error in prod
    - `if (!oldRect) return;` per-frame path remains a silent skip (not a throw)
    - Throw message names the composable + the searched attribute set + a fix
  </acceptance_criteria>
  <done>useFLIP records flip traces in test mode and fails loud on first-resolution missing anchors, dev-gated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Direct unit tests for useFLIP (test-mode + mocked-WAAPI real path + throw)</name>
  <files>src/ui/composables/useFLIP.test.ts</files>
  <read_first>
    - src/ui/composables/useAnimationEvents.test.ts:1-40 — composable-unit-test shape + `waitForIdle` polling helper
    - src/ui/components/helpers/HintOverlay.test.ts:28-45 — matchMedia + ResizeObserver stubs (place FIRST, before composable imports — Pitfall 1)
    - RESEARCH Pitfall 2 (128-RESEARCH.md:220-234) — jsdom getBoundingClientRect always zero; real-path tests MUST stub `Element.prototype.getBoundingClientRect` to return distinct before/after rects or the threshold check silently no-ops
    - RESEARCH Don't-Hand-Roll (128-RESEARCH.md:180) — jsdom `element.animate` is undefined; real-path tests MUST stub `HTMLElement.prototype.animate = () => ({ finished: Promise.resolve(), cancel(){} })`
  </read_first>
  <behavior>
    - Test-mode: enable, capture a container with a `[data-element-id]` child, move it, animate() → getAnimationTrace() contains a {kind:'flip'} entry whose `element` equals the child's id; assert by IDENTITY not pixels (jsdom rects are zero)
    - Real path (mocked WAAPI + distinct rects): with `Element.prototype.getBoundingClientRect` stubbed to distinct before/after and `HTMLElement.prototype.animate` stubbed, animate() invokes the stubbed `animate` for the moved element (spy called) — this satisfies CONTEXT's "RAF/WAAPI chain gets at least one real-path test"
    - Throw path: in dev mode, capturing/animating an element registered by selector but missing its id attribute throws an Error naming the attribute; a prod-mode variant (stub isDevMode false) console.errors instead of throwing
  </behavior>
  <action>
    Create `src/ui/composables/useFLIP.test.ts` with `// @vitest-environment jsdom`. Put matchMedia + ResizeObserver `vi.stubGlobal` calls FIRST (verbatim from HintOverlay.test.ts), before importing useFLIP. Import `enableAnimationTestMode`/`disableAnimationTestMode`/`getAnimationTrace`/`clearAnimationTrace` from `./useAnimationTestMode.js` and reset them in beforeEach/afterEach. Write the three <behavior> groups. For the real-path test, stub `HTMLElement.prototype.animate` and `Element.prototype.getBoundingClientRect` in a beforeEach and restore in afterEach; assert the animate spy was called (positional/pixel assertions are NOT reliable in jsdom — assert the call happened and that a trace was NOT recorded in the non-test-mode path). For the throw test, control dev-mode: if `isDevMode()` cannot be forced via env in-test, stub the module boundary (e.g. `vi.mock('../../utils/dev.js', ...)` exposing a settable flag) — assert `expect(() => animate-trigger).toThrow(/data-element-id|anchor/)` in dev and a `console.error` spy in prod.
  </action>
  <verify>
    <automated>npx vitest run src/ui/composables/useFLIP.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/composables/useFLIP.test.ts` passes
    - A test asserts a `{kind:'flip'}` trace with the moved element's identity (not a pixel value)
    - A real-path test asserts the stubbed `HTMLElement.prototype.animate` spy was called (mocked-WAAPI path exercised)
    - A throw-path test asserts dev throws / prod console.errors on a missing-anchor element
  </acceptance_criteria>
  <done>useFLIP.test.ts green across test-mode, mocked-WAAPI real path, and the fail-loud throw.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composable → DOM lookup | A missing anchor attribute crosses into querySelector/getAttribute and was previously a silent no-op |
| test mode → production bundle | The instant/trace branch must never activate in a shipped game |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-01 | Tampering | useFLIP test-mode branch | mitigate | Branch gated solely by `isAnimationTestModeEnabled()` (default OFF, 128-01); placed above but independent of prefersReducedMotion — a shipped game never enters it |
| T-128-02 | Information disclosure | flip trace contents | mitigate | Trace `element` is the DOM id string from getElementId; meta carries deltaX/deltaY only — no card face/hidden attributes recorded |
| T-128-03 | Denial of Service | fail-loud throw in production | mitigate | Throw gated by `isDevMode()`; production path is `console.error` + skip so a cosmetic anchor gap never crashes a live game |
| T-128-SC | Tampering | package installs | accept | No package installs this phase |
</threat_model>

<verification>
- `npx vitest run src/ui/composables/useFLIP.test.ts` green
- Test-mode branch above prefersReducedMotion (code read)
- Throw dev-gated via isDevMode()
</verification>

<success_criteria>
useFLIP resolves instantly with an assertable flip trace in test mode, fails loud (dev) on first-resolution missing anchors, and has direct unit tests covering all three paths.
</success_criteria>

<output>
Create `.planning/phases/128-animation-drag-drop-test-story/128-03-SUMMARY.md` when done.
</output>
