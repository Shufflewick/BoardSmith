---
phase: 128-animation-drag-drop-test-story
plan: 04
type: execute
wave: 2
depends_on: [128-01]
files_modified:
  - src/ui/composables/useElementAnimation.ts
  - src/ui/composables/useElementAnimation.test.ts
autonomous: true
requirements: [ANIM-01, ANIM-02, ANIM-03]

must_haves:
  truths:
    - "In test mode, useElementAnimation.animateToCurrentPositions() resolves instantly and records an 'element' trace per moved element"
    - "The test-mode branch sits ABOVE the prefersReducedMotion branch"
    - "First-resolution missing anchor (element lacks data-element-id) throws (dev) / console.error+skips (prod) with an actionable message"
    - "useElementAnimation has passing direct unit tests covering test-mode, the mocked-RAF real path, and the throw path"
  artifacts:
    - path: "src/ui/composables/useElementAnimation.ts"
      provides: "Test-mode trace branch + fail-loud anchor throw"
      contains: "isAnimationTestModeEnabled"
    - path: "src/ui/composables/useElementAnimation.test.ts"
      provides: "Direct unit tests (test-mode + mocked-RAF real path + throw)"
  key_links:
    - from: "src/ui/composables/useElementAnimation.ts"
      to: "src/ui/composables/useAnimationTestMode.ts"
      via: "isAnimationTestModeEnabled() + recordTrace() in animateToCurrentPositions()"
      pattern: "recordTrace"
---

<objective>
Add the test-mode trace branch and fail-loud anchor throw to `useElementAnimation`, and give it its first direct unit tests. This composable owns the module-load-time `prefersReducedMotion` ref (the matchMedia read tests must stub around).

Purpose: ANIM-01/02/03 for useElementAnimation.
Output: modified `useElementAnimation.ts` + new `useElementAnimation.test.ts`.

Discretion note (RESEARCH Open Question 1 / Assumption A2): this composable has no first-class named-container concept. Default to trace entries with `from`/`to` = the container's own `data-element-id` (or `undefined` if the container itself has none) rather than inventing a new public `containerName` param — do NOT expand the public API unless a concrete test needs it. Element identity per moved child is still recorded via `data-element-id`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-animation-drag-drop-test-story/128-CONTEXT.md
@.planning/phases/128-animation-drag-drop-test-story/128-RESEARCH.md
@.planning/phases/128-animation-drag-drop-test-story/128-PATTERNS.md

<interfaces>
```typescript
export interface AnimationTrace { kind: 'flip'|'fly'|'element'|'action'; element: string|null; from: string|undefined; to: string|undefined; meta?: Record<string, unknown>; }
export function isAnimationTestModeEnabled(): boolean;
export function recordTrace(entry: AnimationTrace): void;
export function isDevMode(): boolean;
```
</interfaces>

@src/utils/dev.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add test-mode trace branch + fail-loud anchor throw to useElementAnimation</name>
  <files>src/ui/composables/useElementAnimation.ts</files>
  <read_first>
    - src/ui/composables/useElementAnimation.ts:34-46 — module-load-time `prefersReducedMotion` matchMedia read (context; the reason Pitfall 1 stubbing exists)
    - src/ui/composables/useElementAnimation.ts:84-120 — `animateToCurrentPositions()`: prefersReducedMotion branch (test-mode goes ABOVE), and the two silent-skip sites `if (!id) return;` (line 100) / `if (!startPos) return;` (line 103)
    - src/ui/composables/useElementAnimation.ts:66,99 — `el.getAttribute('data-element-id')` identity source for traces + throw message
    - src/ui/composables/useFlyingElements.ts:390-401 — existing throw wording template
    - src/utils/dev.ts — `isDevMode()` gate to reuse
  </read_first>
  <behavior>
    - Test mode ON: animateToCurrentPositions() records one {kind:'element', element:data-element-id, from:container id/undefined, to:container id/undefined, meta:{deltaX,deltaY}} per moved element, clears positions, returns without running the RAF loop
    - Test-mode branch checked BEFORE prefersReducedMotion
    - First-resolution missing anchor: element the caller asked to animate lacks `data-element-id` → dev throw / prod console.error+skip
    - `if (!startPos) return;` (element not previously captured) remains a silent skip (transient, not misconfiguration)
  </behavior>
  <action>
    Import `isAnimationTestModeEnabled`, `recordTrace` from `./useAnimationTestMode.js` and `isDevMode` from `../../utils/dev.js`. In `animateToCurrentPositions()` (line ~84), insert a test-mode branch ABOVE the `prefersReducedMotion` check: iterate the container's captured elements, `recordTrace` one `{kind:'element'}` per moved element (element = `el.getAttribute('data-element-id')`, from/to = container's own `data-element-id` or `undefined` per the discretion note, meta = deltaX/deltaY when computable), clear positions, `return`. Upgrade the FIRST-resolution `if (!id) return;` (line 100) to the fail-loud path: build an actionable message naming `useElementAnimation`, the searched attribute (`data-element-id`), and the fix (ensure the element carries its anchor via `anchorAttrs`/`useSelectable`); gate with `isDevMode()` (throw dev / console.error prod) then skip. Leave `if (!startPos) return;` (line 103) as a silent skip. Match normalizeRect's wording style.
  </action>
  <verify>
    <automated>grep -q "isAnimationTestModeEnabled" src/ui/composables/useElementAnimation.ts && grep -q "isDevMode" src/ui/composables/useElementAnimation.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Test-mode branch precedes the prefersReducedMotion branch in `animateToCurrentPositions()`
    - Fail-loud site uses `isDevMode()`; `if (!startPos) return;` stays a silent skip
    - Public API of useElementAnimation is unchanged (no new required param — discretion note honored)
    - Throw message names the composable + `data-element-id` + a fix
  </acceptance_criteria>
  <done>useElementAnimation records element traces in test mode and fails loud on first-resolution missing anchors, dev-gated, no API expansion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Direct unit tests for useElementAnimation (test-mode + mocked-RAF real path + throw)</name>
  <files>src/ui/composables/useElementAnimation.test.ts</files>
  <read_first>
    - src/ui/composables/useAnimationEvents.test.ts:1-40 — test shape + waitForIdle polling helper
    - src/ui/components/helpers/HintOverlay.test.ts:28-45 — matchMedia + ResizeObserver stubs FIRST (Pitfall 1 — this composable is the module-load-time matchMedia reader)
    - RESEARCH Don't-Hand-Roll (128-RESEARCH.md:179) — RAF chain: prefer `vi.stubGlobal('requestAnimationFrame', ...)` with a manually-ticked queue over fake timers (fake timers do not auto-tick RAF)
    - RESEARCH Pitfall 2 (128-RESEARCH.md:220-234) — jsdom getBoundingClientRect zero; real-path tests MUST stub distinct before/after rects or the threshold check no-ops
  </read_first>
  <behavior>
    - Test-mode: capturePositions(container) then move a `[data-element-id]` child then animateToCurrentPositions(container) → getAnimationTrace() has a {kind:'element'} entry with the child's id (assert by identity, not pixels)
    - Real path (mocked RAF + distinct rects): with requestAnimationFrame stubbed to a manual queue and getBoundingClientRect stubbed distinct, driving the RAF ticks advances the animation (assert isAnimating toggled true then settled) — satisfies CONTEXT's mocked-RAF requirement
    - Throw path: dev-mode animate on an element missing data-element-id throws naming the attribute; prod-mode console.errors
  </behavior>
  <action>
    Create `src/ui/composables/useElementAnimation.test.ts` with `// @vitest-environment jsdom`. matchMedia + ResizeObserver `vi.stubGlobal` FIRST, before the import of useElementAnimation. Reset the trace module in beforeEach/afterEach. Implement the three <behavior> groups. For the mocked-RAF real path, install `vi.stubGlobal('requestAnimationFrame', cb => queue.push(cb))` plus a manual tick driver, and stub `Element.prototype.getBoundingClientRect` for distinct before/after rects; assert `isAnimating` transitions (identity/state assertions, not pixel geometry). For the throw path, force dev/prod via the dev-module boundary (`vi.mock('../../utils/dev.js', ...)` with a settable isDevMode) and assert `toThrow(/data-element-id/)` in dev and a console.error spy in prod.
  </action>
  <verify>
    <automated>npx vitest run src/ui/composables/useElementAnimation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/composables/useElementAnimation.test.ts` passes
    - A test asserts a `{kind:'element'}` trace by element identity
    - A mocked-RAF real-path test drives the RAF queue and asserts animation state transitions (not pixels)
    - A throw-path test asserts dev throws / prod console.errors on a missing `data-element-id`
  </acceptance_criteria>
  <done>useElementAnimation.test.ts green across test-mode, mocked-RAF real path, and the fail-loud throw.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composable → DOM lookup | Missing `data-element-id` was a silent no-op |
| test mode → production bundle | Instant/trace branch must never activate in a shipped game |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-01 | Tampering | useElementAnimation test-mode branch | mitigate | Gated by `isAnimationTestModeEnabled()` (default OFF); independent of prefersReducedMotion |
| T-128-02 | Information disclosure | element trace contents | mitigate | Trace `element` is the data-element-id string; meta carries deltaX/deltaY only |
| T-128-03 | Denial of Service | fail-loud throw in production | mitigate | Throw gated by `isDevMode()`; prod path is console.error + skip |
| T-128-SC | Tampering | package installs | accept | No package installs this phase |
</threat_model>

<verification>
- `npx vitest run src/ui/composables/useElementAnimation.test.ts` green
- Test-mode branch above prefersReducedMotion; throw dev-gated
- No public API expansion (discretion note)
</verification>

<success_criteria>
useElementAnimation resolves instantly with an assertable element trace in test mode, fails loud (dev) on first-resolution missing anchors, and has direct unit tests across all three paths — without expanding its public API.
</success_criteria>

<output>
Create `.planning/phases/128-animation-drag-drop-test-story/128-04-SUMMARY.md` when done.
</output>
