---
phase: 129-migration-games-merc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/BoardSmithGames/hex/src/ui/components/HexBoard.vue
  - ~/BoardSmithGames/hex/src/vite-env.d.ts
  - ~/BoardSmithGames/checkers/src/vite-env.d.ts
  - ~/BoardSmithGames/checkers/src/rules/game.ts
  - ~/BoardSmithGames/checkers/src/rules/index.ts
  - ~/BoardSmithGames/checkers/src/rules/tutorial.ts
  - ~/BoardSmithGames/polyhedral-potions/src/rules/actions.ts
  - ~/BoardSmithGames/demo-complex-ui/src/rules/actions.ts
autonomous: true
requirements: [MIG-03]

must_haves:
  truths:
    - "hex vitest suite passes (>=19) and no stone element trips useFLIP's ANIM-03 dev throw"
    - "checkers, polyhedral-potions, demo-complex-ui, demo-action-panel: vitest suites pass at >= baseline"
    - "All 5 repos in this plan pass `npx tsc --noEmit` (or `npx boardsmith validate` TypeScript: PASS)"
    - "Zero breakage-surface grep hits across these 5 repos"
  artifacts:
    - path: "~/BoardSmithGames/hex/src/ui/components/HexBoard.vue"
      provides: "stone element carries data-element-id recognized by useFLIP"
      contains: "data-element-id"
    - path: "~/BoardSmithGames/hex/src/vite-env.d.ts"
      provides: "vite/client ambient types (fixes ImportMeta.env tsc error)"
      contains: "vite/client"
    - path: "~/BoardSmithGames/checkers/src/vite-env.d.ts"
      provides: "vite/client ambient types"
      contains: "vite/client"
  key_links:
    - from: "HexBoard.vue stone <circle>"
      to: "useFLIP getElementId()"
      via: "data-element-id attribute"
      pattern: "data-element-id"
---

<objective>
Migrate the five non-flagship game repos (hex, checkers, polyhedral-potions, demo-complex-ui, demo-action-panel) to the v4.4 BoardSmith surface: fix the one genuine breakage-surface hit (hex's missing FLIP anchor attribute), close each game's pre-existing `tsc --noEmit` debt (in scope per CONTEXT.md's locked "suite green + tsc/build clean" criterion), and prove each repo's suite is green with zero breakage-surface grep hits.

Purpose: Satisfy MIG-03 for the games that need no new-test adoption — green + breakage-free + tsc-clean only.
Output: Fixed game markup/config/source in each repo, per-repo vitest green, per-repo tsc clean, scoped grep sweep clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-migration-games-merc/129-CONTEXT.md
@.planning/phases/129-migration-games-merc/129-RESEARCH.md

Cross-repo note: game repos at `~/BoardSmithGames/` are SEPARATE git repositories. `node_modules/boardsmith` is a symlink to this repo, so games already run live v4.4 source. Commits made inside a game repo are that repo's commits. NEVER work around a BoardSmith gap inside a game — if the grep sweep or a tsc error reveals a genuine BoardSmith `src/` defect (not a missing game-side type annotation), STOP and write a bug-fix request per `~/BoardSmithGames/CLAUDE.md` rather than patching the game.

<interfaces>
<!-- useFLIP's recognized anchor attributes (src/ui/composables/useFLIP.ts getElementId): -->
<!-- checks in order: data-card-id, data-piece-id, data-element-id, plain id. data-stone-id is NOT recognized. -->
<!-- useElementChangeTracker uses its OWN selector '[data-stone-id]' — do NOT remove data-stone-id, ADD data-element-id alongside it. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hex breakage-surface hit + tsc debt</name>
  <files>~/BoardSmithGames/hex/src/ui/components/HexBoard.vue, ~/BoardSmithGames/hex/src/vite-env.d.ts</files>
  <read_first>
    - 129-RESEARCH.md Pitfall 1 (hex data-stone-id anchor gap) and Pitfall 2 (ImportMeta.env root cause)
    - ~/BoardSmithGames/hex/src/ui/components/HexBoard.vue — the stone `<circle>` element (~line 284) carrying `:data-stone-id="stone.id"`
    - ~/BoardSmithGames/hex/tsconfig.json — confirm no `vite/client` in `types`
  </read_first>
  <action>
    In HexBoard.vue, add `:data-element-id="stone.id"` alongside the existing `:data-stone-id="stone.id"` on the stone `<circle>` element (per 129-CONTEXT.md amendment + RESEARCH Pitfall 1). Do NOT remove `data-stone-id` — `useElementChangeTracker`'s custom selector `[data-stone-id]` depends on it. Both attributes coexist; this makes `useFLIP.getElementId()` find a recognized attribute so the ANIM-03 `reportMissingAnchor()` dev throw no longer fires against real stones.
    Create `~/BoardSmithGames/hex/src/vite-env.d.ts` containing the single line `/// <reference types="vite/client" />` to supply the ambient `ImportMeta.env` type BoardSmith's `useActionController.ts:157` reads (fixes the shared TS2339 error). Verify the game's tsconfig `include` covers `src/` so the ambient file is picked up.
  </action>
  <verify>
    <automated>cd ~/BoardSmithGames/hex && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` in hex reports >= 19 passed (baseline), zero failures
    - `npx tsc --noEmit` in hex exits 0 (the single ImportMeta.env error is gone)
    - `grep -rn "data-element-id" src/ui/components/HexBoard.vue` shows the new attribute on the stone element
    - `data-stone-id` still present in HexBoard.vue (not removed)
  </acceptance_criteria>
  <done>hex suite green, tsc clean, stone carries a useFLIP-recognized anchor.</done>
</task>

<task type="auto">
  <name>Task 2: Close checkers tsc debt</name>
  <files>~/BoardSmithGames/checkers/src/vite-env.d.ts, ~/BoardSmithGames/checkers/src/rules/game.ts, ~/BoardSmithGames/checkers/src/rules/index.ts, ~/BoardSmithGames/checkers/src/rules/tutorial.ts</files>
  <read_first>
    - 129-RESEARCH.md tsc error detail for checkers (3 errors: ImportMeta.env, implicit-any `move` param at src/rules/index.ts:25, CheckersPlayer not exported at src/rules/tutorial.ts:36)
    - ~/BoardSmithGames/checkers/src/rules/game.ts — locate `CheckersPlayer` declaration (declared locally, not exported)
    - ~/BoardSmithGames/checkers/src/rules/index.ts:25 — the `move` parameter needing an explicit type
    - 129-RESEARCH.md Open Question 2 (scope tsc-debt fixes narrowly: type-only)
  </read_first>
  <action>
    Apply three type-only fixes (no behavior change):
    1. Create `~/BoardSmithGames/checkers/src/vite-env.d.ts` with `/// <reference types="vite/client" />` (same ImportMeta.env fix as hex).
    2. In `src/rules/game.ts`, add `export` to the `CheckersPlayer` declaration so `tutorial.ts:36`'s `import { CheckersPlayer } from './game.js'` resolves (TS2459). Do NOT restructure the class — only surface the export.
    3. In `src/rules/index.ts:25`, add an explicit type to the `move` parameter (infer from its usage / the surrounding action signature) to clear TS7006 implicit-any.
    If any of these turns out to require a genuine behavior change or reveals a BoardSmith `src/` defect, STOP and file a bug-fix request instead (per RESEARCH Open Question 2).
  </action>
  <verify>
    <automated>cd ~/BoardSmithGames/checkers && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` in checkers reports >= 38 passed, zero failures
    - `npx tsc --noEmit` in checkers exits 0 (all 3 errors resolved)
    - Changes are type-only: no runtime logic altered (diff shows only export keyword, param type annotation, new vite-env.d.ts)
  </acceptance_criteria>
  <done>checkers suite green, tsc clean, fixes are type-only.</done>
</task>

<task type="auto">
  <name>Task 3: Close polyhedral-potions + demo-complex-ui tsc debt, verify demo-action-panel, scoped grep sweep</name>
  <files>~/BoardSmithGames/polyhedral-potions/src/rules/actions.ts, ~/BoardSmithGames/demo-complex-ui/src/rules/actions.ts</files>
  <read_first>
    - 129-RESEARCH.md tsc error detail for polyhedral-potions (TS2322 at src/rules/actions.ts:52, missing `refs` on ChoiceBoardRefs) and demo-complex-ui (same shape at actions.ts:104 and :168)
    - ~/BoardSmith/src/engine/action/types.ts — the `ChoiceBoardRefs` type (required `refs` property since Phase 94-01, predates v4.4)
    - 129-RESEARCH.md "State of the Art" table + grep-sweep patterns (headless-harness, ElementCollection.shuffle no-arg, playUntilComplete import)
  </read_first>
  <action>
    1. polyhedral-potions: in `src/rules/actions.ts:52`, fix the `boardRefs()` callback return so it includes the required `refs` property on `ChoiceBoardRefs` (match the shape the type demands — read `ChoiceBoardRefs` in BoardSmith to see the exact required shape). Type-only fix; no gameplay change.
    2. demo-complex-ui: apply the same `refs`-property fix at `src/rules/actions.ts:104` and `:168`.
    3. demo-action-panel: no test suite and tsc already PASS — run `npx tsc --noEmit` (or `npx boardsmith validate`) to confirm it stays clean; no code change expected.
    4. Scoped breakage-surface grep sweep across these 5 repos (hex, checkers, polyhedral-potions, demo-complex-ui, demo-action-panel). Run from each repo: `grep -rn "headless-harness" src/ tests/` (expect zero), `grep -rn "\.shuffle(" src/ tests/` (confirm every hit is `Space.shuffle()` no-arg in-game form, NOT a detached `ElementCollection.shuffle()` — zero of the latter), `grep -rn "playUntilComplete" src/ tests/` (expect zero imports from a `boardsmith` subpath). Zero disallowed hits required.
  </action>
  <verify>
    <automated>cd ~/BoardSmithGames/polyhedral-potions && npx vitest run && npx tsc --noEmit && cd ~/BoardSmithGames/demo-complex-ui && npx vitest run && npx tsc --noEmit && cd ~/BoardSmithGames/demo-action-panel && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - polyhedral-potions: >= 24 passed, tsc exits 0
    - demo-complex-ui: >= 4 passed, tsc exits 0
    - demo-action-panel: tsc exits 0 (no suite; no change)
    - Scoped grep sweep across all 5 repos: zero `headless-harness` hits, zero detached `ElementCollection.shuffle()` no-arg hits, zero `boardsmith`-imported `playUntilComplete` hits
  </acceptance_criteria>
  <done>All 5 non-flagship repos green + tsc clean; scoped grep sweep clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BoardSmith src/ → game repos | Games consume the v4.4 API contract; games must not work around gaps (fix in src/ or file bug) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-01 | Tampering | game-side workaround masking a real BoardSmith `src/` defect | mitigate | Fixes here are type-only / markup-only; any error revealing a framework defect triggers a bug-fix request, never an in-game workaround (RESEARCH Open Q2) |
| T-129-02 | Information disclosure | hex FLIP anchor fix (markup) | accept | hex is a perfect-information game (no hidden state); adding data-element-id exposes only the already-public stone id |
</threat_model>

<verification>
- Each of hex, checkers, polyhedral-potions, demo-complex-ui: `npx vitest run` >= baseline and `npx tsc --noEmit` exits 0.
- demo-action-panel: `npx tsc --noEmit` exits 0.
- Scoped breakage-surface grep sweep across all 5 repos returns zero disallowed hits.
</verification>

<success_criteria>
All 5 non-flagship game repos: suite green at >= baseline, tsc clean, zero breakage-surface grep hits, hex stone carries a useFLIP-recognized anchor.
</success_criteria>

<output>
Create `.planning/phases/129-migration-games-merc/129-01-SUMMARY.md` when done. Since game-repo commits live in each game's own repo, note in the SUMMARY which repos were committed (or skipped as zero-change) per Claude's Discretion.
</output>
