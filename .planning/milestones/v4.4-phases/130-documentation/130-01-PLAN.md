---
phase: 130-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/api/testing.md
  - docs/browser-testing.md
  - docs/agent-control.md
  - docs/custom-ui-guide.md
  - docs/migration-guide.md
  - docs/llm-overview.md
  - docs/README.md
autonomous: true
requirements: [DOC-05, DOC-06]

must_haves:
  truths:
    - "An agent reading docs alone can assert per-seat visibility, DOM-leak safety, animation traces, and run headless seeded simulations"
    - "An agent reading docs alone can drive the dev host over WS (getState/getLobby/debugToggle/uiSwitch/debug:logs/debug:flow-state) and via createDevHostClient"
    - "Docs state the v4.4 determinism guarantee: seeded runs reproduce end-to-end, no Math.random fallbacks remain"
    - "Every removed/changed v4.4 API has a before→after migration entry"
    - "llm-overview.md surfaces the new v4.4 testing/dev-host/determinism capabilities so they are discoverable from the agent entry point"
  artifacts:
    - path: "docs/api/testing.md"
      provides: "VIS visibility assertions, ANIM test-mode traces, headless sim, FLOW introspection docs with examples"
      contains: "assertNoHiddenInfoLeak"
    - path: "docs/agent-control.md"
      provides: "DRIVE WS ops + createDevHostClient + Node GameConnection + ERR surfacing recipes"
      contains: "createDevHostClient"
    - path: "docs/custom-ui-guide.md"
      provides: "anchorAttrs requirements, fail-loud animation semantics, once-per-type dev-warning"
      contains: "anchorAttrs"
    - path: "docs/migration-guide.md"
      provides: "v4.4 breaking-changes section with before→after snippets"
      contains: "## v4.4"
    - path: "docs/llm-overview.md"
      provides: "discoverable index of v4.4 agent-ergonomics capabilities"
  key_links:
    - from: "docs/README.md"
      to: "docs/migration-guide.md"
      via: "index table entry"
      pattern: "migration-guide"
    - from: "docs/llm-overview.md"
      to: "docs/agent-control.md"
      via: "cross-reference to dev-host driving"
      pattern: "agent-control"
---

<objective>
Document the entire shipped, migrated v4.4 agent-ergonomics surface (FLOW, VIS, SIM, ERR, DRIVE, ANIM) plus the determinism guarantee and all breaking changes, editing the existing `docs/` guides IN PLACE. No new doc tree.

Purpose: satisfy DOC-05 (all new/changed APIs documented) and DOC-06 (breaking changes recorded) so an agent or author can learn the new testing, dev-host, and determinism guarantees from docs alone.
Output: updated testing guide, agent-control guide, custom-ui guide, migration guide (v4.4 breaking section), llm-overview, and README index.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-documentation/130-CONTEXT.md

# Source-of-truth for exact API names + ready-made examples. Read the SUMMARYs, do not invent symbols.
@.planning/phases/123-*/123-01-SUMMARY.md
@.planning/phases/124-*/124-01-SUMMARY.md
@.planning/phases/125-*/125-02-SUMMARY.md
@.planning/phases/126-*/126-04-SUMMARY.md
@.planning/phases/127-*/127-03-SUMMARY.md
@.planning/phases/128-*/128-01-SUMMARY.md
@.planning/phases/129-*/129-02-SUMMARY.md

# Docs to edit (read before editing to match existing tone/structure)
@docs/api/testing.md
@docs/browser-testing.md
@docs/agent-control.md
@docs/custom-ui-guide.md
@docs/migration-guide.md
@docs/llm-overview.md
@docs/README.md

# Ready-made worked examples live in the sibling game repos (real tests):
#   ~/BoardSmithGames/go-fish (DOM-leak + visibility tests)
#   ~/BoardSmithGames/cribbage (visibility tests)
#   ~/BoardSmithGames/demo-animation (animation-trace tests)
</context>

<constraints>
- NEVER invent a symbol, flag, or command. Every claimed API/CLI flag/example MUST correspond to a real export or command in the codebase. When unsure, grep `src/` before writing (e.g. `grep -rn "enableAnimationTestMode" src/`).
- Lift examples from real tests where possible (phase SUMMARYs point to them; the sibling game repos have runnable ones).
- Edit IN PLACE — do not create new doc files. Follow each file's existing heading depth and voice.
- Agent-first framing: each new capability gets a short "what you can now assert/drive" recipe, not just an API dump.
- DOC-06 placement decision (locked): breaking changes go in `docs/migration-guide.md` as a new `## v4.4` section (repo convention — it already uses versioned sections like `## v3.0`; no BREAKING.md exists). Do NOT create a root BREAKING.md.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Testing guide — visibility, animation traces, headless sim, flow introspection</name>
  <files>docs/api/testing.md, docs/browser-testing.md</files>
  <read_first>
    - docs/api/testing.md (existing Exports/Examples structure — add sections, keep style)
    - .planning/phases/124-*/124-01-SUMMARY.md (VIS: isElementVisible/getVisibleElements/assertHidden/assertVisible/diffPlayerViews/renderAsSeat/assertNoHiddenInfoLeak — three-state, post-playerView, async jsdom, allow predicates elementId-scoped, positive controls)
    - .planning/phases/125-*/125-02-SUMMARY.md (SIM: createHeadlessSession from boardsmith/session; boardsmith simulate --games/--seed/--players/--json, exit codes, replay hints)
    - .planning/phases/128-*/128-01-SUMMARY.md (ANIM: enableAnimationTestMode/getAnimationTrace from boardsmith/ui + testing, {kind,element,from,to} traces)
    - .planning/phases/123-*/123-01-SUMMARY.md (FLOW: getFlowDebugInfo/describeFlowPosition, TestGame.getPendingAction, disabled-choice introspection)
    - ~/BoardSmithGames/go-fish DOM-leak + visibility tests; ~/BoardSmithGames/demo-animation trace tests (copyable examples)
  </read_first>
  <action>
    Add to docs/api/testing.md, under the Examples section (matching its existing sub-heading pattern):
    (1) "Asserting Hidden Information (VIS)" — document isElementVisible/getVisibleElements on TestGame, assertHidden/assertVisible, diffPlayerViews (three-state, judged on final toJSONForPlayer tree), and the async renderAsSeat/assertNoHiddenInfoLeak DOM-leak utility (jsdom, allow predicate is elementId-scoped, positive-control note). Include a go-fish-style recipe: "render as seat 1, assert opponent card rank/suit never appears in the DOM."
    (2) "Animation Traces (ANIM test mode)" — enableAnimationTestMode/getAnimationTrace, the {kind,element,from,to} trace shape, and a "card X flew from A to B" headless assertion recipe. Note the boardsmith/ui + boardsmith/testing export locations.
    (3) "Headless Simulation (SIM)" — createHeadlessSession imported from boardsmith/session, with a seeded-run example; then the `boardsmith simulate` CLI (--games/--seed/--players/--json flags, exit codes for pass/stuck/error, replay hints) as a CI recipe.
    (4) "Flow-Position Debugging (FLOW)" — getFlowDebugInfo/describeFlowPosition (+ appearance in GameStuckError/assertions/toDebugString), TestGame.getPendingAction for mid-multi-step state, and disabled-choice introspection with reasons.
    In docs/browser-testing.md add a short pointer subsection linking the new testing-guide sections and, if applicable, the debug:flow-state devtools getter alongside the existing __BOARDSMITH_DEVTOOLS material.
    Cross-check every symbol name against src/ before writing (grep the testing + session + ui barrels).
  </action>
  <verify>
    <automated>grep -q "assertNoHiddenInfoLeak" docs/api/testing.md && grep -q "getAnimationTrace" docs/api/testing.md && grep -q "createHeadlessSession" docs/api/testing.md && grep -q "boardsmith simulate" docs/api/testing.md && grep -q "getFlowDebugInfo\|describeFlowPosition" docs/api/testing.md && echo OK</automated>
  </verify>
  <done>testing.md documents VIS assertions, ANIM traces, headless sim + simulate CLI, and FLOW introspection, each with a working example; browser-testing.md cross-links the new sections. No invented symbols.</done>
</task>

<task type="auto">
  <name>Task 2: Dev-host protocol + error surfacing + custom-UI anchor requirements</name>
  <files>docs/agent-control.md, docs/custom-ui-guide.md</files>
  <read_first>
    - docs/agent-control.md (existing Introspection APIs / Determinism & Seeding structure)
    - docs/custom-ui-guide.md (existing anchor/board integration sections)
    - .planning/phases/127-*/127-03-SUMMARY.md (DRIVE: getState/getLobby/debugToggle/uiSwitch WS ops; Node-capable GameConnection via wsImplementation ?? globalThis.WebSocket, Node >=22.4 note; createDevHostClient from boardsmith/client)
    - .planning/phases/126-*/126-04-SUMMARY.md (ERR: OpResult warnings[{code,message,source}] + errorCode; onPersistenceError/lastPersistenceError/persistenceHealthy on both hosts; debug:logs ring buffer + DebugPanel Logs tab)
    - .planning/phases/128-*/128-01-SUMMARY.md (ANIM-03: isDevThrowEnabled semantics — positive-signal dev throw, unlabeled never crashes; anchorAttrs(ref, type) + once-per-type dev-warning)
  </read_first>
  <action>
    In docs/agent-control.md add a "Scriptable Dev Host (WS)" section covering getState/getLobby/debugToggle/uiSwitch ops and the debug:logs and debug:flow-state ops, then a createDevHostClient (boardsmith/client) recipe: connect from Node, drive a seat, read state — noting GameConnection now resolves its socket via wsImplementation ?? globalThis.WebSocket (Node >=22.4 note). Add a "Structured Errors (ERR)" subsection: OpResult.warnings[{code,message,source}] and errorCode for branchable failures, plus onPersistenceError/lastPersistenceError/persistenceHealthy for observable save failures. Give a "branch on errorCode" agent recipe.
    In docs/custom-ui-guide.md extend the anchor/board material with an "Anchor Requirements & Fail-Loud" section: anchorAttrs(ref, type) is required on animated custom boards, the once-per-type dev-warning when omitted, and the isDevThrowEnabled fail-loud semantics (animation helpers throw an actionable error in dev when a target lacks its anchor attribute; unlabeled elements never crash production). Reference data-bs-el-id / data-element-id parity with AutoUI.
    Verify every op name and symbol against src/ (grep protocol.ts, client barrel, ui barrel) before writing.
  </action>
  <verify>
    <automated>grep -q "createDevHostClient" docs/agent-control.md && grep -q "getState\|getLobby" docs/agent-control.md && grep -q "debug:logs" docs/agent-control.md && grep -q "errorCode" docs/agent-control.md && grep -q "onPersistenceError" docs/agent-control.md && grep -q "anchorAttrs" docs/custom-ui-guide.md && echo OK</automated>
  </verify>
  <done>agent-control.md documents all DRIVE WS ops + createDevHostClient + Node GameConnection + ERR surfacing with recipes; custom-ui-guide.md documents anchorAttrs requirements, the once-per-type warning, and fail-loud animation semantics. No invented symbols.</done>
</task>

<task type="auto">
  <name>Task 3: v4.4 breaking-changes section, determinism guarantee, llm-overview discoverability, README index</name>
  <files>docs/migration-guide.md, docs/llm-overview.md, docs/README.md</files>
  <read_first>
    - docs/migration-guide.md (existing `## v3.0` versioned-section format — mirror it for `## v4.4`)
    - docs/llm-overview.md (existing Key Concepts / CLI Commands / Common Pitfalls structure)
    - docs/README.md (index table)
    - .planning/phases/123-*/123-01-SUMMARY.md (determinism: ElementCollection.shuffle() requires explicit rng, playUntilComplete deterministic default, GameRunner.seed retrievability, no Math.random fallbacks in space.ts/element-collection.ts)
    - .planning/phases/125-*/125-*-SUMMARY.md (headless-harness moved to src/session/headless-session.ts, clean break, exported from boardsmith/session)
    - .planning/phases/126-*/126-04-SUMMARY.md (onPersistenceError 3-arg signature)
    - .planning/phases/128-*/128-*-SUMMARY.md (animation fail-loud dev throws; anchorAttrs signature change)
  </read_first>
  <action>
    Add a `## v4.4: Agent-Ergonomics` section to docs/migration-guide.md (mirror the `## v3.0` structure: What Changed + numbered before→after steps + Checklist). Cover every removed/changed API with before→after snippets: (a) headless-harness move src/session/testing/headless-harness.ts → import createHeadlessSession from boardsmith/session; (b) ElementCollection.shuffle() now requires an explicit rng argument (no Math.random default); (c) playUntilComplete deterministic-by-default (fixed literal seed instead of Math.random); (d) animation helpers fail loud (dev throw) when a target lacks its anchor attribute; (e) onPersistenceError 3-arg signature; (f) anchorAttrs(ref, type) signature. This section satisfies DOC-06.
    In docs/llm-overview.md add a determinism-guarantee statement (seeded runs reproduce end-to-end, no Math.random fallbacks remain — the guarantee ROADMAP claims) and an "Agent Ergonomics (v4.4)" subsection near CLI Commands / Key Concepts that surfaces the new testing (VIS/ANIM/SIM/FLOW), dev-host driving (DRIVE), and error-surfacing (ERR) capabilities, each linking to agent-control.md / testing.md so they are discoverable from the agent entry point.
    In docs/README.md update the index table + Quick Links so the migration guide and the new capability areas are listed (add `boardsmith simulate` to CLI quick links if the CLI section lists commands).
    Verify shuffle/playUntilComplete/onPersistenceError/anchorAttrs signatures against src/ before writing snippets.
  </action>
  <verify>
    <automated>grep -q "## v4.4" docs/migration-guide.md && grep -q "createHeadlessSession" docs/migration-guide.md && grep -q "shuffle" docs/migration-guide.md && grep -qi "determinis" docs/llm-overview.md && grep -q "agent-control\|simulate" docs/README.md && echo OK</automated>
  </verify>
  <done>migration-guide.md has a complete v4.4 breaking-changes section (all 6 changes, before→after); llm-overview.md states the determinism guarantee and surfaces v4.4 capabilities discoverably; README index/quick-links updated. No invented symbols.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → reader (agent/author) | The only risk surface is a reader trusting a documented example/symbol that is wrong or unsafe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-01 | Information (misinformation) | Any documented symbol/CLI flag/example | mitigate | Every claim grep-verified against src/ during writing; full sweep enforced by plan 130-02 doc-verifier (wave 2) |
| T-130-02 | Tampering | npm/pip/cargo installs | accept | Docs-only phase; no package installs performed |
</threat_model>

<verification>
- All three tasks' grep gates pass.
- No new files created outside the listed docs (no root BREAKING.md).
- Plan 130-02 doc-verifier confirms zero unverifiable symbol/command claims.
</verification>

<success_criteria>
- DOC-05: VIS/ANIM/SIM/FLOW testing surface, DRIVE dev-host protocol, ERR surfacing, and determinism guarantee are all documented with working examples.
- DOC-06: migration-guide.md v4.4 section lists every removed/changed API with before→after guidance.
- llm-overview.md makes the v4.4 surface discoverable from the agent entry point.
</success_criteria>

<output>
Create `.planning/phases/130-documentation/130-01-SUMMARY.md` when done.
</output>
