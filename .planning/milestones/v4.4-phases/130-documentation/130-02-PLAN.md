---
phase: 130-documentation
plan: 02
type: execute
wave: 2
depends_on: [130-01]
files_modified:
  - docs/api/testing.md
  - docs/browser-testing.md
  - docs/agent-control.md
  - docs/custom-ui-guide.md
  - docs/migration-guide.md
  - docs/llm-overview.md
  - docs/README.md
  - .planning/phases/130-documentation/130-VALIDATION.md
autonomous: true
requirements: [DOC-05, DOC-06]

must_haves:
  truths:
    - "Every symbol claimed in the v4.4 docs corresponds to a real export/definition in src/"
    - "Every CLI command/flag claimed in the docs corresponds to a real command/flag"
    - "Cheap doc-embedded commands (e.g. boardsmith simulate --help) run successfully"
    - "130-VALIDATION.md records the verification pass with zero unverifiable claims"
  artifacts:
    - path: ".planning/phases/130-documentation/130-VALIDATION.md"
      provides: "doc-verifier report: extracted claims, grep results, command runs, verdict"
      contains: "verdict"
  key_links:
    - from: "docs (v4.4 sections)"
      to: "src/"
      via: "symbol claim → grep verification"
      pattern: "grep -rn"
---

<objective>
Verify every symbol, CLI command, and example added by plan 130-01 against the live codebase. This IS the phase validation (the doc-verifier pass). Fix any doc errors found; record the pass in 130-VALIDATION.md.

Purpose: mitigate the one real risk of a docs-only phase — a wrong or unsafe example that misleads an agent (T-130-01). Zero unverifiable claims is the bar.
Output: a validated docs set + 130-VALIDATION.md report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/130-documentation/130-CONTEXT.md
@.planning/phases/130-documentation/130-01-PLAN.md

# The docs to verify (edited by plan 130-01):
@docs/api/testing.md
@docs/agent-control.md
@docs/custom-ui-guide.md
@docs/migration-guide.md
@docs/llm-overview.md
@docs/browser-testing.md
@docs/README.md
</context>

<constraints>
- The verifier's judgment is the codebase, not the SUMMARYs. A symbol only "exists" if grep finds it in src/.
- If the gsd-doc-verifier agent is available, use it; otherwise perform the manual extract→grep→run protocol below.
- When a claim fails verification, FIX the doc (correct the symbol/flag or remove the false claim) — do not just report it. Re-run the gate after fixing.
- Only run doc-embedded commands that are cheap and side-effect-free (help text, --dry equivalents). Do NOT run long simulations or anything that mutates game repos.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Extract and grep-verify every symbol claim; run cheap doc commands; fix errors</name>
  <files>docs/api/testing.md, docs/agent-control.md, docs/custom-ui-guide.md, docs/migration-guide.md, docs/llm-overview.md, docs/browser-testing.md, docs/README.md</files>
  <read_first>
    - All seven docs above (the v4.4 sections added/edited by 130-01)
  </read_first>
  <action>
    Extract every API symbol referenced in the v4.4 doc sections (functions, methods, exports, WS op names, types) into a claim list. For each, grep src/ to confirm it exists and is exported where the docs say it is:
      grep -rn "getFlowDebugInfo\|describeFlowPosition\|getPendingAction\|isElementVisible\|getVisibleElements\|assertHidden\|assertVisible\|diffPlayerViews\|renderAsSeat\|assertNoHiddenInfoLeak\|createHeadlessSession\|enableAnimationTestMode\|getAnimationTrace\|getState\|getLobby\|debugToggle\|uiSwitch\|createDevHostClient\|onPersistenceError\|lastPersistenceError\|persistenceHealthy\|errorCode\|anchorAttrs\|isDevThrowEnabled" src/
    Confirm export locations match doc claims: boardsmith/session (createHeadlessSession), boardsmith/client (createDevHostClient), boardsmith/testing + boardsmith/ui (animation trace helpers) — check the relevant barrels (src/session/index.ts, src/client/index.ts, src/testing/index.ts, src/ui/index.ts).
    Verify WS op names against src (protocol.ts / dev-host). Verify determinism claims: no Math.random remains in the documented engine paths — grep space.ts and element-collection.ts for "Math.random" and confirm the shuffle/playUntilComplete signatures documented in migration-guide.md match source.
    Run cheap doc-embedded commands to confirm they exist and parse: `node dist ... simulate --help` or the repo's documented invocation (e.g. `npx boardsmith simulate --help` if a build exists; otherwise confirm the flags via grep of src/cli/commands/simulate*). Do not run a full simulation.
    For each claim that fails, FIX the doc (correct symbol/flag/export path or remove the false example) and re-verify.
  </action>
  <verify>
    <automated>bash -c 'set -e; for s in getFlowDebugInfo describeFlowPosition isElementVisible getVisibleElements assertHidden assertVisible diffPlayerViews renderAsSeat assertNoHiddenInfoLeak createHeadlessSession enableAnimationTestMode getAnimationTrace createDevHostClient onPersistenceError anchorAttrs; do grep -rq "$s" src/ || { echo "MISSING IN SRC: $s"; exit 1; }; done; echo "ALL SYMBOLS PRESENT"'</automated>
  </verify>
  <done>Every symbol/command/flag claimed in the v4.4 docs is confirmed present in src/ (or the doc is fixed); cheap doc commands parse; export-location claims match the barrels.</done>
</task>

<task type="auto">
  <name>Task 2: Write 130-VALIDATION.md report</name>
  <files>.planning/phases/130-documentation/130-VALIDATION.md</files>
  <read_first>
    - Task 1 grep/command results (from this plan's execution)
  </read_first>
  <action>
    Write .planning/phases/130-documentation/130-VALIDATION.md recording: the claim list extracted, grep verdict per symbol (present + file where found), export-location confirmations, cheap commands run and their results, any doc errors found and how they were fixed, and a final verdict line. Mark nyquist_compliant: true (the doc-verifier grep gate is the automated check). State that this pass finalizes DOC-05 + DOC-06.
  </action>
  <verify>
    <automated>grep -qi "verdict" .planning/phases/130-documentation/130-VALIDATION.md && grep -q "nyquist_compliant" .planning/phases/130-documentation/130-VALIDATION.md && echo OK</automated>
  </verify>
  <done>130-VALIDATION.md exists with per-symbol grep verdicts, command results, fixes, and a final PASS verdict; nyquist_compliant recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → reader | Wrong/unsafe documented example is the sole risk; this plan is its mitigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-01 | Information (misinformation) | v4.4 doc symbol/command claims | mitigate | This plan: extract→grep src/→run cheap commands→fix; PASS recorded in 130-VALIDATION.md |
| T-130-02 | Tampering | npm/pip/cargo installs | accept | No installs; only side-effect-free help/grep commands run |
</threat_model>

<verification>
- Symbol-presence gate passes (all documented symbols found in src/).
- 130-VALIDATION.md records zero remaining unverifiable claims.
</verification>

<success_criteria>
- Zero unverifiable symbol/command/example claims remain in the v4.4 docs.
- 130-VALIDATION.md finalizes DOC-05 + DOC-06 with a PASS verdict.
</success_criteria>

<output>
Create `.planning/phases/130-documentation/130-02-SUMMARY.md` when done.
</output>
