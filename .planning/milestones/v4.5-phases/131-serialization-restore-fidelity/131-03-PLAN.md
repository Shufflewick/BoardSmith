---
phase: 131-serialization-restore-fidelity
plan: 03
type: execute
wave: 2
depends_on: ["131-01"]
files_modified:
  - src/session/types.ts
  - src/session/game-session.ts
  - src/session/pending-action-manager.ts
  - src/session/state-history.ts
  - src/session/teaching-disabled-persistence.test.ts
  - src/session/debug-data-gating.test.ts
autonomous: true
requirements: [RST-02, SEC-04, PROC-02]
must_haves:
  truths:
    - "teachingDisabled set at create() is still true after GameSession.restore()"
    - "displayName set at create() survives GameSession.restore()"
    - "registerDebug() payloads (customDebug) are NOT present in broadcast/player-state payloads by default"
    - "A GameSession created with debugEnabled:true DOES surface customDebug; default and production hosts do not"
  artifacts:
    - path: "src/session/types.ts"
      provides: "StoredGameState.teachingDisabled + displayName fields"
      contains: "teachingDisabled"
    - path: "src/session/game-session.ts"
      provides: "persistence write/read + includeDebugData default flip + debugEnabled opt-in"
      contains: "debugEnabled"
    - path: "src/session/teaching-disabled-persistence.test.ts"
      provides: "Red-first RST-02 regression test"
      contains: "teachingDisabled"
    - path: "src/session/debug-data-gating.test.ts"
      provides: "Red-first SEC-04 regression test"
      contains: "customDebug"
  key_links:
    - from: "src/session/game-session.ts create()"
      to: "src/session/game-session.ts restore()"
      via: "StoredGameState.teachingDisabled / displayName round-trip"
      pattern: "teachingDisabled"
    - from: "GameSessionOptions.debugEnabled"
      to: "buildPlayerState includeDebugData"
      via: "opt-in threading (default false)"
      pattern: "debugEnabled"
---

<objective>
Fix RST-02 (F16) and SEC-04 code (F15) at the session layer:
- Persist `teachingDisabled` and `displayName` in `StoredGameState` so the anti-cheat lockout and display name survive `GameSession.restore()`, mirroring the existing `aiConfig` round-trip exactly.
- Flip `includeDebugData` to default `false` at all confirmed hardcoded-`true` call sites and add a `GameSessionOptions.debugEnabled` opt-in so `registerDebug()` payloads are not broadcast to players/spectators by default.

Purpose: F16 is Elevation of Privilege (players regain host-locked hint/heatmap/demo access after a server restart); F15 is Information Disclosure (debug dumps of hidden state broadcast to everyone by default). Both are session-scoped host-policy concerns.
Output: Persistence fields, threaded restore, default-false debug gating, `debugEnabled` opt-in, and two red-first regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-serialization-restore-fidelity/131-CONTEXT.md
@.planning/phases/131-serialization-restore-fidelity/131-PATTERNS.md
@.planning/phases/131-serialization-restore-fidelity/131-FINDINGS-VERIFICATION.md

<interfaces>
From src/session/types.ts:
```typescript
// StoredGameState (block starts line 195):
aiConfig?: AIConfig;                 // line 222 — the sibling-field template
// GameSessionOptions (line 518-526):
teachingDisabled?: boolean;          // line 526 — exists as an option, NOT persisted
```
From src/session/game-session.ts (aiConfig round-trip — the template to mirror):
```typescript
// create(): writes aiConfig into storedState (~line 704)
// restore(): const aiController = storedState.aiConfig ? new AIController(...) : undefined; (~794-796)
// restore() constructor call (~813): displayName is positional arg 6, teachingDisabled is arg 11 — both hardcoded undefined today
// includeDebugData: true hardcoded at 890, 903, 1386, 2120
```
From src/session/utils.ts (the gating check — do NOT change this file):
```typescript
if (options?.includeDebugData) { /* attach customDebug */ }   // line 287
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Red-first regression tests — teachingDisabled/displayName persistence + debug gating</name>
  <files>src/session/teaching-disabled-persistence.test.ts, src/session/debug-data-gating.test.ts</files>
  <read_first>
    - src/session/restore-snapshot-authoritative.test.ts (the exact async GameSession.create/.restore harness + `BL-01` describe-block style, incl. a "documents the pre-fix behavior" red test — the structural template)
    - src/session/game-session.ts (create/restore/broadcast signatures; GameSessionOptions shape; broadcast at 2120)
    - src/session/utils.ts (customDebug attach at line 287; how buildPlayerState surfaces `state.customDebug`)
    - src/engine/element/game.ts (registerDebug at 1181 — how to register debug data on a test game)
    - .planning/phases/131-serialization-restore-fidelity/131-RESEARCH.md (Pitfall 1 & 2 — scope debug tests to GameSession consumers, NOT boardsmith dev / SnapshotSessionHost)
  </read_first>
  <behavior>
    - RST-02: A GameSession created with `teachingDisabled: true` still reports the lockout after snapshot → `GameSession.restore()`. A `displayName` set at create survives restore. Include a "documents pre-fix behavior" test proving the value is lost today.
    - SEC-04: A game with `registerDebug('secret', ...)` registered produces broadcast/player-state payloads WITHOUT `customDebug` by default. With a session created using `debugEnabled: true`, `customDebug` IS present. Spectator/player views never include `customDebug` unless `debugEnabled` is set.
  </behavior>
  <action>Create both test files using the `restore-snapshot-authoritative.test.ts` async pattern. `teaching-disabled-persistence.test.ts`: assert `teachingDisabled`/`displayName` survive `GameSession.restore()`, with a red "pre-fix behavior" case. `debug-data-gating.test.ts`: register debug data, assert `customDebug` absent from default broadcast/buildPlayerState output and present when `debugEnabled:true`. Both suites MUST FAIL on current code. Run them and record the RED output for the SUMMARY (PROC-02). Do NOT wire any `boardsmith dev`/`SnapshotSessionHost` path — scope tests to `GameSession` consumers only (Pitfall 2).</action>
  <verify>
    <automated>npx vitest run src/session/teaching-disabled-persistence.test.ts src/session/debug-data-gating.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist
    - Both FAIL before Task 2 (RED) — failure output captured for the SUMMARY
    - `teaching-disabled-persistence.test.ts` asserts both `teachingDisabled` and `displayName` survive `GameSession.restore()`
    - `debug-data-gating.test.ts` asserts `customDebug` absent by default and present only with `debugEnabled:true`
    - No test references `SnapshotSessionHost` or `boardsmith dev`
  </acceptance_criteria>
  <done>Two failing regression suites committed (RED) covering RST-02 and SEC-04 at the GameSession boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist teachingDisabled/displayName + default-false debug gating with debugEnabled opt-in</name>
  <files>src/session/types.ts, src/session/game-session.ts, src/session/pending-action-manager.ts, src/session/state-history.ts</files>
  <read_first>
    - src/session/types.ts (StoredGameState block 195-242, `aiConfig` at 222; GameSessionOptions 518-526)
    - src/session/game-session.ts (create() storedState literal ~692-715 with `aiConfig` at 704; restore() ~758-813 incl. the constructor call at 813 where displayName=arg6 and teachingDisabled=arg11 are hardcoded undefined; the `#teachingDisabled`/`#displayName` private fields ~232,305; includeDebugData sites 890, 903, 1386, 2120)
    - src/session/pending-action-manager.ts (includeDebugData: true at 226, 250, 355, 357)
    - src/session/state-history.ts (includeDebugData: true at 321, 393)
    - src/session/teaching-disabled-persistence.test.ts, src/session/debug-data-gating.test.ts (the RED tests to turn GREEN)
    - .planning/phases/131-serialization-restore-fidelity/131-RESEARCH.md (Pattern 5; Pitfall 1 — do NOT touch stateless-ops.ts; Pitfall 2 — no CLI wiring for debugEnabled)
  </read_first>
  <action>Two independent fixes:
1. RST-02 persistence (mirror `aiConfig` exactly, per D-RST-02): add `teachingDisabled?: boolean` and `displayName?: string` to the `StoredGameState` interface in `types.ts`. In `create()`, write both into the `storedState` object literal alongside `aiConfig` (both are in-scope locals at that point). In `restore()`, read `storedState.teachingDisabled` and `storedState.displayName` and pass them into the `GameSession` constructor call — replacing the hardcoded `undefined` at positional arg 6 (displayName) and arg 11 (teachingDisabled). Do NOT change the constructor signature order.
2. SEC-04 debug gating (per D-SEC-04): add a `debugEnabled?: boolean` field to `GameSessionOptions` and store it on a private session field (mirror `#teachingDisabled`'s constructor-time-only shape). Flip every hardcoded `includeDebugData: true` to be gated by the session's `debugEnabled` (default `false`) — at `game-session.ts:890, 903, 1386, 2120`, `pending-action-manager.ts:226, 250, 355, 357`, `state-history.ts:321, 393`. For the two default-param sites (890, 903) change `?? true` to `?? this.#debugEnabled` (or equivalent, default false). Do NOT touch `src/session/stateless-ops.ts` (Pitfall 1 — already safe). Do NOT add any `boardsmith dev`/CLI wiring (Pitfall 2 — `debugEnabled` is a GameSession-consumer API only; note this narrowing in the SUMMARY).
Turn both RED test suites GREEN.</action>
  <verify>
    <automated>npx vitest run src/session/teaching-disabled-persistence.test.ts src/session/debug-data-gating.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "teachingDisabled\|displayName" src/session/types.ts` shows both new fields on `StoredGameState`
    - `restore()` no longer passes hardcoded `undefined` for displayName (arg 6) and teachingDisabled (arg 11) — they read from `storedState`
    - `grep -rn "includeDebugData: true" src/session/` returns zero matches (all flipped)
    - `grep -c "includeDebugData" src/session/stateless-ops.ts` is 0 (file untouched for SEC-04)
    - `GameSessionOptions` has a `debugEnabled?: boolean` field
    - Both regression suites pass (GREEN)
  </acceptance_criteria>
  <done>teachingDisabled/displayName persist across restore; debug data gated off by default with an explicit debugEnabled opt-in; stateless-ops untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host policy (create) → persisted state → restore | teachingDisabled lockout must survive restore |
| game debug registry → broadcast payload | debug data must not reach players/spectators by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-03 | Elevation of Privilege | `teachingDisabled` reset on restore (F16) | mitigate | Persist in `StoredGameState`, thread through `restore()` like `aiConfig`; regression test asserts lockout survives restore |
| T-131-04 | Information Disclosure | `includeDebugData` default `true` broadcasts registerDebug dumps (F15) | mitigate | Default to `false` at all 10 confirmed call sites; explicit `GameSessionOptions.debugEnabled` opt-in for trusted GameSession consumers only |
| T-131-04b | Information Disclosure | Assuming stateless-ops/dev-host needs the same fix | accept | Verified non-bug (Pitfall 1): `stateless-ops.ts` never passes `includeDebugData`; no change made — recorded in 131-FINDINGS-VERIFICATION.md |
</threat_model>

<verification>
Both regression suites pass. `grep -rn "includeDebugData: true" src/session/` returns nothing. `stateless-ops.ts` unchanged. RED-then-GREEN recorded in SUMMARY. `npm run test` green before wave merge.
</verification>

<success_criteria>
teachingDisabled/displayName survive GameSession.restore(); registerDebug data not broadcast by default; debugEnabled opt-in works; regression tests proven red-then-green.
</success_criteria>

<output>
Create `.planning/phases/131-serialization-restore-fidelity/131-03-SUMMARY.md` when done, recording RED output and the debugEnabled scope-narrowing note (Pitfall 2).
</output>
