---
phase: 131-serialization-restore-fidelity
plan: 04
type: execute
wave: 2
depends_on: ["131-01"]
files_modified:
  - src/engine/element/game.ts
  - src/session/utils.ts
  - src/engine/element/visible-attributes.test.ts
  - src/session/player-state-visibility.test.ts
autonomous: true
requirements: [SEC-02, SEC-03, PROC-02]
must_haves:
  truths:
    - "static visibleAttributes filters non-listed attributes from non-owners in toJSONForPlayer"
    - "The owning player/seat still sees their own visibleAttributes-restricted attributes (Player owns itself)"
    - "Spectators (non-owners of everything) see the most restrictive view"
    - "state.players is derived from the already-filtered per-viewer tree, not a raw player.toJSON() second pass"
    - "A custom Player attribute or Player-child element hidden from an opponent no longer appears in that opponent's state.players"
    - "Public-by-default preserved: custom Player attributes with no visibleAttributes remain visible (go-fish bookCount)"
  artifacts:
    - path: "src/engine/element/game.ts"
      provides: "visibleAttributes filtering branch in filterElement"
      contains: "visibleAttributes"
    - path: "src/session/utils.ts"
      provides: "state.players derived from filtered truthView"
      contains: "truthView"
    - path: "src/engine/element/visible-attributes.test.ts"
      provides: "Red-first SEC-02 test incl. owner-self-visibility"
      contains: "visibleAttributes"
    - path: "src/session/player-state-visibility.test.ts"
      provides: "Red-first SEC-03 test"
      contains: "state.players"
  key_links:
    - from: "src/session/utils.ts buildPlayerState"
      to: "toJSONForPlayer filterElement"
      via: "state.players derived from truthView Player nodes"
      pattern: "truthView"
    - from: "filterElement visibleAttributes branch"
      to: "Player owner check"
      via: "element instanceof Player ? element.seat === visibilityPosition : getEffectiveOwner()"
      pattern: "instanceof Player"
---

<objective>
Fix SEC-02 (F2) and SEC-03 (F8) as one shared per-viewer redaction mechanism in `Game.toJSONForPlayer`'s `filterElement` closure:
- SEC-02: implement `static visibleAttributes` — filter non-listed attributes from non-owners on otherwise-visible elements (public-by-default; secrecy opt-in), with a `Player`-owns-itself special case.
- SEC-03: route `state.players` through the same filtered tree (`truthView`) instead of the current raw, unfiltered `player.toJSON()` second pass — so Player attributes and Player-child elements inherit all visibility filtering for free.

Purpose: Both are Information Disclosure. `visibleAttributes` is a documented-but-dead security control (a Pit of Failure); `state.players` is a parallel unfiltered broadcast channel that bypasses `isVisibleTo`/`hideFromAll()`. One chokepoint fixes both.
Output: Attribute-level filtering in `filterElement`, `state.players` derived from `truthView`, and two red-first regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-serialization-restore-fidelity/131-CONTEXT.md
@.planning/phases/131-serialization-restore-fidelity/131-PATTERNS.md
@.planning/phases/131-serialization-restore-fidelity/131-FINDINGS-VERIFICATION.md

<interfaces>
From src/engine/element/game.ts (filterElement — the single per-viewer chokepoint):
```typescript
const filterElement = (json: ElementJSON, element: GameElement): ElementJSON | null => { // 2676
  // ... 3 existing redaction branches (count-only, individually-hidden, zone-hidden) ...
  // owner-check precedent (line 2764):
  } else if (zoneVisibility.mode === 'owner' && element.getEffectiveOwner()?.seat !== visibilityPosition) {
  // unfiltered fallthrough (~2790-2807): returns {...json, children} WITHOUT filtering json.attributes  ← the SEC-02 gap
};
```
From src/engine/element/game-element.ts:
```typescript
static visibleAttributes: string[] | undefined;   // line 140 — declared, read NOWHERE today
```
`Player` is already imported in game.ts (line 26). `getEffectiveOwner()` returns undefined for a top-level Player (Pitfall 4).
From src/session/utils.ts (buildPlayerState):
```typescript
const truthView = playerView.state;               // ~212 — already filtered per-viewer
// ~236-262: fullPlayerData = runner.game.players.map(raw player.toJSON())  ← the SEC-03 unfiltered second pass
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Red-first regression tests — visibleAttributes filtering + state.players redaction</name>
  <files>src/engine/element/visible-attributes.test.ts, src/session/player-state-visibility.test.ts</files>
  <read_first>
    - src/engine/element/deck-hand-visibility.test.ts (engine suite style + toJSONForPlayer assertions — the template)
    - src/session/restore-snapshot-authoritative.test.ts (session-layer state.players / buildPlayerState harness)
    - src/engine/element/game.ts (toJSONForPlayer / filterElement — how a viewer's `state` is produced)
    - src/engine/player/player.ts (Player.toJSON at 326-333 adds `_isCurrent`; Player.seat)
    - .planning/phases/131-serialization-restore-fidelity/131-PATTERNS.md (Pattern 2, Pattern 3; Pitfall 4 owner-self-visibility)
    - .planning/phases/131-serialization-restore-fidelity/131-RESEARCH.md (Pitfall 4)
  </read_first>
  <behavior>
    - SEC-02: A subclass with `static visibleAttributes = ['publicField']` and a secret attribute: a non-owner's `toJSONForPlayer` view shows `publicField` but NOT the secret; the OWNER's own view shows both. A `Player` subclass with `static visibleAttributes = ['secretRole']` — the OWNING seat sees `secretRole`, all other seats AND spectators do not (Player owns itself).
    - Public-by-default: an element/Player with NO `visibleAttributes` exposes all custom attributes to everyone (go-fish `bookCount` style).
    - SEC-03: A custom Player attribute hidden via `visibleAttributes` (or a Player-child element hidden via hideFromAll) is absent from an opponent's `state.players`, but present in that player's own `state.players`. `state.players` for a viewer matches the Player nodes in that viewer's filtered `view`.
  </behavior>
  <action>Create `visible-attributes.test.ts` (engine) and `player-state-visibility.test.ts` (session). The engine test covers non-owner attribute redaction, owner-self-visibility (including the Player-owns-itself Pitfall 4 case), spectator most-restrictive view, and public-by-default. The session test drives `buildPlayerState`/`state.players` and asserts a hidden Player attribute is absent from an opponent's `state.players` yet present in the owner's, and that `state.players` agrees with the filtered `view`. Both suites MUST FAIL on current code (`visibleAttributes` is never read; `state.players` is unfiltered). Run and record RED output for the SUMMARY (PROC-02).</action>
  <verify>
    <automated>npx vitest run src/engine/element/visible-attributes.test.ts src/session/player-state-visibility.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist and FAIL before Task 2 (RED) — output captured for the SUMMARY
    - Engine test includes a Player subclass owner-self-visibility case (Pitfall 4)
    - Engine test includes a public-by-default case (no visibleAttributes → all attrs visible)
    - Session test asserts a hidden Player attribute is absent from an opponent's `state.players` and present in the owner's
  </acceptance_criteria>
  <done>Two failing regression suites committed (RED) for SEC-02 and SEC-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement visibleAttributes filtering and derive state.players from truthView</name>
  <files>src/engine/element/game.ts, src/session/utils.ts</files>
  <read_first>
    - src/engine/element/game.ts (filterElement 2676-2811: the 3 redaction branches, owner-check at 2764, unfiltered fallthrough 2790-2807; `Player` import at 26)
    - src/engine/element/game-element.ts (static visibleAttributes at 140; how `json.attributes` is shaped by toJSON)
    - src/engine/player/player.ts (Player.seat; Player.toJSON `_isCurrent` at 326-333 — must survive the truthView path)
    - src/session/utils.ts (buildPlayerState: truthView at ~212; fullPlayerData raw pass 236-262 — the code to replace)
    - src/engine/element/visible-attributes.test.ts, src/session/player-state-visibility.test.ts (RED tests to turn GREEN)
    - .planning/phases/131-serialization-restore-fidelity/131-PATTERNS.md (Pattern 2, Pattern 3, Pitfall 4)
  </read_first>
  <action>Two coordinated changes routing through the single `filterElement` chokepoint (do NOT build a parallel filter):
1. SEC-02 in `game.ts` `filterElement`: before the final unfiltered `return` (~2790-2807), when `(element.constructor as typeof GameElement).visibleAttributes !== undefined` AND the viewer is not the element's owner, filter `json.attributes` down to the whitelisted keys in `visibleAttributes`. Owner check MUST special-case Player (Pitfall 4): use `element instanceof Player ? element.seat === visibilityPosition : element.getEffectiveOwner()?.seat === visibilityPosition` — a plain `getEffectiveOwner()` returns undefined for a top-level Player, which would wrongly hide a player's own restricted attributes from themself. Public-by-default: `visibleAttributes === undefined` means all attributes visible (do NOT flip default semantics — per CONTEXT.md, keeps go-fish `bookCount` working). Spectators are non-owners of everything (most restrictive).
2. SEC-03 in `src/session/utils.ts` `buildPlayerState`: replace the raw `runner.game.players.map(player.toJSON())` second pass (~236-262) with a derivation from the already-filtered `truthView` (computed at ~212) — walk `truthView`'s Player nodes (`className === 'Player'` / the filtered tree's player entries) and flatten those (spread `attributes`, add `seat`) instead of re-reading raw player objects. This makes `state.players` inherit zone/element visibility AND the new `visibleAttributes` filtering. Preserve `Player.toJSON()`'s `_isCurrent` addition (it flows through the same pipeline). Ensure the owner's own `truthView` still exposes their own restricted-but-owned attributes (verified by the Task 1 owner-self-visibility test).
Turn both RED suites GREEN.</action>
  <verify>
    <automated>npx vitest run src/engine/element/visible-attributes.test.ts src/session/player-state-visibility.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "visibleAttributes" src/engine/element/game.ts` shows the new filtering branch consuming the static field
    - `grep -n "instanceof Player" src/engine/element/game.ts` shows the Player-owns-itself owner check
    - `src/session/utils.ts` no longer builds `fullPlayerData` from `runner.game.players.map(...player.toJSON())`; it derives from `truthView`
    - Both regression suites pass (GREEN)
    - Existing go-fish-style public-attribute behavior preserved (public-by-default test green)
    - Full suite `npm run test` green; `npm run audit:duplication` shows no new copy-paste of filter logic (single chokepoint)
  </acceptance_criteria>
  <done>Attribute-level and player-state redaction both route through one filter; opponent views no longer leak restricted Player attributes; public-by-default preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| element attributes → non-owner view | visibleAttributes must redact non-listed attrs for non-owners |
| Player state → broadcast `state.players` | must be filtered per-viewer, not a raw unfiltered channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-05 | Information Disclosure | `static visibleAttributes` declared but never enforced (F2) | mitigate | Implement the filter in `filterElement`'s visible-element fallthrough with a Player-owns-itself owner check; no documented no-op control remains |
| T-131-06 | Information Disclosure | `state.players` raw `player.toJSON()` bypasses all visibility (F8) | mitigate | Derive `state.players` from the already-filtered `truthView`; eliminate the parallel unfiltered channel |
| T-131-05b | Information Disclosure | Owner loses visibility into own restricted attribute (Pitfall 4 regression) | mitigate | Special-case `element instanceof Player` to compare `seat === visibilityPosition`; explicit owner-self-visibility test |
</threat_model>

<verification>
Both regression suites green. `grep -n "instanceof Player" src/engine/element/game.ts` confirms the owner special-case. `state.players` derived from truthView (no raw player.toJSON pass). RED-then-GREEN in SUMMARY. `npm run test` + `npm run audit:duplication` clean before wave merge. Verify against `~/BoardSmithGames/go-fish` that `bookCount` still renders (public-by-default) if a browser check is warranted.
</verification>

<success_criteria>
visibleAttributes enforced (owner sees own, non-owners/spectators redacted); state.players filtered per-viewer via one mechanism; public-by-default preserved; tests proven red-then-green.
</success_criteria>

<output>
Create `.planning/phases/131-serialization-restore-fidelity/131-04-SUMMARY.md` when done, recording RED output.
</output>
