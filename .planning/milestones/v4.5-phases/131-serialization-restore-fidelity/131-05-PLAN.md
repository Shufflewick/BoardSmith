---
phase: 131-serialization-restore-fidelity
plan: 05
type: execute
wave: 3
depends_on: ["131-01", "131-02", "131-04"]
files_modified:
  - src/engine/element/space.ts
  - src/engine/element/game.ts
  - src/engine/element/handler-restore.test.ts
  - docs/common-pitfalls.md
autonomous: true
requirements: [RST-01, SEC-04, PROC-02]
must_haves:
  truths:
    - "onEnter/onExit handlers registered in the game constructor still fire after snapshot restore"
    - "Handlers are re-bound from the constructor-built tree before it is discarded, matched to rebuilt elements by stable identity"
    - "Any handler that cannot be re-matched emits a loud devWarn (no silent loss)"
    - "Docs (game.ts registerDebug JSDoc + docs/common-pitfalls.md) no longer model dumping hidden state into registerDebug"
  artifacts:
    - path: "src/engine/element/game.ts"
      provides: "handler capture-before-discard + rebind-after-rebuild in loadSerializedState; corrected registerDebug JSDoc"
      contains: "loadSerializedState"
    - path: "src/engine/element/space.ts"
      provides: "scoped internal accessor for _eventHandlers"
      contains: "_eventHandlers"
    - path: "src/engine/element/handler-restore.test.ts"
      provides: "Red-first RST-01 regression test"
      contains: "onEnter"
  key_links:
    - from: "src/engine/element/game.ts loadSerializedState (pre-discard)"
      to: "rebuilt tree (post-fromJSON)"
      via: "handler re-attach by class + path/name identity"
      pattern: "_eventHandlers"
    - from: "unmatched handler"
      to: "devWarn"
      via: "loud warning on failed re-bind"
      pattern: "devWarn"
---

<objective>
Fix RST-01 (F10): re-bind `Space.onEnter`/`onExit` handlers across snapshot restore. Before `loadSerializedState` discards the constructor-built tree, capture registered handlers keyed by stable identity; after `fromJSON` rebuilds the tree, re-attach matching handlers and `devWarn` loudly on any that cannot be matched. Also complete SEC-04's documentation half: correct the `registerDebug` JSDoc example in `game.ts` and the `docs/common-pitfalls.md` example so neither models dumping hidden state into `registerDebug` (DOCX-04 same-phase doc rule).

Purpose: RST-01 is silent game-logic loss (onEnter/onExit stop firing after any undo/restore — a correctness footgun). The doc fixes close the SEC-04 loop so guidance stops teaching the insecure pattern the code now gates.
Output: Handler re-binding in `loadSerializedState`, a scoped `Space._eventHandlers` accessor, corrected docs, and a red-first regression test. This plan is the terminal editor of `game.ts` and `space.ts` for the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-serialization-restore-fidelity/131-CONTEXT.md
@.planning/phases/131-serialization-restore-fidelity/131-PATTERNS.md
@.planning/phases/131-serialization-restore-fidelity/131-FINDINGS-VERIFICATION.md

<interfaces>
From src/engine/element/game.ts (the discard point RST-01 must wrap):
```typescript
loadSerializedState(json): void {                 // 2835
  // ...
  this._t.children = [];                          // ~2848 — constructor tree (with _eventHandlers) discarded HERE
  if (json.children) {
    for (const childJson of json.children) {
      const child = GameElement.fromJSON(childJson, this._ctx, this._ctx.classRegistry); // rebuild
      // ...
    }
  }
}
// devWarn already imported (line 40); Player imported (line 26)
// registerDebug JSDoc example lives at ~1172 (method at 1181)
```
From src/engine/element/space.ts:
```typescript
private _eventHandlers: { enter: ElementEventHandler[]; exit: ElementEventHandler[] }; // 106 — PRIVATE
onEnter(...) / onExit(...) push into _eventHandlers;                                    // 208 / 218
```
Pitfall 3: `_eventHandlers` is private → needs a scoped internal accessor (mirror `_t` cross-class-internal convention), NOT a public API widening.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Red-first regression test — onEnter/onExit fire after restore</name>
  <files>src/engine/element/handler-restore.test.ts</files>
  <read_first>
    - src/session/restore-snapshot-authoritative.test.ts (restore-fidelity assertion style — the template)
    - src/engine/element/space.ts (onEnter/onExit at 208/218; _eventHandlers at 106; how handlers fire on element enter/exit)
    - src/engine/element/game.ts (loadSerializedState at 2835; GameRunner.fromSnapshot as the restore primitive)
    - src/runtime/runner.ts (fromSnapshot signature)
    - .planning/phases/131-serialization-restore-fidelity/131-PATTERNS.md (Pattern 4)
  </read_first>
  <behavior>
    - A game whose constructor registers a `space.onEnter(fn)` handler: moving an element into that space fires `fn` (baseline, live game).
    - After `GameRunner.fromSnapshot` (and after undo/restore), moving an element into the restored space STILL fires the handler (a counter/side-effect increments). On current code this fails — the handler is silently dead after restore.
    - An `onExit` handler case symmetric to the above.
  </behavior>
  <action>Create `src/engine/element/handler-restore.test.ts`. Register `onEnter`/`onExit` handlers in a test game constructor that increment a counter (or push to an array). Assert the handler fires on the live game, then snapshot → `GameRunner.fromSnapshot`, then trigger the enter/exit on the restored game and assert the handler STILL fires. This MUST FAIL on current code. Run and record RED output for the SUMMARY (PROC-02).</action>
  <verify>
    <automated>npx vitest run src/engine/element/handler-restore.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/engine/element/handler-restore.test.ts` exists and FAILS before Task 2 (RED) — output captured for the SUMMARY
    - Covers both an onEnter and an onExit handler surviving fromSnapshot
    - The restored-game assertion triggers a real enter/exit (not just inspects internal state)
  </acceptance_criteria>
  <done>Failing RST-01 regression test committed (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-bind onEnter/onExit across restore + correct registerDebug docs</name>
  <files>src/engine/element/space.ts, src/engine/element/game.ts, docs/common-pitfalls.md</files>
  <read_first>
    - src/engine/element/game.ts (loadSerializedState 2835-2857 — the discard/rebuild loop to wrap; devWarn usage at 1638 for the exact helper signature; registerDebug JSDoc at ~1172)
    - src/engine/element/space.ts (_eventHandlers private at 106; onEnter/onExit 208/218 — where to add the scoped accessor; note Plan 02 already added a zoneVisibility accessor here — follow the same convention)
    - docs/common-pitfalls.md (registerDebug example at line 1401)
    - src/engine/element/handler-restore.test.ts (the RED test to turn GREEN)
    - .planning/phases/131-serialization-restore-fidelity/131-PATTERNS.md (Pattern 4; Pitfall 3; Assumption A1 — identity key stability)
    - .planning/phases/131-serialization-restore-fidelity/131-CONTEXT.md (Claude's Discretion: identity-matching scheme; dev-warn loudly on unmatched)
  </read_first>
  <action>Two changes:
1. RST-01 handler re-bind (per D-RST-01): add a scoped internal accessor on `Space` exposing `_eventHandlers` to same-module/engine code (mirror the `_t` cross-class-internal convention and the zoneVisibility accessor added in Plan 02 — do NOT make the field public). In `game.ts` `loadSerializedState`, BEFORE `this._t.children = []`, walk the live constructor tree collecting `Space` instances that have non-empty `_eventHandlers`, keyed by a stable identity (Claude's Discretion: class + tree path, or class + `name` — pick whatever is stable across the serialization round-trip). AFTER the `fromJSON` rebuild loop completes, walk the NEW tree and re-attach captured handlers to spaces matching the same identity key. For any captured handler with no match in the rebuilt tree, emit a loud `devWarn` (reuse the existing `devWarn` import/pattern at game.ts:40/1638) — never silently drop. Do NOT require any game-code changes.
2. SEC-04 docs (DOCX-04 same-phase rule): fix the `registerDebug` JSDoc example in `game.ts` (~1172) and the example in `docs/common-pitfalls.md` (~1401) so neither models dumping hidden/secret state into `registerDebug` — reframe examples to non-secret debug data (e.g. element-tree/counts) and note that debug payloads are dev-only / gated behind `debugEnabled` (the SEC-04 code fix from Plan 03).
Turn the Task 1 test GREEN.</action>
  <verify>
    <automated>npx vitest run src/engine/element/handler-restore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/engine/element/space.ts` exposes a scoped (non-public-API) accessor for `_eventHandlers`; the field remains `private`-intent (not added to the documented public surface)
    - `grep -n "_eventHandlers\|devWarn" src/engine/element/game.ts` shows the capture-before-discard, re-attach-after-rebuild, and unmatched-handler devWarn
    - `handler-restore.test.ts` passes (GREEN) — both onEnter and onExit fire after fromSnapshot
    - `docs/common-pitfalls.md` registerDebug example no longer dumps hidden/secret state; `grep -n "registerDebug" docs/common-pitfalls.md` reflects the corrected guidance
    - The `game.ts` registerDebug JSDoc example (~1172) no longer models dumping hidden state
    - Full suite `npm run test` green; `npm run audit` clean
  </acceptance_criteria>
  <done>onEnter/onExit handlers survive restore with loud devWarn on any unmatched handler; docs no longer teach dumping hidden state into registerDebug.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| constructor-built tree → discarded on restore → rebuilt tree | handler closures must be re-attached, not lost |
| documentation → developer behavior | docs must not teach the insecure registerDebug-dump pattern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-07 | Tampering / Denial of Service (game logic silently stops) | `_eventHandlers` dropped on restore (F10) | mitigate | Capture handlers before tree discard, re-attach by stable identity after rebuild; devWarn on any unmatched handler (no silent loss) |
| T-131-08 | Information Disclosure | Docs modeling hidden-state dumps into `registerDebug` (F15/DOCX-04) | mitigate | Rewrite the game.ts JSDoc + common-pitfalls.md examples to non-secret debug data; note debugEnabled gating |
</threat_model>

<verification>
`npx vitest run src/engine/element/handler-restore.test.ts` passes. `grep -n "_eventHandlers" src/engine/element/game.ts` shows capture + re-attach + devWarn. Docs corrected. RED-then-GREEN in SUMMARY. `npm run test` + `npm run audit` green — this is the phase-terminal engine plan, so the full suite must be green here.
</verification>

<success_criteria>
onEnter/onExit handlers fire after restore; unmatched handlers devWarn loudly; registerDebug docs no longer teach dumping hidden state; regression test proven red-then-green.
</success_criteria>

<output>
Create `.planning/phases/131-serialization-restore-fidelity/131-05-SUMMARY.md` when done, recording RED output and the chosen handler-identity scheme (Claude's Discretion).
</output>
