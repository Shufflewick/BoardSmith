---
phase: 132-engine-element-builder-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/132-engine-element-builder-safety/132-FINDINGS-VERIFICATION.md
autonomous: true
requirements: [PROC-01]
must_haves:
  truths:
    - "Each of F3, F12, F13, F28 has a recorded verdict (LEGITIMATE or REJECTED) written before any fix task runs"
    - "Each LEGITIMATE verdict cites a current file:line trace or a concrete repro of the silent-wrong-path defect against today's post-Phase-131 code"
    - "The F3 verdict explicitly records that Phase 131's WR-03 dev-only detached-destination check does NOT cover self/descendant containment (different bug class)"
    - "The F28 verdict explicitly records that registerAction() currently performs zero validation and that the handler-less check is new registration-time validation (not an extension of startFlow()'s validators)"
  artifacts:
    - path: ".planning/phases/132-engine-element-builder-safety/132-FINDINGS-VERIFICATION.md"
      provides: "Per-finding verification verdicts gating all fix work"
      contains: "VERDICT:"
  key_links: []
---

<objective>
Produce the PROC-01 verification gate for Phase 132: an independent re-verification of all four in-scope audit findings (F3/ENG-01, F12/ENG-05, F13/ENG-06, F28/ENG-08) recorded in `132-FINDINGS-VERIFICATION.md`, each with a LEGITIMATE (repro or current file:line trace) or REJECTED (reasoning) verdict — written BEFORE any fix is planned or written.

Purpose: PROC-01 is a hard, non-negotiable phase gate operationalizing the project's "Prove Before Fix" rule. No fix task in Plans 02-05 may run until this document records a verdict for every finding. 132-RESEARCH.md already contains a fresh file:line re-trace of all four — this plan independently confirms and records those traces against current source, which should be fast.
Output: `.planning/phases/132-engine-element-builder-safety/132-FINDINGS-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-engine-element-builder-safety/132-CONTEXT.md
@.planning/phases/132-engine-element-builder-safety/132-RESEARCH.md
@.planning/phases/132-engine-element-builder-safety/132-PATTERNS.md
@.planning/tmp/v4.5-audit-findings.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify element/action findings (F3, F12) and record verdicts</name>
  <files>.planning/phases/132-engine-element-builder-safety/132-FINDINGS-VERIFICATION.md</files>
  <read_first>
    - .planning/tmp/v4.5-audit-findings.json (original audit text + verdicts for F3, F12)
    - .planning/phases/132-engine-element-builder-safety/132-RESEARCH.md (Findings Verification sections for F3/ENG-01 and F12/ENG-05)
    - src/engine/element/piece.ts (confirm `moveToInternal` signature; the WR-03 `if (isDevMode())` detached-destination ancestor walk ~lines 82-104; the "TREE CORRUPTION" console.error ~116-122; the mutation block that reassigns `this._t.parent = destination` — confirm NO self/descendant containment check exists in ALL modes)
    - src/engine/command/executor.ts (confirm executeMove/executeRemove funnel through `moveToInternal` — single chokepoint claim)
    - src/engine/action/action.ts (confirm resolveArgs second pass ~239-259 has a bare-number branch that rewrites any number matching an element id; confirm `isSerializedElement` at ~278 requires both `id:number` and `className:string`; confirm `looksLikeSerializedElement` at ~269 is looser)
  </read_first>
  <action>Create `.planning/phases/132-engine-element-builder-safety/132-FINDINGS-VERIFICATION.md`. For each finding record one section headed with the finding ID and a line containing the literal token `VERDICT:` followed by `LEGITIMATE` or `REJECTED`. Cover: F3/ENG-01 — trace `moveToInternal` in `piece.ts` proving no self/descendant containment guard runs in any mode; explicitly record that Phase 131's WR-03 check is `isDevMode()`-gated, only `devWarn`s, and detects detached destinations (a different bug class), so it does NOT cover `outer.putInto(inner)` where inner is a descendant of outer; confirm via the call-path list (putInto, remove, Deck.dealTo, executeMove, executeRemove) that `moveToInternal` is the single chokepoint. F12/ENG-05 — trace the resolveArgs second pass (`action.ts` ~239-259) proving the bare-number branch coerces arbitrary numeric non-selection args into GameElements; record that the fix narrows this pass to `isSerializedElement` only and that the first pass (selection args) is intentionally left untouched. Each LEGITIMATE verdict must cite current file:line evidence or concrete repro steps. If a finding does not reproduce against current code, record REJECTED with reasoning instead of inventing a fix. Do NOT write or plan any fix in this task — verdicts only.</action>
  <verify>
    <automated>grep -cE 'VERDICT:' .planning/phases/132-engine-element-builder-safety/132-FINDINGS-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/132-engine-element-builder-safety/132-FINDINGS-VERIFICATION.md` exists
    - Contains a section for each of F3 and F12 with a `VERDICT:` line reading either `LEGITIMATE` or `REJECTED`
    - The F3 section explicitly states WR-03 is dev-only + warns + detects a different bug class, so it does NOT cover self/descendant containment
    - The F3 section lists the move call paths confirming `moveToInternal` is the single chokepoint
    - The F12 section names the exact second-pass line range and confirms `isSerializedElement` already exists (no new helper needed)
    - No fix code written in this task
  </acceptance_criteria>
  <done>F3 and F12 each have a recorded, evidence-backed verdict in 132-FINDINGS-VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify flow/builder findings (F13, F28) and record verdicts</name>
  <files>.planning/phases/132-engine-element-builder-safety/132-FINDINGS-VERIFICATION.md</files>
  <read_first>
    - .planning/tmp/v4.5-audit-findings.json (original audit text for F13, F28)
    - .planning/phases/132-engine-element-builder-safety/132-RESEARCH.md (Findings Verification sections for F13/ENG-06 and F28/ENG-08)
    - src/engine/flow/engine.ts (confirm `executeForEach` ~1151-1176 re-invokes `config.collection(context)` fresh every re-entry against a persisted `itemIndex`; confirm `executeEachPlayer` ~1089-1149 snapshots `eligibleSeats` once guarded by `frame.data?.eligibleSeats === undefined`)
    - src/engine/action/action-builder.ts (confirm constructor default `execute: () => {}` ~line 77; confirm `.build()` ~636-640 returns the same ActionDefinition type with no handler-less marker; confirm `.execute()` terminal method ~625)
    - src/engine/element/game.ts (confirm `registerAction()` ~918-921 performs ZERO validation; confirm `#validateActionReachability` and the PIT-02 element-class check both live in `startFlow()` ~1616-1697, i.e. flow-start-time not registration-time)
    - src/engine/action/types.ts (confirm `ActionDefinition` interface and the existing `undoable?: boolean` field ~451-454 as the flag-shape precedent)
  </read_first>
  <action>Append to `132-FINDINGS-VERIFICATION.md` sections for F13 and F28, each with a `VERDICT:` line. Cover: F13/ENG-06 — trace `executeForEach` proving `config.collection(context)` is re-evaluated on every loop re-entry while `itemIndex` counts iterations (not identity), so a loop body that removes/moves items causes skips; record that `executeEachPlayer`'s `eligibleSeats` snapshot (evaluate once under `frame.data?.X === undefined`, store JSON-plain identity list, re-resolve live object per iteration) is the in-repo template to mirror, and that `frame.data` must store `id:number` for GameElements (never object refs) to survive checkpoint restore. F28/ENG-08 — trace the default no-op `execute: () => {}`, `.build()` returning an unmarked ActionDefinition, and `registerAction()` performing zero validation today; EXPLICITLY record that the handler-less check is NEW registration-time validation inside `registerAction()` itself (not an extension of the `startFlow()`-time `#validateActionReachability`/PIT-02 validators — a registration-time throw is earlier and stricter, so a handler-less action must throw at `registerAction()` even in tests that never call `startFlow()`). Record LEGITIMATE with confirmed line numbers or REJECTED with reasoning. Verdicts only — no fixes.</action>
  <verify>
    <automated>test $(grep -cE 'VERDICT:' .planning/phases/132-engine-element-builder-safety/132-FINDINGS-VERIFICATION.md) -eq 4</automated>
  </verify>
  <acceptance_criteria>
    - The document now contains exactly 4 `VERDICT:` lines total (F3, F12, F13, F28)
    - The F13 section cites the `executeEachPlayer` eligibleSeats snapshot as the mirror template and states GameElement items must be stored as `id:number` in `frame.data`
    - The F28 section explicitly states `registerAction()` currently has zero validation and that the new throw belongs at registration time, NOT merged into `startFlow()`'s validators
    - No fix code written in this plan
  </acceptance_criteria>
  <done>All four findings (F3, F12, F13, F28) carry evidence-backed verdicts; the PROC-01 gate is satisfied and fix plans 02-05 may proceed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game engine state → persisted/restored state | This phase is engine-correctness only; the "boundary" is state integrity across mutation/serialization, not an auth/disclosure boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-01 | Tampering (state integrity) | All 4 findings | mitigate | This plan records a verified verdict per finding BEFORE any fix, ensuring no fix is written against an unproven root cause (Prove Before Fix) |
| T-132-SD | Info Disclosure / Spoofing / Elevation | engine internals | not applicable | Per 132-RESEARCH.md Security Domain: no auth/hidden-information/session boundary is crossed by these four fixes; state-integrity only |
</threat_model>

<verification>
`grep -cE 'VERDICT:' .planning/phases/132-engine-element-builder-safety/132-FINDINGS-VERIFICATION.md` returns 4. Each finding section cites repro or file:line evidence. No production code modified in this plan.
</verification>

<success_criteria>
132-FINDINGS-VERIFICATION.md exists with 4 evidence-backed verdicts, written before any fix. Fix plans 02-05 are unblocked.
</success_criteria>

<output>
Create `.planning/phases/132-engine-element-builder-safety/132-01-SUMMARY.md` when done.
</output>
