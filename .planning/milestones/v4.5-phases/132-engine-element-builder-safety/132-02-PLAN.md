---
phase: 132-engine-element-builder-safety
plan: 02
type: execute
wave: 2
depends_on: ["132-01"]
files_modified:
  - src/engine/element/piece.ts
  - src/engine/element/game-element.test.ts
autonomous: true
requirements: [ENG-01, PROC-02]
must_haves:
  truths:
    - "outer.putInto(inner) where inner is a descendant of outer throws an actionable error instead of detaching outer's subtree"
    - "element.putInto(element) (destination === this) throws the same actionable error"
    - "The thrown error names BOTH the moved element and the destination, and states the invalid relationship"
    - "The new check runs in ALL modes (not gated by isDevMode()) and does not disturb Phase 131's WR-03 dev-only detached-destination devWarn"
    - "A legal putInto (destination is not self and not a descendant of this) still succeeds unchanged"
  artifacts:
    - path: "src/engine/element/piece.ts"
      provides: "moveToInternal self/descendant containment throw at the mutation chokepoint"
      contains: "descendant"
    - path: "src/engine/element/game-element.test.ts"
      provides: "Red-first regression test for ENG-01 in the Piece Movement describe block"
      contains: "putInto"
  key_links:
    - from: "src/engine/element/piece.ts moveToInternal"
      to: "destination._t.parent ancestor chain"
      via: "O(depth) walk checking against `this`"
      pattern: "_t\\.parent"
---

<objective>
Implement ENG-01 (F3): `putInto()` onto the element's own descendant (or itself) throws an actionable error at the `moveToInternal` chokepoint instead of silently detaching the subtree and producing a detached cycle.

Purpose: `moveToInternal` is the single low-level mutation chokepoint for all move paths (putInto, remove, Deck.dealTo, executeMove, executeRemove); a self/descendant `putInto` currently corrupts the element tree in ALL environments. Per CONTEXT this is real production state corruption and must throw unconditionally — distinct from Phase 131's dev-only WR-03 diagnostic.
Output: A containment guard in `moveToInternal` plus a red-first regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-engine-element-builder-safety/132-CONTEXT.md
@.planning/phases/132-engine-element-builder-safety/132-RESEARCH.md
@.planning/phases/132-engine-element-builder-safety/132-PATTERNS.md
@.planning/phases/132-engine-element-builder-safety/132-FINDINGS-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — regression test for self/descendant putInto</name>
  <files>src/engine/element/game-element.test.ts</files>
  <read_first>
    - src/engine/element/game-element.test.ts (the existing `describe('Piece Movement', ...)` block at line 270 — reuse its Game/Piece/Space setup helpers)
    - src/engine/element/piece.ts (current `moveToInternal` and `putInto` signatures so the test drives the real API)
    - .planning/phases/132-engine-element-builder-safety/132-FINDINGS-VERIFICATION.md (F3 verdict — the confirmed corruption shape to assert against)
  </read_first>
  <behavior>
    - Test A: building a parent → child tree, then calling `parent.putInto(child)` throws an error whose message contains both element identities and the word "descendant".
    - Test B: `element.putInto(element)` (self) throws the same class of actionable error.
    - Test C (control): a legal move (`piece.putInto(unrelatedSpace)` where the space is neither self nor a descendant) succeeds and re-parents correctly.
  </behavior>
  <action>Add a new nested block (e.g. `describe('ENG-01 self/descendant containment')`) inside the existing `Piece Movement` describe in `game-element.test.ts`. Assert Test A and Test B via `expect(() => ...).toThrowError(/descendant/)` (or a regex matching the chosen wording that also names both elements), and Test C via a positive re-parent assertion (destination `_t.children` includes the moved piece; moved piece `_t.parent` is the destination). Run the file and record the RED failure output (the throw does not yet exist) for the SUMMARY per PROC-02. Do NOT implement the fix in this task.</action>
  <verify>
    <automated>npx vitest run src/engine/element/game-element.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - New test cases exist inside the `Piece Movement` describe block covering self, descendant, and a legal-move control
    - Running the file shows the self/descendant cases FAILING (no throw yet) — RED output captured for the SUMMARY
    - The control (legal move) case passes both before and after the fix
  </acceptance_criteria>
  <done>Red-first tests exist and fail against unpatched `moveToInternal`; RED output recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — self/descendant containment throw in moveToInternal</name>
  <files>src/engine/element/piece.ts</files>
  <read_first>
    - src/engine/element/piece.ts (the full `moveToInternal` method incl. the WR-03 `if (isDevMode())` block ~82-104 and the "TREE CORRUPTION" console.error ~116-122 for message-tone reference)
    - .planning/phases/132-engine-element-builder-safety/132-PATTERNS.md (the ENG-01 ancestor-walk idiom and the explicit note to add a `destination === this` pre-check for the root-destination edge case)
  </read_first>
  <action>Insert a self/descendant containment guard as the FIRST statement of `moveToInternal`, BEFORE the WR-03 `if (isDevMode())` block, running unconditionally in all modes. First handle the root-destination edge case with an explicit `destination === this` pre-check, then walk `destination` up its `_t.parent` ancestor chain; if `this` is encountered at any level, throw. The thrown `Error` message MUST name both the moved element (`this.name ?? this.constructor.name`) and the destination (`destination.name ?? destination.constructor.name`) and state the invalid relationship (e.g. "Cannot move X into its own descendant Y"). Do NOT gate the throw behind `isDevMode()` and do NOT merge it into the WR-03 block (per RESEARCH Pitfall 1 — WR-03 is a dev-only devWarn for a different bug class and must remain intact). Do not modify any other part of `moveToInternal`. If any doc under `docs/` documents `putInto` self/descendant behavior, update it to state the throw (DOCX-04 same-phase touch); otherwise no doc change is needed.</action>
  <verify>
    <automated>npx vitest run src/engine/element/game-element.test.ts src/engine/element/element-collection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `moveToInternal` throws before any tree mutation when `destination === this` OR `this` is an ancestor of `destination`... i.e. when destination is self or a descendant of this
    - The throw is NOT wrapped in `isDevMode()`; the WR-03 `if (isDevMode())` devWarn block is unchanged
    - `grep -n "isDevMode" src/engine/element/piece.ts` shows the new throw block is not inside the isDevMode guard
    - All three ENG-01 test cases pass (self + descendant throw, legal move succeeds); `game-element.test.ts` and `element-collection.test.ts` are green
  </acceptance_criteria>
  <done>Self/descendant `putInto` throws an actionable, dual-named error in all modes; WR-03 preserved; targeted suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game element tree mutation | `moveToInternal` is the sole low-level re-parent chokepoint; an invalid move corrupts tree integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-01 | Tampering (state integrity) | Piece.moveToInternal | mitigate | Unconditional self/descendant ancestor-walk throw before any `_t.parent`/`_t.children` mutation prevents the detached-cycle corruption |
| T-132-SD | Info Disclosure / auth | — | not applicable | Engine-internal tree mutation; no session/client boundary crossed (132-RESEARCH.md Security Domain) |
</threat_model>

<verification>
`npx vitest run src/engine/element/game-element.test.ts src/engine/element/element-collection.test.ts` green. RED output from Task 1 recorded in SUMMARY. Full suite (`npm test`) run at wave close must stay green (168+ files / 2135+ tests).
</verification>

<success_criteria>
ENG-01 satisfied: self/descendant `putInto` throws an actionable dual-named error in all modes; legal moves unchanged; WR-03 dev diagnostic intact; regression test red-then-green.
</success_criteria>

<output>
Create `.planning/phases/132-engine-element-builder-safety/132-02-SUMMARY.md` when done.
</output>
