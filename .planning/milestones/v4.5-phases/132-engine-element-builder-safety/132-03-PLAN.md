---
phase: 132-engine-element-builder-safety
plan: 03
type: execute
wave: 2
depends_on: ["132-01"]
files_modified:
  - src/engine/action/action.ts
  - src/engine/action/action.test.ts
autonomous: true
requirements: [ENG-05, PROC-02]
must_haves:
  truths:
    - "A non-selection followUp arg holding a bare number survives resolveArgs as a number, not a GameElement"
    - "A non-selection followUp arg shaped {id, className} matching a live element is still resolved to that GameElement"
    - "The first pass (named selection args: element/elements/choice) is unchanged — bare numeric IDs sent for selection-typed args still resolve"
    - "No new helper is added; the second pass uses the existing isSerializedElement (id:number AND className:string), not looksLikeSerializedElement"
  artifacts:
    - path: "src/engine/action/action.ts"
      provides: "resolveArgs second pass narrowed to isSerializedElement, bare-number branch removed"
      contains: "isSerializedElement"
    - path: "src/engine/action/action.test.ts"
      provides: "Red-first regression test for ENG-05 in the executeAction describe block"
      contains: "resolveArgs"
  key_links:
    - from: "src/engine/action/action.ts resolveArgs second pass"
      to: "isSerializedElement"
      via: "shape gate on non-selection args"
      pattern: "isSerializedElement"
---

<objective>
Implement ENG-05 (F12): `resolveArgs`'s second pass (non-selection / followUp args) stops coercing bare numbers into GameElements. Only `{id, className}`-shaped objects (via the existing `isSerializedElement`) are resolved; bare numbers survive as numbers.

Purpose: The second-pass bare-number branch silently rewrites any numeric arg whose value happens to match an element id into a GameElement — an ambiguous, corruption-prone auto-coercion. Clean break per No Backward Compatibility; the first pass (selection-typed args) keeps its intentional looser resolution untouched.
Output: A narrowed second pass in `resolveArgs` plus a red-first regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-engine-element-builder-safety/132-CONTEXT.md
@.planning/phases/132-engine-element-builder-safety/132-RESEARCH.md
@.planning/phases/132-engine-element-builder-safety/132-PATTERNS.md
@.planning/phases/132-engine-element-builder-safety/132-FINDINGS-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — regression test proving bare numbers are not coerced</name>
  <files>src/engine/action/action.test.ts</files>
  <read_first>
    - src/engine/action/action.test.ts (the existing `describe('executeAction', ...)` block at line 447 — reuse its game/action setup for driving resolveArgs via executeAction)
    - src/engine/action/action.ts (resolveArgs first vs second pass; `isSerializedElement` ~278 and `looksLikeSerializedElement` ~269 so the test asserts the right boundary)
    - .planning/phases/132-engine-element-builder-safety/132-FINDINGS-VERIFICATION.md (F12 verdict)
  </read_first>
  <behavior>
    - Test A: an action executed with a non-selection followUp arg holding a bare number equal to a live element's id receives that arg as a NUMBER in its execute handler (not a resolved GameElement).
    - Test B: a non-selection followUp arg shaped `{ id: <liveId>, className: <cls> }` IS resolved to the corresponding GameElement.
    - Test C (control): a named `element` selection arg passed as a bare numeric id still resolves to the element (first pass unchanged).
  </behavior>
  <action>Add a nested block (e.g. `describe('ENG-05 followUp arg resolution')`) inside the existing `executeAction` describe in `action.test.ts`. Capture what the execute handler actually receives (e.g. record the arg into a closure variable or game property) and assert its runtime type/shape. Test A asserts `typeof received === 'number'`; Test B asserts the received value is the GameElement instance; Test C asserts the selection arg still resolves. Run the file and record the RED failure (Test A currently fails because the bare number is coerced to an element). Do NOT implement the fix in this task.</action>
  <verify>
    <automated>npx vitest run src/engine/action/action.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - New cases exist inside the `executeAction` describe covering bare-number (A), serialized-object (B), and selection-arg control (C)
    - Test A FAILS against unpatched code (bare number wrongly coerced) — RED output captured for the SUMMARY
    - Test C passes both before and after the fix (first-pass behavior preserved)
  </acceptance_criteria>
  <done>Red-first tests exist and Test A fails against unpatched `resolveArgs`; RED output recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — narrow resolveArgs second pass to isSerializedElement</name>
  <files>src/engine/action/action.ts</files>
  <read_first>
    - src/engine/action/action.ts (the second pass loop ~239-259 to edit; the first pass ~161-237 to NOT touch; `isSerializedElement` ~275-282)
    - .planning/phases/132-engine-element-builder-safety/132-PATTERNS.md (the ENG-05 target shape + Pitfall 2: do not narrow the first pass)
  </read_first>
  <action>In the resolveArgs SECOND pass only (non-selection args, ~239-259): delete the bare-number branch entirely (the `if (typeof value === 'number')` block that calls `getElementById`), and replace the `looksLikeSerializedElement(value)` gate with `isSerializedElement(value)` so only `{id:number, className:string}`-shaped objects are resolved. Leave the `selectionNames.has(key)` / `value === undefined` guards intact. Do NOT modify the first pass (lines ~161-237) — its looser `looksLikeSerializedElement`/bare-number resolution for selection-typed `element`/`elements`/`choice` args is intentional (RESEARCH Pitfall 2). Do not add any new helper (`isSerializedElement` already exists). Update the inline comment to state bare numbers are never coerced. If any doc under `docs/` documents followUp bare-number arg coercion, update it to reflect the clean break (DOCX-04).</action>
  <verify>
    <automated>npx vitest run src/engine/action/action.test.ts src/engine/action/action-typed-args.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The second-pass bare-number `if (typeof value === 'number')` branch is removed; the object branch gates on `isSerializedElement` (not `looksLikeSerializedElement`)
    - `grep -n "looksLikeSerializedElement" src/engine/action/action.ts` shows it still used in the FIRST pass but NOT in the second pass
    - First-pass selection-arg resolution is unchanged (Test C green)
    - `action.test.ts` and `action-typed-args.test.ts` are green
  </acceptance_criteria>
  <done>Second pass resolves only serialized-element-shaped objects; bare numbers survive; first pass untouched; targeted suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network-serialized action args → resolved execute() args | resolveArgs normalizes untrusted-shaped args server-side before execute; ambiguous coercion is a state-integrity hazard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-01 | Tampering (state integrity) | resolveArgs second pass | mitigate | Restrict coercion to unambiguous `{id, className}` objects; bare numbers can no longer be silently promoted to a colliding element |
| T-132-SD | Elevation / auth | — | not applicable | This is arg-shape normalization, not access control; selection-arg auth path unchanged (132-RESEARCH.md Security Domain) |
</threat_model>

<verification>
`npx vitest run src/engine/action/action.test.ts src/engine/action/action-typed-args.test.ts` green. RED output from Task 1 recorded in SUMMARY. Full suite (`npm test`) at wave close must stay green.
</verification>

<success_criteria>
ENG-05 satisfied: bare numeric non-selection args survive as numbers, `{id, className}` objects still resolve, first pass untouched; regression test red-then-green.
</success_criteria>

<output>
Create `.planning/phases/132-engine-element-builder-safety/132-03-SUMMARY.md` when done.
</output>
