---
phase: 133-engine-flow-action-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/133-engine-flow-action-validation/133-FINDINGS-VERIFICATION.md
autonomous: true
requirements: [PROC-01]
must_haves:
  truths:
    - "Each of F4, F5, F6, F27 has a recorded verdict (LEGITIMATE or REJECTED) written before any fix task runs"
    - "Each LEGITIMATE verdict cites a current (post-Phase-132) file:line trace of the silent-wrong-path defect, not the audit's pre-132 line numbers"
    - "The F4 verdict records that executeEachPlayer builds eligibleSeats via players.slice(startIndex) with no wrap (engine.ts:1127) and that no shipped game uses startingPlayer (no double-fix hazard)"
    - "The F5 verdict records that resumeSimultaneousAction's failure branch returns getState() WITHOUT setting this.actionError, and that resume()'s regular path (engine.ts:279) is the correct sibling to mirror"
    - "The F6 verdict records that the choice branch of validateSelection (action.ts:706-741) has no multiSelect count check, and that ChoiceSelection.multiSelect is structurally identical to the already-enforced ElementsSelection.multiSelect (action.ts:802-817)"
    - "The F27 verdict records that executeSwitch marks the frame completed and continues silently when !branch, and that SwitchConfig inherits an optional name from BaseFlowConfig"
  artifacts:
    - path: ".planning/phases/133-engine-flow-action-validation/133-FINDINGS-VERIFICATION.md"
      provides: "Per-finding verification verdicts gating all fix work in Plans 02-05"
      contains: "VERDICT:"
  key_links: []
---

<objective>
Produce the PROC-01 verification gate for Phase 133: an independent re-verification of all four in-scope audit findings (F4/ENG-02, F5/ENG-03, F6/ENG-04, F27/ENG-07) recorded in `133-FINDINGS-VERIFICATION.md`, each with a LEGITIMATE (current file:line trace or concrete repro) or REJECTED (reasoning) verdict — written BEFORE any fix is planned or written.

Purpose: PROC-01 is a hard, non-negotiable phase gate operationalizing the project's "Prove Before Fix" rule. No fix task in Plans 02-05 may run until this document records a verdict for every finding. 133-RESEARCH.md already re-traced all four against current source; Phase 132's ENG-05/ENG-06 fixes touched adjacent code in engine.ts and action.ts, so line numbers must be re-confirmed here, not assumed from the audit report.
Output: `.planning/phases/133-engine-flow-action-validation/133-FINDINGS-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-engine-flow-action-validation/133-CONTEXT.md
@.planning/phases/133-engine-flow-action-validation/133-RESEARCH.md
@.planning/phases/133-engine-flow-action-validation/133-PATTERNS.md
@.planning/tmp/v4.5-audit-findings.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify flow findings F4 (ENG-02) and F5 (ENG-03) and record verdicts</name>
  <files>.planning/phases/133-engine-flow-action-validation/133-FINDINGS-VERIFICATION.md</files>
  <read_first>
    - .planning/tmp/v4.5-audit-findings.json (original audit text + verifier verdicts for F4, F5)
    - .planning/phases/133-engine-flow-action-validation/133-RESEARCH.md (Pattern 1 eligibleSeats wrap; Pattern 2 resumeSimultaneousAction actionError mirror)
    - src/engine/flow/engine.ts (confirm executeEachPlayer builds `eligibleSeats: players.slice(startIndex).map(...)` with NO wrap at ~line 1127, guarded by `frame.data?.eligibleSeats === undefined` ~1107; confirm resumeSimultaneousAction ~line 426 failure branch returns `this.getState()` WITHOUT setting `this.actionError`; confirm the correct sibling resume() sets `this.actionError = result.error` on failure ~279 and clears it ~immediately after on success)
    - src/runtime/runner.ts (confirm performAction reads `flowState.actionError` as the sole failure signal ~207 and gates `actionHistory.push` on the same check ~217)
  </read_first>
  <action>Create `.planning/phases/133-engine-flow-action-validation/133-FINDINGS-VERIFICATION.md`. For each finding record one section headed with the finding ID and a line containing the literal token `VERDICT:` followed by `LEGITIMATE` or `REJECTED`. Cover: F4/ENG-02 — trace `executeEachPlayer` proving `eligibleSeats` is built by `players.slice(startIndex)` with no wrap (cite the current line, ~1127; note Phase 132's a85c4ae6 executeForEach fix landed nearby so the number may have shifted — record the actual current line); record that with a non-zero `startingPlayer`, seats before `startIndex` are silently dropped for the round; record that grep confirms zero shipped games (8 examples + MERC) pass `eachPlayer({ startingPlayer })`, so there is no working-around-the-bug double-fix hazard. F5/ENG-03 — trace `resumeSimultaneousAction`'s failure branch proving it returns `this.getState()` without setting `this.actionError` (cite current line, ~468-471); record that `resume()` at ~279 IS the correct sibling (sets error on failure, clears on success) and that `runner.ts` performAction (~207/217) is the sole consumer that turns a set `actionError` into `{success:false}` + a skipped `actionHistory.push`. Each LEGITIMATE verdict must cite current file:line evidence. If a finding does not reproduce against current code, record REJECTED with reasoning instead of inventing a fix. Do NOT write or plan any fix in this task — verdicts only.</action>
  <verify>
    <automated>grep -cE 'VERDICT:' .planning/phases/133-engine-flow-action-validation/133-FINDINGS-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `133-FINDINGS-VERIFICATION.md` exists
    - Contains a section for each of F4 and F5 with a `VERDICT:` line reading either `LEGITIMATE` or `REJECTED`
    - The F4 section cites the current (re-confirmed) line of the `players.slice(startIndex)` expression and states seats before startIndex are dropped without wrap
    - The F4 section records that zero shipped games use `eachPlayer({ startingPlayer })`
    - The F5 section names the current line of the failure branch that omits `this.actionError` and names `resume()` (~279) as the sibling to mirror plus `runner.ts` (~207/217) as the sole consumer
    - No fix code written in this task
  </acceptance_criteria>
  <done>F4 and F5 each have a recorded, evidence-backed verdict in 133-FINDINGS-VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify F6 (ENG-04) and F27 (ENG-07) and record verdicts</name>
  <files>.planning/phases/133-engine-flow-action-validation/133-FINDINGS-VERIFICATION.md</files>
  <read_first>
    - .planning/tmp/v4.5-audit-findings.json (original audit text for F6, F27)
    - .planning/phases/133-engine-flow-action-validation/133-RESEARCH.md (Pattern 3 choice-branch multiSelect enforcement; Pattern 4 switchOn throw; Open Questions on error wording)
    - src/engine/action/action.ts (confirm the choice branch `if (selection.type === 'choice' || selection.type === 'element')` ~706 validates array-item membership only with NO count/array-type check; confirm the elements branch ~802-817 already enforces multiSelect min/max)
    - src/engine/action/types.ts (confirm `ChoiceSelection.multiSelect` and `ElementsSelection.multiSelect` share the shape `number | MultiSelectConfig | ((context) => number | MultiSelectConfig | undefined)`)
    - src/engine/flow/engine.ts (confirm executeSwitch marks `frame.completed = true` and returns `{continue:true}` when `!branch`, ~1437-1445)
    - src/engine/flow/types.ts:73-76,178 (confirm `SwitchConfig extends BaseFlowConfig` and that BaseFlowConfig supplies an optional `name` field usable in the ENG-07 error message)
  </read_first>
  <action>Append to `133-FINDINGS-VERIFICATION.md` sections for F6 and F27, each with a `VERDICT:` line. Cover: F6/ENG-04 — trace the choice branch of `validateSelection` (~706-741) proving it does membership checks only and NEVER enforces `multiSelect` count nor rejects non-array submissions; record the elements-branch port source (~802-817) and that `ChoiceSelection.multiSelect` is structurally identical to `ElementsSelection.multiSelect` (types.ts), so the fix is a port PLUS a new explicit non-array rejection (the elements branch degrades a scalar to count=1, which is insufficient here per the locked decision). F27/ENG-07 — trace `executeSwitch` proving it silently completes the frame when no case matches and no default exists; record that `SwitchConfig` inherits an optional `name` from `BaseFlowConfig` (types.ts:73-76), so the error message MAY include the node name (matching the loop maxIterations precedent) but the generalized form `switchOn got '<value>' — no matching case (<keys>) and no default` is the required baseline; record that a plain thrown `Error` here propagates through `runner.ts`'s existing catch to `{success:false, errorCode: ENGINE_ERROR}` (same path as resumeSimultaneousAction's existing throws). Record LEGITIMATE with confirmed line numbers or REJECTED with reasoning. Verdicts only — no fixes.</action>
  <verify>
    <automated>test $(grep -cE 'VERDICT:' .planning/phases/133-engine-flow-action-validation/133-FINDINGS-VERIFICATION.md) -eq 4</automated>
  </verify>
  <acceptance_criteria>
    - The document now contains exactly 4 `VERDICT:` lines total (F4, F5, F6, F27)
    - The F6 section names the choice-branch line range with no count check and confirms `ChoiceSelection.multiSelect` shares the elements-branch shape; explicitly states the non-array rejection is NEW logic beyond a straight port
    - The F27 section confirms `SwitchConfig` inherits `name` from `BaseFlowConfig` and records the required generalized error wording plus the propagation-to-runner path
    - No fix code written in this plan
  </acceptance_criteria>
  <done>All four findings (F4, F5, F6, F27) carry evidence-backed verdicts; the PROC-01 gate is satisfied and fix plans 02-05 may proceed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game engine flow/action state → client-visible action result | This phase is engine-correctness; the boundary is failure-signal integrity (does a silent skip / silent success-lie reach the client), not an auth/disclosure boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01 | Tampering (state/signal integrity) | All 4 findings | mitigate | This plan records a verified verdict per finding BEFORE any fix, ensuring no fix is written against an unproven root cause (Prove Before Fix) |
| T-133-SD | Info Disclosure / Spoofing / Elevation | engine internals | accept | Per 133-RESEARCH.md Security Domain: no auth/hidden-information/session boundary is crossed; ENG-04 is defense-in-depth server-side input validation, captured in Plan 04's threat model |
</threat_model>

<verification>
`grep -cE 'VERDICT:' .planning/phases/133-engine-flow-action-validation/133-FINDINGS-VERIFICATION.md` returns 4. Each finding section cites current file:line evidence or repro. No production code modified in this plan.
</verification>

<success_criteria>
133-FINDINGS-VERIFICATION.md exists with 4 evidence-backed verdicts, written before any fix. Fix plans 02-05 are unblocked.
</success_criteria>

<output>
Create `.planning/phases/133-engine-flow-action-validation/133-01-SUMMARY.md` when done.
</output>
