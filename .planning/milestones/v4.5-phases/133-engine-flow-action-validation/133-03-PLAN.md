---
phase: 133-engine-flow-action-validation
plan: 03
type: execute
wave: 3
depends_on: ["133-01", "133-02"]
files_modified:
  - src/engine/flow/engine.ts
  - src/engine/flow/engine.test.ts
  - src/runtime/runner.test.ts
autonomous: true
requirements: [ENG-03, PROC-02]
must_haves:
  truths:
    - "A failed action inside a simultaneousActionStep sets FlowState.actionError to the failure error and returns getState() (engine-level assertion)"
    - "resumeSimultaneousAction clears this.actionError on a successful action, so a later success in the same step does not leave a stale error from a prior failed attempt"
    - "Through GameRunner.performAction, a failed simultaneous action returns {success:false} and is NOT pushed to actionHistory (runner-level assertion)"
    - "A failed simultaneous action does not disturb awaitingPlayers/awaitingInput — only actionError is set (the existing allDone-gated clearing logic is untouched)"
  artifacts:
    - path: "src/engine/flow/engine.ts"
      provides: "resumeSimultaneousAction actionError set-on-failure / clear-on-success mirror of resume()"
      contains: "this.actionError = result.error"
    - path: "src/engine/flow/engine.test.ts"
      provides: "Red-first engine-level regression test for ENG-03"
      contains: "actionError"
    - path: "src/runtime/runner.test.ts"
      provides: "Red-first runner-level assertion that a failed simultaneous action is not recorded in actionHistory"
      contains: "actionHistory"
  key_links:
    - from: "src/engine/flow/engine.ts resumeSimultaneousAction"
      to: "src/runtime/runner.ts performAction"
      via: "FlowState.actionError as the sole failure signal gating actionHistory.push"
      pattern: "actionError"
---

<objective>
Implement ENG-03 (F5, critical): a failed action inside a `simultaneousActionStep` surfaces `actionError`, returns failure to the client, and is NOT recorded in `actionHistory` — mirroring BOTH halves of the regular resume path (set `actionError` on failure AND clear on success).

Purpose: `resumeSimultaneousAction`'s failure branch currently returns `getState()` without setting `this.actionError`, so the one chokepoint every downstream consumer relies on (`runner.ts` performAction at ~207/217) never sees the failure — the action silently "succeeds" and gets pushed to `actionHistory`. The fix mirrors `resume()`'s already-correct sibling exactly. Per the locked decision the behavior must be asserted at BOTH the engine level and the runner level.
Output: The actionError set/clear mirror in `resumeSimultaneousAction` plus red-first engine and runner regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-engine-flow-action-validation/133-CONTEXT.md
@.planning/phases/133-engine-flow-action-validation/133-RESEARCH.md
@.planning/phases/133-engine-flow-action-validation/133-PATTERNS.md
@.planning/phases/133-engine-flow-action-validation/133-FINDINGS-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — engine + runner regression tests for failed simultaneous action</name>
  <files>src/engine/flow/engine.test.ts, src/runtime/runner.test.ts</files>
  <read_first>
    - src/engine/flow/engine.test.ts (existing simultaneous-action-step coverage ~line 1997, e.g. the 'simultaneous-action-step warning also points to the real API' test — reuse its simultaneousActionStep + FlowEngine setup)
    - src/runtime/runner.test.ts (existing GameRunner.performAction tests — reuse the harness that inspects `result.success` and `runner.actionHistory`)
    - src/engine/flow/engine.ts (resumeSimultaneousAction ~426-500 so the tests drive the real failure branch; note the allDone-gated awaitingPlayers/awaitingInput clearing further down must remain unaffected)
    - .planning/phases/133-engine-flow-action-validation/133-FINDINGS-VERIFICATION.md (F5 verdict — the confirmed missing-actionError shape)
  </read_first>
  <behavior>
    - Engine Test A: drive a simultaneousActionStep, submit a FAILING action (e.g. one that fails validation / below-min) for one player, assert `state.actionError` is set (defined) and `awaitingPlayers`/`awaitingInput` are unaffected.
    - Engine Test B (fail-then-succeed): after the failure, submit a VALID action for another player in the same step and assert `state.actionError` has been cleared (undefined) — proving the success-path clear (RESEARCH Pitfall 2).
    - Runner Test C: through `GameRunner.performAction`, a failing simultaneous action returns `{success:false}` with a defined error AND leaves `runner.actionHistory` length unchanged (not pushed).
  </behavior>
  <action>Add a nested engine test block (e.g. `describe('ENG-03 simultaneous action failure signaling')`) near the existing simultaneous-action-step coverage in `engine.test.ts` covering Test A and the fail-then-succeed Test B. Add Runner Test C in `runner.test.ts` asserting `result.success === false`, `result.error` defined, and `actionHistory` length unchanged after the failing simultaneous action. Run both files and record the RED failure output (Test A: actionError undefined; Test C: action wrongly pushed to actionHistory) for the SUMMARY per PROC-02. Do NOT implement the fix in this task.</action>
  <verify>
    <automated>npx vitest run src/engine/flow/engine.test.ts src/runtime/runner.test.ts -t "ENG-03" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - New engine tests cover a failing simultaneous action (actionError set) and a fail-then-succeed sequence (actionError cleared)
    - New runner test asserts a failing simultaneous action returns {success:false} and does not grow actionHistory
    - Running both files shows the new cases FAILING against unpatched code (actionError undefined at engine level; action pushed to actionHistory at runner level) — RED output captured for the SUMMARY
  </acceptance_criteria>
  <done>Red-first engine + runner tests exist and fail against the unpatched resumeSimultaneousAction; RED output recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — actionError set-on-failure / clear-on-success in resumeSimultaneousAction</name>
  <files>src/engine/flow/engine.ts</files>
  <read_first>
    - src/engine/flow/engine.ts (the correct sibling `resume()` ~273-284 that sets `this.actionError = result.error` on failure and clears it on success; the buggy `resumeSimultaneousAction` failure branch ~465-473 returning `this.getState()` without the set; the allDone-gated awaitingPlayers/awaitingInput clearing further down that must NOT be moved)
    - .planning/phases/133-engine-flow-action-validation/133-PATTERNS.md (the ENG-03 actionError set/clear mirror and the explicit note NOT to move `awaitingInput = false` into this branch)
    - .planning/phases/133-engine-flow-action-validation/133-RESEARCH.md (Pitfall 2 — both halves required)
  </read_first>
  <action>In `resumeSimultaneousAction`, mirror `resume()` exactly around the `result.success` check: set `this.actionError = result.error;` inside the failure branch before `return this.getState();`, and add `this.actionError = undefined;` immediately after the failure check (before the existing `playerState`/`allDone` re-evaluation continuation). Do NOT move `this.awaitingInput = false` into this branch — `resumeSimultaneousAction` has its own allDone-gated awaitingInput/awaitingPlayers clearing further down that is structurally different from resume()'s single-player completion and must remain untouched. Do not modify `runner.ts` — it is the sole (correct) consumer; the fix flows to it through `flowState.actionError` unchanged.</action>
  <verify>
    <automated>npx vitest run src/engine/flow/engine.test.ts src/runtime/runner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `resumeSimultaneousAction` sets `this.actionError = result.error` on failure and sets `this.actionError = undefined` on success, matching resume()'s placement
    - `this.awaitingInput = false` was NOT added to the failure/success branch; the allDone-gated clearing logic is unchanged
    - All three ENG-03 tests pass (engine actionError set + cleared; runner failure not recorded in actionHistory)
    - `runner.ts` unmodified (`git diff --name-only` does not list src/runtime/runner.ts)
    - Targeted engine + runner suites green
  </acceptance_criteria>
  <done>Failed simultaneous actions surface actionError, return failure to the client, and are excluded from actionHistory; success clears the error; regression tests red-then-green at both engine and runner levels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flow action resolution → client-visible success/failure + actionHistory | resumeSimultaneousAction's return is the sole failure signal; a missing actionError makes a failed action look successful and pollutes the undo/replay history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-03 | Tampering (state integrity) / Repudiation | resumeSimultaneousAction failure branch | mitigate | Set actionError on failure so runner.performAction reports {success:false} and skips actionHistory.push; clear on success to avoid stale-error leakage; asserted at engine + runner level |
| T-133-SD | Info Disclosure / auth | engine flow internals | accept | Server-side flow-control failure signaling; no auth/session boundary crossed (133-RESEARCH.md Security Domain) |
</threat_model>

<verification>
`npx vitest run src/engine/flow/engine.test.ts src/runtime/runner.test.ts` green. RED output from Task 1 recorded in SUMMARY. Full suite (`npm test`) at wave close must stay green.
</verification>

<success_criteria>
ENG-03 satisfied: failed simultaneous actions surface actionError and stay out of actionHistory; success clears the error; asserted at both engine and runner level; regression tests red-then-green.
</success_criteria>

<output>
Create `.planning/phases/133-engine-flow-action-validation/133-03-SUMMARY.md` when done.
</output>
