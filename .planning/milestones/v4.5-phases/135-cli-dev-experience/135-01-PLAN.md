---
phase: 135-cli-dev-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/135-cli-dev-experience/135-FINDINGS-VERIFICATION.md
autonomous: true
requirements: [PROC-01]
must_haves:
  truths:
    - "Each of F9, F21, F22, F32, F33, F34 has a recorded verdict (LEGITIMATE or REJECTED) written before any fix task in Plans 02-06 runs"
    - "Each LEGITIMATE verdict cites a current (HEAD, post-Phase-134) file:line trace of the defect, not the audit's original line numbers"
    - "The F21 verdict records the independently re-confirmed real server limit from ~/ShufflewickPubGames/src/upload.ts (MAX_BUNDLE_SIZE = 50 * 1024 * 1024) and states the code (200MB) is wrong, not the comment"
    - "The F32 verdict records that CONTEXT.md's locked decision is default 127.0.0.1 (NOT the 0.0.0.0-by-default the RESEARCH State-of-the-Art table mistakenly attributes to CONTEXT) and that dev.ts:283 currently defaults to 0.0.0.0"
  artifacts:
    - path: ".planning/phases/135-cli-dev-experience/135-FINDINGS-VERIFICATION.md"
      provides: "Per-finding verification verdicts gating all fix work in Plans 02-06"
      contains: "VERDICT:"
  key_links: []
---

<objective>
Produce the PROC-01 verification gate for Phase 135: an independent re-verification of all six in-scope audit findings (F9/CLIX-01, F21/CLIX-03, F22/CLIX-02, F32/CLIX-04, F33/CLIX-05, F34/CLIX-06) recorded in `135-FINDINGS-VERIFICATION.md`, each with a LEGITIMATE (current file:line trace) or REJECTED (reasoning) verdict — written BEFORE any fix is planned or written.

Purpose: PROC-01 operationalizes the project's "Prove Before Fix" rule. No fix task in Plans 02-06 may run until this document records a verdict for every finding. 135-RESEARCH.md already re-traced all six against current source, but two findings need a verdict-recorded correction that downstream plans depend on: (a) F21 — the code constant is wrong (200MB), not the comment (50MB); (b) F32 — the RESEARCH State-of-the-Art table misreads CONTEXT.md and claims 0.0.0.0 stays the default, but CONTEXT.md's locked decision is default 127.0.0.1. This gate is where that correction is recorded authoritatively.
Output: `.planning/phases/135-cli-dev-experience/135-FINDINGS-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-cli-dev-experience/135-CONTEXT.md
@.planning/phases/135-cli-dev-experience/135-RESEARCH.md
@.planning/phases/135-cli-dev-experience/135-PATTERNS.md
@.planning/tmp/v4.5-audit-findings.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify config-integrity findings F9, F21, F22 and record verdicts</name>
  <files>.planning/phases/135-cli-dev-experience/135-FINDINGS-VERIFICATION.md</files>
  <read_first>
    - .planning/tmp/v4.5-audit-findings.json (original audit text + verifier verdicts for F9, F21, F22 — indices noted in RESEARCH.md Sources)
    - .planning/phases/135-cli-dev-experience/135-RESEARCH.md (Pattern 1 derive-not-duplicate; Pitfall 1 build.ts manifest spread; the bundle-size code-vs-comment analysis)
    - src/cli/lib/project-scaffold.ts (confirm: generateBoardsmithJson writes playerCount ~line 109 AND generateRulesIndexTs hardcodes minPlayers/maxPlayers ~240-246 — the "two places"; dead $schema URL ~105)
    - src/cli/commands/build.ts (confirm manifest is a raw { ...config } spread at 108-115 — never loads gameDefinition; imports BUNDLE_PROTOCOL_VERSION at line 6)
    - src/cli/commands/validate.ts (confirm required array includes 'playerCount' at line 94; playerCount object checks 102-107; bundle constant maxTotalBundle = 200 * 1024 * 1024 at line 226 with comment stating 50MB)
    - ~/ShufflewickPubGames/src/upload.ts (independently re-read line 4: MAX_BUNDLE_SIZE = 50 * 1024 * 1024 — the authoritative external server gate)
  </read_first>
  <action>Create `.planning/phases/135-cli-dev-experience/135-FINDINGS-VERIFICATION.md`. For each finding write a section headed with the finding ID and requirement ID, containing a line with the literal token `VERDICT:` followed by `LEGITIMATE` or `REJECTED`. Cover: F9/CLIX-01 — trace the dual authorship of player count (boardsmith.json playerCount key via generateBoardsmithJson AND gameDefinition minPlayers/maxPlayers via generateRulesIndexTs) plus build.ts's raw { ...config } manifest spread that silently forwards any stale playerCount; record that CONTEXT.md locks gameDefinition as the SOLE source. F21/CLIX-03 — trace validate.ts:226 constant = 200MB against its own comment (50MB) and against the external authoritative gate at ~/ShufflewickPubGames/src/upload.ts:4 (50MB); record explicitly that the CODE is wrong and the fix is to change the constant to 50MB (NOT rewrite the comment to 200MB). F22/CLIX-02 — trace that validateMetadata only checks a fixed required list and never rejects unknown top-level keys, so a misspelled gameOptions/playerOptions/colorPalette rides silently. Each LEGITIMATE verdict must cite current HEAD file:line. If a finding does not reproduce, record REJECTED with reasoning. Do NOT write or plan any fix in this task — verdicts only.</action>
  <verify>
    <automated>grep -v '^#' .planning/phases/135-cli-dev-experience/135-FINDINGS-VERIFICATION.md | grep -c "VERDICT:"</automated>
  </verify>
  <done>135-FINDINGS-VERIFICATION.md exists with a VERDICT: line and current file:line trace for each of F9, F21, F22; the F21 section states the constant (not the comment) is wrong and cites upload.ts:4.</done>
  <acceptance_criteria>
    - File contains a section for F9, F21, F22 each with a `VERDICT:` token
    - Each LEGITIMATE verdict cites a current HEAD file:line (not the audit's original numbers)
    - F21 section records the 50MB authoritative value from ~/ShufflewickPubGames/src/upload.ts:4 and that the code constant is the thing to change
    - No fix code is written in this task
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Re-verify dev-host/flag findings F32, F33, F34 and record verdicts</name>
  <files>.planning/phases/135-cli-dev-experience/135-FINDINGS-VERIFICATION.md</files>
  <read_first>
    - .planning/tmp/v4.5-audit-findings.json (original audit text for F32, F33, F34)
    - .planning/phases/135-cli-dev-experience/135-CONTEXT.md (CLIX-04 locked decision: default 127.0.0.1, --host 0.0.0.0 / --lan explicit, loud banner on non-localhost)
    - .planning/phases/135-cli-dev-experience/135-RESEARCH.md (Pattern 2 fail-fast numeric; Pitfall 3 --ai validated pre-clamp; NOTE the State-of-the-Art table's F32 row is WRONG about the default — reconcile against CONTEXT.md, which wins)
    - src/cli/commands/dev.ts (confirm host default '0.0.0.0' at line 283; unguarded parseInt at 280-281; invalidAiPlayers filter against raw pre-clamp playerCount at 302; effectivePlayerCount clamp at 393; minPlayers resolved ~359)
    - src/cli/cli.ts (confirm -t/--template registration at line 27; misleading --host help at line 35)
    - src/cli/commands/init.ts (confirm InitOptions.template at line 16 has no read site in initCommand — full-file grep)
  </read_first>
  <action>Append F32/CLIX-04, F33/CLIX-05, F34/CLIX-06 sections to `135-FINDINGS-VERIFICATION.md`, each with a `VERDICT:` line. F32 — trace dev.ts:283 defaults to 0.0.0.0 and cli.ts:35 help text is misleading; record that CONTEXT.md's LOCKED decision is default 127.0.0.1 with explicit --lan/--host 0.0.0.0 opt-in and a loud non-localhost banner, and explicitly note that 135-RESEARCH.md's State-of-the-Art table incorrectly claims CONTEXT keeps 0.0.0.0-by-default — the CONTEXT decision is authoritative and the fix changes the default. F33 — trace cli.ts:27 registers -t/--template with a fake default and init.ts:16 declares InitOptions.template, with zero read site in initCommand (confirm by full-file grep); verdict that the flag is a silent no-op to remove. F34 — trace dev.ts:280-281 parseInt with no NaN/range guard, dev.ts:302 --ai validated against raw pre-clamp playerCount before minPlayers/maxPlayers are known (~359) and before the effectivePlayerCount clamp (393); record the ordering bug (per Pitfall 3) that --ai validation must MOVE to after effectivePlayerCount is computed. Each LEGITIMATE verdict cites current HEAD file:line. Verdicts only — no fix code.</action>
  <verify>
    <automated>grep -v '^#' .planning/phases/135-cli-dev-experience/135-FINDINGS-VERIFICATION.md | grep -c "VERDICT:"</automated>
  </verify>
  <done>All six findings (F9, F21, F22, F32, F33, F34) now have a VERDICT: line; the F32 verdict records the default-127.0.0.1 CONTEXT decision and flags the RESEARCH State-of-the-Art discrepancy; the F34 verdict records the --ai ordering move.</done>
  <acceptance_criteria>
    - `grep -v '^#' ... | grep -c "VERDICT:"` returns 6 or more
    - F32 section records CONTEXT's default-127.0.0.1 decision AND the RESEARCH-table discrepancy correction
    - F34 section records that --ai validation must move after effectivePlayerCount (dev.ts:393)
    - No fix code is written in this task
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning doc → fix plans | An incorrect verdict here (false LEGITIMATE / false REJECTED, or a wrong "which side is right" call on F21/F32) misdirects every downstream fix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-01 | Spoofing (of evidence) | 135-FINDINGS-VERIFICATION.md | mitigate | Every verdict cites a re-traced current HEAD file:line; F21 additionally cites the external upload.ts:4 authoritative value |
| T-135-02 | Repudiation | verdict record | mitigate | Literal `VERDICT:` token per finding makes the gate machine-checkable before any fix runs |

No package installs in this plan → no supply-chain (T-135-SC) row applies (the entire phase adds zero dependencies).
</threat_model>

<verification>
- `grep -v '^#' 135-FINDINGS-VERIFICATION.md | grep -c "VERDICT:"` >= 6
- Each finding section cites current HEAD file:line evidence
- F21 records the code-is-wrong (change constant to 50MB) call; F32 records the default-127.0.0.1 CONTEXT decision
</verification>

<success_criteria>
All six findings have a recorded, evidence-backed verdict. No fix code written. Plans 02-06 are unblocked.
</success_criteria>

<output>
Create `.planning/phases/135-cli-dev-experience/135-FINDINGS-VERIFICATION.md`, then a `.planning/phases/135-cli-dev-experience/135-01-SUMMARY.md` when done.
</output>
