---
phase: 135-cli-dev-experience
plan: 03
type: execute
wave: 2
depends_on: [135-01]
files_modified:
  - src/cli/lib/project-scaffold.ts
  - src/cli/lib/project-scaffold.test.ts
  - docs/getting-started.md
autonomous: true
requirements: [CLIX-01, CLIX-02, PROC-02]
must_haves:
  truths:
    - "Scaffolded boardsmith.json contains no playerCount key — player count is authored only once, in the generated src/rules/index.ts gameDefinition"
    - "The generated src/rules/index.ts gameDefinition still hardcodes minPlayers/maxPlayers from the scaffold's internal ProjectConfig.playerCount (the single legitimate write site)"
    - "The scaffold's dead $schema URL is removed (no unresolvable https://boardsmith.io/schemas/game.json)"
    - "project-scaffold.test.ts has a regression asserting no playerCount key in the emitted JSON (PROC-02)"
    - "docs/getting-started.md's boardsmith.json example no longer contains the playerCount key (DOCX-04 same-phase doc update — the doc must not teach a config that validate now rejects)"
  artifacts:
    - path: "src/cli/lib/project-scaffold.ts"
      provides: "generateBoardsmithJson without playerCount/$schema; generateRulesIndexTs unchanged as sole player-count write"
    - path: "src/cli/lib/project-scaffold.test.ts"
      provides: "PROC-02 regression on emitted-JSON shape"
    - path: "docs/getting-started.md"
      provides: "boardsmith.json example kept in sync with the scaffold — no playerCount key (DOCX-04)"
  key_links:
    - from: "generateRulesIndexTs"
      to: "gameDefinition.minPlayers/maxPlayers"
      via: "the single scaffold-time write of player count into compiled rules"
      pattern: "minPlayers"
---

<objective>
Make `gameDefinition` (compiled rules) the SOLE source of player count in scaffolded projects: (CLIX-01) drop the `playerCount` key from the emitted `boardsmith.json` so the scaffold no longer writes it in two places — the generated `src/rules/index.ts` gameDefinition remains the one legitimate write site; (CLIX-02) remove the dead `$schema` URL.

Purpose: F9's root is dual authorship. `ProjectConfig.playerCount` stays as the scaffold's INTERNAL generation parameter (it seeds the gameDefinition's hardcoded min/max and App.vue's :player-count attr) — only the JSON OUTPUT key is removed. This is the "delete the duplicate" half of CLIX-01; the "derive at build" half is Plan 04 and the "reject leftover key" half is Plan 05.
Output: leaner scaffold output + a PROC-02 regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-cli-dev-experience/135-CONTEXT.md
@.planning/phases/135-cli-dev-experience/135-PATTERNS.md
@.planning/phases/135-cli-dev-experience/135-FINDINGS-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drop playerCount and dead $schema from generateBoardsmithJson (CLIX-01/CLIX-02)</name>
  <files>src/cli/lib/project-scaffold.ts, src/cli/lib/project-scaffold.test.ts, docs/getting-started.md</files>
  <read_first>
    - src/cli/lib/project-scaffold.ts:103-118 (generateBoardsmithJson — the `playerCount: config.playerCount,` line and the `$schema:` line to remove)
    - src/cli/lib/project-scaffold.ts:21 (ProjectConfig.playerCount interface field — KEEP; it is the internal generation parameter, not the JSON output)
    - src/cli/lib/project-scaffold.ts:240-246 (generateRulesIndexTs hardcoding minPlayers/maxPlayers from config.playerCount — KEEP; this is now the sole write site)
    - src/cli/lib/project-scaffold.ts:105 (dead $schema URL https://boardsmith.io/schemas/game.json)
    - src/cli/lib/project-scaffold.test.ts (existing generateBoardsmithJson describe block — the analog to extend)
    - .planning/phases/135-cli-dev-experience/135-RESEARCH.md (Open Question 2 — $schema decision: drop it; the hand-rolled validate check in Plan 05 is the enforcement, and Plan 05 ships a real schema file for tooling. Do NOT invent a fragile cross-package $schema path here.)
    - docs/getting-started.md:~115 ("Understanding the Generated Code" → boardsmith.json example) — the stale `"playerCount": { "min": 2, "max": 4 }` line that must be dropped so the doc stops teaching a config validate now rejects (DOCX-04). The `"description": "A fun game for 2-4 players"` prose is fine to keep (it is a free-text field, not the removed key).
  </read_first>
  <behavior>
    - Test: JSON.parse(generateBoardsmithJson(config)) has no `playerCount` property.
    - Test: the parsed object has no `$schema` property (dead URL removed).
    - Test: generateRulesIndexTs(config) output still contains `minPlayers: <n>` and `maxPlayers: <m>` from config.playerCount (unchanged single write site).
  </behavior>
  <action>In generateBoardsmithJson, delete the `playerCount: config.playerCount,` line and the `$schema:` line from the emitted object literal (implementing D-CLIX-01 dual-authorship removal and the D-CLIX-02 $schema-drop discretion). Leave ProjectConfig.playerCount and generateRulesIndexTs's minPlayers/maxPlayers hardcode intact — those remain the internal parameter and the single legitimate player-count write. Extend project-scaffold.test.ts with regression assertions (write RED first against pre-fix output per PROC-02, record RED in SUMMARY): no playerCount key, no $schema key, and generateRulesIndexTs still emits min/max. Do NOT drop or rename the estimatedDuration/complexity/categories/scoreboard/ui keys — only playerCount and $schema change. Then, per DOCX-04 (same-phase doc update), remove the `"playerCount": { "min": 2, "max": 4 }` line from the boardsmith.json example in docs/getting-started.md so the doc stops teaching a key that validate (Plan 05) now rejects; keep surrounding keys and the "2-4 players" description prose intact.</action>
  <verify>
    <automated>npx vitest run src/cli/lib/project-scaffold.test.ts --reporter=dot</automated>
  </verify>
  <done>Emitted boardsmith.json has no playerCount and no $schema; generated rules still author min/max once; regression green and RED-proven; docs/getting-started.md example no longer shows playerCount.</done>
  <acceptance_criteria>
    - Parsed generateBoardsmithJson output lacks both `playerCount` and `$schema`
    - generateRulesIndexTs still emits minPlayers/maxPlayers
    - project-scaffold.test.ts regression is green and was RED on pre-fix code
    - `grep -c playerCount docs/getting-started.md` returns 0 (no playerCount key remains in the boardsmith.json example)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scaffold generator → generated project config | A duplicated player-count authorship lets a hand-edit of one copy diverge from the other, silently misconfiguring lobby size |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-06 | Tampering (config drift) | generateBoardsmithJson dual playerCount write | mitigate | Remove the JSON copy so player count is authored exactly once (in compiled rules) — divergence becomes impossible by construction |

No package installs in this plan → no supply-chain row applies.
</threat_model>

<verification>
- `npx vitest run src/cli/lib/project-scaffold.test.ts --reporter=dot` green
- Emitted JSON has no playerCount/$schema; rules still author min/max
</verification>

<success_criteria>
The scaffold authors player count once (in gameDefinition) and emits no dead $schema URL. The CLIX-01 "delete the duplicate" half is complete; Plans 04/05 cover derive-at-build and reject-leftover-key.
</success_criteria>

<output>
Create `.planning/phases/135-cli-dev-experience/135-03-SUMMARY.md` when done (record RED evidence for PROC-02).
</output>
