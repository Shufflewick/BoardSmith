---
phase: 136-client-sdk-protocol
plan: 02
type: execute
wave: 2
depends_on: [136-01]
files_modified:
  - src/types/protocol.ts
  - src/client/types.ts
  - src/client/index.ts
autonomous: true
requirements: [SDK-04, SDK-05, SDK-02, SDK-06]
must_haves:
  truths:
    - "`WebSocketMessage` in protocol.ts includes `UpdateSlotPlayerOptionsMessage` alongside its two already-unioned siblings"
    - "`src/client/types.ts` re-exports the request/response types from `../types/protocol.js` instead of redefining them; the client `CreateGameRequest` again carries `playerIds`"
    - "The outgoing WS message type is the discriminated `action|ping|getState` narrowing of the canonical union, and the incoming type is a discriminated union keyed on `type` — no all-optional bags remain"
    - "`GameConnectionConfig` has `connectImmediately?: boolean` and `connectionTimeout?: number`; `MeepleClientConfig` has `playerId?: string`"
    - "`npx tsc --noEmit` passes — the barrel surface (src/client/index.ts) still resolves every re-exported request type"
  artifacts:
    - path: "src/types/protocol.ts"
      provides: "Canonical WebSocketMessage union with UpdateSlotPlayerOptionsMessage member"
      contains: "UpdateSlotPlayerOptionsMessage"
    - path: "src/client/types.ts"
      provides: "Re-export layer over protocol.ts; new config fields (connectImmediately, connectionTimeout, playerId)"
      contains: "from '../types/protocol.js'"
    - path: "src/client/index.ts"
      provides: "Public barrel surface that still resolves the re-exported request types"
  key_links:
    - from: "src/client/index.ts"
      to: "src/client/types.ts"
      via: "re-export of request types"
      pattern: "from './types"
    - from: "src/client/types.ts"
      to: "src/types/protocol.ts"
      via: "canonical type re-export"
      pattern: "from '../types/protocol.js'"
---

<objective>
Establish the canonical type foundation the rest of Phase 136 builds on: (SDK-05) add `UpdateSlotPlayerOptionsMessage` to the `WebSocketMessage` union in protocol.ts; (SDK-04) delete the duplicated request/response types from `src/client/types.ts` and re-export the canonical ones from `../types/protocol.js`, replace the all-optional `WebSocketOutgoingMessage`/`WebSocketIncomingMessage` bags with discriminated unions, and resolve the `CreateGameRequest` drift (client copy lost `playerIds`). This plan also lands the additive config fields the behavior plans (03, 04) consume: `connectImmediately`/`connectionTimeout` on `GameConnectionConfig` and `playerId` on `MeepleClientConfig`.

Purpose: interface-first ordering. protocol.ts declares itself the single source of truth; every downstream plan imports from here, so the canonical types and config surface must land before their consumers. This is the SDK-04/SDK-05 fix plus the type-level scaffolding for SDK-02/SDK-06 (their behavior lands in Plans 03/04).
Output: canonical union completeness, a re-export-only client/types.ts, discriminated WS unions, and the new config fields — all type-checking clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-client-sdk-protocol/136-CONTEXT.md
@.planning/phases/136-client-sdk-protocol/136-PATTERNS.md
@.planning/phases/136-client-sdk-protocol/136-FINDINGS-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UpdateSlotPlayerOptionsMessage to the WebSocketMessage union (SDK-05)</name>
  <files>src/types/protocol.ts</files>
  <read_first>
    - src/types/protocol.ts:393-414 (the three sibling interfaces UpdatePlayerOptionsMessage / UpdateSlotPlayerOptionsMessage / UpdateGameOptionsMessage — UpdateSlotPlayerOptionsMessage is defined at :401 but orphaned)
    - src/types/protocol.ts:420-435 (the WebSocketMessage union — its two siblings are members, UpdateSlotPlayerOptionsMessage is not)
    - .planning/phases/136-client-sdk-protocol/136-PATTERNS.md (Sibling-union-member addition section — the copy-the-sibling-pattern analog)
  </read_first>
  <action>Add `| UpdateSlotPlayerOptionsMessage` to the `WebSocketMessage` union in protocol.ts, placed adjacent to its two siblings `UpdatePlayerOptionsMessage`/`UpdateGameOptionsMessage`. This is purely additive per the F35 verdict (zero runtime consumers dispatch on this variant over the wire today — the operation goes over HTTP). Do not change the interface definition at :401 — only add it to the union.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "protocol.ts" | grep -q "^0$" && grep -c "UpdateSlotPlayerOptionsMessage" src/types/protocol.ts</automated>
  </verify>
  <done>`UpdateSlotPlayerOptionsMessage` appears both as an interface and as a member of the WebSocketMessage union; tsc reports no protocol.ts errors.</done>
  <acceptance_criteria>
    - `UpdateSlotPlayerOptionsMessage` is a member of the `WebSocketMessage` union
    - The interface definition at protocol.ts:401 is unchanged
    - `npx tsc --noEmit` surfaces no new protocol.ts error
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace duplicated client types with canonical re-exports + discriminated WS unions (SDK-04)</name>
  <files>src/client/types.ts, src/client/index.ts</files>
  <read_first>
    - src/client/types.ts:322-327 (the existing Lobby re-export precedent — `export type { LobbyState, SlotStatus, LobbySlot, LobbyInfo };` with its import at :5 — the exact pattern to replicate)
    - src/client/types.ts:288-310 (drifted CreateGameRequest — missing playerIds, looser inline playerConfigs) and :330-394 (the duplicated ClaimSeatRequest/JoinLobbyResponse/ClaimSeatResponse/LobbyResponse and *Request interfaces to delete)
    - src/client/types.ts:237-276 (the all-optional WebSocketOutgoingMessage/WebSocketIncomingMessage bags to replace)
    - src/types/protocol.ts:212-230 (canonical CreateGameRequest with playerIds and named PlayerConfig); :246-287 (canonical ClaimSeatRequest/ClaimSeatResponse/JoinLobbyResponse); :420-435 (canonical WebSocketMessage union post-Task-1)
    - src/client/index.ts:78-92 (barrel re-exports CreateGameRequest/CreateGameResponse/ApiResponse FROM './types.js' — Pitfall 6: deletions must keep these resolving)
    - src/client/game-connection.ts (grep onmessage / this.ws.onmessage handler + the outgoing sends at ~:140-145/:169/:383 — confirm outgoing is exactly action|ping|getState; enumerate the incoming `type` values the handler switches on to shape the incoming discriminated union)
    - .planning/phases/136-client-sdk-protocol/136-PATTERNS.md (Canonical-source re-export section; Pitfall 7 narrowing target)
  </read_first>
  <action>For every request/response interface duplicated in client/types.ts that has a canonical twin in protocol.ts (CreateGameRequest, ClaimSeatRequest, ClaimSeatResponse, JoinLobbyResponse, LobbyResponse, SetReadyRequest, AddSlotRequest, RemoveSlotRequest, SetSlotAIRequest, UpdateGameOptionsRequest, UpdatePlayerOptionsRequest, and the rest at :330-394): delete the local definition and add a `export type { X } from '../types/protocol.js';` line mirroring the Lobby re-export at :322-327. Where protocol.ts lacks an exact twin for a shape client/types.ts owns, add the canonical interface to protocol.ts first, then re-export — do NOT leave a second definition anywhere (per No Backward Compatibility). Replace `WebSocketOutgoingMessage` with the discriminated narrowing `Extract<WebSocketMessage, { type: 'action' | 'ping' | 'getState' }>` (exactly the three variants game-connection.ts constructs — NOT the full lobby set, per Pitfall 7); replace `WebSocketIncomingMessage` with a discriminated union keyed on `type` built from the canonical incoming message variants the game-connection.ts onmessage handler dispatches on. Confirm the CreateGameRequest drift is resolved (playerIds now reachable via the canonical re-export). Fix src/client/index.ts so its barrel re-export list still resolves against the new re-export surface (Pitfall 6) — add or adjust the `from './types.js'` lines as needed so every previously-public request type stays exported. This is a type-level-only change; no runtime behavior changes.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "TS2305|has no exported member" | grep -c . | grep -q "^0$" && echo OK</automated>
  </verify>
  <done>client/types.ts contains no duplicated request/response interface bodies — only re-exports from '../types/protocol.js'; `playerIds` is reachable on the client CreateGameRequest; WS outgoing/incoming are discriminated unions; index.ts barrel resolves; tsc has zero TS2305 "no exported member" errors.</done>
  <acceptance_criteria>
    - No request/response interface is defined in BOTH client/types.ts and protocol.ts
    - The client `CreateGameRequest` carries `playerIds` (via canonical re-export)
    - `WebSocketOutgoingMessage` is the `action|ping|getState` narrowing; incoming is a `type`-keyed discriminated union
    - `npx tsc --noEmit` reports zero TS2305 / "no exported member" errors (barrel surface intact)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add config fields consumed by Plans 03/04 (SDK-02, SDK-06 scaffolding)</name>
  <files>src/client/types.ts</files>
  <read_first>
    - src/client/types.ts:12-27 (MeepleClientConfig — no playerId field today)
    - src/client/types.ts:167-196 (GameConnectionConfig — add fields alongside the existing optional wsImplementation)
    - src/client/game-connection.ts:69 (existing autoReconnect default; the file currently uses bare 10s literals for the action-response and pong timeouts — connectionTimeout will replace/back those in Plan 03)
    - .planning/phases/136-client-sdk-protocol/136-RESEARCH.md (Open Question 2 resolution — introduce a connectionTimeout config field, default 10s; Pitfall 2 — connectImmediately must be threaded through every GameConnectionConfig construction site by Plans 03/04)
  </read_first>
  <action>Add `playerId?: string` to `MeepleClientConfig` (types.ts:12-27) with a JSDoc noting it is the explicit seat-identity capability token; when omitted the client mints one. Add `connectImmediately?: boolean` (JSDoc: defaults to true; false constructs a GameConnection without dialing — consumed by Plan 03's GameConnection and Plan 05's useGame) and `connectionTimeout?: number` (JSDoc: milliseconds bound for action()-awaits-open and the connection handshake, default 10000 — replaces the bare 10s literals in game-connection.ts, wired in Plan 03) to `GameConnectionConfig` (types.ts:167-196). Type-level additions only — no behavior in this plan; the fields are read by Plans 03 and 04.</action>
  <verify>
    <automated>grep -q "playerId?: string" src/client/types.ts && grep -q "connectImmediately?: boolean" src/client/types.ts && grep -q "connectionTimeout?: number" src/client/types.ts && npx tsc --noEmit 2>&1 | grep -c "types.ts" | grep -q "^0$"</automated>
  </verify>
  <done>MeepleClientConfig has playerId?, GameConnectionConfig has connectImmediately? and connectionTimeout?; tsc clean for types.ts.</done>
  <acceptance_criteria>
    - `MeepleClientConfig.playerId?: string` exists with JSDoc
    - `GameConnectionConfig.connectImmediately?: boolean` and `connectionTimeout?: number` exist with JSDoc
    - No runtime/behavior code changed in this task
    - `npx tsc --noEmit` clean for types.ts
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client SDK types → canonical protocol types | Duplicated/drifted type definitions let a security-relevant field (`playerIds`, the seat-identity token) silently fail to compile-error when omitted, weakening the type guardrail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-03 | Tampering | client CreateGameRequest (drifted, lost playerIds) | mitigate | Delete the duplicate and re-export the canonical type so TypeScript's missing/excess-property checks re-guard the identity field |
| T-136-04 | Information disclosure (type-safety erosion) | all-optional WS message bags | mitigate | Replace with discriminated unions so malformed/incomplete messages fail at compile time instead of silently type-checking |

No package installs in this plan → no supply-chain row applies (zero dependency changes this phase).
</threat_model>

<verification>
- `npx tsc --noEmit` clean (no TS2305 barrel breakage, no protocol.ts/types.ts errors)
- `UpdateSlotPlayerOptionsMessage` is a WebSocketMessage union member
- client/types.ts holds only re-exports for request/response types; playerIds reachable
- New config fields present on GameConnectionConfig and MeepleClientConfig
</verification>

<success_criteria>
Canonical types are the single source of truth for the client SDK, the WS union is complete, and the config surface for SDK-02/SDK-06 exists — Plans 03/04/05 can consume it. Full type-check green.
</success_criteria>

<output>
Create `.planning/phases/136-client-sdk-protocol/136-02-SUMMARY.md` when done.
</output>
