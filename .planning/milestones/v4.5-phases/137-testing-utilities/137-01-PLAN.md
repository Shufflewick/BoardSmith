---
phase: 137-testing-utilities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/137-testing-utilities/137-FINDINGS-VERIFICATION.md
autonomous: true
requirements: [PROC-01]
must_haves:
  truths:
    - "F36 (doAction never throws / class example ignores result) has a recorded verdict written before any fix task runs"
    - "F37 (default seed is test-${Date.now()}, nondeterministic) has a recorded verdict written before any fix task runs"
    - "Each LEGITIMATE verdict cites a current file:line trace proving the defect exists in today's post-Phase-136 source"
    - "The doAction call-site classification (a/b/c) from 137-PATTERNS.md is confirmed against current source so the fix plans migrate exactly the right sites"
  artifacts:
    - path: ".planning/phases/137-testing-utilities/137-FINDINGS-VERIFICATION.md"
      provides: "Per-finding PROC-01 verdicts gating all fix work in Plans 02-03"
      contains: "VERDICT:"
  key_links: []
---

<objective>
Produce the PROC-01 verification gate for Phase 137: an independent re-verification of the two in-scope audit findings — F36 (`TestGame.doAction` never throws; the flagship class-level example models ignoring the result) and F37 (default seed is `test-${Date.now()}`, nondeterministic by default) — recorded in `137-FINDINGS-VERIFICATION.md`, each with a LEGITIMATE (file:line trace / repro) or REJECTED (reasoning) verdict, written BEFORE any fix is planned or written.

Purpose: PROC-01 is a hard, non-negotiable phase gate — no fix task in Plans 02-03 may run until this document records a verdict for both findings. This operationalizes the project's "Prove Before Fix" rule. The gate also confirms the doAction call-site classification table from 137-PATTERNS.md against current source, so the fix plans migrate exactly the right sites.
Output: `.planning/phases/137-testing-utilities/137-FINDINGS-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-testing-utilities/137-CONTEXT.md
@.planning/phases/137-testing-utilities/137-PATTERNS.md
@.planning/phases/137-testing-utilities/137-VALIDATION.md
@.planning/tmp/v4.5-audit-findings.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify F36 (doAction) and F37 (seed), record verdicts, confirm call-site classification</name>
  <files>.planning/phases/137-testing-utilities/137-FINDINGS-VERIFICATION.md</files>
  <read_first>
    - .planning/tmp/v4.5-audit-findings.json (the two testing findings: `src/testing/test-game.ts:272` doAction-never-throws, and `src/testing/test-game.ts:127` seed-is-Date.now — original audit trap text)
    - .planning/phases/137-testing-utilities/137-PATTERNS.md (the doAction call-site sweep table at the bottom, and the seed/error-class analogs)
    - src/testing/test-game.ts (confirm `doAction` returns `ActionExecutionResult` and never throws at ~line 272-278; the class-level `@example` ignoring the result at ~line 94-104; `const seed = options.seed ?? \`test-${Date.now()}\`` at ~line 127)
    - src/testing/simulate-tutorial.ts, src/testing/random-simulation.ts, src/testing/simulate-action.ts, src/testing/action-builder.ts (confirm each `.doAction(` call site and its classification a/b/c)
  </read_first>
  <action>Create `.planning/phases/137-testing-utilities/137-FINDINGS-VERIFICATION.md`. For each of the two findings write a section headed with the finding label and a line containing the literal token `VERDICT:` followed by `LEGITIMATE` or `REJECTED`.

F36 (TST-01) — verify `doAction` returns `ActionExecutionResult` and never throws (cite current `src/testing/test-game.ts` line of the `return this.runner.performAction(...)` body), and that the class-level `@example` models ignoring the result (cite the current `@example` line range). Record LEGITIMATE with those file:line traces, or REJECTED with reasoning if today's source already throws.

F37 (TST-02) — verify the default seed is `test-${Date.now()}` (cite the current line), making no-seed runs nondeterministic. Record LEGITIMATE with the file:line trace, or REJECTED if the default is already a fixed literal.

Then add a "Call-site classification (confirmed)" section: for each `.doAction(` site in `src/testing/` re-confirm its category against current source and its planned disposition — simulate-tutorial.ts (b→tryAction), random-simulation.ts (b→tryAction), simulate-action.ts simulateAction (b→tryAction), simulate-action.ts playUntilComplete internal (b→tryAction), action-builder.ts ActionBuilder.execute (c→simplify), assertions.test.ts (a→no change). Record the CURRENT file:line for each so Plans 02-03 edit the right lines despite any drift. Do NOT write or plan any fix in this task — verdicts and confirmed line traces only.</action>
  <verify>
    <automated>grep -cE 'VERDICT:' .planning/phases/137-testing-utilities/137-FINDINGS-VERIFICATION.md | grep -qx 2 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `137-FINDINGS-VERIFICATION.md` exists with exactly two `VERDICT:` lines (one per finding).
    - Each LEGITIMATE verdict cites a current `src/testing/test-game.ts` file:line trace matching today's source.
    - A "Call-site classification (confirmed)" section lists all six `.doAction(` sites with current file:line and disposition (a/b/c).
    - No fix code is written in this plan.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-infrastructure documentation only; no runtime, no untrusted input, no security boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-01 | Tampering | 137-FINDINGS-VERIFICATION.md verdicts | accept | Verdicts are developer-authored gate artifacts reviewed by the human at phase close; no external actor, no security impact. No package installs in this phase → no supply-chain (T-137-SC) row required. |
</threat_model>

<verification>
`grep -cE 'VERDICT:'` on the verification doc returns 2. Human review confirms verdict quality (PROC-01 manual sign-off per 137-VALIDATION.md).
</verification>

<success_criteria>
Both findings (F36/TST-01, F37/TST-02) have recorded verdicts with current file:line evidence, and the doAction call-site dispositions are confirmed against live source — before any fix runs.
</success_criteria>

<output>
Create `.planning/phases/137-testing-utilities/137-01-SUMMARY.md` when done.
</output>
