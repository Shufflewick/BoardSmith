---
phase: 138-cross-repo-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/BoardSmithGames/checkers/boardsmith.json
  - ~/BoardSmithGames/cribbage/boardsmith.json
  - ~/BoardSmithGames/demo-action-panel/boardsmith.json
  - ~/BoardSmithGames/demo-animation/boardsmith.json
  - ~/BoardSmithGames/demo-complex-ui/boardsmith.json
  - ~/BoardSmithGames/go-fish/boardsmith.json
  - ~/BoardSmithGames/hex/boardsmith.json
  - ~/BoardSmithGames/polyhedral-potions/boardsmith.json
  - ~/BoardSmithGames/checkers/tests/tutorial-preset.test.ts
  - ~/BoardSmithGames/go-fish/tests/game.test.ts
  - ~/BoardSmithGames/go-fish/tests/complete-game.test.ts
  - ~/BoardSmithGames/go-fish/tests/no-hidden-info-leak.test.ts
  - ~/BoardSmithGames/go-fish/tests/no-hidden-info-dom-leak.test.ts
autonomous: true
requirements: [GAMES-01]
must_haves:
  truths:
    - "Every one of the 8 games' boardsmith.json has no playerCount and no dead $schema key"
    - "boardsmith validate passes in all 8 game repos"
    - "checkers suite is green (38 tests)"
    - "go-fish suite is green (84 tests)"
    - "All 8 game suites that have tests run green via npx vitest run"
  artifacts:
    - path: "~/BoardSmithGames/hex/boardsmith.json"
      provides: "manifest without playerCount/$schema (representative of all 8)"
    - path: "~/BoardSmithGames/go-fish/tests/no-hidden-info-dom-leak.test.ts"
      provides: "loop-control migrated to tryAction()"
  key_links:
    - from: "game test files"
      to: "TestGame.doAction/tryAction contract"
      via: "doAction throws on failure (success proof = next line runs); tryAction for loop-termination"
      pattern: "tryAction|doAction"
---

<objective>
Migrate all 8 example games in `~/BoardSmithGames/` onto the v4.5 API surface. Per the fresh grep sweep in 138-RESEARCH.md, the real migration surface is exactly two categories: (1) strip the removed `playerCount` key and dead `$schema` URL from all 8 `boardsmith.json` files, and (2) migrate the `TestGame.doAction().success`-assertion sites (checkers: 1, go-fish: 12) to the new throw-based contract. All other seed-list breaking-change categories returned zero hits (verified in RESEARCH §3-9). No opportunistic refactors.

Purpose: Satisfy GAMES-01 — all 8 game suites green against the fixed API surface.
Output: 8 cleaned manifests, migrated checkers + go-fish test files, all suites green, per-repo commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-cross-repo-migration/138-RESEARCH.md
@.planning/phases/138-cross-repo-migration/138-CONTEXT.md
@.planning/phases/138-cross-repo-migration/138-VALIDATION.md

<interfaces>
<!-- From /Users/jtsmith/BoardSmith/src/testing/test-game.ts:330-398 (v4.5 contract) -->
doAction(playerId, actionName, args): void   // THROWS ActionExecutionError on failure
tryAction(playerId, actionName, args): ActionExecutionResult  // returns { success, ... }, never throws

<!-- Migration rules (from RESEARCH Code Examples) -->
<!-- "should always succeed" site: delete `const result =` + `expect(result.success)`; just call doAction(...) -->
<!-- "loop-termination / failure-detection" site: switch to tryAction() and keep the `if (!result.success) break` -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip playerCount + dead $schema from all 8 game manifests</name>
  <files>~/BoardSmithGames/{checkers,cribbage,demo-action-panel,demo-animation,demo-complex-ui,go-fish,hex,polyhedral-potions}/boardsmith.json</files>
  <read_first>
    - 138-RESEARCH.md "Per-Repo Migration Tables §1" (line/disposition table — every repo: $schema at line 2, playerCount at line 6, remove both)
    - RESEARCH Pitfall 2 (zsh does NOT word-split unquoted vars — use bash arrays `REPOS=(...)` / `for r in "${REPOS[@]}"`, or run the sweep under `bash -c`)
  </read_first>
  <action>For each of the 8 game repos, edit boardsmith.json to delete the `$schema` key (line 2) and the `playerCount` key (line 6). player count is now owned solely by `gameDefinition` per CLIX-01. Leave all other keys untouched — no reformatting, no reordering (scope discipline: migration only). Use the Edit tool per file (do not hand-roll a sed sweep that risks JSON corruption). After editing, run `npx boardsmith validate` in each repo; it must PASS (the new CLI from Phase 135 rejects both removed keys).</action>
  <verify>
    <automated>for r in checkers cribbage demo-action-panel demo-animation demo-complex-ui go-fish hex polyhedral-potions; do echo "== $r =="; grep -c -e '"\$schema"' -e '"playerCount"' ~/BoardSmithGames/$r/boardsmith.json; (cd ~/BoardSmithGames/$r && npx boardsmith validate 2>&1 | tail -2); done</automated>
  </verify>
  <acceptance_criteria>
    - grep count for `$schema`/`playerCount` is 0 in all 8 manifests
    - `npx boardsmith validate` reports PASS in all 8 repos
  </acceptance_criteria>
  <done>All 8 boardsmith.json files carry neither playerCount nor $schema; validate passes in each repo.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate checkers + go-fish doAction().success sites to the throw-based contract</name>
  <files>~/BoardSmithGames/checkers/tests/tutorial-preset.test.ts, ~/BoardSmithGames/go-fish/tests/game.test.ts, ~/BoardSmithGames/go-fish/tests/complete-game.test.ts, ~/BoardSmithGames/go-fish/tests/no-hidden-info-leak.test.ts, ~/BoardSmithGames/go-fish/tests/no-hidden-info-dom-leak.test.ts</files>
  <read_first>
    - 138-RESEARCH.md "Per-Repo Migration Tables §2" (exact file:line classification of all 13 call sites)
    - 138-RESEARCH.md Pitfall 3 (loop-control helpers must move to tryAction(), NOT plain doAction() — deleting the .success check would throw and abort the test)
    - 138-RESEARCH.md "Code Examples" (both before/after migration snippets)
    - The actual call sites in each file before editing
  </read_first>
  <action>Classify and migrate each `doAction().success` site per RESEARCH §2:
  (a) "should always succeed" sites — checkers `tutorial-preset.test.ts:73-77`, go-fish `game.test.ts:338,343`, `complete-game.test.ts:174,179,234,239`, `no-hidden-info-leak.test.ts:37` (+ its downstream `.success` read): delete the `const result =` capture and the `expect(result.success).toBe(true)` line; a bare `testGame.doAction(...)` is now the success proof (it throws on failure).
  (b) "failure-detection loop-control" site — go-fish `no-hidden-info-dom-leak.test.ts:34,38` inside the `driveAFewTurns` helper (used by 4 tests): switch `testGame.doAction(...)` to `testGame.tryAction(...)` and KEEP `if (!result.success) break;` — the loop must stop gracefully when no legal move remains, not throw.
  Leave go-fish `game.test.ts:420` untouched (already void-style, RESEARCH §2 marks it already-compliant). Do not touch any other lines.</action>
  <verify>
    <automated>cd ~/BoardSmithGames/checkers && npx vitest run 2>&1 | tail -4; echo "=== go-fish ==="; cd ~/BoardSmithGames/go-fish && npx vitest run 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - checkers: all 38 tests pass (was 37 passed / 1 failed)
    - go-fish: all 84 tests pass (was 72 passed / 12 failed)
    - No `expect(result.success)` remains after a `doAction()` call in checkers/go-fish test files (grep-confirm)
    - `driveAFewTurns` uses `tryAction`, not `doAction`
  </acceptance_criteria>
  <done>checkers and go-fish suites are fully green; loop-control helper preserved via tryAction().</done>
</task>

<task type="auto">
  <name>Task 3: Confirm all 8 suites green + commit each game repo</name>
  <files>~/BoardSmithGames/{checkers,cribbage,demo-action-panel,demo-animation,demo-complex-ui,go-fish,hex,polyhedral-potions}</files>
  <read_first>
    - 138-RESEARCH.md "Before-State: Existing Suite Results" (baseline counts per repo; demo-action-panel has NO suite — record as known gap, do NOT author one)
    - RESEARCH Pitfall 1 (`npx vitest run` only — bare `npm test` hangs in watch mode)
    - CONTEXT "Scope discipline" (each game commits in its OWN repo; never commit game changes into BoardSmith)
  </read_first>
  <action>Run `npx vitest run` in each of the 7 games that have suites (checkers, cribbage, demo-animation, demo-complex-ui, go-fish, hex, polyhedral-potions) and confirm green. demo-action-panel has no test suite — record it in the SUMMARY as a pre-existing known gap (GAMES-01's "every suite green" is vacuously satisfied); do NOT fabricate a suite (no opportunistic work per CONTEXT).

Then run `npx boardsmith build` in ALL 8 games and confirm each build succeeds — ROADMAP success criterion #1 is "build AND pass test suites"; the manifest edits (playerCount/$schema strip) are exactly the kind of change that can break `boardsmith build` while validate and vitest stay green (build derives manifest playerCount from the compiled gameDefinition since Phase 135). Clean up any build output that shouldn't be committed (check each game's .gitignore; do not commit dist/ or .boardsmith/ artifacts).

Then commit each repo separately with a message like `chore: migrate to boardsmith v4.5 (strip playerCount/$schema; doAction throw contract)`. cd carefully into each game repo before committing — game changes MUST be committed in the game's own repo, never in BoardSmith. Record a per-repo migration ledger (what changed, suite result, build result) in the SUMMARY.</action>
  <verify>
    <automated>for r in checkers cribbage demo-animation demo-complex-ui go-fish hex polyhedral-potions; do echo "== $r =="; (cd ~/BoardSmithGames/$r && npx vitest run 2>&1 | tail -2); done; echo "== demo-action-panel: no suite (known gap) =="; for r in checkers cribbage demo-action-panel demo-animation demo-complex-ui go-fish hex polyhedral-potions; do echo "== build $r =="; (cd ~/BoardSmithGames/$r && npx boardsmith build 2>&1 | tail -2); done</automated>
  </verify>
  <acceptance_criteria>
    - All 7 games-with-suites report 0 failed via `npx vitest run`
    - All 8 games complete `npx boardsmith build` successfully (exit 0, manifest written)
    - demo-action-panel recorded as no-suite known gap in SUMMARY (not silently omitted)
    - Each game repo has its own commit; `git -C ~/BoardSmith status` shows no game-repo changes leaked into BoardSmith
  </acceptance_criteria>
  <done>All 8 games covered (7 green suites + 1 documented no-suite gap + 8/8 builds green); per-repo commits landed; migration ledger in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game repo ↔ BoardSmith repo | Migration edits must land in the game repo; leaking game changes into a BoardSmith commit corrupts library history |
| manifest ↔ CLI validator | boardsmith.json is trusted build-time config; stale removed keys silently mislead player-count resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-01 | Tampering | boardsmith.json edits | mitigate | Per-file Edit tool (not blind sed); `npx boardsmith validate` gates every manifest |
| T-138-02 | Repudiation | cross-repo commits | mitigate | Each game commits in its own repo; Task 3 verifies no game diff leaked into BoardSmith |
| T-138-03 | Information disclosure | go-fish hidden-info suites | mitigate | no-hidden-info-* suites must pass post-migration (SEC-01 empirical closer) |
</threat_model>

<verification>
- All 8 game manifests: 0 grep hits for playerCount/$schema; validate PASS
- checkers 38/38, go-fish 84/84, other 5 suites 0-failed
- demo-action-panel no-suite gap documented
- No game changes in BoardSmith working tree
</verification>

<success_criteria>
GAMES-01 (games portion) satisfied: all 8 example games comply with the changed API surface, every suite green (or documented vacuous for demo-action-panel), each committed in its own repo.
</success_criteria>

<output>
Create `.planning/phases/138-cross-repo-migration/138-01-SUMMARY.md` when done, including the per-repo migration ledger.
</output>
