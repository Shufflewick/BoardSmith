---
phase: 138-cross-repo-migration
plan: 02
type: execute
wave: 2
depends_on: [138-01]
files_modified:
  - scratchpad/verify-138-hex.mjs
  - scratchpad/verify-138-gofish.mjs
  - scratchpad/verify-138-cribbage.mjs
autonomous: true
requirements: [GAMES-01]
must_haves:
  truths:
    - "hex smoke: board loads, a seat is taken, a drag-move round-trips (UIX-04 drag gating live)"
    - "go-fish smoke: a failed ask surfaces exactly one toast; opponent hand stays hidden across a restore (SEC-01)"
    - "cribbage smoke: a multiSelect discard submits and advances the flow (UIX-02 / flow changes)"
    - "port 5173 is free after every smoke (no dev server left running)"
  artifacts:
    - path: "scratchpad/verify-138-hex.mjs"
      provides: "headless Playwright hex drag smoke"
    - path: "scratchpad/verify-138-gofish.mjs"
      provides: "headless Playwright go-fish toast + hidden-info smoke"
    - path: "scratchpad/verify-138-cribbage.mjs"
      provides: "headless Playwright cribbage multiSelect smoke"
  key_links:
    - from: "Playwright script"
      to: "GameShell platform-mode iframe"
      via: "frame with window.__BOARDSMITH_DEVTOOLS + take-seat + iframe controller"
      pattern: "__BOARDSMITH_DEVTOOLS"
---

<objective>
Author and run three headless Playwright browser smokes — hex (grid/drag, exercises UIX-04 drag gating live), go-fish (cards/hidden-info, exercises SEC-01 restore + failure toast), cribbage (complex multi-phase, exercises multiSelect + flow changes) — per the locked CONTEXT decision. These are the empirical closers for the breaking-change categories that are structurally hard to grep-prove safe (SEC-01/03 visibility, UIX-02 multiSelect). Reuse the proven Phase 134 verification patterns.

Purpose: Satisfy the Playwright-smoke half of GAMES-01's verification depth decision.
Output: Three passing headless smoke scripts; port 5173 confirmed free after each run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-cross-repo-migration/138-RESEARCH.md
@.planning/phases/138-cross-repo-migration/138-VALIDATION.md

<interfaces>
<!-- Reference scripts (proven patterns) in this session's scratchpad: -->
<!--   verify-134.mjs / verify-134b..d.mjs -->
<!-- Key patterns to reuse verbatim: -->
<!--   import { chromium } from '/Users/jtsmith/BoardSmith/node_modules/playwright/index.mjs'; -->
<!--   executablePath: '/Users/jtsmith/Library/Caches/ms-playwright/chromium_headless_shell-1208/chrome-headless-shell-mac-arm64/chrome-headless-shell' -->
<!--   goto('http://localhost:5173', { waitUntil: 'domcontentloaded' })  // NEVER networkidle — WS host never idles -->
<!--   gameFrame(): iterate page.frames(), pick the one where window.__BOARDSMITH_DEVTOOLS is truthy -->
<!--   take-seat: page.locator('button', { hasText: /take seat/i }).first().click() -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author hex + cribbage smokes (drag gating, multiSelect)</name>
  <files>scratchpad/verify-138-hex.mjs, scratchpad/verify-138-cribbage.mjs</files>
  <read_first>
    - The scratchpad verify-134*.mjs reference scripts (executablePath, gameFrame() iframe probe, take-seat, iframe controller access — copy these patterns verbatim)
    - CLAUDE.md browser-testing note (WS SPAs never reach networkidle — wait on domcontentloaded / a selector, then proceed)
    - 138-RESEARCH.md §4 (cribbage multiSelect lives in CribbageBoard.vue/actions.ts; hex exercises drag gating)
  </read_first>
  <action>Write two headless Playwright scripts reusing the verify-134 harness. hex smoke: start `npx boardsmith dev` in ~/BoardSmithGames/hex, load :5173, take a seat, locate the GameShell iframe, perform one drag-move of a piece to a legal cell, assert the move round-trips (board state reflects it) — this exercises UIX-04 drag gating live. cribbage smoke: start dev in ~/BoardSmithGames/cribbage, take a seat, drive to the discard phase, perform a multiSelect discard (select 2 cards) and submit, assert the flow advances past discard — this exercises UIX-02 multiSelect + Phase 133 flow changes. Each script MUST kill its dev server (and confirm :5173 free) in a finally block before exiting, per the never-leave-dev-server rule. Scripts exit non-zero on assertion failure.</action>
  <verify>
    <automated>lsof -ti:5173 | head -1 || echo "5173 free before"; SCRATCH=/private/tmp/claude-501/-Users-jtsmith-BoardSmith/d9d38075-c690-4bdc-8645-2e7d6abfd720/scratchpad node "$SCRATCH/verify-138-hex.mjs"; echo "hex exit=$?"; lsof -ti:5173 && echo "LEAK" || echo "5173 free after hex"</automated>
  </verify>
  <acceptance_criteria>
    - verify-138-hex.mjs exits 0; hex drag-move round-trips
    - verify-138-cribbage.mjs exits 0; multiSelect discard advances the flow
    - `lsof -ti:5173` is empty after each script (dev server killed in finally)
  </acceptance_criteria>
  <done>hex and cribbage smokes authored, pass headlessly, and leave port 5173 free.</done>
</task>

<task type="auto">
  <name>Task 2: Author go-fish smoke (failure toast + hidden-info restore)</name>
  <files>scratchpad/verify-138-gofish.mjs</files>
  <read_first>
    - verify-134.mjs (exactly-one-toast assertion pattern — count toast DOM nodes, expect 1)
    - 138-RESEARCH.md §7 (go-fish is the SEC-01/visibility empirical closer; no-hidden-info-* suites already probe this)
    - MEMORY note: v4.2 migration exposed a real go-fish hidden-hand broadcast bug — treat any surfaced failure as signal, not noise
  </read_first>
  <action>Write a headless Playwright script for go-fish: start dev in ~/BoardSmithGames/go-fish, take a seat, load the GameShell iframe. (a) Trigger an illegal/failed "ask" and assert exactly ONE failure toast renders (UIX-01 chokepoint — no duplicates). (b) Read the DOM for the opponent's hand and assert card faces are hidden (contentsHidden holds) — then trigger a state change/restore path if reachable via the debug panel and re-assert the opponent hand stays hidden (SEC-01 restore fidelity). If a real hidden-info leak surfaces, STOP and record it as a surfaced bug (do NOT paper over — it feeds a BoardSmith src fix like the v4.2 precedent). Kill the dev server and confirm :5173 free in a finally block.</action>
  <verify>
    <automated>SCRATCH=/private/tmp/claude-501/-Users-jtsmith-BoardSmith/d9d38075-c690-4bdc-8645-2e7d6abfd720/scratchpad node "$SCRATCH/verify-138-gofish.mjs"; echo "gofish exit=$?"; lsof -ti:5173 && echo "LEAK" || echo "5173 free after gofish"</automated>
  </verify>
  <acceptance_criteria>
    - verify-138-gofish.mjs exits 0
    - Exactly one failure toast on a failed ask
    - Opponent hand faces hidden before AND after a restore path
    - `lsof -ti:5173` empty after the run
    - Any surfaced hidden-info leak recorded as a blocker in SUMMARY (not silently passed)
  </acceptance_criteria>
  <done>go-fish smoke passes; toast-count and hidden-info-restore invariants hold; port 5173 free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player seat ↔ opponent state | Rendered DOM must not leak opponent hidden hands across restore (SEC-01) |
| dev server process ↔ environment | A leaked :5173 process is an OS-state hazard (never-leave-dev-server rule) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-04 | Information disclosure | go-fish opponent hand across restore | mitigate | Smoke asserts hidden faces before/after restore; leak = blocker feeding a BoardSmith src fix |
| T-138-05 | Denial of service | leaked dev server on :5173 | mitigate | finally block kills server; `lsof -ti:5173` gate after every smoke |
</threat_model>

<verification>
- Three smokes exit 0 headlessly
- Exactly-one-toast, drag round-trip, multiSelect flow-advance, hidden-info-restore all hold
- `lsof -ti:5173` empty after each script
</verification>

<success_criteria>
The three CONTEXT-locked Playwright smokes (hex/go-fish/cribbage) pass headlessly, empirically closing the drag-gating, multiSelect, and hidden-info/restore categories; no dev server left running.
</success_criteria>

<output>
Create `.planning/phases/138-cross-repo-migration/138-02-SUMMARY.md` when done. Record any surfaced bug as a blocker.
</output>
