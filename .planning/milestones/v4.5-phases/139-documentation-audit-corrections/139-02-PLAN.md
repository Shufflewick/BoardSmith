---
phase: 139-documentation-audit-corrections
plan: 02
type: execute
wave: 2
depends_on: [139-01]
files_modified:
  - docs/common-patterns.md
  - docs/common-pitfalls.md
  - docs/actions-and-flow.md
  - docs/custom-ui-guide.md
  - docs/ui-components.md
  - docs/agent-control.md
  - docs/teaching-and-tutorials.md
  - docs/core-concepts.md
  - docs/getting-started.md
  - docs/architecture.md
  - docs/conditions.md
  - docs/dice-and-scoring.md
  - docs/element-enrichment.md
  - docs/game-examples.md
  - docs/llm-overview.md
  - docs/migration-guide.md
  - docs/nomenclature.md
  - docs/README.md
  - docs/api/runtime.md
  - docs/api/session.md
  - docs/api/testing.md
  - docs/api/client.md
  - docs/api/ui.md
  - docs/api/index.md
autonomous: true
requirements: [DOCX-04]
must_haves:
  truths:
    - "Every API symbol changed by phases 131-138 has been grepped across all of docs/ and each live hit verified against current src/ (fixed or deleted if stale)"
    - "Zero doc references to removed/nonexistent APIs remain (element.setAttribute, phantom action().do().chooseOnBoard, boardsmith.json playerCount/$schema, doAction-result-checking without try/catch, test-${Date.now()} seed pattern, registerDebug hidden-state dumps)"
    - "The SUMMARY contains a sweep ledger table: symbol → files checked → action taken"
    - "Full BoardSmith suite (~175 files / 2371 tests) stays green and tsc --noEmit clean"
  artifacts:
    - path: .planning/phases/139-documentation-audit-corrections/139-02-SUMMARY.md
      provides: "The DOCX-04 sweep ledger (symbol → files → action)"
      contains: "Sweep Ledger"
  key_links:
    - from: docs/common-patterns.md
      to: src/engine/flow/engine.ts
      via: "dealer-rotation / eachPlayer startingPlayer doc matches the now-wrapping behavior (Phase 133)"
      pattern: "wrap"
    - from: docs/agent-control.md
      to: src/session/game-session.ts
      via: "runner.performAction guidance matches the session.performAction/read-only-runner facade (Phase 134)"
      pattern: "performAction"
---

<objective>
DOCX-04: a grep-verified sweep over ALL of docs/ (and docs-facing JSDoc) for every API symbol changed in phases 131-138. Each doc claim is checked against live source; stale claims are fixed or deleted. Mirrors v4.4 Phase 130's doc-verifier pass.

Purpose: Phases 131-138 changed many contracts (zone-visibility serialization, visibleAttributes enforcement, putInto throw, eachPlayer wrap, simultaneous actionError, multiSelect server enforcement, doAction throws, MeepleClient throwing contract, 127.0.0.1 default, etc.). Docs that still teach the old contract are now actively wrong. This sweep guarantees the docs describe the shipped v4.5 surface.

Context management (23-file footprint): process docs in clusters (engine/session cluster, UI cluster, CLI/SDK/testing cluster). If context pressure rises past ~50%, spawn a sub-agent per doc cluster, have each return its ledger rows, and merge them into the single SUMMARY sweep ledger. The CONTEXT.md discretion note explicitly authorizes "gsd-doc-verifier agents per doc or one sweep agent — pick per context budget."

Output: Corrected docs across the repo + a sweep ledger table in the SUMMARY (symbol → files checked → action taken), plus a green full-suite run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/139-documentation-audit-corrections/139-CONTEXT.md

<read_first>
The full DOCX-04 seed list is in 139-CONTEXT.md (Docs Correction Strategy → DOCX-04). Each seed symbol maps to a phase decision recorded in STATE.md "Decisions" and the phase SUMMARYs. Load the phase SUMMARY on-demand when a grep hit needs the exact new contract:
- .planning/phases/131-*/131-*-SUMMARY.md — zone-visibility now serialized; visibleAttributes ENFORCED (filtered in filterElement); state.players filtered via truthView; debugEnabled GameSession-only; teachingDisabled persisted
- .planning/phases/132-*/132-*-SUMMARY.md — putInto self/descendant THROWS ('descendant' in message); resolveArgs isSerializedElement-only; forEach snapshot {elementId} + type constraint; registerAction handler-less THROWS
- .planning/phases/133-*/133-*-SUMMARY.md — eachPlayer WRAPS (unconditional, no wrap:false); simultaneous actionError contract + FlowHaltedError; multiSelect server-enforced; switchOn THROWS on unmatched
- .planning/phases/134-*/134-*-SUMMARY.md — session.runner is a read-only facade; start() returns Promise<ActionResult>; errorTick/lastError surfaced centrally in GameShell; fill() multiSelect guard; dragProps honors `when`; hook accumulation; board-sizing guidance (responsive/viewport-measured board sizing)
- .planning/phases/135-*/135-*-SUMMARY.md — playerCount removed from boardsmith.json (gameDefinition is source of truth); --lan/--no-open; 127.0.0.1 default; validate rejects unknown keys; 50MB upload limit
- .planning/phases/136-*/136-*-SUMMARY.md — MeepleClient THROWS (MeepleClientError + errorCode); generatePlayerId; opened/awaitReconnect/connectImmediately/connectionTimeout; canonical protocol types
- .planning/phases/137-*/137-*-SUMMARY.md — doAction THROWS ActionExecutionError by default; tryAction is the never-throw escape hatch; 'test-seed' default seed + testGame.seed accessor
- .planning/phases/138-*/138-*-SUMMARY.md — games migrated to all the above (real worked examples now exist to cite)

Known active-trap doc sites already surfaced by the audit (verify + fix these specifically):
- docs/common-patterns.md:54 — "Dealer Rotation" models the OLD non-wrapping eachPlayer startingPlayer (now wraps, Phase 133)
- docs/actions-and-flow.md:906 — TurnOrder.LEFT_OF_DEALER shown with old semantics; forEach section steering toward mutating-collection pattern
- docs/common-pitfalls.md — followUp numeric-arg auto-resolution (now isSerializedElement-only, Phase 132); registerActions phantom API ~1107; registerAction handler-less now throws; registerDebug full-tree dump ~1401 (debug now gated); the imperative-plain-Space-contentsHidden example ~699 (now restore-safe since zone visibility is serialized)
- docs/custom-ui-guide.md — fill()/start()/lastError error-path guidance (Phase 134); multiSelect via fill() vs toggleMultiSelect; board-sizing guidance (Phase 134)
- docs/ui-components.md — dragProps `when` (now honored, Phase 134); board-sizing guidance (Phase 134)
- docs/agent-control.md:174,439 & docs/api/runtime.md — runner.performAction vs session.performAction (Phase 134 read-only facade)
- docs/api/testing.md — doAction contract (now throws) + seed defaults (Phase 137)
- docs/api/client.md / docs/api/session.md — MeepleClient throwing contract, protocol types (Phase 136)
- docs/teaching-and-tutorials.md — teachingDisabled restore persistence (Phase 131)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep-verified sweep of engine + session doc claims (phases 131-134)</name>
  <files>docs/common-patterns.md, docs/common-pitfalls.md, docs/actions-and-flow.md, docs/core-concepts.md, docs/architecture.md, docs/conditions.md, docs/custom-ui-guide.md, docs/ui-components.md, docs/agent-control.md, docs/teaching-and-tutorials.md, docs/element-enrichment.md, docs/dice-and-scoring.md, docs/api/runtime.md, docs/api/session.md, docs/api/ui.md</files>
  <read_first>
    - 139-CONTEXT.md DOCX-04 seed list
    - Phase 131/132/133/134 SUMMARYs (load per-symbol as grep hits require the exact new contract)
    - Known active-trap sites listed in <context> read_first
  </read_first>
  <action>
    Process this cluster's docs in sub-clusters if context pressure rises (see objective's context-management note). For each engine/session/UI seed symbol (phases 131-134), grep all of docs/ for the symbol, open each hit, and verify the claim against current src/. Fix or delete stale claims. Record every symbol in the sweep ledger (symbol → files checked → action taken):

    - Zone visibility (contentsHidden/contentsVisibleToOwner/contentsCountOnly): now serialized and restore-faithful (Phase 131). Remove any "lost on restore" caveats; the docs/common-pitfalls.md:~699 imperative-plain-Space example is now safe — keep the pattern, drop any warning that it leaks across restore.
    - visibleAttributes: now ENFORCED (filtered for non-owners in filterElement, Phase 131). core-concepts.md was corrected in 139-01; verify no OTHER doc still calls it a no-op or, conversely, still teaches it as a security control if 131 only partially implemented — cite 131 SUMMARY for exact scope.
    - state.players filtering (Phase 131): remove any doc that models secrets as Player attributes as safe.
    - debugEnabled (Phase 131): debug is now GameSession-only gated (debugEnabled). Grep docs for debugEnabled / registerDebug / debug-mode teaching; verify any doc showing debug output describes it as gated to GameSession (not broadcast to all seats / not available in every runtime context). Fix or delete claims that model debug state as universally available or leaking to non-owners.
    - teachingDisabled persistence (Phase 131): docs/teaching-and-tutorials.md restore caveat — flag now survives restore.
    - putInto self/descendant now THROWS with 'descendant' in the message (Phase 132): update any doc claiming silent detachment / manual containment checks.
    - registerAction handler-less now THROWS (Phase 132): grep docs for registerAction/registerActions usages; any example that registers an action WITHOUT an execute/handler must be updated — a handler-less registration now throws at registration time. Correct such examples to include the handler (and note the throw as the pit-of-success guard).
    - resolveArgs / followUp numeric args: now isSerializedElement-only (Phase 132). docs/common-pitfalls.md followUp section — plain numbers are no longer auto-resolved; update the guidance.
    - forEach: collection snapshotted once with {elementId} + type constraint (Phase 132). actions-and-flow.md forEach + builders @example — stop steering toward mutating-collection hazard.
    - eachPlayer startingPlayer now WRAPS unconditionally (Phase 133). docs/common-patterns.md:54 Dealer Rotation + actions-and-flow.md:906 TurnOrder.LEFT_OF_DEALER — rewrite to the wrapping semantics.
    - simultaneousActionStep actionError + FlowHaltedError, switchOn throw (Phase 133): update any flow-error docs.
    - multiSelect server-enforced for chooseFrom (Phase 133): remove any "client-side only" implication.
    - session.runner read-only facade + start() Promise<ActionResult> + central lastError/errorTick + fill() multiSelect guard + dragProps `when` honored + hook accumulation + board-sizing guidance (Phase 134): fix docs/custom-ui-guide.md, docs/ui-components.md, docs/agent-control.md:174/439, docs/api/runtime.md, docs/api/session.md, docs/api/ui.md. runner.performAction must be documented as read-only; direct submission goes through session.performAction. Verify the board-sizing guidance (custom-ui-guide.md / ui-components.md) matches the responsive/viewport-measured board sizing shipped in Phase 134 — fix any doc still teaching a fixed board size or a superseded sizing hook.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && test $(grep -rniE "does NOT wrap|silently skip.*player|lost on.*restore.*visibility|client-side only" docs/ | grep -viE "no longer|previously|used to" | wc -l) -eq 0 && test $(grep -rniE "\.chooseOnBoard\(|element\.setAttribute" docs/ | wc -l) -eq 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Zero doc references to removed/phantom engine APIs (element.setAttribute, .chooseOnBoard)
    - No doc still teaches non-wrapping eachPlayer, silent putInto detachment, client-only multiSelect, or lost-on-restore zone visibility as current behavior
    - debugEnabled, registerAction-handler-less-throw, and board-sizing guidance each appear in the sweep ledger with files-checked + action-taken
    - Every phase-131-134 seed symbol appears in the sweep ledger with files-checked + action-taken
  </acceptance_criteria>
  <done>All engine/session/UI doc claims for phases 131-134 verified against src and corrected; ledger rows recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Grep-verified sweep of CLI + SDK + testing doc claims (phases 135-138) + full-suite gate</name>
  <files>docs/getting-started.md, docs/api/client.md, docs/api/session.md, docs/api/testing.md, docs/migration-guide.md, docs/agent-control.md, docs/game-examples.md, docs/llm-overview.md, docs/nomenclature.md, docs/README.md, docs/api/index.md</files>
  <read_first>
    - 139-CONTEXT.md DOCX-04 seed list
    - Phase 135/136/137/138 SUMMARYs
    - src/cli/cli.ts (real flags), src/client/ (MeepleClient contract), src/testing/ (doAction/tryAction/seed)
  </read_first>
  <action>
    Process this cluster's docs in sub-clusters if context pressure rises (see objective's context-management note). For each CLI/SDK/testing seed symbol (phases 135-138), grep all of docs/, verify each hit against src/, fix or delete. Add ledger rows. Then run the full gate.

    - boardsmith.json playerCount / $schema: removed (Phase 135). getting-started.md was handled in 139-01; sweep ALL other docs for playerCount-in-boardsmith.json or $schema examples and remove.
    - CLI flags: --lan, --no-open, 127.0.0.1 default, validate unknown-key rejection, 50MB limit (Phase 135). Verify any doc showing dev/validate/publish matches src/cli/cli.ts.
    - MeepleClient now THROWS (MeepleClientError + optional errorCode); generatePlayerId; opened/awaitReconnect/connectImmediately/connectionTimeout; canonical protocol types (Phase 136). docs/api/client.md + docs/api/session.md — replace any result-checking (`if (!r.success)`) client examples with try/catch throwing-contract examples; document the new connection options.
    - doAction now THROWS ActionExecutionError by default; tryAction is the never-throw escape hatch; 'test-seed' default seed + testGame.seed accessor (Phase 137). docs/api/testing.md + any test examples across docs — remove `const r = doAction(...); if (!r.success)` patterns and `test-${Date.now()}` / ad-hoc seed patterns; teach the throwing default + tryAction + default seed.
    - Cite real migrated examples from Phase 138 games where a worked example strengthens the doc.

    Final gate: run tsc + full suite, preserving vitest's real exit code (do NOT pipe vitest through tail — that masks failures).
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && test $(grep -rniE "test-\$\{Date\.now|\"\\\$schema\"" docs/ | wc -l) -eq 0 && test $(grep -rni "playerCount" docs/ | grep -viE "no longer|removed|previously|used to|gameDefinition|deprecated" | wc -l) -eq 0 && npx tsc --noEmit && { npx vitest run > /tmp/vitest-139.out 2>&1; RC=$?; tail -5 /tmp/vitest-139.out; test $RC -eq 0 && echo PASS; }</automated>
  </verify>
  <acceptance_criteria>
    - Zero boardsmith.json playerCount/$schema examples remain anywhere in docs/ (plain `playerCount` grep across docs/ is clean except legitimate removal-prose filtered by -v)
    - No doc still models doAction/MeepleClient as result-returning where they now throw; tryAction documented as the escape hatch
    - CLI docs match src/cli/cli.ts (--lan, --no-open, 127.0.0.1); no invented flags
    - Every phase-135-138 seed symbol appears in the sweep ledger
    - `npx tsc --noEmit` clean AND full vitest suite green (~2371 tests), with vitest's real exit code gating PASS
    - SUMMARY contains the complete "Sweep Ledger" table (symbol → files checked → action taken) covering ALL four source phases' seed symbols
  </acceptance_criteria>
  <done>All CLI/SDK/testing doc claims verified and corrected; suite + tsc green; ledger complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → developer/agent | Stale docs teach removed contracts; the acute risk is any doc still modeling registerDebug hidden-state dumps or secrets-on-Player as safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-03 | Information Disclosure | common-pitfalls.md registerDebug full-tree/hidden-state example | mitigate | Task 1 verifies debug is now gated (Phase 131 debugEnabled GameSession-only) and removes any doc modeling hidden-state dumps into registerDebug that broadcast to all seats |
| T-139-04 | Information Disclosure | zone-visibility / state.players restore claims | mitigate | Task 1 aligns docs with Phase 131 (visibility now restore-faithful; secrets belong on visibility-managed elements, not Player attributes) |
| T-139-SC | Tampering | npm/pip/cargo installs | accept | Docs-only phase; no package installs |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npx vitest run` — full suite green (baseline ~175 files / 2371 tests), gated on vitest's real exit code (not masked by tail)
- grep gates in both tasks pass (zero stale/removed-symbol references)
- Sweep ledger present in SUMMARY covering all four phases' seed symbols
</verification>

<success_criteria>
- DOCX-04: every phase-131-138 API change grep-verified across docs/; stale claims fixed or deleted
- Ledger table (symbol → files → action) in the SUMMARY
- tsc clean + full suite green
</success_criteria>

<output>
Create `.planning/phases/139-documentation-audit-corrections/139-02-SUMMARY.md` when done, including the complete Sweep Ledger table.
</output>
</output>
