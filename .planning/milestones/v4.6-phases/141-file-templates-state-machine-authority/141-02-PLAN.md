---
phase: 141-file-templates-state-machine-authority
plan: 02
type: execute
wave: 2
depends_on: [141-01]
files_modified:
  - src/cli/slash-command/bs/templates/CHUNK.template.md
  - src/cli/slash-command/bs/templates/SKETCH.template.md
  - src/cli/slash-command/bs/templates.test.ts
autonomous: true
requirements: [TMPL-01, TMPL-02, TMPL-03]
must_haves:
  truths:
    - "CHUNK.template.md exists with an authoritative `Status:` line, the 10-step full-ceremony checklist plus the 3-step light path, interpretation with numbered citations + visibility declaration, findings ledger with stable IDs, append-only revision rounds, per-file build manifest, verified-commit-hash slot, playtest test script, and a restated `ui:` tag"
    - "SKETCH.template.md exists with a slug-ordered chunk list, per-chunk sketch detail (what it builds, citations, `ui:` tag none|touches|major, outcome-based test script), Variants (deferred) list, ideas backlog, sketch version stamp, session lock note, player-count/setup notes, and the mandated core-loop/game-end/final-acceptance chunk guidance"
    - "CHUNK.template.md and state-machine.md use the identical exact step-name strings (drift test asserts agreement)"
    - "SKETCH.template.md and CHUNK.template.md each carry an HTML-comment pointer to state-machine.md at their status line"
  artifacts:
    - path: "src/cli/slash-command/bs/templates/CHUNK.template.md"
      provides: "Authoritative per-chunk state skeleton (status owner)"
      contains: "Status: proposed"
    - path: "src/cli/slash-command/bs/templates/SKETCH.template.md"
      provides: "Game sketch skeleton (ordered slug list, derived pointers)"
      contains: "Variants (deferred)"
  key_links:
    - from: "src/cli/slash-command/bs/templates/CHUNK.template.md"
      to: "src/cli/slash-command/bs/state-machine.md"
      via: "identical step-name list + HTML-comment pointer at Status line"
      pattern: "investigate, redteam, ask, build, test, audit, repair, playtest, revise, close"
---

<objective>
Write the two status-bearing templates: `CHUNK.template.md` (authoritative for its chunk's status) and `SKETCH.template.md` (the derived ordered-slug view), then extend `templates.test.ts` with their drift assertions — including the cross-file check that CHUNK.template.md and state-machine.md list the exact same step names.

This plan carries the bulk of TMPL-01 (exact step names + status enum in the templates), TMPL-02 (each template states its own parse contract), and TMPL-03 (CHUNK owns status; SKETCH is derived and points to the authority doc).

Purpose: These two files are the state substrate the whole bs- pipeline resumes against; every field must be transcribed, not paraphrased.
Output: `CHUNK.template.md`, `SKETCH.template.md`, extended green `templates.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/bs-skills-plan.md
@.planning/phases/141-file-templates-state-machine-authority/141-RESEARCH.md
@.planning/phases/141-file-templates-state-machine-authority/141-CONTEXT.md
@.planning/phases/141-file-templates-state-machine-authority/141-PATTERNS.md
@src/cli/slash-command/bs/state-machine.md
@src/cli/slash-command/bs/templates.test.ts

<interfaces>
<!-- Same canonical strings as Plan 01. CHUNK.template.md MUST reuse the exact step-name and
     status-enum strings that state-machine.md already ships — the drift test asserts they agree. -->
Status enum: proposed | approved | built | verified | verified (user-waived) | stale — re-derive before build  (em-dash)
Step names (full): investigate, redteam, ask, build, test, audit, repair, playtest, revise, close
Step names (light path): build, test, playtest
Status-line grammar: `Status: <enum-value>`
ui: tag values: none | touches | major
</interfaces>

<!-- Content-style analog (read, do not modify): src/cli/slash-command/aspects/dice.md
     (H1 + short pointer line + ## sections; inline guidance, no deeper than ##/###).
     Fill-in guidance is HTML comments (<!-- ... -->) that STAY in the file after filling
     (Pattern 3 — never delete; a cold-resumed session re-reads the same guidance). -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CHUNK.template.md</name>
  <files>src/cli/slash-command/bs/templates/CHUNK.template.md</files>
  <read_first>
    - .planning/phases/141-file-templates-state-machine-authority/141-RESEARCH.md — "Exhaustive per-template field checklist → CHUNK.md" (use as the literal acceptance checklist; cross-check line-by-line) and Pattern 1 (Status-line grammar), Pattern 2 (append-only rounds), Pattern 3 (HTML-comment guidance), Pitfall 1/3/4
    - .planning/bs-skills-plan.md — the numbered `/bs-build-chunk` step walkthrough (steps 1-10) for the per-step content each checklist item represents; "Scaled ceremony" for the light path; UI section for the `ui: touches|major` a11y-floor gating
    - src/cli/slash-command/bs/state-machine.md — copy the exact step-name and status-enum strings from here (do not re-derive)
    - src/cli/slash-command/aspects/dice.md — content-style/heading analog
  </read_first>
  <action>
    Create `src/cli/slash-command/bs/templates/CHUNK.template.md` as a standalone literal skeleton (full content, NOT a thin pointer). Structure: H1 `# Chunk: <!-- slug -->`; immediately below, the authoritative `Status: proposed` line with an HTML comment listing the exact valid enum values (including `verified (user-waived)` and `stale — re-derive before build`) and a one-line pointer comment "This line is authoritative; SKETCH.md's entry is derived and must match — see ../state-machine.md" (TMPL-03/Pitfall 4). Then transcribe every field from the CHUNK.md checklist as `##` sections with inline HTML-comment guidance that stays after filling: a restated `ui:` tag line (none|touches|major, propagated from SKETCH.md — restated so a CHUNK-only session knows whether the a11y floor applies, per RESEARCH Open Question 2); ceremony type (full vs light path, declared at proposal); a Step Checklist rendered as markdown checkboxes using the exact 10 step names in order (`- [ ] investigate` … `- [ ] close`) with a documented light-path subset (`build, test, playtest`); Interpretation (numbered factual claims with citations) + Visibility Declaration (what is hidden from whom); Newly Discovered Citations (appended during investigate); Findings Ledger with stable IDs (audit; round N+1 reports only NEW findings); Revision Rounds as append-only `### Revise 1`, `### Revise 2`, … (Pattern 2, never overwrite); Per-file Build Manifest (file-by-file crash/resume); Playtest Test Script (numbered click-by-click, seat count + per-seat steps, dev-host affordances taught once, build stamp, one-line regression check, standing taste line, second-seat leak check for hidden-info chunks); a "Verified" explicit item-by-item checklist and the recordable `verified (user-waived)` state; Verified Commit Hash slot (recorded at close). Reuse the exact enum/step strings verbatim from state-machine.md so the drift test's cross-file agreement passes. No code, no parser, no new dependency.
  </action>
  <verify>
    <automated>grep -q "Status: proposed" src/cli/slash-command/bs/templates/CHUNK.template.md && grep -q "investigate, redteam, ask, build, test, audit, repair, playtest, revise, close" src/cli/slash-command/bs/templates/CHUNK.template.md && grep -q "state-machine.md" src/cli/slash-command/bs/templates/CHUNK.template.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists as full standalone content (not a `{{BOARDSMITH_ROOT}}` thin pointer).
    - Contains an authoritative `Status: proposed` line whose enum comment lists all six status values including `verified (user-waived)` and `stale — re-derive before build`.
    - Step checklist uses the exact 10 step names in order; the light-path subset `build, test, playtest` is documented.
    - Every CHUNK.md field from the RESEARCH checklist is present: interpretation w/ citations, visibility declaration, findings ledger (stable IDs), append-only revision rounds, per-file build manifest, playtest script (with second-seat leak check + build stamp), verified checklist + user-waived state, verified commit hash, restated `ui:` tag.
    - Carries an HTML-comment pointer to state-machine.md at the Status line (Pitfall 4).
    - Step-name and status-enum strings are byte-identical to those in state-machine.md.
  </acceptance_criteria>
  <done>CHUNK.template.md is a complete, cold-resumable skeleton covering every plan-specified field with exact enum/step strings and an authority pointer.</done>
</task>

<task type="auto">
  <name>Task 2: Write SKETCH.template.md</name>
  <files>src/cli/slash-command/bs/templates/SKETCH.template.md</files>
  <read_first>
    - .planning/phases/141-file-templates-state-machine-authority/141-RESEARCH.md — "Exhaustive per-template field checklist → SKETCH.md" (literal acceptance checklist), Open Question 1 (UI-strategy field belongs in SKETCH.md), Assumptions A1/A2, Pattern 1/3
    - .planning/bs-skills-plan.md — `/bs-ingest-rules` "Outputs" (SKETCH.md contents, mandated chunks, UI-strategy decision) and Terminology (chunk = stable slug, not ordinal)
    - src/cli/slash-command/bs/state-machine.md — for the derived-status wording and the `ui:` tag values
    - src/cli/slash-command/bs/templates/CHUNK.template.md (Task 1) — keep the per-chunk `ui:` tag values and status enum consistent
  </read_first>
  <action>
    Create `src/cli/slash-command/bs/templates/SKETCH.template.md` as a standalone literal skeleton. Structure with `##` sections + inline HTML-comment guidance (kept after filling): a Sketch Version Stamp (bumped by /bs-insert-chunk) and a Session Lock Note (chunk + timestamp) near the top, plus an HTML-comment pointer noting SKETCH.md status entries are DERIVED — CHUNK.md is authoritative (see ../state-machine.md); a Player Counts section (min/max + per-count setup differences); a UI Strategy field (Custom-UI-from-chunk-1 vs AutoUI-with-scheduled-cutover, decided at ingest — per RESEARCH Open Question 1); the Ordered Chunk List keyed by stable slug (guidance: order by listing slugs, reordering never breaks `chunks/<slug>/` references; only the next 2-3 chunks are detailed, the tail stays sketch-level and is re-derived as a delta at close), each chunk entry recording what it builds, cited rulebook sections, a `ui:` tag (`none|touches|major`), a derived status pointer, and an outcome-based human test script (state outcomes not gestures — "move a pawn one space; the board reflects it"); a Variants (deferred) list (out-of-scope-by-default variant/optional/advanced rules); an Ideas Backlog (fed by revise-step triage category (b)); and a Mandated Chunks guidance comment (first chunk = core event loop; sketch must contain a game-end/scoring/winner chunk and a final-acceptance chunk — full game start→finish, coverage check of every non-variant rulebook slice, plus the design-QA/a11y audit). Reuse the exact `ui:` tag and status-enum strings. No code, no parser, no new dependency.
  </action>
  <verify>
    <automated>grep -q "Variants (deferred)" src/cli/slash-command/bs/templates/SKETCH.template.md && grep -q "state-machine.md" src/cli/slash-command/bs/templates/SKETCH.template.md && grep -Eq "none *\| *touches *\| *major" src/cli/slash-command/bs/templates/SKETCH.template.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists as full standalone content (not a thin pointer).
    - Contains: sketch version stamp, session lock note, player-count/setup section, UI-strategy field, slug-ordered chunk list with per-chunk (what-it-builds, citations, `ui:` tag none|touches|major, derived status pointer, outcome-based test script), "Variants (deferred)" list, ideas backlog.
    - Contains the mandated-chunks guidance (core event loop first; game-end/scoring/winner chunk; final-acceptance chunk with coverage check + design-QA/a11y audit).
    - Carries an HTML-comment noting status entries are derived and pointing to state-machine.md (TMPL-03).
    - `ui:` tag and status-enum strings match CHUNK.template.md / state-machine.md.
  </acceptance_criteria>
  <done>SKETCH.template.md is a complete cold-resumable skeleton with every plan-specified field and the derived-status authority pointer.</done>
</task>

<task type="auto">
  <name>Task 3: Extend templates.test.ts with CHUNK/SKETCH + cross-file agreement assertions</name>
  <files>src/cli/slash-command/bs/templates.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/templates.test.ts (from Plan 01) — extend at the marked placeholders; do not restructure the existing state-machine blocks
    - .planning/phases/141-file-templates-state-machine-authority/141-RESEARCH.md — Pitfall 3 (assert the exact step-name string appears in BOTH CHUNK.template.md and state-machine.md), Test Map (describe naming by requirement ID)
    - .planning/phases/141-file-templates-state-machine-authority/141-PATTERNS.md — templates.test.ts pattern (indexOf/slice block isolation)
    - src/cli/slash-command/bs/templates/CHUNK.template.md, SKETCH.template.md (Tasks 1-2) — the files under assertion
  </read_first>
  <action>
    At the reserved placeholder in `templates.test.ts`, add assertions for the two new files without disturbing the existing state-machine.md blocks. Under a `describe('TMPL-01 — exact step names & status enum', ...)`: assert CHUNK.template.md `toContain` each exact status-enum token (including `verified (user-waived)` and `stale — re-derive before build`) and the full step-name list string, and assert SKETCH.template.md contains the `ui:` tag values and the "Variants (deferred)" heading. Under `describe('TMPL-03 — CHUNK/SKETCH ↔ state-machine consistency', ...)`: read BOTH CHUNK.template.md and state-machine.md and assert the identical exact step-name list string appears in each (Pitfall 3), and assert both CHUNK.template.md and SKETCH.template.md contain the substring `state-machine.md` (the authority pointer, Pitfall 4). Under `describe('TMPL-02 — parse contract', ...)`: assert CHUNK.template.md contains a `Status:` line and SKETCH.template.md documents its derived-status pointer. Use `indexOf`/`slice` block isolation where asserting against a specific region. Keep the Plan 03 (ledger) placeholder intact. Plain string matching only; no new dependency.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/templates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/templates.test.ts` is GREEN.
    - A test reads both CHUNK.template.md and state-machine.md and asserts the identical step-name list string in each (fails if either drifts).
    - Assertions cover CHUNK/SKETCH status-enum, step-name, `ui:` tag, and authority-pointer content.
    - Existing Plan 01 state-machine assertions still pass; Plan 03 ledger placeholder is preserved.
    - No markdown-parser import; no package.json change.
  </acceptance_criteria>
  <done>Drift test green and now guards CHUNK/SKETCH invariants and CHUNK↔state-machine step-name agreement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. Static markdown templates + a content-assertion test extension. No runtime execution of file contents, no inputs, no network.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-02 | S/T/R/I/D/E | CHUNK/SKETCH template files + vitest assertions | accept | No attack surface: no runtime, no inputs, no network, no privilege boundary. |
| T-141-SC | Tampering | npm/pip/cargo installs | N/A | Zero packages installed (vitest ^2.1.0 + Node built-ins already present). No supply-chain surface. |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/templates.test.ts` green.
- `grep -c "state-machine.md" src/cli/slash-command/bs/templates/*.template.md` — both new templates carry the authority pointer (Pitfall 4).
</verification>

<success_criteria>
- CHUNK.template.md and SKETCH.template.md exist with every plan-specified field, exact enum/step strings, and authority pointers.
- templates.test.ts green, guarding both files and the CHUNK↔state-machine step-name agreement.
</success_criteria>

<output>
Create `.planning/phases/141-file-templates-state-machine-authority/141-02-SUMMARY.md` when done.
</output>
