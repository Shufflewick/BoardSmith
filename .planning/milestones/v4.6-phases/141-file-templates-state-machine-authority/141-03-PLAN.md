---
phase: 141-file-templates-state-machine-authority
plan: 03
type: execute
wave: 3
depends_on: [141-02]
files_modified:
  - src/cli/slash-command/bs/templates/RULINGS.template.md
  - src/cli/slash-command/bs/templates/DECISIONS.template.md
  - src/cli/slash-command/bs/templates/DESIGN.template.md
  - src/cli/slash-command/bs/templates/ASSETS.template.md
  - src/cli/slash-command/bs/templates.test.ts
autonomous: true
requirements: [TMPL-01, TMPL-02]
must_haves:
  truths:
    - "RULINGS.template.md exists as a designer-decisions ledger where each entry is paired with the rulebook citation it interprets or overrides"
    - "DECISIONS.template.md exists as an implementation-decisions ledger (data model, naming, invariants)"
    - "DESIGN.template.md exists with chosen direction+rationale, --bsg-*/applyTheme() overrides, typography/spacing, component recipes, placeholder policy, do/don't list, and the color-literals-only-in-theme-block rule"
    - "ASSETS.template.md exists as a component/asset ledger with columns needed-by-chunk, requested, received, placeholder-in-use, file path"
    - "The drift test is green for all six templates and state-machine.md (full-suite npm test green)"
  artifacts:
    - path: "src/cli/slash-command/bs/templates/RULINGS.template.md"
      provides: "Designer-decisions ledger skeleton"
      contains: "citation"
    - path: "src/cli/slash-command/bs/templates/DECISIONS.template.md"
      provides: "Implementation-decisions ledger skeleton"
      contains: "invariant"
    - path: "src/cli/slash-command/bs/templates/DESIGN.template.md"
      provides: "Visual-identity contract skeleton"
      contains: "--bsg-"
    - path: "src/cli/slash-command/bs/templates/ASSETS.template.md"
      provides: "Component/asset ledger skeleton"
      contains: "placeholder-in-use"
  key_links:
    - from: "src/cli/slash-command/bs/templates.test.ts"
      to: "src/cli/slash-command/bs/templates/*.template.md"
      via: "readFileSync + toContain assertions for all six templates"
      pattern: "RULINGS|DECISIONS|DESIGN|ASSETS"
---

<objective>
Write the four ledger/reference templates — `RULINGS.template.md`, `DECISIONS.template.md`, `DESIGN.template.md`, `ASSETS.template.md` — then complete `templates.test.ts` so the drift test guards all six templates plus state-machine.md, and the full suite is green.

This plan completes TMPL-01 (all six templates ship) and TMPL-02 (each ledger states its own required-sections parse contract).

Purpose: These four files are the durable side-ledgers the bs- pipeline writes to at ask/build/close/playtest gates; every plan-specified field must be present so downstream skills have a fixed shape to fill.
Output: four `.template.md` ledgers + completed green `templates.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/bs-skills-plan.md
@.planning/phases/141-file-templates-state-machine-authority/141-RESEARCH.md
@.planning/phases/141-file-templates-state-machine-authority/141-CONTEXT.md
@.planning/phases/141-file-templates-state-machine-authority/141-PATTERNS.md
@src/cli/slash-command/bs/templates.test.ts

<!-- Content-style analog (read, do not modify): src/cli/slash-command/aspects/dice.md.
     Fill-in guidance is HTML comments that STAY in the file after filling (Pattern 3).
     These four are full standalone skeletons, NOT thin pointers. No code, no new dependency. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write RULINGS.template.md and DECISIONS.template.md</name>
  <files>src/cli/slash-command/bs/templates/RULINGS.template.md, src/cli/slash-command/bs/templates/DECISIONS.template.md</files>
  <read_first>
    - .planning/phases/141-file-templates-state-machine-authority/141-RESEARCH.md — "Exhaustive per-template field checklist → RULINGS.md" and "→ DECISIONS.md" (literal acceptance checklists), Pattern 3
    - .planning/bs-skills-plan.md — Durable Artifacts table rows for RULINGS.md and DECISIONS.md; "Rulings outrank the rulebook" rule; `/bs-build-chunk` steps ask/playtest/revise (who writes RULINGS) and build/close (who writes DECISIONS); redteam refuted-twice escalation feeds RULINGS
    - src/cli/slash-command/aspects/dice.md — content-style/heading analog
  </read_first>
  <action>
    Create both files as standalone literal ledger skeletons (H1 + short pointer line + `##` sections with inline HTML-comment guidance kept after filling). RULINGS.template.md: a "Rulings" ledger where each entry pairs a designer decision (ambiguity resolution, house rule, or deliberate digital adaptation) with the exact rulebook citation it interprets or overrides; include a guidance comment stating rulings outrank the rulebook and are written by any ask/playtest gate and by redteam refuted-twice escalation; give a single filled-shape example entry inside an HTML comment (not real content) so the required per-entry fields (decision, citation-interpreted-or-overridden, rationale) are obvious. DECISIONS.template.md: an implementation-decisions ledger for data-model choices, naming, and invariants; include the plan's own example inside guidance ("money is a number on Player; spaces indexed 0–39 from GO") and a comment noting it is written by the build and close (rollup) steps. Append-only ledger shape; do not delete guidance comments.
  </action>
  <verify>
    <automated>grep -qi "citation" src/cli/slash-command/bs/templates/RULINGS.template.md && grep -qi "invariant" src/cli/slash-command/bs/templates/DECISIONS.template.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist as full standalone content (not thin pointers).
    - RULINGS.template.md: each ledger entry pairs a decision with the rulebook citation it interprets/overrides; guidance notes rulings outrank the rulebook and lists the writing gates (ask/playtest + redteam refuted-twice).
    - DECISIONS.template.md: ledger covers data-model choices, naming, and invariants; guidance names build/close as the writing steps.
    - Fill-in guidance is HTML comments that remain in the file (Pattern 3); no real game content baked in.
  </acceptance_criteria>
  <done>RULINGS and DECISIONS ledgers transcribe their full checklists with per-entry citation/decision shape and correct writing-gate guidance.</done>
</task>

<task type="auto">
  <name>Task 2: Write DESIGN.template.md and ASSETS.template.md</name>
  <files>src/cli/slash-command/bs/templates/DESIGN.template.md, src/cli/slash-command/bs/templates/ASSETS.template.md</files>
  <read_first>
    - .planning/phases/141-file-templates-state-machine-authority/141-RESEARCH.md — "Exhaustive per-template field checklist → DESIGN.md" and "→ ASSETS.md" (literal acceptance checklists), Pattern 3
    - .planning/bs-skills-plan.md — "UI, Accessibility, and Visual Quality" section (visual identity directions Adopt/Derive/Original, DESIGN.md contents, placeholder policy, "color literals live only in the theme block" rule, "changing DESIGN.md is itself a chunk") and Durable Artifacts rows for DESIGN.md/ASSETS.md
    - src/cli/slash-command/aspects/dice.md — content-style/heading analog
  </read_first>
  <action>
    Create both files as standalone literal skeletons. DESIGN.template.md (the visual-identity contract): `##` sections for Chosen Direction + rationale (Adopt / Derive / Original), `--bsg-*` / `applyTheme()` token overrides, Typography/Spacing, Component Recipes, Placeholder Policy, and a Do/Don't list; include the hard rule as guidance — color literals live only in the theme block, everything else references tokens (inherits Slate AA contrast) — and a comment noting DESIGN.md is written at the first UI chunk's ask and that changing it is itself a chunk (re-opens verified chunks per the restyle/cutover rule). ASSETS.template.md (the component/asset ledger): a table with the exact columns needed-by-chunk, requested, received, placeholder-in-use, file path; guidance noting it is populated at ingest (component inventory with aspect ratios) and at each chunk's ask step, and that a missing asset never blocks — a designed placeholder (correct aspect ratio, DESIGN.md tokens) is recorded instead. Keep guidance comments in place.
  </action>
  <verify>
    <automated>grep -q -- "--bsg-" src/cli/slash-command/bs/templates/DESIGN.template.md && grep -q "placeholder-in-use" src/cli/slash-command/bs/templates/ASSETS.template.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist as full standalone content (not thin pointers).
    - DESIGN.template.md contains: direction+rationale (Adopt/Derive/Original), `--bsg-*`/`applyTheme()` overrides, typography/spacing, component recipes, placeholder policy, do/don't list, and the "color literals only in the theme block" rule.
    - ASSETS.template.md ledger has the exact columns: needed-by-chunk, requested, received, placeholder-in-use, file path; guidance names ingest + per-chunk ask as populating steps.
    - Fill-in guidance is HTML comments retained after filling; no real game content baked in.
  </acceptance_criteria>
  <done>DESIGN and ASSETS templates transcribe their full checklists including the theme-block color rule and the five asset-ledger columns.</done>
</task>

<task type="auto">
  <name>Task 3: Complete templates.test.ts and run the full suite</name>
  <files>src/cli/slash-command/bs/templates.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/templates.test.ts (from Plans 01-02) — extend at the reserved ledger placeholder; do not restructure existing blocks
    - .planning/phases/141-file-templates-state-machine-authority/141-RESEARCH.md — Test Map (describe naming by requirement ID), Pitfall 1 (assert plan-specific phrases, not just headings)
    - the four new template files from Tasks 1-2
  </read_first>
  <action>
    At the reserved placeholder, add a `describe('TMPL-01 — six templates ship with required content', ...)` (or extend the existing TMPL-01 block) that: asserts all six template files exist and read non-empty; asserts RULINGS.template.md mentions the citation-per-entry requirement; DECISIONS.template.md mentions invariants; DESIGN.template.md contains `--bsg-` and the theme-block color rule phrase; ASSETS.template.md contains the exact column labels `needed-by-chunk`, `requested`, `received`, `placeholder-in-use`, and `file path`. Add a `describe('TMPL-02 — each template declares its parse contract', ...)` asserting the two status-bearing templates expose their `Status:`/derived-status grammar and each ledger states its required sections (existence of the plan-named headings). Also add a guard that none of the six `.template.md` files is a `{{BOARDSMITH_ROOT}}` thin pointer (assert `not.toContain('{{BOARDSMITH_ROOT}}')`). Plain string matching; no new dependency. Then run the full suite.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/templates.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/templates.test.ts` GREEN and full `npm test` GREEN (no collateral breakage).
    - Tests assert existence + required-content for all six templates; ASSETS column labels asserted verbatim.
    - A guard asserts no `.template.md` is a `{{BOARDSMITH_ROOT}}` thin pointer.
    - Existing Plan 01/02 assertions still pass; describe-block names include TMPL-01 and TMPL-02 for `-t` selection.
    - No markdown-parser import; no package.json change.
  </acceptance_criteria>
  <done>Drift test guards all eight shipped files; full suite green, closing TMPL-01/02/03 structurally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. Four static markdown ledgers + a content-assertion test completion. No runtime, no inputs, no network.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-03 | S/T/R/I/D/E | ledger template files + vitest assertions | accept | No attack surface: no runtime execution of content, no inputs, no network, no privilege boundary. |
| T-141-SC | Tampering | npm/pip/cargo installs | N/A | Zero packages installed (vitest ^2.1.0 + Node built-ins already present). No supply-chain surface. |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/templates.test.ts` green; `npm test` green.
- `ls src/cli/slash-command/bs/templates/` shows all six `.template.md` files.
- `grep -L "{{BOARDSMITH_ROOT}}" src/cli/slash-command/bs/templates/*.template.md` lists all six (none are thin pointers).
</verification>

<success_criteria>
- All six templates + state-machine.md exist with every plan-specified field.
- templates.test.ts guards all eight files; full suite green (TMPL-01/02/03 structurally satisfied).
</success_criteria>

<output>
Create `.planning/phases/141-file-templates-state-machine-authority/141-03-SUMMARY.md` when done.
</output>
