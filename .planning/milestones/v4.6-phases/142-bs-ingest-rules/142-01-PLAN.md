---
phase: 142-bs-ingest-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/slash-command/bs/ingest.test.ts
  - src/cli/slash-command/bs/ingest-rules.md
autonomous: true
requirements: [INGEST-02, INGEST-06, INGEST-07]
must_haves:
  truths:
    - "Running `npx vitest run src/cli/slash-command/bs/ingest.test.ts -t \"INGEST-02\"` passes"
    - "Running the same suite with -t \"INGEST-06\" passes"
    - "Running the same suite with -t \"INGEST-07\" passes"
    - "The drift suite contains a describe block for each of INGEST-01..07 plus a cross-file consistency block"
    - "The orchestrator skill file cites state-machine.md / templates / the four ingest/*.md reference files by exact path rather than restating their content"
  artifacts:
    - path: "src/cli/slash-command/bs/ingest.test.ts"
      provides: "Structural drift test: one describe per INGEST-01..07 + cross-file consistency (existsSync) block"
      contains: "INGEST-07"
      min_lines: 120
    - path: "src/cli/slash-command/bs/ingest-rules.md"
      provides: "Lean orchestrator skill: state detection (INGEST-07), synthesis artifact list (INGEST-02), UI strategy step (INGEST-06), approval gate, context-economics hard rule, citations to reference files"
      contains: "ingest/transcription.md"
      min_lines: 80
  key_links:
    - from: "src/cli/slash-command/bs/ingest.test.ts"
      to: "src/cli/slash-command/bs/ingest-rules.md"
      via: "readFileSync(join(__dirname, 'ingest-rules.md'))"
      pattern: "ingest-rules\\.md"
    - from: "src/cli/slash-command/bs/ingest-rules.md"
      to: "src/cli/slash-command/bs/state-machine.md"
      via: "citation pointer (never restatement)"
      pattern: "state-machine\\.md"
---

<objective>
Create this phase's structural drift test (`bs/ingest.test.ts`) and the lean orchestrator skill file (`bs/ingest-rules.md`). The test is authored FIRST (Wave 0 gap in 142-VALIDATION.md) and contains a describe block for every INGEST-01..07 requirement plus a cross-file consistency block — most blocks are RED until Plans 02/03 author the referenced reference files, which is expected. The orchestrator is the phase's main deliverable: a thin router that detects project state, dispatches to transcription/interview, synthesizes the ingest artifacts, runs the approval + UI-strategy gates, and delegates all heavyweight prose to `bs/ingest/*.md` files it cites by path.

Purpose: Establish the goal-backward verification harness and the top-level skill so Plans 02/03 author reference files whose per-requirement describe blocks already exist and turn green as each file lands.
Output: `src/cli/slash-command/bs/ingest.test.ts`, `src/cli/slash-command/bs/ingest-rules.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-bs-ingest-rules/142-CONTEXT.md
@.planning/phases/142-bs-ingest-rules/142-RESEARCH.md
@.planning/phases/142-bs-ingest-rules/142-PATTERNS.md
@.planning/bs-skills-plan.md

<interfaces>
<!-- The drift-test pattern this test file MUST mirror. Extracted from the Phase 141 analog. -->
From src/cli/slash-command/bs/templates.test.ts (read in full before writing — it is the direct analog):
- `read(relativePath)` helper: `readFileSync(join(__dirname, relativePath), 'utf-8')` with `__dirname` from `fileURLToPath(import.meta.url)`.
- Named byte-identical marker constants shared across files, e.g.:
  - `SKETCH_LEVEL_MARKER = 'Status: proposed (sketch-level — no CHUNK.md yet)'` (em-dash, NOT hyphen)
  - `DERIVED_POINTER_GRAMMAR = 'Status (derived from chunks/<slug>/CHUNK.md):'`
  - `ui:` tag regex `/none *\| *touches *\| *major/`
- One `describe('TMPL-XX — ...')` block per rule cluster, content loaded once at top of block via `const x = read(...)`.

<!-- Contract strings the orchestrator must cite (NOT restate). Verified against source. -->
From src/cli/slash-command/bs/state-machine.md (Phase 141): "cite this file rather than restating its rules."
From src/cli/slash-command/bs/templates/SKETCH.template.md (Phase 141): "## UI Strategy" section already exists — ingest fills `Strategy: custom-from-chunk-1` / cutover, does not invent the field (INGEST-06).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author bs/ingest.test.ts drift suite (Wave 0)</name>
  <files>src/cli/slash-command/bs/ingest.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/templates.test.ts (FULL — the direct analog; copy the read() helper + marker-constant + describe-per-requirement patterns)
    - .planning/phases/142-bs-ingest-rules/142-RESEARCH.md ("Validation Architecture" → Phase Requirements → Test Map; Pattern 2)
    - .planning/phases/142-bs-ingest-rules/142-PATTERNS.md (the `ingest.test.ts` section, lines ~190-229 — exact patterns to copy)
    - src/cli/slash-command/bs/templates/SKETCH.template.md (for the byte-identical marker strings the sketch-derivation assertions pin)
  </read_first>
  <behavior>
    - `describe('INGEST-01 ...')`: ingest-rules.md references `ingest/transcription.md` and its per-section (not per-page, not bulk) confirmation protocol.
    - `describe('INGEST-02 ...')`: ingest-rules.md synthesis section names ALL SIX sub-artifacts by anchor — `INDEX.md`, variant/edition tagging, component inventory + aspect ratio(s), `ASSETS.md`, visual identity survey, player-count/min-max.
    - `describe('INGEST-03 ...')`: ingest-rules.md references `ingest/interview-fallback.md`; interview-fallback.md's stated Outputs are `rulebook/` files (assert the string `rulebook/` and NOT a PROJECT.md-prose output). (RED until Plan 02.)
    - `describe('INGEST-04 ...')`: ingest-rules.md references `ingest/scaffold.md`; scaffold.md names `boardsmith init`, `tsc --noEmit`, and an explicit kill instruction after the serve check. (RED until Plan 03.)
    - `describe('INGEST-05 ...')`: ingest-rules.md references `ingest/sketch-derivation.md`; sketch-derivation.md contains the byte-identical `SKETCH_LEVEL_MARKER` and the `ui:` none|touches|major tag values and the core-event-loop-first + game-end + final-acceptance mandate. (RED until Plan 03.)
    - `describe('INGEST-06 ...')`: ingest-rules.md's UI-strategy step references SKETCH.template.md's `## UI Strategy` section by name (not a re-invented field), and names both strategy values (`custom-from-chunk-1`, autoui-with-cutover).
    - `describe('INGEST-07 ...')`: ingest-rules.md state-detection step names BOTH the re-run guard (existing `SKETCH.md`) and the old-project migration trio (`PROJECT.md`, `STATE.md`, `HISTORY.md`).
    - `describe('cross-file consistency ...')`: iterate every path ingest-rules.md references — `ingest/transcription.md`, `ingest/interview-fallback.md`, `ingest/sketch-derivation.md`, `ingest/scaffold.md`, `state-machine.md`, `templates/SKETCH.template.md`, `templates/ASSETS.template.md` — assert `existsSync(join(__dirname, p))` true for each. (RED until all files land in Plans 02/03.)
  </behavior>
  <action>Create `src/cli/slash-command/bs/ingest.test.ts` mirroring `templates.test.ts` exactly: same imports, same `read()` helper, same named-constant technique for byte-identical markers (`SKETCH_LEVEL_MARKER`, `ui:` regex) so a future reword of the shared marker fails loudly. Author one `describe` block per INGEST-01..07 per the behavior list above, plus a `cross-file consistency` describe using `existsSync`. Do NOT add a markdown parser — plain `.toContain`/regex string assertions only. Expect the suite to be mostly RED after this task (INGEST-03/04/05 and consistency assert files authored in Plans 02/03); that is the intended Wave-0-first state. This test is the SOLE owner of `ingest.test.ts` — no later plan modifies it.</action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/ingest.test.ts 2>&1 | grep -qE "INGEST-01|INGEST-07" && npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "INGEST-02" </automated>
  </verify>
  <done>The file exists, the suite is collectable by vitest (no syntax/import errors), and grepping its output shows all seven INGEST-NN describe blocks plus the consistency block are present. INGEST-02/06/07 blocks may still be red until Task 2 authors the orchestrator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author bs/ingest-rules.md orchestrator</name>
  <files>src/cli/slash-command/bs/ingest-rules.md</files>
  <read_first>
    - .planning/bs-skills-plan.md (§"1. `/bs-ingest-rules`" lines 54-80; §UI lines 111-127; §Reuse line 165; §Distribution)
    - src/cli/slash-command/bs/state-machine.md (FULL — the citation-not-restatement style; cite its Consistency Check / session-lock / status-enum sections, never restate)
    - src/cli/slash-command/bs/templates/SKETCH.template.md (its "## UI Strategy" and "Mandated Chunks" section names — cite by name for INGEST-06)
    - src/cli/slash-command/instructions.md lines 39-89 (State Detection — adapt to THREE cases; use `ls <file>` direct check, NOT `**/glob`)
    - .planning/phases/142-bs-ingest-rules/142-PATTERNS.md (the `ingest-rules.md` orchestrator section, lines ~20-51)
  </read_first>
  <behavior>
    - INGEST-07: a "State Detection" step names the three on-entry cases — empty dir (proceed), existing bs- project (`SKETCH.md` present → re-run guard requiring explicit confirmation since re-ingest is destructive), old `/design-game` project (`PROJECT.md`+`STATE.md`+`HISTORY.md` present → one-time conversion offer). Cites state-machine.md's Consistency Check rather than reimplementing it.
    - INGEST-02: a "Synthesis" step (orchestrator-only, from subagent summaries) names all six sub-artifacts: `rulebook/INDEX.md`, variant/edition tagging, component inventory with aspect ratios → `ASSETS.md` seed, visual identity survey (evidence only, no decision), min/max player counts + per-count setup diffs.
    - INGEST-06: a "UI Strategy" step made WITH the user, recorded into SKETCH.template.md's `## UI Strategy` section (named, not re-invented); names both `custom-from-chunk-1` (default) and autoui-with-cutover.
    - Approval gate: presents estimated chunk count + rough per-chunk wall time; only next 2-3 chunks detailed; user edits → revise → approve; user ordering wins unless a named hard dependency is violated.
    - Context-economics HARD RULE stated in the orchestrator itself: the orchestrator never reads full rulebook slices; INDEX.md is built exclusively from subagent-returned citedTerms lists.
    - Delegation: cites `ingest/transcription.md`, `ingest/interview-fallback.md`, `ingest/sketch-derivation.md`, `ingest/scaffold.md` by exact path for the heavyweight prose of each step.
  </behavior>
  <action>Author `src/cli/slash-command/bs/ingest-rules.md` as a LEAN orchestrator (target well under a few hundred lines — the 3,072-line instructions.md monolith is the anti-pattern being replaced). Open with the citation-not-restatement convention (mirror state-machine.md line 3). Structure the flow as: 0 State Detection (INGEST-07) → 1 route to transcription (cite ingest/transcription.md) or interview-fallback (cite ingest/interview-fallback.md) → 2 Synthesis (INGEST-02, all six artifacts) → 3 Scaffold+verify (cite ingest/scaffold.md) → 4 Sketch derivation (cite ingest/sketch-derivation.md) → 5 UI strategy (INGEST-06, fill SKETCH.template.md `## UI Strategy`) → 6 Approval gate → 7 Write files from templates (never restate template content). State the context-economics hard rule explicitly in the orchestrator body. Cite every referenced path exactly so the consistency test resolves; do NOT restate template or state-machine content inline.</action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "INGEST-02"; npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "INGEST-06"; npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "INGEST-07"</automated>
  </verify>
  <done>INGEST-02, INGEST-06, INGEST-07 describe blocks pass. The orchestrator cites all four ingest/*.md paths (consistency block will fully green only after Plans 02/03 create those files). No template or state-machine content is restated inline (grep confirms pointers, not copied rule text).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| designer-supplied rulebook (PDF/image/text) → Claude session | Untrusted content crosses here AT RUNTIME when the skill is executed by a designer. This phase ships only the static markdown instructions + a content-assertion test; no runtime code path is introduced. Runtime content-interpretation safety is a Phase 149 (dry-run) concern, not a code surface added here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-01 | Tampering | ingest.test.ts drift markers | mitigate | Byte-identical marker constants pin shared strings across ingest-rules.md / ingest/*.md and SKETCH.template.md so silent drift fails the suite loudly (same technique as templates.test.ts). |
| T-142-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: "no packages recommended"). vitest is already a devDependency. |
| T-142-02 | Information Disclosure | runtime rulebook interpretation | accept | No runtime code shipped; markdown-only. Runtime PDF/image handling is deferred to Phase 149's behavioral dry-run. Low risk: no network/auth/datastore surface introduced. |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "INGEST-02"` — green
- `npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "INGEST-06"` — green
- `npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "INGEST-07"` — green
- INGEST-01/03/04/05 + consistency blocks present but expected RED until Plans 02/03 (do not gate this plan on them)
- No regression to Phase 141: `npx vitest run src/cli/slash-command/bs/templates.test.ts` stays green
</verification>

<success_criteria>
- `bs/ingest.test.ts` exists with all seven INGEST describe blocks + a cross-file consistency block, collectable by vitest
- `bs/ingest-rules.md` exists, is a lean orchestrator that cites (never restates) state-machine.md/templates and delegates to the four ingest/*.md paths
- INGEST-02, INGEST-06, INGEST-07 assertions pass
</success_criteria>

<output>
Create `.planning/phases/142-bs-ingest-rules/142-01-SUMMARY.md` when done
</output>
