---
phase: 142-bs-ingest-rules
plan: 02
type: execute
wave: 2
depends_on: [142-01]
files_modified:
  - src/cli/slash-command/bs/ingest/transcription.md
  - src/cli/slash-command/bs/ingest/interview-fallback.md
autonomous: true
requirements: [INGEST-01, INGEST-03]
must_haves:
  truths:
    - "Running `npx vitest run src/cli/slash-command/bs/ingest.test.ts -t \"INGEST-01\"` passes"
    - "Running the same suite with -t \"INGEST-03\" passes"
    - "transcription.md dispatches fan-out subagents per page range and confirms per rulebook section (not per page, not one bulk gate)"
    - "interview-fallback.md's stated Outputs are the same rulebook/ files as the transcription path, not PROJECT.md prose"
  artifacts:
    - path: "src/cli/slash-command/bs/ingest/transcription.md"
      provides: "Fan-out subagent-prompt spec: per-page-range dispatch, structured return (sectionText/citedTerms/componentMentions), per-section user confirmation, orchestrator context-economics hard rule"
      contains: "citedTerms"
      min_lines: 40
    - path: "src/cli/slash-command/bs/ingest/interview-fallback.md"
      provides: "Extracted old-skill Phase 2 six-question sequence, re-targeted to write rulebook/NN-topic.md + INDEX.md with 'designer statement, ingest session' citations; points at aspects/index.md rather than recopying the aspect table"
      contains: "rulebook/"
      min_lines: 50
  key_links:
    - from: "src/cli/slash-command/bs/ingest/interview-fallback.md"
      to: "src/cli/slash-command/aspects/index.md"
      via: "citation pointer (never restatement)"
      pattern: "aspects/index\\.md"
    - from: "src/cli/slash-command/bs/ingest-rules.md"
      to: "src/cli/slash-command/bs/ingest/transcription.md"
      via: "orchestrator delegation pointer (created in Plan 01)"
      pattern: "ingest/transcription\\.md"
---

<objective>
Author the two input-path reference files the orchestrator delegates to: `ingest/transcription.md` (written-rulebook fan-out path, INGEST-01) and `ingest/interview-fallback.md` (no-rulebook path, INGEST-03). Both must produce the SAME `rulebook/NN-topic.md` + `INDEX.md` output shape — that identity is the core of INGEST-03. transcription.md is net-new authored from RESEARCH.md Pattern 1; interview-fallback.md is a verbatim extraction of the old skill's Phase 2 question sequence, re-targeted from PROJECT.md-prose output to rulebook/ files.

Purpose: Turn the INGEST-01 and INGEST-03 describe blocks (authored RED in Plan 01) green.
Output: `src/cli/slash-command/bs/ingest/transcription.md`, `src/cli/slash-command/bs/ingest/interview-fallback.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142-bs-ingest-rules/142-CONTEXT.md
@.planning/phases/142-bs-ingest-rules/142-RESEARCH.md
@.planning/phases/142-bs-ingest-rules/142-PATTERNS.md
@.planning/bs-skills-plan.md

<interfaces>
<!-- Subagent dispatch/return shape to author transcription.md from. Source: RESEARCH.md Pattern 1. -->
Dispatch one subagent per rulebook page range (pp. 1-8, 9-16, ...):
  Return: { sectionText, citedTerms[], componentMentions[] }
Orchestrator: writes sectionText → rulebook/NN-topic.md; accumulates citedTerms[] → INDEX.md
  WITHOUT re-reading the slice; accumulates componentMentions[] → ASSETS.md seed; confirms each
  section's summary with the user BEFORE the next page range (batched per-section, not per-page, not bulk).

<!-- Old-skill Phase 2 extraction source (verbatim question sequence). Verified line map. -->
instructions.md lines 121-223: Q1 Open Vision (125-135), Q2 Components (136-156), Q3 Turn Structure
  (157-163), Q4 Round Completion (164-171), Q5 Game End (172-180), Q6 Summary+Confirm (181-223).
instructions.md lines 224-260: Phase 2B Aspect Detection table — DO NOT recopy; point at aspects/index.md.
Citation format for interview path: "designer statement, ingest session, Q{n}".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ingest/transcription.md (fan-out, INGEST-01)</name>
  <files>src/cli/slash-command/bs/ingest/transcription.md</files>
  <read_first>
    - .planning/phases/142-bs-ingest-rules/142-RESEARCH.md ("Pattern 1: Orchestrator-delegates-to-subagents" and "Pitfall 2")
    - .planning/phases/142-bs-ingest-rules/142-PATTERNS.md (the `ingest/transcription.md` section, lines ~166-186)
    - .planning/bs-skills-plan.md lines 58-66 (Input modes → Written rulebook; "Ingest respects its own context economics"; Outputs)
    - src/cli/slash-command/bs/state-machine.md (citation style to reuse for the ASSETS.md / SKETCH.md pointers)
  </read_first>
  <action>Author `src/cli/slash-command/bs/ingest/transcription.md` from RESEARCH.md Pattern 1 (no in-repo analog exists). Specify: (1) fan-out one Task subagent per rulebook page range; each returns the structured summary `{ sectionText, citedTerms[], componentMentions[] }` with page/section citations; (2) the orchestrator writes `sectionText` to `rulebook/NN-topic.md`, accumulates `citedTerms[]` into `rulebook/INDEX.md` (term → slice) WITHOUT re-reading the slice it just wrote, and accumulates `componentMentions[]` (with aspect ratios) into the ASSETS.md seed; (3) per-section user confirmation ("here's what I read on pages N-M — correct?") batched by rulebook section, explicitly NOT per-page and NOT one bulk gate; (4) variant/optional/advanced rules tagged out-of-scope-by-default in the slice. State the context-economics HARD RULE prominently here (this is the step where the temptation to "double-check by re-reading the slice" is strongest — Pitfall 2). Cite ASSETS.template.md / SKETCH.template.md by path for downstream shape; never restate their content.</action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "INGEST-01"</automated>
  </verify>
  <done>INGEST-01 describe block passes: transcription.md exists, dispatches per-page-range fan-out, states the per-section (not per-page/bulk) confirmation protocol and the citedTerms-only INDEX rule.</done>
</task>

<task type="auto">
  <name>Task 2: Author ingest/interview-fallback.md (INGEST-03)</name>
  <files>src/cli/slash-command/bs/ingest/interview-fallback.md</files>
  <read_first>
    - src/cli/slash-command/instructions.md lines 121-260 (Phase 2 six-question sequence + Phase 2B aspect table — extraction source)
    - src/cli/slash-command/aspects/index.md (the canonical aspect table to POINT AT, not recopy)
    - .planning/phases/142-bs-ingest-rules/142-RESEARCH.md ("Old-Skill Extraction Map" row Phase 2; "Pitfall 4")
    - .planning/phases/142-bs-ingest-rules/142-PATTERNS.md (the `interview-fallback.md` section, lines ~54-77)
    - .planning/bs-skills-plan.md line 60 (No rulebook → interview fallback produces the SAME rulebook/ files)
  </read_first>
  <action>Author `src/cli/slash-command/bs/ingest/interview-fallback.md` by extracting the old skill's Phase 2 six-question sequence (instructions.md:121-223) verbatim in shape — one-question-at-a-time, blockquoted prompt, "Listen for:" bullets, Clarification Protocol — for vision, components, turn structure, round completion, game end, summary+confirm. Apply the ONE required adaptation (Pitfall 4): re-target the OUTPUT from PROJECT.md prose to the SAME `rulebook/NN-topic.md` + `rulebook/INDEX.md` files the transcription path produces — each answer becomes a cited slice with citation `"designer statement, ingest session, Q{n}"`, with terms fed into INDEX.md exactly like the transcription path's citedTerms. The file's "Outputs" section MUST name `rulebook/` file paths, NOT PROJECT.md fields. For aspect detection, POINT AT `aspects/index.md` (cite, do not recopy the keyword table). Do not modify instructions.md (Phase 148 owns its removal).</action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "INGEST-03"</automated>
  </verify>
  <done>INGEST-03 describe block passes: interview-fallback.md exists, is referenced by the orchestrator, states `rulebook/` files as its Outputs (not PROJECT.md prose), uses the "designer statement, ingest session" citation format, and points at aspects/index.md rather than recopying it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| designer-supplied rulebook / interview answers → Claude session | Untrusted content crosses at RUNTIME only. This plan ships static markdown reference prose; no runtime code path is added. Runtime safety is a Phase 149 concern. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-03 | Tampering | interview-fallback output shape | mitigate | INGEST-03 drift assertion pins that Outputs are `rulebook/` files, not PROJECT.md prose — the exact Pitfall-4 regression is caught by the suite. |
| T-142-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase. |
| T-142-02 | Information Disclosure | runtime content interpretation | accept | Markdown-only; no network/auth/datastore surface. Deferred to Phase 149 dry-run. |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "INGEST-01"` — green
- `npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "INGEST-03"` — green
- Full suite: `npm test` — no Phase 141 regressions
</verification>

<success_criteria>
- `ingest/transcription.md` and `ingest/interview-fallback.md` exist and are cited by the orchestrator
- Both paths produce the identical `rulebook/` + `INDEX.md` output shape
- INGEST-01 and INGEST-03 assertions pass
</success_criteria>

<output>
Create `.planning/phases/142-bs-ingest-rules/142-02-SUMMARY.md` when done
</output>
