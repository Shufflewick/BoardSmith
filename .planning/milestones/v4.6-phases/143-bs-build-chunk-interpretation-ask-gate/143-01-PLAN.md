---
phase: 143-bs-build-chunk-interpretation-ask-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/slash-command/bs/build-chunk.test.ts
autonomous: true
requirements: [BUILD-01, BUILD-02, BUILD-03, BUILD-04, BUILD-12]
must_haves:
  truths:
    - "A structural drift suite exists that fails RED until build-chunk.md and build/{investigate,redteam,ask}.md are authored"
    - "Every byte-identical string the new markdown must share with state-machine.md is pinned as a constant"
    - "The subagent return-shape field names investigate/redteam producers and the build-chunk.md consumer must agree on are fixed here"
  artifacts:
    - path: "src/cli/slash-command/bs/build-chunk.test.ts"
      provides: "BUILD-01..04 + BUILD-12 describe blocks, cross-file consistency, return-shape pins"
      contains: "describe('BUILD-01"
      min_lines: 200
  key_links:
    - from: "src/cli/slash-command/bs/build-chunk.test.ts"
      to: "src/cli/slash-command/bs/state-machine.md"
      via: "byte-identical step-list / status-enum / stale-marker constants"
      pattern: "investigate, redteam, ask, build, test, audit, repair, playtest, revise, close"
    - from: "src/cli/slash-command/bs/build-chunk.test.ts"
      to: "src/cli/slash-command/bs/build/{investigate,redteam,ask}.md"
      via: "REFERENCED_PATHS existence + cross-citation assertions"
      pattern: "build/investigate.md"
---

<objective>
Author `src/cli/slash-command/bs/build-chunk.test.ts` — the structural drift-protection suite for
the `/bs-build-chunk` skill — as a Wave 0 scaffold, mirroring `ingest.test.ts` byte-for-byte in
technique. This test is the interface contract for the entire phase: it fixes the byte-identical
step-name/status-enum/stale-marker strings, the subagent return-shape field names, and the
cross-file existence/citation assertions that Plans 02–05 must satisfy.

Purpose: The test is written first (interface-first) so the markdown authored in Plans 02–05 has a
concrete, executable definition of "done." It fails RED now (the markdown files do not exist yet)
and turns GREEN plan-by-plan as each file is authored.
Output: One new `.test.ts` file. No markdown yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-CONTEXT.md
@.planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-RESEARCH.md
@.planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-PATTERNS.md
@src/cli/slash-command/bs/ingest.test.ts
@src/cli/slash-command/bs/state-machine.md
@src/cli/slash-command/bs/templates/CHUNK.template.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold build-chunk.test.ts — full structural drift suite (RED)</name>
  <files>src/cli/slash-command/bs/build-chunk.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/ingest.test.ts — the exact analog: copy the file-header comment, the `__dirname`/`read()` helper (lines 26–35), the `SKETCH_LEVEL_MARKER`/`UI_TAG_REGEX` constants (lines 42–45), the `REFERENCED_PATHS` array + existence loop (lines 47–56, 282–295), the `RETURN_SHAPE_FIELDS` pin pattern (lines 195–224), and the per-requirement `describe` block shape (every `read()` INSIDE its `it()` body, never describe-level)
    - src/cli/slash-command/bs/state-machine.md — source of the byte-identical strings to pin: Status Enum (line 9), full-ceremony step list (line 27), light-path step list (line 33), stale marker (line 19, em-dash), session-handoff groups (lines 137–140), Redteam Escalation (129–131), Repair Loop Bound (123–124)
    - src/cli/slash-command/bs/templates/CHUNK.template.md — the section names investigate/ask fill: `## Interpretation`, `## Visibility Declaration`, `## Newly Discovered Citations`, `## Ceremony`, `## Step Checklist`
    - .planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-PATTERNS.md — the `build-chunk.test.ts` section (lines 248–341) enumerates every constant and describe block to port
  </read_first>
  <action>
    Create `src/cli/slash-command/bs/build-chunk.test.ts` following `ingest.test.ts` verbatim in structure.

    File header comment: adapt ingest.test.ts's header — state that `build-chunk.md` + its
    `build/*.md` reference files are agent-consumed markdown NOT parsed by runtime code; this suite
    pins strings/citations/cross-file pointers; every `read()` is inside its `it()` so a missing
    file fails only that assertion; note explicitly that BUILD-05..11 assertions do NOT exist yet
    (those steps are authored in Phases 144–146) and Steps 4–10 are asserted only as
    forward-reference stub text in the routing table, per 143-CONTEXT.md.

    Copy the `read()` helper verbatim (same `bs/` directory, same relative resolution).

    Pin these byte-identical constants (quoted verbatim from state-machine.md):
      - `FULL_CEREMONY_STEPS = 'investigate, redteam, ask, build, test, audit, repair, playtest, revise, close'`
      - `LIGHT_PATH_STEPS = 'build, test, playtest'`
      - `STALE_MARKER = 'stale — re-derive before build'` (em-dash, not hyphen)
      - reuse `SKETCH_LEVEL_MARKER` and `UI_TAG_REGEX` from ingest.test.ts verbatim
      - the status enum values `proposed`, `approved`, `built`, `verified`, `verified (user-waived)`

    Define the return-shape field-name contract (Claude's-discretion names, per 143-CONTEXT.md —
    fix them HERE so producers and consumer agree). Use the names from 143-RESEARCH.md Code
    Examples:
      - `INVESTIGATE_RETURN_FIELDS = ['claimsList', 'visibilityDeclaration', 'newlyDiscoveredCitations']`
      - `REDTEAM_REFUTER_FIELDS = ['claimNumber', 'verdict', 'objection']`
      - `REDTEAM_COVERAGE_FIELDS = ['missingInteractions', 'ruleDescription', 'citation']`

    Define `REFERENCED_PATHS` scoped to CURRENT-PHASE files only (per 143-CONTEXT.md "existence
    check covers only files due by the current phase"):
      `build/investigate.md`, `build/redteam.md`, `build/ask.md`, `state-machine.md`,
      `templates/CHUNK.template.md`, `templates/RULINGS.template.md`, `templates/ASSETS.template.md`.
    Do NOT put `build/build.md`, `build/test.md`, `build/audit.md`, `build/repair.md`,
    `build/playtest.md`, `build/revise.md`, `build/close.md` in this existence-checked array.

    Write one `describe` block per requirement:
      - `describe('BUILD-01 — resume routing', …)`: build-chunk.md contains the full-ceremony step
        list verbatim (FULL_CEREMONY_STEPS); asserts routing on "first incomplete step",
        awaiting-playtest re-pose, and the conversational-intent probe (matches /what.?s left/i and
        an insert-intent phrase); asserts the 3-way session-lock branch language (stale / same-chunk
        resume / different live lock).
      - `describe('BUILD-02 — investigate', …)`: build/investigate.md names all six required docs
        (`docs/core-concepts.md`, `docs/common-pitfalls.md`, `docs/actions-and-flow.md`,
        `docs/custom-ui-guide.md`, `docs/ui-components.md`, `docs/dice-and-scoring.md`) plus
        `DESIGN.md`; reads `RULINGS.md` and `DECISIONS.md`; produces `## Interpretation` +
        `## Visibility Declaration` + `## Newly Discovered Citations`; defines INVESTIGATE_RETURN_FIELDS.
      - `describe('BUILD-03 — redteam', …)`: build/redteam.md documents 3 fresh-context agents
        (2 refuters + 1 coverage adversary), "default to refuted", cites state-machine.md
        "Redteam Escalation" (refuted-once → max 1 re-investigate round; refuted-twice → user +
        RULINGS.md), never shows raw verdicts; defines REDTEAM_REFUTER_FIELDS + REDTEAM_COVERAGE_FIELDS.
      - `describe('BUILD-04 — ask gate', …)`: build/ask.md documents the 4-part format (a/b/c/d),
        warns against implementation vocabulary (assert it contains the literal forbidden terms
        list including `action`, `flow`, `state`, `element` as things to AVOID), never-blocking
        placeholder path, gate-before-write (Status: approved written last / after explicit approval).
      - `describe('BUILD-12 — light path', …)`: build-chunk.md contains LIGHT_PATH_STEPS verbatim,
        states the user is told which ceremony is in effect, and notes `approved` is unreachable on
        the light path (proposed → built).

    Add cross-file consistency describe blocks (mirror ingest.test.ts 251–295):
      - every `REFERENCED_PATHS` entry exists on disk AND is cited by exact path string in build-chunk.md
      - Steps 4–10 forward-reference assertion: build-chunk.md's routing table names each of
        `build/{build,test,audit,repair,playtest,revise,close}.md` as plain text carrying the
        literal marker `authored in Phase 144`, `authored in Phase 145`, or `authored in Phase 146`
        (assert the three marker strings appear); do NOT assert those files exist.
      - RETURN_SHAPE_FIELDS pinned across producer + consumer: INVESTIGATE_RETURN_FIELDS defined in
        build/investigate.md AND consumed by the same names in build-chunk.md; REDTEAM_* fields
        defined in build/redteam.md AND consumed in build-chunk.md.

    Do NOT create any markdown files in this task — the suite is expected to be RED.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts 2>&1 | grep -Eq "BUILD-01|BUILD-12"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts` collects and runs (no syntax/collection error); assertions FAIL RED because the markdown files do not exist yet — this is the expected Wave 0 state
    - `grep -c "describe('BUILD-0" src/cli/slash-command/bs/build-chunk.test.ts` returns 4 (BUILD-01,02,03,04) and a fifth block for BUILD-12 is present (`grep -c "describe('BUILD-12" ` returns 1)
    - `grep -F "investigate, redteam, ask, build, test, audit, repair, playtest, revise, close" src/cli/slash-command/bs/build-chunk.test.ts` matches (full-ceremony pin present)
    - `grep -F "stale — re-derive before build" src/cli/slash-command/bs/build-chunk.test.ts` matches with an em-dash
    - `grep -F "authored in Phase 144" src/cli/slash-command/bs/build-chunk.test.ts` matches (forward-reference stub assertion present)
    - `REFERENCED_PATHS` contains `build/investigate.md`, `build/redteam.md`, `build/ask.md` and does NOT contain `build/build.md`
  </acceptance_criteria>
  <done>The drift suite exists, runs, defines all pinned constants + the return-shape field-name contract, and fails RED pending Plans 02–05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This phase authors LLM-instruction markdown + one Vitest file. No auth, network, session, or user-input-parsing surface is introduced (143-RESEARCH.md Security Domain: genuine no-applicable-category). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Tampering | Cross-file string drift (step names / enum) between build-chunk.md and state-machine.md | mitigate | This test file IS the mitigation: byte-identical constant pins fail CI on any drift |
| T-143-SC | Tampering | npm/pip/cargo installs | N/A | Zero package installs this phase — imports only `vitest` + Node builtins already present (143-RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
`npx vitest run src/cli/slash-command/bs/build-chunk.test.ts` executes; RED is the expected and
correct outcome for this plan. Full green arrives across Plans 02–05.
</verification>

<success_criteria>
Test file authored, runs, pins every phase string + return-shape contract, RED pending markdown.
</success_criteria>

<output>
Create `.planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-01-SUMMARY.md` when done.
Record the exact return-shape field names chosen (INVESTIGATE_RETURN_FIELDS, REDTEAM_* fields) so
Plans 02–05 use them verbatim.
</output>
