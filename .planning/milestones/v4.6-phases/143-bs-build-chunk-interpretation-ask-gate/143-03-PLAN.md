---
phase: 143-bs-build-chunk-interpretation-ask-gate
plan: 03
type: execute
wave: 2
depends_on: [143-01]
files_modified:
  - src/cli/slash-command/bs/build/investigate.md
autonomous: true
requirements: [BUILD-02]
must_haves:
  truths:
    - "Investigate reads cited slices plus INDEX-discovered slices, RULINGS.md, DECISIONS.md, the required BoardSmith docs, and DESIGN.md for ui: touches|major chunks"
    - "Investigate produces a numbered, citation-backed claims list plus an explicit hidden-information visibility declaration"
    - "The investigate subagent writes CHUNK.md's sections directly; the orchestrator never re-reads a slice to double-check"
  artifacts:
    - path: "src/cli/slash-command/bs/build/investigate.md"
      provides: "Investigate step reference: context-economics hard rule, fan-out dispatch, required-reading doc list, subagent-writes-CHUNK.md-directly, structured return shape, re-investigate append behavior, Downstream Shape footer"
      contains: "## Visibility Declaration"
      min_lines: 90
  key_links:
    - from: "src/cli/slash-command/bs/build/investigate.md"
      to: "src/cli/slash-command/bs/templates/CHUNK.template.md"
      via: "fills ## Interpretation / ## Visibility Declaration / ## Newly Discovered Citations, never restructures"
      pattern: "## Interpretation"
    - from: "src/cli/slash-command/bs/build/investigate.md"
      to: "src/cli/slash-command/bs/build/redteam.md"
      via: "Downstream Shape — the claims list feeds redteam"
      pattern: "redteam"
---

<objective>
Author `src/cli/slash-command/bs/build/investigate.md` — the investigate step reference file
(BUILD-02). It specifies how a fresh-context subagent reads the chunk's cited + INDEX-discovered
slices, RULINGS.md, DECISIONS.md, the required BoardSmith docs, and DESIGN.md (ui≠none), then writes
a numbered claims list + visibility declaration directly into CHUNK.md and returns only a structured
summary. Mirrors `ingest/transcription.md`.

Purpose: Investigate is the interpretation engine of the build-chunk pipeline and the durable owner
of the doc-reading discipline handed off from `ingest/scaffold.md`.
Output: One new markdown file. Makes the BUILD-02 test block GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-CONTEXT.md
@.planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-RESEARCH.md
@.planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-PATTERNS.md
@src/cli/slash-command/bs/ingest/transcription.md
@src/cli/slash-command/bs/ingest/scaffold.md
@src/cli/slash-command/bs/state-machine.md
@src/cli/slash-command/bs/templates/CHUNK.template.md
@src/cli/slash-command/bs/build-chunk.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author build/investigate.md (BUILD-02)</name>
  <files>src/cli/slash-command/bs/build/investigate.md</files>
  <read_first>
    - src/cli/slash-command/bs/ingest/transcription.md — the exact analog: Context-Economics Hard Rule restated at step level (lines 9–19), Fan-Out Dispatch prompt template + "subagent writes the slice files itself" (20–62), structured-return contract (60–61), "Orchestrator Records / never re-reads" section (112–129), Downstream Shape footer (142–147)
    - src/cli/slash-command/bs/ingest/scaffold.md — Required Reading Pointer section (lines 125–136): the exact doc filenames investigate now owns; it explicitly hands the full required-reading discipline to `/bs-build-chunk`'s investigate step
    - src/cli/slash-command/bs/templates/CHUNK.template.md — the sections investigate fills: `## Interpretation` (append-only, never renumber — line 65 area), `## Visibility Declaration`, `## Newly Discovered Citations`; the parse-contract comment
    - src/cli/slash-command/bs/state-machine.md — "Rulings Outrank Rulebook" (94–96): investigate also reads RULINGS.md
    - src/cli/slash-command/bs/build-chunk.test.ts — the pinned INVESTIGATE_RETURN_FIELDS the return shape must use verbatim (`claimsList`, `visibilityDeclaration`, `newlyDiscoveredCitations`) and the six required doc filenames the BUILD-02 block asserts
    - .planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-PATTERNS.md — investigate.md section (lines 102–164) with every sub-pattern's line refs
  </read_first>
  <action>
    Create `src/cli/slash-command/bs/build/investigate.md`, mirroring `ingest/transcription.md`.

    Header — Context-Economics Hard Rule restated at step level ("this is where the temptation is
    strongest"): the orchestrator never reads the cited/INDEX-discovered slices, RULINGS.md,
    DECISIONS.md, or docs itself, and never re-reads a CHUNK.md section after the subagent writes it;
    the single most tempting mistake is a "let me double-check by re-reading" step — forbidden
    (143-RESEARCH.md Anti-Patterns).

    Required Reading list (cite exact filenames verbatim, per 143-RESEARCH.md Pitfall 6 — do NOT
    re-derive): `docs/core-concepts.md` and `docs/common-pitfalls.md` always; `docs/actions-and-flow.md`
    when the chunk involves actions; `docs/custom-ui-guide.md` + `docs/ui-components.md` for
    `ui: touches|major` chunks; `docs/dice-and-scoring.md` for dice-mechanic chunks; and `DESIGN.md`
    for `ui: touches|major` chunks (DESIGN.md doesn't exist until the first UI chunk's ask, so
    scaffold.md couldn't name it). State that investigate is now the permanent owner of this list.

    Inputs to read: the chunk's cited slices (from CHUNK.md), INDEX-discovered slices (search
    `rulebook/INDEX.md` for the chunk's key terms), RULINGS.md (rulings outrank rulebook — cite
    state-machine.md), DECISIONS.md, the required docs above, DESIGN.md for ui≠none chunks.

    Fan-Out Dispatch: a fresh Task-tool subagent whose prompt names exactly what to read and
    instructs it to WRITE directly into CHUNK.md's `## Interpretation`, `## Visibility Declaration`,
    and `## Newly Discovered Citations` sections (append-only; never renumber existing claims — cite
    CHUNK.template.md's parse contract). The claims list is numbered and each claim carries a
    citation. The Visibility Declaration explicitly states "what is hidden from whom." Newly
    discovered citations are appended to CHUNK.md.

    Structured return (what flows back to the orchestrator — a short summary/pointer ONLY, never the
    full claims text, per 143-RESEARCH.md Pattern 3): use the pinned field names verbatim —
    `claimsList` (pointer/count summary), `visibilityDeclaration` (the declaration text or a
    pointer), `newlyDiscoveredCitations` (list). Add a `Return exactly: { … }` line naming these.

    Re-investigate round behavior (resolve 143-RESEARCH.md Open Question 1 explicitly): a refuted-
    once re-investigate round APPENDS a new claim noting supersession of the objected claim — it does
    NOT renumber or silently rewrite the existing numbered list. State this rule in prose.

    "Orchestrator Records" section: the orchestrator accumulates only the returned summary fields;
    it never re-reads CHUNK.md's `## Interpretation` before handing the claims list to redteam.

    Downstream Shape footer (cite, never restate): the claims list feeds `build/redteam.md` (the
    consumer) and the ask gate; cite CHUNK.template.md's sections without restating their structure.

    Contain no fenced implementation code; dispatch templates are illustrative prose blocks in the
    transcription.md register.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-02"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-02"` passes GREEN
    - `grep -Eo "docs/(core-concepts|common-pitfalls|actions-and-flow|custom-ui-guide|ui-components|dice-and-scoring).md" src/cli/slash-command/bs/build/investigate.md | sort -u | wc -l` returns 6 (all required docs named)
    - `grep -F "DESIGN.md" src/cli/slash-command/bs/build/investigate.md` matches (ui≠none reading)
    - `grep -E "RULINGS.md" src/cli/slash-command/bs/build/investigate.md` and `grep -E "DECISIONS.md" …` both match
    - `grep -F "## Visibility Declaration" src/cli/slash-command/bs/build/investigate.md` and `grep -F "## Interpretation" …` and `grep -F "## Newly Discovered Citations" …` all match
    - `grep -E "claimsList|visibilityDeclaration|newlyDiscoveredCitations" src/cli/slash-command/bs/build/investigate.md` matches the pinned return-shape field names
  </acceptance_criteria>
  <done>investigate.md exists; BUILD-02 test block GREEN; doc list + RULINGS/DECISIONS reads + claims-list/visibility-declaration + return shape all present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | LLM-instruction markdown only; no auth/network/session/input-parsing surface (143-RESEARCH.md Security Domain). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-03 | Information disclosure | Hidden-information handling: the visibility declaration must record what is hidden from whom | mitigate | investigate.md mandates an explicit `## Visibility Declaration` ("what is hidden from whom"); redaction enforcement itself is engine territory, not this authoring phase |
| T-143-01 | Tampering | Orchestrator re-reading a slice to "double-check" (context/framing leakage) | mitigate | Restate the Context-Economics Hard Rule; subagent is sole writer, orchestrator never re-reads |
| T-143-SC | Tampering | npm/pip/cargo installs | N/A | Zero package installs this phase |
</threat_model>

<verification>
`npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-02"` green.
</verification>

<success_criteria>
Investigate reference file authored; BUILD-02 test block green; doc-reading discipline + claims-list
+ visibility declaration + structured return all satisfied.
</success_criteria>

<output>
Create `.planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-03-SUMMARY.md` when done.
</output>
