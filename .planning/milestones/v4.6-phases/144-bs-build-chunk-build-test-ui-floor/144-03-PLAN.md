---
phase: 144-bs-build-chunk-build-test-ui-floor
plan: 03
type: execute
wave: 2
depends_on: [144-01]
files_modified:
  - src/cli/slash-command/bs/build/build.md
  - src/cli/slash-command/bs/build/test.md
autonomous: true
requirements: [BUILD-05, BUILD-06, UIQ-02, UIQ-03]

must_haves:
  truths:
    - "build/build.md documents reading raw slices + the approved interpretation, extends-not-restructures with a user gate, DECISIONS.md append, and per-file Build Manifest resume"
    - "build/build.md cites DESIGN.md's Placeholder Policy by name (never restates it) and states asset swaps never change geometry"
    - "build/test.md documents the ordered command list: tsc, boardsmith lint, unit/integration, full suite, random-sim, then the a11y floor for ui: touches|major"
    - "build/test.md documents all five a11y floor items and names the seven boardsmith eslint sandbox rules"
  artifacts:
    - path: "src/cli/slash-command/bs/build/build.md"
      provides: "Build step (BUILD-05) + placeholder policy in practice (UIQ-02)"
      min_lines: 40
      contains: "## Build Manifest"
    - path: "src/cli/slash-command/bs/build/test.md"
      provides: "Test step command list (BUILD-06) + a11y floor (UIQ-03)"
      min_lines: 40
      contains: "simulateRandomGames"
  key_links:
    - from: "src/cli/slash-command/bs/build/build.md"
      to: "templates/DESIGN.template.md ## Placeholder Policy"
      via: "citation, not restatement"
      pattern: "## Placeholder Policy"
    - from: "src/cli/slash-command/bs/build/test.md"
      to: "boardsmith/testing simulateRandomGames"
      via: "cited real API signature"
      pattern: "simulateRandomGames"
---

<objective>
Author the `{build, test}` session step group's two lean reference files. `build/build.md` is the
build step (BUILD-05) and where the placeholder policy is applied in practice (UIQ-02);
`build/test.md` is the test step command list (BUILD-06) and the per-chunk a11y floor (UIQ-03).
Both follow the established `build/{investigate,redteam,ask}.md` idiom: cite `state-machine.md`
and `templates/*.template.md` sections by name, never restate them.

Purpose: turns the {build,test} group from a forward-reference into live, dispatchable prose.
Output: `build/build.md` + `build/test.md`, greening the BUILD-05/06 + UIQ-02/03 drift blocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-bs-build-chunk-build-test-ui-floor/144-CONTEXT.md
@.planning/phases/144-bs-build-chunk-build-test-ui-floor/144-RESEARCH.md
@.planning/phases/144-bs-build-chunk-build-test-ui-floor/144-PATTERNS.md
@src/cli/slash-command/bs/build/ask.md
@src/cli/slash-command/bs/build/investigate.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author build/build.md (BUILD-05 + UIQ-02)</name>
  <files>src/cli/slash-command/bs/build/build.md</files>
  <read_first>
    - src/cli/slash-command/bs/build/ask.md (opening "Referenced by" framing + "Downstream Shape" footer)
    - src/cli/slash-command/bs/build/investigate.md (fresh-context subagent framing — build is the ONE step exempted from the orchestrator-never-reads-sources rule per 144-RESEARCH.md Pattern 2)
    - src/cli/slash-command/bs/templates/CHUNK.template.md `## Build Manifest` section (fill row-by-row, never a new section)
    - src/cli/slash-command/bs/templates/DESIGN.template.md `## Placeholder Policy` section (cite by name)
    - src/cli/slash-command/bs/build-chunk.md "Git Protocol" citation
    - 144-PATTERNS.md "build/build.md (new)" section (lines 23-93) — every verbatim analog
  </read_first>
  <action>
    Open with "Referenced by `build-chunk.md` Step 4 (`build`, first of the `{build, test}` session
    step group — see `state-machine.md` 'Session Handoff Seams')". Document the BUILD-05 build
    contract: the executor reads (1) the chunk's cited raw rulebook slices AND (2) the approved
    interpretation from CHUNK.md `## Interpretation` — interpretation is design layered on the
    slice, never a replacement; cite the fresh-context EXCEPTION (build may read raw slices
    directly, unlike investigate/redteam). Document: extends existing code, restructuring verified
    code requires a user gate; appends data-model/naming decisions to `DECISIONS.md`; fills CHUNK.md's
    existing `## Build Manifest` table row-by-row (File | Status: written/pending) for file-by-file
    crash-resume; rewrites the chunk's test script in actual interaction terms; git protocol
    (commit BEFORE build starts — WIP vs verified baseline — cite `state-machine.md` "Git Protocol").
    For UIQ-02: when a component has no asset yet, follow this chunk's `DESIGN.md` `## Placeholder
    Policy` verbatim (cite by name, never restate its rules) — correct aspect ratio, DESIGN.md
    tokens, labeled; asset arrival replaces fill, never geometry (zero-layout-diff swap). Close with
    a "Downstream Shape (cite, never restate)" section pointing at `build/test.md`. Do NOT restate
    state-machine.md/template rules inline. No fenced implementation code — directive prose only.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-05"` GREEN.
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "UIQ-02"` GREEN.
    - File contains the literal `## Build Manifest` and `## Placeholder Policy` (citations) and names DECISIONS.md, the user gate for restructuring, and the raw-slice + approved-interpretation dual read.
    - `grep -c "^## Placeholder Policy" src/cli/slash-command/bs/build/build.md` returns 0 (it CITES the DESIGN.md section, it does not create its own).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-05" 2>&1 | grep -q "passed" && npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "UIQ-02" 2>&1 | grep -q "passed" && echo BUILD_MD_GREEN</automated>
  </verify>
  <done>BUILD-05 and UIQ-02 drift blocks green; build.md cites (not restates) Build Manifest, Placeholder Policy, Git Protocol.</done>
</task>

<task type="auto">
  <name>Task 2: Author build/test.md (BUILD-06 + UIQ-03 a11y floor)</name>
  <files>src/cli/slash-command/bs/build/test.md</files>
  <read_first>
    - src/cli/slash-command/bs/ingest/scaffold.md "Verification Sequence" (lines 70-124) — the numbered, non-reorderable, stop-on-failure command-sequence idiom to replicate
    - src/testing/random-simulation.ts signature (144-RESEARCH.md Pattern 4 + 144-PATTERNS.md lines 127-141) — simulateRandomGames({count, playerCounts, timeout?, seed?}), assert results.crashed/stuck === 0
    - src/cli/lib/sandbox-scan.ts doc comment + src/eslint-plugin/index.ts seven rule names (144-PATTERNS.md lines 143-166)
    - CardRenderer.a11y.test.ts + interaction-integration.test.ts precedents (144-PATTERNS.md lines 168-205) — cite both for the keyboard-only floor item
    - 144-CONTEXT.md "A11y floor (UIQ-03, all five items)" + 144-RESEARCH.md Pitfall 1 (sandbox-rule vs regex heuristic) and Pitfall 2 (axe/jsdom contrast handled by the token-discipline grep, not axe)
  </read_first>
  <action>
    Open with the "Referenced by `build-chunk.md` Step 5 (`test`, second of `{build, test}`)"
    framing. Document BUILD-06 as ONE numbered, non-reorderable, stop-on-failure sequence run in the
    GENERATED game project: (1) `tsc --noEmit`; (2) `boardsmith lint` — name the seven sandbox rule
    names explicitly (`no-network`, `no-filesystem`, `no-timers`, `no-nondeterministic`, `no-eval`,
    `no-element-identity-comparison`, `no-element-array-state`) as the hard-gate subset, distinct
    from the regex heuristics the same command also surfaces (Pitfall 1); (3) chunk unit/integration
    tests; (4) the full accumulated suite (regression); (5) a random-sim playthrough citing
    `simulateRandomGames` (count/playerCounts; recommend count:50 light / 100 full — Claude's
    discretion; assert crashed===0, stuck===0); (6) IF `ui: touches|major` → the a11y floor. Document
    all five a11y floor items (UIQ-03): keyboard-only ActionPanel completion as an executable test
    (cite CardRenderer.a11y.test.ts for the individual-control shape AND
    interaction-integration.test.ts for the end-to-end real-wiring shape), an `axe-core` scan on
    board + ActionPanel (frame as structural/semantic only — labels/ARIA/duplicate-IDs — contrast is
    NOT covered by axe in jsdom), the no-color-literal grep with a contrast assertion for new
    game-local pairs (this is what catches contrast, per Pitfall 2), real controls (buttons or
    role/tabindex/keydown) with game-semantic aria-labels + decorative glyphs `aria-hidden`, and
    focus-not-stranded + `prefers-reduced-motion` honored. Note test-step failures loop back to
    `build` (still group 2). Close with a "Downstream Shape" section pointing at the {audit, repair}
    group (Phase 145). Cite the real API and rule names; no reimplementation, no fenced code that
    reimplements the harness.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-06"` GREEN.
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "UIQ-03"` GREEN.
    - File contains `tsc --noEmit`, `boardsmith lint`, `simulateRandomGames`, all seven sandbox rule names, `axe-core`, `prefers-reduced-motion`, `aria-hidden`, and a keyboard-only ActionPanel completion phrase.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-06" 2>&1 | grep -q "passed" && npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "UIQ-03" 2>&1 | grep -q "passed" && echo TEST_MD_GREEN</automated>
  </verify>
  <done>BUILD-06 and UIQ-03 drift blocks green; test.md names the ordered command list, the seven sandbox rules, the real random-sim API, and all five a11y floor items.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated-game source → boardsmith lint sandbox scan | test.md documents (does not implement) the existing AST-based determinism/network/timer/eval enforcement boundary generated games cross at test time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-03-SANDBOX | Tampering / Elevation of Privilege | generated-game code (network/filesystem/eval/nondeterminism) | mitigate (by citation) | Already enforced by `eslint-plugin-boardsmith`'s sandbox rules via `boardsmith lint`; test.md cites this existing mitigation and names the seven rules as the hard gate — it does not build a new one (144-RESEARCH.md Security Domain). |
| T-144-03-NA | S/R/I/D | build.md / test.md reference prose | accept | N/A — agent-consumed markdown, no auth/session/input/crypto surface. |
| T-144-SC | Tampering | npm installs | mitigate | No installs in this plan; axe-core is confined to Plan 02's scaffold-template diff. test.md only cites the axe-core scan pattern. |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-05|BUILD-06|UIQ-02|UIQ-03"` all green.
- Both new files exist and cite (not restate) their shared-file sources.
</verification>

<success_criteria>
- The {build, test} group is live prose; four drift blocks green.
- No BoardSmith manifest change; citation-not-restatement discipline held.
</success_criteria>

<output>
Create `.planning/phases/144-bs-build-chunk-build-test-ui-floor/144-03-SUMMARY.md` when done.
</output>
