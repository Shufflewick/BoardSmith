---
phase: 144-bs-build-chunk-build-test-ui-floor
plan: 04
type: execute
wave: 3
depends_on: [144-01, 144-03]
files_modified:
  - src/cli/slash-command/bs/build/design-ask.md
  - src/cli/slash-command/bs/build/ask.md
  - src/cli/slash-command/bs/build-chunk.md
autonomous: true
requirements: [UIQ-01]

must_haves:
  truths:
    - "build/design-ask.md offers exactly three directions — Adopt / Derive (default) / Original — and writes DESIGN.md only after explicit approval"
    - "build/ask.md dispatches design-ask.md as a pre-check when the chunk is ui: touches|major AND DESIGN.md does not yet exist"
    - "build-chunk.md's dispatch table + Reference Files list treat build/build.md and build/test.md as live (not forward references) and cite build/design-ask.md"
    - "The full unfiltered build-chunk.test.ts suite and npm test are green"
  artifacts:
    - path: "src/cli/slash-command/bs/build/design-ask.md"
      provides: "First-UI-chunk design ask (UIQ-01)"
      min_lines: 40
      contains: "Derive"
    - path: "src/cli/slash-command/bs/build-chunk.md"
      provides: "Live dispatch rows for build/test + design-ask.md registration"
      contains: "build/design-ask.md"
  key_links:
    - from: "src/cli/slash-command/bs/build/ask.md"
      to: "src/cli/slash-command/bs/build/design-ask.md"
      via: "first-UI-chunk pre-check dispatch"
      pattern: "design-ask\\.md"
    - from: "src/cli/slash-command/bs/build-chunk.md"
      to: "build/build.md, build/test.md, build/design-ask.md"
      via: "live dispatch table + Reference Files list"
      pattern: "build/(build|test|design-ask)\\.md"
---

<objective>
Author the first-UI-chunk design ask (UIQ-01) and wire it in. `build/design-ask.md` presents the
3-way Adopt/Derive/Original choice and writes `DESIGN.md`; a small hook in `build/ask.md`
dispatches it as a pre-check for the first UI chunk; and `build-chunk.md`'s routing table + Reference
Files list flip `build/build.md` and `build/test.md` from forward references to live dispatches and
register `build/design-ask.md`. This is the final plan — it runs the unfiltered drift suite and the
full repo suite as the phase gate.

Purpose: closes UIQ-01 and turns Phase 144's whole {build, test} + design-ask surface live.
Output: `build/design-ask.md` + edited `ask.md` + edited `build-chunk.md`; entire suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-bs-build-chunk-build-test-ui-floor/144-CONTEXT.md
@.planning/phases/144-bs-build-chunk-build-test-ui-floor/144-RESEARCH.md
@.planning/phases/144-bs-build-chunk-build-test-ui-floor/144-PATTERNS.md
@src/cli/slash-command/bs/build/ask.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author build/design-ask.md (UIQ-01)</name>
  <files>src/cli/slash-command/bs/build/design-ask.md</files>
  <read_first>
    - src/cli/slash-command/bs/build/ask.md "Gate-Before-Write" (lines 102-126) + "Assets — Never-Blocking Placeholder Request" (lines 80-93) — the gate-before-write / write-order-last / never-blocking discipline to adapt
    - src/cli/slash-command/bs/templates/DESIGN.template.md — the PARSE CONTRACT + `## Chosen Direction` section design-ask.md fills
    - src/cli/slash-command/bs/ingest/... rulebook/00-visual-survey.md reference (read as evidence input)
    - 144-CONTEXT.md "Design ask directions" (A Adopt / B Derive default / C Original via frontend-design skill) + "specifics" (changing DESIGN.md later is itself a chunk; "I don't have art yet" never blocks)
    - 144-PATTERNS.md "build/design-ask.md (new)" section (lines 212-278)
  </read_first>
  <action>
    Open with "Referenced by `build/ask.md`'s pre-check (first-UI-chunk detection: this chunk's
    `## ui:` tag is `touches`|`major` AND DESIGN.md does not yet exist on disk) — runs to completion
    before ask.md's 4-part gate proceeds for that same chunk." Present the three directions:
    (A) Adopt — the physical game's identity, requires user-supplied box art/photos, trade-dress
    caution for someone else's commercial game; (B) Derive — original web design in the game's
    palette/mood, no asset dependence, the DEFAULT recommendation; (C) Original — invoke the
    frontend-design skill for 2-3 one-page throwaway HTML mood sketches. Read
    `rulebook/00-visual-survey.md` as evidence. Apply the gate-before-write discipline: present +
    negotiate, write nothing durable until explicit approval, then fill `DESIGN.md` from
    `DESIGN.template.md` (Chosen Direction, Theme Block, Typography & Spacing, Component Recipes,
    Placeholder Policy, Do/Don't) — cite the template's section names, do not restructure them.
    State explicitly that changing DESIGN.md later is itself a chunk (re-opens verified chunks) and
    that "I don't have art yet" never blocks (placeholder policy + ASSETS.md debt). Close with the
    token discipline note (color literals only in the theme block; everything else references
    `--bsg-*`/applyTheme()). No fenced implementation code — directive prose.
  </action>
  <acceptance_criteria>
    - File names `Adopt`, `Derive`, `Original`, marks `Derive` as the default recommendation, and states it writes `DESIGN.md`.
    - File cites DESIGN.template.md's section names (Chosen Direction, Placeholder Policy) rather than restating their rules.
    - File states the "changing DESIGN.md later is itself a chunk" and "never blocks" rules.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && grep -q "Adopt" src/cli/slash-command/bs/build/design-ask.md && grep -q "Derive" src/cli/slash-command/bs/build/design-ask.md && grep -q "Original" src/cli/slash-command/bs/build/design-ask.md && grep -q "DESIGN.md" src/cli/slash-command/bs/build/design-ask.md && echo DESIGN_ASK_WRITTEN</automated>
  </verify>
  <done>design-ask.md presents the 3-way choice, defaults to Derive, gate-before-writes DESIGN.md, cites the template sections.</done>
</task>

<task type="auto">
  <name>Task 2: Hook design-ask.md into build/ask.md as a first-UI-chunk pre-check</name>
  <files>src/cli/slash-command/bs/build/ask.md</files>
  <read_first>
    - src/cli/slash-command/bs/build/ask.md (full — the 4-part format begins at "## The Fixed 4-Part Presentation Format"; the footer at lines 136-140)
    - 144-RESEARCH.md Open Question 1 (insert as a pre-check BEFORE Part (a), not interleaved) + 144-PATTERNS.md "build/ask.md (small hook edit)" (lines 282-297)
  </read_first>
  <action>
    Insert a new pre-check section (e.g. `## First-UI-Chunk Design Check (pre-check, before Part
    (a))`) BEFORE the 4-part presentation format: if this chunk's `## ui:` tag is `touches` or
    `major` AND `DESIGN.md` does not yet exist on disk, dispatch `build/design-ask.md` to completion
    (it writes DESIGN.md) before continuing to Part (a) for this same chunk. Do NOT restructure the
    existing 4-part presentation. Leave the existing footer's "the next session picks up the step
    group `{build, test}`" text intact.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "UIQ-01"` GREEN (the UIQ-01 block asserts both design-ask.md content AND ask.md's pre-check hook).
    - ask.md contains `design-ask.md` and the touches|major + DESIGN.md-does-not-exist detection phrasing.
    - The existing BUILD-04 ask block still green (4-part format untouched).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "UIQ-01" 2>&1 | grep -q "passed" && npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-04" 2>&1 | grep -q "passed" && echo ASK_HOOK_GREEN</automated>
  </verify>
  <done>ask.md dispatches design-ask.md as a pre-check; UIQ-01 green; BUILD-04 still green.</done>
</task>

<task type="auto">
  <name>Task 3: Flip build/test rows live + register design-ask.md in build-chunk.md; run the phase gate</name>
  <files>src/cli/slash-command/bs/build-chunk.md</files>
  <read_first>
    - src/cli/slash-command/bs/build-chunk.md lines 116-135 (Step 3 dispatch table — `build`/`test` rows carry "authored in Phase 144") and lines 243-261 (Reference Files list)
    - 144-PATTERNS.md "build-chunk.md (routing-table edit)" section (lines 301-322)
  </read_first>
  <action>
    In the Step 3 dispatch table, change `| build | `build/build.md` — authored in Phase 144 |` to
    `| build | `build/build.md` |` and the same for the `test` row (drop the "authored in Phase 144"
    suffix — those files now exist). In the Reference Files list, move `build/build.md` and
    `build/test.md` out of the forward-referenced-only bullets into the live list alongside
    investigate/redteam/ask, and ADD `build/design-ask.md` to the live list. Leave the
    `build/{audit,repair,playtest,revise,close}.md` rows with their "authored in Phase 145/146"
    markers intact (only the 144 marker is removed anywhere in the file). Do NOT change the router's
    routing logic.
  </action>
  <acceptance_criteria>
    - `grep -c "authored in Phase 144" src/cli/slash-command/bs/build-chunk.md` returns 0.
    - build-chunk.md contains `build/design-ask.md`, and still contains `authored in Phase 145` and `authored in Phase 146`.
    - FULL unfiltered `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts` GREEN (all REFERENCED_PATHS exist on disk and are cited; forward-marker + exclusion-list tests pass).
    - `npm test` GREEN (no regression in project-scaffold.ts generators or elsewhere).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && test "$(grep -c 'authored in Phase 144' src/cli/slash-command/bs/build-chunk.md)" = "0" && npx vitest run src/cli/slash-command/bs/build-chunk.test.ts 2>&1 | grep -q "passed" && npm test 2>&1 | tail -5 | grep -qiE "passed|pass" && echo PHASE_GATE_GREEN</automated>
  </verify>
  <done>build/test rows live, design-ask.md registered, only 145/146 markers remain; unfiltered drift suite + full npm test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Reference-file authoring + router-table edits; no runtime input path, no untrusted data crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-04-NA | S/T/R/I/D/E | design-ask.md / ask.md / build-chunk.md (agent-consumed markdown) | accept | N/A — no auth/session/access-control/input-validation/crypto surface (144-RESEARCH.md Security Domain: all ASVS categories N/A for this phase). |
| T-144-SC | Tampering | npm installs | mitigate | No installs in this plan; the axe-core supply-chain surface is confined to Plan 02. |
</threat_model>

<verification>
- Full unfiltered `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts` green (BUILD-01..06, UIQ-01..03, cross-file, forward-marker, exclusion-list).
- `npm test` green.
- `git status --short package.json package-lock.json` empty (no BoardSmith manifest drift).
</verification>

<success_criteria>
- UIQ-01 closed; the whole Phase 144 markdown surface is live.
- Unfiltered drift suite + full repo suite green (phase gate).
</success_criteria>

<output>
Create `.planning/phases/144-bs-build-chunk-build-test-ui-floor/144-04-SUMMARY.md` when done.
</output>
