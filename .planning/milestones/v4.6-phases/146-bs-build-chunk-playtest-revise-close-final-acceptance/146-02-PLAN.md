---
phase: 146-bs-build-chunk-playtest-revise-close-final-acceptance
plan: 02
type: execute
wave: 2
depends_on: [146-01]
files_modified:
  - src/cli/slash-command/bs/build/playtest.md
  - src/cli/slash-command/bs/build/revise.md
autonomous: true
requirements: [BUILD-09, BUILD-10]
must_haves:
  truths:
    - "playtest.md hands the user ONE command they run themselves (npx boardsmith dev) plus a URL — the skill never starts or kills that server"
    - "The playtest script is numbered, click-by-click, states seat count and per-seat steps, teaches dev-host affordances once"
    - "Verified is an explicit item-by-item checklist confirmed one at a time; verified (user-waived) is recordable for honest skipping"
    - "Hidden-info chunks get a second-seat leak check (visual: you should NOT see the other player's hand)"
    - "revise.md triages EACH feedback item into exactly one of four categories and appends rounds without renumbering"
    - "Re-entry after a revise round shows a feedback disposition report + a TARGETED re-test script, never a blind full re-test"
  artifacts:
    - path: "src/cli/slash-command/bs/build/playtest.md"
      provides: "BUILD-09 human-gate playtest reference file (no subagent)"
      min_lines: 90
    - path: "src/cli/slash-command/bs/build/revise.md"
      provides: "BUILD-10 4-category triage + disposition-report reference file"
      min_lines: 80
  key_links:
    - from: "src/cli/slash-command/bs/build/playtest.md"
      to: "src/cli/slash-command/bs/build/close.md"
      via: "light-path cites close.md's '## Bookkeeping Sequence' by name"
      pattern: "Bookkeeping Sequence"
    - from: "src/cli/slash-command/bs/build/playtest.md"
      to: "state-machine.md"
      via: "Write Order citation for the Status write (CHUNK.md then SKETCH.md)"
      pattern: "Write Order"
---

<objective>
Author `build/playtest.md` (BUILD-09 — the human gate) and `build/revise.md` (BUILD-10 — the
4-category triage loop), the first two members of the `{playtest, one revise round, close}`
session step group. Both are pure prose consumed by a future Claude Code session.

Purpose: playtest hands the user the most-polished artifact for a click-by-click browser
verification the SKILL never automates (the user owns the server); revise triages the feedback
item-by-item so a chunk closes on real defects while future ideas and rules changes are routed
honestly.

Output: Two new reference files matching the established `ask.md`/`repair.md` shape, citing
state-machine.md and the CHUNK/SKETCH templates rather than restating them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/146-bs-build-chunk-playtest-revise-close-final-acceptance/146-CONTEXT.md
@.planning/phases/146-bs-build-chunk-playtest-revise-close-final-acceptance/146-RESEARCH.md
@.planning/phases/146-bs-build-chunk-playtest-revise-close-final-acceptance/146-PATTERNS.md
@src/cli/slash-command/bs/build/ask.md
@src/cli/slash-command/bs/build/repair.md
@src/cli/slash-command/bs/state-machine.md
@src/cli/slash-command/bs/templates/CHUNK.template.md
@src/cli/slash-command/bs/templates/SKETCH.template.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author build/playtest.md (BUILD-09 human-gate, no subagent)</name>
  <read_first>
    - src/cli/slash-command/bs/build/ask.md (full — THE analog; copy its header framing, its
      no-subagent "Inputs" shape lines 26-33, its Gate-Before-Write write-order sequence lines
      117-145, and its end-of-file "Downstream Shape (cite, never restate)" section)
    - src/cli/slash-command/bs/templates/CHUNK.template.md (## Playtest Test Script, ## Verified
      Checklist, ## Verified Commit Hash sections — cite/fill these, do NOT redefine them)
    - src/cli/slash-command/bs/state-machine.md ("Write Order" 56-62, "Session Handoff Seams"
      133-148, light-path transitions 37-44 — cite by name)
    - .planning/phases/146-.../146-RESEARCH.md Pitfalls 3/4/5 (no Dispatch Template; no build-stamp
      UI element exists — freshness via hard reload; second-seat leak check is the seat switcher +
      a plain visual outcome)
  </read_first>
  <action>
    Create `src/cli/slash-command/bs/build/playtest.md`. Open with the exact ask.md framing:
    "Referenced by `build-chunk.md` Step 8 (`playtest`, first of the `{playtest, one revise round,
    close}` session step group — see `state-machine.md` 'Session Handoff Seams')." State
    explicitly this step has **no subagent** — the orchestrator narrates the script to the human and
    records answers itself; model the Inputs section on ask.md's (reads CHUNK.md's build manifest +
    the prior chunk's still-open regression items), NOT on a redteam/audit Dispatch Template.
    Author these sections:
    - The numbered click-by-click test script (fill CHUNK.template.md's `## Playtest Test Script`,
      cite don't redefine): states the seat count, per-seat numbered steps each with an
      observable-outcome ("expect:"), teaches the dev-host affordances ONCE (seat selector,
      second-tab-as-player-2, `--ai` fill, Follow-active-seat) with the literal commands
      `npx boardsmith dev`, `npx boardsmith dev --players <count>`, `npx boardsmith dev --ai 1,2`.
    - Build-stamp freshness: there is NO on-screen build indicator (RESEARCH.md Pitfall 4) — instruct
      the user to close any existing `boardsmith dev` tab and open a fresh one (hard reload) after
      this chunk's build/test complete; the `Build stamp:` template field is filled with the actual
      commit hash the orchestrator captured, a textual field, not an on-screen check.
    - The one-line regression check and the standing taste line ("anything look off, cramped, or
      unreadable?").
    - Second-seat leak check FOR HIDDEN-INFO CHUNKS ONLY: describe the outcome to look for ("you
      should NOT see the other player's hand — only its back/count"), via opening a second tab at
      the same URL and using the seat switcher; a plain visual check, "n/a — no hidden info in this
      chunk" otherwise.
    - The Verified gate (mirror ask.md Gate-Before-Write exactly, substituting `verified` /
      `verified (user-waived)` for `approved`): each `## Verified Checklist` item confirmed one at a
      time ("not a vibe"); only after every item is confirmed or the user explicitly waives does
      `Status: verified` / `verified (user-waived)` get written — CHUNK.md first, then SKETCH.md,
      cite `state-machine.md` "Write Order". Presenting is not approving.
    - A Light-Path section: cite `close.md`'s `## Bookkeeping Sequence` BY NAME for the bookkeeping
      the light path performs at playtest (verified-hash capture, Status write order, decision
      rollup, next-2-3 tail detailing) — do NOT restate that sequence here (Pitfall 2).
    - End with `## Downstream Shape (cite, never restate)` pointing to `revise.md` (if issues) or
      `close.md` (if clean), without restating either.
    The user owns the server: this file NEVER instructs the skill to start or kill a dev server.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-09" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `build/playtest.md` exists; BUILD-09 describe block is GREEN
    - Contains byte-exact `verified (user-waived)`
    - Names literal `npx boardsmith dev` and `--ai`; contains a hard-reload/close-the-tab freshness instruction
    - Contains the second-seat leak-check outcome wording for hidden-info chunks
    - Cites `close.md`'s `## Bookkeeping Sequence` by name (does not restate the bookkeeping steps)
    - Cites `state-machine.md` "Write Order"; contains NO "Dispatch Template" heading
    - Contains no instruction for the skill to run/kill a server
  </acceptance_criteria>
  <done>playtest.md authored in ask.md's shape, no subagent, BUILD-09 assertions green.</done>
</task>

<task type="auto">
  <name>Task 2: Author build/revise.md (BUILD-10 4-category triage + disposition report)</name>
  <read_first>
    - src/cli/slash-command/bs/build/repair.md (full — THE analog; copy its two-actions-vs-terminal-
      dispositions structure lines 8-33, its "Loop Bound (cite, never restate)" + "Persisting
      Dispositions" discipline lines 35-70, and its Round-3 plain-language triage-to-user shape
      lines 72-88 for the disposition report)
    - src/cli/slash-command/bs/templates/CHUNK.template.md (## Revision Rounds section — the
      append-only ledger revise fills)
    - src/cli/slash-command/bs/templates/SKETCH.template.md (## Ideas Backlog — where future-scope
      items route)
    - src/cli/slash-command/bs/state-machine.md ("Session Handoff Seams", "Write Order")
  </read_first>
  <action>
    Create `src/cli/slash-command/bs/build/revise.md`. Open with the repair.md framing: "Referenced
    by `build-chunk.md` Step 9 (`revise`, second of the `{playtest, one revise round, close}` session
    step group — see `state-machine.md` 'Session Handoff Seams'). Revise processes each feedback item
    the user reported during `playtest`: every item gets exactly one of four dispositions."
    Author these sections:
    - The 4-category triage (mirror repair's action-vs-disposition rigor): (a) this-chunk defect →
      new `### Revise N` entry in CHUNK.md's `## Revision Rounds`, code fix re-enters build/test as
      needed; (b) future scope → SKETCH.md's `## Ideas Backlog` or hand off to `/bs-insert-chunk`
      (forward reference, Phase 147); (c) not-built-yet (matches the script's "not yet" list) →
      expectation reset, no write; (d) rules change → new RULINGS.md entry. State the closure rule:
      the chunk closes when all (a)-items are resolved regardless of (b)-items.
    - Round-bounding + persistence: rounds appended `revise-1`, `revise-2`, … append-only, never
      renumbered; cite (never restate) the numbering and the write-before-next-round discipline,
      reusing the `(cite, never restate)` heading-suffix convention.
    - The re-entry disposition report (mirror repair.md Round-3 plain-language shape): on re-entry
      after a revise round the user gets a **feedback disposition report** — each item they reported
      and what changed — plus a **targeted re-test script** (only the items affected by this round's
      fixes, a smaller instantiation of `## Playtest Test Script`), NEVER a blind full re-test and
      NEVER a raw finding/agent-transcript.
    - Loop back to playtest until all (a)-items are done.
    - End with `## Downstream Shape (cite, never restate)` pointing to `playtest.md` (re-test) then
      `close.md` (once clean).
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-10" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `build/revise.md` exists; BUILD-10 describe block is GREEN
    - Names all four triage categories and routes each to the correct file (CHUNK ## Revision Rounds / SKETCH ## Ideas Backlog / no-write / RULINGS.md)
    - Contains `revise-1`/`revise-2` append-only numbering
    - Contains both "feedback disposition report" and "targeted re-test"; forbids a blind full re-test
    - States the chunk closes on all (a)-items regardless of (b)-items
  </acceptance_criteria>
  <done>revise.md authored in repair.md's shape, BUILD-10 assertions green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated game dev-host ↔ a second browser seat | The human's hidden-info visual check crosses this boundary — an opponent's hand must not render for the wrong seat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-01 | Information Disclosure | playtest.md second-seat leak check | mitigate | For hidden-info chunks, playtest.md's script instructs a second-seat visual leak check ("you should NOT see the other player's hand — only its back/count") — the human-facing complement to audit.md's automated two-seat visibility diff. |
| T-146-02 | Denial of Service (resource) | dev server lifecycle | accept | The USER owns and runs the server; this skill never starts or kills it, so it cannot leak a process. (Any server the skill itself started was killed in Phase 145; final-acceptance's own agent-started server is mitigated in Plan 03.) |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH.md: Package Legitimacy Audit N/A). |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-09"` and `-t "BUILD-10"` green.
- Neither file restates state-machine.md's git-protocol / lock / handoff rule text (citation only).
- playtest.md contains no "Dispatch Template" heading and no server start/kill instruction.
</verification>

<success_criteria>
playtest.md and revise.md exist in the established reference-file shape; BUILD-09 and BUILD-10
drift assertions are green; the light-path bookkeeping citation to close.md's `## Bookkeeping
Sequence` is in place for Plan 03 to satisfy.
</success_criteria>

<output>
Create `.planning/phases/146-bs-build-chunk-playtest-revise-close-final-acceptance/146-02-SUMMARY.md` when done
</output>
