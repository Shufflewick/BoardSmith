---
phase: 146-bs-build-chunk-playtest-revise-close-final-acceptance
plan: 03
type: execute
wave: 2
depends_on: [146-01]
files_modified:
  - src/cli/slash-command/bs/build/close.md
  - src/cli/slash-command/bs/build/final-acceptance.md
autonomous: true
requirements: [BUILD-11, UIQ-05]
must_haves:
  truths:
    - "close.md records the verified commit hash (git rev-parse HEAD) into CHUNK.md's ## Verified Commit Hash"
    - "close.md re-derives the sketch tail and presents it as a DELTA at a gate — never a silent rewrite — before proposing the next chunk with its ui: tag"
    - "close.md exposes a named ## Bookkeeping Sequence the light path (playtest.md) cites and reuses"
    - "final-acceptance.md documents the coverage check + the 7-point design-QA pass"
    - "final-acceptance.md's drag-drop keyboard-alternates check verifies drag-drop actions have working keyboard alternates end-to-end, reusing build/test.md item 1's ActionPanel keyboard-only pattern"
    - "final-acceptance.md's dispatched agent serves --no-open, waits on the exact ready string, captures, and kills the server before returning"
    - "The SR playthrough exercises Phase 140's useAnnouncer() via a human running VoiceOver (not a dispatched agent)"
  artifacts:
    - path: "src/cli/slash-command/bs/build/close.md"
      provides: "BUILD-11 bookkeeping + sketch-tail delta gate reference file"
      min_lines: 90
    - path: "src/cli/slash-command/bs/build/final-acceptance.md"
      provides: "UIQ-05 7-point design-QA + coverage-check reference file"
      min_lines: 110
  key_links:
    - from: "src/cli/slash-command/bs/build/close.md"
      to: "state-machine.md"
      via: "Git Protocol citation for the verified-hash + commit convention"
      pattern: "Git Protocol"
    - from: "src/cli/slash-command/bs/build/final-acceptance.md"
      to: "src/ui/composables/useAnnouncer.ts"
      via: "SR playthrough names the announce() floor it exercises"
      pattern: "useAnnouncer"
---

<objective>
Author `build/close.md` (BUILD-11 — bookkeeping + sketch-tail delta gate) and
`build/final-acceptance.md` (UIQ-05 — the sketch's mandated final-acceptance chunk's coverage check
+ 7-point design-QA pass). These complete the reference-file set for the `{playtest, one revise
round, close}` group.

Purpose: close leaves a durable, git-anchored, resumable trail and re-derives the sketch tail as a
reviewable delta; final-acceptance defines what "done" means for the whole sketch, not just per
chunk — the full accessibility/design-QA pass on the most polished artifact.

Output: Two new reference files. close.md composes ask.md's gate shape with state-machine.md's Git
Protocol; final-acceptance.md extends design-review.md's serve→capture→kill agent-dispatch shape
plus human-narrated SR/colorblind checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/146-bs-build-chunk-playtest-revise-close-final-acceptance/146-CONTEXT.md
@.planning/phases/146-bs-build-chunk-playtest-revise-close-final-acceptance/146-RESEARCH.md
@.planning/phases/146-bs-build-chunk-playtest-revise-close-final-acceptance/146-PATTERNS.md
@src/cli/slash-command/bs/build/ask.md
@src/cli/slash-command/bs/build/design-review.md
@src/cli/slash-command/bs/build/test.md
@src/cli/slash-command/bs/state-machine.md
@src/cli/slash-command/bs/templates/CHUNK.template.md
@src/cli/slash-command/bs/templates/SKETCH.template.md

<interfaces>
From src/ui/theme.ts (cite these values, do NOT re-derive capture widths):
- `BREAKPOINTS = { compact: 640, medium: 1024, wide: 1440 }` — capture at 375 / 800 / 1440 per design-review.md's existing tier table.

From src/ui/composables/useAnnouncer.ts (name this, do NOT re-describe ARIA plumbing):
- `announce(message: string, opts?: { assertive?: boolean }): void`
- `export function useAnnouncer(): UseAnnouncerReturn`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author build/close.md (BUILD-11 bookkeeping + sketch-tail delta gate)</name>
  <read_first>
    - src/cli/slash-command/bs/build/ask.md (Gate-Before-Write lines 117-146 — the shape the
      sketch-tail delta gate reuses; end-of-file "Downstream Shape" citation)
    - src/cli/slash-command/bs/state-machine.md ("Git Protocol" 114-119, "Write Order" 56-62,
      "Session Handoff Seams" 133-148 — CITE by name, never restate; Pitfall 1 is precisely this
      restatement risk)
    - src/cli/slash-command/bs/templates/CHUNK.template.md (## Verified Commit Hash section 166-172)
    - src/cli/slash-command/bs/templates/SKETCH.template.md (Ordered Chunk List / tail entries)
    - 146-RESEARCH.md Pattern 3 (the authored delta-presentation prose) and Pitfall 2 (light-path
      reconciliation — close.md's bookkeeping must be citable BY NAME from playtest.md)
    - 146-PATTERNS.md build/close.md section (the composite analog and light-path reconciliation target)
  </read_first>
  <action>
    Create `src/cli/slash-command/bs/build/close.md`. Open with the group framing: "Referenced by
    `build-chunk.md` Step 10 (`close`, third and last of the `{playtest, one revise round, close}`
    session step group — see `state-machine.md` 'Session Handoff Seams')." Author:
    - A named `## Bookkeeping Sequence` (this exact heading — playtest.md's light path cites it): a
      self-contained numbered sequence — (1) mark `Status: verified` in CHUNK.md then SKETCH.md's
      derived pointer (cite `state-machine.md` "Write Order"; note the CHUNK.md Status write already
      landed in playtest for the full ceremony, so close's own duty starts after); (2) run
      `git rev-parse HEAD` and write the literal hash into CHUNK.md's `## Verified Commit Hash` (cite
      `state-machine.md` "Git Protocol" for the bisect-anchor rationale and the
      `chunk-<slug>/step-<name>` commit convention — do NOT restate the format or rationale in new
      words); (3) roll up this chunk's DECISIONS.md entries.
    - A `## Sketch-Tail Delta Gate` section modeled on ask.md's fixed-format rigidity: re-derive the
      next 2-3 sketch-level tail entries against the rulebook now the chunk's citations are settled,
      and present ONLY the delta (entries changed/split/merged/newly-detailed) in a fixed named
      structure (e.g. before / after / why — pin the structure so a future drift test can), e.g.
      "Chunk 'auction' split into 'auction-basic' and 'auction-improvements' because …". Gate it like
      ask.md: present, require explicit approval, only then write SKETCH.md's Ordered Chunk List.
      Forbid the silent-rewrite anti-pattern explicitly ("never a silent rewrite").
    - Propose the next chunk with its `ui:` tag, and print the exact next command (non-programmer
      handoff).
    - End with `## Downstream Shape (cite, never restate)`.
    The light path calls this same `## Bookkeeping Sequence` (CONTEXT.md: "close.md is the full
    version playtest's light-path bookkeeping pointed forward to").
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-11" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `build/close.md` exists; BUILD-11 describe block is GREEN
    - Contains byte-exact heading `## Bookkeeping Sequence` and `## Verified Commit Hash` and `git rev-parse HEAD`
    - Cites `state-machine.md` "Git Protocol" and "Write Order" by name; does NOT restate the 24h/commit-format rule text
    - Sketch-tail delta gate present, in a fixed named structure, with an explicit "never a silent rewrite" prohibition
    - Proposes next chunk with its `ui:` tag
  </acceptance_criteria>
  <done>close.md authored; BUILD-11 green; `## Bookkeeping Sequence` heading exists for playtest.md's light-path citation.</done>
</task>

<task type="auto">
  <name>Task 2: Author build/final-acceptance.md (UIQ-05 7-point design-QA + coverage check)</name>
  <read_first>
    - src/cli/slash-command/bs/build/design-review.md (full — THE analog; byte-identical reuse of
      "Single Fresh-Context Adversarial Dispatch" 12-24, "Dev-Host Lifecycle: Serve → Capture → Kill"
      55-86, the breakpoint/capture-width table 87-111, the theme-injection mechanism 119-126, and
      the Findings Destination routing 168-177)
    - src/cli/slash-command/bs/build/test.md (item 1 — the ActionPanel keyboard-only completion run;
      final-acceptance's drag-drop keyboard-alternates check REUSES this exact pattern, does not
      re-derive it)
    - src/ui/composables/useAnnouncer.ts (announce() signature — name it, don't re-describe ARIA)
    - src/ui/theme.ts (BREAKPOINTS 640/1024/1440 — cite by name)
    - 146-RESEARCH.md Pattern 2, Open Question 1 (the automated-vs-human split, and its recommendation
      to fold drag-drop keyboard alternates into the automated/agent-narrated portion via build/test.md
      item 1's keyboard-completion pattern), and the Anti-Pattern "design-QA agent reading CHUNK.md's
      ## Interpretation" (forbidden)
  </read_first>
  <action>
    Create `src/cli/slash-command/bs/build/final-acceptance.md`. Frame it (CONTEXT.md): the sketch's
    mandated final-acceptance chunk is a special sketch chunk, not a per-chunk step — the same
    `{playtest, revise, close}` group applies to it, but its own content dispatches here for the
    coverage check + 7-point design-QA pass. Author:
    - A `## Coverage Check`: every non-variant rulebook slice must be cited by a closed chunk (read
      SKETCH.md's Ordered Chunk List + each CHUNK.md's Interpretation citations) — this is part of
      what "done" means for the sketch.
    - The 7-point design-QA pass (UIQ-05), matching the canonical spec's enumeration order: (1)
      screen-reader playthrough — HUMAN-narrated, user runs VoiceOver; name `useAnnouncer()` /
      `announce()` as the Phase-140 floor this exercises; (2) 200% zoom; (3) compact touch targets;
      (4) colorblind pass on game-added colors; (5) both Slate themes; (6) drag-drop keyboard
      alternates end-to-end — verify every drag-drop action has a working keyboard alternate, reusing
      build/test.md item 1's ActionPanel keyboard-only completion pattern (cite it by name; do not
      re-derive the keyboard-only run); (7) mobile layout via iframe-shrink.
    - Split the seven across an agent-dispatched portion and a human-narrated portion (RESEARCH.md
      Open Question 1 recommendation; Claude's Discretion per CONTEXT.md — DOCUMENT the split): a
      single fresh-context Task-tool dispatch handles the screenshot/measurement checks (200% zoom,
      touch targets, both themes via `iframe.contentDocument.documentElement.dataset.theme`, mobile
      via iframe-shrink) AND the drag-drop keyboard-alternates end-to-end check (reusing build/test.md
      item 1's ActionPanel keyboard-only completion run — the agent narrates completing every drag-drop
      action via keyboard), reusing design-review.md's serve→capture→kill lifecycle verbatim — serve
      `npx boardsmith dev --no-open`, wait for the exact `Ready! Press Ctrl+C to stop.` string (never
      `networkidle`), capture at 375/800/1440 citing `BREAKPOINTS`, and KILL the server as an
      explicit numbered step before returning. The SR playthrough and colorblind judgment are
      human-narrated sections OUTSIDE the dispatch template. Reuse design-review.md's fresh-context
      independence discipline: the dispatched agent NEVER inherits CHUNK.md's `## Interpretation` or
      prior framing — do not relax it because it is the "final" pass.
    - Findings destination: route findings through CHUNK.md's `## Findings Ledger` + build/repair.md's
      fix-or-refute loop (cite design-review.md's Findings Destination), or a final-acceptance section
      — Claude's Discretion, document the choice.
    - End with `## Downstream Shape (cite, never restate)` — this chunk still runs the standard
      playtest/revise/close semantics on top of this content.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "UIQ-05" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `build/final-acceptance.md` exists; UIQ-05 describe block is GREEN
    - Names all seven design-QA checks; SR playthrough names `useAnnouncer` and `VoiceOver`
    - The drag-drop keyboard-alternates check verifies drag-drop actions have working keyboard alternates end-to-end and cites build/test.md item 1's ActionPanel keyboard-only pattern
    - Dispatched agent portion contains `--no-open`, the exact `Ready! Press Ctrl+C to stop.` string, an explicit kill-the-server step, and never waits on `networkidle`
    - Cites `BREAKPOINTS` values 640/1024/1440; contains a `## Coverage Check`
    - Dispatched agent is fresh-context and never reads CHUNK.md's `## Interpretation`
  </acceptance_criteria>
  <done>final-acceptance.md authored in design-review.md's shape with the human/agent split documented and the drag-drop keyboard-alternates check folded into the automated portion; UIQ-05 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| final-acceptance dispatched agent ↔ dev server process | The agent starts a real dev server for capture; it must be killed before the agent returns (repo hard rule). |
| generated game UI ↔ assistive tech (VoiceOver) | The SR playthrough surfaces whether hidden/decorative content leaks into the accessibility tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-02 | Denial of Service (resource leak) | final-acceptance dispatched agent's dev server | mitigate | Reuse design-review.md's "Serve → Capture → Kill" as one unbroken numbered sequence with an explicit kill step before the agent returns; `--no-open` so no orphaned browser tab; never `networkidle` (hangs forever). |
| T-146-01 | Information Disclosure | SR playthrough / colorblind pass | mitigate | The SR playthrough exercises the useAnnouncer() floor and surfaces content that leaks into the a11y tree; the design-QA pass is human-judged for what should/should not be perceivable. |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH.md: Package Legitimacy Audit N/A). |
</threat_model>

<verification>
- `-t "BUILD-11"` and `-t "UIQ-05"` green.
- close.md and final-acceptance.md cite state-machine.md / design-review.md / test.md rather than
  restating their rule text.
- final-acceptance.md's server discipline is byte-identical-reused, not re-derived.
</verification>

<success_criteria>
close.md and final-acceptance.md exist; BUILD-11 and UIQ-05 assertions green; close.md's
`## Bookkeeping Sequence` heading satisfies playtest.md's light-path citation from Plan 02.
</success_criteria>

<output>
Create `.planning/phases/146-bs-build-chunk-playtest-revise-close-final-acceptance/146-03-SUMMARY.md` when done
</output>
</output>
