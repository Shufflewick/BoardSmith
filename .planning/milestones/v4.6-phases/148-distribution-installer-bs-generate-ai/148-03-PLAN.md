---
phase: 148-distribution-installer-bs-generate-ai
plan: 03
type: execute
wave: 3
depends_on: [148-01, 148-02]
files_modified:
  - src/cli/commands/install-claude-command.test.ts
autonomous: true
requirements: [DIST-01, DIST-02]

must_haves:
  truths:
    - "A real install into a temp dir produces all 5 bs-<name>/SKILL.md dirs + build/ + ingest/ + templates/ + state-machine.md + aspects/ at the correct relative depth"
    - "Every markdown-relative shared-tree reference in the installed SKILL.md bodies resolves to a file that exists in the installed tree"
    - "No *.test.ts file appears anywhere in the installed tree"
    - "design-game command/template/instructions are absent from the installed set AND grep of installer source is 0"
    - "generate-ai is absent; bs-generate-ai (with its 5 hooks) is present"
    - "The real installer test runs with skipLink:true so no npm link / global side-effect occurs"
    - "The full test suite is green and no npm dependency was added"
  artifacts:
    - path: "src/cli/commands/install-claude-command.test.ts"
      provides: "Real install-to-temp-dir integration test covering DIST-01 + DIST-02, npm-link-free via skipLink"
      contains: "mkdtempSync"
      min_lines: 60
  key_links:
    - from: "src/cli/commands/install-claude-command.test.ts"
      to: "installClaudeCommand"
      via: "real invocation against a temp cwd (local mode) with skipLink:true — file-copy layer only, no npm link"
      pattern: "skipLink"
---

<objective>
Author the REAL installer unit test (mirroring `project-scaffold.test.ts`'s temp-dir harness): install into a scratch temp dir with `skipLink:true`, assert the full DIST-01/DIST-02 layout, assert every installed SKILL.md relative reference resolves, assert `.test.ts` exclusion, assert no design-game residue and the bs-generate-ai rename — then prove the full suite green with zero new dependencies.

Purpose: This is the phase's verification gate. It runs AFTER Plan 01's skill edits and Plan 02's installer rewire so the references-resolve assertion validates the real installed layout. It tests the file-copy layer ONLY — Plan 02 added a `skipLink` option specifically so this test never runs the npm link step and never leaves a global side-effect.

Output: install-claude-command.test.ts (green) + full-suite-green confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-distribution-installer-bs-generate-ai/148-RESEARCH.md
@.planning/phases/148-distribution-installer-bs-generate-ai/148-PATTERNS.md
@.planning/phases/148-distribution-installer-bs-generate-ai/148-VALIDATION.md
@.planning/phases/148-distribution-installer-bs-generate-ai/148-01-SUMMARY.md
@.planning/phases/148-distribution-installer-bs-generate-ai/148-02-SUMMARY.md

<read_first>
- src/cli/lib/project-scaffold.test.ts — PRIMARY analog: `mkdtempSync(join(tmpdir(), 'bs-…'))` + `try { … } finally { rmSync(dir, {recursive:true, force:true}) }` (lines 117, 145-147); repoRoot via `import.meta.url` (line 129); flat `expect().toBe/.toContain` assertion style
- src/cli/commands/validate.test.ts lines 172-190 — SECONDARY analog: temp `cwd` scoping idiom for a CLI command that reads `process.cwd()` (the installer's local mode targets `join(process.cwd(),'.claude','skills')`)
- src/cli/slash-command/bs/ingest.test.ts lines 26-35 — the `read(relativePath)` `__dirname`-relative helper; adapt it to read from the INSTALLED temp tree for the references-resolve assertion
- src/cli/commands/install-claude-command.ts (post-Plan-02) — the `installClaudeCommand({ local: true, force: true, skipLink: true })` entry point under test; note local mode uses `process.cwd()`, so the test must `process.chdir(tempDir)` (save/restore original cwd in finally) OR pass a cwd the same way validate.test.ts does. Plan 02 added the `skipLink` option to `InstallOptions` expressly for this test.
- .planning/phases/148-distribution-installer-bs-generate-ai/148-VALIDATION.md — the Per-Task Verification Map + "Real test asserts" paragraph (the authoritative assertion checklist)

<interfaces>
Installed layout to assert (under tempDir/.claude/skills/):
  bs-ingest-rules/SKILL.md, bs-build-chunk/SKILL.md, bs-check-status/SKILL.md,
  bs-insert-chunk/SKILL.md, bs-generate-ai/SKILL.md,
  build/ (13 .md), ingest/ (4 .md), templates/ (6 .md), state-machine.md, aspects/ (5 .md)

References-resolve rule (SKILL.md entry-point refs are anchored to the .claude/skills root):
  For each backtick-quoted markdown ref in an installed SKILL.md, strip a leading
  '${CLAUDE_SKILL_DIR}/../' or '../' prefix, then assert existsSync(join(skillsRoot, stripped)).
  Skip game-project artifacts (rulebook/, chunks/, SKETCH.md, CHUNK.md, DESIGN.md,
  ASSETS.md, INDEX.md, PROJECT.md, STATE.md, HISTORY.md).

Scope note (WARNING resolution — option (a), prose-citation rationale):
  This references-resolve check is scoped to the 5 SKILL.md ENTRY POINTS only, NOT the internal
  cross-refs inside build/*.md and ingest/*.md. Those files contain bare citations like
  `state-machine.md` or `templates/foo.md` that are one level too shallow to resolve as literal
  paths from their own location. They are intentionally left unchecked: they are prose citations
  resolved by the orchestrating entry point's already-established `${CLAUDE_SKILL_DIR}` context
  (the agent reads them WITH the entry point's context), not independently Read-resolved from the
  cited file's own directory. This mirrors the current source tree exactly and is unchanged by
  this phase — so scanning build/*.md + ingest/*.md would produce false failures. The test
  documents this scoping decision in a comment above the references-resolve block.
</interfaces>
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the real install-to-temp-dir integration test (skipLink, no npm link)</name>
  <files>src/cli/commands/install-claude-command.test.ts</files>
  <behavior>
    - DIST-01 "installs bs- skill family": after `installClaudeCommand({local:true, force:true, skipLink:true})` into a temp cwd, all 5 `bs-<name>/SKILL.md` exist AND `build/`, `ingest/`, `templates/`, `state-machine.md`, `aspects/` exist at `.claude/skills/` root
    - DIST-01 "no dangling references": every stripped backtick-relative ref in each installed SKILL.md (entry points only) resolves via `existsSync(join(skillsRoot, stripped))`; specifically `aspects/index.md` (via ingest/interview-fallback.md's `../aspects/`) exists
    - DIST-01 "test files excluded": recursive walk of the installed tree finds zero `*.test.ts`
    - DIST-01 "no design-game residue": no `design-game*` file in the installed tree; and reading `src/cli/commands/install-claude-command.ts` yields zero `design-game` substring matches
    - DIST-01 "no npm link side-effect": the install ran with `skipLink:true`, so no global link occurs — the test touches only its temp dir
    - DIST-02 "bs-generate-ai renamed and repositioned": `bs-generate-ai/SKILL.md` exists, `generate-ai/` does NOT; the SKILL body contains all 5 hooks (objectives/threatResponseMoves/playoutPolicy/moveOrdering/uctConstant) and late-sketch-chunk framing
  </behavior>
  <action>
Create `src/cli/commands/install-claude-command.test.ts` mirroring project-scaffold.test.ts:
- `mkdtempSync(join(tmpdir(), 'bs-install-'))`; wrap the whole body in `try { … } finally { process.chdir(origCwd); rmSync(dir, {recursive:true, force:true}); }`. Save `origCwd = process.cwd()` and `process.chdir(dir)` so the installer's local-mode `process.cwd()` targets the temp dir. NEVER call global-mode install.
- Import `installClaudeCommand` from `./install-claude-command.js`. Call `await installClaudeCommand({ local: true, force: true, skipLink: true })`. The `skipLink:true` is MANDATORY — Plan 02 added it precisely so this test skips the `npm link --force` block. Do NOT run a real install without it (CONTEXT.md: no npm link side-effects in the test).
- `skillsRoot = join(dir, '.claude', 'skills')`.
- Implement a `read()` helper + a recursive `walk()` to enumerate all files under skillsRoot for the `.test.ts`-exclusion and reference-resolve checks.
- For references-resolve: scope the scan to the 5 SKILL.md ENTRY POINTS only (not the internal build/*.md or ingest/*.md cross-refs — see the <interfaces> "Scope note"; add a code comment there explaining these are prose citations resolved by the entry point's context, unchanged by this phase). Regex-extract backtick-quoted paths from each SKILL.md, strip `${CLAUDE_SKILL_DIR}/../` or leading `../`, skip the game-project artifact list, assert `existsSync(join(skillsRoot, stripped))`.
- Add an explicit no-side-effect guard: optionally spy on `child_process.execSync` (or assert it is not called with an `npm link` argument) to prove the link step never runs; at minimum assert the test writes only under `dir`. Because the install ran with `skipLink:true`, no `~/.claude/skills` or global npm-link mutation can occur.
- Name the `describe`/`it` blocks so `-t "DIST-01"` and `-t "DIST-02"` select them (VALIDATION.md sampling uses these selectors).
- Flat `expect().toBe/.toContain/.toEqual` assertions, no snapshots, no custom matchers.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/install-claude-command.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/cli/commands/install-claude-command.test.ts -t "DIST-01"` green
    - `npx vitest run src/cli/commands/install-claude-command.test.ts -t "DIST-02"` green
    - Test invokes the installer with `skipLink: true`: `grep -c "skipLink" src/cli/commands/install-claude-command.test.ts` >= 1
    - Test file contains `mkdtempSync` and `rmSync(` with `{ recursive: true, force: true }` in a `finally`
    - Test asserts zero `*.test.ts` in the installed tree and asserts `aspects/index.md` resolves
    - No npm-link / global side-effect: the install ran with `skipLink:true` so no `npm link` executes; `find ~/.claude/skills -name 'bs-ingest-rules' 2>/dev/null` is unaffected by the test (test only writes under the temp dir). Optionally an execSync spy asserts npm-link is never invoked.
  </acceptance_criteria>
  <done>The integration test installs to a temp dir with skipLink:true (no npm link), asserts the full layout + entry-point references-resolve + .test.ts-exclusion + no-design-game + bs-generate-ai rename, cleans up in finally, and is green.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite green + zero-dep + zero-residual gate</name>
  <files>src/cli/commands/install-claude-command.test.ts</files>
  <action>
Run the phase gate:
1. `npm test` (full accumulated suite) — must be green, including the bs/ drift suites (unchanged-green after Plan 01's substring-preserving edits).
2. Confirm zero new dependency: `git diff package.json package-lock.json` shows no added dependency lines.
3. Confirm zero design-game residue across the whole repo surface touched: `grep -rc design-game src/cli` == 0.
4. If any suite is red, fix the cause (do NOT weaken assertions or fake data per CLAUDE.md). Common expected fix sites: the references-resolve regex over-matching a game-project artifact (extend the skip list), or a stripped-prefix edge case.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -15 && echo "design-game residue:" && grep -rc design-game src/cli | grep -v ':0' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (full suite green)
    - `npx vitest run src/cli/slash-command/bs/` green (drift suites unaffected)
    - `grep -rc design-game src/cli | grep -v ':0'` returns nothing (zero files with residue)
    - `git diff --stat package.json package-lock.json` shows no dependency additions
  </acceptance_criteria>
  <done>Full suite green, bs/ drift suites green, zero design-game residue in src/cli, zero new npm dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → filesystem (temp dir only) | The test confines writes to a temp dir; the skipLink option guarantees no global ~/.claude skills install or npm-link side-effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-08 | Tampering | test leaving global ~/.claude/skills or npm-link side-effect | mitigate | `installClaudeCommand({ skipLink: true })` skips the link step entirely; local-mode-only install into a temp cwd; rmSync + chdir-restore in finally; assert only filesystem layout |
| T-148-04 | Tampering | *.test.ts leaking into installed set (regression guard) | mitigate | Test asserts recursive walk finds zero `*.test.ts` |
| T-148-05 | Information disclosure | design-game residue (regression guard) | mitigate | Test + Task 2 grep assert zero design-game in installed tree and installer source |
| T-148-SC | Tampering | npm/pip/cargo installs | N/A | No package installs; Task 2 asserts package.json/lock unchanged |
| T-148-09 | Spoofing / Repudiation / Elevation / DoS | — | N/A | Local test harness; no auth/session/network surface |
</threat_model>

<verification>
- `npx vitest run src/cli/commands/install-claude-command.test.ts` green (DIST-01 + DIST-02 selectors)
- Test invokes installer with `skipLink:true` — no npm link runs
- `npm test` full suite green
- `npx vitest run src/cli/slash-command/bs/` green
- `grep -rc design-game src/cli` == 0 everywhere
- No global ~/.claude/skills or npm-link side-effect from the test
</verification>

<success_criteria>
The real installer test proves the DIST-01/DIST-02 layout (5 bs- skills + shared tree, entry-point references resolve, .test.ts excluded, design-game absent, bs-generate-ai present with hooks) with `skipLink:true` so no npm link runs, the full suite is green, and no npm dependency was added.
</success_criteria>

<output>
Create `.planning/phases/148-distribution-installer-bs-generate-ai/148-03-SUMMARY.md` when done
</output>
</output>
