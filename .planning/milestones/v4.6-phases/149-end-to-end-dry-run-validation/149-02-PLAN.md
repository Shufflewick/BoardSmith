---
phase: 149-end-to-end-dry-run-validation
plan: 02
type: execute
wave: 2
depends_on: ["149-01"]
files_modified:
  - "<scratch>/go-fish-dryrun/**"   # generated chunk-1 code + CHUNK.md sections + tests (throwaway)
autonomous: true
requirements: [VAL-01]
must_haves:
  truths:
    - "The chunk-1 build steps (investigate → redteam → build → test, plus audit → repair) run by following the real build-chunk skill files"
    - "Generated chunk-1 Go Fish code compiles, lints clean, and passes its automated test step for real"
    - "simulateRandomGames reaches terminal state without crash/stuck/timeout/exceededMaxActions"
    - "The audit visibility lens proves opponent hands and drawn-card identity do NOT leak (diffPlayerViews + assertNoHiddenInfoLeak)"
    - "The ask-gate proposal and the playtest script are captured as artifacts (not blocked on a human)"
  artifacts:
    - path: "<scratch>/go-fish-dryrun/src/rules/"
      provides: "generated chunk-1 game code (game/elements/actions/flow equivalents)"
    - path: "<scratch>/go-fish-dryrun/chunks/*/CHUNK.md"
      provides: "Interpretation + Visibility Declaration + Redteam Rounds + Findings Ledger filled by the build steps"
      contains: "Visibility Declaration"
    - path: "<scratch>/go-fish-dryrun/ASK-PROPOSAL.md"
      provides: "captured ask-gate design proposal artifact"
    - path: "<scratch>/go-fish-dryrun/PLAYTEST-SCRIPT.md"
      provides: "captured click-by-click playtest script artifact (feeds the Plan 03 HUMAN-UAT item)"
  key_links:
    - from: "generated ask action"
      to: "diffPlayerViews / assertNoHiddenInfoLeak"
      via: "audit visibility lens on generated code"
      pattern: "diffPlayerViews|assertNoHiddenInfoLeak"
    - from: "generated chunk-1 code"
      to: "simulateRandomGames"
      via: "test step random-sim run"
      pattern: "simulateRandomGames"
---

<objective>
Dry-run the chunk-1 BUILD leg of `/bs-build-chunk` against the Go Fish core event loop by having fresh-context subagents FOLLOW the actual build-chunk machine steps (investigate → redteam → build → test, then audit → repair), producing real generated chunk-1 code and running the REAL automated checks (tsc, `boardsmith lint`, unit/integration, full suite, `simulateRandomGames`, the a11y floor, and the audit's `diffPlayerViews`/`assertNoHiddenInfoLeak` hidden-hand leak check) against that generated code. The `ask` and `playtest` HUMAN GATES are run up-to and CAPTURED as artifacts, not blocked.

Purpose: Prove the build-chunk skills are followable end-to-end and that the generated game actually compiles, passes its test discipline, and correctly redacts hidden hands — the single highest-value proof of the whole pipeline.
Output: Generated chunk-1 code + filled CHUNK.md sections + captured ask/playtest artifacts + a record of every real check's pass/fail (for Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/149-end-to-end-dry-run-validation/149-CONTEXT.md
@.planning/phases/149-end-to-end-dry-run-validation/149-RESEARCH.md
@.planning/phases/149-end-to-end-dry-run-validation/149-01-SUMMARY.md

<path_translation_note>
Same as Plan 01: skills are read FROM SOURCE. `${CLAUDE_SKILL_DIR}/../bs-shared/X` → `src/cli/slash-command/bs/X`. `investigate/redteam/ask/build/test/audit/repair` all live under `src/cli/slash-command/bs/build/`. Every subagent dispatch MUST state this translation. Fresh-context-per-step is a HARD rule in the skill files — never let one conversation narrate all steps; each step is a genuine separate Task dispatch reading the real skill file, or the dry-run proves nothing about the skill's context-economics design.
</path_translation_note>

<scaled_fanout_note>
Scaling down the skill's full adversarial fan-out is ACCEPTABLE for a dry-run (e.g. fewer redteam/audit agents) AS LONG AS each step's LOGIC is exercised (framing-free dispatch, independent context, the real return shape). Record any scaling in the artifacts so Plan 03's report notes it. Chunk 1 is NEVER light-ceremony — it is the mandated first full-ceremony chunk. Do not shortcut a step "because Go Fish is well-known."
</scaled_fanout_note>

<hidden_info_baseline>
The hand-built go-fish encodes the redaction the audit must confirm: `Hand.contentsVisibleToOwner()`, `Pond.contentsHidden()`, and every `game.message()` reveals only the PUBLIC outcome (who asked / rank / count / "Go Fish!") — never the drawn card's identity. The generated chunk-1 code must follow the same public/private discipline; any `game.message()` leaking a drawn card's identity to all seats is exactly the defect `diffPlayerViews`/`assertNoHiddenInfoLeak` exists to catch. Chunk 1 may legitimately OMIT/stub `checkForBooks()` + the 13-book win condition (out of scope) as long as it's documented as an intentional depth cut, not a missed rule.
</hidden_info_baseline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: investigate + redteam → CHUNK.md Interpretation, Visibility Declaration, Redteam Rounds; capture ask-gate proposal</name>
  <read_first>
    - src/cli/slash-command/bs/build-chunk.md (Steps 1-3 dispatch: investigate, redteam, ask)
    - src/cli/slash-command/bs/build/investigate.md
    - src/cli/slash-command/bs/build/redteam.md
    - src/cli/slash-command/bs/build/ask.md
    - <scratch>/go-fish-dryrun/SKETCH.md + chunks/<slug>/CHUNK.md (from Plan 01)
  </read_first>
  <action>Dispatch a fresh-context INVESTIGATE subagent following `bs/build/investigate.md` for chunk 1: it reads the cited + INDEX-discovered rulebook slices, RULINGS.md, DECISIONS.md, and the doc set (core-concepts, common-pitfalls, actions-and-flow — chunk 1 has actions; custom-ui-guide/ui-components — ui: major), and WRITES directly into CHUNK.md `## Interpretation`, `## Visibility Declaration`, `## Newly Discovered Citations`, returning `{claimsList, visibilityDeclaration, newlyDiscoveredCitations}`. The Visibility Declaration MUST state: asked rank + outcome (gave cards / go fish) are public; a card drawn from the pond is hidden from all other seats until played; opponents' hand contents are hidden from everyone but the owner. Then dispatch a REDTEAM round following `bs/build/redteam.md` — framing-free (claims list only, no investigator rationale), scaled to 2 refuters + 1 coverage adversary (record the scaling). The coverage adversary should catch omitted rules like "empty-hand auto-draw before the turn ends" or "extra turn on a matching pond draw". Write verdicts into CHUNK.md `## Redteam Rounds`. Finally run the `ask` step up-to the human gate (`bs/build/ask.md`) and CAPTURE its design proposal to `<scratch>/go-fish-dryrun/ASK-PROPOSAL.md` — the designer subagent auto-approves; do NOT block.</action>
  <verify>
    <automated>grep -q "Visibility Declaration" "$SCRATCH/go-fish-dryrun/chunks/"*/CHUNK.md && test -f "$SCRATCH/go-fish-dryrun/ASK-PROPOSAL.md"</automated>
  </verify>
  <acceptance_criteria>
    - CHUNK.md `## Interpretation` has a numbered claims list; `## Visibility Declaration` states public-outcome / hidden-drawn-card / hidden-opponent-hands
    - CHUNK.md `## Redteam Rounds` has refuter verdicts + coverage-adversary findings; any fan-out scaling recorded
    - `ASK-PROPOSAL.md` captured (ask gate not blocked)
  </acceptance_criteria>
  <done>Interpretation + Visibility Declaration + Redteam Rounds filled by real fresh-context dispatches; ask-gate proposal captured.</done>
</task>

<task type="auto">
  <name>Task 2: build + test → generate chunk-1 code and run the REAL automated test step</name>
  <read_first>
    - src/cli/slash-command/bs/build-chunk.md (Steps build + test dispatch)
    - src/cli/slash-command/bs/build/build.md (fresh-context raw-slice read exception; real controller wiring)
    - src/cli/slash-command/bs/build/test.md (the non-reorderable numbered command sequence + a11y floor)
    - src/testing/random-simulation.ts (simulateRandomGames result fields)
    - src/cli/lib/sandbox-scan.ts (the 7 boardsmith lint rules)
  </read_first>
  <action>Dispatch a fresh-context BUILD subagent following `bs/build/build.md` to generate the chunk-1 code in the scratch project (game/elements/actions/flow equivalents + the chunk-1 UI + generated tests): deal hands, the `ask` action (choose target player, then a rank the asker holds — enforced), give-all-matching-on-hit / draw-from-pond-on-miss, extra turn on a hit or a matching pond draw. It MUST wire the REAL controller pattern (`useActionController` + `useBoardActionBridge` + `createBoardInteraction`) from the start — NOT a mocked controller (Pitfall 4: a mock passes a shallow keyboard test but misses the real fill → fetchChoicesForPick → snapshotVersion++ → currentChoices chain). Public `game.message()` calls must reveal only public outcomes, never a drawn card's identity. Then dispatch a TEST subagent following `bs/build/test.md` and RUN the numbered sequence for REAL against the generated code: (1) `npx tsc --noEmit`, (2) `npx boardsmith lint` (7 sandbox rules), (3) chunk unit/integration tests, (4) full accumulated suite, (5) `simulateRandomGames` asserting `crashed===0 && stuck===0 && timedOut===0 && exceededMaxActions===0`, (6) the 5-item a11y floor (keyboard-only ActionPanel completion against the REAL controller, axe-core scan, no-color-literal grep + contrast, real aria-labelled controls, focus + prefers-reduced-motion). Kill any dev/preview server started. Record each check's real pass/fail for the Plan 03 report. If a check fails due to a SKILL-INSTRUCTION defect (not a generation slip), note it as a candidate pipeline defect.</action>
  <verify>
    <automated>cd "$SCRATCH/go-fish-dryrun" && npx tsc --noEmit && npx boardsmith lint && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Generated chunk-1 code exists and `npx tsc --noEmit` is clean
    - `npx boardsmith lint` passes (all 7 sandbox rules)
    - Chunk unit/integration + full generated suite pass
    - `simulateRandomGames` reports crashed/stuck/timedOut/exceededMaxActions all 0
    - a11y floor runs against the REAL controller (not a mock); axe-core scan clean
    - Any server started was killed; each check's pass/fail recorded
  </acceptance_criteria>
  <done>Generated chunk-1 Go Fish code compiles, lints clean, and passes its real automated test step including random-sim and the a11y floor.</done>
</task>

<task type="auto">
  <name>Task 3: audit + repair → visibility/fidelity/undo lenses (real leak check) + repair loop; capture playtest script</name>
  <read_first>
    - src/cli/slash-command/bs/build-chunk.md (audit + repair + playtest dispatch)
    - src/cli/slash-command/bs/build/audit.md (3 lenses + design-review lens for ui: major)
    - src/cli/slash-command/bs/build/design-review.md
    - src/cli/slash-command/bs/build/repair.md
    - src/cli/slash-command/bs/build/playtest.md
    - src/testing/view-diff.ts, src/testing/dom-leak.ts (diffPlayerViews, assertNoHiddenInfoLeak)
  </read_first>
  <action>Dispatch fresh-context AUDIT subagents following `bs/build/audit.md`: the fidelity lens, the VISIBILITY lens (highest value), the undo lens, plus the design-review lens (`design-review.md`) because chunk 1 is `ui: major`. Scale the fan-out if needed but exercise every lens's logic (record scaling). The visibility lens MUST run `diffPlayerViews(testGame, seatA, seatB)` and `assertNoHiddenInfoLeak(...)` for REAL against the generated code — proving opponent hands and any drawn-card identity are redacted. Findings land in CHUNK.md `## Findings Ledger` with stable IDs (F1, F2, …). Then dispatch a REPAIR subagent following `bs/build/repair.md` (fix-or-refute-with-citation, max 3 rounds, round-N+1 auditors see only new findings) until the ledger is clean; re-run the affected test-step checks after repair. Finally run the `playtest` step up-to its human gate (`bs/build/playtest.md`) and CAPTURE its click-by-click script to `<scratch>/go-fish-dryrun/PLAYTEST-SCRIPT.md` — do NOT start a server for a human here; that server + walkthrough is the deferred HUMAN-UAT item (Plan 03). Record the leak-check result and any surfaced skill defects for the report.</action>
  <verify>
    <automated>grep -q "Findings Ledger" "$SCRATCH/go-fish-dryrun/chunks/"*/CHUNK.md && test -f "$SCRATCH/go-fish-dryrun/PLAYTEST-SCRIPT.md" && cd "$SCRATCH/go-fish-dryrun" && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - CHUNK.md `## Findings Ledger` populated by real fresh-context audit lenses (fidelity, visibility, undo, design-review)
    - Visibility lens ran `diffPlayerViews` + `assertNoHiddenInfoLeak` for real and confirms NO opponent-hand / drawn-card leak
    - Repair loop closed the ledger (fix-or-refute-with-citation); post-repair checks green
    - `PLAYTEST-SCRIPT.md` captured (playtest gate not blocked); no human server started
  </acceptance_criteria>
  <done>Chunk-1 code audited (leak check passing), repaired to a clean ledger, and the playtest script captured — chunk-1 build leg complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player seat A ↔ player seat B (in generated game) | Opponent hand contents + drawn-card identity must not cross seats — the core Go Fish hidden-info boundary |
| dry-run → hand-built baseline | `~/BoardSmithGames/go-fish/` stays READ-ONLY |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-02 | Information Disclosure | generated `ask` action + Hand/Pond visibility in the chunk-1 code | mitigate | Audit visibility lens runs `diffPlayerViews(testGame, seatA, seatB)` + `assertNoHiddenInfoLeak(...)` for real on the generated code; build subagent instructed that `game.message()` reveals only public outcomes, never a drawn card's identity |
| T-149-01 | Tampering | hand-built go-fish baseline | mitigate | Build leg generates only in the scratch dir; the hand-built game is read (Plan 03) but never written |
| T-149-SC | Tampering | npm installs | accept | No new packages; generated project uses the scaffold's pre-pinned devDeps + `file:` dep on this repo |
| T-149-S/R/D/E | Spoofing/Repudiation/DoS/Elevation | — | accept | N/A — local dry-run, no auth/session/network surface (the `no-network` sandbox rule is itself asserted by `boardsmith lint` in Task 2) |
</threat_model>

<verification>
- Generated chunk-1 code compiles, lints clean, passes unit/integration + full suite
- `simulateRandomGames` all-zero on the four failure fields
- a11y floor runs against the REAL controller; axe-core clean
- Visibility lens `diffPlayerViews` + `assertNoHiddenInfoLeak` confirm no hidden-hand leak
- Findings ledger closed by repair; ask + playtest gates captured as artifacts
- Any server started was killed; `~/BoardSmithGames/` untouched
</verification>

<success_criteria>
The chunk-1 build leg ran end-to-end by following the real build-chunk skills, producing generated Go Fish chunk-1 code that compiles, passes its real automated test step (including random-sim and the a11y floor), and passes the audit's two-seat hidden-hand leak check, with the ask-gate proposal and playtest script captured as artifacts and every check's outcome recorded for the report.
</success_criteria>

<output>
Create `.planning/phases/149-end-to-end-dry-run-validation/149-02-SUMMARY.md` when done. Record each real check's pass/fail, the leak-check result, any fan-out scaling, and any surfaced pipeline defects (for Plan 03).
</output>
