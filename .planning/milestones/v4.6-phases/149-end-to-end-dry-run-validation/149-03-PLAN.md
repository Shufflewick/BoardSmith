---
phase: 149-end-to-end-dry-run-validation
plan: 03
type: execute
wave: 3
depends_on: ["149-01", "149-02"]
files_modified:
  - "src/cli/slash-command/bs/**"   # pipeline-defect fixes (skill markdown) — only files with surfaced defects
  - ".planning/phases/149-end-to-end-dry-run-validation/149-DRYRUN-REPORT.md"
  - ".planning/phases/149-end-to-end-dry-run-validation/149-HUMAN-UAT.md"
autonomous: true
requirements: [VAL-01]
must_haves:
  truths:
    - "The dry-run's chunk-1 output is compared against the hand-built go-fish on rule fidelity, hidden-info handling, BoardSmith idiom, and artifact/template quality"
    - "Every surfaced pipeline defect is fixed at source in the bs- skill files, with the bs/ drift suites and full BoardSmith suite staying green"
    - "149-DRYRUN-REPORT.md documents what ran, the comparison table, the defect log + dispositions, and the deferred human-playtest items"
    - "A HUMAN-UAT item exists for the browser playtest (the one gate the milestone ships with outstanding)"
  artifacts:
    - path: ".planning/phases/149-end-to-end-dry-run-validation/149-DRYRUN-REPORT.md"
      provides: "the dry-run report: run log + comparison table + defect ledger + deferred items"
      contains: "Comparison"
    - path: ".planning/phases/149-end-to-end-dry-run-validation/149-HUMAN-UAT.md"
      provides: "deferred browser-playtest manual-verification item"
      contains: "playtest"
  key_links:
    - from: "surfaced pipeline defect"
      to: "src/cli/slash-command/bs/*.md"
      via: "fix at source, not worked around in output"
      pattern: "bs/"
    - from: "any bs- skill fix"
      to: "bs/*.test.ts drift suites"
      via: "re-run to stay green"
      pattern: "vitest run src/cli/slash-command/bs/"
---

<objective>
Close the dry-run: compare the chunk-1 dry-run output against the hand-built `~/BoardSmithGames/go-fish/` (READ-ONLY) on the four locked axes, FIX every surfaced pipeline defect at source in the bs- skill files (keeping the drift suites + full BoardSmith suite green), write `149-DRYRUN-REPORT.md`, and create the HUMAN-UAT item for the deferred browser playtest.

Purpose: Turn the dry-run's observations into shipped pipeline fixes and an honest completion record — every defect that would have shipped a broken pipeline to a real designer is fixed before the milestone closes, with the single human-by-design playtest gate captured as the one outstanding item.
Output: Fixed bs- skill files (drift-green), 149-DRYRUN-REPORT.md, 149-HUMAN-UAT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/149-end-to-end-dry-run-validation/149-CONTEXT.md
@.planning/phases/149-end-to-end-dry-run-validation/149-RESEARCH.md
@.planning/phases/149-end-to-end-dry-run-validation/149-01-SUMMARY.md
@.planning/phases/149-end-to-end-dry-run-validation/149-02-SUMMARY.md
@.planning/phases/149-end-to-end-dry-run-validation/149-VALIDATION.md

<comparison_rules>
Compare on the four LOCKED axes (CONTEXT.md), NOT line-for-line identity: (1) rule fidelity — does the generated core loop match Go Fish (ask a held rank, give-all-or-Go-Fish-draw, extra turn on hit/matching draw)?; (2) hidden-info handling — does it redact opponent hands / drawn-card identity via visibleAttributes-style declarations like the hand-built `contentsVisibleToOwner()`/`contentsHidden()`?; (3) BoardSmith idiom — correct engine usage (elements/actions/flow)?; (4) artifact quality — do SKETCH.md/CHUNK.md parse against the templates? A data-model or naming difference is ACCEPTABLE DIVERGENCE (document it), not a defect. Chunk-1's out-of-scope omissions (books/13-book win) are acceptable IF documented as intentional depth cuts. Hand-built baseline files: `~/BoardSmithGames/go-fish/src/rules/{game,elements,actions,flow}.ts` — READ ONLY.
</comparison_rules>

<defect_disposition>
Each discrepancy is RECONCILED (fix the pipeline at the bs- skill source) or DOCUMENTED (acceptable divergence). Pipeline defects = skill-logic bugs, template gaps, gate friction, wrong/missing instructions in the bs- skills. Fix at SOURCE — never work around in the generated output (CLAUDE.md: no hacks/fallbacks). Every fix re-runs the bs/ drift suites; if a fix intentionally changes pinned skill text, update the drift test in lockstep. Also fix any harness-level issue found (e.g. the Pitfall-2 `bs-shared/` path-layout assumption vs. the Phase 148 installer) — but categorize it correctly (harness-mapping vs. real skill/installer defect).
</defect_disposition>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compare dry-run chunk-1 vs hand-built go-fish on the four axes</name>
  <read_first>
    - .planning/phases/149-end-to-end-dry-run-validation/149-02-SUMMARY.md (what the build leg produced + checks)
    - <scratch>/go-fish-dryrun/src/rules/ + SKETCH.md + chunks/<slug>/CHUNK.md (dry-run output)
    - ~/BoardSmithGames/go-fish/src/rules/{game,elements,actions,flow}.ts (READ-ONLY baseline)
    - 149-RESEARCH.md "Hand-Built Go-Fish Baseline" section
  </read_first>
  <action>Compare the dry-run's chunk-1 artifacts against the hand-built go-fish on the four locked axes (see comparison_rules). For each axis, note MATCH / ACCEPTABLE-DIVERGENCE / DEFECT with concrete evidence (file:line where load-bearing). Rule fidelity: ask-a-held-rank enforcement, give-all-on-hit, Go-Fish draw, extra-turn-on-hit-or-matching-draw, empty-hand auto-draw. Hidden-info: does the generated code redact opponent hands + drawn-card identity as the baseline does (`contentsVisibleToOwner()`/`contentsHidden()`, public-only `game.message()`)? BoardSmith idiom: elements/actions/flow usage vs. baseline. Artifact quality: SKETCH.md/CHUNK.md template-parse. Produce a comparison table (the core of the report). READ the go-fish baseline only — never write it.</action>
  <verify>
    <automated>test -d "$SCRATCH/go-fish-dryrun/src" && test -d ~/BoardSmithGames/go-fish/src/rules</automated>
  </verify>
  <acceptance_criteria>
    - A comparison table exists covering all four axes, each row marked MATCH / ACCEPTABLE-DIVERGENCE / DEFECT with evidence
    - Hidden-info row explicitly references the leak-check result from Plan 02
    - Out-of-scope omissions (books/win) recorded as intentional depth cuts, not defects
    - `~/BoardSmithGames/go-fish/` was only read, never modified
  </acceptance_criteria>
  <done>Comparison of dry-run chunk-1 vs hand-built go-fish complete on all four axes, with a defect/divergence-classified table.</done>
</task>

<task type="auto">
  <name>Task 2: Fix every surfaced pipeline defect at source; keep drift suites + full suite green</name>
  <read_first>
    - .planning/phases/149-end-to-end-dry-run-validation/149-01-SUMMARY.md, 149-02-SUMMARY.md (defects surfaced by ingest + build legs)
    - Task 1 comparison table (this plan)
    - src/cli/slash-command/bs/ (the skill files to fix)
    - src/cli/slash-command/bs/{ingest,build-chunk,templates}.test.ts, status-tools.test.ts (drift suites)
  </read_first>
  <action>Aggregate every DEFECT surfaced across the three legs (ingest friction, build/test skill-instruction bugs, audit gaps, comparison-found rule/idiom/template defects, and any Pitfall-2 path-layout/installer issue). For each: fix it AT SOURCE in the relevant `src/cli/slash-command/bs/*.md` skill file (or the installer/scaffold if that is the true source) — never work around it in the generated output. If a fix changes a drift-pinned string, update the corresponding assertion in the bs/ drift test in lockstep. After the fixes, run `npx vitest run src/cli/slash-command/bs/` (drift suites MUST stay green) and then the full `npm test` (no regression from the skill/scaffold edits). If NO defects were surfaced, record that explicitly (a clean dry-run is a valid outcome) and still run the drift suites to confirm the baseline is green. Log each defect + its disposition (fixed at <file> / documented as acceptable divergence) for the report.</action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/cli/slash-command/bs/ && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Every surfaced pipeline defect is fixed at source in bs- skill files (or the true source) OR documented as acceptable divergence — none worked around in generated output
    - `npx vitest run src/cli/slash-command/bs/` is green (drift-pinned strings updated in lockstep with any skill text change)
    - Full `npm test` is green (no regression from the fixes)
    - Defect log with dispositions captured for the report
  </acceptance_criteria>
  <done>All surfaced pipeline defects fixed at source with bs/ drift suites and the full BoardSmith suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Write 149-DRYRUN-REPORT.md and create the HUMAN-UAT playtest item</name>
  <read_first>
    - .planning/phases/149-end-to-end-dry-run-validation/149-01-SUMMARY.md, 149-02-SUMMARY.md
    - Task 1 comparison table + Task 2 defect log (this plan)
    - <scratch>/go-fish-dryrun/PLAYTEST-SCRIPT.md (captured in Plan 02)
    - .planning/phases/149-end-to-end-dry-run-validation/149-VALIDATION.md (Manual-Only Verifications section)
  </read_first>
  <action>Write `.planning/phases/149-end-to-end-dry-run-validation/149-DRYRUN-REPORT.md` documenting: (a) WHAT RAN — the scratch path, ingest path (interview-fallback), which build-chunk steps ran and any fan-out scaling, and every real automated check's pass/fail (tsc, boardsmith lint, unit/integration, full suite, simulateRandomGames, a11y floor, diffPlayerViews/assertNoHiddenInfoLeak); (b) the COMPARISON TABLE from Task 1; (c) the DEFECT LOG from Task 2 with each disposition (fixed at <file> / documented divergence) and drift-suite-green confirmation; (d) DEFERRED items — the human browser-playtest and the live-installed-skills run. Then create `.planning/phases/149-end-to-end-dry-run-validation/149-HUMAN-UAT.md` as the manual-verification item for the browser playtest: the user runs `npx boardsmith dev` on the scratch chunk-1 project (or hand-built go-fish) and walks the captured PLAYTEST-SCRIPT.md — this is the ONE gate the milestone ships with outstanding (verification status human_needed, per VALIDATION.md). Note the scratch dir is throwaway; if cleaning it up, first copy PLAYTEST-SCRIPT.md into the phase dir so the UAT item is self-contained.</action>
  <verify>
    <automated>test -f .planning/phases/149-end-to-end-dry-run-validation/149-DRYRUN-REPORT.md && grep -qi "comparison" .planning/phases/149-end-to-end-dry-run-validation/149-DRYRUN-REPORT.md && test -f .planning/phases/149-end-to-end-dry-run-validation/149-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `149-DRYRUN-REPORT.md` contains the run log (all real checks' pass/fail), the comparison table, the defect log + dispositions, and the deferred items
    - `149-HUMAN-UAT.md` is a self-contained browser-playtest manual item (script available even after scratch cleanup)
    - Report states the completion bar: dry-run ran clean, comparison documented, defects fixed (drift green), human playtest deferred
  </acceptance_criteria>
  <done>149-DRYRUN-REPORT.md and 149-HUMAN-UAT.md written; the dry-run's outcome and the one outstanding human gate are recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dry-run → hand-built baseline | The comparison READS `~/BoardSmithGames/go-fish/` but must never write it |
| skill fix → drift suites | Any bs- skill edit crosses the pinned-string boundary the drift tests guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-01 | Tampering | hand-built go-fish baseline | mitigate | Task 1 opens go-fish files read-only for comparison; no write path to `~/BoardSmithGames/` in any task |
| T-149-02 | Information Disclosure | generated hidden-hand redaction (reported) | mitigate | The report records the Plan 02 `diffPlayerViews`/`assertNoHiddenInfoLeak` leak-check result; any leak surfaced becomes a fixed-at-source defect in Task 2 |
| T-149-03 | Tampering | bs- skill fixes vs. drift suites | mitigate | Every fix re-runs `npx vitest run src/cli/slash-command/bs/` + full `npm test`; drift-pinned strings updated in lockstep so a fix cannot silently break the guard |
| T-149-SC | Tampering | npm installs | accept | No new packages introduced; fixes are skill-markdown edits |
| T-149-S/R/D/E | Spoofing/Repudiation/DoS/Elevation | — | accept | N/A — local dry-run, no auth/session/network surface |
</threat_model>

<verification>
- Comparison table covers all four axes with defect/divergence classification
- Every surfaced defect fixed at source or documented; `npx vitest run src/cli/slash-command/bs/` green; full `npm test` green
- 149-DRYRUN-REPORT.md contains run log + comparison table + defect log + deferred items
- 149-HUMAN-UAT.md created for the browser playtest (the one outstanding gate)
- `~/BoardSmithGames/` untouched
</verification>

<success_criteria>
The dry-run is closed: chunk-1 output compared against the hand-built go-fish on all four locked axes, every surfaced pipeline defect fixed at source with the bs/ drift suites and full BoardSmith suite green, the dry-run report written, and the human browser-playtest captured as the single outstanding HUMAN-UAT item — VAL-01 satisfied for the chunk-1 dry-run scope with verification status human_needed by design.
</success_criteria>

<output>
Create `.planning/phases/149-end-to-end-dry-run-validation/149-03-SUMMARY.md` when done. Reference 149-DRYRUN-REPORT.md and 149-HUMAN-UAT.md, and note whether any bs- skill files were changed (and their drift-suite status).
</output>
