---
phase: 150-regenerate-the-pipeline-built-go-fish-stable-location
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/BoardSmithGames/go-fish-dryrun/                       # entire scaffolded project (created here)
  - ~/BoardSmithGames/go-fish-dryrun/rulebook/*.md          # ingest slices + INDEX + visual survey
  - ~/BoardSmithGames/go-fish-dryrun/ASSETS.md
  - ~/BoardSmithGames/go-fish-dryrun/SKETCH.md
  - ~/BoardSmithGames/go-fish-dryrun/chunks/core-event-loop/CHUNK.md
  - ~/BoardSmithGames/go-fish-dryrun/RULINGS.md
  - ~/BoardSmithGames/go-fish-dryrun/DECISIONS.md
autonomous: true
requirements: [GEN-01]

must_haves:
  truths:
    - "A durable Go Fish project exists at ~/BoardSmithGames/go-fish-dryrun/ (NOT under /tmp), scaffolded via the real bs- ingest leg"
    - "The freshly scaffolded skeleton compiles (tsc --noEmit clean) and serves (dev reachable then killed) before any ingest content is written"
    - "SKETCH.md lists chunk-1 core-event-loop detailed, with game-end-scoring + final-acceptance as sketch-level tail entries"
    - "chunk-1 CHUNK.md exists and byte-matches CHUNK.template.md's PARSE CONTRACT headings"
    - "RULINGS.md and DECISIONS.md are seeded as empty ledgers"
  artifacts:
    - path: "~/BoardSmithGames/go-fish-dryrun/SKETCH.md"
      provides: "Ordered chunk list, UI Strategy (custom-from-chunk-1), mandated tail"
      contains: "Status: proposed (sketch-level — no CHUNK.md yet)"
    - path: "~/BoardSmithGames/go-fish-dryrun/chunks/core-event-loop/CHUNK.md"
      provides: "Chunk-1 detail (ui: major, full ceremony, cited slices)"
      contains: "## Ceremony"
    - path: "~/BoardSmithGames/go-fish-dryrun/rulebook/INDEX.md"
      provides: "Term → slice cross-reference from interview citedTerms"
    - path: "~/BoardSmithGames/go-fish-dryrun/rulebook/00-visual-survey.md"
      provides: "Visual evidence for the first UI chunk's design ask"
  key_links:
    - from: "bs/ingest/scaffold.md"
      to: "~/BoardSmithGames/go-fish-dryrun/"
      via: "npx boardsmith init go-fish-dryrun (creates subdir, git-inits, file: dep to local BoardSmith)"
      pattern: "boardsmith init"
    - from: "bs/ingest/interview-fallback.md"
      to: "~/BoardSmithGames/go-fish-dryrun/rulebook/*.md"
      via: "interview answers → rulebook slices → citedTerms → INDEX.md"
      pattern: "rulebook/"
---

<objective>
Run the `bs-` pipeline's ingest leg (following the real skill files) against a clean, complete standard Go Fish ruleset into a STABLE, durable project directory `~/BoardSmithGames/go-fish-dryrun/` — a sibling of the other example games, NOT `/tmp`. This reproduces the Phase-149 ingest output (which lived only in a deleted `/tmp` scratch dir) somewhere permanent, so Phase 151 has a real pipeline-built game to playtest.

Purpose: GEN-01 — the pipeline's OUTPUT must survive cleanup. This plan lays the scaffold + sketch/chunk state that the chunk-1 build leg (Plan 02) then builds against.
Output: A scaffolded, compiling, serving Go Fish project with rulebook slices, INDEX.md, ASSETS.md, 00-visual-survey.md, SKETCH.md, chunk-1 CHUNK.md, and empty RULINGS/DECISIONS ledgers.

Scope discipline: interview-fallback ingest path (no rulebook to transcribe), matching the 149 input so output stays comparable. The hand-built `~/BoardSmithGames/go-fish/` is READ-ONLY reference — NEVER written.

Path translation (repo-relative dry-run, same as 149): every `${CLAUDE_SKILL_DIR}/../bs-shared/X` reference in a skill file resolves to `/Users/jtsmith/BoardSmith/src/cli/slash-command/bs/X` (the `ingest/`, `build/`, `templates/`, `state-machine.md` siblings live directly under `src/cli/slash-command/bs/` in source — there is no separate `bs-shared/` dir until install).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-regenerate-the-pipeline-built-go-fish-stable-location/150-CONTEXT.md

# The ingest leg skill files to FOLLOW (read as you execute each step):
@src/cli/slash-command/bs/ingest-rules.md
@src/cli/slash-command/bs/ingest/scaffold.md
@src/cli/slash-command/bs/ingest/interview-fallback.md
@src/cli/slash-command/bs/ingest/sketch-derivation.md

# Source-of-truth for what the 149 ingest produced (reproduce its shape):
@.planning/milestones/v4.6-phases/149-end-to-end-dry-run-validation/149-DRYRUN-REPORT.md
@.planning/milestones/v4.6-phases/149-end-to-end-dry-run-validation/149-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold + verify the durable project</name>
  <files>~/BoardSmithGames/go-fish-dryrun/ (entire scaffold)</files>
  <read_first>
    - src/cli/slash-command/bs/ingest-rules.md (Step 0 state detection + Step 1 Scaffold)
    - src/cli/slash-command/bs/ingest/scaffold.md (name derivation, init, verification sequence, kill step)
    - 149-DRYRUN-REPORT.md §1 "Ingest leg (149-01)" and §3 D1/D2 dispositions (the tsconfig `vite/client` and `git init` scaffold defects were fixed at source — a fresh scaffold now compiles clean and is a git repo)
  </read_first>
  <action>
    Follow `bs/ingest/scaffold.md`. From parent dir `~/BoardSmithGames/`, run `npx boardsmith init go-fish-dryrun` (the "designer" game-name answer is "Go Fish"; the project/dir name is `go-fish-dryrun` to keep it visibly distinct from the hand-built `go-fish` reference). Because init unconditionally creates `<cwd>/<name>` and errors if it exists, confirm `~/BoardSmithGames/go-fish-dryrun/` does NOT already exist first. Then `cd ~/BoardSmithGames/go-fish-dryrun`, `npm install`, run the non-reorderable verification sequence: (1) `npx tsc --noEmit` clean; (2) `npx boardsmith dev --no-open`, wait for the exact line `Ready! Press Ctrl+C to stop.`, curl the resolved dev URL for a non-error response; (3) KILL the dev server process. Confirm the `boardsmith` dependency resolved to `file:/Users/jtsmith/BoardSmith` (local-monorepo detection) and that `.git/` exists in the new project (D2 fix). Do NOT leave any dev server running.
  </action>
  <acceptance_criteria>
    - `~/BoardSmithGames/go-fish-dryrun/` exists and its realpath does NOT contain `/tmp` or `/private/tmp`
    - `cd ~/BoardSmithGames/go-fish-dryrun && npx tsc --noEmit` exits 0 on the fresh, unmodified scaffold (no local tsconfig workaround needed — D1 fixed at source)
    - `~/BoardSmithGames/go-fish-dryrun/.git/` exists (`git -C ~/BoardSmithGames/go-fish-dryrun rev-parse --is-inside-work-tree` → true)
    - `~/BoardSmithGames/go-fish-dryrun/package.json` declares `boardsmith` as `file:/Users/jtsmith/BoardSmith` (or a `file:` path resolving to it)
    - `npx boardsmith dev --no-open` reached `Ready! Press Ctrl+C to stop.` and a curl to the dev URL returned non-error; the process was then killed
    - `pgrep -fl "boardsmith dev"` returns nothing (no server left running)
  </acceptance_criteria>
  <verify>
    <automated>test -d ~/BoardSmithGames/go-fish-dryrun && ! (realpath ~/BoardSmithGames/go-fish-dryrun | grep -q '/tmp') && cd ~/BoardSmithGames/go-fish-dryrun && npx tsc --noEmit && git rev-parse --is-inside-work-tree && ! pgrep -fl "boardsmith dev"</automated>
  </verify>
  <done>Durable, git-initialized, compiling, serve-verified go-fish-dryrun scaffold exists outside /tmp; no dev server running.</done>
</task>

<task type="auto">
  <name>Task 2: Interview-fallback ingest + Step-3 synthesis</name>
  <files>~/BoardSmithGames/go-fish-dryrun/rulebook/*.md, ~/BoardSmithGames/go-fish-dryrun/ASSETS.md, ~/BoardSmithGames/go-fish-dryrun/rulebook/00-visual-survey.md, ~/BoardSmithGames/go-fish-dryrun/rulebook/INDEX.md</files>
  <read_first>
    - src/cli/slash-command/bs/ingest-rules.md (Step 2 route-to-interview, Step 3 synthesis)
    - src/cli/slash-command/bs/ingest/interview-fallback.md (the no-rulebook structured interview, Output Re-Target, citation format "designer statement, ingest session, Q{n}")
    - 149-RESEARCH.md "Standard Go Fish Rules" section (the clean, complete canonical ruleset to feed) + the 4 ambiguity points to cover
  </read_first>
  <action>
    Working inside `~/BoardSmithGames/go-fish-dryrun/`, follow `bs/ingest/interview-fallback.md`. Route to the interview-fallback path (INGEST-03) — Go Fish's rules are supplied as clean plain text, no illustrated rulebook to transcribe, matching the 149 input. Run the full structured interview answering from the canonical standard Go Fish ruleset in 149-RESEARCH.md ("Standard Go Fish Rules"), covering all four known ambiguity points (must-hold-the-rank-to-ask; matching-draw grants extra turn; empty-pond/empty-hand end trigger; 5-vs-7 cards per player count). Write the `rulebook/NN-topic.md` slices from the answers with citation format `designer statement, ingest session, Q{n}`, collecting `citedTerms[]`/`componentMentions[]`. Then run Step 3 synthesis (orchestrator, from returned summaries only — never re-read a slice): build `rulebook/INDEX.md` (term→slice), seed `ASSETS.md` (component inventory: standard 52-card deck, per-player hands, pond/draw pile, books space), write `rulebook/00-visual-survey.md` (thin, per interview-path Step-3 synthesis), and record min/max player counts (2-6) + per-count deal difference (7 cards for 2-3, 5 for 4-6).
  </action>
  <acceptance_criteria>
    - `~/BoardSmithGames/go-fish-dryrun/rulebook/` contains one or more `NN-topic.md` slice files
    - `rulebook/INDEX.md` exists and is a term→slice cross-reference table (built from citedTerms)
    - `rulebook/00-visual-survey.md` exists (interview-path thin survey — expected, not a defect)
    - `ASSETS.md` exists and lists the Go Fish component inventory as debt (deck/hands/pond/books)
    - At least one slice uses the interview citation format `designer statement, ingest session, Q` (grep: `grep -rl 'designer statement, ingest session' ~/BoardSmithGames/go-fish-dryrun/rulebook/`)
    - The four ambiguity points are each addressed in a slice (must-hold-rank, matching-draw extra turn, empty-pond end trigger, 5-vs-7 deal)
  </acceptance_criteria>
  <verify>
    <automated>test -f ~/BoardSmithGames/go-fish-dryrun/rulebook/INDEX.md && test -f ~/BoardSmithGames/go-fish-dryrun/rulebook/00-visual-survey.md && test -f ~/BoardSmithGames/go-fish-dryrun/ASSETS.md && grep -rlq 'designer statement, ingest session' ~/BoardSmithGames/go-fish-dryrun/rulebook/</automated>
  </verify>
  <done>rulebook/ slices + INDEX.md + ASSETS.md + 00-visual-survey.md written from the interview path; four ambiguity points covered; citation format correct.</done>
</task>

<task type="auto">
  <name>Task 3: Sketch derivation + Step-7 write files</name>
  <files>~/BoardSmithGames/go-fish-dryrun/SKETCH.md, ~/BoardSmithGames/go-fish-dryrun/chunks/core-event-loop/CHUNK.md, ~/BoardSmithGames/go-fish-dryrun/RULINGS.md, ~/BoardSmithGames/go-fish-dryrun/DECISIONS.md</files>
  <read_first>
    - src/cli/slash-command/bs/ingest/sketch-derivation.md (chunk-carving heuristic, lazy-tail 2-3-chunk detail cap, Mandated Chunks contract)
    - src/cli/slash-command/bs/ingest-rules.md (Step 4 derivation, Step 5 UI strategy, Step 6 approval gate — auto-approve here, Step 7 write files + write order)
    - src/cli/slash-command/bs/templates/SKETCH.template.md and templates/CHUNK.template.md (the PARSE CONTRACT headings the written files must byte-match)
    - 149-DRYRUN-REPORT.md §2 axis (d) (the exact sketch/chunk shape to reproduce, incl. the mandated tail marker string)
  </read_first>
  <action>
    Follow `bs/ingest/sketch-derivation.md` + `ingest-rules.md` Steps 4-7. Derive the sketch: first chunk = `core-event-loop` (deal + one ask-a-rank turn resolving to give-cards-or-go-fish + extra-turn rule), `ui: major`, `full` ceremony; then the mandated tail entries `game-end-scoring` (books/13-book win) and `final-acceptance`, left as sketch-level tail entries with the exact marker `Status: proposed (sketch-level — no CHUNK.md yet)` and NO `chunks/<slug>/` directory. Set UI Strategy = `custom-from-chunk-1` (default). Auto-approve the Step-6 gate (headless dry-run, matching 149 — no live human, per CONTEXT.md's scaled-execution grant). Then Step 7 (single write point, write order CHUNK.md before SKETCH.md): copy-and-fill `SKETCH.template.md` → `SKETCH.md`; create `chunks/core-event-loop/CHUNK.md` from `CHUNK.template.md` (chunk-1 only — tail entries get no directory); seed `RULINGS.md` + `DECISIONS.md` as empty ledgers from their templates. Do NOT create `DESIGN.md` (written at the first UI chunk's ask). Verify both SKETCH.md and CHUNK.md carry every required PARSE-CONTRACT heading from their templates, in order.
  </action>
  <acceptance_criteria>
    - `SKETCH.md` exists with an ordered chunk list whose first entry is `core-event-loop` and whose tail entries `game-end-scoring` + `final-acceptance` each carry the literal `Status: proposed (sketch-level — no CHUNK.md yet)` (grep exact string)
    - `SKETCH.md` `## UI Strategy` section = `custom-from-chunk-1`
    - `chunks/core-event-loop/CHUNK.md` exists; `chunks/game-end-scoring/` and `chunks/final-acceptance/` do NOT exist (tail entries have no directory)
    - `chunks/core-event-loop/CHUNK.md` declares `## ui:` `major` and `## Ceremony` `full`
    - `RULINGS.md` and `DECISIONS.md` exist as empty (template) ledgers; `DESIGN.md` does NOT exist
    - Every PARSE-CONTRACT heading from SKETCH.template.md / CHUNK.template.md is present in order in the written files (drift check per template)
  </acceptance_criteria>
  <verify>
    <automated>cd ~/BoardSmithGames/go-fish-dryrun && test -f SKETCH.md && test -f chunks/core-event-loop/CHUNK.md && ! test -d chunks/final-acceptance && grep -q 'Status: proposed (sketch-level — no CHUNK.md yet)' SKETCH.md && grep -q 'custom-from-chunk-1' SKETCH.md && test -f RULINGS.md && test -f DECISIONS.md && ! test -f DESIGN.md</automated>
  </verify>
  <done>SKETCH.md + chunk-1 CHUNK.md + empty ledgers written to the durable project, byte-matching template PARSE CONTRACTs, with the mandated tail intact and no DESIGN.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill-file path references → source layout | `${CLAUDE_SKILL_DIR}/../bs-shared/X` must resolve to `src/cli/slash-command/bs/X`; a wrong mapping produces false "file not found" |
| dry-run writes → hand-built go-fish reference | writes must land in `go-fish-dryrun`, never `go-fish` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-01 | Tampering | hand-built `~/BoardSmithGames/go-fish/` | mitigate | init runs in `go-fish-dryrun`; Plan 04 verifies `go-fish/src/rules` unchanged. Warning sign: any `cd ~/BoardSmithGames/go-fish` write |
| T-150-02 | Information/Repudiation | skill path translation | accept | Repo-relative dry-run, same as 149 which resolved cleanly with zero "file not found"; path map documented in objective |
| T-150-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed into BoardSmith; `go-fish-dryrun` runs `npm install` of the scaffold template's own already-audited pinned devDeps (axe-core ^4.12.1 confirmed). No install-time legitimacy gate needed |
</threat_model>

<verification>
- Fresh scaffold compiles + serves before ingest content is written (Task 1 gate).
- Interview-fallback path produces rulebook/ slices with correct citation format (Task 2 gate).
- SKETCH.md + chunk-1 CHUNK.md byte-match template PARSE CONTRACTs; mandated tail intact; no DESIGN.md (Task 3 gate).
- No `boardsmith dev` process left running after Task 1's serve check.
- `~/BoardSmithGames/go-fish-dryrun/` realpath is NOT under /tmp.
</verification>

<success_criteria>
The durable `~/BoardSmithGames/go-fish-dryrun/` project exists (outside /tmp), compiles clean, is a git repo, and carries the full ingest-leg output: rulebook slices + INDEX + ASSETS + visual survey, SKETCH.md (chunk-1 detailed, mandated tail sketch-level), chunk-1 CHUNK.md, and empty RULINGS/DECISIONS ledgers — ready for Plan 02's chunk-1 build leg.
</success_criteria>

<output>
Create `.planning/phases/150-regenerate-the-pipeline-built-go-fish-stable-location/150-01-SUMMARY.md` when done. Record the exact scaffold path and confirm no dev server left running.
</output>
