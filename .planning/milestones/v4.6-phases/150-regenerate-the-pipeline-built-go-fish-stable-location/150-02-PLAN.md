---
phase: 150-regenerate-the-pipeline-built-go-fish-stable-location
plan: 02
type: execute
wave: 2
depends_on: [150-01]
files_modified:
  - ~/BoardSmithGames/go-fish-dryrun/chunks/core-event-loop/CHUNK.md   # Interpretation, Visibility Declaration, Redteam Rounds, Status
  - ~/BoardSmithGames/go-fish-dryrun/chunks/core-event-loop/ASK-PROPOSAL.md
  - ~/BoardSmithGames/go-fish-dryrun/src/rules/game.ts
  - ~/BoardSmithGames/go-fish-dryrun/src/rules/elements.ts
  - ~/BoardSmithGames/go-fish-dryrun/src/rules/actions.ts
  - ~/BoardSmithGames/go-fish-dryrun/src/rules/flow.ts
  - ~/BoardSmithGames/go-fish-dryrun/src/ui/                            # chunk-1 UI (ui: major, real controller wiring)
autonomous: true
requirements: [GEN-01]

must_haves:
  truths:
    - "chunk-1 CHUNK.md carries a numbered Interpretation claims list + a Visibility Declaration (opponent hands hidden, pond hidden, drawn-card identity hidden, ask + outcome public)"
    - "CHUNK.md ## Redteam Rounds records the scaled adversarial pass (2 refuters + 1 coverage adversary, real logic)"
    - "The ask gate is captured as ASK-PROPOSAL.md (4-part format, zero implementation vocabulary), auto-approved → Status: approved"
    - "Generated src/rules/{game,elements,actions,flow}.ts implement chunk-1: deal + ask-a-rank + give-or-go-fish + extra-turn"
    - "The chunk-1 UI wires the REAL controller (useActionController/useBoardActionBridge), not a mock"
  artifacts:
    - path: "~/BoardSmithGames/go-fish-dryrun/src/rules/actions.ts"
      provides: "The ask action: self-scoped rank choice, hit=transfer-all+extraTurn, miss=draw+extraTurn-if-matches"
      min_lines: 40
    - path: "~/BoardSmithGames/go-fish-dryrun/src/rules/flow.ts"
      provides: "game-loop → eachPlayer → turn-loop with extra-turn continuation"
      min_lines: 20
    - path: "~/BoardSmithGames/go-fish-dryrun/src/rules/elements.ts"
      provides: "Card/Hand(contentsVisibleToOwner)/Pond(contentsHidden), Books defined"
    - path: "~/BoardSmithGames/go-fish-dryrun/chunks/core-event-loop/ASK-PROPOSAL.md"
      provides: "Captured ask-gate presentation, 4-part format"
  key_links:
    - from: "src/rules/game.ts"
      to: "src/rules/actions.ts + flow.ts"
      via: "registerAction(ask) + setFlow(game-loop)"
      pattern: "registerAction|setFlow|startFlow"
    - from: "src/rules/elements.ts Hand"
      to: "hidden-info guarantee"
      via: "contentsVisibleToOwner() on Hand, contentsHidden() on Pond"
      pattern: "contentsVisibleToOwner|contentsHidden"
---

<objective>
Run the chunk-1 `core-event-loop` build leg's CODE-PRODUCING steps — `investigate → redteam → ask → build` (full-ceremony group 1 + build) — of `/bs-build-chunk`, following the real skill files, against the sketch/chunk state Plan 01 wrote. This generates the chunk-1 Go Fish game code (`src/rules/{game,elements,actions,flow}.ts` + a real-controller-wired UI) into the durable `~/BoardSmithGames/go-fish-dryrun/` project.

Purpose: GEN-01 — the pipeline OUTPUT is the generated game code. This plan produces it; Plan 03 verifies + hardens it.
Output: chunk-1 CHUNK.md filled (Interpretation, Visibility Declaration, Redteam Rounds, Status: approved → built), ASK-PROPOSAL.md captured, and the generated `src/rules/*` + `src/ui/*` code.

Scaled fan-out (locked, CONTEXT.md): the redteam adversarial fan-out may be self-dispatched — one real pass per lens (2 refuters + 1 coverage adversary) rather than full Task-subagent fan-out. Every lens's real logic and real APIs must still run; only the number of independent dispatches is reduced. The durable artifact is the goal, not re-proving the fan-out.

Ask gate: captured as an artifact (ASK-PROPOSAL.md) and auto-approved (headless dry-run, matching 149) — NOT presented to a live human. The live human gate is Phase 151's playtest.

Path translation: `${CLAUDE_SKILL_DIR}/../bs-shared/X` → `src/cli/slash-command/bs/X` (repo-relative). Work inside `~/BoardSmithGames/go-fish-dryrun/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-regenerate-the-pipeline-built-go-fish-stable-location/150-CONTEXT.md

# The build-leg skill files to FOLLOW (read as you execute each step):
@src/cli/slash-command/bs/build-chunk.md
@src/cli/slash-command/bs/build/investigate.md
@src/cli/slash-command/bs/build/redteam.md
@src/cli/slash-command/bs/build/ask.md
@src/cli/slash-command/bs/build/build.md

# Source-of-truth for the chunk-1 shape to reproduce + the hand-built READ-ONLY reference:
@.planning/milestones/v4.6-phases/149-end-to-end-dry-run-validation/149-DRYRUN-REPORT.md
@.planning/milestones/v4.6-phases/149-end-to-end-dry-run-validation/149-RESEARCH.md

<interfaces>
<!-- READ-ONLY hand-built reference (comparison baseline, NEVER write): -->
<!-- ~/BoardSmithGames/go-fish/src/rules/{game,elements,actions,flow}.ts -->
<!-- Data model: Card extends BaseCard (suit, rank); Hand extends BaseHand (contentsVisibleToOwner()); -->
<!--   Pond extends Deck (contentsHidden()); Books extends Space (contentsVisible()). -->
<!-- ask action: two chooseFrom (target player, then rank filtered to ranks the asker holds via .condition()); -->
<!--   hit → transfer all matching cards, data.extraTurn:true; miss → draw from pond, extraTurn only if drawn card matches asked rank. -->
<!-- flow: game-loop (while !isFinished) → eachPlayer player-turns → turn-loop reading ctx.lastActionResult.data.extraTurn. -->
<!-- Drawn-card identity is NEVER in a public game.message() — only the public outcome. -->
<!-- Expected chunk-1 depth cut (149): isFinished() = "pond empty" (book-forming out of scope); Books defined-but-unwired. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Group 1 — investigate → redteam → ask (scaled fan-out)</name>
  <files>~/BoardSmithGames/go-fish-dryrun/chunks/core-event-loop/CHUNK.md, ~/BoardSmithGames/go-fish-dryrun/chunks/core-event-loop/ASK-PROPOSAL.md, ~/BoardSmithGames/go-fish-dryrun/RULINGS.md</files>
  <read_first>
    - src/cli/slash-command/bs/build-chunk.md (Step 0-3 + Step Group 1 dispatch, Context-Economics Hard Rule, scaled-fan-out allowance)
    - src/cli/slash-command/bs/build/investigate.md (cited-slice reading, claims list + visibility declaration authoring)
    - src/cli/slash-command/bs/build/redteam.md (3-way adversarial fan-out, escalation, no-framing rule)
    - src/cli/slash-command/bs/build/ask.md (4-part presentation, gate-before-write)
    - 149-DRYRUN-REPORT.md §2 axis (a)/(b) (the rule-fidelity + visibility claims the run must reproduce; incl. redteam Round-1 claim 7 empty-pond edge case)
  </read_first>
  <action>
    Follow `bs/build-chunk.md` Step Group 1 against chunk `core-event-loop`. INVESTIGATE (fresh-context, per investigate.md): read chunk-1's cited slices + INDEX-discovered slices + RULINGS.md + DECISIONS.md + the relevant BoardSmith docs (core-concepts, common-pitfalls, actions-and-flow, and for ui:major custom-ui-guide/ui-components) — write a numbered claims list + a Visibility Declaration directly into CHUNK.md's `## Interpretation` and `## Visibility Declaration`. The visibility declaration MUST state: opponents' hand contents hidden from all but owner; pond/draw-pile hidden from all; identity of a card drawn from the pond on a miss hidden from other seats; the asked-for rank and the give/go-fish outcome are public. REDTEAM (scaled: one real self-dispatched pass per lens — 2 refuters + 1 coverage adversary, framing-free, claims-list only, refuters default REFUTED on uncertainty): record verdicts + the coverage adversary's missingInteractions in CHUNK.md `## Redteam Rounds` (expect the empty-pond edge case to surface as a coverage finding, per 149). ASK (captured, not live): write `ASK-PROPOSAL.md` in the mandated 4-part format (plain-language interpretation, citations, deferred list, zero implementation vocabulary) following Gate-Before-Write order, auto-approve, then write `Status: approved` to CHUNK.md last. Check off investigate/redteam/ask on the Step Checklist as each completes.
  </action>
  <acceptance_criteria>
    - `chunks/core-event-loop/CHUNK.md` `## Interpretation` contains a numbered claims list
    - `chunks/core-event-loop/CHUNK.md` `## Visibility Declaration` states opponent-hand-hidden, pond-hidden, drawn-card-identity-hidden, ask+outcome-public
    - `chunks/core-event-loop/CHUNK.md` `## Redteam Rounds` records the refuters' verdicts + coverage adversary findings (incl. an empty-pond / edge-case interaction)
    - `chunks/core-event-loop/ASK-PROPOSAL.md` exists in 4-part format with zero action/flow/element implementation vocabulary
    - `chunks/core-event-loop/CHUNK.md` Status line reads `approved` (written last, after the captured ask)
    - investigate / redteam / ask are checked off on the Step Checklist
  </acceptance_criteria>
  <verify>
    <automated>cd ~/BoardSmithGames/go-fish-dryrun && grep -q '## Visibility Declaration' chunks/core-event-loop/CHUNK.md && grep -q '## Redteam Rounds' chunks/core-event-loop/CHUNK.md && test -f chunks/core-event-loop/ASK-PROPOSAL.md && grep -Eq 'Status:.*approved' chunks/core-event-loop/CHUNK.md</automated>
  </verify>
  <done>Group-1 state (claims, visibility declaration, redteam rounds, captured ask, Status: approved) persisted in CHUNK.md + ASK-PROPOSAL.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build — generate chunk-1 game code + real-controller UI</name>
  <files>~/BoardSmithGames/go-fish-dryrun/src/rules/game.ts, ~/BoardSmithGames/go-fish-dryrun/src/rules/elements.ts, ~/BoardSmithGames/go-fish-dryrun/src/rules/actions.ts, ~/BoardSmithGames/go-fish-dryrun/src/rules/flow.ts, ~/BoardSmithGames/go-fish-dryrun/src/ui/</files>
  <read_first>
    - src/cli/slash-command/bs/build/build.md (code-writing step, fresh-context raw-slice exception, per-file build manifest, real-controller UI wiring requirement)
    - src/cli/slash-command/bs/build-chunk.md (Step Group 2 build dispatch, Git Protocol: commit before build)
    - 149-RESEARCH.md Pitfall 4 (a11y floor needs REAL useActionController/useBoardActionBridge wiring, not a mock) + "Hand-Built Go-Fish Baseline" (the chunk-1 mapping)
    - 149-DRYRUN-REPORT.md §2 axis (c) (acceptable divergence: Books defined-but-unwired; chunk-1 is the thinnest end-to-end slice)
  </read_first>
  <behavior>
    - Deal: 7 cards/player for 2-3 players, 5 for 4-6, from a standard 52-card pond (draw pile)
    - ask action: asker chooses a target opponent, then a rank filtered to ranks the asker actually holds (self-scoped choice, enforced via .condition())
    - Hit (opponent holds the rank): ALL matching cards transfer to asker; asker gets an extra turn (data.extraTurn: true)
    - Miss (opponent lacks it): asker draws top of pond; extra turn ONLY if the drawn card matches the asked rank; otherwise turn passes
    - Empty pond on a miss: turn ends cleanly (no crash)
    - Hidden info: Hand contentsVisibleToOwner(); Pond contentsHidden(); no public game.message() ever reveals a drawn card's identity — only the public give/go-fish outcome
  </behavior>
  <action>
    Follow `bs/build/build.md`. Commit before build (Git Protocol). Generate chunk-1 `core-event-loop` code into `src/rules/{game,elements,actions,flow}.ts` implementing the `<behavior>` above, mirroring the hand-built idiom (Card/Hand/Pond/Books elements; `Action.create(...).chooseFrom(target).chooseFrom(rank).condition(...).execute(...)`; `game-loop`→`eachPlayer`→`turn-loop` reading `ctx.lastActionResult.data.extraTurn`). Books/13-book win + scoring are OUT of chunk-1 scope — `Books` may be defined-but-unwired (documented acceptable divergence, not a shortcut). Wire the chunk-1 UI with the REAL controller pattern (`useActionController` + `useBoardActionBridge` + `createBoardInteraction`) from the start — never a mock controller (Pitfall 4). Do NOT wire book-forming or a 13-book win condition (out of scope). The `isFinished()` terminal condition question is resolved in Plan 03's test step (149 found pond-empty is the honest depth-cut) — build the loop so that fix is reachable.
  </action>
  <acceptance_criteria>
    - `cd ~/BoardSmithGames/go-fish-dryrun && npx tsc --noEmit` exits 0 (generated code compiles)
    - `src/rules/actions.ts` defines the ask action with a rank choice self-scoped to the asker's held ranks and a hit-transfers-all / miss-draws branch setting `extraTurn`
    - `src/rules/elements.ts` declares `Hand` with `contentsVisibleToOwner()` and the pond/draw-pile with `contentsHidden()` (grep both)
    - `src/rules/flow.ts` composes `game-loop`/`eachPlayer`/turn-loop reading the extra-turn result
    - The chunk-1 UI imports the real controller (grep `useActionController` in `src/ui/`), not a hand-rolled fake
    - No public `game.message()` includes a drawn card's rank+suit identity on a miss (only the public outcome)
    - `chunks/core-event-loop/CHUNK.md` Status line reads `built`; build step checked off
  </acceptance_criteria>
  <verify>
    <automated>cd ~/BoardSmithGames/go-fish-dryrun && npx tsc --noEmit && grep -q 'contentsVisibleToOwner' src/rules/elements.ts && grep -q 'contentsHidden' src/rules/elements.ts && grep -rq 'useActionController' src/ui/</automated>
  </verify>
  <done>Compiling chunk-1 Go Fish code + real-controller UI generated in the durable project; CHUNK.md Status: built.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated game code → replay/sandbox integrity | generated code must obey the 7 sandbox rules (no-network/timers/nondeterministic/etc.) — enforced in Plan 03's `boardsmith lint` |
| per-seat view boundary | opponent hand + drawn-card identity must not cross to other seats — declared here, proven in Plan 03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-03 | Information Disclosure | ask action public messages | mitigate | Visibility Declaration (Task 1) forbids drawn-card identity in public messages; build (Task 2) follows hand-built discipline; Plan 03 audit proves via diffPlayerViews/assertNoHiddenInfoLeak |
| T-150-04 | Tampering | generated code determinism | mitigate | Real controller wiring + no timers/RNG-outside-engine; Plan 03 `boardsmith lint` (7 AST rules) is the hard gate |
| T-150-SC | Tampering | npm/cargo/pip installs | accept | No new packages; scaffold devDeps already audited (Plan 01) |
</threat_model>

<verification>
- Group-1 state persisted in CHUNK.md (claims, visibility declaration, redteam rounds) + ASK-PROPOSAL.md captured; Status: approved.
- Generated `src/rules/*` compiles clean (`tsc --noEmit`).
- Hidden-info elements declared (`contentsVisibleToOwner`/`contentsHidden`).
- UI wires the real controller (not a mock).
- Books defined-but-unwired documented as intentional chunk-1 depth cut.
</verification>

<success_criteria>
The durable `go-fish-dryrun` project contains generated, compiling chunk-1 Go Fish code (deal + ask + give-or-go-fish + extra-turn) with a real-controller UI and correct hidden-info element declarations; CHUNK.md records the full group-1 ceremony and Status: built — ready for Plan 03's test/audit/repair verification.
</success_criteria>

<output>
Create `.planning/phases/150-regenerate-the-pipeline-built-go-fish-stable-location/150-02-SUMMARY.md` when done. Record the generated file list + LOC and any divergence from the hand-built baseline.
</output>
