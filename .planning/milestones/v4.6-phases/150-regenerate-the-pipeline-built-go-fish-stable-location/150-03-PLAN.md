---
phase: 150-regenerate-the-pipeline-built-go-fish-stable-location
plan: 03
type: execute
wave: 3
depends_on: [150-02]
files_modified:
  - ~/BoardSmithGames/go-fish-dryrun/chunks/core-event-loop/CHUNK.md    # Findings Ledger, Step Checklist, Status
  - ~/BoardSmithGames/go-fish-dryrun/src/rules/game.ts                  # isFinished() depth-cut fix + any repair
  - ~/BoardSmithGames/go-fish-dryrun/src/rules/actions.ts              # any repair
  - ~/BoardSmithGames/go-fish-dryrun/src/rules/flow.ts                 # any repair
  - ~/BoardSmithGames/go-fish-dryrun/src/**/*.test.ts                  # generated chunk unit/integration + leak tests
autonomous: true
requirements: [GEN-01]

must_haves:
  truths:
    - "The generated chunk-1 project passes tsc --noEmit, boardsmith lint (7 sandbox rules), and its unit/integration + full accumulated suite"
    - "simulateRandomGames (50 × [2,3,4] players) reaches a terminal state: crashed=0, stuck=0, timedOut=0, exceededMaxActions=0"
    - "The 5-item a11y floor passes with real controller wiring (ui: major chunk)"
    - "diffPlayerViews → 0 attributeDiffs and assertNoHiddenInfoLeak passes for both seats (allow-predicate proven non-vacuous)"
    - "Any generated-code bug found (e.g. unreachable isFinished terminal condition) is fixed in the generated code and re-verified"
  artifacts:
    - path: "~/BoardSmithGames/go-fish-dryrun/chunks/core-event-loop/CHUNK.md"
      provides: "Findings Ledger with dispositions; Step Checklist test/audit/repair checked"
      contains: "## Findings Ledger"
    - path: "~/BoardSmithGames/go-fish-dryrun/src/rules/game.ts"
      provides: "isFinished() honest depth-cut terminal condition (pond empty)"
      contains: "isFinished"
  key_links:
    - from: "generated leak test"
      to: "src/testing view APIs"
      via: "diffPlayerViews + assertNoHiddenInfoLeak imported from boardsmith/testing"
      pattern: "diffPlayerViews|assertNoHiddenInfoLeak"
    - from: "test step"
      to: "terminal-state proof"
      via: "simulateRandomGames asserting the four zero fields"
      pattern: "simulateRandomGames"
---

<objective>
Run the chunk-1 build leg's VERIFICATION steps — `test → audit → repair` — of `/bs-build-chunk`, following the real skill files, against the code Plan 02 generated. This runs every automated discipline (tsc, boardsmith lint, unit/integration, full suite, simulateRandomGames, a11y floor, two-seat hidden-info leak check) for real against the durable project, and fixes any generated-code bug found — reproducing the 149 dry-run's clean chunk-1 bar into the permanent location.

Purpose: GEN-01 — the pipeline OUTPUT must be a compiling, passing, non-leaking, terminating game. This plan proves it and hardens it.
Output: CHUNK.md Findings Ledger + all Step-Checklist verification items checked; any generated-code repair applied and re-verified; the project passing its full automated bar.

Expected reproduction (149): `simulateRandomGames` will time out on every game if `isFinished()` is the naive "pond empty AND all hands empty" (book-forming — the only card-removal mechanism — is out of chunk-1 scope, so all-hands-empty is unreachable). The honest depth-cut fix is `isFinished()` = "pond empty". This is a GENERATED-CODE bug the test step is DESIGNED to catch and repair — fix it in the generated code (document in CHUNK.md claim 6), not by weakening the check.

Scaled fan-out (locked): the audit adversarial lenses (fidelity, visibility, undo + design-review) may be self-dispatched — one real pass per lens — but every lens's real logic and real APIs (diffPlayerViews, assertNoHiddenInfoLeak, simulateRandomGames, tsc, boardsmith lint) must genuinely run.

Path translation: `${CLAUDE_SKILL_DIR}/../bs-shared/X` → `src/cli/slash-command/bs/X` (repo-relative). Work inside `~/BoardSmithGames/go-fish-dryrun/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-regenerate-the-pipeline-built-go-fish-stable-location/150-CONTEXT.md

# The verification-leg skill files to FOLLOW:
@src/cli/slash-command/bs/build-chunk.md
@src/cli/slash-command/bs/build/test.md
@src/cli/slash-command/bs/build/audit.md
@src/cli/slash-command/bs/build/repair.md
@src/cli/slash-command/bs/build/design-review.md

# Source-of-truth for the clean-run bar to reproduce:
@.planning/milestones/v4.6-phases/149-end-to-end-dry-run-validation/149-DRYRUN-REPORT.md
@.planning/milestones/v4.6-phases/149-end-to-end-dry-run-validation/149-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: test step — full automated discipline (+ fix depth-cut bug)</name>
  <files>~/BoardSmithGames/go-fish-dryrun/src/**/*.test.ts, ~/BoardSmithGames/go-fish-dryrun/src/rules/game.ts, ~/BoardSmithGames/go-fish-dryrun/chunks/core-event-loop/CHUNK.md</files>
  <read_first>
    - src/cli/slash-command/bs/build/test.md (the non-reorderable numbered command sequence + a11y floor)
    - src/cli/slash-command/bs/build-chunk.md (Step Group 2 test dispatch)
    - 149-RESEARCH.md "test" mechanics + Pitfall 4 (real controller a11y wiring) + the simulateRandomGames invocation pattern
    - 149-DRYRUN-REPORT.md §1 "Build leg" table (the exact pass bar: lint 0 errors, 9/9 chunk tests, sim 0/0/0/0) + the isFinished pond-empty depth-cut fix
  </read_first>
  <action>
    Follow `bs/build/test.md`'s non-reorderable numbered sequence against `~/BoardSmithGames/go-fish-dryrun/`: (1) `npx tsc --noEmit` clean; (2) `npx boardsmith lint` — the 7 AST sandbox rules (no-network, no-filesystem, no-timers, no-nondeterministic, no-eval, no-element-identity-comparison, no-element-array-state) must be 0 errors (informational warnings non-blocking); (3) run the generated chunk unit/integration tests; (4) run the full accumulated project suite; (5) `simulateRandomGames(GoFishGame, { count: 50, playerCounts: [2,3,4] })` asserting `crashed===0 && stuck===0 && timedOut===0 && exceededMaxActions===0`; (6) the 5-item a11y floor (keyboard-only ActionPanel completion against the REAL controller wiring, axe-core scan, no-color-literal grep + contrast, real controls with aria-labels, focus management + prefers-reduced-motion). If simulateRandomGames times out on every game (expected — unreachable naive terminal condition), FIX the generated code: set `isFinished()` to the honest depth-cut "pond empty" (book-forming out of chunk-1 scope), document it in CHUNK.md claim 6 + the code, then re-run step (5) until all four fields are 0. Never leave a dev/test server running. Check off the test step on the Step Checklist.
  </action>
  <acceptance_criteria>
    - `cd ~/BoardSmithGames/go-fish-dryrun && npx tsc --noEmit` exits 0
    - `npx boardsmith lint` reports 0 errors across the 7 sandbox rules
    - The generated chunk unit/integration tests pass (`npx vitest run`) and the full accumulated suite passes
    - `simulateRandomGames` (50 × [2,3,4]) reports crashed=0, stuck=0, timedOut=0, exceededMaxActions=0 (after the isFinished depth-cut fix if needed)
    - The 5-item a11y floor passes; the keyboard-only test imports the REAL `useActionController`/`useBoardActionBridge` (not a mock)
    - If the depth-cut fix was applied, `src/rules/game.ts` `isFinished()` = pond-empty and CHUNK.md claim 6 documents it
    - `pgrep -fl "boardsmith dev"` returns nothing
  </acceptance_criteria>
  <verify>
    <automated>cd ~/BoardSmithGames/go-fish-dryrun && npx tsc --noEmit && npx boardsmith lint && npx vitest run && ! pgrep -fl "boardsmith dev"</automated>
  </verify>
  <done>Generated chunk-1 passes tsc, lint, unit/integration + full suite, terminating random-sim, and the a11y floor; any depth-cut bug fixed in generated code.</done>
</task>

<task type="auto">
  <name>Task 2: audit + repair — fidelity, visibility, undo lenses + leak proof</name>
  <files>~/BoardSmithGames/go-fish-dryrun/chunks/core-event-loop/CHUNK.md, ~/BoardSmithGames/go-fish-dryrun/src/rules/actions.ts, ~/BoardSmithGames/go-fish-dryrun/src/rules/flow.ts, ~/BoardSmithGames/go-fish-dryrun/src/**/*.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/build/audit.md (3 fresh-context lenses fidelity/visibility/undo + design-review, Findings Ledger persistence, no-framing rule)
    - src/cli/slash-command/bs/build/repair.md (fix-or-refute-with-citation loop, max 3 audit rounds, round-N+1 sees only new findings)
    - src/cli/slash-command/bs/build/design-review.md (ui:major screenshot design-review dispatch)
    - 149-DRYRUN-REPORT.md §2 axis (b) (the visibility lens is the highest-value check: diffPlayerViews→0, assertNoHiddenInfoLeak both seats, non-vacuous control)
  </read_first>
  <action>
    Follow `bs/build/audit.md` (scaled fan-out: one real self-dispatched pass per lens, framing-free — the audit dispatch must NOT read CHUNK.md's ## Interpretation conclusion). Run the three lenses against the generated chunk-1 code: FIDELITY (does the ask/give-or-go-fish/extra-turn behavior match the cited rules); VISIBILITY (the highest-value lens for this hidden-info game — run `diffPlayerViews(testGame, 1, 2)` expecting 0 attributeDiffs, and `assertNoHiddenInfoLeak(...)` for both seats, and PROVE the allow-predicate non-vacuous via a deliberately-injected control case that correctly throws `Hidden-info leak`); UNDO (state round-trips cleanly). For ui:major, dispatch `design-review.md` (serve-capture-kill lifecycle — kill any server started). Land findings in CHUNK.md `## Findings Ledger` with stable IDs (F1, F2...). Then follow `bs/build/repair.md`: fix-or-refute each finding with a citation, max 3 rounds, re-running the affected checks after each fix. Check off audit + repair on the Step Checklist. Never leave a server running.
  </action>
  <acceptance_criteria>
    - `chunks/core-event-loop/CHUNK.md` `## Findings Ledger` exists with stable finding IDs and a disposition (fixed or refuted-with-citation) for each
    - A generated leak test invokes `diffPlayerViews` and `assertNoHiddenInfoLeak` (grep both in `src/**/*.test.ts`); it passes with 0 attributeDiffs for both seats
    - The allow-predicate is proven non-vacuous (a control case correctly throws `Hidden-info leak`)
    - The ui:major design-review ran; any server it started was killed (`pgrep -fl "boardsmith dev"` empty)
    - audit + repair checked off on the Step Checklist; re-run of affected checks passes after repair
    - `npx tsc --noEmit && npx vitest run` still green after repair
  </acceptance_criteria>
  <verify>
    <automated>cd ~/BoardSmithGames/go-fish-dryrun && grep -q '## Findings Ledger' chunks/core-event-loop/CHUNK.md && grep -rq 'assertNoHiddenInfoLeak' src/ && grep -rq 'diffPlayerViews' src/ && npx tsc --noEmit && npx vitest run && ! pgrep -fl "boardsmith dev"</automated>
  </verify>
  <done>Audit lenses run with real APIs, hidden-info leak proven absent (non-vacuously), all findings dispositioned and repaired, project still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-seat view boundary | opponent hand + drawn-card identity must not leak across seats — this plan PROVES it |
| generated code → replay/sandbox integrity | 7 sandbox rules enforced by `boardsmith lint` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-05 | Information Disclosure | opponent hand / drawn-card identity | mitigate | Visibility lens runs diffPlayerViews (0 attributeDiffs) + assertNoHiddenInfoLeak both seats, allow-predicate proven non-vacuous via a failing control |
| T-150-06 | Denial of Service | flow deadlock / non-terminating game | mitigate | simulateRandomGames asserts stuck=0/timedOut=0; unreachable-terminal-condition bug fixed to pond-empty depth cut |
| T-150-04 | Tampering | generated code determinism | mitigate | `boardsmith lint` (no-network/timers/nondeterministic/etc.) is the hard gate, 0 errors required |
| T-150-SC | Tampering | npm/cargo/pip installs | accept | No new packages introduced |
</threat_model>

<verification>
- tsc clean, boardsmith lint 0 errors, unit/integration + full suite green.
- simulateRandomGames 50×[2,3,4]: crashed=0, stuck=0, timedOut=0, exceededMaxActions=0.
- a11y floor 5/5 with real controller wiring.
- diffPlayerViews 0 attributeDiffs; assertNoHiddenInfoLeak both seats pass; control case proves non-vacuous.
- Findings Ledger fully dispositioned; project still green after repair.
- No server left running.
</verification>

<success_criteria>
The regenerated chunk-1 Go Fish in `~/BoardSmithGames/go-fish-dryrun/` compiles, lints clean, passes its generated tests + full suite, reaches a terminal state under random simulation, passes the a11y floor, and provably does not leak opponent hands or drawn-card identity — matching the 149 dry-run's clean chunk-1 bar, now in a permanent location.
</success_criteria>

<output>
Create `.planning/phases/150-regenerate-the-pipeline-built-go-fish-stable-location/150-03-SUMMARY.md` when done. Record the automated-check results table (mirroring 149-DRYRUN-REPORT §1) and any repair applied.
</output>
