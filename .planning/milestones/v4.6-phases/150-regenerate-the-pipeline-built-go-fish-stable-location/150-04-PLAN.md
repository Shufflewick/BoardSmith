---
phase: 150-regenerate-the-pipeline-built-go-fish-stable-location
plan: 04
type: execute
wave: 4
depends_on: [150-03]
files_modified:
  - .planning/milestones/v4.6-phases/149-end-to-end-dry-run-validation/149-HUMAN-UAT.md   # repointed to the stable location (in THIS repo)
autonomous: true
requirements: [GEN-01]

must_haves:
  truths:
    - "The regenerated location (~/BoardSmithGames/go-fish-dryrun/) and the exact dev-server run command are recorded"
    - "149-HUMAN-UAT.md's 'What to run' + numbered script point at the stable location, not the deleted /tmp scratch dir"
    - "The numbered click-by-click playtest script is unchanged in substance (only paths updated)"
    - "The generated project is preserved (NOT deleted) — it is the pending-Phase-151 playtest artifact"
    - "The hand-built ~/BoardSmithGames/go-fish/ is verified unchanged (READ-ONLY invariant held): no src/rules source files modified"
    - "No boardsmith dev server is left running after the final reachability check"
  artifacts:
    - path: ".planning/milestones/v4.6-phases/149-end-to-end-dry-run-validation/149-HUMAN-UAT.md"
      provides: "Repointed playtest script for Phase 151"
      contains: "go-fish-dryrun"
  key_links:
    - from: "149-HUMAN-UAT.md What to run"
      to: "~/BoardSmithGames/go-fish-dryrun/"
      via: "cd <stable dir> && npx boardsmith dev --players 2"
      pattern: "go-fish-dryrun"
    - from: "phase artifact"
      to: "Phase 151 human gate"
      via: "preserved compiling+serving project + recorded run command"
      pattern: "boardsmith dev"
---

<objective>
Close out Phase 150's success criterion 3: record the regenerated location + exact dev-server run command, repoint `149-HUMAN-UAT.md`'s playtest script at the new stable location, guarantee the generated project is preserved (not deleted), and verify the hand-built `~/BoardSmithGames/go-fish/` READ-ONLY invariant held throughout. This hands Phase 151 a permanent, serve-verified, playtest-ready pipeline-built game.

Purpose: GEN-01 — the pipeline OUTPUT must be a recorded, preserved, playtest-ready artifact with a repointed UAT script.
Output: an updated `149-HUMAN-UAT.md` (in this repo), a recorded location + run command, and verified READ-ONLY + preservation invariants.

Do NOT delete `~/BoardSmithGames/go-fish-dryrun/`. Do NOT write into `~/BoardSmithGames/go-fish/`. Do NOT leave any `boardsmith dev` server running.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-regenerate-the-pipeline-built-go-fish-stable-location/150-CONTEXT.md

# The file to repoint (currently references the deleted /tmp scratch dir):
@.planning/milestones/v4.6-phases/149-end-to-end-dry-run-validation/149-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final serve check + record location and run command</name>
  <files>(no source edits — verification + a recorded run command captured into the SUMMARY)</files>
  <read_first>
    - .planning/phases/150-.../150-01-SUMMARY.md and 150-03-SUMMARY.md (the recorded scaffold path + clean-run results)
    - src/cli/slash-command/bs/ingest/scaffold.md (the dev serve-check + kill discipline to reuse)
    - CLAUDE.md project rule: never leave a boardsmith dev server running
  </read_first>
  <action>
    Do one final reachability check on the durable project to confirm it still serves after all build/verify work: `cd ~/BoardSmithGames/go-fish-dryrun && npx boardsmith dev --no-open`, wait for `Ready! Press Ctrl+C to stop.`, curl the resolved dev URL for a non-error response, then KILL the process. Record the exact human-facing run command for Phase 151: `cd ~/BoardSmithGames/go-fish-dryrun && npx boardsmith dev --players 2`. Confirm no server is left running.
  </action>
  <acceptance_criteria>
    - `npx boardsmith dev --no-open` in `~/BoardSmithGames/go-fish-dryrun/` reached `Ready! Press Ctrl+C to stop.` and a curl returned non-error; process then killed
    - `pgrep -fl "boardsmith dev"` returns nothing
    - The exact run command `cd ~/BoardSmithGames/go-fish-dryrun && npx boardsmith dev --players 2` is recorded in the plan SUMMARY
    - The regenerated location `~/BoardSmithGames/go-fish-dryrun/` (realpath, confirmed NOT under /tmp) is recorded in the SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>test -d ~/BoardSmithGames/go-fish-dryrun && ! (realpath ~/BoardSmithGames/go-fish-dryrun | grep -q '/tmp') && ! pgrep -fl "boardsmith dev"</automated>
  </verify>
  <done>Durable project confirmed still serving (server killed); location + exact dev-server run command recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint 149-HUMAN-UAT.md at the stable location</name>
  <files>.planning/milestones/v4.6-phases/149-end-to-end-dry-run-validation/149-HUMAN-UAT.md</files>
  <read_first>
    - .planning/milestones/v4.6-phases/149-end-to-end-dry-run-validation/149-HUMAN-UAT.md (current content — "Why this exists", "What to run", numbered script)
    - src/cli/slash-command/bs/build/playtest.md (the playtest-gate semantics the script embodies — keep the script's substance intact)
  </read_first>
  <action>
    Edit `149-HUMAN-UAT.md` to point at the regenerated stable location. Update the "What to run" section: replace the deleted `/tmp/bs-dryrun-149.L7EU9I/go-fish-dryrun/` scratch reference with `~/BoardSmithGames/go-fish-dryrun/` as the primary pipeline-OUTPUT project (the direct proof); keep the hand-built `~/BoardSmithGames/go-fish/` as the optional side-by-side taste-comparison option. Update the `cd <chosen project dir>` example and any path in "Why this exists" that names the throwaway /tmp dir (note the project is now durable, no longer OS-temp-cleaned). Set the primary run command to `cd ~/BoardSmithGames/go-fish-dryrun && npx boardsmith dev --players 2`. Keep the numbered click-by-click script (items 1-3, regression/taste/second-seat-leak-check sections) IDENTICAL IN SUBSTANCE — only paths change, not the steps or expectations. Preserve the "Kill the server when done" instruction and the Verified Checklist.
  </action>
  <acceptance_criteria>
    - `149-HUMAN-UAT.md` contains `~/BoardSmithGames/go-fish-dryrun` (grep) and the run command `npx boardsmith dev --players 2`
    - No live reference to `/tmp/bs-dryrun-149` remains as the place to run (any mention is historical context only, clearly marked as the deleted scratch dir)
    - The numbered script's items 1, 2, 3 + Regression check + Taste check + Second-seat leak check sections are unchanged in substance (same steps, same expectations)
    - The Verified Checklist (6 items) and the "Kill the server when done" instruction are preserved
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/milestones/v4.6-phases/149-end-to-end-dry-run-validation/149-HUMAN-UAT.md; grep -q 'go-fish-dryrun' "$F" && grep -q 'npx boardsmith dev --players 2' "$F" && grep -q 'Second-seat leak check' "$F"</automated>
  </verify>
  <done>149-HUMAN-UAT.md repointed at the durable location; numbered script substance unchanged; run command updated.</done>
</task>

<task type="auto">
  <name>Task 3: Verify READ-ONLY invariant + preservation</name>
  <files>(verification only — no source edits)</files>
  <read_first>
    - .planning/phases/150-.../150-01-SUMMARY.md (confirms go-fish was READ-ONLY referenced during ingest)
    - CLAUDE.md / 150-CONTEXT.md (the hand-built ~/BoardSmithGames/go-fish/ is READ-ONLY; go-fish-dryrun must be preserved)
  </read_first>
  <action>
    Verify two invariants. READ-ONLY: confirm the hand-built `~/BoardSmithGames/go-fish/` has no modified tracked SOURCE files — `git -C ~/BoardSmithGames/go-fish status --short -- src/rules/` must be empty (no changes to game/elements/actions/flow/ai/tutorial). Note: a stray untracked Vite build artifact (e.g. `vite.config.ts.timestamp-*.mjs`) at the repo root is a pre-existing dev-server byproduct, NOT a source change — it does not violate the invariant; do not clean it (that itself would be a write). If ANY tracked file under `src/rules/` shows as modified, STOP and report a READ-ONLY violation. PRESERVATION: confirm `~/BoardSmithGames/go-fish-dryrun/` still exists, is NOT under /tmp, is a git repo, and compiles (`npx tsc --noEmit`) — proving it was not deleted and is playtest-ready. Confirm no `boardsmith dev` server is running.
  </action>
  <acceptance_criteria>
    - `git -C ~/BoardSmithGames/go-fish status --short -- src/rules/` is empty (no hand-built source files modified — READ-ONLY invariant held)
    - `~/BoardSmithGames/go-fish-dryrun/` exists, realpath NOT under /tmp, is a git work tree, and `npx tsc --noEmit` exits 0 (preserved + compiling)
    - `pgrep -fl "boardsmith dev"` returns nothing
    - The SUMMARY explicitly records: READ-ONLY invariant held, project preserved (not deleted), no server left running
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git -C ~/BoardSmithGames/go-fish status --short -- src/rules/)" && test -d ~/BoardSmithGames/go-fish-dryrun && ! (realpath ~/BoardSmithGames/go-fish-dryrun | grep -q '/tmp') && cd ~/BoardSmithGames/go-fish-dryrun && npx tsc --noEmit && ! pgrep -fl "boardsmith dev"</automated>
  </verify>
  <done>Hand-built go-fish/src/rules verified unchanged; go-fish-dryrun preserved, outside /tmp, compiling; no server running.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dry-run writes → hand-built go-fish reference | the READ-ONLY invariant boundary — verified here |
| phase artifact → Phase 151 human gate | the preserved project + repointed UAT script the human consumes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-01 | Tampering | hand-built `~/BoardSmithGames/go-fish/` | mitigate | Task 3 asserts `git status -- src/rules/` empty; STOP-on-violation |
| T-150-07 | Repudiation | recorded location / run command | mitigate | Task 1 records exact path + command into the SUMMARY; Task 2 embeds them in the UAT script |
| T-150-08 | Denial of Service | leftover dev server | mitigate | Every serve check ends in a kill; each task asserts `pgrep` empty |
| T-150-SC | Tampering | npm/cargo/pip installs | accept | No new packages introduced in this plan |
</threat_model>

<verification>
- 149-HUMAN-UAT.md points at `~/BoardSmithGames/go-fish-dryrun/` with the exact run command; script substance unchanged.
- Location + run command recorded in the SUMMARY.
- `git -C ~/BoardSmithGames/go-fish status --short -- src/rules/` empty (READ-ONLY held).
- go-fish-dryrun exists, outside /tmp, is a git repo, compiles (preserved + playtest-ready).
- No boardsmith dev server left running.
</verification>

<success_criteria>
Phase 150 is complete: the pipeline-built Go Fish lives durably at `~/BoardSmithGames/go-fish-dryrun/` (compiling + serving), its location and exact dev-server run command are recorded, `149-HUMAN-UAT.md` points a human at it with the numbered script intact, the hand-built reference is provably unchanged, and nothing was deleted or left running — ready for Phase 151's human playtest.
</success_criteria>

<output>
Create `.planning/phases/150-regenerate-the-pipeline-built-go-fish-stable-location/150-04-SUMMARY.md` when done. Record the stable location, the exact run command, the READ-ONLY invariant result, and the preservation confirmation.
</output>
