---
phase: 153-dev-host-multi-client-turn-desync-fix
plan: 02
type: checkpoint
wave: 2
depends_on: [153-01]
files_modified: []
autonomous: false
requirements: [DEVHOST-01, DEVHOST-02]

must_haves:
  truths:
    - "A scripted 2-client dev-host session — reload mid-game → reconnect → hand a seat to AI and back — never shows a client 'your turn' when it is not that client's turn (ROADMAP Phase 153 SC-3)"
    - "The reload-mid-game trigger (the exact DEF-C mechanism) is exercised against a REAL running boardsmith dev server + browser, not only in-process tests"
    - "The check waits on selectors / domcontentloaded and NEVER on networkidle (a live-WS dev-host never reaches network-idle — CLAUDE.md, RESEARCH Pitfall 6)"
  artifacts:
    - path: ".planning/phases/153-dev-host-multi-client-turn-desync-fix/153-02-SUMMARY.md"
      provides: "recorded 2-client scripted-session result (per-client turn view across reload/reconnect/AI-handoff, screenshot or console tally, confirmation the dev server was killed)"
  key_links:
    - from: "fixed dev.ts (socket-identity guard, plan 153-01)"
      to: "no stale 'your turn' in a real 2-client browser session"
      via: "scripted Playwright: reload seat mid-game, reconnect, hand a seat to AI and back; assert per-client isMyTurn matches the server"
      pattern: "your turn"
---

<objective>
Perform ROADMAP.md Phase 153 Success Criterion 3 — the human-facing scripted 2-client proof that no
in-process test can perform: a real `npx boardsmith dev` session driven in a browser, where a seat
reloads mid-game, reconnects, and is handed to AI and back, and NO client is ever shown "your turn" when
it is not its turn. This validates the plan-153-01 dev.ts fix end-to-end over real WS transport, closing
the DEF-C class in the exact conditions the human playtest surfaced it (a reload/reconnect storm).

Purpose: BoardSmith CLAUDE.md — "confirm features work end-to-end in the browser before marking work
complete." The fix's automated proof lives in 153-01; this plan is its live-browser confirmation.
Output: A recorded result in 153-02-SUMMARY.md. This plan is NON-autonomous (needs a running dev server +
browser) and gated on 153-01 landing — mirroring how Phase 152's browser proof (152-05) was structured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/153-dev-host-multi-client-turn-desync-fix/153-CONTEXT.md
@.planning/phases/153-dev-host-multi-client-turn-desync-fix/153-RESEARCH.md

<interfaces>
<!-- SC-3 references -->
- The DEF-C symptom being ruled out: a client whose UI still says "your turn" after the server has moved
  on (originally observed as "Not Player 1's turn" rejection during a 2-browser reload/reconnect storm).
- The fix under test (plan 153-01): dev.ts's WS close handler now ignores a stale close whose socket is no
  longer the registered one for its clientId, so a reload's reconnect is not silently orphaned.
- Chrome extension is currently UNAVAILABLE — Playwright is the standing headless fallback (memory:
  browser-testing-playwright-fallback; used successfully in Phase 152).
- Real-time SPA rule (CLAUDE.md + RESEARCH Pitfall 6): a live-WS dev-host NEVER reaches `networkidle`. Wait
  for `domcontentloaded` / the `load` event / a specific board or seat selector (e.g. the "init"-triggered
  board render), never `waitForLoadState('networkidle')` — it will hang forever.
- Dev-host shape: `npx boardsmith dev` serves on :5173; each browser tab is a real player connecting over
  WS, persisting its clientId in localStorage (so a tab reload reconnects with the same clientId — the
  exact trigger). Two tabs/contexts = two clients. Kill the dev server (:5173) when done — never leave it
  running (CLAUDE.md hard rule; memory: never-leave-dev-server).
- Scope (RESEARCH Open Q2): the reload-mid-game trigger is the CORE assertion (one reload is enough to hit
  the race). The "hand a seat to AI and back" leg is a second assertion in the same script, not a separate
  harness. Keep the script narrow — the root cause is already fully covered by 153-01's automated tests.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Scripted 2-client reload/reconnect/AI-handoff browser proof (no stale "your turn")</name>
  <files></files>
  <read_first>
    - .planning/phases/153-dev-host-multi-client-turn-desync-fix/153-RESEARCH.md (Pitfall 6 network-idle trap; Open Q2 scope guidance; the DEF-C symptom mechanism)
    - .planning/phases/152-asset-completeness-in-bs-build-chunk/152-05-PLAN.md (the Phase-152 browser-proof checkpoint shape this mirrors)
    - src/cli/dev-host/DevHost.vue (lines ~29-36, 124 — clientId localStorage persistence + hello-on-WS-open, confirming a tab reload reconnects with the same clientId)
  </read_first>
  <what-built>
    The plan-153-01 dev.ts socket-identity guard is landed and green in automated tests. This checkpoint
    confirms it in a real browser: a scripted 2-client `npx boardsmith dev` session (Playwright headless,
    extension unavailable) that reloads a seat mid-game, reconnects, and hands a seat to AI and back, and
    asserts no client is ever shown "your turn" out of turn.
  </what-built>
  <how-to-verify>
    1. Pick a simple 2-player reference game (e.g. `cd ~/BoardSmithGames/go-fish` or another alternating-turn
       game) and start `npx boardsmith dev --no-open` (serves :5173). Confirm it compiles/serves.
    2. Drive two Playwright browser contexts (two seats) against http://localhost:5173. Wait on a board/seat
       selector or `domcontentloaded` for each — NEVER `networkidle` (it never fires; the page holds a live
       WS — RESEARCH Pitfall 6).
    3. Advance the game so it is Seat 1's turn. RELOAD Seat 1's tab mid-game; wait for its board to
       re-render (selector, not idle); confirm it reconnects to the same seat (same persisted clientId).
    4. CORE assertion: after the reload/reconnect, neither client shows "your turn" when the server says it
       is the other seat's turn; the reloaded seat can still act and its action is reflected on both clients
       (broadcast delivery survived the reconnect — the DEF-C fix).
    5. Second assertion: hand one seat to AI and back (AI-seat takeover), and confirm turn view stays
       consistent across the handoff.
    6. Record per-client turn view (isMyTurn / whose-turn) at each step, plus a screenshot or console tally,
       in 153-02-SUMMARY.md. Then KILL the dev server (:5173) — never leave it running.
  </how-to-verify>
  <verify>
    <manual>Across reload → reconnect → AI-handoff-and-back, no client ever displays "your turn" when it is not that client's turn; the reloaded seat keeps receiving broadcasts and can still act. Result (per-client turn view per step + screenshot/console tally) recorded in 153-02-SUMMARY.md. Dev server confirmed killed.</manual>
  </verify>
  <acceptance_criteria>
    - The check ran against a REAL `npx boardsmith dev` server (:5173) driven by Playwright (extension unavailable), not an in-process harness.
    - A seat was reloaded mid-game and reconnected to the same seat (same persisted clientId) — the exact DEF-C trigger was exercised.
    - No client displayed "your turn" when it was not that client's turn, at any step (reload, reconnect, AI-handoff-and-back). Recorded per-client per-step in 153-02-SUMMARY.md.
    - The reloaded seat kept receiving broadcasts and could still act post-reconnect (delivery survived — proving the 153-01 fix in the browser).
    - No `waitForLoadState('networkidle')` was used anywhere in the script (selectors / domcontentloaded only).
    - The dev server started for this check is confirmed killed (no :5173 left running).
  </acceptance_criteria>
  <resume-signal>Type "approved" if no stale "your turn" was observed across reload/reconnect/AI-handoff, or describe the observed desync.</resume-signal>
</task>

</tasks>

<verification>
- 153-02-SUMMARY.md records a clean 2-client reload/reconnect/AI-handoff result (no stale "your turn").
- The check was Playwright-driven against a real `npx boardsmith dev` server, waiting on selectors (never networkidle).
- No dev server left running (:5173 killed).
</verification>

<success_criteria>
ROADMAP.md Phase 153 Success Criterion 3 is satisfied: a scripted 2-client dev-host session (reload
mid-game → reconnect → hand a seat to AI and back) never shows "your turn" on a client whose turn it is
not — confirming the plan-153-01 fix end-to-end in a real browser.
</success_criteria>

<output>
Create `.planning/phases/153-dev-host-multi-client-turn-desync-fix/153-02-SUMMARY.md` when done
</output>
