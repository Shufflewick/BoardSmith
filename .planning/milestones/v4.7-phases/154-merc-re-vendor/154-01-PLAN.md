---
phase: 154-merc-re-vendor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [VENDOR-01]

must_haves:
  truths:
    - "MERC's vendored boardsmith tgz is refreshed to current BoardSmith HEAD (carries DEF-B 281e8155 + Phase 152 asset + Phase 153 dev-host fixes)"
    - "MERC's full test suite passes at or above its last known-green baseline (738 passing) against the re-vendored copy"
    - "Any MERC-side change beyond the mechanical re-vendor bookkeeping is either unnecessary (none needed) or documented as a real compatibility gap — never a silent patch to force green"
  artifacts:
    - path: ".planning/phases/154-merc-re-vendor/154-01-SUMMARY.md"
      provides: "recorded re-vendor result: new tgz name, MERC test pass/fail counts vs baseline, any compatibility gaps, and the MERC commit hash"
  key_links:
    - from: "BoardSmith HEAD (v4.7 fixes)"
      to: "green MERC test suite"
      via: "boardsmith pack --target <MERC> ; npm test in MERC"
      pattern: "re-vendor"
---

<objective>
Re-vendor MERC's BoardSmith copy to current HEAD so it carries DEF-B (`281e8155`) + the v4.7 Phase 152
(asset completeness) and Phase 153 (dev-host turn-desync) fixes, and prove it by MERC's own test suite
staying green (baseline 738 passing). MERC uses a vendored `.tgz` (not a symlink), so this re-vendor is
the only way the fixes reach it — and running MERC's suite against the fresh copy is the milestone's
capstone cross-repo integration proof.

Output: a re-vendored + committed MERC repo, and a recorded result in 154-01-SUMMARY.md. NON-autonomous
(touches the sibling MERC repo, runs its suite, and needs judgment on any regression).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/154-merc-re-vendor/154-CONTEXT.md

<interfaces>
- Re-vendor tool: `node bin/boardsmith.js pack --target ~/Dropbox/MERC/BoardSmith/MERC` (from BoardSmith
  root) — packs a timestamped `boardsmith-0.0.1-<ts>.tgz`, copies to MERC `vendor/`, rewrites MERC
  `package.json` boardsmith dep + overrides, runs `npm install` in MERC.
- MERC test command: `npm test` (= `vitest --run`) from `~/Dropbox/MERC/BoardSmith/MERC`.
- Prior vendored build: `boardsmith-0.0.1-20260703202418.tgz` (v4.5-era) — pre-DEF-B/pre-v4.7.
- Baseline: 738 passing / 7 (per prior-milestone campaign records).
</interfaces>
</context>

<tasks>

<task type="execute">
  <name>Task 1: Pre-flight — confirm BoardSmith HEAD is clean and carries the fixes</name>
  <files></files>
  <read_first>
    - .planning/phases/154-merc-re-vendor/154-CONTEXT.md
  </read_first>
  <action>
    From the BoardSmith repo: confirm the working tree is clean (only the untracked
    boardsmith-audit-report-3.html is acceptable), `git merge-base --is-ancestor 281e8155 HEAD`
    succeeds, and the Phase 152/153 artifacts exist (src/cli/lib/asset-scan.ts, generateAssetImageVue
    in project-scaffold.ts, src/cli/dev-host/connection-handler.ts). Confirm the full BoardSmith suite
    is green (npm test — already verified 2677, re-run if uncommitted changes exist). Abort with a clear
    message if HEAD is dirty or missing a fix (a re-vendor of an incomplete HEAD would not carry the v4.7 fixes).
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && git merge-base --is-ancestor 281e8155 HEAD && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git merge-base --is-ancestor 281e8155 HEAD` exits 0.
    - BoardSmith working tree clean except the known untracked audit report.
    - asset-scan.ts, connection-handler.ts, and generateAssetImageVue present.
  </acceptance_criteria>
  <done>BoardSmith HEAD verified clean and carrying DEF-B + v4.7 fixes — safe to pack.</done>
</task>

<task type="execute">
  <name>Task 2: Re-vendor into MERC via boardsmith pack --target</name>
  <files></files>
  <read_first>
    - src/cli/commands/pack.ts (the --target integrate path — copies tgz, updates deps, npm installs)
  </read_first>
  <action>
    Run `node bin/boardsmith.js pack --target ~/Dropbox/MERC/BoardSmith/MERC` from the BoardSmith root.
    Confirm a NEW timestamped `boardsmith-0.0.1-<ts>.tgz` (ts newer than 20260703202418) landed in MERC's
    vendor/, MERC's package.json boardsmith dependency now points at it (`file:./vendor/<new>.tgz`), and
    the pack's own `npm install` in MERC succeeded. If pack does not run npm install itself, run
    `npm install` in MERC. Do NOT hand-edit the tarball or deps — let the tool own the mechanical bump.
  </action>
  <verify>
    <automated>node -e "const d=require('/Users/jtsmith/Dropbox/MERC/BoardSmith/MERC/package.json'); const v=d.dependencies.boardsmith; if(!/vendor\/boardsmith-0\.0\.1-/.test(v)) throw new Error('dep not updated: '+v); if(/20260703202418/.test(v)) throw new Error('still on old tgz'); console.log('dep now:', v)"</automated>
  </verify>
  <acceptance_criteria>
    - A new `boardsmith-0.0.1-<ts>.tgz` (ts > 20260703202418) exists in MERC's vendor/.
    - MERC package.json boardsmith dep points at the new tgz (not the 20260703202418 one).
    - npm install in MERC completed without error (node_modules/boardsmith reflects the new build).
  </acceptance_criteria>
  <done>MERC's vendored boardsmith is refreshed to current HEAD.</done>
</task>

<task type="execute">
  <name>Task 3: Run MERC's suite, compare to baseline, document any gap</name>
  <files></files>
  <read_first>
    - ~/Dropbox/MERC/BoardSmith/MERC/package.json (test script)
  </read_first>
  <action>
    From ~/Dropbox/MERC/BoardSmith/MERC run `npm test` (vitest --run) and capture the pass/fail/skip
    counts. Compare against the 738-passing baseline. If green at same-or-better: record the numbers.
    If ANY test regresses: DO NOT silently patch MERC to go green. Diagnose which v4.7 change caused it
    (asset scaffold, AutoUI renderer guard, or dev-host connection handler), and record it as a real
    compatibility gap in the SUMMARY (what broke, root cause, and either the minimal justified MERC-side
    adjustment or a BoardSmith-side follow-up recommendation). Prefer a BoardSmith-side fix if the break
    reveals a v4.7 regression; a MERC-side change is only for genuine MERC-specific API drift and must be
    documented. Optionally also run MERC's `boardsmith lint`/`validate` as a secondary check.
  </action>
  <verify>
    <manual>MERC `npm test` output captured; pass count >= 738 (or every regression documented as a real compatibility gap with root cause, not silently patched).</manual>
  </verify>
  <acceptance_criteria>
    - MERC `npm test` ran to completion against the re-vendored copy; pass/fail/skip counts recorded in 154-01-SUMMARY.md.
    - Pass rate is at or above the 738-passing baseline, OR every regression is documented with root cause and disposition (no silent patch).
  </acceptance_criteria>
  <done>MERC suite result recorded and compared to baseline; any gap documented, not hidden.</done>
</task>

<task type="execute">
  <name>Task 4: Commit the re-vendor in the MERC repo</name>
  <files></files>
  <read_first>
    - ~/Dropbox/MERC/BoardSmith/MERC (git log for the established re-vendor commit-message pattern)
  </read_first>
  <action>
    In the MERC repo, stage the re-vendor changes (package.json, package-lock.json, the new vendor tgz,
    any boardsmith.json bookkeeping the pattern produces) and commit with a message following MERC's
    established pattern: `chore: re-vendor boardsmith (v4.7 playtest follow-up: DEF-B + ASSET/DEVHOST fixes)`.
    Do NOT push. Record the commit hash in the SUMMARY. Only commit the mechanical re-vendor + any
    documented compatibility adjustment — nothing unrelated.
  </action>
  <verify>
    <automated>git -C /Users/jtsmith/Dropbox/MERC/BoardSmith/MERC log --oneline -1 | grep -iq "re-vendor" && echo committed</automated>
  </verify>
  <acceptance_criteria>
    - A MERC commit exists whose message follows the re-vendor pattern and references v4.7 / DEF-B.
    - The commit contains the vendor tgz + dep bump (and only re-vendor-related changes).
    - Commit hash recorded in 154-01-SUMMARY.md.
  </acceptance_criteria>
  <done>The re-vendor is committed in MERC — the milestone's cross-repo integration proof is on record.</done>
</task>

</tasks>

<verification>
- 154-01-SUMMARY.md records: new tgz name, MERC pass/fail/skip vs the 738 baseline, any compatibility gaps, and the MERC commit hash.
- BoardSmith repo itself unchanged (only planning docs); all real changes are in the MERC repo.
</verification>

<success_criteria>
VENDOR-01: MERC's vendored BoardSmith copy carries DEF-B + the v4.7 asset and dev-host fixes, and MERC's
full test suite passes at or above its last known-green baseline against the re-vendored copy.
</success_criteria>

<output>
Create `.planning/phases/154-merc-re-vendor/154-01-SUMMARY.md` when done
</output>
</content>
