---
phase: 159-mcts-soundness-dynamic-multiselect
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/utils/snapshot.ts
  - src/ai/mcts-bot.ts
  - src/ai/mcts-redaction.test.ts
autonomous: true
requirements: [AI-02, PROC-01]

must_haves:
  truths:
    - "The MCTS bot's cloned/search state derives from toJSONForPlayer(botSeat), so it does NOT expose another seat's hidden attributes"
    - "A scenario exploitable with the naive bot (a move that betrays knowledge of a hidden card) is non-exploitable after the fix"
    - "In a single simultaneous step, a later co-decider does not see an earlier co-decider's committed pick"
    - "The redacted clone round-trips through loadSerializedState into a valid playable game (restorable, not just serializable)"
    - "Non-bot createSnapshot callers (runner.ts) are unaffected — the default still captures full toJSON"
  artifacts:
    - path: "src/engine/utils/snapshot.ts"
      provides: "createSnapshot gains an opt-in per-seat redacted path (forSeat) — default unchanged for non-bot callers"
      contains: "forSeat"
    - path: "src/ai/mcts-bot.ts"
      provides: "captureSnapshot clones the bot's redacted view; the simultaneous-step loop no longer leaks co-decider picks"
    - path: "src/ai/mcts-redaction.test.ts"
      provides: "AI-02 redaction + exploitability + simultaneous + restorability regression (>=2 moves), driving MCTSBot directly"
  key_links:
    - from: "src/ai/mcts-bot.ts"
      to: "toJSONForPlayer"
      via: "captureSnapshot passes the bot seat so createSnapshot redacts"
      pattern: "forSeat|toJSONForPlayer"
---

<objective>
Close AI-02: the MCTS bot clones un-redacted truth. `captureSnapshot` (`mcts-bot.ts:1067`) calls
`createSnapshot`, which serializes `state: game.toJSON()` (`snapshot.ts:184`) — the FULL, un-redacted
tree — so the search sees opponents' hidden info. Separately, the simultaneous-step loops
(`mcts-bot.ts:865-872`, `:901-908`) resolve `awaitingPlayers` one at a time on a fully-visible clone, so
a later co-decider sees an earlier one's committed pick.

Purpose: make the bot sound (non-exploitable where the naive bot was) by (1) cloning the bot's per-seat
REDACTED view via the existing `game.toJSONForPlayer(botSeat)` — reuse, do not invent redaction — and
(2) fixing the simultaneous loop so co-deciders don't leak picks. This is a redact-only soundness fix;
determinization (sampling hidden worlds) is explicitly DEFERRED (CONTEXT §out of scope).

CAUTION (RESEARCH pitfall): `createSnapshot` has a non-bot caller (`runner.ts:527`). Do NOT change its
default. Add an opt-in per-seat path (`forSeat`) and have ONLY the bot pass its seat.

Output: `createSnapshot` gains an opt-in redacted path; `captureSnapshot` uses it; the simultaneous loop
is corrected; net-new redaction / exploitability / simultaneous / restorability coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-mcts-soundness-dynamic-multiselect/159-CONTEXT.md
@.planning/phases/159-mcts-soundness-dynamic-multiselect/159-RESEARCH.md

@src/engine/utils/snapshot.ts
@src/ai/mcts-restore.test.ts
</context>

<interfaces>
<!-- Extracted from the codebase. Use these directly; do not go exploring. -->

`createSnapshot(game, gameType, actionHistory?, seed?)`, `snapshot.ts:173-192` — currently
`state: game.toJSON()` at `:184`. Add an opt-in per-seat redacted path (e.g. an `opts?: { forSeat?: number }`
5th arg): when `forSeat` is provided, use `game.toJSONForPlayer(forSeat)`; otherwise keep `game.toJSON()`.
Default behavior for existing callers is UNCHANGED.

Non-bot caller that MUST keep full truth — do NOT pass a seat here:
`runner.ts:527` `createSnapshot(this.game, this.gameType, this.actionHistory, this.seed)`.

`game.toJSONForPlayer(player: P | number | null)`, `game.ts:2738` — the canonical per-seat serializer.
Uses `getEffectiveVisibility` / `isVisibleTo` / `redactHiddenElementAttrs`; a hidden leaf loses its
identity-bearing `name` and gets `attributes.__hidden`, count-only zones expose only `childCount`.
REUSE THIS — it is what `createPlayerView` (`snapshot.ts:261`) already uses. Spectator = seat `-1`.

Bot clone/restore path (context):
- `captureSnapshot`, `mcts-bot.ts:1066-1068` — pass `this.playerIndex` into the new redacted path.
- `restoreGame`, `mcts-bot.ts:1088-1121` → `game.loadSerializedState(snapshot.state)` — STATE-AUTHORITATIVE,
  no replay. The redacted `state` MUST round-trip through this into a valid playable game (RESEARCH
  pitfall: verify restorable, not just serializable — a `__hidden`/count-only element must load).

Simultaneous-step loops to correct:
- `enumerateMovesForSimulation`, `mcts-bot.ts:865-872` — picks the FIRST `awaitingPlayers` seat on the
  live clone.
- `getCurrentPlayerFromFlowState`, `mcts-bot.ts:901-908` — same first-awaiting pick.
Fix so a committed co-decider move within one simultaneous step is not visible to a later co-decider
(e.g. enumerate all co-deciders from the SAME pre-reveal snapshot; mechanism at Claude's discretion,
provided the test proves no leak).

MCTSBot short-circuits at `mcts-bot.ts:224` when `allMoves.length === 1` — bypasses cloning. Redaction
tests MUST offer >=2 moves so the clone path is exercised (CONTEXT / RESEARCH). In-file `Game` subclass
harness per `mcts-restore.test.ts`, NOT the excluded checkers-dependent `mcts-bot.test.ts`.
</interfaces>

<decisions>
## Locked decisions implemented here (CONTEXT §MCTS Hidden-Info / §Simultaneous-Reveal)

- **D-08 (redact-only):** the bot clones `toJSONForPlayer(botSeat)`, not `toJSON`. Reuse the existing
  redaction; do NOT invent one and do NOT fabricate/guess hidden state — hidden elements clone in their
  redacted form and the bot treats them as fixed unknowns. NO determinization (deferred).
- **CAUTION (non-bot callers):** add an OPT-IN `forSeat` path to `createSnapshot`; do NOT change its
  default. `runner.ts:527` must keep full `toJSON`. A test asserts the non-bot path is unredacted.
- **Simultaneous soundness:** redaction alone only PARTLY fixes the leak (a redacted clone hides pending
  picks); the enumeration loop must ALSO be corrected. Fix BOTH the clone source and the loop. Scope:
  the bot's own-seat decision AND rollout simulation of simultaneous steps.
- **Restorability:** a redacted clone must round-trip through `loadSerializedState` (STATE-AUTHORITATIVE,
  no replay) into a valid playable game — verified by a guard test, not assumed.
- **>=2 moves** in every clone/redaction test so the `:224` short-circuit doesn't bypass the clone path.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): redaction + exploitability + simultaneous + restorability tests, proven failing</name>
  <files>src/ai/mcts-redaction.test.ts</files>
  <behavior>
    - RED (redaction): a hidden-info `Game` subclass gives one seat a hidden attribute (e.g. an
      opponent's face-down card value). The bot's captured/cloned search state (`captureSnapshot` →
      restored game) does NOT expose that hidden attribute — pre-fix it DOES (clone from `toJSON`).
      The action offers >=2 moves so the clone path runs.
    - RED (exploitability): a scenario where the naive bot's chosen move betrays knowledge of the hidden
      card — pre-fix the bot picks the move only a cheater could; post-fix it cannot (its view is
      redacted). Assert on the observable move choice, not internals.
    - RED (simultaneous): a simultaneous step with two co-deciders — a later co-decider's enumerated
      moves must not depend on an earlier co-decider's committed pick. Pre-fix they do.
    - Restorability: the redacted snapshot round-trips through `restoreGame` / `loadSerializedState`
      into a valid, playable game (flow intact, actions enumerable) — a guard the fix must keep green.
  </behavior>
  <action>
Create `src/ai/mcts-redaction.test.ts` with in-file `Game` subclasses (model on `mcts-restore.test.ts`).
Build a hidden-info fixture: a zone whose contents are visible only to their owner (use the engine's
existing visibility API so `toJSONForPlayer` redacts it), seeded so an opponent holds a known-to-the-test
hidden value. Register an action with >=2 choices so the bot clones (not the `:224` short-circuit).

Assert against CURRENT, UNFIXED source:
- capture the bot's snapshot (or drive `MCTSBot` far enough that `captureSnapshot` runs) and assert the
  restored `state` does NOT contain the opponent's hidden attribute — expect FAIL today.
- exploitability: construct the scenario where a full-info bot makes the tell-tale move; assert the bot
  does NOT — expect FAIL today.
- simultaneous: assert a later co-decider's move set is independent of the earlier pick — expect FAIL.
- restorability: assert `restoreGame(snapshot)` yields a playable game (used both as a pre-existing
  invariant and the post-fix guard).

Run, capture the verbatim RED output (PROC-01: observed, not asserted), record it in the commit message.
Commit test-only as a deliberately RED commit — no `snapshot.ts` / `mcts-bot.ts` change in this task.
  </action>
  <verify>
    <automated>npx vitest run src/ai/mcts-redaction.test.ts 2>&1 | tee /tmp/159-03-red.txt; grep -q "FAIL" /tmp/159-03-red.txt</automated>
  </verify>
  <done>Test file runs and FAILS: the bot's clone exposes the hidden attribute / makes the exploitable move / leaks the co-decider pick. Failure output captured verbatim in the commit message. No production source modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): opt-in redacted snapshot + bot clone source + simultaneous loop fix</name>
  <files>src/engine/utils/snapshot.ts, src/ai/mcts-bot.ts</files>
  <behavior>
    - `createSnapshot` with the new `forSeat` option serializes `state` via `toJSONForPlayer(forSeat)`;
      without it, unchanged (`toJSON`).
    - `captureSnapshot` passes the bot seat, so the search clone is redacted.
    - The simultaneous-step loops no longer feed an earlier co-decider's committed pick to a later one.
  </behavior>
  <action>
`snapshot.ts`: add an opt-in per-seat path to `createSnapshot` (e.g. a 5th `opts?: { forSeat?: number }`
arg). When `forSeat` is provided, `state: game.toJSONForPlayer(forSeat)`; otherwise keep
`game.toJSON()`. Do NOT touch `createActionCheckpoint` or `runner.ts` — the default must stay full-truth
for the non-bot caller.

`mcts-bot.ts`: `captureSnapshot` (`:1066-1068`) passes `{ forSeat: this.playerIndex }`. Correct the
simultaneous loops (`enumerateMovesForSimulation` `:865-872`, `getCurrentPlayerFromFlowState`
`:901-908`) so co-deciders are enumerated without seeing each other's committed picks within one
simultaneous step (e.g. resolve all co-deciders from the same pre-reveal snapshot). Fix BOTH the clone
source and the loop (redaction alone is insufficient per CONTEXT).

Confirm the redacted `state` still restores: `restoreGame` → `loadSerializedState` must accept the
`__hidden`/count-only shapes `toJSONForPlayer` produces (Task 1's restorability assertion is the guard).
  </action>
  <verify>
    <automated>npx vitest run src/ai/mcts-redaction.test.ts src/ai/mcts-restore.test.ts</automated>
  </verify>
  <done>Task 1 tests PASS: clone is redacted, exploitable move is gone, co-decider leak closed, redacted snapshot restores. mcts-restore still green (no collateral break). RED→GREEN before/after recorded for the SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: non-bot-caller guard + adversarial exploitability + full suite</name>
  <files>src/ai/mcts-redaction.test.ts</files>
  <behavior>
    - Non-bot guard: `createSnapshot` WITHOUT `forSeat` (the `runner.ts` shape) still captures full,
      un-redacted `toJSON` — the fix must not silently redact for everyone.
    - Adversarial exploitability: re-run the tell-tale-move scenario across multiple seeds / both seats
      and confirm the redacted bot never leaks hidden knowledge (not just the one seed in Task 1).
    - Full suite stays at or above the pre-phase baseline (runner / session snapshot consumers unbroken).
  </behavior>
  <action>
Add a guard case asserting `createSnapshot(game, type, history, seed)` (no `forSeat`) yields a snapshot
whose `state` still contains a hidden attribute — proving the default path is unchanged for non-bot
callers (RESEARCH CAUTION). Strengthen the exploitability case: iterate seeds / swap which seat is the
bot and assert non-exploitability holds across all (CONTEXT §specifics: "non-exploitable where the naive
bot was"). Run the full suite to confirm no regression in `runner.ts` / session snapshot consumers.
  </action>
  <verify>
    <automated>npx vitest run src/ai/mcts-redaction.test.ts && npm test</automated>
  </verify>
  <done>Non-bot default proven unredacted; exploitability non-repro across seeds/seats; full suite at or above pre-phase baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| opponent's hidden game state → MCTS search clone | The bot must reason over ONLY its own seat's information; cloning full `toJSON` crosses this boundary and lets the bot "see" opponents' hidden cards/attributes |
| earlier co-decider's committed pick → later co-decider (same simultaneous step) | Within one simultaneous reveal, a pick committed on the shared clone must not become visible to a co-decider still choosing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-06 | Information Disclosure | `captureSnapshot` → `createSnapshot(state: game.toJSON())` clones un-redacted truth, exposing opponents' hidden attributes to the bot (THE AI-02 leak) | mitigate | Bot clones `toJSONForPlayer(botSeat)` via opt-in `forSeat`; Task 1/3 assert the clone omits hidden attrs and the exploitable move is gone |
| T-159-07 | Information Disclosure | Simultaneous-step loop feeds an earlier co-decider's committed pick to a later co-decider on the shared visible clone | mitigate | Correct `enumerateMovesForSimulation` / `getCurrentPlayerFromFlowState` so co-deciders enumerate from the pre-reveal snapshot; Task 1 simultaneous assertion |
| T-159-08 | Tampering | Changing `createSnapshot`'s default would redact for non-bot callers (runner/session), corrupting authoritative full-truth snapshots | mitigate | `forSeat` is opt-in; default unchanged; Task 3 guard asserts the non-bot path stays full `toJSON` |
| T-159-09 | Denial of Service (soundness) | A redacted clone that fails to round-trip through `loadSerializedState` would strand the search | mitigate | Task 1 restorability guard; reuse the same `toJSONForPlayer` shapes `createPlayerView` already round-trips |
| T-159-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase (RESEARCH.md: no new libraries) |
</threat_model>

<verification>
- `npx vitest run src/ai/mcts-redaction.test.ts src/ai/mcts-restore.test.ts`
- `npm test` — full suite at or above pre-phase baseline
- Grep gate: the bot clones a redacted per-seat view —
  `grep -v '^\s*\*' src/ai/mcts-bot.ts | grep -Ec 'forSeat|playerIndex'` reaches captureSnapshot (>= 1
  new reference at the capture site)
- Grep gate: the redacted path exists in the serializer —
  `grep -c 'toJSONForPlayer' src/engine/utils/snapshot.ts` returns >= 2 (createPlayerView + new forSeat path)
- Grep gate: default unchanged for non-bot callers —
  `grep -c 'game.toJSON()' src/engine/utils/snapshot.ts` returns >= 1 (createSnapshot default + createActionCheckpoint)
</verification>

<success_criteria>
- The bot's search clone derives from `toJSONForPlayer(botSeat)` and exposes no opponent hidden attrs.
- A scenario exploitable with the naive bot is non-exploitable after (across seeds/seats).
- Co-deciders in one simultaneous step do not see each other's picks.
- The redacted clone round-trips through `loadSerializedState` into a valid playable game.
- Non-bot `createSnapshot` callers still capture full truth.
- PROC-01 satisfied: RED observed and captured before GREEN; adversarial exploitability exercised.
</success_criteria>

<output>
Create `.planning/phases/159-mcts-soundness-dynamic-multiselect/159-03-SUMMARY.md` when done.
Include the verbatim RED output and the matching GREEN output for the redaction + exploitability
regression (PROC-01 requires the pre-fix failure to be recorded, not asserted).
</output>
