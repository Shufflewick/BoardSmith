---
phase: 164-library-misc-action-panel-loop-visual-debug-view
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/utils/color-contrast.ts
  - src/ui/utils/color-contrast.test.ts
  - src/ui/components/PlayerToken.vue
  - src/ui/components/PlayerToken.contrast.test.ts
autonomous: true
requirements: [LIBX-03, PROC-01]

must_haves:
  truths:
    - "contrastInk() returns black ink for a light seat color and white ink for a dark seat color, chosen by WCAG relative-luminance contrast"
    - "The helper parses hex (#rgb/#rrggbb) and rgb()/rgba() strings with NO DOM/canvas dependency (unit-testable in plain node/vitest)"
    - "PlayerToken renders the computed ink color and a text-shadow derived opposite the chosen ink (halo only under white ink; dropped when ink is dark)"
    - "An unparseable color throws a clear actionable error (fail loud, not silent wrong-contrast)"
  artifacts:
    - path: "src/ui/utils/color-contrast.ts"
      provides: "Pure WCAG-luminance contrastInk helper + color parser"
      exports: ["contrastInk"]
      min_lines: 30
    - path: "src/ui/components/PlayerToken.vue"
      provides: "Dynamic ink + text-shadow bound from contrastInk(color)"
      contains: "contrastInk"
  key_links:
    - from: "src/ui/components/PlayerToken.vue"
      to: "src/ui/utils/color-contrast.ts"
      via: "import + bind ink/textShadow into the .ini :style"
      pattern: "contrastInk"
---

<objective>
LIBX-03 (D30): `PlayerToken`'s glyph ink is hardcoded `rgba(255,255,255,.95)`, so the identity letter is invisible/low-contrast on light seat colors. Make the ink luminance-adaptive (black on light seats, white on dark) via a small pure WCAG-relative-luminance helper, with the `text-shadow` halo derived opposite the chosen ink.

Purpose: Legible player glyphs on any seat color; a reusable, DOM-free contrast utility.
Output: `src/ui/utils/color-contrast.ts` (`contrastInk`), a pure unit test, PlayerToken wiring, a component render test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-library-misc-action-panel-loop-visual-debug-view/164-CONTEXT.md
@.planning/phases/164-library-misc-action-panel-loop-visual-debug-view/164-RESEARCH.md

<interfaces>
<!-- Orchestrator resolution 2: DO NOT use Canvas 2D. Pure hex + rgb()/rgba() parser only.
     Seat colors come from src/session/colors.ts (STANDARD_PLAYER_COLORS, all hex). -->
From src/ui/components/PlayerToken.vue (current):
  - color?: string prop (CSS color; falls back to accent token) — PlayerToken.vue:28
  - style binding at :58 already spreads `{ '--tc': color }`; add ink + textShadow to the .ini :style
  - .tok .ini scoped CSS at :80-87 hardcodes `color: rgba(255,255,255,.95)` and `text-shadow: 0 1px 2px rgba(0,0,0,.5)` — BOTH must become dynamic inline styles (remove/override the hardcoded CSS)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure WCAG contrastInk helper + unit test (PROC-01)</name>
  <read_first>
    - src/session/colors.ts (STANDARD_PLAYER_COLORS — the controlled hex palette this must cover)
    - RESEARCH.md Pattern 3 (WCAG 2.1 §1.4.3 relative-luminance formula: srgb gamma-correct + 0.2126/0.7152/0.0722; contrastRatio (l+0.05) ratio)
  </read_first>
  <behavior>
    - contrastInk('#ecf0f1') (near-white seat) → ink '#000000'.
    - contrastInk('#2c3e50') (dark seat) → ink '#ffffff'.
    - contrastInk white-ink case → textShadow is a dark halo (e.g. '0 1px 2px rgba(0,0,0,.5)').
    - contrastInk black-ink case → textShadow 'none' (dropped when ink is dark, per CONTEXT).
    - Accepts 'rgb(44,62,80)' and 'rgba(44,62,80,1)' and '#2c3e50' and '#abc' equivalently.
    - Unparseable input (e.g. 'not-a-color') throws an actionable Error naming the bad value and the supported formats.
  </behavior>
  <action>Create src/ui/utils/color-contrast.ts exporting `contrastInk(seatColor: string): { ink: '#000000' | '#ffffff'; textShadow: string }`. Implement a PURE parser (no DOM, no canvas — per orchestrator resolution 2) that accepts `#rgb`, `#rrggbb`, `rgb(r,g,b)`, `rgba(r,g,b,a)`. Compute WCAG relative luminance via sRGB gamma correction and 0.2126/0.7152/0.0722 coefficients; pick ink as whichever of black/white has the higher WCAG contrast ratio against the seat color; derive textShadow opposite the ink (dark halo under white ink, 'none' under black ink). Throw a clear Error for unsupported formats (Pit of Success: fail loud with a fixable message). Create src/ui/utils/color-contrast.test.ts asserting every behavior above — this is the fail-on-pre-fix/pass-after PROC-01 unit (the file does not exist pre-fix). Keep the math DOM-free so it runs in plain vitest without jsdom canvas.</action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/ui/utils/color-contrast.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `contrastInk` exported from src/ui/utils/color-contrast.ts.
    - All behavior tests pass; the file imports NO 'canvas'/document/getContext (grep -c "getContext\|createElement\|canvas" src/ui/utils/color-contrast.ts == 0).
    - Unsupported-format test asserts a thrown Error whose message contains the offending value.
  </acceptance_criteria>
  <done>Pure contrast helper green in plain node/vitest; no DOM dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire contrastInk into PlayerToken + component test (PROC-01)</name>
  <read_first>
    - src/ui/components/PlayerToken.vue (full file — :28 color prop, :58 style spread, :63 .ini span, :80-87 scoped .tok .ini hardcoded color/text-shadow)
    - src/ui/utils/color-contrast.ts (Task 1 output)
    - An existing GameShell/*.test.ts for @vue/test-utils mount + style-assertion patterns
  </read_first>
  <behavior>
    - Mounting PlayerToken with a light color (e.g. '#ecf0f1') renders the .ini letter with black ink and no (or 'none') text-shadow.
    - Mounting with a dark color (e.g. '#2c3e50') renders white ink and a dark-halo text-shadow.
    - When color is absent (falls back to accent token) the component still renders without throwing (choose a safe default ink; do NOT throw for the no-color path).
  </behavior>
  <action>In src/ui/components/PlayerToken.vue import `contrastInk` from '../utils/color-contrast'. Compute ink + textShadow from the `color` prop (guard the absent-color path — when color is undefined, keep a sensible default, do not call the throwing parser with undefined). Bind `color` and `text-shadow` as inline styles on the `.ini` span (extend the existing :style binding shape used for `--tc` at :58) and REMOVE the hardcoded `color: rgba(255,255,255,.95)` and `text-shadow: 0 1px 2px rgba(0,0,0,.5)` from the scoped `.tok .ini` CSS (:86-87) so the inline computed values are the single source of truth (no-backward-compat: delete the hardcoded rule). Create src/ui/components/PlayerToken.contrast.test.ts (@vue/test-utils, no PlayerToken*test* exists today) asserting the light-vs-dark ink + text-shadow render behavior above.</action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/ui/components/PlayerToken.contrast.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "255, 255, 255\|255,255,255" src/ui/components/PlayerToken.vue` == 0 (hardcoded white ink removed).
    - PlayerToken imports contrastInk; ink + text-shadow are inline-bound on .ini.
    - Component test green for both light and dark seats + the no-color path renders without throwing.
  </acceptance_criteria>
  <done>PlayerToken glyph ink adapts to seat color; component test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| game palette → color parser | A game may supply an arbitrary `ColorChoice.value` string as a seat color. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-03-01 | Denial of Service | contrastInk parser | mitigate | Pure bounded parser over hex/rgb; no regex catastrophic backtracking; unsupported input throws immediately (no unbounded work). |
| T-164-03-02 | Information Disclosure | error message on bad color | accept | Error names the offending value + supported formats only; no internal paths/stack leaked (per CLAUDE.md actionable-error rule). |
</threat_model>

<verification>
- `npx vitest run src/ui/utils/color-contrast.test.ts src/ui/components/PlayerToken.contrast.test.ts` green.
- Full suite deferred to phase gate.
</verification>

<success_criteria>
- Ink is luminance-adaptive; halo derived opposite ink; hardcoded white removed.
- Helper is pure and DOM-free; both tests green.
</success_criteria>

<output>
Create `.planning/phases/164-library-misc-action-panel-loop-visual-debug-view/164-02-SUMMARY.md` when done.
</output>
