---
phase: 166-skills-defects-session-lock-ui-library-boundary
plan: 02
type: execute
wave: 2
depends_on: ["166-01"]
files_modified:
  - src/cli/slash-command/bs/build/build.md
  - src/cli/slash-command/bs/build/investigate.md
  - src/cli/slash-command/bs/build/final-acceptance.md
  - src/cli/slash-command/bs/build-chunk.test.ts
autonomous: true
requirements: [SKILLDEF-02, SKILLDEF-03, PROC-01]

must_haves:
  truths:
    - "build.md states the agent controls the game board ONLY"
    - "build.md states node_modules/boardsmith is a live symlink, read-only, never patched"
    - "build.md states a library gap is FILED, never patched or suppressed"
    - "build.md states built-in BoardSmith UI must never be suppressed — an unusable built-in surface is a library gap to FILE, not a feature to switch off"
    - "build.md states never set platformActionPanelEscapeHatch without explicit client direction"
    - "build.md states .suppressFromDock() is the only sanctioned per-action dock-hiding mechanism"
    - "investigate.md Required Reading points at the read-only-library boundary rule"
    - "final-acceptance.md states never override an explicit client instruction on design judgment"
    - "Each new drift assertion fails against pre-fix source and passes after the edit (PROC-01)"
  artifacts:
    - path: "src/cli/slash-command/bs/build/build.md"
      provides: "## Boundaries section (board-only, read-only library, file-not-patch, never-suppress) + escape-hatch don't"
      contains: "## Boundaries"
    - path: "src/cli/slash-command/bs/build/investigate.md"
      provides: "Required Reading pointer to the read-only-library boundary"
    - path: "src/cli/slash-command/bs/build/final-acceptance.md"
      provides: "Never-override-explicit-client-instruction rule at the client gate"
    - path: "src/cli/slash-command/bs/build-chunk.test.ts"
      provides: "Failing-first drift assertions for SKILLDEF-02 + SKILLDEF-03 prose"
  key_links:
    - from: "src/cli/slash-command/bs/build/investigate.md"
      to: "src/cli/slash-command/bs/build/build.md"
      via: "Required Reading pointer to the ## Boundaries rule"
      pattern: "node_modules/boardsmith"
    - from: "src/cli/slash-command/bs/build/build.md"
      to: "src/engine/action/types.ts (.suppressFromDock)"
      via: "names the sanctioned per-action dock-hiding mechanism"
      pattern: "suppressFromDock"
---

<objective>
Fix SKILLDEF-02 and SKILLDEF-03: the bs-skills never stated the game/library boundary, so an agent
suppressed built-in BoardSmith UI and considered editing the library (the
`bs-skills-never-suppress-builtin-ui.md` Failure-2 filing). Add an explicit "## Boundaries" section
to build.md stating the agent controls the game board only, `node_modules/boardsmith` (a live
symlink to the client's real checkout) is read-only and never patched, and a library gap is FILED
not patched or suppressed — including never suppressing a built-in surface. Alongside it, forbid
using the LIBX-01 fenced escape hatch `platformActionPanelEscapeHatch` without the client, naming
`.suppressFromDock()` as the only sanctioned per-action dock-hiding mechanism. Add a read-only
pointer in investigate.md's Required Reading and a never-override-client rule at final-acceptance's
client gate.

Purpose: The right path (file a library gap) must be the obvious path; patching the library or
switching off built-in UI must be explicitly out of bounds.
Output: Edited build.md / investigate.md / final-acceptance.md (repo source) + failing-first tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/166-skills-defects-session-lock-ui-library-boundary/166-CONTEXT.md
@.planning/phases/166-skills-defects-session-lock-ui-library-boundary/166-VALIDATION.md

CRITICAL: Edit only the REPO source-of-truth copies under `src/cli/slash-command/bs/`. NEVER edit
`~/.claude/skills/bs-*`. There is no `bs-shared/` directory in the repo; the CONTEXT.md
`bs-shared/...` paths resolve to `src/cli/slash-command/bs/...`.

DEPENDS ON 166-01: this plan appends to `build-chunk.test.ts`, which 166-01 also edits. It runs in
wave 2 (after 166-01 lands) to avoid a git-write race on the shared test file. Add a NEW describe
block for SKILLDEF-02/03; do not touch 166-01's SKILLDEF-01 block.

<interfaces>
Library rename (LIBX-01, shipped Phase 164 — reference by the NEW name only):
- `src/ui/components/GameShell.vue:147` — `platformActionPanelEscapeHatch?: boolean` (was `suppressActionPanel`). This is the fenced whole-panel escape hatch SKILLDEF-03 forbids using without the client.
- `src/engine/action/types.ts:467-469` and `src/engine/element/action-metadata.ts:76` — per-action `suppressFromDock` metadata field. `.suppressFromDock()` is the ONLY sanctioned per-action dock-hiding mechanism.

Grep-confirmed clean slate (all pure additions, zero regression risk): no bs-skill currently
references `node_modules`, "suppress", `platformActionPanelEscapeHatch`, or `suppressFromDock` —
every new assertion is red-first by construction.

Edit-target anchors:
- build.md: scope/design prose lives near the top ("Extends, Never Restructures" ~28-38, UI/placeholder prose ~76-97). Add "## Boundaries" as a new section (near the scope prose) and place the escape-hatch "don't" in the UI-writing region so it sits beside the placeholder/UI rules.
- investigate.md: "Required Reading" is the opening region (~1-16); add the read-only-library pointer there.
- final-acceptance.md: the design-QA / client-judgment gate region (~78-79 human-narrated checks, ~145-172 downstream/client-gate). Add the never-override-explicit-client-instruction rule at the client-gate.

Drift-test conventions: same `read()` / named-marker-constant / `toContain` / one-describe-per-requirement pattern as 166-01; `read('build/build.md')`, `read('build/investigate.md')`, `read('build/final-acceptance.md')` are all already used in build-chunk.test.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: "## Boundaries" section in build.md + read-only pointer in investigate.md + never-override-client in final-acceptance.md (SKILLDEF-02)</name>
  <files>src/cli/slash-command/bs/build/build.md, src/cli/slash-command/bs/build/investigate.md, src/cli/slash-command/bs/build/final-acceptance.md, src/cli/slash-command/bs/build-chunk.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/build/build.md (full — 103 lines; scope prose ~28-45, UI prose ~76-97)
    - src/cli/slash-command/bs/build/investigate.md (Required Reading region ~1-16)
    - src/cli/slash-command/bs/build/final-acceptance.md (human-narrated checks ~78-79 and downstream/client-gate ~145-172)
    - src/cli/slash-command/bs/build-chunk.test.ts (marker-constant block + BUILD-05 `describe('BUILD-05 — build step')` and BUILD-02 investigate blocks for the read()/toContain pattern)
    - findings: ~/BoardSmithLab/findings/bs-skills-never-suppress-builtin-ui.md (Failure 2 — what the agent actually did wrong)
  </read_first>
  <action>
    Add a new "## Boundaries" section to build.md (place it near the scope prose, e.g. after
    "Extends, Never Restructures") stating four rules in the existing skill voice: (1) the agent
    controls the GAME BOARD only — the game's own source, not the platform; (2) `node_modules/boardsmith`
    is a LIVE SYMLINK to the client's real BoardSmith checkout — it is READ-ONLY and must NEVER be
    patched or edited (a change there silently mutates the client's library); (3) a shortfall in the
    library is a LIBRARY GAP that is FILED, never patched — name the file-a-gap path as the correct
    move; (4) built-in BoardSmith UI must NEVER be suppressed — a built-in surface that cannot drive
    the game is a library gap to FILE, not a feature to switch off. Frame (3) and (4) as the
    pit-of-success right-path: filing is the easy/correct path, patching/suppressing is out of bounds.
    In investigate.md's Required Reading, add a pointer sentence to build.md's "## Boundaries"
    (specifically the read-only-library rule) so the interpretation step carries the boundary before
    any code is proposed. In final-acceptance.md, at the client-judgment gate, add the rule: never
    override an explicit client instruction on design judgment — when the client has directed a
    design choice, the agent does not silently overrule it (this closes the "agent decided to suppress
    built-in UI against implied client intent" seam). Then add failing-first assertions to
    build-chunk.test.ts under a new `describe('SKILLDEF-02 — game/library boundary')`: build.md
    `toContain('## Boundaries')`, `node_modules/boardsmith`, a read-only marker, a FILE-not-patch
    marker, and a never-suppress-built-in-UI marker; investigate.md `toContain` the boundary pointer;
    final-acceptance.md `toContain` the never-override-client-instruction marker. Use named constants.
  </action>
  <acceptance_criteria>
    - `grep -F '## Boundaries' src/cli/slash-command/bs/build/build.md` matches
    - `grep -F 'node_modules/boardsmith' src/cli/slash-command/bs/build/build.md` matches, with "read-only" and "symlink" in the same section (grep both tokens)
    - build.md "## Boundaries" contains a FILE-not-patch rule (grep for `file` + `never patch`/`not patch`) and a never-suppress-built-in-UI rule (grep for `suppress` + `built-in`)
    - `grep -iF 'boundaries' src/cli/slash-command/bs/build/investigate.md` OR a `node_modules/boardsmith` pointer matches in the Required Reading region
    - final-acceptance.md contains a never-override-explicit-client-instruction rule (grep for `client` + `override`/`overrule`)
    - build-chunk.test.ts SKILLDEF-02 assertions FAIL on pre-edit source, PASS after (confirm red-first)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts</automated>
  </verify>
  <done>build-chunk.test.ts green; build.md carries the four-rule "## Boundaries" section; investigate.md points at it; final-acceptance.md forbids overriding an explicit client instruction.</done>
</task>

<task type="auto">
  <name>Task 2: Fenced escape-hatch "don't" in build.md UI section (SKILLDEF-03)</name>
  <files>src/cli/slash-command/bs/build/build.md, src/cli/slash-command/bs/build-chunk.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/build/build.md UI-writing region (~76-97, the Placeholders/AssetImage prose) where the SKILLDEF-02 "## Boundaries" section from Task 1 now also lives
    - src/engine/action/types.ts:467-469 and src/engine/element/action-metadata.ts:76 (the `suppressFromDock` per-action field)
    - src/ui/components/GameShell.vue:147 (the `platformActionPanelEscapeHatch` fenced prop — LIBX-01 rename)
    - src/cli/slash-command/bs/build-chunk.test.ts (the SKILLDEF-02 block added in Task 1 — add SKILLDEF-03 assertions in a sibling describe or same block)
  </read_first>
  <action>
    In build.md's UI-writing section (beside the boundary/placeholder prose), add the SKILLDEF-03
    "don't": never set `platformActionPanelEscapeHatch` (the LIBX-01 rename of the old
    `suppressActionPanel`) without EXPLICIT client direction — it fences off the entire built-in
    action panel and switching it off without the client is exactly the never-suppress-built-in-UI
    violation from "## Boundaries". State that `.suppressFromDock()` (the per-action metadata field)
    is the ONLY sanctioned per-action dock-hiding mechanism, used only when the game design
    legitimately calls for hiding a specific action from the dock — not as a way to suppress the
    panel wholesale. Reference the fenced hatch by its NEW name only (`platformActionPanelEscapeHatch`),
    never the retired `suppressActionPanel`. Add failing-first assertions to build-chunk.test.ts
    under `describe('SKILLDEF-03 — fenced escape hatch')`: build.md `toContain('platformActionPanelEscapeHatch')`
    with a never-without-client marker, `toContain('suppressFromDock')` as the sanctioned mechanism,
    and (guard against regression to the old name) assert build.md does NOT contain `suppressActionPanel`.
    Use named marker constants.
  </action>
  <acceptance_criteria>
    - `grep -F 'platformActionPanelEscapeHatch' src/cli/slash-command/bs/build/build.md` matches, with a never-without-client instruction in the same region (grep `client`)
    - `grep -F 'suppressFromDock' src/cli/slash-command/bs/build/build.md` matches, named as the only sanctioned per-action dock-hiding mechanism
    - `grep -c 'suppressActionPanel' src/cli/slash-command/bs/build/build.md` returns 0 (retired name absent)
    - build-chunk.test.ts SKILLDEF-03 assertions FAIL on pre-edit source, PASS after (confirm red-first)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs</automated>
  </verify>
  <done>build-chunk.test.ts + full bs suite green; build.md forbids `platformActionPanelEscapeHatch` without the client and names `.suppressFromDock()` as the only sanctioned per-action dock-hiding mechanism; retired name `suppressActionPanel` appears nowhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (new) | Static prose edits to skill markdown + vitest drift assertions. No runtime code, no network, no untrusted input, no package installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-02 | Tampering | Editing installed `~/.claude/skills/` copy instead of repo source | mitigate | All edits pinned to `src/cli/slash-command/bs/`; drift tests read only repo copies |
| T-166-03 | Information disclosure | Referencing the retired `suppressActionPanel` name misleads future agents | mitigate | Acceptance criterion asserts `suppressActionPanel` count is 0 in build.md; only the LIBX-01 name is used |
| T-166-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; nothing to audit |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs` green.
- Full suite `npm run test` green (~3032 baseline) — no unrelated regression.
- Grep confirms zero occurrences of the retired `suppressActionPanel` name anywhere under src/cli/slash-command/bs.
</verification>

<success_criteria>
SKILLDEF-02 success criterion 2 is TRUE: the skills state both boundaries explicitly (board-only,
read-only symlinked library, never-suppress built-in UI, file gaps not patch). SKILLDEF-03 success
criterion 3 is TRUE: the skills forbid the fenced `platformActionPanelEscapeHatch` without the client
and name `.suppressFromDock()` as the sanctioned mechanism. Both regression-tested per PROC-01.
</success_criteria>

<output>
Create `.planning/phases/166-skills-defects-session-lock-ui-library-boundary/166-02-SUMMARY.md` when done.
</output>
