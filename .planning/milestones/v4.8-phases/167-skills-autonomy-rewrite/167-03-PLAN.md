---
phase: 167-skills-autonomy-rewrite
plan: 03
type: execute
wave: 3
depends_on: [167-02]
files_modified:
  - src/cli/slash-command/bs/state-machine.md
  - src/cli/slash-command/bs/build-chunk.md
  - src/cli/slash-command/bs/build/final-acceptance.md
  - src/cli/slash-command/bs/build/close.md
  - src/cli/slash-command/bs/build-chunk.test.ts
autonomous: true
requirements: [SKILLAUTO-06, SKILLAUTO-07, PROC-01]

must_haves:
  truths:
    - "The session never winds down before ≥50% context consumed (a floor), and still obeys the existing 60% harness-warning ceiling"
    - "Research, audits, large reads, and repairs are offloaded to sub-agents so the main thread fills slowly and long autonomous runs are possible"
    - "The orchestrator-never-reads-the-big-stuff rule is preserved"
    - "Finishing a GAME emits a loud, unambiguous banner + a clean summary card (what shipped, test count, deferred items)"
    - "A lighter chunk-level completion line is emitted per chunk"
  artifacts:
    - path: "src/cli/slash-command/bs/state-machine.md"
      provides: "≥50% wind-down floor beneath the existing 60% ceiling"
      contains: "50%"
    - path: "src/cli/slash-command/bs/build/final-acceptance.md"
      provides: "Game-level loud completion banner + summary card"
      contains: "shipped"
    - path: "src/cli/slash-command/bs/build/close.md"
      provides: "Chunk-level completion line"
      contains: "complete"
    - path: "src/cli/slash-command/bs/build-chunk.test.ts"
      provides: "SKILLAUTO-06 + SKILLAUTO-07 drift describe blocks"
      contains: "SKILLAUTO-06"
  key_links:
    - from: "src/cli/slash-command/bs/state-machine.md"
      to: "build-chunk.md Context-Economics Hard Rule"
      via: "50% floor + sub-agent offload citation"
      pattern: "sub-agent|subagent"
    - from: "src/cli/slash-command/bs/build/final-acceptance.md"
      to: "the game-completion banner"
      via: "loud banner + summary card emission"
      pattern: "banner"
---

<objective>
SKILLAUTO-06 (B.6 context) + SKILLAUTO-07 (B.7 loud completion).
- B.6: never wind down before ≥50% context consumed (a FLOOR) while keeping the existing 60% "obey-the-harness-warning" CEILING. The substantive lever is sub-agent OFFLOAD: research, audits, large reads, and repairs are offloaded to sub-agents so the main thread fills slowly and long autonomous runs are possible. Preserve the existing "orchestrator never reads the big stuff" rule.
- B.7: finishing a GAME emits a LOUD, unambiguous banner + a clean summary card (what shipped, test count, deferred items) — never buried in a wall of text. A lighter chunk-level completion line is also added.

Purpose: The 50% floor + sub-agent offload is what makes the Plan A/B autonomous run actually LONG (not bailing at 40% on a "feels big" hunch), and the loud completion makes the terminus unmistakable instead of buried.
Output: ≥50% floor beneath the 60% ceiling + sub-agent offload prose in state-machine.md/build-chunk.md, a game-level banner + summary card in final-acceptance.md, a chunk-level completion line in close.md, and fail-pre/pass-post drift tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/167-skills-autonomy-rewrite/167-CONTEXT.md
@.planning/phases/167-skills-autonomy-rewrite/167-VALIDATION.md
@.planning/phases/167-skills-autonomy-rewrite/167-02-SUMMARY.md

<interfaces>
Drift-test convention: see 167-01-PLAN.md `<interfaces>`.

Edit-target anchors (167-CONTEXT.md `<decisions>` SKILLAUTO-06/07):
- state-machine.md:220-233 (Context-low escape hatch — the existing 60% low-water mark; add the ≥50% floor beneath it)
- build-chunk.md:19-36 (Context-Economics Hard Rule — orchestrator never reads the big stuff; PRESERVE), 276-278 (context-low continuation), 358-372 (Session Handoff Seams context framing)
- build/final-acceptance.md:142-168 (Sub-Step Resumability tail — NEW game-level banner + summary card goes here, at/after the game's terminal step)
- build/close.md:106-142 (Propose the Next Chunk / Downstream Shape — NEW lighter chunk-level completion line goes here)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ≥50% wind-down floor + sub-agent offload (state-machine.md, build-chunk.md) (+ drift tests)</name>
  <read_first>
    - src/cli/slash-command/bs/state-machine.md (Context-low escape hatch 221-233 — the existing 60% rule)
    - src/cli/slash-command/bs/build-chunk.md (Context-Economics Hard Rule 19-36; context-low continuation 276-278; Session Handoff Seams 358-372)
    - src/cli/slash-command/bs/build-chunk.test.ts (BUILD-13 handoff describe 611; marker constants at top)
    - .planning/phases/167-skills-autonomy-rewrite/167-CONTEXT.md `<decisions>` SKILLAUTO-06 (≥50% floor; keep 60% ceiling; sub-agent offload; preserve orchestrator-never-reads)
  </read_first>
  <behavior>
    - Test (build-chunk.test.ts, new describe "SKILLAUTO-06 — context floor + offload"):
      · state-machine.md states a ≥50% wind-down FLOOR — the session never winds down before at least 50% context consumed — beneath the existing 60% harness-warning ceiling (both numbers present).
      · state-machine.md / build-chunk.md state research, audits, large reads, and repairs are offloaded to sub-agents so the main thread fills slowly and long runs are possible.
      · build-chunk.md's Context-Economics Hard Rule "orchestrator never reads the big stuff" is preserved (grep still passes).
    All fail pre-edit for the floor + offload assertions.
  </behavior>
  <action>
    In state-machine.md's "Context-low escape hatch" (221-233), add an explicit ≥50% wind-down FLOOR beneath the existing 60% low-water ceiling: the session never winds down before at least 50% of the context window is consumed — stopping earlier (e.g. at 40% because the work "feels big") is exactly the premature bail this floor forbids — while the 60% ceiling (obey-the-harness-warning) is unchanged. State that the substantive lever for staying under the ceiling is sub-agent OFFLOAD: research, audits, large reads, and repairs are dispatched to sub-agents so the main thread fills slowly and long autonomous runs are possible. Do NOT weaken the 60% number.

    In build-chunk.md, reinforce the sub-agent offload at the context-low continuation (276-278) and Session Handoff Seams (358-372) by citing the ≥50% floor and naming which heavy work classes (research/audits/large reads/repairs) go to sub-agents. Explicitly preserve — do not touch the substance of — the Context-Economics Hard Rule (19-36) "orchestrator never reads rulebook slices/docs/generated code itself"; cite it as the mechanism the offload rides on.

    Author the SKILLAUTO-06 drift describe (append). Add marker constants for "50%" and the offload phrasing. Fail-pre, then pass-post.
  </action>
  <acceptance_criteria>
    - state-machine.md contains "50%" (floor) AND "60%" (ceiling) with the floor framed as never-wind-down-before.
    - state-machine.md / build-chunk.md name sub-agent offload of research/audits/large reads/repairs.
    - build-chunk.md still contains the "orchestrator never reads" Context-Economics Hard Rule text (preserved).
    - build-chunk.test.ts has a new `describe('SKILLAUTO-06` block.
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts</automated>
  </verify>
  <done>≥50% floor added beneath the preserved 60% ceiling; sub-agent offload of heavy work classes codified; orchestrator-never-reads preserved; SKILLAUTO-06 drift block passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Loud game-level completion banner + summary card + chunk-level line (final-acceptance.md, close.md) (+ drift tests)</name>
  <read_first>
    - src/cli/slash-command/bs/build/final-acceptance.md (Findings Destination 130-138; Sub-Step Resumability tail 140-175; Downstream Shape 172+)
    - src/cli/slash-command/bs/build/close.md (Propose the Next Chunk 105-119; Downstream Shape 133-142)
    - src/cli/slash-command/bs/build-chunk.test.ts (UIQ-05 final-acceptance describes 664/707/781; BUILD-11 close describe 586)
    - .planning/phases/167-skills-autonomy-rewrite/167-CONTEXT.md `<decisions>` SKILLAUTO-07 (game-level banner + summary card: shipped/test-count/deferred; lighter chunk-level line)
  </read_first>
  <behavior>
    - Test (build-chunk.test.ts, new describe "SKILLAUTO-07 — loud completion"):
      · build/final-acceptance.md emits a loud, unambiguous game-completion banner plus a summary card naming what SHIPPED, the TEST COUNT, and DEFERRED items — an unmistakable terminus, not buried in a wall of text.
      · build/close.md emits a lighter chunk-level completion line at each chunk close.
    Both fail pre-edit.
  </behavior>
  <action>
    In build/final-acceptance.md (at/after the game's terminal step, near the Sub-Step Resumability / Downstream Shape tail 140-175), add a game-level LOUD completion output: a clearly-delimited banner marking the game as finished plus a clean summary card with three named fields — what SHIPPED (the chunks/behaviors), the TEST COUNT (accumulated suite total), and DEFERRED items (variants/ideas-backlog/waived). Instruct that it is emitted as a distinct, visually-loud block, never buried in narration. Field layout/wording is at your discretion consistent with the skill voice.

    In build/close.md (Propose the Next Chunk / Downstream Shape 105-142), add a lighter chunk-level completion line emitted at each chunk close (one-line "chunk <slug> complete — <status>" style), distinct from the game-level banner. Keep it consistent with the auto-advance framing from Plan B (the chunk-level line does not itself stop the session).

    Author the SKILLAUTO-07 drift describe (append). Add marker constants for the banner + the three summary-card fields (shipped / test count / deferred) + the chunk-level line. Fail-pre, then pass-post. Run the full bs suite.
  </action>
  <acceptance_criteria>
    - build/final-acceptance.md contains a game-completion banner marker AND a summary card naming shipped + test count + deferred.
    - build/close.md contains a chunk-level completion line.
    - build-chunk.test.ts has a new `describe('SKILLAUTO-07` block.
    - `npx vitest run src/cli/slash-command/bs` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs</automated>
  </verify>
  <done>Game-level loud banner + summary card (shipped/test-count/deferred) added; chunk-level completion line added; SKILLAUTO-07 drift block passes; full bs suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Prose/spec authority edits only — no code, network, external input, or package installs.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-03 | Tampering | context-floor/ceiling numbers + completion-banner prose | mitigate | Drift assertions pin both the 50% floor and the preserved 60% ceiling, plus the banner + three summary-card fields |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs` green.
- ≥50% floor present beneath the preserved 60% ceiling; sub-agent offload codified; orchestrator-never-reads preserved; game-level banner + summary card + chunk-level line present.
</verification>

<success_criteria>
Success Criterion 4 (ROADMAP) met: the context threshold is ≥50% before winding down with heavy work offloaded to sub-agents; completion is a loud, unambiguous banner + summary card.
</success_criteria>

<output>
Create `.planning/phases/167-skills-autonomy-rewrite/167-03-SUMMARY.md` when done.
</output>
