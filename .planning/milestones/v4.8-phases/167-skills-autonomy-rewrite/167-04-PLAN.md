---
phase: 167-skills-autonomy-rewrite
plan: 04
type: execute
wave: 4
depends_on: [167-03]
files_modified:
  - src/cli/slash-command/bs/build/close.md
  - src/cli/slash-command/bs/build/build.md
  - src/cli/slash-command/bs/check-status.md
  - src/cli/slash-command/bs/state-machine.md
  - src/cli/slash-command/bs/build/test.md
  - src/cli/slash-command/bs/build/playtest.md
  - src/cli/slash-command/bs/build-chunk.test.ts
  - src/cli/slash-command/bs/status-tools.test.ts
autonomous: true
requirements: [SKILLAUTO-08, PROC-01]

must_haves:
  truths:
    - "At close, the filings/library-gap ledger + asset-debt + waived-chunk ledgers are reconciled against what the chunk changed — the paperwork is audited, not just the code"
    - "When a fix lands, the relevant filing/ruling is re-touched at close (RULINGS re-touch strengthened)"
    - "A sim/test that stops exercising this chunk's new actions FAILS LOUD — it does not pass quietly"
  artifacts:
    - path: "src/cli/slash-command/bs/build/close.md"
      provides: "Close-time ledger reconciliation step in the Bookkeeping Sequence"
      contains: "reconcil"
    - path: "src/cli/slash-command/bs/state-machine.md"
      provides: "Strengthened close-time RULINGS/filing re-touch on fix"
      contains: "re-touch"
    - path: "src/cli/slash-command/bs/build/test.md"
      provides: "Fail-loud assertion that the sim exercised this chunk's new actions"
      contains: "exercised"
    - path: "src/cli/slash-command/bs/build-chunk.test.ts"
      provides: "SKILLAUTO-08 drift describe block"
      contains: "SKILLAUTO-08"
  key_links:
    - from: "src/cli/slash-command/bs/build/close.md"
      to: "check-status.md ledgers + build/build.md filings"
      via: "close-time reconciliation reads the surfaced ledgers"
      pattern: "ledger"
    - from: "src/cli/slash-command/bs/build/test.md"
      to: "the chunk's new actions"
      via: "assert the sim exercised this chunk's target actions"
      pattern: "exercised"
---

<objective>
SKILLAUTO-08 (B.9): close the three recurring process gaps.
- B.9a close-time ledger reconciliation: at `close`, reconcile the filings/library-gap ledger + asset-debt + waived-chunk ledgers against what the chunk actually changed ("audit the paperwork, not just the code"); re-touch the relevant filing when a fix lands.
- B.9b shared ruling store: RULINGS.md is already a shared cross-session per-game store — STRENGTHEN it so close-time reconciliation re-touches relevant rulings/filings when a fix lands.
- B.9c fail-loud sims: add an assertion that the sim/test actually EXERCISED this chunk's new actions — a sim that stops exercising its target FAILS LOUD, not passes quietly.

Purpose: These three gaps are the "green but wrong" traps the battery post-mortem surfaced — paperwork drifting from code, rulings not re-touched on a fix, and a sim that silently stopped covering its target. Closing them keeps the autonomous run HONEST.
Output: a reconciliation step in close.md's Bookkeeping Sequence sourcing the check-status.md/build.md ledgers, a strengthened RULINGS re-touch in state-machine.md, a fail-loud "sim exercised this chunk" assertion in test.md + a playtest freshness guard, and fail-pre/pass-post drift tests across build-chunk.test.ts + status-tools.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/167-skills-autonomy-rewrite/167-CONTEXT.md
@.planning/phases/167-skills-autonomy-rewrite/167-VALIDATION.md
@.planning/phases/167-skills-autonomy-rewrite/167-03-SUMMARY.md

<interfaces>
Drift-test convention: see 167-01-PLAN.md `<interfaces>`. status-tools.test.ts uses the same read()/marker/describe pattern (reads check-status.md / insert-chunk.md).

Edit-target anchors (167-CONTEXT.md `<decisions>` SKILLAUTO-08):
- build/close.md:19-66 (Bookkeeping Sequence — ADD a reconciliation step; today it is 4 items: commit-hash, decision rollup, lock release)
- build/build.md:54-61 (the library-gap FILE-don't-patch filings — the filings ledger source)
- check-status.md:71-82 (waived-verifications scan + outstanding-asset-debts ledger surfacing — the ledgers reconciliation reads)
- state-machine.md:103-105 (Rulings Outrank Rulebook — STRENGTHEN close-time re-touch here)
- build/test.md:46-72 (random-sim playthrough — ADD the "sim exercised this chunk's new actions" assertion)
- build/playtest.md:59-70 (Build-Stamp Freshness — the freshness guard)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close-time ledger reconciliation + strengthened RULINGS/filing re-touch (close.md, build.md, check-status.md, state-machine.md) (+ drift tests)</name>
  <read_first>
    - src/cli/slash-command/bs/build/close.md (Bookkeeping Sequence 18-67 — the 4 numbered items)
    - src/cli/slash-command/bs/build/build.md (Boundaries — library-gap FILE-don't-patch filings 41-61)
    - src/cli/slash-command/bs/check-status.md (waived-verifications 71-77, outstanding-asset-debts ledger 79-82)
    - src/cli/slash-command/bs/state-machine.md (Rulings Outrank Rulebook 103-105)
    - src/cli/slash-command/bs/build-chunk.test.ts (BUILD-11 close describe 586); src/cli/slash-command/bs/status-tools.test.ts (ledger-surfacing describes; marker convention)
    - .planning/phases/167-skills-autonomy-rewrite/167-CONTEXT.md `<decisions>` SKILLAUTO-08 (a) + (b)
  </read_first>
  <behavior>
    - Test (build-chunk.test.ts, new describe "SKILLAUTO-08 — close-time reconciliation + RULINGS re-touch"):
      · build/close.md's Bookkeeping Sequence contains a reconciliation step that audits the filings/library-gap ledger + asset-debt + waived-chunk ledgers against what the chunk changed, and re-touches the relevant filing/ruling when a fix lands.
      · state-machine.md's "Rulings Outrank Rulebook" is strengthened so close-time reconciliation re-touches relevant rulings/filings on a fix.
    - Test (status-tools.test.ts, new describe "SKILLAUTO-08 — ledgers surfaced for close reconciliation"):
      · check-status.md surfaces the waived-chunk + asset-debt ledgers that close reconciliation consumes (the source-of-truth the reconciliation reads).
    All fail pre-edit.
  </behavior>
  <action>
    In build/close.md's Bookkeeping Sequence (18-67), ADD a reconciliation step (keep the sequence append-only end-to-end; the lock release stays the TERMINAL write — insert the new step BEFORE the release, renumbering the release as the final item). The step reconciles, against what this chunk actually changed: (1) the filings / library-gap ledger (cite build/build.md's FILE-don't-patch filings), (2) the asset-debt ledger, and (3) the waived-chunk ledger — "audit the paperwork, not just the code." When a fix this chunk landed closes or advances a filing, re-touch that filing (mark it resolved/updated); do not leave the paperwork stale. Cite check-status.md's ledger-surfacing sections by name as the ledgers being reconciled (citation-not-restatement).

    In build/build.md, ensure the library-gap filings (54-61) are named as a reconcilable ledger close can re-touch (light cross-reference; do not restate the FILE-don't-patch rule — Plan E asserts that text survives verbatim).

    In state-machine.md's "Rulings Outrank Rulebook" (103-105), STRENGTHEN the shared-ruling-store note so close-time reconciliation re-touches relevant rulings/filings when a fix lands (RULINGS.md remains the cross-session per-game authority; the strengthening is the close-time re-touch obligation).

    In check-status.md, ensure the waived-verifications (71-77) + outstanding-asset-debts (79-82) sections are explicitly framed as the ledgers close reconciliation consumes (a forward-reference/cross-reference is enough).

    Author the two drift describes (append; build-chunk.test.ts + status-tools.test.ts). Add marker constants for "reconcil"/"re-touch"/ledger names. Fail-pre, then pass-post.
  </action>
  <acceptance_criteria>
    - build/close.md's Bookkeeping Sequence contains a reconciliation step (grep "reconcil") that names the filings/library-gap + asset-debt + waived-chunk ledgers AND re-touches a filing on a fix; the lock release is still the terminal write.
    - state-machine.md "Rulings Outrank Rulebook" contains a strengthened close-time re-touch (grep "re-touch"/"retouch").
    - check-status.md frames its waived + asset-debt ledgers as consumed by close reconciliation.
    - build-chunk.test.ts has a new `describe('SKILLAUTO-08` block; status-tools.test.ts has a new `describe('SKILLAUTO-08` block.
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts src/cli/slash-command/bs/status-tools.test.ts` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts src/cli/slash-command/bs/status-tools.test.ts</automated>
  </verify>
  <done>Close-time ledger reconciliation added (release still terminal); RULINGS/filing re-touch strengthened; ledgers surfaced for reconciliation; both SKILLAUTO-08 drift blocks pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail-loud sims — assert the sim exercised this chunk's new actions (test.md, playtest.md) (+ drift tests)</name>
  <read_first>
    - src/cli/slash-command/bs/build/test.md (random-sim playthrough item 5, lines 46-72; the four SimulationResults zero-assertions)
    - src/cli/slash-command/bs/build/playtest.md (Build-Stamp Freshness 59-70 — the freshness guard)
    - src/cli/slash-command/bs/build-chunk.test.ts (BUILD-06 test describe 359; the SKILLAUTO-08 describe from Task 1)
    - .planning/phases/167-skills-autonomy-rewrite/167-CONTEXT.md `<decisions>` SKILLAUTO-08 (c)
  </read_first>
  <behavior>
    - Test (build-chunk.test.ts, extend the SKILLAUTO-08 describe or add "SKILLAUTO-08 — fail-loud sims"):
      · build/test.md's random-sim step requires an assertion that the sim ACTUALLY EXERCISED this chunk's new actions — a sim that stops exercising its target FAILS LOUD (the assertion fails), rather than passing quietly on the four zero-checks alone.
      · build/playtest.md's freshness guard is reinforced so a stale/non-exercising run is caught, not silently accepted.
    Fails pre-edit (today only crashed/stuck/timedOut/exceededMaxActions are asserted).
  </behavior>
  <action>
    In build/test.md's random-sim playthrough (item 5, 46-72), add a required assertion, alongside the existing four `results.* === 0` checks, that the sim actually EXERCISED this chunk's new actions — e.g. assert that the run's move/action coverage includes each new action this chunk introduced, so a sim that (through a rules or wiring change) stops reaching its target action FAILS LOUD instead of passing quietly. Frame it as a hard gate, not advice; name that "passing the four zero-checks while never exercising the chunk's target action is a silent-coverage failure this assertion exists to catch." Use the real simulation surface (do not invent a fake coverage API — if the exact field/helper is uncertain, describe the assertion in terms the executor verifies against `src/testing/random-simulation.ts`'s SimulationResults shape).

    In build/playtest.md's Build-Stamp Freshness (59-70), reinforce that a stale run (HMR/stale-module or a script that no longer drives the chunk's new action) is caught — the freshness guard plus the test.md exercised-assertion together prevent a quietly-passing non-exercising run.

    Extend the SKILLAUTO-08 drift block (or add a sibling describe) asserting the exercised-this-chunk fail-loud requirement in test.md and the reinforced freshness guard in playtest.md. Add marker constants. Fail-pre, then pass-post. Run the full bs suite.
  </action>
  <acceptance_criteria>
    - build/test.md contains an "exercised this chunk's new actions" fail-loud assertion requirement (grep "exercised") alongside the four zero-checks.
    - build/playtest.md's freshness guard reinforced against a non-exercising/stale run.
    - build-chunk.test.ts asserts both (extends/sibling of the SKILLAUTO-08 describe).
    - `npx vitest run src/cli/slash-command/bs` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs</automated>
  </verify>
  <done>Fail-loud "sim exercised this chunk's new actions" assertion added to test.md; playtest freshness guard reinforced; SKILLAUTO-08 fail-loud drift assertions pass; full bs suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Prose/spec authority edits only — no code, network, external input, or package installs. The B.9 gaps ARE the "green but wrong" tampering-analog risks (stale paperwork, quietly-passing sims).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-04 | Tampering | ledger/paperwork drift + silent-coverage sim | mitigate | Close-time reconciliation + fail-loud exercised-assertion close the drift; drift tests pin both; lock release remains terminal write |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs` green.
- Close-time reconciliation of filings/asset-debt/waived ledgers added; RULINGS/filing re-touch strengthened; fail-loud "sim exercised this chunk" assertion added; playtest freshness guard reinforced.
</verification>

<success_criteria>
Success Criterion 5 (ROADMAP) met: the three B.9 process gaps are closed — close-time ledger reconciliation, a strengthened shared cross-session ruling authority store, and fail-loud tests/sims.
</success_criteria>

<output>
Create `.planning/phases/167-skills-autonomy-rewrite/167-04-SUMMARY.md` when done.
</output>
