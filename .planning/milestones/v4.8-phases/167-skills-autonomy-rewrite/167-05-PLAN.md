---
phase: 167-skills-autonomy-rewrite
plan: 05
type: execute
wave: 5
depends_on: [167-04]
files_modified:
  - src/cli/slash-command/bs/state-machine.md
  - src/cli/slash-command/bs/build-chunk.md
  - src/cli/slash-command/bs/build-chunk.test.ts
autonomous: true
requirements: [PROC-02, PROC-01]

must_haves:
  truths:
    - "The top-level autonomy prose states explicitly that autonomy = how-to-build, never what-the-rules-are; genuine rule ambiguity is surfaced (batched) never fabricated"
    - "Every Part D discipline rule TEXT still survives after all prior 167 edits: escalate-don't-hack/file-don't-workaround, reuse-not-rebuild, honest-derived labeling, surface-don't-fabricate, in-process redteam, build-literally"
    - "The full bs drift suite is green at the phase gate"
  artifacts:
    - path: "src/cli/slash-command/bs/state-machine.md"
      provides: "Explicit autonomy=how-not-what statement woven into the top-level autonomy prose"
      contains: "how"
    - path: "src/cli/slash-command/bs/build-chunk.test.ts"
      provides: "PROC-02 'Part D survives' regression describe block"
      contains: "PROC-02"
  key_links:
    - from: "src/cli/slash-command/bs/build-chunk.test.ts"
      to: "every Part D anchor file (build.md, test.md, redteam.md, playtest.md, state-machine.md)"
      via: "regression assertions that each Part D rule text still present"
      pattern: "Part D"
---

<objective>
PROC-02: preserve every Part D provenance discipline through the autonomy rewrite. Autonomy applies to HOW to build, never to WHAT the rules are; genuine rule ambiguity is still surfaced (batched, per Plan B), never fabricated. This plan (a) weaves an explicit "autonomy = how-not-what; ambiguity surfaced never fabricated" statement into the top-level autonomy prose, and (b) adds a "Part D survives" regression drift-test block asserting each Part D rule's TEXT is still present AFTER all of Plans 01-04's edits.

This is the final verification plan — it runs LAST (wave 5) so its regression net can assert Part D survived every prior edit to state-machine.md / build-chunk.md / build/*.md.

Purpose: The autonomy rewrite's one real hazard (Part B "Caution" note) is eroding a provenance discipline while relaxing gates. This regression block is the standing net against that erosion.
Output: an explicit autonomy=how-not-what statement + a PROC-02 "Part D survives" regression describe covering all six Part D disciplines, and a green full-suite phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/167-skills-autonomy-rewrite/167-CONTEXT.md
@.planning/phases/167-skills-autonomy-rewrite/167-VALIDATION.md
@.planning/phases/167-skills-autonomy-rewrite/167-04-SUMMARY.md

<interfaces>
Drift-test convention: see 167-01-PLAN.md `<interfaces>`.

Part D anchors to assert survive (167-CONTEXT.md `<decisions>` PROC-02):
- escalate-don't-hack / file-don't-workaround: build/build.md:40-61 (Boundaries — the 4 file-don't-patch rules), 122-130 (never fence the whole panel without the client)
- reuse-not-rebuild: build/test.md:71-72 ("do not reimplement a hand-rolled random-play loop"), build/build.md:28-38 (Extends Never Restructures)
- honest-derived labeling: state-machine.md:74-98 (Cold-Resume Parse Contract / Consistency Check), :15 (`verified (user-waived)` recorded honestly)
- surface-don't-fabricate: state-machine.md:80-82 (parse failure → STOP and ask, never guess), build/build.md:23-26 (interpretation layered on raw slice, never a replacement), :54-61 (file the gap)
- in-process redteam: state-machine.md:150-154 (Redteam Escalation), build/redteam.md (the adversarial-subagent shape)
- build-literally: build/build.md:10-26 (Fresh-Context Exception — read the raw slice), build/playtest.md:32-52 (numbered click-by-click outcome-based script)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Weave the explicit "autonomy = how-not-what; surface never fabricate" statement into the top-level autonomy prose (+ drift test)</name>
  <read_first>
    - src/cli/slash-command/bs/state-machine.md (Session Handoff Seams 156-235 — the top-level autonomy/continuous-run prose the statement is woven into; Redteam Escalation 150-154; batched-queue model added by Plan B)
    - src/cli/slash-command/bs/build-chunk.md (header 1-18; Session Handoff Seams 358-372)
    - src/cli/slash-command/bs/build-chunk.test.ts (marker convention; the SKILLAUTO describes added by Plans 01-04)
    - .planning/phases/167-skills-autonomy-rewrite/167-CONTEXT.md `<decisions>` PROC-02
  </read_first>
  <behavior>
    - Test (build-chunk.test.ts, new describe "PROC-02 — autonomy is how-not-what"):
      · state-machine.md (and/or build-chunk.md's autonomy framing) states explicitly that autonomy applies to HOW to build, never to WHAT the rules are, and that genuine rule ambiguity is surfaced (batched) never fabricated.
    Fails pre-edit (no such explicit statement today).
  </behavior>
  <action>
    Weave an explicit statement into the top-level autonomy prose (state-machine.md Session Handoff Seams region, and mirror a one-line cross-reference in build-chunk.md's header/Session Handoff Seams): autonomy governs HOW the game is built, never WHAT the rules ARE; genuine rule ambiguity is SURFACED (batched — cite Plan B's batched-question queue) and NEVER fabricated. Tie it to the Redteam Escalation (150-154) and the ask triple-gate so the "surface, don't fabricate" boundary is unmistakable at the top level, not just buried per-step.

    Author the PROC-02 "autonomy is how-not-what" drift describe (append). Add marker constants. Fail-pre, then pass-post.
  </action>
  <acceptance_criteria>
    - state-machine.md contains an explicit "autonomy = how to build, never what the rules are" statement AND "surfaced ... never fabricated" phrasing.
    - build-chunk.md carries a mirrored cross-reference to that statement.
    - build-chunk.test.ts has a new `describe('PROC-02 — autonomy is how-not-what'` block.
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts</automated>
  </verify>
  <done>Explicit autonomy=how-not-what / surface-never-fabricate statement woven into top-level prose + mirrored in build-chunk.md; PROC-02 statement drift block passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PROC-02 "Part D survives" regression describe covering all six disciplines (+ phase-gate full suite)</name>
  <read_first>
    - src/cli/slash-command/bs/build/build.md (Boundaries 41-61, never-fence-panel 122-130, Extends-Never-Restructures 28-38, Fresh-Context Exception 10-26)
    - src/cli/slash-command/bs/build/test.md (reuse-not-rebuild "do not reimplement a hand-rolled random-play loop" 71-72)
    - src/cli/slash-command/bs/build/redteam.md (adversarial-subagent shape) + state-machine.md Redteam Escalation 150-154
    - src/cli/slash-command/bs/build/playtest.md (numbered outcome-based script 32-52)
    - src/cli/slash-command/bs/state-machine.md (Cold-Resume Parse Contract / STOP-and-ask 74-98,80-82; `verified (user-waived)` 15)
    - src/cli/slash-command/bs/build-chunk.test.ts (existing describes — the new PROC-02 block appends after them)
    - .planning/phases/167-skills-autonomy-rewrite/167-CONTEXT.md `<decisions>` PROC-02 (the six anchor disciplines)
  </read_first>
  <behavior>
    - Test (build-chunk.test.ts, new describe "PROC-02 — Part D survives the autonomy rewrite"): one `it()` per Part D discipline, each asserting the anchor rule's TEXT is still present after Plans 01-04's edits:
      · escalate-don't-hack / file-don't-workaround — build/build.md Boundaries (file the gap, never patch node_modules/boardsmith) + never-fence-panel-without-client.
      · reuse-not-rebuild — build/test.md "do not reimplement a hand-rolled random-play loop" + build/build.md Extends-Never-Restructures.
      · honest-derived labeling — state-machine.md `verified (user-waived)` recorded honestly + the parse-contract.
      · surface-don't-fabricate — state-machine.md parse-failure STOP-and-ask (never guess) + build/build.md file-the-gap.
      · in-process redteam — state-machine.md Redteam Escalation + build/redteam.md adversarial-subagent shape.
      · build-literally — build/build.md Fresh-Context Exception (read the raw slice) + build/playtest.md numbered outcome-based script.
    These assertions should PASS immediately if Plans 01-04 preserved Part D (they are a regression net, not a fail-pre/pass-post pair). If ANY fails, a prior plan eroded a Part D rule — the executor investigates and restores the eroded text (this is the plan's real job: catch erosion).
  </behavior>
  <action>
    Author the PROC-02 "Part D survives the autonomy rewrite" regression describe in build-chunk.test.ts (append after all prior describes). Declare marker constants quoting the exact surviving TEXT of each Part D anchor (reuse existing constants where present — e.g. NODE_MODULES_BOARDSMITH, ESCAPE_HATCH_PROP, STATUS_ENUM_VALUES already exist). One `it()` per discipline reading the relevant file(s) and asserting `toContain` the anchor text.

    Run the block. If every assertion passes, Part D survived — done. If any assertion fails, a Plan 01-04 edit eroded that Part D rule: investigate the offending file, restore the eroded rule text (extend-don't-restructure — reconcile the autonomy edit with the preserved discipline), and re-run until green. Do NOT weaken the assertion to make it pass — the assertion is the source of truth for what must survive.

    Finally run the FULL bs suite as the phase gate; confirm green (~3053 baseline plus this phase's added assertions).
  </action>
  <acceptance_criteria>
    - build-chunk.test.ts has a new `describe('PROC-02 — Part D survives` block with one `it()` per the six disciplines.
    - Each Part D anchor text is asserted present (grep the marker constants in the new block).
    - `npx vitest run src/cli/slash-command/bs` green (full bs drift suite).
    - `npm run test` green (full-suite phase gate; no unrelated regressions).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs && npm run test</automated>
  </verify>
  <done>PROC-02 "Part D survives" regression block covers all six disciplines and passes; any erosion found is restored; full bs suite + full project suite green at the phase gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Prose/spec authority edits + a regression test block. No code, network, external input, or package installs. This plan IS the Tampering mitigation for the whole phase — the standing net against provenance-discipline erosion.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-05 | Tampering | erosion of any Part D discipline by the autonomy rewrite | mitigate | The PROC-02 "Part D survives" regression describe asserts each of the six disciplines' anchor text survives all prior 167 edits; failure blocks the phase gate until restored |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs` green.
- `npm run test` green (phase gate, full suite).
- Explicit autonomy=how-not-what statement present; all six Part D disciplines asserted present after Plans 01-04.
</verification>

<success_criteria>
Success Criterion 6 (ROADMAP) met: the rewrite preserves every Part D discipline (escalate-don't-hack, reuse-not-rebuild, honest-derived labeling, surface-don't-fabricate, in-process redteam, build-literally); autonomy applies to how-to-build never what-the-rules-are; genuine rule ambiguity is surfaced (batched) never fabricated. Phase gate: full suite green.
</success_criteria>

<output>
Create `.planning/phases/167-skills-autonomy-rewrite/167-05-SUMMARY.md` when done.
</output>
