---
phase: 168-platform-feature-spike-seed-to-state
plan: 02
type: execute
wave: 2
depends_on: [168-01]
files_modified:
  - src/session/stateless-ops.ts
  - src/cli/dev-host/multiplayer-host.ts
  - src/cli/commands/dev.ts
  - src/session/seed-to-state.test.ts
autonomous: true
requirements: [FEAT-01, PROC-01]
must_haves:
  truths:
    - "A game started with a seed reproduces the exact recorded state — same element tree, flowState, and current player — instead of a fresh start"
    - "The seeded game can take the next legal action from the recorded state"
    - "Loading the same seed twice yields byte-identical state (RNG randomState pinned)"
    - "Without the seed wiring the started state differs from the recorded state (the test fails on pre-wiring behavior)"
  artifacts:
    - path: "src/session/stateless-ops.ts"
      provides: "handleStart seed branch — returns the seed snapshot's state envelope instead of a fresh start when a seed is threaded"
      contains: "runnerFromSnapshot"
    - path: "src/cli/commands/dev.ts"
      provides: "--seed <file> CLI flag that reads a GameStateSnapshot JSON and threads it into the dev host"
      contains: "seed"
    - path: "src/session/seed-to-state.test.ts"
      provides: "record -> seed -> load -> assert deterministic-load integration test (fail-without / pass-with + load-twice-identical)"
  key_links:
    - from: "src/cli/commands/dev.ts"
      to: "src/cli/dev-host/multiplayer-host.ts"
      via: "--seed file parsed to a seed snapshot passed through MultiplayerHostOptions into hostOptions"
      pattern: "seed"
    - from: "src/cli/dev-host/multiplayer-host.ts"
      to: "src/session/stateless-ops.ts"
      via: "hostOptions.seedSnapshot -> handleStart runnerFromSnapshot branch"
      pattern: "seedSnapshot"
    - from: "src/testing/test-game.ts"
      to: "src/session/seed-to-state.test.ts"
      via: "TestGame.getSnapshot() recorded as the seed fixture the test loads"
      pattern: "getSnapshot"
---

<objective>
Build the thin PoC that proves deterministic seed-to-state: a `--seed <file>` flag on `boardsmith dev` that seeds the host's initial snapshot from a recorded GameStateSnapshot instead of `handleStart`'s fresh start, plus a record→seed→load→assert integration test that fails without the wiring and passes with it.

Purpose: satisfy FEAT-01 success criterion 2 (a thin PoC loads a game into a declared target state deterministically) and PROC-01 (the fix ships a test that fails on pre-wiring behavior and passes after). Compose the EXISTING load path (GameRunner.fromSnapshot via handleStart) — do NOT rebuild any load machinery.

Output: the `--seed` wiring across stateless-ops/multiplayer-host/dev.ts + `src/session/seed-to-state.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/168-platform-feature-spike-seed-to-state/168-CONTEXT.md
@.planning/phases/168-platform-feature-spike-seed-to-state/168-VALIDATION.md
@docs/seed-to-state.md

<read_first>
- src/session/stateless-ops.ts — `handleStart` (~291) is the SEED PLUG-IN POINT: today it does `new GameRunner(...).start()` then returns `stateEnvelope(runner, playerCount)`. `executeOp` (~1077) dispatches `op.type === 'start'` to `handleStart` (~1096) and already receives a `hostOptions` param (~1083, currently `{ teachingDisabled? }`). `stateEnvelope` (~233) and `runnerFromSnapshot(snapshot, def)` (used at ~321 for every non-start op) are the reuse primitives — restoring the seed is exactly `stateEnvelope(runnerFromSnapshot(seedSnapshot, def), playerCount)`.
- src/cli/dev-host/multiplayer-host.ts — `startGame` (~646) builds `hostOptions = { teachingDisabled: ... }` (~698) and the `executeOp` closure (~699-700) that passes `hostOptions` on the `start` op. `MultiplayerHostOptions` (interface earlier in file) is where a new optional `seedSnapshot` rides in.
- src/cli/commands/dev.ts — `DevOptions` interface (~217 area) + `devCommand` (~555) + `new MultiplayerHost(...)` construction (this file). The `--seed <file>` commander option is registered wherever the `dev` command options are declared, parsed here, and passed into MultiplayerHost.
- src/session/snapshot-session-host.ts — `SnapshotSessionHost.start` (~319) issues `{ type: 'start' }` and stores `res.snapshot` as the opaque authoritative state; no change needed if the seed rides through `hostOptions` into `handleStart`.
- src/engine/utils/snapshot.ts — `GameStateSnapshot` (~12): the seed file's JSON shape.
- src/testing/test-game.ts — `TestGame.create` + `TestGame.doAction` + `TestGame.getSnapshot` (~423): used by the test to record a target state.
- src/testing/test-game.test.ts — reference in-repo `Game` subclasses driven via `TestGame.create(...)` (e.g. FixtureGame ~50, SeedFixtureGame ~516): pattern for the small in-repo game the PoC test drives.
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread a seed snapshot through handleStart, MultiplayerHost, and a --seed CLI flag</name>
  <files>src/session/stateless-ops.ts, src/cli/dev-host/multiplayer-host.ts, src/cli/commands/dev.ts</files>
  <behavior>
    - Given a seed GameStateSnapshot threaded via hostOptions, handleStart returns that seed's state envelope (via runnerFromSnapshot + stateEnvelope), NOT a freshly-started game.
    - Given NO seed, handleStart is unchanged (fresh start).
    - The dev `--seed <file>` flag reads a JSON file, parses it to a GameStateSnapshot, and routes it to the host so the first started state IS the seeded state.
    - A missing/unreadable seed file fails fast and loud with an actionable message (path + reason), never a silent fresh-start fallback.
  </behavior>
  <action>
Extend the seed to travel on the NON-PERSISTING host channel so it never pollutes `snapshot.gameOptions`: add an optional `seedSnapshot?: GameStateSnapshot` to `executeOp`'s `hostOptions` param in stateless-ops.ts. In `handleStart`, when `hostOptions?.seedSnapshot` is present, return `{ success: true, ...stateEnvelope(runnerFromSnapshot(seedSnapshot, def), gameOptions.playerCount) }` — reusing the SAME `runnerFromSnapshot` + `stateEnvelope` primitives every other op uses; otherwise keep the existing fresh-start path unchanged. Thread `hostOptions` into the `handleStart` call at the `op.type === 'start'` dispatch. Do NOT add a new op type and do NOT touch GameRunner.fromSnapshot — the load machinery is reused as-is.

In multiplayer-host.ts, add an optional `seedSnapshot?: GameStateSnapshot` to `MultiplayerHostOptions` and include it in the `hostOptions` object built in `startGame` (~698) so the `start`-op executeOp closure forwards it. When a seed is present, the first started state is the seeded state.

In commands/dev.ts, register a `--seed <file>` option on the `dev` command, add `seed?: string` to `DevOptions`, and in `devCommand` read the file (fail fast with an actionable error naming the path if it is missing or not valid JSON — no fallback to a fresh start), parse it to a GameStateSnapshot, and pass it as `seedSnapshot` into the `MultiplayerHost` construction. Follow the existing `exitOnDevFlagError` pattern for flag parsing errors.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "stateless-ops|multiplayer-host|commands/dev" | head; echo TSC_DONE</automated>
  </verify>
  <done>handleStart returns the seed's state envelope when a seed rides in hostOptions (else fresh start); MultiplayerHostOptions carries seedSnapshot into hostOptions; `boardsmith dev --seed <file>` parses a GameStateSnapshot and threads it in, failing loud on a missing/invalid file. tsc clean for the touched files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record-from-play fixture + deterministic-load integration test (fail-without / pass-with)</name>
  <files>src/session/seed-to-state.test.ts</files>
  <behavior>
    - RECORD: a small in-repo Game subclass is driven via TestGame.doAction to a DISTINCT mid-game state (not the fresh-start state); TestGame.getSnapshot() captures it as the seed.
    - PASS-WITH: executing the start op with the seed threaded via hostOptions yields state equal to the recorded snapshot's element tree / flowState / current player — and the seeded game can take the next legal action.
    - FAIL-WITHOUT: executing the start op WITHOUT the seed (fresh start) yields state that differs from the recorded mid-game state (asserts the wiring is load-bearing, PROC-01).
    - LOAD-TWICE-IDENTICAL: loading the same seed twice produces byte-identical state (RNG randomState pinned).
  </behavior>
  <action>
Create src/session/seed-to-state.test.ts. Define a minimal in-repo Game subclass (mirror the FixtureGame/SeedFixtureGame pattern in test-game.test.ts) with an action that measurably mutates state (e.g. draws/moves an element or advances a counter/turn) so a mid-game snapshot is provably distinct from a fresh start — this keeps the PoC deterministically reachable inside a library-repo vitest test with no cross-repo import. Note in a top comment WHY an in-repo game is used rather than the sibling `~/BoardSmithGames/go-fish` (go-fish is not a dependency of this repo and cannot be imported from a library vitest test; 168-CONTEXT.md sanctions an in-repo TestGame-based target when go-fish is not cleanly reachable — the PoC proves the DETERMINISTIC-LOAD capability, which is target-agnostic).

RECORD leg: `TestGame.create(TheGame, { playerCount, seed })`, drive N real actions via `doAction` to a distinct mid-game state, capture `getSnapshot()`, and serialize it through `JSON.parse(JSON.stringify(...))` to prove it survives a real seed-file round-trip.

PASS-WITH leg: call `executeOp(def, gameOptions, null, null, { type: 'start' }, { seedSnapshot })` and assert the returned envelope's state equals the recorded snapshot's element tree, flowState, and current player; then perform the next legal action against the seeded snapshot and assert it succeeds.

FAIL-WITHOUT leg: call the same start op with NO seed (fresh start) and assert its state does NOT equal the recorded mid-game state — this is the assertion that would fail if handleStart ignored the seed.

LOAD-TWICE-IDENTICAL leg: run the seeded start op twice and assert deep-equal state (RNG randomState identical).

Name describe blocks by requirement id (FEAT-01 / PROC-01). Build `def` (GameDefinitionLike) from the in-repo game the same way the existing stateless-ops tests do (search src/session for existing executeOp test setup and reuse that shape rather than hand-rolling).
  </action>
  <verify>
    <automated>npx vitest run src/session/seed-to-state.test.ts</automated>
  </verify>
  <done>The integration test is green: record→seed→load reproduces the exact recorded state and takes the next legal action, a fresh start differs from it, and loading the seed twice is byte-identical. The FAIL-WITHOUT assertion demonstrably fails against a handleStart that ignores the seed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed file → JSON.parse → snapshot | `--seed <file>` reads a local, author-controlled JSON file into an authoritative game snapshot loaded via fromSnapshot. Dev-tool only; not a network or production surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-168-02-T | Tampering | `--seed` file → handleStart | accept | Dev-only CLI; the seed is an author-authored local file, same trust level as the game source. Malformed JSON fails fast with an actionable error (no silent fresh-start fallback); GameRunner.fromSnapshot already validates snapshot structure on load. |
| T-168-02-D | Denial of service | dev host start with a crafted seed | accept | Local dev host, single operator; a bad seed at worst throws at start. No remote input path. |
| T-168-02-SC | Tampering | package installs | n/a | No npm/pip/cargo installs in this plan — no new dependencies added. |
</threat_model>

<verification>
- `npx vitest run src/session/seed-to-state.test.ts` green (record→seed→load→assert, fail-without/pass-with, load-twice-identical).
- Phase gate: full suite green (`npm run test`, ~3111 baseline after Phase 167).
- `boardsmith dev --seed <missing-file>` exits with an actionable error naming the path (manual/CLI smoke — no silent fresh start).
</verification>

<success_criteria>
FEAT-01 PoC + PROC-01: `--seed <file>` seeds the dev host's initial snapshot from a recorded GameStateSnapshot via the existing handleStart/fromSnapshot path (no rebuilt load machinery); a record→seed→load→assert integration test proves the seeded state matches the recorded target exactly, can advance, and is deterministic across two loads — and fails on pre-wiring (fresh-start) behavior.
</success_criteria>

<output>
Create `.planning/phases/168-platform-feature-spike-seed-to-state/168-02-SUMMARY.md` when done.
</output>
