---
phase: 169-post-fix-game-de-workaround-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md
autonomous: true
requirements: [SWEEP-01, PROC-01]

must_haves:
  truths:
    - "A Dxx ↔ repo-BUG-n crosswalk exists covering all 5 game repos' own ledger ids"
    - "Each relevant v4.8 library fix has a grep-verified present/absent verdict in the symlinked boardsmith"
    - "Every later per-repo plan can cite this crosswalk as its removal gate"
  artifacts:
    - path: ".planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md"
      provides: "Dxx↔BUG-n crosswalk + library-fix-present checklist (the SC-5/PROC-01 gate)"
      contains: "hasNonUndoableAction"
  key_links:
    - from: "169-CROSSWALK.md"
      to: "node_modules/boardsmith/src/session/utils.ts"
      via: "grep-verified fix-present checklist"
      pattern: "assertUndoAllowed|hasNonUndoableAction"
---

<objective>
Build the foundation that gates every removal in this phase: a `Dxx ↔ repo-BUG-n` crosswalk assembled from all five game repos' own bug ledgers, paired with a grep-verified "this library fix is present in the live symlinked boardsmith" checklist.

Purpose: The five repos track library bugs under their OWN ids (`BUG 3/6/7/8`, `BS-5/9`, `BSR-5`, `BOARDSMITH-BUG-02`), not the `Dxx` scheme. No removal can be justified without mapping a repo's own filing to the v4.8 defect and proving the specific fix shipped. This doc is the SC-5/PROC-01 gate cited by plans 02-06.
Output: `.planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md` in THIS library repo. No game-repo edits in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/169-post-fix-game-de-workaround-sweep/169-CONTEXT.md
@.planning/phases/169-post-fix-game-de-workaround-sweep/169-VALIDATION.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the Dxx ↔ repo-BUG-n crosswalk from all five ledgers</name>
  <read_first>
    - ~/BoardSmithGames/lanternfall/BOARDSMITH-BUGS.md
    - ~/BoardSmithGames/seven/BOARDSMITH-REQUESTS.md
    - ~/BoardSmithGames/one-two-punch/BOARDSMITH-BUGS.md
    - ~/BoardSmithGames/doom-machine/BOARDSMITH-BUGS.md
    - ~/BoardSmithGames2/seven/BOARDSMITH-BUG-0{1,2,3,4,5}-*.md
    - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CONTEXT.md (the <code_context> per-repo target inventory)
  </read_first>
  <action>Create `.planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md`. Read every one of the five repos' bug/request ledgers and produce a table mapping each repo's own filing id to the corresponding v4.8 `Dxx` defect and the v4.8 requirement/phase that fixed it (e.g. `one-two-punch BUG 3 ↔ D1/UNDO-01/Phase 155`; `one-two-punch BUG 8 ↔ D8/AI-02/Phase 159`; `seven BSR-5 ↔ D1`; `BoardSmithGames2/seven BOARDSMITH-BUG-02 ↔ D1`; `doom BS-5 ↔ D9/AI-01`; `doom BS-9 ↔ D23/SPACE-02`; `lanternfall BUG 6 ↔ D29/LIBX-02`; `lanternfall SPACE-05/D26`). One row per filing, columns: repo, repo-id, filing title, Dxx, v4.8 req, fix phase, sweep target file:line (from CONTEXT <code_context>), disposition-hint (removable-if-verified / keep-legit / defer / reclassify / no-op). Explicitly list the recorded no-op/withdrawn items: D32/DRAWDROP (platform-side, none in game src), the two WITHDRAWN lanternfall filings (seat display names; seat chips ignoring `--bsg-seat-N`), and MERC (out of scope). Do NOT edit any game repo.</action>
  <verify>
    <automated>test -f .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md && grep -q "BUG 8" .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md && grep -q "BOARDSMITH-BUG-02" .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md && grep -q "WITHDRAWN" .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md</automated>
  </verify>
  <acceptance_criteria>
    - 169-CROSSWALK.md exists with a per-filing crosswalk table covering all 5 repos
    - Each of one-two-punch BUG 3/8, seven BSR-5, doom BS-5/BS-9, BSG2/seven BOARDSMITH-BUG-02, lanternfall BUG 6 appears mapped to a Dxx + fix phase
    - No-op/withdrawn/out-of-scope items (D32, 2 withdrawn lanternfall filings, MERC) explicitly recorded
    - Zero edits to any game repo
  </acceptance_criteria>
  <done>Crosswalk table complete; every repo filing maps to a Dxx (or is marked no-op/withdrawn/out-of-scope).</done>
</task>

<task type="auto">
  <name>Task 2: Grep-verify each library fix is present in the live symlinked boardsmith</name>
  <read_first>
    - node_modules/boardsmith is a symlink to /Users/jtsmith/BoardSmith — verify the SOURCE at /Users/jtsmith/BoardSmith/src (the live code all 5 repos consume)
    - /Users/jtsmith/BoardSmith/src/session/utils.ts (D1)
    - /Users/jtsmith/BoardSmith/src/engine/utils/resolve-multiselect.ts + src/engine/element/action-metadata.ts (D9)
    - /Users/jtsmith/BoardSmith/src/engine/flow/builders.ts (D29)
    - /Users/jtsmith/BoardSmith/src/engine/element/space.ts + game-element.ts (D22/D23/D24)
    - /Users/jtsmith/BoardSmith/src/engine/action/types.ts (D28 suppressFromDock)
    - /Users/jtsmith/BoardSmith/src/ui/composables/useAutoZoom.ts (D12), src/ui/utils/color-contrast.ts (D30), src/ui/components/GameShell.vue (D31 displayedState)
  </read_first>
  <action>Append a "Library Fix-Present Checklist" section to 169-CROSSWALK.md. For each Dxx that any repo's removable target depends on, record the exact grep command run against `/Users/jtsmith/BoardSmith/src/...` (equivalently `node_modules/boardsmith/src/...` via the symlink) and its verdict PRESENT/ABSENT. Use these anchors: D1 → `assertUndoAllowed` + `hasNonUndoableAction` in session/utils.ts; D9 → `resolveMultiSelect` in engine/utils/resolve-multiselect.ts AND `buildPickMetadata` in engine/element/action-metadata.ts; D8 → `toJSONForPlayer` used in the ai/ MCTS clone path; D12 → the ResizeObserver/re-fit path in useAutoZoom.ts; D29 → `unbounded` on the loop config in engine/flow/builders.ts; D24/SPACE-03 → hidden child-count suppression in space.ts; D22/SPACE-01 sealed + D23/SPACE-02 moveToInternal reparent in space.ts/game-element.ts; D26/SPACE-05 → availableActions/actionMetadata reconciliation; D28/LIBX-01 → `suppressFromDock` in engine/action/types.ts; D30 → `contrastInk`/color-contrast; D31 → `displayedState` in GameShell.vue. Record any ABSENT verdict as a BLOCKER that forbids the corresponding removal (skip + record, never silent) per SC-5/PROC-01. State the rule explicitly at the top of the section.</action>
  <verify>
    <automated>grep -q "Fix-Present Checklist" .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md && grep -Eq "PRESENT|ABSENT" .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md && grep -q "assertUndoAllowed" .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md</automated>
  </verify>
  <acceptance_criteria>
    - Checklist records a PRESENT/ABSENT verdict + the exact grep command for each of D1, D8, D9, D12, D22, D23, D24, D26, D28, D29, D30, D31
    - The "ABSENT ⇒ removal is a BLOCKER, not a silent skip" rule is stated
    - Verdicts reflect actual grep output against the live symlinked source (no assumed PRESENT)
  </acceptance_criteria>
  <done>Every fix a downstream removal depends on has a grep-backed PRESENT/ABSENT verdict; the gate rule is written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library src → game repos | Games consume boardsmith via live symlink; a wrong "fix present" verdict could authorize an unsafe removal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-01 | Tampering | fix-present verdict | mitigate | Verdicts are grep-backed against the live symlinked source with the exact command recorded; ABSENT forbids removal |
| T-169-SC | Tampering | npm/pip/cargo installs | accept | This phase installs no packages; it removes dead game code + edits docs/ledgers only |
</threat_model>

<verification>
- 169-CROSSWALK.md exists with both the crosswalk table and the Fix-Present Checklist
- Every repo filing id maps to a Dxx (or no-op/withdrawn/out-of-scope)
- Every downstream-relevant fix has a grep-backed PRESENT/ABSENT verdict
</verification>

<success_criteria>
The crosswalk foundation is complete and usable as the removal gate for plans 02-06: repo ids ↔ Dxx mapped, and each library fix's presence proven or marked ABSENT (blocker).
</success_criteria>

<output>
Create `.planning/phases/169-post-fix-game-de-workaround-sweep/169-01-SUMMARY.md` when done.
</output>
