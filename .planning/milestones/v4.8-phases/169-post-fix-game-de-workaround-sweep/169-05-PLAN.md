---
phase: 169-post-fix-game-de-workaround-sweep
plan: 05
type: execute
wave: 5
depends_on: ["169-01"]
files_modified:
  - ~/BoardSmithGames/doom-machine/src/rules/actions.ts
  - ~/BoardSmithGames/doom-machine/src/rules/roll-conditions.ts
  - ~/BoardSmithGames/doom-machine/src/rules/elements.ts
  - ~/BoardSmithGames/doom-machine/src/ui/board-height.ts
  - ~/BoardSmithGames/doom-machine/BOARDSMITH-BUGS.md
  - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md
autonomous: true
requirements: [SWEEP-01, PROC-01]

must_haves:
  truths:
    - "doom's D9/BS-5 single-select enumeration rewrite is DEFERRED with rationale; only its stale comment is refreshed"
    - "doom's board-height cap (D12-adjacent) and one-slot-one-pile (BS-9/D23) are removed only if ZOOM-01/SPACE-02 verified + suite green; else kept-and-noted"
    - "BS-10 is reclassified as a game-side art-path fix already handled (not a library bug); the `<base href>` gap becomes a scaffold-default recommendation"
    - "doom-machine `npx vitest run` is green after the sweep"
    - "pre-existing dirty-tree files are recorded and NOT folded into the sweep commit; every sweep commit is file-scoped"
  artifacts:
    - path: "~/BoardSmithGames/doom-machine/BOARDSMITH-BUGS.md"
      provides: "Updated BS-5 (defer), BS-9 (D23), BS-10 (reclassify), D32 (no-op) entries"
  key_links:
    - from: "doom sweep"
      to: "169-CROSSWALK.md verdicts (D12, D23)"
      via: "removal gate"
      pattern: "PRESENT"
---

<objective>
Conservatively sweep `~/BoardSmithGames/doom-machine` (SOLO, no AI): DEFER the risky D9/BS-5 single-select→native-multiSelect rewrite (comment-refresh only), gate the board-height cap (D12-adjacent) and one-slot-one-pile teardown (BS-9/D23) on verified fixes + green suite, and RECLASSIFY BS-10 as a game-side art-path fix already handled — folding the scaffold `<base href="/">` gap into a scaffold-default recommendation, NOT re-fixing it as a library bug. Record D32/DRAWDROP as no-op (platform-side, none in game src). Kept items are valid recorded outcomes.

Purpose: Clean doom's dead weight without risking its 38-test suite; correct the stale BS-10 filing.
Output: `sweep/v4.8-dework` branch in doom-machine with gated changes + BS-10 reclassification + updated `BOARDSMITH-BUGS.md`; 169-CROSSWALK.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md
@.planning/phases/169-post-fix-game-de-workaround-sweep/169-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record pre-existing dirty tree + green baseline + confirm D12/D23 verdicts; DEFER D9 rewrite (comment-only)</name>
  <read_first>
    - ~/BoardSmithGames/doom-machine/src/rules/actions.ts (lines ~39-86 single-select chooseFrom enumerating subsets; BS-5/D9)
    - ~/BoardSmithGames/doom-machine/src/rules/roll-conditions.ts (lines ~273-276)
    - ~/BoardSmithGames/doom-machine/src/App.vue (lines ~14-17)
    - ~/BoardSmithGames/doom-machine/src/rules/flow.ts (lines ~70-148 actionStep maxMoves — legit, keep)
    - ~/BoardSmithGames/doom-machine/BOARDSMITH-BUGS.md
    - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md (D12/ZOOM-01, D23/SPACE-02 verdicts)
  </read_first>
  <action>cd into `~/BoardSmithGames/doom-machine`. FIRST, BEFORE branching: run `git -C ~/BoardSmithGames/doom-machine status --porcelain` and RECORD the exact pre-existing dirty state verbatim in the summary. Do NOT discard, stash, or commit any pre-existing WIP — branching a dirty tree with `git checkout -b` carries the WIP forward (fine), but the sweep must NOT claim credit for it. Create + checkout `sweep/v4.8-dework` off master (never push/touch master). Run `npx vitest run`; record the green baseline. Confirm D12/ZOOM-01 and D23/SPACE-02 verdicts in 169-CROSSWALK.md. DEFER the D9/BS-5 single-select-enumeration → native panel multiSelect rewrite (risky game-logic change) — do NOT rewrite; only REFRESH the stale BS-5 comment(s) in actions.ts/roll-conditions.ts/App.vue to note the library now supports function-valued multiSelect natively (D9/AI-01) and that the rewrite is deferred with rationale. Keep `actionStep({maxMoves})` in flow.ts (legit). Run `npx vitest run` (comment-only edits must stay green).</action>
  <verify>
    <automated>cd ~/BoardSmithGames/doom-machine && git rev-parse --abbrev-ref HEAD | grep -q "sweep/v4.8-dework" && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Pre-existing dirty-tree state (`git status --porcelain`) recorded verbatim BEFORE branching; no pre-existing WIP discarded, stashed, or committed
    - `sweep/v4.8-dework` checked out; baseline recorded; `npx vitest run` green
    - D9/BS-5 rewrite NOT attempted; only its stale comment refreshed + deferral rationale written
    - `actionStep({maxMoves})` untouched
  </acceptance_criteria>
  <done>Pre-existing dirty tree recorded; D9 rewrite deferred (comment-only); baseline green on the sweep branch.</done>
</task>

<task type="auto">
  <name>Task 2: Gate board-height cap (D12) + one-slot-one-pile (BS-9/D23), verify green</name>
  <read_first>
    - ~/BoardSmithGames/doom-machine/src/ui/board-height.ts + src/ui/components/GameTable.vue (lines ~55-68 board-height cap; D12-adjacent Fit)
    - ~/BoardSmithGames/doom-machine/src/rules/elements.ts (line ~92 one-slot-one-pile) + game.ts (line ~1223) (BS-9 = D23 slot teardown, SPACE-02)
    - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md
  </read_first>
  <action>On the sweep branch: IFF D12/ZOOM-01 is PRESENT (useAutoZoom re-fits on resize, Phase 158), assess whether the board-height cap in board-height.ts + GameTable.vue:~55-68 is still needed; if it is purely a stale-fit compensator, remove it and run `npx vitest run` — revert-on-red (record kept-and-noted). IFF D23/SPACE-02 is PRESENT (Space removal/re-parent), assess the one-slot-one-pile constraint (elements.ts:~92, game.ts:~1223); only relax/remove it if genuinely a teardown workaround AND the suite stays green — otherwise keep + note (this is single-game correctness; be extra conservative). Keep the whole suite green throughout.</action>
  <verify>
    <automated>cd ~/BoardSmithGames/doom-machine && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - board-height cap removed only on D12 PRESENT + green; else kept-noted
    - one-slot-one-pile relaxed only on D23 PRESENT + green; else kept-noted
    - every red-causing removal reverted and recorded; `npx vitest run` green
  </acceptance_criteria>
  <done>D12/D23 targets assessed + gated; doom suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Reclassify BS-10, record D32 no-op, reconcile ledgers + crosswalk (file-scoped commit)</name>
  <read_first>
    - ~/BoardSmithGames/doom-machine/src/rules/cards.ts + components referencing absolute `/cards/*.png`
    - ~/BoardSmithGames/doom-machine/BOARDSMITH-BUGS.md (BS-10 entry)
    - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md
  </read_first>
  <action>Reclassify BS-10 in `~/BoardSmithGames/doom-machine/BOARDSMITH-BUGS.md`: it is a GAME-SIDE art-path fix already handled (absolute `/cards/`, commit `6949fde`), NOT a library bug — mark it resolved/reclassified with that rationale and note the scaffold `<base href="/">` gap belongs in a future scaffold-default RECOMMENDATION (record it as such in the crosswalk, not as an open engine defect). Verify by inspection that card art paths load as-is (no re-fix). Record D32/DRAWDROP as no-op (grep confirms no `[DRAWDROP]` in doom `src/` — platform-side). Update BS-5 (deferred), BS-9 (D23 outcome), and any swept entries in the ledger. COMMIT FILE-SCOPED: `git add` ONLY the sweep-touched files (`src/rules/actions.ts`, `src/rules/roll-conditions.ts`, `src/rules/elements.ts`, `src/ui/board-height.ts`, `src/ui/components/GameTable.vue`, `src/App.vue`, `BOARDSMITH-BUGS.md` — only those actually edited) and commit — NEVER `git add -A` / `git add .` / `git commit -a`. Do NOT stage any pre-existing dirty files recorded in Task 1. Update this repo's 169-CROSSWALK.md with doom's per-target outcomes, the BS-10 reclassification, the `<base href>` scaffold recommendation, and the D32 no-op. In the SUMMARY, list the pre-existing dirty files this sweep deliberately did NOT touch.</action>
  <verify>
    <automated>cd ~/BoardSmithGames/doom-machine && ! grep -rq "\[DRAWDROP\]" src/ && grep -qi "BS-10" BOARDSMITH-BUGS.md && git log --oneline -1 | grep -iq sweep</automated>
  </verify>
  <acceptance_criteria>
    - BS-10 reclassified as game-side art-path already-handled; `<base href>` folded into a scaffold-default recommendation (crosswalk), not re-fixed as a library bug
    - D32/DRAWDROP recorded no-op (grep confirms absent from doom `src/`)
    - Commit is FILE-SCOPED: only sweep-touched files staged; pre-existing WIP NOT committed; SUMMARY lists untouched dirty files
    - BOARDSMITH-BUGS.md + 169-CROSSWALK.md updated; commit on sweep branch, not pushed
  </acceptance_criteria>
  <done>BS-10 reclassified; D32 no-op recorded; ledger + crosswalk reconciled for doom; commit file-scoped with pre-existing WIP excluded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| removal vs. suite | doom's 38-test single-game correctness suite is the oracle; be extra conservative |
| sweep commit vs. pre-existing WIP | The audited sweep commit must contain ONLY sweep-touched files, never pre-existing dirty-tree WIP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-05a | Tampering | D9/BS-5 rewrite | accept | Rewrite DEFERRED (risky game-logic change); comment-only refresh — no behavior change |
| T-169-05b | Tampering | D12/D23 removals | mitigate | Gated on PRESENT verdicts + green `npx vitest run`; revert-on-red; keep-and-note default |
| T-169-05c | Repudiation | BS-10 reclassification | mitigate | Verified by inspection as game-side already-handled; `<base href>` recorded as scaffold recommendation, not a re-fix |
| T-169-05d | Repudiation | sweep commit vs. pre-existing WIP | mitigate | Record dirty tree before branching; FILE-SCOPED `git add` only; never `-a`/`-A` |
| T-169-SC | Tampering | npm installs | accept | No installs; comment/ledger + gated dead-code edits only |
</threat_model>

<verification>
- doom-machine `npx vitest run` green on the sweep branch
- D9 rewrite deferred; D12/D23 gated; BS-10 reclassified; D32 no-op recorded
- Pre-existing WIP excluded from the file-scoped commit
- Ledger + crosswalk updated
</verification>

<success_criteria>
doom-machine is swept conservatively with a green suite: the risky D9 rewrite is explicitly deferred, D12/D23 targets are gated (removed-if-verified or kept-and-noted), and BS-10 is reclassified rather than re-fixed. Kept items are recorded, not forced; no pre-existing WIP folded into the sweep commit.
</success_criteria>

<output>
Create `.planning/phases/169-post-fix-game-de-workaround-sweep/169-05-SUMMARY.md` when done.
</output>
