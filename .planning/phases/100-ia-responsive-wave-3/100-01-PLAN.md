---
phase: 100-ia-responsive-wave-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/components/GameShell.vue
  - src/ui/components/GameShell.ia.test.ts
autonomous: true
requirements: [IA-02, IA-03, IA-04]

must_haves:
  truths:
    - "The shell renders as .app > .stage(sidebar | boardregion) + a full-width .actionbar sibling below the stage"
    - "When it is the player's turn, a prompt line is always visible in the actionbar (boardPrompt ?? currentPick.prompt), even when every pick is board-anchored"
    - "When all current choices are board-anchored, the prompt line stays visible and only the action buttons are suppressed"
    - "The action dock (actionbar buttons) renders only when isMyTurn or awaitingPlayerNames.length"
    - "The boardregion reserves the actionbar's measured height via a ResizeObserver-set --bsg-dock-h instead of a hardcoded 80px"
  artifacts:
    - path: "src/ui/components/GameShell.vue"
      provides: "Slate side-by-side shell + full-width actionbar with persistent prompt + conditional dock + ResizeObserver dock-height var"
      contains: "boardregion"
    - path: "src/ui/components/GameShell.ia.test.ts"
      provides: "Integration tests for persistent prompt, button suppression, conditional dock"
      contains: "allCurrentChoicesAnchored"
  key_links:
    - from: "src/ui/components/GameShell.vue (.actionbar .turn)"
      to: "boardPrompt / actionController.currentPick"
      via: "prompt source-of-truth binding"
      pattern: "boardPrompt"
    - from: "src/ui/components/GameShell.vue (ResizeObserver)"
      to: "document.documentElement --bsg-dock-h"
      via: "borderBoxSize → setProperty"
      pattern: "ResizeObserver"
---

<objective>
Rebuild GameShell's game screen from the current column stack (header → main → footer) into the canonical Slate structure: `.app > .stage(.sidebar | .boardregion) + .actionbar` where the full-width `.actionbar` is a sibling of `.stage`. Wire the two always-correct surfaces inside the actionbar: a persistent prompt line (`boardPrompt ?? currentPick.prompt`) that survives even when every pick is board-anchored, and the action dock (buttons) that renders only when the player is actionable and is suppressed (buttons only) when all choices are board-anchored. Replace the hardcoded `padding-bottom: 80px` spacer with a ResizeObserver-measured `--bsg-dock-h`.

Purpose: Get the chrome out of the board's way and guarantee the player is never left with a silent board (IA-02, IA-03, IA-04). This is the structural keystone every other Phase 100 plan builds on.
Output: Restructured GameShell.vue + new GameShell.ia.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-ia-responsive-wave-3/100-CONTEXT.md
@.planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md
@planning/mockups/boardsmith-chrome.html

<interfaces>
<!-- Computeds/refs already defined in GameShell.vue <script setup> — use directly, no exploration. -->
- platformMode: ref<boolean>                       (GameShell.vue:138)
- isMyTurn: from useGame                            (destructured GameShell.vue:242)
- awaitingPlayerNames: computed<{seat,name,color}[]> (GameShell.vue:460)
- boardPrompt: ref<string|null>                     (GameShell.vue:485)
- currentPlayerName: computed<string>               (GameShell.vue:444)
- currentPlayerColor: computed<string|undefined>    (GameShell.vue:471)
- actionController.currentPick: computed<PickMetadata|null> (useActionController.ts:614; .prompt is a string)
- actionController.allCurrentChoicesAnchored: computed<boolean> (useActionController.ts:674)
- props.suppressActionPanel: boolean                (GameShell.vue:102)
- IMPORTANT: refs inside actionController are NOT auto-unwrapped in template — mirror the existing access pattern `actionController.allCurrentChoicesAnchored.value` (GameShell.vue:1425), i.e. `actionController.currentPick.value?.prompt`.
- Layout tokens already emitted by theme.ts: --bsg-rail (64px), --bsg-side (286px), --bsg-s1..s6 spacing, --bsg-surface, --bsg-line.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure the game-screen template into .app/.stage/.sidebar/.boardregion/.actionbar</name>
  <files>src/ui/components/GameShell.vue</files>
  <read_first>
    - src/ui/components/GameShell.vue lines 1288-1478 (current game-screen template + footer)
    - planning/mockups/boardsmith-chrome.html lines 67-91, 155-218, 283-457 (target DOM: .stage, .sidebar, .side-head, .boardregion, .actionbar)
    - .planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md "Architecture Patterns" Pattern 1 + "Target DOM Structure"
  </read_first>
  <action>
    Replace the `currentScreen === 'game'` block (currently `.game-shell__game` with `<GameHeader>`, `.game-shell__main` > `.game-shell__sidebar` + `.game-shell__content`, then `<footer class="game-shell__action-bar">`).
    New structure mirroring the mockup: outer `.game-shell__game` contains `<div class="stage">` then `<div class="actionbar">` as siblings (actionbar OUTSIDE stage so it spans full width). Inside `.stage`: `<aside class="sidebar">` with a `.side-head` block (placeholder `.sw-btn` host button + `.menubtn` controls trigger — wiring is deferred to plan 100-04; render static buttons now with aria-labels) followed by a `.side-scroll` wrapper holding the existing PlayersPanel + sidebar-extra slot + GameHistory; then `<main class="boardregion" id="main" role="main">` holding the existing zoom-container `<component>`/`#game-board` slot block unchanged (keep all slot props including :set-board-prompt).
    Keep `<GameHeader>` for now but leave it as the first child of `.game-shell__game` (its !platformMode gate is plan 100-04 — do NOT remove here). Keep DebugPanel, error-banner, ZoomPreviewOverlay, Toast where they are. The `.actionbar` body is built in Task 2 — for this task render an empty `<div class="actionbar" ref="actionbarRef" role="region" aria-label="Actions">` placeholder.
    Do NOT inline any CSS hex literals (lint:css color-no-hex with empty ignore list). Use existing class names that Task 3 styles.
  </action>
  <verify>
    <automated>npm run test -- src/ui/components/GameShell.theme.test.ts && npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -i "GameShell" || echo "no GameShell type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class="stage"' src/ui/components/GameShell.vue` ≥ 1 and `grep -c 'class="boardregion"' src/ui/components/GameShell.vue` ≥ 1 and `grep -c 'class="actionbar"' src/ui/components/GameShell.vue` ≥ 1
    - `.actionbar` appears as a sibling of `.stage` (not nested inside it) — confirm by reading the template tree
    - PlayersPanel, sidebar-extra slot, and GameHistory all live inside `.sidebar > .side-scroll`; the board `<component>`/`#game-board` slot lives inside `.boardregion`
    - The board slot still passes `:set-board-prompt="setBoardPrompt"` unchanged
    - Existing GameShell.theme.test.ts still passes
  </acceptance_criteria>
  <done>The game screen renders as .app > .stage(.sidebar | .boardregion) + a full-width .actionbar sibling; theme tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persistent prompt + conditional dock + ResizeObserver dock-height</name>
  <files>src/ui/components/GameShell.vue, src/ui/components/GameShell.ia.test.ts</files>
  <read_first>
    - src/ui/components/GameShell.vue lines 1423-1450 (current footer with ActionPanel + suppress gate)
    - src/ui/components/GameShell.vue lines 460-489 (awaitingPlayerNames, boardPrompt, currentPlayerColor)
    - src/ui/composables/useActionController.ts lines 614-686 (currentPick + allCurrentChoicesAnchored)
    - planning/mockups/boardsmith-chrome.html lines 213-238, 430-456 (.actionbar, .turn strip, .end group)
    - .planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md "Pitfall 5" + "Pattern 3" (ResizeObserver)
  </read_first>
  <behavior>
    - When isMyTurn is true and currentPick has a prompt, the `.turn` prompt line renders the prompt text.
    - When boardPrompt is non-null it takes precedence over currentPick.prompt.
    - When allCurrentChoicesAnchored is true, the `.turn` prompt line still renders BUT the ActionPanel/buttons are absent.
    - When isMyTurn is false and awaitingPlayerNames is empty, the entire actionbar dock content (prompt + buttons) is absent.
    - ResizeObserver on the actionbar element sets document.documentElement style --bsg-dock-h to the measured borderBox block size.
  </behavior>
  <action>
    Fill the `.actionbar` placeholder. Outer gate: render dock content only when `isMyTurn || awaitingPlayerNames.length` (matches IA-04; the bare "it's X's turn" case is the players panel's job, not the dock). Inside, always render a `<span class="turn">` showing the prompt = `boardPrompt ?? actionController.currentPick.value?.prompt` plus a small active-player identity (a color swatch bound to `currentPlayerColor` + `currentPlayerName`); keep the existing `aria-live="polite"` vh prompt mirror. Then render the action buttons surface — keep the existing `<slot name="action-panel">` + `<ActionPanel>` block verbatim (preserve every current prop including :show-undo, parity via useBoardInteraction must not change) but gate ONLY that buttons block with `v-if="!props.suppressActionPanel && !actionController.allCurrentChoicesAnchored.value"`. Keep the time-travel-banner inside the dock.
    Add a `const actionbarRef = ref<HTMLElement | null>(null)` and bind it on `.actionbar`. In onMounted create a ResizeObserver that reads `entries[0]?.borderBoxSize?.[0]?.blockSize ?? 0` and `document.documentElement.style.setProperty('--bsg-dock-h', h + 'px')`; observe actionbarRef.value when present; disconnect in onUnmounted. Import ref/onMounted/onUnmounted if not already imported.
    Create src/ui/components/GameShell.ia.test.ts: mount GameShell with stubbed props/state to assert the four behaviors above (prompt visible when isMyTurn; prompt survives + buttons absent when allCurrentChoicesAnchored=true; whole dock absent when !isMyTurn && no awaiting). Follow the mounting pattern in GameShell.theme.test.ts. Use a fake ResizeObserver global if jsdom lacks one, and assert the --bsg-dock-h setProperty is invoked.
    No fenced CSS/hex in template.
  </action>
  <verify>
    <automated>npm run test -- src/ui/components/GameShell.ia.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- src/ui/components/GameShell.ia.test.ts` passes with assertions for: prompt-visible-when-isMyTurn, prompt-survives-and-buttons-suppressed-when-anchored, dock-absent-when-not-actionable
    - `grep -c "ResizeObserver" src/ui/components/GameShell.vue` ≥ 1 and `grep -c "bsg-dock-h" src/ui/components/GameShell.vue` ≥ 1
    - The ActionPanel block retains every prop it had at GameShell.vue:1429-1443 (no parity regression)
    - The buttons block v-if uses `!actionController.allCurrentChoicesAnchored.value`; the prompt `.turn` is NOT inside that v-if
  </acceptance_criteria>
  <done>Prompt is always visible while actionable (incl. board-anchored-only), buttons gate independently, dock height is published to --bsg-dock-h, and the new test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Baseline Slate layout CSS for the new shell</name>
  <files>src/ui/components/GameShell.vue</files>
  <read_first>
    - src/ui/components/GameShell.vue lines 1481-1620 (current scoped CSS: __game, __main, __sidebar, __content, __action-bar, the 768px media block)
    - planning/mockups/boardsmith-chrome.html lines 67-97, 155-164, 213-218 (.app, .stage, .boardregion, .sidebar, .actionbar CSS)
    - .planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md "Pitfall 1" + "Pitfall 2" (container-type needs definite height + min-height:0 chain) and "Key CSS Values Being Replaced"
  </read_first>
  <action>
    Replace the old `.game-shell__game/__main/__sidebar/__content/__action-bar` rules with rules for the new classes. `.game-shell__game` = flex column, height 100dvh (keep the 100vh fallback line). `.stage` = `flex:1; min-height:0; display:flex; position:relative`. `.sidebar` = `flex:none; width:var(--bsg-side); display:flex; flex-direction:column; min-height:0; background:var(--bsg-surface); border-right:1px solid var(--bsg-line)`. `.side-head` = flex row per mockup line 74. `.side-scroll` = `flex:1; min-height:0; overflow:auto; padding:var(--bsg-s3)`. `.boardregion` = `flex:1; min-width:0; min-height:0; position:relative; display:grid; place-items:center; padding:var(--bsg-s1); container-type:size; padding-bottom:max(var(--bsg-s3), var(--bsg-dock-h, 0px), env(safe-area-inset-bottom))`. `.actionbar` = `flex:none; background:var(--bsg-surface); border-top:1px solid var(--bsg-line); display:flex; align-items:center; flex-wrap:wrap; gap:8px; padding:9px var(--bsg-s4); padding-bottom:calc(9px + env(safe-area-inset-bottom)); max-height:min(40dvh, 320px); overflow-y:auto` (IA-04 cap). Style `.turn` and `.menubtn`/`.sw-btn` placeholders per mockup using tokens only.
    DELETE the hardcoded `padding-bottom: 80px` (GameShell.vue:1542) and the `.game-shell--platform .game-shell__content { padding-bottom: 60px }` rule (≈1503). Leave the `@media (min-width:768px)` block in place for now (plan 100-05 replaces it with the real tiers) but retarget its selectors to the new class names so the layout is not broken between waves.
    Use ONLY var(--bsg-*) tokens, rgba(), or color-mix() — no hex literals (color-no-hex, empty ignore list).
  </action>
  <verify>
    <automated>npm run lint:css && npm run test -- src/ui/components/GameShell.ia.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint:css` exits 0 (no hex literals introduced)
    - `grep -c "container-type" src/ui/components/GameShell.vue` ≥ 1 (on .boardregion)
    - `grep -E "padding-bottom:\s*80px|padding-bottom:\s*60px" src/ui/components/GameShell.vue` returns nothing
    - `.boardregion` padding-bottom references `var(--bsg-dock-h` ; `.actionbar` max-height references `min(40dvh, 320px)` and `overflow-y:auto`
    - `.stage`, `.sidebar`, `.boardregion` each carry `min-height:0` (or boardregion min-height:0)
  </acceptance_criteria>
  <done>The new shell lays out side-by-side with a full-width actionbar, the board container is height-definite for container queries, the 80px/60px spacers are gone, and lint:css is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| host iframe → GameShell | postMessage already validated by isOriginAllowed; this plan adds no new inbound message handling |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01-01 | Tampering | scoped CSS scrim/dock overlay | accept | actionbar is a flow sibling, not fixed; cannot cover browser UI; dock max-height capped |
| T-100-01-02 | DoS | ResizeObserver on actionbar | mitigate | single observer, disconnected on unmount; writes one CSS var, no layout thrash loop |
| T-100-01-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan; no new dependencies |
</threat_model>

<verification>
- `npm run test` full suite stays green (1002+ baseline)
- `npm run lint:css` green
- Manual (Phase 103 will formalize): board renders centered with full-width actionbar; prompt visible on your turn; dock hidden when not your turn
</verification>

<success_criteria>
IA-02, IA-03, IA-04 satisfied: persistent prompt at the actionbar (boardPrompt ?? currentPick.prompt), buttons suppressed when board-anchored while prompt survives, dock only when actionable with ResizeObserver-measured reservation; new GameShell.ia.test.ts passes; lint:css green.
</success_criteria>

<output>
Create `.planning/phases/100-ia-responsive-wave-3/100-01-SUMMARY.md` when done. Note in the SUMMARY: GameShell.vue template+CSS were restructured (Phase 103 regression surface).
</output>
