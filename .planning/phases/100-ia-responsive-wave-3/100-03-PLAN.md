---
phase: 100-ia-responsive-wave-3
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/components/auto-ui/renderers/CardRenderer.vue
  - src/ui/components/auto-ui/renderers/HandRenderer.vue
  - src/ui/components/auto-ui/renderers/DeckRenderer.vue
  - src/cli/dev-host/DevHost.vue
autonomous: true
requirements: [IA-05]

must_haves:
  truths:
    - "Card surfaces (hand, single card, deck stack) size from a shared --card-w clamp, not a fixed 60px/84px"
    - "Each card keeps a ≥44px tap zone at the small end of the clamp"
    - "The dev host fills the dynamic viewport via 100dvh (with 100vh fallback)"
  artifacts:
    - path: "src/ui/components/auto-ui/renderers/CardRenderer.vue"
      provides: "Fluid --card-w card sizing"
      contains: "--card-w"
    - path: "src/cli/dev-host/DevHost.vue"
      provides: "100dvh dev host height"
      contains: "100dvh"
  key_links:
    - from: "HandRenderer / DeckRenderer card surfaces"
      to: "--card-w clamp"
      via: "shared width token"
      pattern: "--card-w"
---

<objective>
Make the card-family auto-UI renderers fluid (IA-05): hand cards, single cards, and deck stacks size from a shared `--card-w: clamp(44px, 14cqw, 84px)` token (with the matching ~1.4 aspect height) instead of the hardcoded 60px×84px, keeping a ≥44px tap zone at the small end. Also fix the dev host to use `100dvh` (Phase 97 pattern) so mobile toolbars don't clip it.

Purpose: Cards and the dev shell fit their container by construction (IA-05). These are the card-subset regression files for Phase 103.
Output: Updated CardRenderer.vue, HandRenderer.vue, DeckRenderer.vue, DevHost.vue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/100-ia-responsive-wave-3/100-CONTEXT.md
@.planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md

<interfaces>
- CardRenderer.vue fixed sizes at lines 506-508, 526-528, 537-539, 548-550 (60/45/84).
- HandRenderer.vue fixed sizes at lines 554-556, 570-572, 580-582 (60/45/84).
- DeckRenderer.vue fixed sizes at lines 256-257 (60×84 stack container).
- DevHost.vue uses height:100vh at line 356.
- cqw resolves only if an ancestor sets container-type; the grid archetype (plan 100-02) and GameShell.boardregion (plan 100-01) provide it. For hands rendered outside those, the clamp still floors at 44px / ceils at 84px, so it degrades safely.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared --card-w clamp across Card / Hand / Deck renderers</name>
  <files>src/ui/components/auto-ui/renderers/CardRenderer.vue, src/ui/components/auto-ui/renderers/HandRenderer.vue, src/ui/components/auto-ui/renderers/DeckRenderer.vue, src/ui/components/auto-ui/renderers/CardRenderer.fluid.test.ts</files>
  <read_first>
    - src/ui/components/auto-ui/renderers/CardRenderer.vue lines 500-552 (card-image / card-back / sprite sizing)
    - src/ui/components/auto-ui/renderers/HandRenderer.vue lines 548-596 (card-back-small / card-image / card-sprite)
    - src/ui/components/auto-ui/renderers/DeckRenderer.vue lines 250-260 (stack container)
    - .planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md Fixed-Pixel Inventory rows for HandRenderer/CardRenderer/DeckRenderer + IA-05 decision (--card-w clamp, ≥44px tap zone)
  </read_first>
  <behavior>
    - Each card surface width resolves to `var(--card-w)` where `--card-w: clamp(44px, 14cqw, 84px)`.
    - Each card surface height resolves to `calc(var(--card-w) * 1.4)` (preserves the 60:84 ≈ 1.4 ratio).
    - The minimum rendered width is ≥44px (the clamp floor) so the tap target meets the touch floor.
  </behavior>
  <action>
    In each of the three renderers, define `--card-w: clamp(44px, 14cqw, 84px)` on the renderer root (or the card element) and a `--card-h: calc(var(--card-w) * 1.4)`. Replace every `width:60px; min-width:45px; height:84px` triple (CardRenderer 506-508/526-528/537-539/548-550; HandRenderer 554-556/570-572/580-582) with `width:var(--card-w); height:var(--card-h)` and drop the now-redundant min-width (the clamp floor of 44px is the tap floor). Replace DeckRenderer's `width:60px; height:84px` stack container (256-257) with `width:var(--card-w); height:var(--card-h)`. Keep all other card styling (radius, shadow, card-back token from Phase 99) untouched. Tokens/calc only — no new hex.
    Create CardRenderer.fluid.test.ts asserting the card element style resolves width to `var(--card-w)` (assert the CSS/inline style references the token, not a 60px literal). Mirror an existing renderer test mount.
  </action>
  <verify>
    <automated>npm run test -- src/ui/components/auto-ui/renderers/CardRenderer.fluid.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "width:\s*60px|height:\s*84px" src/ui/components/auto-ui/renderers/CardRenderer.vue src/ui/components/auto-ui/renderers/HandRenderer.vue src/ui/components/auto-ui/renderers/DeckRenderer.vue` returns nothing
    - `grep -rc -- "--card-w" src/ui/components/auto-ui/renderers/CardRenderer.vue src/ui/components/auto-ui/renderers/HandRenderer.vue src/ui/components/auto-ui/renderers/DeckRenderer.vue` each ≥ 1
    - The clamp floor is 44px (`grep -r "clamp(44px" ...` matches in each file)
    - `npm run test -- src/ui/components/auto-ui/renderers/CardRenderer.fluid.test.ts` passes
  </acceptance_criteria>
  <done>All three card renderers size from a shared --card-w clamp with a ≥44px floor and a 1.4 aspect; fluid test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Dev host uses 100dvh</name>
  <files>src/cli/dev-host/DevHost.vue</files>
  <read_first>
    - src/cli/dev-host/DevHost.vue line 356 (height:100vh)
    - .planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md "Pitfall 8"
  </read_first>
  <action>
    Replace `height:100vh` (line 356) with a `100vh` fallback line immediately followed by `height:100dvh` (the Phase 97 pattern used in GameShell.vue:1483-1484). No other DevHost changes in this plan (DebugPanel reskin + dev chrome collapse are Phase 102).
  </action>
  <verify>
    <automated>grep -c "100dvh" src/cli/dev-host/DevHost.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "100dvh" src/cli/dev-host/DevHost.vue` ≥ 1
    - A `100vh` fallback line precedes the `100dvh` line on the same selector
  </acceptance_criteria>
  <done>The dev host fills the dynamic viewport with a 100vh fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | CSS-only renderer + dev-host changes |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-03-01 | n/a | card sizing | accept | cosmetic CSS; no inputs, no boundary crossed |
| T-100-03-SC | Tampering | npm/pip/cargo installs | accept | no package installs; CSS-only change |
</threat_model>

<verification>
- `npm run test` full suite green
- `npm run lint:css` green
- Phase 103 regression note: CardRenderer, HandRenderer, DeckRenderer changed (fluid card sizing).
</verification>

<success_criteria>
IA-05 (card subset): card surfaces use a shared --card-w clamp with a ≥44px tap floor; no 60px/84px fixed card sizes remain; dev host uses 100dvh; fluid test passes; lint:css green.
</success_criteria>

<output>
Create `.planning/phases/100-ia-responsive-wave-3/100-03-SUMMARY.md` when done. List CardRenderer/HandRenderer/DeckRenderer as the Phase 103 card-sizing regression surface.
</output>
