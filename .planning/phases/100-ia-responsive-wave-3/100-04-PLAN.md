---
phase: 100-ia-responsive-wave-3
plan: 04
type: execute
wave: 2
depends_on: ["100-01"]
files_modified:
  - src/ui/components/GameShell.vue
  - src/ui/components/GameHeader.vue
  - src/ui/components/ControlsMenu.vue
  - src/ui/components/auto-ui/ActionPanel.vue
  - src/ui/components/GameShell.ia.test.ts
autonomous: true
requirements: [IA-01]

must_haves:
  truths:
    - "In platform mode no standing BoardSmith <GameHeader> renders"
    - "A ⋯ controls menu in the sidebar .side-head holds Auto-end-turn, Undo, the zoom magnifier, New Game and Leave, and still emits the existing handleMenuItemClick bridge for leave/new-game"
    - "Connection status is a small corner dot driven by a real postMessage heartbeat receiver, not the hardcoded 'connected' badge"
    - "The showUndo toggle is gone; undo shows whenever canUndo is true"
  artifacts:
    - path: "src/ui/components/ControlsMenu.vue"
      provides: "The ⋯ popover controls menu (Auto toggle, Undo, zoom magnifier, New Game, Leave)"
      contains: "menuitem"
    - path: "src/ui/components/GameShell.vue"
      provides: "Header gate + heartbeat receiver + corner dot + ControlsMenu mount"
      contains: "platformMode"
  key_links:
    - from: "ControlsMenu Leave/New Game"
      to: "GameShell handleMenuItemClick"
      via: "menu-item-click emit (bridge preserved)"
      pattern: "handleMenuItemClick"
    - from: "GameShell heartbeat receiver"
      to: "connection corner dot"
      via: "postMessage heartbeat → connectionHealth ref → dot class"
      pattern: "heartbeat"
---

<objective>
Remove the standing header in platform mode and relocate its controls per the mockup (IA-01). Gate `<GameHeader>` behind `v-if="!platformMode"` (dev/standalone keeps it). Build a new `ControlsMenu.vue` ⋯ popover mounted in the sidebar `.side-head` carrying Auto-end-turn, Undo, the demoted zoom magnifier, New Game, and Leave — and keep emitting the existing `handleMenuItemClick` bridge so a future host can wire leave/new-game (do not strand them). Replace the hardcoded `'connected'` badge with a small corner dot driven by a real postMessage heartbeat receiver. Delete the `showUndo` toggle entirely; undo shows whenever `canUndo` is true.

Purpose: Board is the hero — no chrome bar over the board in platform mode, controls stay reachable, connection truth comes from a real signal (IA-01).
Output: ControlsMenu.vue (new) + edits to GameShell.vue, GameHeader.vue, ActionPanel.vue + extended GameShell.ia.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/100-ia-responsive-wave-3/100-CONTEXT.md
@.planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md
@planning/mockups/boardsmith-chrome.html
@.planning/phases/100-ia-responsive-wave-3/100-01-SUMMARY.md

<interfaces>
- GameShell.vue: platformMode ref (138); zoomLevel ref (213); showUndo ref (215); autoEndTurn ref (214); handleMenuItemClick(id) (1240) — emits 'leave'/'new-game' and calls leaveGame() locally; the platformMessageHandler (≈618-694) is where a heartbeat case is added; isOriginAllowed already gates inbound messages.
- GameHeader.vue: emits update:zoom (33-37), update:autoEndTurn (76), update:showUndo (84); .connection-badge at 91, 144-153.
- ActionPanel.vue (src/ui/components/auto-ui/ActionPanel.vue): showUndo prop (60); undo button v-if `canUndo && showUndo !== false` (796).
- Mockup ⋯ menu: boardsmith-chrome.html lines 245-266 (.menu popover CSS), 459-505 (menu items incl Auto end turn, Undo, Fit board zoom, New game, Leave game).
- HamburgerMenu.vue stays untouched/tombstoned (Open Question 2 — do NOT delete it here).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ControlsMenu popover + mount in side-head; gate GameHeader by !platformMode</name>
  <files>src/ui/components/ControlsMenu.vue, src/ui/components/GameShell.vue</files>
  <read_first>
    - src/ui/components/GameShell.vue lines 1326-1336 (GameHeader usage) + the new .side-head from 100-01
    - src/ui/components/GameHeader.vue lines 56-93 (header-center controls that move)
    - planning/mockups/boardsmith-chrome.html lines 239-266, 459-505 (.menubtn, .menu popover, menu items)
    - src/ui/components/HamburgerMenu.vue (existing popover/menu pattern to mirror for a11y attributes)
  </read_first>
  <action>
    Create src/ui/components/ControlsMenu.vue: a `.menubtn` trigger (aria-haspopup="menu", aria-expanded) plus a `.menu` popover (role="menu") matching the mockup's items: an Auto-end-turn toggle (role menuitemcheckbox; v-model:autoEndTurn), an Undo last action item (emits 'undo'), a "Fit board" zoom magnifier (range input bound v-model:zoom — labelled an accessibility magnifier, NOT the fit strategy), a New game item (emits menu-item-click 'new-game'), and a Leave game item (emits menu-item-click 'leave'). Props: autoEndTurn, zoom, canUndo; emits update:autoEndTurn, update:zoom, undo, menu-item-click. Open/close on click + Escape + outside-click (mirror HamburgerMenu); full dialog focus-trap is deferred to Phase 101 — wire click/Escape only. Use var(--bsg-*) tokens only, no hex.
    In GameShell.vue: change `<GameHeader ... />` to `v-if="!platformMode"`. Mount `<ControlsMenu>` inside the `.side-head` placeholder created in 100-01, wiring `v-model:auto-end-turn="autoEndTurn"`, `v-model:zoom="zoomLevel"`, `:can-undo="canUndo && !isViewingHistory"`, `@undo="handleUndo"`, `@menu-item-click="handleMenuItemClick"`. Keep the `.sw-btn` host button placeholder.
  </action>
  <verify>
    <automated>npm run test -- src/ui/components/GameShell.theme.test.ts && npm run lint:css</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/components/ControlsMenu.vue exists, exports a component with role="menu" popover containing Auto-end-turn, Undo, zoom magnifier, New game, Leave game
    - GameShell.vue `<GameHeader` line carries `v-if="!platformMode"`
    - `<ControlsMenu` is mounted in `.side-head` and binds `@menu-item-click="handleMenuItemClick"`
    - `npm run lint:css` exits 0
  </acceptance_criteria>
  <done>The header is gone in platform mode and a working ⋯ controls menu lives above the players, still emitting the leave/new-game bridge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Heartbeat receiver + corner connection dot</name>
  <files>src/ui/components/GameShell.vue, src/ui/components/GameShell.ia.test.ts</files>
  <read_first>
    - src/ui/components/GameShell.vue lines 618-694 (platformMessageHandler + isOriginAllowed gate) and 1331 (the `platformMode ? 'connected' : connectionStatus` hardcode)
    - .planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md "Security Domain" (validate inbound payload) + IA-01 decision (heartbeat dot)
  </read_first>
  <behavior>
    - A `connectionHealth` ref starts 'connecting'; a heartbeat postMessage (source 'shufflewick', type 'heartbeat') flips it to 'connected' and resets a staleness timer.
    - If no heartbeat arrives within the timeout window, connectionHealth becomes 'stale'/'disconnected'.
    - A small corner dot element renders with a class bound to connectionHealth (connected/stale), replacing the hardcoded 'connected' string.
    - Malformed heartbeat payloads (wrong shape) do not change connectionHealth.
  </behavior>
  <action>
    Add a `connectionHealth = ref<'connecting'|'connected'|'stale'>('connecting')` and a heartbeat handler in the platformMessageHandler: when `data.type === 'heartbeat'` (already past isOriginAllowed), set connectionHealth='connected' and (re)arm a timeout (e.g. setTimeout/clearTimeout) that sets 'stale' after a window (~10s). Validate the message shape before acting (ignore if not an object / wrong source). Clear the timer on unmount. Render a small `<span class="conn-dot" :class="connectionHealth">` corner dot in the platform-mode shell (absolute corner of `.boardregion` or `.app`); in non-platform mode keep using the existing connectionStatus via GameHeader. Remove the hardcoded `platformMode ? 'connected' : ...` reliance for the dot (the GameHeader badge path stays for dev/standalone).
    Extend GameShell.ia.test.ts: assert (a) GameHeader is absent when platformMode=true; (b) the corner dot renders; (c) dispatching a valid heartbeat message sets connectionHealth='connected' and the dot class updates; (d) a malformed heartbeat is ignored.
    Tokens only — dot colors from var(--bsg-ok)/var(--bsg-warn) etc., no hex.
  </action>
  <verify>
    <automated>npm run test -- src/ui/components/GameShell.ia.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "heartbeat" src/ui/components/GameShell.vue` ≥ 1 and `grep -c "connectionHealth" src/ui/components/GameShell.vue` ≥ 1
    - `grep -E "'connected'" src/ui/components/GameShell.vue` no longer appears as a hardcoded connection-status value fed to the dot (the GameHeader dev path may still pass connectionStatus)
    - GameShell.ia.test.ts asserts header-absent-in-platformMode, dot-renders, heartbeat-updates-health, malformed-heartbeat-ignored — all pass
  </acceptance_criteria>
  <done>Connection truth comes from a validated heartbeat and shows as a corner dot; tests cover the header gate + heartbeat.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the showUndo toggle; undo follows canUndo</name>
  <files>src/ui/components/GameShell.vue, src/ui/components/GameHeader.vue, src/ui/components/auto-ui/ActionPanel.vue</files>
  <read_first>
    - src/ui/components/GameShell.vue line 215 (showUndo ref), 1334 (v-model:show-undo on GameHeader), 1437 (:show-undo on ActionPanel)
    - src/ui/components/GameHeader.vue lines 21, 28, 80-86 (showUndo prop/emit/toggle)
    - src/ui/components/auto-ui/ActionPanel.vue lines 60, 794-796 (showUndo prop + undo v-if)
    - .planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md "Open Question 3" recommendation
  </read_first>
  <action>
    Remove the `showUndo` ref (GameShell.vue:215) and its bindings: drop `v-model:show-undo` on GameHeader (1334) and `:show-undo` on ActionPanel (1437). In GameHeader.vue remove the showUndo prop, the update:showUndo emit, and the showUndo toggle `<label>` (lines 80-86). In ActionPanel.vue remove the showUndo prop (60) and change the undo button v-if (796) from `canUndo && showUndo !== false` to `canUndo`. No backward-compat shim (No Backward Compatibility rule). Verify no remaining references to showUndo anywhere in src/.
  </action>
  <verify>
    <automated>! grep -rn "showUndo\|show-undo" src/ui/ && npm run test -- src/ui/components/GameShell.ia.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "showUndo\|show-undo" src/ui/` returns nothing
    - ActionPanel undo button v-if is exactly `canUndo` (no showUndo clause)
    - GameShell.ia.test.ts still passes
  </acceptance_criteria>
  <done>The showUndo escape hatch is removed across shell, header, and action panel; undo visibility follows canUndo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| host iframe → GameShell | inbound postMessage (init, game_state, server_response, heartbeat) — origin-checked by isOriginAllowed |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-04-01 | Spoofing | forged heartbeat message | mitigate | isOriginAllowed gate (existing) + validate message is an object with the expected source/type before updating connectionHealth |
| T-100-04-02 | Tampering | malformed heartbeat payload | mitigate | shape-check before acting; ignore otherwise (covered by test) |
| T-100-04-03 | Repudiation | Leave/New Game stranded if host never wires them | accept | bridge still emitted AND handleMenuItemClick calls leaveGame() locally — controls never stranded |
| T-100-04-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan |
</threat_model>

<verification>
- `npm run test` full suite green
- `npm run lint:css` green
- Bridge end-to-end: ControlsMenu Leave/New Game still routes through handleMenuItemClick (host contract preserved — highest-risk item #4)
</verification>

<success_criteria>
IA-01 satisfied: no standing header in platform mode; ⋯ controls menu holds the relocated controls and preserves the leave/new-game bridge; connection shows as a heartbeat-driven corner dot; showUndo removed; tests + lint green.
</success_criteria>

<output>
Create `.planning/phases/100-ia-responsive-wave-3/100-04-SUMMARY.md` when done. Note the postMessage contract change (heartbeat inbound + bridge preserved) for Phase 103 cross-repo verification.
</output>
