---
phase: 100-ia-responsive-wave-3
plan: 05
type: execute
wave: 3
depends_on: ["100-04"]
files_modified:
  - src/ui/components/GameShell.vue
  - src/ui/components/PlayersPanel.vue
  - src/ui/components/GameHistory.vue
autonomous: true
requirements: [IA-06]

must_haves:
  truths:
    - "Layout reflows across compact/medium/wide tiers on the shared scale (640/768/1024/1440) instead of the lone 768px snap"
    - "Sidebar width is clamp(220px,22vw,320px), collapsible to the --bsg-rail rail; the board stage centers with a max-width at ≥1440"
    - "On phones the board fills the viewport, the sidebar defaults to the rail and expands as an overlay over the board only (with a scrim), and the players panel collapses to a one-line seat strip that carries the turn status"
    - "History becomes an on-demand bottom sheet on phones instead of a standing max-height:40vh aside"
    - "Each player is identified by color + shape + letter token, and the active player shows a natural turn-status sentence"
  artifacts:
    - path: "src/ui/components/GameShell.vue"
      provides: "Responsive tiers + rail state + phone overlay/scrim CSS"
      contains: "container"
    - path: "src/ui/components/PlayersPanel.vue"
      provides: "Color+shape+letter token, turn-status sentence, one-line seat-strip mode"
      contains: "seat-strip"
    - path: "src/ui/components/GameHistory.vue"
      provides: "On-demand bottom-sheet mode for phones"
      contains: "sheet"
  key_links:
    - from: "GameShell rail state"
      to: ".sidebar / .scrim classes"
      via: "sidebarRail ref + class binding"
      pattern: "sidebarRail"
    - from: "PlayersPanel seat strip"
      to: "currentPlayerSeat / active player"
      via: "turn-status sentence"
      pattern: "isPlayerActive"
---

<objective>
Replace the single 768px breakpoint with real compact/medium/wide responsive tiers on the shared scale (IA-06). Sidebar becomes `clamp(220px,22vw,320px)`, collapsible to the `--bsg-rail` rail; the board stage centers with a max-width at ≥1440. On phones the board fills the viewport, the sidebar defaults to the rail and expands as an overlay over the board only (scrim-dismissed), the players panel collapses to a one-line seat strip carrying the turn status, and History becomes an on-demand bottom sheet (dropping the standing `max-height:40vh` aside). Add the `@media (orientation:landscape) and (max-height:600px)` row-layout branch. Give PlayersPanel the color+shape+letter player token and the active-player turn-status sentence.

Purpose: The layout works on real devices and the turn status is always legible at every breakpoint (IA-06, completing IA-02's per-breakpoint guarantee).
Output: GameShell.vue responsive CSS + rail state, PlayersPanel.vue token+seat-strip, GameHistory.vue bottom-sheet mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/100-ia-responsive-wave-3/100-CONTEXT.md
@.planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md
@planning/mockups/boardsmith-chrome.html
@.planning/phases/100-ia-responsive-wave-3/100-01-SUMMARY.md
@.planning/phases/100-ia-responsive-wave-3/100-04-SUMMARY.md

<interfaces>
- GameShell.vue: the baseline shell CSS from 100-01 (.stage/.sidebar/.boardregion/.actionbar) + the leftover `@media (min-width:768px)` block this plan replaces; historyCollapsed ref (211).
- PlayersPanel.vue: props players[{seat,name,color}], playerSeat, currentPlayerSeat, awaitingPlayerSeats; isPlayerActive(seat) (27-31); existing .player-card/.current/.turn-indicator-dot.
- GameHistory.vue: v-model:collapsed; existing GameHistory collapse pattern reusable for the sheet.
- Mockup rail/overlay/scrim: boardsmith-chrome.html lines 68-71, 155-211, 270-278 + the token shape set (.sh-circle/.sh-square/... lines 125-139) and .turn sentence (219-226).
- Shared breakpoint scale: 640 / 768 / 1024 / 1440. Prefer @container on the boardregion for renderer reflow where practical; @media for the shell chrome.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Responsive tiers, sidebar rail, phone overlay + scrim, landscape branch</name>
  <files>src/ui/components/GameShell.vue</files>
  <read_first>
    - src/ui/components/GameShell.vue: the 100-01 baseline shell CSS + the residual `@media (min-width:768px)` block (was ≈1575) and the mobile sidebar `max-height:40vh` (was ≈1571) / `width:280px` (was ≈1586)
    - planning/mockups/boardsmith-chrome.html lines 68-71, 155-163, 270-278 (rail, overlay, scrim, reduced-motion)
    - .planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md "Pattern 4/5" + IA-06 decision + "Key CSS Values Being Replaced"
  </read_first>
  <action>
    Add a `sidebarRail = ref<boolean>(false)` (default expanded; default true under a compact match in onMounted) plus a toggle (the `.side-edge` button) and bind a `rail` class on `.sidebar` / the shell root. Add a `.scrim` element inside `.stage` that, when the sidebar is expanded on phones, click-collapses to the rail (`@click="sidebarRail = true"`).
    CSS: replace the lone `@media (min-width:768px)` block and the mobile `max-height:40vh` / `width:280px` rules. Desktop sidebar `width:clamp(220px,22vw,320px)`; rail mode `.sidebar.rail { width:var(--bsg-rail) }` hiding text rows (mirror mockup `body.rail` rules). At `@media (min-width:1440px)` center the `.stage`/board with a `max-width` and auto margins. Compact tier `@media (max-width:639px)`: board fills viewport; `.sidebar:not(.rail)` becomes `position:absolute; inset:0 auto 0 0; z-index:50; width:min(86vw,var(--bsg-side)); box-shadow:var(--bsg-shadow)` overlaying the board only (it sits inside `.stage`, never over the `.actionbar`); scrim becomes visible/interactive. Add `@media (orientation:landscape) and (max-height:600px)` switching to the row layout so the board is not crushed. Use 100dvh + env(safe-area-inset-*). Tokens/rgba/color-mix only — no hex.
  </action>
  <verify>
    <automated>npm run lint:css && npm run test -- src/ui/components/GameShell.ia.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "min-width:\s*1024px|min-width:\s*1440px|max-width:\s*639px|max-width:\s*640px" src/ui/components/GameShell.vue` ≥ 2 (multiple tiers, not a lone 768)
    - `grep -E "max-height:\s*40vh" src/ui/components/GameShell.vue` returns nothing
    - `grep -c "sidebarRail" src/ui/components/GameShell.vue` ≥ 1 and a `.scrim` element exists inside `.stage`
    - sidebar width uses `clamp(220px, 22vw, 320px)`; rail mode uses `var(--bsg-rail)`
    - an `orientation:landscape` + `max-height:600px` media branch exists
    - `npm run lint:css` exits 0
  </acceptance_criteria>
  <done>The shell reflows across compact/medium/wide tiers with a rail-collapsible sidebar, phone board-only overlay + scrim, centered wide stage, and a landscape branch.</done>
</task>

<task type="auto">
  <name>Task 2: PlayersPanel color+shape+letter token, turn-status sentence, one-line seat strip</name>
  <files>src/ui/components/PlayersPanel.vue</files>
  <read_first>
    - src/ui/components/PlayersPanel.vue (full — 122 lines)
    - planning/mockups/boardsmith-chrome.html lines 125-139 (.tok shape set), 171-196 (.player rows + rail), 219-226 (.turn sentence)
    - .planning/phases/100-ia-responsive-wave-3/100-CONTEXT.md IA-02 reconciliation (turn status lives in players panel + seat strip)
  </read_first>
  <action>
    Add a player identity token: color (player.color) + a deterministic shape (map seat index → the mockup's letter-friendly shape set sh-circle/sh-square/sh-hexagon/sh-octagon/sh-diamond/sh-pentagon/sh-shield/sh-plus via clip-path) + the player's initial letter centered. Render it in each `.player-card` (replacing the bare color swatch as the primary identity). For the active player (isPlayerActive), render a natural turn-status sentence — e.g. "{name} is playing…" and for the local player ("(You)") an affirmative variant — sourced from name + currentPlayerSeat/awaitingPlayerSeats already passed in (no "your turn" wording per mockup intent).
    Add a `seatStrip?: boolean` prop (or a `:class` mode) that renders a compact one-line seat strip variant: the token row inline with the active player's status sentence, for phone use. Keep the existing breathe dot honoring prefers-reduced-motion. Tokens only — no hex; shapes via clip-path, colors via the player.color CSS var.
  </action>
  <verify>
    <automated>npm run lint:css && npm run test -- src/ui/components 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PlayersPanel renders a color+shape+letter token per player (clip-path shape set present)
    - The active player shows a turn-status sentence (not the literal "your turn")
    - A one-line seat-strip mode exists (prop/class) for compact layout
    - `npm run lint:css` exits 0
  </acceptance_criteria>
  <done>Players are identified by color+shape+letter, the active player has a readable turn-status sentence, and a phone seat-strip mode carries that status on one line.</done>
</task>

<task type="auto">
  <name>Task 3: GameHistory on-demand bottom sheet for phones</name>
  <files>src/ui/components/GameHistory.vue, src/ui/components/GameShell.vue</files>
  <read_first>
    - src/ui/components/GameHistory.vue (full — collapse pattern, v-model:collapsed)
    - src/ui/components/GameShell.vue: where GameHistory is mounted in the sidebar .side-scroll (from 100-01) and historyCollapsed ref (211)
    - planning/mockups/boardsmith-chrome.html lines 208-211, 346-353 (log section, collapsible)
  </read_first>
  <action>
    Add a `sheet?: boolean` mode (or responsive class) to GameHistory so that on compact viewports it renders as an on-demand bottom sheet (a toggle pill that slides a panel up from the bottom of `.stage`, dismissible) instead of a standing aside. Reuse GameHistory's existing collapse mechanism for open/close. In GameShell, bind the sheet mode under the compact tier (drive from the same compact matchMedia / a `historySheet` ref) so the standing history is replaced on phones; on medium/wide it stays the inline sidebar history. The sheet overlays within `.stage` (never the actionbar). Tokens only — no hex; full dialog semantics/focus-trap are Phase 101.
  </action>
  <verify>
    <automated>npm run lint:css && npm run test -- src/ui/components 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - GameHistory supports a bottom-sheet mode (prop/class) reusing its collapse pattern
    - On compact width the history is the on-demand sheet, not a standing max-height:40vh aside
    - The sheet renders inside `.stage` and does not cover the `.actionbar`
    - `npm run lint:css` exits 0
  </acceptance_criteria>
  <done>History is an on-demand bottom sheet on phones and the inline sidebar history on larger tiers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Responsive CSS + component presentation; no inbound data path changed |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-05-01 | Spoofing | phone sidebar overlay / scrim covering controls | mitigate | overlay + scrim live inside .stage; .actionbar is a sibling and stays reachable; scrim click collapses to rail |
| T-100-05-02 | Tampering | seat-strip identity (color/shape/letter) collision | accept | shape+letter disambiguates same-color/same-initial players (mockup collision-note rationale) |
| T-100-05-SC | Tampering | npm/pip/cargo installs | accept | no package installs; CSS/template only |
</threat_model>

<verification>
- `npm run test` full suite green
- `npm run lint:css` green
- Manual (Phase 103): resize across 639/768/1024/1440 + landscape phone — board fits, sidebar rails, seat strip shows turn status, history sheet opens on demand
- Phase 103 regression note: PlayersPanel + GameHistory presentation changed
</verification>

<success_criteria>
IA-06 satisfied: real compact/medium/wide tiers with rail-collapsible sidebar, phone board-fill + seat strip carrying turn status + on-demand history sheet, centered wide stage, landscape branch; PlayersPanel token+sentence complete; tests + lint green.
</success_criteria>

<output>
Create `.planning/phases/100-ia-responsive-wave-3/100-05-SUMMARY.md` when done. Note PlayersPanel + GameHistory + GameShell responsive CSS as Phase 103 regression surface.
</output>
