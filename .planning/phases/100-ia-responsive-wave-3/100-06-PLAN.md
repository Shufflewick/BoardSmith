---
phase: 100-ia-responsive-wave-3
plan: 06
type: execute
wave: 4
depends_on: ["100-05"]
files_modified:
  - src/ui/components/GameOverCard.vue
  - src/ui/components/GameShell.vue
  - src/ui/components/auto-ui/AutoUI.vue
  - src/ui/components/GameOverCard.test.ts
autonomous: true
requirements: [IA-07]

must_haves:
  truths:
    - "When flowState.complete is true a result card overlays the board behind a Slate scrim instead of the dead-end 'Game Over!' banner"
    - "The result card names winners from the captured winnerSeats × players and shows per-player result info"
    - "The card offers Rematch and New Game as primary Slate actions, routed through the existing bridge"
    - "winnerSeats is captured (validated as number[]) from the game_state postMessage instead of being discarded"
    - "The final board stays visible behind the scrim (AutoUI no longer replaces the board with a banner)"
  artifacts:
    - path: "src/ui/components/GameOverCard.vue"
      provides: "Slate result card: scrim + winners + Rematch/New Game"
      contains: "game-over"
    - path: "src/ui/components/GameShell.vue"
      provides: "winnerSeats capture + GameOverCard mount in boardregion"
      contains: "winnerSeats"
  key_links:
    - from: "GameShell game_state handler"
      to: "winnerSeats ref"
      via: "validated Array<number> capture"
      pattern: "winnerSeats"
    - from: "GameOverCard actions"
      to: "handleMenuItemClick / restart bridge"
      via: "new-game / rematch emit"
      pattern: "new-game"
---

<objective>
Replace the AutoUI dead-end "Game Over!" banner with a proper Slate result card (IA-07). When `flowState.complete` is true, a `GameOverCard.vue` overlays the board behind a Slate scrim (`rgba(18,20,23,.66)` dark / light equivalent), names the winners from a newly-captured `winnerSeats` list crossed with `players`, shows per-player result info, and offers Rematch + New Game as primary Slate actions wired through the existing bridge. Capture `winnerSeats` (validated `number[]`) from the `game_state` postMessage that currently discards it; degrade gracefully to "Game Over" without names in dev WS mode where winners are not sent. The final board stays visible behind the scrim.

Purpose: Game Over is a real result moment with next actions, not a banner that hides the board (IA-07). Dialog semantics/focus-trap are Phase 101 — build the visual card + actions only here.
Output: GameOverCard.vue (new) + GameShell.vue winnerSeats capture/mount + AutoUI.vue banner removal + GameOverCard.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/100-ia-responsive-wave-3/100-CONTEXT.md
@.planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md
@planning/mockups/boardsmith-chrome.html
@.planning/phases/100-ia-responsive-wave-3/100-01-SUMMARY.md
@.planning/phases/100-ia-responsive-wave-3/100-05-SUMMARY.md

<interfaces>
- AutoUI.vue lines 34-52: `v-if="flowState?.complete"` renders `.game-complete` banner then v-else the AutoRenderer board. Target: always render the board; the result card lives in GameShell over the boardregion.
- GameShell.vue: game_state postMessage handler (≈670-692) captures only view.flowState + view.state; `data.winners: number[]` is discarded. players computed (441). flowState available at state.value?.flowState (FlowState.complete at engine flow/types.ts:244). handleMenuItemClick (1240) handles 'new-game'/'leave'; handleRestartGame exists for dev restart.
- Player scores are NOT a standard engine field — show player attributes from the players array, not a hardcoded .score (RESEARCH "Winner Data Gap").
- Scrim positioned INSIDE .boardregion (absolute, not fixed) so it cannot cover browser UI or the .actionbar (RESEARCH Security/Pitfall).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GameOverCard component (scrim + winners + actions)</name>
  <files>src/ui/components/GameOverCard.vue, src/ui/components/GameOverCard.test.ts</files>
  <read_first>
    - planning/mockups/boardsmith-chrome.html lines 234-238 (.btn.primary/.ghost), 24-53 (token palette incl scrim-equivalent surfaces)
    - .planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md "Pattern 6" (Game Over card with scrim) + "Winner Data Gap"
    - src/ui/components/PlayersPanel.vue (player token treatment to reuse for winner identity, from 100-05)
  </read_first>
  <behavior>
    - Given winnerSeats=[1] and players=[{seat:0,name:'A'},{seat:1,name:'B'}], the card names B as winner.
    - Given winnerSeats=[] (dev degrade), the card renders "Game Over" with no winner names and still shows the actions.
    - The card emits 'rematch' and 'new-game' when those buttons are clicked.
  </behavior>
  <action>
    Create src/ui/components/GameOverCard.vue. Props: `winnerSeats: number[]`, `players: {seat,name,color}[]`. Emits: 'rematch', 'new-game'. Render a `.game-over-scrim` (absolute inset:0; place-items center; background a Slate scrim via `color-mix`/`rgba` mapped to tokens — dark rgba(18,20,23,.66) equivalent / light rgba(243,242,239,.75) equivalent, selected by theme tokens, NOT raw hex) wrapping a `.game-over-card` with `role="dialog" aria-labelledby` (include role only; aria-modal + focus-trap are Phase 101), a title ("Game Over" when no winners; "{name} wins" / "{names} win" when winnerSeats present), a winners list reusing the player token identity, and a `.game-over-actions` row with a primary Slate "New Game" button (emit 'new-game') and a "Rematch" button (emit 'rematch'). Tokens/rgba/color-mix only — absolutely no hex literals (color-no-hex, empty ignore list; do not paste the mockup's #121417).
    Create GameOverCard.test.ts asserting the three behaviors above (winner naming, dev-degrade no-names, emits on click). Mirror an existing component test mount.
  </action>
  <verify>
    <automated>npm run test -- src/ui/components/GameOverCard.test.ts && npm run lint:css</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/components/GameOverCard.vue exists with a scrim + card + Rematch/New Game buttons and role="dialog"
    - GameOverCard.test.ts passes: winner-named, dev-degrade-no-names, emits-rematch/new-game
    - `grep -E "#[0-9a-fA-F]{3,6}" src/ui/components/GameOverCard.vue` returns nothing (no hex) and `npm run lint:css` exits 0
  </acceptance_criteria>
  <done>A reusable Slate result card renders winners (or degrades gracefully) and emits Rematch/New Game; its test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Capture winnerSeats + mount GameOverCard; remove AutoUI banner</name>
  <files>src/ui/components/GameShell.vue, src/ui/components/auto-ui/AutoUI.vue</files>
  <read_first>
    - src/ui/components/GameShell.vue lines 670-692 (game_state handler dropping data.winners) + the .boardregion from 100-01
    - src/ui/components/auto-ui/AutoUI.vue lines 34-71 (game-complete banner + v-else board + .game-complete CSS)
    - .planning/phases/100-ia-responsive-wave-3/100-RESEARCH.md "Pitfall 6" + "Security Domain" (validate winners is number[])
  </read_first>
  <action>
    In GameShell.vue add `const winnerSeats = ref<number[]>([])`. In the `game_state` handler (≈686), after the origin-checked branch, capture winners with validation: `winnerSeats.value = Array.isArray(data.winners) && data.winners.every((n: unknown) => typeof n === 'number') ? data.winners as number[] : []`. Mount `<GameOverCard v-if="state?.flowState?.complete" :winner-seats="winnerSeats" :players="players" @new-game="handleMenuItemClick('new-game')" @rematch="..." />` inside `.boardregion` (so the scrim overlays the board, which stays mounted behind it). Wire 'rematch' to the appropriate restart path (platform: handleMenuItemClick('new-game') or a rematch op if present; dev: handleRestartGame) — choose the existing path that restarts the same table; do NOT invent a new bridge message. In dev WS mode winnerSeats stays [] and the card degrades (acceptable per Open Question 1).
    In AutoUI.vue remove the `.game-complete` banner block (lines 37-42) and the `v-else` so the AutoRenderer board ALWAYS renders (the result card is now GameShell's job); delete the now-unused `.game-complete` CSS. Keep the `flowState` prop (other consumers may read it) but it no longer gates the board.
  </action>
  <verify>
    <automated>npm run test -- src/ui/components/GameShell.ia.test.ts src/ui/components/GameOverCard.test.ts && npm run lint:css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "winnerSeats" src/ui/components/GameShell.vue` ≥ 2 (ref + binding) and the capture validates `Array.isArray` + every-number
    - `<GameOverCard` is mounted inside `.boardregion` gated by `flowState?.complete`
    - `grep -c "game-complete" src/ui/components/auto-ui/AutoUI.vue` returns 0; the AutoRenderer board renders unconditionally
    - Add/extend a GameShell.ia.test.ts case asserting winnerSeats is captured from a valid game_state message and ignored when winners is malformed
    - `npm run lint:css` exits 0
  </acceptance_criteria>
  <done>winnerSeats is captured and validated, the result card overlays the still-visible board on completion, and AutoUI's dead-end banner is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| host iframe → GameShell | inbound game_state.winners now consumed — must be validated |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-06-01 | Tampering | malformed winners payload | mitigate | isOriginAllowed gate (existing) + Array.isArray + every(typeof === 'number') before assigning winnerSeats; else [] |
| T-100-06-02 | Spoofing | scrim overlay covering browser UI / actionbar | mitigate | scrim is absolute inside .boardregion (not fixed); cannot cover the actionbar (sibling of .stage) or browser chrome |
| T-100-06-03 | DoS | premature game-over from forged flowState.complete | accept | flowState already arrives via the same origin-checked state path used everywhere; no new trust surface |
| T-100-06-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan |
</threat_model>

<verification>
- `npm run test` full suite green (1002+ baseline plus new cases)
- `npm run lint:css` green
- Manual (Phase 103): finish a game in platform mode → result card names winner over the visible board, New Game/Rematch work; dev mode → "Game Over" degrades gracefully
- Phase 103 regression note: AutoUI no longer gates the board on completion
</verification>

<success_criteria>
IA-07 satisfied: a Slate result card (winners from validated winnerSeats, final board behind a token scrim, Rematch/New Game primary actions via the existing bridge) replaces the dead-end banner; winnerSeats captured + validated; AutoUI banner removed; tests + lint green.
</success_criteria>

<output>
Create `.planning/phases/100-ia-responsive-wave-3/100-06-SUMMARY.md` when done. Note: game_state.winners is now consumed (postMessage contract) for Phase 103 cross-repo verification.
</output>
