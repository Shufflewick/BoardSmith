---
phase: 101-accessibility-wcag-wave-4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/composables/useSelectable.ts
  - src/ui/composables/useSelectable.test.ts
  - src/ui/composables/useFocusTrap.ts
  - src/ui/composables/useFocusTrap.test.ts
  - src/ui/theme.ts
  - src/ui/theme.contrast.test.ts
autonomous: true
requirements: [A11Y-01, A11Y-07, A11Y-09]

must_haves:
  truths:
    - "A renderer can wire @click AND @keydown.enter/space to the same selection through one composable import"
    - "A grid renderer can move focus with Arrow/Home/End keys and activate a cell with Enter/Space through one composable import"
    - "A dialog can move focus in on open, trap Tab, optionally close on Escape, and restore focus on close through one composable import"
    - "Toast .warning text meets >= 4.5:1 contrast in BOTH light and dark themes"
  artifacts:
    - path: "src/ui/composables/useSelectable.ts"
      provides: "useSelectable (element mode) + useSelectableGrid (grid mode roving tabindex)"
      exports: ["useSelectable", "useSelectableGrid"]
      min_lines: 60
    - path: "src/ui/composables/useFocusTrap.ts"
      provides: "shared focus trap + Escape + restore + inert, with escapeToClose option"
      exports: ["useFocusTrap"]
      min_lines: 40
    - path: "src/ui/composables/useSelectable.test.ts"
      provides: "Enter/Space activation + roving tabindex nav tests"
    - path: "src/ui/composables/useFocusTrap.test.ts"
      provides: "focus move/trap/restore + escapeToClose tests"
  key_links:
    - from: "src/ui/composables/useSelectable.ts"
      to: "src/ui/composables/useBoardInteraction.ts"
      via: "triggerElementSelect wrapping"
      pattern: "triggerElementSelect"
    - from: "src/ui/theme.contrast.test.ts"
      to: "src/ui/theme.ts"
      via: "--bsg-warn token contrast assertion"
      pattern: "bsg-warn"
---

<objective>
Build the three shared foundations every later plan consumes, so the keyboard/dialog/contrast behavior is defined in exactly ONE place each and divergence is impossible by construction.

Purpose: A11Y-01 (keyboard) and A11Y-07 (dialogs) are architectural — they must be single-sourced composables before any renderer or dialog is touched. The Phase-99 carry-over Toast `.warning` contrast bug (3.37:1 light) is fixed at the token layer here so the contrast test is green before the rest of the wave lands.
Output: `useSelectable.ts` (element + grid modes), `useFocusTrap.ts`, their unit tests, the `theme.ts` `--bsg-warn` light fix, and the extended `theme.contrast.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md
@.planning/phases/101-accessibility-wcag-wave-4/101-CONTEXT.md

<interfaces>
triggerElementSelect signature [VERIFIED src/ui/composables/useBoardInteraction.ts:128]:
```typescript
triggerElementSelect: (element: { id?: number; name?: string; notation?: string }) => void;
```
Injected via tryUseBoardInteraction() which returns null outside GameShell (custom-UI parity).

Canonical roving-tabindex JS: planning/mockups/boardsmith-chrome.html lines 569-618.
Contrast helpers: src/ui/theme.contrast.test.ts — contrastRatio() at line 84, extractHex() at line 54.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useSelectable.ts (element + grid modes) + unit tests</name>
  <files>src/ui/composables/useSelectable.ts, src/ui/composables/useSelectable.test.ts</files>
  <read_first>
    - src/ui/composables/useBoardInteraction.ts (triggerElementSelect signature at :128, tryUseBoardInteraction)
    - planning/mockups/boardsmith-chrome.html lines 569-618 (canonical roving-tabindex implementation)
    - .planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md "Pattern 1" and "Pattern 2" (lines 198-276)
    - src/ui/components/auto-ui/renderers/CardRenderer.vue:301-325 (the handleClick pattern being centralized)
  </read_first>
  <behavior>
    - Element mode: calling the returned activate handler with Enter or Space (and a click) invokes triggerElementSelect exactly once with the identity object; no-op when not selectable or when disabled.
    - Element mode attrs expose role="button", tabindex "0" when selectable else "-1".
    - Grid mode: ArrowRight/Left move the cursor +/-1 (clamped), ArrowDown/Up move +/-COLS, Home goes to row start (cur - cur%COLS), End to row end (cur - cur%COLS + COLS-1); Enter/Space activates the current cell via triggerElementSelect.
    - Grid mode: only the current cell carries tabindex 0; all others tabindex -1 (single tab stop).
  </behavior>
  <action>
    Create `src/ui/composables/useSelectable.ts` exporting BOTH `useSelectable` (element mode) and `useSelectableGrid` (grid mode) from the single file (resolves research Open Question 2). `useSelectable(identity, boardInteraction, isActionSelectable, isDisabled)` returns `{ handleClick, handleKeydown, attrs }` where handleKeydown calls preventDefault and activates on `Enter`/` ` only, and the body wraps the EXISTING `triggerElementSelect` — it MUST NOT re-implement disabled/drop-target logic (that lives in triggerElementSelect already). `attrs` is a computed exposing `role: 'button'`, `tabindex` ('0' when isActionSelectable else '-1'), and `aria-disabled` (true|undefined). `useSelectableGrid(cells, cols, getIdentity, boardInteraction)` returns `{ currentIdx, focusCell, handleGridKeydown }` implementing the mockup's roving-tabindex math exactly (Arrow/Home/End/Enter/Space) and calling triggerElementSelect on activation. Keep all selection business logic in triggerElementSelect — this composable is a thin keyboard+ARIA wrapper only. Write `useSelectable.test.ts` first (RED) covering the behaviors above; mock boardInteraction with a vi.fn triggerElementSelect.
  </action>
  <verify>
    <automated>npx vitest run src/ui/composables/useSelectable.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function useSelectable" src/ui/composables/useSelectable.ts` returns 1
    - `grep -c "export function useSelectableGrid" src/ui/composables/useSelectable.ts` returns 1
    - useSelectable.test.ts asserts triggerElementSelect called once on Enter and once on Space, zero times when isDisabled
    - useSelectable.test.ts asserts ArrowRight/ArrowDown/Home/End move currentIdx per the mockup math
    - `npx vitest run src/ui/composables/useSelectable.test.ts` exits 0
  </acceptance_criteria>
  <done>useSelectable.ts exports both modes, wraps triggerElementSelect without duplicating selection logic, and its test passes covering keyboard activation + roving nav.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useFocusTrap.ts (trap + Escape + restore + inert) + unit tests</name>
  <files>src/ui/composables/useFocusTrap.ts, src/ui/composables/useFocusTrap.test.ts</files>
  <read_first>
    - .planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md "Pattern 3" (lines 278-307) and A11Y-07 change map (lines 495-512)
    - src/ui/components/GameOverCard.vue (current role="dialog" aria-modal="false" markup)
    - src/ui/components/HamburgerMenu.test.ts (jsdom test harness pattern to mirror)
  </read_first>
  <behavior>
    - open(): saves document.activeElement, then on nextTick focuses the first focusable element inside the dialog ref.
    - Tab from the last focusable wraps to the first; Shift+Tab from the first wraps to the last (focus stays inside).
    - Escape calls the close callback ONLY when escapeToClose is true (default true); when escapeToClose is false Escape does nothing.
    - close(): restores focus to the previously-focused element.
    - While open, inert is applied to sibling top-level nodes and removed on close.
  </behavior>
  <action>
    Create `src/ui/composables/useFocusTrap.ts` exporting `useFocusTrap(dialogRef, options?)` where `options.escapeToClose` defaults to true and an `onClose` callback is accepted. Implement `open()` (save activeElement, nextTick focus first focusable), `handleKeydown(e)` (Tab cycle within the focusable set; Escape -> onClose only when escapeToClose), and `close()` (restore saved focus). Apply the `inert` attribute to all sibling top-level children of the dialog's portal/host while open and remove it on close (research Pitfall 3 — aria-modal alone does not stop Tab). Use a shared getFocusable() helper. Write `useFocusTrap.test.ts` first (RED) in a `// @vitest-environment jsdom` file covering the behaviors above, including the escapeToClose:false case GameOverCard will use.
  </action>
  <verify>
    <automated>npx vitest run src/ui/composables/useFocusTrap.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "escapeToClose" src/ui/composables/useFocusTrap.ts` returns >= 1
    - useFocusTrap.test.ts asserts focus moves to first focusable on open and restores on close
    - useFocusTrap.test.ts asserts Escape closes when escapeToClose true and does nothing when false
    - `npx vitest run src/ui/composables/useFocusTrap.test.ts` exits 0
  </acceptance_criteria>
  <done>useFocusTrap.ts implements move/trap/Escape(optional)/restore/inert; test green including escapeToClose:false.</done>
</task>

<task type="auto">
  <name>Task 3: Fix --bsg-warn light contrast in theme.ts + extend contrast test</name>
  <files>src/ui/theme.ts, src/ui/theme.contrast.test.ts</files>
  <read_first>
    - src/ui/theme.ts (light-mode --bsg-warn value ~:86 and --bsg-accent-ink; dark --bsg-warn; --bsg-ink-2 :50/:78, --bsg-ink-3 :51/:79, --bsg-surface)
    - src/ui/theme.contrast.test.ts (contrastRatio at :84, extractHex at :54, "Both-theme WCAG contrast" describe at :125)
    - .planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md A11Y-09 contrast block (lines 568-571)
  </read_first>
  <action>
    In `src/ui/theme.ts`, darken the LIGHT-mode `--bsg-warn` value (currently ~#b5832a, 3.37:1 on white) to reach >= 4.5:1 against `--bsg-accent-ink` for small text — target approximately #8c6318 or darker; keep it Slate-warm, introduce NO new token. Leave the dark-mode warn as-is unless the test proves it fails. In `src/ui/theme.contrast.test.ts`, extend the existing "Both-theme WCAG contrast" describe block (:125) with three assertions using extractHex()+contrastRatio(): (1) Toast warning `--bsg-warn` vs `--bsg-accent-ink` >= 4.5 in BOTH themes; (2) muted text `--bsg-ink-2` vs `--bsg-surface` >= 4.5 both themes; (3) `--bsg-ink-3` vs `--bsg-surface` >= 3.0 both themes (non-text floor). Do not duplicate the contrast formula — reuse the existing helpers.
  </action>
  <verify>
    <automated>npx vitest run src/ui/theme.contrast.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/theme.contrast.test.ts` exits 0 with the 3 new assertions present
    - `grep -c "bsg-warn" src/ui/theme.contrast.test.ts` returns >= 1
    - contrastRatio(warn, accentInk) >= 4.5 in both light and dark theme token sets
    - `npm run lint:css` exits 0 (theme.ts exempt, but no .vue hex introduced)
  </acceptance_criteria>
  <done>Light-mode --bsg-warn >= 4.5:1; contrast test extended for warn + both muted inks; suite green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/ui/composables/useSelectable.test.ts src/ui/composables/useFocusTrap.test.ts src/ui/theme.contrast.test.ts` all green.
- `grep -rn "triggerElementSelect" src/ui/composables/useSelectable.ts` confirms the composable wraps the existing entry point (no duplicated selection logic).
- `npm run lint:css` exits 0.
</verification>

<success_criteria>
- Both composables exist with the exported surfaces and pass their tests.
- Toast `.warning` contrast >= 4.5:1 in both themes, proven by the extended contrast test.
- No new npm dependency added; no hex literal introduced into any .vue file.
</success_criteria>

<output>
Create `.planning/phases/101-accessibility-wcag-wave-4/101-01-SUMMARY.md` when done.
</output>
