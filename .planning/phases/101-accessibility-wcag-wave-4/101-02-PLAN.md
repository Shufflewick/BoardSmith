---
phase: 101-accessibility-wcag-wave-4
plan: 02
type: execute
wave: 2
depends_on: [101-01]
files_modified:
  - src/ui/components/auto-ui/renderers/CardRenderer.vue
  - src/ui/components/auto-ui/renderers/PieceRenderer.vue
  - src/ui/components/auto-ui/renderers/DeckRenderer.vue
  - src/ui/components/auto-ui/renderers/CardRenderer.a11y.test.ts
autonomous: true
requirements: [A11Y-01, A11Y-04]

must_haves:
  truths:
    - "A keyboard user can Tab to a selectable card/piece/deck and activate it with Enter or Space"
    - "Each card/piece/deck exposes aria-label + aria-selected/-disabled from the same booleans that drive its CSS"
    - "Clicking still works; drag still works (these renderers are not the drag path being regression-tested, but click parity is preserved)"
  artifacts:
    - path: "src/ui/components/auto-ui/renderers/CardRenderer.vue"
      provides: "useSelectable-driven card with role=button, tabindex, aria-label/state"
      contains: "useSelectable"
    - path: "src/ui/components/auto-ui/renderers/CardRenderer.a11y.test.ts"
      provides: "jsdom assertions for role/tabindex/aria on selectable card"
  key_links:
    - from: "src/ui/components/auto-ui/renderers/CardRenderer.vue"
      to: "src/ui/composables/useSelectable.ts"
      via: "useSelectable import + handler binding"
      pattern: "useSelectable"
---

<objective>
Migrate the three element renderers CardRenderer, PieceRenderer, DeckRenderer from copy-pasted mouse-only `handleClick` to the shared `useSelectable()` composable, and bind ARIA name/state from the same booleans as the CSS.

Purpose: A11Y-01 (keyboard) + A11Y-04 (semantic names/state) for these renderers. The old `handleClick` body is DELETED, not refactored — the composable is the only wiring point so divergence is impossible.
Output: Three migrated renderers + a CardRenderer jsdom a11y test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md
@src/ui/composables/useSelectable.ts

<interfaces>
useSelectable(identity, boardInteraction, isActionSelectable, isDisabled) -> { handleClick, handleKeydown, attrs }
attrs computed: { role:'button', tabindex:'0'|'-1', 'aria-disabled': true|undefined }
Renderer anchors [VERIFIED 101-RESEARCH.md "Concrete File Map"]:
  CardRenderer.vue  — handleClick:301, @click:349, isSelectable:77, isSelected:78, isDropTarget:134, isDisabled:143
  PieceRenderer.vue — handleClick:92,  @click:140, isSelected:67, isDisabled:81
  DeckRenderer.vue  — handleClick:102, @click:125, isSelectable:46, isSelected:47, isDisabled:77
aria-label format (spec, locked): "e5, white knight, selectable" / "selected" / "drop target" / "unavailable".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate CardRenderer + jsdom a11y test</name>
  <files>src/ui/components/auto-ui/renderers/CardRenderer.vue, src/ui/components/auto-ui/renderers/CardRenderer.a11y.test.ts</files>
  <read_first>
    - src/ui/components/auto-ui/renderers/CardRenderer.vue (handleClick:301-325, @click:349, isSelectable:77, isSelected:78, isDropTarget:134, isDisabled:143)
    - src/ui/composables/useSelectable.ts (element-mode return shape)
    - src/ui/components/auto-ui/renderers/CardRenderer.fluid.test.ts (existing jsdom mount harness to mirror)
  </read_first>
  <behavior>
    - When isSelectable is true the root .card-container mounts with role="button" and tabindex="0".
    - Pressing Enter or Space on the card calls triggerElementSelect once (drop-target activation included via the same path).
    - aria-selected reflects isSelected; aria-disabled reflects isDisabled; aria-label is the spec string built from notation/name + state.
  </behavior>
  <action>
    DELETE the `handleClick` function (CardRenderer.vue:301-325). Import and call `useSelectable(elementIdentity, boardInteraction, isActionSelectable, isDisabled)`; bind `@click="handleClick"` and `@keydown="handleKeydown"` plus `v-bind="attrs"` on the root `.card-container` (replacing the bare `@click` at :349). Wire the already-computed `isDropTarget` set so Enter/click activation reaches drop targets (the shared triggerElementSelect already handles this — just ensure selectable/drop-target cards get tabindex 0). Add a computed `ariaLabel` producing the spec string ("<notation/name>, <occupant?>, selectable|selected|drop target|unavailable") and bind `:aria-label`, `:aria-selected="isSelected || undefined"`, `:aria-disabled="isDisabled || undefined"` — all derived from the SAME booleans (isSelectable/isSelected/isDropTarget/isDisabled) that already drive the CSS classes. Write `CardRenderer.a11y.test.ts` (`// @vitest-environment jsdom`) first asserting the behaviors above.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/auto-ui/renderers/CardRenderer.a11y.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "handleClick" src/ui/components/auto-ui/renderers/CardRenderer.vue` returns 0
    - `grep -c "useSelectable" src/ui/components/auto-ui/renderers/CardRenderer.vue` returns 1
    - CardRenderer.a11y.test.ts: selectable card has role="button" and tabindex="0"
    - CardRenderer.a11y.test.ts: keydown Enter triggers exactly one triggerElementSelect call
    - `npx vitest run src/ui/components/auto-ui/renderers/CardRenderer.a11y.test.ts` exits 0
  </acceptance_criteria>
  <done>CardRenderer has zero handleClick, uses useSelectable, exposes role/tabindex/aria-label/-selected/-disabled, jsdom test green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate PieceRenderer (+ parametrized jsdom keyboard assertion)</name>
  <files>src/ui/components/auto-ui/renderers/PieceRenderer.vue, src/ui/components/auto-ui/renderers/CardRenderer.a11y.test.ts</files>
  <read_first>
    - src/ui/components/auto-ui/renderers/PieceRenderer.vue (handleClick:92-95, @click:140, isSelected:67, isDisabled:81)
    - src/ui/components/auto-ui/renderers/CardRenderer.a11y.test.ts (the shared renderer a11y test from Task 1 — add a parametrized case)
    - src/ui/composables/useSelectable.ts
  </read_first>
  <action>
    DELETE `handleClick` (:92-95). Wire `useSelectable` exactly as in Task 1: bind `@click`, `@keydown`, `v-bind="attrs"` on the root element (replacing @click at :140). Add a computed `ariaLabel` from the piece name + state and bind `:aria-label`, `:aria-selected="isSelected || undefined"`, `:aria-disabled="isDisabled || undefined"` from the same booleans as the CSS. No fallback values — if the piece has no name, build the label from its notation/id. Then EXTEND `CardRenderer.a11y.test.ts` with a parametrized case mounting PieceRenderer (jsdom): assert a selectable piece has role="button" + tabindex="0" and that a keydown Enter fires exactly one triggerElementSelect (closes warning-3 sampling gap — behavioral, not grep-only).
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/auto-ui/renderers/CardRenderer.a11y.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "handleClick" src/ui/components/auto-ui/renderers/PieceRenderer.vue` returns 0
    - `grep -c "useSelectable" src/ui/components/auto-ui/renderers/PieceRenderer.vue` returns 1
    - `grep -c "aria-label" src/ui/components/auto-ui/renderers/PieceRenderer.vue` returns >= 1
    - CardRenderer.a11y.test.ts: PieceRenderer case — selectable piece has role="button" + tabindex="0"; keydown Enter triggers exactly one triggerElementSelect
    - `npx vitest run src/ui/components/auto-ui/renderers/CardRenderer.a11y.test.ts` exits 0
  </acceptance_criteria>
  <done>PieceRenderer migrated to useSelectable with ARIA name/state; no handleClick remains; behavioral jsdom case green.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate DeckRenderer (+ parametrized jsdom keyboard assertion)</name>
  <files>src/ui/components/auto-ui/renderers/DeckRenderer.vue, src/ui/components/auto-ui/renderers/CardRenderer.a11y.test.ts</files>
  <read_first>
    - src/ui/components/auto-ui/renderers/DeckRenderer.vue (handleClick:102-106, @click:125, isSelectable:46, isSelected:47, isDisabled:77)
    - src/ui/components/auto-ui/renderers/CardRenderer.a11y.test.ts (shared renderer a11y test — add a parametrized case)
    - src/ui/composables/useSelectable.ts
  </read_first>
  <action>
    DELETE `handleClick` (:102-106). Wire `useSelectable` (bind `@click`, `@keydown`, `v-bind="attrs"` on the root, replacing @click at :125). Add a computed `ariaLabel` (e.g. "<deck name>, N cards, selectable") and bind `:aria-label`, `:aria-selected`, `:aria-disabled` from the same booleans (isSelectable/isSelected/isDisabled) driving the CSS. Then EXTEND `CardRenderer.a11y.test.ts` with a parametrized case mounting DeckRenderer (jsdom): assert a selectable deck has role="button" + tabindex="0" and keydown Enter fires exactly one triggerElementSelect (closes warning-3 sampling gap).
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/auto-ui/renderers/CardRenderer.a11y.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "handleClick" src/ui/components/auto-ui/renderers/DeckRenderer.vue` returns 0
    - `grep -c "useSelectable" src/ui/components/auto-ui/renderers/DeckRenderer.vue` returns 1
    - `grep -c "aria-label" src/ui/components/auto-ui/renderers/DeckRenderer.vue` returns >= 1
    - CardRenderer.a11y.test.ts: DeckRenderer case — selectable deck has role="button" + tabindex="0"; keydown Enter triggers exactly one triggerElementSelect
    - `npx vitest run src/ui/components/auto-ui/renderers/CardRenderer.a11y.test.ts` exits 0
  </acceptance_criteria>
  <done>DeckRenderer migrated to useSelectable with ARIA name/state; no handleClick remains; behavioral jsdom case green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/ui/components/auto-ui/renderers/` green (existing fluid tests + new a11y test).
- `for f in CardRenderer PieceRenderer DeckRenderer; do grep -c handleClick src/ui/components/auto-ui/renderers/$f.vue; done` all 0.
- `npm run lint:css` exits 0 (no hex literals introduced).
</verification>

<success_criteria>
- All three renderers use the shared composable; zero `handleClick` remains in them.
- ARIA name/state present and derived from the same booleans as CSS.
- CardRenderer jsdom a11y test green.
</success_criteria>

<output>
Create `.planning/phases/101-accessibility-wcag-wave-4/101-02-SUMMARY.md` when done.
</output>
