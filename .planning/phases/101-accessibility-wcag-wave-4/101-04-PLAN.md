---
phase: 101-accessibility-wcag-wave-4
plan: 04
type: execute
wave: 2
depends_on: [101-01]
files_modified:
  - src/ui/components/auto-ui/renderers/GridBoardRenderer.vue
  - src/ui/components/auto-ui/renderers/HexBoardRenderer.vue
  - src/ui/components/auto-ui/renderers/GridBoardRenderer.a11y.test.ts
  - src/ui/components/auto-ui/renderers/HexBoardRenderer.a11y.test.ts
autonomous: true
requirements: [A11Y-01, A11Y-04]

must_haves:
  truths:
    - "The grid board is a single tab stop; Arrow/Home/End move focus between cells via roving tabindex; Enter/Space selects the focused cell"
    - "The grid board container is role=grid with aria-label 'Game board, COLS by ROWS'; each cell is role=gridcell with a per-cell aria-label"
    - "Hex cells are keyboard-reachable role=gridcell groups; the hex SVG container carries a group/grid role and per-cell aria-label"
    - "Drag still works on both boards (verified in plan 101-11)"
  artifacts:
    - path: "src/ui/components/auto-ui/renderers/GridBoardRenderer.vue"
      provides: "useSelectableGrid roving tabindex + role=grid/gridcell + aria-label"
      contains: "useSelectableGrid"
    - path: "src/ui/components/auto-ui/renderers/GridBoardRenderer.a11y.test.ts"
      provides: "jsdom assertions for grid roles + arrow-key navigation + Enter activation"
  key_links:
    - from: "src/ui/components/auto-ui/renderers/GridBoardRenderer.vue"
      to: "src/ui/composables/useSelectable.ts"
      via: "useSelectableGrid import"
      pattern: "useSelectableGrid"
    - from: "src/ui/components/auto-ui/renderers/HexBoardRenderer.vue"
      to: "src/ui/composables/useSelectable.ts"
      via: "useSelectableGrid import"
      pattern: "useSelectableGrid"
---

<objective>
Migrate the two grid renderers to the shared `useSelectableGrid()` roving-tabindex mode: GridBoardRenderer (HTML cells) and HexBoardRenderer (SVG `<g>` cells). Add `role="grid"`/`role="gridcell"`, per-cell `aria-label`, and arrow-key navigation, mirroring the canonical mockup.

Purpose: A11Y-01 (the hardest part — 2D keyboard nav) + A11Y-04 for board renderers. The old `handleCellClick`/`handleHexClick` bodies are DELETED.
Output: Two migrated grid renderers + a GridBoardRenderer jsdom a11y test. Hex `<g tabindex=0>` ships first with a manual-VoiceOver verification note (research Open Question 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md
@src/ui/composables/useSelectable.ts

<interfaces>
useSelectableGrid(cells, cols, getIdentity, boardInteraction) -> { currentIdx, focusCell, handleGridKeydown }
Anchors [VERIFIED 101-RESEARCH.md]:
  GridBoardRenderer.vue — handleCellClick:145-166, @click.stop:213, board container .board-grid, cells .grid-cell (per-cell isDropTarget:155, isDisabled:140); cols/rows from gridResult
  HexBoardRenderer.vue  — handleHexClick:159-163, <g @click>:216, <g class="hex-cell-group">, piece <title> names :235-243
Roving-tabindex math: planning/mockups/boardsmith-chrome.html:569-618.
ARIA: role="grid" aria-label="Game board, COLS by ROWS"; role="gridcell" + "c8, selectable" per cell. Hex SVG -> role="group"/"grid" + per-cell <g role="gridcell" tabindex="0" aria-label>.
Note: GridBoardRenderer.vue:145 handleCellClick also has a passive selectElement branch for non-selectable named cells — preserve that branch's behavior under keyboard activation (only triggerElementSelect for selectable cells; passive select otherwise).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate GridBoardRenderer to grid mode + jsdom a11y test</name>
  <files>src/ui/components/auto-ui/renderers/GridBoardRenderer.vue, src/ui/components/auto-ui/renderers/GridBoardRenderer.a11y.test.ts</files>
  <read_first>
    - src/ui/components/auto-ui/renderers/GridBoardRenderer.vue (handleCellClick:145-166, @click.stop:213, .board-grid container, .grid-cell, isDropTarget:155, isDisabled:140)
    - src/ui/composables/useSelectable.ts (useSelectableGrid)
    - src/ui/components/auto-ui/renderers/GridBoardRenderer.fluid.test.ts (jsdom mount harness to mirror)
    - planning/mockups/boardsmith-chrome.html:569-618 (canonical roving-tabindex)
  </read_first>
  <behavior>
    - .board-grid renders role="grid" with aria-label "Game board, COLS by ROWS" derived from gridResult.cols/rows.
    - Each .grid-cell renders role="gridcell"; only the current roving cell has tabindex="0", others "-1".
    - ArrowRight/Left/Up/Down/Home/End move focus per the mockup math; Enter/Space on a selectable cell calls triggerElementSelect once; a non-selectable named cell uses the passive selectElement branch.
    - Each cell exposes aria-label ("c8, selectable"|"selected"|"drop target"|"unavailable") and aria-disabled from the same booleans as CSS.
  </behavior>
  <action>
    DELETE `handleCellClick` (:145-166). Wire `useSelectableGrid(cells, cols, cellIdentity, boardInteraction)`; put `role="grid"` + `:aria-label="\`Game board, ${cols} by ${rows}\`"` + `@keydown="handleGridKeydown"` on the `.board-grid` container; put `role="gridcell"`, `:tabindex="idx === currentIdx ? '0' : '-1'"`, `:aria-label`, `:aria-selected`, `:aria-disabled`, and `@click` (still selecting that cell) on each `.grid-cell` (replacing @click.stop at :213). Preserve the original selectable-vs-passive branch: selectable cells -> triggerElementSelect; non-selectable named cells -> passive selectElement. Write `GridBoardRenderer.a11y.test.ts` (`// @vitest-environment jsdom`) first asserting grid/gridcell roles, single tab stop, ArrowRight focus move, and Enter activation.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/auto-ui/renderers/GridBoardRenderer.a11y.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "handleCellClick" src/ui/components/auto-ui/renderers/GridBoardRenderer.vue` returns 0
    - `grep -c "useSelectableGrid" src/ui/components/auto-ui/renderers/GridBoardRenderer.vue` returns 1
    - `grep -c "role=\"grid\"" src/ui/components/auto-ui/renderers/GridBoardRenderer.vue` returns >= 1
    - `grep -c "role=\"gridcell\"" src/ui/components/auto-ui/renderers/GridBoardRenderer.vue` returns >= 1
    - a11y test: board has role="grid", cells role="gridcell", ArrowRight moves the tab stop, Enter triggers one select
    - `npx vitest run src/ui/components/auto-ui/renderers/GridBoardRenderer.a11y.test.ts` exits 0
  </acceptance_criteria>
  <done>GridBoardRenderer uses useSelectableGrid with role=grid/gridcell, roving tabindex, per-cell ARIA; passive branch preserved; a11y test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate HexBoardRenderer to grid mode (SVG g cells) + jsdom a11y test</name>
  <files>src/ui/components/auto-ui/renderers/HexBoardRenderer.vue, src/ui/components/auto-ui/renderers/HexBoardRenderer.a11y.test.ts</files>
  <read_first>
    - src/ui/components/auto-ui/renderers/HexBoardRenderer.vue (handleHexClick:159-163, <g @click>:216, hex-cell-group, piece <title>:235-243)
    - src/ui/composables/useSelectable.ts (useSelectableGrid)
    - src/ui/components/auto-ui/renderers/GridBoardRenderer.a11y.test.ts (Task 1 jsdom harness to mirror)
    - planning/mockups/boardsmith-chrome.html:569-618
    - .planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md Pitfall 1 (SVG focus, lines 607-611) and Open Question 3 (lines 799-802)
  </read_first>
  <action>
    DELETE `handleHexClick` (:159-163). Wire `useSelectableGrid` for the hex cells. Put `role="group"` (or role="grid" if the cells form a regular row/col index) + `:aria-label="displayLabel"` + `@keydown="handleGridKeydown"` on the SVG root; put `role="gridcell"`, `:tabindex="idx === currentIdx ? '0' : '-1'"`, `:aria-label` (cell notation + occupant + state), and `@click` on each `<g class="hex-cell-group">` (replacing @click at :216). Keep the existing `<title>` (:235-243) for SVG-native occupant names. Add a non-color owner glyph per cell so seat identity is not color-only. Add a code comment marking the hex `<g tabindex="0">` focus as needing a manual VoiceOver/Safari pass (research Open Question 3 — ship tabindex-on-g first; escalate to overlay buttons only if it fails verification). Then write `HexBoardRenderer.a11y.test.ts` (`// @vitest-environment jsdom`) giving automated regression surface for the highest-risk renderer: assert each `<g>` cell is role="gridcell" with a per-cell aria-label and exactly one cell has tabindex="0"; simulate ArrowRight keydown on the SVG root and assert the roving `currentIdx` advances (i.e. the tabindex="0" cell moves). Mark the suite/file with a comment: "partial — SVG focus in Safari/VoiceOver requires manual verification (research Open Q3)".
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/auto-ui/renderers/HexBoardRenderer.a11y.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "handleHexClick" src/ui/components/auto-ui/renderers/HexBoardRenderer.vue` returns 0
    - `grep -c "useSelectableGrid" src/ui/components/auto-ui/renderers/HexBoardRenderer.vue` returns 1
    - `grep -c "role=\"gridcell\"" src/ui/components/auto-ui/renderers/HexBoardRenderer.vue` returns >= 1
    - `grep -c "tabindex" src/ui/components/auto-ui/renderers/HexBoardRenderer.vue` returns >= 1
    - A comment flags the manual VoiceOver/Safari hex-focus verification
    - HexBoardRenderer.a11y.test.ts: each `<g>` cell is role="gridcell" with aria-label; exactly one cell has tabindex="0"; ArrowRight advances the roving tab stop
    - `npx vitest run src/ui/components/auto-ui/renderers/HexBoardRenderer.a11y.test.ts` exits 0
  </acceptance_criteria>
  <done>HexBoardRenderer uses useSelectableGrid on SVG <g> cells with role=gridcell, tabindex, per-cell ARIA + non-color owner glyph; manual-VO note present; jsdom roving-tabindex test green; no handleHexClick remains.</done>
</task>

</tasks>

<verification>
- `for f in GridBoardRenderer HexBoardRenderer; do grep -Ec "handleCellClick|handleHexClick" src/ui/components/auto-ui/renderers/$f.vue; done` all 0.
- `npx vitest run src/ui/components/auto-ui/renderers/` green (fluid + a11y tests).
- `npm run lint:css` exits 0.
- MANUAL (carry to verify-work): VoiceOver + Safari can land focus on hex cells and Enter activates (research Open Question 3).
</verification>

<success_criteria>
- Both grid renderers use useSelectableGrid; zero `handleCellClick`/`handleHexClick` remain.
- role=grid/gridcell + per-cell aria-label + roving tabindex present; GridBoardRenderer a11y test green.
</success_criteria>

<output>
Create `.planning/phases/101-accessibility-wcag-wave-4/101-04-SUMMARY.md` when done.
</output>
