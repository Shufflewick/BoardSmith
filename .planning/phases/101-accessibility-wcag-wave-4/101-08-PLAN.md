---
phase: 101-accessibility-wcag-wave-4
plan: 08
type: execute
wave: 2
depends_on: [101-01]
files_modified:
  - src/ui/components/HamburgerMenu.vue
  - src/ui/components/ControlsMenu.vue
  - src/ui/components/GameOverCard.vue
  - src/ui/components/HamburgerMenu.test.ts
  - src/ui/components/GameOverCard.test.ts
autonomous: true
requirements: [A11Y-07, A11Y-09]

must_haves:
  truths:
    - "Opening the hamburger drawer moves focus into it, traps Tab inside, closes on Escape, and restores focus to the toggle on close"
    - "The ControlsMenu popover traps focus and closes on Escape, restoring focus to its trigger"
    - "The Game Over overlay is aria-modal=true and traps focus; Escape does NOT close it (no dismiss action exists)"
    - "The hamburger button and the Controls ⋯ trigger meet a >= 44px target floor"
  artifacts:
    - path: "src/ui/components/HamburgerMenu.vue"
      provides: "role=dialog aria-modal + useFocusTrap lifecycle"
      contains: "useFocusTrap"
    - path: "src/ui/components/GameOverCard.vue"
      provides: "aria-modal=true + useFocusTrap(escapeToClose:false)"
      contains: "aria-modal=\"true\""
  key_links:
    - from: "src/ui/components/HamburgerMenu.vue"
      to: "src/ui/composables/useFocusTrap.ts"
      via: "useFocusTrap on open/close"
      pattern: "useFocusTrap"
    - from: "src/ui/components/GameOverCard.vue"
      to: "src/ui/composables/useFocusTrap.ts"
      via: "useFocusTrap escapeToClose:false"
      pattern: "escapeToClose"
---

<objective>
Give the three dialogs real modal semantics through the shared `useFocusTrap()` composable: HamburgerMenu drawer, ControlsMenu popover, GameOverCard overlay. Single composable -> identical behavior, no divergence.

Purpose: A11Y-07 (dialog semantics + focus lifecycle) + A11Y-09 (hamburger + ⋯ target floor).
Output: Three dialogs wired to useFocusTrap with correct aria-modal/role/Escape behavior + extended tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md
@src/ui/composables/useFocusTrap.ts

<interfaces>
useFocusTrap(dialogRef, { escapeToClose, onClose }) -> { open, close, handleKeydown }
Anchors [VERIFIED 101-RESEARCH.md A11Y-07 table, lines 499-512]:
  HamburgerMenu.vue:84 — <div v-if="isOpen" class="menu-drawer"> (no role/trap); hamburger-btn :147-148 (28x20px)
  ControlsMenu.vue:105 — <div v-if="isOpen" role="menu"> (no trap); ⋯ trigger :93 (~38x38)
  GameOverCard.vue:74 — <div class="game-over-scrim" aria-modal="false"> (flip to true; Escape must NOT close — research Open Question 1: escapeToClose:false)
Existing tests to EXTEND: HamburgerMenu.test.ts, GameOverCard.test.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HamburgerMenu dialog semantics + focus trap + 44px button</name>
  <files>src/ui/components/HamburgerMenu.vue, src/ui/components/HamburgerMenu.test.ts</files>
  <read_first>
    - src/ui/components/HamburgerMenu.vue (drawer :84, hamburger-btn :147-148)
    - src/ui/composables/useFocusTrap.ts
    - src/ui/components/HamburgerMenu.test.ts (existing jsdom harness)
  </read_first>
  <action>
    Add `role="dialog" aria-modal="true" aria-label="Game menu"` to the drawer (:84) and a template ref. Wire `useFocusTrap(drawerRef, { escapeToClose: true, onClose: closeMenu })`: call `open()` when the drawer opens (watch isOpen) and `close()` when it closes; bind `@keydown="handleKeydown"` on the drawer for Tab-trap + Escape. The composable applies `inert` to the rest while open. Add `min-width: 44px; min-height: 44px` to `.hamburger-btn` (:147-148). Extend HamburgerMenu.test.ts asserting `role="dialog"` + `aria-modal="true"` and that opening moves focus inside. Tokens only.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/HamburgerMenu.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "role=\"dialog\"" src/ui/components/HamburgerMenu.vue` returns >= 1
    - `grep -c "aria-modal=\"true\"" src/ui/components/HamburgerMenu.vue` returns >= 1
    - `grep -c "useFocusTrap" src/ui/components/HamburgerMenu.vue` returns 1
    - `.hamburger-btn` has min-width:44px and min-height:44px
    - HamburgerMenu.test.ts asserts role=dialog + aria-modal=true; `npx vitest run src/ui/components/HamburgerMenu.test.ts` exits 0
  </acceptance_criteria>
  <done>Hamburger drawer is a focus-trapped aria-modal dialog with a 44px toggle; test extended and green.</done>
</task>

<task type="auto">
  <name>Task 2: ControlsMenu focus trap + Escape + 44px trigger</name>
  <files>src/ui/components/ControlsMenu.vue</files>
  <read_first>
    - src/ui/components/ControlsMenu.vue (popover :105 role=menu, ⋯ trigger :93; defer comment :12)
    - src/ui/composables/useFocusTrap.ts
  </read_first>
  <action>
    Keep `role="menu"` on the popover (:105) per the mockup `#menu` semantics. Add a template ref and wire `useFocusTrap(menuRef, { escapeToClose: true, onClose: closeMenu })` — call open()/close() on the isOpen transition and bind `@keydown="handleKeydown"` so Tab stays inside and Escape closes, restoring focus to the ⋯ trigger. Add `min-height: 44px` (and min-width where needed) to the ⋯ trigger (:93). Remove the line-:12 comment that deferred this to Phase 101 once implemented. Tokens only.
  </action>
  <verify>
    <automated>grep -c "useFocusTrap" src/ui/components/ControlsMenu.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useFocusTrap" src/ui/components/ControlsMenu.vue` returns 1
    - `grep -c "role=\"menu\"" src/ui/components/ControlsMenu.vue` returns >= 1 (retained)
    - The ⋯ trigger rule includes `min-height: 44px`
    - `npm run lint:css` exits 0
  </acceptance_criteria>
  <done>ControlsMenu traps focus, closes on Escape, restores focus; ⋯ trigger meets 44px floor.</done>
</task>

<task type="auto">
  <name>Task 3: GameOverCard aria-modal=true + focus trap (Escape disabled)</name>
  <files>src/ui/components/GameOverCard.vue, src/ui/components/GameOverCard.test.ts</files>
  <read_first>
    - src/ui/components/GameOverCard.vue (scrim :74 aria-modal="false"; defer comment :12)
    - src/ui/composables/useFocusTrap.ts
    - src/ui/components/GameOverCard.test.ts (existing harness)
    - .planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md Open Question 1 (lines 789-792)
  </read_first>
  <action>
    Flip `aria-modal="false"` to `aria-modal="true"` (:74). Add a template ref and wire `useFocusTrap(cardRef, { escapeToClose: false })` — focus moves into the card on open and Tab stays trapped, but Escape does NOTHING (the game is over; there is no dismiss — research Open Question 1). The user leaves only via Rematch/New Game. Remove the line-:12 deferral comment. Extend GameOverCard.test.ts asserting `aria-modal="true"` and that focus is trapped (Escape does not close). Tokens only.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/GameOverCard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "aria-modal=\"true\"" src/ui/components/GameOverCard.vue` returns >= 1
    - `grep -c "aria-modal=\"false\"" src/ui/components/GameOverCard.vue` returns 0
    - `grep -c "escapeToClose: false" src/ui/components/GameOverCard.vue` returns 1
    - GameOverCard.test.ts asserts aria-modal=true; `npx vitest run src/ui/components/GameOverCard.test.ts` exits 0
  </acceptance_criteria>
  <done>GameOverCard is aria-modal=true with a focus trap that ignores Escape; test extended and green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/ui/components/HamburgerMenu.test.ts src/ui/components/GameOverCard.test.ts` green.
- All three dialogs import useFocusTrap exactly once each (no per-component hand-rolled trap).
- `npm run lint:css` exits 0.
</verification>

<success_criteria>
- All three dialogs share useFocusTrap with correct aria-modal/Escape semantics and focus restore.
- Hamburger + ⋯ triggers meet the 44px floor.
</success_criteria>

<output>
Create `.planning/phases/101-accessibility-wcag-wave-4/101-08-SUMMARY.md` when done.
</output>
