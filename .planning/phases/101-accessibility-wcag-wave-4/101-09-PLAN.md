---
phase: 101-accessibility-wcag-wave-4
plan: 09
type: execute
wave: 2
depends_on: [101-01]
files_modified:
  - src/ui/components/DebugPanel.vue
  - src/cli/dev-host/DevHost.vue
autonomous: true
requirements: [A11Y-06, A11Y-09]

must_haves:
  truths:
    - "Every icon-only dev control has an accessible name and a visible focus ring"
    - "Dev color swatches and dev controls meet at least the 24px target floor (44px where space allows)"
  artifacts:
    - path: "src/cli/dev-host/DevHost.vue"
      provides: "named + adequately-sized dev controls and color swatches"
      contains: "aria-label"
    - path: "src/ui/components/DebugPanel.vue"
      provides: "named debug toggle/controls with focus-visible reachability"
  key_links:
    - from: "src/cli/dev-host/DevHost.vue"
      to: "global :focus-visible ring"
      via: "named focusable controls"
      pattern: "aria-label"
---

<objective>
Bring the dev-server chrome (DevHost + DebugPanel) up to the wave's minimum a11y floor — accessible names, focus reachability, and target sizes ONLY. No reskin, no ARIA tabs, no D-shortcut gating (all deferred to Phase 102).

Purpose: A11Y-06 (focus reachability for dev controls) + A11Y-09 (names + target floor). Stays strictly inside the deferral boundary.
Output: Named dev controls + swatch/control target floors in DevHost and DebugPanel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md

<interfaces>
Anchors [VERIFIED 101-RESEARCH.md A11Y-09 targets, lines 558-566]:
  DevHost.vue — color swatches ~22x22px -> min 24x24px (44 preferred)
  DebugPanel.vue — toggle + controls need names + focus reachability
Scope guardrail (CONTEXT/RESEARCH): DebugPanel reskin, ARIA tabs, D-shortcut gating = Phase 102 — DO NOT do them here. Only names/focus/target-size.
All colors via var(--bsg-*) (lint:css). The global :focus-visible ring (plan 05) covers focus appearance once controls are real focusable buttons.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DevHost dev controls — names + swatch target floor</name>
  <files>src/cli/dev-host/DevHost.vue</files>
  <read_first>
    - src/cli/dev-host/DevHost.vue (color swatches ~22x22; icon-only dev controls)
    - .planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md A11Y-09 targets (lines 558-566)
  </read_first>
  <action>
    Give every icon-only DevHost control an `aria-label` and ensure it is a real focusable `<button>` (so the global :focus-visible ring applies); mark decorative glyphs `aria-hidden`. Bump color swatches to `min-width: 24px; min-height: 24px` (use 44px where layout allows). Do NOT collapse the chrome, add a seat switcher, or restyle beyond token compliance — those are Phase 102. Tokens only; no hex.
  </action>
  <verify>
    <automated>grep -c "aria-label" src/cli/dev-host/DevHost.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "aria-label" src/cli/dev-host/DevHost.vue` returns >= 1 (added names)
    - Swatch rule includes `min-width: 24px` (or larger) and `min-height: 24px`
    - `grep -cE "#[0-9a-fA-F]{3,6}" src/cli/dev-host/DevHost.vue` returns 0 (no new hex) — or unchanged if pre-existing literals are out of this task's lines
    - `npm run lint:css` exits 0
  </acceptance_criteria>
  <done>DevHost icon controls are named focusable buttons; swatches meet the 24px floor; no scope creep into Phase 102.</done>
</task>

<task type="auto">
  <name>Task 2: DebugPanel controls — names + focus reachability</name>
  <files>src/ui/components/DebugPanel.vue</files>
  <read_first>
    - src/ui/components/DebugPanel.vue (toggle + icon controls)
    - .planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md (DebugPanel scope: names/focus/target only)
  </read_first>
  <action>
    Ensure the DebugPanel toggle and its controls are real focusable `<button>`s with `aria-label`s (decorative glyphs `aria-hidden`) and meet the 24px target floor. Do NOT add the ARIA tabs pattern, reskin to Slate, or gate the `D` shortcut — those are explicitly Phase 102. Tokens only.
  </action>
  <verify>
    <automated>grep -c "aria-label" src/ui/components/DebugPanel.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "aria-label" src/ui/components/DebugPanel.vue` returns >= 1
    - No ARIA `role="tab"`/`role="tablist"` introduced (Phase 102 boundary respected): `grep -c "role=\"tab" src/ui/components/DebugPanel.vue` returns 0
    - `npm run lint:css` exits 0
  </acceptance_criteria>
  <done>DebugPanel controls are named and focus-reachable at the target floor; no Phase-102 work introduced.</done>
</task>

</tasks>

<verification>
- `npm test` stays green; `npm run lint:css` exits 0.
- `grep -c "role=\"tab" src/ui/components/DebugPanel.vue` == 0 (deferral boundary held).
</verification>

<success_criteria>
- Dev controls in both files are named, focusable, and meet the 24px floor — names/focus/size only, nothing from Phase 102.
</success_criteria>

<output>
Create `.planning/phases/101-accessibility-wcag-wave-4/101-09-SUMMARY.md` when done.
</output>
