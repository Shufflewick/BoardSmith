---
phase: 101-accessibility-wcag-wave-4
plan: 10
type: execute
wave: 3
depends_on: [101-02, 101-03, 101-04]
files_modified:
  - src/ui/components/auto-ui/renderers/CardRenderer.vue
  - src/ui/components/auto-ui/renderers/GridBoardRenderer.vue
  - src/ui/components/auto-ui/renderers/HexBoardRenderer.vue
  - src/ui/components/auto-ui/renderers/DeckRenderer.vue
  - src/ui/components/auto-ui/renderers/HandRenderer.vue
  - src/ui/components/auto-ui/renderers/PieceRenderer.vue
  - src/ui/components/auto-ui/renderers/SpaceRenderer.vue
  - src/ui/components/auto-ui/renderers/DieRenderer.vue
autonomous: true
requirements: [A11Y-08, A11Y-09]

must_haves:
  truths:
    - "Disabled board elements show a hatch pattern (not opacity-only) so disabled-ness is perceivable without relying on faintness"
    - "Every renderer pulse animation is silenced under prefers-reduced-motion"
  artifacts:
    - path: "src/ui/components/auto-ui/renderers/CardRenderer.vue"
      provides: "disabled hatch via repeating-linear-gradient(var(--bsg-cell)) instead of opacity:.35"
      contains: "repeating-linear-gradient"
  key_links:
    - from: "renderer .is-disabled CSS"
      to: "var(--bsg-cell)"
      via: "hatch pattern token"
      pattern: "repeating-linear-gradient"
---

<objective>
Sweep all 8 renderers' disabled states from `opacity:.35` to a tokenized hatch pattern (A11Y-09) and ensure each renderer's infinite pulse animation is silenced under reduced motion (A11Y-08). This is the CSS-only sweep that runs AFTER the renderer migration plans so it touches the same files without conflict.

Purpose: A11Y-09 (disabled = non-color cue, not just faint) + A11Y-08 (reduced-motion guards belt-and-suspenders to the global block).
Output: 8 renderers with hatch-based disabled state and motion-safe pulses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md

<interfaces>
Disabled opacity anchors [VERIFIED 101-RESEARCH.md A11Y-09 table, lines 543-554]:
  CardRenderer:481, GridBoardRenderer:382, HexBoardRenderer:341 (SVG), DeckRenderer:224, HandRenderer:446, PieceRenderer:253, SpaceRenderer:240, DieRenderer:164
Pulse animations [101-RESEARCH.md A11Y-08 table, lines 530-537]:
  CardRenderer:447 pulse-card 2s infinite; GridBoardRenderer:352 pulse-drop-target 1s infinite; HandRenderer:405 pulse-hand 2s infinite
Hatch token [VERIFIED theme.ts:44/:72]: var(--bsg-cell). Pattern: repeating-linear-gradient(45deg, var(--bsg-cell), var(--bsg-cell) 5px, transparent 5px, transparent 10px). SVG hex: opacity:.4 + fill pattern overlay (research line 549). lint:css color-no-hex on .vue — tokens only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Disabled hatch for the 6 element/HTML renderers</name>
  <files>src/ui/components/auto-ui/renderers/CardRenderer.vue, src/ui/components/auto-ui/renderers/DeckRenderer.vue, src/ui/components/auto-ui/renderers/HandRenderer.vue, src/ui/components/auto-ui/renderers/PieceRenderer.vue, src/ui/components/auto-ui/renderers/SpaceRenderer.vue, src/ui/components/auto-ui/renderers/DieRenderer.vue</files>
  <read_first>
    - The six renderers' .is-disabled rules: CardRenderer:481, DeckRenderer:224, HandRenderer:446, PieceRenderer:253, SpaceRenderer:240, DieRenderer:164
    - .planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md A11Y-09 (lines 543-554), Pitfall 4 (lines 625-629)
  </read_first>
  <action>
    In each of the six `.is-disabled` rules replace `opacity: .35` with a hatch pattern `background-image: repeating-linear-gradient(45deg, var(--bsg-cell), var(--bsg-cell) 5px, transparent 5px, transparent 10px);` plus a mild reduced opacity if needed for clarity. Disabled-ness must read as a PATTERN, not just faintness (it already carries `aria-disabled` from plans 02/03). All colors via `var(--bsg-*)` — never hex (Pitfall 4 would fail lint:css).
  </action>
  <verify>
    <automated>npm run lint:css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -lc "repeating-linear-gradient" src/ui/components/auto-ui/renderers/CardRenderer.vue` finds the pattern in each of the six files
    - `grep -c "opacity: .35" src/ui/components/auto-ui/renderers/CardRenderer.vue` returns 0 (and same for the other five)
    - `npm run lint:css` exits 0
  </acceptance_criteria>
  <done>All six element/HTML renderers use a tokenized hatch for disabled, no opacity:.35 remains.</done>
</task>

<task type="auto">
  <name>Task 2: Grid + Hex disabled hatch + reduced-motion pulse guards</name>
  <files>src/ui/components/auto-ui/renderers/GridBoardRenderer.vue, src/ui/components/auto-ui/renderers/HexBoardRenderer.vue, src/ui/components/auto-ui/renderers/CardRenderer.vue, src/ui/components/auto-ui/renderers/HandRenderer.vue</files>
  <read_first>
    - GridBoardRenderer.vue (.is-disabled :382, pulse-drop-target :352), HexBoardRenderer.vue (.hex-polygon.is-disabled :341 SVG)
    - CardRenderer.vue (pulse-card :447), HandRenderer.vue (pulse-hand :405)
    - .planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md A11Y-08 (lines 528-537)
  </read_first>
  <action>
    GridBoardRenderer `.grid-cell.is-disabled` (:382): replace opacity-only with the same `repeating-linear-gradient(var(--bsg-cell))` hatch. HexBoardRenderer `.hex-polygon.is-disabled` (:341): SVG can't use background hatch — use `opacity: .4` plus an SVG fill-pattern overlay (or token stroke) so disabled is still a non-color cue; keep `aria-disabled` on the `<g>`. For the three pulse keyframes (pulse-card :447, pulse-drop-target GridBoardRenderer:352, pulse-hand HandRenderer:405) wrap the `animation:` declarations in `@media (prefers-reduced-motion: no-preference)` (or add a `@media (prefers-reduced-motion: reduce) { animation: none }` guard) so they are motion-safe even though the global block already neutralizes duration. Tokens only.
  </action>
  <verify>
    <automated>npm run lint:css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "repeating-linear-gradient" src/ui/components/auto-ui/renderers/GridBoardRenderer.vue` returns >= 1
    - `grep -c "prefers-reduced-motion" src/ui/components/auto-ui/renderers/CardRenderer.vue` returns >= 1
    - `grep -c "prefers-reduced-motion" src/ui/components/auto-ui/renderers/GridBoardRenderer.vue` returns >= 1
    - `grep -c "prefers-reduced-motion" src/ui/components/auto-ui/renderers/HandRenderer.vue` returns >= 1
    - HexBoardRenderer disabled state uses a non-color cue (pattern/stroke), not opacity alone
    - `npm run lint:css` exits 0
  </acceptance_criteria>
  <done>Grid/Hex disabled use non-color cues; the three renderer pulses are motion-guarded.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/ui/components/auto-ui/renderers/` green (migration tests still pass — CSS-only changes).
- `for f in CardRenderer GridBoardRenderer HexBoardRenderer DeckRenderer HandRenderer PieceRenderer SpaceRenderer DieRenderer; do grep -c "opacity: .35" src/ui/components/auto-ui/renderers/$f.vue; done` all 0.
- `npm run lint:css` exits 0.
</verification>

<success_criteria>
- All 8 renderers signal disabled with a non-color cue (hatch/pattern), not opacity alone.
- Renderer pulses are silenced under reduced motion.
</success_criteria>

<output>
Create `.planning/phases/101-accessibility-wcag-wave-4/101-10-SUMMARY.md` when done.
</output>
