---
phase: 101-accessibility-wcag-wave-4
plan: 11
type: execute
wave: 3
depends_on: [101-02, 101-03, 101-04]
files_modified:
  - src/ui/components/auto-ui/renderers/useSelectable.divergence.test.ts
  - src/ui/components/auto-ui/renderers/drag-keyboard-parity.test.ts
autonomous: true
requirements: [A11Y-01, A11Y-02]

must_haves:
  truths:
    - "No renderer wires click/keydown to selection outside the shared composable — a test fails if any handleClick / handleCellClick / handleHexClick reappears"
    - "Two-step click/Enter selection works AND drag-to-select still works after the migration (drag is preserved as progressive enhancement)"
  artifacts:
    - path: "src/ui/components/auto-ui/renderers/useSelectable.divergence.test.ts"
      provides: "source-grep guard asserting the composable is the only selection-wiring point"
    - path: "src/ui/components/auto-ui/renderers/drag-keyboard-parity.test.ts"
      provides: "regression test that drag still triggers selection alongside keyboard"
  key_links:
    - from: "useSelectable.divergence.test.ts"
      to: "renderers/*.vue"
      via: "static source scan for forbidden handlers"
      pattern: "handleClick|handleCellClick|handleHexClick"
---

<objective>
Lock in the architectural invariant and prevent the highest-risk regression: a divergence guard that fails CI if any renderer reintroduces its own selection wiring, plus a drag regression test proving drag still works as progressive enhancement after the keyboard migration.

Purpose: Makes Critical finding #1 (keyboard everywhere via ONE composable) divergence-impossible going forward, and guarantees drag was not broken by the migration (A11Y-01). Also asserts the action-panel keyboard path stays operable (A11Y-02 regression coverage).
Output: Two test files that gate the renderer interaction contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md
@src/ui/composables/useDragDrop.ts

<interfaces>
Forbidden post-migration tokens in renderers (must be 0): handleClick, handleCellClick, handleHexClick, and `@click` bound directly to triggerElementSelect.
Drag harness references: src/ui/composables/useDragDropTargets.test.ts, src/ui/composables/interaction-integration.test.ts, src/ui/composables/useDragDrop.ts.
The 8 renderers live in src/ui/components/auto-ui/renderers/.
After plans 02/03/04, every renderer imports useSelectable / useSelectableGrid.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Divergence grep-guard test</name>
  <files>src/ui/components/auto-ui/renderers/useSelectable.divergence.test.ts</files>
  <read_first>
    - The 8 migrated renderers in src/ui/components/auto-ui/renderers/ (confirm each imports useSelectable/useSelectableGrid and has no handleClick)
    - .planning/phases/101-accessibility-wcag-wave-4/101-RESEARCH.md "Divergence risk" (lines 414-416)
  </read_first>
  <behavior>
    - The test reads each renderer .vue source and asserts it contains none of: a `function handleClick`, `handleCellClick`, `handleHexClick`, nor an `@click` bound directly to `triggerElementSelect`.
    - The test asserts each interactive renderer imports `useSelectable` or `useSelectableGrid` (the only permitted wiring point).
  </behavior>
  <action>
    Create `useSelectable.divergence.test.ts` that uses `fs.readFileSync` over the 8 renderer files (CardRenderer, PieceRenderer, DeckRenderer, HandRenderer, SpaceRenderer, DieRenderer, GridBoardRenderer, HexBoardRenderer). Assert zero matches for `/function handleClick|handleCellClick|handleHexClick/` and zero `@click="..triggerElementSelect"` direct bindings; assert each file matches `/useSelectable(Grid)?/`. This is the divergence-impossible guard the research demands — a future edit that re-adds a per-renderer handler fails this test. Keep the forbidden-token list and the file list as named arrays so the failure message names the offending file.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/auto-ui/renderers/useSelectable.divergence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The test enumerates all 8 renderer files and asserts zero forbidden handlers in each
    - The test asserts each renderer imports useSelectable or useSelectableGrid
    - `npx vitest run src/ui/components/auto-ui/renderers/useSelectable.divergence.test.ts` exits 0
    - Failure message names the offending renderer file (verified by temporarily — and reverting — a forbidden token, or by construction)
  </acceptance_criteria>
  <done>A passing source-grep guard makes renderer selection-wiring divergence impossible.</done>
</task>

<task type="auto">
  <name>Task 2: Drag-still-works regression + keyboard parity test</name>
  <files>src/ui/components/auto-ui/renderers/drag-keyboard-parity.test.ts</files>
  <read_first>
    - src/ui/composables/useDragDrop.ts and useDragDropTargets.test.ts (drag selection path + existing harness)
    - src/ui/composables/interaction-integration.test.ts (cross-layer interaction harness to mirror)
    - src/ui/composables/useSelectable.ts (keyboard path)
  </read_first>
  <behavior>
    - Keyboard path: Enter/Space on a selectable renderer element triggers selection (one triggerElementSelect call).
    - Drag path: a drag-drop onto a valid drop target still triggers the same selection/move — drag is NOT broken by the migration (progressive enhancement preserved).
    - Both paths funnel through the same useBoardInteraction selection entry, proving parity.
  </behavior>
  <action>
    Create `drag-keyboard-parity.test.ts` (`// @vitest-environment jsdom`) that, mirroring the existing interaction-integration harness, mounts a renderer (e.g. CardRenderer or GridBoardRenderer) inside a board-interaction context with a spied selection entry point and asserts: (1) keyboard Enter triggers selection; (2) a drag-drop interaction (via the useDragDrop path) onto a drop target still triggers selection. This regression-tests the CONTEXT requirement "drag still works" after keyboard became the primary path. Reuse existing drag test utilities — do not hand-roll a drag simulator if one exists in useDragDropTargets.test.ts.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/auto-ui/renderers/drag-keyboard-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The test asserts keyboard Enter/Space triggers selection
    - The test asserts a drag-drop onto a drop target still triggers selection (drag regression)
    - `npx vitest run src/ui/components/auto-ui/renderers/drag-keyboard-parity.test.ts` exits 0
  </acceptance_criteria>
  <done>Both keyboard and drag selection paths are proven working post-migration; drag regression covered.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/ui/components/auto-ui/renderers/` green including both new gate tests.
- `grep -rEc "function handleClick|handleCellClick|handleHexClick" src/ui/components/auto-ui/renderers/*.vue` sums to 0.
- `npm test` full suite green (>= 1066 baseline maintained).
</verification>

<success_criteria>
- Divergence guard passes and would fail on any reintroduced per-renderer handler.
- Drag and keyboard both proven to trigger selection — Critical finding #1 closed without breaking drag.
</success_criteria>

<output>
Create `.planning/phases/101-accessibility-wcag-wave-4/101-11-SUMMARY.md` when done.
</output>
