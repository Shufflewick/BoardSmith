---
phase: 102-material-dev-debug-wave-5
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/components/auto-ui/AutoRenderer.vue
  - src/ui/components/auto-ui/AutoRenderer.loading.test.ts
  - src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue
autonomous: true
requirements: [DEV-05]
must_haves:
  truths:
    - "While gameView is null the renderer shows an animated skeleton, not a bare 'Waiting…' line"
    - "If no game state arrives within the timeout, a Retry affordance appears"
    - "Under prefers-reduced-motion the skeleton is static (no pulsing animation)"
    - "Players see a friendly unsupported-layout message with no dev jargon"
    - "Only in dev builds does the unsupported panel show the extra console/Debug guidance block"
  artifacts:
    - path: "src/ui/components/auto-ui/AutoRenderer.vue"
      provides: "Loading skeleton with timeout→retry"
      contains: "skeleton"
    - path: "src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue"
      provides: "Player/dev split message"
      contains: "import.meta.env.DEV"
    - path: "src/ui/components/auto-ui/AutoRenderer.loading.test.ts"
      provides: "Coverage for skeleton + timeout→retry"
  key_links:
    - from: "AutoRenderer loading state"
      to: "retry affordance"
      via: "setTimeout → retryVisible ref"
      pattern: "setTimeout"
    - from: "UnsupportedTopologyPanel dev block"
      to: "build env"
      via: "import.meta.env.DEV gate"
      pattern: "import.meta.env.DEV"
---

<objective>
Give the auto-UI loading and unsupported-layout states a clear voice (DEV-05): replace the bare "Waiting for game state…" text in `AutoRenderer.vue` with an animated skeleton that escalates to a timeout→retry affordance, and split `UnsupportedTopologyPanel.vue` into a friendly player-facing message plus a dev-only guidance block that never reaches players.

Purpose: A spinner-forever or a raw diagnostic panel both read as broken; voiced states tell the user what is happening and what to do next.
Output: Skeleton+retry loading state, player/dev-split unsupported panel, one new test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-material-dev-debug-wave-5/102-CONTEXT.md
@.planning/phases/102-material-dev-debug-wave-5/102-RESEARCH.md

<interfaces>
AutoRenderer loading branch: AutoRenderer.vue lines 153-156 — `v-if="!gameView"` div with
`<p>Waiting for game state…</p>`. gameView comes from inject/props in this component.

DEV-CONTEXT SIGNAL (corrects RESEARCH assumption A5): GameShell provides 'platformRequest'
UNCONDITIONALLY (GameShell.vue:627), so injecting it is NOT a dev gate. The reliable
dev signal is `import.meta.env.DEV` (used as isDevBuild in GameShell.vue:150) — it is
statically dropped from production bundles. Use `import.meta.env.DEV` for the dev-only block.

UnsupportedTopologyPanel.vue is currently static (no props). Locked player-facing copy
(keep verbatim, do not paraphrase): heading "This layout cannot be auto-generated", body
"This game uses a layout that is outside the auto-UI's supported set (grid, hex, stack,
hand). Build a custom UI component for this game — see the custom UI guide." Per CLAUDE.md
the dev block must NOT contain stack traces, element IDs, attribute names, or internal paths.

Slate tokens: --bsg-surface, --bsg-line, --bsg-line-2, --bsg-ink-3, --bsg-field,
--bsg-r-sm, --bsg-warn, --bsg-accent, --bsg-dur-base.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Skeleton loading state with timeout→retry</name>
  <files>src/ui/components/auto-ui/AutoRenderer.vue, src/ui/components/auto-ui/AutoRenderer.loading.test.ts</files>
  <read_first>
    - src/ui/components/auto-ui/AutoRenderer.vue (loading branch lines 149-176; script setup top for where gameView is sourced and where to add refs; scoped style block starting line 177)
    - src/ui/components/auto-ui/renderer-registry.test.ts (reference for mounting AutoRenderer / providing inject deps in this repo's tests)
  </read_first>
  <behavior>
    - Test: when mounted with no gameView, the component renders a skeleton element (e.g. class "auto-renderer-skeleton") containing multiple placeholder bars, and does NOT render the old "Waiting for game state…" text.
    - Test: the Retry button is hidden initially and becomes visible after the timeout elapses (use vi.useFakeTimers + advanceTimersByTime past the threshold).
    - Test: clicking Retry resets the timed-out state (retry affordance hides again) and re-arms the timeout — assert retryVisible returns to false after click.
  </behavior>
  <action>
    In the `v-if="!gameView"` branch, replace the single `<p>` with a skeleton: a container div class "auto-renderer-skeleton" holding 3-4 placeholder bar divs of varying widths. Add an `aria-busy="true"` and an sr-only/visually-hidden text "Loading game…" for screen readers. Below the skeleton, conditionally render a Retry block (`v-if="loadTimedOut"`) with a short message ("Still waiting for the game host…") and a `<button>` Retry.

    In script setup add `const loadTimedOut = ref(false)` and a timer. On mount (or via a watcher that runs while gameView is null) start a setTimeout of 8000ms that sets loadTimedOut true; clear/cancel it when gameView becomes non-null or on unmount. The Retry handler sets loadTimedOut false, re-arms the timer, and emits a 'retry' event (so a parent can re-request state) — define the emit. Do not fabricate game state locally (no fallbacks per CLAUDE.md); Retry only re-requests.

    Style the skeleton bars with --bsg-field background and a subtle pulse keyframe (opacity/translate), gated so `@media (prefers-reduced-motion: reduce)` disables the animation (static bars). Use only Slate tokens; no raw hex (lint:css covers this file).

    Create AutoRenderer.loading.test.ts (@vitest-environment jsdom, @vue/test-utils mount, vi.useFakeTimers) covering the behaviors above.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/auto-ui/AutoRenderer.loading.test.ts && npm run lint:css</automated>
  </verify>
  <acceptance_criteria>
    - New test file passes; lint:css exits 0.
    - Source assertion: `grep -n "Waiting for game state" src/ui/components/auto-ui/AutoRenderer.vue` returns nothing.
    - Source assertion: `grep -n "loadTimedOut" src/ui/components/auto-ui/AutoRenderer.vue` and `grep -n "prefers-reduced-motion" src/ui/components/auto-ui/AutoRenderer.vue` both return hits.
  </acceptance_criteria>
  <done>Loading shows a reduced-motion-aware skeleton that escalates to a Retry button after 8s; Retry re-arms and emits; test green; lint:css green.</done>
</task>

<task type="auto">
  <name>Task 2: Split UnsupportedTopologyPanel into player message + dev-only guidance</name>
  <files>src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue</files>
  <read_first>
    - src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue (full file, lines 1-55 — current single combined panel)
    - src/ui/components/GameShell.vue lines 150-178 (confirms import.meta.env.DEV is the dev-build signal)
  </read_first>
  <action>
    Keep the existing amber player-facing panel exactly as-is (verbatim heading + body copy — locked). Add a SECOND block rendered with `v-if="isDev"` below the player message, containing dev-only guidance: a short line such as "Dev: this topology has no auto-UI archetype — open the Debug panel (Ctrl/Cmd+D) to inspect the element tree, then implement a custom UI." Include a real link to the custom UI guide (use the canonical docs path/URL referenced elsewhere in the repo for the custom UI guide — search docs for the existing guide reference rather than inventing one). The dev block must NOT print element IDs, attribute names, stack traces, or internal file paths (CLAUDE.md: never leak implementation details).

    In script setup add `const isDev = import.meta.env.DEV` and expose it to the template. Do NOT inject 'platformRequest' as a dev indicator (it is always provided — RESEARCH A5 was wrong). Style the dev block with a quieter treatment distinct from the loud amber player panel (e.g. --bsg-surface background, --bsg-ink-3 text, --bsg-line border) so it is visibly a separate developer aside.
  </action>
  <verify>
    <automated>npm run lint:css && npx vitest run src/ui/components/auto-ui/archetype-selector.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - lint:css exits 0; existing archetype-selector test still green.
    - Source assertion: `grep -n "import.meta.env.DEV" src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue` returns a hit.
    - Source assertion: the verbatim player-facing heading "This layout cannot be auto-generated" and body copy are unchanged.
    - Source assertion: the dev block is gated by `v-if="isDev"` and contains a real guide link.
  </acceptance_criteria>
  <done>Players see only the friendly amber message; dev builds additionally show a quiet guidance aside with a real guide link and no leaked internals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → player | error/empty states are shown to untrusted end users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-04 | Information Disclosure | UnsupportedTopologyPanel dev block | mitigate | Gate dev guidance behind import.meta.env.DEV (statically dropped in prod) and exclude IDs/paths/traces |
| T-102-05 | Denial of Service | AutoRenderer loading | accept | Retry re-requests host state only; no local fabrication, no infinite resource use (single re-armed 8s timer) |
</threat_model>

<verification>
- `npx vitest run src/ui/components/auto-ui/AutoRenderer.loading.test.ts` green.
- `npm run lint:css` green.
- `npm run test` stays at baseline + 1 new file.
</verification>

<success_criteria>
DEV-05 satisfied: skeleton + timeout→retry loading; player/dev-split unsupported panel using import.meta.env.DEV; no leaked internals; lint:css green.
</success_criteria>

<output>
Create `.planning/phases/102-material-dev-debug-wave-5/102-02-SUMMARY.md` when done
</output>
