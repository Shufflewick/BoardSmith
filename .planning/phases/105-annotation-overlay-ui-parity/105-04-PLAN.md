---
phase: 105-annotation-overlay-ui-parity
plan: 04
type: execute
wave: 3
depends_on: ["105-01", "105-03"]
files_modified:
  - src/ui/components/GameShell.vue
  - src/ui/components/GameShell.tutorial.test.ts
autonomous: true
requirements: [TUT-01]

must_haves:
  truths:
    - "TutorialOverlay is mounted once in .boardregion and renders for both the slot (custom UI) and the dev UI-switcher (AutoUI) paths"
    - "MR-01 closed: the projected PlayerGameState.tutorial reaches useActionController in production wiring"
    - "suppressAutoFill goes live in production (no longer inert / test-only)"
  artifacts:
    - path: "src/ui/components/GameShell.vue"
      provides: "TutorialOverlay mount + tutorialStep computed threaded into useActionController"
      contains: "TutorialOverlay"
    - path: "src/ui/components/GameShell.tutorial.test.ts"
      provides: "Test proving the projected step reaches the controller (MR-01 closure guard)"
  key_links:
    - from: "GameShell useActionController options (~:390)"
      to: "state.value.state.tutorial"
      via: "tutorialStep = computed(() => state.value?.state?.tutorial)"
      pattern: "tutorialStep.*computed.*tutorial"
    - from: "GameShell .boardregion"
      to: "TutorialOverlay"
      via: "single mount inside #main, sibling of GameOverCard"
      pattern: "<TutorialOverlay"
---

<objective>
Wire the overlay and close MR-01 in GameShell — the production integration seam. Per
105-CONTEXT Area 3 (and the Phase 104 carry-forward in STATE.md):

1. **Close MR-01:** add `const tutorialStep = computed(() => state.value?.state?.tutorial)` near
   the `useActionController` construction (~:390) and pass it as the `tutorialStep` option. The
   controller already accepts `tutorialStep?: Ref<TutorialStepView | undefined>`
   (useActionControllerTypes.ts:294) and uses it to gate `tryAutoFillSelection` — but NO production
   code currently passes it, so `suppressAutoFill` is inert end-to-end. This makes it live.
2. **Mount the overlay:** add `<TutorialOverlay />` once inside `.boardregion` (#main, alongside
   GameOverCard at ~:1681) so it overlays BOTH the custom-UI `#game-board` slot AND the dev
   UI-switcher `<component>` path with one mount. The overlay injects `gameState` itself
   (no props needed) and renders only when content is present (v-if internal).

Purpose: Without this, the whole phase is inert in the real app — the overlay never mounts and
suppressAutoFill never fires. This is the explicit "do not leave suppressAutoFill inert in
production" requirement (CONTEXT specifics).
Output: GameShell wired + an MR-01 closure test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/105-annotation-overlay-ui-parity/105-CONTEXT.md

<interfaces>
GameShell.vue:
- useActionController({...}) constructed at ~:390 — add `tutorialStep` to its options object.
- `state` is the reactive ref; `state.value.state` is the PlayerGameState; `.tutorial` is TutorialStepView|undefined.
- provide('gameState', state) at :657 — TutorialOverlay reads this via inject.
- .boardregion is <main id="main"> at :1665; GameOverCard mounts at :1681; the zoom-container +
  #game-board slot / UI-switcher <component> render at :1688-1737.
useActionControllerTypes.ts:294: `tutorialStep?: Ref<TutorialStepView | undefined>` (already accepted).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close MR-01 — thread the projected tutorial step into useActionController</name>
  <read_first>
    - src/ui/components/GameShell.vue:388-410 (useActionController construction + options)
    - src/ui/composables/useActionControllerTypes.ts:280-295 (tutorialStep option contract)
    - src/ui/composables/useActionController.test.ts:2130-2180 (how the controller consumes tutorialStep — mirror the assertion shape)
    - STATE.md:103-104 (MR-01 description)
  </read_first>
  <files>src/ui/components/GameShell.vue</files>
  <behavior>
    - A `tutorialStep` computed derives `state.value?.state?.tutorial` and is passed to
      useActionController so the controller's `tryAutoFillSelection` sees `suppressAutoFill`.
    - When the active step has `suppressAutoFill: true`, the controller does NOT auto-fill the
      single-enabled choice (the behavior already implemented in 104-03, now reachable in prod).
    - When no tutorial is active, `tutorialStep` is `undefined` → default auto-fill behavior (no regression).
  </behavior>
  <action>
    Add `const tutorialStep = computed(() => state.value?.state?.tutorial)` in the GameShell setup
    (near the controller construction) and add `tutorialStep,` to the useActionController options
    object. Import `computed` if not already imported. Touch nothing else in the options.
  </action>
  <acceptance_criteria>
    - `grep -c "tutorialStep" src/ui/components/GameShell.vue` ≥ 2 (declaration + option pass).
    - `grep -c "state.value?.state?.tutorial\|state.value.*state.*tutorial" src/ui/components/GameShell.vue` ≥ 1.
    - `npx eslint src/ui/components/GameShell.vue` clean; no new type errors in GameShell from `npx tsc --noEmit`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mount TutorialOverlay once in .boardregion (covers both UI paths)</name>
  <read_first>
    - src/ui/components/GameShell.vue:1662-1738 (.boardregion / GameOverCard / zoom-container / slot + UI-switcher)
    - src/ui/components/helpers/TutorialOverlay.vue (the component from Plan 03 — injects gameState, self-gates via v-if)
    - 105-UI-SPEC.md §3 z-index table (overlay z-20 sits above turn prompt, below GameOverCard scrim z-50)
  </read_first>
  <files>src/ui/components/GameShell.vue</files>
  <action>
    Import TutorialOverlay and add a single `<TutorialOverlay />` inside `<main class="boardregion">`
    — as a sibling of GameOverCard, OUTSIDE the zoom-container so the overlay positions in
    boardregion coordinates and is not scaled by `--zoom-level` (it measures rects itself). One
    mount serves both the `#game-board` slot (custom UI) and the dev UI-switcher `<component>`
    path because both render inside the same `.boardregion`. Do not pass props — the overlay
    injects gameState. Confirm it sits below the GameOverCard scrim per the z-index contract.
  </action>
  <acceptance_criteria>
    - `grep -c "<TutorialOverlay" src/ui/components/GameShell.vue` ≥ 1 and import present (`grep -c "TutorialOverlay" src/ui/components/GameShell.vue` ≥ 2).
    - The mount is inside `.boardregion`/#main (verify by reading the template region — not inside the actionbar).
    - `npx vitest run src/ui/components/GameShell.ia.test.ts` still passes (no shell regression).
    - `npm run lint:css` + `npx eslint src/ui/components/GameShell.vue` clean.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test — the projected tutorial step reaches the controller (MR-01 closure guard)</name>
  <read_first>
    - src/session/build-player-state.test.ts:313-319, 462-483 (building a real PlayerGameState with a suppressAutoFill step)
    - src/ui/composables/useActionController.test.ts:2130-2180 (the suppressAutoFill behavior contract)
    - the Task 1 wiring
  </read_first>
  <files>src/ui/components/GameShell.tutorial.test.ts</files>
  <behavior>
    - Mounting GameShell with a `state` whose `state.tutorial = { stepId, suppressAutoFill: true }`
      results in the action controller receiving that step — proven by an observable consequence:
      a single-enabled selection is NOT auto-filled (mirrors the 104-03 suppressAutoFill behavior)
      OR the controller's exposed tutorialStep ref reflects the projected value.
    - With no tutorial in state, the controller's tutorialStep is undefined and auto-fill proceeds.
    - This is the regression guard that MR-01 stays closed (a future refactor dropping the option
      would fail this test).
  </behavior>
  <action>
    Write a @vue/test-utils + Vitest spec that mounts GameShell (or the minimal harness used by
    GameShell.ia.test.ts) with an injected state carrying a suppressAutoFill tutorial step, and
    asserts the suppression behavior is reachable WITHOUT the test itself injecting the option
    into the controller (the whole point of MR-01: production wiring does it). Prefer asserting
    the behavioral consequence over internal refs where feasible.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/ui/components/GameShell.tutorial.test.ts` passes.
    - The test does NOT manually pass `tutorialStep` into useActionController — it relies on GameShell's production wiring (proving MR-01 closure).
    - Covers both the active-step (suppression reachable) and no-tutorial (default) cases.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PlayerGameState.tutorial → controller/overlay | Server-projected tutorial state drives UI behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-08 | Tampering | tutorialStep computed | mitigate | Reads from the already-validated PlayerGameState projection (same trust level as the rest of state); optional-chained so a malformed/absent tutorial yields undefined (default behavior), never a throw. |
| T-105-09 | Elevation of privilege | suppressAutoFill in prod | accept | suppressAutoFill only withholds a convenience auto-fill; it cannot expand the legal action set (gating is engine-side, Phase 104) — no privilege change. |
</threat_model>

<verification>
- `npx vitest run src/ui/components/GameShell.tutorial.test.ts src/ui/components/GameShell.ia.test.ts` green.
- `npx eslint src/ui/components/GameShell.vue` + `npm run lint:css` clean.
- No new `tsc --noEmit` errors attributable to GameShell.vue.
</verification>

<success_criteria>
- TutorialOverlay mounts once in .boardregion, serving both UI paths.
- MR-01 closed: projected tutorial step reaches useActionController via production wiring, proven by test.
- suppressAutoFill is live in production.
</success_criteria>

<output>
Create `.planning/phases/105-annotation-overlay-ui-parity/105-04-SUMMARY.md` when done.
</output>
