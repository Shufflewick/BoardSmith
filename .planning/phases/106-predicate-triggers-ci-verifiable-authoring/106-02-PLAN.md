---
phase: 106-predicate-triggers-ci-verifiable-authoring
plan: 02
type: execute
wave: 2
depends_on: ["106-01"]
files_modified:
  - src/engine/tutorial/predicates.ts
  - src/engine/tutorial/predicates.test.ts
  - src/engine/index.ts
autonomous: true
requirements: [TUT-03]
must_haves:
  truths:
    - "An author can express 'before the first turn', 'after N turns', and 'when an action becomes forced' as named helpers that compile to labeled advanceWhen predicates — NOT an event bus"
    - "Each helper returns a labeled ObjectCondition entry that spreads into advanceWhen and is evaluated by the shared evaluator"
    - "The helpers are exported from the public engine surface for game authors"
  artifacts:
    - path: "src/engine/tutorial/predicates.ts"
      provides: "beforeFirstTurn, afterTurns(n), whenForced(actionName) named-predicate helpers"
      contains: "export function afterTurns"
  key_links:
    - from: "src/engine/index.ts"
      to: "src/engine/tutorial/predicates.ts"
      via: "re-export of the named helpers"
      pattern: "predicates"
---

<objective>
Ship the named-predicate helpers that make TUT-03 triggers ergonomic without introducing an event subsystem.

Purpose: CONTEXT Area 1 — triggers are STATE PREDICATES, evaluated post-action. Provide sugar (`beforeFirstTurn`, `afterTurns(n)`, `whenForced(actionName)`-style) that compile to labeled `advanceWhen` conditions, so authors never hand-roll the labeled-record shape and never reach for a `turn-start`/`capture` emitter.

Output: `src/engine/tutorial/predicates.ts` with the helpers, exported from the engine public surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-predicate-triggers-ci-verifiable-authoring/106-CONTEXT.md

<interfaces>
src/engine/tutorial/types.ts — TutorialGateContext { game: Game; seat: number }; advanceWhen is a labeled record `Record<string,(ctx:TutorialGateContext)=>boolean>` (narrowed in Plan 01). Each helper returns such a record (typically a single-label entry) so authors spread several into one advanceWhen.

src/engine/element/game.ts:
  - getAvailableActions(player) → ActionDefinition[] (use to detect "only one action available" for whenForced).
  - getPlayer(seat) → Player | undefined.
  - getFlowState()/flow round info — candidate signal for turn counting (see discretion note).

Plan 01 delivered: evaluateAdvanceWhen / autoAdvanceTutorial consume whatever advanceWhen these helpers produce — no special-casing; helpers MUST return plain labeled predicates.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Named-predicate helpers (beforeFirstTurn / afterTurns / whenForced)</name>
  <read_first>
    - src/engine/tutorial/types.ts:100-105,193-198 (TutorialGateContext + narrowed advanceWhen)
    - src/engine/element/game.ts:900-947 (getTutorialDisabledActions, getAvailableActions usage)
    - src/engine/tutorial/progress.ts (Plan 01 — confirm advanceWhen evaluation contract)
    - src/session/tutorial-controller.test.ts:31-89 (in-test Game + flow pattern for tests)
  </read_first>
  <files>src/engine/tutorial/predicates.ts, src/engine/tutorial/predicates.test.ts</files>
  <behavior>
    - beforeFirstTurn(): returns a labeled entry whose predicate is true at tutorial start / before the learner has acted (covers the "fire immediately on entry, then auto-advance off the intro step" example evaluated once at start by Plan 03).
    - afterTurns(n): returns a labeled entry true once the learner has completed n of their own turns. Turn derivation is author-discretion per CONTEXT — derive from observable game state (flow round / a per-seat turn count). Document the chosen signal in the JSDoc; do NOT add a bespoke counter subsystem if game state already exposes the signal.
    - whenForced(actionName): returns a labeled entry true when `actionName` is the ONLY action available to the seat (generic, checkers-agnostic stand-in for "the first time a capture becomes forced"). Reuses getAvailableActions(player).
    - Every helper produces a labeled ObjectCondition entry (e.g. { 'after 2 turns': (ctx) => ... }) that the shared evaluator runs unchanged. Helpers are pure predicate factories — NO events, NO subscriptions, NO timers.
  </behavior>
  <action>
    Create `src/engine/tutorial/predicates.ts` exporting the three helpers (exact names/signatures at discretion, consistent with CONTEXT examples). Each returns a single-label `Record<string,(ctx:TutorialGateContext)=>boolean>` so authors compose with object-spread into `advanceWhen`. Use only read-only game inspection (`ctx.game`, `getAvailableActions`, flow/round state) — predicates must not mutate. For `afterTurns`, pick the cleanest existing turn signal and document it; if none exists cleanly, derive from the flow round for the seat (note the choice in JSDoc — do not invent persisted state). Write `predicates.test.ts` with an in-test Game subclass: beforeFirstTurn true at start; afterTurns(n) flips true after the nth turn and not before; whenForced(action) true only when that action is the sole available one.
  </action>
  <verify>
    <automated>npx vitest run src/engine/tutorial/predicates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "export function (beforeFirstTurn|afterTurns|whenForced)" src/engine/tutorial/predicates.ts` shows the helpers (names may differ; assert the three concepts exist).
    - A test composes `advanceWhen: { ...afterTurns(2) }` and asserts Plan 01's evaluateAdvanceWhen returns fired=false after 1 turn and fired=true after 2.
    - whenForced test: predicate is false when 2+ actions are available, true when exactly one (its action) is available.
    - `grep -rinE "addEventListener|emit\(|on\(.action|setTimeout|new EventTarget" src/engine/tutorial/predicates.ts` returns NOTHING (no event bus / timers).
  </acceptance_criteria>
  <done>Authors can build advanceWhen from composable named predicates with no event subsystem.</done>
</task>

<task type="auto">
  <name>Task 2: Export helpers from the public engine surface</name>
  <read_first>
    - src/engine/index.ts (existing export blocks; confirm tutorial types/gate re-exports)
  </read_first>
  <files>src/engine/index.ts</files>
  <action>
    Re-export the named-predicate helpers (and any helper return type) from `src/engine/index.ts` alongside the existing tutorial type exports, so game authors import them from the package entry. Do not change unrelated exports.
  </action>
  <verify>
    <automated>npx vitest run src/engine/tutorial && node -e "import('./src/engine/index.ts').catch(()=>process.exit(0))" 2>/dev/null; grep -nE "beforeFirstTurn|afterTurns|whenForced|predicates" src/engine/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "afterTurns|predicates" src/engine/index.ts` matches (helpers re-exported).
    - `npx vitest run src/engine` stays green.
  </acceptance_criteria>
  <done>The TUT-03 helper API is reachable from the public engine entry point.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/engine/tutorial` green.
- Helpers compile to labeled advanceWhen predicates evaluated by the Plan 01 shared evaluator; no event/timer machinery introduced.
</verification>

<success_criteria>
- beforeFirstTurn / afterTurns(n) / whenForced(actionName) exist, are pure predicate factories, and are exported for authors.
</success_criteria>

<output>
Create `.planning/phases/106-predicate-triggers-ci-verifiable-authoring/106-02-SUMMARY.md` when done.
</output>
