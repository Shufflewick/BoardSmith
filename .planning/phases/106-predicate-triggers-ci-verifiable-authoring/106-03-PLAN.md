---
phase: 106-predicate-triggers-ci-verifiable-authoring
plan: 03
type: execute
wave: 2
depends_on: ["106-01"]
files_modified:
  - src/session/game-session.ts
  - src/session/tutorial-controller.ts
  - src/session/tutorial-controller.test.ts
  - src/session/tutorial-autoadvance.test.ts
autonomous: true
requirements: [TUT-03]
must_haves:
  truths:
    - "After every action completes, the session evaluates advanceWhen server-side and auto-advances any running tutorial whose predicate fires, then re-broadcasts so clients render the new step (UI-agnostic parity)"
    - "advanceWhen is also evaluated once at tutorial start (covers 'before first turn')"
    - "TutorialController.start() throws an actionable error on a tutorial with zero steps, and rejects non-function predicates at start (MR-03)"
    - "TutorialController step transitions use the SINGLE engine nextProgress/initialProgress (no duplicated forward-advance logic)"
  artifacts:
    - path: "src/session/game-session.ts"
      provides: "post-action + start-time auto-advance hook calling autoAdvanceTutorial then re-broadcasting"
      contains: "autoAdvanceTutorial"
    - path: "src/session/tutorial-controller.ts"
      provides: "fail-loud start + shared step-transition delegation"
      contains: "validateTutorialDefinition"
  key_links:
    - from: "src/session/game-session.ts"
      to: "src/engine/tutorial/progress.ts"
      via: "autoAdvanceTutorial(game, seat) after broadcast"
      pattern: "autoAdvanceTutorial"
    - from: "src/session/tutorial-controller.ts"
      to: "src/engine/tutorial/progress.ts"
      via: "nextProgress / initialProgress / validateTutorialDefinition"
      pattern: "nextProgress|initialProgress|validateTutorialDefinition"
---

<objective>
Wire the engine auto-advance pump into the live session and make the tutorial lifecycle fail loud (MR-03).

Purpose: CONTEXT Area 1 — predicates are evaluated SERVER-SIDE after each action and once at start; on a true predicate the controller advances and the session re-broadcasts. This is the parity hard-rule: auto-advance happens in engine/session, the client just renders the advanced step. CONTEXT Area 3 — MR-03: `start()` on empty steps throws; non-function predicates throw at start. Also collapse the controller's private forward-advance onto the engine's single `nextProgress`.

Output: a post-action + start-time hook in `GameSession`, a fail-loud + delegating `TutorialController`, and tests proving server-side auto-advance + re-broadcast.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-predicate-triggers-ci-verifiable-authoring/106-CONTEXT.md

<interfaces>
src/session/game-session.ts:
  :863-906 performAction → this.#runner.performAction(...) → #save() → broadcast() (:905) → #scheduleAICheck()
  :1494-1529 startTutorial/advanceTutorial/skipTutorial/exitTutorial delegating to #tutorialController
  :1559 broadcast() — the universal post-mutation funnel (calls runner.captureCheckpoint() then pushes to clients)

src/session/tutorial-controller.ts:
  :71-81 #requireDefinition (throws when no def)
  :87-111 #forwardAdvance (currentStepId → next or completed) — REPLACE body with engine nextProgress
  :125-136 start() — REPLACE body with engine initialProgress + validateTutorialDefinition
  Controller is constructed as: new TutorialController(() => this.#runner, { broadcast: () => this.broadcast() })

Plan 01 (src/engine/tutorial/progress.ts) provides:
  autoAdvanceTutorial(game, seat): { advanced: boolean; finalStepId: string|null }
  nextProgress(def, currentStepId), initialProgress(def), validateTutorialDefinition(def)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server-side auto-advance hook (post-action + at start) with re-broadcast</name>
  <read_first>
    - src/session/game-session.ts:863-921 (performAction body + broadcast call site)
    - src/session/game-session.ts:1486-1530 (tutorial lifecycle delegates)
    - src/session/game-session.ts:1559-1600 (broadcast funnel)
    - src/engine/tutorial/progress.ts (autoAdvanceTutorial signature + semantics)
    - src/engine/element/game.ts:479 + tutorialProgress Map (seat → progress) to enumerate running seats
  </read_first>
  <files>src/session/game-session.ts</files>
  <action>
    After `broadcast()` in `performAction` (~:905, before/after `#scheduleAICheck()` is fine — must run after the primary broadcast), evaluate auto-advance for every seat with a running tutorial. Enumerate `this.#runner.game.tutorialProgress` keys (an opponent's action can satisfy a learner's predicate), call `autoAdvanceTutorial(game, seat)` for each; if ANY seat advanced, call `broadcast()` once more so clients render the new step. Keep it allocation-light and a no-op when no tutorial is running (autoAdvanceTutorial already returns advanced=false fast). Do NOT introduce a parallel broadcast path — reuse the existing `broadcast()` funnel.

    **`advanceWhen` is evaluated on POST-ACTION transitions ONLY — do NOT run the auto-advance pump at `startTutorial` (flash-and-skip guard).** When a tutorial starts, the learner ENTERS step[0] and its `content` always renders; the pump must never advance off the just-entered step on a predicate that happens to be true at entry (otherwise step[0]'s content never shows — the exact "skip past a step" Pit-of-Failure the plan-checker flagged). The "before the player's first turn" case is simply step[0] showing at start — it needs no `advanceWhen`. `autoAdvanceTutorial` advances the CURRENT step only after a real action has occurred; an intro step advances either via the player's first action (its `advanceWhen`) or via explicit `advance()`/`skip()`.
  </action>
  <verify>
    <automated>npx vitest run src/session/tutorial-controller.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "autoAdvanceTutorial" src/session/game-session.ts` shows the call in performAction (post-broadcast). It is NOT called in startTutorial — step[0] renders on entry and is never auto-skipped at start (flash-and-skip guard).
    - The hook iterates running-tutorial seats (not just the acting player) so an opponent move can advance the learner.
    - When no tutorial is running, performAction performs no extra broadcast (verified by a spy in Task 3 test).
  </acceptance_criteria>
  <done>Predicates are evaluated server-side after each action and at start; a fired predicate advances the controller and re-broadcasts.</done>
</task>

<task type="auto">
  <name>Task 2: Fail-loud lifecycle (MR-03) + delegate step-transition to engine</name>
  <read_first>
    - src/session/tutorial-controller.ts (full)
    - src/engine/tutorial/progress.ts (nextProgress, initialProgress, validateTutorialDefinition)
  </read_first>
  <files>src/session/tutorial-controller.ts</files>
  <action>
    In `#requireDefinition` (or at the top of `start`), call `validateTutorialDefinition(def)` so `start()` on zero steps throws an actionable error and a non-function gate/advanceWhen throws at start (MR-03). Replace `#forwardAdvance`'s hand-rolled index math with a call to engine `nextProgress(def, currentStepId)`, and `start`'s first-step construction with `initialProgress(def)` — single source of truth for step transitions. Preserve existing broadcast-once-per-call behavior and the running/completed/exited semantics. Keep `skip` semantically distinct from `advance` only insofar as it already is (LR-01 is discretionary — leave the existing behavior unless trivially improved).
  </action>
  <verify>
    <automated>npx vitest run src/session/tutorial-controller.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "validateTutorialDefinition" src/session/tutorial-controller.ts` matches.
    - `grep -nE "findIndex|nextIndex" src/session/tutorial-controller.ts` returns NOTHING (transition math moved to engine nextProgress).
    - Existing controller lifecycle tests still pass.
  </acceptance_criteria>
  <done>start() fails loud on empty/malformed tutorials and the controller shares the engine step-transition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests — server-side auto-advance, re-broadcast, opponent-triggered, MR-03</name>
  <read_first>
    - src/session/tutorial-controller.test.ts:1-95 (in-test Game + makeRunner pattern; broadcast spy via vi.fn)
    - src/session/tutorial-serialization.test.ts:55 (alternate in-test game shape)
  </read_first>
  <files>src/session/tutorial-autoadvance.test.ts, src/session/tutorial-controller.test.ts</files>
  <behavior>
    - A step whose advanceWhen fires after the learner's action auto-advances on the NEXT performAction and the session broadcasts a second time (spy: broadcast called for the action AND for the advance).
    - A step whose advanceWhen depends on an OPPONENT action advances the learner after the opponent moves.
    - **Flash-and-skip guard:** starting a tutorial whose step[0].advanceWhen would already be true at entry does NOT auto-advance off step[0] at start — after startTutorial the active step is still step[0] (its content renders); it only advances after a subsequent action. (This is the corrected semantics: no start-time pump.)
    - No tutorial running → performAction triggers exactly one broadcast (no spurious re-broadcast).
    - MR-03: startTutorial on a definition with zero steps throws an actionable error.
  </behavior>
  <action>
    Add `src/session/tutorial-autoadvance.test.ts` driving a GameSession (or the controller + runner harness used in tutorial-controller.test.ts, whichever exercises performAction's hook) with a broadcast spy. Cover the five behaviors above using an in-test Game subclass with `move`/`pass` actions and advanceWhen built from Plan 01 raw labeled predicates (do NOT depend on Plan 02 helpers — keep this plan's tests independent). Add the empty-steps MR-03 case to the controller test file if not already covered.
  </action>
  <verify>
    <automated>npx vitest run src/session/tutorial-autoadvance.test.ts src/session/tutorial-controller.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Auto-advance test asserts the broadcast spy was called twice across a single performAction that triggers an advance, once when it does not.
    - Opponent-triggered advance test passes.
    - Empty-steps startTutorial throws with an actionable message.
    - `npx vitest run src/session` stays green.
  </acceptance_criteria>
  <done>Server-side auto-advance + re-broadcast + MR-03 fail-loud are proven by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game author → session | advanceWhen predicates run inside the session post-action hook (server authority). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-04 | Denial of Service | post-action auto-advance hook on every action in non-tutorial games | mitigate | autoAdvanceTutorial returns fast (advanced=false) when no running tutorial; the hook performs no extra broadcast in normal play (asserted by spy test) |
| T-106-05 | Tampering | client attempting to drive step via crafted action | accept | TutorialController remains the only progress writer for lifecycle; auto-advance is server-computed from authored predicates, not client input |
</threat_model>

<verification>
- `npx vitest run src/session` green.
- A fired predicate advances server-side and re-broadcasts; non-tutorial play adds zero broadcasts.
- MR-03: empty/malformed tutorials throw at start.
</verification>

<success_criteria>
- TUT-03 runtime delivery: predicates evaluated server-side after each action + at start, auto-advancing and re-broadcasting UI-agnostically.
</success_criteria>

<output>
Create `.planning/phases/106-predicate-triggers-ci-verifiable-authoring/106-03-SUMMARY.md` when done.
</output>
