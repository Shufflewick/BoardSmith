---
phase: 106-predicate-triggers-ci-verifiable-authoring
plan: 05
type: execute
wave: 3
depends_on: ["106-02", "106-04"]
files_modified:
  - src/testing/tutorial-ci-demo.test.ts
autonomous: true
requirements: [TUT-04]
must_haves:
  truths:
    - "A complete in-repo tutorial (in-test game + named-helper advanceWhen) is GREEN when the rules are intact"
    - "Deliberately breaking a rule turns that SAME tutorial test RED — both the 'taught action gated out' break and the 'predicate never fires' break are demonstrated"
    - "The proof lives in THIS repo using a minimal in-test Game subclass (no external game package dependency)"
  artifacts:
    - path: "src/testing/tutorial-ci-demo.test.ts"
      provides: "Criterion #3 demonstration: green-when-intact, red-when-broken"
      contains: "toThrow"
  key_links:
    - from: "src/testing/tutorial-ci-demo.test.ts"
      to: "src/testing/simulate-tutorial.ts"
      via: "simulateTutorial + assertTutorialCompletes"
      pattern: "simulateTutorial"
    - from: "src/testing/tutorial-ci-demo.test.ts"
      to: "src/engine/tutorial/predicates.ts"
      via: "named-helper advanceWhen (afterTurns / whenForced)"
      pattern: "afterTurns|whenForced|beforeFirstTurn"
---

<objective>
Prove success criterion #3 in this repo: a rule change that breaks the tutorial fails the test, demonstrated by a deliberately broken rule (green → red).

Purpose: CONTEXT Area 2 + Specific Ideas — ship a demonstration test using a minimal in-test game and a tutorial authored with the named-predicate helpers, then prove the SAME test goes RED when a rule is deliberately broken: (a) the taught action is gated out of the game, and (b) a predicate (e.g. the forced-capture condition) never fires because the rule was removed. This is the must-have artifact distinguishing a vacuously-passing test from a real CI guard. The real checkers tutorial test is Phase 109 (cross-repo) — this phase ships the in-repo proof.

Output: `src/testing/tutorial-ci-demo.test.ts` demonstrating green-when-intact and red-both-ways.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-predicate-triggers-ci-verifiable-authoring/106-CONTEXT.md

<interfaces>
src/testing/simulate-tutorial.ts (Plan 04): simulateTutorial(testGame, tutorialDef, { seat, scenario, seed }) → { completed, finalStepId, stepsVisited }; throws on gate drift / predicate non-fire / failed action.
src/testing/tutorial-assertions.ts (Plan 04): assertTutorialStep, assertTutorialCompletes.
src/engine/tutorial/predicates.ts (Plan 02): beforeFirstTurn / afterTurns(n) / whenForced(actionName) — compose into advanceWhen.
src/session/tutorial-controller.test.ts:31-89 — in-test Game subclass + flow pattern to mirror for the demo game.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Intact tutorial demonstration — GREEN</name>
  <read_first>
    - src/session/tutorial-controller.test.ts:31-89 (in-test Game subclass, registerActions, defineFlow)
    - src/testing/simulate-tutorial.ts + src/testing/tutorial-assertions.ts (Plan 04 API)
    - src/engine/tutorial/predicates.ts (Plan 02 helpers)
  </read_first>
  <files>src/testing/tutorial-ci-demo.test.ts</files>
  <behavior>
    - A minimal in-test Game subclass with a `move` action (the taught action) and at least one rule that can make a follow-up move "forced" (so whenForced(...) has something to fire on) — e.g. a `capture` action that becomes the only available action under a board condition.
    - A TutorialDefinition with: an intro step (advanceWhen: beforeFirstTurn-driven entry), a taught-move step gated to `move`, and a tip step whose advanceWhen uses a named helper (whenForced('capture') or afterTurns(n)).
    - A scripted scenario that legally walks the tutorial to completion. simulateTutorial returns completed=true; assertTutorialCompletes passes; intermediate expectStep assertions pass.
  </behavior>
  <action>
    Create `src/testing/tutorial-ci-demo.test.ts`. Build the in-test game + tutorial (advanceWhen composed from Plan 02 helpers) and a passing scenario. Pin a seed for determinism. This is the canonical "author a tutorial as a CI test" example — keep it readable; it doubles as documentation for game authors.
  </action>
  <verify>
    <automated>npx vitest run src/testing/tutorial-ci-demo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The intact-rules test passes: simulateTutorial completes and assertTutorialCompletes does not throw.
    - advanceWhen is built from a Plan 02 named helper (`grep -nE "afterTurns|whenForced|beforeFirstTurn" src/testing/tutorial-ci-demo.test.ts` matches).
    - No import from any external game package (`grep -nE "checkers|BoardSmithGames|@boardsmith/" src/testing/tutorial-ci-demo.test.ts` returns NOTHING).
  </acceptance_criteria>
  <done>An in-repo tutorial authored with named helpers passes as a normal Vitest test.</done>
</task>

<task type="auto">
  <name>Task 2: Deliberately broken rules — RED both ways</name>
  <read_first>
    - Task 1 demo game + tutorial (same file)
    - src/testing/simulate-tutorial.ts (which conditions throw)
  </read_first>
  <files>src/testing/tutorial-ci-demo.test.ts</files>
  <behavior>
    - Break #1 (taught action no longer legal under the step): a variant of the demo game where the taught `move` becomes illegal for the step. PREFERRED: keep `move` available but step-gated-out so the failure hits the gate-legality assertion (this exercises getTutorialDisabledActions). If the variant instead removes the action entirely, the failure surfaces on the doAction/illegal-action assertion — both are valid drift. Running the SAME scenario through simulateTutorial THROWS either way; the matcher accepts EITHER drift message (gated-out OR illegal/unavailable action), so the demo can't fail to fail.
    - Break #2 (predicate never fires): a variant where the rule the predicate depends on is removed (e.g. `capture` never becomes forced, so whenForced('capture') / the tip-step advanceWhen never fires). The scenario's expected advance does not happen → simulateTutorial THROWS ("expected advance ... did not fire").
    - Both demonstrate that a rule change that breaks the tutorial fails the test rather than rotting silently.
  </behavior>
  <action>
    Add two `it(...)` cases (or table-driven variants) to the demo file constructing broken-rule game variants (subclass overrides or a flag that removes the action / disables the forced condition). Use `expect(() => simulateTutorial(brokenGame, tutorialDef, {...})).toThrow(...)` to assert the test would go RED. Add a top-of-file comment explaining this is the criterion #3 proof (green-when-intact from Task 1, red-when-broken here). Do not weaken the assertions to a generic toThrow with no matcher — match the relevant drift message so the proof is specific.
  </action>
  <verify>
    <automated>npx vitest run src/testing/tutorial-ci-demo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Two broken-rule cases each assert `toThrow` with a message matcher and pass.
    - Break #1 throws on EITHER the gate-legality assertion (taught action gated out) OR the illegal/unavailable-action assertion (the matcher accepts either drift message); Break #2 throws on the predicate-did-not-fire assertion.
    - `npx vitest run src/testing` is green (the demo file proves drift fails WITHOUT making the suite red).
  </acceptance_criteria>
  <done>The same tutorial test is demonstrably RED when a rule is deliberately broken — criterion #3 proven in-repo.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/testing/tutorial-ci-demo.test.ts` green: intact passes, broken-rule cases assert failure.
- No external game-package import; in-test Game subclass only.
</verification>

<success_criteria>
- Criterion #3 demonstrated in this repo: green when rules intact, red (test throws) when a rule is deliberately broken, both for gate-out and predicate-non-fire breaks.
</success_criteria>

<output>
Create `.planning/phases/106-predicate-triggers-ci-verifiable-authoring/106-05-SUMMARY.md` when done.
</output>
