---
phase: 107-ai-assisted-teaching
plan: 03
type: execute
wave: 3
depends_on: ["107-02"]
files_modified:
  - src/session/ai-controller.ts
  - src/session/game-session.ts
  - src/session/ai-controller.test.ts
  - src/session/teaching.test.ts
autonomous: true
requirements: [AI-02]
must_haves:
  truths:
    - "An AI move is announced (narration text set) BEFORE it executes, with a configurable delay"
    - "A player can start an AI-vs-AI demo where all seats are AI-controlled and each move is announced"
    - "Stopping the demo restores the original AI controller and clears narration"
    - "Demo mode reuses the existing all-seats AI mechanism — no new bot/training"
  artifacts:
    - path: "src/session/ai-controller.ts"
      provides: "onBeforeMove optional 4th param fired between bot.play() and onMove()"
      contains: "onBeforeMove"
    - path: "src/session/game-session.ts"
      provides: "startDemo/stopDemo/isDemoRunning + narration hook wiring in #checkAITurn"
      contains: "startDemo"
    - path: "src/session/ai-controller.test.ts"
      provides: "onBeforeMove ordering + timing coverage"
      contains: "onBeforeMove"
  key_links:
    - from: "AIController.checkAndPlay()"
      to: "onBeforeMove(action, player, args)"
      via: "awaited between bot.play() (line 128) and onMove() (line 131)"
      pattern: "onBeforeMove"
    - from: "GameSession.startDemo()"
      to: "all-seats AIController override + narration hook"
      via: "save/replace #aiController, set #narrationText, schedule AI check"
      pattern: "startDemo|stopDemo"
---

<objective>
Add the pre-move announcement seam (AI-02): an `onBeforeMove` hook in `AIController.checkAndPlay()` fired between the MCTS compute and the move execution, and a `GameSession` demo mode that overrides all seats to AI control, announces each move (engine-derived text + configurable delay), then executes.

Purpose: A narrated AI-vs-AI demo needs the move announced before it lands on the board. The hook threads as an optional fourth callback so non-demo play is unaffected.
Output: `onBeforeMove?` param on `checkAndPlay()`; `startDemo()`/`stopDemo()`/`isDemoRunning` on `GameSession`; narration text injected via the Plan 02 `#narrationText` field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-ai-assisted-teaching/107-RESEARCH.md
@.planning/phases/107-ai-assisted-teaching/107-PATTERNS.md
@.planning/phases/107-ai-assisted-teaching/107-02-SUMMARY.md

<interfaces>
From src/session/ai-controller.ts:
- `async checkAndPlay(runner, actionHistory, onMove): Promise<{action,player,args}|null>` (70-74). `#thinking` guard (76, 95, 140). `canSeatAct` check (110). `const move = await bot.play()` (128); `const success = await onMove(...)` (131). Error path rethrows; finally resets `#thinking` (136-141).

From src/session/game-session.ts (after Plan 02):
- `#aiController?` mutable (184). `#narrationText: string | null` (added Plan 02; injected in broadcast as `state.narration`).
- `#checkAITurn()` (~1636): calls `this.#aiController.checkAndPlay(runner, actionHistory, async onMove)` where onMove performs the action, saves, and broadcasts.
- Constructor builds the original AIController from the game definition's AI config.

From 107-UI-SPEC.md Copywriting Contract:
- Narration format: "[Player name]: [move description]" e.g. "AI: move c3 → d4". Default delay 1200ms. Error mid-demo narration: "Demo stopped."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: onBeforeMove hook in AIController.checkAndPlay()</name>
  <files>src/session/ai-controller.ts, src/session/ai-controller.test.ts</files>
  <read_first>
    - src/session/ai-controller.ts (checkAndPlay 70-144, bot.play 128, onMove 131, finally 136-141)
    - src/session/ai-circuit-breaker.test.ts (AIController direct construction, checkAndPlay invocation, spies)
    - 107-PATTERNS.md section "src/session/ai-controller.ts — extend" + "src/session/ai-controller.test.ts — new test file"
    - 107-RESEARCH.md Pattern 3
  </read_first>
  <behavior>
    - `checkAndPlay(runner, history, onMove, onBeforeMove?)` calls `onBeforeMove(action, player, args)` exactly once, after `bot.play()` resolves and BEFORE `onMove(...)`.
    - When `onBeforeMove` is omitted, behavior is byte-for-byte the prior behavior (no announce).
    - If `onBeforeMove` awaits a delay, `onMove` is not called until that delay resolves (ordering proven by a spy recording call order).
    - An error inside `onBeforeMove` propagates and `#thinking` is reset in finally (no stuck thinking flag).
  </behavior>
  <action>
    Add an optional fourth parameter `onBeforeMove?: (action: string, player: number, args: Record<string, unknown>) => Promise<void>` to `checkAndPlay`. Between `const move = await bot.play()` (128) and `const success = await onMove(...)` (131) insert `if (onBeforeMove) await onBeforeMove(move.action, aiPlayer, move.args);`. Leave the `#thinking` guard, `canSeatAct` check, error rethrow, and finally block unchanged. In src/session/ai-controller.test.ts (new, analog ai-circuit-breaker.test.ts) add: a spy onBeforeMove is called once; an order-recording test proving onBeforeMove runs before onMove; a delay test proving onMove waits for an onBeforeMove that awaits a timer (use fake timers or a resolved-order assertion, no real long sleeps); an error-in-onBeforeMove test proving #thinking resets (isThinking() false afterward).
  </action>
  <verify>
    <automated>npx vitest run src/session/ai-controller.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onBeforeMove" src/session/ai-controller.ts` >= 2 (signature + call site).
    - Test proves onBeforeMove fires before onMove (call-order array).
    - Test proves omitting onBeforeMove leaves move execution unchanged.
  </acceptance_criteria>
  <done>onBeforeMove fires between MCTS compute and move execution; default path unchanged; thinking flag safe on error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GameSession demo mode (startDemo/stopDemo) + narration wiring</name>
  <files>src/session/game-session.ts, src/session/teaching.test.ts</files>
  <read_first>
    - src/session/game-session.ts (#aiController 184, constructor AIController build ~225, #checkAITurn ~1636, broadcast 1579, #narrationText from Plan 02)
    - 107-RESEARCH.md Example 5 (startDemo/stopDemo/isDemoRunning signatures), Pitfall 6 (restore on stop), Open Question 2 (GameShell calls stopDemo on game-over — keep session passive)
    - 107-UI-SPEC.md Copywriting Contract (narration format, default delay 1200ms, "Demo stopped.")
  </read_first>
  <behavior>
    - `startDemo({ narrator?, delay? })` saves the current `#aiController`, replaces it with an all-seats AIController (players = every seat), registers a narration onBeforeMove hook, and schedules the first AI check; `isDemoRunning` becomes true.
    - The narration hook sets `#narrationText` to `narrator?.(...)` or the default `"${playerName}: ${actionName} ${argsummary}"`, broadcasts the announcement, awaits the delay (default 1200ms), then returns (the move executes after).
    - `stopDemo()` restores the saved `#aiController`, clears the narration hook and `#narrationText`, sets `isDemoRunning` false, and broadcasts.
    - `#checkAITurn()` passes the demo narration hook as the fourth `checkAndPlay` argument when demo mode is active.
  </behavior>
  <action>
    Add private fields `#demoMode = false`, `#savedAIController?: AIController<G>`, `#onBeforeMove?` (the narration closure), and a `#demoDelay = 1200`. Add `startDemo(options?: { narrator?: (action, player, args) => string; delay?: number }): void`: store `#savedAIController = this.#aiController`; build a new AIController with `players` set to all seats (reuse the existing AIConfig level), assign to `#aiController`; set `#demoDelay = options?.delay ?? 1200`; set `#onBeforeMove` to an async closure that computes narration text (options.narrator or default `${playerName}: ${actionName} ${formatArgs(args)}` using stored player names), sets `this.#narrationText`, calls `this.broadcast()`, then `await new Promise(r => setTimeout(r, this.#demoDelay))`; set `#demoMode = true`; schedule the AI check (same scheduler the session already uses). Add `stopDemo(): void`: restore `this.#aiController = this.#savedAIController`, clear `#savedAIController`, `#onBeforeMove`, set `#narrationText = null`, `#demoMode = false`, broadcast. Add `get isDemoRunning(): boolean { return this.#demoMode; }`. In `#checkAITurn()`, pass `this.#onBeforeMove` as the fourth arg to `checkAndPlay` (undefined when not in demo). After the move executes and broadcasts, clear `#narrationText` so the announcement is transient (one cycle). Use Vue/engine player-name source already on the session for the name; format args as a compact summary (e.g. the destination notation or key:value pairs), never raw HTML. Add demo tests to src/session/teaching.test.ts: startDemo sets all seats AI + isDemoRunning true; narration hook sets state.narration before the move; stopDemo restores the original controller and clears narration.
  </action>
  <verify>
    <automated>npx vitest run src/session/teaching.test.ts -t "demo|narration" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "startDemo\|stopDemo" src/session/game-session.ts` >= 2; `get isDemoRunning` present.
    - Test: after startDemo, isDemoRunning true and the demo AIController controls all seats.
    - Test: stopDemo restores the originally-saved controller and sets narration to null.
    - Narration text rendered as plain string (no v-html anywhere); test asserts text equals the formatted "Player: move ..." string.
  </acceptance_criteria>
  <done>Demo mode announces each move before it executes via onBeforeMove + delay, reuses the all-seats AI mechanism, and restores cleanly on stop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine move data → narration text | actionName + args formatted into a string shown to all clients |
| demo mode → AIController override | in-memory controller swap; must restore to avoid leaving all-AI active |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-06 | Tampering | XSS via narration text | mitigate | Text built from engine-controlled actionName + args summary; rendered via Vue text interpolation only (Plan 04 BoardMessage), never v-html (matches TutorialOverlay T-105-05) |
| T-107-07 | Elevation of Privilege | demo override not restored | mitigate | `stopDemo()` restores `#savedAIController` and clears narration; GameShell calls stopDemo on game-over/unmount (Plan 04); `#demoMode` tracks state |
| T-107-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages (RESEARCH Package Legitimacy Audit — none) |
</threat_model>

<verification>
- `npx vitest run src/session` green.
- onBeforeMove proven to fire before onMove; default path unchanged.
- startDemo overrides all seats + narrates; stopDemo restores original controller.
</verification>

<success_criteria>
- AI-02: a player can watch an AI-vs-AI demo in which each move is announced (narration text set + broadcast) before it executes, paced by a configurable delay.
- The demo reuses the existing all-seats AI mechanism (no new bot/training) and restores the original controller on stop.
</success_criteria>

<output>
Create `.planning/phases/107-ai-assisted-teaching/107-03-SUMMARY.md` when done.
</output>
