---
phase: 107-ai-assisted-teaching
plan: 04
type: execute
wave: 4
depends_on: ["107-02", "107-03"]
files_modified:
  - src/ui/components/helpers/overlay-utils.ts
  - src/ui/components/helpers/HintOverlay.vue
  - src/ui/components/helpers/HeatmapOverlay.vue
  - src/ui/components/helpers/BoardMessage.vue
  - src/ui/components/ControlsMenu.vue
  - src/ui/components/GameShell.vue
  - src/ui/components/helpers/HintOverlay.test.ts
  - src/ui/components/helpers/HeatmapOverlay.test.ts
autonomous: true
requirements: [AI-01, AI-02, AI-03]
must_haves:
  truths:
    - "A move hint renders a ring + bubble on the suggested target cell, resolved via data-bs-el-id anchors"
    - "A heatmap renders per-candidate-cell chips with a numeric % badge (non-color cue) when visible"
    - "An AI-demo move announcement renders as a narration card above the board"
    - "All three render identically in custom UI and AutoUI because they resolve shared data-bs-el-* anchors (no renderer coupling)"
  artifacts:
    - path: "src/ui/components/helpers/HintOverlay.vue"
      provides: "single-annotation ring + bubble overlay (z-index 20)"
      min_lines: 40
    - path: "src/ui/components/helpers/HeatmapOverlay.vue"
      provides: "per-cell evaluation chips with % badge (z-index 15)"
      min_lines: 40
    - path: "src/ui/components/helpers/overlay-utils.ts"
      provides: "shared cssEscape + buildSelector (no copy-paste across overlays)"
      contains: "buildSelector"
    - path: "src/ui/components/ControlsMenu.vue"
      provides: "Teaching group + teaching-action emit"
      contains: "Teaching"
  key_links:
    - from: "HintOverlay/HeatmapOverlay"
      to: "data-bs-el-id anchors"
      via: "document.querySelector(buildSelector(...)) + getBoundingClientRect"
      pattern: "data-bs-el-id|buildSelector"
    - from: "ControlsMenu teaching-action emit"
      to: "GameSession.requestHint/setHeatmapVisible/startDemo/stopDemo"
      via: "GameShell @teaching-action handler"
      pattern: "teaching-action"
    - from: "GameShell"
      to: "HintOverlay + HeatmapOverlay mounted in .boardregion"
      via: "alongside TutorialOverlay (line 1704), outside zoom-container"
      pattern: "HintOverlay|HeatmapOverlay"
---

<objective>
Render the three teaching aids in the UI (AI-01 hint, AI-02 demo narration, AI-03 heatmap), reusing the Phase 105 TutorialOverlay infrastructure exactly, and wire the Teaching controls through ControlsMenu → GameShell → GameSession. Prove custom-UI/AutoUI parity (project hard-rule) via component tests.

Purpose: Every hard problem here was solved by TutorialOverlay (Teleport, fixed positioning, ResizeObserver, data-bs-el-id anchoring). This plan is structural reuse, not invention. Parity is automatic because overlays resolve the shared anchors, not renderer internals.
Output: A shared `overlay-utils.ts`, `HintOverlay.vue`, `HeatmapOverlay.vue`, a `narration` variant on `BoardMessage.vue`, a Teaching group in `ControlsMenu.vue`, GameShell mounting + wiring, and parity component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-ai-assisted-teaching/107-RESEARCH.md
@.planning/phases/107-ai-assisted-teaching/107-PATTERNS.md
@.planning/phases/107-ai-assisted-teaching/107-UI-SPEC.md
@.planning/phases/107-ai-assisted-teaching/107-02-SUMMARY.md
@.planning/phases/107-ai-assisted-teaching/107-03-SUMMARY.md

<interfaces>
From Plan 02 (broadcast state shapes the overlays read):
- `PlayerGameState.hint?: { annotation: Annotation }`
- `PlayerGameState.heatmap?: { visible: boolean; entries: HeatmapEntry[] }`
- `PlayerGameState.narration?: { text: string }`
- `HeatmapEntry { cellRef: ElementRef; normalizedValue: number; isBest: boolean }`
- GameSession methods: `requestHint(seat)`, `clearHint(seat)`, `setHeatmapVisible(seat, visible)`.

From Plan 03:
- GameSession: `startDemo(options?)`, `stopDemo()`, `get isDemoRunning`.

From src/ui/components/helpers/TutorialOverlay.vue (analog — copy infra):
- cssEscape polyfill (65-71), buildSelector (115-135), readRadius (138-141), measure() (187-268), rebuildObservers() (276-312), onScroll + watchEffect + lifecycle (314-346), ringStyle (350-358), `.bsg-tutorial-ring` + keyframes + reduced-motion CSS (452-520). Injects `gameState` (98).

From src/ui/components/GameShell.vue:
- `provide('gameState', state)` (666); TutorialOverlay import (39) + mount (1704, outside zoom-container); ControlsMenu used (1777); GameOverCard watches flowState.complete (1690).

From src/ui/components/ControlsMenu.vue:
- `withDefaults(defineProps<{...}>())` (19); `defineEmits<{...}>()` (35); Session grouplabel (192); menuitemcheckbox toggle pattern (148-155); `.grouplabel` CSS (261).

From 107-UI-SPEC.md: z-index HintOverlay 20, HeatmapOverlay 15, narration 10; chip size `min(floor(min(w,h)*0.68),52)` min 24; intensity `15 + round(value*70)`; badge `round(value*100)+"%"`; best-move 2px solid border; copy strings ("Get a hint", "Watch AI demo"/"Stop demo", "Show move quality", "Teaching", hint bubble "Suggested move", narration "AI Demo" prefix).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared overlay-utils + HintOverlay + HeatmapOverlay + BoardMessage narration variant</name>
  <files>src/ui/components/helpers/overlay-utils.ts, src/ui/components/helpers/HintOverlay.vue, src/ui/components/helpers/HeatmapOverlay.vue, src/ui/components/helpers/BoardMessage.vue</files>
  <read_first>
    - src/ui/components/helpers/TutorialOverlay.vue (full — extract cssEscape/buildSelector/measure/rebuildObservers/ringStyle/CSS)
    - src/ui/components/helpers/BoardMessage.vue (variant prop 60-86, class binding 92-103, annotation CSS 159-196)
    - 107-PATTERNS.md sections "HintOverlay.vue", "HeatmapOverlay.vue", "BoardMessage.vue — extend", "Shared Patterns"
    - 107-UI-SPEC.md Component Inventory + Color + Chip visual spec + Accessibility Checklist
    - 107-RESEARCH.md Pattern 4 + Anti-Patterns (no copy-paste cssEscape; color is never the only cue)
  </read_first>
  <action>
    Create src/ui/components/helpers/overlay-utils.ts exporting `cssEscape(value)` (the TutorialOverlay polyfill) and `buildSelector(target: AnnotationTarget): string` (the exact element/action/panel switch from TutorialOverlay 115-135). Refactor TutorialOverlay to import these from overlay-utils (remove its local copies) so there is a single source. Create HintOverlay.vue: inject `gameState`, `annotation = computed(() => gameState?.value?.state?.hint?.annotation ?? null)`, `hasContent = annotation !== null`; reuse the TutorialOverlay measure/rebuildObservers/onScroll/lifecycle infra adapted for a single annotation; render one `.bsg-tutorial-ring` (with the "i" chip glyph) + one `BoardMessage variant="annotation"` resolved by anchor; `<Teleport to="body">`, root `position:fixed; inset:0; z-index:20; pointer-events:none`; reuse `.bsg-tutorial-ring` CSS classes/keyframes; reduced-motion = instant; ring/chip `aria-hidden`, bubble inherits BoardMessage role=status/aria-live=polite. Create HeatmapOverlay.vue: inject `gameState`, `entries = computed(() => gameState?.value?.state?.heatmap?.visible ? entries : [])`, `hasContent = entries.length > 0`; per entry resolve `buildSelector({kind:'element', ref: entry.cellRef})`, compute chip size `Math.max(24, Math.min(Math.floor(Math.min(rect.w,rect.h)*0.68),52))`, center via `translate(-50%,-50%)`; chip background `color-mix(in srgb, var(--bsg-accent) {15+round(value*70)}%, transparent)`, border 1px (non-best) / 2px solid var(--bsg-accent) (isBest), text `round(value*100)+"%"` in `var(--bsg-text-xs)` weight 600, text color `--bsg-accent-ink` when intensity>=55 else `--bsg-ink`; `<Teleport to="body">`, root z-index 15, pointer-events:none; chips `aria-hidden="true"`; ResizeObserver on `.boardregion` + each resolved cell; reduced-motion = instant. No literal colors/spacing outside the pinned ring OFFSET and chip-size floor. Extend BoardMessage.vue: add `'narration'` to the `variant` prop union; three-way class binding (annotation / narration / position); add `.bsg-board-message--narration` wrapper (top:var(--bsg-s3), horizontally centered, z-index 10) and `.bsg-board-message__content--narration` card (background var(--bsg-surface-3), border 1px var(--bsg-line-2), radius var(--bsg-r-md), padding var(--bsg-s2) var(--bsg-s4), max-width min(280px,90%), box-shadow var(--bsg-shadow-sm)); render text via interpolation only (never v-html); role=status/aria-live=polite; "AI Demo" prefix in var(--bsg-ink-2).
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/helpers/HintOverlay.test.ts src/ui/components/helpers/HeatmapOverlay.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - overlay-utils.ts exports cssEscape + buildSelector; `grep -c "function cssEscape\|const cssEscape" src/ui/components/helpers/TutorialOverlay.vue` == 0 (deduped — imported instead).
    - HintOverlay reads `state.hint?.annotation`; HeatmapOverlay reads `state.heatmap` and renders only when `visible`.
    - HeatmapOverlay chip text contains "%" (grep for `%` badge); chips `aria-hidden="true"`.
    - `grep -c "v-html" src/ui/components/helpers/BoardMessage.vue` == 0; narration variant present.
    - No literal hex colors in the new overlays (`grep -nE "#[0-9a-fA-F]{3,6}" src/ui/components/helpers/HintOverlay.vue src/ui/components/helpers/HeatmapOverlay.vue` empty).
  </acceptance_criteria>
  <done>Two new overlays + narration variant render the teaching aids via shared anchors and Slate tokens, color is never the only cue, no v-html.</done>
</task>

<task type="auto">
  <name>Task 2: ControlsMenu Teaching group + GameShell mount & wiring</name>
  <files>src/ui/components/ControlsMenu.vue, src/ui/components/GameShell.vue</files>
  <read_first>
    - src/ui/components/ControlsMenu.vue (defineProps 19-33, defineEmits 35-40, Session grouplabel 192, menuitemcheckbox 148-155, disabled menuitem pattern, .grouplabel CSS 261)
    - src/ui/components/GameShell.vue (TutorialOverlay import 39 + mount 1704, ControlsMenu usage 1777, provide gameState 666, GameOverCard/flowState.complete 1690)
    - 107-PATTERNS.md sections "ControlsMenu.vue — extend" and "GameShell.vue — extend"
    - 107-UI-SPEC.md Teaching Controls Placement + Copywriting Contract + Z-Index Stack
    - 107-RESEARCH.md Example 4 (ControlsMenu props/emits), Open Question 2 (GameShell calls stopDemo on game-over), Open Question 3 (Teaching group only when game has AI config)
  </read_first>
  <action>
    Extend ControlsMenu.vue props with `showHint?: boolean` (undefined hides the whole Teaching group), `hintDisabled?: boolean`, `isDemoRunning?: boolean`, `isHeatmapVisible?: boolean`; add emit `'teaching-action': ['hint' | 'demo-toggle' | 'heatmap-toggle']`. After the Session group, add a `v-if="showHint !== undefined"` block: a `.sep` + `.grouplabel` "Teaching"; a role=menuitem "Get a hint" `:disabled="hintDisabled"` emitting `('teaching-action','hint')` then close(); a role=menuitem button whose label is `isDemoRunning ? 'Stop demo' : 'Watch AI demo'` emitting `('teaching-action','demo-toggle')` then close(); a role=menuitemcheckbox "Show move quality" with `:aria-checked="isHeatmapVisible"` and the existing `.toggle` markup emitting `('teaching-action','heatmap-toggle')` (no close). In GameShell.vue: import HintOverlay + HeatmapOverlay; mount `<HintOverlay />` and `<HeatmapOverlay />` immediately after `<TutorialOverlay />` (line 1704, same outside-zoom-container placement + comment rationale). Wire ControlsMenu: pass `:show-hint` (true only when the game definition has a non-null `ai` config, else undefined), `:hint-disabled` (true when the local player is not at a decision point — reuse the existing isMyTurn/availableActions signals), `:is-demo-running="session.isDemoRunning"`, `:is-heatmap-visible` (from current broadcast `state.heatmap?.visible`); add `@teaching-action` handler that maps 'hint'→`session.requestHint(seat)` (catch failure → existing toast "Hint unavailable — the AI could not suggest a move."), 'demo-toggle'→`session.isDemoRunning ? session.stopDemo() : session.startDemo()`, 'heatmap-toggle'→`session.setHeatmapVisible(seat, !current)`. Add a watcher on `flowState.complete` (alongside the existing GameOverCard logic) that calls `session.stopDemo()` if `session.isDemoRunning` — keeping GameSession passive. Ensure parity: do NOT branch the overlay mounts on custom-vs-auto UI — they mount once over `.boardregion` for both paths (same as TutorialOverlay).
  </action>
  <verify>
    <automated>npx vitest run src/ui 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "teaching-action" src/ui/components/ControlsMenu.vue` >= 1; Teaching group gated on `showHint !== undefined`.
    - `grep -c "HintOverlay\|HeatmapOverlay" src/ui/components/GameShell.vue` >= 2 (import + mount each).
    - GameShell @teaching-action maps to requestHint/setHeatmapVisible/startDemo/stopDemo; demo stops on flowState.complete.
    - Overlay mounts are not conditional on UI mode (single mount, both paths).
  </acceptance_criteria>
  <done>Teaching controls surface in ControlsMenu only when AI is configured; GameShell mounts both overlays once and wires every teaching action to the session.</done>
</task>

<task type="auto">
  <name>Task 3: Parity component tests (custom UI + AutoUI anchors)</name>
  <files>src/ui/components/helpers/HintOverlay.test.ts, src/ui/components/helpers/HeatmapOverlay.test.ts</files>
  <read_first>
    - src/ui/components/helpers/TutorialOverlay.test.ts (jsdom env, matchMedia/ResizeObserver stubs, makeGameState helper, mount/inject pattern, parity fixtures)
    - 107-PATTERNS.md sections "HintOverlay.test.ts" and "HeatmapOverlay.test.ts"
    - 107-RESEARCH.md Validation Architecture (component test rows) + 107-VALIDATION.md (parity is automated at component level; visual deferred to Phase 110)
  </read_first>
  <behavior>
    - HintOverlay: with `state.hint.annotation` targeting a `data-bs-el-id`, the ring resolves to that element's rect; no ring when hint absent.
    - HeatmapOverlay: chips render for each entry when `visible:true`; none when `visible:false` or `entries:[]`; the `isBest` entry gets the 2px-solid-border class; each chip shows a "%" badge.
    - Parity: both overlays resolve targets via `data-bs-el-id` regardless of whether the fixture markup mimics a custom-UI board element or an AutoUI grid cell (same anchor, two fixtures → identical resolution), mirroring the Phase 105 dual-path parity test.
  </behavior>
  <action>
    Create HintOverlay.test.ts and HeatmapOverlay.test.ts following TutorialOverlay.test.ts (jsdom env, stub matchMedia + ResizeObserver, provide injected `gameState` ref). For HintOverlay: `makeGameState(annotation?)` returning `{ state: { hint: annotation ? { annotation } : undefined } }`; assert a ring renders and resolves the `data-bs-el-id` target, and renders nothing when hint is undefined. For HeatmapOverlay: `makeGameState(entries, visible=true)`; mount fixtures that include elements carrying `data-bs-el-id` matching each entry's `cellRef.id`; assert chip count equals entry count when visible, zero when `visible:false` or empty entries, the `isBest` chip carries the best-move border class, and each chip's text matches `round(value*100)+"%"`. Add a parity assertion: define two fixtures whose target elements use the SAME `data-bs-el-id` but different surrounding structure (one custom-UI-like, one AutoUI-grid-like); assert both resolve and position the overlay identically (same anchor attribute → same selector path). Use real component mounts and DOM querying — no stubbed positions beyond the standard ResizeObserver/matchMedia globals.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/helpers/HintOverlay.test.ts src/ui/components/helpers/HeatmapOverlay.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist and pass under `npx vitest run src/ui`.
    - HeatmapOverlay test asserts chip count == entries when visible and 0 when not visible.
    - A parity test asserts identical anchor resolution across a custom-UI-like and an AutoUI-grid-like fixture (same data-bs-el-id).
  </acceptance_criteria>
  <done>Component-level parity proven: hint + heatmap overlays resolve the shared data-bs-el-id anchors identically across both UI fixtures (final visual confirmation deferred to Phase 110).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broadcast state → DOM rendering | hint/heatmap/narration text + refs rendered into overlays |
| overlay layer → player input | fixed overlays sit over the board |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-08 | Tampering | XSS via narration/hint text | mitigate | BoardMessage renders text via Vue interpolation only; `grep -c v-html` == 0 asserted; text is engine-derived |
| T-107-09 | Tampering | overlay intercepting clicks | mitigate | Overlay roots + chips all `pointer-events: none` (T-105-07 pattern); test/CSS asserts |
| T-107-10 | Information Disclosure | hint highlights leak state in wrong UI | accept | Checkers perfect-info; parity via shared anchors means no per-renderer leak path; imperfect-info gating deferred to Phase 109+ |
| T-107-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages (RESEARCH Package Legitimacy Audit — none) |
</threat_model>

<verification>
- `npx vitest run src/ui` green; full suite `npx vitest run` green before verify-work.
- HintOverlay + HeatmapOverlay resolve `data-bs-el-id` anchors; parity proven across both UI fixtures.
- No v-html; overlays pointer-events:none; heatmap chips carry numeric % badge (non-color cue).
</verification>

<success_criteria>
- AI-01: the requested hint highlights the suggested target cell via the annotation overlay (TUT-01 reuse).
- AI-02: each AI-demo move is announced as a narration card before it executes.
- AI-03: a toggled heatmap visualizes per-candidate-cell evaluation with intensity + numeric % badge.
- Parity: all three render identically in custom UI and AutoUI (component tests over shared anchors).
</success_criteria>

<output>
Create `.planning/phases/107-ai-assisted-teaching/107-04-SUMMARY.md` when done.
</output>
