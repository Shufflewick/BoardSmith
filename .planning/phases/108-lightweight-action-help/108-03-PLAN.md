---
phase: 108-lightweight-action-help
plan: 03
type: execute
wave: 3
depends_on: ["108-02"]
files_modified:
  - src/ui/components/ControlsMenu.vue
  - src/ui/components/ControlsMenu.action-help.test.ts
  - src/ui/components/GameShell.vue
  - src/ui/components/GameShell.action-help.test.ts
autonomous: true
requirements: [HELP-02]
must_haves:
  truths:
    - "A player can toggle 'Show action help' on/off from the ControlsMenu Play group (visible in non-AI games)"
    - "The toggle state persists across actions and reloads via localStorage (key boardsmith_action_help), defaulting ON when absent"
    - "isActionHelpVisible is threaded into BOTH the AutoUI ActionPanel and the #game-board custom-UI slot props (parity)"
    - "The dead disabledActions field is threaded from GameShell broadcast state into ActionPanel"
  artifacts:
    - path: "src/ui/components/ControlsMenu.vue"
      provides: "'Show action help' menuitemcheckbox in Play group + isActionHelpVisible prop + help-toggle emit"
      contains: "help-toggle"
    - path: "src/ui/components/GameShell.vue"
      provides: "getActionHelpEnabled/setActionHelpEnabled + isActionHelpVisible ref + disabledActions computed + wiring + slot prop"
      contains: "boardsmith_action_help"
  key_links:
    - from: "src/ui/components/ControlsMenu.vue"
      to: "GameShell handleTeachingAction"
      via: "emit teaching-action 'help-toggle'"
      pattern: "help-toggle"
    - from: "src/ui/components/GameShell.vue"
      to: "localStorage"
      via: "get/setActionHelpEnabled read/write boardsmith_action_help"
      pattern: "boardsmith_action_help"
    - from: "src/ui/components/GameShell.vue"
      to: "ActionPanel + #game-board slot"
      via: ":is-action-help-visible + :disabled-actions threaded to both paths"
      pattern: "is-action-help-visible"
---

<objective>
Deliver HELP-02: a global "Show action help" toggle in the ControlsMenu Play group that emits `help-toggle`, persisted to `localStorage` (`boardsmith_action_help`, default ON) as a GameShell ref, threaded as `isActionHelpVisible` into BOTH the AutoUI `ActionPanel` and the custom-UI `#game-board` slot props (parity). GameShell also threads the broadcast `disabledActions` field into ActionPanel, completing the dead-field wiring from Plan 02.

Purpose: One global client preference holds across actions and reloads, drives the affordance in both UI paths, and is placed in the Play group (NOT the AI-gated Teaching group) so it shows in non-AI games. Pure client display state — no server round-trip.
Output: ControlsMenu toggle + emit; GameShell localStorage helpers + ref + computed + wiring + slot props; tests for the emit, persistence default/round-trip, and dual-path threading parity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/108-lightweight-action-help/108-CONTEXT.md
@.planning/phases/108-lightweight-action-help/108-RESEARCH.md
@.planning/phases/108-lightweight-action-help/108-PATTERNS.md
@.planning/phases/108-lightweight-action-help/108-UI-SPEC.md

<interfaces>
ControlsMenu (src/ui/components/ControlsMenu.vue):
- New prop `isActionHelpVisible?: boolean` (mirrors existing `isHeatmapVisible?: boolean`).
- Extend emit union: `'teaching-action': [action: 'hint' | 'demo-toggle' | 'heatmap-toggle' | 'help-toggle']`.
- New `menuitemcheckbox` button placed in the PLAY group (after "Auto end turn", before "Undo last action") — NOT inside `<template v-if="showHint !== undefined">` (Teaching group), so it shows in non-AI games (Pitfall 6). Mirrors the heatmap toggle template exactly: `:aria-checked="isActionHelpVisible"`, `@click="emit('teaching-action','help-toggle')"`, `.toggle :class="{ on: isActionHelpVisible }"`.

GameShell (src/ui/components/GameShell.vue):
- localStorage helpers `getActionHelpEnabled()` / `setActionHelpEnabled(value)` keyed `boardsmith_action_help`, try/catch, default true when key absent (mirror getPlayerName ~lines 88-96).
- `const isActionHelpVisible = ref(getActionHelpEnabled())`.
- `disabledActions` computed cast from `(state.value?.state as any)?.disabledActions` (mirror actionMetadata computed ~line 324).
- `handleTeachingAction` (~lines 681-717): extend union with `'help-toggle'` and add a branch that flips `isActionHelpVisible.value` and calls `setActionHelpEnabled` — NO platformRequest.
- ControlsMenu usage (~lines 1871-1885): add `:is-action-help-visible="isActionHelpVisible"`.
- ActionPanel usage (~lines 1916-1929): add `:is-action-help-visible="isActionHelpVisible"` and `:disabled-actions="isViewingHistory ? undefined : disabledActions"`.
- `#game-board` slot (~lines 1828-1847) AND the dev UI-switcher `<component :is="selectedUiComponent">` block (~lines 1810-1827): add `:is-action-help-visible="isActionHelpVisible"` to slot props for custom-UI parity.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add "Show action help" toggle to ControlsMenu Play group</name>
  <files>src/ui/components/ControlsMenu.vue, src/ui/components/ControlsMenu.action-help.test.ts</files>
  <read_first>
    - src/ui/components/ControlsMenu.vue (defineProps ~lines 19-45 incl. isHeatmapVisible; emit union ~lines 47-58; Play group "Auto end turn"/"Undo" rows ~lines 163-178; heatmap toggle blueprint ~lines 264-277; toggle CSS ~lines 400-427)
    - 108-PATTERNS.md section "src/ui/components/ControlsMenu.vue"
    - 108-RESEARCH.md Pitfall 6 (toggle MUST be in Play group, not the showHint-gated Teaching group)
    - 108-UI-SPEC.md section 4 (toggle template + placement) and "Copywriting Contract" ("Show action help")
    - src/ui/components/helpers/HeatmapOverlay.test.ts (component test style for a menu emit)
  </read_first>
  <behavior>
    - Test: ControlsMenu renders a `menuitemcheckbox` labelled "Show action help" even when `showHint` is undefined (non-AI game).
    - Test: clicking it emits `teaching-action` with payload `'help-toggle'`.
    - Test: `aria-checked` reflects the `isActionHelpVisible` prop (true → checked, false → unchecked); the `.toggle` pill carries the `on` class when true.
  </behavior>
  <action>
    In src/ui/components/ControlsMenu.vue add `isActionHelpVisible?: boolean` to defineProps (next to isHeatmapVisible) and add `'help-toggle'` to the `teaching-action` emit union. Insert a new `menuitemcheckbox` button in the PLAY group after the "Auto end turn" row and before "Undo last action" — copy the heatmap toggle structure verbatim, swapping the label to "Show action help", the circled-question-mark SVG (24x24 viewBox per UI-SPEC), `:aria-checked="isActionHelpVisible"`, `@click="emit('teaching-action','help-toggle')"`, and `.toggle :class="{ on: isActionHelpVisible }"`. Do NOT place it inside the `<template v-if="showHint !== undefined">` Teaching group. No new CSS (the `.toggle`/`.toggle.on` pill styles are inherited). Create src/ui/components/ControlsMenu.action-help.test.ts covering the behavior cases above.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/ControlsMenu.action-help.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "help-toggle" src/ui/components/ControlsMenu.vue` returns at least 1 and appears in the emit union.
    - The "Show action help" button is NOT inside a `v-if="showHint` block (verify by reading the Play group placement).
    - Test asserts the toggle renders with `showHint` undefined and emits `'help-toggle'` on click.
    - `npx vitest run src/ui/components/ControlsMenu.action-help.test.ts` passes.
  </acceptance_criteria>
  <done>ControlsMenu shows a persistent "Show action help" pill in the Play group (visible in non-AI games) that emits `help-toggle` and reflects `isActionHelpVisible` — covered by component tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GameShell localStorage persistence + dual-path wiring</name>
  <files>src/ui/components/GameShell.vue, src/ui/components/GameShell.action-help.test.ts</files>
  <read_first>
    - src/ui/components/GameShell.vue (getPlayerName/setPlayerName ~lines 88-96; actionMetadata computed ~line 324; handleTeachingAction ~lines 681-717; ControlsMenu usage ~lines 1871-1885; #game-board slot ~lines 1828-1847; dev UI-switcher component block ~lines 1810-1827; ActionPanel usage ~lines 1916-1929)
    - 108-PATTERNS.md section "src/ui/components/GameShell.vue" (all 7 numbered edits)
    - 108-RESEARCH.md Pitfall 2 (localStorage persists between jsdom tests — clear it in beforeEach) and "Open Questions" (disabledActions as prop; isActionHelpVisible surfaced in slot props — both resolved YES)
    - 108-UI-SPEC.md section 5 (localStorage key, default ON) and "Parity Contract"
    - src/ui/components/GameShell.tutorial.test.ts (existing GameShell test harness/mount style to mirror)
  </read_first>
  <behavior>
    - Test: with no `boardsmith_action_help` key set, GameShell initializes `isActionHelpVisible` to true (default ON).
    - Test: with `boardsmith_action_help='false'` in localStorage, GameShell initializes to false.
    - Test: invoking the help-toggle handler flips the value AND writes the new string value to localStorage (persists across actions/reloads).
    - Test (parity): `isActionHelpVisible` is passed to the AutoUI ActionPanel AND exposed in the `#game-board` slot props, so a custom UI receives the same flag (assert both bindings present — e.g. slot scope exposes is-action-help-visible).
    - Test: `disabledActions` from broadcast `state.state.disabledActions` is threaded into ActionPanel.
  </behavior>
  <action>
    In src/ui/components/GameShell.vue add `getActionHelpEnabled()`/`setActionHelpEnabled(value)` near the player-name helpers, keyed `boardsmith_action_help`, wrapped in try/catch, returning true when the key is absent. Add `const isActionHelpVisible = ref(getActionHelpEnabled())`. Add a `disabledActions` computed casting `(state.value?.state as any)?.disabledActions` to `Record<string,string> | undefined`, mirroring the actionMetadata computed. Extend `handleTeachingAction`'s union with `'help-toggle'` and add a branch that flips `isActionHelpVisible.value` and calls `setActionHelpEnabled(isActionHelpVisible.value)` with NO platformRequest. Pass `:is-action-help-visible="isActionHelpVisible"` to ControlsMenu. Pass `:is-action-help-visible="isActionHelpVisible"` and `:disabled-actions="isViewingHistory ? undefined : disabledActions"` to ActionPanel. Add `:is-action-help-visible="isActionHelpVisible"` to the `#game-board` slot props AND to the dev UI-switcher `<component :is="selectedUiComponent">` block (custom-UI + switcher parity). Create src/ui/components/GameShell.action-help.test.ts with `beforeEach(() => localStorage.clear())` covering the behavior cases above.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/GameShell.action-help.test.ts src/ui/components/ControlsMenu.action-help.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "boardsmith_action_help" src/ui/components/GameShell.vue` returns at least 1 (in get/set helpers).
    - `grep -c "is-action-help-visible" src/ui/components/GameShell.vue` returns at least 3 (ControlsMenu, ActionPanel, slot — plus switcher block).
    - `grep -n "disabled-actions" src/ui/components/GameShell.vue` matches in the ActionPanel binding.
    - GameShell.action-help.test.ts uses `localStorage.clear()` in beforeEach and asserts default-ON, persisted-OFF, toggle-writes-localStorage, and dual-path threading.
    - `npx vitest run src/ui` is green; full suite `npx vitest run` is green.
  </acceptance_criteria>
  <done>The global toggle persists via localStorage (default ON), survives reloads, and drives the affordance identically in AutoUI and custom-UI paths; the dead disabledActions field is threaded into ActionPanel — covered by GameShell tests including a parity assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → client state | persisted toggle string is read back on init |
| broadcast state → ActionPanel | disabledActions reasons flow from server to UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-05 | Tampering | localStorage boardsmith_action_help | mitigate | Read inside try/catch; treat any value other than the string `'true'`/`'false'` as the default (ON); never eval the stored value. Default-safe degradation in private browsing. |
| T-108-06 | Repudiation/UX | toggle not visible in non-AI games | mitigate | Toggle placed in the Play group, not the showHint-gated Teaching group (Pitfall 6); test asserts visibility with showHint undefined. |

No package installs in this plan — supply-chain checkpoint not applicable.
</threat_model>

<verification>
- `npx vitest run` (full suite) is green.
- Toggle visible with `showHint` undefined; localStorage default ON; toggle round-trips and persists.
- `isActionHelpVisible` reaches both ActionPanel and `#game-board` slot props (parity).
</verification>

<success_criteria>
- A player can toggle action help on/off globally and the setting holds across actions and reloads (HELP-02).
- The toggle drives the "?" affordance identically in custom UI and AutoUI (parity hard-rule).
- The v2.8 disabledActions reason is now rendered through GameShell wiring (closes the why-disabled loop end-to-end).
</success_criteria>

<output>
Create `.planning/phases/108-lightweight-action-help/108-03-SUMMARY.md` when done
</output>
