---
phase: 109-checkers-tutorial-content
plan: 04
type: execute
wave: 3
depends_on: ["109-03"]
files_modified:
  - ~/BoardSmithGames/checkers/tests/tutorial.test.ts   # checkers repo (NEW, Wave 0 scaffold)
autonomous: true
requirements: [CHK-01, CHK-02, CHK-03, CHK-04]
must_haves:
  truths:
    - "simulateTutorial walks the full checkers tutorial (capture-tip → execute-capture → multi-jump-continue → confirm-turn) and assertTutorialCompletes passes against intact rules"
    - "Deliberately breaking the mandatory-capture rule makes the walkthrough fail with a Tutorial drift error (green→red proven)"
    - "stepsVisited confirms every taught beat (capture-tip, execute-capture, multi-jump-continue) was actually reached, not skipped"
  artifacts:
    - path: "~/BoardSmithGames/checkers/tests/tutorial.test.ts"
      provides: "CI-verifiable tutorial walkthrough + green→red break proof"
      contains: "simulateTutorial"
  key_links:
    - from: "~/BoardSmithGames/checkers/tests/tutorial.test.ts"
      to: "CHECKERS_TUTORIAL + simulateTutorial/assertTutorialCompletes"
      via: "boardsmith/testing DSL over the tutorialSetup preset"
      pattern: "simulateTutorial\\("
---

<objective>
Author the CI-verifiable artifact (TUT-04) for the checkers tutorial: a test that walks the complete tutorial headlessly and FAILS when a checkers rule drifts. This is the highest-risk item — the test must prove green→red, not pass vacuously.

Purpose: Satisfies the phase goal's CI-verifiable requirement and provides regression protection for CHK-01/02/03 end-to-end.
Output: `~/BoardSmithGames/checkers/tests/tutorial.test.ts` with an intact-walkthrough test (assertTutorialCompletes + stepsVisited) and a green→red break test.

CROSS-REPO: this file lives in `~/BoardSmithGames/checkers` (SEPARATE git repo). Run and commit IN THAT REPO. boardsmith/testing resolves live via the symlink — no rebuild.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/109-checkers-tutorial-content/109-RESEARCH.md
@.planning/phases/109-checkers-tutorial-content/109-PATTERNS.md

<interfaces>
<!-- DSL + scenario seams verified by research. Executor uses directly. -->

boardsmith/testing exports: TestGame.create, simulateTutorial, assertTutorialCompletes, type TutorialScenarioMove.
simulateTutorial(testGame, tutorialDef, { seat, scenario, seed }) → { completed, finalStepId, stepsVisited }. Pumps autoAdvance once at start.
Scenario move: { seat?, action, args?, expectStep? }. Default seat is the tutorial seat; seat-2 moves MUST be explicit.

Two-step move args: { piece: <CheckerPiece instance>, destination: <full DestinationChoice object> }. Build destination from getValidMoves():
  { pieceId, fromNotation: from.notation, toNotation: to.notation, isCapture, becomesKing, capturedNotations: captures.map(s=>s.notation) }.

PITFALLS (RESEARCH):
- Include seat-2 turns ({ seat:2, action:'move', args }, { seat:2, action:'endTurn' }) wherever eachPlayer requires them, or you get "Not Player N's turn".
- Derive piece ids + destinations from testGame.game.getValidMoves(), NEVER hard-code notation.
- Green→red break: monkey-patch testGame.game.playerHasCaptures = () => false (or subclass overriding getValidMoves to drop captures). The capture-tip advanceWhen then never fires → simulateTutorial throws /Tutorial drift/.

Analog: BoardSmith src/testing/tutorial-ci-demo.test.ts (full structure, lines 229-341 intact + break). Pattern: 109-PATTERNS.md "tests/tutorial.test.ts (NEW)".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI tutorial walkthrough + green→red break proof</name>
  <files>~/BoardSmithGames/checkers/tests/tutorial.test.ts (NEW) (checkers repo)</files>
  <read_first>
    - BoardSmith src/testing/tutorial-ci-demo.test.ts (intact walkthrough + green→red break structure)
    - ~/BoardSmithGames/checkers/tests/tutorial-preset.test.ts (Plan 03 — how piece ids/destinations are derived from the preset)
    - ~/BoardSmithGames/checkers/src/rules/tutorial.ts (the four step ids + advanceWhen conditions from Plan 03)
    - 109-PATTERNS.md section "tests/tutorial.test.ts (NEW)" + "Test scenario structure" shared pattern
    - 109-VALIDATION.md (sampling + green→red sign-off requirement)
  </read_first>
  <action>
    Working dir: `cd ~/BoardSmithGames/checkers`. Create `tests/tutorial.test.ts`.
    Intact test ("tutorial completes against intact rules"): create `TestGame.create(CheckersGame, { playerCount:2, playerNames:['Learner','Opponent'], seed:'checkers-tutorial-pinned', tutorialSetup:true })`. Derive the learner's first capture move and the continuation jump from `getValidMoves()` (filter `captures.length>0`); build full `DestinationChoice` args for each. Construct a `WALKTHROUGH: TutorialScenarioMove[]` that: executes the capture (expectStep 'multi-jump-continue'), executes the continuation jump (expectStep 'confirm-turn'), ends seat-1's turn, then includes the required seat-2 move + endTurn so the eachPlayer flow advances. Call `simulateTutorial(testGame, CHECKERS_TUTORIAL, { seat:1, scenario:WALKTHROUGH, seed:'checkers-tutorial-pinned' })`, then `assertTutorialCompletes(result)` and assert `result.stepsVisited` contains 'capture-tip', 'execute-capture', and 'multi-jump-continue' (proves no beat was silently skipped).
    Break test ("mandatory-capture rule removed → tutorial drifts"): same setup, then `testGame.game.playerHasCaptures = () => false` (simulate the rule removal). Assert `expect(() => simulateTutorial(testGame, CHECKERS_TUTORIAL, { seat:1, scenario:WALKTHROUGH })).toThrow(/Tutorial drift/)`. Confirm the failure is for the RIGHT reason — the thrown drift references the capture-tip step failing to advance (the predicate no longer fires), not an unrelated error.
    If the start-of-run auto-advance pumps past capture-tip (RESEARCH Pitfall 4), adjust the preset/step design with Plan 03's author so the capture-tip beat is genuinely reached before advancing — do not weaken the assertion to make it pass.
  </action>
  <verify>
    <automated>cd ~/BoardSmithGames/checkers && npx vitest run tests/tutorial.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The intact test passes: `assertTutorialCompletes` succeeds and `stepsVisited` contains capture-tip, execute-capture, multi-jump-continue.
    - GREEN→RED PROOF: the break test passes by asserting the walkthrough THROWS `/Tutorial drift/` once `playerHasCaptures` is forced false — and the executor has confirmed the drift is attributable to the capture-tip step not advancing (the right reason), documented in the SUMMARY.
    - Piece ids + destinations are derived from `getValidMoves()` (grep in checkers repo: `grep -c 'getValidMoves' tests/tutorial.test.ts` ≥ 1); no hard-coded notation literals drive the moves.
    - Required seat-2 turns are present in the scenario (grep: `grep -c 'seat: 2' tests/tutorial.test.ts` ≥ 1).
    - Full checkers suite (`npx vitest run`) green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→game | Headless CI harness drives the engine; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-05 | Repudiation | vacuous green test (passes without proving the rule) | mitigate | Mandatory green→red proof: break test asserts a /Tutorial drift/ throw, confirmed attributable to capture-tip |
| T-109-SC | Tampering | npm/pip/cargo installs | accept | No package installs (audit N/A) |
</threat_model>

<verification>
- `cd ~/BoardSmithGames/checkers && npx vitest run tests/tutorial.test.ts` green.
- Intact walkthrough completes; the deliberate mandatory-capture break fails the tutorial (green→red), for the right reason.
</verification>

<success_criteria>
- The checkers tutorial is a CI-verifiable artifact (TUT-04): a checkers-rules change that breaks the taught beats fails this test.
- Full step-transition walkthrough (CHK-01/02/03) verified headlessly.
</success_criteria>

<output>
Create `.planning/phases/109-checkers-tutorial-content/109-04-SUMMARY.md` in the BoardSmith repo. Record the green→red confirmation (the drift was attributable to capture-tip).
Commit the test IN THE CHECKERS REPO (`cd ~/BoardSmithGames/checkers && git add/commit`).
</output>
