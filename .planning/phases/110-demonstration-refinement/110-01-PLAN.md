---
phase: 110-demonstration-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/session/snapshot-session-host.ts          # BoardSmith
  - src/session/snapshot-session-host.test.ts      # BoardSmith
  - src/ui/components/GameShell.vue                # BoardSmith
  - src/ui/components/GameShell.test.ts            # BoardSmith (create if absent)
autonomous: true
requirements: [DEMO-01]

must_haves:
  truths:
    - "When any seat is AI in the dev host, every broadcast playerView carries state.hasAIPlayers === true"
    - "The Teaching controls group becomes visible in platform (dev-host) mode when AI seats are present, while the production lobby path is unchanged"
    - "Per-seat hint/heatmap and game-wide narration/isDemoRunning/hasAIPlayers are merged into broadcast views AFTER buildPlayerState, leaving buildPlayerState pure"
  artifacts:
    - path: "src/session/snapshot-session-host.ts"
      provides: "transientTeachingState Map + demoRunning/narrationText/lastPlayerViews fields + mergeTransientState() + broadcastCurrent() + hasAIPlayers injection"
      contains: "mergeTransientState"
    - path: "src/ui/components/GameShell.vue"
      provides: "showHintProp reads broadcast state.hasAIPlayers as an OR-fallback"
      contains: "hasAIPlayers"
  key_links:
    - from: "src/ui/components/GameShell.vue"
      to: "broadcast state.hasAIPlayers"
      via: "showHintProp computed OR-condition"
      pattern: "hasAIPlayers"
    - from: "SnapshotSessionHost.apply()"
      to: "adapters.broadcast"
      via: "mergeTransientState(res.playerViews)"
      pattern: "mergeTransientState"
---

<objective>
Lay the dev-host teaching foundation: give `SnapshotSessionHost` a place to hold transient teaching state (hint/heatmap/narration/demo flag) and merge it into every broadcast after `buildPlayerState`, mirroring the production `GameSession.broadcast()` injection. Also inject a `hasAIPlayers` signal whenever the host has AI seats, and teach `GameShell.showHintProp` to read it so the Teaching controls un-hide in the dev host (platform mode, where `lobbyInfo` is always null).

Purpose: Every subsequent plan (hint, heatmap, demo loop) writes into `transientTeachingState` and relies on `broadcastCurrent()` / the merge to surface results. Without this foundation the ops have nowhere to persist results and the controls stay hidden. This is the load-bearing substrate.
Output: `snapshot-session-host.ts` with transient-state plumbing + `GameShell.vue` showHintProp fix + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-demonstration-refinement/110-RESEARCH.md
@.planning/phases/110-demonstration-refinement/110-PATTERNS.md
@.planning/phases/110-demonstration-refinement/110-VALIDATION.md

<interfaces>
<!-- From PATTERNS.md — extracted; executor should use directly, no exploration needed. -->

SnapshotSessionAdapters (snapshot-session-host.ts:38-44):
  playerCount: number
  executeOp: (snapshot, pendingState|null, op: Op) => Promise<OpResult>
  broadcast: (playerViews: unknown[], meta: { isComplete; winners }) => void
  aiSeats?: Array<{ seat: number; level?: string }>
  persist?: (state) => void | Promise<void>

Production injection to replicate (game-session.ts:1925-1934):
  per-seat: state.hint, state.heatmap  (keyed by seat position)
  game-wide: state.narration = { text }, state.isDemoRunning = true

PlayerGameState fields (types.ts:457-484): hint, heatmap, narration, isDemoRunning, hasAIPlayers (new).
GameShell.vue:687-689 isDemoRunning reads (state.value?.state as any)?.isDemoRunning.
GameShell.vue:694 current showHintProp reads lobbyInfo.value?.slots only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add transient teaching state + merge + broadcastCurrent to SnapshotSessionHost</name>
  <files>src/session/snapshot-session-host.ts</files>
  <read_first>
    - src/session/snapshot-session-host.ts (full file — apply() lines ~26-37, handleOp ~47-65, runAITurns ~67-86)
    - PATTERNS.md analog: `src/session/snapshot-session-host.ts` section (apply() + runAITurns guard) and the game-session.ts:1925-1934 broadcast-injection analog
  </read_first>
  <action>
    Add private fields to SnapshotSessionHost: `transientTeachingState = new Map<number, { hint?: { annotation: Annotation }; heatmap?: { visible: boolean; entries: HeatmapEntry[] } }>()`, `demoRunning = false`, `narrationText: string | null = null`, and `lastPlayerViews: unknown[] = []`. Import `Annotation` and `HeatmapEntry` from the session types (same import sites the production GameSession uses).

    Add a private `mergeTransientState(playerViews: unknown[]): unknown[]` method that returns the input unchanged when there is no transient state AND no AI seats (short-circuit), otherwise maps each view by `seat = i + 1`: copy `view.state`, apply per-seat `hint`/`heatmap` from `transientTeachingState.get(seat)`, and apply the game-wide signals to ALL seats — `state.narration = { text: this.narrationText }` when narrationText set, `state.isDemoRunning = true` when demoRunning, `state.hasAIPlayers = true` when `(this.adapters.aiSeats?.length ?? 0) > 0`. This mirrors game-session.ts:1925-1934 exactly. Per D-Area1, `hasAIPlayers` must be injected on EVERY broadcast when AI seats exist — that is the un-hide signal.

    Modify `apply()` to cache `this.lastPlayerViews = res.playerViews` BEFORE broadcasting, then broadcast `this.mergeTransientState(res.playerViews)` instead of the raw `res.playerViews`. Do NOT mutate buildPlayerState — the merge stays above it (Architectural Responsibility Map: buildPlayerState stays pure).

    Add a private `broadcastCurrent(): void` that re-broadcasts `this.mergeTransientState(this.lastPlayerViews)` with the current `{ isComplete, winners }` — used by later plans to re-broadcast transient changes without re-running an op.

    Do NOT add hint/heatmap/demo op handling here — that is Plans 02/03. This task only establishes the storage + merge + broadcastCurrent primitives.
  </action>
  <verify>
    <automated>npx vitest run src/session/snapshot-session-host.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `mergeTransientState`, `broadcastCurrent`, and the four new fields exist on SnapshotSessionHost.
    - Behavior: `apply()` passes a MERGED view array to `adapters.broadcast` (verified by Task 3 test); buildPlayerState is untouched.
    - Behavior: when `adapters.aiSeats` is empty AND no transient state set, `mergeTransientState` returns the input array unchanged (identity short-circuit).
    - Existing snapshot-session-host tests still pass (no regression to apply/runAITurns).
  </acceptance_criteria>
  <done>SnapshotSessionHost owns transientTeachingState + the merge/broadcastCurrent primitives; apply() broadcasts merged views; existing suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Un-hide Teaching controls in platform mode via showHintProp</name>
  <files>src/ui/components/GameShell.vue, src/ui/components/GameShell.test.ts</files>
  <read_first>
    - src/ui/components/GameShell.vue (showHintProp computed line ~694; isDemoRunning computed ~687-689 as the state-access model)
    - PATTERNS.md analog: `src/ui/components/GameShell.vue` section
    - RESEARCH.md Pitfall 5 (breaking production showHintProp)
  </read_first>
  <action>
    Change `showHintProp` (GameShell.vue:694) from a single `lobbyInfo`-based expression to an OR: first return `true` if `lobbyInfo.value?.slots?.some(s => s.aiLevel != null)` (production lobby path, UNCHANGED — must keep working); then return `true` if `(state.value?.state as any)?.hasAIPlayers` (dev-host broadcast signal injected by Plan 01 Task 1); otherwise `undefined`. Use the same `state.value?.state` access shape as the existing `isDemoRunning` computed at line ~687. Per D-Area1, this is the ONLY UI change needed — `ControlsMenu.vue`'s `v-if="showHint !== undefined"` gate is unchanged and now resolves correctly in dev. Per RESEARCH Pitfall 5, this OR is safe by construction: production runs `GameSession` (never sets hasAIPlayers), so the new branch can only fire in the dev host.

    Add a unit test in GameShell.test.ts (create the file following the existing GameShell test conventions if it does not exist): assert `showHintProp === undefined` when BOTH `lobbyInfo` and `state.state.hasAIPlayers` are absent (production-no-AI case); `=== true` when `state.state.hasAIPlayers` is true and lobbyInfo is null (dev-host case); `=== true` when lobbyInfo has an AI slot (production lobby case).
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/GameShell.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: showHintProp references `hasAIPlayers`.
    - Behavior: three-case test green (no-AI → undefined; dev-host hasAIPlayers → true; lobby AI slot → true).
    - Production lobby path is byte-for-byte equivalent for the lobbyInfo branch (no behavioral change when lobbyInfo present).
  </acceptance_criteria>
  <done>Teaching controls un-hide in platform mode when AI seats present; production lobby gating unchanged; platform-mode test green.</done>
</task>

<task type="auto">
  <name>Task 3: BotGame fixture + transient-merge / hasAIPlayers broadcast tests</name>
  <files>src/session/snapshot-session-host.test.ts</files>
  <read_first>
    - src/session/snapshot-session-host.test.ts (full — makeAdapters + broadcastLog pattern lines ~79-92; existing SimpleGame fixture)
    - PATTERNS.md analog: `src/session/snapshot-session-host.test.ts` section (BotGame fixture requirement)
    - RESEARCH.md Wave 0 Gaps (SimpleGame has no bot — a BotGame fixture with objectives + 2+ legal moves is required)
  </read_first>
  <action>
    Add a `BotGame` inline fixture next to the existing SimpleGame: a trivial game with an `objectives` evaluator and at least 2 legal moves at the decision point (so MCTS has a real choice — see MEMORY note: the bot short-circuits with only 1 move), plus an `ai` config on its `GameDefinitionLike`. This fixture is consumed by Plans 02/03 too, so make it reusable/exported within the test module.

    Add tests that exercise THIS plan's foundation only: (1) with `aiSeats` set on the adapters, every entry in the broadcast `playerViews` carries `state.hasAIPlayers === true` after any normal action; (2) with NO aiSeats and no transient state, `mergeTransientState` is identity — broadcast views equal the raw executeOp views (assert hasAIPlayers absent); (3) directly seed `transientTeachingState` for seat 1 with a fake hint, call the path that triggers broadcastCurrent (or apply), and assert seat-1's view has `state.hint` while seat-2's does not (per-seat isolation — RESEARCH Pitfall 6); (4) set `narrationText` and `demoRunning` and assert ALL seats' views carry `state.narration` and `state.isDemoRunning` (game-wide signals).
  </action>
  <verify>
    <automated>npx vitest run src/session/snapshot-session-host.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A reusable BotGame fixture exists with objectives + 2+ legal moves + ai config.
    - hasAIPlayers appears on all broadcast views iff aiSeats present.
    - Per-seat hint isolation proven (seat 1 has hint, seat 2 does not).
    - Game-wide narration/isDemoRunning applied to all seats.
    - Identity short-circuit proven when no transient state and no AI seats.
  </acceptance_criteria>
  <done>Foundation behaviors covered by tests; BotGame fixture ready for downstream plans; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (platformRequest) → SnapshotSessionHost | Untrusted seat/op payloads cross here; dev-host only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-01 | Information disclosure | mergeTransientState seat mapping | mitigate | Per-seat hint/heatmap keyed strictly by `seat = i+1`; test 3 proves no cross-seat leak (RESEARCH Pitfall 6) |
| T-110-02 | Tampering | showHintProp production path | accept | Production runs GameSession which never sets hasAIPlayers; OR-branch is unreachable in prod (RESEARCH Pitfall 5), test-guarded |
</threat_model>

<verification>
- `npx vitest run src/session/snapshot-session-host.test.ts` green
- `npx vitest run src/ui/components/GameShell.test.ts` green
- No change to buildPlayerState (grep confirms injection stays in the host)
</verification>

<success_criteria>
SnapshotSessionHost holds transient teaching state and merges it (per-seat hint/heatmap + game-wide narration/demo/hasAIPlayers) into every broadcast; Teaching controls un-hide in the dev host without breaking the production lobby path.
</success_criteria>

<output>
Create `.planning/phases/110-demonstration-refinement/110-01-SUMMARY.md` when done.
</output>
