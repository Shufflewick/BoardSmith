---
phase: 110-demonstration-refinement
plan: 05
type: execute
wave: 4
depends_on: [110-03, 110-04]
files_modified:
  - .planning/phases/110-demonstration-refinement/110-DEMO-SCRIPT.md     # BoardSmith (new artifact)
  - .planning/phases/110-demonstration-refinement/110-REFINEMENTS.md      # BoardSmith (new artifact, filled during the gate)
autonomous: false
requirements: [DEMO-01]

must_haves:
  truths:
    - "A concrete, ordered demo script exists covering all five teaching beats in BOTH custom checkers UI and AutoUI"
    - "`cd ~/BoardSmithGames/checkers && npx boardsmith dev` launches with no console errors on startup and the Teaching controls are visible with --ai"
    - "The user has walked the script and captured friction/refinement requests as the hands-on refinement checkpoint (DEMO-01 success criterion #4)"
  artifacts:
    - path: ".planning/phases/110-demonstration-refinement/110-DEMO-SCRIPT.md"
      provides: "Ordered browser walkthrough checklist for the five beats in both UIs"
      min_lines: 30
    - path: ".planning/phases/110-demonstration-refinement/110-REFINEMENTS.md"
      provides: "Captured friction + refinement requests from the live session"
  key_links:
    - from: "110-DEMO-SCRIPT.md"
      to: "boardsmith dev (checkers)"
      via: "step-by-step launch + walkthrough instructions"
      pattern: "boardsmith dev"
---

<objective>
Close DEMO-01 with the irreducible human gate. First (autonomous) produce a concrete demo script/checklist and confirm `boardsmith dev` launches clean with the Teaching controls visible. Then (human gate) hand off to the user for the live, hands-on refinement walkthrough across all five teaching beats in BOTH the custom checkers UI and AutoUI, capturing observed friction and refinement requests to inform the substrate before the future cribbage (v2 CRIB) milestone.

Purpose: This is the whole point of Phase 110 — the user's explicit "demonstrate to me so we can refine" gate. It is a refinement checkpoint, NOT a sign-off (CONTEXT D-Area4).
Output: 110-DEMO-SCRIPT.md + a clean dev-host launch + 110-REFINEMENTS.md populated during the session.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-demonstration-refinement/110-CONTEXT.md
@.planning/phases/110-demonstration-refinement/110-VALIDATION.md
@.planning/phases/110-demonstration-refinement/110-03-SUMMARY.md
@.planning/phases/110-demonstration-refinement/110-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the demo script + confirm boardsmith dev launches clean</name>
  <files>.planning/phases/110-demonstration-refinement/110-DEMO-SCRIPT.md</files>
  <read_first>
    - .planning/phases/110-demonstration-refinement/110-VALIDATION.md (Manual-Only Verifications row — the five-beat gate)
    - .planning/phases/110-demonstration-refinement/110-CONTEXT.md (Area 4 walkthrough list)
    - 110-01..04 SUMMARYs (to reference the exact control names/ops as built)
  </read_first>
  <action>
    Before writing, confirm the full automated suites are green: `npx vitest run` in BoardSmith and `cd ~/BoardSmithGames/checkers && npx vitest run`. Then launch `cd ~/BoardSmithGames/checkers && npx boardsmith dev --ai 2` in the background, confirm it serves with NO console errors on startup and that the Teaching controls group is visible, then KILL the dev server (CLAUDE.md hard rule — never leave a process running).

    Write 110-DEMO-SCRIPT.md as an ordered, checkbox walkthrough the user follows. It MUST cover, in both the custom checkers UI AND AutoUI (use the dev-host UI switcher): (1) checkers tutorial start→finish — all 4 beats: two-step selection→destination with gating + overlays, the predicate-triggered mandatory-capture tip, the forced multi-jump continuation, launchable from the game/dev host; (2) AI move hint — the suggested destination square highlights (relies on Plan 04); (3) AI-vs-AI narrated demo — each move announced before it plays at a readable pace, and it STOPS cleanly; (4) evaluation heatmap — toggles on/off with per-cell intensity + best-move badge; (5) action help — hover (pointer) + tap (touch) reveal and the global toggle persisting across actions. Include the exact launch command, where each control lives, and the expected observable for each step. End with a pointer to record findings in 110-REFINEMENTS.md.
  </action>
  <verify>
    <automated>cd ~/BoardSmithGames/checkers && npx vitest run src/rules && cd /Users/jtsmith/BoardSmith && npx vitest run src/session src/ui src/cli/dev-host</automated>
  </verify>
  <acceptance_criteria>
    - Both suites green (BoardSmith + checkers).
    - `boardsmith dev` confirmed launching clean (no startup console errors) with Teaching controls visible — and the server was killed afterward.
    - 110-DEMO-SCRIPT.md exists, ordered, covers all five beats in BOTH UIs with exact commands + expected observables.
  </acceptance_criteria>
  <done>A runnable demo script exists and the dev host is confirmed clean; no lingering dev-server process.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live browser demonstration + refinement capture (human gate)</name>
  <action>PAUSE for the user. This is a non-autonomous human gate: do not auto-advance. Present 110-DEMO-SCRIPT.md, ensure the dev host is running for the user (and killed afterward), and wait for the user to walk all five teaching beats in both UIs. Record every friction point and refinement request into 110-REFINEMENTS.md. This is a refinement checkpoint, not a sign-off.</action>
  <what-built>The full dev-host teaching wiring (hint, heatmap, AI-vs-AI narrated demo, un-hidden Teaching controls) plus the checkers destination-highlight hint, all automated-tested headlessly, with a step-by-step 110-DEMO-SCRIPT.md.</what-built>
  <how-to-verify>
    Follow 110-DEMO-SCRIPT.md in the browser:
    1. `cd ~/BoardSmithGames/checkers && npx boardsmith dev --ai 2` and open the dev host.
    2. Run the checkers tutorial start→finish (4 beats: guided selection→destination, mandatory-capture tip, multi-jump continuation) — overlays + gating + the predicate tip all fire live.
    3. Request an AI hint — the suggested destination square highlights.
    4. Start the AI-vs-AI narrated demo — each move is announced before it plays at a readable pace; stop it and confirm it halts cleanly (no runaway moves).
    5. Toggle the evaluation heatmap on/off — per-cell intensity + best-move badge.
    6. Action help — hover/tap reveal + the global toggle persists.
    7. Repeat the teaching beats in AutoUI via the dev-host UI switcher (parity).
    Capture every friction point and refinement request into 110-REFINEMENTS.md (this is a refinement checkpoint, NOT a sign-off — larger requests are logged as backlog for CRIB, not necessarily built now).
  </how-to-verify>
  <resume-signal>Type "approved" when the demonstration is complete and friction is captured, or describe what to fix/re-demo.</resume-signal>
</task>

</tasks>

<verification>
- Both automated suites green and `boardsmith dev` confirmed clean BEFORE the handoff (no lingering process)
- 110-DEMO-SCRIPT.md covers all five beats in both UIs
- 110-REFINEMENTS.md captures the live-session findings
</verification>

<success_criteria>
DEMO-01 satisfied: the checkers tutorial + all teaching features (TUT/AI/HELP) are demonstrated end-to-end in the browser in both UIs, and observed friction/refinements are captured for the substrate ahead of the cribbage milestone.
</success_criteria>

<output>
Create `.planning/phases/110-demonstration-refinement/110-05-SUMMARY.md` when done, linking 110-DEMO-SCRIPT.md and 110-REFINEMENTS.md.
</output>
