---
phase: 111-host-gated-teaching-lockout
plan: 05
type: execute
wave: 3
depends_on: ["111-03", "111-04"]
files_modified: []
autonomous: false
requirements: [LOCK-01]
must_haves:
  truths:
    - "With --lock-teaching, the four teaching affordances are gone from ControlsMenu in the browser; action help still works"
    - "A crafted teaching op (issued from the browser console / devtools) is rejected fail-loud while --lock-teaching is set"
    - "Without --lock-teaching, all teaching features still work (default unchanged)"
    - "The full BoardSmith vitest suite and eslint are green"
  artifacts: []
  key_links:
    - from: "boardsmith dev --lock-teaching"
      to: "browser: hidden affordances + rejected crafted op"
      via: "broadcast state.teachingDisabled + server guards"
      pattern: "teachingDisabled"
---

<objective>
Final verification gate for LOCK-01: prove the lockout end-to-end in the browser (criterion 2/3 demonstrated live) and prove default-unchanged via the full test suite (criterion 5). This is the integration checkpoint that ties Plans 01–04 together.

Purpose: The lockout is anti-cheat — it must be observed working in a real browser session, and the existing Phase 107–110 behavior must be proven intact.
Output: A passing full-suite + eslint run, plus a human-verified browser demonstration with and without the flag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-host-gated-teaching-lockout/111-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite + eslint green (default-unchanged proof)</name>
  <read_first>
    - .planning/phases/111-host-gated-teaching-lockout/111-CONTEXT.md (criterion 5 — Phase 107–110 suites are the default-unchanged proof)
  </read_first>
  <action>
    Run the full BoardSmith vitest suite and eslint. Confirm zero regressions versus the pre-phase baseline — in particular the Phase 107–110 session + UI suites (teaching, tutorial-controller, stateless-ops, snapshot-session-host, GameShell.*, ControlsMenu.*). If any previously-green test now fails, fix the cause in the owning plan's files before proceeding — do not weaken or skip a test to make it pass.
  </action>
  <verify>
    <automated>npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes with no new failures versus baseline (criterion 5).
    - `npm run lint` clean.
  </acceptance_criteria>
  <done>Full vitest suite and eslint are green; default behavior is proven unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The host teaching lockout (LOCK-01): a session-level teachingDisabled flag enforced server-side (fail-loud op rejection in both GameSession and the dev-host path) and reflected into broadcast state, with GameShell/ControlsMenu hiding the affordances. Exercised via the new `boardsmith dev --lock-teaching` flag.
  </what-built>
  <how-to-verify>
    1. In `~/BoardSmithGames/checkers`, run: `npx boardsmith dev --ai 2 --lock-teaching` (serves on http://localhost:5173). Open the browser via the Claude Chrome extension.
    2. Open the ControlsMenu (the controls/⋯ menu). CONFIRM: there is NO "Get a hint", NO "Watch AI demo", NO "Show move quality", and NO "Start tutorial" item.
    3. CONFIRM action help still works: the per-action "?" affordance reveals help text on hover/tap, and the "Show action help" toggle is present and functional.
    4. Anti-cheat (crafted op): in the iframe's devtools console, issue a teaching op directly via the platform bridge (e.g. post a `server_request` with op `hint` / `heatmap-toggle` / `start-tutorial` / `demo-start`). CONFIRM each returns `success:false` with "Teaching features are disabled for this session." — the hidden UI cannot be bypassed.
    5. Stop the server. Re-run WITHOUT the flag: `npx boardsmith dev --ai 2`. CONFIRM all four teaching affordances are back and functional (default unchanged).
    6. Kill the dev server when done (do not leave :5173 running — per project rule).
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser player (incl. devtools) → running dev host | A player with console access crafts teaching ops to bypass the hidden UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-12 | Tampering | live crafted op via devtools | mitigate | Verified live in step 4 — the server rejects the op fail-loud regardless of UI state, proving the anti-cheat control holds against a real bypass attempt |

No package-manager installs in this plan — supply-chain (T-111-SC) is N/A.
</threat_model>

<verification>
- `npm test && npm run lint` green (criterion 5).
- Human-verified browser demonstration: affordances hidden + action help working + crafted op rejected (criteria 2, 3) and default-restored without the flag (criterion 5).
</verification>

<success_criteria>
- The lockout is demonstrated working live in the browser, including a bypass attempt that the server refuses.
- The full suite and eslint are green; default behavior is unchanged.
- LOCK-01 is fully satisfied across all five success criteria.
</success_criteria>

<output>
Create `.planning/phases/111-host-gated-teaching-lockout/111-05-SUMMARY.md` when done.
</output>
