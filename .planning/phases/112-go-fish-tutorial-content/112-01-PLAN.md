---
phase: 112-go-fish-tutorial-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/tutorial/gate.ts
  - src/engine/tutorial/gate.test.ts
  - src/engine/tutorial/types.ts
autonomous: true
requirements: [GFT-01]
must_haves:
  truths:
    - "gate.selections with a primitive { value } matcher allows the matching primitive choice"
    - "gate.selections with a primitive { value } matcher blocks all non-matching primitive choices"
    - "Existing object / element-ref matching (id > notation > name, field equality) is unchanged"
  artifacts:
    - path: src/engine/tutorial/gate.ts
      provides: "selectionMatchesValue primitive-matcher support"
      contains: "'value' in matcher"
    - path: src/engine/tutorial/gate.test.ts
      provides: "Primitive-matcher unit cases"
      contains: "value: '7'"
  key_links:
    - from: src/engine/tutorial/gate.ts
      to: getGateReasonForValue
      via: "selectionMatchesValue called per choice in the gate hot path"
      pattern: "selectionMatchesValue"
---

<objective>
Close the one confirmed BoardSmith substrate gap that blocks go-fish tutorial rank gating: `selectionMatchesValue` in `src/engine/tutorial/gate.ts` early-returns `false` for any non-object value, so a `gate.selections: { rank: { value: '7' } }` matcher silently blocks ALL primitive rank choices (go-fish's `rank` selection yields primitive strings like `'7'`, `'Q'`).

Purpose: Make declarative `gate.selections` work for `chooseFrom()` selections whose choices are primitive strings/numbers, so go-fish (Wave 2) can gate the ask-for-rank step the same declarative way checkers gates piece/destination selections. This is a real substrate gap per CONTEXT.md ("extend the matcher minimally in BoardSmith with a unit test; do not gate around it"), NOT a go-fish workaround.

Output: A minimal primitive-matcher branch in `selectionMatchesValue`, updated `SelectionMatcher` JSDoc, and unit cases proving primitive match / gate / type-strictness and that existing object matching is untouched. BoardSmith full vitest suite must stay green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-go-fish-tutorial-content/112-RESEARCH.md

<interfaces>
<!-- The exact current implementation the executor is changing. From src/engine/tutorial/gate.ts ~line 66. -->
<!-- selectionMatchesValue(matcher, value): -->
<!--   if (typeof value !== 'object' || value === null) return false;  // ← this line blocks ALL primitives -->
<!--   const val = value as Record<string, unknown>; -->
<!--   if ('id' in matcher) return val['id'] === matcher['id']; -->
<!--   if ('notation' in matcher) return val['notation'] === matcher['notation']; -->
<!--   if ('name' in matcher) return val['name'] === matcher['name']; -->
<!--   return Object.entries(matcher).every(([k, v]) => val[k] === v); -->

<!-- getGateReasonForValue(step, actionName, choice, selectionName) is the caller (action.ts line 388). -->
<!-- `choice` is the RAW value from the choices() function before extractChoiceValue unwrapping. -->
<!-- go-fish 'rank' choices() returns string[] → raw choice is a primitive string '7'. -->
<!-- go-fish 'target' choices() returns { value:number, display:string }[] → object, already matches via field equality. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing primitive-matcher unit cases (RED)</name>
  <files>src/engine/tutorial/gate.test.ts</files>
  <read_first>
    - src/engine/tutorial/gate.ts (the file under change; read `selectionMatchesValue` ~line 66 and `getGateReasonForValue`)
    - src/engine/tutorial/gate.test.ts (existing test structure; add cases inside the existing allow-list/selections describe block, matching its style)
    - src/engine/tutorial/types.ts (SelectionMatcher type + its JSDoc that currently says primitive choices are "not supported")
  </read_first>
  <action>
    Add unit cases to `src/engine/tutorial/gate.test.ts` that exercise primitive `{ value }` matching through `getGateReasonForValue(step, 'ask', choice, 'rank')` with a step whose gate is `{ action: 'ask', selections: { rank: { value: '7' } } }`. Assert four behaviors: (a) `getGateReasonForValue(step, 'ask', '7', 'rank')` returns null (allowed); (b) `getGateReasonForValue(step, 'ask', 'Q', 'rank')` returns a non-null reason (gated); (c) `getGateReasonForValue(step, 'ask', 7, 'rank')` returns a non-null reason (number 7 must NOT match string '7' — strict equality); (d) add/keep one case proving an existing OBJECT choice still matches via the `target` field-equality path (e.g. matcher `{ value: 2 }` against choice `{ value: 2, display: 'Bob' }` returns null, and `{ value: 3, display: 'Cat' }` is gated) so the regression guard is explicit. These cases MUST fail against the current implementation (the primitive ones will report gated for '7' because the current code early-returns false). Do NOT modify gate.ts in this task.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/engine/tutorial/gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - gate.test.ts contains a case asserting `getGateReasonForValue(..., '7', 'rank')` is null with matcher `{ value: '7' }`
    - gate.test.ts contains a case asserting `'Q'` and numeric `7` are both gated (non-null reason) under the same matcher
    - gate.test.ts contains a regression case proving the existing `target` object field-equality path still resolves
    - Running `npx vitest run src/engine/tutorial/gate.test.ts` shows the NEW primitive cases FAILING (RED) and the pre-existing cases still passing
  </acceptance_criteria>
  <done>New primitive-matcher cases exist and fail (RED) against unmodified gate.ts; existing cases unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Implement primitive matcher + JSDoc (GREEN)</name>
  <files>src/engine/tutorial/gate.ts, src/engine/tutorial/types.ts</files>
  <read_first>
    - src/engine/tutorial/gate.ts (apply the change inside `selectionMatchesValue` opening guard)
    - src/engine/tutorial/types.ts (the `SelectionMatcher` JSDoc paragraph that says primitive choices need a predicate — replace it)
    - .planning/phases/112-go-fish-tutorial-content/112-RESEARCH.md (Q1 "Minimal fix" section — the exact branch to add)
  </read_first>
  <action>
    In `selectionMatchesValue` (gate.ts), replace the opening `if (typeof value !== 'object' || value === null) return false;` guard with a primitive branch: when the value is a non-object/null, return `'value' in matcher && matcher['value'] === value` (strict equality). Leave the object path (id > notation > name precedence, then general field equality) exactly as-is below the guard. Update the `SelectionMatcher` JSDoc in `types.ts`: remove the "NOTE: not supported — use a TutorialGateCondition predicate instead" paragraph and document the `{ value: primitiveValue }` form for gating `chooseFrom()` selections whose choices are primitive strings/numbers (cite go-fish rank `{ value: '7' }` as the canonical example). Keep the change minimal — no new helper, no signature change.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/engine/tutorial/gate.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/engine/tutorial/gate.ts` `selectionMatchesValue` contains `'value' in matcher` in the primitive branch with strict `===` comparison
    - All gate.test.ts cases (including the Task 1 primitive cases) pass (GREEN)
    - `src/engine/tutorial/types.ts` SelectionMatcher JSDoc documents the `{ value: primitiveValue }` form and no longer says primitive choices are unsupported
    - `cd /Users/jtsmith/BoardSmith && npm test` passes (full BoardSmith suite green — no regression from the matcher change)
  </acceptance_criteria>
  <done>Primitive `{ value }` matching works, JSDoc updated, full BoardSmith vitest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Change is internal engine logic on already-trusted tutorial-author input; no untrusted input crosses a new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering | selectionMatchesValue widened to accept primitives | mitigate | Strict `===` equality only; unit cases prove number 7 ≠ string '7' and non-matching primitives stay gated, so the gate cannot be loosened to allow unintended choices. |
| T-112-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan or phase; nothing to verify. |
</threat_model>

<verification>
- `cd /Users/jtsmith/BoardSmith && npx vitest run src/engine/tutorial/gate.test.ts` — all primitive + object cases pass.
- `cd /Users/jtsmith/BoardSmith && npm test` — full suite green (no regression).
</verification>

<success_criteria>
- Primitive `{ value: '7' }` matcher allows `'7'`, gates `'Q'` and numeric `7`.
- Existing object/element-ref matching unchanged (regression case green).
- BoardSmith full vitest suite green — the cross-repo symlink will serve this fixed matcher live to go-fish in Wave 2.
</success_criteria>

<output>
Create `.planning/phases/112-go-fish-tutorial-content/112-01-SUMMARY.md` when done.
</output>
