---
phase: 112-go-fish-tutorial-content
plan: 03
type: execute
wave: 3
depends_on: ["112-02"]
files_modified:
  - /Users/jtsmith/BoardSmithGames/go-fish/tests/tutorial.test.ts
autonomous: true
requirements: [GFT-06]
must_haves:
  truths:
    - "simulateTutorial walks the go-fish tutorial to completed with all four beat ids in stepsVisited"
    - "The CI test goes RED when a real go-fish rule is broken (checkForBooks patched to never score)"
    - "A secondary break (turn continuation disabled) also makes the walkthrough fail, tying the test to GFT-04"
  artifacts:
    - path: /Users/jtsmith/BoardSmithGames/go-fish/tests/tutorial.test.ts
      provides: "CI-verifiable tutorial walkthrough + green→red proof"
      contains: "simulateTutorial"
  key_links:
    - from: /Users/jtsmith/BoardSmithGames/go-fish/tests/tutorial.test.ts
      to: GO_FISH_TUTORIAL
      via: "simulateTutorial(testGame, GO_FISH_TUTORIAL, ...)"
      pattern: "GO_FISH_TUTORIAL"
    - from: /Users/jtsmith/BoardSmithGames/go-fish/tests/tutorial.test.ts
      to: GoFishGame.checkForBooks
      via: "monkey-patch to force RED"
      pattern: "checkForBooks = \\(\\) => \\[\\]"
---

<objective>
Author the CI-verifiable tutorial artifact (GFT-06): `tests/tutorial.test.ts` in go-fish using `simulateTutorial` + `assertTutorialCompletes`, proving the four-beat walkthrough completes AND goes red when a real go-fish rule is deliberately broken. This is the same standard as checkers' Phase 109 — the tutorial is a living regression test, not vacuous.

Purpose: Tie the tutorial directly to go-fish's book-formation and turn-continuation rules so a future rule change that breaks the teaching beats fails CI. The green test asserts all four beat ids in `stepsVisited`; the red test patches `checkForBooks` to never score, which prevents the `book-formed` advance from firing and surfaces a "Tutorial drift" throw.

Output: `tests/tutorial.test.ts` with a green walkthrough and two break tests (primary: checkForBooks; secondary: extraTurn/turn-continuation). go-fish full suite green; the break tests assert `.toThrow(/Tutorial/)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/112-go-fish-tutorial-content/112-RESEARCH.md

<interfaces>
<!-- Testing DSL from boardsmith/testing (symlinked live): -->
<!--   TestGame.create(GoFishGame, { playerCount, playerNames, seed, tutorialSetup: true }) -->
<!--   simulateTutorial(testGame, GO_FISH_TUTORIAL, { seat, scenario, seed }) → { status, stepsVisited, ... } -->
<!--   assertTutorialCompletes(result) — throws unless status === 'completed' -->
<!--   TutorialScenarioMove = { action, args, expectStep, seat? } -->
<!-- simulateTutorial does NOT call setup(); tutorialSetup: true on TestGame.create supplies the preset. -->
<!-- Green walkthrough (Option A): two asks by seat 1 — { target:2, rank:'7' } expectStep 'turn-continuation'; { target:2, rank:'Q' } expectStep 'book-formed'. -->
<!-- Red (primary): testGame.game.checkForBooks = () => [] → bookCount never increments → book-formed advance never fires → throws /Tutorial drift/. -->
<!-- If Option B preset was chosen in 112-02, use the Option-B scenario/expectStep ordering from RESEARCH Q6/Q4. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Green walkthrough — tutorial completes, all four beats visited</name>
  <files>/Users/jtsmith/BoardSmithGames/go-fish/tests/tutorial.test.ts</files>
  <read_first>
    - /Users/jtsmith/BoardSmithGames/checkers/tests/tutorial.test.ts (the CI test template: simulateTutorial usage, scenario moves, stepsVisited assertions)
    - /Users/jtsmith/BoardSmithGames/go-fish/src/rules/tutorial.ts (the GO_FISH_TUTORIAL authored in 112-02 — read the actual step ids and preset option used)
    - /Users/jtsmith/BoardSmithGames/go-fish/src/rules/index.ts (confirm GO_FISH_TUTORIAL and GoFishGame exports for the test import)
    - /Users/jtsmith/BoardSmithGames/go-fish/.planning-not-applicable — instead read .planning/phases/112-go-fish-tutorial-content/112-02-SUMMARY.md for Option A/B + pond size
    - .planning/phases/112-go-fish-tutorial-content/112-RESEARCH.md (Q6 CI green test + Code Examples CI skeleton; Pitfall 6: NO P2 moves needed)
  </read_first>
  <action>
    Create `tests/tutorial.test.ts`. In a `describe('Go Fish tutorial — CI walkthrough (GFT-06)')`, add the green `it`: build `TestGame.create(GoFishGame, { playerCount: 2, playerNames: ['Learner','Opponent'], seed: 'go-fish-tutorial-pinned', tutorialSetup: true })`, define the `TutorialScenarioMove[]` walkthrough matching the preset option chosen in 112-02 (Option A: ask `{target:2,rank:'7'}` expectStep `turn-continuation`, then ask `{target:2,rank:'Q'}` expectStep `book-formed`), run `simulateTutorial(testGame, GO_FISH_TUTORIAL, { seat: 1, scenario, seed: 'go-fish-tutorial-pinned' })`, call `assertTutorialCompletes(result)`, and assert `result.stepsVisited` contains all four ids `ask-for-rank`, `turn-continuation`, `go-fish-tip`, `book-formed`. Do NOT add opponent (seat 2) moves — the tutorial completes entirely within the learner's turn (RESEARCH Pitfall 6). Import `GoFishGame` and `GO_FISH_TUTORIAL` from `../src/rules/index.js`.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmithGames/go-fish && npm test -- tutorial</automated>
  </verify>
  <acceptance_criteria>
    - `tests/tutorial.test.ts` exists and imports `simulateTutorial`, `assertTutorialCompletes`, `TestGame`, `TutorialScenarioMove` from `boardsmith/testing`
    - The green test asserts `assertTutorialCompletes(result)` and that `stepsVisited` contains all four beat ids
    - Scenario contains only seat-1 asks (no opponent moves)
    - `cd /Users/jtsmith/BoardSmithGames/go-fish && npm test -- tutorial` passes the green test
  </acceptance_criteria>
  <done>The go-fish tutorial walkthrough completes via simulateTutorial with all four beats in stepsVisited; green test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Green→red proof — broken go-fish rule fails the test</name>
  <files>/Users/jtsmith/BoardSmithGames/go-fish/tests/tutorial.test.ts</files>
  <read_first>
    - /Users/jtsmith/BoardSmithGames/go-fish/src/rules/game.ts (confirm checkForBooks is the SOLE path that increments bookCount; confirm flow extraTurn read for the secondary break)
    - /Users/jtsmith/BoardSmithGames/checkers/tests/tutorial.test.ts (how the checkers red test patches playerHasCaptures / asserts toThrow)
    - .planning/phases/112-go-fish-tutorial-content/112-RESEARCH.md (Q6 red test + Assumption A2: verify bookCount is only incremented inside checkForBooks)
  </read_first>
  <action>
    Add a primary break `it` to `tests/tutorial.test.ts`: build the same tutorialSetup TestGame, then `testGame.game.checkForBooks = () => []` (RESEARCH Q6 — books never score, so `bookCount` stays 0 and the book-formed advance never fires), run `simulateTutorial` with the same scenario, and assert it `.toThrow(/Tutorial drift/)`. Before relying on this, confirm by reading game.ts that `bookCount` is incremented ONLY inside `checkForBooks` (Assumption A2); if it is incremented elsewhere, patch that path too or switch the break to the documented turn-continuation break. Add a secondary break `it` tied to GFT-04: disable turn continuation (force the ask execute's `extraTurn` to false for the patched game, or stub the turn-continuation rule the same monkey-patch way) and assert `simulateTutorial(...)` `.toThrow(/Tutorial/)` because the learner's second ask is rejected once the turn ends. Both break tests MUST genuinely go red against the intact tutorial (prove by running). Per CLAUDE.md, do not leave any patched global state leaking between tests — each break builds its own TestGame.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmithGames/go-fish && npm test -- tutorial && cd /Users/jtsmith/BoardSmith && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Primary break test patches `checkForBooks = () => []` and asserts `.toThrow(/Tutorial drift/)`
    - Secondary break test disables turn continuation and asserts `.toThrow(/Tutorial/)`, tying the artifact to GFT-04
    - SUMMARY confirms (from game.ts read) that bookCount is incremented only by checkForBooks (Assumption A2 verified) — or documents the additional patch needed
    - `cd /Users/jtsmith/BoardSmithGames/go-fish && npm test -- tutorial` passes (green walkthrough + both break tests pass by throwing as expected)
    - `cd /Users/jtsmith/BoardSmith && npm test` stays green
  </acceptance_criteria>
  <done>The tutorial CI artifact proves green→red on two real go-fish rule breaks (book formation + turn continuation); both repos' suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only code; no runtime trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-03 | Tampering | Monkey-patched globals across tests | mitigate | Each break test constructs its own TestGame; no shared/patched state leaks between cases (CLAUDE.md: no hidden state). |
| T-112-SC | Tampering | npm/pip/cargo installs | accept | No package installs; nothing to verify. |
</threat_model>

<verification>
- `cd /Users/jtsmith/BoardSmithGames/go-fish && npm test -- tutorial` — green walkthrough + both break tests green.
- `cd /Users/jtsmith/BoardSmithGames/go-fish && npm test` — full go-fish suite green.
- `cd /Users/jtsmith/BoardSmith && npm test` — BoardSmith suite stays green.
</verification>

<success_criteria>
- simulateTutorial completes with all four beat ids in stepsVisited.
- Two deliberate go-fish rule breaks make the test go red (book formation + turn continuation).
- Both repos' full suites green before phase verification.
</success_criteria>

<output>
Create `.planning/phases/112-go-fish-tutorial-content/112-03-SUMMARY.md` when done. Confirm Assumption A2 verification.
</output>
