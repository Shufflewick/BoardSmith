---
phase: 112-go-fish-tutorial-content
plan: 04
type: execute
wave: 3
depends_on: ["112-02"]
files_modified: []
autonomous: false
requirements: [GFT-05]
must_haves:
  truths:
    - "The ControlsMenu shows a Start-tutorial entry for go-fish in both GameShell and the boardsmith dev host"
    - "Starting the tutorial anchors the first overlay to a named card in the learner's hand, not a board cell"
  artifacts: []
  key_links:
    - from: gameDefinition.tutorial
      to: ControlsMenu "Start tutorial"
      via: "hasTutorial PlayerGameState flag (v4.1 substrate)"
      pattern: "tutorial: GO_FISH_TUTORIAL"
---

<objective>
Visually confirm GFT-05 (and the GFT-01 card-anchoring parity): the go-fish tutorial wired in 112-02 is launchable from within the game in both GameShell and the `boardsmith dev` host, and the first ask-step annotation overlay anchors to a NAMED card in the learner's hand — not a board cell. This is the highest-risk parity item of the milestone (card anchoring vs checkers' board-square anchoring) and the CLAUDE.md hard rule that features are verified in the browser, not just by passing tests.

Purpose: The CI test (112-03) proves logical completion; this checkpoint proves the launch surface lights up purely from `tutorial: GO_FISH_TUTORIAL` (no new BoardSmith substrate) and that the overlay's `ref: { name }` resolves to AutoUI's `data-bs-el-id` on a card. If AutoUI does not surface a resolvable anchor for named cards, that is a flagged BoardSmith/AutoUI substrate gap to fix in BoardSmith — NOT a go-fish workaround.

Output: Human-verified confirmation (or a logged substrate-gap finding). No code changes in the happy path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-go-fish-tutorial-content/112-RESEARCH.md
@.planning/phases/112-go-fish-tutorial-content/112-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Browser smoke — launch tutorial in dev host, verify card anchoring</name>
  <files>(verification only — no files modified; automation done in 112-02)</files>
  <action>This is a human-verification gate, not an implementation task. All automation (attaching tutorial: GO_FISH_TUTORIAL to gameDefinition) was completed in 112-02. The executor's job here is to launch the dev host and confirm the launch surface + card-anchored overlay per the steps below, then kill the dev server. If anchoring fails, record a BoardSmith/AutoUI substrate-gap finding rather than modifying go-fish.</action>
  <read_first>
    - .planning/phases/112-go-fish-tutorial-content/112-VALIDATION.md (Manual-Only Verifications section — the exact smoke steps)
    - .planning/phases/112-go-fish-tutorial-content/112-RESEARCH.md (Q2 card anchoring; Q5 launch surface)
  </read_first>
  <what-built>
    112-02 attached `tutorial: GO_FISH_TUTORIAL` to go-fish's `gameDefinition`. The v4.1 substrate (ControlsMenu "Start tutorial" entry shown when `hasTutorial=true`, the `start-tutorial` op, GameShell `handleTeachingAction`) should light up automatically in both GameShell and the dev host. The first tutorial step anchors its overlay to a learner's held card by name (e.g. '7H').
  </what-built>
  <how-to-verify>
    1. Run `cd /Users/jtsmith/BoardSmithGames/go-fish && npx boardsmith dev` (serves on http://localhost:5173).
    2. Open the game in the browser (use the Claude Code Chrome extension per CLAUDE.md).
    3. Confirm the ControlsMenu shows a "Start tutorial" entry (it must appear because gameDefinition.tutorial now exists).
    4. Click "Start tutorial". Confirm the tutorial starts and the first ask-for-rank annotation overlay visibly anchors to the learner's held seven card in the hand panel — NOT to a board cell, and not floating unanchored.
    5. Confirm the same entry appears in the dev-host chrome path (the platform-mode iframe) — identical to the checkers tutorial entry point.
    6. KILL the dev server (Ctrl-C) before finishing — never leave `boardsmith dev` running (CLAUDE.md hard rule + project memory).
    If the overlay does NOT anchor to the card (e.g. AutoUI does not surface a `data-bs-el-id` for the named card), STOP and report it as a flagged BoardSmith/AutoUI substrate gap — do not patch go-fish to fake the anchor.
  </how-to-verify>
  <acceptance_criteria>
    - ControlsMenu "Start tutorial" entry visible for go-fish in GameShell AND dev host
    - First overlay anchors to a named card in the learner's hand (not a board cell), confirmed visually
    - Dev server killed after verification
    - If anchoring fails: a substrate-gap finding is recorded for BoardSmith (not worked around in go-fish)
  </acceptance_criteria>
  <resume-signal>Type "approved" if the tutorial launches in both hosts and the overlay anchors to a card; otherwise describe what you saw (especially any anchoring failure).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only; no code change in the happy path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-04 | Denial of Service | Stray `boardsmith dev` process | mitigate | Checkpoint explicitly requires killing the dev server after verification (CLAUDE.md hard rule). |
| T-112-SC | Tampering | npm/pip/cargo installs | accept | No package installs; nothing to verify. |
</threat_model>

<verification>
- Human confirms the launch entry in both hosts and card-anchored overlay, then approves.
- Dev server confirmed killed.
</verification>

<success_criteria>
- GFT-05: go-fish tutorial launchable in GameShell and the boardsmith dev host, identical to checkers.
- GFT-01 parity: first overlay anchors to a named card in hand, proving card-game `anchorAttrs` parity with checkers' board-square anchoring.
</success_criteria>

<output>
Create `.planning/phases/112-go-fish-tutorial-content/112-04-SUMMARY.md` when done (record approval or substrate-gap finding).
</output>
