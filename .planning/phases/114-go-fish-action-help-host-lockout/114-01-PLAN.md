---
phase: 114-go-fish-action-help-host-lockout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/BoardSmithGames/go-fish/src/rules/actions.ts
  - ~/BoardSmithGames/go-fish/tests/action-help.test.ts
autonomous: true
requirements: [GFHELP-01]

must_haves:
  truths:
    - "The go-fish ask action carries author-supplied help text"
    - "The help text propagates to ActionMetadata.help (the field both AutoUI and the custom action panel read)"
    - "The auto-draw (Go Fish!) consequence is covered within the ask help text — no separate draw action exists or is invented"
  artifacts:
    - path: "~/BoardSmithGames/go-fish/src/rules/actions.ts"
      provides: ".help(text) on the ask action"
      contains: ".help("
    - path: "~/BoardSmithGames/go-fish/tests/action-help.test.ts"
      provides: "integration test asserting ask ActionMetadata.help carries the authored string"
      contains: "buildActionMetadata"
  key_links:
    - from: "~/BoardSmithGames/go-fish/src/rules/actions.ts (.help on ask)"
      to: "ActionMetadata.help"
      via: "buildActionMetadata (boardsmith/session)"
      pattern: "metadata\\['ask'\\]\\?\\.help"
---

<objective>
Add author-supplied help text to go-fish's `ask` action (its only player action) so that hovering/tapping the action reveals it, governed by the global "Show action help" toggle. Prove the help propagates to `ActionMetadata.help` — the single field both AutoUI and the custom GameTable.vue action panel read.

Purpose: GFHELP-01 — prove the v4.1 Phase 108 action-help substrate generalizes to a card game with zero BoardSmith `src/` changes (pure authoring reuse).
Output: One `.help()` call on `ask` + an integration test asserting metadata propagation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-go-fish-action-help-host-lockout/114-CONTEXT.md
@.planning/phases/114-go-fish-action-help-host-lockout/114-RESEARCH.md

<interfaces>
<!-- The action-help substrate is built (BoardSmith v4.1 Phase 108). Reuse as-is. -->

`.help(text)` builder — BoardSmith src/engine/action/action-builder.ts (mirrors `.prompt()`):
  Action.create('ask').prompt('...').help('...')  // help is a string; copied to ActionDefinition.help

Propagation — boardsmith/session export:
  buildActionMetadata(game, player, ['ask']) => Record<string, ActionMetadata>
  metadata['ask'].help === <the authored string>

Checkers analog (the authoring pattern to mirror) — ~/BoardSmithGames/checkers/src/rules/actions.ts:
  .help('Pick one of your pieces, then a highlighted destination square. ...')  // on `move`
  .help('Confirm your move and pass play to your opponent. You can Undo until you end your turn.')  // on `endTurn`

Canonical propagation test analog — BoardSmith src/session/action-help-propagation.test.ts:
  const metadata = buildActionMetadata(game, player, ['move']);
  expect(metadata['move']?.help).toBe('...');

go-fish action registration — ~/BoardSmithGames/go-fish/src/rules/game.ts:81:
  this.registerAction(createAskAction(this));  // populates game._actions so buildActionMetadata sees 'ask'

createTestGame harness — boardsmith/testing:
  const testGame = createTestGame(GoFishGame, { playerCount: 2, playerNames: ['Alice','Bob'] });
  // testGame.game has _actions populated; testGame.game.getPlayer(1) returns the seat-1 Player
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .help(text) to the ask action</name>
  <files>~/BoardSmithGames/go-fish/src/rules/actions.ts</files>
  <read_first>
    - ~/BoardSmithGames/go-fish/src/rules/actions.ts (the `ask` action — `Action.create('ask').prompt('Ask another player for cards').chooseFrom('target', ...).chooseFrom('rank', ...)`; draw is automatic inside execute via game.drawFromPond — there is NO draw action)
    - ~/BoardSmithGames/checkers/src/rules/actions.ts (the `.help()` authoring pattern on `move`/`endTurn` to mirror)
    - .planning/phases/114-go-fish-action-help-host-lockout/114-RESEARCH.md ("go-fish additions" item 1; Pitfall 2 — one action only, do not invent a draw action)
  </read_first>
  <action>
    Add a single `.help(text)` call to the `ask` action chain in `createAskAction`, placed immediately after `.prompt('Ask another player for cards')` (mirroring how checkers chains `.help()` after `.prompt()`). The help string must be ONE clear, actionable sentence covering both the ask and its Go-Fish auto-draw consequence — the draw is part of `ask` resolution, so do NOT add a separate draw action and do NOT add per-selection (target/rank) help (deferred substrate-wide per CONTEXT Area 1).

    Suggested wording (final wording at your discretion, keep it actionable per CLAUDE.md): "Ask an opponent for a rank you already hold. If they have it they give you all of them and you go again; if not, you draw from the pond (Go Fish!)." Implements GFHELP-01.
  </action>
  <verify>
    <automated>cd ~/BoardSmithGames/go-fish && grep -n "\.help(" src/rules/actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - `~/BoardSmithGames/go-fish/src/rules/actions.ts` contains a `.help(` call on the `ask` action chain (grep matches).
    - No new action is defined (the file still has exactly one `Action.create(` — the `ask` action).
    - `cd ~/BoardSmithGames/go-fish && npm test` stays green (existing 48 tests unaffected).
  </acceptance_criteria>
  <done>The `ask` action carries an actionable help string covering ask + the Go-Fish auto-draw; no draw action invented; go-fish suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test — ask ActionMetadata.help propagation</name>
  <files>~/BoardSmithGames/go-fish/tests/action-help.test.ts</files>
  <behavior>
    - Test 1: buildActionMetadata(testGame.game, seat-1 player, ['ask']) returns metadata whose `['ask'].help` equals the exact string authored in Task 1 (propagation through buildActionMetadata).
    - Test 2 (guardrail): the same metadata `['ask'].help` is a non-empty string and mentions the Go-Fish draw consequence (assert it is defined and length > 0 — proves the auto-draw is documented within ask help, satisfying GFHELP "each action").
  </behavior>
  <read_first>
    - ~/BoardSmithGames/go-fish/tests/game.test.ts (the `createTestGame(GoFishGame, { playerCount, playerNames })` harness + `testGame.game.getPlayer(1)` access pattern used across go-fish tests)
    - /Users/jtsmith/BoardSmith/src/session/action-help-propagation.test.ts (the canonical `buildActionMetadata(...)` → `expect(metadata['name']?.help).toBe(...)` assertion shape)
    - ~/BoardSmithGames/go-fish/src/rules/actions.ts (to read the exact authored help string committed in Task 1)
    - .planning/phases/114-go-fish-action-help-host-lockout/114-RESEARCH.md (Validation Architecture — GFHELP-01 integration row)
  </read_first>
  <action>
    Create `tests/action-help.test.ts`. Build a 2-player go-fish game via `createTestGame(GoFishGame, { playerCount: 2, playerNames: ['Alice','Bob'] })`. Import `buildActionMetadata` from `boardsmith/session`. Call `buildActionMetadata(testGame.game, testGame.game.getPlayer(1)!, ['ask'])` and assert `metadata['ask']?.help` equals the exact authored string (reference the real string, not a paraphrase) and is a defined non-empty string. This proves help reaches `ActionMetadata.help` — the field both AutoUI and the custom GameTable.vue action panel consume via `getActionMetadata('ask')?.help`. Name the describe block so `npm test -- help` selects it.
  </action>
  <verify>
    <automated>cd ~/BoardSmithGames/go-fish && npm test -- help</automated>
  </verify>
  <acceptance_criteria>
    - `~/BoardSmithGames/go-fish/tests/action-help.test.ts` exists and imports `buildActionMetadata` from `boardsmith/session`.
    - `cd ~/BoardSmithGames/go-fish && npm test -- help` passes (asserts `metadata['ask'].help` is the authored string).
    - The asserted string matches the string in `src/rules/actions.ts` exactly (not a paraphrase) — proven by the test passing.
  </acceptance_criteria>
  <done>`npm test -- help` (go-fish) is green; the test fails if `.help()` is removed from `ask` (real propagation, not vacuous).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author content → engine | `.help()` text is static author-supplied content compiled into the game; no runtime/untrusted input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01 | Information disclosure | ask `.help()` text | accept | Help text is intentionally player-visible UX copy; no secrets, no PII, no implementation details (CLAUDE.md: never leak internals). |
| T-114-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan (pure authoring against symlinked boardsmith); package legitimacy gate not triggered. |
</threat_model>

<verification>
- `cd ~/BoardSmithGames/go-fish && npm test` — full go-fish suite green (was 48 tests; now +1 file).
- `cd ~/BoardSmithGames/go-fish && grep -n "\.help(" src/rules/actions.ts` — confirms the authoring change.
- `cd /Users/jtsmith/BoardSmith && npm test` — BoardSmith suite unaffected (no `src/` changes; symlink, no re-vendor).
</verification>

<success_criteria>
- The `ask` action carries one actionable help string covering ask + Go-Fish auto-draw.
- `buildActionMetadata(..., ['ask'])` yields `help` === that string (propagates to both UIs' metadata).
- go-fish suite green; BoardSmith suite green; no BoardSmith `src/` changes.
</success_criteria>

<output>
Create `.planning/phases/114-go-fish-action-help-host-lockout/114-01-SUMMARY.md` when done.
</output>
