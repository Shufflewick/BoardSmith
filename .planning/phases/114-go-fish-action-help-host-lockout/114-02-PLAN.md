---
phase: 114-go-fish-action-help-host-lockout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/BoardSmithGames/go-fish/tests/host-lockout.test.ts
autonomous: true
requirements: [GFLOCK-01]

must_haves:
  truths:
    - "A teachingDisabled go-fish session rejects requestHint, startDemo, and startTutorial with the fail-loud error 'Teaching features are disabled for this session.'"
    - "Under the same lockout, action help is unaffected — ask ActionMetadata.help still carries its string (action help is NOT a teaching affordance, per D-06)"
    - "The lockout is construction-time: the locked session is created with teachingDisabled:true and never toggled mid-game"
  artifacts:
    - path: "~/BoardSmithGames/go-fish/tests/host-lockout.test.ts"
      provides: "go-fish lockout test mirroring checkers teaching.test.ts makeLockedSession + the action-help-stays half"
      contains: "teachingDisabled"
  key_links:
    - from: "GameSession.create({ teachingDisabled: true })"
      to: "fail-loud guard throw"
      via: "requestHint / startDemo / startTutorial"
      pattern: "Teaching features are disabled for this session"
    - from: "teachingDisabled:true session"
      to: "ActionMetadata.help still present"
      via: "buildActionMetadata (help gate is independent of teachingDisabled)"
      pattern: "metadata\\['ask'\\]\\?\\.help"
---

<objective>
Verify (do NOT rebuild) that the v4.1 `teachingDisabled` host lockout generalizes to a card game: a locked go-fish session rejects `requestHint`/`startDemo`/`startTutorial` fail-loud, AND action help stays enabled under the same lockout. Mirror checkers' BoardSmith `teaching.test.ts` `makeLockedSession` pattern plus the ControlsMenu LOCK-01-B "action help survives lockout" half.

Purpose: GFLOCK-01 — prove the lockout substrate gates go-fish teaching affordances without disabling action help.
Output: One go-fish integration test asserting BOTH halves (ops throw + action-help kept).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-go-fish-action-help-host-lockout/114-CONTEXT.md
@.planning/phases/114-go-fish-action-help-host-lockout/114-RESEARCH.md

<interfaces>
<!-- The teachingDisabled lockout substrate is built (BoardSmith v4.1 Phase 111). Reuse as-is. -->

GameSession.create — boardsmith/session (signature accepts GameClass + teachingDisabled):
  GameSession.create({
    gameType: 'go-fish',
    GameClass: GoFishGame,
    playerCount: 2,
    playerNames: ['Alice','Bob'],
    seed: 'test',
    teachingDisabled: true,
  })

Fail-loud guard sites — all throw 'Teaching features are disabled for this session.' when teachingDisabled:
  requestHint(seat)        // async — rejects
  startDemo(...)           // sync — throws
  startTutorial(seat)      // sync — throws
  (NOT blocked: exitTutorial; action-help toggle — always available, D-06)

Checkers analog (the lockout test shape to mirror) — BoardSmith src/session/teaching.test.ts:541-617:
  function makeLockedSession() { return GameSession.create({ ..., teachingDisabled: true }); }
  await expect((session as any).requestHint(1)).rejects.toThrow('Teaching features are disabled for this session.');
  expect(() => (session as any).startDemo()).toThrow('Teaching features are disabled for this session.');
  expect(() => (session as any).startTutorial(1)).toThrow('Teaching features are disabled for this session.');

Action-help-stays half (the other half to assert) — BoardSmith src/ui/components/ControlsMenu.tutorial-toggle.test.ts:158-171 (LOCK-01-B):
  // D-06: action help is NOT gated by teachingDisabled.
  // In go-fish (no ControlsMenu unit test in-repo), assert the metadata equivalent:
  buildActionMetadata(game, player, ['ask'])['ask'].help  // still defined under teachingDisabled

go-fish exports — ~/BoardSmithGames/go-fish/src/rules/index.ts:
  export { GoFishGame } ...; export const gameDefinition = { gameClass: GoFishGame, gameType: 'go-fish', ai: {...}, tutorial: GO_FISH_TUTORIAL };
  // gameDefinition.ai present → showHint !== undefined → teaching affordances exist to be gated.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: go-fish host-lockout test — ops throw + action help stays (both halves)</name>
  <files>~/BoardSmithGames/go-fish/tests/host-lockout.test.ts</files>
  <behavior>
    - HALF A (teaching gated, fail-loud): On a `teachingDisabled:true` go-fish session, `requestHint(seat)` rejects with 'Teaching features are disabled for this session.'; `startDemo(...)` throws the same; `startTutorial(seat)` throws the same. Assert the EXACT message (real fail-loud error, not a silent no-op).
    - HALF B (action help survives): Under teachingDisabled, action help is unaffected — `buildActionMetadata(game, player, ['ask'])['ask'].help` is still the authored non-empty string. (Equivalent of ControlsMenu LOCK-01-B for a card game: the "Show action help" affordance is never gated by teachingDisabled.)
    - CONTROL (no false positive): A default (unlocked) go-fish session does NOT throw the lockout error — e.g. invoking a teaching op on the unlocked session fails for a different reason or not at all, never with 'Teaching features are disabled for this session.'
  </behavior>
  <read_first>
    - /Users/jtsmith/BoardSmith/src/session/teaching.test.ts (lines ~541-617: `makeLockedSession()`, the four-op throw assertions, exitTutorial-allowed, and the unlocked-control assertions — the exact shape to mirror)
    - /Users/jtsmith/BoardSmith/src/ui/components/ControlsMenu.tutorial-toggle.test.ts (lines ~158-171: LOCK-01-B — action help survives lockout; this is HALF B's intent)
    - ~/BoardSmithGames/go-fish/tests/demo.test.ts (how go-fish imports from `boardsmith/session` and constructs the game from `gameDefinition`/`GoFishGame`)
    - ~/BoardSmithGames/go-fish/src/rules/index.ts (GoFishGame + gameDefinition exports, including gameDefinition.ai which lights up teaching affordances)
    - ~/BoardSmithGames/go-fish/src/rules/actions.ts (to reference the exact `ask` help string for HALF B)
    - .planning/phases/114-go-fish-action-help-host-lockout/114-RESEARCH.md (Validation Architecture — GFLOCK-01; Pitfall 4 — construction-time lockout)
  </read_first>
  <action>
    Create `tests/host-lockout.test.ts`. Add a local `makeLockedSession()` helper that calls `GameSession.create` (imported from `boardsmith/session`) with `{ gameType: 'go-fish', GameClass: GoFishGame, playerCount: 2, playerNames: ['Alice','Bob'], seed: 'test', teachingDisabled: true }` — mirroring checkers `teaching.test.ts`. The lockout is set at construction and never toggled (Pitfall 4).

    HALF A: assert `requestHint(1)` rejects with the exact 'Teaching features are disabled for this session.' message; assert `startDemo(...)` and `startTutorial(1)` throw the same exact message. Cast the session via `as unknown as { ... }` for the teaching methods, exactly as the checkers analog does.

    HALF B: build a 2-player GoFishGame (via `createTestGame` from `boardsmith/testing`, as in the action-help test) and assert `buildActionMetadata(game, player, ['ask'])['ask']?.help` is still the authored non-empty string — proving teachingDisabled does NOT gate action help (D-06). Do NOT assert only HALF A.

    CONTROL: assert a default (unlocked) session does NOT throw the lockout error for the same ops (mirror the checkers "default session does not reject with lockout error" cases). Name the describe block so `npm test -- lock` selects it.

    Do NOT change any BoardSmith `src/`. If a teaching op fails to throw the lockout error against go-fish, STOP and flag it as a real substrate gap (do not write a go-fish workaround) — but this is not expected; the guards are game-agnostic.
  </action>
  <verify>
    <automated>cd ~/BoardSmithGames/go-fish && npm test -- lock</automated>
  </verify>
  <acceptance_criteria>
    - `~/BoardSmithGames/go-fish/tests/host-lockout.test.ts` exists; imports `GameSession` (and `buildActionMetadata`) from `boardsmith/session` and `GoFishGame` from the rules.
    - The test asserts the EXACT string 'Teaching features are disabled for this session.' for `requestHint`, `startDemo`, AND `startTutorial` (HALF A — grep the file for that string).
    - The test asserts `metadata['ask'].help` is still present under teachingDisabled (HALF B — grep the file for `buildActionMetadata` / `help`).
    - The test includes an unlocked-control case proving no false-positive lockout error.
    - `cd ~/BoardSmithGames/go-fish && npm test -- lock` passes.
  </acceptance_criteria>
  <done>`npm test -- lock` (go-fish) green; both halves asserted (teaching ops throw fail-loud + action help survives); construction-time lockout; no BoardSmith `src/` changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host config → session | `teachingDisabled` is a host-supplied construction-time flag; the session enforces it via fail-loud guards. This test verifies the boundary holds for go-fish. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-02 | Elevation of privilege | teaching ops (requestHint/startDemo/startTutorial) under a locked session | mitigate | This plan's test asserts each op throws the exact fail-loud lockout error — a learner/client cannot invoke teaching features the host disabled. |
| T-114-03 | Tampering | action-help gate vs teaching lockout | mitigate | HALF B asserts action help is NOT collaterally disabled by teachingDisabled (D-06), preventing a regression that over-gates UX copy. |
| T-114-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed (pure test authoring against symlinked boardsmith); legitimacy gate not triggered. |
</threat_model>

<verification>
- `cd ~/BoardSmithGames/go-fish && npm test -- lock` — both halves green.
- `cd ~/BoardSmithGames/go-fish && npm test` — full go-fish suite green.
- `cd /Users/jtsmith/BoardSmith && npm test` — BoardSmith suite unaffected (no `src/` changes; symlink, no re-vendor).
</verification>

<success_criteria>
- Locked go-fish session: `requestHint`/`startDemo`/`startTutorial` each throw the exact fail-loud lockout error.
- Action help survives the lockout (ask metadata.help still present).
- Unlocked control proves no false-positive lockout.
- go-fish + BoardSmith suites green; no BoardSmith `src/` changes.
</success_criteria>

<output>
Create `.planning/phases/114-go-fish-action-help-host-lockout/114-02-SUMMARY.md` when done.
</output>
