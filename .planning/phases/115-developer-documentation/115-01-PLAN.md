---
phase: 115-developer-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/teaching-and-tutorials.md
  - docs/README.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-04]

must_haves:
  truths:
    - "A developer can read one guide and learn TutorialDefinition, the start/advance/skip/exit lifecycle, action gating, and annotation overlay targets (cell/piece/card/panel/action)"
    - "The guide shows how to author predicate triggers and a CI-verifiable tutorial test with simulateTutorial/assertTutorialCompletes, including a green->red rule-break demonstration"
    - "The guide documents move hint, narrated AI-vs-AI demo, heatmap (noted board-only), teachingDisabled host lockout, and per-action .help()"
    - "Checkers (grid, notation anchors) and go-fish (cards, name anchors) appear side-by-side showing the anchorAttrs parity path; custom UIs must emit anchorAttrs, AutoUI does it automatically"
    - "The new guide is discoverable from docs/README.md"
  artifacts:
    - path: "docs/teaching-and-tutorials.md"
      provides: "The single teaching & tutorials developer guide covering DOC-01..04"
      min_lines: 200
      contains: "TutorialDefinition"
    - path: "docs/README.md"
      provides: "Docs index linking the new guide"
      contains: "teaching-and-tutorials.md"
  key_links:
    - from: "docs/README.md"
      to: "docs/teaching-and-tutorials.md"
      via: "markdown link in the Documents table"
      pattern: "teaching-and-tutorials\\.md"
    - from: "docs/teaching-and-tutorials.md"
      to: "checkers + go-fish source"
      via: "real codebase-verified excerpts with file references"
      pattern: "(CHECKERS_TUTORIAL|GO_FISH_TUTORIAL)"
---

<objective>
Author one developer guide, `docs/teaching-and-tutorials.md`, that teaches a BoardSmith developer how to build a tutorial, predicate triggers, CI-verifiable tutorial tests, AI teaching features (hint / narrated demo / heatmap), per-action help, and host teaching-lockout — illustrated with REAL, codebase-verified checkers (grid) and go-fish (card) worked examples shown side-by-side so the `anchorAttrs` parity path is immediately legible. Link the guide from `docs/README.md`.

Purpose: This is the capstone of the v4.2 tutorial-primitives milestone. The substrate is fully built and proven across v4.1 (checkers) and v4.2 Phases 112-114 (go-fish); this guide synthesizes it into a single authoring reference so a developer starting a new game knows exactly where their UI must emit `anchorAttrs`.

Output: `docs/teaching-and-tutorials.md` (new) + a link in `docs/README.md`. NO `src/` or game-code changes — this is documentation only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-developer-documentation/115-CONTEXT.md
@.planning/phases/115-developer-documentation/115-RESEARCH.md

# Style reference — match this sibling guide's tone, headings, fenced-TS-example structure:
@docs/custom-ui-guide.md
@docs/README.md
</context>

<authoring_rules>
- Every code example and every cited symbol/type/method/flag MUST come from the live source files listed in each task's `<read_first>`. NO invented API. Prefer concise excerpts (a few lines) plus a `file:` reference over pasting whole files (whole-file pastes rot).
- When you cite a symbol, open the real file and confirm the exact name/signature before writing it into the guide. Plan 02 (doc-verifier) will check every claim against the live source; mismatches are blockers.
- Match the existing `docs/*.md` style: H1 title, H2 sections, fenced ```ts / ```vue code blocks, short intro paragraphs. Mirror `docs/custom-ui-guide.md`.
- Cross-repo sources live at `~/BoardSmithGames/checkers` and `~/BoardSmithGames/go-fish` (read-only — do not modify game code).
- Section order (CONTEXT Area 1): (1) Authoring a tutorial, (2) Predicate triggers, (3) CI-verifiable tutorial tests (+ green->red), (4) AI teaching (hint, demo, heatmap board-only), (5) Action help, (6) Host teaching lockout, (7) Parity: checkers vs go-fish worked examples + caveats.
</authoring_rules>

<tasks>

<task type="auto">
  <name>Task 1: Create the guide and write DOC-01 (tutorial authoring) + DOC-02 (predicate triggers + CI green->red) sections, and link from README</name>
  <files>docs/teaching-and-tutorials.md, docs/README.md</files>
  <read_first>
    - src/engine/tutorial/types.ts (TutorialDefinition { setup?, steps }, TutorialStep { id, gate, content?, advanceWhen?, suppressAutoFill? }, TutorialGateAllowList { action, selections? }, Annotation { text, target?, placement? }, AnnotationTarget union: {kind:'element',ref} | {kind:'action',actionName} | {kind:'panel'})
    - src/engine/tutorial/gate.ts (selectionMatchesValue id>notation>name precedence; getGateReasonForValue)
    - src/engine/tutorial/predicates.ts (predicate trigger / advanceWhen forms)
    - src/session/game-session.ts (startTutorial(seat), exitTutorial — lifecycle: start/advance/skip/exit)
    - src/testing/simulate-tutorial.ts, src/testing/tutorial-assertions.ts, src/testing/index.ts (simulateTutorial, assertTutorialCompletes, TutorialScenarioMove, stepsVisited, TestGame.create tutorialSetup)
    - src/testing/tutorial-ci-demo.test.ts (the canonical green->red CI pattern to mirror)
    - ~/BoardSmithGames/checkers/src/rules/tutorial.ts (CHECKERS_TUTORIAL — real TutorialDefinition for examples)
    - ~/BoardSmithGames/go-fish/src/rules/tutorial.ts (GO_FISH_TUTORIAL — real TutorialDefinition for examples)
    - ~/BoardSmithGames/checkers/tests/tutorial.test.ts and ~/BoardSmithGames/go-fish/tests/tutorial.test.ts (real CI tests)
    - docs/custom-ui-guide.md and docs/README.md (style + the Documents table to append to)
  </read_first>
  <action>
    Create docs/teaching-and-tutorials.md with an H1 title and a short intro framing the guide as the single authoring reference for BoardSmith teaching primitives. Write Section 1 "Authoring a tutorial" covering: the TutorialDefinition shape ({ setup?, steps }); TutorialStep fields (id, gate, content, advanceWhen, suppressAutoFill); the start/advance/skip/exit lifecycle via GameSession.startTutorial(seat) / exitTutorial and the "Start tutorial" control surfaced when gameDefinition.tutorial exists; action gating via TutorialGateAllowList { action, selections? } and how selectionMatchesValue resolves element refs by id>notation>name (note the v4.2-112 primitive-via-{ value } fix); and the annotation overlay targets — the AnnotationTarget union covering element (cell/piece/card), action, and panel kinds. Use a real excerpt from CHECKERS_TUTORIAL and/or GO_FISH_TUTORIAL with a file reference. Write Section 2 "Predicate triggers" covering advanceWhen predicate forms from predicates.ts with a real example. Write Section 3 "CI-verifiable tutorial tests" covering simulateTutorial + assertTutorialCompletes asserting beats in stepsVisited, TestGame.create with tutorialSetup, and the green->red demonstration (mirror tutorial-ci-demo.test.ts: a deliberate rule break — e.g. go-fish checkForBooks returning [] or checkers disabling mandatory capture — makes the test go red, proving the tutorial is a living regression test). Honestly note the auto-advance caveat: a step whose advanceWhen is already true auto-advances via the pump and may not appear separately in stepsVisited (R-05 dwell-time backlog). Then append a row to the Documents table in docs/README.md linking [Teaching & Tutorials](./teaching-and-tutorials.md) with a one-line description. Implement DOC-01 and DOC-02.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && test -f docs/teaching-and-tutorials.md && grep -q "TutorialDefinition" docs/teaching-and-tutorials.md && grep -q "simulateTutorial" docs/teaching-and-tutorials.md && grep -q "assertTutorialCompletes" docs/teaching-and-tutorials.md && grep -qi "advanceWhen" docs/teaching-and-tutorials.md && grep -q "teaching-and-tutorials.md" docs/README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - docs/teaching-and-tutorials.md exists with H1 title and Sections 1-3.
    - Guide contains: TutorialDefinition, TutorialStep, TutorialGateAllowList, AnnotationTarget (cell/piece/card/panel/action), startTutorial/exitTutorial (start/advance/skip/exit lifecycle).
    - Guide contains: simulateTutorial, assertTutorialCompletes, stepsVisited, and a green->red rule-break demonstration.
    - Guide notes the auto-advance / R-05 dwell-time caveat.
    - docs/README.md Documents table contains a link to ./teaching-and-tutorials.md.
    - Every cited symbol matches the live source files in read_first (no invented API).
  </acceptance_criteria>
  <done>Sections 1-3 written from real source excerpts; README links the guide; coverage greps pass.</done>
</task>

<task type="auto">
  <name>Task 2: Write DOC-03 (AI teaching: hint, narrated demo, heatmap board-only) + action help + host lockout sections</name>
  <files>docs/teaching-and-tutorials.md</files>
  <read_first>
    - src/session/game-session.ts (requestHint(seat) -> hintTargetFromMove hook -> Annotation -> HintOverlay, cleared on next action R-06; demoStart/demoStop ops + runDemoLoop narrate-before-execute with demoAbort before+after delay; setHeatmapVisible board-only; teachingDisabled lockout throwing "Teaching features are disabled for this session." from requestHint/setHeatmapVisible/startDemo/startTutorial)
    - src/ui/components/helpers/HintOverlay.vue (hint ring/bubble)
    - src/ui/components/helpers/ActionHelpPopover.vue (the "?" affordance)
    - src/engine/action/types.ts and src/engine/action/action-builder.ts (ActionDefinition.help, the .help(text) builder, ActionMetadata.help via buildActionMetadata)
    - src/cli/commands/dev.ts, src/cli/cli.ts, src/cli/dev-host/config-types.ts (the boardsmith dev --lock-teaching flag wiring)
    - ~/BoardSmithGames/checkers/src/rules/{actions.ts,ai.ts} (hintTargetFromMove returning { notation }; heatmap candidate cells)
    - ~/BoardSmithGames/go-fish/src/rules/{actions.ts,ai.ts} (getGoFishHintTarget returning { name: rank }; .help() usage)
    - ~/BoardSmithGames/go-fish/tests/{demo.test.ts,action-help.test.ts,host-lockout.test.ts} (real behavior the prose must match)
  </read_first>
  <action>
    Append to docs/teaching-and-tutorials.md. Write Section 4 "AI teaching" covering: the move hint (GameSession.requestHint(seat) -> the game's hintTargetFromMove hook -> an Annotation rendered by HintOverlay, cleared on the next action per R-06); the narrated AI-vs-AI demo (demoStart/demoStop ops driving runDemoLoop with narrate-before-execute and demoAbort checked before AND after the delay so no timer leaks); and the evaluation heatmap — explicitly documented as BOARD-ONLY (it shades grid cells via data-bs-el anchors and does not apply to a gridless card game; it stays available for grid games; a card-game move-quality visualization is a deferred future idea). Document the known caveats honestly: the AI-vs-AI demo affordance is gated on an AI seat being present, and driving a seat via the dev host's Follow-active-seat removes that AI presence (a dev-host testing caveat); and the heatmap "Show move quality" toggle currently shows for gridless games when unlocked because it is gated on showHint rather than board presence (a known cosmetic substrate quirk to be fixed later). Write Section 5 "Action help" covering ActionDefinition.help via the .help(text) builder, propagated through buildActionMetadata to ActionMetadata.help and surfaced by ActionHelpPopover; note the "Show action help" toggle is gated by hasActionHelp and is NOT subject to teachingDisabled. Write Section 6 "Host teaching lockout" covering GameSessionOptions.teachingDisabled (throws "Teaching features are disabled for this session." from requestHint/setHeatmapVisible/startDemo/startTutorial; the UI hides the Teaching group) and the boardsmith dev --lock-teaching flag. Implement DOC-03.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && grep -q "requestHint" docs/teaching-and-tutorials.md && grep -qi "heatmap" docs/teaching-and-tutorials.md && grep -qi "board-only" docs/teaching-and-tutorials.md && grep -q "teachingDisabled" docs/teaching-and-tutorials.md && grep -q "lock-teaching" docs/teaching-and-tutorials.md && grep -q "\.help(" docs/teaching-and-tutorials.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Section 4 documents requestHint/hintTargetFromMove, the narrated demo (demoStart/demoStop), and the heatmap explicitly labeled board-only.
    - The demo AI-seat-gating dev-host caveat and the heatmap showHint-gating quirk are both documented.
    - Section 5 documents .help() / ActionDefinition.help / ActionHelpPopover and notes action help is NOT gated by teachingDisabled.
    - Section 6 documents teachingDisabled (with the exact thrown message) and the --lock-teaching dev flag.
    - All cited symbols match the live source (no invented API).
  </acceptance_criteria>
  <done>Sections 4-6 written from real source excerpts; coverage greps pass.</done>
</task>

<task type="auto">
  <name>Task 3: Write DOC-04 parity section — checkers vs go-fish worked examples side-by-side + the anchorAttrs path</name>
  <files>docs/teaching-and-tutorials.md</files>
  <read_first>
    - src/ui/composables/useBoardInteraction.ts (anchorAttrs(ref) emitting data-bs-el-id / data-bs-el-notation / data-bs-el-name; useSelectable / useSelectableGrid spreading anchorAttrs automatically in AutoUI renderers)
    - src/ui/components/helpers/overlay-utils.ts (buildSelector resolving anchors id>notation>name)
    - ~/BoardSmithGames/checkers/src/rules/tutorial.ts (CHECKERS_TUTORIAL; ref: { notation } on board squares)
    - ~/BoardSmithGames/checkers/src/rules/actions.ts (hintTargetFromMove(move) => { notation })
    - ~/BoardSmithGames/checkers/tests/tutorial.test.ts (checkers CI test)
    - ~/BoardSmithGames/go-fish/src/rules/tutorial.ts (GO_FISH_TUTORIAL; ref: { name } on a named card)
    - ~/BoardSmithGames/go-fish/src/rules/actions.ts (getGoFishHintTarget(move) => { name: rank })
    - ~/BoardSmithGames/go-fish/src/ui/components/GameTable.vue (custom UI binding anchorAttrs({ id, name }) on cards + the rank card-group)
    - ~/BoardSmithGames/go-fish/tests/{tutorial.test.ts,hint-target.test.ts} (go-fish CI tests)
  </read_first>
  <action>
    Append Section 7 "Parity: checkers vs go-fish" to docs/teaching-and-tutorials.md. Present the two worked examples side-by-side (a table or paired code blocks — author's discretion) along three parity axes, each with a real excerpt + file reference: (1) tutorial definition — CHECKERS_TUTORIAL using ref: { notation } on board squares vs GO_FISH_TUTORIAL using ref: { name } on a named card; (2) overlay anchoring — checkers grid squares carry notation automatically via GridBoardRenderer cellAttrs (AutoUI), whereas go-fish's custom GameTable.vue must explicitly emit anchorAttrs({ id, name }) on cards and the rank card-group; (3) hint target — checkers hintTargetFromMove(move) => { notation } vs go-fish getGoFishHintTarget(move) => { name: rank } with the rank-group anchorAttrs({ name: rank }). State the central parity lesson prominently (do not bury it): annotation/hint overlays anchor through data-bs-el-* attributes resolved by buildSelector in id>notation>name precedence; AutoUI renderers emit these automatically via useSelectable/anchorAttrs, but a CUSTOM UI MUST opt in by spreading anchorAttrs — board games anchor by notation, card games anchor by name. Reference the matching CI tests (checkers/tests/tutorial.test.ts and go-fish/tests/tutorial.test.ts) so a reader sees both proven. Implement DOC-04.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && grep -q "anchorAttrs" docs/teaching-and-tutorials.md && grep -q "CHECKERS_TUTORIAL" docs/teaching-and-tutorials.md && grep -q "GO_FISH_TUTORIAL" docs/teaching-and-tutorials.md && grep -qi "notation" docs/teaching-and-tutorials.md && grep -q "getGoFishHintTarget\|hintTargetFromMove" docs/teaching-and-tutorials.md && grep -q "data-bs-el" docs/teaching-and-tutorials.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Section 7 shows checkers and go-fish side-by-side across tutorial def, overlay anchoring, and hint target.
    - The central parity lesson is stated explicitly: custom UIs must emit anchorAttrs (data-bs-el-*), AutoUI does it automatically; board=notation, card=name.
    - Both CHECKERS_TUTORIAL and GO_FISH_TUTORIAL appear with real excerpts and file references.
    - All cited symbols match the live source (no invented API).
  </acceptance_criteria>
  <done>Section 7 written with real side-by-side excerpts; parity lesson explicit; coverage greps pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only phase: no executable code, no external input, no package installs, no network. The deliverable is a markdown file. No trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Information Disclosure | docs/teaching-and-tutorials.md | accept | Guide cites only public API names already exported from `boardsmith`; no secrets, paths, or internals beyond the documented public surface. |
| T-115-02 | Tampering (doc drift) | cited API excerpts | mitigate | Plan 02 doc-verifier checks every cited symbol/snippet against live source; mismatches are blockers (factual-accuracy gate, not a security control but the integrity guard for this phase). |
</threat_model>

<verification>
- docs/teaching-and-tutorials.md exists and covers DOC-01..04 (coverage greps in each task pass).
- docs/README.md links the new guide.
- No `src/` or game-code changes (this plan touches only the two docs files).
</verification>

<success_criteria>
- One guide at docs/teaching-and-tutorials.md teaches tutorial authoring, predicate triggers, CI tests (green->red), AI teaching (hint/demo/heatmap board-only), action help, and host lockout.
- Checkers and go-fish appear side-by-side with the anchorAttrs parity path explicit.
- README links the guide.
- All examples are real and drawn from the live source.
</success_criteria>

<output>
Create `.planning/phases/115-developer-documentation/115-01-SUMMARY.md` when done.
</output>
