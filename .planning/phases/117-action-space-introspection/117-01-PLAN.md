---
phase: 117-action-space-introspection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/session/index.ts
autonomous: true
requirements: [INTRO-F1, INTRO-02]
must_haves:
  truths:
    - "A consumer can import ElementDiff from the session barrel"
    - "A consumer can import ActionMetadata from the session barrel"
    - "UndoResult is not double-exported (already present)"
  artifacts:
    - path: "src/session/index.ts"
      provides: "ElementDiff + ActionMetadata type exports"
      contains: "ElementDiff"
  key_links:
    - from: "src/session/index.ts"
      to: "src/session/state-history.ts (ElementDiff)"
      via: "type re-export from ./game-session.js"
      pattern: "ElementDiff"
    - from: "src/session/index.ts"
      to: "src/session/types.ts (ActionMetadata)"
      via: "type re-export from ./types.js"
      pattern: "ActionMetadata"
---

<objective>
Add the two missing type exports to the session barrel that the rest of Phase 117 (and Phase 122 docs) depend on. This is the foundational, zero-logic plan: `ElementDiff` (INTRO-F1) and `ActionMetadata` (needed by INTRO-02's `getActionSchema` return type and by external consumers).

Purpose: The five `GameSession` checkpoint/rewind methods are already implemented and public; the only gap is type exports. `getActionSchema` (Plan 04) returns `ActionMetadata`, which external callers must be able to import by name.
Output: Updated `src/session/index.ts` barrel with both type exports; `npx tsc --noEmit` clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-action-space-introspection/117-RESEARCH.md
@.planning/v4.3-API-DESIGN.md

<interfaces>
<!-- Already-defined types being exposed. No new types created here. -->
<!-- src/session/state-history.ts:43 -->
ElementDiff { added: number[]; removed: number[]; changed: number[]; fromIndex: number; toIndex: number }
game-session.ts:142 already re-exports `type { UndoResult, ElementDiff } from './state-history.js'`,
but session/index.ts only forwards UndoResult (line 114), NOT ElementDiff.

<!-- src/session/types.ts:356 -->
ActionMetadata { name: string; prompt?: string; help?: string; selections: PickMetadata[] }
Defined in types.ts; NOT currently forwarded by session/index.ts. PickMetadata IS already exported (index.ts:76).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ElementDiff and ActionMetadata to the session barrel</name>
  <files>src/session/index.ts</files>
  <read_first>
    - src/session/index.ts (the barrel — confirm `UndoResult` is ALREADY exported at the `./game-session.js` block, lines ~110-115; confirm `PickMetadata` already exported at line ~76; confirm `ActionMetadata` is absent)
    - src/session/state-history.ts:43 (ElementDiff definition)
    - src/session/types.ts:356 (ActionMetadata definition)
    - .planning/v4.3-API-DESIGN.md (INTRO-F1 entry line ~782; INTRO-02 entry lines 153-163 — `ActionMetadata` must be exported)
    - .planning/phases/117-action-space-introspection/117-RESEARCH.md (Target 6 + Pitfall 3 — `UndoResult` already exported, DO NOT re-export it)
  </read_first>
  <action>
    In src/session/index.ts add `type ElementDiff` to the existing `export { ... } from './game-session.js'` block alongside the already-present `type UndoResult` (game-session.ts re-exports ElementDiff from state-history.js, so it is reachable through that barrel). Separately add `ActionMetadata` to the existing `export type { ... } from './types.js'` block next to the already-present `PickMetadata`. Do NOT add `UndoResult` again — per research Pitfall 3 it is already exported at the game-session block and a duplicate causes a TypeScript "already declared" error. Do not touch state-history.ts or types.ts — only the barrel. This is the per D-INTRO-F1 expose-only change (no logic) plus the `ActionMetadata` export INTRO-02 requires.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5; grep -n "ElementDiff\|ActionMetadata" src/session/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ElementDiff" src/session/index.ts` shows ElementDiff in the game-session.js export block
    - `grep -n "ActionMetadata" src/session/index.ts` shows ActionMetadata in the types.js export-type block
    - `UndoResult` appears exactly once in src/session/index.ts (not duplicated)
    - `npx tsc --noEmit` exits 0 with no new errors
  </acceptance_criteria>
  <done>Both ElementDiff and ActionMetadata import cleanly from the session barrel; tsc clean; UndoResult not duplicated.</done>
</task>

</tasks>

<threat_model>
No untrusted input crosses any boundary in this plan — it is a type-export-only change with no runtime logic. No new trust boundary, no network/auth/secrets surface. No package-manager installs in this phase (RESEARCH.md "Standard Stack": no new packages), so no supply-chain checkpoint applies.
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- Barrel exports present via grep
- `npx vitest run src/session/` green (no regressions from a barrel-only change)
</verification>

<success_criteria>
`import { ElementDiff, ActionMetadata } from 'boardsmith/session'` (or the local barrel path) compiles. UndoResult export unchanged and not duplicated.
</success_criteria>

<output>
Create `.planning/phases/117-action-space-introspection/117-01-SUMMARY.md` when done.
</output>
