---
phase: 117-action-space-introspection
plan: 03
type: execute
wave: 3
depends_on: ["117-02"]
files_modified:
  - src/engine/utils/arg-builder.ts
  - src/engine/utils/arg-builder.test.ts
  - src/engine/index.ts
autonomous: true
requirements: [INTRO-03]
must_haves:
  truths:
    - "A caller can build action args from plain selection values without hand-constructing element-id shapes"
    - "Default ('in-process') format returns element objects suitable for runner.performAction"
    - "format:'wire' returns JSON-safe { __elementId } / { __elementRef } references"
    - "An unknown selection name for the action is rejected with an actionable error"
  artifacts:
    - path: "src/engine/utils/arg-builder.ts"
      provides: "buildActionArgs(actionName, selectionValues, game, seat, options?)"
      exports: ["buildActionArgs"]
      min_lines: 30
    - path: "src/engine/utils/arg-builder.test.ts"
      provides: "in-process + wire + validation coverage"
  key_links:
    - from: "src/engine/utils/arg-builder.ts"
      to: "src/engine/utils/serializer.ts (serializeValue)"
      via: "wire-format serialization"
      pattern: "serializeValue"
    - from: "src/engine/index.ts"
      to: "src/engine/utils/arg-builder.ts"
      via: "barrel export"
      pattern: "buildActionArgs"
---

<objective>
Implement INTRO-03: a new public utility `buildActionArgs(actionName, selectionValues, game, seat, options?)` that turns plain selection values into ready-to-submit action args — in-process element objects by default (D-02), JSON-safe wire references when `{ format: 'wire' }` is requested.

Purpose: Today callers must hand-construct `{ __elementId }` / `{ value, display }` shapes to submit actions; the bot does this internally with private helpers. This utility makes the correct path the easy path for headless/test/agent callers.
Output: New `src/engine/utils/arg-builder.ts` (+ test), barrel export from `src/engine/index.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-action-space-introspection/117-RESEARCH.md
@.planning/phases/117-action-space-introspection/117-VALIDATION.md
@.planning/v4.3-API-DESIGN.md

<interfaces>
<!-- Public signature to build (design doc INTRO-03, lines 180-206) -->
buildActionArgs(
  actionName: string,
  selectionValues: Record<string, unknown>,   // plain JS: element objects, strings, numbers
  game: Game,
  seat: number,
  options?: { format?: 'in-process' | 'wire' }  // default 'in-process'
): Record<string, unknown>

<!-- Reuse (DO NOT hand-roll element->id) -->
// src/engine/utils/serializer.ts:9
type SerializedReference = { __elementRef: string } | { __elementId: number } | { __playerRef: number }
// src/engine/utils/serializer.ts:27
serializeValue(value: unknown, game: Game, options?: { useBranchPaths?: boolean }): unknown  // default: IDs

// validation source — public:
game.getAction(actionName): ActionDefinition | undefined  // game.ts:875 ; actionDef.selections[].name
// runner.performAction accepts element objects directly for in-process use (runner.ts:128)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the arg-builder test (in-process, wire, validation)</name>
  <files>src/engine/utils/arg-builder.test.ts</files>
  <read_first>
    - src/engine/utils/serializer.ts:9,27 (wire reference shapes + serializeValue contract)
    - src/runtime/runner.test.ts (analog: building a real game + performAction with element-object args end-to-end)
    - src/engine/utils/snapshot.test.ts (analog: engine util test setup with a registered action that has an element selection)
    - .planning/phases/117-action-space-introspection/117-RESEARCH.md (Target 3 — validation scope, wire format)
    - .planning/v4.3-API-DESIGN.md (INTRO-03 lines 173-206 — D-02 default in-process)
  </read_first>
  <action>
    Create src/engine/utils/arg-builder.test.ts covering: (1) in-process (default) — passing element objects for an element selection returns those same element objects unchanged, and the result is accepted by runner.performAction (trace a real value config->engine->runtime per CLAUDE.md); (2) wire format — `{ format: 'wire' }` converts an element-object arg into a `{ __elementId: number }` (or `{ __elementRef }`) reference and the result is JSON.stringify-safe; (3) primitive passthrough — string/number selection values are returned as-is in both formats; (4) validation — an unknown selection name in selectionValues throws an actionable error naming the bad selection and the action. This is RED — buildActionArgs does not exist yet.
  </action>
  <verify>
    <automated>npx vitest run src/engine/utils/arg-builder.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File exists and imports `buildActionArgs` from engine utils
    - Test currently FAILS (function not implemented) — RED confirmed
    - Test asserts JSON.stringify safety of the wire-format result
    - Test asserts an actionable throw on an unknown selection name
  </acceptance_criteria>
  <done>RED test covering in-process passthrough, wire serialization, primitive passthrough, and validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement buildActionArgs and export from the engine barrel</name>
  <files>src/engine/utils/arg-builder.ts, src/engine/index.ts</files>
  <behavior>
    - default format returns element-object args unchanged (no serialization)
    - format:'wire' replaces element objects with serializeValue(..., { useBranchPaths: false }) output ({ __elementId })
    - primitives pass through unchanged in both formats
    - unknown selection name throws naming the action + selection
  </behavior>
  <read_first>
    - src/engine/utils/serializer.ts:9,27 (serializeValue — use it; do NOT copy the bot's _t.id pattern)
    - src/engine/element/game.ts:875 (getAction -> ActionDefinition.selections for name validation)
    - src/engine/index.ts:175,211 (barrel export pattern — add buildActionArgs)
    - .planning/phases/117-action-space-introspection/117-RESEARCH.md (Pattern: INTRO-03; anti-pattern "do not access game._actions"; use game.getAction)
  </read_first>
  <action>
    Create src/engine/utils/arg-builder.ts exporting `buildActionArgs(actionName, selectionValues, game, seat, options?)`. First validate: look up the ActionDefinition via game.getAction(actionName) (throw an actionable error if the action is unknown), then for each key in selectionValues confirm it matches a selection name in actionDef.selections — throw an actionable error naming the bad key and the action's valid selection names if not (V5 input validation). For the in-process path (default per D-02) return a shallow copy of selectionValues unchanged — runner.performAction accepts element objects directly, so no serialization. For the `{ format: 'wire' }` path, map each value through serializeValue(value, game, { useBranchPaths: false }) so element objects become `{ __elementId }` and players become `{ __playerRef }`; primitives are returned untouched by serializeValue. Do NOT reimplement element->id extraction (no `_t.id` access) and do NOT import session. Export buildActionArgs from the engine barrel src/engine/index.ts.
  </action>
  <verify>
    <automated>npx vitest run src/engine/utils/arg-builder.test.ts 2>&1 | tail -15 && grep -n "buildActionArgs" src/engine/index.ts && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `buildActionArgs` exported from src/engine/index.ts (grep confirms)
    - `npx vitest run src/engine/utils/arg-builder.test.ts` exits 0 (GREEN)
    - `grep -n "_t.id\|__elementId" src/engine/utils/arg-builder.ts` shows no hand-rolled `_t.id` access (serialization delegated to serializeValue)
    - Unknown-selection error message names the action and the offending selection
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>buildActionArgs builds validated in-process and wire args via serializeValue; exported from the barrel; tests green.</done>
</task>

</tasks>

<threat_model>
Trust boundary: `selectionValues` is caller-supplied. Mitigation (V5): buildActionArgs validates every selection name against the action's `ActionDefinition.selections` before producing args, rejecting typos/injection with an actionable error rather than silently passing them through. Wire-format output is consumed server-side only (cross-process arg injection from untrusted senders is out of scope for Phase 117 per RESEARCH.md Security Domain). No network/auth/secrets surface; no package installs.
</threat_model>

<verification>
- `npx vitest run src/engine/` green
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
A caller can produce ready-to-submit args from plain selection values: element objects by default (accepted directly by runner.performAction), JSON-safe references under `{ format: 'wire' }`, with unknown selections rejected actionably.
</success_criteria>

<output>
Create `.planning/phases/117-action-space-introspection/117-03-SUMMARY.md` when done.
</output>
