---
phase: 117-action-space-introspection
plan: 04
type: execute
wave: 4
depends_on: ["117-01", "117-03"]
files_modified:
  - src/engine/element/game.ts
  - src/engine/index.ts
  - src/engine/element/get-action-space.test.ts
  - src/engine/element/image-leak.test.ts
autonomous: true
requirements: [INTRO-01, INTRO-02, INTRO-05]
must_haves:
  truths:
    - "A caller can call game.getActionSpace(seat) to get every legal action with selections, dependsOn, choices, and a ready-to-submit argTemplate, in one JSON-serializable structure"
    - "argTemplate uses null for optional selections and { __required: true } for required ones (D-01)"
    - "A caller can call game.getActionSchema(name, seat) to get a single action's ActionMetadata without executing it"
    - "A hidden element belonging to one seat does NOT appear in another seat's getActionSpace validElements (no hidden-info leak)"
    - "createPlayerView / runner.getPlayerView remain the documented perspective-aware path (INTRO-05, no new filter)"
  artifacts:
    - path: "src/engine/element/game.ts"
      provides: "getActionSpace + getActionSchema methods; ActionSpaceView/ActionSchemaView types"
      contains: "getActionSpace"
    - path: "src/engine/element/get-action-space.test.ts"
      provides: "INTRO-01 + INTRO-02 engine->session integration coverage"
    - path: "src/engine/element/image-leak.test.ts"
      provides: "INTRO-05 hidden-info leak regression for getActionSpace"
  key_links:
    - from: "src/engine/element/game.ts (getActionSpace)"
      to: "buildActionMetadata (session)"
      via: "buildActionMetadata(this, player, availableActionNames)"
      pattern: "buildActionMetadata"
    - from: "src/engine/index.ts"
      to: "ActionSpaceView / ActionSchemaView"
      via: "barrel type export"
      pattern: "ActionSpaceView"
---

<objective>
Implement INTRO-01 (`game.getActionSpace(seat): ActionSpaceView`), INTRO-02 convenience (`game.getActionSchema(actionName, seat): ActionMetadata | undefined`), and INTRO-05's security-critical leak regression test — the keystone introspection surface. `getActionSpace` wraps existing primitives in the exact locked order; it does not build a parallel validator.

Purpose: This is the single method every later v4.3 layer depends on — "what can this seat do right now, with what choices?" — emitted as one JSON-serializable structure. INTRO-05 here is the [BLOCKING] guarantee that this surface never leaks hidden information across seats.
Output: New `getActionSpace`/`getActionSchema` methods + `ActionSpaceView`/`ActionSchemaView` types on `src/engine/element/game.ts`, barrel-exported; new `get-action-space.test.ts`; extended `image-leak.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-action-space-introspection/117-RESEARCH.md
@.planning/phases/117-action-space-introspection/117-VALIDATION.md
@.planning/v4.3-API-DESIGN.md
@src/engine/element/game.ts

<interfaces>
<!-- New types to define (design doc INTRO-01, lines 87-112) — define in game.ts, export from engine barrel -->
ActionSpaceView { actions: ActionSchemaView[] }
ActionSchemaView {
  name: string; prompt?: string; help?: string;
  selections: PickMetadata[];                          // existing type, src/session/types.ts
  argTemplate: Record<string, null | { __required: true }>;  // D-01: optional->null, required->{__required:true}
}

<!-- New methods on Game<P,B> (game.ts) -->
getActionSpace(seat: number): ActionSpaceView
getActionSchema(actionName: string, seat: number): ActionMetadata | undefined

<!-- Implementation primitives (locked order, INTRO-01 lines 115-121) -->
availableActionsForSeat(game.getFlowState(), seat): string[]   // engine barrel
game.getPlayer(seat): P | undefined                            // game.ts:1749
buildActionMetadata(game, player, names): Record<string, ActionMetadata>  // session/utils.ts:35
// ActionMetadata.selections: PickMetadata[]  (PickMetadata carries optional, choices, validElements, dependsOn)

<!-- INTRO-05 (DOCUMENT, do not rebuild) -->
createPlayerView(game, playerPosition): PlayerStateView   // snapshot.ts:194 — hidden-info filter via toJSONForPlayer
runner.getPlayerView(playerPosition): PlayerStateView      // runner.ts:281
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write get-action-space integration test (INTRO-01 + INTRO-02)</name>
  <files>src/engine/element/get-action-space.test.ts</files>
  <read_first>
    - src/session/action-help-propagation.test.ts (analog: building a real Game with registered actions + buildActionMetadata behavior)
    - src/session/seat-activity-canonical.test.ts (analog: seat activity / availableActionsForSeat setup)
    - src/session/types.ts:356,293 (ActionMetadata + PickMetadata shapes the assertions check)
    - .planning/v4.3-API-DESIGN.md (INTRO-01 lines 84-123, INTRO-02 lines 129-165 — exact return shapes, D-01 argTemplate)
    - .planning/phases/117-action-space-introspection/117-VALIDATION.md (per-requirement map for INTRO-01/02)
  </read_first>
  <action>
    Create src/engine/element/get-action-space.test.ts driving a real Game with at least one registered action that has both a required and an optional selection (trace a real value engine->session per CLAUDE.md). Assert: (1) getActionSpace(seat).actions contains only actions legal for that seat (cross-seat scoping); (2) each ActionSchemaView carries name + selections (PickMetadata with dependsOn/choices where applicable); (3) argTemplate has one key per selection with `null` for optional selections and `{ __required: true }` for required ones (D-01); (4) the whole ActionSpaceView survives JSON.stringify->parse unchanged (serializable); (5) getActionSchema(actionName, seat) returns an ActionMetadata with the expected selections and returns undefined for an unknown action. RED — methods do not exist yet.
  </action>
  <verify>
    <automated>npx vitest run src/engine/element/get-action-space.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File exists referencing game.getActionSpace and game.getActionSchema
    - Test FAILS (methods not implemented) — RED confirmed
    - Test asserts D-01 argTemplate (null optional / { __required: true } required)
    - Test asserts JSON round-trip stability of ActionSpaceView
  </acceptance_criteria>
  <done>RED integration test covering getActionSpace, argTemplate, JSON-serializability, and getActionSchema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement getActionSpace + getActionSchema and export the view types</name>
  <files>src/engine/element/game.ts, src/engine/index.ts</files>
  <behavior>
    - getActionSpace(seat) returns { actions: [] } when the seat has no player
    - getActionSpace(seat) returns one ActionSchemaView per legal action, each with argTemplate per D-01
    - getActionSchema(name, seat) returns ActionMetadata for a known action, undefined otherwise
    - ActionSpaceView is JSON-serializable (no element refs in the template)
  </behavior>
  <read_first>
    - src/engine/element/game.ts:875,934,1495,1749 (getAction, getSelectionChoices, getFlowState, getPlayer — the methods to compose)
    - src/engine/index.ts:175,211,231 (barrel: availableActionsForSeat already exported line 175; PlayerStateView exported line 231 — follow that pattern for the new view types)
    - .planning/phases/117-action-space-introspection/117-RESEARCH.md (Target 1 implementation chain; Target 2 + Pitfall 1 + OQ-1 — AVOID engine<->session circular dep)
    - .planning/v4.3-API-DESIGN.md (INTRO-01 implementation constraint lines 115-125; INTRO-02 convenience method lines 146-151)
  </read_first>
  <action>
    Add to src/engine/element/game.ts the `ActionSpaceView` and `ActionSchemaView` interfaces and two methods on Game<P,B>. Implement `getActionSpace(seat)` in the locked order (INTRO-01): availableActionsForSeat(this.getFlowState(), seat) -> if no this.getPlayer(seat) return { actions: [] } -> buildActionMetadata(this, player, actionNames) -> map each ActionMetadata entry to an ActionSchemaView, deriving argTemplate from its selections (D-01: a selection whose `optional` is true -> null; otherwise -> { __required: true }). Do NOT call evaluateCondition directly — buildActionMetadata already does condition re-checking (no parallel validator). Implement `getActionSchema(actionName, seat)` as the thin convenience: resolve the Player, call buildActionMetadata(this, player, [actionName]) and return the single entry (or undefined if absent). CRITICAL per research Pitfall 1 / OQ-1: implement via buildActionMetadata delegation and do NOT add a fresh `import { buildSingleActionMetadata }` from session into engine — that creates an engine<->session circular dependency; verify the import graph stays acyclic (tsc/bundler clean). Export `ActionSpaceView` and `ActionSchemaView` as types from the engine barrel src/engine/index.ts alongside PlayerStateView.
  </action>
  <verify>
    <automated>npx vitest run src/engine/element/get-action-space.test.ts 2>&1 | tail -15 && grep -n "ActionSpaceView\|ActionSchemaView" src/engine/index.ts && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `getActionSpace` and `getActionSchema` exist on Game (grep in game.ts)
    - `ActionSpaceView` + `ActionSchemaView` exported from src/engine/index.ts
    - `npx vitest run src/engine/element/get-action-space.test.ts` exits 0 (GREEN)
    - `grep -n "evaluateCondition\|buildSingleActionMetadata" src/engine/element/game.ts` shows NO new direct call/import added by this task (delegation only)
    - `npx tsc --noEmit` clean (no circular-dependency error)
  </acceptance_criteria>
  <done>getActionSpace/getActionSchema return the locked shapes with D-01 argTemplate, no parallel validator, no circular dependency; view types exported.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Hidden-info leak regression for getActionSpace (INTRO-05)</name>
  <files>src/engine/element/image-leak.test.ts</files>
  <read_first>
    - src/engine/element/image-leak.test.ts (existing leak test — extend it; mirror its hidden-element setup)
    - src/engine/element/deck-hand-visibility.test.ts (analog: constructing a game with an element hidden from another seat)
    - src/engine/utils/snapshot.ts:194 (createPlayerView -> toJSONForPlayer filtering — the single correct hidden-info path; INTRO-05)
    - .planning/phases/117-action-space-introspection/117-VALIDATION.md (Security-Critical: INTRO-05 leak regression — mandatory)
    - .planning/phases/117-action-space-introspection/117-RESEARCH.md (Target 5 — hidden-info mechanism, security requirement)
  </read_first>
  <action>
    Extend src/engine/element/image-leak.test.ts with a case that constructs a game where one seat owns a hidden element (e.g. a card in an opponent's hand with restricted visibility) that is a valid pick for some action's element selection. Call game.getActionSpace from a different seat and assert that NO id belonging to the hidden element's family appears in any returned ActionSchemaView.selections[].validElements. This guards against buildActionMetadata leaking hidden element IDs via PickMetadata.validElements. This assertion is a BLOCKING acceptance criterion — a leak here is a security regression and the phase cannot pass with it red. Also add a short doc comment in the test (or reference) pointing future code at createPlayerView / runner.getPlayerView as the only sanctioned perspective-aware path (INTRO-05 document-not-rebuild) — never raw game.toJSON().
  </action>
  <verify>
    <automated>npx vitest run src/engine/element/image-leak.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - [BLOCKING] image-leak.test.ts has a new case calling game.getActionSpace from a non-owning seat
    - The case asserts the hidden element's id is absent from every returned validElements array
    - `npx vitest run src/engine/element/image-leak.test.ts` exits 0
  </acceptance_criteria>
  <done>getActionSpace proven not to leak hidden-element IDs across seats; existing leak coverage still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seat N caller -> getActionSpace(other seat's hidden state) | A caller asks the action space for a seat; the returned metadata must not expose elements hidden from that perspective. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Information Disclosure | game.getActionSpace -> buildActionMetadata -> PickMetadata.validElements | mitigate | [BLOCKING] Task 3 leak regression in image-leak.test.ts: a hidden element owned by another seat must not appear in validElements; hidden-info filtering stays on the existing toJSONForPlayer path (INTRO-05) — getActionSpace adds no raw game.toJSON() path. |
| T-117-02 | Information Disclosure | cross-seat action scoping | mitigate | getActionSpace derives actions only from availableActionsForSeat(flowState, seat); buildActionMetadata evaluates per-player conditions, so seat N never sees seat M's actions (asserted in get-action-space.test.ts). |
| T-117-03 | Tampering | npm/pip/cargo installs | accept | No package-manager installs in Phase 117 (RESEARCH.md "Standard Stack": no new packages). No supply-chain surface; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run src/engine/` green (incl. get-action-space + image-leak)
- `npx tsc --noEmit` clean
- Phase gate: `npx vitest run` (full suite) green before /gsd:verify-work
</verification>

<success_criteria>
A caller gets the full legal action space for a seat — selections, dependsOn, choices, and a D-01 argTemplate — in one JSON-serializable structure, plus single-action schema via getActionSchema, with a [BLOCKING] proof that hidden information never leaks across seats. No parallel validator, no engine<->session circular dependency.
</success_criteria>

<output>
Create `.planning/phases/117-action-space-introspection/117-04-SUMMARY.md` when done.
</output>
