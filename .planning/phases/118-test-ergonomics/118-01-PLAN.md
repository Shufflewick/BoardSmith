---
phase: 118-test-ergonomics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/testing/test-game.ts
  - src/testing/test-game.test.ts
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "A test author reads observable game state via testGame.getPlayerView(seat) and gets a typed PlayerStateView (IDE surfaces the type, no JSON parsing)"
    - "A test author reads per-game custom properties via testGame.game.<prop> with full game-class typing"
    - "getPlayerView keeps using the Phase 117 perspective filter — hidden info stays excluded (no raw game.toJSON leak)"
  artifacts:
    - path: "src/testing/test-game.ts"
      provides: "getPlayerView with explicit PlayerStateView return annotation + JSDoc documenting both read patterns"
      contains: "getPlayerView"
    - path: "src/testing/test-game.test.ts"
      provides: "TEST-01 typed-state-access + hidden-info-exclusion tests"
      contains: "getPlayerView"
  key_links:
    - from: "src/testing/test-game.ts getPlayerView"
      to: "this.runner.getPlayerView"
      via: "delegation to Phase 117 perspective view"
      pattern: "runner\\.getPlayerView"
---

<objective>
Make TEST-01 real: a test author can read observable game state through a typed API
without parsing snapshot JSON or knowing per-game accessor methods.

`testGame.getPlayerView(seat)` already exists (src/testing/test-game.ts:243) and already
delegates to the Phase 117 perspective view via `runner.getPlayerView`. The deliverable is
NOT a new API surface — it is (a) an explicit `: PlayerStateView` return-type annotation so
IDEs surface the type, and (b) JSDoc documenting the two correct read patterns:
`getPlayerView(seat)` for perspective-correct observable state, and `testGame.game.<prop>`
for typed per-game property access. Plus the Wave-0 test file proving both.

Purpose: Remove the "parse snapshot JSON" footgun from test authoring (Pit of Success).
Output: Annotated + documented `getPlayerView`, new `test-game.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-test-ergonomics/118-RESEARCH.md
@.planning/phases/118-test-ergonomics/118-CONTEXT.md

<interfaces>
<!-- Executor uses these directly — no codebase exploration needed. -->

From src/testing/test-game.ts (existing):
- `export class TestGame<G extends Game = Game>`
- `readonly game: G` — typed access to the game instance for per-game properties
- `getPlayerView(playerSeat: number)` at :243 → `return this.runner.getPlayerView(playerSeat);`
- `getPlayer(position: number): Player` at :178

PlayerStateView (from src/engine/utils/snapshot.ts:97, re-exported via runtime/engine):
- `{ player: number; state: ElementJSON; flowState?: { awaitingInput: boolean; isMyTurn: boolean; availableActions?: string[] }; messages; phase: string; complete: boolean; winners?: number[]; actionMetadata?; tutorial?; disabledActions? }`
- `.state` is `ElementJSON` (perspective-filtered, hidden elements obscured) — NOT the typed domain model.

The image-leak sanctioned path: hidden-info filtering happens inside `createPlayerView` /
`runner.getPlayerView` (Phase 117 / INTRO-05). Do NOT introduce any raw `game.toJSON()` read.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing TEST-01 tests (RED)</name>
  <files>src/testing/test-game.test.ts</files>
  <read_first>
    - src/testing/test-game.ts (TestGame, getPlayerView:243, getPlayer:178, game field)
    - src/testing/random-simulation.test.ts (Vitest import + createTestGame usage pattern for this module)
    - .planning/phases/118-test-ergonomics/118-RESEARCH.md ("TEST-01" section + Test Map rows)
    - .planning/v4.3-API-DESIGN.md (TEST-01 section)
  </read_first>
  <action>
    Create src/testing/test-game.test.ts using Vitest (describe/it/expect). Use an existing
    in-repo test game class as the fixture — reuse whatever game the sibling test files in
    src/testing/ already import (check random-simulation.test.ts / simulate-tutorial.test.ts for
    the established fixture import) so no new game is invented. Cover:
    (1) getPlayerView(seat) returns an object whose shape matches PlayerStateView — assert presence
    of `player`, `phase`, `complete`, and `flowState` fields, and that `player` equals the requested seat.
    (2) Hidden-info exclusion: in a game with a hidden zone (e.g. a player hand or deck), the view
    returned by getPlayerView for one seat does NOT expose another seat's hidden element contents —
    assert the perspective filter is in effect (compare against the seat's own view).
    (3) Typed per-game access: testGame.game is the concrete game class and a known per-game property
    is readable directly (typed), demonstrating the second documented read pattern.
    Tests describe the intended contract; some may already pass since getPlayerView exists — that is
    acceptable for this annotation-only requirement. Do NOT weaken any assertion to force green.
  </action>
  <verify>
    <automated>npx vitest run src/testing/test-game.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/testing/test-game.test.ts exists and is picked up by Vitest.
    - Contains at least one hidden-info-exclusion assertion (cross-layer testing→engine perspective integration test).
    - `npx vitest run src/testing/test-game.test.ts` runs (assertions reflect real contract, none stubbed).
  </acceptance_criteria>
  <done>Test file exists, runs under Vitest, and encodes the TEST-01 contract (typed view, hidden-info exclusion, typed per-game access).</done>
</task>

<task type="auto">
  <name>Task 2: Annotate + document getPlayerView (GREEN)</name>
  <files>src/testing/test-game.ts</files>
  <read_first>
    - src/testing/test-game.ts (getPlayerView:243 + existing import block for PlayerStateView)
    - src/engine/utils/snapshot.ts (PlayerStateView interface — confirm export name/path to import)
    - .planning/phases/118-test-ergonomics/118-RESEARCH.md ("TEST-01" full detail, lines ~150-208)
  </read_first>
  <action>
    Add an explicit `: PlayerStateView` return-type annotation to `getPlayerView(playerSeat: number)`
    in src/testing/test-game.ts. Import the `PlayerStateView` type from its canonical source
    (the runtime/engine barrel that already re-exports it — match however ActionExecutionResult is
    already imported in this file; prefer a `type` import). Do not change the body — it MUST keep
    delegating to `this.runner.getPlayerView(playerSeat)` (the Phase 117 perspective filter); no raw
    `game.toJSON()`. Expand the JSDoc to document the two correct read patterns explicitly: (1)
    `testGame.getPlayerView(seat)` for perspective-correct observable state (hidden info excluded), and
    (2) `testGame.game.<prop>` for typed per-game custom-property access. Per CLAUDE.md "Pit of Success",
    make the JSDoc steer authors away from parsing the raw `.state` JSON.
  </action>
  <verify>
    <automated>npx vitest run src/testing/ && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - getPlayerView signature is exactly `getPlayerView(playerSeat: number): PlayerStateView`.
    - Body unchanged (still `return this.runner.getPlayerView(playerSeat);`).
    - JSDoc documents both read patterns (getPlayerView and testGame.game.<prop>).
    - `npx tsc --noEmit` is clean.
    - `npx vitest run src/testing/` green (test-game.test.ts + existing testing tests).
  </acceptance_criteria>
  <done>getPlayerView has explicit PlayerStateView return type + two-pattern JSDoc; tsc clean; all src/testing tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testing → engine (perspective view) | getPlayerView must surface only perspective-filtered state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01 | Information Disclosure | testGame.getPlayerView | mitigate | Keep delegating to runner.getPlayerView (Phase 117 createPlayerView filter); never read raw game.toJSON(). Hidden-info-exclusion test in test-game.test.ts asserts no cross-seat leak. |

No network/auth/secrets in this phase; no other STRIDE categories apply (in-process test utility).
</threat_model>

<verification>
- `npx vitest run src/testing/` green.
- `npx tsc --noEmit` clean.
- getPlayerView annotated `: PlayerStateView` with two-pattern JSDoc; body unchanged.
</verification>

<success_criteria>
A test author reads typed observable state via getPlayerView (IDE shows PlayerStateView) and
typed per-game props via testGame.game.<prop>, with hidden info still excluded — no JSON parsing.
</success_criteria>

<output>
Create `.planning/phases/118-test-ergonomics/118-01-SUMMARY.md` when done.
</output>
