---
phase: 118-test-ergonomics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/testing/simulate-action.ts
  - src/testing/index.ts
  - src/testing/play-until-complete.test.ts
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "A test author drives a game to completion with one call: playUntilComplete(testGame)"
    - "playUntilComplete handles BOTH sequential (flowState.currentPlayer) and simultaneous (flowState.awaitingPlayers) turns without hanging"
    - "On a dead-ended (stuck-but-not-complete) game OR when maxMoves is exceeded, it throws a structured GameStuckError with an actionable message — never hangs"
    - "strategy:'first' + injectable rng make runs reproducible"
    - "Moves come from Phase 117 enumerateLegalMoves — no hand-rolled enumeration"
  artifacts:
    - path: "src/testing/simulate-action.ts"
      provides: "playUntilComplete function + GameStuckError class"
      contains: "playUntilComplete"
    - path: "src/testing/index.ts"
      provides: "barrel exports for playUntilComplete, GameStuckError, options type"
      contains: "playUntilComplete"
    - path: "src/testing/play-until-complete.test.ts"
      provides: "TEST-02 tests incl. sequential completion, simultaneous turns, both GameStuckError variants, determinism"
      contains: "GameStuckError"
  key_links:
    - from: "src/testing/simulate-action.ts playUntilComplete"
      to: "enumerateLegalMoves (engine)"
      via: "move enumeration per active seat"
      pattern: "enumerateLegalMoves"
    - from: "playUntilComplete"
      to: "testGame.doAction / testGame.isComplete"
      via: "drive + termination check"
      pattern: "doAction|isComplete"
---

<objective>
Build TEST-02: `playUntilComplete(testGame, options?)` — one call drives a game to completion,
guards against infinite/stuck loops, and throws a structured `GameStuckError` instead of hanging.

CRITICAL correctness requirement (research Pitfall 2): the loop MUST determine active seats from
BOTH `flowState.currentPlayer` (sequential) AND `flowState.awaitingPlayers` filtered to
`!p.completed` (simultaneous). Treating an undefined currentPlayer as "no active player" causes an
infinite loop on simultaneous-action games. Moves MUST be picked via the Phase 117
`enumerateLegalMoves(game, seat)` primitive — do NOT hand-roll selection enumeration.

Purpose: The marquee ergonomic — replace bespoke play loops with one safe, reproducible call.
Output: `playUntilComplete` + `GameStuckError` in simulate-action.ts, barrel exports, test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-test-ergonomics/118-RESEARCH.md
@.planning/phases/118-test-ergonomics/118-CONTEXT.md

<interfaces>
<!-- Executor uses these directly. -->

From src/engine/utils/enumerate-moves.ts:34 (exported via src/engine/index.ts:209):
- `enumerateLegalMoves(game: Game, seat: number, options?: { maxPerAction?: number }): Array<{ action: string; args: Record<string, unknown> }>`
- Returns in-process element objects (not wire IDs). Returns `[]` when game.getFlowState() is falsy.

From src/testing/test-game.ts:
- `testGame.game: G` (the Game instance to pass to enumerateLegalMoves)
- `testGame.getFlowState(): FlowState | undefined`
- `testGame.isComplete(): boolean`  (delegates to runner: flowState?.complete ?? false)
- `testGame.doAction(seat, actionName, args): ActionExecutionResult` ({ success, error?, flowState? })

FlowState (src/engine/flow/types.ts:240):
- `{ complete: boolean; awaitingInput: boolean; currentPlayer?: number; awaitingPlayers?: PlayerAwaitingState[] }`
- `PlayerAwaitingState = { playerIndex: number; availableActions: string[]; completed: boolean }` (playerIndex is 1-indexed seat)

From src/testing/simulate-action.ts (existing home for simulation helpers):
- `simulateAction`, `simulateActions`, `assertActionSucceeds`, `assertActionFails`, `type SimulateActionResult`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing TEST-02 tests (RED)</name>
  <files>src/testing/play-until-complete.test.ts</files>
  <read_first>
    - src/testing/random-simulation.test.ts (Vitest pattern + in-repo game fixtures used by this module)
    - src/testing/test-game.ts (TestGame API: doAction, isComplete, getFlowState, game)
    - src/engine/utils/enumerate-moves.ts (enumerateLegalMoves return shape)
    - .planning/phases/118-test-ergonomics/118-RESEARCH.md ("TEST-02" full detail + loop skeleton + Pitfall 2 + Test Map rows)
  </read_first>
  <action>
    Create src/testing/play-until-complete.test.ts (Vitest). Reuse existing in-repo game fixtures the
    sibling testing tests already import (find a game that reaches a natural finish, e.g. a simple
    sequential game, for the completion test). Cover the full TEST-02 contract:
    (1) playUntilComplete(testGame) drives a 2-player SEQUENTIAL game to completion — afterwards
    testGame.isComplete() is true (cross-layer testing→engine integration test exercising enumerateLegalMoves).
    (2) strategy:'first' is deterministic: two runs from identical start produce identical action history.
    (3) injectable rng: passing a seeded/stub rng produces a reproducible run.
    (4) GameStuckError thrown when a game dead-ends (active seat exists but enumerateLegalMoves returns
    [] and game is not complete) — construct or stub a fixture that reaches a no-legal-move-not-complete state.
    (5) GameStuckError thrown when maxMoves is exceeded (set maxMoves very low, e.g. 1, on a game that
    needs more moves) — assert it throws (does NOT hang) and the error carries iteration/availableActions/flowState.
    (6) SIMULTANEOUS turns: a game using awaitingPlayers (simultaneous action step) is driven without
    hanging — if no such fixture exists in-repo, construct a minimal test game with a simultaneous step,
    or stub a TestGame-like whose getFlowState returns awaitingPlayers; assert the loop acts on all
    incomplete awaiting seats and terminates. Do NOT skip this case — it is the highest-risk behavior.
    Tests must fail/error now because playUntilComplete and GameStuckError do not yet exist.
  </action>
  <verify>
    <automated>npx vitest run src/testing/play-until-complete.test.ts 2>&1 | grep -qi "playUntilComplete\|GameStuckError" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists; references playUntilComplete and GameStuckError.
    - Covers: sequential completion, strategy:'first' determinism, injectable rng, GameStuckError-on-dead-end, GameStuckError-on-maxMoves, simultaneous (awaitingPlayers) turns.
    - Fails now (symbols undefined) — genuine RED, no stubbed-green.
  </acceptance_criteria>
  <done>play-until-complete.test.ts encodes all six TEST-02 behaviors and fails because the implementation is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement playUntilComplete + GameStuckError (GREEN)</name>
  <files>src/testing/simulate-action.ts, src/testing/index.ts</files>
  <read_first>
    - src/testing/simulate-action.ts (existing helpers + import style)
    - src/testing/index.ts (current barrel export block for simulate-action)
    - src/engine/utils/enumerate-moves.ts (enumerateLegalMoves) and src/engine/index.ts:209 (export path)
    - src/engine/flow/types.ts:240 (FlowState, PlayerAwaitingState)
    - .planning/phases/118-test-ergonomics/118-RESEARCH.md ("TEST-02" full detail + loop skeleton, lines ~556-608)
  </read_first>
  <behavior>
    - Drives a sequential 2p game to isComplete() === true.
    - Drives a simultaneous (awaitingPlayers) game to completion without hanging — acts on every incomplete awaiting seat each iteration.
    - strategy:'first' picks moves[0]; 'random' picks moves[floor(rng()*len)]; rng defaults to Math.random.
    - Throws GameStuckError when active seats exist but all have zero legal moves and game not complete.
    - Throws GameStuckError when maxMoves (default 1000) is exceeded without completion.
    - GameStuckError exposes iteration, availableActions, flowState; message names the active seat, legal-move count, and distinguishes "no legal moves" vs "maxMoves exceeded".
  </behavior>
  <action>
    In src/testing/simulate-action.ts add `class GameStuckError extends Error` with
    `readonly name = 'GameStuckError' as const`, `readonly iteration: number`,
    `readonly availableActions: string[]`, `readonly flowState: FlowState | undefined`, set via the
    constructor (call super(message) with a composed actionable message per CLAUDE.md). Add
    `interface PlayUntilCompleteOptions { maxMoves?: number; strategy?: 'random' | 'first'; rng?: () => number }`
    and `function playUntilComplete(testGame: TestGame, options?: PlayUntilCompleteOptions): void`.
    Defaults: maxMoves=1000, strategy='random', rng=Math.random. Loop up to maxMoves iterations:
    return early if testGame.isComplete(); read flowState = testGame.getFlowState(); if not
    awaitingInput, continue (flow is auto-advancing). Determine activeSeats: if
    flowState.currentPlayer !== undefined push it; ELSE iterate flowState.awaitingPlayers and push
    playerIndex for each `!completed` entry. For each active seat call
    `enumerateLegalMoves(testGame.game, seat)`; pick move ('first' => [0], else rng index); call
    testGame.doAction(seat, move.action, move.args); track that a move was made. If activeSeats
    non-empty but NO move was made (all enumerations empty) and game not complete, throw GameStuckError
    (dead-end variant — message distinguishes availableActions non-empty-but-no-legal-moves vs empty).
    After the loop, if still not complete, throw GameStuckError (maxMoves-exceeded variant). Import
    enumerateLegalMoves from the engine barrel (../engine/index.js) and FlowState type as needed; do
    NOT hand-roll enumeration. Then export `playUntilComplete`, `GameStuckError`, and
    `type PlayUntilCompleteOptions` from src/testing/index.ts (extend the existing simulate-action export block).
  </action>
  <verify>
    <automated>npx vitest run src/testing/play-until-complete.test.ts && npx vitest run src/testing/ && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - playUntilComplete(testGame: TestGame, options?: { maxMoves?: number; strategy?: 'random'|'first'; rng?: () => number }): void exists.
    - GameStuckError extends Error with iteration, availableActions, flowState; actionable message.
    - Active-seat resolution handles currentPlayer AND awaitingPlayers (filter !completed).
    - Move enumeration uses enumerateLegalMoves (grep confirms import, no bespoke selection traversal).
    - Barrel exports playUntilComplete, GameStuckError, PlayUntilCompleteOptions.
    - play-until-complete.test.ts green; `npx vitest run src/testing/` green; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>playUntilComplete + GameStuckError implemented, simultaneous+sequential handled, exported; all TEST-02 tests and existing testing tests green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testing → engine (enumerateLegalMoves) | In-process move enumeration; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-02 | Denial of Service (test hang) | playUntilComplete loop | mitigate | Bounded by maxMoves; simultaneous-turn handling prevents infinite loop; GameStuckError thrown instead of hanging. Tests cover both stuck variants + simultaneous turns. |

No network/auth/secrets; in-process test utility only — no other STRIDE categories apply.
</threat_model>

<verification>
- `npx vitest run src/testing/` green (incl. play-until-complete.test.ts).
- `npx tsc --noEmit` clean.
- enumerateLegalMoves imported and used (no hand-rolled enumeration).
- Both stuck variants + simultaneous-turn case exercised by tests.
</verification>

<success_criteria>
One call (`playUntilComplete(testGame)`) drives sequential and simultaneous games to completion,
reproducibly with strategy/rng, and throws a structured actionable GameStuckError instead of hanging.
</success_criteria>

<output>
Create `.planning/phases/118-test-ergonomics/118-02-SUMMARY.md` when done.
</output>
