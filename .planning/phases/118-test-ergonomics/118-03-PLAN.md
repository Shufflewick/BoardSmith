---
phase: 118-test-ergonomics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/testing/assertions.ts
  - src/testing/assertions.test.ts
autonomous: true
requirements: [TEST-03, TEST-04]
must_haves:
  truths:
    - "A failed assertActionAvailable message automatically explains WHY the action is unavailable (reason + per-selection notes from debugActionAvailability)"
    - "A passing assertActionAvailable does NOT call debugActionAvailability (no perf regression)"
    - "assertFlowState supports actionsMode: 'exact' (default) fails on extra actions; 'contains' allows extra actions"
    - "Omitting actionsMode preserves current exact behavior (zero migration impact, per D-06)"
  artifacts:
    - path: "src/testing/assertions.ts"
      provides: "assertActionAvailable trace-on-failure + assertFlowState actionsMode option"
      contains: "debugActionAvailability"
    - path: "src/testing/assertions.test.ts"
      provides: "TEST-03 + TEST-04 tests"
      contains: "actionsMode"
  key_links:
    - from: "src/testing/assertions.ts assertActionAvailable (failure branch)"
      to: "testGame.game.debugActionAvailability"
      via: "trace enrichment after resolving Player"
      pattern: "debugActionAvailability"
    - from: "src/testing/assertions.ts assertFlowState"
      to: "extra-actions check"
      via: "guarded behind actionsMode === 'exact'"
      pattern: "actionsMode"
---

<objective>
Build TEST-03 and TEST-04, both confined to src/testing/assertions.ts (co-located to avoid a
same-file collision — they ship in one plan).

TEST-03: wire `testGame.game.debugActionAvailability(actionName, player)` (game.ts:1072, returns
ActionDebugInfo with `reason` + `details.selections[]` each `{name, choices, passed, note?}`) into the
FAILURE branch of `assertActionAvailable` (assertions.ts:180) so a failed availability assertion
explains itself. Call it ONLY on failure (no perf regression on passing assertions). Signature
unchanged. Per D-05 use `debugActionAvailability` — NOT the stale `traceAction()` in debug.ts.

TEST-04: add `actionsMode?: 'exact' | 'contains'` to `ExpectedFlowState` and guard the extra-actions
check (assertions.ts:103) behind `(expected.actionsMode ?? 'exact') === 'exact'`. Default exact (D-06),
zero migration impact.

Purpose: Self-explaining availability failures + opt-in permissive action-list assertions.
Output: extended assertions.ts + new assertions.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-test-ergonomics/118-RESEARCH.md
@.planning/phases/118-test-ergonomics/118-CONTEXT.md

<interfaces>
<!-- Executor uses these directly. -->

From src/testing/assertions.ts (existing):
- `interface ExpectedFlowState { currentPlayer?; actions?: string[]; complete?; awaitingInput?; phase? }` (:15)
- `assertFlowState(testGame: TestGame, expected: ExpectedFlowState): FlowStateAssertionResult` (:64)
  - extra-actions block at :103: `const extraActions = actualActions.filter(a => !expected.actions!.includes(a)); if (extraActions.length > 0) errors.push(...)`
- `assertActionAvailable(testGame: TestGame, playerSeat: number, actionName: string): void` (:180)
  - failure branch: `if (!availableActions.includes(actionName)) throw new Error(...)`

From src/testing/test-game.ts:
- `getPlayer(position: number): Player` (:178) — throws if invalid
- `game: G`

From src/engine/element/game.ts:1072:
- `debugActionAvailability(actionName: string, player: P): ActionDebugInfo`

ActionDebugInfo (src/engine/action/types.ts:646):
- `{ actionName; available: boolean; reason: string; details: { conditionPassed: boolean; conditionNote?: string; selections: PickDebugInfo[] } }`
- `PickDebugInfo = { name: string; choices: number; passed: boolean; note?: string }`

DO NOT use traceAction() from src/testing/debug.ts — it reads (game as any).actions (stale plain
object); the engine uses game._actions (private Map). D-05 locks debugActionAvailability.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing TEST-03 + TEST-04 tests (RED)</name>
  <files>src/testing/assertions.test.ts</files>
  <read_first>
    - src/testing/assertions.ts (assertActionAvailable:180, assertFlowState:64, ExpectedFlowState:15)
    - src/testing/random-simulation.test.ts (Vitest pattern + in-repo game fixtures)
    - src/engine/element/game.ts (debugActionAvailability:1072 + ActionDebugInfo return)
    - .planning/phases/118-test-ergonomics/118-RESEARCH.md ("TEST-03" + "TEST-04" full detail + Test Map rows)
  </read_first>
  <action>
    Create src/testing/assertions.test.ts (Vitest), reusing an in-repo game fixture that has at least
    one action with selections (so debugActionAvailability has a non-trivial trace). Cover:
    TEST-03 — (1) a failed assertActionAvailable (assert on an action that is NOT available for the seat)
    throws an Error whose message contains debugInfo.reason text and per-selection note lines (cross-layer
    testing→engine integration test: the trace must flow from the engine through the assertion message).
    (2) a PASSING assertActionAvailable does NOT invoke debugActionAvailability — spy on
    testGame.game.debugActionAvailability (vi.spyOn) and assert it is not called when the action IS available.
    TEST-04 — (3) assertFlowState({ actions: [<subset>], actionsMode: 'exact' }) throws when extra actions
    are actually available. (4) assertFlowState({ actions: [<subset>], actionsMode: 'contains' }) passes
    despite extra available actions. (5) assertFlowState({ actions: [<subset>] }) with NO actionsMode
    behaves as exact (throws on extra) — pins the D-06 backward-compatible default.
    Tests must fail now (actionsMode unsupported; failure message lacks the trace).
  </action>
  <verify>
    <automated>npx vitest run src/testing/assertions.test.ts 2>&1 | grep -qi "actionsMode\|reason\|debugActionAvailability" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists; covers TEST-03 (trace on failure, spy proves no-call on pass) and TEST-04 (exact/contains/default).
    - Includes a spy assertion that debugActionAvailability is NOT called on the passing path.
    - Fails now (feature absent) — genuine RED.
  </acceptance_criteria>
  <done>assertions.test.ts encodes TEST-03 + TEST-04 contracts and fails against current assertions.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire trace into assertActionAvailable (TEST-03, GREEN)</name>
  <files>src/testing/assertions.ts</files>
  <read_first>
    - src/testing/assertions.ts (assertActionAvailable:180 failure branch + import block)
    - src/testing/test-game.ts (getPlayer:178)
    - src/engine/element/game.ts:1072 (debugActionAvailability) + src/engine/action/types.ts:646 (ActionDebugInfo, PickDebugInfo)
    - .planning/phases/118-test-ergonomics/118-RESEARCH.md ("TEST-03 full detail" lines ~298-376 + trace-wiring example ~611-628)
  </read_first>
  <action>
    In assertActionAvailable's failure branch (where the action is NOT in availableActions), before
    throwing: resolve `const player = testGame.getPlayer(playerSeat);` then
    `const debugInfo = testGame.game.debugActionAvailability(actionName, player);`. Build per-selection
    lines from `debugInfo.details.selections` (each: a ✓/✗ marker from `passed`, the selection `name`,
    the `choices` count, and the optional `note`). Throw a single Error whose message includes: the
    original "Action ... not available for player ..." line, the available-actions list, a `Why: ${debugInfo.reason}`
    line, and (when present) a `Selections:` block with the per-selection lines. Keep the signature
    `(testGame, playerSeat, actionName): void` unchanged. The current-player guard branch (when
    flowState.currentPlayer !== playerSeat) stays as-is. Do NOT call debugActionAvailability on the
    success path — only in the failure branch (Pitfall 4: no perf regression). Do NOT import or use
    traceAction (D-05). Import ActionDebugInfo type only if needed for clarity.
  </action>
  <verify>
    <automated>npx vitest run src/testing/assertions.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Failure message includes debugInfo.reason and per-selection note/marker lines.
    - debugActionAvailability called ONLY in the failure branch (spy test green).
    - Signature unchanged; no traceAction usage (grep -c traceAction in assertions.ts == 0).
    - assertions.test.ts TEST-03 cases green; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Failed assertActionAvailable self-explains via debugActionAvailability trace; passing path untouched; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add actionsMode to assertFlowState (TEST-04, GREEN)</name>
  <files>src/testing/assertions.ts</files>
  <read_first>
    - src/testing/assertions.ts (ExpectedFlowState:15, assertFlowState extra-actions block:103)
    - .planning/phases/118-test-ergonomics/118-RESEARCH.md ("TEST-04 full detail" lines ~380-426)
  </read_first>
  <action>
    Add `actionsMode?: 'exact' | 'contains'` to the `ExpectedFlowState` interface with JSDoc: 'exact'
    (default) fails on both missing AND extra actions (backward-compatible); 'contains' fails only on
    missing actions, extra allowed (per D-06). In assertFlowState, keep the missing-actions check
    unconditional. Guard ONLY the extra-actions block (assertions.ts:103) behind
    `const mode = expected.actionsMode ?? 'exact'; if (mode === 'exact') { ... existing extra-actions
    push ... }`. Pure additive change — callers omitting actionsMode keep exact behavior, no migration.
    Update the existing `actions?` JSDoc to point at actionsMode for permissive matching.
  </action>
  <verify>
    <automated>npx vitest run src/testing/assertions.test.ts && npx vitest run src/testing/ && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ExpectedFlowState has actionsMode?: 'exact' | 'contains' with JSDoc.
    - Extra-actions check runs only when mode === 'exact'; missing-actions check always runs.
    - Default (omitted) === exact (TEST-04 default-case test green).
    - assertions.test.ts fully green; `npx vitest run src/testing/` green; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>assertFlowState supports exact (default) and contains modes; existing exact behavior preserved; all testing tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testing → engine (debugActionAvailability) | In-process trace read on assertion failure; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-03 | Tampering (misleading diagnostics) | assertActionAvailable trace | mitigate | Use debugActionAvailability (game._actions Map, actively maintained) per D-05, NOT stale traceAction; spy test confirms no-call on success path. |

No network/auth/secrets; in-process test utility only.
</threat_model>

<verification>
- `npx vitest run src/testing/` green (incl. assertions.test.ts).
- `npx tsc --noEmit` clean.
- No traceAction usage in assertions.ts.
- debugActionAvailability invoked only on failure (spy-verified).
</verification>

<success_criteria>
Failed availability assertions explain themselves (reason + selection notes); assertFlowState offers
explicit exact (default) vs contains action-list matching with zero migration impact.
</success_criteria>

<output>
Create `.planning/phases/118-test-ergonomics/118-03-SUMMARY.md` when done.
</output>
