---
phase: 118-test-ergonomics
plan: 04
type: execute
wave: 2
depends_on: ["118-01", "118-02"]
files_modified:
  - src/testing/action-builder.ts
  - src/testing/test-game.ts
  - src/testing/index.ts
  - src/testing/action-builder.test.ts
autonomous: true
requirements: [TEST-05]
must_haves:
  truths:
    - "A test author drives a multi-step / dependent-selection action through a fluent ActionBuilder instead of low-level resolveChoices/selectionStep calls"
    - "ActionBuilder.getChoices(selectionName) returns ONLY enabled choices (disabled === false), respecting already-set selections (dependent selections)"
    - "ActionBuilder.execute() throws a descriptive error when the action fails; buildArgs() returns the accumulated args without executing"
    - "testGame.action(name, seat) factory returns an ActionBuilder"
  artifacts:
    - path: "src/testing/action-builder.ts"
      provides: "ActionBuilder class delegating to game.getSelectionChoices"
      contains: "class ActionBuilder"
    - path: "src/testing/test-game.ts"
      provides: "action(name, seat) convenience factory"
      contains: "action("
    - path: "src/testing/index.ts"
      provides: "ActionBuilder barrel export"
      contains: "ActionBuilder"
    - path: "src/testing/action-builder.test.ts"
      provides: "TEST-05 tests (enabled-only choices, dependent selection, execute error, buildArgs, factory)"
      contains: "ActionBuilder"
  key_links:
    - from: "src/testing/action-builder.ts ActionBuilder.getChoices"
      to: "testGame.game.getSelectionChoices"
      via: "in-process choice resolution, filter disabled === false"
      pattern: "getSelectionChoices"
    - from: "src/testing/test-game.ts action()"
      to: "new ActionBuilder(this, name, seat)"
      via: "convenience factory"
      pattern: "new ActionBuilder"
---

<objective>
Build TEST-05: an `ActionBuilder` class (src/testing/action-builder.ts) so test authors drive
multi-step / dependent selections ergonomically instead of hand-constructing args or dropping to
low-level resolveChoices/selectionStep. It delegates to the existing engine primitive
`game.getSelectionChoices(actionName, selName, player, args)` (game.ts:978, returns
AnnotatedChoice[]), filtering to enabled choices (`c.disabled === false`, matching
enumerate-moves.ts:250) before exposing them. Provide a `testGame.action(name, seat)` factory.
`execute()` throws a descriptive error on action failure; `buildArgs()` returns accumulated values.

Wave 2: this plan touches test-game.ts (also touched by 118-01) and index.ts (also touched by
118-02), so it runs after both to avoid file collisions.

Purpose: Ergonomic multi-step action authoring (Pit of Success — enabled choices only by default).
Output: action-builder.ts, action() factory on TestGame, barrel export, test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-test-ergonomics/118-RESEARCH.md
@.planning/phases/118-test-ergonomics/118-CONTEXT.md

<interfaces>
<!-- Executor uses these directly. -->

From src/engine/element/game.ts:978:
- `getSelectionChoices(actionName: string, selectionName: string, player: P, args: Record<string, unknown> = {}): AnnotatedChoice<unknown>[]`
- AnnotatedChoice has at minimum `{ value: unknown; disabled: boolean; ... }`. Enabled filter pattern
  (confirmed src/engine/utils/enumerate-moves.ts:250): `result.filter(c => c.disabled === false)`.

From src/testing/test-game.ts:
- `game: G`
- `getPlayer(position: number): Player` (:178)
- `doAction(seat, actionName, args): ActionExecutionResult` ({ success: boolean; error?: string; ... })

From src/testing/index.ts: existing barrel (add ActionBuilder export alongside test-game / simulate-action exports).

Note (Pitfall 3): getChoices MUST filter `disabled === false`; returning disabled values causes
later "no valid choices" action failures.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing TEST-05 tests (RED)</name>
  <files>src/testing/action-builder.test.ts</files>
  <read_first>
    - src/testing/random-simulation.test.ts (Vitest pattern + in-repo fixtures)
    - src/engine/element/game.ts (getSelectionChoices:978, AnnotatedChoice shape)
    - src/testing/test-game.ts (doAction, getPlayer, game)
    - .planning/phases/118-test-ergonomics/118-RESEARCH.md ("TEST-05 full detail" lines ~429-504 + usage pattern + Pitfall 3 + Test Map rows)
  </read_first>
  <action>
    Create src/testing/action-builder.test.ts (Vitest), reusing an in-repo game fixture with a
    multi-step or element-selection action (ideally with a dependent selection where the second
    selection's choices depend on the first). Cover:
    (1) getChoices(selectionName) returns only enabled choices — if the fixture has a disabled choice,
    assert it is excluded; assert returned values are choice values (not AnnotatedChoice wrappers).
    (2) Dependent selection: after select(firstSel, value), getChoices(secondSel) reflects choices
    constrained by the accumulated arg (cross-layer testing→engine integration test through getSelectionChoices).
    (3) execute() runs the action successfully when args are valid (game state advances as expected).
    (4) execute() throws a descriptive Error (naming the action + seat) when the action fails (e.g. an
    invalid/missing selection value).
    (5) buildArgs() returns the accumulated selection values as a Record without executing.
    (6) testGame.action(name, seat) returns an ActionBuilder instance.
    Tests must fail now (ActionBuilder and testGame.action absent).
  </action>
  <verify>
    <automated>npx vitest run src/testing/action-builder.test.ts 2>&1 | grep -qi "ActionBuilder\|action(" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists; covers enabled-only choices, dependent selection, execute success, execute error, buildArgs, factory.
    - Includes a dependent-selection integration assertion (testing→engine via getSelectionChoices).
    - Fails now — genuine RED.
  </acceptance_criteria>
  <done>action-builder.test.ts encodes the full TEST-05 contract and fails against the absent implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ActionBuilder + action() factory + barrel (GREEN)</name>
  <files>src/testing/action-builder.ts, src/testing/test-game.ts, src/testing/index.ts</files>
  <read_first>
    - src/engine/element/game.ts:978 (getSelectionChoices) + src/engine/utils/enumerate-moves.ts:250 (disabled filter)
    - src/testing/test-game.ts (getPlayer:178, doAction, game; import style)
    - src/testing/index.ts (existing export blocks)
    - .planning/phases/118-test-ergonomics/118-RESEARCH.md ("TEST-05 full detail" lines ~449-504)
  </read_first>
  <behavior>
    - getChoices(selectionName) returns enabled-only choice values (disabled === false filter), respecting accumulated args (dependent selections).
    - select(name, value) is fluent (returns this) and accumulates into internal args.
    - execute() calls doAction; throws a descriptive Error naming action + seat + underlying error on failure.
    - buildArgs() returns a shallow copy of accumulated args without executing.
    - testGame.action(name, seat) returns a new ActionBuilder.
  </behavior>
  <action>
    Create src/testing/action-builder.ts exporting `class ActionBuilder`. Constructor
    `(testGame: TestGame, actionName: string, seat: number)` stores them and an internal
    `args: Record<string, unknown> = {}`. Methods: `getChoices(selectionName: string): unknown[]` —
    resolve `const player = this._testGame.getPlayer(this._seat)` then return
    `this._testGame.game.getSelectionChoices(this._actionName, selectionName, player, this._args)
    .filter(c => c.disabled === false).map(c => c.value)`. `select(selectionName: string, value: unknown): this`
    — `this._args = { ...this._args, [selectionName]: value }; return this;`. `execute(): void` — call
    `const result = this._testGame.doAction(this._seat, this._actionName, this._args)`; if
    `!result.success` throw `new Error("ActionBuilder.execute(): action '<name>' failed for seat <seat>: <result.error>")`
    (actionable per CLAUDE.md). `buildArgs(): Record<string, unknown>` — return `{ ...this._args }`.
    In src/testing/test-game.ts add `action(actionName: string, seat: number): ActionBuilder { return new ActionBuilder(this, actionName, seat); }`
    (import ActionBuilder from './action-builder.js'). In src/testing/index.ts export `ActionBuilder`
    from './action-builder.js'. Watch the test-game.ts ↔ action-builder.ts import cycle: import the
    ActionBuilder VALUE in test-game.ts and import the TestGame TYPE-ONLY in action-builder.ts
    (`import type { TestGame }`) to keep it a type-level edge.
  </action>
  <verify>
    <automated>npx vitest run src/testing/action-builder.test.ts && npx vitest run src/testing/ && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ActionBuilder with constructor(testGame, actionName, seat), getChoices, select (fluent), execute (throws descriptive on failure), buildArgs.
    - getChoices filters disabled === false (grep confirms) and maps to values; uses getSelectionChoices (no bespoke resolution).
    - testGame.action(name, seat) returns ActionBuilder; ActionBuilder exported from barrel.
    - action-builder.ts imports TestGame type-only (no runtime import cycle).
    - action-builder.test.ts green; `npx vitest run src/testing/` green; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>ActionBuilder + factory + barrel export implemented; enabled-only dependent-choice resolution via getSelectionChoices; all TEST-05 tests and existing testing tests green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testing → engine (getSelectionChoices) | In-process dependent-choice resolution; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-05 | Tampering (invalid choices) | ActionBuilder.getChoices | mitigate | Filter disabled === false before exposing choices (Pitfall 3), matching enumerate-moves.ts:250; execute() surfaces engine failure as descriptive error rather than silent bad state. |

No network/auth/secrets; in-process test utility only.
</threat_model>

<verification>
- `npx vitest run src/testing/` green (incl. action-builder.test.ts).
- `npx tsc --noEmit` clean.
- getChoices uses getSelectionChoices + disabled filter; no hand-rolled selection evaluation.
- No runtime import cycle between test-game.ts and action-builder.ts.
</verification>

<success_criteria>
A test author drives multi-step / dependent-selection actions fluently via testGame.action(name, seat)
.getChoices/.select/.execute, seeing only enabled choices, with execute() failing loudly and
buildArgs() exposing accumulated args.
</success_criteria>

<output>
Create `.planning/phases/118-test-ergonomics/118-04-SUMMARY.md` when done.
</output>
