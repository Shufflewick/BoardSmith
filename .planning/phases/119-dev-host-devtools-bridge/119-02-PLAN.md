---
phase: 119-dev-host-devtools-bridge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/composables/useActionController.ts
  - src/ui/global.d.ts
  - src/ui/composables/useActionController.devtools.test.ts
autonomous: true
requirements: [DEV-03]

must_haves:
  truths:
    - "A boardsmith:action-resolved CustomEvent fires with success:true on a successful execute() action"
    - "The event fires with success:false and error set when execute() fails"
    - "The event fires with the correct action name on the selection-step completion path (name captured before it is cleared)"
    - "Dispatch is gated by import.meta.env.DEV — no dispatch in production builds"
  artifacts:
    - path: "src/ui/composables/useActionController.ts"
      provides: "window.dispatchEvent(new CustomEvent('boardsmith:action-resolved', ...)) at execute() send branches and the selection-step completion path"
      contains: "boardsmith:action-resolved"
    - path: "src/ui/global.d.ts"
      provides: "BoardsmithActionResolvedDetail interface"
      contains: "BoardsmithActionResolvedDetail"
    - path: "src/ui/composables/useActionController.devtools.test.ts"
      provides: "unit tests for all three dispatch paths (execute success, execute failure, selection-step completion)"
      contains: "boardsmith:action-resolved"
  key_links:
    - from: "useActionController.execute()"
      to: "window boardsmith:action-resolved event"
      via: "window.dispatchEvent gated by isDevBuild"
      pattern: "boardsmith:action-resolved"
    - from: "useActionController selection-step completion (line ~1505)"
      to: "window boardsmith:action-resolved event"
      via: "completedActionName captured before currentAction.value = null"
      pattern: "completedActionName"
---

<objective>
Emit a `boardsmith:action-resolved` CustomEvent (detail `{ action, success, seat, error? }`) on `window` inside the game iframe whenever an action resolves, so agents confirm outcomes without polling. Dispatch at all resolution paths and distinguish success from failure.

Purpose: DEV-03 — the observable action-outcome signal. RESEARCH correction: there is only ONE existing `actionCompletedTick` increment site (line 1505, selection-step path); `execute()` uses an `isExecuting` toggle, not the tick. So NEW dispatches must be added at execute()'s two internal send branches (~1005, ~1069) AND its catch branch AND alongside line 1505. At the selection-step path, `currentAction.value` is nulled at ~line 1492 before line 1505 — capture the name first.
Output: dispatches in `useActionController.ts`, `BoardsmithActionResolvedDetail` in `global.d.ts`, and `useActionController.devtools.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-dev-host-devtools-bridge/119-RESEARCH.md
@.planning/phases/119-dev-host-devtools-bridge/119-VALIDATION.md

<interfaces>
From src/ui/composables/useActionController.ts (VERIFIED via research):
- `execute()` (line ~968): two send branches — no-metadata path `const result = await sendAction(actionName, args)` (~1005) and metadata path `const result = await sendAction(actionName, finalArgs)` (~1069); each has a catch that sets lastError. In scope: `actionName` (param), `result.success`/`result.error`, `playerSeat?.value ?? 0`.
- `handleOnSelectFill()` (line ~1462-1508): `const player = playerSeat?.value ?? 0` at ~1468; inside `if (result.actionComplete)` the code sets `currentAction.value = null` at ~1492, then `actionCompletedTick.value++` at ~1505 in the `else` branch of `if (result.followUp)`.
- `const actionCompletedTick = ref(0)` at ~186.

To add to src/ui/global.d.ts:
```
interface BoardsmithActionResolvedDetail {
  action: string;
  success: boolean;
  seat: number;
  error?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dispatch boardsmith:action-resolved at all resolution paths (dev-gated) + type + tests</name>
  <files>src/ui/composables/useActionController.ts, src/ui/global.d.ts, src/ui/composables/useActionController.devtools.test.ts</files>
  <read_first>
    - src/ui/composables/useActionController.ts (lines ~149, ~186, ~960-1100, ~1462-1508) — the execute() send branches, the handleOnSelectFill completion block, the actionCompletedTick ref, and where playerSeat is in closure.
    - src/ui/composables/useActionController.test.ts (setup: createMockSendAction, createTestMetadata, availableActions/actionMetadata/isMyTurn refs) — reuse this harness in the new test file.
    - src/ui/composables/useActionController.picks.test.ts — pattern for driving the selection-step path (pickStep completion) in a unit test.
    - src/ui/global.d.ts — where to add the BoardsmithActionResolvedDetail interface (alongside existing global declarations).
    - .planning/phases/119-dev-host-devtools-bridge/119-RESEARCH.md (DEV-03 section, Code Examples) — exact dispatch shape and the completedActionName capture-before-null requirement.
  </read_first>
  <behavior>
    - Test 1: after a successful `execute('playCard', ...)`, a `boardsmith:action-resolved` event fired on window with detail `{ action: 'playCard', success: true, seat }` and no `error` key.
    - Test 2: when sendAction returns `{ success: false, error }` (or throws), the event fires with `{ success: false, error }` for the same action.
    - Test 3: on the selection-step completion path (pickStep returns `{ success: true, actionComplete: true }` with no followUp), the event fires with `action` equal to the action name that was active BEFORE completion (non-empty), `success: true`.
    - Test 4: dispatch is guarded so it only runs when `import.meta.env.DEV` is true (vitest sets DEV true by default — assert the event fires under test).
  </behavior>
  <action>Add `const isDevBuild = import.meta.env.DEV` near the top of the composable if not already present in scope (mirror the GameShell.vue:174 pattern; do NOT use `__BOARDSMITH_DEV__`). In `global.d.ts`, add the `BoardsmithActionResolvedDetail` interface. In `execute()`, after each `const result = await sendAction(...)` (both the ~1005 no-metadata branch and the ~1069 metadata branch), when `isDevBuild` is true, call `window.dispatchEvent(new CustomEvent<BoardsmithActionResolvedDetail>('boardsmith:action-resolved', { detail: { action: actionName, success: result.success, seat: playerSeat?.value ?? 0, ...(result.success ? {} : { error: result.error }) } }))`. In each corresponding catch branch, dispatch with `success: false` and `error` from the caught error's message. In `handleOnSelectFill()`, inside `if (result.actionComplete)`, add `const completedActionName = currentAction.value` BEFORE the existing `currentAction.value = null` at ~1492; then in the `else` branch immediately after `actionCompletedTick.value++` (~1505), when `isDevBuild` is true, dispatch the event with `action: completedActionName ?? ''`, `success: true`, `seat: player`. Write the new test file `useActionController.devtools.test.ts` covering the four behaviors above, spying on `window.dispatchEvent` (or listening for the event). Do NOT alter existing tick semantics or the isExecuting toggle.</action>
  <verify>
    <automated>npx vitest run src/ui/composables/useActionController.devtools.test.ts src/ui/composables/useActionController.test.ts src/ui/composables/useActionController.picks.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/composables/useActionController.devtools.test.ts` exits 0 with the four behavior tests passing (execute success, execute failure, selection-step completion, dev-gate active under test).
    - Detail objects use the exact event name `'boardsmith:action-resolved'` and shape `{ action, success, seat, error? }`; `error` is absent on success.
    - The selection-step test asserts `detail.action` is the pre-completion action name (non-empty, not `''`/`null`) — proves capture-before-null.
    - Existing `useActionController.test.ts` and `useActionController.picks.test.ts` still exit 0 (no regression to tick/isExecuting semantics).
    - `grep -n "__BOARDSMITH_DEV__" src/ui/composables/useActionController.ts` returns nothing — the guard uses `import.meta.env.DEV`.
    - `npx vue-tsc --noEmit` reports no new errors; `BoardsmithActionResolvedDetail` resolves in useActionController.ts.
  </acceptance_criteria>
  <done>boardsmith:action-resolved fires with correct detail on execute() success, execute() failure, and selection-step completion; dev-gated; typed; all three test files green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game iframe → any window listener | The CustomEvent is dispatched on the iframe's `window`; a same-origin outer page (DevHost) can listen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-02 | Information Disclosure | action name/seat/error in CustomEvent | mitigate | Dispatch gated by `import.meta.env.DEV`; dead-code-eliminated in production Vite builds. Detail carries only non-secret action name + seat ordinal + validation error text (no PII). |
| T-119-03 | Tampering | out-of-turn / invalid action | accept | Server-side validation rejects invalid actions; the event faithfully reports `success:false` + error — the event is a mirror of the server verdict, not an authorization surface. |
</threat_model>

<verification>
- `npx vitest run src/ui/composables/` green.
- No `__BOARDSMITH_DEV__` runtime reference introduced.
</verification>

<success_criteria>
DEV-03 satisfied: every action resolution (both execute paths and the selection-step path) fires a dev-only `boardsmith:action-resolved` CustomEvent that distinguishes success from failure, unit-proven across all three paths.
</success_criteria>

<output>
Create `.planning/phases/119-dev-host-devtools-bridge/119-02-SUMMARY.md` when done
</output>
