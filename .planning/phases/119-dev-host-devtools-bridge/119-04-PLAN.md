---
phase: 119-dev-host-devtools-bridge
plan: 04
type: execute
wave: 3
depends_on: [119-01, 119-02, 119-03]
files_modified:
  - .planning/phases/119-dev-host-devtools-bridge/119-AGENT-LOOP.md
autonomous: false

requirements: [DEV-04]

must_haves:
  truths:
    - "The DISCOVER→SELECT→DRIVE→CONFIRM agent loop is documented with a runnable browser-console harness"
    - "The loop is browser-proven in a custom-UI game (go-fish): success===true on a legal move and success===false on an illegal attempt"
    - "The loop is browser-proven in AutoUI (go-fish switched via the dev-chrome UI dropdown): success===true and success===false"
    - "No dev server is left running after the proof"
  artifacts:
    - path: ".planning/phases/119-dev-host-devtools-bridge/119-AGENT-LOOP.md"
      provides: "agent-loop harness + exact repro steps for custom UI and AutoUI, success + failure"
      contains: "boardsmith:action-resolved"
  key_links:
    - from: "window.__BOARDSMITH_DEVTOOLS (outer page)"
      to: "[data-bs-el-id] element in iframe.contentDocument"
      via: "getBoardInteractionState().validElements → querySelector"
      pattern: "data-bs-el-id"
    - from: "iframe.contentWindow"
      to: "agent confirm step"
      via: "addEventListener('boardsmith:action-resolved')"
      pattern: "boardsmith:action-resolved"
---

<objective>
Document the fastest agent UI loop (DISCOVER → SELECT → DRIVE → CONFIRM) and browser-prove it end-to-end against a running `boardsmith dev` host — in a custom-UI game AND an AutoUI game, each showing `success===true` on a legal move and `success===false` on an illegal attempt.

Purpose: DEV-04 is the acceptance gate for the whole phase. It proves DEV-01 (stable selectors), DEV-02 (synchronous devtools global), and DEV-03 (action-resolved signal) compose into a working, vision-free agent loop. No new source beyond the harness/notes doc.
Output: `119-AGENT-LOOP.md` (harness + repro steps) and a completed browser proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-dev-host-devtools-bridge/119-RESEARCH.md
@.planning/phases/119-dev-host-devtools-bridge/119-VALIDATION.md
@.planning/phases/119-dev-host-devtools-bridge/119-01-SUMMARY.md
@.planning/phases/119-dev-host-devtools-bridge/119-02-SUMMARY.md
@.planning/phases/119-dev-host-devtools-bridge/119-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the agent-loop harness + repro doc</name>
  <files>.planning/phases/119-dev-host-devtools-bridge/119-AGENT-LOOP.md</files>
  <read_first>
    - .planning/phases/119-dev-host-devtools-bridge/119-RESEARCH.md (DEV-04 section — Browser proof harness, iframe structure, click routing, and the success===false approach via direct execute() with invalid args since a click on a non-valid element silently no-ops per useSelectable.ts:48).
    - .planning/phases/119-dev-host-devtools-bridge/119-03-SUMMARY.md — the final __BOARDSMITH_DEVTOOLS read-method names and message shape as actually shipped.
    - .planning/phases/119-dev-host-devtools-bridge/119-02-SUMMARY.md — the final boardsmith:action-resolved detail shape as shipped.
    - CLAUDE.md (repo) — go-fish dev command, dev-host iframe/platform-mode structure, UI switcher, and the hard rule to kill any dev server started.
  </read_first>
  <action>Write 119-AGENT-LOOP.md documenting the four-step loop with a copy-pasteable browser-console harness (outer dev-host page): DISCOVER via `window.__BOARDSMITH_DEVTOOLS.getActionMetadata()` + `getBoardInteractionState().validElements`; SELECT via `iframe.contentDocument.querySelector('[data-bs-el-id="<id>"]')`; DRIVE via dispatching a `MouseEvent('click', { bubbles: true })` on the element; CONFIRM via `iframe.contentWindow.addEventListener('boardsmith:action-resolved', ...)`. Include the exact repro steps for custom UI (`cd ~/BoardSmithGames/go-fish && npx boardsmith dev`, open :5173, seat 0) and for AutoUI (switch the dev-chrome UI dropdown to "Auto UI"). Document the success===true path (click a valid element) and the success===false path (from the iframe JS context, call the action controller's execute() with invalid args / an out-of-turn action — a click on a non-valid element silently no-ops and fires nothing). End with a "kill the dev server" reminder. Keep the doc scoped as a Phase 119 harness/notes file; the polished public doc is Phase 122 DOC-03.</action>
  <verify>
    <automated>test -f .planning/phases/119-dev-host-devtools-bridge/119-AGENT-LOOP.md && grep -q "boardsmith:action-resolved" .planning/phases/119-dev-host-devtools-bridge/119-AGENT-LOOP.md && grep -q "data-bs-el-id" .planning/phases/119-dev-host-devtools-bridge/119-AGENT-LOOP.md && grep -q "__BOARDSMITH_DEVTOOLS" .planning/phases/119-dev-host-devtools-bridge/119-AGENT-LOOP.md</automated>
  </verify>
  <acceptance_criteria>
    - Doc exists and documents all four steps (DISCOVER, SELECT, DRIVE, CONFIRM) with a runnable console harness.
    - Doc includes distinct repro sections for custom UI (go-fish) and AutoUI (dev-chrome UI-switch), each covering success===true and success===false.
    - The success===false instructions use a direct execute()/out-of-turn approach (NOT a click on a non-valid element), matching research Pitfall 4.
    - Doc ends with an explicit "kill the dev server" step.
  </acceptance_criteria>
  <done>119-AGENT-LOOP.md is written with a runnable harness and exact repro steps for both UI paths and both outcomes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live browser proof of the agent loop (custom UI + AutoUI, success + failure)</name>
  <what-built>The full devtools bridge (DEV-01 stable selectors, DEV-02 `window.__BOARDSMITH_DEVTOOLS`, DEV-03 `boardsmith:action-resolved` event) and the 119-AGENT-LOOP.md harness. This checkpoint runs the live browser proof — it cannot run in jsdom (needs a real dev process, real cross-frame iframe, and the full action pipeline).</what-built>
  <how-to-verify>
    1. `cd ~/BoardSmithGames/go-fish && npx boardsmith dev` (serves on http://localhost:5173/).
    2. Open http://localhost:5173/ in Chrome (Claude Code Chrome extension). Wait to be seated as Seat 0.
    3. Open the OUTER page DevTools console (not inside the iframe). Run the harness from 119-AGENT-LOOP.md.
    4. Confirm `window.__BOARDSMITH_DEVTOOLS` is present; `getAvailableActions()` is non-empty and `getBoardInteractionState().validElements` lists ids on your turn (DISCOVER).
    5. `querySelector('[data-bs-el-id="<validId>"]')` in `iframe.contentDocument` returns the element (SELECT); dispatch a bubbling click (DRIVE); observe `boardsmith:action-resolved` with `success:true` (CONFIRM).
    6. Trigger an illegal action via a direct execute() with invalid args / out-of-turn from the iframe context; observe `boardsmith:action-resolved` with `success:false` and an `error`.
    7. Switch the dev-chrome UI dropdown to "Auto UI"; confirm `[data-bs-el-id]` selectors still resolve and repeat steps 4–6 (success AND failure) in AutoUI.
    8. Kill the dev server (Ctrl+C) — do not leave it running.
  </how-to-verify>
  <resume-signal>Type "approved" once success===true AND success===false are observed in BOTH custom UI and AutoUI, or describe what failed. If the live proof cannot be completed, record it as a `human_needed` verification item with these exact steps.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outer dev-host page → iframe.contentDocument/contentWindow | The agent (running in the outer page) reads the devtools global, queries the same-origin iframe DOM, drives clicks, and listens for the resolution event. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-07 | Information Disclosure | dev-only harness/global usage | accept | The loop only exercises the DEV-01/02/03 surfaces, all gated by `import.meta.env.DEV` and confined to localhost:5173. No new runtime surface; the doc is planning-only. |
| T-119-08 | Denial of Service | leftover dev server process | mitigate | The proof and doc both mandate killing the dev server (project hard rule); made an explicit checkpoint step and acceptance criterion. |
</threat_model>

<verification>
- 119-AGENT-LOOP.md exists with harness + both UI paths + both outcomes.
- Live browser proof: success===true AND success===false observed in custom UI AND AutoUI.
- Dev server killed after proof.
</verification>

<success_criteria>
DEV-04 satisfied: the change→drive→confirm agent loop is documented and browser-proven end-to-end in both a custom-UI game and an AutoUI game, each distinguishing a legal success from an illegal failure via the `boardsmith:action-resolved` signal — with no dev server left running.
</success_criteria>

<output>
Create `.planning/phases/119-dev-host-devtools-bridge/119-04-SUMMARY.md` when done
</output>
