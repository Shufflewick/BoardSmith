---
phase: 120-authoring-pit-of-success-guards
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eslint-plugin/rules/no-element-identity-comparison.ts
  - src/eslint-plugin/rules/no-element-identity-comparison.test.ts
  - src/eslint-plugin/index.ts
autonomous: true
requirements: [PIT-04]
must_haves:
  truths:
    - "no-element-identity-comparison flags === / !== between GameElement-looking operands and GameElement[].includes(element)"
    - "The rule auto-fixes `a === b` to `a.id === b.id` for simple binary expressions"
    - "The rule does NOT flag clearly-non-element comparisons (documented syntactic heuristic bounds)"
    - "The rule is registered in index.ts under boardsmith/recommended and reachable"
    - "A RuleTester harness exists and runs under vitest (first RuleTester coverage in the repo)"
  artifacts:
    - path: "src/eslint-plugin/rules/no-element-identity-comparison.ts"
      provides: "Syntactic ESLint rule with fixable: 'code'"
      contains: "fixable"
    - path: "src/eslint-plugin/rules/no-element-identity-comparison.test.ts"
      provides: "RuleTester valid/invalid fixtures + auto-fix output assertion"
    - path: "src/eslint-plugin/index.ts"
      provides: "Rule registered in plugin.rules + recommended config"
      contains: "no-element-identity-comparison"
  key_links:
    - from: "src/eslint-plugin/index.ts"
      to: "no-element-identity-comparison rule"
      via: "import + rules map + recommended config entry"
      pattern: "no-element-identity-comparison"
---

<objective>
PIT-04 (rule 1 of 2): Add the `boardsmith/no-element-identity-comparison` ESLint rule — a SYNTACTIC-ONLY rule (no type info; matches the existing no-network/no-timers/no-nondeterministic plain-AST style) that flags `===`/`!==` between GameElement-looking operands and `GameElement[].includes(element)`, auto-fixing `a === b` → `a.id === b.id`. This plan also stands up the first-ever RuleTester harness in the repo (Wave 0) since no ESLint rule tests exist anywhere.

Purpose: Element identity comparison is unreliable (proxies / clones); IDs are the correct key. This makes the footgun a lint error with an actionable message + auto-fix.
Output: The rule, its RuleTester test, and its registration in index.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-authoring-pit-of-success-guards/120-CONTEXT.md
@.planning/phases/120-authoring-pit-of-success-guards/120-RESEARCH.md
@.planning/phases/120-authoring-pit-of-success-guards/120-VALIDATION.md

<interfaces>
Rule style to mirror (verified): src/eslint-plugin/rules/no-nondeterministic.ts — `import type { Rule } from 'eslint'; const rule: Rule.RuleModule = { meta: { type: 'problem', docs, messages: {...}, schema: [] }, create(context) { return { BinaryExpression(node) {...}, CallExpression(node) {...} }; } }; export default rule;`. For the auto-fix add `meta.fixable: 'code'` and a `fix(fixer)` callback in `context.report()` (first fixable rule in this plugin — consult ESLint RuleFixer API directly; no local precedent).

Registration (verified): src/eslint-plugin/index.ts — import the rule (`.js` extension in the import specifier), add to `plugin.rules` map, and add `'boardsmith/no-element-identity-comparison': 'error'` to `plugin.configs.recommended.rules`, mirroring the existing `'no-network'` entries exactly.

RuleTester harness (Wave 0) — DEPENDENCY DECISION: use the plain `eslint` package's `RuleTester` (already a dependency) configured with `@typescript-eslint/parser` (already a devDependency: ^8.52.0) via `languageOptions: { parser }`. Do NOT add `@typescript-eslint/rule-tester` as a new dependency — CLAUDE.md hard rule "Don't add dependencies without discussing" governs, and RESEARCH.md (HIGH confidence) recommends the plain RuleTester to avoid a new dep. (Note: 120-VALIDATION.md/handoff prose mentions `@typescript-eslint/rule-tester`; that is superseded here to honor the no-new-dependency rule — plain RuleTester + the already-present TS parser is functionally sufficient for TS-syntax fixtures.) RuleTester's Mocha-style describe/it resolve to vitest globals (`globals: true` in vitest.config.ts). Test files under `src/**/*.test.ts` are already picked up by the vitest include glob — no config change needed.

Syntactic heuristic for "GameElement-looking" (documented, accepts false-positive/negative bounds per RESEARCH Pitfall #3): base the match on same-file evidence — (a) a variable/param whose declared TYPE annotation names GameElement or a subtype, or whose initializer is a known collection-returning call (`.all(...)`, `.first(...)`, `.firstN(...)`, `.last(...)`, `.lastN(...)`); (b) `class X extends GameElement`/known-subclass declarations visible in the same file cross-referenced to operand names. Player extends GameElement (src/engine/player/player.ts:79), so Player comparisons are in-scope. Document the heuristic + its accepted misses in the rule JSDoc/description.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stand up the RuleTester harness + no-element-identity-comparison RED fixtures</name>
  <files>src/eslint-plugin/rules/no-element-identity-comparison.test.ts</files>
  <read_first>
    - src/eslint-plugin/rules/no-nondeterministic.ts (rule + implicit test-shape precedent — note there are NO existing rule tests to copy)
    - vitest.config.ts (confirm globals:true and the src/**/*.test.ts include glob)
    - eslint.config.mjs (confirm parserOptions.project:false — syntactic only, no type checker)
    - .planning/phases/120-authoring-pit-of-success-guards/120-RESEARCH.md (Pitfall #3, #4; "Existing rule style to mirror")
  </read_first>
  <behavior>
    - invalid: `card1 === card2` (both typed via `: Card` where Card extends GameElement) → 1 error, message id for identity comparison; auto-fix output `card1.id === card2.id`.
    - invalid: `a !== b` (GameElement-typed) → 1 error; auto-fix `a.id !== b.id`.
    - invalid: `hand.includes(card)` where `hand` is `GameElement[]`/`Card[]` (or assigned from `.all(Card)`) → 1 error, no auto-fix asserted.
    - valid: `a.id === b.id` → no error.
    - valid: `count === 3` / `name === 'x'` (plainly non-element operands) → no error.
    - documented false-negative fixture (valid, with a comment): a cross-file element compared with no same-file type evidence → not flagged (heuristic limitation), asserted as valid to make the bound explicit.
  </behavior>
  <action>
    Create the RuleTester test file first (RED — the rule module does not exist yet, so the import fails / assertions fail). Instantiate `new RuleTester({ languageOptions: { parser: tseslintParser, parserOptions: { ecmaVersion: 'latest', sourceType: 'module' } } })` importing the parser from `@typescript-eslint/parser`. Import the rule from `./no-element-identity-comparison.js`. Encode the valid/invalid fixtures from the behavior list, including `output:` on the two auto-fixable invalid cases. This establishes the repo's first RuleTester convention — keep it minimal and copyable for plan 120-05.
  </action>
  <verify>
    <automated>npx vitest run src/eslint-plugin/rules/no-element-identity-comparison.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - RuleTester instantiates with the TS parser and runs under vitest (no new dependency added — grep package.json shows no `@typescript-eslint/rule-tester`).
    - Test file compiles and fails only because the rule is not yet implemented (RED).
  </acceptance_criteria>
  <done>First RuleTester harness in the repo exists; identity-comparison fixtures encoded and RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement no-element-identity-comparison + register in index.ts (GREEN)</name>
  <files>src/eslint-plugin/rules/no-element-identity-comparison.ts, src/eslint-plugin/index.ts</files>
  <read_first>
    - src/eslint-plugin/rules/no-nondeterministic.ts (Rule.RuleModule shape)
    - src/eslint-plugin/index.ts (registration pattern — plugin.rules + configs.recommended.rules)
    - .planning/v4.3-API-DESIGN.md (PIT-04 rule 1 — exact message + auto-fix intent)
  </read_first>
  <action>
    Implement the rule as a plain `Rule.RuleModule` with `meta.type:'problem'`, `meta.fixable:'code'`, a `messages` dict, and `schema:[]`. In `create(context)` return a visitor with a `BinaryExpression` handler (match `operator === '==='` or `'!=='` where both operands satisfy the same-file "GameElement-looking" heuristic described in the interfaces block) and a `CallExpression` handler (match `<arr>.includes(<el>)` where `<arr>` is GameElement[]-looking). For BinaryExpression matches, report with a `fix(fixer)` that rewrites each operand `X` to `X.id` (replace the full expression `left op right` with `left.id op right.id`); only auto-fix simple identifier/member operands, skip the fix when an operand is a complex expression. For includes(), report without a fix. Message text must steer to `element.id === other.id` and explain identity comparison is unreliable. Document the syntactic heuristic + accepted false-pos/neg bounds in the rule's JSDoc and `meta.docs.description`. Then register the rule in index.ts: add the import (with `.js` specifier), add it to `plugin.rules`, and add `'boardsmith/no-element-identity-comparison': 'error'` to `plugin.configs.recommended.rules`.
  </action>
  <verify>
    <automated>npx vitest run src/eslint-plugin</automated>
  </verify>
  <acceptance_criteria>
    - All fixtures from Task 1 pass, including the two auto-fix `output:` assertions.
    - index.ts registers the rule in both the rules map and recommended config; `plugin.configs.recommended.rules['boardsmith/no-element-identity-comparison'] === 'error'`.
    - `npm run lint` does not report the new rule file itself as violating repo lint (beyond the 3 known pre-existing no-shadow errors).
  </acceptance_criteria>
  <done>no-element-identity-comparison implemented, auto-fixing simple binaries, registered and reachable via boardsmith/recommended.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game author → lint tooling | Static analysis over author source; runs outside the engine, no runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-04a | Tampering | auto-fix correctness | mitigate | Fix restricted to simple identifier/member operands; complex operands skipped; output asserted by RuleTester fixtures. |
| T-120-04b | Spoofing | false positives from syntactic heuristic | accept | Documented bound (RESEARCH Pitfall #3); real games exercised in Phase 121, not here. |
| T-120-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed — plain `eslint` RuleTester + already-present `@typescript-eslint/parser`; no legitimacy gate required. |
</threat_model>

<verification>
- `npx vitest run src/eslint-plugin` green (harness + rule).
- `grep package.json` confirms no `@typescript-eslint/rule-tester` added.
- Rule reachable via `plugin.configs.recommended.rules`.
</verification>

<success_criteria>
no-element-identity-comparison catches identity/includes footguns with an actionable message and auto-fix, registered under boardsmith/recommended, tested by the repo's first RuleTester harness with no new dependency.
</success_criteria>

<output>
Create `.planning/phases/120-authoring-pit-of-success-guards/120-04-SUMMARY.md` when done.
</output>
