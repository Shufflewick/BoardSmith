---
phase: 123-determinism-flow-introspection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/flow/describe-flow-position.ts
  - src/engine/flow/describe-flow-position.test.ts
  - src/engine/flow/types.ts
  - src/engine/element/game.ts
autonomous: true
requirements: [FLOW-01]
must_haves:
  truths:
    - "A developer can call game.getFlowDebugInfo() and get a structured object with phase, step, path, and awaiting fields"
    - "The returned object has a describe() method producing a human-readable one-liner like 'phase *pegging* -> step *player-turn*, waiting on seat 2'"
    - "The step field falls back to the flow node type (e.g. 'action-step') when the node has no config.name, and never emits 'undefined' or throws"
    - "The phase field reads FlowState.currentPhase directly rather than re-deriving it from the path"
  artifacts:
    - path: "src/engine/flow/describe-flow-position.ts"
      provides: "Path-following flow-node walker + FlowDebugInfo describe() formatter"
      contains: "describeFlowPosition"
    - path: "src/engine/flow/types.ts"
      provides: "FlowDebugInfo interface"
      contains: "interface FlowDebugInfo"
    - path: "src/engine/element/game.ts"
      provides: "Game.getFlowDebugInfo() facade method"
      contains: "getFlowDebugInfo"
  key_links:
    - from: "src/engine/element/game.ts"
      to: "src/engine/flow/describe-flow-position.ts"
      via: "getFlowDebugInfo delegates to describeFlowPosition"
      pattern: "describeFlowPosition\\("
    - from: "src/engine/flow/describe-flow-position.ts"
      to: "FlowState.currentPhase"
      via: "phase field read directly from flow state"
      pattern: "currentPhase"
---

<objective>
Build the FLOW-01 flow-position debug primitive: a new path-following flow-node walker (`describeFlowPosition`), a `FlowDebugInfo` type, and a `Game.getFlowDebugInfo()` facade method that mirrors the existing `debugActionAvailability()` pattern. This is the single source of truth every later layer (TestGame passthrough, GameStuckError/assertActionAvailable messages, devtools bridge) reuses.

Purpose: Developers get accurate, human- and machine-readable insight into "where in the flow are we right now" without hand-parsing the raw FlowPosition path index stack.
Output: `describe-flow-position.ts` (+ test), `FlowDebugInfo` type in `flow/types.ts`, `Game.getFlowDebugInfo()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-determinism-flow-introspection/123-CONTEXT.md
@.planning/phases/123-determinism-flow-introspection/123-RESEARCH.md
@.planning/phases/123-determinism-flow-introspection/123-PATTERNS.md

<interfaces>
<!-- Extracted contracts the executor needs. Use directly — no codebase exploration required. -->

FlowPosition (src/engine/flow/types.ts:38) — `path: number[]` is an index stack into the FlowNode tree rooted at FlowDefinition.root. Also carries `iterations`, `frameData?`, `playerIndex?`, `variables`.

FlowState (src/engine/flow/types.ts:240) — carries `currentPhase?: string` (line ~254, already correctly tracked by the engine), `currentPlayer?`, `awaitingPlayers?`, `complete`.

PhaseConfig.name is REQUIRED (flow/types.ts:202-204). All other node configs (SequenceConfig, ActionStepConfig, etc.) have OPTIONAL `name?: string` via BaseFlowConfig — games set these inconsistently (verified: checkers names 'move-step', cribbage names 'play-or-go-step', but many nodes are unnamed).

walkFlowNodes (src/engine/flow/walk-flow-nodes.ts) — existing pre-order generator with a `switch (node.type)` dispatch + `const _exhaustive: never = node` exhaustiveness guard covering: sequence (config.steps[]), loop/repeat/each-player/for-each/phase (config.do), if (config.then/config.else), switch (config.cases/config.default), and leaves action-step/simultaneous-action-step/execute. REUSE this switch shape verbatim; do NOT invent a new node-type dispatch convention.

debugActionAvailability (src/engine/element/game.ts:1237) — the facade-method pattern to mirror: gather raw data, then format into a structured object via a private formatter. getFlowState() lives at game.ts:1754; this._flowDefinition holds the root FlowNode.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Path-following flow-position walker + FlowDebugInfo type</name>
  <files>src/engine/flow/describe-flow-position.ts, src/engine/flow/describe-flow-position.test.ts, src/engine/flow/types.ts</files>
  <read_first>
    - src/engine/flow/walk-flow-nodes.ts (the switch-per-node-type + exhaustiveness-guard shape to reuse verbatim)
    - src/engine/flow/types.ts (FlowNode variants, FlowPosition lines 38-49, FlowState lines 240-270, PhaseConfig.name required at 202-204, BaseFlowConfig.name optional)
    - src/engine/flow/engine.ts (confirm how FlowEngine assigns child indices per node type — especially switch case-key ordering — so the path-follow descent matches engine bookkeeping)
  </read_first>
  <behavior>
    - describeFlowPosition(root, position, flowState) returns { phase?, step?, path, awaiting, describe() }
    - Given a nested sequence -> phase -> action-step flow with a valid position path, phase = flowState.currentPhase, step = most-specific named node's config.name
    - Given a position whose deepest node has NO config.name, step falls back to that node's type string (e.g. 'action-step') — never 'undefined', never throws
    - describe() produces "phase *<phase>* -> step *<step>*, waiting on seat <n>" using flowState.currentPlayer; when no currentPlayer, uses awaitingPlayers; when neither, omits the "waiting on" clause gracefully
    - path field echoes position.path unchanged (machine consumers get exact position)
    - awaiting = { currentPlayer: flowState.currentPlayer, awaitingPlayers: flowState.awaitingPlayers }
    - A path that indexes beyond available children (invalid/partial) degrades gracefully to the deepest reachable node rather than throwing
  </behavior>
  <action>
    Add an exported `FlowDebugInfo` interface to src/engine/flow/types.ts following the exact doc-comment-per-field style of the sibling `FlowPosition` interface (lines 38-49). Fields: `phase?: string`, `step?: string`, `path: number[]`, `awaiting: { currentPlayer?: number; awaitingPlayers?: number[] }`, and a `describe(): string` method.

    Create src/engine/flow/describe-flow-position.ts exporting `describeFlowPosition(root: FlowNode, position: FlowPosition, flowState: FlowState): FlowDebugInfo`. Walk `root` following `position.path` as an index stack: at each depth, descend into the child selected by `path[depth]` using the SAME `switch (node.type)` structure and `const _exhaustive: never = node` exhaustiveness guard as walkFlowNodes (sequence -> config.steps[idx], loop/repeat/each-player/for-each/phase -> config.do, if -> then/else by index, switch -> stable-ordered cases/default matching engine.ts index assignment, leaves terminate). Collect the most-specific named node overall for `step`. Per RESEARCH.md Pitfall 1, set `phase` DIRECTLY from `flowState.currentPhase` — do NOT re-derive phase from the path. Per Pitfall 2, when a step node's `config.name` is absent, fall back to `node.type`. The describe() method formats the one-liner exactly as the requirement example: `phase *<phase>* -> step *<step>*, waiting on seat <n>`.

    Create the colocated test src/engine/flow/describe-flow-position.test.ts covering the behaviors above, including: named step, unnamed step (type fallback), nested loop/each-player, and an out-of-range path (graceful degradation).
  </action>
  <verify>
    <automated>npx vitest run src/engine/flow/describe-flow-position.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `src/engine/flow/describe-flow-position.ts` exports `describeFlowPosition` and its `switch (node.type)` block ends with a `never`-typed exhaustiveness guard
    - `grep -n "interface FlowDebugInfo" src/engine/flow/types.ts` returns a match
    - The test file asserts type-fallback (unnamed node yields `step` equal to node.type, never the literal string "undefined")
    - The test file asserts an out-of-range path does not throw
    - `npx vitest run src/engine/flow/describe-flow-position.test.ts --silent` passes
  </acceptance_criteria>
  <done>describeFlowPosition + FlowDebugInfo exist, tested for named/unnamed/nested/out-of-range cases, all green.</done>
</task>

<task type="auto">
  <name>Task 2: Game.getFlowDebugInfo() facade method</name>
  <files>src/engine/element/game.ts</files>
  <read_first>
    - src/engine/element/game.ts (debugActionAvailability at 1237-1255 — the facade pattern; getFlowState at 1754; this._flowDefinition storage; _formatActionDebugInfo at ~1458 for the "private formatter" convention)
    - src/engine/flow/describe-flow-position.ts (the helper created in Task 1)
    - src/engine/flow/types.ts (FlowDebugInfo type created in Task 1)
  </read_first>
  <action>
    Add a public `getFlowDebugInfo(): FlowDebugInfo` method to the Game class, placed as a peer adjacent to `getFlowState()` (line 1754) or `debugActionAvailability()` (line 1237) per CONTEXT.md ("peer of debugActionAvailability"). It reads the current flow state via `this.getFlowState()`, uses `this._flowDefinition` (the stored root FlowNode) and the FlowState's position, and delegates to `describeFlowPosition(root, position, flowState)` imported from `../flow/describe-flow-position.js`. Import `FlowDebugInfo` from `../flow/types.js`. When there is no active flow (no flow definition or no flow state), return a well-formed FlowDebugInfo whose describe() states there is no active flow — never throw and never return undefined. Add a JSDoc block with an `@example` mirroring debugActionAvailability's doc style, showing the "phase *pegging* -> step *player-turn*, waiting on seat 2" output. Do NOT re-implement any tree walking here — the walker lives entirely in describe-flow-position.ts.
  </action>
  <verify>
    <automated>npx vitest run src/engine/flow --silent && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "game.ts" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "getFlowDebugInfo" src/engine/element/game.ts` returns the method definition
    - The method body contains a call matching `describeFlowPosition\(`
    - The no-active-flow branch returns a FlowDebugInfo (never throws, never undefined)
    - `npx vitest run src/engine/flow --silent` passes and tsc reports no new errors in game.ts
  </acceptance_criteria>
  <done>game.getFlowDebugInfo() returns a FlowDebugInfo for active flows and degrades gracefully with no active flow; typechecks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| introspection API → caller | getFlowDebugInfo() returns flow structural data (phase/step/path/awaiting) to any caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-01 | Information Disclosure | getFlowDebugInfo() | accept | Flow position (phase/step/awaiting seat) is public structural game state, not per-seat hidden info; no card/hand contents exposed. No seat filtering needed. |
| T-123-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (internal TypeScript only; RESEARCH.md confirms no new dependencies). |
</threat_model>

<verification>
- `npx vitest run src/engine/flow --silent` green
- `describeFlowPosition` handles named, unnamed (type-fallback), nested, and out-of-range paths without throwing
- `game.getFlowDebugInfo().describe()` produces the requirement's example format
</verification>

<success_criteria>
- FLOW-01 core primitive exists: describeFlowPosition + FlowDebugInfo + Game.getFlowDebugInfo()
- Phase read from FlowState.currentPhase (not re-derived); step falls back to node type gracefully
- All new/touched flow tests green; no new tsc errors
</success_criteria>

<output>
Create `.planning/phases/123-determinism-flow-introspection/123-01-SUMMARY.md` when done
</output>
