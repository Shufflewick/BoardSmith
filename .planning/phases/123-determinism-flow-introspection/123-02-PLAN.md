---
phase: 123-determinism-flow-introspection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/element/space.ts
  - src/engine/element/space.test.ts
  - src/engine/element/element-collection.ts
  - src/engine/element/element-collection.test.ts
  - src/testing/simulate-action.ts
  - src/runtime/runner.ts
  - src/testing/play-until-complete.test.ts
autonomous: true
requirements: [FLOW-04]
must_haves:
  truths:
    - "Space.shuffleInternal() throws an actionable error when no seeded RNG is reachable, instead of silently falling back to Math.random"
    - "ElementCollection.shuffle() requires an explicit rng argument (no Math.random default)"
    - "playUntilComplete is deterministic by default: run twice with no options produces identical command history; options.seed varies it"
    - "An auto-generated game seed is retrievable after the fact (GameRunner captures the seed even when the game auto-generated it)"
  artifacts:
    - path: "src/engine/element/space.test.ts"
      provides: "Regression test for shuffle throw-on-missing-rng"
      contains: "shuffle"
    - path: "src/engine/element/element-collection.test.ts"
      provides: "Regression test for shuffle-requires-rng"
      contains: "shuffle"
    - path: "src/testing/play-until-complete.test.ts"
      provides: "Same-seed-twice determinism regression test"
      contains: "determinis"
  key_links:
    - from: "src/engine/element/space.ts"
      to: "this._ctx.random"
      via: "shuffleInternal reads seeded rng, throws if absent"
      pattern: "_ctx\\.random"
    - from: "src/testing/simulate-action.ts"
      to: "SeededRandom"
      via: "playUntilComplete constructs a seeded rng from a default seed"
      pattern: "SeededRandom|createSeededRandom"
    - from: "src/runtime/runner.ts"
      to: "game.getConstructorOptions"
      via: "GameRunner reads back the effective seed after construction"
      pattern: "getConstructorOptions"
---

<objective>
Close FLOW-04: eliminate every reachable `Math.random` fallback in the seeded-run path. Fix the two engine RNG fallback sites (`Space.shuffleInternal` throws when no seeded rng is reachable; `ElementCollection.shuffle` requires an explicit rng), make `playUntilComplete` deterministic by default via a seeded rng, and make an auto-generated game seed retrievable so any run is replayable. Add regression tests everywhere, since these primitives are used by every game and a silent break would corrupt fairness rather than throw.

Purpose: Seeded runs are fully deterministic end-to-end — the foundation Phase 125 (headless simulation) depends on.
Output: Two engine RNG fixes + tests, deterministic `playUntilComplete` + determinism regression test, seed retrievability fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-determinism-flow-introspection/123-CONTEXT.md
@.planning/phases/123-determinism-flow-introspection/123-RESEARCH.md
@.planning/phases/123-determinism-flow-introspection/123-PATTERNS.md

<interfaces>
<!-- Extracted contracts the executor needs. -->

FALLBACK SITES (must be fixed):
- src/engine/element/space.ts:279 — `shuffleInternal()` currently does `const random = this._ctx.random ?? Math.random`. Fisher-Yates loop over `this._t.children` follows.
- src/engine/element/element-collection.ts:211 — `shuffle(random: () => number = Math.random)`. Confirmed ZERO callers across BoardSmith + all ~/BoardSmithGames/* + MERC (clean break, safe to make required).
- src/testing/simulate-action.ts:297 — `const rng = options?.rng ?? Math.random`. `options.rng?: () => number` documented at lines 244-254 (keep as escape hatch). Used at line 363 to pick random legal moves.

SEED THREADING (already correct — reuse, do not rebuild):
- Game constructor seeds a mulberry32 rng into `_ctx.random` (game.ts:591-603); GameOptions.seed at game.ts:220-221.
- game.getConstructorOptions() (game.ts:2442) returns a Record including the effective `seed`.
- src/utils/random.ts exports `class SeededRandom` (line 45) and `function createSeededRandom(seed: string): () => number` (line 193).
- random-simulation.ts is the seed-and-record reference pattern: accepts optional seed, generates one when absent, RETURNS it so runs replay.

SEED RETRIEVABILITY GAP:
- runner.ts:91 sets `this.seed = options.gameOptions.seed` — this captures a PASSED seed but is undefined when the Game auto-generates its own seed. Fix: after `this.game` is constructed, read the effective seed back via `this.game.getConstructorOptions().seed` so an auto-generated run is replayable.

ACTIONABLE-THROW STYLE to mirror: game.ts restoreFlow() (1762-1776) throws with a specific contextual message + the concrete fix, e.g. `throw new Error('Flow position invalid: ... Valid path prefix: [...]')`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Engine RNG fallback fixes (space.ts + element-collection.ts) with regression tests</name>
  <files>src/engine/element/space.ts, src/engine/element/space.test.ts, src/engine/element/element-collection.ts, src/engine/element/element-collection.test.ts</files>
  <read_first>
    - src/engine/element/space.ts (shuffleInternal at ~277-283, the Fisher-Yates loop over this._t.children; how _ctx is populated)
    - src/engine/element/element-collection.ts (shuffle at 210-217, the Fisher-Yates loop)
    - src/engine/element/game.ts:1762-1776 (restoreFlow — the actionable-throw message style to mirror)
    - src/engine/element/game.ts:591-603 (constructor seeds _ctx.random — so a game-attached Space always has a seeded rng)
  </read_first>
  <behavior>
    - Space.shuffleInternal() on a Space whose _ctx.random is set (normal game-attached element) shuffles deterministically using _ctx.random (unchanged behavior)
    - Space.shuffleInternal() on a Space with no reachable _ctx.random throws an Error whose message names the concrete fix (attach the element to a Game via its constructor, or the element tree is disconnected)
    - ElementCollection.shuffle(rng) with an explicit rng shuffles using it (Fisher-Yates, unchanged)
    - ElementCollection.shuffle() called with NO argument is a TypeScript error at compile time and, if invoked untyped, throws rather than silently using Math.random
  </behavior>
  <action>
    In src/engine/element/space.ts, replace `const random = this._ctx.random ?? Math.random` (line 279) with a guard: when `this._ctx.random` is absent (null/undefined), throw a new Error whose message names the actionable fix, mirroring the restoreFlow() throw style in game.ts:1762-1776 (specific + tells the developer how to fix — attach the element to a Game so it inherits the seeded rng, or pass an explicit rng). When present, use `this._ctx.random`. Never reference Math.random.

    In src/engine/element/element-collection.ts, remove the `= Math.random` default from `shuffle(random: () => number = Math.random)` (line 211) entirely, making `random: () => number` a required parameter. This is a clean break (zero callers confirmed). Never reference Math.random.

    Create src/engine/element/space.test.ts (new file — none exists): assert that a game-attached Space shuffles deterministically with a fixed seed, and that a Space with no reachable seeded rng throws an Error whose message includes the actionable guidance. Create src/engine/element/element-collection.test.ts (new file — none exists): assert shuffle(rng) with a stub rng produces the expected deterministic order, and that calling shuffle without an rng is rejected (compile-time type error documented; runtime throw when called via an untyped path).
  </action>
  <verify>
    <automated>npx vitest run src/engine/element/space.test.ts src/engine/element/element-collection.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/engine/element/space.ts | grep -c 'Math.random'` returns 0
    - `grep -v '^#' src/engine/element/element-collection.ts | grep -c 'Math.random'` returns 0
    - `grep -n "= Math.random" src/engine/element/element-collection.ts` returns nothing (default removed)
    - space.test.ts asserts a throw with an actionable message when no seeded rng is reachable
    - element-collection.test.ts asserts deterministic order with a stub rng
    - `npx vitest run src/engine/element/space.test.ts src/engine/element/element-collection.test.ts --silent` passes
  </acceptance_criteria>
  <done>Both engine RNG fallback sites fixed and covered by new regression tests; no Math.random remains in either file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic playUntilComplete + seed retrievability + determinism regression test</name>
  <files>src/testing/simulate-action.ts, src/runtime/runner.ts, src/testing/play-until-complete.test.ts</files>
  <read_first>
    - src/testing/simulate-action.ts (playUntilComplete around 273-363: options doc 240-254, `const rng = options?.rng ?? Math.random` at 297, move-pick at 363)
    - src/testing/random-simulation.ts (seed-and-record reference: optional seed, generate-when-absent, return on results — lines 36-42, 519, 534-535)
    - src/utils/random.ts (SeededRandom class line 45, createSeededRandom line 193)
    - src/runtime/runner.ts (seed field 83-84, `this.seed = options.gameOptions.seed` line 91, game constructed just above)
    - src/engine/element/game.ts:2442 (getConstructorOptions returns the effective seed)
    - src/testing/play-until-complete.test.ts (existing file — add a new determinism case; do not rewrite existing cases)
  </read_first>
  <behavior>
    - playUntilComplete(testGame) with NO options runs deterministically: two fresh games played this way produce identical command history
    - playUntilComplete(testGame, { seed: 'x' }) and { seed: 'y' } produce different play-throughs; same seed twice produces identical command history
    - options.rng, when supplied, still overrides (escape hatch preserved)
    - GameRunner.seed is defined even when the game auto-generated its seed (retrievable for replay)
  </behavior>
  <action>
    In src/testing/simulate-action.ts, change the default rng derivation (line 297). Add an `options.seed?: string` to the PlayUntilCompleteOptions doc/type alongside the existing `options.rng`. When `options.rng` is supplied, use it (escape hatch). Otherwise construct a seeded rng from `options.seed ?? 'playUntilComplete-default'` via `createSeededRandom(seed)` (or `new SeededRandom(seed)`) imported from `../utils/random.js`. The default must be the fixed literal seed, NEVER Math.random. Mirror random-simulation.ts's seed-and-record intent (a fixed default seed makes the default run reproducible; options.seed varies it).

    In src/runtime/runner.ts, after `this.game` is constructed, set `this.seed` to the effective seed by reading back `this.game.getConstructorOptions().seed` when `options.gameOptions.seed` was not supplied — so an auto-generated seed is captured and replayable. Keep the passed seed when present.

    In src/testing/play-until-complete.test.ts, add a determinism regression test: play the same game to completion twice via playUntilComplete with no options, assert the two command histories are identical (deep-equal); and assert that two runs with different explicit seeds diverge. Name the test so it matches `-t "determinis"`.
  </action>
  <verify>
    <automated>npx vitest run src/testing/play-until-complete.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/testing/simulate-action.ts | grep -c 'Math.random'` returns 0 (no reachable Math.random default remains; escape-hatch rng only)
    - simulate-action.ts imports SeededRandom or createSeededRandom from ../utils/random.js
    - runner.ts references `getConstructorOptions` for seed read-back
    - play-until-complete.test.ts contains a test matching `determinis` that deep-equals two same-default-run command histories
    - `npx vitest run src/testing/play-until-complete.test.ts --silent` passes
  </acceptance_criteria>
  <done>playUntilComplete is deterministic by default with an optional seed; auto-generated seeds are retrievable; determinism regression test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RNG source → game state | The seeded rng determines shuffle/randomization outcomes for every game |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-02 | Tampering | Space.shuffleInternal / ElementCollection.shuffle | mitigate | Silent Math.random fallback corrupts fairness without failing; replaced with throw-on-missing-rng (space) and required-rng param (collection) so misuse fails loud, not silent. Regression tests in both new .test.ts files. |
| T-123-03 | Information Disclosure | mulberry32 seeded rng | accept | PRNG is for fairness/reproducibility, not cryptographic unpredictability; predictable-seed is a known accepted tradeoff already baked into Game.random (RESEARCH.md Security Domain). Out of scope for FLOW-04. |
| T-123-SC | Tampering | npm/pip/cargo installs | accept | No package installs (internal TypeScript only; RESEARCH.md confirms no new dependencies). |
</threat_model>

<verification>
- `npm test` full suite green (this plan touches Space/ElementCollection used by every game — full regression is mandatory, not optional)
- Sanity-run at least one shuffle-heavy game (go-fish or cribbage) still passes its suite before calling the plan done
- No `Math.random` remains in space.ts, element-collection.ts, or the reachable path of simulate-action.ts
</verification>

<success_criteria>
- FLOW-04 satisfied: no reachable Math.random fallback; playUntilComplete deterministic by default; auto seed retrievable
- Regression tests exist for both shuffle sites and for same-seed-twice determinism
- Full suite green
</success_criteria>

<output>
Create `.planning/phases/123-determinism-flow-introspection/123-02-SUMMARY.md` when done
</output>
