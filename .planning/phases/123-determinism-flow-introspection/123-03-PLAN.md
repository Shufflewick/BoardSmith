---
phase: 123-determinism-flow-introspection
plan: 03
type: execute
wave: 2
depends_on: ["123-01", "123-02"]
files_modified:
  - src/runtime/runner.ts
  - src/testing/test-game.ts
  - src/testing/test-game.test.ts
  - src/testing/simulate-action.ts
  - src/testing/assertions.ts
  - src/testing/assertions.test.ts
  - src/testing/debug.ts
  - src/testing/debug.test.ts
autonomous: true
requirements: [FLOW-01, FLOW-02, FLOW-03]
must_haves:
  truths:
    - "A developer can call testGame.getFlowDebugInfo() and get the same FlowDebugInfo the engine produces"
    - "A developer can call testGame.getPendingAction(seat) mid multi-step action and get a read-only snapshot (current step, completed selections, accumulated args) — never the live mutable object"
    - "A developer can query a seat's action space with disabled choices AND their disable reasons in one TestGame call, without calling getSelectionChoices per selection"
    - "GameStuckError and assertActionAvailable failure messages embed the readable flow position via getFlowDebugInfo().describe()"
    - "toDebugString(game) output includes the human-readable flow position (per FLOW-01 locked decision)"
  artifacts:
    - path: "src/testing/test-game.ts"
      provides: "getFlowDebugInfo(), getPendingAction(seat), disabled-choices helper"
      contains: "getPendingAction"
    - path: "src/runtime/runner.ts"
      provides: "GameRunner pending-action tracking + getFlowDebugInfo passthrough"
      contains: "getFlowDebugInfo"
  key_links:
    - from: "src/testing/test-game.ts"
      to: "src/runtime/runner.ts"
      via: "getFlowDebugInfo/getPendingAction delegate to runner"
      pattern: "this\\.runner\\.(getFlowDebugInfo|getPendingAction)"
    - from: "src/testing/simulate-action.ts"
      to: "getFlowDebugInfo().describe()"
      via: "GameStuckError message embeds readable flow position"
      pattern: "getFlowDebugInfo\\(\\)\\.describe\\(\\)"
    - from: "src/testing/assertions.ts"
      to: "getFlowDebugInfo().describe()"
      via: "assertActionAvailable message embeds readable flow position"
      pattern: "getFlowDebugInfo\\(\\)\\.describe\\(\\)"
---

<objective>
Expose the FLOW-01/02/03 introspection surface through the testing layer. Add `TestGame.getFlowDebugInfo()` (passthrough), `TestGame.getPendingAction(seat)` (new read-only snapshot — requires new GameRunner plumbing since GameRunner bypasses the session's PendingActionManager), and a one-call disabled-choices helper (FLOW-02, reusing the existing AnnotatedChoice.disabled machinery). Embed the readable flow position into GameStuckError and assertActionAvailable failure messages.

Purpose: Developers can inspect flow position, disabled choices with reasons, and mid-multi-step action state directly from TestGame without hand-parsing internal state.
Output: TestGame introspection methods + GameRunner pending-action tracking + enriched error messages, all tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-determinism-flow-introspection/123-CONTEXT.md
@.planning/phases/123-determinism-flow-introspection/123-RESEARCH.md
@.planning/phases/123-determinism-flow-introspection/123-PATTERNS.md
@.planning/phases/123-determinism-flow-introspection/123-01-SUMMARY.md
@.planning/phases/123-determinism-flow-introspection/123-02-SUMMARY.md

<interfaces>
<!-- Extracted contracts the executor needs. -->

FROM PLAN 01 (available):
- Game.getFlowDebugInfo(): FlowDebugInfo (src/engine/element/game.ts). FlowDebugInfo has phase?, step?, path, awaiting, describe().

TESTGAME PASSTHROUGH PATTERN (mirror exactly):
- TestGame wraps a GameRunner: `readonly runner: GameRunner<G>` and `readonly game: G` (= runner.game). (test-game.ts:66-72)
- Existing thin passthroughs: getFlowState() delegates to this.runner.getFlowState() (runner.ts:256 delegates to this.game.getFlowState()); getPlayerView(seat) at test-game.ts:294; getPlayer(seat) available.
- doAction() (test-game.ts:203-209) delegates to this.runner.performAction(...).

PENDING-ACTION GAP (FLOW-03 — needs NEW plumbing, NOT a one-line passthrough):
- GameRunner.performAction (runner.ts:128-195) calls this.game.continueFlow(actionName, args, playerIndex) DIRECTLY — it does NOT go through PendingActionManager. GameRunner has NO pending-action instance today.
- PendingActionManager is session-layer only (game-session.ts:1885 getPendingAction delegates to it). TestGame has no GameSession.
- ActionExecutor exposes createPendingActionState(actionName, playerPosition) and isPendingActionComplete (used in action.test.ts:1684+). This is the session-free path GameRunner can use to track multi-step action state directly.
- PendingActionState type: src/engine/action/types.ts:137 (current step, completed selections/RepeatingSelectionState, accumulated args).

DISABLED CHOICES (FLOW-02 — reuse, do NOT rebuild):
- Game.getSelectionChoices() (game.ts:1015) returns AnnotatedChoice[] with `disabled: string | false` per choice. Game.getActionSpace(seat) (game.ts:1048) returns the full legal action set for a seat (v4.3 INTRO-01).
- pick-handler.ts already forwards disabled correctly (lines 239-241) and pick-handler.test.ts already covers disabled threading (describe block at line 94) — VERIFY only, do not modify pick-handler.

ERROR EMBED SITES:
- GameStuckError (simulate-action.ts:193-219) already carries a flowState field; throw sites ~343/388/399/424 — enrich the message string only.
- assertActionAvailable (assertions.ts:206-238) already embeds debugActionAvailability output — extend the two thrown messages with one more \n-joined line.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GameRunner pending-action tracking + TestGame.getPendingAction(seat) read-only snapshot (FLOW-03)</name>
  <files>src/runtime/runner.ts, src/testing/test-game.ts, src/testing/test-game.test.ts</files>
  <read_first>
    - src/runtime/runner.ts (performAction 128-195, especially the continueFlow call at 171; class fields near 83)
    - src/engine/action/action.ts (createPendingActionState / isPendingActionComplete signatures and usage)
    - src/engine/action/types.ts:137 (PendingActionState / RepeatingSelectionState shape)
    - src/session/pending-action-manager.ts:235 (getPendingAction — the read-only snapshot shape to mirror)
    - src/testing/test-game.ts (passthrough shape at 128, 294; doAction 203-209)
    - src/engine/action/action.test.ts:1684+ (how createPendingActionState is exercised)
  </read_first>
  <behavior>
    - After starting a multi-step (repeating-selection) action via TestGame but before completing it, testGame.getPendingAction(seat) returns a snapshot with the current step index, completed selections, and accumulated args
    - The returned snapshot is a COPY: mutating it does not affect subsequent getPendingAction() results or game state
    - When no action is pending for the seat, getPendingAction(seat) returns undefined
    - An out-of-range seat returns undefined (does not throw), matching getActionSpace/getActionSchema bounds behavior
  </behavior>
  <action>
    In src/runtime/runner.ts, add lightweight pending-action tracking so multi-step actions performed through the runner have inspectable state. Use ActionExecutor.createPendingActionState / isPendingActionComplete directly (the session-free path) — do NOT depend on session-layer PendingActionManager (GameRunner has no GameSession). Track the current PendingActionState per player position as multi-step actions progress through performAction; clear it when the action completes. Add `getPendingAction(playerPosition: number): PendingActionState | undefined` returning a read-only snapshot — a spread/deep copy, never the live mutable object (RESEARCH.md Anti-Pattern). Also add `getFlowDebugInfo()` passthrough to `this.game.getFlowDebugInfo()` mirroring the getFlowState() passthrough at runner.ts:256 (needed here so both TestGame passthroughs delegate to runner consistently).

    In src/testing/test-game.ts, add `getPendingAction(seat: number): PendingActionState | undefined` delegating to `this.runner.getPendingAction(seat)` and `getFlowDebugInfo(): FlowDebugInfo` delegating to `this.runner.getFlowDebugInfo()`, both following the one-line passthrough shape at test-game.ts:128. Import PendingActionState from the engine action types and FlowDebugInfo from the flow types via their public import paths.

    In src/testing/test-game.test.ts, add cases (matching `-t "getPendingAction"`): mid multi-step action snapshot correctness; snapshot immutability (mutate the returned copy, re-fetch, assert unchanged); undefined when nothing pending; undefined for out-of-range seat. Add a `getFlowDebugInfo` passthrough case asserting it equals game.getFlowDebugInfo().
  </action>
  <verify>
    <automated>npx vitest run src/testing/test-game.test.ts -t "getPendingAction" --silent && npx vitest run src/testing/test-game.test.ts -t "getFlowDebugInfo" --silent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "getPendingAction" src/runtime/runner.ts` and `src/testing/test-game.ts` both return matches
    - `grep -n "getFlowDebugInfo" src/runtime/runner.ts` returns a passthrough to this.game.getFlowDebugInfo()
    - runner.ts uses createPendingActionState (not session PendingActionManager)
    - test-game.test.ts proves the returned snapshot is a copy (mutation does not leak back)
    - test-game.test.ts proves undefined for out-of-range seat (no throw)
    - both `-t` vitest runs pass
  </acceptance_criteria>
  <done>TestGame.getPendingAction(seat) returns an immutable snapshot backed by new GameRunner tracking; getFlowDebugInfo passthrough works; tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Disabled-choices introspection helper on TestGame (FLOW-02)</name>
  <files>src/testing/test-game.ts, src/testing/test-game.test.ts</files>
  <read_first>
    - src/engine/element/game.ts:1015 (getSelectionChoices — returns AnnotatedChoice[] with disabled), :1048 (getActionSpace — full legal action set for a seat)
    - src/session/pick-handler.ts:239-241 (existing disabled forwarding — REFERENCE only, do not modify)
    - src/session/pick-handler.test.ts:94 (existing disabled-threading coverage — confirm the gameplay path still rejects disabled submissions; do not duplicate)
    - src/testing/test-game.ts (getActionSpace passthrough if present; the passthrough shape)
  </read_first>
  <behavior>
    - A one-call TestGame method returns, for a seat, each available action's selections WITH each choice's disabled reason included (disabled choices are present in the introspection result, not filtered out)
    - Disabled choices carry their reason string; enabled choices carry disabled=false (or equivalent)
    - The gameplay pick path is unaffected — a disabled choice still cannot be submitted (verified via existing pick-handler.test.ts, not re-implemented)
  </behavior>
  <action>
    In src/testing/test-game.ts, add a disabled-choices introspection helper (name at Claude's discretion following the v4.3 getActionSpace family conventions, e.g. `getActionSpaceWithChoices(seat)` or `getSelectionChoices(seat, actionName)` passthrough) that surfaces the full action space for a seat with disabled choices AND their reasons included. Build it on the EXISTING engine machinery — delegate to Game.getActionSpace(seat) / Game.getSelectionChoices() which already compute AnnotatedChoice.disabled (v2.8). Do NOT build a parallel disabled-choice evaluator and do NOT modify pick-handler.ts (its gameplay filtering is correct and must stay). Return a structure that includes disabled choices with their reason strings so a caller sees why each is disabled in one call.

    In src/testing/test-game.test.ts, add cases (matching `-t "disabled"`): a seat with an action whose choices include a disabled one asserts the disabled choice appears in the introspection result with its reason string, alongside enabled choices. Confirm (comment referencing pick-handler.test.ts) the gameplay path still rejects a disabled submission — do not re-test the gameplay rejection here.
  </action>
  <verify>
    <automated>npx vitest run src/testing/test-game.test.ts -t "disabled" --silent && npx vitest run src/session/pick-handler.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - The new helper delegates to getActionSpace/getSelectionChoices (grep shows the call; no new disabled evaluator introduced)
    - `git diff --stat src/session/pick-handler.ts` shows NO changes (verify-only per RESEARCH.md)
    - test-game.test.ts asserts a disabled choice is PRESENT in the introspection result with its reason string
    - both vitest runs pass (introspection surface + gameplay rejection still green)
  </acceptance_criteria>
  <done>TestGame surfaces disabled choices with reasons in one call, reusing existing machinery; gameplay filtering untouched and still green.</done>
</task>

<task type="auto">
  <name>Task 3: Embed readable flow position in GameStuckError, assertActionAvailable, and toDebugString (FLOW-01 integration)</name>
  <files>src/testing/simulate-action.ts, src/testing/assertions.ts, src/testing/assertions.test.ts, src/testing/debug.ts, src/testing/debug.test.ts</files>
  <read_first>
    - src/testing/simulate-action.ts:193-219 (GameStuckError class) and throw sites ~343/388/399/424
    - src/testing/assertions.ts:206-238 (assertActionAvailable — existing debugActionAvailability embed pattern to extend)
    - src/testing/test-game.ts (getFlowDebugInfo added in this plan's Task 1 — use it as the source of the describe() string)
    - src/testing/debug.ts:51 (toDebugString(game, options) — currently prints players/tree, missing flow position; add the flow-position line here per the FLOW-01 locked decision)
    - src/testing/debug.test.ts (existing toDebugString tests — add a flow-position assertion; do not rewrite existing cases)
  </read_first>
  <behavior>
    - When a game gets stuck, the GameStuckError message includes the readable flow position (getFlowDebugInfo().describe() output) in addition to the existing flowState field
    - When assertActionAvailable fails, the thrown error message includes the readable flow position line alongside the existing "Available actions"/"Why" sections
    - toDebugString(game) output contains the human-readable flow position (phase/step/waiting-on-seat) sourced from game.getFlowDebugInfo().describe()
  </behavior>
  <action>
    In src/testing/simulate-action.ts, at each `throw new GameStuckError(...)` site (~343/388/399/424), enrich the message string argument to include the readable flow position obtained from the testGame/game's getFlowDebugInfo().describe(). Do NOT change the GameStuckError class shape — only the message text passed in. Reuse the flowState already in scope at each site (source the describe() from the game/testGame in scope).

    In src/testing/assertions.ts, extend the two thrown-error messages in assertActionAvailable (206-238) with one additional \n-joined line embedding testGame.getFlowDebugInfo().describe() (or testGame.game.getFlowDebugInfo().describe()), following the existing string-concatenation style already used for the `Why:`/`Available actions:` sections.

    In src/testing/assertions.test.ts, add/extend a case (matching `-t "assertActionAvailable"`) asserting the thrown message contains the flow-position describe() substring (e.g. matches /phase .* step /).

    In src/testing/debug.ts, integrate the flow position into toDebugString(game, options) (line 51) per the FLOW-01 locked CONTEXT decision ("integrated into toDebugString() — one source of truth all layers reuse"). Add a flow-position line to the output sourced from `game.getFlowDebugInfo().describe()`, placed alongside the existing players/tree sections; degrade gracefully when there is no active flow (the describe() no-active-flow branch from Plan 01 handles this). Reuse getFlowDebugInfo() — do NOT recompute flow position here. In src/testing/debug.test.ts, add an assertion that toDebugString output contains the flow-position text.
  </action>
  <verify>
    <automated>npx vitest run src/testing/assertions.test.ts -t "assertActionAvailable" --silent && npx vitest run src/testing/debug.test.ts --silent && npx vitest run src/testing/simulate-action.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getFlowDebugInfo().describe()" src/testing/simulate-action.ts` returns at least 1
    - `grep -c "getFlowDebugInfo().describe()" src/testing/assertions.ts` returns at least 1
    - `grep -c "getFlowDebugInfo" src/testing/debug.ts` returns at least 1 (toDebugString integration)
    - debug.test.ts asserts toDebugString output contains the flow-position text
    - GameStuckError class definition is unchanged (only message strings enriched)
    - assertions.test.ts asserts the failure message contains the flow-position describe() text
    - both vitest runs pass
  </acceptance_criteria>
  <done>GameStuckError, assertActionAvailable, and toDebugString all surface the readable flow position; class shapes unchanged; tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| introspection API → caller (seat param) | getPendingAction(seat) / disabled-choices helper take a seat and could expose another seat's hidden info |
| snapshot → caller | getPendingAction returns internal pending-action state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-04 | Information Disclosure | TestGame.getPendingAction / disabled-choices helper | accept | TestGame is a test-only harness (never in a production seat-isolated context); it intentionally has full-game visibility for authoring tests. Per-seat leak protection is Phase 124's concern (VIS utilities). No production surface here. |
| T-123-05 | Tampering | getPendingAction snapshot | mitigate | Return a spread/deep copy, never the live PendingActionState — caller mutation cannot corrupt session/flow state. Enforced by an immutability test case in Task 1. |
| T-123-06 | Elevation of Privilege | disabled-choices introspection vs gameplay path | mitigate | Introspection surfaces disabled choices for display only; gameplay pick path (pick-handler.ts) stays filtered so a disabled choice still cannot be SUBMITTED. Verified by existing pick-handler.test.ts (not modified). |
| T-123-SC | Tampering | npm/pip/cargo installs | accept | No package installs (internal TypeScript only). |
</threat_model>

<verification>
- `npm test` full suite green after this plan's wave
- getPendingAction returns an immutable snapshot; disabled choices surfaced with reasons; error messages carry flow position
- pick-handler.ts unchanged (git diff clean)
</verification>

<success_criteria>
- FLOW-01 integration, FLOW-02, FLOW-03 all satisfied through the testing layer
- getPendingAction snapshot is immutable; disabled-choices helper reuses existing machinery; gameplay filtering preserved
- All new/touched testing tests green
</success_criteria>

<output>
Create `.planning/phases/123-determinism-flow-introspection/123-03-SUMMARY.md` when done
</output>
