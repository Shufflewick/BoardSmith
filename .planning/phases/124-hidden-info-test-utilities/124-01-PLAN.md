---
phase: 124-hidden-info-test-utilities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/testing/visibility.ts
  - src/testing/visibility.test.ts
  - src/testing/test-game.ts
  - src/testing/assertions.ts
  - src/testing/assertions.test.ts
  - src/testing/index.ts
autonomous: true
requirements: [VIS-01]

must_haves:
  truths:
    - "A developer can call testGame.isElementVisible(element, seat) and get the same result the wire serializer uses"
    - "A developer can call testGame.getVisibleElements(seat) and receive a live, queryable ElementCollection filtered to that seat"
    - "assertHidden/assertVisible throw with a message naming the element, the seat, and the leaked attribute keys"
    - "count-only zones report as NOT visible (identity hidden), matching toJSONForPlayer's own branch"
  artifacts:
    - path: "src/testing/visibility.ts"
      provides: "isElementVisible + getVisibleElements standalone functions delegating to element.isVisibleTo"
      exports: ["isElementVisible", "getVisibleElements"]
    - path: "src/testing/visibility.test.ts"
      provides: "owner/hidden/count-only/all coverage for VIS-01 against a fixture game"
    - path: "src/testing/assertions.ts"
      provides: "assertHidden + assertVisible with rich failure messages"
      contains: "assertHidden"
  key_links:
    - from: "src/testing/visibility.ts"
      to: "GameElement.isVisibleTo"
      via: "direct delegation (no re-derivation of visibility rules)"
      pattern: "isVisibleTo"
    - from: "src/testing/test-game.ts"
      to: "src/testing/visibility.ts"
      via: "TestGame.isElementVisible/getVisibleElements methods delegate to the standalone functions"
      pattern: "getVisibleElements"
    - from: "src/testing/index.ts"
      to: "src/testing/visibility.ts"
      via: "export barrel"
      pattern: "from './visibility.js'"
---

<objective>
Deliver VIS-01: per-seat element-visibility assertions that never drift from the wire serializer.

Add a testing-layer visibility surface built as a thin wrapper over the engine's single
visibility primitive `GameElement.isVisibleTo(seat)` — the exact function `toJSONForPlayer`
calls internally. No parallel visibility evaluator is created (that would be a drift risk by
construction and violate the CONTEXT.md "same serialization path" locked decision).

Purpose: Developers assert hidden info stays hidden without hand-parsing ElementJSON.
Output: `src/testing/visibility.ts` (new), TestGame methods, `assertHidden`/`assertVisible`
in `assertions.ts`, barrel exports, and colocated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-hidden-info-test-utilities/124-CONTEXT.md
@.planning/phases/124-hidden-info-test-utilities/124-RESEARCH.md
@.planning/phases/124-hidden-info-test-utilities/124-PATTERNS.md

<interfaces>
<!-- Engine primitive this plan wraps — call it, never re-derive. -->
From src/engine/element/game-element.ts:696
  isVisibleTo(player: Player | number): boolean
    // computes getEffectiveVisibility() + getEffectiveOwner()?.seat, then canPlayerSee(...)
    // count-only is treated identically to hidden (returns false unless seat in addPlayers)

From src/engine/element/game.ts:2682
  // toJSONForPlayer's own branch: visibility.mode === 'count-only' && !element.isVisibleTo(visibilityPosition)
  // confirms isVisibleTo alone (no extra count-only special-casing) matches wire behavior

From src/testing/test-game.ts (existing method style to mirror)
  getPlayerView(playerSeat: number): PlayerStateView   // thin passthrough w/ "what NOT to do" JSDoc

From src/testing/assertions.ts:206-241 (rich-failure-message convention)
  assertActionAvailable(testGame, playerSeat, actionName): void  // embeds WHY + concrete evidence
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: visibility.ts + TestGame visibility methods (VIS-01 core)</name>
  <files>src/testing/visibility.ts, src/testing/visibility.test.ts, src/testing/test-game.ts</files>
  <read_first>
    - src/engine/element/game-element.ts:663-739 (isVisibleTo, getEffectiveVisibility, getEffectiveOwner — the primitive to delegate to)
    - src/engine/element/game.ts:2671-2700 (toJSONForPlayer's visibility branches incl. line 2682 count-only equivalence — proves no extra special-casing needed)
    - src/testing/test-game.ts:9-19 (barrel import convention) and :329-362 (getPlayerView JSDoc + passthrough style to mirror)
    - src/testing/test-game.test.ts (existing fixture-game authoring pattern — the model for the VIS-01 fixture; note the maxIterations loop gotcha noted in 123-03-SUMMARY)
    - src/engine/index.ts (confirm Game, GameElement, ElementCollection are exported from the barrel)
  </read_first>
  <behavior>
    - isElementVisible(element, seat) returns element.isVisibleTo(seat) — true for a card the seat owns, false for an opponent's owner-hidden card.
    - getVisibleElements(game, seat) returns an ElementCollection containing exactly the elements where isVisibleTo(seat) is true; result is queryable with the existing collection API (e.g. .length, further .filter).
    - A count-only zone's identity children are NOT in getVisibleElements(seat) for a non-owner seat (count-only == hidden for identity).
    - Spectator (seat 0) is supported: getVisibleElements(game, 0) returns only globally-visible elements.
    - TestGame.isElementVisible(element, seat) and TestGame.getVisibleElements(seat) delegate to the standalone functions (getVisibleElements passes this.game).
  </behavior>
  <action>
    Create src/testing/visibility.ts exporting two standalone functions: `isElementVisible(element: GameElement, seat: number): boolean` returning `element.isVisibleTo(seat)`, and `getVisibleElements(game: Game, seat: number): ElementCollection<GameElement>` returning `game.all(GameElement).filter((e) => e.isVisibleTo(seat))`. Import Game, GameElement, and the ElementCollection type from '../engine/index.js' (barrel, .js extension, NodeNext ESM). Add a JSDoc block on each following the getPlayerView "what NOT to do" convention (warn against hand-parsing ElementJSON — that is the whole point of VIS-01). Do NOT inspect ElementJSON.attributes.__hidden as a proxy and do NOT re-implement owner/zone/count-only rules — delegate to isVisibleTo only.

    In src/testing/test-game.ts add two thin methods mirroring the getPlayerView passthrough style: `isElementVisible(element: GameElement, seat: number): boolean` delegating to the standalone function, and `getVisibleElements(seat: number): ElementCollection<GameElement>` delegating with `this.game`. Import from './visibility.js'. Add GameElement / ElementCollection to the existing '../engine/index.js' import line rather than a new import block. (VIS-01 requirement text specifies these live "on TestGame".)

    Create src/testing/visibility.test.ts with a minimal fixture game exposing four zones: an owner-visible element, an owner-hidden (opponent) element, a count-only zone, and an all-visible element. Follow the existing test-game.test.ts fixture pattern; if the fixture uses loop(), give it maxIterations and a body that actually runs (per the 123-03 fixture gotcha). Assert the behaviors listed above through BOTH the standalone functions and the TestGame methods.
  </action>
  <verify>
    <automated>npx vitest run src/testing/visibility.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - src/testing/visibility.ts exports isElementVisible and getVisibleElements; grep confirms the body calls `.isVisibleTo(` and does NOT contain `__hidden`.
    - TestGame.isElementVisible and TestGame.getVisibleElements exist and delegate (grep for `getVisibleElements` in test-game.ts).
    - visibility.test.ts covers owner-visible, owner-hidden, count-only-hidden, all-visible, and spectator(seat 0) cases; `npx vitest run src/testing/visibility.test.ts` is green.
    - `npx tsc --noEmit` shows no new errors from visibility.ts / test-game.ts.
  </acceptance_criteria>
  <done>VIS-01 live-element visibility API exists on TestGame and as standalone functions, verified against all visibility modes.</done>
</task>

<task type="auto">
  <name>Task 2: assertHidden/assertVisible + barrel exports</name>
  <files>src/testing/assertions.ts, src/testing/assertions.test.ts, src/testing/index.ts</files>
  <read_first>
    - src/testing/assertions.ts:1-12 (import convention) and :206-241 (assertActionAvailable rich-failure-message structure to match)
    - src/testing/index.ts:64-72 (existing "Assertion helpers" export block) and the "// Debug utilities" / "// Tutorial DSL" section-comment style
    - .planning/phases/124-hidden-info-test-utilities/124-CONTEXT.md (specifics section — the exact target failure-message wording)
  </read_first>
  <action>
    Add `assertHidden(element: GameElement, seat: number): void` and `assertVisible(element: GameElement, seat: number): void` to src/testing/assertions.ts. assertHidden throws when `element.isVisibleTo(seat)` is true, with a message naming `element.constructor.name`#`element.id`, the seat, and the leaked serialized attribute keys (`Object.keys(element.toJSON().attributes ?? {})`) — matching the CONTEXT.md specifics wording ("is visible to seat N (expected hidden): serialized attributes [...] present in seat N's view"). assertVisible throws when isVisibleTo is false, naming element + seat. Add GameElement to the existing '../engine/index.js' import line (do not create a new import block).

    In src/testing/index.ts add a new section-comment block `// Hidden-info visibility utilities (VIS-01)` exporting `isElementVisible`, `getVisibleElements` from './visibility.js', and add `assertHidden`, `assertVisible` to the existing "Assertion helpers" export block.

    In src/testing/assertions.test.ts add cases: assertHidden passes for a hidden element and throws (message contains the seat number and the attribute-keys list) for a visible element; assertVisible is the inverse.
  </action>
  <verify>
    <automated>npx vitest run src/testing/assertions.test.ts --reporter=dot && npm test</automated>
  </verify>
  <acceptance_criteria>
    - assertHidden/assertVisible exist in assertions.ts and are exported from index.ts.
    - isElementVisible/getVisibleElements are exported from index.ts under the VIS-01 section comment.
    - assertions.test.ts asserts the failure message contains both the seat and the leaked attribute keys.
    - Full suite green: `npm test` passes (catches export-barrel regressions in shared index.ts).
  </acceptance_criteria>
  <done>VIS-01 assertion helpers ship with actionable failure messages and are exposed through the testing barrel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine visibility rules → testing wrapper | The wrapper must expose EXACTLY what the wire exposes; any divergence gives a false "safe" reading |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01 | Information Disclosure | isElementVisible / getVisibleElements | mitigate | Delegate to element.isVisibleTo(seat) only; never re-derive owner/zone/count-only rules and never read `__hidden` as a proxy. visibility.test.ts covers owner/hidden/count-only/all/spectator so the wrapper cannot silently widen a seat's view. |
| T-124-02 | Tampering | getVisibleElements returns live ElementCollection | accept | Returning live engine elements is a locked CONTEXT.md decision (queryable via existing collection API). Test-layer only, trusted callers; JSDoc documents read-intent. No mutation path is introduced by this plan. |
| T-124-SC | Tampering | package installs | n/a | No packages installed this phase (RESEARCH confirms all deps pre-existing); no supply-chain checkpoint required. |
</threat_model>

<verification>
- `npx vitest run src/testing/visibility.test.ts src/testing/assertions.test.ts` green.
- `npm test` full suite green (shared index.ts barrel touched).
- `npx tsc --noEmit` no new errors.
- grep proves visibility.ts delegates to isVisibleTo and contains no `__hidden` string.
</verification>

<success_criteria>
VIS-01 satisfied: developers can assert per-seat visibility on TestGame and via standalone
functions/assertions, all backed by the single engine visibility primitive, with no ElementJSON
hand-parsing and no drift from the wire serializer.
</success_criteria>

<output>
Create `.planning/phases/124-hidden-info-test-utilities/124-01-SUMMARY.md` when done
</output>
