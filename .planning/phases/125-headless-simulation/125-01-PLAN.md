---
phase: 125-headless-simulation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/session/headless-session.ts
  - src/session/index.ts
  - src/session/headless-session.test.ts
  - src/session/testing/headless-harness.ts
  - src/runtime/runner.test.ts
  - src/session/testing/eachplayer-clone.test.ts
  - src/session/testing/parity-contract.test.ts
  - src/session/testing/undo-authoritative.test.ts
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "A developer can import { createHeadlessSession } from 'boardsmith/session'"
    - "The internal path src/session/testing/headless-harness.ts no longer exists (clean break, no shim)"
    - "Seeded headless runs are deterministic: same seed twice produces identical broadcasts"
    - "AI seats drive their own ops without an explicit send()"
  artifacts:
    - path: "src/session/headless-session.ts"
      provides: "createHeadlessSession public factory (moved from testing/headless-harness.ts)"
      contains: "export function createHeadlessSession"
    - path: "src/session/index.ts"
      provides: "public barrel export of createHeadlessSession"
      contains: "createHeadlessSession"
    - path: "src/session/headless-session.test.ts"
      provides: "public-path unit tests (determinism, AI seats, action ops)"
  key_links:
    - from: "src/session/index.ts"
      to: "./headless-session.js"
      via: "export re-export"
      pattern: "from './headless-session"
    - from: "src/session/headless-session.ts"
      to: "./snapshot-session-host.js, ./stateless-ops.js"
      via: "relative imports one level shallower than the old testing/ location"
      pattern: "from './(snapshot-session-host|stateless-ops)"
---

<objective>
Promote `createHeadlessSession` from the test-internal path `src/session/testing/headless-harness.ts` to the public session surface (`boardsmith/session`), with a clean break — no re-export shim at the old path. Implements SIM-01.

Purpose: Developers (and agents) need a supported, in-process way to drive a game session headlessly (seed control, AI seats, action ops) without reaching into `src/session/testing/`. The code already exists and is proven; this is a file move + public export + documentation + a dedicated public-path test — not new logic.

Output: `src/session/headless-session.ts` (moved), a new `Headless Simulation` export block in `src/session/index.ts`, JSDoc worked example, `src/session/headless-session.test.ts`, and 4 migrated call sites. The old file is deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-headless-simulation/125-CONTEXT.md
@.planning/phases/125-headless-simulation/125-PATTERNS.md

<interfaces>
<!-- Full current content of the file being MOVED. Copy verbatim; only imports change. -->
<!-- Source: src/session/testing/headless-harness.ts (35 lines) -->

The moved file's imports change from `../snapshot-session-host.js` / `../stateless-ops.js`
to `./snapshot-session-host.js` / `./stateless-ops.js` (one level shallower).

createHeadlessSession signature (KEEP UNCHANGED per CONTEXT — Open Question 1 resolved: no rename):
```typescript
export function createHeadlessSession(
  def: GameDefinitionLike,
  gameOptions: { playerCount: number; seed?: string },
  aiSeats: Array<{ seat: number; level?: string }> = [],
): {
  host: SnapshotSessionHost;
  broadcasts: unknown[];
  start(): Promise<void>;
  send(seat: number, op: Op): Promise<...>;  // returns host.handleOp result
}
```

Existing export-block convention in src/session/index.ts (banner + grouped export, see lines 108-137):
```typescript
// ============================================
// Headless Simulation
// ============================================

export { createHeadlessSession } from './headless-session.js';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move headless-harness → headless-session, export publicly, migrate 4 call sites, delete old file</name>
  <files>src/session/headless-session.ts, src/session/index.ts, src/session/testing/headless-harness.ts, src/runtime/runner.test.ts, src/session/testing/eachplayer-clone.test.ts, src/session/testing/parity-contract.test.ts, src/session/testing/undo-authoritative.test.ts</files>
  <read_first>
    - src/session/testing/headless-harness.ts (file being moved — copy content verbatim, change only the two relative imports)
    - src/session/index.ts (barrel export conventions — the `// Core Classes` banner style at ~lines 108-137)
    - src/runtime/runner.test.ts (import at ~line 17)
    - src/session/testing/eachplayer-clone.test.ts, parity-contract.test.ts, undo-authoritative.test.ts (import at ~line 2 of each)
  </read_first>
  <action>
    Create `src/session/headless-session.ts` with the exact contents of `src/session/testing/headless-harness.ts`, changing only the two relative imports from `../snapshot-session-host.js`/`../stateless-ops.js` to `./snapshot-session-host.js`/`./stateless-ops.js`. Keep the function name `createHeadlessSession` unchanged (RESEARCH Open Question 1 resolved: no rename).

    Add a new `// ============ Headless Simulation ============` banner section to `src/session/index.ts` (mirroring the existing Core Classes banner style) that does `export { createHeadlessSession } from './headless-session.js';`.

    Update the 4 consuming import paths (per 125-PATTERNS.md "Import-path-only edits" table): `src/runtime/runner.test.ts` → `'../session/headless-session.js'`; each of `eachplayer-clone.test.ts` / `parity-contract.test.ts` / `undo-authoritative.test.ts` → `'../headless-session.js'`.

    Delete `src/session/testing/headless-harness.ts`. Do NOT add any re-export shim at the old path (clean break per CONTEXT + CLAUDE.md "No Backward Compatibility"). Leave the rest of `src/session/testing/` (fixtures, other test files) in place. Guards T-125-01.
  </action>
  <verify>
    <automated>test $(grep -rl "headless-harness" src/ | grep -v '^#' | wc -l | tr -d ' ') -eq 0 && npx vitest run src/runtime/runner.test.ts src/session/testing/eachplayer-clone.test.ts src/session/testing/parity-contract.test.ts src/session/testing/undo-authoritative.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "headless-harness" src/` returns zero files (old path fully gone, no shim).
    - `src/session/testing/headless-harness.ts` does not exist.
    - The 4 migrated test files pass under vitest.
    - `src/session/index.ts` contains `export { createHeadlessSession } from './headless-session.js'`.
  </acceptance_criteria>
  <done>createHeadlessSession lives at src/session/headless-session.ts, is exported from the session barrel, all 4 call sites import the new path, and the old file is deleted with zero remaining references.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add JSDoc worked example + public-path unit tests</name>
  <files>src/session/headless-session.ts, src/session/headless-session.test.ts</files>
  <read_first>
    - src/session/headless-session.ts (the moved file from Task 1 — add JSDoc @example)
    - src/session/index.ts (~lines 8-31: existing module-level @example fenced typescript block to model the new @example after)
    - src/session/testing/eachplayer-clone.test.ts (~lines 1-30: import + createHeadlessSession + start()/send() assertion pattern)
    - src/session/testing/fixtures/ (existing game fixture usable from the new test; import path from src/session/ is './testing/fixtures/...')
  </read_first>
  <behavior>
    - Determinism: creating two sessions with the SAME gameOptions.seed and sending the same op sequence yields identical send() results and identical broadcasts arrays (deep-equal).
    - AI seats: passing aiSeats: [{ seat: 2, level: 'easy' }] causes seat 2 to act (a broadcast/state change for seat 2 occurs) after start() without an explicit send(2, ...).
    - Action op round-trip: send(seat, { type: 'action', ... }) with a valid action returns success true.
  </behavior>
  <action>
    Add a JSDoc block with an `@example` fenced `typescript` block to `createHeadlessSession` in `src/session/headless-session.ts`, modeled on the module-level `@example` in `src/session/index.ts` — show `createHeadlessSession(gameDefinition, { playerCount: 2, seed: 'demo' })` → `await session.start()` → `await session.send(1, { type: 'action', ... })` and reading `session.broadcasts`.

    Create `src/session/headless-session.test.ts` importing `createHeadlessSession` from `./headless-session.js` and a fixture definition from `./testing/fixtures/...`. Write the three test cases from `<behavior>`. Use `structuredClone`-safe expectations (the harness already forces structuredClone; assertions compare the recorded `broadcasts` and `send()` return values). This test asserts on the PUBLIC path only — do not import anything from the (now deleted) testing/headless-harness path.
  </action>
  <verify>
    <automated>npx vitest run src/session/headless-session.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/session/headless-session.test.ts` exists and passes with ≥3 test cases (determinism, AI seats, action op round-trip).
    - `createHeadlessSession` in headless-session.ts carries a JSDoc `@example` fenced typescript block.
    - The test imports `createHeadlessSession` from `./headless-session.js` (public path), not from any testing/ harness path.
  </acceptance_criteria>
  <done>The public headless-session path has a worked JSDoc example and a dedicated passing unit test covering seeded determinism, AI seats, and action ops.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → createHeadlessSession op payload | In-process op crosses a structuredClone gate (mirrors the production iframe postMessage boundary); non-cloneable data must throw here, not silently pass |
| game seed → RNG | Seed determines all randomness; determinism is the contract downstream SIM-02 depends on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-01 | Tampering | old path src/session/testing/headless-harness.ts | mitigate | Delete old file; verify `grep -rl headless-harness src/` == 0 so no stale internal path re-exposes the harness (Task 1 automated gate) |
| T-125-02 | Information disclosure | createHeadlessSession broadcast boundary | accept | The existing `structuredClone(views)`/`structuredClone(op)` guards are preserved verbatim in the move — no behavior change; in-process only, no network surface (RESEARCH Security Domain) |
| T-125-SC | Tampering (supply chain) | npm installs | accept | This plan adds zero new packages; no install task exists, so slopcheck/legitimacy gate is not triggered (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `grep -rl "headless-harness" src/` returns nothing.
- `npx vitest run src/session/headless-session.test.ts src/runtime/runner.test.ts src/session/testing/eachplayer-clone.test.ts src/session/testing/parity-contract.test.ts src/session/testing/undo-authoritative.test.ts` all green.
- Full suite `npm test` green at wave merge.
</verification>

<success_criteria>
- `import { createHeadlessSession } from 'boardsmith/session'` resolves (barrel export present).
- No reference anywhere in `src/` to the old `headless-harness` path; old file deleted.
- Public-path unit test proves seeded determinism, AI seats, and action ops.
</success_criteria>

<output>
Create `.planning/phases/125-headless-simulation/125-01-SUMMARY.md` when done.
</output>
