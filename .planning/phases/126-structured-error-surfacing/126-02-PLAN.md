---
phase: 126-structured-error-surfacing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/session/game-session.ts
  - src/session/snapshot-session-host.ts
  - src/session/game-session.test.ts
  - src/session/snapshot-session-host.test.ts
  - src/session/ai-circuit-breaker.test.ts
autonomous: true
requirements: [ERR-03]

must_haves:
  truths:
    - "A storage save failure calls onPersistenceError and sets lastPersistenceError without throwing or crashing gameplay"
    - "A persistence failure during an AI turn does NOT increment #aiConsecutiveFailures (no false '[AI] Giving up' log)"
    - "SnapshotSessionHost.apply() persist failures surface through the same onPersistenceError shape"
  artifacts:
    - path: "src/session/game-session.ts"
      provides: "onPersistenceError injectable hook + lastPersistenceError getter + #persistSafely wrapping all 3 save sites"
      contains: "onPersistenceError"
    - path: "src/session/snapshot-session-host.ts"
      provides: "symmetric persistence-error hook wrapping apply()'s persist?.() call"
      contains: "onPersistenceError"
  key_links:
    - from: "src/session/game-session.ts #checkAITurn"
      to: "#persistSafely (persistence failure counter)"
      via: "save moved out of the AI try/catch scope so failures do not count against AI"
      pattern: "persistSafely"
---

<objective>
Close ERR-03: storage save failures become observable via a shared `onPersistenceError` hook + `lastPersistenceError` status, symmetric across both hosts, and stop being misclassified as AI failures.

Purpose: Three inconsistent storage paths exist today — `create()` is fire-and-forget `.catch(console.error)`, `#save()` has NO catch (throws propagate / unhandled rejection), and `#checkAITurn` calls `#save()` INSIDE the AI try/catch so a storage outage increments `#aiConsecutiveFailures` and logs a misleading "[AI] Giving up" (Pitfall 2, a confirmed bug-adjacent finding). `SnapshotSessionHost.apply()`'s `persist?.()` also has no catch. Per RESEARCH A1, the dev host wires no persist adapter today — so "both hosts" means the API surface must exist symmetrically on both classes.

Output: A never-rethrowing `#persistSafely` wrapper + injectable hook + status field on GameSession (all 3 sites) and the symmetric shape on SnapshotSessionHost.apply().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-structured-error-surfacing/126-RESEARCH.md
@.planning/phases/126-structured-error-surfacing/126-PATTERNS.md

<interfaces>
From src/session/game-session.ts:
  #aiConsecutiveFailures = 0;                 // circuit-breaker precedent to mirror (~line 226, logic ~2105-2120)
  create(...): storage.save(storedState).catch(err => console.error(...))  // ~703-707 fire-and-forget
  async #save(): Promise<void> { if (!this.#storage) return; ...; await this.#storage.save(...) }  // ~1971 no catch
  #checkAITurn: await this.#save()  // ~1294 path + AI-callback path; the AI path is inside a broad try/catch (Pitfall 2)

From src/session/snapshot-session-host.ts:
  SnapshotSessionAdapters.persist?: (state) => void | Promise<void>   // :21 (optional, unused by dev host today)
  private async apply(res, seat?): await this.adapters.persist?.({...})  // :197 no catch
  const MAX_AI_MOVES = 500;  // :9 — capped-constant naming convention

Sanctioned error shape (never .stack): error instanceof Error ? error.message : String(error)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GameSession persistence hook + #persistSafely across all 3 save sites (+ fix AI misclassification)</name>
  <files>src/session/game-session.ts, src/session/game-session.test.ts, src/session/ai-circuit-breaker.test.ts</files>
  <read_first>
    - src/session/game-session.ts:2105-2120 (#aiConsecutiveFailures circuit breaker — structural twin for #persistenceConsecutiveFailures)
    - src/session/game-session.ts:703-707 (create() fire-and-forget save), :1971-1978 (#save() no catch), :1294 + :1318 + AI-callback #save() call (~2092 region)
    - src/session/game-session.ts:75, 201, 277-289 (constructor + StorageAdapter injection — where onPersistenceError constructor option is added)
    - src/session/ai-circuit-breaker.test.ts (existing — extend for the Pitfall 2 regression guard, do NOT duplicate)
    - src/session/game-session.test.ts (existing — extend)
  </read_first>
  <behavior>
    - Test (write FIRST, per RESEARCH A2 — reproduce the bug before fixing): a rejecting StorageAdapter during an AI turn does NOT increment #aiConsecutiveFailures and produces no "[AI] Giving up" log; it DOES set lastPersistenceError and call onPersistenceError
    - Test: a rejecting save on a normal action → performAction resolves (does not throw / no unhandled rejection), lastPersistenceError populated, onPersistenceError called with (entry, consecutiveCount)
    - Test: a successful save resets #persistenceConsecutiveFailures to 0
    - Test: lastPersistenceError.message contains no stack trace / file path
    - Test: onPersistenceError that itself throws is swallowed (do not introduce a new silent-crash; fail-loud via console echo but never rethrow)
  </behavior>
  <action>Add an injectable `onPersistenceError?: (error: {message:string;timestamp:number}, consecutiveFailures:number) => void` constructor option, a public `lastPersistenceError` getter (backing field `{message,timestamp}|null`), and a private `#persistenceConsecutiveFailures` counter mirroring the `#aiConsecutiveFailures` shape. Add `async #persistSafely(fn: () => Promise<void>)` that awaits fn, resets the counter on success, and on failure increments the counter, sets `lastPersistenceError` (sanitized message, no stack), invokes the hook (guarded so a throwing hook cannot crash), keeps a `console.error` dev echo, and NEVER rethrows. Route create()'s save, `#save()`, and the AI-turn save through `#persistSafely`. For Pitfall 2: ensure the AI-turn save runs OUTSIDE (before) the AI-move try/catch scope so a persistence failure counts against persistence, not AI.</action>
  <verify>
    <automated>npx vitest run src/session/game-session.test.ts src/session/ai-circuit-breaker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "persistSafely" src/session/game-session.ts` >= 4 (definition + 3 call sites)
    - `grep -c "error.stack" src/session/game-session.ts` == 0
    - ai-circuit-breaker.test.ts contains a test proving a storage failure during an AI turn leaves #aiConsecutiveFailures unincremented
    - game-session.test.ts asserts lastPersistenceError set + onPersistenceError called + no throw
    - suite green
  </acceptance_criteria>
  <done>All three GameSession save sites are non-crashing and observable; the AI/persistence misclassification is fixed with a passing regression test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Symmetric persistence-error hook on SnapshotSessionHost.apply()</name>
  <files>src/session/snapshot-session-host.ts, src/session/snapshot-session-host.test.ts</files>
  <read_first>
    - src/session/snapshot-session-host.ts:179-198 (apply() — the persist?.() call with no catch)
    - src/session/snapshot-session-host.ts:9, 74 (MAX_AI_MOVES / MAX_DEMO_MOVES capped-constant + comment convention)
    - src/session/snapshot-session-host.ts:15-25 (SnapshotSessionAdapters — where onPersistenceError adapter is added alongside persist?)
    - src/session/game-session.ts (Task 1's #persistSafely shape — duplicate-but-identical private helper is the established style; there is no shared base class, mirroring serializeFlowDebugInfo-as-function convention)
    - src/session/snapshot-session-host.test.ts (existing — extend)
  </read_first>
  <behavior>
    - Test: a persist adapter that rejects → apply() does not throw, calls onPersistenceError, sets lastPersistenceError
    - Test: absent persist adapter (dev-host default) → apply() behaves exactly as today (no-op, no error)
    - Test: successful persist resets the consecutive-failure counter
  </behavior>
  <action>Add an optional `onPersistenceError` to `SnapshotSessionAdapters` (same signature as GameSession's) and a `lastPersistenceError` field. Wrap the `apply()` `persist?.()` call in a `#persistSafely`-style guard identical in shape to GameSession's (duplicate private helper — this codebase does not use shared base classes for the two hosts). Never rethrow; sanitized message only. Keep the dev host's zero-persist default working unchanged (the guard is a no-op when `persist` is undefined).</action>
  <verify>
    <automated>npx vitest run src/session/snapshot-session-host.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onPersistenceError" src/session/snapshot-session-host.ts` >= 1
    - `grep -c "error.stack" src/session/snapshot-session-host.ts` == 0
    - snapshot-session-host.test.ts asserts a rejecting persist adapter does not throw and fires onPersistenceError
    - test confirms undefined persist adapter path is unchanged
    - suite green
  </acceptance_criteria>
  <done>SnapshotSessionHost exposes the same persistence-error surface as GameSession; both hosts are symmetric.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage adapter → session | I/O failure crosses into gameplay; must not crash or misclassify |
| session error text → hook consumer | error message must stay sanitized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-03 | Denial of Service | #save() unhandled rejection crashing Node | mitigate | #persistSafely never rethrows; persistence stays async/non-blocking |
| T-126-04 | Repudiation | persistence failure logged as AI failure (Pitfall 2) | mitigate | Move AI-turn save out of AI try/catch; count against #persistenceConsecutiveFailures with regression test |
| T-126-05 | Information Disclosure | lastPersistenceError.message | mitigate | Sanitized message shape only; no error.stack (grep gate == 0) |
| T-126-06 | Denial of Service | throwing onPersistenceError hook | mitigate | Hook invocation guarded; a throwing hook cannot crash gameplay |
| T-126-SC | Tampering | package installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npx vitest run src/session/game-session.test.ts src/session/snapshot-session-host.test.ts src/session/ai-circuit-breaker.test.ts` green
- `grep -rn "error.stack" src/session/game-session.ts src/session/snapshot-session-host.ts` returns nothing
- `grep -rn "cli/dev-host" src/session/` returns nothing (the hook is injected; no dev-host import)
</verification>

<success_criteria>
ROADMAP Success Criterion 3: storage save failures are observable by the caller (event/hook + status), not a fire-and-forget `.catch(console.error)`.
</success_criteria>

<output>
Create `.planning/phases/126-structured-error-surfacing/126-02-SUMMARY.md` when done.
</output>
