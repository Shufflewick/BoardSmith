---
phase: 126-structured-error-surfacing
plan: 03
type: execute
wave: 2
depends_on: ["126-01"]
files_modified:
  - src/session/types.ts
  - src/session/pick-handler.ts
  - src/session/stateless-ops.ts
  - src/cli/dev-host/bridge.ts
  - src/session/pick-handler.test.ts
  - src/session/stateless-ops.test.ts
  - src/cli/dev-host/bridge.test.ts
autonomous: true
requirements: [ERR-01]

must_haves:
  truths:
    - "A throwing boardRefs() produces a structured warning and the choice is still returned/selectable"
    - "A throwing display() produces a structured warning, falls back to a default label, and echoes to console"
    - "getChoices() still hard-fails with its existing errorCode (regression unchanged)"
    - "Warnings ride OpResult and survive the dev-host bridge shapeResult allowlist"
  artifacts:
    - path: "src/session/types.ts"
      provides: "WarningEntry {code, message, source} type + warnings? on the pick response type"
      contains: "WarningEntry"
    - path: "src/session/pick-handler.ts"
      provides: "warnings pushed at boardRefs/display/boardRef catch sites, aggregated top-level array"
      contains: "warnings"
    - path: "src/cli/dev-host/bridge.ts"
      provides: "warnings passed through shapeResult 'action' and 'selection_step' cases"
      contains: "warnings"
  key_links:
    - from: "src/session/pick-handler.ts getPickChoices()"
      to: "OpResult.warnings on the wire"
      via: "warnings threaded through stateless-ops handlers then bridge shapeResult"
      pattern: "warnings"
---

<objective>
Close ERR-01: `boardRefs()` / `display()` / `boardRef()` failures surface as structured, inspectable warnings on the pick/op result instead of silent console-only degradation — while keeping the existing graceful fallback (never flip success:false).

Purpose: These three sites currently degrade silently (boardRefs logs then continues; display's `catch {}` logs nothing at all — the worst offender; boardRef's IIFE catch falls back silently). `getChoices()` already hard-fails with an errorCode and must stay that way (regression guard). Per RESEARCH Pitfall 4, `shapeResult` is a manual allowlist — warnings added to OpResult are invisible on the `action`/`selection_step` wire cases unless explicitly forwarded.

Output: A `WarningEntry` type, warnings pushed at the three soft-fail sites and aggregated into a top-level `warnings` array on the pick response, threaded onto OpResult, and forwarded through the two affected bridge shapeResult cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-structured-error-surfacing/126-RESEARCH.md
@.planning/phases/126-structured-error-surfacing/126-PATTERNS.md
@.planning/phases/126-structured-error-surfacing/126-01-SUMMARY.md

<interfaces>
From src/session/pick-handler.ts:
  getPickChoices(...) // method at :152 containing all three switch cases (:198-329)
  boardRefs catch: :230-237  (console.error('boardRefs() error (ignored):', e))
  display catch:   :356-362  (catch {} — NO logging today; fallback validElem.display = element.name || String(element.id))
  boardRef IIFE catch: :378-380  (catch { return { id: element.id } })
  getChoices hard-fail siblings (DO NOT CHANGE): :202-207, :271-276, :286-291 (errorCode CHOICES/ELEMENTS_EVALUATION_ERROR)

From src/session/types.ts: ChoiceWithRefs (:256), ValidElement (:268) — do NOT widen these; warnings go on the top-level response, disambiguated by WarningEntry.source.

From src/session/stateless-ops.ts (after Plan 01): OpResult has error/category/errorCode; add warnings?. handleResolveChoices (:382-394), handleSelectionStep (:338-358) wrap the pick response.

From src/cli/dev-host/bridge.ts: shapeResult (:215) — 'action' (:217), 'resolve_choices' (:219 full passthrough), 'selection_step' (:221) manual allowlist.

Stable warning codes (CONTEXT): BOARD_REFS_ERROR, DISPLAY_ERROR, CHOICES_ERROR (reserve for boardRef), PERSISTENCE_ERROR. Sanitized message shape only; never error.stack.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WarningEntry type + push warnings at the three soft-fail pick-handler sites</name>
  <files>src/session/types.ts, src/session/pick-handler.ts, src/session/pick-handler.test.ts</files>
  <read_first>
    - src/session/types.ts:247-280 (ElementRef/ChoiceWithRefs/ValidElement — add WarningEntry here + warnings? on the pick response type; do NOT widen ChoiceWithRefs/ValidElement)
    - src/session/pick-handler.ts:152-329 (getPickChoices — the aggregation point for a top-level warnings array)
    - src/session/pick-handler.ts:230-237 (boardRefs), :356-362 (display), :378-380 (boardRef)
    - src/session/pick-handler.ts:202-207 (getChoices hard-fail sibling — the errorCode shape to leave untouched)
    - src/session/pick-handler.test.ts (existing — extend)
  </read_first>
  <behavior>
    - Test: a choice whose boardRefs() throws → response.warnings contains {code:'BOARD_REFS_ERROR', source referencing the choice}, choice still present/selectable, success stays true
    - Test: an element whose display() throws → response.warnings contains {code:'DISPLAY_ERROR'}, validElem.display falls back to element.name||id, AND a console.error echo now fires (was missing)
    - Test: an element whose boardRef() throws → warnings contains a stable code, fallback {id} still returned
    - Test (regression): a throwing getChoices() still returns success:false with the existing CHOICES_EVALUATION_ERROR errorCode (behavior unchanged)
    - Test: warning messages contain no stack trace / file path
  </behavior>
  <action>Add `export interface WarningEntry { code: string; message: string; source: string }` to types.ts and an optional `warnings?: WarningEntry[]` on the pick-choices response type. In getPickChoices, build a local `const warnings: WarningEntry[] = []` and attach it to the returned response. At the boardRefs catch push `{code:'BOARD_REFS_ERROR', message: sanitized, source: 'boardRefs(...)'}` and KEEP the existing console.error echo. At the display catch, add the missing `console.error('display() error (ignored):', e)` echo AND push `{code:'DISPLAY_ERROR', ...}` while keeping the name/id fallback. At the boardRef IIFE catch, push a stable-coded warning while keeping the `{id}` fallback. Use codes as string literals from the reserved taxonomy; sanitized message only (no error.stack). Do NOT change the getChoices hard-fail sites and do NOT widen ChoiceWithRefs/ValidElement.</action>
  <verify>
    <automated>npx vitest run src/session/pick-handler.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WarningEntry" src/session/types.ts` >= 1
    - `grep -c "BOARD_REFS_ERROR\|DISPLAY_ERROR" src/session/pick-handler.ts` >= 2
    - `grep -v '^\s*//' src/session/pick-handler.ts | grep -c "error.stack"` == 0
    - display() catch now emits a console.error (grep confirms an echo in that catch block) AND pushes a warning
    - pick-handler.test.ts asserts warnings for boardRefs + display + boardRef, and the getChoices regression (still errorCode hard-fail); suite green
  </acceptance_criteria>
  <done>All three soft-fail sites emit structured warnings + console echo while preserving graceful fallback; getChoices hard-fail unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread warnings onto OpResult and through the bridge shapeResult allowlist</name>
  <files>src/session/stateless-ops.ts, src/cli/dev-host/bridge.ts, src/session/stateless-ops.test.ts, src/cli/dev-host/bridge.test.ts</files>
  <read_first>
    - src/session/stateless-ops.ts:103-142 (OpResult — add warnings? beside errorCode from Plan 01)
    - src/session/stateless-ops.ts:338-358 (handleSelectionStep), :382-394 (handleResolveChoices) — carry warnings from the pick response onto OpResult
    - src/cli/dev-host/bridge.ts:215-229 (shapeResult 'action', 'resolve_choices' full passthrough, 'selection_step') — Pitfall 4: manual allowlist
    - src/cli/dev-host/bridge.test.ts (existing — extend; assert warnings survive the wire shape)
  </read_first>
  <behavior>
    - Test: an op whose pick response carried warnings → OpResult.warnings is populated (resolve_choices + selection_step paths)
    - Test: shapeResult('action', result) forwards result.warnings; shapeResult('selection_step', result) forwards result.warnings
    - Test: shapeResult('resolve_choices', result) still carries warnings (already full passthrough — regression guard)
  </behavior>
  <action>Add optional `warnings?: WarningEntry[]` to the `OpResult` interface. In handleResolveChoices and handleSelectionStep, copy the pick response's `warnings` onto the returned OpResult on the success path (warnings never flip success:false). In bridge.ts shapeResult add `warnings: result.warnings` to the `'action'` and `'selection_step'` cases (the `'resolve_choices'` case is already a full passthrough — leave it). Import/reference WarningEntry from the session types barrel; do not duplicate the type.</action>
  <verify>
    <automated>npx vitest run src/session/stateless-ops.test.ts src/cli/dev-host/bridge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "warnings" src/cli/dev-host/bridge.ts` >= 2 (action + selection_step cases)
    - `grep -c "warnings" src/session/stateless-ops.ts` >= 2 (field + threading)
    - bridge.test.ts asserts warnings survive shapeResult for 'action' and 'selection_step'
    - stateless-ops.test.ts asserts OpResult.warnings populated from a pick response carrying warnings
    - suite green
  </acceptance_criteria>
  <done>Warnings ride OpResult and are visible through the dev-host bridge for the action and selection-step wire cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game-authored boardRefs/display/boardRef callback → pick response | user code throws; message crosses toward the wire |
| session OpResult → dev-host bridge → browser | warning text reaches a client; must stay sanitized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-07 | Information Disclosure | WarningEntry.message | mitigate | Sanitized message shape only; never error.stack (grep gate == 0) |
| T-126-08 | Tampering | soft-fail becoming hard-fail | mitigate | warnings never flip success:false; getChoices hard-fail left unchanged (regression test) |
| T-126-09 | Information Disclosure | warnings dropped/altered at bridge allowlist | mitigate | Explicitly forward warnings in every relevant shapeResult case; bridge.test asserts survival |
| T-126-SC | Tampering | package installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npx vitest run src/session/pick-handler.test.ts src/session/stateless-ops.test.ts src/cli/dev-host/bridge.test.ts` green
- `grep -rn "error.stack" src/session/pick-handler.ts` returns nothing
- `npm test` green after wave merge
</verification>

<success_criteria>
ROADMAP Success Criterion 1: when boardRefs()/display()/getChoices() fail, the pick/op result carries a structured, inspectable warning (or the existing errorCode for getChoices) — not just a console.error.
</success_criteria>

<output>
Create `.planning/phases/126-structured-error-surfacing/126-03-SUMMARY.md` when done.
</output>
