---
phase: 127-scriptable-dev-host
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/dev-host/multiplayer-host.ts
  - src/cli/dev-host/multiplayer-host.test.ts
  - src/cli/dev-host/DevHost.vue
  - src/cli/dev-host/DevHost.debug-relay.test.ts
autonomous: true
requirements: [DRIVE-01, DRIVE-03]
must_haves:
  truths:
    - "A connected, seated client can send getState and receive its own seat's current game_state view (with flowDebugInfo/pendingAction/warnings enrichments), correlated by requestId"
    - "A connected client can send getLobby in ANY phase (including lobby, before joining) and receive the dev host's lobby payload, correlated by requestId"
    - "getState on an unseated client or before the game starts returns an actionable error, never another seat's view"
    - "A debugToggle op is accepted host-side and relayed to every connected client; DevHost.vue reacts by calling the existing toggleDebug()"
    - "A uiSwitch op is accepted host-side and relayed; DevHost.vue reacts by driving the existing onUiSelect() code path (no parallel logic)"
  artifacts:
    - path: "src/cli/dev-host/multiplayer-host.ts"
      provides: "getState/getLobby/debugToggle/uiSwitch ClientInbound variants + handlers"
      contains: "handleGetState"
    - path: "src/cli/dev-host/DevHost.vue"
      provides: "onHostMessage relay cases for debugToggle/uiSwitch"
      contains: "debugToggle"
    - path: "src/cli/dev-host/DevHost.debug-relay.test.ts"
      provides: "jsdom component test proving the page-side relay drives toggleDebug/onUiSelect"
  key_links:
    - from: "src/cli/dev-host/multiplayer-host.ts handleGetState"
      to: "session.viewForSeat(seat) + session.meta()"
      via: "reuse of the reinitSeat view/meta builder (single source of truth)"
      pattern: "viewForSeat"
    - from: "src/cli/dev-host/DevHost.vue onHostMessage"
      to: "toggleDebug() / onUiSelect()"
      via: "existing page-local functions (no duplication)"
      pattern: "toggleDebug"
---

<objective>
Add the four new host-level dev-host WS ops to `MultiplayerHost` and wire their page-side reactions in `DevHost.vue`:
- `getState` (DRIVE-01): returns the requesting client's own seat view, same shape/builder as the `game_state` broadcast.
- `getLobby` (DRIVE-01): returns the dev host's lobby payload, works in every phase.
- `debugToggle` / `uiSwitch` (DRIVE-03): host relays to connected clients; `DevHost.vue` drives its existing `toggleDebug()` / `onUiSelect()` functions.

Purpose: Give the last browser-only dev-host capabilities a scriptable, typed WS entry point without duplicating any state-computation or page logic.
Output: New `ClientInbound`/`HostOutbound` variants + handlers in `multiplayer-host.ts`, `onHostMessage` relay cases in `DevHost.vue`, unit tests, and a jsdom component test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-scriptable-dev-host/127-CONTEXT.md
@.planning/phases/127-scriptable-dev-host/127-PATTERNS.md

<interfaces>
<!-- Extracted from src/cli/dev-host/multiplayer-host.ts — executor uses these directly, no exploration needed. -->

Existing union members (lines 39-55) — ADD new variants alongside these; the dev host has its
OWN ClientInbound/HostOutbound union in multiplayer-host.ts. Do NOT use protocol.ts's
GetStateMessage/GetLobbyMessage (those belong to the separate production GameConnection protocol).

```typescript
export type HostOutbound =
  | { type: 'lobby'; phase: LobbyPhase; seats: SeatInfo[]; minPlayers: number; playerCount: number }
  | { type: 'joined'; seat: number }
  | { type: 'error'; message: string }
  | { type: 'init'; seat: number }
  | { type: 'game_state'; view: unknown; isComplete: boolean; winners: number[] }
  | { type: 'server_response'; requestId: string | null; result: Record<string, unknown> }
  | { type: 'follow'; enabled: boolean; seat: number };

export type ClientInbound =
  | { type: 'hello' }
  | { type: 'join'; seat: number; name?: string; color?: string }
  | { type: 'leave' }
  | { type: 'restart' }
  | { type: 'server_request'; requestId: string; op: string; payload: Record<string, unknown> }
  | { type: 'follow'; enabled: boolean };
```

State fields available on the class:
- `private readonly connected = new Set<string>()`  (line 102) — fan-out target for relays
- `private readonly clientSeat = new Map<string, number>()`  (line 101)
- `private followerClientId: string | null`  (line 108)
- `private effectiveActiveSeat(): number`  (line 365)
- `this.phase` ('lobby' | 'playing' | ...), `this.session` (nullable DevSession)
- `send: (clientId, message: HostOutbound) => void`  (line 91)

Reusable analogs (copy verbatim, do not reinvent):
- `handleServerRequest` guard chain (lines 314-333): phase-not-playing error → seat-not-found error → then act
- `reinitSeat` view/meta builder (lines 477-484): `session.viewForSeat(seat)` + `session.meta()` → `{ type:'game_state', view, isComplete, winners }`
- `lobbyMessage()` (lines 564-572): the existing lobby payload shape
- `broadcastLobby()` (lines 574-577): `for (const clientId of this.connected) this.send(clientId, message)` — the fan-out template
- `handleFollow` + DevHost.vue `case 'follow':` (multiplayer-host.ts 240-272 / DevHost.vue 177-179): the ONLY precedent for "WS message → page reacts locally, no bridge.ts"

DevHost.vue page-local functions to CALL (never duplicate) — `onUiSelect()` (lines 281-283) posts `{type:'dev-ui-select', name: selectedUi.value}`; `toggleDebug()` (lines 301-303) posts `{type:'dev-debug-toggle'}`. `selectedUi` is a ref (line 57).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getState/getLobby host query ops</name>
  <files>src/cli/dev-host/multiplayer-host.ts, src/cli/dev-host/multiplayer-host.test.ts</files>
  <read_first>
    - src/cli/dev-host/multiplayer-host.ts:39-55 (union), :206-221 (handleMessage switch), :314-333 (handleServerRequest guard chain), :477-484 (reinitSeat view/meta builder), :564-572 (lobbyMessage)
    - src/cli/dev-host/multiplayer-host.test.ts:1-40 (in-process fake-send harness — construct a minimal PassGame, drive handleMessage, assert recorded send calls)
    - 127-PATTERNS.md "add getState/getLobby" section; 127-RESEARCH.md Pitfall 5 (guard chain) + Pitfall 6 (requestId echo)
  </read_first>
  <behavior>
    - getState (seated, phase playing): sends a `game_state`-shaped message for the caller's own seat with `requestId` echoed from the request
    - getState (phase not playing OR session null): sends `{type:'error', message: ...}` — actionable, no view
    - getState (connected but unseated): sends `{type:'error', ...}`, never a view
    - getState resolves seat ONLY from followerClientId/clientSeat — a client-supplied `seat` field is ignored (no such field on the variant)
    - getLobby (any phase, including lobby before join): sends the lobbyMessage() payload with `requestId` echoed
  </behavior>
  <action>
    Add `{ type: 'getState'; requestId?: string }` and `{ type: 'getLobby'; requestId?: string }` to `ClientInbound`. Add a `requestId?: string | null` field to the existing `game_state` HostOutbound variant (reuse it — do NOT invent a new response type name; getState must be provably equal to the broadcast shape). Add a `requestId?: string | null` field to the `lobby` HostOutbound variant.

    Add two `case` arms to `handleMessage` dispatching to `handleGetState`/`handleGetLobby`.

    `handleGetState(clientId, msg)`: copy `handleServerRequest`'s FULL guard chain verbatim — first `if (this.phase !== 'playing' || !this.session)` → error return; then resolve `seat = clientId === this.followerClientId ? this.effectiveActiveSeat() : this.clientSeat.get(clientId)`; then `if (seat === undefined)` → error return. Only then build via `this.session.viewForSeat(seat)` + `this.session.meta()` and `this.send(clientId, { type: 'game_state', view, isComplete: meta.isComplete, winners: meta.winners, requestId: msg.requestId ?? null })`. Echo requestId directly — do NOT build a requestOrigin map (response goes to the same asker; per Pitfall 6).

    `handleGetLobby(clientId, msg)`: NOT gated on phase (must work in lobby). Send `{ ...this.lobbyMessage(), requestId: msg.requestId ?? null }`.

    Add unit tests in multiplayer-host.test.ts covering the five behaviors above using the existing fake-send harness.
  </action>
  <verify>
    <automated>npx vitest run src/cli/dev-host/multiplayer-host.test.ts -t "getState"</automated>
    <automated>npx vitest run src/cli/dev-host/multiplayer-host.test.ts -t "getLobby"</automated>
  </verify>
  <acceptance_criteria>
    - getState test asserts the returned view equals `session.viewForSeat(seat)` for the caller's OWN seat and echoes requestId
    - getState test asserts an error (not a view) when called before join and when called in lobby phase
    - getLobby test asserts a lobby payload is returned in lobby phase (before any join) with requestId echoed
    - `npx vitest run src/cli/dev-host/multiplayer-host.test.ts` fully green
  </acceptance_criteria>
  <done>getState/getLobby handlers implemented with full guard chain and requestId echo; unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: debugToggle/uiSwitch host relay ops</name>
  <files>src/cli/dev-host/multiplayer-host.ts, src/cli/dev-host/multiplayer-host.test.ts</files>
  <read_first>
    - src/cli/dev-host/multiplayer-host.ts:240-272 (handleFollow relay half), :574-577 (broadcastLobby fan-out over this.connected)
    - 127-PATTERNS.md "add debugToggle/uiSwitch" section; 127-RESEARCH.md Pitfall 4 (broadcast-to-all target, optional requester ack)
  </read_first>
  <behavior>
    - debugToggle: every connected clientId receives a `{type:'debugToggle'}` message
    - uiSwitch: every connected clientId receives a `{type:'uiSwitch', name}` message carrying the requested UI name
    - a headless requester with no iframe causes no error (relay is fan-out only; documented as expected)
  </behavior>
  <action>
    Add `{ type: 'debugToggle' }` and `{ type: 'uiSwitch'; name: string }` to `ClientInbound`, and matching `HostOutbound` relay variants `{ type: 'debugToggle' }` and `{ type: 'uiSwitch'; name: string }`. Follow the existing host-op naming style (join/leave/follow/restart).

    Add `case` arms to `handleMessage` → `handleDebugToggle`/`handleUiSwitch`. Each mirrors `broadcastLobby`'s fan-out: `for (const cid of this.connected) this.send(cid, { type: 'debugToggle' })` (and the uiSwitch equivalent forwarding `msg.name`). Do NOT route through bridge.ts (these are relays, not game ops) and do NOT reconstruct any DOM/debug logic host-side. Add a JSDoc note on each handler that a scripted-only client with no browser tab connected will see no visible effect — this is expected, not a bug (per Pitfall 4).

    Add unit tests asserting each op fans out to all connected clients.
  </action>
  <verify>
    <automated>npx vitest run src/cli/dev-host/multiplayer-host.test.ts -t "debug"</automated>
    <automated>npx vitest run src/cli/dev-host/multiplayer-host.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - debugToggle test registers 2+ connected clients and asserts BOTH receive the relay
    - uiSwitch test asserts the `name` payload is forwarded intact to all connected clients
    - no game-state mutation occurs (session untouched) for either op
  </acceptance_criteria>
  <done>debugToggle/uiSwitch relay handlers fan out over this.connected; unit tests green.</done>
</task>

<task type="auto">
  <name>Task 3: DevHost.vue page-side relay cases + component test</name>
  <files>src/cli/dev-host/DevHost.vue, src/cli/dev-host/DevHost.debug-relay.test.ts</files>
  <read_first>
    - src/cli/dev-host/DevHost.vue:139-181 (onHostMessage switch, existing case 'follow' at 177-179), :281-283 (onUiSelect), :301-303 (toggleDebug), :57 (selectedUi ref)
    - src/cli/dev-host/DevHost.restart.test.ts:1 (@vitest-environment jsdom), :27-61 (FakeWebSocket harness), :94-104 (mount(DevHost) + ws.simulateOpen/simulateMessage) — reuse this harness verbatim, do not invent a new one
    - 127-PATTERNS.md "DevHost.vue relay cases" + "DevHost.debug-relay.test.ts" sections
  </read_first>
  <action>
    In `onHostMessage`, add two cases mirroring the existing `case 'follow':` shape:
    - `case 'debugToggle':` → call `toggleDebug()` (the exact function the header button already calls); `break`.
    - `case 'uiSwitch':` → set `selectedUi.value = msg.name as string`, then call `onUiSelect()` (drives the existing dev-ui-select postMessage path); `break`.
    Do not duplicate the bodies of toggleDebug/onUiSelect — call them.

    Create src/cli/dev-host/DevHost.debug-relay.test.ts with `// @vitest-environment jsdom`, reusing DevHost.restart.test.ts's FakeWebSocket harness + `mount(DevHost, {props:{config}})`. Spy on the iframe postMessage path (`postToGame` target — assert via the iframe contentWindow.postMessage spy as DevHost.restart.test.ts does). Simulate `{type:'debugToggle'}` and assert a `dev-debug-toggle` postMessage is emitted; simulate `{type:'uiSwitch', name:'custom'}` and assert a `dev-ui-select` postMessage with the name is emitted.
  </action>
  <verify>
    <automated>npx vitest run src/cli/dev-host/DevHost.debug-relay.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts a debugToggle host message produces exactly one `dev-debug-toggle` postToGame call
    - Test asserts a uiSwitch host message with name='custom' produces a `dev-ui-select` postToGame with `name:'custom'`
    - No parallel toggle/select logic added — the cases call the existing functions
  </acceptance_criteria>
  <done>DevHost.vue relays debugToggle/uiSwitch to the existing page functions; jsdom component test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripted/browser client → MultiplayerHost (WS) | Untrusted, unauthenticated client input crosses here (clientId is client-supplied — pre-existing loopback dev-tool model) |
| MultiplayerHost → DevHost.vue page (relay) | Host re-emits control commands; page acts on its own iframe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-01 | Information Disclosure | handleGetState | mitigate | Resolve seat ONLY from server-tracked followerClientId/clientSeat, never a client-supplied seat field (no such field on the getState variant); reuse `viewForSeat(seat)` so the caller can only ever see its own seat — copies handleServerRequest exactly (V4 Access Control) |
| T-127-02 | Elevation of Privilege | debugToggle/uiSwitch handlers | mitigate | Relay-only fan-out; no bridge.ts routing, no game-state mutation, no DOM reconstruction host-side — control ops cannot alter game state |
| T-127-03 | Spoofing | clientId (hello) | accept | Pre-existing: dev host is a loopback/LAN dev tool with no auth model by design; this phase adds no new trust boundary and must not bolt on auth |
| T-127-04 | Information Disclosure | handleGetState pre-start | mitigate | Full guard chain (phase-not-playing → seat-not-found) returns actionable errors before any view is built (Pitfall 5) |
| T-127-SC | Tampering | npm installs | accept | Zero new dependencies this phase (`ws` already present, `globalThis.WebSocket` is native) — no supply-chain surface added |
</threat_model>

<verification>
- `npx vitest run src/cli/dev-host/multiplayer-host.test.ts` green (getState/getLobby/debugToggle/uiSwitch)
- `npx vitest run src/cli/dev-host/DevHost.debug-relay.test.ts` green
- No new `WireOp` entries added to bridge.ts (grep confirms host ops stay host-level)
</verification>

<success_criteria>
- getState returns only the caller's own seat view, correlated by requestId; guards unseated/pre-start
- getLobby works in every phase, correlated by requestId
- debugToggle/uiSwitch relay to all connected clients; DevHost.vue drives its existing functions
- Zero new dependencies; no bridge.ts routing for the new ops
</success_criteria>

<output>
Create `.planning/phases/127-scriptable-dev-host/127-01-SUMMARY.md` when done
</output>
