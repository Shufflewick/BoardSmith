---
phase: 127-scriptable-dev-host
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/game-connection.ts
  - src/client/types.ts
  - src/client/game-connection.test.ts
autonomous: true
requirements: [DRIVE-02]
must_haves:
  truths:
    - "GameConnection constructs and connects in a Node process using globalThis.WebSocket — no browser-only `new WebSocket()` global required"
    - "A caller can inject a WebSocket implementation via GameConnectionConfig.wsImplementation for exotic runtimes"
    - "When neither globalThis.WebSocket nor an injected implementation is available (e.g. Node <22.4), the constructor throws an actionable error naming the Node requirement and the override escape hatch — never a bare ReferenceError"
  artifacts:
    - path: "src/client/game-connection.ts"
      provides: "injectable WebSocket constructor (wsCtor) replacing the two browser-global touch points"
      contains: "wsImplementation"
    - path: "src/client/types.ts"
      provides: "GameConnectionConfig.wsImplementation optional field"
      contains: "wsImplementation"
    - path: "src/client/game-connection.test.ts"
      provides: "Node-environment unit test for injection + fail-loud guard"
  key_links:
    - from: "src/client/game-connection.ts constructor"
      to: "config.wsImplementation ?? globalThis.WebSocket"
      via: "single resolved wsCtor field used by both `new` and static readyState reads"
      pattern: "wsImplementation \\?\\? "
---

<objective>
Make `GameConnection` usable from a Node process (DRIVE-02) by routing its two browser-global touch points through an injectable WebSocket constructor: `config.wsImplementation ?? globalThis.WebSocket`, with a fail-loud actionable guard when neither is available.

Purpose: The client SDK must construct and connect from Node scripts (native on Node ≥22.4) without the browser `WebSocket` global, while keeping the production wire protocol unchanged.
Output: An injectable `wsCtor` in `game-connection.ts`, a `wsImplementation?` config field, and a Node-environment unit test.

Scope fence (per CONTEXT amendment / RESEARCH Pitfall 1): This makes GameConnection Node-capable for ITS OWN (production `/games/:gameId`) protocol. GameConnection does NOT speak the dev-host protocol and is NOT used by the Plan 03 dev-host integration test. Do not attempt to make GameConnection bilingual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/127-scriptable-dev-host/127-CONTEXT.md
@.planning/phases/127-scriptable-dev-host/127-PATTERNS.md

<interfaces>
<!-- From src/client/game-connection.ts + types.ts. The ONLY browser-global touch points. -->

Static readyState reads (lines 71, 118, 157, 361, 422): `WebSocket.OPEN` / `WebSocket.CONNECTING`
Instance construction (line 80): `this.ws = new WebSocket(wsUrl);`
Class field (line 22): `private ws: WebSocket | null = null;`
Constructor (line 53): `constructor(baseUrl: string, config: GameConnectionConfig)`
Config type (types.ts:167): `export interface GameConnectionConfig { ... }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Injectable wsCtor + fail-loud guard + config field</name>
  <files>src/client/game-connection.ts, src/client/types.ts, src/client/game-connection.test.ts</files>
  <read_first>
    - src/client/game-connection.ts:20-53 (fields + constructor), :71, :80, :118, :157, :361, :422 (the six browser-global touch points)
    - src/client/types.ts:167 (GameConnectionConfig)
    - 127-PATTERNS.md "game-connection.ts Node-capability fix" + "Fail-loud actionable errors" shared pattern; 127-RESEARCH.md Pitfall 2 (engines/Node 22.4) + Code Examples (lines 252-261)
    - src/cli/dev-host/multiplayer-host.test.ts:1-40 (general vitest style — no direct GameConnection test precedent exists; do not use vi.stubGlobal, inject via wsImplementation instead since Node default env may lack global WebSocket)
  </read_first>
  <behavior>
    - Constructing GameConnection with an injected `wsImplementation` (a minimal fake WebSocket class) succeeds and `connect()` instantiates via the injected ctor, not the global
    - `connect()` guard and OPEN checks compare against the injected ctor's OPEN/CONNECTING static values
    - Constructing with neither globalThis.WebSocket nor wsImplementation available throws an Error whose message names the Node >=22.4 requirement AND the wsImplementation escape hatch (assert on the message text)
    - On Node >=22.4 (this dev machine), constructing without an override resolves wsCtor to globalThis.WebSocket and does not throw
  </behavior>
  <action>
    Add `wsImplementation?: typeof WebSocket;` to `GameConnectionConfig` in types.ts with a JSDoc noting it overrides the resolved WebSocket constructor for runtimes lacking a global (e.g. Node <22.4).

    In `game-connection.ts`, add a private field resolved once in the constructor: `this.#wsCtor = config.wsImplementation ?? (globalThis as { WebSocket?: typeof WebSocket }).WebSocket`. Immediately after, add a fail-loud guard: if `!this.#wsCtor`, throw an Error reading approximately "GameConnection requires a WebSocket implementation. Node <22.4 has no global WebSocket — upgrade to Node >=22.4 or pass `wsImplementation` in GameConnectionConfig." Route line 80's `new WebSocket(wsUrl)` through `new this.#wsCtor(wsUrl)`, and all `WebSocket.OPEN`/`WebSocket.CONNECTING` static reads (lines 71, 118, 157, 361, 422) through `this.#wsCtor.OPEN`/`this.#wsCtor.CONNECTING`. Keep the field type on `this.ws` as `InstanceType<typeof WebSocket> | null` (unchanged behavior).

    Engines decision (RESEARCH Pitfall 2, "engines note updated as appropriate"): do NOT bump `package.json` `engines.node` to `>=22.4`. BoardSmith is a browser-first library; forcing 22.4 on all consumers over-constrains the many who never use GameConnection in Node. The fail-loud guard IS the enforcement (Pit of Success: the wrong path — Node <22.4 + GameConnection — fails loud with an actionable message; unaffected consumers are untouched). Document the Node >=22.4 native-path requirement in the constructor's JSDoc `@remarks`. Record this as a documented decision in the SUMMARY.

    Create src/client/game-connection.test.ts (default Node vitest environment) with a minimal FakeWebSocket class (readyState, OPEN/CONNECTING statics, send/close no-ops, addEventListener registry) injected via `wsImplementation`, plus a fail-loud test that temporarily removes globalThis.WebSocket and asserts the thrown message.
  </action>
  <verify>
    <automated>npx vitest run src/client/game-connection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test constructs GameConnection with an injected FakeWebSocket and asserts connect() uses it (fake instance created, no throw)
    - Test asserts the fail-loud path throws with a message containing both "22.4" and "wsImplementation"
    - Production wire protocol/message shapes unchanged (no edits to the message-handling switch beyond the ctor/static-read routing)
    - `npx vitest run src/client/game-connection.test.ts` green
  </acceptance_criteria>
  <done>GameConnection routes WebSocket construction/static reads through an injectable, guarded wsCtor; Node-environment unit test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GameConnection → production game server (WS) | Client SDK connects out to a (production) server; runtime-environment change only, no protocol change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-05 | Denial of Service | GameConnection ctor on Node <22.4 | mitigate | Fail-loud actionable guard replaces a cryptic ReferenceError, so callers get a clear, recoverable error path instead of a runtime crash (CLAUDE.md fail-fast rule) |
| T-127-06 | Tampering | injected wsImplementation | accept | Injection is an opt-in developer escape hatch for exotic runtimes; a caller supplying a hostile ctor already controls their own process — no privilege boundary crossed |
| T-127-SC | Tampering | npm installs | accept | Zero new dependencies — `globalThis.WebSocket` is Node-native; no supply-chain surface added |
</threat_model>

<verification>
- `npx vitest run src/client/game-connection.test.ts` green
- `grep -n "new WebSocket(" src/client/game-connection.ts` returns no bare-global construction (routed through wsCtor)
</verification>

<success_criteria>
- GameConnection constructs/connects in Node via globalThis.WebSocket or an injected implementation
- Fail-loud actionable error when no implementation is available
- Production protocol unchanged; zero new dependencies
</success_criteria>

<output>
Create `.planning/phases/127-scriptable-dev-host/127-02-SUMMARY.md` when done
</output>
