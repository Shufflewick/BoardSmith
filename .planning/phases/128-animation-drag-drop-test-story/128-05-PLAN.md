---
phase: 128-animation-drag-drop-test-story
plan: 05
type: execute
wave: 2
depends_on: [128-01]
files_modified:
  - src/ui/composables/useFlyingElements.ts
  - src/ui/composables/useFlyingElements.test.ts
autonomous: true
requirements: [ANIM-01, ANIM-02, ANIM-03]

must_haves:
  truths:
    - "In test mode, useFlyingElements fly/autoWatch resolves instantly and records a 'fly' trace with from/to container names"
    - "The autoWatch path records {from:'opponentHand', to:'myHand'}-style container-name traces (the CONTEXT ergonomics target)"
    - "First-resolution missing start/end target throws (dev) / console.error+skips (prod); the per-frame mid-flight re-check stays a silent complete"
    - "useFlyingElements has passing direct unit tests covering test-mode, the mocked-RAF real path, and the throw path"
  artifacts:
    - path: "src/ui/composables/useFlyingElements.ts"
      provides: "Test-mode trace branch + fail-loud first-resolution throw (mirrors normalizeRect)"
      contains: "isAnimationTestModeEnabled"
    - path: "src/ui/composables/useFlyingElements.test.ts"
      provides: "Direct unit tests (test-mode + mocked-RAF real path + throw)"
  key_links:
    - from: "src/ui/composables/useFlyingElements.ts"
      to: "src/ui/composables/useAnimationTestMode.ts"
      via: "isAnimationTestModeEnabled() + recordTrace() in flyCardInternal()"
      pattern: "recordTrace"
---

<objective>
Add the test-mode trace branch and first-resolution fail-loud throw to `useFlyingElements`, and give it its first direct unit tests. This composable is where CONTEXT's flagship assertion lives: `{kind:'fly', from:'opponentHand', to:'myHand'}` via the autoWatch container names.

Purpose: ANIM-01/02/03 for useFlyingElements.
Output: modified `useFlyingElements.ts` + new `useFlyingElements.test.ts`.

Discretion note (RESEARCH A3): for `fly`/`flyMultiple` calls where `startRect`/`endRect` are raw `DOMRect` (no HTMLElement, no container), `from`/`to` are not derivable from geometry — accept `undefined` in the trace (honest) and let `meta` carry the raw rect. The autoWatch path DOES have first-class container names (`AutoWatchContainer.name`) — use those for from/to there.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-animation-drag-drop-test-story/128-CONTEXT.md
@.planning/phases/128-animation-drag-drop-test-story/128-RESEARCH.md
@.planning/phases/128-animation-drag-drop-test-story/128-PATTERNS.md

<interfaces>
```typescript
export interface AnimationTrace { kind: 'flip'|'fly'|'element'|'action'; element: string|null; from: string|undefined; to: string|undefined; meta?: Record<string, unknown>; }
export function isAnimationTestModeEnabled(): boolean;
export function recordTrace(entry: AnimationTrace): void;
export function isDevMode(): boolean;
// Existing (useFlyingElements.ts): AutoWatchContainer { ref; name: string }  — name is the from/to source for autoWatch
// Existing FlyConfig.id (caller-supplied string) is the trace `element` for manual fly()
```
</interfaces>

@src/utils/dev.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add test-mode trace branch + first-resolution throw to useFlyingElements</name>
  <files>src/ui/composables/useFlyingElements.ts</files>
  <read_first>
    - src/ui/composables/useFlyingElements.ts:94-97 — import block (add the test-mode import alongside)
    - src/ui/composables/useFlyingElements.ts:390-401 — `normalizeRect` EXISTING throw: the fail-loud precedent to match wording; this is where a function-typed rect resolving null already throws
    - src/ui/composables/useFlyingElements.ts:445-629 — `flyCardInternal`: prefersReducedMotion guard at top (test-mode goes ABOVE); startRect resolution is the FIRST-resolution throw site (before `flyingCards.value.push`); the per-frame `getRect(endTarget)` null-check at 529-537 must STAY a silent complete (target legitimately disappeared mid-flight — RESEARCH Pitfall 3, do NOT throw there)
    - src/ui/composables/useFlyingElements.ts:234-239 — `AutoWatchContainer { ref; name }`: the from/to source for autoWatch traces (matches CONTEXT's opponentHand→myHand example)
    - src/utils/dev.ts — `isDevMode()` gate to reuse
  </read_first>
  <behavior>
    - Test mode ON: fly/flyMultiple/autoWatch record one {kind:'fly'} trace per flown element and resolve immediately without running the RAF chain
    - autoWatch trace: element = engine element id, from/to = the source/destination AutoWatchContainer.name
    - manual fly() trace: element = FlyConfig.id; from/to = derived from HTMLElement anchor attrs when a container/element was passed, else undefined (raw DOMRect case — A3)
    - Test-mode branch checked BEFORE prefersReducedMotion
    - First-resolution: a start/end target the caller asked to fly that resolves null triggers dev throw / prod console.error+skip (extends the normalizeRect precedent to the non-function rect cases at initial resolution)
    - Per-frame mid-flight endTarget null (529-537) stays a silent complete
  </behavior>
  <action>
    Import `isAnimationTestModeEnabled`, `recordTrace` from `./useAnimationTestMode.js` and `isDevMode` from `../../utils/dev.js` (alongside imports at 94-97). In `flyCardInternal()` (line ~445), insert a test-mode branch ABOVE the `prefersReducedMotion` guard: derive `element`/`from`/`to` per the <behavior> mapping (autoWatch container names when available, FlyConfig.id + anchor-derived or undefined otherwise), `recordTrace` one `{kind:'fly'}` entry with `meta` carrying the resolved start/end rects, and resolve/return without pushing onto `flyingCards` or starting RAF. For the fail-loud path: at the FIRST resolution of `startRect`/`endRect` (before `flyingCards.value.push`), when a target the caller explicitly asked to animate resolves null, build an actionable message naming `useFlyingElements`, which target (start vs end) and the config id, and how to fix (ensure the target element/ref carries its anchor); gate with `isDevMode()` (throw dev / console.error+return prod). Reuse and match `normalizeRect`'s existing wording style. Do NOT alter the per-frame re-check at 529-537 (legitimate mid-flight disappearance stays a silent complete).
  </action>
  <verify>
    <automated>grep -q "isAnimationTestModeEnabled" src/ui/composables/useFlyingElements.ts && grep -q "isDevMode" src/ui/composables/useFlyingElements.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Test-mode branch precedes the prefersReducedMotion guard in `flyCardInternal()`
    - autoWatch trace records from/to as AutoWatchContainer.name values
    - First-resolution null target is dev-gated fail-loud; the 529-537 per-frame path is unchanged (silent complete)
    - Throw message matches normalizeRect's style and names start/end + config id
  </acceptance_criteria>
  <done>useFlyingElements records fly traces (autoWatch from/to = container names) in test mode and fails loud on first-resolution null targets, dev-gated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Direct unit tests for useFlyingElements (test-mode + mocked-RAF real path + throw)</name>
  <files>src/ui/composables/useFlyingElements.test.ts</files>
  <read_first>
    - src/ui/composables/useAnimationEvents.test.ts:1-40 — test shape + waitForIdle polling helper (model for the RAF-chain real-path test)
    - src/ui/components/helpers/HintOverlay.test.ts:28-45 — matchMedia + ResizeObserver stubs FIRST (Pitfall 1)
    - RESEARCH Don't-Hand-Roll (128-RESEARCH.md:179) — RAF chain: `vi.stubGlobal('requestAnimationFrame', ...)` with a manual tick queue
    - RESEARCH Pitfall 2 — jsdom getBoundingClientRect zero; supply distinct rects where movement is asserted
    - CONTEXT specifics (128-CONTEXT.md:71) — the flagship assertion ergonomics: `toContainEqual({kind:'fly', from:'opponentHand', to:'myHand', ...})`
  </read_first>
  <behavior>
    - Test-mode autoWatch: configure autoWatch with two named containers, move an element between them, flush watchers → getAnimationTrace() `toContainEqual` a {kind:'fly', from:<sourceName>, to:<destName>} entry (the CONTEXT flagship assertion)
    - Test-mode manual fly(): fly({id, startRect: <HTMLElement>, endRect: <HTMLElement>}) records a {kind:'fly', element:id} trace and resolves its promise immediately
    - Real path (mocked RAF): a fly() with mocked requestAnimationFrame drives the RAF chain to completion and resolves the fly promise (assert the promise settles and flyingCards drains) — satisfies CONTEXT's mocked-RAF requirement
    - Throw path: fly() whose start target resolves null throws (dev) naming start/end + id; prod console.errors
  </behavior>
  <action>
    Create `src/ui/composables/useFlyingElements.test.ts` with `// @vitest-environment jsdom`. matchMedia + ResizeObserver `vi.stubGlobal` FIRST, before importing useFlyingElements. Reset the trace module in beforeEach/afterEach. Implement the four <behavior> groups. Use `toContainEqual` for the autoWatch flagship trace assertion (assert on from/to container names + kind, by identity — not on rects/pixels, which are zero in jsdom). For the mocked-RAF real path, install a manual RAF queue via `vi.stubGlobal` and a tick driver, and use the `waitForIdle`-style polling helper from useAnimationEvents.test.ts to await the fly promise/flyingCards drain. For the throw path, force dev/prod via `vi.mock('../../utils/dev.js', ...)` and assert `toThrow` (dev) / console.error spy (prod).
  </action>
  <verify>
    <automated>npx vitest run src/ui/composables/useFlyingElements.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/composables/useFlyingElements.test.ts` passes
    - A test asserts `getAnimationTrace()` `toContainEqual` a `{kind:'fly', from, to}` autoWatch entry with container-name from/to (the CONTEXT flagship assertion)
    - A mocked-RAF real-path test drives the RAF queue and awaits the fly promise/flyingCards drain
    - A throw-path test asserts dev throws / prod console.errors on a null start target
  </acceptance_criteria>
  <done>useFlyingElements.test.ts green across test-mode (incl. the flagship autoWatch from/to assertion), mocked-RAF real path, and the fail-loud throw.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composable → target resolution | A null start/end target was a silent no-op / silent complete |
| test mode → production bundle | Instant/trace branch must never activate in a shipped game |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-01 | Tampering | useFlyingElements test-mode branch | mitigate | Gated by `isAnimationTestModeEnabled()` (default OFF); independent of prefersReducedMotion |
| T-128-02 | Information disclosure | fly trace contents | mitigate | Trace records element id + container names + rects in meta; `FlyConfig.elementData` (rank/suit) is NOT copied into the trace — element identity only |
| T-128-03 | Denial of Service | first-resolution throw in production | mitigate | Throw gated by `isDevMode()`; prod path console.error + skip; the mid-flight per-frame path stays a non-throwing silent complete |
| T-128-SC | Tampering | package installs | accept | No package installs this phase |
</threat_model>

<verification>
- `npx vitest run src/ui/composables/useFlyingElements.test.ts` green
- Flagship autoWatch from/to assertion present
- First-resolution throw dev-gated; 529-537 per-frame path unchanged
</verification>

<success_criteria>
useFlyingElements resolves instantly with an assertable fly trace (autoWatch from/to = container names, matching CONTEXT's ergonomics target), fails loud (dev) on first-resolution null targets while preserving graceful mid-flight completion, and has direct unit tests across all three paths.
</success_criteria>

<output>
Create `.planning/phases/128-animation-drag-drop-test-story/128-05-SUMMARY.md` when done.
</output>
