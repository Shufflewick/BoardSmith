---
phase: 128-animation-drag-drop-test-story
plan: 06
type: execute
wave: 2
depends_on: [128-01]
files_modified:
  - src/ui/composables/useActionAnimations.ts
  - src/ui/composables/useActionAnimations.test.ts
autonomous: true
requirements: [ANIM-01, ANIM-02, ANIM-03]

must_haves:
  truths:
    - "In test mode, useActionAnimations records an 'action' trace with human-readable from/to selector strings and resolves instantly without delegating to fly()"
    - "The two existing console.warn sites (source/destination not found) are upgraded to dev-throw + prod console.error reusing the same message text"
    - "useActionAnimations has passing direct unit tests covering test-mode, the setTimeout-based real path, and the throw path"
  artifacts:
    - path: "src/ui/composables/useActionAnimations.ts"
      provides: "Test-mode trace branch (own selector strings) + warn→dev-throw upgrade at both sites"
      contains: "isAnimationTestModeEnabled"
    - path: "src/ui/composables/useActionAnimations.test.ts"
      provides: "Direct unit tests (test-mode + setTimeout real path + throw)"
  key_links:
    - from: "src/ui/composables/useActionAnimations.ts"
      to: "src/ui/composables/useAnimationTestMode.ts"
      via: "isAnimationTestModeEnabled() + recordTrace() at the animate entry point"
      pattern: "recordTrace"
---

<objective>
Add the test-mode trace branch (sourced from useActionAnimations' OWN interpolated selector strings — RESEARCH Open Question 2) and upgrade its two existing `console.warn` sites to dev-throw + prod-console.error, then give it its first direct unit tests.

Purpose: ANIM-01/02/03 for useActionAnimations.
Output: modified `useActionAnimations.ts` + new `useActionAnimations.test.ts`.

Resolution of RESEARCH Open Question 2: useActionAnimations records its trace at its OWN level (where the human-readable `elementSelector`/`destinationSelector` strings are already interpolated), and RETURNS EARLY in test mode WITHOUT delegating to `useFlyingElements.fly()` — this avoids double-recording and yields the best from/to. Mirror the existing reduced-motion early-return shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-animation-drag-drop-test-story/128-CONTEXT.md
@.planning/phases/128-animation-drag-drop-test-story/128-RESEARCH.md
@.planning/phases/128-animation-drag-drop-test-story/128-PATTERNS.md

<interfaces>
```typescript
export interface AnimationTrace { kind: 'flip'|'fly'|'element'|'action'; element: string|null; from: string|undefined; to: string|undefined; meta?: Record<string, unknown>; }
export function isAnimationTestModeEnabled(): boolean;
export function recordTrace(entry: AnimationTrace): void;
export function isDevMode(): boolean;
```
</interfaces>

@src/utils/dev.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test-mode trace (own selectors) + upgrade both warn sites to dev-throw</name>
  <files>src/ui/composables/useActionAnimations.ts</files>
  <read_first>
    - src/ui/composables/useActionAnimations.ts:370-499 — the selector-resolve → warn → animate region (the animate entry point where the test-mode branch and both warn sites live)
    - src/ui/composables/useActionAnimations.ts:387-395 — Site 1: source element `console.warn` (upgrade to dev-throw + prod console.error, REUSE the exact message string)
    - src/ui/composables/useActionAnimations.ts:483-493 — Site 2: destination element `console.warn` (same upgrade; message already carries selectorDesc + args)
    - src/ui/composables/useActionAnimations.ts:385,479 — where `elementSelector`/`destinationSelector` string templates are interpolated (the from/to source for the trace — RESEARCH Open Question 2)
    - src/utils/dev.ts — `isDevMode()` gate to reuse
  </read_first>
  <behavior>
    - Test mode ON: at the animate entry point, record one {kind:'action', element:<action/selection value>, from:<interpolated elementSelector>, to:<interpolated destinationSelector>, meta:{action, args}} and return WITHOUT calling fly() (no double-record)
    - Test-mode branch checked BEFORE the existing reduced-motion / animation path
    - Site 1 (source not found) and Site 2 (destination not found): `if (isDevMode()) throw new Error(<existing message>); else console.error(<existing message>); return;` — reuse the exact existing message strings verbatim
  </behavior>
  <action>
    Import `isAnimationTestModeEnabled`, `recordTrace` from `./useAnimationTestMode.js` and `isDevMode` from `../../utils/dev.js`. At the animate entry point in the 370-499 region (after the selector strings are interpolated at 385/479 but before delegating to `fly()`), insert a test-mode branch: if `isAnimationTestModeEnabled()`, `recordTrace` one `{kind:'action'}` entry using the already-interpolated `elementSelector`/`destinationSelector` strings as `from`/`to`, the action's selection value as `element`, and `meta:{action, args}`, then `return`. At Site 1 (387-395) and Site 2 (483-493), replace `console.warn(msg); return;` with `if (isDevMode()) { throw new Error(msg); } else { console.error(msg); } return;` — keep the EXACT existing message strings (they already carry selector/action/args, satisfying ANIM-03's actionable requirement). Do not change any other behavior.
  </action>
  <verify>
    <automated>grep -q "isAnimationTestModeEnabled" src/ui/composables/useActionAnimations.ts && grep -c "console.warn" src/ui/composables/useActionAnimations.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test-mode branch precedes the delegation to fly() and returns early (no double-record)
    - Both former `console.warn` sites now branch on `isDevMode()` (throw dev / console.error prod) and reuse the original message text
    - `from`/`to` in the action trace are the interpolated selector strings (RESEARCH Open Question 2 resolution)
  </acceptance_criteria>
  <done>useActionAnimations records an action trace from its own selectors in test mode and fails loud (dev) at both former warn sites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Direct unit tests for useActionAnimations (test-mode + setTimeout real path + throw)</name>
  <files>src/ui/composables/useActionAnimations.test.ts</files>
  <read_first>
    - src/ui/composables/useAnimationEvents.test.ts:1-40 — test shape; fake-timer usage for setTimeout-based waits
    - src/ui/components/helpers/HintOverlay.test.ts:28-45 — matchMedia + ResizeObserver stubs FIRST (Pitfall 1)
    - src/ui/composables/useActionAnimations.ts:370-499 — the animate entry, watch trigger, and both selector-resolution sites (what the tests drive)
  </read_first>
  <behavior>
    - Test-mode: trigger an action animation with resolvable selector strings → getAnimationTrace() contains a {kind:'action', from:<elementSelector>, to:<destinationSelector>} entry and no real timers run
    - Real path (fake timers): with source+destination elements present in the DOM and `vi.useFakeTimers`, triggering the animation and advancing timers drives the setTimeout-based CSS-transition wait to completion (assert the animation lifecycle settles)
    - Throw path: dev-mode trigger with a missing source (or destination) element throws with the existing selector/action/args message; prod-mode console.errors
  </behavior>
  <action>
    Create `src/ui/composables/useActionAnimations.test.ts` with `// @vitest-environment jsdom`. matchMedia + ResizeObserver `vi.stubGlobal` FIRST, before importing useActionAnimations. Reset the trace module in beforeEach/afterEach. Implement the three <behavior> groups. Use `vi.useFakeTimers()` + `vi.advanceTimersByTime` for the setTimeout-based real path (this composable's timing is setTimeout, not RAF, so fake timers work directly here — unlike the RAF composables). For the throw path, force dev/prod via `vi.mock('../../utils/dev.js', ...)` and assert `toThrow` (dev) with a message matching the selector/action, and a console.error spy (prod). Assert the test-mode trace's from/to equal the interpolated selector strings.
  </action>
  <verify>
    <automated>npx vitest run src/ui/composables/useActionAnimations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/composables/useActionAnimations.test.ts` passes
    - A test asserts a `{kind:'action', from, to}` trace whose from/to are the interpolated selector strings
    - A fake-timer real-path test advances timers and asserts the setTimeout-based lifecycle settles
    - A throw-path test asserts dev throws / prod console.errors on a missing source or destination element
  </acceptance_criteria>
  <done>useActionAnimations.test.ts green across test-mode, the setTimeout real path, and the fail-loud throw.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composable → selector resolution | A missing source/destination element was a silent warn-then-no-op |
| test mode → production bundle | Instant/trace branch must never activate in a shipped game |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-01 | Tampering | useActionAnimations test-mode branch | mitigate | Gated by `isAnimationTestModeEnabled()` (default OFF); returns early without delegating to fly() |
| T-128-02 | Information disclosure | action trace contents | mitigate | Trace records interpolated selector strings + action name + args in meta; no hidden element attributes copied |
| T-128-03 | Denial of Service | warn→throw upgrade in production | mitigate | Throw gated by `isDevMode()`; prod path is console.error (not throw) reusing the existing message — a misconfigured selector never crashes a live game |
| T-128-SC | Tampering | package installs | accept | No package installs this phase |
</threat_model>

<verification>
- `npx vitest run src/ui/composables/useActionAnimations.test.ts` green
- Both former warn sites now dev-gated throws; test-mode returns early (no fly() double-record)
</verification>

<success_criteria>
useActionAnimations resolves instantly with an assertable action trace sourced from its own interpolated selector strings, fails loud (dev) at both former warn sites reusing the original messages, and has direct unit tests across all three paths.
</success_criteria>

<output>
Create `.planning/phases/128-animation-drag-drop-test-story/128-06-SUMMARY.md` when done.
</output>
