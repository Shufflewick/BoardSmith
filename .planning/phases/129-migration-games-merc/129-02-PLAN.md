---
phase: 129-migration-games-merc
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/BoardSmithGames/go-fish/package.json
  - ~/BoardSmithGames/go-fish/src/vite-env.d.ts
  - ~/BoardSmithGames/go-fish/tsconfig.json
  - ~/BoardSmithGames/go-fish/tests/no-hidden-info-dom-leak.test.ts
  - ~/BoardSmithGames/go-fish/tests/visibility.test.ts
  - ~/BoardSmithGames/cribbage/tests/visibility.test.ts
  - ~/BoardSmithGames/demo-animation/package.json
  - ~/BoardSmithGames/demo-animation/tests/animation-trace.test.ts
autonomous: false
requirements: [MIG-03]
user_setup: []

must_haves:
  truths:
    - "go-fish DOM-leak test proves opponent card identity never reaches the DOM when rendered as the opponent seat"
    - "go-fish DOM-leak test has a positive control: an injected leak makes the assertion fail"
    - "cribbage visibility test proves own-hand visible to owner, hidden from opponent, and crib hidden from both"
    - "demo-animation trace test proves a card's move records an animation trace (from/to containers)"
    - "go-fish, cribbage, demo-animation suites pass at >= baseline + new tests; all three tsc clean"
  artifacts:
    - path: "~/BoardSmithGames/go-fish/tests/no-hidden-info-dom-leak.test.ts"
      provides: "render-level hidden-info leak proof (flagship)"
      contains: "assertNoHiddenInfoLeak"
    - path: "~/BoardSmithGames/cribbage/tests/visibility.test.ts"
      provides: "hand/crib visibility assertions"
      contains: "assertHidden"
    - path: "~/BoardSmithGames/demo-animation/tests/animation-trace.test.ts"
      provides: "animation-trace assertion (showcase)"
      contains: "getAnimationTrace"
  key_links:
    - from: "go-fish DOM-leak test"
      to: "boardsmith/testing assertNoHiddenInfoLeak"
      via: "import + render as opponent seat"
      pattern: "assertNoHiddenInfoLeak"
    - from: "demo-animation trace test"
      to: "boardsmith/ui enableAnimationTestMode + getAnimationTrace"
      via: "test-mode capture"
      pattern: "getAnimationTrace"
---

<objective>
Adopt the v4.4 hidden-info + animation test utilities in the three flagship games: go-fish gets the render-level DOM-leak proof (the milestone's flagship: an agent-built card game proves in CI that opponent cards never leak into the DOM) plus TestGame-level visibility assertions; cribbage gets hand/crib visibility assertions; demo-animation gets one animation-trace test. Also closes go-fish's pre-existing tsc debt.

Purpose: MIG-03 meaningful (not blanket) adoption of the new utilities per CONTEXT.md's locked scope.
Output: New test files in three repos, go-fish devDeps + tsc fixes, all three suites green + tsc clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/129-migration-games-merc/129-CONTEXT.md
@.planning/phases/129-migration-games-merc/129-RESEARCH.md

Cross-repo note: go-fish, cribbage, demo-animation are SEPARATE git repos; `node_modules/boardsmith` symlinks to this repo (live v4.4). Commits land in each game's own repo.

<interfaces>
<!-- CORRECTED signatures (RESEARCH's code examples were wrong on arg order — use these, verified against src): -->
<!-- src/testing/dom-leak.ts -->
export async function assertNoHiddenInfoLeak<G extends Game>(testGame: TestGame<G>, seat: number, options?: AssertNoHiddenInfoLeakOptions): Promise<void>;
// options.allow?: (value, {attribute, elementId, elementLabel}) => boolean  (allowlist predicate)
// Requires `// @vitest-environment jsdom` as the FIRST line of the test file. Mounts the REAL AutoUI stack internally via @vue/test-utils; no component arg needed.
// Positive control: if you inject a forbidden marker into the rendered surface, it MUST throw.

<!-- src/testing/visibility.ts + src/testing/assertions.ts -->
export function isElementVisible(element: GameElement, seat: number): boolean;
export function assertHidden(element: GameElement, seat: number): void;   // NOTE: (element, seat) — NOT (testGame, element, seat)
export function assertVisible(element: GameElement, seat: number): void;

<!-- src/ui/composables/useAnimationTestMode.ts (re-exported from boardsmith/ui AND boardsmith/testing) -->
export function enableAnimationTestMode(): void;
export function disableAnimationTestMode(): void;
export function getAnimationTrace(): readonly AnimationTrace[];
export function clearAnimationTrace(): void;
export interface AnimationTrace { kind: 'flip'|'fly'|'element'|'action'; element: string|null; from: string|undefined; to: string|undefined; }

<!-- createTestGame from boardsmith/testing -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify jsdom + @vue/test-utils package legitimacy</name>
  <what-built>Nothing yet — this gate precedes the devDependency installs. RESEARCH's Package Legitimacy Audit marked both packages `[ASSUMED]` because slopcheck was unavailable in the environment; per the graceful-degradation protocol these installs require a blocking human verification (never auto-approvable, `workflow.auto_advance` ignored).</what-built>
  <how-to-verify>
    Confirm both packages are legitimate before install:
    1. Visit https://www.npmjs.com/package/jsdom — confirm it is the long-established DOM package (github.com/jsdom/jsdom), version ^29.1.1 exists, high weekly downloads.
    2. Visit https://www.npmjs.com/package/@vue/test-utils — confirm it is the official Vue org test-utils (github.com/vuejs/test-utils), version ^2.4.11 exists.
    3. Both versions match BoardSmith's own devDependencies (package.json: `@vue/test-utils": "^2.4.11"`, `jsdom": "^29.1.1"`).
    These were pre-approved in the 129-CONTEXT.md amendment ("devDependency approval") but the [ASSUMED] tag requires this explicit gate.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the go-fish (and demo-animation) devDep installs, or describe concerns.</resume-signal>
  <files>~/BoardSmithGames/go-fish/package.json, ~/BoardSmithGames/demo-animation/package.json</files>
  <action>Do NOT run any `npm install` until this gate is approved. Verify jsdom and @vue/test-utils legitimacy on npmjs.com per <how-to-verify> (both [ASSUMED] in RESEARCH's Package Legitimacy Audit because slopcheck was unavailable). This checkpoint is never auto-approvable — `workflow.auto_advance` is ignored per the package-legitimacy gate. Once approved, Tasks 2 and 3 may install the devDeps at the pinned versions.</action>
  <verify><human-check>Reviewer confirmed jsdom (github.com/jsdom/jsdom, ^29.1.1) and @vue/test-utils (github.com/vuejs/test-utils, ^2.4.11) on npmjs.com and approved the installs.</human-check></verify>
  <done>Human reviewer typed "approved" after verifying both packages on npmjs.com; devDep installs unblocked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: go-fish — devDeps, tsc debt, DOM-leak test (with positive control) + visibility test</name>
  <files>~/BoardSmithGames/go-fish/package.json, ~/BoardSmithGames/go-fish/tsconfig.json, ~/BoardSmithGames/go-fish/src/vite-env.d.ts, ~/BoardSmithGames/go-fish/tests/no-hidden-info-dom-leak.test.ts, ~/BoardSmithGames/go-fish/tests/visibility.test.ts</files>
  <read_first>
    - 129-RESEARCH.md "Code Examples → go-fish DOM-leak test" and "Standard Stack" (exact devDep versions + why they're a genuine gap) and tsc error detail for go-fish (4 errors)
    - ~/BoardSmithGames/go-fish/tests/no-hidden-info-leak.test.ts — the EXISTING broadcast-level test; name/structure the new DOM-leak test alongside it, using the same game-setup helpers
    - ~/BoardSmith/src/testing/dom-leak.ts:405 — the real `assertNoHiddenInfoLeak(testGame, seat, options)` signature + the `// @vitest-environment jsdom` requirement (RESEARCH assumption A2 flagged the arg shape — trust the src, not RESEARCH's example)
    - ~/BoardSmithGames/go-fish/src/rules/index.js — confirm `GoFishGame` / `GoFishPlayer` exports
  </read_first>
  <behavior>
    - DOM-leak test: rendering the game as the opponent seat (seat 2) shows opponent hand cards as backs only — no rank/suit identity appears in any DOM surface. `assertNoHiddenInfoLeak(testGame, 2)` resolves without throwing after driving a few turns so both hands hold cards.
    - Positive control: when a forbidden marker (an opponent card's rank/suit identity) is deliberately injected into the rendered surface (via the `gameViewOverride`/allow-inversion mechanism, OR a separate assertion that a known-hidden card's identity string is NOT present and would be present if leaked), the assertion throws. The test suite must contain an assertion that PROVES the matcher can fail — not just a passing happy path.
    - Visibility test: a player's own hand card is `isElementVisible(card, ownSeat) === true` and `assertHidden(card, opponentSeat)` passes.
  </behavior>
  <action>
    1. Install devDeps: `cd ~/BoardSmithGames/go-fish && npm install --save-dev jsdom@^29.1.1 @vue/test-utils@^2.4.11` (approved in Task 1 gate). Also add `@types/node` (or `"types": ["node"]`) to fix the `node:fs`/`node:url`/`node:path` TS2307 errors in the existing `tests/hint-target.test.ts`.
    2. Close remaining go-fish tsc debt: create `src/vite-env.d.ts` if missing (`/// <reference types="vite/client" />`); resolve the `tests/demo.test.ts:137` `GameDefinitionLike` construct-signature mismatch (TS2345) with a minimal type-correct fix (align the `gameClass`/options generic — read the call site; do NOT loosen to `any`). If this reveals a genuine BoardSmith `src/` defect, STOP and file a bug-fix request.
    3. Create `tests/no-hidden-info-dom-leak.test.ts` with `// @vitest-environment jsdom` as the FIRST line. Use `createTestGame(GoFishGame, {playerCount:2, playerNames:['Alice','Bob'], seed:'dom-leak-1'})`, drive a few turns so both hands hold cards, then `await assertNoHiddenInfoLeak(testGame, 2)`. Add the positive control per <behavior> — prove the matcher fails on an injected/known leak so the test cannot be a silent no-op.
    4. Create `tests/visibility.test.ts` (TestGame-level, no jsdom needed) asserting own-hand `isElementVisible`/`assertVisible` for the owner and `assertHidden` for the opponent, using the CORRECTED `(element, seat)` signatures.
  </action>
  <verify>
    <automated>cd ~/BoardSmithGames/go-fish && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` in go-fish reports >= 78 passed PLUS the new DOM-leak and visibility tests, zero failures
    - The DOM-leak test file contains a positive-control assertion that fails when a leak is present (verify by temporarily forcing the leak locally — it must throw; then revert)
    - `npx tsc --noEmit` in go-fish exits 0 (all 4 pre-existing errors resolved)
    - `package.json` devDependencies include `jsdom` and `@vue/test-utils` at the approved versions
    - `assertHidden`/`assertVisible`/`isElementVisible` called with `(element, seat)` — no testGame-first misuse
  </acceptance_criteria>
  <done>go-fish suite green with the flagship DOM-leak proof (positive control included) + visibility assertions; tsc clean; devDeps declared.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cribbage visibility test + demo-animation animation-trace test</name>
  <files>~/BoardSmithGames/cribbage/tests/visibility.test.ts, ~/BoardSmithGames/demo-animation/package.json, ~/BoardSmithGames/demo-animation/tests/animation-trace.test.ts</files>
  <read_first>
    - 129-RESEARCH.md "Code Examples → cribbage visibility assertions" and "→ demo-animation animation-trace test" (assumption A3: autoFlyUpUp is the simplest no-flip candidate)
    - ~/BoardSmith/src/ui/composables/useActionAnimations.test.ts — the canonical enableAnimationTestMode/getAnimationTrace/clearAnimationTrace usage pattern (waitFor trace length, toMatchObject on from/to)
    - ~/BoardSmithGames/cribbage tests + rules — locate the hand + crib element collections and the player seats
    - ~/BoardSmithGames/demo-animation/src/ui/components/ — the zone-a → zone-b useFlyingElements autoWatch setup; confirm demo-animation's current devDeps (add @vue/test-utils + jsdom only if the trace test mounts a component)
  </read_first>
  <behavior>
    - cribbage: each player's own hand card is visible to its owner (`assertVisible`/`isElementVisible` true) and hidden from the opponent (`assertHidden`); crib cards are hidden from BOTH players until the show phase.
    - demo-animation: enabling animation test mode and moving a card from zone-a to zone-b records an `AnimationTrace` with `kind:'fly'`, `from:'zone-a'`, `to:'zone-b'`; no flip trace for the autoFly path.
  </behavior>
  <action>
    1. cribbage: create `tests/visibility.test.ts` using `createTestGame(CribbageGame, {playerCount:2, seed:'vis-1'})`. Assert own-hand cards `assertVisible(card, ownerSeat)` + `assertHidden(card, opponentSeat)`, and crib cards `assertHidden(card, seat)` for both seats. Use CORRECTED `(element, seat)` signatures. No new devDeps (TestGame-level, no DOM mounting). cribbage tsc already PASSES — do not touch config.
    2. demo-animation: create `tests/animation-trace.test.ts` with `// @vitest-environment jsdom` if it mounts a component. `clearAnimationTrace()` + `enableAnimationTestMode()` in beforeEach, `disableAnimationTestMode()` in afterEach. Mount the zone board, trigger the autoFlyUpUp (zone-a → zone-b) move, then assert `getAnimationTrace()` contains an entry with `kind:'fly'`, `from:'zone-a'`, `to:'zone-b'` (use `waitFor` on trace length like the reference test). If the test mounts a Vue component, install `@vue/test-utils@^2.4.11` (and `jsdom@^29.1.1` if not already present) as devDeps — approved under Task 1's gate.
  </action>
  <verify>
    <automated>cd ~/BoardSmithGames/cribbage && npx vitest run && npx tsc --noEmit && cd ~/BoardSmithGames/demo-animation && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - cribbage: >= 20 passed PLUS the new visibility test, tsc exits 0
    - demo-animation: >= 8 passed PLUS the new animation-trace test, tsc exits 0
    - The trace test asserts concrete from/to container identities (zone-a → zone-b), not merely that some trace exists
    - Any demo-animation devDep additions match approved versions
  </acceptance_criteria>
  <done>cribbage + demo-animation suites green with the new visibility + animation-trace tests; both tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game engine → rendered DOM (opponent seat) | Hidden card identity must never cross into the DOM when rendering as a non-owner seat — the exact boundary the go-fish DOM-leak test guards |
| npm registry → go-fish/demo-animation devDeps | New devDependency installs (jsdom, @vue/test-utils) cross a supply-chain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-03 | Information disclosure | go-fish opponent-seat DOM render | mitigate | `assertNoHiddenInfoLeak(testGame, 2)` in CI; MUST include a positive control (injected leak throws) so the matcher cannot silently pass — a green test with no positive control gives false confidence |
| T-129-04 | Information disclosure | cribbage hand/crib visibility | mitigate | `assertHidden`/`assertVisible` on the final post-playerView tree; crib hidden from both until show phase |
| T-129-SC | Tampering | npm devDep installs (jsdom, @vue/test-utils) | mitigate | Task 1 blocking-human legitimacy checkpoint (npmjs.com verification) precedes every install; versions pinned to BoardSmith's own devDeps |
</threat_model>

<verification>
- go-fish: `npx vitest run` (>= 78 + new tests) + `npx tsc --noEmit` exit 0; DOM-leak positive control proven to fail on a leak.
- cribbage: `npx vitest run` (>= 20 + new) + tsc 0.
- demo-animation: `npx vitest run` (>= 8 + new) + tsc 0.
</verification>

<success_criteria>
Three flagship games adopt the exact utilities scoped in CONTEXT.md; go-fish DOM-leak test (with positive control), go-fish + cribbage visibility assertions, demo-animation trace test all pass; all three suites green at >= baseline + new tests; all three tsc clean; devDeps declared at approved versions.
</success_criteria>

<output>
Create `.planning/phases/129-migration-games-merc/129-02-SUMMARY.md` when done. Note which game repos were committed and record that the DOM-leak positive control was verified.
</output>
