---
phase: 129-migration-games-merc
plan: 03
type: execute
wave: 2
depends_on: [129-01, 129-02]
files_modified:
  - ~/Dropbox/MERC/BoardSmith/MERC/package.json
  - ~/Dropbox/MERC/BoardSmith/MERC/package-lock.json
autonomous: true
requirements: [MIG-03, MIG-04]

must_haves:
  truths:
    - "MERC is re-vendored against a freshly packed v4.4 BoardSmith tarball"
    - "MERC's full suite passes at >= 738 passed / 7 skipped"
    - "MERC's own breakage-surface grep sweep is clean"
    - "Final repo-wide grep sweep across all 9 repos returns zero breakage-surface hits"
    - "boardsmith simulate runs cleanly against hex and checkers"
  artifacts:
    - path: "~/Dropbox/MERC/BoardSmith/MERC/package.json"
      provides: "boardsmith dependency + overrides pointing at the new v4.4 tarball"
      contains: "boardsmith-0.0.1-"
  key_links:
    - from: "MERC package.json boardsmith ref"
      to: "freshly packed v4.4 tarball in MERC/vendor/"
      via: "file:./vendor/boardsmith-0.0.1-<new-timestamp>.tgz"
      pattern: "file:./vendor/boardsmith-0.0.1-"
---

<objective>
Re-vendor BoardSmith v4.4 into MERC (the vendored canary) per its established `3a81e81` re-vendor commit pattern, keep its suite green (>= 738/7), and run the final phase-wide verification: a repo-wide breakage-surface grep sweep across all 9 repos and a `boardsmith simulate` smoke run against hex + checkers.

This plan is Wave 2 — it packs the tarball AFTER Waves 1's game work is done, so if any Wave 1 task had surfaced a genuine BoardSmith `src/` gap (none expected per RESEARCH), the fix would already be in `src/` before the pack.

Purpose: MIG-04 (MERC re-vendored + green) and the MIG-03 phase-gate (all-repos grep sweep clean).
Output: MERC package.json/package-lock.json boardsmith-ref bump, MERC suite green, phase-wide grep sweep clean, simulate smoke clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/129-migration-games-merc/129-CONTEXT.md
@.planning/phases/129-migration-games-merc/129-RESEARCH.md

Cross-repo note: MERC at `~/Dropbox/MERC/BoardSmith/MERC` is a SEPARATE git repo and does NOT symlink BoardSmith — it uses a VENDORED tarball (`boardsmith pack` → .tgz → install). Commits land in MERC's own repo. `vendor/` is gitignored (never `git add` tarballs). The re-vendor commit touches only `package.json` + `package-lock.json`.

<interfaces>
<!-- Re-vendor procedure (exact, from 129-RESEARCH.md, extracted verbatim from commit 3a81e81; proven identical across 5 prior re-vendor commits): -->
<!-- 1. In BoardSmith: `npm pack` → produces boardsmith-0.0.1-<timestamp>.tgz -->
<!-- 2. cp boardsmith-0.0.1-*.tgz ~/Dropbox/MERC/BoardSmith/MERC/vendor/ -->
<!-- 3. In MERC package.json: update BOTH boardsmith refs (dependencies.boardsmith AND overrides.boardsmith) to file:./vendor/boardsmith-0.0.1-<new-timestamp>.tgz -->
<!-- 4. npm install (regenerates package-lock.json entry) -->
<!-- 5. npx vitest --run  → match/exceed 738 passed | 7 skipped (28 files) -->
<!-- 6. git add package.json package-lock.json (NOT vendor/) ; commit "chore: re-vendor boardsmith (v4.4 ...)" -->

<!-- CAVEAT (RESEARCH): MERC has PRE-EXISTING uncommitted WIP unrelated to this phase — a package.json version bump (0.0.27→0.0.28) and an AssignToSquadPanel.vue change. Per 121-SUMMARY precedent, do NOT touch/commit these. Stage only the boardsmith-ref lines. If `npm install` conflicts with the pending version bump, STOP and ask the user how to handle it. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-vendor BoardSmith v4.4 into MERC + green</name>
  <files>~/Dropbox/MERC/BoardSmith/MERC/package.json, ~/Dropbox/MERC/BoardSmith/MERC/package-lock.json</files>
  <read_first>
    - 129-RESEARCH.md "Pattern: MERC re-vendor commit (exact procedure, extracted from 3a81e81)" + the Caveat about pre-existing uncommitted MERC WIP + Pitfall 3 (MERC's LOCAL playUntilComplete helper is NOT the BoardSmith export)
    - ~/Dropbox/MERC/BoardSmith/MERC/package.json — the current `dependencies.boardsmith` + `overrides.boardsmith` tarball refs (currently v4.3 `boardsmith-0.0.1-20260701000512.tgz`)
    - .planning/milestones/v4.3-phases/121-game-merc-migration/121-SUMMARY.md — prior migration playbook + "left user's uncommitted change untouched" precedent
  </read_first>
  <action>
    1. In BoardSmith repo: `cd ~/BoardSmith && npm pack` → produces `boardsmith-0.0.1-<new-timestamp>.tgz`.
    2. Copy it into MERC's vendor dir: `cp ~/BoardSmith/boardsmith-0.0.1-*.tgz ~/Dropbox/MERC/BoardSmith/MERC/vendor/` (use the newly produced tarball's exact filename).
    3. In MERC `package.json`, update BOTH boardsmith refs — `dependencies.boardsmith` AND `overrides.boardsmith` — to `file:./vendor/boardsmith-0.0.1-<new-timestamp>.tgz`. Do NOT touch the pre-existing uncommitted version bump (0.0.27→0.0.28) or AssignToSquadPanel.vue WIP.
    4. `cd ~/Dropbox/MERC/BoardSmith/MERC && npm install` to regenerate the package-lock.json boardsmith entry. If npm install conflicts with the pending version bump, STOP and ask the user.
    5. Run MERC's suite: `npx vitest --run`. When auditing the breakage surface, confirm MERC's `playUntilComplete` is its OWN local helper (`tests/helpers/auto-play.ts`, imports only GameRunner) and NOT the BoardSmith export (Pitfall 3) — so no determinism-related behavior change applies.
    6. Grep-sweep MERC's own `src/` + `tests/` for breakage-surface hits: `grep -rn "headless-harness"` (zero), `grep -rn "\.shuffle("` (confirm all are Space.shuffle no-arg), `grep -rn "import.*playUntilComplete" tests/` (confirm it resolves to the local helper, NOT a boardsmith subpath).
    7. Commit in MERC's repo: `git add package.json package-lock.json` (NEVER add vendor/) + `git commit -m "chore: re-vendor boardsmith (v4.4 agent-ergonomics gaps: VIS/SIM/ERR/DRIVE/ANIM/FLOW + MIG)"`.
  </action>
  <verify>
    <automated>cd ~/Dropbox/MERC/BoardSmith/MERC && npx vitest --run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest --run` in MERC reports >= 738 passed | 7 skipped (28 files) — matches or exceeds the v4.3 baseline
    - MERC `package.json` `dependencies.boardsmith` AND `overrides.boardsmith` both point at the new `file:./vendor/boardsmith-0.0.1-<new-timestamp>.tgz`
    - MERC breakage-surface grep sweep: zero `headless-harness` hits, zero detached `ElementCollection.shuffle()` no-arg hits, `playUntilComplete` confirmed local (no boardsmith-subpath import)
    - Commit touches ONLY package.json + package-lock.json (no vendor/ tarball, no unrelated WIP files)
  </acceptance_criteria>
  <done>MERC re-vendored against v4.4, suite green at >= baseline, MERC grep sweep clean, re-vendor committed without touching pre-existing WIP.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-wide grep sweep (all 9 repos) + simulate smoke on hex/checkers</name>
  <files>(read-only verification — no files modified)</files>
  <read_first>
    - 129-RESEARCH.md "State of the Art" table (the four removed/changed API forms) + grep-sweep patterns + Pitfall 3
    - 129-CONTEXT.md Verification method (scripted grep sweep across all repos, zero hits required) + Specifics (simulate already smoke-tested go-fish in Phase 125; re-run on hex/checkers is cheap validation)
  </read_first>
  <action>
    1. Run the final repo-wide breakage-surface grep sweep across all 9 repos (8 games + MERC). For each repo, in `src/` and `tests/`: `grep -rn "headless-harness"` (expect zero everywhere), `grep -rn "\.shuffle("` (every hit must be `Space.shuffle()` no-arg in-game form — zero detached `ElementCollection.shuffle()` no-arg calls), `grep -rn "playUntilComplete"` (zero imports from any `boardsmith` subpath; MERC's local helper is exempt per Pitfall 3). Zero disallowed hits required across all 9 repos.
    2. Run `boardsmith simulate` smoke against hex and checkers (varied game shapes vs the go-fish run already done in Phase 125): from each repo run the `boardsmith simulate` CLI for a small seeded run and confirm it completes without error and produces a completed-game result. This validates the v4.4 simulate CLI against these game shapes.
  </action>
  <verify>
    <automated>for r in checkers cribbage demo-action-panel demo-animation demo-complex-ui go-fish hex polyhedral-potions; do echo "== $r =="; grep -rn "headless-harness" ~/BoardSmithGames/$r/src ~/BoardSmithGames/$r/tests 2>/dev/null; done; echo "== MERC =="; grep -rn "headless-harness" ~/Dropbox/MERC/BoardSmith/MERC/src ~/Dropbox/MERC/BoardSmith/MERC/tests 2>/dev/null; echo "SWEEP DONE (any output above headless-harness = FAIL)"</automated>
  </verify>
  <acceptance_criteria>
    - Repo-wide grep sweep across all 9 repos: zero `headless-harness` import hits, zero detached `ElementCollection.shuffle()` no-arg hits, zero `boardsmith`-subpath `playUntilComplete` imports (MERC local helper exempt)
    - `boardsmith simulate` completes without error against hex and against checkers (each produces a completed-game result)
  </acceptance_criteria>
  <done>Phase-wide grep sweep clean across all 9 repos; simulate smoke passes on hex + checkers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BoardSmith pack → MERC vendor | The vendored tarball is the exact v4.4 surface MERC consumes; a stale/mispacked tarball would silently test the wrong version |
| removed-API references (all repos) | Any lingering import of a removed API path is a broken-contract boundary the grep sweep guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-05 | Tampering | MERC re-vendor (wrong/stale tarball) | mitigate | Pack AFTER Wave 1 (this plan is Wave 2); verify both package.json refs point at the freshly timestamped tarball; suite green at >= baseline confirms the right version loaded |
| T-129-06 | Repudiation | phase-wide grep sweep completeness | mitigate | Sweep all 9 repos for all four removed/changed API forms; Pitfall 3 documents the one legitimate exemption (MERC's same-named local helper) so it is not mistaken for a hit |
</threat_model>

<verification>
- MERC: `npx vitest --run` >= 738/7; both package.json boardsmith refs updated; MERC grep sweep clean.
- Phase-wide: grep sweep across all 9 repos returns zero disallowed hits; `boardsmith simulate` completes on hex + checkers.
</verification>

<success_criteria>
MERC re-vendored against freshly packed v4.4 BoardSmith, suite green at >= baseline, re-vendor committed cleanly (no unrelated WIP); phase-wide grep sweep across all 9 repos clean; simulate smoke passes on hex + checkers.
</success_criteria>

<output>
Create `.planning/phases/129-migration-games-merc/129-03-SUMMARY.md` when done. Record the new tarball timestamp, MERC's final suite counts, and the phase-wide grep-sweep result.
</output>
