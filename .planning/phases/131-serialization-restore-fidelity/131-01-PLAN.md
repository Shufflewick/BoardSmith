---
phase: 131-serialization-restore-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/131-serialization-restore-fidelity/131-FINDINGS-VERIFICATION.md
autonomous: true
requirements: [PROC-01]
must_haves:
  truths:
    - "Each of F1, F2, F7, F8, F10, F15, F16 has a recorded verdict (LEGITIMATE or REJECTED) written before any fix task runs"
    - "Each LEGITIMATE verdict cites either a repro (how to reproduce the leak/loss) or a current file:line trace of the discarded/unfiltered state"
    - "Each REJECTED verdict states why the finding does not reproduce against current code"
    - "The stateless-ops.ts SEC-04 non-bug (Pitfall 1) is explicitly recorded as verified so no fix touches that file"
  artifacts:
    - path: ".planning/phases/131-serialization-restore-fidelity/131-FINDINGS-VERIFICATION.md"
      provides: "Per-finding verification verdicts gating all fix work"
      contains: "VERDICT:"
  key_links: []
---

<objective>
Produce the PROC-01 verification gate for Phase 131: an independent re-verification of all seven in-scope audit findings (F1, F2, F7, F8, F10, F15, F16) recorded in `131-FINDINGS-VERIFICATION.md`, each with a LEGITIMATE (repro or file:line trace) or REJECTED (reasoning) verdict — written BEFORE any fix is planned or written.

Purpose: PROC-01 is a hard, non-negotiable phase gate — no fix task in Plans 02-05 may run until this document records a verdict for every finding. This operationalizes the project's "Prove Before Fix" rule.
Output: `.planning/phases/131-serialization-restore-fidelity/131-FINDINGS-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-serialization-restore-fidelity/131-CONTEXT.md
@.planning/phases/131-serialization-restore-fidelity/131-RESEARCH.md
@.planning/phases/131-serialization-restore-fidelity/131-PATTERNS.md
@.planning/tmp/v4.5-audit-findings.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify engine-side findings (F1, F7, F2, F10) and record verdicts</name>
  <files>.planning/phases/131-serialization-restore-fidelity/131-FINDINGS-VERIFICATION.md</files>
  <read_first>
    - .planning/tmp/v4.5-audit-findings.json (original audit text + verdicts for F1, F2, F7, F10)
    - .planning/phases/131-serialization-restore-fidelity/131-RESEARCH.md (Architecture Patterns, Common Pitfalls, restore-path enumeration)
    - src/engine/element/space.ts (confirm `_zoneVisibility` in `unserializableAttributes` at lines 114-117, no `toJSON`/`fromJSON` override — F1/F7)
    - src/engine/element/game-element.ts (confirm `static visibleAttributes` declared line 140, read nowhere — F2; `_visibility` toJSON/fromJSON at 795 / 1026 as the template)
    - src/engine/element/game.ts (confirm `filterElement` at 2676, unfiltered fallthrough ~2790-2807; `loadSerializedState` discard at 2835-2857 — F10)
  </read_first>
  <action>Create `.planning/phases/131-serialization-restore-fidelity/131-FINDINGS-VERIFICATION.md`. For each engine-side finding record one section headed with the finding ID and a line containing the literal token `VERDICT:` followed by `LEGITIMATE` or `REJECTED`. Cover: F1/F7 (zone visibility `_zoneVisibility` unserialized — trace `space.ts:114-117` exclusion + absence of any Space `toJSON`/`fromJSON` and confirm no restore path exists); F2 (`static visibleAttributes` at `game-element.ts:140` read nowhere — confirm via `grep -rn "visibleAttributes" src/` that only the declaration and docs reference it, no consuming filter); F10 (`onEnter`/`onExit` handlers dropped — trace `loadSerializedState` `this._t.children = []` discard at `game.ts:2835-2857` destroying the constructor tree that holds `Space._eventHandlers`). Each LEGITIMATE verdict must include either concrete repro steps or the current file:line trace proving the defect exists in today's code. If any finding does not reproduce, record REJECTED with reasoning instead of inventing a fix. Do NOT write or plan any fix in this task — verdicts only.</action>
  <verify>
    <automated>grep -cE 'VERDICT:' .planning/phases/131-serialization-restore-fidelity/131-FINDINGS-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/131-serialization-restore-fidelity/131-FINDINGS-VERIFICATION.md` exists
    - Contains a section for each of F1, F7, F2, F10 with a `VERDICT:` line reading either `LEGITIMATE` or `REJECTED`
    - Each LEGITIMATE verdict cites a current file:line trace or explicit repro steps (not just a restatement of the audit)
    - `grep -n "visibleAttributes" src/engine/element/game.ts` result is discussed in the F2 section (confirming no consuming read exists pre-fix)
  </acceptance_criteria>
  <done>F1, F7, F2, F10 each have a recorded, evidence-backed verdict in 131-FINDINGS-VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify session-side findings (F8, F15, F16) and record verdicts</name>
  <files>.planning/phases/131-serialization-restore-fidelity/131-FINDINGS-VERIFICATION.md</files>
  <read_first>
    - .planning/tmp/v4.5-audit-findings.json (original audit text for F8, F15, F16)
    - .planning/phases/131-serialization-restore-fidelity/131-RESEARCH.md (Pitfall 1 — the stateless-ops.ts SEC-04 non-bug; `includeDebugData` call-site list)
    - src/session/utils.ts (confirm `buildPlayerState` raw `player.toJSON()` unfiltered second pass ~236-262; `if (options?.includeDebugData)` gate at 287 — F8, F15)
    - src/session/game-session.ts (confirm `includeDebugData` hardcoded true at 890, 903, 1386, 2120; `restore()` passes `undefined` for teachingDisabled/displayName — F15, F16)
    - src/session/stateless-ops.ts (confirm `buildViews`/`buildSpectatorView` never pass `includeDebugData` — the Pitfall 1 non-bug)
    - src/session/types.ts (confirm `StoredGameState` at 195+ has `aiConfig` at 222 but NO `teachingDisabled`/`displayName` field — F16)
    - src/session/pending-action-manager.ts, src/session/state-history.ts (confirm `includeDebugData: true` sites)
  </read_first>
  <action>Append to `131-FINDINGS-VERIFICATION.md` sections for F8, F15, F16, each with a `VERDICT:` line. Cover: F8 (`state.players` built from raw unfiltered `player.toJSON()` at `utils.ts:236-262`, bypassing all visibility — trace the parallel-to-`truthView` code path); F15 (`includeDebugData` hardcoded `true` — enumerate and confirm the exact current call sites: `game-session.ts:890,903,1386,2120`, `pending-action-manager.ts:226,250,355,357`, `state-history.ts:321,393`, and EXPLICITLY record that `stateless-ops.ts` `buildViews`/`buildSpectatorView` do NOT pass `includeDebugData` and are therefore already safe — no fix there, per Pitfall 1); F16 (`teachingDisabled`/`displayName` absent from `StoredGameState`, `restore()` passes `undefined` — trace `types.ts:195-242` missing field + the `restore()` constructor call). Record LEGITIMATE with the confirmed line numbers or REJECTED with reasoning. Verdicts only — no fixes.</action>
  <verify>
    <automated>test $(grep -cE 'VERDICT:' .planning/phases/131-serialization-restore-fidelity/131-FINDINGS-VERIFICATION.md) -eq 7</automated>
  </verify>
  <acceptance_criteria>
    - The document now contains exactly 7 `VERDICT:` lines total (F1, F7, F2, F10, F8, F15, F16)
    - The F15 section explicitly lists the confirmed `includeDebugData: true` call sites AND states that `stateless-ops.ts` requires no change (Pitfall 1)
    - The F16 section confirms `StoredGameState` lacks `teachingDisabled` and `displayName` today and that `restore()` passes `undefined` for both
    - No fix code was written in this plan
  </acceptance_criteria>
  <done>All seven findings (F1, F2, F7, F8, F10, F15, F16) carry evidence-backed verdicts; the PROC-01 gate is satisfied and fix plans may proceed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game engine state → broadcast payload to seats/spectators | Per-viewer redaction must hold; findings are leaks across this boundary |
| live game instance → snapshot → restored instance | Constructor-applied config (visibility, handlers, lockouts) must survive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-01 | Information Disclosure | All 7 findings | mitigate | This plan records a verified verdict per finding BEFORE any fix, ensuring no fix is written against an unproven root cause (Prove Before Fix) |
</threat_model>

<verification>
`grep -cE 'VERDICT:' .planning/phases/131-serialization-restore-fidelity/131-FINDINGS-VERIFICATION.md` returns 7. Each finding section cites repro or file:line evidence. No production code modified in this plan.
</verification>

<success_criteria>
131-FINDINGS-VERIFICATION.md exists with 7 evidence-backed verdicts, written before any fix. Fix plans 02-05 are unblocked.
</success_criteria>

<output>
Create `.planning/phases/131-serialization-restore-fidelity/131-01-SUMMARY.md` when done.
</output>
