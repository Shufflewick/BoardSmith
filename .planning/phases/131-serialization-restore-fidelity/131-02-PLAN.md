---
phase: 131-serialization-restore-fidelity
plan: 02
type: execute
wave: 2
depends_on: ["131-01"]
files_modified:
  - src/engine/element/space.ts
  - src/engine/element/game-element.ts
  - src/engine/element/types.ts
  - src/session/stateless-ops.ts
  - src/engine/element/zone-visibility-restore.test.ts
autonomous: true
requirements: [SEC-01, PROC-02]
must_haves:
  truths:
    - "A zone hidden via contentsHidden/contentsVisibleToOwner/contentsCountOnly stays hidden to the correct viewers after every restore path"
    - "toJSONForPlayer(opponent) is byte-identical (JSON.stringify strict equality) before and after each restore path"
    - "Zone visibility changed at runtime mid-game (e.g. re-hide) also survives restore"
    - "There is exactly one mechanism for zone-visibility-across-restore — the stateless-ops buildSpectatorView workaround is removed"
  artifacts:
    - path: "src/engine/element/space.ts"
      provides: "_zoneVisibility serialization via toJSON override"
      contains: "zoneVisibility"
    - path: "src/engine/element/types.ts"
      provides: "ElementJSON.zoneVisibility field"
      contains: "zoneVisibility"
    - path: "src/engine/element/zone-visibility-restore.test.ts"
      provides: "Red-first regression test across all restore paths"
      contains: "toJSONForPlayer"
  key_links:
    - from: "src/engine/element/space.ts toJSON"
      to: "src/engine/element/game-element.ts fromJSON"
      via: "json.zoneVisibility round-trip"
      pattern: "zoneVisibility"
    - from: "src/session/stateless-ops.ts buildSpectatorView"
      to: "serialized _zoneVisibility"
      via: "removal of manual zone-hiding workaround"
      pattern: "zone"
---

<objective>
Fix SEC-01 (F1/F7): serialize `Space._zoneVisibility` so hidden-zone visibility survives every snapshot restore path, exactly mirroring how element-level `_visibility` already round-trips. Remove the now-redundant zone-visibility workaround in `stateless-ops.ts` so there is a single authoritative mechanism.

Purpose: This is the flagship finding — today zone visibility is discarded on every restore, silently over-revealing hidden hands/decks to opponents after any undo, rewind, or session restore. Information Disclosure.
Output: Serialized/restored `_zoneVisibility`, `ElementJSON.zoneVisibility` type field, unified spectator view, and a regression test asserting byte-identity across all 5 restore paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-serialization-restore-fidelity/131-CONTEXT.md
@.planning/phases/131-serialization-restore-fidelity/131-PATTERNS.md
@.planning/phases/131-serialization-restore-fidelity/131-FINDINGS-VERIFICATION.md

<interfaces>
From src/engine/element/game-element.ts (the exact template to mirror):
```typescript
// toJSON (~line 795) — explicit-only emission
if (this._visibility?.explicit) {
  json.visibility = this._visibility;
}
// fromJSON static (~line 1026) — restore
if (json.visibility) {
  element._visibility = json.visibility;
}
static visibleAttributes: string[] | undefined; // line 140
```
From src/engine/element/types.ts (line 88-93):
```typescript
export type ElementJSON = {
  // ...
  visibility?: VisibilityState;
};
```
From src/engine/element/space.ts:
```typescript
private _zoneVisibility?: VisibilityState;        // line 112
static override unserializableAttributes = [      // line 114
  ...GameElement.unserializableAttributes,
  '_eventHandlers',
  '_zoneVisibility',
];
// contentsHidden() etc set this._zoneVisibility with explicit:true (visibilityFromMode)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Red-first regression test — zone visibility across all restore paths</name>
  <files>src/engine/element/zone-visibility-restore.test.ts</files>
  <read_first>
    - src/engine/element/deck-hand-visibility.test.ts (exact suite style + game setup for hidden zones — the structural template)
    - src/session/tutorial-serialization.test.ts (byte-identity snapshot→fromSnapshot assertion at ~line 120)
    - src/session/restore-snapshot-authoritative.test.ts (session-layer GameSession.restore / undo / time-travel patterns)
    - .planning/phases/131-serialization-restore-fidelity/131-RESEARCH.md (Code Examples — "Verified restore-path enumeration": all 5 paths funnel through GameRunner.fromSnapshot at runner.ts:535-598)
    - src/runtime/runner.ts (fromSnapshot / fromCheckpoint signatures)
  </read_first>
  <behavior>
    - A Deck/Hand made hidden (contentsHidden / contentsVisibleToOwner / contentsCountOnly) reports `__hidden` (or count-only) contents for a non-owner via `game.toJSONForPlayer(opponentSeat)`.
    - After `GameRunner.fromSnapshot`, `JSON.stringify(restored.toJSONForPlayer(opponentSeat))` strictly equals the pre-restore value.
    - Same byte-identity assertion holds after: undo (StateHistory), rewind/time-travel (getStateAtAction), GameSession.restore, and the stateless-ops path.
    - A zone whose visibility is CHANGED at runtime mid-game (re-hidden after being shown) also survives the round-trip.
  </behavior>
  <action>Create `src/engine/element/zone-visibility-restore.test.ts` following the `deck-hand-visibility.test.ts` suite shape. Each `it()` sets up a game with a hidden zone, captures `JSON.stringify(game.toJSONForPlayer(opponentSeat))`, runs the game through one restore path, then asserts strict equality of `JSON.stringify(restored.toJSONForPlayer(opponentSeat))`. Cover all five restore paths enumerated in RESEARCH (GameRunner.fromSnapshot as the primitive; plus undo, rewind, GameSession.restore, stateless ops where a Game-level round trip is exercisable at the engine layer — use fromSnapshot round-trips for the engine-only paths and defer the session-driven paths to their integration equivalent if a GameSession harness is not available in an engine-unit test, but at minimum cover fromSnapshot + a runtime-changed-visibility case). Also add the mid-game visibility-change case. This test MUST FAIL on current code (zone visibility is not serialized). Run it and record the RED failure output in the plan SUMMARY per PROC-02.</action>
  <verify>
    <automated>npx vitest run src/engine/element/zone-visibility-restore.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/engine/element/zone-visibility-restore.test.ts` exists
    - Running the file FAILS before Task 2 (the RED state) — failure output captured for the SUMMARY
    - At least one `it()` asserts `JSON.stringify(...toJSONForPlayer(opponent))` strict equality across a fromSnapshot round-trip
    - A distinct `it()` covers zone visibility changed at runtime mid-game
  </acceptance_criteria>
  <done>Failing regression test committed (RED), covering byte-identity of opponent view across restore, including runtime visibility change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serialize/restore _zoneVisibility and unify the spectator-view workaround</name>
  <files>src/engine/element/space.ts, src/engine/element/game-element.ts, src/engine/element/types.ts, src/session/stateless-ops.ts</files>
  <read_first>
    - src/engine/element/space.ts (lines 106-230: `_zoneVisibility`, `unserializableAttributes`, `contentsHidden`/`contentsVisibleToOwner`/`contentsCountOnly`, `_eventHandlers` — do NOT remove `_eventHandlers` from the exclusion list)
    - src/engine/element/game-element.ts (lines 780-830 toJSON; 1000-1040 static fromJSON — the `_visibility` special-case at 795 / 1026 is the exact template; also find the `new ElementClass(ctx)` construction point in fromJSON where a Space-specific restore hook can run)
    - src/engine/element/types.ts (ElementJSON at 88-93; add `zoneVisibility?: VisibilityState` alongside `visibility`)
    - src/session/stateless-ops.ts (locate the `buildSpectatorView` zone-visibility workaround via `grep -n "zone" src/session/stateless-ops.ts` — the comment documents it as a stopgap because zone visibility was not serialized)
    - src/engine/element/zone-visibility-restore.test.ts (the RED test this task must turn GREEN)
  </read_first>
  <action>Implement zone-visibility serialization mirroring `_visibility` exactly (per D-SEC-01 in CONTEXT.md):
1. In `src/engine/element/types.ts`, add `zoneVisibility?: VisibilityState;` to `ElementJSON` next to the existing `visibility?: VisibilityState` field.
2. In `src/engine/element/space.ts`, add a `Space.toJSON()` override that calls `super.toJSON()` and, when `this._zoneVisibility` is set (guard on `this._zoneVisibility?.explicit`, matching the `_visibility.explicit` precedent — `contentsHidden()` et al already set `explicit: true` via `visibilityFromMode`), sets `json.zoneVisibility = this._zoneVisibility`. Remove `'_zoneVisibility'` from `unserializableAttributes` per CONTEXT.md's explicit instruction (the `_`-prefix still excludes it from the generic attribute loop, exactly as `_visibility` is handled). Keep `'_eventHandlers'` in the exclusion list (closures — handled by Plan 05).
3. Restore in the deserialization path: in `GameElement.fromJSON` (game-element.ts), after the element is constructed, if the element `instanceof Space` and `json.zoneVisibility` is present, assign it back to the space's `_zoneVisibility`. Since `_zoneVisibility` is private on `Space`, add a scoped internal accessor/method on `Space` (mirror the codebase's `_t` cross-class-internal convention) rather than making the field public — do NOT widen the public API surface.
4. In `src/session/stateless-ops.ts`, remove the now-redundant `buildSpectatorView` zone-visibility workaround (and its stopgap comment) so serialization is the single mechanism. Do NOT change `includeDebugData` handling here (Pitfall 1 — out of scope for this plan).
Turn the Task 1 test GREEN. Do NOT introduce a parallel Space-specific storage side-channel — use only the `toJSON`/`fromJSON` round-trip.</action>
  <verify>
    <automated>npx vitest run src/engine/element/zone-visibility-restore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/engine/element/types.ts` declares `zoneVisibility?: VisibilityState` on `ElementJSON`
    - `grep -n "zoneVisibility" src/engine/element/space.ts` shows both the toJSON emission and the restore accessor
    - `'_zoneVisibility'` no longer appears in `Space.unserializableAttributes`; `'_eventHandlers'` still does
    - The `buildSpectatorView` zone-visibility workaround/comment is gone from `src/session/stateless-ops.ts` (verify with `grep -n "not serialized\|zone" src/session/stateless-ops.ts`)
    - `npx vitest run src/engine/element/zone-visibility-restore.test.ts` passes (GREEN)
    - Full engine visibility suite still green: `npx vitest run src/engine/element/deck-hand-visibility.test.ts`
  </acceptance_criteria>
  <done>Zone visibility survives all tested restore paths byte-identically; spectator-view workaround removed; one authoritative mechanism.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live game → snapshot → restored game | `_zoneVisibility` must survive the round-trip |
| restored game → toJSONForPlayer(opponent) | Hidden zone contents must not leak to non-owners after restore |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-02 | Information Disclosure | `Space._zoneVisibility` discarded on restore (F1/F7) | mitigate | Serialize `_zoneVisibility` via toJSON/fromJSON like `_visibility`; regression test asserts `JSON.stringify(toJSONForPlayer(opponent))` byte-identity across all restore paths |
| T-131-02b | Information Disclosure | Divergent spectator-view workaround masking the real gap | mitigate | Remove the `buildSpectatorView` stopgap so serialization is the single source of truth (no two divergent redaction paths) |
</threat_model>

<verification>
`npx vitest run src/engine/element/zone-visibility-restore.test.ts` passes. RED-then-GREEN recorded in SUMMARY (PROC-02). `grep -n zoneVisibility src/engine/element/space.ts src/engine/element/types.ts` shows the round-trip. Zone-visibility workaround removed from stateless-ops.ts. Full suite `npm run test` green before wave merge.
</verification>

<success_criteria>
A hidden zone remains hidden to the correct viewers after restore, with `toJSONForPlayer(opponent)` byte-identical before/after; single serialization mechanism; regression test proven red-then-green.
</success_criteria>

<output>
Create `.planning/phases/131-serialization-restore-fidelity/131-02-SUMMARY.md` when done, recording the RED failure output before the fix.
</output>
