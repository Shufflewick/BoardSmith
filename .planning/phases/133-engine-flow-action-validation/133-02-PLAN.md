---
phase: 133-engine-flow-action-validation
plan: 02
type: execute
wave: 2
depends_on: ["133-01"]
files_modified:
  - src/engine/flow/engine.ts
  - src/engine/flow/engine.test.ts
  - src/engine/flow/turn-order.ts
  - docs/common-patterns.md
autonomous: true
requirements: [ENG-02, PROC-02]
must_haves:
  truths:
    - "eachPlayer with a non-zero startingPlayer visits ALL players in one round, in wrapped order [start..end, 0..start-1]"
    - "eachPlayer with no startingPlayer (startIndex === 0) is unchanged — the wrap formula degenerates to the full original list with no special-casing"
    - "TurnOrder preset JSDoc (module note, CONTINUE, START_FROM) no longer claims eachPlayer does NOT wrap around; the stale 'manually structure your flow' workaround text is removed"
    - "docs/common-patterns.md dealer rotation example is correct as written under the wrap fix and contains no 'does not wrap' caveat"
  artifacts:
    - path: "src/engine/flow/engine.ts"
      provides: "executeEachPlayer eligibleSeats wrap-around construction"
      contains: "slice(0, startIndex)"
    - path: "src/engine/flow/engine.test.ts"
      provides: "Red-first regression test for ENG-02 in the EachPlayer Execution describe block"
      contains: "startingPlayer"
  key_links:
    - from: "src/engine/flow/engine.ts executeEachPlayer"
      to: "frame.data.eligibleSeats iteration loop"
      via: "opaque ordered seat list consumed unchanged downstream"
      pattern: "eligibleSeats"
---

<objective>
Implement ENG-02 (F4, critical): `eachPlayer` with a `startingPlayer` wraps around the full player list so every player gets a turn that round, instead of truncating at the end of the list and silently skipping seats before `startIndex`. Correct the now-false TurnOrder preset docs and the dealer-rotation pattern in the same phase.

Purpose: Truncation was never a sane board-game semantic — a dealer-relative round must visit everyone. Per the locked decision the wrap is ALWAYS on with no `wrap: false` opt-out. The fix is a single array-concat expression at the one chokepoint (`executeEachPlayer`), which every `eachPlayer` caller — including `TurnOrder.LEFT_OF_DEALER`/`START_FROM`/`CONTINUE` — benefits from with zero call-site changes.
Output: The wrap fix in `executeEachPlayer`, corrected preset/pattern docs, and a red-first regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-engine-flow-action-validation/133-CONTEXT.md
@.planning/phases/133-engine-flow-action-validation/133-RESEARCH.md
@.planning/phases/133-engine-flow-action-validation/133-PATTERNS.md
@.planning/phases/133-engine-flow-action-validation/133-FINDINGS-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — regression test for eachPlayer startingPlayer wrap-around</name>
  <files>src/engine/flow/engine.test.ts</files>
  <read_first>
    - src/engine/flow/engine.test.ts (the existing `describe('EachPlayer Execution', ...)` block ~line 303 — reuse its defineFlow + FlowEngine + visitedPlayers-push setup used by the 'should iterate through all players' / 'should iterate backward' tests ~304-356)
    - src/engine/flow/engine.ts (executeEachPlayer ~1100-1160 so the test drives the real startingPlayer config path)
    - .planning/phases/133-engine-flow-action-validation/133-FINDINGS-VERIFICATION.md (F4 verdict — confirmed truncation shape to assert against)
  </read_first>
  <behavior>
    - Test A: a game with 4 players and `eachPlayer({ startingPlayer: () => seat3Player })` drives all four turns and records visited seats in order [3, 4, 1, 2] (full wrap), NOT [3, 4] (truncated).
    - Test B (control): `eachPlayer` with no `startingPlayer` still visits all players in natural order [1, 2, 3, 4], proving the fix is a no-op for the common case.
  </behavior>
  <action>Add a nested block (e.g. `describe('ENG-02 startingPlayer wrap-around')`) inside the existing `EachPlayer Execution` describe in `engine.test.ts`. Build a 4-player flow, push each visited seat into an array as the flow iterates, and assert Test A's visited order is `[3, 4, 1, 2]` (wrapped) via a deep-equal assertion. Add Test B as a control asserting the no-startingPlayer path yields `[1, 2, 3, 4]`. Run the file and record the RED failure output (Test A currently yields the truncated `[3, 4]`) for the SUMMARY per PROC-02. Do NOT implement the fix in this task.</action>
  <verify>
    <automated>npx vitest run src/engine/flow/engine.test.ts -t "EachPlayer" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - New test cases exist inside the `EachPlayer Execution` describe block covering a non-zero startingPlayer wrap and a no-startingPlayer control
    - Running the file shows Test A FAILING (visited order is the truncated `[3, 4]`, not `[3, 4, 1, 2]`) — RED output captured for the SUMMARY
    - Test B (control) passes both before and after the fix
  </acceptance_criteria>
  <done>Red-first test exists and fails against the unpatched truncating `executeEachPlayer`; RED output recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — eligibleSeats wrap fix + preset/pattern doc corrections</name>
  <files>src/engine/flow/engine.ts, src/engine/flow/turn-order.ts, docs/common-patterns.md</files>
  <read_first>
    - src/engine/flow/engine.ts (the full `executeEachPlayer` eligibleSeats block ~1107-1130, specifically the `eligibleSeats: players.slice(startIndex).map(p => p.seat)` line ~1127)
    - src/engine/flow/turn-order.ts (module-level "does NOT wrap around" JSDoc ~25-27; the same claim repeated near CONTINUE ~45-49 and START_FROM ~64-69; leave LEFT_OF_DEALER's nextAfter dealer-advance example intact — it is a different concern)
    - docs/common-patterns.md (the Dealer Rotation section ~1-64 using `eachPlayer({ startingPlayer: () => game.playerAfterDealer })`; scan the whole file for any 'does not wrap'/'skips players' caveat before closing DOCX-04)
    - .planning/phases/133-engine-flow-action-validation/133-PATTERNS.md (Pattern ENG-02 — the exact one-line replacement and the slice degenerate-case reasoning)
  </read_first>
  <action>Change the single `eligibleSeats` expression in `executeEachPlayer` to `[...players.slice(startIndex), ...players.slice(0, startIndex)].map(p => p.seat)` (per ENG-02, the wrap is unconditional — do NOT add a `startIndex === 0` special case; `slice(0)` + `slice(0,0)` already degenerates to the full array, per RESEARCH Pitfall 1). Change no other line in the block; the downstream `nextIndex` iteration treats `eligibleSeats` as an opaque ordered list and is untouched. Then correct the stale docs (DOCX-04, same phase): in `turn-order.ts`, update the module-level note, the `CONTINUE` doc, and the `START_FROM` doc to state that `eachPlayer`/these presets now wrap around the full player list starting from the given player, and delete the now-inaccurate "does NOT wrap around" / "manually structure your flow" workaround text; leave `LEFT_OF_DEALER`'s nextAfter example alone. In `docs/common-patterns.md`, ensure the Dealer Rotation prose contains no "does not wrap"/"skips players seated before the dealer" caveat (the code sample is already correct under the fix); remove or correct any such caveat if present.</action>
  <verify>
    <automated>npx vitest run src/engine/flow/engine.test.ts src/engine/action/action.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `executeEachPlayer` builds `eligibleSeats` via the wrap concat `[...players.slice(startIndex), ...players.slice(0, startIndex)]`; no `startIndex === 0` branch was added
    - `grep -n "slice(0, startIndex)" src/engine/flow/engine.ts` matches the new expression
    - Both ENG-02 tests pass (wrapped order `[3, 4, 1, 2]` and control `[1, 2, 3, 4]`)
    - `grep -in "does not wrap\|does NOT wrap" src/engine/flow/turn-order.ts docs/common-patterns.md` returns no matches (stale caveat removed)
    - Targeted engine + action suites green
  </acceptance_criteria>
  <done>eachPlayer wraps around unconditionally; TurnOrder preset docs and the dealer-rotation pattern match the new behavior; regression test red-then-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flow turn-order state → per-player turn eligibility | executeEachPlayer decides which seats act this round; a truncated list silently disenfranchises players |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-02 | Tampering (turn-order integrity) | executeEachPlayer eligibleSeats | mitigate | Unconditional wrap-around construction guarantees every eligible seat is scheduled; regression test asserts the wrapped order for a non-zero startingPlayer |
| T-133-SD | Info Disclosure / Elevation | engine flow internals | accept | Pure server-side flow-control; no auth/session/hidden-information boundary crossed (133-RESEARCH.md Security Domain) |
</threat_model>

<verification>
`npx vitest run src/engine/flow/engine.test.ts src/engine/action/action.test.ts` green. RED output from Task 1 recorded in SUMMARY. Full suite (`npm test`) at wave close must stay green (168 files / 2148+ tests baseline).
</verification>

<success_criteria>
ENG-02 satisfied: eachPlayer with startingPlayer wraps so every player gets a turn; preset docs + dealer pattern corrected; regression test red-then-green; no wrap opt-out added.
</success_criteria>

<output>
Create `.planning/phases/133-engine-flow-action-validation/133-02-SUMMARY.md` when done.
</output>
