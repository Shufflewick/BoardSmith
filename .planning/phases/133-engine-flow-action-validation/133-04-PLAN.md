---
phase: 133-engine-flow-action-validation
plan: 04
type: execute
wave: 2
depends_on: ["133-01"]
files_modified:
  - src/engine/action/action.ts
  - src/engine/action/action.test.ts
autonomous: true
requirements: [ENG-04, PROC-02]
must_haves:
  truths:
    - "A chooseFrom multiSelect submission with too few items fails validation with an 'at least N' error naming the selection"
    - "A chooseFrom multiSelect submission with too many items fails validation with an 'at most N' error naming the selection"
    - "A non-array value submitted to a multiSelect-configured choice selection is REJECTED with an 'expected an array' error (not silently degraded to count=1)"
    - "Function-valued multiSelect on the choice branch is resolved (called with context) before enforcing min/max, matching the elements branch"
    - "A valid in-bounds array submission to a multiSelect choice still passes; a non-multiSelect choice selection is unaffected"
  artifacts:
    - path: "src/engine/action/action.ts"
      provides: "choice-branch multiSelect count + array-type enforcement in validateSelection"
      contains: "multiSelect"
    - path: "src/engine/action/action.test.ts"
      provides: "Red-first regression tests for ENG-04 in the validateSelection describe block"
      contains: "chooseFrom"
  key_links:
    - from: "src/engine/action/action.ts validateSelection choice branch"
      to: "ChoiceSelection.multiSelect config"
      via: "resolve function-valued config then enforce min/max + array-type"
      pattern: "multiSelect"
---

<objective>
Implement ENG-04 (F6): the choice-type branch of `validateSelection` enforces `chooseFrom` `multiSelect` min/max count server-side (porting the already-tested elements-branch logic) AND explicitly rejects non-array values when `multiSelect` is configured — the port alone is insufficient because the elements branch degrades a scalar to count=1, which the locked decision explicitly disallows here.

Purpose: Today the choice branch validates array-item membership only; `ChoiceSelection.multiSelect` bounds are shipped to the client as a UX convenience but never enforced server-side, so a non-conforming (or malicious) client can submit out-of-bounds or wrong-shaped selections. This closes the server-side gap. `ChoiceSelection.multiSelect` is structurally identical to the enforced `ElementsSelection.multiSelect`.
Output: The choice-branch count + array-type enforcement in `validateSelection` plus red-first regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-engine-flow-action-validation/133-CONTEXT.md
@.planning/phases/133-engine-flow-action-validation/133-RESEARCH.md
@.planning/phases/133-engine-flow-action-validation/133-PATTERNS.md
@.planning/phases/133-engine-flow-action-validation/133-FINDINGS-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — regression tests for choice-branch multiSelect enforcement</name>
  <files>src/engine/action/action.test.ts</files>
  <read_first>
    - src/engine/action/action.test.ts (the `describe('validateSelection', ...)` block ~line 254 and the F31 elements-branch multiSelect tests ~2223-2320 in `describe('Element selection API (F23/F28)', ...)` ~2162 — copy their shape, swapping chooseElements for chooseFrom)
    - src/engine/action/action-builder.ts (`chooseFrom` signature ~195-260 and how a choice-type multiSelect config is declared, so the tests configure the real API)
    - src/engine/action/action.ts (the choice branch ~706-741 and the elements-branch enforcement ~802-817 being ported)
    - .planning/phases/133-engine-flow-action-validation/133-FINDINGS-VERIFICATION.md (F6 verdict — confirmed no count/array-type check exists on the choice branch)
  </read_first>
  <behavior>
    - Test A (too few): a `chooseFrom` with `multiSelect: {min:2}` rejects a submission of 1 item with an 'at least 2 choices' error naming the selection.
    - Test B (too many): a `chooseFrom` with `multiSelect: {max:2}` rejects a submission of 3 items with an 'at most 2 choices' error.
    - Test C (number form): `multiSelect: 2` (number = max, implicit min 1) enforces min 1 / max 2.
    - Test D (function form): `multiSelect: () => ({min:1, max:1})` is resolved and enforced.
    - Test E (non-array rejection): submitting a bare scalar (e.g. `'red'` instead of `['red']`) to a multiSelect choice is REJECTED with an 'expected an array' error — the case with NO elements-branch equivalent.
    - Test F (control): a valid in-bounds array submission passes.
  </behavior>
  <action>Add tests inside the `validateSelection` describe (sibling to the F31 elements-branch multiSelect tests) covering Tests A–F above for `chooseFrom` choice selections. Match the assertion style of the existing elements-branch tests (`expect(errors).toContain(...)` / regex on the error strings). Test E has no elements-branch analog to copy — write it fresh asserting the array-type rejection. Run the file and record the RED failure output (all count/array-type cases currently pass validation when they should fail) for the SUMMARY per PROC-02. Do NOT implement the fix in this task.</action>
  <verify>
    <automated>npx vitest run src/engine/action/action.test.ts -t "validateSelection" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - New tests cover too-few, too-many, number-form, function-form, non-array rejection, and an in-bounds control for chooseFrom multiSelect
    - Running the file shows the count + array-type cases FAILING against unpatched code (no server-side enforcement yet) — RED output captured for the SUMMARY
    - The in-bounds control passes both before and after the fix
  </acceptance_criteria>
  <done>Red-first tests exist and fail against the unenforced choice branch; RED output recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — choice-branch multiSelect count + array-type enforcement</name>
  <files>src/engine/action/action.ts</files>
  <read_first>
    - src/engine/action/action.ts (the choice branch `if (selection.type === 'choice' || selection.type === 'element')` ~706-741 and its Array.isArray/else structure; the elements-branch port source ~802-817; confirm `ChoiceSelection` is already imported/used elsewhere in this file, e.g. ~362, ~1097)
    - src/engine/action/types.ts (`ChoiceSelection.multiSelect` shape — identical to ElementsSelection.multiSelect)
    - .planning/phases/133-engine-flow-action-validation/133-PATTERNS.md (the ENG-04 fix block: resolve function-valued config, reject non-array outright, else enforce min/max with 'choice' wording)
    - .planning/phases/133-engine-flow-action-validation/133-RESEARCH.md (Pattern 3 + Pitfall 3 — non-array rejection is NEW logic, stricter than the elements branch)
  </read_first>
  <action>Insert multiSelect enforcement in the choice branch after the existing `if (Array.isArray(value)) {...} else {...}` membership block, still inside the `if (selection.type === 'choice' || selection.type === 'element')` guard. Resolve the config via `(selection as ChoiceSelection).multiSelect`, calling it with `context` if it is a function. When the resolved config is defined: if `value` is NOT an array, push an actionable error naming the selection and stating a multiSelect selection expects an array (report the received `typeof`/JSON value); otherwise compute `min` (number form → 1, else `min ?? 1`), `max` (number form → the number, else `.max`), and `count = value.length`, and push 'requires at least N choice(s)' / 'requires at most N choice(s)' errors naming the selection, mirroring the elements-branch wording with 'choice' instead of 'element'. Claude's Discretion (RESEARCH-recommended): if it reads cleanly, extract a shared `enforceMultiSelectCount(value, multiSelectConfig, selectionName, unitLabel, errors)` helper used by BOTH this choice block and the elements branch (~802-817), passing `'element'`/`'choice'` as the unit label and adding the array-type rejection only for the choice path — dedupe only if it stays clean, otherwise keep the block inline. Do not change the membership-check logic already present.</action>
  <verify>
    <automated>npx vitest run src/engine/action/action.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The choice branch resolves `(selection as ChoiceSelection).multiSelect` (calling function form with context) and enforces min/max on `value.length`
    - A non-array value with multiSelect configured is rejected with an 'expected an array' error — NOT silently accepted as count=1
    - All six ENG-04 tests pass (too-few, too-many, number form, function form, non-array rejection, in-bounds control)
    - If a shared helper was extracted, the elements-branch tests (F31, ~2223-2320) still pass unchanged
    - `action.test.ts` green
  </acceptance_criteria>
  <done>chooseFrom multiSelect min/max + array-type are enforced server-side; non-array submissions are rejected; elements-branch behavior preserved; regression tests red-then-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-submitted action args → server-side validateSelection | multiSelect bounds shipped to the client are a UX convenience; validateSelection is the authoritative server-side gate — a non-conforming/malicious client can submit out-of-bounds or wrong-shaped selections |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-04 | Tampering / Elevation (input validation) | validateSelection choice branch | mitigate | Defense-in-depth: enforce multiSelect count + array-type server-side so client-side bounds are no longer the only check (ASVS-style input validation at the trust boundary) |
| T-133-SD | Info Disclosure / auth | action validation internals | accept | No auth/session/crypto surface; this is input-count validation only (133-RESEARCH.md Security Domain) |
</threat_model>

<verification>
`npx vitest run src/engine/action/action.test.ts` green. RED output from Task 1 recorded in SUMMARY. Full suite (`npm test`) at wave close must stay green. Choice-branch multiSelect usage across shipped games is verified in Phase 138 (cross-repo).
</verification>

<success_criteria>
ENG-04 satisfied: choice-branch multiSelect min/max + array-type enforced server-side; non-array submissions rejected; elements branch unaffected; regression tests red-then-green.
</success_criteria>

<output>
Create `.planning/phases/133-engine-flow-action-validation/133-04-SUMMARY.md` when done.
</output>
