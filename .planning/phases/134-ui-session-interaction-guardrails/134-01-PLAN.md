---
phase: 134-ui-session-interaction-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/134-ui-session-interaction-guardrails/134-FINDINGS-VERIFICATION.md
autonomous: true
requirements: [PROC-01]
must_haves:
  truths:
    - "Each of F17, F18, F19, F29, F30, F31 has a recorded verdict (LEGITIMATE or REJECTED) written before any fix task in Plans 02-05 runs"
    - "Each LEGITIMATE verdict cites a current (post-Phase-133) file:line trace of the silent-wrong-path defect, not the audit's original line numbers"
    - "The F29 verdict records the exact count and locations of #runner assignment sites in game-session.ts (research says 5: 341, 379, 484, 1462, 1482) and confirms zero production consumers call .performAction through session.runner"
  artifacts:
    - path: ".planning/phases/134-ui-session-interaction-guardrails/134-FINDINGS-VERIFICATION.md"
      provides: "Per-finding verification verdicts gating all fix work in Plans 02-05"
      contains: "VERDICT:"
  key_links: []
---

<objective>
Produce the PROC-01 verification gate for Phase 134: an independent re-verification of all six in-scope audit findings (F17/UIX-01, F18/UIX-02, F19/UIX-03, F29/SESS-01, F30/UIX-04, F31/UIX-05) recorded in `134-FINDINGS-VERIFICATION.md`, each with a LEGITIMATE (current file:line trace or concrete repro) or REJECTED (reasoning) verdict — written BEFORE any fix is planned or written.

Purpose: PROC-01 is a hard, non-negotiable phase gate operationalizing the project's "Prove Before Fix" rule. No fix task in Plans 02-05 may run until this document records a verdict for every finding. 134-RESEARCH.md already re-traced all six against current source; Phase 133 shipped between the audit and now, so line numbers must be re-confirmed here, not assumed.
Output: `.planning/phases/134-ui-session-interaction-guardrails/134-FINDINGS-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-ui-session-interaction-guardrails/134-CONTEXT.md
@.planning/phases/134-ui-session-interaction-guardrails/134-RESEARCH.md
@.planning/phases/134-ui-session-interaction-guardrails/134-PATTERNS.md
@.planning/tmp/v4.5-audit-findings.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify composable findings F17, F18, F30, F31 and record verdicts</name>
  <files>.planning/phases/134-ui-session-interaction-guardrails/134-FINDINGS-VERIFICATION.md</files>
  <read_first>
    - .planning/tmp/v4.5-audit-findings.json (original audit text + verifier verdicts for F17, F18, F30, F31)
    - .planning/phases/134-ui-session-interaction-guardrails/134-RESEARCH.md (Patterns 1-4; Pitfall 1 on start()'s two synchronous failure modes)
    - src/ui/composables/useActionController.ts (confirm: start() sets lastError and returns void at the "is not available" and "No metadata" branches ~1252-1262; fill() never consults resolveMultiSelectConfig ~1311; toggleMultiSelect's reverse guard ~1727-1735 is the mirror template; setBeforeAutoExecute is single-slot ref ~179-183 / 919-942; auto-execute watcher consumes the single hook ~853-858)
    - src/ui/composables/useDragDrop.ts (confirm dragProps ~212-235 ignores options.when; evalCondition helper ~299-302 is used by drag()/dragClasses() but NOT dragProps)
    - grep -n "lastError" src/ui (confirm zero consumers of lastError outside useActionController.ts — the F17 evidence that custom UIs get no failure feedback)
  </read_first>
  <action>Create `.planning/phases/134-ui-session-interaction-guardrails/134-FINDINGS-VERIFICATION.md`. For each finding record one section headed with the finding ID and requirement ID and a line containing the literal token `VERDICT:` followed by `LEGITIMATE` or `REJECTED`. Cover: F17/UIX-01 — trace that `lastError` has zero consumers outside useActionController.ts (custom UIs silently swallow failures) and that `start()` returns void so failure is not programmatically detectable; record the two synchronous failure branches start() actually detects (unavailable action, missing metadata) per Pitfall 1. F18/UIX-02 — trace that `fill()` never calls resolveMultiSelectConfig and would silently forward a scalar to a multiSelect pick; cite the current line and the toggleMultiSelect reverse-guard sibling that proves the symmetric guard is the intended shape. F30/UIX-04 — trace that dragProps ignores options.when while drag()/dragClasses() already gate on evalCondition; cite current lines. F31/UIX-05 — trace that setBeforeAutoExecute overwrites a single ref (replace semantics) and that useActionController.test.ts:521 currently asserts replace-semantics. Each LEGITIMATE verdict must cite current file:line evidence. If a finding does not reproduce against current code, record REJECTED with reasoning instead of inventing a fix. Do NOT write or plan any fix in this task — verdicts only.</action>
  <verify>
    <automated>grep -c "VERDICT:" .planning/phases/134-ui-session-interaction-guardrails/134-FINDINGS-VERIFICATION.md</automated>
  </verify>
  <done>134-FINDINGS-VERIFICATION.md exists with a VERDICT: line and current file:line trace for each of F17, F18, F30, F31.</done>
  <acceptance_criteria>
    - File contains a section for F17, F18, F30, F31 each with a `VERDICT:` token
    - Each LEGITIMATE verdict cites a current post-Phase-133 file:line (not the audit's original numbers)
    - No fix code is written in this task
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Re-verify F19 (board 0x0) and F29 (runner facade) and record verdicts</name>
  <files>.planning/phases/134-ui-session-interaction-guardrails/134-FINDINGS-VERIFICATION.md</files>
  <read_first>
    - .planning/tmp/v4.5-audit-findings.json (original audit text for F19, F29)
    - .planning/phases/134-ui-session-interaction-guardrails/134-RESEARCH.md (UIX-03 Structural Fix Evaluation — the REJECT verdict evidence; Pitfall 2 on transient startup 0x0; SESS-01 facade design + zero-production-consumer grep)
    - src/ui/components/GameShell.vue (confirm .game-shell__zoom-container { width: max-content } ~2817-2839; zoomContainerEl wiring to useAutoZoom ~287-291)
    - src/ui/composables/useAutoZoom.ts (confirm the <1px board is treated as "not laid out yet" and waits, never erroring; SETTLE_MS constant ~28)
    - src/session/game-session.ts (confirm get runner() ~858-860 returns raw #runner; enumerate ALL #runner assignment sites — research says 5: constructor 341, 379, 484, 1462, 1482; confirm GameRunner.performAction has identical name/signature to session.performAction; confirm game-session.ts never calls this.runner internally, only this.#runner)
    - grep -rn "\.runner\b" src (confirm session.runner's only consumers are test files doing read-only operations — getSnapshot/.game/.actionHistory — with zero .performAction calls through the accessor)
  </read_first>
  <action>Append F19/UIX-03 and F29/SESS-01 sections to `134-FINDINGS-VERIFICATION.md`, each with a `VERDICT:` line. F19 — trace that a percentage-width / container-type custom board collapses to 0x0 inside the max-content zoom container and that useAutoZoom silently waits forever with no dev signal; record the research verdict that the structural CSS fix is REJECTED (it breaks v4.0 zoom-to-fit intrinsic-size measurement) so dev-error + docs is the complete fix. F29 — trace that get runner() exposes the raw GameRunner whose performAction is an easy lookalike wrong path beside session.performAction; record the EXACT current line of every #runner assignment site (re-grep, do not trust the audit's count of 3 — research found 5) and confirm zero production consumers call .performAction through session.runner. Each LEGITIMATE verdict cites current file:line. Verdicts only — no fix code.</action>
  <verify>
    <automated>grep -c "VERDICT:" .planning/phases/134-ui-session-interaction-guardrails/134-FINDINGS-VERIFICATION.md</automated>
  </verify>
  <done>All six findings (F17, F18, F19, F29, F30, F31) now have a VERDICT: line; the F29 verdict lists the exact current #runner assignment sites.</done>
  <acceptance_criteria>
    - `grep -c "VERDICT:"` returns 6 or more
    - F29 section records the exact current line numbers of every #runner assignment site
    - F19 section records the structural-CSS-fix REJECTION rationale
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning doc → fix plans | An incorrect verdict here (false LEGITIMATE / false REJECTED) misdirects every downstream fix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-01 | Spoofing (of evidence) | 134-FINDINGS-VERIFICATION.md | mitigate | Every verdict must cite a re-traced current file:line; audit's original numbers are not accepted as proof |
| T-134-02 | Repudiation | verdict record | mitigate | Literal `VERDICT:` token per finding makes the gate machine-checkable before any fix runs |

No package installs in this plan → no T-134-SC supply-chain row applies (phase adds zero dependencies).
</threat_model>

<verification>
- `grep -c "VERDICT:" 134-FINDINGS-VERIFICATION.md` >= 6
- Each finding section cites current file:line evidence
</verification>

<success_criteria>
All six findings have a recorded, evidence-backed verdict. No fix code written. Plans 02-05 are unblocked.
</success_criteria>

<output>
Create `.planning/phases/134-ui-session-interaction-guardrails/134-01-SUMMARY.md` when done.
</output>
