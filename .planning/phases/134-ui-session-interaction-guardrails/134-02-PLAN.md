---
phase: 134-ui-session-interaction-guardrails
plan: 02
type: execute
wave: 2
depends_on: [134-01]
files_modified:
  - src/ui/composables/useActionController.ts
  - src/ui/composables/useActionControllerTypes.ts
  - src/ui/composables/useActionController.test.ts
  - src/ui/composables/useActionController.picks.test.ts
autonomous: true
requirements: [UIX-01, UIX-02, UIX-05, PROC-02]
must_haves:
  truths:
    - "start() returns a checkable ActionResult for its two synchronous pre-check failures (unavailable action, missing metadata) plus a success acknowledgment that wizard mode began — never the eventual server outcome"
    - "start() emits a devWarn (UI-SPEC copy) when called for an action not in availableActions"
    - "fill() rejects a scalar passed for a multiSelect pick with the UI-SPEC actionable error and does not mutate state or forward to the server"
    - "setBeforeAutoExecute registers multiple hooks that run in registration order and returns an unregister function that removes only that hook"
    - "useActionController.test.ts:521 asserts accumulation (['first','second']) instead of replace-semantics"
  artifacts:
    - path: "src/ui/composables/useActionController.ts"
      provides: "start() return+devWarn (UIX-01), fill() multiSelect guard (UIX-02), hook accumulation (UIX-05)"
      contains: "beforeAutoExecuteHooks"
    - path: "src/ui/composables/useActionControllerTypes.ts"
      provides: "start() Promise<ActionResult> signature; setBeforeAutoExecute () => void return type"
  key_links:
    - from: "fill()"
      to: "resolveMultiSelectConfig()"
      via: "reuse existing helper for the guard (no re-derivation)"
      pattern: "resolveMultiSelectConfig"
    - from: "auto-execute watcher"
      to: "beforeAutoExecuteHooks"
      via: "sequential await over the array in registration order"
      pattern: "beforeAutoExecuteHooks"
---

<objective>
Wire three missing guardrails into `useActionController.ts` (the composable shared by ActionPanel and every custom UI, so parity holds by construction): (UIX-01) `start()` returns a checkable `ActionResult` and devWarns on an unavailable action; (UIX-02) `fill()` rejects a scalar for a multiSelect pick with an actionable error; (UIX-05) `setBeforeAutoExecute` accumulates hooks in registration order and returns an unregister function.

Purpose: these are the "silent wrong path" footguns F17/F18/F31 — each is a MISSING call to machinery that already exists in the same file (a devWarn convention, `resolveMultiSelectConfig`, an existing hook slot). Resist inventing new abstractions.
Output: guarded composable + flipped RED test as the PROC-02 regression for UIX-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-ui-session-interaction-guardrails/134-CONTEXT.md
@.planning/phases/134-ui-session-interaction-guardrails/134-RESEARCH.md
@.planning/phases/134-ui-session-interaction-guardrails/134-PATTERNS.md
@.planning/phases/134-ui-session-interaction-guardrails/134-UI-SPEC.md
@.planning/phases/134-ui-session-interaction-guardrails/134-FINDINGS-VERIFICATION.md

<interfaces>
<!-- Reuse these existing shapes; do NOT invent a new result type (UI-SPEC directive). -->
From src/ui/composables/useActionControllerTypes.ts:122-134:
  interface ActionResult { success: boolean; error?: string; data?: Record<string, unknown>; message?: string; followUp?: FollowUpAction }
  interface ValidationResult { valid: boolean; error?: string }

Existing helper to reuse (src/ui/composables/useActionController.ts:1695-1706):
  resolveMultiSelectConfig(selection): { min?: number; max?: number } | undefined
    // handles both static multiSelect and dependsOn-scoped multiSelectByDependentValue

Existing devWarn(key, message) convention — mirror toggleMultiSelect's no-selection guard (~1716-1725).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: start() returns ActionResult + devWarns on unavailable action (UIX-01)</name>
  <files>src/ui/composables/useActionController.ts, src/ui/composables/useActionControllerTypes.ts, src/ui/composables/useActionController.test.ts</files>
  <read_first>
    - src/ui/composables/useActionController.ts (start() ~1245-1309: the two failure branches at ~1253 "is not available" and ~1260 "No metadata"; confirm none of start()'s steps call sendAction() — the eventual action executes later via the watch(isReady) auto-execute watcher ~853-858)
    - src/ui/composables/useActionControllerTypes.ts (ActionResult/ValidationResult ~122-134; start()'s public signature ~395-400)
    - src/ui/composables/useDragDropTargets.ts (Case B ~277-306 currently discards start()'s return value — confirm the widening is additive there, no consumer breaks)
    - .planning/phases/134-ui-session-interaction-guardrails/134-UI-SPEC.md (Copywriting Contract row "UIX-01 start() called for unavailable action"; Interaction & Accessibility Contract "Return-value contract")
    - .planning/phases/134-ui-session-interaction-guardrails/134-RESEARCH.md (Pitfall 1 — start() MUST NOT imply it reflects the eventual server outcome)
  </read_first>
  <behavior>
    - start('unavailableAction') resolves to { success: false, error } AND emits the UIX-01 devWarn (once per key)
    - start('actionWithNoMetadata') resolves to { success: false, error } reflecting the missing-metadata branch
    - start('validAction') resolves to a success acknowledgment ({ success: true }) that wizard mode began — it does NOT wait for or reflect the eventual auto-executed server result
  </behavior>
  <action>Widen start()'s return type from `Promise<void>` to `Promise<ActionResult>` (reuse ActionResult per UI-SPEC — do not introduce a third shape; update the signature in useActionControllerTypes.ts too). At the "is not available" branch, in addition to setting lastError, emit `devWarn(key, message)` using the UI-SPEC UIX-01 copy verbatim (`start('{actionName}') was ignored: '{actionName}' is not in the current player's available actions...`) and return `{ success: false, error: lastError.value }`. At the "No metadata" branch return `{ success: false, error }`. On the success path (wizard mode began) return `{ success: true }` — per Pitfall 1 this represents ONLY that start's synchronous pre-checks passed, NOT the eventual server outcome; add a JSDoc note stating exactly that. Add `it()` blocks in useActionController.test.ts covering the three behaviors above (target with `-t "start"`).</action>
  <verify>
    <automated>npx vitest run src/ui/composables/useActionController.test.ts -t "start"</automated>
  </verify>
  <done>start() returns ActionResult for both synchronous failures and the success ack; devWarn fires on unavailable action; new tests green; useDragDropTargets.ts still compiles.</done>
  <acceptance_criteria>
    - start()'s declared return type is Promise<ActionResult> in both the impl and useActionControllerTypes.ts
    - devWarn copy matches the UI-SPEC UIX-01 row verbatim
    - JSDoc explicitly states the return value reflects only start()'s synchronous pre-checks, not the eventual server result
    - Tests assert the three behaviors and pass
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fill() rejects scalar for a multiSelect pick (UIX-02)</name>
  <files>src/ui/composables/useActionController.ts, src/ui/composables/useActionController.picks.test.ts</files>
  <read_first>
    - src/ui/composables/useActionController.ts (fill() ~1311-1345: the choice-object auto-unwrap block ~1321-1332 MUST run before the new guard; the selection.repeat ~1335 and hasOnSelect/pendingOnServer ~1340 branches bypass validateSelection so the guard MUST be placed before them; toggleMultiSelect's reverse guard ~1727-1735 is the mirror; resolveMultiSelectConfig ~1695-1706)
    - .planning/phases/134-ui-session-interaction-guardrails/134-UI-SPEC.md (Copywriting Contract row "UIX-02")
    - .planning/phases/134-ui-session-interaction-guardrails/134-PATTERNS.md (Pattern 3 — exact insertion point and error text)
    - src/ui/composables/useActionController.picks.test.ts (existing multiSelect/pick test patterns and helpers to mirror)
  </read_first>
  <behavior>
    - fill('multiPick', 5) returns { valid: false, error } with the UIX-02 copy, sets lastError, and does NOT mutate selection state or reach the server
    - fill('multiPick', [5, 6]) still works (array accepted, no rejection)
    - fill('singlePick', 5) still works (non-multiSelect scalar accepted — the guard is symmetric to toggleMultiSelect, not a blanket scalar ban)
    - a single choice-object {value,display} for a multiSelect pick is unwrapped first, then rejected by the multiSelect guard (not by a confusing validateSelection error)
  </behavior>
  <action>Insert the UIX-02 guard in fill() immediately AFTER the choice-object unwrap block (~1332) and BEFORE the selection.repeat / hasOnSelect / pendingOnServer branches (~1335-1341). Resolve the selection's config via the existing `resolveMultiSelectConfig(selection)` helper (do not re-derive dependsOn/multiSelectByDependentValue). If a config exists AND the incoming value is not an array, set `lastError.value` to the UI-SPEC UIX-02 error string (verbatim, interpolating selectionName, min, max) and return `{ valid: false, error }` without mutating state. Add `it()` blocks in useActionController.picks.test.ts covering the four behaviors above (target with `-t "multiSelect"`).</action>
  <verify>
    <automated>npx vitest run src/ui/composables/useActionController.picks.test.ts -t "multiSelect"</automated>
  </verify>
  <done>fill() rejects a scalar for a multiSelect pick with the UI-SPEC error, accepts arrays and non-multiSelect scalars unchanged; guard sits after unwrap and before the repeat/onSelect routes; tests green.</done>
  <acceptance_criteria>
    - The guard calls resolveMultiSelectConfig (grep confirms reuse, not a parallel lookup)
    - Error copy matches the UI-SPEC UIX-02 row verbatim
    - Guard is positioned after the choice-object unwrap and before the repeat/onSelect branches
    - Tests assert reject-scalar, accept-array, accept-non-multi-scalar, and unwrap-then-reject
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: setBeforeAutoExecute accumulates hooks + flip the RED test (UIX-05)</name>
  <files>src/ui/composables/useActionController.ts, src/ui/composables/useActionControllerTypes.ts, src/ui/composables/useActionController.test.ts</files>
  <read_first>
    - src/ui/composables/useActionController.ts (single-slot beforeAutoExecuteHook ref ~179-183; setBeforeAutoExecute ~919-942 incl. the "REPLACES" JSDoc to remove; auto-execute watcher consumer ~853-858)
    - src/ui/composables/useActionControllerTypes.ts (setBeforeAutoExecute return type — widen void -> () => void)
    - src/ui/composables/useActionController.test.ts (line 521 test `'setBeforeAutoExecute replaces the previous hook (single-slot)'` — the RED test to invert)
    - .planning/phases/134-ui-session-interaction-guardrails/134-PATTERNS.md (Pattern 2 — array registry, sequential await, unregister closure)
    - .planning/phases/134-ui-session-interaction-guardrails/134-UI-SPEC.md (Copywriting Contract row "UIX-05" JSDoc language)
  </read_first>
  <behavior>
    - Two registered hooks both fire, in registration order: ['first', 'second']
    - The watcher awaits each hook sequentially (not Promise.all) so ordering is deterministic
    - setBeforeAutoExecute returns an unregister fn; calling it removes only that hook (the other still fires)
  </behavior>
  <action>Replace the single-slot `beforeAutoExecuteHook` ref with `beforeAutoExecuteHooks: Ref<Hook[]>` (seed from initialBeforeAutoExecute if present). Change `setBeforeAutoExecute(hook)` to push and return an unregister closure that splices that exact hook by identity. Change the auto-execute watcher to `for (const hook of beforeAutoExecuteHooks.value) { await hook(...) }` (sequential await, registration order). Update the return type in useActionControllerTypes.ts from void to `() => void`. Remove the "REPLACES the previous hook" JSDoc and replace with the UI-SPEC UIX-05 language ("registers an additional hook; hooks run in registration order; call the returned function to unregister"). Flip useActionController.test.ts:521: rename to reflect accumulation and change the final assertion from `['second']` to `['first', 'second']`; add a case asserting the unregister fn removes only its own hook. This flipped test is the PROC-02 regression for UIX-05.</action>
  <verify>
    <automated>npx vitest run src/ui/composables/useActionController.test.ts -t "beforeAutoExecute"</automated>
  </verify>
  <done>Hooks accumulate and run in order; unregister removes only its hook; the formerly-replace test now asserts accumulation; JSDoc no longer says "REPLACES".</done>
  <acceptance_criteria>
    - beforeAutoExecuteHooks is an array; watcher awaits sequentially
    - setBeforeAutoExecute return type is () => void in impl and types file
    - useActionController.test.ts:521 asserts ['first', 'second'] and an unregister case exists
    - grep of useActionController.ts shows no remaining "REPLACES" JSDoc for this hook
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| custom-UI/script → useActionController | Developer input (action name, fill value, hook) crosses into the shared controller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-03 | Tampering (malformed selection reaches server) | fill() multiSelect path | mitigate | UIX-02 guard rejects scalar-for-multiSelect client-side before routing (server-side enforcement already landed in Phase 133 ENG-04 — this is a UX guardrail, not the security boundary) |
| T-134-04 | Information disclosure (error copy) | start()/fill() error strings | accept | Copy is actionable and leaks no stack/line/internal path per CLAUDE.md + UI-SPEC Copywriting Contract |

No package installs in this plan → no supply-chain row.
</threat_model>

<verification>
- Targeted vitest for each task passes
- `npx vitest run src/ui/composables/useActionController.test.ts src/ui/composables/useActionController.picks.test.ts` green
</verification>

<success_criteria>
UIX-01 (start half), UIX-02, and UIX-05 shipped in useActionController.ts with the UI-SPEC copy, reusing existing machinery; the RED replace-semantics test flipped to green accumulation.
</success_criteria>

<output>
Create `.planning/phases/134-ui-session-interaction-guardrails/134-02-SUMMARY.md` when done. Record the RED state of the flipped test (assertion before/after) per PROC-02.
</output>
