---
phase: 134-ui-session-interaction-guardrails
plan: 03
type: execute
wave: 2
depends_on: [134-01]
files_modified:
  - src/ui/components/GameShell.vue
  - src/ui/components/GameShell.test.ts
  - src/ui/composables/useDragDrop.ts
  - src/ui/composables/useDragDrop.test.ts
autonomous: true
requirements: [UIX-01, UIX-03, UIX-04, PROC-02]
must_haves:
  truths:
    - "GameShell fires toast.error and routes text into the existing assertiveMessage live region whenever actionController.lastError transitions to a new non-null value"
    - "The failure toast surfaces identically for custom UIs and ActionPanel (both share the same actionController instance provided at the shell level) — no parallel error path added inside ActionPanel"
    - "A dev-mode console.error fires once when the zoom container measures 0x0 AFTER game state has arrived and the #game-board slot has children — and does NOT fire on the normal transient 0x0 during startup"
    - "dragProps() returns inert props (draggable:false, no live handlers) when its documented when option evaluates false, reusing the existing evalCondition helper"
  artifacts:
    - path: "src/ui/components/GameShell.vue"
      provides: "central lastError->toast watch (UIX-01), 0x0 board dev-check (UIX-03)"
      contains: "toast.error"
    - path: "src/ui/composables/useDragDrop.ts"
      provides: "dragProps when-gating (UIX-04)"
      contains: "evalCondition"
  key_links:
    - from: "GameShell.vue watch"
      to: "actionController.lastError"
      via: "watch -> toast.error + assertiveMessage"
      pattern: "actionController.lastError"
    - from: "dragProps()"
      to: "evalCondition()"
      via: "reuse existing helper to gate inert props"
      pattern: "evalCondition"
---

<objective>
Close the three browser-tier guardrails: (UIX-01 part 1) GameShell centrally watches `actionController.lastError` and fires `toast.error` + the existing assertive live region so custom UIs get the same failure feedback ActionPanel already gets; (UIX-03) a dev-mode `console.error` when a responsive custom board silently collapses to 0x0 inside the max-content zoom container; (UIX-04) `dragProps()` honors its documented `when` option by returning inert props.

Purpose: F17 (client end), F19, F30 — again all MISSING calls to existing machinery (the toast/live-region chokepoint, useAutoZoom's settle timer, the evalCondition helper). The structural CSS fix for UIX-03 is REJECTED per research (it breaks v4.0 zoom-to-fit) — do NOT change the zoom container CSS.
Output: guarded GameShell + drag composable, with component/unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-ui-session-interaction-guardrails/134-CONTEXT.md
@.planning/phases/134-ui-session-interaction-guardrails/134-RESEARCH.md
@.planning/phases/134-ui-session-interaction-guardrails/134-PATTERNS.md
@.planning/phases/134-ui-session-interaction-guardrails/134-UI-SPEC.md
@.planning/phases/134-ui-session-interaction-guardrails/134-FINDINGS-VERIFICATION.md

<interfaces>
<!-- Existing GameShell machinery to reuse — do not add a second toast owner or live region. -->
GameShell.vue: politeMessage/assertiveMessage refs (~339-340, rendered ~1849-1850 as role="status"/role="alert");
  actionController provided via provide('actionController', actionController) (~966);
  handleUndo() toast-on-failure pattern (~651-676) and Game-Over assertiveMessage watch (~1763-1786) are the sibling patterns to mirror;
  zoomContainerEl wired into useAutoZoom (~287-291); .game-shell__zoom-container { width: max-content } (~2817-2839) — DO NOT modify.
useAutoZoom.ts: export const SETTLE_MS = 300 (~28) — import for consistent settle timing.
useDragDrop.ts: evalCondition(options) helper (~299-302) already used by drag()/dragClasses(); dragProps (~212-235) currently ignores options.when; UseDragDropReturn['dragProps'] type (~178).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Central lastError -> toast + live region in GameShell (UIX-01 part 1)</name>
  <files>src/ui/components/GameShell.vue, src/ui/components/GameShell.test.ts</files>
  <read_first>
    - src/ui/components/GameShell.vue (handleUndo toast pattern ~651-676; Game-Over assertiveMessage watch ~1763-1786; politeMessage/assertiveMessage ~339-340; provide('actionController') ~966 — the watch must be placed after actionController is constructed)
    - .planning/phases/134-ui-session-interaction-guardrails/134-UI-SPEC.md (Copywriting Contract row "UIX-01 action-failure toast"; Interaction & Accessibility Contract — assertive priority, single chokepoint, non-color-cue FLAG #1)
    - .planning/phases/134-ui-session-interaction-guardrails/134-RESEARCH.md (Pattern 1 — no flush:'sync' needed; Open Question 2 — no dedup needed against undo toast)
    - src/ui/components/GameShell.test.ts (existing jsdom component test patterns; header `// @vitest-environment jsdom`)
    - src/ui/components/Toast.vue (verify the error variant carries a non-color cue per FLAG #1; if it does NOT, flag back to the phase owner in the SUMMARY — do not add icon assets or new deps)
  </read_first>
  <behavior>
    - When actionController.lastError transitions null -> "boom", exactly one toast.error("boom") fires and assertiveMessage becomes "boom"
    - A single failed action that sets lastError multiple times within one tick produces exactly one toast (Vue batches; no flush:'sync')
    - When lastError is the engine's error string, it is surfaced verbatim; when absent, the UIX-01 fallback copy is used; never render undefined/[object Object]/.stack
  </behavior>
  <action>Add a `watch(actionController.lastError, (err) => { if (!err) return; toast.error(err); assertiveMessage.value = err; })` co-located near the existing toast-owning code (mirror handleUndo / the Game-Over watch), after actionController is defined. Use the existing useToast().error() (default 4000ms, existing dismiss affordance — no new duration). Route text into the existing assertiveMessage live region (do NOT add a third live region, do NOT add an ActionPanel error path — parity holds because both share the provided actionController). Apply the UI-SPEC fallback copy only when no engine error string is present. In GameShell.test.ts add a jsdom `it()` ("action failure toast") asserting one toast per failed action and the assertiveMessage update. Verify Toast.vue's error variant has a non-color cue; if missing, record the FLAG #1 flag-back in the SUMMARY (out of scope to fix here).</action>
  <verify>
    <automated>npx vitest run src/ui/components/GameShell.test.ts -t "action failure toast"</automated>
  </verify>
  <done>lastError transitions fire a single toast.error + assertiveMessage update; no parallel ActionPanel path; test green; Toast.vue non-color-cue status recorded.</done>
  <acceptance_criteria>
    - Exactly one toast per failed action (watch, default flush)
    - Text routed into the existing assertiveMessage ref (no new live region)
    - Fallback copy matches UI-SPEC when engine error string absent
    - Test asserts single-toast and live-region behavior
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dev-mode 0x0 board console.error (UIX-03)</name>
  <files>src/ui/components/GameShell.vue, src/ui/components/GameShell.test.ts</files>
  <read_first>
    - src/ui/composables/useAutoZoom.ts (SETTLE_MS ~28; the <1px "not laid out yet" wait ~36; onBoardResize/endStartup state machine ~89-115 — DO NOT change measurement behavior)
    - src/ui/components/GameShell.vue (gameView/state refs; zoomContainerEl ~287-291; #game-board slot mount ~2110-2168; .game-shell__zoom-container CSS ~2817-2839 — DO NOT modify CSS)
    - .planning/phases/134-ui-session-interaction-guardrails/134-UI-SPEC.md (Copywriting Contract row "UIX-03" — exact console.error copy)
    - .planning/phases/134-ui-session-interaction-guardrails/134-RESEARCH.md (Pitfall 2 — gate on BOTH state-arrived AND slot-has-children; Open Question 1 recommendation — keep the check in GameShell, do not widen useAutoZoom's public signature; UIX-03 Structural Fix Evaluation — REJECT the CSS fix)
  </read_first>
  <behavior>
    - After gameView becomes non-null and one settle delay elapses, if zoomContainerEl measures width<1 or height<1 AND has children, console.error fires once with the UIX-03 copy
    - The check does NOT fire during the normal startup transient 0x0 (before state arrives / before children mount)
    - Import and reuse SETTLE_MS from useAutoZoom for the delay (no new timing constant)
  </behavior>
  <action>Add a `watch(gameView, ...)` (immediate:false) in GameShell that, on a non-null view, awaits nextTick then a `setTimeout(..., SETTLE_MS)` (imported from useAutoZoom), reads `zoomContainerEl.value.getBoundingClientRect()`, and if width<1 or height<1 AND `el.children.length > 0`, fires `console.error` with the UI-SPEC UIX-03 copy verbatim (the message points at the new "Board Sizing" section authored in Plan 05). Gate on both state-arrived (non-null view) and slot-has-children to avoid the Pitfall-2 false positive. Do NOT modify the zoom container CSS (structural fix rejected). Add a jsdom `it()` in GameShell.test.ts stubbing getBoundingClientRect to assert the error fires on post-state 0x0-with-children and does NOT fire on the startup transient (no children / null view).</action>
  <verify>
    <automated>npx vitest run src/ui/components/GameShell.test.ts -t "board sizing"</automated>
  </verify>
  <done>console.error fires once on a genuine post-state 0x0 collapse with children, never on normal startup; CSS untouched; test green.</done>
  <acceptance_criteria>
    - Check gated on non-null gameView AND el.children.length > 0
    - Uses imported SETTLE_MS, not a new constant
    - console.error copy matches UI-SPEC UIX-03 verbatim
    - .game-shell__zoom-container CSS is unchanged (grep diff shows no CSS edit)
    - Test covers both the fire and the no-fire-on-startup cases
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: dragProps() honors when (UIX-04)</name>
  <files>src/ui/composables/useDragDrop.ts, src/ui/composables/useDragDrop.test.ts</files>
  <read_first>
    - src/ui/composables/useDragDrop.ts (dragProps ~212-235 always draggable:true; evalCondition ~299-302; drag()'s inert-props precedent ~324-327; dragClasses ~307-310; UseDragDropReturn['dragProps'] type ~178; DragOptions/DragProps ~109-132)
    - .planning/phases/134-ui-session-interaction-guardrails/134-PATTERNS.md (Pattern 4 — exact widened return shape)
    - .planning/phases/134-ui-session-interaction-guardrails/134-UI-SPEC.md (UIX-04 row — behavioral only, silent, no copy)
    - src/ui/composables/useDragDrop.test.ts (existing test patterns to mirror)
  </read_first>
  <behavior>
    - dragProps(ref, { when: false }) returns { draggable: false } with no onDragstart/onDragend handlers
    - dragProps(ref, { when: () => false }) returns inert props (function form evaluated via evalCondition)
    - dragProps(ref) and dragProps(ref, { when: true }) return the full draggable props unchanged
  </behavior>
  <action>Gate dragProps() on the existing `evalCondition(options)` helper (reuse — do not write a second when-evaluator): when it returns false, return `{ draggable: false }`; otherwise return the current full props object unchanged. Widen the return type to `DragProps | { draggable: false }` in both the function and the `UseDragDropReturn['dragProps']` signature (~178). No copy/UI change (silent behavioral fix). Add `it()` blocks in useDragDrop.test.ts (target `-t "when"`) covering boolean-false, function-false, and the unchanged default/true cases.</action>
  <verify>
    <automated>npx vitest run src/ui/composables/useDragDrop.test.ts -t "when"</automated>
  </verify>
  <done>dragProps returns inert props when when is false (boolean or function), full props otherwise; return type widened; tests green.</done>
  <acceptance_criteria>
    - dragProps calls evalCondition (grep confirms reuse, not a second implementation)
    - Return type widened to DragProps | { draggable: false } in impl and UseDragDropReturn
    - Tests cover boolean-false, function-false, default, and true
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session/engine → GameShell chrome | Action-failure signal (lastError) crosses into user-facing surfacing |
| developer board markup → GameShell zoom container | Custom board sizing crosses into the shell's measurement/layout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-05 | Information disclosure (error copy) | lastError -> toast text | mitigate | Surface engine error verbatim only when actionable; fallback copy leaks no stack/line/path per UI-SPEC + CLAUDE.md |
| T-134-06 | Denial of service (log spam) | UIX-03 console.error | mitigate | Fire once per genuine post-state 0x0-with-children; gate on state-arrived + children to avoid startup false positives (Pitfall 2) |
| T-134-07 | Tampering (silent no-op drag) | dragProps when-gating | mitigate | Inert props when when=false make the documented option behave as specified (was silently ignored) |

No package installs in this plan → no supply-chain row.
</threat_model>

<verification>
- Targeted vitest per task passes
- `npx vitest run src/ui/components/GameShell.test.ts src/ui/composables/useDragDrop.test.ts` green
- grep confirms .game-shell__zoom-container CSS unchanged
</verification>

<success_criteria>
UIX-01 (toast half), UIX-03, and UIX-04 shipped; parity by construction; zoom CSS untouched; non-color-cue status for Toast.vue recorded.
</success_criteria>

<output>
Create `.planning/phases/134-ui-session-interaction-guardrails/134-03-SUMMARY.md` when done. Record RED state per PROC-02 and the Toast.vue FLAG #1 outcome.
</output>
