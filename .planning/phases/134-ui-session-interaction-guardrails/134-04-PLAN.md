---
phase: 134-ui-session-interaction-guardrails
plan: 04
type: execute
wave: 2
depends_on: [134-01]
files_modified:
  - src/session/game-session.ts
  - src/session/game-session.test.ts
autonomous: true
requirements: [SESS-01, PROC-02]
must_haves:
  truths:
    - "session.runner returns a read-only facade (state/view/history getters only) — performAction is present neither at the type level nor at runtime on the returned object"
    - "The facade is rebuilt at ALL FIVE #runner assignment sites (constructor 341, 379, 484, 1462, 1482) so session.runner is referentially stable across reads within a runner generation"
    - "All existing read-only test consumers of session.runner (getSnapshot/.game/.actionHistory) continue to compile and pass unchanged"
    - "All write paths still flow through session.performAction (persistence/broadcast/checkpoints/tutorials/AI scheduling intact)"
  artifacts:
    - path: "src/session/game-session.ts"
      provides: "ReadOnlyRunnerFacade interface + buildRunnerFacade + #runnerFacade field + narrowed get runner()"
      contains: "ReadOnlyRunnerFacade"
  key_links:
    - from: "get runner()"
      to: "#runnerFacade"
      via: "returns the cached narrower object, not #runner"
      pattern: "#runnerFacade"
    - from: "every #runner assignment site"
      to: "buildRunnerFacade(this.#runner)"
      via: "paired rebuild keeps facade in sync with the live runner"
      pattern: "buildRunnerFacade"
---

<objective>
Replace `GameSession.runner`'s raw `GameRunner` exposure with a genuinely narrower read-only facade so `session.runner.performAction(...)` — an easy lookalike wrong path beside `session.performAction(...)` that silently skips persistence/broadcast/checkpoints — is unreachable at both the type level and runtime (SESS-01/F29).

Purpose: clean break per No Backward Compatibility. Research confirms ZERO production consumers and only read-only test consumers, so the facade is additive at the public boundary — no internal migration needed (game-session.ts uses this.#runner directly everywhere).
Output: read-only facade + a test proving the read surface works and performAction is unreachable (runtime + @ts-expect-error).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-ui-session-interaction-guardrails/134-CONTEXT.md
@.planning/phases/134-ui-session-interaction-guardrails/134-RESEARCH.md
@.planning/phases/134-ui-session-interaction-guardrails/134-PATTERNS.md
@.planning/phases/134-ui-session-interaction-guardrails/134-UI-SPEC.md
@.planning/phases/134-ui-session-interaction-guardrails/134-FINDINGS-VERIFICATION.md

<interfaces>
<!-- Concrete facade shape from RESEARCH.md Code Examples — a genuinely narrower runtime object
     (NOT a type-cast of #runner), so untyped/JS callers also get a TypeError on .performAction. -->
interface ReadOnlyRunnerFacade<G extends Game = Game> {
  readonly game: G;
  readonly actionHistory: readonly SerializedAction[];
  getSnapshot(): GameStateSnapshot;
  getPlayerView(playerPosition: number): PlayerStateView;
  getAllPlayerViews(): PlayerStateView[];
  getFlowState(): FlowState | undefined;
  getFlowDebugInfo(): FlowDebugInfo;
  getPendingAction(playerPosition: number): PendingActionState | undefined;
  isComplete(): boolean;
  getWinners(): Player[];
}
// buildRunnerFacade(runner) returns { get game(){...}, get actionHistory(){...}, getSnapshot: () => runner.getSnapshot(), ... }
// GameRunner public read-only members confirmed in src/runtime/runner.ts (game 68, actionHistory 74, getSnapshot/... 66-494)

#runner assignment sites (re-grep to confirm current lines before editing): 341 (constructor), 379, 484, 1462, 1482.
Current getter to replace: get runner(): GameRunner<G> at ~858-860.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ReadOnlyRunnerFacade + rebuild at all 5 #runner sites + narrow get runner()</name>
  <files>src/session/game-session.ts</files>
  <read_first>
    - src/session/game-session.ts (#runner field ~236; get runner() ~858-860; RE-GREP all #runner assignment sites — research says 5: 341, 379, 484, 1462, 1482 — confirm current lines and that none is missed; confirm no internal use of this.runner, only this.#runner)
    - src/runtime/runner.ts (GameRunner member inventory 66-494 — confirm the exact names/signatures of game, actionHistory, getSnapshot, getPlayerView, getAllPlayerViews, getFlowState, getFlowDebugInfo, getPendingAction, isComplete, getWinners so the facade interface matches; confirm existing import paths for SerializedAction/GameStateSnapshot/PlayerStateView/FlowState/FlowDebugInfo/PendingActionState/Player)
    - .planning/phases/134-ui-session-interaction-guardrails/134-PATTERNS.md (SESS-01 section — the exact facade template and the runtime-throw fallback that is satisfied for free by the narrower object)
    - .planning/phases/134-ui-session-interaction-guardrails/134-RESEARCH.md (Pitfall 3 — must be a genuinely narrower object, not a type-cast; Code Examples — buildRunnerFacade)
  </read_first>
  <action>Add an exported `ReadOnlyRunnerFacade<G>` interface and a module-level `buildRunnerFacade<G>(runner)` factory returning a genuinely narrower object literal (getters for game/actionHistory delegating to the live runner; arrow methods delegating getSnapshot/getPlayerView/getAllPlayerViews/getFlowState/getFlowDebugInfo/getPendingAction/isComplete/getWinners) — NOT a cast of #runner (per Pitfall 3, so `.performAction` is `undefined` at runtime for JS callers, naturally throwing). Add a private `#runnerFacade: ReadOnlyRunnerFacade<G>` field. At EACH of the 5 #runner assignment sites, immediately assign `this.#runnerFacade = buildRunnerFacade(this.#runner)` (use `session.#runnerFacade` at the static/other-instance site 484 as appropriate to match the existing assignment target). Narrow `get runner()` to return `this.#runnerFacade` typed as `ReadOnlyRunnerFacade<G>`. Do NOT change any write path — session.performAction and all internal this.#runner usage stay intact. Confirm the module still typechecks.</action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "game-session" || true</automated>
  </verify>
  <done>get runner() returns the cached facade; facade rebuilt at all 5 sites; performAction absent from the facade type and object; module typechecks with no game-session errors.</done>
  <acceptance_criteria>
    - ReadOnlyRunnerFacade exported; buildRunnerFacade is a genuinely narrower object (no `as` cast of #runner)
    - #runnerFacade rebuilt at every one of the 5 #runner assignment sites (grep count matches)
    - get runner() return type is ReadOnlyRunnerFacade<G>; performAction is not a member
    - No write path altered; session.performAction unchanged
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SESS-01 regression test — read surface works, performAction unreachable</name>
  <files>src/session/game-session.test.ts</files>
  <read_first>
    - src/session/game-session.test.ts (existing session construction/test helpers to build a live session for the test)
    - src/session/restore-snapshot-authoritative.test.ts (an existing read-only session.runner consumer — mirror how it reaches getSnapshot/.game/.actionHistory)
    - .planning/phases/134-ui-session-interaction-guardrails/134-UI-SPEC.md (Copywriting Contract row "SESS-01" — the runtime-throw fallback message; primary guard is the compile-time type)
    - .planning/phases/134-ui-session-interaction-guardrails/134-PATTERNS.md (SESS-01 runtime-throw fallback satisfied by the narrower object)
  </read_first>
  <action>Add a "runner facade" describe block to game-session.test.ts. RED-first per PROC-02: (1) assert the read surface works — session.runner.getSnapshot(), .game, .actionHistory, and at least one of getFlowState/isComplete return the same values as before the facade. (2) assert performAction is unreachable at runtime: `expect((session.runner as any).performAction).toBeUndefined()` (the narrower object has no such key). (3) add a `// @ts-expect-error` compile-time assertion that `session.runner.performAction(...)` does not typecheck — this line failing to error would itself fail tsc, locking in the type-level guard. Record the RED state (pre-facade the runtime assertion and @ts-expect-error would both fail) in the SUMMARY.</action>
  <verify>
    <automated>npx vitest run src/session/game-session.test.ts -t "runner facade"</automated>
  </verify>
  <done>Facade read surface verified; performAction undefined at runtime; @ts-expect-error confirms it does not typecheck; test green.</done>
  <acceptance_criteria>
    - Test asserts getSnapshot/.game/.actionHistory read through the facade
    - Runtime assertion: (session.runner as any).performAction is undefined
    - A // @ts-expect-error line covers session.runner.performAction not typechecking
    - RED state recorded in SUMMARY per PROC-02
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host/test code → GameSession.runner | In-process host/test reaches session state; the accessor was never network-reachable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-08 | Tampering (session-state consistency) | session.runner.performAction lookalike | mitigate | Read-only facade removes performAction from both the type and the runtime object, so persistence/broadcast/checkpoint bookkeeping cannot be silently skipped via the runner accessor (ASVS V3/V4 hardening — reduces misuse surface) |
| T-134-09 | Information disclosure | facade getters (game/view/history) | accept | These are the same read surfaces already exposed and consumed read-only today; no new data surfaced |

No package installs in this plan → no supply-chain row.
</threat_model>

<verification>
- `npx vitest run src/session/game-session.test.ts` green (incl. "runner facade")
- `npx tsc --noEmit` reports no game-session errors and the @ts-expect-error line is satisfied
- Full suite green at wave merge (persistence/broadcast/AI paths unaffected)
</verification>

<success_criteria>
SESS-01 shipped: session.runner is a read-only facade, performAction unreachable at type and runtime, all write paths intact, zero consumer breakage.
</success_criteria>

<output>
Create `.planning/phases/134-ui-session-interaction-guardrails/134-04-SUMMARY.md` when done. Record the RED state of the facade test per PROC-02 and confirm the 5 rebuild sites.
</output>
