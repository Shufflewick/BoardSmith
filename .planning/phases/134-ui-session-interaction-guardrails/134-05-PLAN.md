---
phase: 134-ui-session-interaction-guardrails
plan: 05
type: execute
wave: 3
depends_on: [134-02, 134-03, 134-04]
files_modified:
  - docs/custom-ui-guide.md
autonomous: false
requirements: [UIX-01, UIX-02, UIX-03, PROC-02]
must_haves:
  truths:
    - "docs/custom-ui-guide.md models checking start()'s returned ActionResult (and states it reflects only the two synchronous pre-checks, not the eventual server outcome)"
    - "docs models the fill() multiSelect rejection and the corrective toggleMultiSelect()/array pattern"
    - "docs has a 'Board Sizing' section whose title/content matches the UIX-03 dev-console-error copy, so the error's pointer resolves to a real section"
    - "toast surfacing and drag gating are verified live in a reference game via boardsmith dev, and the dev server is killed afterward"
  artifacts:
    - path: "docs/custom-ui-guide.md"
      provides: "result-checking guidance (UIX-01), multiSelect fill guidance (UIX-02), Board Sizing section (UIX-03)"
      contains: "Board Sizing"
  key_links:
    - from: "UIX-03 console.error message"
      to: "docs/custom-ui-guide.md 'Board Sizing' section"
      via: "the error text names the section, which must now exist"
      pattern: "Board Sizing"
---

<objective>
Land the same-phase documentation updates (DOCX-04) that the shipped guardrails require, and perform the CLAUDE.md-mandated live browser verification of the two user-visible behaviors (failure toast + drag gating).

Purpose: the UIX-03 console.error points developers at a "Board Sizing" doc section — that section must actually exist. The UIX-01 result-checking and UIX-02 multiSelect guidance replace fire-and-forget/scalar examples that now behave differently. Browser verification is a hard project rule for UI changes.
Output: updated custom-ui-guide.md + a human-verified live check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-ui-session-interaction-guardrails/134-CONTEXT.md
@.planning/phases/134-ui-session-interaction-guardrails/134-RESEARCH.md
@.planning/phases/134-ui-session-interaction-guardrails/134-UI-SPEC.md
@.planning/phases/134-ui-session-interaction-guardrails/134-02-SUMMARY.md
@.planning/phases/134-ui-session-interaction-guardrails/134-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/custom-ui-guide.md — result-checking, multiSelect, Board Sizing (DOCX-04)</name>
  <files>docs/custom-ui-guide.md</files>
  <read_first>
    - docs/custom-ui-guide.md (RE-GREP current offsets — research traced ~120 fire-and-forget start() example, ~141 unchecked fill() example, ~242/316 candidate spots for the new section; the file may have shifted, find the actual current locations)
    - .planning/phases/134-ui-session-interaction-guardrails/134-UI-SPEC.md (Copywriting Contract rows UIX-01/UIX-02/UIX-03 — the doc must match the shipped copy)
    - .planning/phases/134-ui-session-interaction-guardrails/134-RESEARCH.md (Pitfall 1 — do NOT imply start()'s result reflects the eventual server outcome; UIX-03 Structural Fix Evaluation — explain why max-content sizing exists so the corrective guidance is accurate)
    - .planning/phases/134-ui-session-interaction-guardrails/134-02-SUMMARY.md (as-shipped start()/fill() behavior)
    - .planning/phases/134-ui-session-interaction-guardrails/134-03-SUMMARY.md (as-shipped 0x0 console.error copy — the doc section title must match its pointer)
  </read_first>
  <action>Rewrite the fire-and-forget start() example to show awaiting and branching on the returned ActionResult, with an explicit note that start()'s result reflects ONLY its two synchronous pre-checks (availability, metadata) and that eventual server failures surface via the toast / should use execute() (per Pitfall 1). Replace the unchecked fill() example with a multiSelect example showing the rejection error and the corrective toggleMultiSelect()/confirmMultiSelect()-or-array pattern. Add a new "Board Sizing" section (title must match the UIX-03 console.error pointer exactly) covering: why .game-shell__zoom-container uses width:max-content (intrinsic-size measurement for zoom-to-fit), why percentage-width / container-type board roots collapse to 0x0 inside it, and the corrective pattern (give the board root a definite width, or measure .boardregion). Keep copy actionable and free of internal paths beyond the intended pointer.</action>
  <verify>
    <automated>grep -c "Board Sizing" docs/custom-ui-guide.md</automated>
  </verify>
  <done>Docs model result-checking, the multiSelect rejection/correction, and contain a "Board Sizing" section matching the UIX-03 error pointer.</done>
  <acceptance_criteria>
    - start() example checks the ActionResult and notes the synchronous-only semantics (no implication it reflects eventual server outcome)
    - fill() multiSelect example shows the rejection + toggleMultiSelect/array correction
    - A "Board Sizing" section exists whose title matches the shipped UIX-03 console.error copy
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live browser verification of toast + drag gating (kill dev server after)</name>
  <what-built>Central failure toast (UIX-01) and dragProps when-gating (UIX-04), shipped in Plans 02-03; plus the UIX-03 dev-console.error and UIX-02 fill guard.</what-built>
  <how-to-verify>
    1. Start a reference game dev host: `cd ~/BoardSmithGames/go-fish && npx boardsmith dev` (serves :5173). For drag-gating exercise use a game with drag (e.g. `~/BoardSmithGames/hex`) if go-fish has no draggable pieces.
    2. In the browser, trigger an action failure from the game UI (e.g. attempt an action that the engine will reject / that is not currently available). Confirm a red error toast (`--bsg-danger`) appears with the actionable engine message, and that it is announced (assertive live region). This proves UIX-01 surfaces failures for the shipped UI the same way a custom UI now would.
    3. If exercising drag: with a `dragProps({ when: false })`-gated element (or a devtools snippet calling dragProps with when:false), confirm the element is not draggable and attaches no drag handlers.
    4. Optionally, in devtools, mount a percentage-width board to confirm the UIX-03 console.error fires (and does NOT fire on a normally-sized board).
    5. Kill the dev server (`Ctrl-C` / kill the :5173 process) — never leave it running.
  </how-to-verify>
  <resume-signal>Type "approved" if the toast surfaces and drag gating behaves, or describe what you observed. Confirm the dev server was killed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-console error pointer → docs | The UIX-03 error names a doc section that must exist for the guidance to be reachable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-10 | Repudiation (broken pointer) | UIX-03 error -> "Board Sizing" section | mitigate | Doc section title verified to match the shipped console.error copy; grep gate confirms the section exists |
| T-134-11 | Information disclosure (docs) | corrective examples | accept | Examples use public API only; no internal paths beyond the intended doc pointer |

No package installs in this plan → no supply-chain row.
</threat_model>

<verification>
- `grep -c "Board Sizing" docs/custom-ui-guide.md` >= 1
- Human-verify checkpoint approved; dev server confirmed killed
- Full suite green at phase gate (`npm test`)
</verification>

<success_criteria>
Docs match the shipped guardrail behavior; toast + drag gating verified live; no dev server left running.
</success_criteria>

<output>
Create `.planning/phases/134-ui-session-interaction-guardrails/134-05-SUMMARY.md` when done. Record the browser-verification outcome and confirm the dev server was killed.
</output>
