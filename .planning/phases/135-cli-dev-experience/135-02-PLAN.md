---
phase: 135-cli-dev-experience
plan: 02
type: execute
wave: 2
depends_on: [135-01]
files_modified:
  - src/cli/cli.ts
  - src/cli/commands/init.ts
  - src/cli/commands/init.test.ts
autonomous: true
requirements: [CLIX-04, CLIX-05, PROC-02]
must_haves:
  truths:
    - "`boardsmith init` has no -t/--template option — the flag is gone from cli.ts registration and InitOptions has no template field"
    - "`boardsmith dev --host` help text states the real default (127.0.0.1) and documents --lan as the LAN opt-in"
    - "`boardsmith dev` registers a --lan boolean flag (shorthand for --host 0.0.0.0)"
    - "init.test.ts has a regression test that fails on the pre-fix InitOptions (PROC-02)"
  artifacts:
    - path: "src/cli/cli.ts"
      provides: "init command without -t/--template; corrected --host help; new --lan flag"
      contains: "--lan"
    - path: "src/cli/commands/init.ts"
      provides: "InitOptions without template field"
    - path: "src/cli/commands/init.test.ts"
      provides: "PROC-02 regression asserting no template surface"
  key_links:
    - from: "src/cli/cli.ts dev command"
      to: "src/cli/commands/dev.ts (Plan 06)"
      via: "--lan / --host options consumed by devCommand"
      pattern: "--lan"
---

<objective>
Clean up the `boardsmith` CLI option surface: (CLIX-05) remove the dead `-t/--template` flag from both `cli.ts` and `InitOptions`; (CLIX-04, registration half) correct the misleading `--host` help text to state the real default and register a `--lan` shorthand. The dev-side behavior (default 127.0.0.1 + loud banner) lands in Plan 06, which consumes the `--lan`/`--host` options this plan registers.

Purpose: F33 is the canonical silent no-op — a flag that parses and does nothing. Per No Backward Compatibility, it is removed outright, not deprecated. F32's registration surface (help copy + --lan) must be correct before Plan 06 wires the handler behavior.
Output: trimmed commander registration + InitOptions, plus a PROC-02 regression in init.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-cli-dev-experience/135-CONTEXT.md
@.planning/phases/135-cli-dev-experience/135-PATTERNS.md
@.planning/phases/135-cli-dev-experience/135-FINDINGS-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove -t/--template from cli.ts and InitOptions (CLIX-05)</name>
  <files>src/cli/cli.ts, src/cli/commands/init.ts, src/cli/commands/init.test.ts</files>
  <read_first>
    - src/cli/cli.ts:24-28 (the init command block; the `.option('-t, --template ...')` line to delete — target the `.command().description().action()` shape used by validate/lint)
    - src/cli/commands/init.ts:15-17 (InitOptions interface with the dead `template: string` field; confirm no read site in initCommand via full-file grep)
    - src/cli/commands/init.test.ts (existing describe blocks / generateGameTs assertions — the analog to extend)
    - src/cli/cli.ts:62 (the pack command's -t/--target — a DIFFERENT flag; do NOT touch it)
  </read_first>
  <behavior>
    - Test: parsing `boardsmith init myGame` (no template flag) succeeds and produces the same scaffold as before.
    - Test: InitOptions has no `template` property (type-level / structural assertion that the field is gone).
    - Test (RED on pre-fix code): asserting the init command's registered options do NOT include a `template` option fails before the fix, passes after.
  </behavior>
  <action>Delete the `.option('-t, --template <template>', ...)` line from the init command registration in cli.ts (leaving `.command('init <name>').description(...).action(initCommand)`). Remove the `template: string` field from the `InitOptions` interface in init.ts. Since RESEARCH.md A2 confirmed zero read sites, no handler logic changes. Add/extend a regression test in init.test.ts that asserts the template surface is gone — write it RED first against the pre-fix code (per PROC-02) and record the RED in the SUMMARY. Do not touch the pack command's `-t/--target` flag.</action>
  <verify>
    <automated>npx vitest run src/cli/commands/init.test.ts --reporter=dot</automated>
  </verify>
  <done>`-t/--template` is absent from cli.ts, `InitOptions` has no template field, and init.test.ts's new regression passes (and demonstrably failed on pre-fix code).</done>
  <acceptance_criteria>
    - `grep -n "template" src/cli/cli.ts` shows no init-command template option
    - `grep -n "template" src/cli/commands/init.ts` shows no InitOptions.template field
    - init.test.ts regression asserts absence of the template surface and is green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Correct --host help text and register --lan shorthand (CLIX-04 registration)</name>
  <files>src/cli/cli.ts</files>
  <read_first>
    - src/cli/cli.ts:35 (current misleading `--host` help string)
    - src/cli/cli.ts:39 (the `--lock-teaching` boolean-flag registration — the style analog for a boolean --lan)
    - .planning/phases/135-cli-dev-experience/135-CONTEXT.md (CLIX-04: default 127.0.0.1; --lan is shorthand for --host 0.0.0.0; loud banner on non-localhost)
    - 135-PATTERNS.md:248-252 — WARNING: its example `--host` help string is STALE (claims the default stays 0.0.0.0, the same error as RESEARCH's State-of-the-Art table). Do NOT copy it; the locked default is 127.0.0.1.
  </read_first>
  <action>In cli.ts's dev command registration, rewrite the `--host` help text to state the ACTUAL new default per the CONTEXT locked decision — local-only 127.0.0.1 by default — and to point users at --lan / --host 0.0.0.0 for LAN play (implementing D-CLIX-04; note: both 135-RESEARCH.md's State-of-the-Art table AND 135-PATTERNS.md:248-252's example help string wrongly say the default stays 0.0.0.0 — do NOT copy either; CONTEXT.md's default-127.0.0.1 decision governs). Register a new `--lan` boolean flag (no value) as the network-exposure shorthand, following the `--lock-teaching` boolean style. Do not implement resolution/banner behavior here — dev.ts (Plan 06) consumes `options.lan`/`options.host`. Ensure help text and the --lan description name the security tradeoff plainly ("serves to your whole network").</action>
  <verify>
    <automated>npx vitest run src/cli --reporter=dot</automated>
  </verify>
  <done>cli.ts registers `--lan`, and the `--host` help text states default 127.0.0.1 with the LAN opt-in path; full src/cli suite green.</done>
  <acceptance_criteria>
    - `grep -n "\-\-lan" src/cli/cli.ts` shows the registered boolean flag
    - `--host` help text no longer implies 0.0.0.0-by-default; it names 127.0.0.1 as default
    - `npx vitest run src/cli` is green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI args → dev host bind address | The --host/--lan options this plan registers ultimately choose whether the dev server is exposed to the LAN (Info Disclosure surface, resolved in Plan 06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-04 | Information Disclosure | dev host bind (--host/--lan registration) | mitigate | Help copy makes network exposure explicit and opt-in; the default-localhost + banner enforcement lands in Plan 06 |
| T-135-05 | Tampering (silent no-op flag) | init -t/--template | mitigate | Flag removed entirely so it can never silently swallow an operator's intent |

No package installs in this plan → no supply-chain row applies.
</threat_model>

<verification>
- `npx vitest run src/cli --reporter=dot` green
- No `template` option on the init command; `--lan` registered on dev; `--host` help states default 127.0.0.1
</verification>

<success_criteria>
CLIX-05 fully resolved (flag + type field gone with a RED-proven regression); CLIX-04 registration surface (help + --lan) ready for Plan 06 to consume.
</success_criteria>

<output>
Create `.planning/phases/135-cli-dev-experience/135-02-SUMMARY.md` when done (record the RED test evidence for PROC-02).
</output>
