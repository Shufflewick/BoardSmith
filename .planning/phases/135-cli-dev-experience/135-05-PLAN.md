---
phase: 135-cli-dev-experience
plan: 05
type: execute
wave: 2
depends_on: [135-01]
files_modified:
  - src/cli/commands/validate.ts
  - src/cli/commands/validate.test.ts
  - src/cli/lib/config-schema.ts
  - src/cli/lib/boardsmith.schema.json
  - src/cli/lib/bundle-limits.ts
autonomous: true
requirements: [CLIX-01, CLIX-02, CLIX-03, PROC-02]
must_haves:
  truths:
    - "`boardsmith validate` rejects any unknown top-level boardsmith.json key with a did-you-mean suggestion (e.g. 'gameOption' -> 'gameOptions')"
    - "A leftover `playerCount` key in boardsmith.json is rejected with a pointed migration message (player count now derived from gameDefinition)"
    - "The allowed-key set and the did-you-mean logic are hand-rolled (no new dependency) and sourced from a single shipped schema file (src/cli/lib/boardsmith.schema.json) so keys cannot drift"
    - "The bundle-size limit is one exported constant (bundle-limits.ts MAX_BUNDLE_SIZE = 50 * 1024 * 1024) matching the external server gate; validate.ts imports it and the comment agrees"
    - "validate.test.ts asserts unknown-key rejection, the playerCount migration message, and MAX_BUNDLE_SIZE === 50MB — RED on pre-fix code (PROC-02)"
  artifacts:
    - path: "src/cli/lib/boardsmith.schema.json"
      provides: "Single source of valid top-level boardsmith.json keys (JSON Schema, additionalProperties:false)"
    - path: "src/cli/lib/config-schema.ts"
      provides: "allowed-key set derived from the schema + hand-rolled levenshtein suggestKey + findUnknownKeys, consumed by validate.ts and dev.ts"
      contains: "suggestKey"
    - path: "src/cli/lib/bundle-limits.ts"
      provides: "MAX_BUNDLE_SIZE = 50 * 1024 * 1024 shared constant"
      contains: "50 * 1024 * 1024"
    - path: "src/cli/commands/validate.ts"
      provides: "unknown-key rejection + playerCount migration message + 50MB bundle check"
  key_links:
    - from: "src/cli/commands/validate.ts"
      to: "config-schema.ts findUnknownKeys/suggestKey"
      via: "new ValidationResult check registered alongside the existing checks"
      pattern: "findUnknownKeys"
    - from: "config-schema.ts"
      to: "boardsmith.schema.json"
      via: "Object.keys(schema.properties) as the single allowed-key source"
      pattern: "schema.properties"
---

<objective>
Turn `boardsmith validate` into a real config gate: (CLIX-02) reject unknown top-level `boardsmith.json` keys with hand-rolled did-you-mean suggestions; (CLIX-01, reject half) reject a leftover `playerCount` key with a migration message; (CLIX-03) fix the bundle-size constant from a stale 200MB to the actual 50MB server limit, sourced from one exported constant. This plan also creates the shared `config-schema.ts` that Plan 06 consumes for dev-startup warnings.

Purpose: F22 (silent typo'd keys), F21 (constant disagrees with its own comment and the real server), and F9's reject-leftover-key half. Per CLAUDE.md no-new-deps, the allowed-key check + levenshtein are hand-rolled; per the CONTEXT/orchestrator "prefer shipping a schema file" steer, the allowed keys live in one shipped `boardsmith.schema.json` that config-schema.ts derives from — single source, no drift, editor-usable.
Output: two new lib modules, a hardened validate.ts, and a new validate.test.ts (validate.ts has zero coverage today).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-cli-dev-experience/135-CONTEXT.md
@.planning/phases/135-cli-dev-experience/135-RESEARCH.md
@.planning/phases/135-cli-dev-experience/135-PATTERNS.md
@.planning/phases/135-cli-dev-experience/135-FINDINGS-VERIFICATION.md

<interfaces>
<!-- config-schema.ts is a NEW module (no repo analog) consumed by BOTH validate.ts (this plan) and dev.ts (Plan 06). Define these exports as the contract Plan 06 imports. -->
Target exports for src/cli/lib/config-schema.ts:
  export const ALLOWED_TOP_LEVEL_KEYS: readonly string[];   // derived from boardsmith.schema.json properties
  export function suggestKey(unknown: string): string | undefined;   // levenshtein-nearest allowed key within a small edit distance
  export function findUnknownKeys(config: Record<string, unknown>): { key: string; suggestion?: string }[];

ValidationResult shape to reuse — src/cli/commands/validate.ts:8-13:
  interface ValidationResult { name: string; passed: boolean; message: string; details?: string[]; }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship boardsmith.schema.json + hand-rolled config-schema.ts (CLIX-02 foundation)</name>
  <files>src/cli/lib/boardsmith.schema.json, src/cli/lib/config-schema.ts, src/cli/commands/validate.test.ts</files>
  <read_first>
    - src/cli/lib/project-scaffold.ts:103-118 (the canonical set of legitimate top-level keys the scaffold emits — name, displayName, description, estimatedDuration, complexity, categories, thumbnail, scoreboard, ui — the allowed-key list must cover exactly these plus any other keys validate.ts/build.ts legitimately read like version/paths; enumerate from the scaffold + validate.ts reads, NOT from guesswork)
    - src/cli/commands/validate.ts (full: reads of config.* to ensure every legitimately-consumed key is in the allowed set; ValidationResult shape 8-13; results array 31-46)
    - src/cli/lib/sandbox-scan.ts (structural precedent for a small standalone lib module with named exports imported by validate.ts)
    - .planning/phases/135-cli-dev-experience/135-RESEARCH.md (Don't Hand-Roll table — classic DP levenshtein ~20 lines, no dependency; Open Question 2 — ship a real schema file)
    - .planning/phases/135-cli-dev-experience/135-PATTERNS.md (No Analog Found: config-schema.ts is a new pattern)
  </read_first>
  <behavior>
    - Test: ALLOWED_TOP_LEVEL_KEYS equals Object.keys(boardsmith.schema.json.properties) (single source, no drift).
    - Test: suggestKey('gameOption') returns 'gameOptions' (nearest within edit-distance threshold); suggestKey('completelyUnrelatedXyz') returns undefined.
    - Test: findUnknownKeys({ name:'x', gameOption:{} }) returns [{ key:'gameOption', suggestion:'gameOptions' }] and ignores valid keys.
  </behavior>
  <action>Create `src/cli/lib/boardsmith.schema.json` — a draft-07 JSON Schema with `additionalProperties: false` whose `properties` lists exactly the legitimate top-level keys (derive the list from generateBoardsmithJson's emitted keys plus any keys validate.ts/build.ts legitimately read; NOTE playerCount is deliberately NOT a valid key). Create `src/cli/lib/config-schema.ts` exporting `ALLOWED_TOP_LEVEL_KEYS` (from `Object.keys(schema.properties)` — single source), a hand-rolled classic-DP `levenshtein` helper, `suggestKey(unknown)` (nearest allowed key within a small edit-distance threshold, else undefined), and `findUnknownKeys(config)`. No new dependency (CLAUDE.md). Create validate.test.ts (new) and write these three behaviors RED first against the not-yet-existing module (PROC-02), then GREEN. Include `gameOptions`/`playerOptions`/`colorPalette` typo cases from the F22 finding.</action>
  <verify>
    <automated>npx vitest run src/cli/commands/validate.test.ts --reporter=dot</automated>
  </verify>
  <done>boardsmith.schema.json ships the key set; config-schema.ts exports suggestKey/findUnknownKeys/ALLOWED_TOP_LEVEL_KEYS derived from it; validate.test.ts covers the did-you-mean behavior green.</done>
  <acceptance_criteria>
    - ALLOWED_TOP_LEVEL_KEYS is derived from the schema file (asserted in test)
    - suggestKey maps 'gameOption' -> 'gameOptions' and returns undefined for far-off strings
    - No new dependency added (levenshtein hand-rolled)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire unknown-key rejection + playerCount migration into validate.ts (CLIX-02/CLIX-01)</name>
  <files>src/cli/commands/validate.ts, src/cli/commands/validate.test.ts</files>
  <read_first>
    - src/cli/commands/validate.ts:86-143 (validateMetadata — required list at 94 includes 'playerCount' to REMOVE; playerCount object checks 102-107 to REMOVE; issues[] accumulation pattern)
    - src/cli/commands/validate.ts:31-46 (results array where the new unknown-key check registers)
    - src/cli/lib/config-schema.ts (findUnknownKeys/suggestKey from Task 1)
    - .planning/phases/135-cli-dev-experience/135-PATTERNS.md (validate.ts section — ValidationResult reuse; the migration-message copy shape "Unknown key 'playerCount' — player count is now derived from your gameDefinition, remove this key.")
  </read_first>
  <behavior>
    - Test: validate on a config with `gameOption` (typo) fails with a message naming the unknown key and suggesting `gameOptions`.
    - Test: validate on a config with a leftover `playerCount` key fails with the migration message (player count derived from gameDefinition).
    - Test (RED on pre-fix): pre-fix validate PASSES a config carrying an unknown key / playerCount — the new check flips it to FAIL.
  </behavior>
  <action>Add a new `ValidationResult`-returning check that calls `findUnknownKeys(config)` and fails with a per-key message including the `suggestKey` suggestion when present; register it in the results array alongside the existing checks. Special-case a leftover `playerCount` key with the pointed migration message (folding CLIX-01's reject-half into the unknown-key path). Remove `playerCount` from validateMetadata's `required` list and delete the now-invalid playerCount min/max object checks (102-107). Extend validate.test.ts with the unknown-key + migration-message regressions, RED-first per PROC-02 (record RED in SUMMARY). Update any BoardSmith-internal test fixtures / template boardsmith.json that still carry playerCount so the full suite stays green (games/MERC are Phase 138's scope — only in-repo fixtures here).</action>
  <verify>
    <automated>npx vitest run src/cli/commands/validate.test.ts --reporter=dot</automated>
  </verify>
  <done>validate rejects unknown keys with did-you-mean and rejects leftover playerCount with a migration message; playerCount removed from required; regressions green and RED-proven.</done>
  <acceptance_criteria>
    - validate FAILs on an unknown top-level key and names a suggestion
    - validate FAILs on a leftover playerCount key with the migration message
    - `grep -n "playerCount" src/cli/commands/validate.ts` shows no required-list entry / min-max checks
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Single 50MB bundle-size constant (CLIX-03)</name>
  <files>src/cli/lib/bundle-limits.ts, src/cli/commands/validate.ts, src/cli/commands/validate.test.ts</files>
  <read_first>
    - src/cli/commands/validate.ts:221-262 (validateBundleSize — local `maxTotalBundle = 200 * 1024 * 1024` at 226 with the comment stating 50MB; usages at 249-259)
    - ~/ShufflewickPubGames/src/upload.ts:4 (authoritative external gate MAX_BUNDLE_SIZE = 50 * 1024 * 1024)
    - .planning/phases/135-cli-dev-experience/135-RESEARCH.md (CLIX-03 — the CODE is wrong, not the comment; discretion: constant lives in an importable module both validate and any server code can reference)
    - .planning/phases/135-cli-dev-experience/135-FINDINGS-VERIFICATION.md (F21 verdict — 50MB is the authoritative value)
  </read_first>
  <behavior>
    - Test: bundle-limits.ts exports MAX_BUNDLE_SIZE === 50 * 1024 * 1024.
    - Test (RED on pre-fix): asserting the enforced total-bundle limit equals 50MB fails against the pre-fix 200MB local.
  </behavior>
  <action>Create `src/cli/lib/bundle-limits.ts` exporting `MAX_BUNDLE_SIZE = 50 * 1024 * 1024` with a comment referencing the external ~/ShufflewickPubGames/src/upload.ts:4 gate as the source of truth to mirror. In validate.ts, delete the local `maxTotalBundle = 200 * 1024 * 1024` and import/use `MAX_BUNDLE_SIZE` instead; update the accompanying comment so code and comment agree (both 50MB). Fix, don't rewrite the comment to 200MB (Anti-Pattern). Extend validate.test.ts asserting the exported constant equals 50MB — RED first (PROC-02) against the pre-fix 200MB, record RED in SUMMARY.</action>
  <verify>
    <automated>npx vitest run src/cli/commands/validate.test.ts --reporter=dot</automated>
  </verify>
  <done>MAX_BUNDLE_SIZE = 50MB is the single source, validate.ts imports it, comment agrees, regression green and RED-proven.</done>
  <acceptance_criteria>
    - `grep -rn "200 \* 1024" src/cli/commands/validate.ts` returns nothing
    - bundle-limits.ts exports MAX_BUNDLE_SIZE === 50 * 1024 * 1024
    - validate.ts imports and uses the shared constant; comment states 50MB
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-edited boardsmith.json → validate/publish gate | Unknown/misspelled keys and a stale bundle limit let misconfiguration pass silently to publish |
| local bundle-size pre-check → external games-worker gate | The CLI check is advisory; the worker at ~/ShufflewickPubGames/src/upload.ts is authoritative — CLI must MATCH it, not diverge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-09 | Tampering (silent misconfig) | validate unknown-key gap | mitigate | Reject unknown top-level keys with did-you-mean; allowed set is single-sourced from the shipped schema |
| T-135-10 | Information Disclosure (false-pass on oversize bundle) | stale 200MB limit | mitigate | Align the constant to the authoritative 50MB server gate via one exported module both sides reference |
| T-135-11 | Tampering (key-list drift) | ALLOWED_TOP_LEVEL_KEYS | mitigate | Derive keys from the shipped schema file (Object.keys(properties)) so validator and schema cannot diverge |

No package installs in this plan → no supply-chain row applies (levenshtein hand-rolled, no dependency added).
</threat_model>

<verification>
- `npx vitest run src/cli/commands/validate.test.ts --reporter=dot` green
- Unknown-key rejection + playerCount migration + 50MB constant all covered
</verification>

<success_criteria>
validate is a real gate: unknown keys and leftover playerCount are rejected with actionable messages, and the bundle limit matches the authoritative 50MB server gate from a single source. config-schema.ts is ready for Plan 06's dev-startup warning.
</success_criteria>

<output>
Create `.planning/phases/135-cli-dev-experience/135-05-SUMMARY.md` when done (record RED evidence for each PROC-02 regression).
</output>
