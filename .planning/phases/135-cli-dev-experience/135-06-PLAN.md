---
phase: 135-cli-dev-experience
plan: 06
type: execute
wave: 3
depends_on: [135-01, 135-02, 135-05]
files_modified:
  - src/cli/commands/dev.ts
  - src/cli/commands/dev.test.ts
autonomous: true
requirements: [CLIX-01, CLIX-02, CLIX-04, CLIX-06, PROC-02]
must_haves:
  truths:
    - "`boardsmith dev` defaults its bind host to 127.0.0.1 (local-only); --lan or --host 0.0.0.0 is required to expose the LAN"
    - "When the effective bind host is non-localhost, dev prints a loud startup banner naming the network-exposure tradeoff"
    - "Non-numeric --players/--port/--ai error immediately with an actionable message (simulate.ts fail-fast style); out-of-range --players ERRORS (naming the game's max) instead of silently clamping"
    - "--ai seats are validated against the effective post-resolution player count (validation moved after that count is known), not the raw pre-clamp value"
    - "dev.ts resolves minPlayers/maxPlayers from a single read of gameDefinition (config.playerCount/minPlayers fallbacks removed)"
    - "dev startup warns loudly on unknown top-level boardsmith.json keys via config-schema.findUnknownKeys"
    - "dev.test.ts asserts default host, banner-on-non-localhost, numeric-flag errors, and post-move --ai validation WITHOUT opening real sockets (createViteServer/WSS mocked or pure functions extracted) — RED on pre-fix code (PROC-02)"
  artifacts:
    - path: "src/cli/commands/dev.ts"
      provides: "default 127.0.0.1 + --lan resolution + non-localhost banner (CLIX-04); fail-fast numeric flags + moved --ai check + out-of-range error (CLIX-06); single gameDefinition player-count read (CLIX-01); unknown-key startup warn (CLIX-02)"
      contains: "findUnknownKeys"
    - path: "src/cli/commands/dev.test.ts"
      provides: "PROC-02 regressions without real sockets (new file)"
  key_links:
    - from: "dev.ts host resolution"
      to: "options.lan / options.host"
      via: "--lan (registered in Plan 02) resolves to 0.0.0.0; default 127.0.0.1"
      pattern: "127.0.0.1"
    - from: "dev.ts startup"
      to: "config-schema.findUnknownKeys"
      via: "loud warn on unknown boardsmith.json keys (Plan 05 module)"
      pattern: "findUnknownKeys"
    - from: "dev.ts --ai validation"
      to: "effectivePlayerCount"
      via: "moved to after the effective count is resolved (dev.ts:393 region)"
      pattern: "effectivePlayerCount"
---

<objective>
Harden the `boardsmith dev` command handler: (CLIX-04) default the bind host to 127.0.0.1 and print a loud banner when binding non-localhost, resolving the `--lan` shorthand registered in Plan 02; (CLIX-06) fail fast on non-numeric `--players`/`--port`/`--ai`, ERROR (not clamp) on out-of-range `--players`, and MOVE the `--ai` bounds check to after the effective player count is known; (CLIX-01) collapse the three-way minPlayers/maxPlayers fallback to a single `gameDefinition` read; (CLIX-02) warn loudly at startup on unknown boardsmith.json keys via the Plan 05 `config-schema` module.

Purpose: dev.ts is where the phase's flag/host footguns live. CONTEXT.md's locked CLIX-04 decision is default 127.0.0.1 (135-RESEARCH.md's State-of-the-Art table wrongly said the default stays 0.0.0.0 — the CONTEXT decision governs; see the F32 verdict in 135-FINDINGS-VERIFICATION.md). CLIX-06's --ai ordering is the Pitfall-3 trap: the effective count is only known after the game runtime loads (~dev.ts:393), so the check must move there, not just change its operand.
Output: hardened dev.ts + a new dev.test.ts that verifies behavior without binding real sockets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-cli-dev-experience/135-CONTEXT.md
@.planning/phases/135-cli-dev-experience/135-RESEARCH.md
@.planning/phases/135-cli-dev-experience/135-PATTERNS.md
@.planning/phases/135-cli-dev-experience/135-FINDINGS-VERIFICATION.md

<interfaces>
<!-- Consumed from Plan 05 (already merged in Wave 2). Import, do not redefine. -->
From src/cli/lib/config-schema.ts:
  export function findUnknownKeys(config: Record<string, unknown>): { key: string; suggestion?: string }[];
  export function suggestKey(unknown: string): string | undefined;

Fail-fast numeric pattern to copy — src/cli/commands/simulate.ts:145-153:
  const playersCount = Number(options.players);
  if (!Number.isInteger(playersCount) || playersCount < 1) {
    console.error(chalk.red(`Error: --players must be a positive integer, got "${options.players}"`));
    process.exit(1);
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fail-fast numeric flags + move --ai validation after effective count (CLIX-06)</name>
  <files>src/cli/commands/dev.ts, src/cli/commands/dev.test.ts</files>
  <read_first>
    - src/cli/commands/dev.ts:280-281 (unguarded parseInt of --port/--players)
    - src/cli/commands/dev.ts:302-308 (invalidAiPlayers filter against raw pre-clamp playerCount — the block to MOVE)
    - src/cli/commands/dev.ts:359 (minPlayers/maxPlayers resolved) and :393 (effectivePlayerCount = Math.min(Math.max(playerCount, minPlayers), maxPlayers))
    - src/cli/commands/simulate.ts:145-153 (the fail-fast numeric idiom to copy)
    - src/cli/commands/simulate.test.ts:101-136 (arg-validation test style; mkdtempSync fixture 101-123 — arg validation short-circuits before any Vite/WS work, so no real server is needed for these tests)
    - .planning/phases/135-cli-dev-experience/135-RESEARCH.md (Pitfall 3 — --ai must move after effectivePlayerCount; out-of-range --players ERRORS per CLIX-06, so the Math.min/Math.max clamp becomes an error branch)
  </read_first>
  <behavior>
    - Test: `--port abc` / `--players abc` / `--ai abc` each exit non-zero with an actionable message before any server work.
    - Test: `--players 6` for a game whose max is 4 ERRORS naming the max (no silent clamp).
    - Test (RED on pre-fix): pre-fix `--players 6` clamps silently (no error) — the fix flips it to an error; pre-fix `--ai 5` in a 2-player game validates against the wrong (raw) count.
  </behavior>
  <action>Replace dev.ts's unguarded `parseInt(options.port)` / `parseInt(options.players)` (280-281) with the simulate.ts `Number(...)` + `Number.isInteger` fail-fast guards (actionable messages naming the flag and the bad value), keeping dev.ts's `process.exit(1)` convention. Apply the same NaN guard to `--ai` seat parsing. MOVE the `invalidAiPlayers` block (302-308) to AFTER `effectivePlayerCount` is computed (393) and validate seats against `effectivePlayerCount`, not the raw pre-clamp `playerCount` (Pitfall 3). Change the out-of-range `--players` handling: instead of `Math.min(Math.max(...))` silently clamping, ERROR when `playerCount` is outside [minPlayers, maxPlayers] with a message naming the game's min/max (per CLIX-06). Extract the pure validation into a testable helper if needed so dev.test.ts can assert without a real Vite/WS server. Create dev.test.ts (new) with the RED-first regressions (PROC-02), record RED in SUMMARY.</action>
  <verify>
    <automated>npx vitest run src/cli/commands/dev.test.ts --reporter=dot</automated>
  </verify>
  <done>Non-numeric flags error immediately; out-of-range --players errors naming the max; --ai validated against effectivePlayerCount post-move; regressions green and RED-proven.</done>
  <acceptance_criteria>
    - `--port abc`/`--players abc`/`--ai abc` exit non-zero with actionable messages
    - `--players` out of [min,max] ERRORS (no clamp) naming the bound
    - The invalidAiPlayers check runs after effectivePlayerCount and compares against it
    - dev.test.ts opens no real sockets
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Default host 127.0.0.1 + non-localhost banner + collapse player-count read (CLIX-04/CLIX-01)</name>
  <files>src/cli/commands/dev.ts, src/cli/commands/dev.test.ts</files>
  <read_first>
    - src/cli/commands/dev.ts:283 (`const host = options.host ?? '0.0.0.0'` — the default to change to 127.0.0.1 + resolve --lan)
    - src/cli/commands/dev.ts:359-360 (the three-way minPlayers/maxPlayers fallback to collapse to a single gameDefinition read)
    - src/cli/commands/dev.ts:596-601 (existing per-URL network banner — do NOT duplicate; the new banner is the default-exposure warning, distinct)
    - .planning/phases/135-cli-dev-experience/135-CONTEXT.md (CLIX-04 locked: default 127.0.0.1; --lan == --host 0.0.0.0; loud banner on non-localhost)
    - .planning/phases/135-cli-dev-experience/135-FINDINGS-VERIFICATION.md (F32 verdict — default-127.0.0.1 governs over the RESEARCH State-of-the-Art table)
  </read_first>
  <behavior>
    - Test: with no --host and no --lan, the resolved bind host is 127.0.0.1.
    - Test: --lan (or --host 0.0.0.0) resolves to 0.0.0.0 and triggers a loud non-localhost banner naming the exposure ("serving to your whole network — use --host 127.0.0.1 for local-only").
    - Test: with default host, the non-localhost banner is NOT printed.
    - Test: minPlayers/maxPlayers come from gameDefinition alone (no config.playerCount fallback).
  </behavior>
  <action>Change dev.ts's host resolution so the default is 127.0.0.1 (local-only) and `options.lan` resolves to '0.0.0.0' (implementing D-CLIX-04; note in a code comment that this reverses the previous LAN-by-default and that 135-RESEARCH.md's State-of-the-Art table was corrected by the F32 verdict). Add a loud startup banner (chalk.yellow) printed once when the effective host is non-localhost (not 127.0.0.1/localhost), naming the network-exposure tradeoff and the --host 127.0.0.1 opt-out; keep the existing per-URL network banner untouched. Collapse the minPlayers/maxPlayers three-way fallback (359-360) to a single read of `gameDefinition.minPlayers`/`maxPlayers` — remove the `config.playerCount`/`config.minPlayers` fallbacks since boardsmith.json no longer carries them (CLIX-01). Extend dev.test.ts asserting default host, banner presence/absence, and the collapsed read — RED-first per PROC-02, no real sockets (mock createViteServer/WSS or test the extracted resolution helper).</action>
  <verify>
    <automated>npx vitest run src/cli/commands/dev.test.ts --reporter=dot</automated>
  </verify>
  <done>Default host is 127.0.0.1; non-localhost binding prints the loud banner; minPlayers/maxPlayers read solely from gameDefinition; regressions green and RED-proven.</done>
  <acceptance_criteria>
    - Default resolved host is 127.0.0.1; --lan/--host 0.0.0.0 resolves to 0.0.0.0
    - Loud banner prints only when binding non-localhost
    - `grep -n "config.playerCount" src/cli/commands/dev.ts` returns nothing in the minPlayers chain
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Loud dev-startup warning on unknown boardsmith.json keys (CLIX-02)</name>
  <files>src/cli/commands/dev.ts, src/cli/commands/dev.test.ts</files>
  <read_first>
    - src/cli/commands/dev.ts:310-317 (config load via JSON.parse — where the unknown-key warn plugs in, after config is parsed)
    - src/cli/lib/config-schema.ts (findUnknownKeys/suggestKey from Plan 05 — import, do not redefine)
    - .planning/phases/135-cli-dev-experience/135-CONTEXT.md (CLIX-02: dev startup WARNS loudly, does not exit — validate is the hard gate; dev is advisory)
    - .planning/phases/135-cli-dev-experience/135-PATTERNS.md (chalk error/dim conventions)
  </read_first>
  <behavior>
    - Test: dev startup on a config with an unknown key logs a loud warning naming the key and any suggestion, and DOES NOT exit (dev continues; validate is the hard gate).
    - Test: dev startup on a clean config logs no unknown-key warning.
  </behavior>
  <action>After dev.ts parses boardsmith.json (310-317), call `findUnknownKeys(config)` and, for any results, print a loud chalk.yellow warning per key (naming the key and its suggestKey suggestion when present) — a WARNING that does not exit (CONTEXT: dev warns loudly, validate rejects). Reuse the Plan 05 module; do not re-implement key logic. Extend dev.test.ts asserting the warn-on-unknown / silent-on-clean behavior — RED-first per PROC-02.</action>
  <verify>
    <automated>npx vitest run src/cli/commands/dev.test.ts --reporter=dot</automated>
  </verify>
  <done>dev warns loudly (without exiting) on unknown boardsmith.json keys using config-schema; regression green and RED-proven.</done>
  <acceptance_criteria>
    - `grep -n "findUnknownKeys" src/cli/commands/dev.ts` shows the reused import
    - Unknown-key config triggers a warning but not a process exit
    - Clean config triggers no warning
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/network → dev host bind | Binding 0.0.0.0 exposes Vite `/@fs/` project source and an unauthenticated WS join endpoint to anyone on the LAN (Info Disclosure / unauthenticated join) |
| operator CLI args → dev runtime | Non-numeric/out-of-range flags previously clamped/ignored silently, diverging the running game from operator intent |
| boardsmith.json (hand-editable) → dev runtime | Misspelled keys silently ignored, giving false confidence the config took effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-12 | Information Disclosure / Elevation of Privilege | dev host default bind (Vite /@fs/ + unauthenticated WS) | mitigate | Default to 127.0.0.1 (local-only); require explicit --lan/--host 0.0.0.0 for LAN; print a loud banner whenever binding non-localhost so exposure is never silent |
| T-135-13 | Tampering (silent clamp/ignore) | dev numeric flag parsing | mitigate | Fail fast on NaN; ERROR (not clamp) on out-of-range --players; validate --ai against the effective count |
| T-135-14 | Spoofing (config that looks applied but isn't) | unknown boardsmith.json keys | mitigate | Loud startup warning via findUnknownKeys names the ignored/typo'd key and a suggestion |

No package installs in this plan → no supply-chain row applies.
</threat_model>

<verification>
- `npx vitest run src/cli/commands/dev.test.ts --reporter=dot` green (no real sockets bound)
- Default host 127.0.0.1; non-localhost banner; numeric fail-fast; --ai post-move; unknown-key warn
</verification>

<success_criteria>
`boardsmith dev` is local-only by default with loud opt-in exposure, fails fast on bad flags, errors on out-of-range --players, validates --ai against the effective count, reads player count solely from gameDefinition, and warns on unknown config keys — all six dev-surface behaviors covered without binding real sockets in tests.
</success_criteria>

<output>
Create `.planning/phases/135-cli-dev-experience/135-06-SUMMARY.md` when done (record RED evidence for each PROC-02 regression).
</output>
