---
phase: 136-client-sdk-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/136-client-sdk-protocol/136-FINDINGS-VERIFICATION.md
autonomous: true
requirements: [PROC-01]
must_haves:
  truths:
    - "Each of F23, F24, F25, F26, F35, F38 has a recorded verdict (LEGITIMATE or REJECTED) written before any fix task in Plans 02-05 runs"
    - "Each LEGITIMATE verdict cites a current (HEAD, post-Phase-135) file:line trace of the defect, not the audit's original line numbers"
    - "The F25 verdict records the errorCode scope boundary: lobby-manager.ts populates zero errorCode fields today, so the client error type carries errorCode as OPTIONAL and lobby-manager changes are OUT OF SCOPE"
    - "The F26 verdict records the confirmed CreateGameRequest drift (client copy lost playerIds vs canonical protocol.ts) and the barrel re-export chain (index.ts re-exports from ./types.js) that deletions must preserve"
  artifacts:
    - path: ".planning/phases/136-client-sdk-protocol/136-FINDINGS-VERIFICATION.md"
      provides: "Per-finding verification verdicts gating all fix work in Plans 02-05"
      contains: "VERDICT:"
  key_links: []
---

<objective>
Produce the PROC-01 verification gate for Phase 136: an independent re-verification of all six in-scope audit findings (F23/SDK-01, F24/SDK-02, F25/SDK-03, F26/SDK-04, F35/SDK-05, F38/SDK-06) recorded in `136-FINDINGS-VERIFICATION.md`, each with a LEGITIMATE (current file:line trace) or REJECTED (reasoning) verdict — written BEFORE any fix is planned or written.

Purpose: PROC-01 operationalizes the project's "Prove Before Fix" rule. No fix task in Plans 02-05 may run until this document records a verdict for every finding. 136-RESEARCH.md already re-traced all six against current source (zero REJECTED), but two verdicts carry a scope-boundary correction that downstream plans depend on: (a) F25 — the "throw with the server's ErrorCode" decision meets a session-layer reality where `lobby-manager.ts` populates no errorCode field, so the client error type's errorCode must be OPTIONAL and lobby-manager is out of scope; (b) F26 — the barrel re-export chain (`src/client/index.ts` re-exports request types FROM `./types.js`) means every deletion needs a matching re-export line or the public surface breaks. This gate records those boundaries authoritatively.
Output: `.planning/phases/136-client-sdk-protocol/136-FINDINGS-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-client-sdk-protocol/136-CONTEXT.md
@.planning/phases/136-client-sdk-protocol/136-RESEARCH.md
@.planning/phases/136-client-sdk-protocol/136-PATTERNS.md
@.planning/tmp/v4.5-audit-findings.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify connection-lifecycle findings F23, F24, F38 and record verdicts</name>
  <files>.planning/phases/136-client-sdk-protocol/136-FINDINGS-VERIFICATION.md</files>
  <read_first>
    - .planning/tmp/v4.5-audit-findings.json (original audit text + verifier verdicts for F23, F24, F38 — indices 22, 23, 37 per RESEARCH.md Sources)
    - .planning/phases/136-client-sdk-protocol/136-RESEARCH.md (Pattern 1 dev-host-client precedent; Code Examples for fire-and-forget connect() and silent-resolve action(); Pitfall 1 reconnect() asymmetry; Pitfall 2 connectImmediately threading)
    - src/client/game-connection.ts (confirm: action() at :128 returns {success:false,error:'Not connected'} when not OPEN; disconnect() mutates this.config.autoReconnect=false at :100; reconnect() sets it true at :106; scheduleReconnect() guard at :338; no `opened` promise anywhere in the file)
    - src/client/client.ts (confirm: connect() fire-and-forget at :171, constructor unconditionally calls generatePlayerId() at :37; generatePlayerId() throw at :586-587 naming "MeepleClientConfig" playerId field; setPlayerId() exists at :542)
    - src/client/types.ts (confirm MeepleClientConfig at :12-27 has NO playerId field; GameConnectionConfig at :167-196 has no connectImmediately field)
    - src/client/vue.ts (confirm setTimeout(100) "allow connection to establish" hack ~:112-122; the autoConnect-false open-then-disconnect at ~:140-143)
  </read_first>
  <action>Create `.planning/phases/136-client-sdk-protocol/136-FINDINGS-VERIFICATION.md`. For each finding write a section headed with the finding ID and requirement ID, containing a line with the literal token `VERDICT:` followed by `LEGITIMATE` or `REJECTED`. Cover: F23/SDK-01 — trace action()'s silent {success:false} return before open (game-connection.ts:128) and connect()'s fire-and-forget (client.ts:171) with no awaitable open signal; record that the fix mirrors dev-host-client's `opened` promise (NOT by importing/extending it — its header forbids sharing) and that action() must AWAIT open bounded by a timeout then send, rejecting loudly on failure. F24/SDK-02 — trace disconnect() mutating config.autoReconnect (game-connection.ts:100) as its suppression mechanism vs reconnect()-only restoration (:106), and the missing connectImmediately option forcing vue.ts to open-and-kill; record the locked fix (private #userDisconnected flag cleared by connect(); connectImmediately on GameConnectionConfig) and flag Pitfall 1 (audit reconnect() for redundancy once connect() clears the flag). F38/SDK-06 — trace generatePlayerId()'s error (client.ts:586) naming a playerId field that MeepleClientConfig (types.ts:12-27) does not have; record the fix adds playerId?:string to the config, constructor skips generation when provided, and corrects the "Node 16+" wording (crypto landed unflagged in Node 19). Each LEGITIMATE verdict cites current HEAD file:line. If a finding does not reproduce, record REJECTED with reasoning. Do NOT write or plan any fix in this task — verdicts only.</action>
  <verify>
    <automated>grep -v '^#' .planning/phases/136-client-sdk-protocol/136-FINDINGS-VERIFICATION.md | grep -c "VERDICT:"</automated>
  </verify>
  <done>136-FINDINGS-VERIFICATION.md exists with a VERDICT: line and current file:line trace for each of F23, F24, F38; the F23 section records the mirror-not-inherit constraint and the await-then-send action() semantics.</done>
  <acceptance_criteria>
    - File contains a section for F23, F24, F38 each with a `VERDICT:` token
    - Each LEGITIMATE verdict cites a current HEAD file:line (not the audit's original numbers)
    - F23 section records mirror-dev-host-client (not import/extend) and action()-awaits-open semantics
    - F24 section records the #userDisconnected + connectImmediately locked fix and the reconnect()-redundancy audit note
    - No fix code is written in this task
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Re-verify protocol/error-contract findings F25, F26, F35 and record verdicts</name>
  <files>.planning/phases/136-client-sdk-protocol/136-FINDINGS-VERIFICATION.md</files>
  <read_first>
    - .planning/tmp/v4.5-audit-findings.json (original audit text for F25, F26, F35 — indices 24, 25, 34)
    - .planning/phases/136-client-sdk-protocol/136-RESEARCH.md (18-method migration table; Pitfall 3 shared helper; Pitfall 4 lobby-manager errorCode gap; Pitfall 5 response.ok/non-2xx; Pitfall 6 barrel re-export chain; Pitfall 7 WS-union narrowing to action|ping|getState; Open Questions 1 and 2)
    - src/client/client.ts (confirm the 12 raw-JSON methods return `await response.json()` with no success/ok check — createGame ~:215, claimSeat ~:342, joinLobby, setReady, addSlot, removeSlot, setSlotAI, leavePosition, kickPlayer, updatePlayerOptions, updateSlotPlayerOptions, updateGameOptions; the 6 already-throwing methods check `!data.success` at :64/:89/:117/:238/:264/:284/:305/:326/:378 with no errorCode; private fetch() at :550 never checks response.ok)
    - src/client/types.ts (confirm CreateGameRequest at :288 lost playerIds vs canonical; all-optional WS bags at :237-276; Lobby re-export precedent at :322-327; duplicated request types :330-394)
    - src/client/index.ts (confirm barrel re-exports CreateGameRequest etc. FROM './types.js' at :81/:92 — deletion without re-export breaks this)
    - src/types/protocol.ts (confirm canonical CreateGameRequest with playerIds at :212-230; UpdateSlotPlayerOptionsMessage defined at :401 but ABSENT from the WebSocketMessage union at :420-435 whose siblings UpdatePlayerOptionsMessage/UpdateGameOptionsMessage ARE members)
    - src/session/lobby-manager.ts (grep for `errorCode` — confirm zero occurrences; every lobby failure is {success:false,error:'string'} only)
    - src/ui/components/GameShell.vue (confirm defensive double-handling: `if (result.success)` inside try/catch at ~:1235 createGame, ~:1372 joinLobby, ~:1437 handleJoinLobby, ~:1608-1620 updateSlotPlayerOptions)
  </read_first>
  <action>Append F25/SDK-03, F26/SDK-04, F35/SDK-05 sections to `136-FINDINGS-VERIFICATION.md`, each with a `VERDICT:` line. F25 — trace the split error contract (12 raw-JSON methods vs 6 throwers, all inconsistent) and the missing response.ok check (Pitfall 5: non-2xx non-JSON body throws an unrelated SyntaxError); record the fix routes ALL 18 methods through ONE shared private helper, and record the SCOPE BOUNDARY (Pitfall 4 / Open Question 1): lobby-manager.ts populates zero errorCode fields, so the client error type carries `errorCode?` OPTIONAL and lobby-manager changes are OUT OF SCOPE this phase. Also record that in-repo consumers (GameShell.vue defensive .success checks) migrate to the throwing contract THIS phase (not deferred to Phase 138). F26 — trace the CreateGameRequest drift (client types.ts:288 lost playerIds present in canonical protocol.ts:212-230) and the barrel re-export chain (Pitfall 6: index.ts re-exports from ./types.js, so every deletion needs a matching `export type { X } from '../types/protocol.js'` line); record the WS-bag narrowing target (Pitfall 7: outgoing narrows to exactly action|ping|getState, the only variants game-connection.ts constructs — NOT the full lobby set). F35 — trace UpdateSlotPlayerOptionsMessage defined at protocol.ts:401 but absent from the union at :420-435 while its two siblings are members; record it is a purely additive type-completeness fix with zero runtime consumers today. Each LEGITIMATE verdict cites current HEAD file:line. Verdicts only — no fix code.</action>
  <verify>
    <automated>grep -v '^#' .planning/phases/136-client-sdk-protocol/136-FINDINGS-VERIFICATION.md | grep -c "VERDICT:"</automated>
  </verify>
  <done>All six findings (F23, F24, F25, F26, F35, F38) now have a VERDICT: line; the F25 verdict records the optional-errorCode / lobby-manager-out-of-scope boundary and the shared-helper design; the F26 verdict records the barrel re-export requirement and the outgoing-union narrowing set.</done>
  <acceptance_criteria>
    - `grep -v '^#' ... | grep -c "VERDICT:"` returns 6 or more
    - F25 section records: one shared helper, response.ok check, and errorCode OPTIONAL with lobby-manager.ts out of scope
    - F26 section records the barrel re-export chain requirement and the action|ping|getState outgoing narrowing set
    - F35 section records the additive union member with zero current runtime consumers
    - No fix code is written in this task
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning doc → fix plans | An incorrect verdict here (false LEGITIMATE / false REJECTED, or a wrong scope call on F25's errorCode boundary) misdirects every downstream fix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-01 | Spoofing (of evidence) | 136-FINDINGS-VERIFICATION.md | mitigate | Every verdict cites a re-traced current HEAD file:line; F25 additionally cites the grepped zero-errorCode reality of lobby-manager.ts |
| T-136-02 | Repudiation | verdict record | mitigate | Literal `VERDICT:` token per finding makes the gate machine-checkable before any fix runs |

No package installs in this plan → no supply-chain (T-136-SC) row applies (the entire phase adds zero dependencies — 136-RESEARCH.md Package Legitimacy Audit: "not applicable").
</threat_model>

<verification>
- `grep -v '^#' 136-FINDINGS-VERIFICATION.md | grep -c "VERDICT:"` >= 6
- Each finding section cites current HEAD file:line evidence
- F25 records the optional-errorCode / lobby-manager-out-of-scope boundary; F26 records the barrel re-export requirement
</verification>

<success_criteria>
All six findings have a recorded, evidence-backed verdict. No fix code written. Plans 02-05 are unblocked.
</success_criteria>

<output>
Create `.planning/phases/136-client-sdk-protocol/136-FINDINGS-VERIFICATION.md`, then a `.planning/phases/136-client-sdk-protocol/136-01-SUMMARY.md` when done.
</output>
