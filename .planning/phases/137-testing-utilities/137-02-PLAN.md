---
phase: 137-testing-utilities
plan: 02
type: execute
wave: 2
depends_on: [137-01]
files_modified:
  - src/testing/test-game.ts
  - src/testing/test-game.test.ts
  - src/testing/action-builder.ts
  - src/testing/simulate-tutorial.ts
  - src/testing/random-simulation.ts
  - src/testing/simulate-action.ts
  - docs/api/testing.md
  - docs/agent-control.md
autonomous: true
requirements: [TST-01, PROC-02]
must_haves:
  truths:
    - "TestGame.doAction throws by default when the action fails, with a message naming the action and seat and including the debugActionAvailability trace"
    - "TestGame.tryAction(seat, action, args) returns ActionExecutionResult without throwing, for tests that deliberately expect failure"
    - "The four category-(b) harness call sites (simulate-tutorial, random-simulation, simulateAction, playUntilComplete internal) call tryAction and keep their existing failure-handling control flow intact"
    - "ActionBuilder.execute() no longer hand-rolls a !result.success throw — it calls doAction and lets it throw"
    - "The class-level @example and the doc examples in docs/api/testing.md and docs/agent-control.md no longer model ignoring the doAction result"
    - "Full suite is green — no harness loop breaks on the throw-flip"
  artifacts:
    - path: "src/testing/test-game.ts"
      provides: "doAction throw-on-failure, tryAction escape hatch, ActionExecutionError class"
      contains: "tryAction"
    - path: "src/testing/test-game.test.ts"
      provides: "RED-then-green regression tests for TST-01 (throw + tryAction)"
      contains: "tryAction"
  key_links:
    - from: "src/testing/test-game.ts doAction"
      to: "game.debugActionAvailability"
      via: "failure-path trace fold into thrown ActionExecutionError message"
      pattern: "debugActionAvailability"
    - from: "src/testing/action-builder.ts execute"
      to: "src/testing/test-game.ts doAction"
      via: "delegates throw to doAction (no local !success check)"
      pattern: "doAction"
---

<objective>
TST-01 (F36): Make `TestGame.doAction` fail loud by default — it now THROWS an `ActionExecutionError` carrying the rich `debugActionAvailability` trace when the action fails, instead of silently returning a `{success:false}` result callers routinely ignore. Add a sibling `tryAction()` with the old never-throw signature for tests that deliberately exercise the failure path, migrate the four harness call sites that depend on failure-as-a-branch to `tryAction`, simplify `ActionBuilder.execute()`, and fix the class-level and doc examples that model ignoring the result (DOCX-04).

Purpose: aligns TestGame with the library's fail-loud doctrine (the `GameStuckError` / `assertActionAvailable` house style) so a silently-failing setup move surfaces as a loud test failure at its origin instead of a confusing downstream assertion.
Output: `doAction` throws, `tryAction` added, four call sites migrated, ActionBuilder simplified, examples fixed, RED-then-green tests, full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-testing-utilities/137-CONTEXT.md
@.planning/phases/137-testing-utilities/137-PATTERNS.md
@.planning/phases/137-testing-utilities/137-FINDINGS-VERIFICATION.md

<interfaces>
<!-- Contracts the executor needs — extracted from current source. Use directly; no exploration required. -->

Error-class shape to mirror — src/testing/simulate-action.ts (GameStuckError, ~lines 194-221):
  extends Error; `readonly name = '<ClassName>' as const`; structured `readonly` fields; `Object.setPrototypeOf(this, <ClassName>.prototype)` in constructor.

Rich trace to fold into the throw — src/testing/assertions.ts (assertActionAvailable, ~lines 207-242):
  calls `testGame.game.debugActionAvailability(actionName, player)` → `{ reason, details.selections[] }`;
  each selection: `{ name, passed, choices, note }`; also appends `testGame.game.getFlowDebugInfo().describe()` as "Flow position:".

Current doAction (being replaced) — src/testing/test-game.ts (~272-278):
  doAction(playerSeat, actionName, args = {}): ActionExecutionResult { return this.runner.performAction(actionName, playerSeat, args); }

ActionExecutionResult — src/runtime (imported in test-game.ts:20): { success: boolean; error?: string; errorCode?: string; ... }

Fixture for tests — src/testing/test-game.test.ts (~lines 1-80): FixtureGame/FixtureCard 2-player game (registerAction 'pass', loop/eachPlayer/actionStep flow). Extend with an always-illegal action name to exercise the throw.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for doAction-throws and tryAction-returns</name>
  <files>src/testing/test-game.test.ts</files>
  <read_first>
    - src/testing/test-game.test.ts (FixtureGame/FixtureCard fixture at ~lines 1-80; existing describe organization)
    - .planning/phases/137-testing-utilities/137-PATTERNS.md ("New RED/GREEN test file — fixture pattern to reuse")
  </read_first>
  <behavior>
    - Test 1: `testGame.doAction(seat, 'notAnAction', {})` (unavailable action or wrong seat) THROWS; the thrown error names the action and seat and includes the availability trace (e.g. message contains the action name and a "Why:" / available-actions fragment). Assert via `expect(() => ...).toThrow(/notAnAction/)` and inspect the message.
    - Test 2: `testGame.tryAction(seat, 'notAnAction', {})` returns `{ success: false }` WITHOUT throwing (assert `result.success === false`, no throw).
    - Test 3: a valid action via `doAction` (e.g. the fixture's `'pass'`) does NOT throw and completes normally.
  </behavior>
  <action>Append a new `describe('doAction throw-on-failure (TST-01)', ...)` block to `src/testing/test-game.test.ts` co-located with the existing fixture. Extend or reuse `FixtureGame` so an always-unavailable action name exists to trigger the failure path (the fixture already registers only `'pass'`, so any other name is unavailable). Write the three tests above against the CURRENT (never-throwing) `doAction`; `tryAction` does not exist yet, so Test 2 will fail to compile/run — that is the intended RED state. Do NOT implement the fix in this task. Commit as `test(137-02): add failing tests for doAction throw + tryAction`.</action>
  <verify>
    <automated>npx vitest run src/testing/test-game.test.ts 2>&1 | grep -qiE 'fail|error|tryAction' && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - New describe block asserts doAction throws on unavailable action, tryAction returns {success:false} without throwing, and valid doAction does not throw.
    - Suite for test-game.test.ts is RED (tryAction unresolved / doAction does not yet throw).
    - RED state recorded for the SUMMARY (PROC-02).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — ActionExecutionError, doAction throws, tryAction escape hatch, ActionBuilder simplified</name>
  <files>src/testing/test-game.ts, src/testing/action-builder.ts</files>
  <read_first>
    - src/testing/test-game.ts (doAction ~272-278; class-level @example ~94-104; import of ActionExecutionResult at line 20; the `game` and `getPlayer`/seat accessors used by assertions.ts)
    - src/testing/simulate-action.ts (GameStuckError shape ~194-221 — the class to mirror)
    - src/testing/assertions.ts (assertActionAvailable ~207-242 — the exact trace-building code to reuse: debugActionAvailability reason + per-selection lines + getFlowDebugInfo().describe())
    - src/testing/action-builder.ts (execute() ~91-98 — the hand-rolled throw to delete)
  </read_first>
  <action>In `src/testing/test-game.ts`, add an exported `ActionExecutionError` class mirroring `GameStuckError` exactly: `extends Error`, `readonly name = 'ActionExecutionError' as const`, structured `readonly` fields (`actionName: string`, `playerSeat: number`, `args: Record<string, unknown>`, and the wrapped `result: ActionExecutionResult`), and `Object.setPrototypeOf(this, ActionExecutionError.prototype)` in the constructor (per TST-01 discretion — a dedicated error class reads best in vitest output; do not reuse GameStuckError).

Rename the current never-throwing `doAction` body into a new public `tryAction(playerSeat, actionName, args = {}): ActionExecutionResult` that keeps the EXACT old behavior (`return this.runner.performAction(actionName, playerSeat, args)` — never throws). Then rewrite `doAction(playerSeat, actionName, args = {}): void` to call `tryAction(...)` and, on `!result.success`, build the actionable message and throw `ActionExecutionError`. Build the message the same way `assertActionAvailable` does on its failure path only (no cost on success): call `this.game.debugActionAvailability(actionName, this.getPlayer(playerSeat))`, fold in `debugInfo.reason`, the per-selection `✓/✗ 'name': N choices — note` lines, `result.error`/`result.errorCode`, and `Flow position: ${this.game.getFlowDebugInfo().describe()}`. If the action name is unregistered or the seat cannot act (debugActionAvailability not meaningful), fall back to a plain message built from `result.error`. Note in a code comment that TST-02 (Plan 03) will append `testGame.seed` to this message.

Update the class-level `@example` (~94-104) so it no longer models ignoring the result: drop the redundant explicit `seed` (now defaulted deterministically in Plan 03) or keep it, and show the happy path calling `doAction` with no `if (!result.success)` gate, plus a one-line note that `doAction` throws on failure and `tryAction` returns a result for expected-failure tests.

Also update the OTHER two stale JSDoc examples flagged by 137-PATTERNS.md: the `doAction` method JSDoc (~line 266, `const result = testGame.doAction(...); if (!result.success)` pattern) and `createTestGame`'s JSDoc (~line 451, `expect(result.success).toBe(true)` pattern) — both must model the new throw/tryAction semantics.

In `src/testing/action-builder.ts`, simplify `execute()` to a single `this._testGame.doAction(this._seat, this._actionName, this._args);` — delete the local `!result.success` throw (the new default throw is a strict superset naming action/seat plus the trace).

Make the RED tests from Task 1 pass. Commit as `feat(137-02): doAction throws ActionExecutionError, add tryAction escape hatch`.</action>
  <verify>
    <automated>npx vitest run src/testing/test-game.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ActionExecutionError` exported from test-game.ts with named `readonly name`, structured fields, and `setPrototypeOf`.
    - `doAction` returns `void` and throws `ActionExecutionError` (with debugActionAvailability trace) on failure; `tryAction` returns `ActionExecutionResult` and never throws.
    - `ActionBuilder.execute()` contains no local `!result.success` throw.
    - Class-level `@example` no longer models ignoring the result.
    - No stale ignore-the-result doAction example remains anywhere in test-game.ts: `grep -n "result.success" src/testing/test-game.ts` returns no doAction-example matches in JSDoc (incl. ~266 and ~451; tryAction examples checking `.success` are fine).
    - Task 1 tests are green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Migrate the four category-(b) harness call sites to tryAction + fix doc examples</name>
  <files>src/testing/simulate-tutorial.ts, src/testing/random-simulation.ts, src/testing/simulate-action.ts, docs/api/testing.md, docs/agent-control.md</files>
  <read_first>
    - .planning/phases/137-testing-utilities/137-FINDINGS-VERIFICATION.md (the confirmed current file:line for each call site)
    - .planning/phases/137-testing-utilities/137-PATTERNS.md (the four call-site migration sections — each explains WHY it must be tryAction, not the throw)
    - src/testing/simulate-tutorial.ts (~227-235), src/testing/random-simulation.ts (~401-414), src/testing/simulate-action.ts (simulateAction ~49-56; playUntilComplete internal ~383)
    - docs/api/testing.md (doAction examples ~line 404 and ~175-179), docs/agent-control.md (~line 165)
  </read_first>
  <action>Migrate each category-(b) site from `.doAction(` to `.tryAction(` so its existing failure-as-a-branch control flow is preserved (the throw-flip would otherwise break these loops on the first rejected move):
- `src/testing/simulate-tutorial.ts` (~228): keep the tutorial-specific error message with `activeStep.id`; only swap `doAction`→`tryAction`.
- `src/testing/random-simulation.ts` (~402): the counted `consecutiveFailures` retry branch must keep working — swap to `tryAction`.
- `src/testing/simulate-action.ts` `simulateAction` (~55): this function's contract is "return the result, don't throw" — swap to `tryAction`.
- `src/testing/simulate-action.ts` `playUntilComplete` internal loop (~383): it batches multi-seat failures before deciding a dead-end — swap to `tryAction` to keep the batching (a throw would lose sibling-seat diagnostics).

Then fix the doc examples per DOCX-04 so none model ignoring a potentially-failing `doAction` result: in `docs/api/testing.md` (the doAction usage near line 404 and the `result.success` example near 175-179) and `docs/agent-control.md` (near line 165), show the happy path without an ignored result and point expected-failure cases at `tryAction()` / `simulateAction`. Grep both docs for remaining `.doAction(` examples that model checking/ignoring `result.success` for the happy path and correct them. Run the full suite — it must stay green; any newly-surfaced silently-failing setup move is a real bug to fix (that is the intent of TST-01). Commit as `refactor(137-02): migrate harness call sites to tryAction, fix doc examples`.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
    - The four category-(b) sites call `tryAction`; `grep -n '\.doAction(' src/testing/simulate-tutorial.ts src/testing/random-simulation.ts src/testing/simulate-action.ts` finds no runtime call (only JSDoc if any).
    - docs/api/testing.md and docs/agent-control.md examples no longer model ignoring the doAction result; expected-failure paths reference tryAction/simulateAction.
    - Full suite (`npm run test`) is green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-harness SDK correctness only; no runtime service, no untrusted input, no security boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-02 | Denial of Service (false-negative masking) | TestGame.doAction silent failure | mitigate | doAction now throws on failure, eliminating the silent-pass footgun that masks broken setup moves; escape hatch (tryAction) is opt-in and explicit. |
| T-137-03 | Tampering | debugActionAvailability trace in error message | accept | Trace is developer-facing test output only (no PII, no external actor); mirrors existing GameStuckError/assertActionAvailable exposure. No package installs → no supply-chain row. |
</threat_model>

<verification>
`npx vitest run src/testing/test-game.test.ts` green (throw + tryAction behavior), then `npm run test` green (harness migrations keep loops intact; throw-flip surfaces no un-fixed silent failures). Baseline before phase: 175 files / 2358 tests.
</verification>

<success_criteria>
`TestGame.doAction` fails loud by default with an actionable trace, `tryAction` is the documented escape hatch, the four harness loops still handle failure as a branch, ActionBuilder is simplified, the class-level and doc examples model the new semantics, and the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/137-testing-utilities/137-02-SUMMARY.md` when done. Record the RED state from Task 1 (PROC-02).
</output>
