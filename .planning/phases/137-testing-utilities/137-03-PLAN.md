---
phase: 137-testing-utilities
plan: 03
type: execute
wave: 3
depends_on: [137-02]
files_modified:
  - src/testing/test-game.ts
  - src/testing/test-game.test.ts
  - src/testing/assertions.ts
  - src/testing/simulate-action.ts
  - docs/api/testing.md
autonomous: true
requirements: [TST-02, PROC-02]
must_haves:
  truths:
    - "Two TestGames constructed with no seed produce identical shuffles / command history — deterministic by default"
    - "The default seed is a fixed literal (not test-${Date.now()})"
    - "testGame.seed exposes the resolved seed (fixed default or caller-supplied)"
    - "Assertion-helper failure messages include testGame.seed so a failing run is one copy-paste from a deterministic repro"
    - "Full suite is green"
  artifacts:
    - path: "src/testing/test-game.ts"
      provides: "fixed literal default seed + readonly seed field + accessor"
      contains: "readonly seed"
    - path: "src/testing/test-game.test.ts"
      provides: "RED-then-green regression tests for TST-02 (seedless determinism + seed accessor)"
      contains: "seed"
  key_links:
    - from: "src/testing/test-game.ts constructor"
      to: "GameRunner seed / createSeededRandom"
      via: "resolved fixed-literal seed threaded to runner and stored on instance"
      pattern: "readonly seed"
    - from: "src/testing/assertions.ts / test-game.ts throw / simulate-action.ts GameStuckError"
      to: "testGame.seed"
      via: "seed appended to failure messages for deterministic repro"
      pattern: "seed"
---

<objective>
TST-02 (F37): Make `TestGame`'s default seed a fixed literal (e.g. `'test-seed'`) instead of `test-${Date.now()}`, so no-seed test runs are deterministic and reproducible in CI — matching `playUntilComplete`'s `options?.seed ?? 'playUntilComplete-default'` doctrine. Expose the resolved seed via `testGame.seed`, and include it in assertion-helper failure messages (the `assertActionAvailable` throw, the new `doAction` `ActionExecutionError` message from Plan 02, and the `GameStuckError` messages in `playUntilComplete`) so a failing test's output is one copy-paste away from a deterministic repro.

Purpose: aligns TestGame with the library's deterministic-by-default doctrine established in v4.4 Phase 123, and makes every seeded/random failure trivially reproducible.
Output: fixed default seed, `testGame.seed` accessor, seed in failure messages, RED-then-green tests, full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-testing-utilities/137-CONTEXT.md
@.planning/phases/137-testing-utilities/137-PATTERNS.md
@.planning/phases/137-testing-utilities/137-FINDINGS-VERIFICATION.md

<interfaces>
<!-- Contracts the executor needs — extracted from current source. -->

Seed doctrine to mirror — src/testing/simulate-action.ts (~256-268, 311-315):
  `options?.seed ?? 'playUntilComplete-default'` — fixed literal default, NEVER Math.random; JSDoc explains deterministic-by-default.

Current TestGame seed (being replaced) — src/testing/test-game.ts (~line 127, inside static create):
  `const seed = options.seed ?? \`test-${Date.now()}\`;`  then threaded into GameRunner construction.

Failure-message sites to enrich with seed:
  - src/testing/assertions.ts assertActionAvailable (~207-242) — appends `Flow position: ...describe()`; add `Seed: <seed>`.
  - src/testing/test-game.ts doAction → ActionExecutionError message (added in Plan 02) — add `Seed: <seed>`.
  - src/testing/simulate-action.ts GameStuckError messages (playUntilComplete) — surface `testGame.seed` alongside existing repro context.

Note: Plan 02 (137-02) already landed doAction-throws + tryAction; this plan edits the same test-game.ts and test-game.test.ts (hence the wave-3 dependency).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for seedless determinism + testGame.seed accessor</name>
  <files>src/testing/test-game.test.ts</files>
  <read_first>
    - src/testing/test-game.test.ts (fixture pattern; the describe blocks added in Plan 02)
    - .planning/phases/137-testing-utilities/137-PATTERNS.md ("New RED/GREEN test file" — the note to add a shuffled Deck so determinism is observable)
    - src/testing/test-game.ts (static create ~123-135; where the seed is resolved and threaded)
  </read_first>
  <behavior>
    - Test 1: two TestGames created with NO seed option produce identical shuffles / command history (extend the fixture with a shuffled Deck, or assert equal serialized initial state) — deterministic by default.
    - Test 2: `testGame.seed` returns the resolved seed string; with no seed option it equals the fixed literal default; with an explicit `seed` option it equals that value.
  </behavior>
  <action>Append a new `describe('deterministic default seed (TST-02)', ...)` block to `src/testing/test-game.test.ts`. Extend the fixture (or add a small local game) with a shuffled `Deck` so two seedless constructions can be compared for identical shuffle order (compare command history or serialized initial element state). Add a test asserting `testGame.seed` exists and returns the resolved seed for both the seedless (fixed literal) and explicit-seed cases. Write against CURRENT source where `testGame.seed` does not exist and the default is `Date.now()` — both tests fail (RED). Do NOT implement the fix. Commit as `test(137-03): add failing tests for fixed default seed + testGame.seed`.</action>
  <verify>
    <automated>npx vitest run src/testing/test-game.test.ts 2>&1 | grep -qiE 'fail|error|seed' && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - New describe block asserts seedless determinism (identical shuffles) and `testGame.seed` for default + explicit cases.
    - Suite for test-game.test.ts is RED (`testGame.seed` unresolved and/or Date.now default makes seedless runs differ).
    - RED state recorded for the SUMMARY (PROC-02).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GREEN — fixed default seed, testGame.seed accessor, seed in failure messages</name>
  <files>src/testing/test-game.ts, src/testing/assertions.ts, src/testing/simulate-action.ts, docs/api/testing.md</files>
  <read_first>
    - src/testing/test-game.ts (static create ~123-135 seed resolution; the ActionExecutionError message added in Plan 02)
    - src/testing/assertions.ts (assertActionAvailable ~207-242 — the `Flow position:` append point)
    - src/testing/simulate-action.ts (playUntilComplete GameStuckError construction; the `'playUntilComplete-default'` literal as the naming precedent)
    - docs/api/testing.md (seed / determinism guidance)
  </read_first>
  <action>In `src/testing/test-game.ts`, replace `` const seed = options.seed ?? `test-${Date.now()}` `` with a fixed literal default: `const seed = options.seed ?? 'test-seed';` (Claude's discretion on the exact literal — match the `'playUntilComplete-default'` naming style; a stable string, never `Date.now()`, never Math.random). Add a `readonly seed: string` field on the TestGame instance set from the resolved `seed` local (thread it in the same place the constructor already threads `runner`/`game`), and expose it via `testGame.seed`. Add a JSDoc note on the seed option mirroring the playUntilComplete doctrine (deterministic by default; pass an explicit `seed` to vary the run).

Thread `this.seed` into the failure messages so a failing run is one copy-paste from a deterministic repro: append `Seed: ${this.seed}` to the `doAction` `ActionExecutionError` message (added in Plan 02), pass/append the seed in `assertActionAvailable`'s thrown message alongside the existing `Flow position:` line (it already has `testGame`, so read `testGame.seed`), and surface `testGame.seed` in the `GameStuckError` messages built by `playUntilComplete` in `simulate-action.ts`.

Update the seed/determinism guidance in `docs/api/testing.md` to state the default seed is a fixed literal (deterministic by default) and that `testGame.seed` exposes it. Make the Task 1 RED tests pass. Commit as `feat(137-03): fixed default seed, expose testGame.seed, seed in failure messages`.</action>
  <verify>
    <automated>npx vitest run src/testing/ && grep -q "readonly seed" src/testing/test-game.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Default seed is a fixed literal; `grep -n "Date.now" src/testing/test-game.ts` finds no seed usage.
    - `testGame.seed` returns the resolved seed (fixed default or explicit).
    - `assertActionAvailable` throw, `doAction` ActionExecutionError message, and `playUntilComplete` GameStuckError messages all include the seed.
    - docs/api/testing.md documents the fixed default seed and `testGame.seed`.
    - Task 1 tests green; full `npx vitest run src/testing/` green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-harness determinism only; no runtime service, no untrusted input, no security boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-04 | Repudiation (non-reproducible failures) | TestGame default seed = Date.now() | mitigate | Fixed literal default makes every seedless run reproducible; seed surfaced in failure messages so any failure is one copy-paste from a deterministic repro. No package installs → no supply-chain row. |
</threat_model>

<verification>
`npx vitest run src/testing/` green (seedless determinism + testGame.seed), then `npm run test` green. Two seedless TestGames produce identical shuffles (Test 1); seed appears in assertion/throw/GameStuckError failure output.
</verification>

<success_criteria>
No-seed TestGame runs are deterministic and reproducible in CI, `testGame.seed` exposes the resolved seed, failure messages carry it for one-copy-paste repro, and the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/137-testing-utilities/137-03-SUMMARY.md` when done. Record the RED state from Task 1 (PROC-02).
</output>
