---
phase: 138-cross-repo-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/Dropbox/MERC/BoardSmith/MERC/package.json
  - ~/Dropbox/MERC/BoardSmith/MERC/package-lock.json
  - ~/Dropbox/MERC/BoardSmith/MERC/boardsmith.json
  - ~/Dropbox/MERC/BoardSmith/MERC/vendor
  - src/
autonomous: true
requirements: [GAMES-02]
must_haves:
  truths:
    - "MERC's pre-existing uncommitted tree is captured as a standalone WIP commit before the re-vendor"
    - "A true pre-re-vendor baseline (738-test target) is recorded before touching the vendor pointer"
    - "MERC is re-vendored onto a fresh BoardSmith tarball and its full suite is green"
    - "Any gap surfaced is fixed in BoardSmith src/ with a red-first test — never worked around in MERC"
    - "BoardSmith's own suite (175 files / 2368 tests) stays green after any src fix"
  artifacts:
    - path: "~/Dropbox/MERC/BoardSmith/MERC/package.json"
      provides: "dependencies.boardsmith + overrides.boardsmith pointed at the new tarball"
    - path: "~/Dropbox/MERC/BoardSmith/MERC/boardsmith.json"
      provides: "manifest without playerCount/$schema"
  key_links:
    - from: "MERC package.json"
      to: "vendor/boardsmith-0.0.1-<timestamp>.tgz"
      via: "npm pack from ~/BoardSmith + npm install"
      pattern: "boardsmith-0\\.0\\.1"
---

<objective>
Re-vendor MERC (`~/Dropbox/MERC/BoardSmith/MERC`) onto the v4.5 BoardSmith and bring its suite green. Per the locked CONTEXT decision, FIRST commit MERC's pre-existing uncommitted changes as a standalone WIP commit so the re-vendor sits on a clean, recoverable base. Then follow the reconstructed re-vendor procedure (RESEARCH "MERC Re-Vendor Procedure"), establish the true baseline, and iterate: any gap surfaced is fixed in BoardSmith `src/` with a red-first regression test, re-vendor, re-run — NEVER worked around in MERC. RESEARCH §5 confirms the known ENG-05 bare-number followUp sites are already dual-shape-tolerant, but this must be confirmed empirically by the live suite, not trusted blind.

Purpose: Satisfy GAMES-02 — MERC re-vendored and green; gaps fixed at source.
Output: WIP commit + re-vendor commit in MERC; any BoardSmith src fixes committed in BoardSmith; MERC suite green; BoardSmith suite still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-cross-repo-migration/138-RESEARCH.md
@.planning/phases/138-cross-repo-migration/138-CONTEXT.md

<interfaces>
<!-- MERC uncommitted tree at research time (RESEARCH Runtime State Inventory): -->
<!--   M boardsmith.json (gameId regenerated + reformatted) -->
<!--   M package.json / package-lock.json (vendor pointer -> protocol.tgz, 0.0.27 -> 0.0.29) -->
<!--   M src/ui/components/AssignToSquadPanel.vue (--bs-* -> --bsg-* CSS rename) -->
<!-- Re-vendor pattern (historical commits 87cee4a v4.4, 3a81e81 v4.3): -->
<!--   npm pack from ~/BoardSmith -> copy .tgz into MERC/vendor/ -> -->
<!--   update BOTH dependencies.boardsmith AND overrides.boardsmith paths -> npm install -> npx vitest run -->
<!--   ENG-05 dual-shape resolver already present: rebel-economy.ts getUnit/getSector, rebel-equipment.ts reEquipContinue -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WIP-commit MERC's pre-existing tree + record true baseline</name>
  <files>~/Dropbox/MERC/BoardSmith/MERC</files>
  <read_first>
    - 138-RESEARCH.md "Runtime State Inventory" + Pitfall 4 (the 4 pre-existing modified files; check `git status --short` in MERC FIRST)
    - 138-CONTEXT.md USER DECISION (2026-07-03): commit MERC's pre-existing uncommitted changes first as-is, as a WIP commit, so the re-vendor sits on a clean recoverable base
    - RESEARCH Pitfall 1 (`npx vitest run`, never bare `npm test`)
  </read_first>
  <action>In `~/Dropbox/MERC/BoardSmith/MERC`: run `git status --short` and `git diff --stat` to snapshot the current dirty state (boardsmith.json, package.json, package-lock.json, AssignToSquadPanel.vue). Per the locked user decision, commit ALL of it as-is as a standalone WIP commit: `git add -A && git commit -m "wip: snapshot in-progress local state before v4.5 re-vendor"`. Do NOT reconcile, discard, or fold it into the migration — just capture it so the working tree is clean. Then run `npx vitest run` against the CURRENT (pre-re-vendor) committed vendor tarball to record MERC's true baseline number (CONTEXT claims 738 — verify the actual count; it is the reference the re-vendor must not regress below). Record both the git SHA and baseline count in the SUMMARY.</action>
  <verify>
    <automated>cd ~/Dropbox/MERC/BoardSmith/MERC && git status --short | head; echo "--- clean tree expected above (empty) ---"; git log --oneline -1; npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `git status --short` in MERC is empty (WIP committed)
    - Top commit is the `wip: snapshot ...` commit
    - Pre-re-vendor baseline test count recorded in SUMMARY
  </acceptance_criteria>
  <done>MERC tree clean on a WIP commit; true baseline recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Re-vendor MERC onto the fresh BoardSmith tarball</name>
  <files>~/Dropbox/MERC/BoardSmith/MERC/vendor, ~/Dropbox/MERC/BoardSmith/MERC/package.json, ~/Dropbox/MERC/BoardSmith/MERC/package-lock.json, ~/Dropbox/MERC/BoardSmith/MERC/boardsmith.json</files>
  <read_first>
    - 138-RESEARCH.md "MERC Re-Vendor Procedure" (7-step reconstructed procedure; both dependencies.boardsmith AND overrides.boardsmith updated together; check `.gitignore` before assuming vendor/*.tgz is/ isn't tracked)
    - 138-RESEARCH.md "Don't Hand-Roll" (mirror the `npm pack` filename-timestamp pattern; do not invent a packaging script)
    - RESEARCH §1 (MERC boardsmith.json also needs playerCount/$schema stripped — player count owned by gameDefinition in src/rules/)
  </read_first>
  <action>From `~/BoardSmith`, produce a fresh tarball via `npm pack` (default output `boardsmith-0.0.1.tgz`; rename/copy to the established `boardsmith-0.0.1-<YYYYMMDDHHMMSS>.tgz` convention). Copy it into `~/Dropbox/MERC/BoardSmith/MERC/vendor/`. Edit MERC's package.json to point BOTH `dependencies.boardsmith` and `overrides.boardsmith` at the new tarball. Also strip `playerCount` + `$schema` from MERC's boardsmith.json (RESEARCH §1). Run `npm install` in MERC to regenerate package-lock.json. Do NOT commit yet — the commit lands in Task 3 after the suite is green.</action>
  <verify>
    <automated>cd ~/Dropbox/MERC/BoardSmith/MERC && grep -c boardsmith-0.0.1 package.json; grep -c -e '"\$schema"' -e '"playerCount"' boardsmith.json; ls vendor/*.tgz | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - Both boardsmith path fields in package.json point at the new tarball
    - MERC boardsmith.json has 0 playerCount/$schema hits
    - New tarball present in vendor/; npm install completed without error
  </acceptance_criteria>
  <done>MERC vendored onto the fresh v4.5 tarball; manifest cleaned; ready to run the suite.</done>
</task>

<task type="auto">
  <name>Task 3: Iterate MERC suite to green (gaps fixed in BoardSmith src red-first) + commit</name>
  <files>~/Dropbox/MERC/BoardSmith/MERC, src/</files>
  <read_first>
    - 138-RESEARCH.md §2 (MERC dictator-hire.test.ts:27,39,53,97,127,131 doAction sites — classify: single setup-moves drop the assertion; loop sites 127/131 that gate on .success move to tryAction per Pitfall 3)
    - 138-RESEARCH.md §5 + §9 + Open Question 1 (ENG-05 already-tolerant + shuffle-seed sites — confirm empirically via the live suite, don't trust blind)
    - CONTEXT "MERC process" locked decision (gaps fixed in BoardSmith src/ with red-first test; NEVER worked around in MERC)
  </read_first>
  <action>Run `npx vitest run` in MERC. For each failure, classify: (a) a MERC test-file migration (e.g. dictator-hire.test.ts doAction `.success` sites — same rules as Plan 01: drop assertion for setup moves, use tryAction for loop-control) — fix in MERC; (b) a genuine BoardSmith API gap — fix in BoardSmith `src/` with a red-first regression test added to the BoardSmith suite, then re-vendor (repeat Task 2's pack+install), and re-run. NEVER work around a gap in MERC. Iterate until MERC's suite meets/exceeds its baseline (738 target) with 0 failures. After green: commit MERC's package.json/package-lock.json/boardsmith.json/vendor change with `chore: re-vendor boardsmith (v4.5 pit-of-success hardening: SEC/ENG/RST/SESS/UIX/CLIX/SDK/TST + GAMES)`; commit any MERC test migrations in MERC; commit any BoardSmith src fixes separately in BoardSmith. Finally confirm BoardSmith's own suite still green (`npm run test`, 175 files / 2368 tests). Record the full migration ledger (failures found, where fixed) in the SUMMARY.</action>
  <verify>
    <automated>cd ~/Dropbox/MERC/BoardSmith/MERC && npx vitest run 2>&1 | tail -4; echo "=== BoardSmith suite ==="; cd ~/BoardSmith && npm run test 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - MERC `npx vitest run`: 0 failed, count >= 738 baseline
    - Any BoardSmith src fix has a red-first regression test and is committed in BoardSmith (not MERC)
    - BoardSmith suite still green (175 files / 2368 tests)
    - MERC re-vendor + migration committed in MERC; no MERC changes leaked into BoardSmith commits
    - Migration ledger in SUMMARY
  </acceptance_criteria>
  <done>MERC suite green on the v4.5 re-vendor; gaps fixed at source; BoardSmith suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MERC vendored copy ↔ BoardSmith source | Vendored tarball must match current src/; a stale/wrong tarball silently ships old behavior |
| MERC repo ↔ BoardSmith repo | Gap fixes belong in BoardSmith src (with tests); working around in MERC hides real library bugs |
| pre-existing WIP ↔ v4.5 migration | Unrelated dirty state must be isolated in its own commit, not conflated with the migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-06 | Tampering | vendored tarball freshness | mitigate | `npm pack` from current src + both package.json path fields updated + npm install regenerates lock |
| T-138-07 | Repudiation | conflated commits | mitigate | Task 1 WIP-commits pre-existing state standalone; src fixes committed in BoardSmith, MERC changes in MERC |
| T-138-08 | Elevation of privilege | worked-around gap masking a real bug | mitigate | Locked rule: gaps fixed in BoardSmith src/ red-first, never in MERC |
| T-138-SC | Tampering | npm install during re-vendor | accept | No new external packages; install only regenerates lock against the local file: tarball (RESEARCH: no package legitimacy audit applicable) |
</threat_model>

<verification>
- MERC tree clean on WIP commit before re-vendor
- MERC suite: 0 failed, >= 738
- BoardSmith suite still 175/2368 green
- src fixes red-first + committed in BoardSmith; MERC commits in MERC
</verification>

<success_criteria>
GAMES-02 satisfied: MERC re-vendored onto v4.5 and green; every surfaced gap fixed in BoardSmith `src/` with a red-first test (never worked around in MERC); BoardSmith's own suite still green.
</success_criteria>

<output>
Create `.planning/phases/138-cross-repo-migration/138-03-SUMMARY.md` when done, including the MERC migration ledger (baseline, failures found, where fixed, final counts).
</output>
