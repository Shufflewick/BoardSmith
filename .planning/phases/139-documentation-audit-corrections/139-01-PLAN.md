---
phase: 139-documentation-audit-corrections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/core-concepts.md
  - docs/getting-started.md
  - src/engine/element/game.ts
  - src/engine/element/player.ts
autonomous: true
requirements: [DOCX-01, DOCX-02, DOCX-03]
must_haves:
  truths:
    - "docs/core-concepts.md teaches direct-mutation + state-authoritative snapshots, not an event-sourcing command model"
    - "docs/core-concepts.md contains zero references to element.setAttribute() (a nonexistent method)"
    - "registerActions()/registerAction() JSDoc and the game.ts:1644 runtime error message use the real Action.create(...).chooseElement(...).execute(...) API"
    - "docs/getting-started.md documents the CLI that actually ships (--no-open, --lan, 127.0.0.1 default, no playerCount/$schema in boardsmith.json)"
    - "tsc --noEmit stays clean after JSDoc source edits"
  artifacts:
    - path: docs/core-concepts.md
      provides: "Corrected Commands/architecture sections + visibleAttributes claims"
      contains: "state-authoritative"
    - path: src/engine/element/game.ts
      provides: "Real-API registerActions JSDoc + corrected runtime error message"
      contains: "Action.create"
  key_links:
    - from: docs/core-concepts.md
      to: src/engine/element/piece.ts
      via: "documented mutation model matches piece.ts:73-75 direct mutation (no MoveCommand)"
      pattern: "state-authoritative"
    - from: src/engine/element/game.ts
      to: src/engine/action/action-builder.ts
      via: "JSDoc example uses the real Action.create builder"
      pattern: "Action\\.create"
---

<objective>
Fix the three named docs-teaching-nonexistent-API findings from Audit #3 (F11/DOCX-01, F14/DOCX-02, F20/DOCX-03). Each finding is re-verified against live source before rewriting, then corrected to teach the real, shipped API.

Purpose: The docs are the first thing a developer (and CLAUDE.md directs agents) reads. Today core-concepts.md teaches a removed event-sourcing model + a nonexistent `element.setAttribute()`, the primary action-registration JSDoc models a phantom `action().do().chooseOnBoard()` API (and a runtime error message points to it), and getting-started.md has residual CLI drift. This plan closes all three.

Output: Corrected docs/core-concepts.md, docs/getting-started.md, and the registerActions JSDoc + error message in src/engine/element/game.ts (plus any phantom-API JSDoc spread in player.ts). A re-verification trace recorded in the SUMMARY (audit file:line claims re-checked before each rewrite).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-documentation-audit-corrections/139-CONTEXT.md

<read_first>
Docs being corrected:
- docs/core-concepts.md (Commands (Low-Level) section ~line 164-186; visibleAttributes example ~line 137-146 and the "Hidden elements show only visibleAttributes" claim ~line 323; "single source of truth" boardsmith.json claim)
- docs/getting-started.md (CLI usage + boardsmith.json example)

Live source proving the real model (the corrections must match these, not the docs):
- src/engine/element/piece.ts:73-75 — putInto mutates the tree directly (moveToInternal); records nothing
- src/engine/element/space.ts:271-273 — shuffle mutates directly; no ShuffleCommand
- src/engine/element/game.ts:842-852 — commandHistory populated ONLY via game.execute() (engine ANIMATE path; game code never calls it)
- src/runtime/runner.ts:513-534 — fromSnapshot documents the real "fully STATE-AUTHORITATIVE, NO replay" model
- src/engine/element/game-element.ts:140 — `static visibleAttributes` is declared but read NOWHERE (dead security-looking field; DOCX-04 handles cross-doc sweep, but core-concepts.md's active teaching of it is corrected here)

registerActions real vs phantom API:
- src/engine/element/game.ts:915-954 — registerActions/registerAction methods + JSDoc (example at ~930-946 uses phantom action()/.do()/.chooseOnBoard())
- src/engine/element/game.ts:390, 424-428, 1644 — same phantom API appears in the Game class docstring, the class-level @example, and a RUNTIME error message ("Call this.registerActions(action('name')...)")
- src/engine/element/player.ts:176, 304 — phantom-API JSDoc spread
- src/engine/action/action-builder.ts — the REAL builder: Action.create(name).chooseElement(...)/.chooseFrom(...)/.execute(...)
- A real worked example lives in ~/BoardSmithGames/go-fish/src/rules/ (Action.create used in a shipped game) — lift the example from real game source, do not invent one

Real CLI surface (getting-started.md must match src/cli/cli.ts exactly):
- src/cli/cli.ts:31-40 — dev flags: -p/--port (default 5173), --host (default 127.0.0.1, local-only), --lan (0.0.0.0 shorthand), --players (default 2), --ai, --ai-level, --lock-teaching, --no-open
- src/cli/cli.ts:25-27 — init (note: -t/--template is currently the only real template; do not document nonexistent templates)
- Phase 135 already removed playerCount/$schema from the boardsmith.json example and fixed --worker-port/--host/--lan; this task audits the RESIDUAL (esp. --no-open, the 127.0.0.1 default, publish target)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite core-concepts.md to the real state-authoritative model (DOCX-01 / F11)</name>
  <files>docs/core-concepts.md</files>
  <read_first>
    - docs/core-concepts.md (Commands (Low-Level) section, visibleAttributes example + line ~323 claim, boardsmith.json "single source of truth" claim)
    - src/engine/element/piece.ts:73-75, src/engine/element/space.ts:271-273, src/engine/element/game.ts:842-852, src/runtime/runner.ts:513-534, src/engine/element/game-element.ts:140
  </read_first>
  <action>
    First re-verify F11's claims against live source (record the file:line trace in the SUMMARY): confirm putInto/shuffle/create mutate directly and record nothing, that commandHistory is populated only via game.execute() (engine-internal ANIMATE path, never called by game code), and that element.setAttribute() does not exist on GameElement.

    Then rewrite the affected sections of core-concepts.md to teach the REAL model:
    - Delete the "Commands (Low-Level)" auto-generated-command table (MoveCommand/ShuffleCommand/etc.) and the `element.setAttribute(key, value)` row entirely.
    - Replace with an accurate description: game logic mutates the element tree directly inside action `execute` callbacks; state is captured via state-authoritative snapshots (cite the runner.ts:513-534 model in prose, not code); commandHistory is an internal MCTS/debug mechanism NOT populated by element methods and NOT a complete audit log.
    - Remove the false claims "Only commands are sent over the network" and "Actions can be undone by reversing commands" — describe the real snapshot/undo model instead.
    - Fix the `static visibleAttributes` teaching (example ~137-146 and the "Hidden elements show only visibleAttributes" assertion ~323): visibleAttributes is a dead no-op field (read nowhere in src). Attribute-level filtering does not exist. Document the REAL controls: zone visibility (contentsHidden/contentsVisibleToOwner) and per-element visibility (hideFromAll/showOnlyTo) hide whole elements, plus the coarse `static playerView` escape hatch. Do NOT teach secrets-as-Player-attributes.
    - If core-concepts.md asserts boardsmith.json is the "single source of truth" for lobby/playerCount config, correct it: gameDefinition in src/rules/index.ts is the source of truth for player count (per Phase 135 CLIX-01).
    - Use direct property mutation as the model everywhere; no `setAttribute`, no command/event-sourcing vocabulary.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && ! grep -q "setAttribute" docs/core-concepts.md && ! grep -qiE "MoveCommand|ShuffleCommand|event.sourc|reversing commands" docs/core-concepts.md && grep -qi "state-authoritative" docs/core-concepts.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `setAttribute` in docs/core-concepts.md
    - Zero occurrences of MoveCommand/ShuffleCommand/event-sourcing/"reversing commands"
    - Document teaches state-authoritative snapshots + direct mutation
    - visibleAttributes no longer taught as a working attribute-hiding control
    - SUMMARY records the pre-rewrite re-verification trace
  </acceptance_criteria>
  <done>core-concepts.md teaches the real shipped model with no phantom command layer and no setAttribute.</done>
</task>

<task type="auto">
  <name>Task 2: Correct registerActions JSDoc + runtime error message to the real Action.create API (DOCX-02 / F14)</name>
  <files>src/engine/element/game.ts, src/engine/element/player.ts</files>
  <read_first>
    - src/engine/element/game.ts:390, 424-428, 915-954, 1644 (phantom action()/.do()/.chooseOnBoard() in JSDoc + class docstring + runtime error)
    - src/engine/element/player.ts:176, 304 (phantom-API JSDoc spread)
    - src/engine/action/action-builder.ts (real Action.create(...).chooseElement/.chooseFrom/.execute)
    - ~/BoardSmithGames/go-fish/src/rules/ (a real shipped Action.create example to lift)
  </read_first>
  <action>
    First re-verify F14 (record trace in SUMMARY): confirm action()/.do()/.chooseOnBoard() are not exported (src/engine/action/index.ts exports only Action) and that Action.create + .chooseElement/.chooseFrom/.execute is the real builder.

    Rewrite every phantom-API occurrence to the real API, using a working example lifted from an actual game (go-fish preferred, per Claude's Discretion in CONTEXT):
    - game.ts registerActions/registerAction JSDoc (~930-946): replace `action('drawCard').do(...).chooseOnBoard(...)` with `Action.create('drawCard').chooseElement(...).execute(...)` matching a real game's shape.
    - game.ts class docstring @example (~424-428): same correction.
    - game.ts:1644 RUNTIME error message: change "Call this.registerActions(action('${name}')...)" to the real "Call this.registerActions(Action.create('${name}')...)" so the actionable-error rule is honored (an error must not point to a nonexistent function).
    - player.ts:176, 304 JSDoc: same phantom→real correction.
    - Since these are JSDoc + a string literal (no logic change), tsc must stay clean.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && ! grep -qE "\.chooseOnBoard\(|action\('[a-zA-Z]+'\)\.do\(" src/engine/element/game.ts src/engine/element/player.ts && npx tsc --noEmit && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Zero `.chooseOnBoard(` and zero `action('name').do(` phantom-API occurrences in game.ts/player.ts
    - game.ts:1644 error message names Action.create, not action()
    - JSDoc examples use Action.create(...).chooseElement/.execute lifted from a real game
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The canonical action-registration docs and the registration runtime error teach the real builder API.</done>
</task>

<task type="auto">
  <name>Task 3: Residual getting-started.md CLI audit (DOCX-03 / F20)</name>
  <files>docs/getting-started.md</files>
  <read_first>
    - docs/getting-started.md (CLI usage sections + boardsmith.json example)
    - src/cli/cli.ts:25-40 (real init + dev flags)
    - src/cli/commands/dev.ts:35, 122, 788 (--no-open behavior); Phase 135 already removed playerCount/$schema and fixed --host/--lan/--worker-port
  </read_first>
  <action>
    First re-verify what Phase 135 already fixed vs. what remains (record trace in SUMMARY): grep getting-started.md for playerCount, $schema, --worker-port, --host, 0.0.0.0. Then audit the RESIDUAL against src/cli/cli.ts and fix any remaining drift:
    - Ensure `--no-open` is documented (exists in cli.ts:40 — do not auto-launch a browser tab; used for script/CI driving so an uncontrolled tab does not claim seat 1).
    - Ensure `--host` is described as defaulting to 127.0.0.1 (local-only) with `--lan` as the 0.0.0.0 network shorthand (per Phase 135 CLIX). Any lingering claim that dev serves the LAN by default is wrong.
    - Ensure the dev flags shown match cli.ts exactly: -p/--port (5173), --players (2), --ai, --ai-level, --lock-teaching, --no-open. Remove any flag not in cli.ts.
    - Confirm the boardsmith.json example has NO playerCount and NO $schema (Phase 135 removed them; verify they did not regress).
    - Do not document nonexistent init templates (only card-game exists today).
    - Verify the publish target / any port references are accurate.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && ! grep -q "playerCount" docs/getting-started.md && ! grep -q '"\$schema"' docs/getting-started.md && grep -q "\-\-no-open" docs/getting-started.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Zero playerCount and zero $schema in the boardsmith.json example
    - --no-open documented; --host default described as 127.0.0.1 with --lan for network
    - Every dev flag shown exists in src/cli/cli.ts; none invented
    - SUMMARY records the residual-vs-already-fixed trace
  </acceptance_criteria>
  <done>getting-started.md documents only the CLI that actually ships.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → developer mental model | Docs are the source developers/agents copy; teaching an insecure or nonexistent pattern propagates into game code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-01 | Information Disclosure | core-concepts.md visibleAttributes teaching | mitigate | Task 1 removes the dead-field teaching; docs no longer imply attribute-level hiding works, so devs use real element/zone visibility instead of a no-op |
| T-139-02 | Tampering | registerActions runtime error → nonexistent API | mitigate | Task 2 corrects game.ts:1644 so the actionable-error rule holds and devs are not steered to a phantom function |
| T-139-SC | Tampering | npm/pip/cargo installs | accept | Docs-only phase; no package installs introduced |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (JSDoc edits are source files)
- grep gates in each task pass (zero phantom/removed-symbol references)
- Full suite remains green at phase end (run at end of Plan 02): `npx vitest run`
</verification>

<success_criteria>
- DOCX-01: core-concepts.md free of event-sourcing model + setAttribute; teaches state-authoritative snapshots and real visibility controls
- DOCX-02: registerActions JSDoc + game.ts:1644 error + player.ts JSDoc use Action.create real API; tsc clean
- DOCX-03: getting-started.md matches the real CLI (incl. --no-open, 127.0.0.1 default)
</success_criteria>

<output>
Create `.planning/phases/139-documentation-audit-corrections/139-01-SUMMARY.md` when done, including the per-finding re-verification traces.
</output>
</output>
