---
phase: 141-file-templates-state-machine-authority
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/slash-command/bs/state-machine.md
  - src/cli/slash-command/bs/templates.test.ts
autonomous: true
requirements: [TMPL-03, TMPL-02]
must_haves:
  truths:
    - "state-machine.md states CHUNK.md owns its chunk status, SKETCH.md is derived, CHUNK.md wins on contradiction, and write order is CHUNK→SKETCH"
    - "state-machine.md lists the exact status enum and the exact 10 full-ceremony step names plus the 3-step light path"
    - "state-machine.md states the cold-resume parse contract (parse failure → stop and ask, never guess)"
    - "templates.test.ts drift test exists and passes for every state-machine.md invariant"
  artifacts:
    - path: "src/cli/slash-command/bs/state-machine.md"
      provides: "Shared authority-rules reference doc cited by every bs- skill"
      contains: "CHUNK.md wins"
    - path: "src/cli/slash-command/bs/templates.test.ts"
      provides: "Drift-protection vitest content-assertion test"
      contains: "describe('TMPL-03"
  key_links:
    - from: "src/cli/slash-command/bs/templates.test.ts"
      to: "src/cli/slash-command/bs/state-machine.md"
      via: "readFileSync + toContain string assertions"
      pattern: "readFileSync.*state-machine\\.md"
---

<objective>
Write `src/cli/slash-command/bs/state-machine.md` — the single shared authority-rules document every bs- skill will cite (never duplicate) — and scaffold `src/cli/slash-command/bs/templates.test.ts`, the drift-protection vitest that string-asserts the shipped reference files contain their exact required content.

This plan owns the state-machine authority rules (TMPL-03) and the parse-contract statement (TMPL-02 structural half), plus the test file that Plans 02 and 03 will extend.

Purpose: Cold-resume safety and chunk-status authority are enforced from day one as file states, and a CI test prevents silent drift of enum values / step names.
Output: `state-machine.md`, `templates.test.ts` (green for state-machine.md invariants).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/bs-skills-plan.md
@.planning/phases/141-file-templates-state-machine-authority/141-RESEARCH.md
@.planning/phases/141-file-templates-state-machine-authority/141-CONTEXT.md
@.planning/phases/141-file-templates-state-machine-authority/141-PATTERNS.md

<interfaces>
<!-- Canonical strings that MUST appear verbatim (case-sensitive) across state-machine.md,
     the templates, and the drift test. Source: bs-skills-plan.md "State-machine authority rules
     (hard)" + "Hard Rules", and 141-CONTEXT.md locked format decision. -->

Status enum (exact): proposed | approved | built | verified | verified (user-waived)
CHUNK-level stale marker (set by /bs-insert-chunk): stale — re-derive before build
  (NOTE: the dash in "stale — re-derive before build" is an em-dash "—", not a hyphen.)

Step names (exact, full ceremony, this order):
  investigate, redteam, ask, build, test, audit, repair, playtest, revise, close
Step names (exact, light path — trivial chunks): build, test, playtest

Status-line grammar (the parse contract): a line matching `Status: <enum-value>` (case-sensitive).
</interfaces>

<!-- Existing on-disk analogs to mirror (read, do not modify):
     src/cli/slash-command/aspects/index.md  — rules-list/registry style for state-machine.md
     src/cli/commands/init.test.ts (lines 15-21, 74-96) — readFileSync + toContain drift-test pattern -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write state-machine.md authority-rules doc</name>
  <files>src/cli/slash-command/bs/state-machine.md</files>
  <read_first>
    - .planning/bs-skills-plan.md — sections "State-machine authority rules (hard)", "Hard Rules" (Subagent discipline / Context management / Human gates / Git protocol), and the numbered `/bs-build-chunk` step walkthrough (steps 1-10) for step names, repair-loop bound, and redteam escalation
    - .planning/phases/141-file-templates-state-machine-authority/141-RESEARCH.md — "Code Examples → state-machine.md skeleton shape" (transcribe every rule section it lists, expanded to full prose) and Pitfall 3/4
    - .planning/phases/141-file-templates-state-machine-authority/141-PATTERNS.md — state-machine.md section (mirror `aspects/index.md`'s bold-lead-in numbered-rule style)
    - src/cli/slash-command/aspects/index.md — the analog rules-doc structure to match
  </read_first>
  <action>
    Create `src/cli/slash-command/bs/state-machine.md` as a standalone rules-reference document (full content, NOT a thin pointer). Open with an H1 title and a one-line mission comment stating bs- skills cite this file rather than restating its rules. Transcribe verbatim, one `##` section each: the Status Enum (exact values including `verified (user-waived)` and the em-dash stale marker `stale — re-derive before build`); the full-ceremony Step Names (the 10 words in order per D-CONTEXT) and the light-path subset (build, test, playtest); Authority (CHUNK.md owns its status, SKETCH.md holds only ordered list + derived pointers, CHUNK.md wins on contradiction and the session logs+repairs SKETCH.md) — this is TMPL-03; Write Order (always CHUNK.md first then SKETCH.md; every write leaves the file cold-resumable — append-only round entries, `Status:` line updated last); Cold-Resume Parse Contract (a state file that does not parse against its template → STOP and ask the user, never guess) — this is TMPL-02; Consistency Check on entry (every sketch slug has a `chunks/<slug>/` directory, every directory has a SKETCH.md entry, all statuses parse, no stale session lock; report and confirm before proceeding); Rulings Outrank Rulebook (every rulebook-reading agent also reads RULINGS.md); Restyle/Cutover Rule (any change that re-styles/re-lays-out previously verified surfaces flips those chunks back to `built`); Session Lock (SKETCH.md carries a chunk+timestamp lock note; a second concurrent session warns instead of clobbering); Git Protocol (commit at every step completion `chunk-<slug>/step-<name>`, revise rounds `chunk-<slug>/revise-2`, commit BEFORE build starts, `close` records the verified commit hash in CHUNK.md); Repair Loop Bound (max 3 audit rounds, round N+1 auditors report only NEW findings via stable ledger IDs, remaining findings triaged with the user); Redteam Escalation (refuted once → re-investigate with objections, max 1 re-investigate round; refuted twice → escalate to user, ruling recorded in RULINGS.md); Session Handoff Seams (structural: {investigate+redteam+ask}, {build+test}, {audit+repair}, {playtest+one revise round}; one step group per session). Match `aspects/index.md`'s bold-lead-in terse-sentence style. Do NOT invent rules beyond the plan; do NOT add a markdown parser or code.
  </action>
  <verify>
    <automated>grep -q "CHUNK.md wins" src/cli/slash-command/bs/state-machine.md && grep -q "stale — re-derive before build" src/cli/slash-command/bs/state-machine.md && grep -q "investigate, redteam, ask, build, test, audit, repair, playtest, revise, close" src/cli/slash-command/bs/state-machine.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `src/cli/slash-command/bs/state-machine.md` exists.
    - Contains the exact status enum tokens `proposed`, `approved`, `built`, `verified`, `verified (user-waived)`, and `stale — re-derive before build` (em-dash).
    - Contains the exact full step-name list string `investigate, redteam, ask, build, test, audit, repair, playtest, revise, close` and the light-path list `build, test, playtest`.
    - Contains the Authority rule text asserting CHUNK.md wins on contradiction and write order CHUNK→SKETCH.
    - Contains the cold-resume parse-contract sentence (parse failure → stop and ask, never guess).
    - Is standalone full content (NOT a `{{BOARDSMITH_ROOT}}` thin pointer); does not modify design-game.template.md, instructions.md, or aspects/*.
  </acceptance_criteria>
  <done>state-machine.md transcribes every authority/write-order/cold-resume/consistency/git/repair/escalation/handoff rule from the plan with exact enum and step-name strings.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold templates.test.ts with state-machine.md drift assertions</name>
  <files>src/cli/slash-command/bs/templates.test.ts</files>
  <read_first>
    - src/cli/commands/init.test.ts (lines 1-21, 74-96) — copy the header-comment + import + readFileSync/__dirname boilerplate and the describe/indexOf/slice/toContain assertion shape verbatim (drop the generateGameTs/generateTestTs import)
    - .planning/phases/141-file-templates-state-machine-authority/141-RESEARCH.md — "Validation Architecture → Phase Requirements → Test Map" (describe-block naming by requirement ID so `-t "TMPL-03"` selects them) and Pitfall 3 (cross-file step-name agreement)
    - .planning/phases/141-file-templates-state-machine-authority/141-PATTERNS.md — templates.test.ts section
    - src/cli/slash-command/bs/state-machine.md (from Task 1) — the file under assertion
  </read_first>
  <action>
    Create `src/cli/slash-command/bs/templates.test.ts`. Open with a block header comment citing TMPL-01/02/03 and the drift-protection rationale (mirror init.test.ts's header style). Keep the `readFileSync` + `fileURLToPath`/`dirname`/`join` + `__dirname` boilerplate verbatim from init.test.ts; add a small helper that reads a file relative to `__dirname` (e.g. `read('state-machine.md')`, `read(join('templates', 'CHUNK.template.md'))`). In THIS plan add only the state-machine.md assertions, grouped in `describe` blocks named by requirement ID so the RESEARCH test-map `-t` selectors work: a `describe('TMPL-03 — authority & write order', ...)` asserting state-machine.md `toContain` the CHUNK-wins authority text and the write-order CHUNK→SKETCH text; a `describe('TMPL-02 — parse contract', ...)` asserting state-machine.md contains the cold-resume stop-and-ask sentence; and shared assertions that state-machine.md contains the exact full step-name list string and each exact status-enum token including `verified (user-waived)` and `stale — re-derive before build`. Use plain `toContain`/`not.toContain` on raw file text — no markdown parser, no snapshot, no new dependency. Leave clearly-marked placeholders/TODO comments for the CHUNK/SKETCH assertions (Plan 02) and the four ledger templates (Plan 03) so the later plans extend rather than restructure; do NOT write assertions against files that do not yet exist (they would fail).
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/templates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/cli/slash-command/bs/templates.test.ts` exists and the vitest run is GREEN (all present assertions pass).
    - Contains `describe` blocks whose names include `TMPL-03` and `TMPL-02` (so `npx vitest run ... -t "TMPL-03"` selects at least one test).
    - Reads `state-machine.md` from disk via `readFileSync`/`join(__dirname, ...)` (no hardcoded absolute paths).
    - Asserts the exact status-enum tokens and the exact full step-name list string against state-machine.md.
    - No import of any markdown-parsing library; no new package added to package.json.
    - Placeholder comments mark where Plan 02 (CHUNK/SKETCH) and Plan 03 (ledgers) will add assertions.
  </acceptance_criteria>
  <done>Drift test runs green, asserts every state-machine.md invariant, and reserves extension points for the later plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan ships two static reference files (markdown + a content-assertion vitest) with no runtime code, no external inputs, no network, and no user-supplied data crossing any boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-01 | S/T/R/I/D/E | markdown reference files + vitest content test | accept | No attack surface: no runtime execution of file contents, no inputs, no network, no privilege boundary. Nothing to mitigate. |
| T-141-SC | Tampering | npm/pip/cargo installs | N/A | This plan installs zero packages (uses already-present vitest ^2.1.0 + Node built-ins). No supply-chain surface; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/templates.test.ts` is green.
- `grep -c "state-machine.md" src/cli/slash-command/bs/state-machine.md` — file is standalone (self-title only), not a thin pointer to instructions.md.
</verification>

<success_criteria>
- state-machine.md exists with all authority/write-order/cold-resume/consistency/git/repair/escalation/handoff rules and exact enum + step-name strings (TMPL-03 core, TMPL-02 parse-contract statement).
- templates.test.ts exists, green, asserting state-machine.md invariants, with extension points for Plans 02/03.
</success_criteria>

<output>
Create `.planning/phases/141-file-templates-state-machine-authority/141-01-SUMMARY.md` when done.
</output>
