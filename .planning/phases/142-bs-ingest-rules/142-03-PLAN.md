---
phase: 142-bs-ingest-rules
plan: 03
type: execute
wave: 2
depends_on: [142-01]
files_modified:
  - src/cli/slash-command/bs/ingest/scaffold.md
  - src/cli/slash-command/bs/ingest/sketch-derivation.md
autonomous: true
requirements: [INGEST-04, INGEST-05]
must_haves:
  truths:
    - "Running `npx vitest run src/cli/slash-command/bs/ingest.test.ts -t \"INGEST-04\"` passes"
    - "Running the same suite with -t \"INGEST-05\" passes"
    - "Running the same suite with -t \"consistency\" passes (every path the orchestrator references now exists)"
    - "scaffold.md instructs init from the parent dir with a derived name, then tsc --noEmit, then dev --no-open + serve-check + explicit kill in the same numbered sequence"
    - "sketch-derivation.md caps detail at the next 2-3 chunks with the byte-identical sketch-level tail marker and mandates core-event-loop-first + game-end + final-acceptance chunks"
  artifacts:
    - path: "src/cli/slash-command/bs/ingest/scaffold.md"
      provides: "Name-derivation rules (old Phase 1B verbatim), corrected init-creates-new-subdir framing, tsc --noEmit compile gate, dev --no-open serve-check + explicit kill"
      contains: "Ready! Press Ctrl+C to stop."
      min_lines: 45
    - path: "src/cli/slash-command/bs/ingest/sketch-derivation.md"
      provides: "Chunking heuristic honoring SKETCH.template.md's Mandated Chunks + Ordered Chunk List; core-event-loop-first, game-end + final-acceptance, ui: tag, outcome-based scripts, 2-3-chunk detail cap"
      contains: "Status: proposed (sketch-level — no CHUNK.md yet)"
      min_lines: 45
  key_links:
    - from: "src/cli/slash-command/bs/ingest/sketch-derivation.md"
      to: "src/cli/slash-command/bs/templates/SKETCH.template.md"
      via: "citation pointer to Mandated Chunks / Ordered Chunk List (never restatement)"
      pattern: "SKETCH\\.template\\.md|Mandated Chunks"
    - from: "src/cli/slash-command/bs/ingest/scaffold.md"
      to: "npx boardsmith init / dev --no-open"
      via: "CLI invocation with explicit kill after serve check"
      pattern: "kill"
---

<objective>
Author the two scaffold/sketch reference files: `ingest/scaffold.md` (project scaffold + compile/serve verification, INGEST-04) and `ingest/sketch-derivation.md` (the chunking heuristic that fills SKETCH.template.md, INGEST-05). scaffold.md extracts the old skill's Phase 1B naming rules verbatim while CORRECTING its stale directory-choice framing (init always creates a new subdirectory), and replaces the old "open a browser and eyeball it" check with an automated `--no-open` serve-check + explicit kill. sketch-derivation.md cites (never restates) SKETCH.template.md's Mandated Chunks contract and pins the byte-identical sketch-level tail marker. This is the last plan — completing it turns the cross-file consistency gate green.

Purpose: Turn INGEST-04, INGEST-05, and the cross-file consistency describe blocks (authored RED in Plan 01) green, completing the phase.
Output: `src/cli/slash-command/bs/ingest/scaffold.md`, `src/cli/slash-command/bs/ingest/sketch-derivation.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142-bs-ingest-rules/142-CONTEXT.md
@.planning/phases/142-bs-ingest-rules/142-RESEARCH.md
@.planning/phases/142-bs-ingest-rules/142-PATTERNS.md
@.planning/bs-skills-plan.md

<interfaces>
<!-- Exact CLI ground truth (verified against source this planning pass). -->
src/cli/cli.ts:25          `.command('init <name>')`  — init ALWAYS takes a name
src/cli/commands/init.ts:18-19  `existsSync(projectPath)` → `Error: Directory "<name>" already exists`
                            (there is NO in-place/cwd mode — run init from the PARENT dir)
src/cli/commands/dev.ts:788 `Skipping auto-open (--no-open): connect a client to claim seat 1 yourself.`
src/cli/commands/dev.ts:791 `Ready! Press Ctrl+C to stop.`   ← the exact ready-state line to wait for
Old-skill extraction: instructions.md:90-120 (naming rules — extract 96-106 verbatim; DROP the
  108-117 directory-choice logic), instructions.md:2271-2313 (Phase 6 verify — compile-check
  2275-2290 extract; serve-check 2293-2302 REPLACE with the killable --no-open sequence).

<!-- SKETCH.template.md contract to CITE (Phase 141 output), never restate. -->
"## Mandated Chunks": first chunk = core event loop; must contain a game-end/scoring/winner chunk;
  must contain a final-acceptance chunk (full playthrough + coverage check + a11y audit).
Tail-entry marker (BYTE-IDENTICAL, em-dash): `Status: proposed (sketch-level — no CHUNK.md yet)`
`ui:` tag values: `none | touches | major`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ingest/scaffold.md (INGEST-04)</name>
  <files>src/cli/slash-command/bs/ingest/scaffold.md</files>
  <read_first>
    - src/cli/slash-command/instructions.md lines 90-120 (Phase 1B naming rules — extract 96-106 verbatim; the 108-117 directory logic is the anti-pattern to correct, NOT extract)
    - src/cli/slash-command/instructions.md lines 2271-2313 (Phase 6 verify — compile-check to extract, serve-check to replace)
    - src/cli/commands/init.ts (lines 1-60 — init errors if dir exists; toPascalCase helper)
    - src/cli/commands/dev.ts (grep `--no-open` / `Ready!` — lines 35, 788, 791; the exact ready-state string)
    - .planning/phases/142-bs-ingest-rules/142-RESEARCH.md ("Scaffold step — exact CLI commands"; Pitfall 1; Pitfall 3)
    - .planning/phases/142-bs-ingest-rules/142-PATTERNS.md (the `scaffold.md` section, lines ~80-134)
  </read_first>
  <action>Author `src/cli/slash-command/bs/ingest/scaffold.md`. Extract the old Phase 1B name-derivation rules verbatim (instructions.md:96-106: Display Name / Project Name kebab-case / Class Name PascalCase; lowercase, hyphenate spaces/special chars, collapse consecutive hyphens, strip leading/trailing hyphens, PascalCase class names) and note the CLI's own `toPascalCase`/`toDisplayName` helpers already implement this. CORRECT the stale framing per Pitfall 1: `npx boardsmith init <name>` ALWAYS creates `<cwd>/<name>` and ERRORS (`Error: Directory "<name>" already exists`, init.ts:19) if it exists — instruct running init from the PARENT of where the project should live using the derived kebab-case name; do NOT extract the old "if empty use cwd" directory-choice logic. Then a numbered verification sequence in ONE block: (1) `cd <name> && npx tsc --noEmit` (compile gate; on error, fix and re-run until clean); (2) `npx boardsmith dev --no-open`, wait for the exact ready-state line `Ready! Press Ctrl+C to stop.` (or curl the resolved URL) to confirm it serves; (3) KILL the process — an explicit numbered step in the SAME sequence, never a footnote (repo hard rule + Pitfall 3). Failures stop with an actionable message. Cite docs required-reading list by pointer rather than recopying it.</action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "INGEST-04"</automated>
  </verify>
  <done>INGEST-04 describe block passes: scaffold.md names `boardsmith init`, `tsc --noEmit`, the exact `Ready! Press Ctrl+C to stop.` serve-check, and an explicit kill step; corrects the init-creates-new-subdir framing (no "use current directory" instruction).</done>
</task>

<task type="auto">
  <name>Task 2: Author ingest/sketch-derivation.md (INGEST-05) + green the consistency gate</name>
  <files>src/cli/slash-command/bs/ingest/sketch-derivation.md</files>
  <read_first>
    - src/cli/slash-command/bs/templates/SKETCH.template.md (FULL — Mandated Chunks + Ordered Chunk List + status-line grammar + `ui:` tag; CITE by section name, never restate)
    - src/cli/slash-command/bs/state-machine.md (lazy-tail / consistency-check exemption for tail entries)
    - .planning/bs-skills-plan.md lines 71-78 (SKETCH.md contract, first chunk = core event loop, mandated chunks, approval gate, negotiation posture)
    - .planning/phases/142-bs-ingest-rules/142-RESEARCH.md ("Sketch Derivation"; Pitfall 5)
    - .planning/phases/142-bs-ingest-rules/142-PATTERNS.md (the `sketch-derivation.md` section, lines ~138-163)
  </read_first>
  <action>Author `src/cli/slash-command/bs/ingest/sketch-derivation.md` as the chunking heuristic that FILLS SKETCH.template.md (never restates its structure — cite the template's "Mandated Chunks" and "Ordered Chunk List" sections by name). State: (1) first chunk is always the core event loop (smallest slice where a human takes one browser action and sees a response); (2) the sketch MUST contain a game-end/scoring/winner chunk and a final-acceptance chunk (full playthrough + non-variant-slice coverage check + design-QA/a11y audit); (3) each chunk records a `ui:` tag with the exact values `none | touches | major`; (4) test scripts state OUTCOMES not gestures ("move a pawn one space; the board reflects it"); (5) HARD CAP: only the next 2-3 chunks are detailed — everything beyond uses the BYTE-IDENTICAL tail marker `Status: proposed (sketch-level — no CHUNK.md yet)` and no `chunks/<slug>/` dir is created for tail entries (Pitfall 5); (6) variants tagged out-of-scope-by-default, listed under SKETCH.md "Variants (deferred)"; (7) negotiation posture: user ordering wins unless a named hard dependency is violated. Use the tail marker string verbatim so the drift constant in ingest.test.ts matches.</action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "INGEST-05"; npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "consistency"</automated>
  </verify>
  <done>INGEST-05 AND the cross-file consistency describe blocks pass. sketch-derivation.md cites SKETCH.template.md's Mandated Chunks, uses the byte-identical tail marker + `ui:` tag values, and every path the orchestrator references now exists on disk. Full suite `npm test` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude session ↔ local CLI (`boardsmith init` / `dev`) | The scaffold step spawns a local dev server. No untrusted network boundary is crossed; the only safety concern is process lifecycle (a leaked long-running server), addressed by the mandated explicit-kill step. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-04 | Denial of Service | leaked dev server process | mitigate | scaffold.md mandates an explicit KILL step in the same numbered sequence as start/verify (repo hard rule + Pitfall 3); INGEST-04 drift assertion pins the kill instruction's presence. |
| T-142-05 | Tampering | sketch tail-marker drift | mitigate | The byte-identical `Status: proposed (sketch-level — no CHUNK.md yet)` marker is pinned as a shared constant asserted on both sketch-derivation.md and SKETCH.template.md, failing loudly on drift. |
| T-142-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase. |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "INGEST-04"` — green
- `npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "INGEST-05"` — green
- `npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "consistency"` — green
- Full suite `npm test` — green (all seven INGEST blocks + consistency + Phase 141's 44 tests)
</verification>

<success_criteria>
- `ingest/scaffold.md` and `ingest/sketch-derivation.md` exist and are cited by the orchestrator
- scaffold.md's serve-check ends with an explicit kill; init framing corrected
- sketch-derivation.md fills SKETCH.template.md with the mandated chunks, ui: tags, and 2-3-chunk detail cap
- INGEST-04, INGEST-05, and cross-file consistency assertions pass; full suite green
</success_criteria>

<output>
Create `.planning/phases/142-bs-ingest-rules/142-03-SUMMARY.md` when done
</output>
