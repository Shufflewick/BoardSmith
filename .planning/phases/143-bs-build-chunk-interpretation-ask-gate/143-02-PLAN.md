---
phase: 143-bs-build-chunk-interpretation-ask-gate
plan: 02
type: execute
wave: 2
depends_on: [143-01]
files_modified:
  - src/cli/slash-command/bs/build-chunk.md
autonomous: true
requirements: [BUILD-01, BUILD-12]
must_haves:
  truths:
    - "Running /bs-build-chunk resumes at the first incomplete step of the current chunk, including mid-loop and awaiting-playtest states"
    - "Conversational intents (what's left / do X next) route to status/insert behavior instead of misbuilding"
    - "Trivial chunks run the light path (build → test → playtest) with the user explicitly told which ceremony is in effect"
    - "The orchestrator names all 10 steps and dispatches investigate/redteam/ask to their reference files; Steps 4–10 are forward-referenced with 'authored in Phase 14X' markers"
  artifacts:
    - path: "src/cli/slash-command/bs/build-chunk.md"
      provides: "Lean orchestrator: entry consistency check, 3-way session lock, conversational-intent probe, resume routing, full+light routing tables, dispatch sections, session-handoff seams, gate-before-write for ask, Reference Files + Installed Location footer"
      contains: "investigate, redteam, ask, build, test, audit, repair, playtest, revise, close"
      min_lines: 150
  key_links:
    - from: "src/cli/slash-command/bs/build-chunk.md"
      to: "src/cli/slash-command/bs/state-machine.md"
      via: "cite-not-restate for enum, step names, session lock, write order, consistency check, handoff seams"
      pattern: "state-machine.md"
    - from: "src/cli/slash-command/bs/build-chunk.md"
      to: "src/cli/slash-command/bs/build/{investigate,redteam,ask}.md"
      via: "delegate-to-reference-file dispatch, consuming return-shape field names by pinned name"
      pattern: "build/investigate.md"
---

<objective>
Author the FULL `src/cli/slash-command/bs/build-chunk.md` orchestrator: the lean router that
detects state, runs the entry consistency check, resolves the 3-way session lock, intercepts
conversational intents, resumes at the first incomplete step, routes full vs. light ceremony, and
dispatches investigate/redteam/ask to their reference files — with Steps 4–10 named as
forward-references. Mirrors `ingest-rules.md` idiom byte-for-byte.

Purpose: This is the single entry point for `/bs-build-chunk` and the home of BUILD-01 (resume
routing) and BUILD-12 (light-path routing). It defines the routing the reference files plug into.
Output: One new markdown file. Makes the BUILD-01 and BUILD-12 test blocks GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-CONTEXT.md
@.planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-RESEARCH.md
@.planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-PATTERNS.md
@.planning/bs-skills-plan.md
@src/cli/slash-command/bs/ingest-rules.md
@src/cli/slash-command/bs/state-machine.md
@src/cli/slash-command/bs/build-chunk.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author build-chunk.md orchestrator (BUILD-01 + BUILD-12)</name>
  <files>src/cli/slash-command/bs/build-chunk.md</files>
  <read_first>
    - src/cli/slash-command/bs/ingest-rules.md — the exact orchestrator analog: header/citation-discipline (lines 1–8), Context-Economics Hard Rule (13–19), Step 0 state-detection + `ls <file>` (never glob) idiom (21–63), delegation-to-reference-file shape (65–91), Step 6 Approval Gate + Step 7 "single point where gated state is written" split (153–187), session-ending handoff (189–190), Reference Files + Installed Location footer (192–214)
    - src/cli/slash-command/bs/state-machine.md — CITE (never restate): Status Enum (5), full-ceremony step list (27), light-path list (33) + light-path transitions (37–44), Consistency Check (75–92), Session Lock 3-way branch (102–112), Write Order (56–61), Session Handoff Seams / 4 step groups (133–148), Rulings Outrank Rulebook (94–96), Git Protocol (114–119)
    - src/cli/slash-command/bs/build-chunk.test.ts — the contract: the exact return-shape field names (INVESTIGATE_RETURN_FIELDS, REDTEAM_* fields) this orchestrator must consume by name, the "authored in Phase 144/145/146" marker strings, and the REFERENCED_PATHS this file must cite verbatim
    - .planning/bs-skills-plan.md §"/bs-build-chunk" — canonical prose for steps 1–3, ceremony scaling, conversational intents, one-line progress narration
    - .planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-PATTERNS.md — build-chunk.md section (lines 19–98): every orchestrator sub-pattern with line refs
    - 142-REVIEW-FIX lessons (via 143-RESEARCH.md Patterns 3 & 4): gate-before-write (never write Status: approved speculatively); subagents write state directly, orchestrator never re-reads a slice to "double-check"
  </read_first>
  <action>
    Create `src/cli/slash-command/bs/build-chunk.md`, a lean router mirroring `ingest-rules.md`.

    Header + citation discipline: copy ingest-rules.md's "cite state-machine.md and templates
    rather than restating; this file is a lean router" framing verbatim in spirit.

    Context-Economics Hard Rule section: restate that the orchestrator never reads rulebook
    slices/docs itself and never re-reads a slice/CHUNK.md section after a subagent writes it;
    `build/investigate.md` and `build/redteam.md` restate it because those are the
    highest-temptation steps.

    Step 0 — Entry: run the Consistency Check (cite state-machine.md "Consistency Check"); then the
    Session Lock 3-way branch (cite state-machine.md "Session Lock") — implement ALL THREE outcomes
    literally (143-RESEARCH.md Pitfall 5): same-chunk resume → refresh timestamp silently, no
    warning; <24h different work → warn concurrent session; >24h any chunk → report stale, user
    confirms clearing. Use literal `ls <file>` checks in the current directory, never `**/glob`.

    Conversational-intent probe (BUILD-01): before any step dispatch, intercept intents like
    "what's left?" → route internally to status behavior (Phase 147 `/bs-check-status`), "do the
    Chance cards next" → route internally to insert behavior (Phase 147 `/bs-insert-chunk`); neither
    → continue. Name those future skills as the routing targets.

    Resume routing (BUILD-01): read SKETCH.md → first non-verified chunk → its CHUNK.md → route to
    the first incomplete step per its Step Checklist. Awaiting-playtest states re-pose the pending
    question verbatim as the first move.

    Ceremony routing table: quote the full-ceremony 10-step list verbatim
    (`investigate, redteam, ask, build, test, audit, repair, playtest, revise, close`) and the
    light-path list verbatim (`build, test, playtest`). For full ceremony, step ∈
    {investigate, redteam, ask} → delegate to the matching `build/*.md`; steps 4–10 → forward
    references naming each exact path `build/{build,test,audit,repair,playtest,revise,close}.md`
    with the literal marker "authored in Phase 144" (build,test), "authored in Phase 145"
    (audit,repair), "authored in Phase 146" (playtest,revise,close).

    Light path (BUILD-12 — routing, NOT a step; no `build/light.md` file per Pitfall 3): trivial-
    tagged chunks run build → test → playtest; the user is explicitly told which ceremony is in
    effect; `approved` is unreachable (proposed → built on proposal acceptance); playtest performs
    close's bookkeeping for light chunks. Cite state-machine.md light-path transitions; do not
    restate them.

    Dispatch sections (delegate-to-reference-file shape): "Delegate the entire investigate sequence
    to `build/investigate.md` …", "… redteam sequence to `build/redteam.md` …", "… ask-gate
    presentation to `build/ask.md` …". In the records/dispatch prose, consume the pinned
    return-shape field names by exact name (INVESTIGATE_RETURN_FIELDS: `claimsList`,
    `visibilityDeclaration`, `newlyDiscoveredCitations`; REDTEAM: `claimNumber`, `verdict`,
    `objection`, `missingInteractions`) so the test's producer↔consumer pins resolve.

    Gate-before-write for ask (orchestrator-side, cite state-machine.md Write Order): present via
    ask, gate, then write `Status: approved` LAST — never speculatively. RULINGS.md/ASSETS.md
    entries from the ask conversation are written only after explicit approval. Investigate's
    claims list / visibility declaration are NOT gated (written progressively).

    Session-handoff seams: cite state-machine.md "Session Handoff Seams" (4 step groups); every
    session ends by printing the exact next command to run (`/bs-build-chunk`) — non-programmer
    handoff. Give 1–2 example one-line progress narration lines per step group as a style guide,
    not a script (per 143-RESEARCH.md Open Question 2).

    Git Protocol section: cite state-machine.md "Git Protocol" (commit `chunk-<slug>/step-<name>`
    at each step completion; commit before build starts — 144's territory, cited now).

    Footer: Reference Files list — `build/{investigate,redteam,ask}.md` as authored-now, then the
    seven `build/{build,test,audit,repair,playtest,revise,close}.md` forward-reference entries with
    the "authored in Phase 14X" markers, then the shared `state-machine.md` + `templates/*.md`
    list; then the identical "Installed location" paragraph (paths resolve against this skill file's
    directory; installer bundles `bs/` as one unit in Phase 148).

    Contain NO fenced implementation code; this is directive orchestration prose.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-01" && npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-12"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-01"` passes GREEN
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-12"` passes GREEN
    - `grep -F "investigate, redteam, ask, build, test, audit, repair, playtest, revise, close" src/cli/slash-command/bs/build-chunk.md` matches
    - `grep -F "build, test, playtest" src/cli/slash-command/bs/build-chunk.md` matches (light path)
    - `grep -c "authored in Phase 14" src/cli/slash-command/bs/build-chunk.md` returns >= 3 (144/145/146 markers all present)
    - `grep -E "build/investigate.md|build/redteam.md|build/ask.md" src/cli/slash-command/bs/build-chunk.md` matches all three dispatch citations
    - Session-lock section has three distinct outcomes (grep for "stale", "same chunk"/"resume", and "concurrent"/"different")
  </acceptance_criteria>
  <done>build-chunk.md exists as a lean router; BUILD-01 and BUILD-12 test blocks GREEN; investigate/redteam/ask are dispatched, Steps 4–10 forward-referenced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | LLM-instruction markdown only; no auth/network/session/input-parsing surface (143-RESEARCH.md Security Domain). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Tampering | Speculative write of `Status: approved` before user authorization (a resumed session finding an authorization that never happened) | mitigate | Gate-before-write: orchestrator writes Status: approved LAST, only after explicit ask approval; cite state-machine.md Write Order |
| T-143-02 | Tampering | Step-name/enum drift from state-machine.md | mitigate | build-chunk.md quotes the lists verbatim; 143-01 drift test pins them byte-identically |
| T-143-SC | Tampering | npm/pip/cargo installs | N/A | Zero package installs this phase |
</threat_model>

<verification>
`npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-01"` and `-t "BUILD-12"` both
green. Full suite still RED on investigate/redteam/ask blocks until Plans 03–05 land.
</verification>

<success_criteria>
Full orchestrator authored; resume routing + light-path routing test blocks green; investigate/
redteam/ask dispatched; Steps 4–10 forward-referenced with phase markers.
</success_criteria>

<output>
Create `.planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-02-SUMMARY.md` when done.
</output>
