---
phase: 143-bs-build-chunk-interpretation-ask-gate
plan: 05
type: execute
wave: 2
depends_on: [143-01]
files_modified:
  - src/cli/slash-command/bs/build/ask.md
autonomous: true
requirements: [BUILD-04]
must_haves:
  truths:
    - "The ask gate presents a fixed 4-part format: plain designer-language rules with citations, ambiguity questions with options, a 'what you will NOT see yet' list, and zero implementation vocabulary"
    - "Assets are requested here with a never-blocking placeholder path; 'I don't have art yet' never blocks"
    - "Status: approved and any RULINGS.md/ASSETS.md entries from the ask conversation are written only after explicit user approval (gate-before-write); investigate's claims list is NOT re-gated"
  artifacts:
    - path: "src/cli/slash-command/bs/build/ask.md"
      provides: "Ask-gate reference: 4-part presentation skeleton, implementation-vocabulary ban, never-blocking asset request, gate-before-write with Status-last write order, RULINGS/ASSETS ledger fills"
      contains: "what you will NOT see yet"
      min_lines: 80
  key_links:
    - from: "src/cli/slash-command/bs/build/ask.md"
      to: "src/cli/slash-command/bs/templates/ASSETS.template.md"
      via: "appends a never-blocking placeholder row to the 5-column ledger, never restructures the header"
      pattern: "ASSETS"
    - from: "src/cli/slash-command/bs/build/ask.md"
      to: "src/cli/slash-command/bs/state-machine.md"
      via: "cite Write Order — Status: approved written last, only after explicit approval"
      pattern: "Write Order"
---

<objective>
Author `src/cli/slash-command/bs/build/ask.md` — the ask-gate reference file (BUILD-04). It
specifies the fixed 4-part plain-language presentation, the total ban on implementation vocabulary,
the never-blocking asset request, and the gate-before-write discipline that writes `Status: approved`
(and any RULINGS.md/ASSETS.md entries) only after the user's explicit yes. Mirrors `ingest-rules.md`
Step 6 (Approval Gate) + Step 7 (Write Files).

Purpose: The ask gate is the human-approval boundary — the point where a plain-language design is
authorized before a single line of code is written.
Output: One new markdown file. Makes the BUILD-04 test block GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-CONTEXT.md
@.planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-RESEARCH.md
@.planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-PATTERNS.md
@.planning/bs-skills-plan.md
@src/cli/slash-command/bs/ingest-rules.md
@src/cli/slash-command/bs/state-machine.md
@src/cli/slash-command/bs/templates/RULINGS.template.md
@src/cli/slash-command/bs/templates/ASSETS.template.md
@src/cli/slash-command/bs/build-chunk.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author build/ask.md (BUILD-04)</name>
  <files>src/cli/slash-command/bs/build/ask.md</files>
  <read_first>
    - src/cli/slash-command/bs/ingest-rules.md — Step 6 Approval Gate negotiation posture ("the user's answer wins; do not proceed to writing final files until the user has explicitly approved", lines 153–160) + Step 7 "single point where gated state is written" + the already-written carve-out (162–187)
    - src/cli/slash-command/bs/state-machine.md — CITE (never restate): "Write Order" (56–61: Status line updated LAST) and Status Enum note that `approved` is set at the ask step (line 12)
    - src/cli/slash-command/bs/templates/ASSETS.template.md — the 5-column ledger `needed-by-chunk | requested | received | placeholder-in-use | file path` (lines 36–38) ask appends a row to; never restructure the header
    - src/cli/slash-command/bs/templates/RULINGS.template.md — the `### Ruling N` Decision/Citation/Rationale shape (35–51) an ask-gate house-rule choice fills
    - .planning/bs-skills-plan.md §"/bs-build-chunk" step 3 — canonical ask-gate spec (cite, don't restate)
    - .planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-RESEARCH.md — ask gate 4-part skeleton (lines 469–491), Pitfall 2 (implementation-vocabulary leakage; forbid `action`/`flow`/`state`/`element`), Pattern 4 (gate-before-write; investigate output NOT re-gated)
    - src/cli/slash-command/bs/build-chunk.test.ts — the BUILD-04 assertions (4-part markers, forbidden-vocabulary list including `action`/`flow`/`state`/`element`, never-blocking placeholder, Status-approved-written-last)
  </read_first>
  <action>
    Create `src/cli/slash-command/bs/build/ask.md`, mirroring `ingest-rules.md` Steps 6–7.

    4-part presentation skeleton (143-RESEARCH.md Code Example; Claude's discretion on exact copy —
    keep the register of the example claims: rent/turn/hand/board):
      (a) Rules interpretation in plain game-designer language WITH citations;
      (b) Ambiguities as concrete questions with explicit options (Option A / Option B);
      (c) A "what you will NOT see yet" deferred list;
      (d) Zero implementation vocabulary — the presentation body must contain NO engine terms.
    Include an explicit prohibition subsection naming the forbidden words to keep out of the
    user-facing body: `action`, `flow`, `state`, `element` (and BoardSmith API vocabulary generally,
    per Pitfall 2). Use only designer language in the file's own example copy.

    Asset request (never-blocking): request any needed assets here; "I don't have art yet" never
    blocks — a placeholder that matches the final layout is used; debt is recorded by appending a
    row to ASSETS.md's 5-column ledger (fill, never restructure the header). Reference the
    DESIGN/ASSETS placeholder policy.

    House-rule / adaptation choices made at ask go to RULINGS.md as a `### Ruling N` entry (fill
    RULINGS.template.md's Decision / Citation / Rationale shape).

    Gate-before-write (143-RESEARCH.md Pattern 4; the load-bearing CR-01 fix from 142-REVIEW-FIX):
    present the 4-part format, negotiate (the user's answer wins unless a hard dependency is
    violated), GATE — do not write anything durable until the user has explicitly approved. Only
    after the explicit yes, write `Status: approved` and any RULINGS.md/ASSETS.md entries; the
    `Status:` line is written LAST (cite state-machine.md "Write Order"). State the carve-out
    explicitly: investigate's claims list / visibility declaration were written progressively and
    are NOT re-gated or re-copied here (cite the ingest-rules.md Step 7 carve-out precedent).

    Footer: cite `build/redteam.md` (upstream settled interpretation) and note the next session runs
    step group 2 `{build, test}` (Phase 144).

    Contain no fenced implementation code.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-04"` passes GREEN
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts` (unfiltered — this is the last wave-2 plan, so the full suite including cross-file consistency and RETURN_SHAPE pins must be 100% green with 0 failures)
    - `grep -F "what you will NOT see yet" src/cli/slash-command/bs/build/ask.md` matches
    - all four parts (a)/(b)/(c)/(d) are present as labeled sections
    - the file names `action`, `flow`, `state`, and `element` as FORBIDDEN vocabulary (in a prohibition context, not used to describe the design)
    - `grep -iF "never block" src/cli/slash-command/bs/build/ask.md` or equivalent never-blocking placeholder language matches; `grep -F "ASSETS.md" …` matches
    - gate-before-write present: `grep -F "Write Order" …` (cites state-machine.md) and language that Status: approved is written only after explicit approval / written last
    - `grep -F "RULINGS.md" src/cli/slash-command/bs/build/ask.md` matches (house-rule choices recorded)
  </acceptance_criteria>
  <done>ask.md exists; BUILD-04 test block GREEN; 4-part format + zero-impl-vocab ban + never-blocking assets + gate-before-write + RULINGS/ASSETS fills all present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | LLM-instruction markdown only; no auth/network/session/input-parsing surface (143-RESEARCH.md Security Domain). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Tampering | `Status: approved` written speculatively before the user actually approves (a resumed session finding an authorization that never happened) | mitigate | Gate-before-write: present → gate → write; Status line written LAST, only after explicit yes; cite state-machine.md Write Order |
| T-143-06 | Repudiation | A house-rule / adaptation chosen at ask left unrecorded | mitigate | Ask-gate choices are recorded as durable `### Ruling N` entries in RULINGS.md; asset debt recorded in ASSETS.md |
| T-143-SC | Tampering | npm/pip/cargo installs | N/A | Zero package installs this phase |
</threat_model>

<verification>
`npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-04"` green. After this plan,
`npm test` runs the full suite fully green (all Wave 2 markdown authored).
</verification>

<success_criteria>
Ask-gate reference file authored; BUILD-04 test block green; 4-part format, vocabulary ban,
never-blocking assets, and gate-before-write all satisfied.
</success_criteria>

<output>
Create `.planning/phases/143-bs-build-chunk-interpretation-ask-gate/143-05-SUMMARY.md` when done.
</output>
