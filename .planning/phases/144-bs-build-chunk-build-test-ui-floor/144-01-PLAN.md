---
phase: 144-bs-build-chunk-build-test-ui-floor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/slash-command/bs/build-chunk.test.ts
autonomous: true
requirements: [BUILD-05, BUILD-06, UIQ-01, UIQ-02, UIQ-03]

must_haves:
  truths:
    - "Running the drift suite collects five new describe blocks (BUILD-05, BUILD-06, UIQ-01, UIQ-02, UIQ-03) without a collection-phase crash"
    - "REFERENCED_PATHS names build/build.md, build/test.md, build/design-ask.md"
    - "FORWARD_REFERENCE_MARKERS no longer carries 'authored in Phase 144' (only 145/146 remain)"
    - "The exclusion-list test no longer treats build/build.md or build/test.md as excluded"
  artifacts:
    - path: "src/cli/slash-command/bs/build-chunk.test.ts"
      provides: "Drift-pin describe blocks for all five Phase 144 requirements + updated path/marker constants"
      contains: "describe('BUILD-05"
  key_links:
    - from: "src/cli/slash-command/bs/build-chunk.test.ts"
      to: "build/build.md, build/test.md, build/design-ask.md"
      via: "REFERENCED_PATHS exists-on-disk + cite loops"
      pattern: "build/(build|test|design-ask)\\.md"
---

<objective>
Author the Wave-0 drift-test scaffold for Phase 144 by extending the existing
`build-chunk.test.ts`. This is a structural drift-pin suite (agent-consumed markdown is not
parsed by runtime code), so the assertions land FIRST and are RED until Plans 03/04 author the
markdown files — exactly the intended Wave-0-first state the file's own header already documents
for Phases 143's blocks.

Purpose: gives every downstream markdown plan a real `npx vitest run ... -t "<REQ>"` verify
command that exists before the prose is written (Nyquist).
Output: extended `build-chunk.test.ts` with five new describe blocks and updated constant arrays.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-bs-build-chunk-build-test-ui-floor/144-CONTEXT.md
@.planning/phases/144-bs-build-chunk-build-test-ui-floor/144-RESEARCH.md
@.planning/phases/144-bs-build-chunk-build-test-ui-floor/144-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the path + marker constant arrays and the exclusion-list test</name>
  <files>src/cli/slash-command/bs/build-chunk.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/build-chunk.test.ts (lines 89-110: REFERENCED_PATHS + FORWARD_REFERENCE_MARKERS; lines 378-391: the exclusion-list `it`)
    - 144-PATTERNS.md "build-chunk.test.ts (extend)" section (lines 325-381) — verbatim before/after for both arrays and the excluded[] array
  </read_first>
  <action>
    Edit `REFERENCED_PATHS` to append `'build/build.md'`, `'build/test.md'`,
    `'build/design-ask.md'` after the existing `'build/ask.md'` entry. Edit
    `FORWARD_REFERENCE_MARKERS` to drop `'authored in Phase 144'` (leave only the 145 and 146
    markers). In the `it('REFERENCED_PATHS does NOT include any Phase 144-146 step file')` test,
    remove `'build/build.md'` and `'build/test.md'` from the `excluded` array (they now exist AND
    are referenced) while keeping `build/{audit,repair,playtest,revise,close}.md` excluded. Do NOT
    touch the pre-existing BUILD-01..04/12 blocks or the return-shape field constants.
  </action>
  <acceptance_criteria>
    - `grep -c "build/build.md" src/cli/slash-command/bs/build-chunk.test.ts` returns 2 (one in REFERENCED_PATHS, one still excluded? no — after edit: 1 in REFERENCED_PATHS only). Confirm build/build.md appears in REFERENCED_PATHS and NOT in the excluded[] array.
    - `FORWARD_REFERENCE_MARKERS` contains exactly `'authored in Phase 145'` and `'authored in Phase 146'`; grep for `authored in Phase 144` in the test file returns 0.
    - excluded[] still contains build/audit.md, build/repair.md, build/playtest.md, build/revise.md, build/close.md.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "build-chunk.test.ts" | grep -qx 0 && echo TSCLEAN</automated>
  </verify>
  <done>Both constant arrays and the exclusion-list test updated; test file still type-checks.</done>
</task>

<task type="auto">
  <name>Task 2: Add BUILD-05, BUILD-06, UIQ-01, UIQ-02, UIQ-03 describe blocks</name>
  <files>src/cli/slash-command/bs/build-chunk.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/build-chunk.test.ts (lines 143-178: BUILD-02 block — the describe-per-requirement shape to replicate; every read() INSIDE its own it() body)
    - 144-CONTEXT.md "Step Semantics" (build contract, test command list, a11y floor five items) and "A11y floor (UIQ-03, all five items)"
    - 144-PATTERNS.md "describe-per-requirement pattern to replicate" (lines 373-381)
  </read_first>
  <action>
    Append five new `describe` blocks after the existing BUILD-12 block, each with `read()` called
    INSIDE every `it()` body (never describe-level, per the file header). Pin verbatim strings the
    Plan 03/04 markdown MUST contain:
    - `describe('BUILD-05 — build step')`: reads `build/build.md`; assert it names reading raw
      rulebook slices AND the approved interpretation, the "extends rather than restructures"
      contract with a user gate for restructuring, an append to `DECISIONS.md`, and cites the
      existing `## Build Manifest` CHUNK section (assert the exact string `## Build Manifest`).
    - `describe('BUILD-06 — test step')`: reads `build/test.md`; assert the exact command tokens
      `tsc --noEmit`, `boardsmith lint`, the full-suite/regression phrasing, and
      `simulateRandomGames`; assert the seven sandbox rule names (`no-network`, `no-filesystem`,
      `no-timers`, `no-nondeterministic`, `no-eval`, `no-element-identity-comparison`,
      `no-element-array-state`).
    - `describe('UIQ-01 — design ask')`: reads `build/design-ask.md`; assert the three direction
      names `Adopt`, `Derive`, `Original`, that `Derive` is the default recommendation, and that
      it writes `DESIGN.md`. Also read `build/ask.md` and assert the first-UI-chunk pre-check hook
      text (the `## ui:` touches|major AND DESIGN.md-does-not-exist detection) is present.
    - `describe('UIQ-02 — designed placeholders')`: reads `build/build.md`; assert it cites
      `DESIGN.md`'s `## Placeholder Policy` by name (citation, not restatement — assert the string
      `## Placeholder Policy`) and states asset swap never changes geometry/layout.
    - `describe('UIQ-03 — a11y floor')`: reads `build/test.md`; assert all five floor items by
      pinned phrase: keyboard-only ActionPanel completion, `axe-core` scan, no-color-literal grep
      (contrast), real controls with game-semantic aria-labels + `aria-hidden` decorative glyphs,
      focus management + `prefers-reduced-motion`.
    Use `.toContain`/`.toMatch` with specific backticked or multi-word phrases (avoid vacuous
    single-word matches like bare `'action'`), mirroring the BUILD-04 block's discipline.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts` COLLECTS all five new blocks (they appear in output) and does not crash during collection.
    - Each of the five blocks is RED (the markdown files do not exist yet) — this is the intended Wave-0-first state, not a failure of this plan.
    - `grep -c "describe('BUILD-05\|describe('BUILD-06\|describe('UIQ-01\|describe('UIQ-02\|describe('UIQ-03" src/cli/slash-command/bs/build-chunk.test.ts` confirms all five present.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/cli/slash-command/bs/build-chunk.test.ts 2>&1 | grep -E "BUILD-05|BUILD-06|UIQ-01|UIQ-02|UIQ-03" | grep -qi "build-05\|build-06\|uiq-01" && echo BLOCKS_COLLECTED</automated>
  </verify>
  <done>Five new describe blocks collected and RED (files not yet authored); the pre-existing BUILD-01..04/12 blocks remain green; suite collection does not crash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Test-file authoring only; no runtime input path, no untrusted data crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-01-NA | — | build-chunk.test.ts (vitest drift-pin) | accept | N/A — structural markdown-drift test, no auth/session/input/crypto surface (144-RESEARCH.md Security Domain: all ASVS categories N/A). |
| T-144-SC | Tampering | npm installs | mitigate | No installs in this plan. The axe-core supply-chain surface is confined to Plan 02; this plan adds no dependency. |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts` collects all blocks; BUILD-01..04/12 green, BUILD-05/06 + UIQ-01/02/03 RED (expected).
- `git status --short package.json package-lock.json` shows no diff (no installs performed).
</verification>

<success_criteria>
- Five drift-pin blocks present and RED; constant arrays updated; exclusion-list test updated.
- Test file type-checks; no BoardSmith manifest change.
</success_criteria>

<output>
Create `.planning/phases/144-bs-build-chunk-build-test-ui-floor/144-01-SUMMARY.md` when done.
</output>
