---
phase: 144-bs-build-chunk-build-test-ui-floor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/lib/project-scaffold.ts
  - src/cli/lib/project-scaffold.test.ts
autonomous: true
requirements: [UIQ-03]

must_haves:
  truths:
    - "A game generated by `boardsmith init` ships axe-core as a devDependency"
    - "A game generated by `boardsmith init` ships @vue/test-utils as a devDependency"
    - "The scaffold output includes tests/a11y.example.test.ts demonstrating the axe-core scan"
    - "BoardSmith's own package.json / package-lock.json carry NO axe-core line"
  artifacts:
    - path: "src/cli/lib/project-scaffold.ts"
      provides: "axe-core + @vue/test-utils in generatePackageJson devDeps; generateA11yExampleTestTs() generator; a11y example test scaffold entry"
      contains: "axe-core"
    - path: "src/cli/lib/project-scaffold.test.ts"
      provides: "Unit assertions for the axe-core devDep + a11y example harness file"
      contains: "axe-core"
  key_links:
    - from: "generateScaffoldFiles"
      to: "generateA11yExampleTestTs"
      via: "GeneratedFile entry { path: 'tests/a11y.example.test.ts' }"
      pattern: "a11y\\.example\\.test\\.ts"
---

<objective>
Make the one real library change of Phase 144: the `boardsmith init` scaffold template gains an
`axe-core` devDependency, a `@vue/test-utils` devDependency (needed to mount a component in the
example test), and one new generated file `tests/a11y.example.test.ts` demonstrating the
axe-core scan pattern every UI chunk's a11y floor (UIQ-03) copies. Decided once at the
scaffold-template level, inherited by every generated game — never a per-game decision.

Purpose: gives generated games a runnable, copyable a11y-floor precedent.
Output: edited `project-scaffold.ts` + extended `project-scaffold.test.ts`.

CRITICAL: `axe-core` is added ONLY inside `generatePackageJson()`'s returned template-string
literal. NEVER run `npm install axe-core` or `slopcheck install axe-core` against BoardSmith's own
repo root — both perform a real install into THIS repo (144-RESEARCH.md Pitfall 3). Verify
`git status --short` shows no axe-core diff on BoardSmith's own manifests as a hard gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-bs-build-chunk-build-test-ui-floor/144-RESEARCH.md
@.planning/phases/144-bs-build-chunk-build-test-ui-floor/144-PATTERNS.md

<interfaces>
From src/cli/lib/project-scaffold.ts (already read — use directly, no exploration):
- `export interface GeneratedFile { path: string; content: string }` (line 29)
- `export function generatePackageJson(config: ProjectConfig): string` (line 121) — returns
  `JSON.stringify(pkg, null, 2)`; current devDependencies: `@vitejs/plugin-vue` ^5.0.0,
  `typescript` ^5.7.0, `vitest` ^2.0.0.
- `export function generateGameTableVue(): string` (line 357) — model for the new
  string-template generator (doc-commented top, returns file content).
- `export function generateScaffoldFiles(config: ProjectConfig): GeneratedFile[]` (line 468) —
  array of `{ path, content }`; existing entries include `src/ui/components/GameTable.vue`
  (line 479) and `.gitignore` (line 480).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — assert axe-core devDep + a11y example harness in scaffold output</name>
  <files>src/cli/lib/project-scaffold.test.ts</files>
  <read_first>
    - src/cli/lib/project-scaffold.test.ts (the existing `generateBoardsmithJson`/`generatePackageJson` describe blocks + the `import { ... } from './project-scaffold.js'` block, ~lines 7-14)
    - 144-PATTERNS.md "project-scaffold.test.ts (extend)" section (lines 479-509) — verbatim assertion shape
  </read_first>
  <behavior>
    - Test: `JSON.parse(generatePackageJson(config)).devDependencies` has property `axe-core`.
    - Test: same devDependencies has property `@vue/test-utils`.
    - Test: `generateScaffoldFiles(config)` includes an entry whose `path === 'tests/a11y.example.test.ts'`.
    - Test: that entry's `content` imports `axe-core` and calls `axe.run(` (structural — proves the harness is the axe pattern, not an empty stub).
  </behavior>
  <action>
    Extend the existing test file with a `describe('generatePackageJson — axe-core scaffold
    devDependency')` block and a `describe('generateScaffoldFiles — a11y example test harness')`
    block, using the same `JSON.parse(generateX(config))` / `.toHaveProperty` / `.some(f => ...)`
    idiom already in the file. Add `generatePackageJson`, `generateScaffoldFiles`,
    `generateA11yExampleTestTs` to the existing import block rather than adding a new import line.
    Reuse the file's existing `config` fixture. These assertions are RED until Task 2 lands.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/cli/lib/project-scaffold.test.ts -t "axe"` shows the new assertions RED (generateA11yExampleTestTs not yet exported → import/type error is acceptable RED for this task's own commit if committed alongside a stub; prefer committing Task 1 + Task 2 together if the missing export breaks collection).
    - The block names contain the token `axe` so `-t "axe"` selects them.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && grep -q "a11y.example.test.ts" src/cli/lib/project-scaffold.test.ts && grep -q "axe-core" src/cli/lib/project-scaffold.test.ts && echo REDWRITTEN</automated>
  </verify>
  <done>New assertions written against axe-core devDep + a11y example harness file; block names include "axe".</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — add axe-core devDep, @vue/test-utils, and the a11y example generator</name>
  <files>src/cli/lib/project-scaffold.ts</files>
  <read_first>
    - src/cli/lib/project-scaffold.ts lines 121-147 (generatePackageJson devDeps), 357-452 (generateGameTableVue — the generator-function shape), 468-482 (generateScaffoldFiles array)
    - 144-PATTERNS.md "project-scaffold.ts (edit — REAL CODE)" section (lines 384-475) — the verbatim devDeps block, the GeneratedFile entry, and the generateA11yExampleTestTs() body
    - 144-RESEARCH.md Code Examples (the axe.run against mounted GameTable.vue shape) + Open Question 2 (target GameTable.vue, not App.vue/GameShell)
  </read_first>
  <action>
    In `generatePackageJson`, add `'axe-core': '^4.12.1'` and `'@vue/test-utils': '^2.4.11'` to the
    devDependencies object (per 144-RESEARCH.md verified version 4.12.1, dequelabs, MPL-2.0). Add a
    new `export function generateA11yExampleTestTs(): string` modeled on `generateGameTableVue` — a
    doc-commented string-template returning `tests/a11y.example.test.ts` that mounts the scaffold's
    own `GameTable.vue` with `@vue/test-utils`, runs `axe.run(wrapper.element)`, and asserts
    `results.violations` is empty; use the `// @vitest-environment jsdom` pragma. Add the
    `{ path: 'tests/a11y.example.test.ts', content: generateA11yExampleTestTs() }` entry to
    `generateScaffoldFiles` (before `.gitignore`). Do NOT modify any other generator. Do NOT run any
    package install — axe-core lives only in this returned template-string literal.
  </action>
  <acceptance_criteria>
    - `JSON.parse(generatePackageJson(config)).devDependencies['axe-core'] === '^4.12.1'`.
    - `generateScaffoldFiles(config)` contains a `tests/a11y.example.test.ts` entry whose content contains `axe.run(` and `@vitest-environment jsdom`.
    - `git status --short package.json package-lock.json` prints NOTHING (BoardSmith's own manifests untouched — hard gate per CLAUDE.md + 144-RESEARCH.md Pitfall 3).
    - `grep -c "axe-core" package.json` at BoardSmith repo root returns 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/cli/lib/project-scaffold.test.ts -t "axe" 2>&1 | grep -q "passed" && test -z "$(git status --short package.json package-lock.json)" && ! grep -q "axe-core" package.json && echo GREEN_AND_CLEAN</automated>
  </verify>
  <done>axe-core + @vue/test-utils in the generated devDeps; a11y example harness generated; scaffold unit tests green; BoardSmith's own manifests carry no axe-core.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BoardSmith repo → generated game project | The scaffold template string is copied verbatim into a new game's package.json; a tampered/typosquatted devDependency name would propagate to every generated game. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-SC | Tampering | axe-core devDependency added to `generatePackageJson()` template string | mitigate | Version pinned to `^4.12.1`; identity verified in 144-RESEARCH.md Package Legitimacy Audit (npm, dequelabs/axe-core, MPL-2.0, slopcheck `[OK]`). devDependency-only (test-time), never bundled into a shipped game's production build. No install performed against BoardSmith's own repo. |
| T-144-SC-SELF | Tampering | BoardSmith's own package.json/package-lock.json | mitigate | Hard gate: `git status --short` on BoardSmith manifests must show no axe-core diff after any tooling run (144-RESEARCH.md Pitfall 3 — `slopcheck install` performs a real install). axe-core is added ONLY inside the returned template-string literal. |
| T-144-02-NA | S/R/I/D/E | scaffold generator functions | accept | N/A — pure string-template generation, no auth/session/access-control/input-validation/crypto surface (144-RESEARCH.md Security Domain). |

Package legitimacy: axe-core is `[OK]` per 144-RESEARCH.md Package Legitimacy Audit (already run this research cycle; identity unchanged) — no blocking-human checkpoint required.
</threat_model>

<verification>
- `npx vitest run src/cli/lib/project-scaffold.test.ts` fully green.
- `git status --short package.json package-lock.json` empty; `grep -c axe-core package.json` (repo root) is 0.
</verification>

<success_criteria>
- Generated games ship axe-core + @vue/test-utils + a runnable a11y example harness.
- BoardSmith's own dependency manifests are unchanged.
</success_criteria>

<output>
Create `.planning/phases/144-bs-build-chunk-build-test-ui-floor/144-02-SUMMARY.md` when done.
</output>
