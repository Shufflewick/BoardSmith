---
phase: 145-bs-build-chunk-audit-repair-design-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/slash-command/bs/build/audit.md
  - src/cli/slash-command/bs/build/repair.md
  - src/cli/slash-command/bs/build-chunk.md
  - src/cli/slash-command/bs/build-chunk.test.ts
autonomous: true
requirements: [BUILD-07, BUILD-08]

must_haves:
  truths:
    - "build-chunk.md's Step 3 dispatch table names build/audit.md and build/repair.md as live dispatches (no 'authored in Phase 145' marker on those rows)"
    - "audit.md names 3 audit lenses (rulebook fidelity, visibility-leak two-seat diff, undo sanity) and forbids reading the interpretation"
    - "audit.md cites diffPlayerViews and assertNoHiddenInfoLeak by exact name for the visibility-leak lens"
    - "repair.md enforces the max-3-round / only-new-findings / refute-with-citation rules by citing state-machine.md 'Repair Loop Bound', never restating it"
    - "Round results persist to CHUNK.md's ## Findings Ledger before the next audit round starts"
    - "The BUILD-07 and BUILD-08 describe blocks pass against the authored files"
  artifacts:
    - path: "src/cli/slash-command/bs/build/audit.md"
      provides: "Fresh-context adversarial audit-step dispatch prose: 3 lenses, never-reads-interpretation, Findings Ledger write, round persistence"
      contains: "diffPlayerViews"
      min_lines: 60
    - path: "src/cli/slash-command/bs/build/repair.md"
      provides: "Fix-or-refute repair loop, round-bound enforcement, round-3 plain-language user triage"
      contains: "Repair Loop Bound"
      min_lines: 50
    - path: "src/cli/slash-command/bs/build-chunk.md"
      provides: "Router with audit/repair rows graduated to live dispatch and registered in Reference Files"
      contains: "build/audit.md"
    - path: "src/cli/slash-command/bs/build-chunk.test.ts"
      provides: "BUILD-07 and BUILD-08 content-drift pins + updated REFERENCED_PATHS/exclusion/marker arrays"
      contains: "BUILD-07"
  key_links:
    - from: "src/cli/slash-command/bs/build/audit.md"
      to: "src/cli/slash-command/bs/templates/CHUNK.template.md"
      via: "cites ## Findings Ledger by name (never restructures it)"
      pattern: "Findings Ledger"
    - from: "src/cli/slash-command/bs/build/repair.md"
      to: "src/cli/slash-command/bs/state-machine.md"
      via: "cites Repair Loop Bound"
      pattern: "Repair Loop Bound"
    - from: "src/cli/slash-command/bs/build-chunk.md"
      to: "src/cli/slash-command/bs/build/audit.md"
      via: "Step 3 dispatch table row (live, no phase marker)"
      pattern: "build/audit.md"
---

<objective>
Author the `{audit, repair}` session step group of `/bs-build-chunk`: `build/audit.md`
(BUILD-07 — fresh adversarial agents across 3 lenses writing a stable-ID Findings Ledger) and
`build/repair.md` (BUILD-08 — bounded fix-or-refute loop with round-3 user triage). Graduate the
`audit`/`repair` rows in `build-chunk.md` from forward references to live dispatches, and pin the
new content with BUILD-07/BUILD-08 drift tests.

Purpose: This is the first point in the build-chunk pipeline where a lifecycle agent can fail a
chunk for a rulebook-fidelity defect or a hidden-information leak that the automated test suite
(build/test.md) cannot catch — leaks are not type errors.

Output: `build/audit.md`, `build/repair.md`, edited `build-chunk.md`, edited `build-chunk.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-bs-build-chunk-audit-repair-design-review/145-CONTEXT.md
@.planning/phases/145-bs-build-chunk-audit-repair-design-review/145-RESEARCH.md
@.planning/phases/145-bs-build-chunk-audit-repair-design-review/145-PATTERNS.md

<authoring_idiom>
These are markdown reference files in the `bs-` skill idiom: lean orchestrator (build-chunk.md)
delegates heavyweight step prose to fat reference files under `build/`. Reference files CITE the
governing rules in `state-machine.md` and `templates/CHUNK.template.md` by section name — they
never restate them (citation-not-restatement). The closest analog for both new files is
`build/redteam.md` (fresh-context adversarial dispatch, per-round persistence, vote-privacy);
the closest analog for repair's fix-or-refute branch is `build/build.md`'s "Extends, Never
Restructures" (name the non-default path explicitly, record it durably).
</authoring_idiom>

<interfaces>
<!-- Real APIs the visibility-leak lens must cite by exact name (already exported from boardsmith/testing). -->
From src/testing/view-diff.ts (VIS-02):
  diffPlayerViews(testGame, seatA, seatB) — atomic overload; returns { onlyInA, onlyInB, attributeDiffs, describe() }
From src/testing/dom-leak.ts (VIS-03):
  assertNoHiddenInfoLeak(...) — DOM-rendered leak assertion (catches UI-smuggled hidden values)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author build/audit.md (BUILD-07)</name>
  <files>src/cli/slash-command/bs/build/audit.md</files>
  <read_first>
    - src/cli/slash-command/bs/build/redteam.md — header/framing (lines 1-9), "Independence: Fresh-Context, No-Framing Dispatch" (23-39), "Three Dispatch Templates" field-name shape (41-72), "Persisting the Round" (108-134). This is the exact adversarial-dispatch + round-persistence analog.
    - src/cli/slash-command/bs/state-machine.md — "Session Handoff Seams" (the {audit, repair} group is group 3), and the "Rulings Outrank Rulebook" rule audit cites (never restates).
    - src/cli/slash-command/bs/templates/CHUNK.template.md — "## Findings Ledger" section (stable-ID, round-aware, only-new-findings, max-3) and the sibling "## Redteam Rounds" precedent.
    - 145-PATTERNS.md — "audit.md (reference file, fan-out/dispatch)" section for the shape-by-shape mapping.
    - 145-RESEARCH.md — Pattern 1/2/4, "Don't Hand-Roll" (cite diffPlayerViews/assertNoHiddenInfoLeak by exact name, not prose).
  </read_first>
  <action>
    Author `build/audit.md` in the redteam.md idiom. Header must name the requirement ID
    (BUILD-07), name the step group `{audit, repair}` and cite `state-machine.md` "Session Handoff
    Seams", and state in one sentence what audit catches that the prior `test` step cannot
    (rules-fidelity / hidden-info-leak defects that are not type errors).

    Include a callout naming audit's specific temptation — silently reading the settled
    `## Interpretation` instead of the raw rulebook slice — and state audit's own no-framing rule:
    audit agents read raw slices + RULINGS.md + the code, NEVER the interpretation (interpretation
    errors from investigate/redteam must stay visible). Cite `state-machine.md` "Rulings Outrank
    Rulebook" by name; do not restate it — apply it (auditors read RULINGS.md alongside the raw
    slice so they do not "fix" a house rule back to the printed rule).

    Define exactly 3 lenses, each dispatched as a SEPARATE Task-tool subagent (fresh context, no
    inherited conversation, no build/investigate framing, no peer-lens findings): (1) rulebook
    fidelity, (2) visibility-leak — a two-seat diff, (3) undo sanity. For UI chunks (ui:
    touches|major) a 4th agent is dispatched via `build/design-review.md` (forward-reference it
    here; it is authored in this phase's Plan 02). For the visibility-leak lens, cite
    `diffPlayerViews(testGame, seatA, seatB)` (atomic overload — note it avoids the WR-02
    different-instants footgun) and `assertNoHiddenInfoLeak` by exact name, per the interfaces
    block; describe the check by citing the real functions, not in prose alone.

    Provide a fenced dispatch-template shape per lens with an explicit "Return exactly: { ... }"
    line. Use flat, grep-able field names following the redteam.md precedent — Claude's discretion
    on exact names (e.g. `findingId`, `lens`, `description`, `citation`, `severity`); do NOT invent
    a new ledger structure. State that the orchestrator appends `### Audit Round N` to CHUNK.md's
    `## Findings Ledger` (cite CHUNK.template.md by name) BEFORE repair starts (cite
    `state-machine.md` "Write Order"), with a cold-resume rule mirroring redteam.md's "Persisting
    the Round." Do NOT place fenced code blocks containing implementations inside prose beyond the
    dispatch-template and the one-line API citation.
  </action>
  <acceptance_criteria>
    - File names all three lenses using the strings the test pins: "fidelity", "visibility" (two-seat / leak diff), and "undo".
    - Contains the exact strings `diffPlayerViews` and `assertNoHiddenInfoLeak`.
    - Contains an explicit never-reads-interpretation rule (e.g. the literal phrase "never" near "interpretation") and cites "Rulings Outrank Rulebook".
    - Cites "## Findings Ledger" and "Session Handoff Seams" by name.
    - `grep -c 'diffPlayerViews' src/cli/slash-command/bs/build/audit.md` returns ≥ 1.
  </acceptance_criteria>
  <verify>
    <automated>test -f src/cli/slash-command/bs/build/audit.md &amp;&amp; grep -q 'diffPlayerViews' src/cli/slash-command/bs/build/audit.md &amp;&amp; grep -q 'assertNoHiddenInfoLeak' src/cli/slash-command/bs/build/audit.md</automated>
  </verify>
  <done>audit.md exists, names 3 lenses, cites both leak APIs by exact name, forbids reading the interpretation, and cites the Findings Ledger + governing state-machine.md sections.</done>
</task>

<task type="auto">
  <name>Task 2: Author build/repair.md (BUILD-08)</name>
  <files>src/cli/slash-command/bs/build/repair.md</files>
  <read_first>
    - src/cli/slash-command/bs/state-machine.md — "Repair Loop Bound" (max 3 rounds, only-new-findings, round-3 triage) — the rule repair.md cites and applies, never restates.
    - src/cli/slash-command/bs/build/redteam.md — "Escalation Logic (cite, never restate)" (84-96) and "Vote-Privacy" (136-149, including the concrete plain-language example question). Round-3 triage reuses the vote-privacy plain-language discipline.
    - src/cli/slash-command/bs/build/build.md — "Extends, Never Restructures" — the shape for the fix-or-refute-with-citation branch (name the non-default path, record it durably).
    - src/cli/slash-command/bs/build/test.md — "Failures Loop Back to build" (139-144) — the same-session-group loop-back shape to copy for repair→audit.
    - src/cli/slash-command/bs/templates/CHUNK.template.md — "## Findings Ledger" disposition column (fixed | deferred | refuted).
    - 145-PATTERNS.md — "repair.md" section.
  </read_first>
  <action>
    Author `build/repair.md` in the same idiom. Header names BUILD-08, names the `{audit, repair}`
    group, and cites `state-machine.md` "Session Handoff Seams". State the two allowed outcomes per
    finding: FIX the code, OR refute-with-citation (record the rulings/rulebook citation directly
    in the Findings Ledger entry's disposition — never silently mark a finding "handled," and never
    change code to refute). Model this branch's shape on build.md's "Extends, Never Restructures."

    Cite `state-machine.md` "Repair Loop Bound" by name for the max-3-round cap, the
    only-new-findings rule (round N+1 auditors see the ledger and report ONLY NEW findings), and
    the round-3 triage — do NOT re-derive the bound in repair.md's own words. Describe the
    repair→audit loop-back: repair and audit are in the SAME session group, so a repair round that
    produces fixes loops back to audit WITHOUT a session handoff (copy test.md's group-boundary
    citation shape). Write each round's dispositions into the Findings Ledger BEFORE the next audit
    round is dispatched (cold-resume: a crashed repair round resumes from the ledger).

    For after round 3, describe the user triage as plain-language options in designer register —
    real blocker / defer to a later chunk / auditor was wrong (refuted) — never a raw finding text
    or agent verdict; cite redteam.md's "Vote-Privacy" discipline by name.
  </action>
  <acceptance_criteria>
    - Cites the exact string "Repair Loop Bound".
    - Contains the max-3-round bound wording, the "only new findings" / only-new-findings rule, and the refute-with-citation concept (all three phrases the BUILD-08 pin asserts).
    - Names the three plain-language triage options for round 3.
    - Does NOT restate the numeric bound as an independent authority (it cites state-machine.md).
  </acceptance_criteria>
  <verify>
    <automated>test -f src/cli/slash-command/bs/build/repair.md &amp;&amp; grep -q 'Repair Loop Bound' src/cli/slash-command/bs/build/repair.md</automated>
  </verify>
  <done>repair.md exists, cites Repair Loop Bound, documents fix-or-refute-with-citation, the same-group loop-back to audit, and round-3 plain-language triage.</done>
</task>

<task type="auto">
  <name>Task 3: Graduate audit/repair rows in the router and pin BUILD-07/BUILD-08 tests</name>
  <files>src/cli/slash-command/bs/build-chunk.md, src/cli/slash-command/bs/build-chunk.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/build-chunk.md — dispatch table rows for `audit`/`repair` ("— authored in Phase 145"), the "Steps 6–10 are named here as forward references only" prose, the "`build/build.md` and `build/test.md` are now live dispatches" sentence (the exact Phase-144 graduation precedent), and the Reference Files list (both the live list and the "forward-referenced only" list).
    - src/cli/slash-command/bs/build-chunk.test.ts — REFERENCED_PATHS array, FORWARD_REFERENCE_MARKERS array, the `describe('BUILD-06 — test step', ...)` block (the template for the new describes, with `read()` called INSIDE each `it()`), the "REFERENCED_PATHS does NOT include any Phase 145-146 step file" exclusion test, and the "carries the three 'authored in Phase 14X' forward-reference markers" test.
    - 145-PATTERNS.md — "build-chunk.md" and "build-chunk.test.ts" sections for the exact edit shapes.
  </read_first>
  <action>
    In `build-chunk.md`: change the `audit` and `repair` dispatch-table rows from
    `build/audit.md — authored in Phase 145` / `build/repair.md — authored in Phase 145` to plain
    live rows (`build/audit.md` / `build/repair.md`), mirroring how the `build`/`test` rows read
    now. Edit the surrounding forward-reference prose to drop audit/repair from the
    forward-reference set (retain playtest/revise/close as still forward-referenced), following the
    exact edit shape Phase 144 used when build/test graduated. In the Reference Files list, move
    `build/audit.md` and `build/repair.md` from the "forward-referenced only (not yet authored)"
    list into the live list with one-line descriptions (keep `build/playtest.md`,
    `build/revise.md`, `build/close.md` in the forward-referenced list). Do NOT touch
    design-review.md here — it is registered in Plan 02.

    In `build-chunk.test.ts`: add `build/audit.md` and `build/repair.md` to the `REFERENCED_PATHS`
    array (existing literal shape). Remove `'build/audit.md'` and `'build/repair.md'` from the
    `excluded` array in the "REFERENCED_PATHS does NOT include any Phase 145-146 step file" test,
    keeping only `build/playtest.md`/`build/revise.md`/`build/close.md`; update that `it()` title
    from "Phase 145-146" to "Phase 146". Narrow `FORWARD_REFERENCE_MARKERS` to only
    `'authored in Phase 146'` (drop `'authored in Phase 145'`, since the audit/repair rows no
    longer carry it), and update the "carries the three markers" test title/count accordingly. Add
    `describe('BUILD-07 — audit', ...)` and `describe('BUILD-08 — repair', ...)` blocks modeled on
    the BUILD-06 block (call `read()` inside each `it()`): BUILD-07 asserts the 3 lens names, the
    never-reads-interpretation rule, and `diffPlayerViews`/`assertNoHiddenInfoLeak` by exact name;
    BUILD-08 asserts the max-3-round + only-new-findings phrasing, the refute-with-citation phrase,
    and the citation of "Repair Loop Bound".
  </action>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-07"` passes.
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-08"` passes.
    - The full `build-chunk.test.ts` file is green (existing BUILD-01..06/UIQ-03 + exclusion + marker tests still pass after the array edits).
    - `grep -c 'authored in Phase 145' src/cli/slash-command/bs/build-chunk.md` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts</automated>
  </verify>
  <done>build-chunk.md's audit/repair rows are live dispatches with no Phase-145 marker and both files registered in the live Reference Files list; build-chunk.test.ts is fully green with new BUILD-07/BUILD-08 pins and updated path/marker/exclusion arrays.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated game → each seat's client | Hidden game state (opponent hands, face-down cards) must never cross into a seat that should not see it. The audit's visibility-leak lens authored here is the instruction that catches a breach at this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-01 | Information Disclosure | Generated game's per-seat player view (the surface the visibility-leak lens inspects) | mitigate | audit.md instructs the leak lens to cite `diffPlayerViews(testGame, seatA, seatB)` and `assertNoHiddenInfoLeak` by exact name — the purpose-built two-seat + DOM leak detectors — rather than hand-rolling a JSON diff that would miss the id-anonymization asymmetry. This is the phase's core security surface. |
| T-145-02 | Tampering | Auditor reading `## Interpretation` instead of raw slices | mitigate | audit.md's no-framing rule forbids the interpretation in every dispatch prompt, keeping interpretation-level errors visible to the auditor (cites "Rulings Outrank Rulebook"). |
| T-145-SC | Tampering | npm/pip/cargo installs | N/A | Phase 145 installs no packages — it authors markdown reference files and extends one existing Vitest file. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts` fully green (new BUILD-07/BUILD-08 blocks + existing blocks + updated exclusion/marker arrays).
- `grep -c 'authored in Phase 145' src/cli/slash-command/bs/build-chunk.md` == 0.
- audit.md contains `diffPlayerViews` and `assertNoHiddenInfoLeak`; repair.md contains `Repair Loop Bound`.
</verification>

<success_criteria>
- `build/audit.md` and `build/repair.md` exist in the redteam.md/build.md idiom, citing (not restating) state-machine.md and CHUNK.template.md.
- build-chunk.md's audit/repair rows are live dispatches registered in the live Reference Files list.
- BUILD-07 and BUILD-08 requirements are pinned and green.
</success_criteria>

<output>
Create `.planning/phases/145-bs-build-chunk-audit-repair-design-review/145-01-SUMMARY.md` when done
</output>
