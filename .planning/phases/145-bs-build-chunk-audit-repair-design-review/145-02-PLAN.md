---
phase: 145-bs-build-chunk-audit-repair-design-review
plan: 02
type: execute
wave: 2
depends_on: [145-01]
files_modified:
  - src/cli/slash-command/bs/build/design-review.md
  - src/cli/slash-command/bs/build-chunk.md
  - src/cli/slash-command/bs/build-chunk.test.ts
autonomous: true
requirements: [UIQ-04]

must_haves:
  truths:
    - "design-review.md is a single fresh-context adversarial agent dispatched by audit for ui: touches|major chunks"
    - "It captures the 3 Slate breakpoints (640/1024/1440) × 2 themes = 6 screenshots into chunks/<slug>/shots/"
    - "It diffs against the previous chunk's stored shots for cohesion drift and reviews against DESIGN.md tokens"
    - "Its findings land in the same CHUNK.md Findings Ledger the audit lenses write to (same repair loop, no separate track)"
    - "It starts the dev host with --no-open, waits on the ready line / a selector (never networkidle), and KILLS the server before returning"
  artifacts:
    - path: "src/cli/slash-command/bs/build/design-review.md"
      provides: "UI-chunk screenshot design-review agent: server serve-check-kill, breakpoint/theme grid, theme injection, cohesion diff, ledger destination"
      contains: "chunks/<slug>/shots/"
      min_lines: 60
    - path: "src/cli/slash-command/bs/build-chunk.md"
      provides: "design-review.md registered in the live Reference Files list"
      contains: "build/design-review.md"
    - path: "src/cli/slash-command/bs/build-chunk.test.ts"
      provides: "UIQ-04 content-drift pins + design-review.md added to REFERENCED_PATHS"
      contains: "UIQ-04"
  key_links:
    - from: "src/cli/slash-command/bs/build/design-review.md"
      to: "src/cli/slash-command/bs/build/audit.md"
      via: "dispatched by audit for ui chunks; findings flow to the same Findings Ledger"
      pattern: "Findings Ledger"
    - from: "src/cli/slash-command/bs/build/design-review.md"
      to: "src/cli/commands/dev.ts"
      via: "npx boardsmith dev --no-open + explicit kill"
      pattern: "--no-open"
---

<objective>
Author `build/design-review.md` (UIQ-04): the screenshot-armed adversarial design-review agent
that audit dispatches for `ui: touches|major` chunks. It starts the dev host, captures the 3
Slate breakpoints × 2 themes (6 shots) into `chunks/<slug>/shots/`, diffs against the previous
chunk's stored shots for cohesion drift, reviews against `DESIGN.md`, feeds findings into the
same Findings Ledger / repair loop, and kills the dev server before returning. Register it in
`build-chunk.md` and pin it with a UIQ-04 drift test.

Purpose: Without this agent, no lifecycle step can fail a chunk for bad UI — "invoke a
frontend-design pass" would be one-shot generation with no verification. This is the first point
where visual/cohesion defects (not just a11y mechanics) can fail a chunk.

Output: `build/design-review.md`, edited `build-chunk.md`, edited `build-chunk.test.ts`. This is
the final plan of the phase — it must leave the full unfiltered suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-bs-build-chunk-audit-repair-design-review/145-CONTEXT.md
@.planning/phases/145-bs-build-chunk-audit-repair-design-review/145-RESEARCH.md
@.planning/phases/145-bs-build-chunk-audit-repair-design-review/145-PATTERNS.md

<depends_on_note>
Plan 145-01 authored build/audit.md (which forward-references this file) and graduated the
audit/repair rows. design-review.md is dispatched BY audit, not a top-level dispatch-table step —
so it is registered only in build-chunk.md's Reference Files list, and added to REFERENCED_PATHS;
it is NOT added to the dispatch table and NOT part of the forward-reference-marker set.
</depends_on_note>

<browser_tool_reality>
The exact Claude-in-Chrome MCP tool names are NOT verifiable from this repo (no .mcp.json, no prior
skill names one). Write the screenshot mechanism MECHANISM-FIRST ("drive whatever browser-automation
tooling this session provides — navigate, resize, inject script, capture") and name the
Playwright-headless-via-scratchpad-script path as the explicit proven fallback (project memory
`browser-testing-playwright-fallback`: launch chromium, page.goto(':5173'), page.frames() to reach
the GameShell iframe). Never hardcode a single `mcp__` tool string.
</browser_tool_reality>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author build/design-review.md (UIQ-04)</name>
  <files>src/cli/slash-command/bs/build/design-review.md</files>
  <read_first>
    - src/cli/slash-command/bs/ingest/scaffold.md — "Verification Sequence" steps 2-3 (serve-check with `npx boardsmith dev --no-open`, wait for "Ready! Press Ctrl+C to stop.", explicit numbered kill step). This is the exact server-lifecycle precedent — NOT test.md (which never starts a server).
    - src/cli/slash-command/bs/build/redteam.md — "Independence: Fresh-Context, No-Framing Dispatch" (23-39), scaled to a single dispatch.
    - src/cli/slash-command/bs/build/audit.md — the file authored in Plan 01 that dispatches this agent for ui chunks; match its ledger-destination framing.
    - src/cli/slash-command/bs/templates/DESIGN.template.md — the token-discipline / do-don't source of truth the agent reviews against.
    - src/cli/slash-command/bs/templates/CHUNK.template.md — "## Findings Ledger" (findings destination; cite, never restructure).
    - 145-RESEARCH.md — Pitfalls 1-5 (theme has no toggle UI → script document.documentElement.dataset.theme; iframe programmatic resize needs contentWindow.location.reload(); never networkidle; --no-open or the real browser steals seat 1; never hardcode an MCP tool name). BREAKPOINTS constant compact:640/medium:1024/wide:1440 from src/ui/theme.ts.
    - 145-PATTERNS.md — "design-review.md" section.
  </read_first>
  <action>
    Author `build/design-review.md` as a single fresh-context adversarial agent (redteam
    independence discipline scaled to one dispatch), dispatched by audit only for `ui:
    touches|major` chunks. Header names UIQ-04 and states it is dispatched from `build/audit.md`.

    Document the dev-host lifecycle as a numbered serve-check-kill sequence copied from
    scaffold.md's shape: (1) `cd` into the generated game project and start
    `npx boardsmith dev --no-open` (state that `--no-open` is required or the machine's real
    browser steals seat 1); (2) wait for the exact ready line `Ready! Press Ctrl+C to stop.` and/or
    a specific board/seat selector — explicitly instruct waiting on `domcontentloaded`/the `load`
    event/a selector, NEVER `networkidle` (the dev host holds an open WebSocket and never reaches
    idle); (3) an explicit, numbered kill step in the SAME sequence (never a footnote) — killing
    the dev server before returning is a repo hard rule.

    Describe the capture loop: for each of the 3 Slate breakpoints × 2 themes (6 screenshots),
    save to `chunks/<slug>/shots/` with names like `compact-light.png` … `wide-dark.png`. Cite
    `src/ui/theme.ts`'s `BREAKPOINTS` (compact:640, medium:1024, wide:1440) by exact name and
    value. Set the theme by injecting `document.documentElement.dataset.theme` (or the GameShell
    iframe's own `contentDocument` — note no theme-toggle UI button exists), not a click. For the
    compact breakpoint, after setting `iframe.style.width` call
    `iframe.contentWindow.location.reload()` so the board remeasures (ResizeObserver does not fire
    on programmatic iframe resize). Write the screenshot mechanism mechanism-first with the
    Playwright-headless fallback per the browser_tool_reality note; do not hardcode an MCP tool
    name.

    State that the agent diffs the 6 shots against the previous chunk's stored shots in
    `chunks/<slug>/shots/` (cohesion drift) and reviews against DESIGN.md tokens + frontend-design
    craft criteria, and that its findings land in the SAME `## Findings Ledger` (cite
    CHUNK.template.md by name) the audit lenses write to — same repair loop, not a separate track.
    Note the shots directory is committed (evidence trail + next-chunk cohesion-diff source).
  </action>
  <acceptance_criteria>
    - Contains the exact path string `chunks/<slug>/shots/`.
    - Contains the exact flag `--no-open` and an explicit server-kill instruction.
    - Names the 3×2 grid: the three breakpoint values `640`, `1024`, `1440` and two themes producing 6 shots.
    - Contains an explicit "never networkidle" instruction (or equivalent wait-on-selector/domcontentloaded guidance naming networkidle as prohibited).
    - Cites "Findings Ledger" as the findings destination.
    - Names the Playwright fallback (no hardcoded `mcp__` tool string present).
  </acceptance_criteria>
  <verify>
    <automated>test -f src/cli/slash-command/bs/build/design-review.md &amp;&amp; grep -q 'chunks/&lt;slug&gt;/shots/' src/cli/slash-command/bs/build/design-review.md &amp;&amp; grep -q -- '--no-open' src/cli/slash-command/bs/build/design-review.md</automated>
  </verify>
  <done>design-review.md exists with the serve-check-kill sequence, the 3×2 breakpoint/theme grid to chunks/&lt;slug&gt;/shots/, theme-injection + iframe-reload caveats, never-networkidle rule, Playwright fallback, and Findings-Ledger destination.</done>
</task>

<task type="auto">
  <name>Task 2: Register design-review.md and pin UIQ-04; run the full suite green</name>
  <files>src/cli/slash-command/bs/build-chunk.md, src/cli/slash-command/bs/build-chunk.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/build-chunk.md — the Reference Files live list (as edited by Plan 01) where audit.md/repair.md now live; add design-review.md alongside in the same one-line-description style.
    - src/cli/slash-command/bs/build-chunk.test.ts — the REFERENCED_PATHS array (now including audit.md/repair.md after Plan 01), and the `describe('UIQ-03 — a11y floor', ...)` block as the template for the new UIQ-04 block (read() inside each it()).
    - 145-PATTERNS.md — "build-chunk.test.ts" section, UIQ-04 pin list.
  </read_first>
  <action>
    In `build-chunk.md`: add `build/design-review.md` to the live Reference Files list with a
    one-line description (e.g. the UI-chunk screenshot design-review agent dispatched by audit).
    Do NOT add it to the dispatch table (it is not a top-level step) and do NOT add any
    forward-reference marker for it.

    In `build-chunk.test.ts`: add `'build/design-review.md'` to the `REFERENCED_PATHS` array
    (its "exists on disk" + "build-chunk.md cites it" assertions then apply automatically). Add a
    `describe('UIQ-04 — design-review', ...)` block modeled on the UIQ-03 block, calling `read()`
    inside each `it()`, asserting: the `chunks/<slug>/shots/` path string; the server-kill
    instruction; the 3×2 breakpoint/theme grid (the values 640/1024/1440 and 6 shots); the
    `--no-open` flag; and the never-networkidle rule. Confirm design-review.md is NOT in the
    exclusion `excluded` array (it never was) and carries no forward-reference marker.

    Then run the full unfiltered `build-chunk.test.ts` and the whole repo suite to confirm no
    regression across the other bs- skill tests.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "UIQ-04"` passes.
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts` is fully green (unfiltered).
    - `npm test` is green (no other bs- skill or repo test regressed).
    - build-chunk.md cites `build/design-review.md` in its Reference Files list.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts &amp;&amp; npm test</automated>
  </verify>
  <done>design-review.md is registered in build-chunk.md, pinned by a green UIQ-04 drift block, added to REFERENCED_PATHS, and the full unfiltered build-chunk.test.ts plus npm test are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design-review agent → local dev-host process | The agent spawns a long-lived `npx boardsmith dev` server and a browser session on localhost; both must be torn down before the agent returns. |
| host postMessage → GameShell theme tokens | `applyTheme()` restricts host-supplied overrides to the `--bsg-*` key pattern (BSG_KEY_RE) to prevent CSS injection — unchanged by this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-03 | Denial of Service (resource leak) | Orphaned `npx boardsmith dev` process left running after the design-review agent returns | mitigate | design-review.md mandates an explicit numbered kill step in the same serve-check-kill sequence (scaffold.md precedent + CLAUDE.md hard rule: never leave a dev server running). |
| T-145-04 | Tampering / Injection | Direct `document.documentElement.dataset.theme` script injection for screenshots | accept | Same-origin dev-tooling action on a local dev host; not a new attack surface. `applyTheme()`'s BSG_KEY_RE control on host-supplied overrides is unchanged and must not be conflated with this dev-only theme-attribute set. |
| T-145-SC | Tampering | npm/pip/cargo installs | N/A | Phase 145 installs no packages (Playwright is an existing project pattern, not a new install this phase). Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts` fully green (unfiltered), including the new UIQ-04 block and Plan 01's BUILD-07/BUILD-08 blocks.
- `npm test` green.
- design-review.md contains `chunks/<slug>/shots/`, `--no-open`, the breakpoint values 640/1024/1440, an explicit server-kill instruction, and a never-networkidle rule.
</verification>

<success_criteria>
- `build/design-review.md` exists in the scaffold.md/redteam.md idiom with the full 3×2 screenshot grid, theme-injection + iframe-reload caveats, never-networkidle rule, Playwright fallback, server-kill discipline, and Findings-Ledger destination.
- UIQ-04 is pinned and green; design-review.md is registered and on disk per REFERENCED_PATHS.
- Full unfiltered build-chunk.test.ts + npm test green — phase-complete gate.
</success_criteria>

<output>
Create `.planning/phases/145-bs-build-chunk-audit-repair-design-review/145-02-SUMMARY.md` when done
</output>
