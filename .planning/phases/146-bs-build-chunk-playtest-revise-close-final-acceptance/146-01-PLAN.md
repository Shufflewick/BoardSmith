---
phase: 146-bs-build-chunk-playtest-revise-close-final-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/slash-command/bs/build-chunk.test.ts
autonomous: true
requirements: [BUILD-09, BUILD-10, BUILD-11, BUILD-13, UIQ-05]
must_haves:
  truths:
    - "A drift test asserts playtest.md documents the numbered script, item-by-item checklist, and verified (user-waived)"
    - "A drift test asserts revise.md documents the 4-category triage + disposition report + targeted re-test"
    - "A drift test asserts close.md documents verified-hash recording + sketch-tail delta gate"
    - "A drift test asserts build-chunk.md cites (never restates) git protocol / handoff seams / session lock"
    - "A drift test asserts final-acceptance.md documents the 7-point design-QA pass, including the drag-drop keyboard-alternates end-to-end check"
    - "The forward-reference-marker assertion is INVERTED: build-chunk.md must contain ZERO 'authored in Phase 146' strings"
    - "REFERENCED_PATHS includes the 4 new files and asserts they exist on disk"
  artifacts:
    - path: "src/cli/slash-command/bs/build-chunk.test.ts"
      provides: "BUILD-09/10/11/13 + UIQ-05 describe blocks; inverted forward-ref assertions; extended REFERENCED_PATHS"
      contains: "describe('BUILD-09"
  key_links:
    - from: "src/cli/slash-command/bs/build-chunk.test.ts"
      to: "build/playtest.md, build/revise.md, build/close.md, build/final-acceptance.md"
      via: "REFERENCED_PATHS existence check + per-file content assertions"
      pattern: "build/final-acceptance.md"
---

<objective>
Extend the `build-chunk.test.ts` drift-protection suite (Wave 0 scaffold) with the structural
assertions for BUILD-09/10/11/13 and UIQ-05, and INVERT the existing forward-reference-marker
assertions so they now demand the skill be complete (zero "authored in Phase 146" markers, the 4
new reference files registered and present on disk).

Purpose: Establish the RED drift tests FIRST so Plans 02-04 author content against a fixed
contract. These assertions go RED the moment this plan lands (the four files don't exist yet, the
markers are still present) and turn GREEN plan-by-plan as content is authored — the same
Wave-0-first discipline 143-01 used.

Output: An extended `build-chunk.test.ts` with five new per-requirement describe blocks, an
extended `REFERENCED_PATHS`, and inverted marker assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/146-bs-build-chunk-playtest-revise-close-final-acceptance/146-CONTEXT.md
@.planning/phases/146-bs-build-chunk-playtest-revise-close-final-acceptance/146-RESEARCH.md
@.planning/phases/146-bs-build-chunk-playtest-revise-close-final-acceptance/146-VALIDATION.md

<interfaces>
<!-- The existing drift-test conventions this plan extends. Use them verbatim; do NOT invent a new helper. -->

From src/cli/slash-command/bs/build-chunk.test.ts (existing, lines 33-112):
- `read(relativePath)` — reads a bs/ file relative to __dirname; call it INSIDE each `it()` body (never at describe level) so a missing file fails only that assertion.
- `REFERENCED_PATHS` (line 95-109) — array of paths build-chunk.md must cite AND that must exist on disk.
- `FORWARD_REFERENCE_MARKERS = ['authored in Phase 146']` (line 112) — the marker currently asserted PRESENT.
- One `describe('<REQ-ID> — <name>', ...)` block per requirement is the established pattern (see BUILD-08 line 385, UIQ-04 line 410).

The two assertions to INVERT (currently green, must flip):
- Line 517 `it('carries the "authored in Phase 146" forward-reference marker', ...)` — asserts PRESENCE. Flip to assert ABSENCE (zero occurrences in build-chunk.md).
- Line 555 `it('REFERENCED_PATHS does NOT include any Phase 146 step file', ...)` — asserts the 4 files are EXCLUDED. Flip to assert they ARE included (and thus covered by the existing on-disk existence loop at line 549).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BUILD-09/10/11 + UIQ-05 content describe blocks and extend REFERENCED_PATHS</name>
  <read_first>
    - src/cli/slash-command/bs/build-chunk.test.ts (full — the pattern to mirror; BUILD-08 block at 385, UIQ-04 block at 410 are the closest analogs for content-assertion shape)
    - .planning/phases/146-bs-build-chunk-playtest-revise-close-final-acceptance/146-VALIDATION.md ("Pins" line — the exact behaviors each describe must assert)
    - .planning/phases/146-bs-build-chunk-playtest-revise-close-final-acceptance/146-RESEARCH.md (Phase Requirements → Test Map table)
  </read_first>
  <action>
    Add four new describe blocks mirroring the existing one-describe-per-requirement pattern
    (each `it()` calls `read('build/<file>.md')` inside its body):
    - `describe('BUILD-09 — playtest', ...)` against `build/playtest.md`: assert it documents a
      numbered click-by-click script, per-seat steps and seat counts, dev-host affordances taught
      once (assert the literal `npx boardsmith dev` command and `--ai` are named), a build stamp /
      hard-reload freshness check (assert `/reload/i` or `Build stamp`), a one-line regression
      check, the taste line, a second-seat leak check for hidden-info chunks, an item-by-item
      Verified Checklist ("one at a time"/"not a vibe"), and that `verified (user-waived)` is
      recordable (assert the byte-exact string `verified (user-waived)`).
    - `describe('BUILD-10 — revise', ...)` against `build/revise.md`: assert it names all four
      triage categories (this-chunk defect / future scope / not-built-yet / rules change → RULINGS.md),
      the append-only `## Revision Rounds` ledger (assert `revise-1`/`revise-2` numbering), and the
      re-entry "feedback disposition report" + "targeted re-test" (assert both phrases, and that a
      blind full re-test is forbidden).
    - `describe('BUILD-11 — close', ...)` against `build/close.md`: assert it documents recording
      the verified commit hash (assert `git rev-parse HEAD` and `## Verified Commit Hash`), the
      sketch-tail delta re-derivation presented as a delta/gate before writing SKETCH.md (assert
      "delta" and "never a silent rewrite" or equivalent), and proposing the next chunk with its
      `ui:` tag. Assert a named "Bookkeeping Sequence" section exists (the byte-exact heading
      `## Bookkeeping Sequence`) since playtest.md's light path cites it.
    - `describe('UIQ-05 — final-acceptance', ...)` against `build/final-acceptance.md`: assert it
      documents all seven design-QA checks — screen-reader playthrough (assert `useAnnouncer` and
      `VoiceOver`), 200% zoom, compact touch targets, colorblind pass, both Slate themes, drag-drop
      keyboard alternates end-to-end, mobile layout via iframe-shrink — plus the coverage check
      (every non-variant slice cited by a closed chunk), the `--no-open` + server-kill discipline for
      any dispatched agent, and the BREAKPOINTS values 640/1024/1440. For the drag-drop check add a
      dedicated assertion that pins its content so it can't be silently dropped again: assert
      final-acceptance.md names `drag-drop` (or `drag and drop`) together with a keyboard-alternate
      phrase (e.g. `/keyboard alternate/i`) AND cites the `build/test.md` item-1 ActionPanel
      keyboard-only pattern it reuses (assert `build/test.md` appears near the drag-drop check).
    Extend `REFERENCED_PATHS` (line 95-109) with `'build/playtest.md'`, `'build/revise.md'`,
    `'build/close.md'`, `'build/final-acceptance.md'`. Do NOT touch the marker assertions in this
    task (that is Task 2).
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts 2>&1 | grep -E "BUILD-09|BUILD-10|BUILD-11|UIQ-05 — final" | head</automated>
  </verify>
  <acceptance_criteria>
    - Four new describe blocks exist: `describe('BUILD-09 — playtest'`, `describe('BUILD-10 — revise'`, `describe('BUILD-11 — close'`, `describe('UIQ-05 — final-acceptance'`
    - `REFERENCED_PATHS` contains all four new paths
    - The UIQ-05 block asserts all seven design-QA checks, including a dedicated drag-drop keyboard-alternates assertion that pins the check content and its `build/test.md` item-1 reuse
    - The new BUILD-09/10/11/UIQ-05 assertions run and are RED (files don't exist yet) — RED is the intended Wave-0 state, NOT a failure of this plan
    - Byte-exact strings asserted where they are drift-pins: `verified (user-waived)`, `## Verified Commit Hash`, `## Bookkeeping Sequence`, `git rev-parse HEAD`
  </acceptance_criteria>
  <done>Four content describe blocks added; REFERENCED_PATHS extended with the 4 new files; the UIQ-05 block pins the drag-drop keyboard-alternates check; tsc/vitest collects the suite without a syntax error.</done>
</task>

<task type="auto">
  <name>Task 2: Add BUILD-13 describe block and invert the forward-reference-marker assertions</name>
  <read_first>
    - src/cli/slash-command/bs/build-chunk.test.ts (the two assertions at line 517 and 555, and the FORWARD_REFERENCE_MARKERS constant at 112)
    - src/cli/slash-command/bs/state-machine.md ("Git Protocol" 114-119, "Session Lock" 102-112, "Session Handoff Seams" 133-148 — the sources BUILD-13 must assert are CITED not restated)
  </read_first>
  <action>
    Add `describe('BUILD-13 — git protocol / handoff / lock citation, no restatement', ...)`
    against `build/build-chunk.md`: assert build-chunk.md CITES `state-machine.md` "Git Protocol",
    "Session Lock", and "Session Handoff Seams" by name (assert the phrase `cite `state-machine.md``
    appears near each), and assert the commit convention `chunk-<slug>/step-<name>` appears as a
    quoted example. The BUILD-13 discipline is citation, so keep assertions to citation-phrasing
    presence, not a ban on the numbers appearing at all (build-chunk.md legitimately quotes the
    `chunk-<slug>/step-<name>` example format in its Git Protocol pointer).
    Then INVERT the two existing assertions:
    - Flip line-517's `it('carries the "authored in Phase 146" forward-reference marker', ...)` to
      `it('carries ZERO "authored in Phase 146" forward-reference markers (skill complete)', ...)`
      asserting `read('build-chunk.md')` does NOT contain any string in FORWARD_REFERENCE_MARKERS
      (`expect(buildChunk).not.toContain('authored in Phase 146')`). Repurpose or keep
      FORWARD_REFERENCE_MARKERS as the now-absent-marker constant (do not delete the constant name;
      the drift test still references it as the forbidden string).
    - Flip line-555's `it('REFERENCED_PATHS does NOT include any Phase 146 step file', ...)` to
      `it('REFERENCED_PATHS now includes the four Phase 146 step files', ...)` asserting
      `build/playtest.md`, `build/revise.md`, `build/close.md`, `build/final-acceptance.md` ARE all
      present in REFERENCED_PATHS (their on-disk existence is then covered by the existing loop at
      line 549). Update the describe-header/comment at line 509 that says "Steps 4-10
      forward-reference stubs" to reflect that the stubs are now retired.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "BUILD-13" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `describe('BUILD-13` block exists and asserts citation phrasing for Git Protocol / Session Lock / Session Handoff Seams
    - Line-517 assertion now asserts ABSENCE of `authored in Phase 146` (RED until Plan 04 edits build-chunk.md)
    - Line-555 assertion now asserts the four new paths ARE in REFERENCED_PATHS (turns green once Plans 02-03 land the files, since the existence loop then passes)
    - No assertion anywhere still demands the presence of `authored in Phase 146`
  </acceptance_criteria>
  <done>BUILD-13 describe added; both forward-ref assertions inverted; the suite's remaining RED failures are exactly the not-yet-authored-content assertions and the not-yet-edited build-chunk.md marker assertion — all of which later plans resolve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan edits a test file only — no runtime code, no network, no process, no input parsing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-01 | Information Disclosure | build-chunk.test.ts | accept | Test source; no attack surface. The hidden-info leak surface it *asserts on* is mitigated in Plan 02 (playtest second-seat leak check) and its own T-146-01 entry. |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this plan (RESEARCH.md: Package Legitimacy Audit N/A). |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts` collects with no syntax/tsc error.
- New RED assertions are limited to (a) the four not-yet-authored reference files and (b) the
  not-yet-edited `authored in Phase 146` absence check — all intended Wave-0 RED, resolved by
  Plans 02-04.
</verification>

<success_criteria>
Five new/inverted requirement contracts pinned in the drift suite; REFERENCED_PATHS extended; no
assertion demands the forward-reference marker's presence any longer.
</success_criteria>

<output>
Create `.planning/phases/146-bs-build-chunk-playtest-revise-close-final-acceptance/146-01-SUMMARY.md` when done
</output>
</output>
