---
phase: 146-bs-build-chunk-playtest-revise-close-final-acceptance
plan: 04
type: execute
wave: 3
depends_on: [146-01, 146-02, 146-03]
files_modified:
  - src/cli/slash-command/bs/build-chunk.md
autonomous: true
requirements: [BUILD-13]
must_haves:
  truths:
    - "build-chunk.md contains ZERO 'authored in Phase 146' forward-reference markers — the 10-step engine is fully live"
    - "The playtest/revise/close dispatch rows carry no phase-marker suffix; the 4 new files are registered in Reference Files"
    - "Step Groups 2-4's forward-reference section is reconciled into live Step Group 4 Dispatch prose"
    - "Git protocol, session-handoff seams, and the session lock are surfaced CITING state-machine.md, never restated (BUILD-13)"
    - "The full build-chunk.test.ts drift suite is green and npm test is green"
  artifacts:
    - path: "src/cli/slash-command/bs/build-chunk.md"
      provides: "The fully-live 10-step orchestrator with all forward references retired"
      contains: "Step Group 4 Dispatch"
  key_links:
    - from: "src/cli/slash-command/bs/build-chunk.md"
      to: "build/playtest.md, build/revise.md, build/close.md, build/final-acceptance.md"
      via: "dispatch table rows + Reference Files list (live, no phase marker)"
      pattern: "build/final-acceptance.md"
    - from: "src/cli/slash-command/bs/build-chunk.md"
      to: "state-machine.md"
      via: "Git Protocol / Session Lock / Session Handoff Seams citations (BUILD-13)"
      pattern: "cite `state-machine.md`"
---

<objective>
Retire every "authored in Phase 146" forward-reference marker in `build-chunk.md`, register the
four new reference files, reconcile the Step Groups 2-4 forward-reference section into live prose,
and confirm the git-protocol / handoff-seam / session-lock content is CITED (never restated) per
BUILD-13. Then assert the whole skill is complete: zero markers remain and the full suite is green.

Purpose: This is the closing plan — after it lands, `build-chunk.md` is a fully live 10-step engine
with no pending markers, and the drift suite's inverted marker assertions (from Plan 01) go green.

Output: An edited `build-chunk.md` and a green full test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/146-bs-build-chunk-playtest-revise-close-final-acceptance/146-CONTEXT.md
@.planning/phases/146-bs-build-chunk-playtest-revise-close-final-acceptance/146-PATTERNS.md
@src/cli/slash-command/bs/build-chunk.md
@src/cli/slash-command/bs/state-machine.md

<interfaces>
<!-- Exact edit spots in build-chunk.md (verified this planning session). -->
- Dispatch table rows 127-129: `playtest`/`revise`/`close` rows each carry ` — authored in Phase 146`.
- Explanatory paragraph 131-136: "Steps 8–10 … named here as forward references only …".
- Light-path note ~152 (inside 146-153): "… `close`'s duty, authored in Phase 146; Step 2's lazy tail-entry detailing …".
- Step Groups 2-4 forward-reference section 206-213: `## Step Groups 2–4 (forward reference)`.
- Reference Files forward-ref sublist 264-268: "And, forward-referenced only (not yet authored): … — authored in Phase 146".
- Git Protocol section 229-234, Session Handoff Seams 214-220, Step 0 Session Lock 32-52 (BUILD-13 — already citation-shaped; verify/tighten, do not restate).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire all 7 markers, register the 4 files, reconcile Step Group 4 dispatch prose</name>
  <read_first>
    - src/cli/slash-command/bs/build-chunk.md (full — the file being edited; all edit spots above)
    - src/cli/slash-command/bs/build-chunk.md Step Group 1 Dispatch section 155-204 (the shape live
      dispatch prose takes — per-step delegation + persistence discipline + end-of-group print)
    - 146-PATTERNS.md build-chunk.md edit section (the exact before/after for every spot)
  </read_first>
  <action>
    Edit `src/cli/slash-command/bs/build-chunk.md`:
    1. Dispatch table rows 127-129: drop the ` — authored in Phase 146` suffix from the playtest,
       revise, and close rows so they match the marker-less shape of the build/test/audit/repair
       rows above.
    2. Explanatory paragraph 131-136: replace with a plain statement that all 10 steps now have live
       dispatch targets (mirror the sentence style already used for build/test/audit/repair one
       paragraph earlier); remove the "When Phase 146 lands …" framing.
    3. Light-path note ~152: drop `, authored in Phase 146`; keep the rest of the sentence, now
       pointing at a live `close.md` — cite `close.md`'s `## Bookkeeping Sequence` by name so the
       existing "playtest performs close's bookkeeping for light chunks" promise resolves to a real
       named section (Pitfall 2 reconciliation).
    4. Reference Files sublist 264-268: delete the "And, forward-referenced only (not yet authored):"
       header and its three markered lines; fold all FOUR new files (playtest, revise, close,
       final-acceptance) into the main bulleted list (248-262) using the one-line-description style
       of the `build/repair.md` entry (final-acceptance.md's description notes it is the
       sketch's mandated-chunk design-QA pass).
    5. Step Groups 2-4 section 206-213: reconcile the `## Step Groups 2–4 (forward reference)` heading
       — now that all groups are live, author a live `## Step Group 4 Dispatch — {playtest, one revise
       round, close}` section in Step-Group-1's shape (per-step dispatch delegation to
       playtest.md/revise.md/close.md, per-step persistence to CHUNK.md's Step Checklist, and the
       end-of-group "print the exact next command + confirm the game folder is saved" close). If
       groups 2-3 dispatch prose already lives in their own reference files, state that explicitly
       and scope this section to Group 4; remove the "(forward reference)" qualifier from the heading.
  </action>
  <verify>
    <automated>! grep -q "authored in Phase" src/cli/slash-command/bs/build-chunk.md && echo "ZERO markers OK"</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "authored in Phase" src/cli/slash-command/bs/build-chunk.md` succeeds (zero occurrences)
    - The three dispatch rows for playtest/revise/close carry no phase-marker suffix
    - All four new files appear in the main Reference Files bulleted list with a one-line description each; the "forward-referenced only" sublist is gone
    - A live `## Step Group 4 Dispatch` section exists; the heading no longer says "(forward reference)"
    - The light-path note cites `close.md`'s `## Bookkeeping Sequence` by name
  </acceptance_criteria>
  <done>All 7 markers retired, 4 files registered, Step Group 4 dispatch prose live.</done>
</task>

<task type="auto">
  <name>Task 2: Verify BUILD-13 citation discipline and assert the full suite green</name>
  <read_first>
    - src/cli/slash-command/bs/build-chunk.md (Git Protocol 229-234, Session Handoff Seams 214-220,
      Step 0 Session Lock 32-52 — confirm each surfaces the rule via `cite `state-machine.md`` and
      does not restate the 24h staleness number or `chunk-<slug>/step-<name>` format as an
      authoritative rule rather than a quoted example)
    - src/cli/slash-command/bs/state-machine.md (the canonical sections being cited)
  </read_first>
  <action>
    Confirm (and tighten if needed) that build-chunk.md's BUILD-13 surfaces are citations, not
    restatements: the Git Protocol section cites `state-machine.md` "Git Protocol"; the Session
    Handoff Seams section cites `state-machine.md` "Session Handoff Seams"; Step 0 cites
    `state-machine.md` "Session Lock". The `chunk-<slug>/step-<name>` convention and the 24-hour
    staleness may appear only as quoted examples adjacent to a citation pointer, never as freshly
    restated rule text. Make no other content changes. Then run the full drift suite and the repo
    suite to prove the skill is complete and nothing else drifted.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts 2>&1 | tail -5 && ! grep -q "authored in Phase" src/cli/slash-command/bs/build-chunk.md && npm test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Full `build-chunk.test.ts` suite is GREEN (all BUILD-01..13 + UIQ-01..05 + cross-file + return-shape blocks)
    - `-t "BUILD-13"` green: Git Protocol / Session Lock / Session Handoff Seams are cited, not restated
    - `! grep -q "authored in Phase" src/cli/slash-command/bs/build-chunk.md` succeeds
    - `npm test` is green (no sibling drift in ingest.test.ts / templates.test.ts)
  </acceptance_criteria>
  <done>Skill complete: zero markers, BUILD-13 citation-clean, full suite and npm test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan edits one markdown router file — no runtime code, no process, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-03 | Tampering (doc-accuracy) | build-chunk.md dispatch/citation edits | mitigate | The drift suite (build-chunk.test.ts) pins every dispatch path, citation phrase, and the zero-marker invariant; the BUILD-13 block guards against a restatement replacing a citation. |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH.md: Package Legitimacy Audit N/A). |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts` fully green.
- `! grep -q "authored in Phase" src/cli/slash-command/bs/build-chunk.md` succeeds.
- `npm test` green.
</verification>

<success_criteria>
build-chunk.md is a fully live 10-step engine: zero forward-reference markers, four new files
registered, Step Group 4 dispatch live, BUILD-13 citation-clean, and the entire drift suite plus
npm test green.
</success_criteria>

<output>
Create `.planning/phases/146-bs-build-chunk-playtest-revise-close-final-acceptance/146-04-SUMMARY.md` when done
</output>
