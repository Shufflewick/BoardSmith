---
phase: 147-bs-check-status-insert-chunk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/slash-command/bs/status-tools.test.ts
  - src/cli/slash-command/bs/check-status.md
autonomous: true
requirements: [STAT-01]
must_haves:
  truths:
    - "Running /bs-check-status reports all 7 items: chunks done/remaining, current chunk+step, outstanding playtest feedback, waived verifications, asset debts, ideas-backlog size, and the exact next command"
    - "check-status is read-only — it never instructs or performs any state mutation"
    - "check-status cites state-machine.md's Consistency Check on entry and derives current chunk/step using the same rules build-chunk.md already uses"
  artifacts:
    - path: "src/cli/slash-command/bs/check-status.md"
      provides: "STAT-01 top-level thin reader skill, full content (non-thin-pointer)"
      min_lines: 90
    - path: "src/cli/slash-command/bs/status-tools.test.ts"
      provides: "Drift-test scaffold (read helper + constants + REFERENCED_PATHS/REFERENCED_SECTIONS) and STAT-01 describe block"
      contains: "describe('STAT-01"
  key_links:
    - from: "src/cli/slash-command/bs/check-status.md"
      to: "state-machine.md § Consistency Check / Status Enum (exact) / Session Lock"
      via: "cite-not-restate by exact heading"
      pattern: "Consistency Check \\(every bs- entry point"
    - from: "src/cli/slash-command/bs/check-status.md"
      to: "templates/SKETCH.template.md, CHUNK.template.md, ASSETS.template.md"
      via: "Reference Files footer (no dangling pointers)"
      pattern: "templates/ASSETS.template.md"
---

<objective>
Author `/bs-check-status` (STAT-01) — the thin, read-only status-reader skill — and stand up the `status-tools.test.ts` drift suite that pins its STAT-01 contract.

Purpose: A designer needs one command to see exactly where the project stands (7 items) and what to run next, without any risk of the status view mutating state.
Output:
- `src/cli/slash-command/bs/check-status.md` (new top-level skill, no `build/` subfolder)
- `src/cli/slash-command/bs/status-tools.test.ts` (new drift test — scaffold + STAT-01 block)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/147-bs-check-status-insert-chunk/147-CONTEXT.md
@.planning/phases/147-bs-check-status-insert-chunk/147-RESEARCH.md
@.planning/phases/147-bs-check-status-insert-chunk/147-PATTERNS.md

<read_first>
<!-- Author check-status.md by mirroring ingest-rules.md's thin-skill idiom; do NOT restate state-machine.md content. -->
- `src/cli/slash-command/bs/ingest-rules.md` — CLOSEST STRUCTURAL ANALOG. Copy: (1) the opening cite-not-restate preamble (lines 1-8), (2) the Reference Files + "Installed location" footer (lines 192-214, incl. the "Installer-phase dependency" caveat — Phase 148 has not wired install yet), (3) the "print the exact next command" closing idiom (line 189).
- `src/cli/slash-command/bs/build-chunk.md` lines 14-21 (Context-Economics Hard Rule — "reading state files is exactly the orchestrator's job"), lines 32-36 (Step 0 Consistency-Check invocation idiom, `ls <file>` never glob), lines 58-63 (the "what's left?" self-answer this skill supersedes — the SEED for item-1/item-2 derivation), line 72-76 (the exact "first chunk whose status is neither `verified` nor `verified (user-waived)`" resume-target rule and "first incomplete step on the Step Checklist" rule — REUSE these verbatim in spirit, do NOT invent new derivation logic).
- `.planning/bs-skills-plan.md` §3 "/bs-check-status" (line 103-105) — the canonical 7-item contract.
- `src/cli/slash-command/bs/state-machine.md` — the sections check-status CITES: `## Consistency Check (every bs- entry point, before proceeding)`, `## Status Enum (exact)` (for the `verified (user-waived)` scan), `## Session Lock` (read-only lock-state reporting).
- `src/cli/slash-command/bs/templates/SKETCH.template.md` — `Sketch Version:` field (line 12), `Session Lock:` field (line 15), `## Ordered Chunk List` (per-entry `- Status (derived from chunks/<slug>/CHUNK.md):` grammar and the sketch-level tail grammar), `## Ideas Backlog` (item 6 = its size).
- `src/cli/slash-command/bs/templates/CHUNK.template.md` — `## Step Checklist` (current-step = first unchecked `- [ ]`), `## Revision Rounds` (item 3 outstanding feedback), waived-status comment.
- `src/cli/slash-command/bs/templates/ASSETS.template.md` — the 5-column `| needed-by-chunk | requested | received | placeholder-in-use | file path |` ledger; item 5 debts = rows where `requested=yes` AND `received=no`.
- `src/cli/slash-command/bs/ingest.test.ts` lines 25-56 — the drift-test scaffold to COPY VERBATIM: `read()` helper, `existsSync` import, per-`it()` read isolation (files don't exist yet at Wave 0), and the `REFERENCED_PATHS` array pattern.
- `src/cli/slash-command/bs/templates.test.ts` lines 30-69 — named byte-identical-marker constants (`STALE_MARKER`, the `'verified (user-waived)'` value).
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold status-tools.test.ts + STAT-01 drift block (RED)</name>
  <files>src/cli/slash-command/bs/status-tools.test.ts</files>
  <behavior>
    Test-first — this block goes RED because check-status.md does not exist yet, then Task 2 greens it. Copy the ingest.test.ts scaffold verbatim (`__dirname`, `read()`, `existsSync`, per-`it()` reads).
    Declare shared constants at top (reused by Plan 02's STAT-02 block):
      - STALE_MARKER = 'stale — re-derive before build'  (em-dash — byte-exact)
      - WAIVED_STATUS = 'verified (user-waived)'
      - REFERENCED_PATHS = ['state-machine.md','templates/SKETCH.template.md','templates/CHUNK.template.md','templates/ASSETS.template.md']
      - REFERENCED_SECTIONS = ['## Consistency Check (every bs- entry point, before proceeding)','## Status Enum (exact)','## Session Lock']  (the headings check-status must cite AND which must exist in state-machine.md)
    describe('STAT-01 — check-status.md read-only status reader'):
      - Test: check-status.md exists and is non-thin-pointer full content (min length gate, e.g. > 2500 chars).
      - Test: enumerates all 7 report items — assert phrases for (1) chunks done/remaining, (2) current chunk + step, (3) outstanding playtest feedback, (4) waived verifications (contains WAIVED_STATUS) + a proposed batch playtest, (5) asset debts (references ASSETS.md), (6) ideas backlog size, (7) exact next command.
      - Test: READ-ONLY — expect(checkStatus).not.toMatch(/write .*SKETCH|edit .*CHUNK|mutate|bump .*version/i) AND a positive assertion the file states it performs no writes / never mutates state.
      - Test: cites each REFERENCED_SECTIONS heading by exact string (loop) AND that each heading actually exists in state-machine.md (guards Pitfall 2 — a cited heading that doesn't exist).
      - Test: references each REFERENCED_PATHS entry, and each path exists via existsSync(join(__dirname, p)) (no dangling pointers). Note: the two skill paths themselves are added to a separate existence check greened by Task 2.
  </behavior>
  <action>Create `src/cli/slash-command/bs/status-tools.test.ts`. Copy the header/`read()`/`existsSync`/per-`it()`-isolation scaffold from ingest.test.ts (lines 25-56) and the named-constant style from templates.test.ts. Add the shared constants above and the `STAT-01` describe block. Do NOT author STAT-02 assertions here — that block belongs to Plan 02 (shared-file sequencing). Every `read()` call MUST be inside its `it()` body so a not-yet-authored file fails only its own assertion.</action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/status-tools.test.ts -t "STAT-01" 2>&1 | grep -q "STAT-01" && echo "block runs (RED expected until Task 2)"</automated>
  </verify>
  <done>status-tools.test.ts exists; STAT-01 describe block runs; assertions are RED only because check-status.md is absent (constants/scaffold themselves error-free).</done>
</task>

<task type="auto">
  <name>Task 2: Author check-status.md (STAT-01, GREEN)</name>
  <files>src/cli/slash-command/bs/check-status.md</files>
  <action>
Create `src/cli/slash-command/bs/check-status.md` as a top-level thin READER skill (no `build/` subfolder), mirroring ingest-rules.md's structure:
- Opening cite-not-restate preamble adapted: state it is a lean READER (not a dispatcher) that reads SKETCH.md/CHUNK.md/ASSETS.md directly and dispatches NO subagents — cite build-chunk.md's "reading state files is exactly the orchestrator's job" sanction.
- Step 0: Consistency Check on entry — cite state-machine.md `## Consistency Check (every bs- entry point, before proceeding)`; use literal `ls <file>` checks, never `**/glob`. Report problems and confirm how to proceed; do NOT repair (repair is build-chunk.md's job).
- Body: read SKETCH.md then the current CHUNK.md then ASSETS.md and synthesize the SEVEN items. Derive item 1 (done = derived-status `verified` or `verified (user-waived)`; remaining = everything else incl. sketch-level tail entries), item 2 (current chunk = first entry whose derived status is neither `verified` nor `verified (user-waived)`; current step = first unchecked `- [ ]` in that chunk's `## Step Checklist` — REUSE build-chunk.md's identical rules, cite them), item 3 (`## Revision Rounds` open items), item 4 (scan Ordered Chunk List for `verified (user-waived)`, batch them, propose a batch playtest per bs-skills-plan.md §8), item 5 (ASSETS.md ledger rows requested=yes & received=no, informational not blocking — "a missing asset never blocks a chunk"), item 6 (`## Ideas Backlog` item count), item 7 (derived next command: `/bs-build-chunk` mid-ceremony, `/bs-insert-chunk` if reshaping was just discussed, `/bs-ingest-rules` if nothing started).
- READ-ONLY posture explicit: state the skill performs no writes — not even refreshing the session-lock timestamp (per RESEARCH Pitfall 4). It may REPORT lock state (cite `## Session Lock`) but never take/refresh it.
- Cite `## Status Enum (exact)` for the `verified (user-waived)` value; do NOT restate the enum.
- Reference Files + "Installed location" footer: list `state-machine.md`, `templates/SKETCH.template.md`, `templates/CHUNK.template.md`, `templates/ASSETS.template.md`; carry the same "Installer-phase dependency (Phase 148 not yet wired)" caveat as ingest-rules.md.
No fenced code blocks of implementation — directive prose only. Keep it thin (~ingest-rules.md length, ≤ ~150 lines).
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/status-tools.test.ts -t "STAT-01"</automated>
  </verify>
  <done>All STAT-01 assertions green: file is full content, all 7 items enumerated, read-only assertion passes, all cited state-machine.md headings exist and are referenced, all referenced template paths exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| check-status skill → shared SKETCH/CHUNK/ASSETS state | check-status reads project state files that a concurrent build session may hold a lock on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-01 | Tampering | check-status.md accidentally mutating SKETCH.md/CHUNK.md (e.g. "helpfully" refreshing the session lock or fixing a consistency-check problem) | mitigate | Skill states read-only posture explicitly; `status-tools.test.ts` read-only assertion (`.not.toMatch(/write|edit|mutate|bump version/)`) pins it — a reword that adds mutation language fails the drift test |
| T-147-02 | Information Disclosure | Status report leaking internal file paths / stack traces to the user | accept | Report surfaces only sketch/chunk state the designer owns; no secrets, no PII, no external surface (markdown skill consumed by the project's own LLM session) |
| T-147-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase — vitest already present; no Package Legitimacy Audit required |
| S/R/D/E | Spoofing/Repudiation/DoS/Elevation | — | N/A | No auth, network, session, or privilege surface — content-only markdown + a local drift test |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/status-tools.test.ts -t "STAT-01"` green.
- check-status.md ≤ ~150 lines yet non-thin (passes the min-length gate) — thinness sanity check.
- No writes described anywhere in check-status.md (grep negative for mutation verbs against SKETCH/CHUNK).
</verification>

<success_criteria>
- STAT-01 fully covered: `/bs-check-status` enumerates all 7 report items, is provably read-only, and cites the correct (existing) state-machine.md headings.
- status-tools.test.ts scaffold + shared constants + STAT-01 block in place for Plan 02 to extend.
</success_criteria>

<output>
Create `.planning/phases/147-bs-check-status-insert-chunk/147-01-SUMMARY.md` when done.
</output>
