---
phase: 147-bs-check-status-insert-chunk
plan: 02
type: execute
wave: 2
depends_on: ["147-01"]
files_modified:
  - src/cli/slash-command/bs/status-tools.test.ts
  - src/cli/slash-command/bs/insert-chunk.md
  - src/cli/slash-command/bs/build-chunk.md
autonomous: true
requirements: [STAT-02]
must_haves:
  truths:
    - "Running /bs-insert-chunk can add, reorder, split, or remove chunks"
    - "insert-chunk re-validates dependency order against citations and diffs the new chunk's citations against CLOSED chunks to flag overlaps"
    - "insert-chunk marks any already-detailed pending CHUNK.md as 'stale — re-derive before build' (byte-exact) and bumps the sketch version stamp, CHUNK.md-first then SKETCH.md"
    - "insert-chunk guards SKETCH.template.md's ## Mandated Chunks invariants (first-chunk / game-end / final-acceptance) survive a reshape"
    - "build-chunk.md's two stale Phase-147 forward-references now route to the shipped /bs-check-status and /bs-insert-chunk skills"
  artifacts:
    - path: "src/cli/slash-command/bs/insert-chunk.md"
      provides: "STAT-02 sketch-editor skill, full content, all 4 ops enumerated"
      min_lines: 90
    - path: "src/cli/slash-command/bs/status-tools.test.ts"
      provides: "STAT-02 describe block (green) + build-chunk forward-ref retirement assertion"
      contains: "describe('STAT-02"
    - path: "src/cli/slash-command/bs/build-chunk.md"
      provides: "Step 1 intents routed to the now-existing skills (no 'Phase 147 / until it lands' text)"
  key_links:
    - from: "src/cli/slash-command/bs/insert-chunk.md"
      to: "state-machine.md § Write Order + SKETCH.template.md 'Sketch Version:' field"
      via: "cite Write Order section AND the template field (NOT a nonexistent state-machine 'Sketch Version' heading)"
      pattern: "Sketch Version:"
    - from: "src/cli/slash-command/bs/insert-chunk.md"
      to: "CHUNK.md Status line"
      via: "byte-exact stale marker"
      pattern: "stale — re-derive before build"
---

<objective>
Author `/bs-insert-chunk` (STAT-02) — the thin sketch-editor skill — extend `status-tools.test.ts` with its STAT-02 drift block, and retire build-chunk.md's two now-stale Phase-147 forward-references.

Purpose: A designer must be able to reshape the sketch (add/reorder/split/remove chunks) safely — re-validating dependency order, flagging citation overlaps with closed chunks, marking invalidated pending chunks stale, and bumping the version stamp so a concurrently resumed build session detects the change.
Output:
- `src/cli/slash-command/bs/insert-chunk.md` (new top-level skill)
- `src/cli/slash-command/bs/status-tools.test.ts` (STAT-02 block appended)
- `src/cli/slash-command/bs/build-chunk.md` (Step 1 forward-refs updated)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/147-bs-check-status-insert-chunk/147-CONTEXT.md
@.planning/phases/147-bs-check-status-insert-chunk/147-RESEARCH.md
@.planning/phases/147-bs-check-status-insert-chunk/147-PATTERNS.md
@.planning/phases/147-bs-check-status-insert-chunk/147-01-SUMMARY.md

<read_first>
<!-- insert-chunk.md = ingest-rules.md structure + build-chunk.md write-order/stale idiom. Cite, never restate. -->
- `src/cli/slash-command/bs/check-status.md` — the sibling skill shipped in Plan 01; match its preamble/footer conventions and the shared constants already in status-tools.test.ts.
- `src/cli/slash-command/bs/ingest-rules.md` — structure/preamble/footer analog; lines 157-160 the "Negotiation posture" phrasing (user's ordering wins unless a hard dependency is violated — name it concretely, propose the minimal prerequisite) to REUSE verbatim in spirit for op (a).
- `src/cli/slash-command/bs/state-machine.md` — CITE: `## Write Order` (CHUNK.md-first-then-SKETCH.md, Status line last), `## Status Enum (exact)` (holds the stale marker), `## Consistency Check (every bs- entry point, before proceeding)`, `## Session Lock`. NOTE (Pitfall 2, VERIFIED): there is NO `## Sketch Version` heading in state-machine.md — cite the `## Write Order` section for the write-order half and SKETCH.template.md's `Sketch Version:` FIELD for the version stamp. Do NOT emit a citation string like `state-machine.md "Sketch Version"`.
- `src/cli/slash-command/bs/build-chunk.md` — lines 58-67 (Step 1: the two Phase-147 forward-reference bullets to REPLACE — `grep -n "Phase 147" build-chunk.md` → lines 63 & 65; note line 75 also references check-status but is a correct live pointer, leave it and line 347 alone), line 77 (consumer behavior: "A chunk whose Status line reads `stale — re-derive before build` stops routing" — insert-chunk is the PRODUCER, cross-reference don't restate), lines 80-84 (final-acceptance / `## Mandated Chunks` detection insert-chunk must not silently break).
- `src/cli/slash-command/bs/templates/SKETCH.template.md` — `Sketch Version: 1` field (line 12) + its inline comment ("Bumped by /bs-insert-chunk on every structural change..."), `Session Lock:` field (line 15, must be preserved untouched by op d), `## Ordered Chunk List` (per-entry `- Citations:` — read by ops a/b), `## Mandated Chunks` (first chunk = core event loop; game-end chunk required; final-acceptance chunk required — the invariants op a must guard, OQ2 resolved YES).
- `src/cli/slash-command/bs/templates/CHUNK.template.md` — `Status:` line (line 8, where the stale marker is set), `## Interpretation` + `## Newly Discovered Citations` (BOTH scanned by op b for each closed chunk's citation set).
- `.planning/bs-skills-plan.md` §4 "/bs-insert-chunk" (lines 107-109) — the canonical 4-op contract + the "chunk `movement` implemented 05-movement.md; your insertion also cites it" overlap-flag example.
- `src/cli/slash-command/bs/ingest.test.ts` lines 47-56 — `REFERENCED_PATHS` existsSync idiom; `src/cli/slash-command/bs/build-chunk.test.ts` — one-`describe`-per-requirement-ID breadth. NOTE: build-chunk.test.ts does NOT pin the "Phase 147" forward-ref text (verified), so editing those bullets is safe.
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append STAT-02 drift block to status-tools.test.ts (RED)</name>
  <files>src/cli/slash-command/bs/status-tools.test.ts</files>
  <behavior>
    Test-first — goes RED until insert-chunk.md (Task 2) and the build-chunk.md edit (Task 3) land. Reuse the STALE_MARKER / WAIVED_STATUS / REFERENCED_PATHS constants already declared by Plan 01. Add REFERENCED_SECTIONS_INSERT = ['## Write Order','## Consistency Check (every bs- entry point, before proceeding)','## Status Enum (exact)','## Session Lock'].
    describe('STAT-02 — insert-chunk.md sketch editor'):
      - insert-chunk.md exists and is non-thin full content (min length gate).
      - Enumerates all 4 operations by name: (a) dependency-order re-validation, (b) closed-chunk citation-overlap diff, (c) stale-marking, (d) version-stamp bump. Assert a phrase per op.
      - Pins STALE_MARKER byte-exact (em-dash) as the CHUNK.md Status value it sets.
      - Documents the version-stamp bump AND cites it correctly: contains 'Sketch Version:' (the SKETCH.template.md field) AND '## Write Order'; and does NOT cite a nonexistent state-machine heading — expect(insertChunk).not.toMatch(/state-machine\.md[^\n]*"?Sketch Version"?/).
      - Op b scans BOTH '## Interpretation' and '## Newly Discovered Citations' (assert both phrases present).
      - Write order documented CHUNK.md-first-then-SKETCH.md (assert both order words / cite ## Write Order).
      - Mandated-Chunks guard present (OQ2): contains '## Mandated Chunks' and a phrase about first-chunk/game-end/final-acceptance invariants surviving a reshape.
      - Cites each REFERENCED_SECTIONS_INSERT heading by exact string AND each exists in state-machine.md.
      - References each REFERENCED_PATHS entry; each exists via existsSync.
    describe('STAT-02 — build-chunk.md forward-ref retirement'):
      - build-chunk.md no longer contains the stale forward-ref phrasing: expect(buildChunk).not.toMatch(/ships as\s+`?\/bs-check-status`?\s*\(Phase 147\)/) and .not.toMatch(/ships as\s+`?\/bs-insert-chunk`?\s*\(Phase 147\)/) and .not.toMatch(/until it lands/).
      - build-chunk.md now routes to the live skills: contains '/bs-check-status' and '/bs-insert-chunk' (as live pointers, not Phase-147 futures).
    Also add a describe('both skills exist') existence check for check-status.md AND insert-chunk.md via existsSync.
  </behavior>
  <action>Edit `src/cli/slash-command/bs/status-tools.test.ts` — append the STAT-02 describe blocks and the build-chunk forward-ref retirement assertions. Reuse existing constants; add REFERENCED_SECTIONS_INSERT. Keep every `read()` inside its `it()` body.</action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/status-tools.test.ts -t "STAT-02" 2>&1 | grep -q "STAT-02" && echo "block runs (RED expected until Tasks 2-3)"</automated>
  </verify>
  <done>STAT-02 blocks run; RED only because insert-chunk.md is absent and build-chunk.md still holds the forward-refs.</done>
</task>

<task type="auto">
  <name>Task 2: Author insert-chunk.md (STAT-02, GREEN for the editor block)</name>
  <files>src/cli/slash-command/bs/insert-chunk.md</files>
  <action>
Create `src/cli/slash-command/bs/insert-chunk.md` as a top-level thin EDITOR skill (no `build/` subfolder), mirroring ingest-rules.md structure + build-chunk.md's mutation idiom:
- Opening cite-not-restate preamble adapted: lean EDITOR of SKETCH.md/CHUNK.md state; reads state files directly; per CONTEXT discretion it does the citation diff INLINE (no subagents) — it FLAGS overlaps, it does NOT trigger revise rounds, re-investigate, or dispatch redteam (RESEARCH Pitfall 3).
- Step 0: Consistency Check on entry — cite `## Consistency Check (every bs- entry point, before proceeding)`; `ls <file>` never glob.
- The four operations, each named and cited:
  (a) Dependency-order re-validation — compare the moved/new chunk's `- Citations:` (SKETCH.md) / `## Interpretation` citations (CHUNK.md if detailed) against ordered positions of chunks covering the same rulebook section; a chunk citing rules a not-yet-`verified`/`verified (user-waived)` chunk covers is a violation — name it concretely, propose the minimal prerequisite. Negotiation posture (reuse ingest-rules.md lines 157-160 phrasing): user's ordering wins unless a HARD dependency is violated. FOLD IN the `## Mandated Chunks` invariant guard (OQ2 resolved YES): a reshape must not move the final-acceptance chunk off the tail, drop the game-end chunk, or displace the first/core-event-loop chunk — flag concretely if it would.
  (b) Closed-chunk citation-overlap diff — for each CLOSED chunk (`verified` or `verified (user-waived)`), union its `## Interpretation` + `## Newly Discovered Citations` citation sets; intersect with the new/edited chunk's citations; flag each overlap with the closed chunk's slug ("chunk `movement` implemented 05-movement.md; your insertion also cites it — that chunk may need a revise round"). Flag only — resolution is a later /bs-build-chunk revise round.
  (c) Stale-marking — for any already-DETAILED PENDING (has a `chunks/<slug>/` dir, not closed) CHUNK.md the sketch change invalidates, set its `Status:` line to `stale — re-derive before build` (byte-exact em-dash). Content is NOT wiped — only the Status line changes. Cross-reference that build-chunk.md already documents the consumer side (Status `stale — re-derive before build` stops routing) — do not restate it.
  (d) Version-stamp bump — increment SKETCH.md's `Sketch Version: N` → `N+1` (cite SKETCH.template.md's `Sketch Version:` field + its inline comment). Preserve the adjacent `Session Lock:` line untouched.
- Write Order — cite state-machine.md `## Write Order`: CHUNK.md edits (op c) land FIRST, then the SKETCH.md version bump (op d) SECOND; the `Status:`/version line is written last within each file. Do NOT restate the rule; cite the section.
- Close by printing the exact next command (likely `/bs-build-chunk`, or another `/bs-insert-chunk` if more reshaping is queued).
- Reference Files + "Installed location" footer: `state-machine.md`, `templates/SKETCH.template.md`, `templates/CHUNK.template.md`, `templates/ASSETS.template.md`; same "Installer-phase dependency (Phase 148)" caveat.
No fenced implementation code — directive prose only. Keep thin (≤ ~160 lines).
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/status-tools.test.ts -t "insert-chunk.md sketch editor"</automated>
  </verify>
  <done>STAT-02 editor-block assertions green: 4 ops enumerated, stale marker byte-exact, version-stamp cited correctly (field + Write Order, no phantom heading), op-b scans both citation sections, Mandated-Chunks guard present, correct headings cited & exist.</done>
</task>

<task type="auto">
  <name>Task 3: Retire build-chunk.md's Phase-147 forward-refs + full-suite green</name>
  <files>src/cli/slash-command/bs/build-chunk.md</files>
  <action>Edit the two Step 1 bullets at build-chunk.md lines 58-67 (`grep -n "Phase 147" build-chunk.md` → 63 & 65). Replace the "what's left?" bullet's tail so instead of "the fuller status view ships as `/bs-check-status` (Phase 147); until it lands, this summary IS the status behavior" it points the user at the now-live `/bs-check-status` for the fuller view (the inline summary remains a valid quick answer, but the forward-reference framing is removed). Replace the reordering bullet so instead of "ships as `/bs-insert-chunk` (Phase 147). Until it lands, tell the user reordering is not wired up yet..." it routes reordering/insert intents to the now-live `/bs-insert-chunk` (per No-Backward-Compatibility: remove the stale stopgap, don't leave it). Leave line 75 (already a correct live `/bs-check-status` pointer) and line 347 (stale-marker consumer doc) unchanged. Then run the full status-tools suite and the full test suite.</action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/status-tools.test.ts && npx vitest run src/cli/slash-command/bs/build-chunk.test.ts && npm test</automated>
  </verify>
  <done>build-chunk.md no longer contains "(Phase 147)"/"until it lands" forward-ref phrasing and routes both intents to the live skills; status-tools.test.ts fully green; build-chunk.test.ts still green; `npm test` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| insert-chunk edit ↔ concurrently-resumed build session | insert-chunk MUTATES shared SKETCH.md/CHUNK.md that a build session may have read at an earlier version and could resume against |
| new/edited chunk citations ↔ closed chunks' committed citations | untrusted reshape crosses into already-verified work; overlap could silently invalidate a closed chunk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-03 | Tampering | A build session resumes against a sketch reshaped under it and builds stale structure | mitigate | Op (d) bumps `Sketch Version: N→N+1`; build-chunk.md Step 0/2 detects the version mismatch and re-reads. The version stamp IS the concurrency-safety surface. Pinned by STAT-02 drift assertions (version bump + `## Write Order` cite). |
| T-147-04 | Tampering | Half-written SKETCH/CHUNK state read as valid on a cold resume (torn write) | mitigate | Op ordering cites `## Write Order`: CHUNK.md first, SKETCH.md second, `Status:`/version line last — every intermediate state stays valid-for-cold-resume. Pinned by the write-order drift assertion. |
| T-147-05 | Tampering | insert-chunk silently overwrites an invalidated pending CHUNK.md's content instead of only flipping its Status | mitigate | Skill sets ONLY the `Status:` line to the byte-exact `stale — re-derive before build`; content preserved (RESEARCH anti-pattern). Byte-exact marker pinned. |
| T-147-06 | Tampering | A reshape breaks a `## Mandated Chunks` invariant (final-acceptance off tail / dropped game-end) undetected | mitigate | Op (a) folds in the Mandated-Chunks invariant guard (OQ2); pinned by the Mandated-Chunks drift assertion. |
| T-147-SC | Tampering | npm/pip/cargo installs | N/A | No package installs — vitest already present; no Package Legitimacy Audit required |
| S/R/I/D/E | Spoofing/Repudiation/Info-Disclosure/DoS/Elevation | — | N/A | No auth, network, session, secret, or privilege surface — content-only markdown + local drift test |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/status-tools.test.ts` fully green (STAT-01 + STAT-02).
- `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts` green (forward-ref edit did not break existing pins).
- `npm test` green (final phase gate).
- `grep -n "Phase 147" src/cli/slash-command/bs/build-chunk.md` returns only line ~347 (the unrelated stale-marker consumer doc), never the two Step-1 forward-ref bullets.
</verification>

<success_criteria>
- STAT-02 fully covered: `/bs-insert-chunk` documents all 4 ops, sets the byte-exact stale marker, bumps + correctly cites the version stamp (field + `## Write Order`, no phantom heading), scans both citation sections, and guards the Mandated-Chunks invariants.
- build-chunk.md's two stale Phase-147 forward-references now route to the shipped skills.
- Full `bs/` suite + `npm test` green.
</success_criteria>

<output>
Create `.planning/phases/147-bs-check-status-insert-chunk/147-02-SUMMARY.md` when done.
</output>
