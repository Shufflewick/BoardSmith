---
phase: 148-distribution-installer-bs-generate-ai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/slash-command/bs/ingest-rules.md
  - src/cli/slash-command/bs/build-chunk.md
  - src/cli/slash-command/bs/check-status.md
  - src/cli/slash-command/bs/insert-chunk.md
  - src/cli/slash-command/generate-ai-instructions.md
  - src/cli/slash-command/instructions.md
  - src/cli/slash-command/design-game.template.md
  - src/cli/slash-command/generate-ai.template.md
autonomous: true
requirements: [DIST-01, DIST-02]

must_haves:
  truths:
    - "Each of the 5 bs- entry-point files carries Agent Skills frontmatter (name/description/disable-model-invocation)"
    - "Every bare shared-tree relative reference in the 5 entry points is anchored with ${CLAUDE_SKILL_DIR}/../ so it resolves one level up from the skill's own directory"
    - "bs-generate-ai keeps all 5 AI hooks and gains late-sketch-chunk framing"
    - "design-game source files (instructions.md, design-game.template.md) and the dead generate-ai.template.md no longer exist"
    - "The bs/ drift suites (ingest/build-chunk/status-tools/templates.test.ts) stay green after the edits"
  artifacts:
    - path: "src/cli/slash-command/bs/ingest-rules.md"
      provides: "bs-ingest-rules SKILL body with frontmatter + CLAUDE_SKILL_DIR-anchored refs"
      contains: "name: bs-ingest-rules"
    - path: "src/cli/slash-command/generate-ai-instructions.md"
      provides: "bs-generate-ai SKILL body with frontmatter + late-sketch-chunk framing + 5 hooks preserved"
      contains: "name: bs-generate-ai"
  key_links:
    - from: "src/cli/slash-command/bs/ingest-rules.md"
      to: ".claude/skills/templates/SKETCH.template.md"
      via: "CLAUDE_SKILL_DIR-anchored relative reference"
      pattern: "CLAUDE_SKILL_DIR.*templates/SKETCH.template.md"
---

<objective>
Prepare the SOURCE skill content for the Agent Skills install layout that Plan 02 will copy: give the 5 bs- entry points Claude Code Agent Skills frontmatter, anchor their shared-tree references with `${CLAUDE_SKILL_DIR}/../` so they resolve after the entry points move into per-skill `bs-<name>/` directories, reframe `generate-ai-instructions.md` as `bs-generate-ai`, and delete the dead `/design-game` sources.

Purpose: The installer (Plan 02) is a near-verbatim copy step. All content decisions — frontmatter, reference-path form, hook preservation, dead-file removal — must land in the source tree FIRST so Plan 02 copies final content and Plan 03's references-resolve test passes.

Output: 5 edited entry-point files + reframed bs-generate-ai body; 3 deleted dead source files; bs/ drift suites still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-distribution-installer-bs-generate-ai/148-RESEARCH.md
@.planning/phases/148-distribution-installer-bs-generate-ai/148-PATTERNS.md

<read_first>
- .planning/phases/148-distribution-installer-bs-generate-ai/148-RESEARCH.md — "The Load-Bearing Question" §3 (target layout), Pattern 1 (`${CLAUDE_SKILL_DIR}`-anchored reference), Pitfall 1 (aspects/ depth — DO NOT touch interview-fallback.md), Pitfall 2 (one-level path shift), Code Example "SKILL.md frontmatter"
- .planning/phases/148-distribution-installer-bs-generate-ai/148-PATTERNS.md — "5 bs/*.md entry-point skill files" section (frontmatter block to add, the full grep-for-every-inline-ref instruction), "RENAME generate-ai-instructions.md" section
- src/cli/slash-command/bs/ingest-rules.md — canonical "Reference Files" + "Installed location" shape (lines 192-214) to mirror across the other 3 entry points; also the many inline refs at lines 3,8,18,23,53,56,61,67,82,84,103,105,112,132,134,142,150,167,171,172,177
- src/cli/slash-command/bs/ingest.test.ts — the drift suite; note assertions are substring `toContain('templates/SKETCH.template.md')` / `toContain('ingest/transcription.md')` / `toContain('state-machine.md')` (lines 49-55, 59-208), which SURVIVE a `${CLAUDE_SKILL_DIR}/../` prefix since the substring is still present
- src/cli/slash-command/generate-ai-instructions.md — the 5-hook content (lines 5-15: objectives/threatResponseMoves/playoutPolicy/moveOrdering/uctConstant + Hex reference); note the `---` lines in it are markdown horizontal rules, NOT frontmatter
- .planning/bs-skills-plan.md lines 159-171 (§AI Opponents late-sketch-chunk framing) + line 171 (§Distribution)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add frontmatter + CLAUDE_SKILL_DIR-anchored refs to the 4 authored entry points</name>
  <files>src/cli/slash-command/bs/ingest-rules.md, src/cli/slash-command/bs/build-chunk.md, src/cli/slash-command/bs/check-status.md, src/cli/slash-command/bs/insert-chunk.md</files>
  <action>
For each of the 4 entry-point files:

1. Prepend an Agent Skills frontmatter block (per RESEARCH Code Example "SKILL.md frontmatter"):
   `name: bs-<ingest-rules|build-chunk|check-status|insert-chunk>`, a one-sentence `description` (what it does + "Use when..."), and `disable-model-invocation: true` (these are deliberate designer-triggered workflow steps — RESEARCH Open Question 2 resolves all 5 to disable-model-invocation for this phase). Keep the existing `# ...` H1 title line directly after the frontmatter.

2. Rewrite EVERY backtick-quoted shared-tree relative reference in the body from its bare form to the `${CLAUDE_SKILL_DIR}/../<same-path>` form (RESEARCH Pattern 1 / Pitfall 2). Targets: `ingest/…`, `build/…`, `templates/…`, and `state-machine.md`. Do NOT rewrite refs to game-project artifacts (`rulebook/…`, `chunks/…`, `SKETCH.md`, `CHUNK.md`, `DESIGN.md`, `ASSETS.md`, `INDEX.md`, `PROJECT.md`, `STATE.md`, `HISTORY.md`) — those live in the designer's project, not the install tree. Grep each file for backtick-quoted `ingest/`, `build/`, `templates/`, `state-machine.md` to find every occurrence (not just the trailing "Reference Files" list — see PATTERNS.md's enumerated line list for ingest-rules.md).

3. Update the "Installed location" paragraph in each file so its prose describes the NEW layout: this file installs as `.claude/skills/bs-<name>/SKILL.md`, and the shared `ingest/`, `build/`, `templates/`, `state-machine.md` live one directory up (siblings of the `bs-<name>/` dir), reached via `${CLAUDE_SKILL_DIR}/../`. Preserve the "the installer phase MUST preserve this layout" intent.

Because the drift-suite assertions are substrings (`templates/SKETCH.template.md`, `state-machine.md`, `ingest/transcription.md`), the `${CLAUDE_SKILL_DIR}/../` prefix keeps them green — verify this holds after editing, do not weaken any assertion.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "^name: bs-" src/cli/slash-command/bs/{ingest-rules,build-chunk,check-status,insert-chunk}.md | wc -l` returns 4
    - `grep -c 'disable-model-invocation: true' src/cli/slash-command/bs/ingest-rules.md` >= 1
    - `grep -rc 'CLAUDE_SKILL_DIR' src/cli/slash-command/bs/ingest-rules.md` >= 1 (and same for the other 3)
    - No remaining bare backtick-quoted `` `state-machine.md` `` without a `${CLAUDE_SKILL_DIR}/../` prefix in any of the 4 files: `grep -nE '\`state-machine\.md\`' src/cli/slash-command/bs/{ingest-rules,build-chunk,check-status,insert-chunk}.md` returns only prefixed occurrences (manual scan)
    - `npx vitest run src/cli/slash-command/bs/` is fully green (ingest/build-chunk/status-tools/templates.test.ts all pass)
  </acceptance_criteria>
  <done>All 4 entry points have Agent Skills frontmatter, all shared-tree refs are `${CLAUDE_SKILL_DIR}/../`-anchored, "Installed location" prose updated, bs/ drift suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Reframe generate-ai as bs-generate-ai; delete dead design-game sources</name>
  <files>src/cli/slash-command/generate-ai-instructions.md, src/cli/slash-command/instructions.md, src/cli/slash-command/design-game.template.md, src/cli/slash-command/generate-ai.template.md</files>
  <action>
1. In `generate-ai-instructions.md`: prepend Agent Skills frontmatter (`name: bs-generate-ai`, a description mentioning "AI opponent generator … Use as a late sketch chunk after game-end/scoring exists (MCTS needs terminal states); also for --ai solo-playtest seat-fill", `disable-model-invocation: true`). Add a short framing paragraph near the top positioning it as a late sketch chunk per bs-skills-plan.md §AI Opponents (line 161): invoked after game-end/scoring exists since MCTS needs terminal states, and usable earlier for `--ai` solo-playtest seat-fill. PRESERVE the 5 hooks section (objectives/threatResponseMoves/playoutPolicy/moveOrdering/uctConstant) and the Hex reference verbatim — do NOT restructure the hook list (CONTEXT.md locked: capability preserved, only renamed + repositioned). This body has no BOARDSMITH_ROOT placeholders and operates against the DESIGNER's game project (`npx boardsmith …`), so it needs no install-tree path anchoring.

2. DELETE the dead design-game sources: `git rm src/cli/slash-command/instructions.md src/cli/slash-command/design-game.template.md`. Also `git rm src/cli/slash-command/generate-ai.template.md` — it is the old `{{BOARDSMITH_ROOT}}` thin-pointer template that the Agent Skills model replaces (the reframed `generate-ai-instructions.md` becomes the SKILL body directly; no template wrapper remains).
  </action>
  <verify>
    <automated>test ! -e src/cli/slash-command/instructions.md && test ! -e src/cli/slash-command/design-game.template.md && test ! -e src/cli/slash-command/generate-ai.template.md && grep -c 'name: bs-generate-ai' src/cli/slash-command/generate-ai-instructions.md</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/slash-command/instructions.md`, `design-game.template.md`, `generate-ai.template.md` all absent
    - `generate-ai-instructions.md` contains `name: bs-generate-ai` and all 5 hook names: `grep -cE 'objectives|threatResponseMoves|playoutPolicy|moveOrdering|uctConstant' src/cli/slash-command/generate-ai-instructions.md` >= 5
    - Late-sketch-chunk framing present: `grep -ciE 'late sketch chunk|terminal state|game-end' src/cli/slash-command/generate-ai-instructions.md` >= 1
    - `grep -rc 'design-game' src/cli/slash-command/` returns 0 (source content clean of design-game after the deletes)
  </acceptance_criteria>
  <done>bs-generate-ai body carries frontmatter + late-chunk framing + all 5 hooks; the 3 dead source files are deleted; no design-game string remains under src/cli/slash-command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored markdown → Claude Code runtime | Skill body text the model later reads and acts on; malformed refs cause "file not found" at runtime, not a security breach |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-01 | Tampering | reference-path edits invalidating drift suites | mitigate | Substring-preserving `${CLAUDE_SKILL_DIR}/../` prefix keeps `toContain(...)` assertions green; Task 1 verify runs the full bs/ suite |
| T-148-02 | Information disclosure | residual design-game content shipping | mitigate | Task 2 deletes source files + grep-gate `design-game`=0 under slash-command |
| T-148-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this plan (content edits + file deletes only) |
| T-148-03 | Spoofing / Elevation / Repudiation / DoS | — | N/A | No auth/session/network/input-validation surface — local markdown edits |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/` fully green
- `grep -rc 'design-game' src/cli/slash-command/` == 0
- All 5 skill bodies (4 entry points + bs-generate-ai) carry `name:` frontmatter
</verification>

<success_criteria>
5 skill bodies ready for verbatim copy by Plan 02: frontmatter present, shared-tree refs anchored, hooks preserved, dead design-game/template sources deleted, drift suites green.
</success_criteria>

<output>
Create `.planning/phases/148-distribution-installer-bs-generate-ai/148-01-SUMMARY.md` when done
</output>
