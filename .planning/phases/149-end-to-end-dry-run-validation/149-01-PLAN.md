---
phase: 149-end-to-end-dry-run-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "<scratch>/go-fish-dryrun/**"   # throwaway scratch project; NOT committed; go-fish hand-built repo untouched
autonomous: true
requirements: [VAL-01]
must_haves:
  truths:
    - "A scratch Go Fish project scaffolded by `npx boardsmith init` compiles clean and serves"
    - "The interview-fallback ingest path produces rulebook slices + INDEX + ASSETS + visual survey from standard Go Fish rules"
    - "A SKETCH.md exists whose first chunk is the core event loop (ui: major) and which parses against SKETCH.template.md"
    - "A chunk-1 CHUNK.md and empty RULINGS/DECISIONS ledgers exist, ready for the build-chunk leg"
  artifacts:
    - path: "<scratch>/go-fish-dryrun/package.json"
      provides: "scaffolded project with file: dep on this BoardSmith repo"
      contains: "boardsmith"
    - path: "<scratch>/go-fish-dryrun/rulebook/INDEX.md"
      provides: "ingest rulebook slice index"
    - path: "<scratch>/go-fish-dryrun/SKETCH.md"
      provides: "chunk plan whose chunk 1 = core event loop"
      contains: "core event loop"
    - path: "<scratch>/go-fish-dryrun/chunks/*/CHUNK.md"
      provides: "chunk-1 CHUNK.md scaffold for the build leg"
  key_links:
    - from: "bs/ingest-rules.md"
      to: "SKETCH.template.md"
      via: "Step 7 writes SKETCH.md against the template"
      pattern: "SKETCH"
    - from: "bs/ingest/interview-fallback.md"
      to: "rulebook/INDEX.md"
      via: "interview answers synthesized into rulebook slices"
      pattern: "INDEX"
---

<objective>
Dry-run the INGEST leg of the bs- pipeline against standard Go Fish rules by having subagents FOLLOW THE ACTUAL authored skill instructions — not a hand-waved approximation. Produces a compiling+serving scratch scaffold, rulebook slices + INDEX + ASSETS + visual survey, and a SKETCH.md + chunk-1 CHUNK.md + empty ledgers that the chunk-1 build leg (Plan 02) consumes.

Purpose: Prove the ingest skills are followable end-to-end by a fresh-context LLM and that the artifacts they fill actually parse against the Phase 141 templates — cross-skill friction the per-phase drift tests cannot catch.
Output: A throwaway scratch project with a full ingest output set; ingest observations captured for the Plan 03 dry-run report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-end-to-end-dry-run-validation/149-CONTEXT.md
@.planning/phases/149-end-to-end-dry-run-validation/149-RESEARCH.md

<path_translation_note>
The dry-run reads the skills FROM SOURCE (`src/cli/slash-command/bs/`), not from an installed `.claude/skills/` tree. Every `${CLAUDE_SKILL_DIR}/../bs-shared/X` reference inside a skill file resolves in THIS checkout to `src/cli/slash-command/bs/X` — `ingest/`, `build/`, `templates/`, and `state-machine.md` are direct siblings under `src/cli/slash-command/bs/`. There is NO `bs-shared/` directory in source. Every subagent dispatch prompt MUST state this translation up front, or the subagent will report false "file not found: bs-shared/…" errors. A genuine layout mismatch here would itself be a defect for the Plan 03 report (check against the Phase 148 installer).
</path_translation_note>

<scratch_workspace_note>
Create a session-scoped throwaway scratch PARENT directory (e.g. `mktemp -d`), then run `init` inside it so it creates `<parent>/go-fish-dryrun`. `npx boardsmith init` unconditionally creates `<cwd>/<name>` and ERRORS if it already exists — never run it inside `~/BoardSmithGames/`. Record the chosen scratch path in the SUMMARY so Plan 02/03 can find it. The scratch project uses a `file:` dependency on this BoardSmith repo (mirrors sibling games) so any bs- library fix is picked up without re-vendoring. The hand-built `~/BoardSmithGames/go-fish/` is READ-ONLY reference — never write to it.
</scratch_workspace_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold + verify the scratch project (ingest Step 1)</name>
  <read_first>
    - src/cli/slash-command/bs/ingest-rules.md (Step 0 state detection + Step 1 scaffold)
    - src/cli/slash-command/bs/ingest/scaffold.md (the verification sequence)
    - src/cli/commands/init.ts (directory-creation behavior)
    - src/cli/lib/project-scaffold.ts (generated package.json, axe-core/@vue/test-utils devDeps)
  </read_first>
  <action>Create a throwaway scratch parent dir (`mktemp -d`). Dispatch a fresh-context subagent that FOLLOWS `bs/ingest-rules.md` Step 1 via `bs/ingest/scaffold.md` (give it the path-translation note above). The subagent derives names from designer answer "Go Fish", runs `npx boardsmith init go-fish-dryrun` in the scratch parent, then executes scaffold.md's non-reorderable Verification Sequence for real: (1) `cd go-fish-dryrun && npx tsc --noEmit` clean; (2) `npx boardsmith dev --no-open`, wait for the exact line `Ready! Press Ctrl+C to stop.`, curl the resolved dev URL for a non-error response; (3) KILL the dev server before returning (repo hard rule — never leave a server running). Ensure the scaffold `package.json` uses a `file:` dep on this BoardSmith repo. Record the scratch path. Do NOT read or write anything under `~/BoardSmithGames/`.</action>
  <verify>
    <automated>test -f "$SCRATCH/go-fish-dryrun/package.json" && cd "$SCRATCH/go-fish-dryrun" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `<scratch>/go-fish-dryrun/` exists with a scaffolded project (package.json contains `boardsmith`, axe-core + @vue/test-utils devDeps present)
    - `npx tsc --noEmit` is clean in the scratch project
    - `npx boardsmith dev --no-open` reached `Ready! Press Ctrl+C to stop.`, curl returned non-error, and the server was killed
    - No file under `~/BoardSmithGames/` was modified
  </acceptance_criteria>
  <done>Compiling, serving scratch scaffold exists in a throwaway dir; dev server killed; scratch path recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Ingest interview-fallback → rulebook slices + INDEX + ASSETS + visual survey (Steps 2-3)</name>
  <read_first>
    - src/cli/slash-command/bs/ingest-rules.md (Steps 2-3: input mode + synthesis)
    - src/cli/slash-command/bs/ingest/interview-fallback.md (the chosen input path)
    - src/cli/slash-command/bs/templates/ASSETS.template.md
    - 149-RESEARCH.md "Standard Go Fish Rules" section (the plain-text ruleset to feed)
  </read_first>
  <action>Run the INTERVIEW-FALLBACK ingest path (INGEST-03) — deliberately chosen over transcription because it is the path least exercised by the Phase 142 drift tests and CONTEXT.md names it as acceptable input. Dispatch a fresh-context "designer" subagent scripted with the standard, CLEAN, complete Go Fish ruleset from 149-RESEARCH.md (2-6 players; deal 7 for 2-3 / 5 for 4-6; ask an opponent for a rank you hold; give-all-or-Go-Fish-draw; extra turn on hit or matching draw; 4-of-a-kind = book; 13 books ends, most books wins). Dispatch the ingest subagent that FOLLOWS `bs/ingest-rules.md` Steps 2-3 via `interview-fallback.md`, interviewing the designer subagent and synthesizing answers into `rulebook/NN-topic.md` slices + `INDEX.md` + `ASSETS.md` + `00-visual-survey.md` in the scratch project, using the interview citation format ("designer statement, ingest session, Q{n}"). Feed a clean ruleset (prove-the-pipeline-works framing) but capture for the Plan 03 report any of the four known ambiguities (ask-requires-holding, extra-turn-on-matching-draw, empty-hand-empty-pond end, card-count-by-player-count) that the ingest surfaces despite clean input.</action>
  <verify>
    <automated>test -f "$SCRATCH/go-fish-dryrun/rulebook/INDEX.md" && ls "$SCRATCH/go-fish-dryrun/rulebook/"*.md | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - `rulebook/INDEX.md` + at least two `rulebook/NN-*.md` slices exist covering deal / ask-turn / go-fish-draw / books+win
    - `ASSETS.md` and `00-visual-survey.md` exist
    - Interview-fallback citation format used ("designer statement, ingest session, Q{n}")
    - Ingest observations captured (path followed, any friction, surfaced ambiguities) for the report
  </acceptance_criteria>
  <done>Rulebook slices + INDEX + ASSETS + visual survey produced via the interview-fallback path from the Go Fish ruleset.</done>
</task>

<task type="auto">
  <name>Task 3: Sketch derivation + write files → SKETCH.md + chunk-1 CHUNK.md + ledgers (Steps 4-7)</name>
  <read_first>
    - src/cli/slash-command/bs/ingest-rules.md (Steps 4-7: sketch, UI strategy, approval, write)
    - src/cli/slash-command/bs/ingest/sketch-derivation.md (chunk rules, mandated chunks, ui: tags, sketch-level tail marker)
    - src/cli/slash-command/bs/templates/SKETCH.template.md
    - src/cli/slash-command/bs/templates/CHUNK.template.md
    - src/cli/slash-command/bs/templates/RULINGS.template.md, DECISIONS.template.md
  </read_first>
  <action>Dispatch a subagent that FOLLOWS `bs/ingest-rules.md` Steps 4-7 via `sketch-derivation.md`. Derive the SKETCH: chunk 1 = the core event loop (deal + one ask-a-rank turn resolving to give-cards-or-go-fish + extra-turn rule), tagged `ui: major` (first playable surface); it MUST also list — as mandated sketch-level tail chunks with the exact marker `Status: proposed (sketch-level — no CHUNK.md yet)` — a game-end/scoring/books chunk and a final-acceptance chunk (matching/scoring/win are OUT OF SCOPE to BUILD here, but must appear in the sketch per the Mandated Chunks rule). Record the UI Strategy decision (custom-from-chunk-1 vs autoui-with-cutover). The `ask`-equivalent human approval gate (Step 6) is CAPTURED as an artifact, not blocked — the designer subagent auto-approves. Write files (Step 7): `SKETCH.md`, `chunks/<slug>/CHUNK.md` for chunk 1 only, and empty `RULINGS.md` + `DECISIONS.md` ledgers, all against their templates. Then verify SKETCH.md and CHUNK.md parse against `SKETCH.template.md`/`CHUNK.template.md` (required sections present, marker strings byte-exact).</action>
  <verify>
    <automated>test -f "$SCRATCH/go-fish-dryrun/SKETCH.md" && grep -q "sketch-level — no CHUNK.md yet" "$SCRATCH/go-fish-dryrun/SKETCH.md" && ls "$SCRATCH/go-fish-dryrun/chunks/"*/CHUNK.md | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - `SKETCH.md` exists, chunk 1 = core event loop tagged `ui: major`, and it parses against SKETCH.template.md (required sections present)
    - Mandated game-end/scoring and final-acceptance chunks appear as sketch-level tail entries with the exact `Status: proposed (sketch-level — no CHUNK.md yet)` marker
    - `chunks/<slug>/CHUNK.md` (chunk 1) exists and parses against CHUNK.template.md; empty RULINGS.md + DECISIONS.md ledgers exist
    - Ingest artifact-quality observations (any template-parse friction) captured for the report
  </acceptance_criteria>
  <done>SKETCH.md + chunk-1 CHUNK.md + ledgers written and template-parseable; ingest leg complete, ready for the build leg.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dry-run → hand-built baseline | The read-only `~/BoardSmithGames/go-fish/` must never be written by the ingest leg |
| dry-run → BoardSmith repo | Ingest generates only in the scratch dir; no BoardSmith source is modified in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-01 | Tampering | hand-built go-fish baseline | mitigate | All ingest output goes to the throwaway scratch dir; subagents may READ go-fish only later (Plan 03) and never write it; `init` run in a fresh parent, never inside `~/BoardSmithGames/` |
| T-149-SC | Tampering | npm/scaffold installs | accept | `npx boardsmith init` installs only the scaffold template's pre-pinned devDeps (axe-core/@vue/test-utils, audited Phase 144) + a `file:` dep on this repo; NO new packages added to BoardSmith — package legitimacy N/A |
| T-149-S/R/D/E | Spoofing/Repudiation/DoS/Elevation | — | accept | N/A — local CLI + subagent dry-run, no auth/session/network-facing surface |
</threat_model>

<verification>
- Scratch scaffold compiles (`tsc --noEmit`) and served (dev killed after)
- Rulebook slices + INDEX + ASSETS + visual survey exist (interview-fallback path)
- SKETCH.md (chunk 1 = core event loop, ui: major, mandated tail chunks) + chunk-1 CHUNK.md + ledgers exist and parse against templates
- No file under `~/BoardSmithGames/` modified
</verification>

<success_criteria>
The ingest leg of the pipeline ran end-to-end against Go Fish by following the real skill files, producing a compiling+serving scaffold and a template-parseable ingest output set (rulebook slices, INDEX, ASSETS, visual survey, SKETCH.md, chunk-1 CHUNK.md, empty ledgers), with ingest observations captured for the Plan 03 report.
</success_criteria>

<output>
Create `.planning/phases/149-end-to-end-dry-run-validation/149-01-SUMMARY.md` when done. Record the scratch workspace path, the ingest path followed, and any friction/defects observed (for Plan 03).
</output>
