---
phase: 152-asset-completeness-in-bs-build-chunk
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/slash-command/bs/build/build.md
  - src/cli/slash-command/bs/build/test.md
  - src/cli/slash-command/bs/build-chunk.test.ts
autonomous: true
requirements: [ASSET-01, ASSET-02]

must_haves:
  truths:
    - "build.md's UIQ-02 section prohibits any bare asset <img> and mandates routing card/piece art through AssetImage.vue"
    - "test.md's ordered stop-on-failure sequence gains a build-blocking asset-reachability gate, conditional on ui: touches|major, mirroring the a11y-floor conditional"
    - "The new test.md gate cites asset-scan.ts / scanAssetReachability as the single source of truth — it does not restate scan logic in prose"
    - "build-chunk.test.ts regression-asserts both new prose additions so drift is caught"
  artifacts:
    - path: "src/cli/slash-command/bs/build/build.md"
      provides: "extended UIQ-02 prohibiting bare asset <img>, citing AssetImage.vue"
      contains: "AssetImage"
    - path: "src/cli/slash-command/bs/build/test.md"
      provides: "new ordered asset-reachability gate item (ui: touches|major-conditional)"
      contains: "asset-reachability"
    - path: "src/cli/slash-command/bs/build-chunk.test.ts"
      provides: "UIQ-02 + BUILD-06 prose regression assertions"
  key_links:
    - from: "test.md asset-reachability gate"
      to: "src/cli/lib/asset-scan.ts scanAssetReachability"
      via: "cite-not-restate reference"
      pattern: "asset-scan|scanAssetReachability"
    - from: "build.md UIQ-02"
      to: "AssetImage.vue"
      via: "route-all-art mandate"
      pattern: "AssetImage"
---

<objective>
Wire the ASSET-01/ASSET-02 guarantees into the `bs-build-chunk` skill prose and its regression tests.
Extend `build/build.md`'s UIQ-02 Placeholders section to prohibit any bare asset `<img>` and mandate
routing all card/piece art through `AssetImage.vue`. Add a new build-blocking, `ui: touches|major`-
conditional asset-reachability gate to `build/test.md`'s ordered stop-on-failure sequence (same shape
as the a11y-floor conditional), citing `asset-scan.ts`'s `scanAssetReachability` as the single source
of truth rather than restating scan logic. Extend `build-chunk.test.ts`'s existing UIQ-02 and BUILD-06
describe blocks so the new prose is regression-locked.

Purpose: Make the wrong path (a bare asset `<img>`) hard at authoring time and make an asset-less
chunk fail the pipeline's own bar.
Output: Extended build.md + test.md prose, plus matching skill-guidance assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/152-asset-completeness-in-bs-build-chunk/152-CONTEXT.md
@.planning/phases/152-asset-completeness-in-bs-build-chunk/152-RESEARCH.md

<interfaces>
<!-- Existing prose + test structure to extend. Do NOT restate scan/policy rules; cite them. -->

build/build.md `## Placeholders — UIQ-02 (cite DESIGN.md, never restate)` (~line 76-88):
  Already cites DESIGN.md's ## Placeholder Policy and guarantees a zero-layout-diff asset swap.
  EXTEND here: prohibit a bare asset `<img>`; all card/piece art routes through AssetImage.vue.

build/test.md `## The Ordered Sequence (non-reorderable, stop-on-failure)` (~line 10-76):
  Numbered items 1-6; item 6 is `A11y floor (conditional on ui: touches|major)`. The a11y floor is the
  conditional-shape precedent to mirror. Item 2 (sandbox lint) is the "cite the single-source-of-truth
  scan, run the real command, do not reimplement" precedent.

Contract to cite (from plan 03, same wave — reference by name, prose only, no import):
  src/cli/lib/asset-scan.ts → export function scanAssetReachability(cwd): AssetViolation[]  (non-empty = FAIL)

build-chunk.test.ts describe blocks to extend:
  describe('UIQ-02 — designed placeholders', ...)  (~line 467) — uses read('build/build.md')
  describe('BUILD-06 — test step', ...)            (~line 341) — uses read('build/test.md')
  Helper: `read(relativePath)` returns file contents; assertions use `expect(text).toContain(...)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend build.md UIQ-02 to prohibit bare asset &lt;img&gt; and mandate AssetImage.vue</name>
  <files>src/cli/slash-command/bs/build/build.md</files>
  <read_first>
    - src/cli/slash-command/bs/build/build.md (## Placeholders — UIQ-02 section, ~76-88)
    - .planning/phases/152-asset-completeness-in-bs-build-chunk/152-CONTEXT.md (Fallback Strategy decisions)
  </read_first>
  <action>
    In the `## Placeholders — UIQ-02` section, add prose stating that a chunk's UI MUST NEVER emit a bare
    asset `<img>` for card/piece art: all such art routes through the scaffold-shipped `AssetImage.vue`
    component (emitted by `project-scaffold.ts` into every project from Chunk 0), which draws the game-semantic
    fallback and overlays the real image only on load. Keep citing DESIGN.md's `## Placeholder Policy` by name
    (do not restate its rules) — frame `AssetImage.vue` as the operationalization of that policy and the reason
    the zero-layout-diff guarantee holds structurally. State that a bare asset `<img>` is a test-step failure
    (forward-reference the test.md gate) so the prohibition is enforced, not merely advised.
  </action>
  <verify>
    <automated>grep -q "AssetImage" src/cli/slash-command/bs/build/build.md && grep -qi "bare" src/cli/slash-command/bs/build/build.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - build.md's UIQ-02 section names `AssetImage.vue` and prohibits a bare asset `<img>`.
    - It still cites `## Placeholder Policy` by name (does not restate DESIGN.md's rules).
    - It references the test.md gate as the enforcement mechanism.
  </acceptance_criteria>
  <done>UIQ-02 mandates routing all card/piece art through AssetImage.vue and prohibits bare asset &lt;img&gt;, enforcement forward-referenced to the test gate.</done>
</task>

<task type="auto">
  <name>Task 2: Add the asset-reachability gate to test.md's ordered sequence</name>
  <files>src/cli/slash-command/bs/build/test.md</files>
  <read_first>
    - src/cli/slash-command/bs/build/test.md (## The Ordered Sequence, items 1-6; item 2 sandbox-lint cite pattern; item 6 a11y-floor conditional shape)
    - .planning/phases/152-asset-completeness-in-bs-build-chunk/152-CONTEXT.md (Automated Check decisions)
  </read_first>
  <action>
    Add a new numbered item to the ordered, non-reorderable, stop-on-failure sequence: an
    **asset-reachability gate**, conditional on `ui: touches|major` (a `ui: none` chunk skips it, mirroring the
    a11y-floor conditional's exact shape). The gate runs `scanAssetReachability(cwd)` from
    `src/cli/lib/asset-scan.ts` — cite it as the single source of truth exactly as item 2 cites
    `sandbox-scan.ts` ("run the real check, do not reimplement or duplicate this scan"). State the failure
    semantics: any bare asset `<img>` outside `AssetImage.vue` is a build-blocking FAIL that routes the chunk
    back to `repair` (same discipline as every other item), never silently worked around. Do NOT restate the
    scan's internal heuristic in prose — cite the module. Place the item so it reads as part of the same
    numbered sequence and note it is build-blocking.
  </action>
  <verify>
    <automated>grep -qi "asset-reachability" src/cli/slash-command/bs/build/test.md && grep -q "asset-scan\|scanAssetReachability" src/cli/slash-command/bs/build/test.md && grep -qi "touches" src/cli/slash-command/bs/build/test.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - test.md contains a new ordered, build-blocking asset-reachability gate item.
    - It is conditional on `ui: touches|major` (mirrors the a11y-floor conditional).
    - It cites `asset-scan.ts` / `scanAssetReachability` as the single source of truth and does not restate scan logic.
    - Failure routes the chunk back to `repair`.
  </acceptance_criteria>
  <done>test.md's ordered sequence includes the ui-conditional, build-blocking asset-reachability gate citing the asset-scan module.</done>
</task>

<task type="auto">
  <name>Task 3: Extend build-chunk.test.ts UIQ-02 + BUILD-06 blocks with the new prose assertions</name>
  <files>src/cli/slash-command/bs/build-chunk.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/build-chunk.test.ts (describe('UIQ-02 — designed placeholders') ~467, describe('BUILD-06 — test step') ~341, read() helper ~37)
    - src/cli/slash-command/bs/build/build.md (post-Task-1 content)
    - src/cli/slash-command/bs/build/test.md (post-Task-2 content)
  </read_first>
  <behavior>
    - `-t "bare.*img|AssetImage"`: an assertion in the UIQ-02 block that build.md names AssetImage.vue and prohibits a bare asset `<img>`.
    - `-t "asset-reachability"`: an assertion in the BUILD-06 block that test.md contains the asset-reachability gate, cites asset-scan/scanAssetReachability, and is `ui: touches|major`-conditional.
  </behavior>
  <action>
    In the existing `describe('UIQ-02 — designed placeholders', ...)` block add an `it` (title matching
    `bare.*img|AssetImage`) asserting `read('build/build.md')` contains `AssetImage` and the bare-`<img>`
    prohibition. In the existing `describe('BUILD-06 — test step', ...)` block add an `it` (title matching
    `asset-reachability`) asserting `read('build/test.md')` contains the gate name, cites
    `asset-scan`/`scanAssetReachability`, and includes the `touches` conditional. Use the existing `read()`
    helper and `expect(...).toContain(...)` style already used in these blocks.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts` exits 0.
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "AssetImage"` matches and passes.
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts -t "asset-reachability"` matches and passes.
    - New assertions live inside the existing UIQ-02 and BUILD-06 describe blocks.
  </acceptance_criteria>
  <done>build-chunk.test.ts regression-locks both new prose additions; the skill-guidance suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chunk-authoring LLM → generated UI source | Prohibition is enforced by the build-blocking test.md gate, not by trust in the author |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-04 | Repudiation (silent scope-drift) | build.md/test.md prose | mitigate | build-chunk.test.ts regression-asserts the new prose so a future edit that removes the prohibition/gate fails CI |
| T-152-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages (prose + test edits only) |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts` green.
- `npm test` (repo full suite) stays green after this plan's wave.
</verification>

<success_criteria>
build.md prohibits bare asset `<img>` and routes art through AssetImage.vue; test.md carries a
build-blocking, ui-conditional asset-reachability gate citing the asset-scan module; both are
regression-locked in build-chunk.test.ts.
</success_criteria>

<output>
Create `.planning/phases/152-asset-completeness-in-bs-build-chunk/152-04-SUMMARY.md` when done
</output>
