---
phase: 152-asset-completeness-in-bs-build-chunk
plan: 05
type: checkpoint
wave: 2
depends_on: [152-01, 152-02, 152-03, 152-04]
files_modified: []
autonomous: false
requirements: [ASSET-01]

must_haves:
  truths:
    - "A Go Fish game whose card art is rendered through the fixed scaffold path shows every card with naturalWidth > 0 and zero broken <img> in a real browser, without any hand-added asset set"
    - "The proof is a real-browser observation (not a jsdom unit test), because jsdom cannot decode real images (Pitfall 4)"
  artifacts:
    - path: ".planning/phases/152-asset-completeness-in-bs-build-chunk/152-05-SUMMARY.md"
      provides: "recorded browser-verification result (per-card naturalWidth, broken-img count, screenshot or console tally)"
  key_links:
    - from: "fixed scaffold (AssetImage.vue + AutoUI guards)"
      to: "zero broken <img> in a running Go Fish"
      via: "regenerate/rewire Go Fish card art through the fallback wrapper, browser-verify"
      pattern: "naturalWidth"
---

<objective>
Perform ROADMAP.md Phase 152 Success Criterion 1 — the real-browser regeneration proof — which none
of the autonomous plans (152-01..04) can perform because jsdom cannot decode real images (Pitfall 4).
This is the literal phase goal: a `bs-build-chunk`-generated game renders zero broken `<img>` for card
art in a real browser, with no hand-added asset set.

Purpose: Close DEF-A's exact failure class end-to-end in the browser (BoardSmith CLAUDE.md: "confirm
features work end-to-end in the browser before marking work complete"), not just in unit tests.
Output: A recorded browser-verification result in 152-05-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/152-asset-completeness-in-bs-build-chunk/152-CONTEXT.md
@.planning/phases/152-asset-completeness-in-bs-build-chunk/152-RESEARCH.md

<interfaces>
<!-- DEF-A repro + fix references -->
- The DEF-A failure: `~/BoardSmithGames/go-fish-dryrun/src/ui/components/GameTable.vue` renders a bare
  `<img v-if="cardImage(...)" :src="cardImage(...)">` with no load/error guard; Vite's SPA fallback
  served index.html (HTTP 200) for the missing `/cards/*.svg`, so every card rendered broken.
- The fix under test: the scaffold now ships `AssetImage.vue` (plan 152-01) and the AutoUI renderers
  are guarded (plan 152-02). Card art routed through the fallback wrapper must NEVER show a broken
  image, even when the asset file is absent — the drawn rank+suit fallback shows through instead.
- The `149-HUMAN-UAT.md` proof shape: `naturalWidth > 0` per card `<img>`, counting broken images.
- Browser testing: use the Claude Code Chrome extension (per user CLAUDE.md). Real-time SPA — wait for
  `domcontentloaded`/a card selector, never network-idle (it never fires with live WS/polling).
</interfaces>
</context>

<tasks>

<task type="checkpoint">
  <name>Task 1: Browser-verify Go Fish card art renders with zero broken <img></name>
  <files></files>
  <read_first>
    - .planning/milestones/v4.6-phases/149-end-to-end-dry-run-validation/149-HUMAN-UAT.md (the DEF-A proof script shape — naturalWidth check)
    - src/cli/lib/project-scaffold.ts (confirm AssetImage.vue is emitted by generateScaffoldFiles after plan 152-01)
    - ~/BoardSmithGames/go-fish/src/ui/components/GameTable.vue (how card art is currently rendered)
  </read_first>
  <action>
    Prove the fix in a real browser against Go Fish. Two acceptable routes — pick the one that best
    matches the fixed scaffold path without hand-adding assets:
    (a) Regenerate Go Fish's card-art UI through the fixed scaffold (so card `<img>`s route through the
        emitted `AssetImage.vue` wrapper), OR
    (b) Rewire Go Fish's existing GameTable card render to go through the scaffold `AssetImage.vue`
        wrapper (the sanctioned no-broken-image path), matching what a freshly `bs-build-chunk`-generated
        game would produce.
    Then: `cd ~/BoardSmithGames/go-fish && npx boardsmith dev --no-open` (serves :5173). In the browser
    (Chrome extension), open a seat, wait for a card selector (NOT network-idle), and tally every card
    `<img>`'s `naturalWidth`. Critically: verify the zero-broken-image guarantee holds WITHOUT a
    hand-added asset set — i.e. temporarily confirm that even with the `/cards/*.svg` assets absent, the
    drawn rank+suit fallback renders (no broken `<img>`), then (if assets are present) that real art
    reveals on load. Record per-card naturalWidth results and the broken-image count (must be 0). Capture
    a screenshot or console tally. KILL the dev server (:5173) when done — never leave it running.
  </action>
  <verify>
    <manual>Every card renders either real art (naturalWidth > 0) or the drawn rank+suit fallback; zero broken <img> elements; no hand-added asset set was required to avoid broken images. Result recorded in 152-05-SUMMARY.md.</manual>
  </verify>
  <acceptance_criteria>
    - Zero broken `<img>` elements observed in a running Go Fish in a real browser.
    - The zero-broken guarantee is demonstrated to NOT depend on a hand-added asset set (drawn fallback shows through when the asset file is absent).
    - Per-card `naturalWidth` tally and broken-image count (0) recorded in 152-05-SUMMARY.md, with a screenshot or console tally.
    - The dev server started for this check is confirmed killed.
  </acceptance_criteria>
  <done>A real browser confirms Go Fish card art renders with zero broken images through the fixed scaffold path, no hand-added assets, and the result is recorded.</done>
</task>

</tasks>

<verification>
- 152-05-SUMMARY.md records a zero broken-`<img>` result from a real browser against Go Fish.
- No dev server left running (:5173 killed).
</verification>

<success_criteria>
ROADMAP.md Phase 152 Success Criterion 1 is satisfied: regenerating/rewiring a game via the fixed
`bs-build-chunk` scaffold path produces zero broken `<img>` elements when browser-verified, without
hand-adding an asset set.
</success_criteria>

<output>
Create `.planning/phases/152-asset-completeness-in-bs-build-chunk/152-05-SUMMARY.md` when done
</output>
</content>
