---
phase: 160-simultaneous-step-correctness
plan: 02
type: execute
wave: 2
depends_on: [160-01]
files_modified:
  - src/session/utils.ts
  - src/session/state-history.ts
  - src/session/stateless-ops.ts
  - src/session/testing/simultaneous-undo.test.ts
  - src/session/testing/parity-contract.test.ts
autonomous: true
requirements: [SIM-02, PROC-01]

must_haves:
  truths:
    - "Seat-2 can undo its own committed simultaneous action even though it is not flowState.currentPlayer — the undo is no longer pinned to seat-1"
    - "Undoing seat-2's simultaneous action leaves seat-1's committed action (and every co-decider's) intact"
    - "The rewind boundary comes from seat-2's own committed action in history, not the turn-wide moveCount"
    - "Phase 155's assertUndoAllowed fences (.notUndoable / finished / execute-barrier) still apply per-seat and are never bypassed for simultaneous undo"
    - "The stateless and stateful executors make the identical per-seat undo decision (parity)"
  artifacts:
    - path: "src/session/utils.ts"
      provides: "Awaiting-aware undo eligibility + per-seat rewind-boundary helper reused by both executors"
      contains: "canSeatAct"
    - path: "src/session/state-history.ts"
      provides: "undoToTurnStart / rewindToAction accept any awaiting seat, not just currentPlayer"
    - path: "src/session/stateless-ops.ts"
      provides: "handleUndo / handleDebugRewind accept any awaiting seat, not just currentPlayer"
    - path: "src/session/testing/simultaneous-undo.test.ts"
      provides: "SIM-02 regression — non-seat-1 undo works, co-deciders untouched, fences hold"
  key_links:
    - from: "src/session/utils.ts"
      to: "canSeatAct"
      via: "undo eligibility reuses the awaiting-aware seat predicate"
      pattern: "canSeatAct"
    - from: "src/session/stateless-ops.ts"
      to: "src/session/state-history.ts"
      via: "both executors call the same shared eligibility + boundary helper (parity)"
      pattern: "canSeatAct|simultaneousUndoBoundary"
---

<objective>
Close SIM-02 (D4): a simultaneous-step undo is pinned to `flowState.currentPlayer` (seat-1), so any
other awaiting seat cannot undo its own committed action. Replace the `currentPlayer` gate with
awaiting-aware eligibility and compute the rewind boundary from the requesting seat's OWN committed
action, not the turn-wide `moveCount`.

Purpose: in a simultaneous step each seat's action is independent; a co-decider must be able to take
back its own commit without disturbing the others. Today `canUndo` (`utils.ts:317`), both executors
(`state-history.ts:281`, `stateless-ops.ts:467`), and `computeUndoInfo` (`utils.ts:174-195`, which
bails when `currentPlayer === undefined` and uses `moveCount`) all assume a single sequential actor.

Depends on Plan 01 (D3): restoring a correct per-seat `completed` map only works once `getState()`
stops aliasing the live array; this plan also imports the `simultaneous-fixture.ts` created there.

Output: an awaiting-aware eligibility predicate + a per-seat rewind-boundary helper in `utils.ts`,
reused by BOTH executors (parity), with Phase 155's `assertUndoAllowed` fences layered on top per-seat,
plus RED→GREEN regression and parity/adversarial verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-simultaneous-step-correctness/160-CONTEXT.md
@.planning/phases/160-simultaneous-step-correctness/160-RESEARCH.md
@.planning/phases/155-undo-rewind-family-correctness/155-CONTEXT.md

@src/session/utils.ts
@src/session/state-history.ts
@src/session/stateless-ops.ts
@src/engine/flow/seat-activity.ts
@src/session/testing/parity-contract.test.ts
@src/session/testing/fixtures/simultaneous-fixture.ts
</context>

<interfaces>
<!-- Extracted from the codebase. Use these directly; do not go exploring. -->

`src/engine/flow/seat-activity.ts:58` — REUSE this, it is already awaiting-aware:
```
export function canSeatAct(flowState: SeatActivityState | undefined | null, seat: number): boolean
// simultaneous: any awaiting seat not completed with actions; sequential: currentPlayer
```

`src/session/utils.ts:174` `computeUndoInfo(actionHistory, currentPlayer, moveCount)` — bails when
`currentPlayer === undefined` (`:179`) and scopes the boundary to `actionHistory.length - moveCount`
(`:186`). In a simultaneous step `currentPlayer` is a single/undefined seat and `moveCount` is
turn-wide → wrong for a co-decider. Do NOT break its existing sequential contract (UNDO-03 relies on
it); add a per-seat path (a new helper or an awaiting-aware branch) rather than repurposing moveCount.

`src/session/utils.ts:310-317` `buildPlayerState` — computes `canUndo`:
```
const canUndo = isMyTurn && actionsThisTurn > 0 && flowState?.currentPlayer === playerPosition && !hasNonUndoableAction;
```
The `flowState?.currentPlayer === playerPosition` clause is the advisory pin to relax for awaiting seats.

`src/session/utils.ts:254` `assertUndoAllowed({ game, actionHistory, turnStartActionIndex, executeBarrierIndex })`
— Phase 155's server-side fence (finished-phase / non-undoable / execute-barrier). Layer it on top of
the per-seat boundary; NEVER bypass it for simultaneous undo.

`src/session/state-history.ts:271` `undoToTurnStart(playerPosition)` — pin at `:281`
(`flowState?.currentPlayer !== playerPosition → "It's not your turn"`); computes boundary via
`computeUndoInfo(..., flowState.moveCount)` at `:289`; already calls `assertUndoAllowed` at `:315`
inside a try/catch. `rewindToAction` at `:366` calls `assertUndoAllowed` at `:401`.

`src/session/stateless-ops.ts:449` `handleUndo` — pin at `:467`
(`flowState?.currentPlayer !== op.player → NOT_YOUR_TURN`); boundary via `computeUndoInfo(..., flowState.moveCount)`
at `:475`; `assertUndoAllowed` at `:490`. `handleDebugRewind` at `:992` calls `assertUndoAllowed` at `:1020`.

Action-history entries carry `{ player: number; undoable?: boolean; name?: string }` (see
`computeUndoInfo`/`assertUndoAllowed` signatures) — the per-seat boundary is derivable by finding the
requesting seat's own committed simultaneous action(s) in `runner.actionHistory` by `player`.
</interfaces>

<decisions>
## Locked decisions implemented here

- **D4 (any-seat):** replace the `currentPlayer` gate with awaiting-aware eligibility — allow undo for
  any seat that `canSeatAct` reports as awaiting (or was awaiting and has committed this step), not just
  `flowState.currentPlayer`. Sites: `canUndo` (`utils.ts:317`), `undoToTurnStart` (`state-history.ts:281`),
  `handleUndo` (`stateless-ops.ts:467`), and `computeUndoInfo`'s `currentPlayer === undefined` bail.
- **D4 (per-seat scope):** a simultaneous undo rewinds ONLY the requesting seat's own committed
  action(s) this step — co-deciders' actions are left intact. Compute the boundary from that seat's own
  action in `actionHistory` (by `player`), NOT the turn-wide `moveCount` (which assumes sequential turns).
- **D4 (fences hold):** Phase 155's `assertUndoAllowed` (`.notUndoable()` / `finished` /
  `executeBarrierIndex`) is layered on top of the per-seat boundary and is NEVER bypassed for
  simultaneous undo.
- **Discretion (parity):** put the awaiting-aware eligibility predicate AND the per-seat boundary
  computation in a shared helper in `utils.ts` (preferred), consumed by BOTH executors — so the two
  cannot drift (the parity contract this repo enforces). Do NOT patch one executor without its twin.
- **No conflation:** this is the ENGINE/session undo path (fixed here, on Plan 01's D3 checkpoint fix);
  the MCTS `undoCommands` gap is separate (`v4.8-MCTS-UNDO`, Phase 169).
- Sequential-turn undo behavior (non-simultaneous steps) is UNCHANGED — the awaiting-aware branch only
  applies when `flowState.awaitingPlayers?.length > 0`.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): non-seat-1 simultaneous undo regression, proven failing</name>
  <files>src/session/testing/simultaneous-undo.test.ts</files>
  <behavior>
    - Drive `simultaneous-fixture.ts` so both seats commit their independent simultaneous action; then
      seat-2 sends `{type:'undo', player:2}`. Pre-fix it is REFUSED ("It's not your turn" — pinned to
      currentPlayer); post-fix it succeeds.
    - After seat-2's undo, seat-1's committed action is untouched (seat-1 still `completed`, its state
      unchanged) and seat-2 is awaiting again.
    - The stateful twin (`GameSession` / `undoToTurnStart(2)`) behaves identically.
    - Fence still holds: a `.notUndoable()` simultaneous action, or an undo attempted once the game is
      `finished`, is still REFUSED per-seat (Phase 155 fence not bypassed).
    - Negative control: seat-1's own simultaneous undo still works (we didn't break seat-1).
  </behavior>
  <action>
Create `src/session/testing/simultaneous-undo.test.ts` importing `simultaneous-fixture.ts` (from Plan
01). Drive the stateless path with `createHeadlessSession(...).send(seat, {type:'undo', player:seat})`
and the stateful path with `GameSession.create(...)` + `undoToTurnStart(seat)` (see
`parity-contract.test.ts` for both harnesses). If the fixture's simultaneous actions are all undoable,
add a `.notUndoable()` variant action (or a finished-phase path) to prove the fence-still-holds case
without bypassing `assertUndoAllowed`.

Write against CURRENT source (Plan 01's engine fix is in; the session pin is NOT). Assert on the
REFUSAL/message pre-fix and on the co-decider-untouched invariant. Capture the ACTUAL RED output
verbatim (PROC-01 — seat-2 undo refused for the RIGHT reason: the currentPlayer pin, NOT a missing
symbol). Commit test-only; write NO session fix in this task. Note the negative-control (seat-1 undo)
and fence-holds results in the commit body.
  </action>
  <verify>
    <automated>npx vitest run src/session/testing/simultaneous-undo.test.ts 2>&1 | tee /tmp/160-02-red.txt; grep -q "FAIL" /tmp/160-02-red.txt</automated>
  </verify>
  <done>Test file exists, runs, and FAILS because seat-2's simultaneous undo is refused by the currentPlayer pin. RED captured verbatim in the commit message. No production source modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): awaiting-aware eligibility + per-seat boundary helper, wired at all sites</name>
  <files>src/session/utils.ts, src/session/state-history.ts, src/session/stateless-ops.ts</files>
  <behavior>
    - A shared helper in `utils.ts` answers "may seat N undo now?" (awaiting-aware via `canSeatAct`,
      relaxing the currentPlayer pin) and computes seat N's per-seat rewind boundary from its own
      committed action(s) in history.
    - `canUndo` (buildPlayerState), `undoToTurnStart`, `handleUndo` all accept an awaiting seat, not
      just `currentPlayer`, and rewind only that seat's own action(s).
    - `assertUndoAllowed` is still called on the per-seat boundary at every site — fences intact.
    - Sequential (non-simultaneous) undo behavior is unchanged.
  </behavior>
  <action>
Add to `src/session/utils.ts` a shared helper (Claude's discretion on exact shape, e.g.
`simultaneousUndoBoundary(flowState, actionHistory, seat)` returning the per-seat
`turnStartActionIndex`/`actionsThisTurn`, plus an eligibility check reusing `canSeatAct`). When
`flowState.awaitingPlayers?.length > 0`, eligibility is `canSeatAct(flowState, seat)` (or awaited-and-
committed) rather than `currentPlayer === seat`, and the boundary is derived from the seat's own
`player`-matched action(s) in `actionHistory` — NOT `moveCount`. Leave `computeUndoInfo`'s sequential
contract intact; branch on simultaneous.

Wire ALL sites (parity — a change to `stateless-ops.ts` without its `state-history.ts` twin is the
failure mode the parity contract exists to prevent):
- `utils.ts:317` `buildPlayerState` — relax the `flowState?.currentPlayer === playerPosition` clause to
  the awaiting-aware predicate; feed the per-seat boundary into `canUndo`.
- `state-history.ts:281` `undoToTurnStart` — replace the currentPlayer pin with the awaiting-aware
  eligibility; use the per-seat boundary for the rewind target; keep the `assertUndoAllowed` call
  (`:315`) on that boundary.
- `stateless-ops.ts:467` `handleUndo` — same relaxation; per-seat boundary; keep `assertUndoAllowed`
  (`:490`).
Keep `handleDebugRewind` / `rewindToAction` targeting explicit indices as today (they take an explicit
target; just ensure the awaiting-seat case isn't newly rejected). Undo restores seat N's OWN
checkpoint; co-deciders' committed actions remain in history.

Run Task 1 to GREEN. Record before/after for the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/session/testing/simultaneous-undo.test.ts</automated>
  </verify>
  <done>Task 1 test passes. Any awaiting seat can undo its own simultaneous action; co-deciders untouched; boundary is per-seat; fences intact; sequential undo unchanged. Both executors updated in parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: parity assertions + adversarial verification</name>
  <files>src/session/testing/parity-contract.test.ts</files>
  <behavior>
    - Stateless and stateful executors return the SAME decision and message for: seat-2 simultaneous
      undo (allowed), seat-2 undo across a `.notUndoable()` simultaneous action (refused), seat-2 undo
      once finished (refused).
    - Adversarial: a raw `{type:'undo', player:2}` op sent without consulting `canUndo` succeeds only
      when the per-seat boundary + fences allow it.
    - Adversarial: seat-2 cannot use undo to rewind seat-1's action (attempt a boundary that would
      cross a co-decider's committed action → refused / no-op on seat-1).
    - Adversarial: the debug-rewind twins do not become a bypass for the per-seat fence.
  </behavior>
  <action>
Extend `parity-contract.test.ts` with a simultaneous `describe` block driving `simultaneous-fixture`
through BOTH executors and asserting decision + message equality (the locked drift detector). Add the
adversarial cases above — each hand-builds the op, never reads `state.canUndo` first, and asserts the
attempt is bounded to the requesting seat and cannot cross into a co-decider's action or past a fence.
Then run the full suite; the Phase 155 undo-authoritative tests and Plan 01's engine tests must stay
GREEN (if a Phase 155 test goes red, the awaiting-aware branch leaked into the sequential path —
investigate).
  </action>
  <verify>
    <automated>npx vitest run src/session/testing/parity-contract.test.ts && npm test</automated>
  </verify>
  <done>Parity block green; all adversarial attempts correctly bounded/refused; full suite at or above the pre-phase baseline pass count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → session op handler (`executeOp`) | Untrusted `{type:'undo', player:N}` op literals cross here; the client's `canUndo` is advisory only |
| co-decider seat → another seat's committed action | A seat could attempt to undo past its own boundary into a co-decider's committed simultaneous action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-04 | Elevation of Privilege | `handleUndo` / `undoToTurnStart` — relaxing the currentPlayer pin could let a seat rewind BEYOND its own action into a co-decider's committed state | mitigate | Per-seat boundary derived from the requesting seat's OWN `player`-matched action only; adversarial test proves seat-2 cannot cross into seat-1's action (Task 3) |
| T-160-05 | Tampering | Simultaneous undo bypassing Phase 155's `.notUndoable()` / finished / execute-barrier fences | mitigate | `assertUndoAllowed` still called on the per-seat boundary at every site (Task 2); fence-holds regression + adversarial rewind-twin cases (Tasks 1, 3) |
| T-160-06 | Repudiation | A per-seat undo refusal returning a silent no-op leaves the seat unaware | mitigate | Refusal carries the existing actionable messages (NOT_YOUR_TURN relaxed, UNDO_NOT_ALLOWED preserved); parity asserts message equality (Task 3) |
| T-160-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase (RESEARCH.md: no new libraries) |
</threat_model>

<verification>
- `npx vitest run src/session/testing/simultaneous-undo.test.ts src/session/testing/parity-contract.test.ts`
- `npm test` — full suite at or above pre-phase baseline
- Grep gate: undo eligibility routes through the awaiting-aware predicate —
  `grep -v '^\s*\*' src/session/utils.ts | grep -c 'canSeatAct'` returns >= 1
- Grep gate: the fence is still called from both executors (not bypassed for simultaneous) —
  `grep -c 'assertUndoAllowed' src/session/state-history.ts src/session/stateless-ops.ts` returns >= 4
</verification>

<success_criteria>
- Any awaiting seat can undo its own committed simultaneous action; not just seat-1.
- The rewind is scoped to the requesting seat; co-deciders' actions remain intact.
- The boundary comes from that seat's own history action, not the turn-wide moveCount.
- Phase 155's fences still refuse notUndoable / finished / barrier-crossing per-seat.
- Stateless and stateful executors agree on decision and message.
- PROC-01 satisfied: RED observed and captured before GREEN; adversarial bounds proven.
</success_criteria>

<output>
Create `.planning/phases/160-simultaneous-step-correctness/160-02-SUMMARY.md` when done.
Include the verbatim RED (seat-2 refused) and GREEN output (PROC-01), and confirmation that the
sequential undo path (Phase 155 tests) stayed green.
</output>
