---
phase: 164-library-misc-action-panel-loop-visual-debug-view
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/action/types.ts
  - src/engine/action/action-builder.ts
  - src/engine/element/action-metadata.ts
  - src/engine/element/action-metadata.test.ts
  - src/session/types.ts
  - src/ui/components/auto-ui/ActionPanel.vue
  - src/ui/components/auto-ui/ActionPanel.dock-suppression.test.ts
  - src/ui/components/GameShell.vue
  - src/ui/components/GameShell.action-panel-suppression.test.ts
  - src/ui/composables/useBoardActionBridge.ts
autonomous: true
requirements: [LIBX-01, PROC-01]

must_haves:
  truths:
    - "An action declared with .suppressFromDock() is absent from ActionPanel's rendered dock buttons but still present in availableActions and executable via the board substrate (useBoardInteraction)"
    - "When every dock action is suppressed, GameShell falls back to the bare turn-prompt strip (never zero turn indicator)"
    - "The blunt all-or-nothing prop is renamed to platformActionPanelEscapeHatch and still fully suppresses the panel under the new name"
    - "platformActionPanelEscapeHatch is documented as a platform-only escape hatch and de-documented from the ordinary author-facing bridge/scaffold surface"
  artifacts:
    - path: "src/engine/action/action-builder.ts"
      provides: ".suppressFromDock() builder method (mirrors .manual())"
      contains: "suppressFromDock"
    - path: "src/engine/element/action-metadata.ts"
      provides: "buildActionMetadata threads suppressFromDock into ActionMetadata"
      contains: "suppressFromDock"
    - path: "src/ui/components/auto-ui/ActionPanel.vue"
      provides: "visibleActions filters out suppressFromDock actions"
      contains: "suppressFromDock"
    - path: "src/ui/components/GameShell.vue"
      provides: "platformActionPanelEscapeHatch prop + allDockActionsSuppressed fallback"
      contains: "platformActionPanelEscapeHatch"
  key_links:
    - from: "src/engine/action/action-builder.ts"
      to: "src/engine/element/action-metadata.ts"
      via: "definition.suppressFromDock → ActionMetadata.suppressFromDock spread"
      pattern: "suppressFromDock"
    - from: "src/ui/components/auto-ui/ActionPanel.vue"
      to: "actionsWithMetadata"
      via: "visibleActions .filter(a => !a.suppressFromDock)"
      pattern: "filter.*suppressFromDock"
    - from: "src/ui/components/GameShell.vue"
      to: "allDockActionsSuppressed"
      via: "turn-strip v-if OR escape hatch; ActionPanel mount v-if negation"
      pattern: "allDockActionsSuppressed"
---

<objective>
LIBX-01 (D28 + C.3-lib): Make action-panel/dock suppression per-action (declared on the action definition, flowing through actionMetadata exactly like the existing `.manual()` channel) instead of all-or-nothing, and fence the blunt whole-panel prop behind a loud platform-only escape hatch (`platformActionPanelEscapeHatch`).

Purpose: The action owns its dock presentation (Pit of Success); a suppressed action stays fully executable via the board/custom UI — only the dock *button* disappears. The old blunt prop is renamed loud, documented platform-only, and Phase 166 (SKILLDEF-03) will reference the new name.
Output: `suppressFromDock` on ActionDefinition/builder/ActionMetadata, ActionPanel dock filter, GameShell all-suppressed turn-prompt fallback + prop rename, PROC-01 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-library-misc-action-panel-loop-visual-debug-view/164-CONTEXT.md
@.planning/phases/164-library-misc-action-panel-loop-visual-debug-view/164-RESEARCH.md

<interfaces>
<!-- Orchestrator resolution 1: rename suppressActionPanel → platformActionPanelEscapeHatch;
     per-action suppression = NEW suppressFromDock on ActionDefinition, mirroring `manual` EXACTLY. -->
Existing `manual` precedent to mirror (three layers):
  - src/engine/action/types.ts:459-463  → `manual?: boolean` on ActionDefinition
  - src/engine/action/action-builder.ts:164-167 → `manual(): this { this.definition.manual = true; return this; }`
  - src/engine/element/action-metadata.ts:71-76 → `...(actionDef.manual ? { manual: true } : {})`
  - src/session/types.ts:396-405 → ActionMetadata { manual?: boolean }
ActionPanel filter point:
  - src/ui/components/auto-ui/ActionPanel.vue:166-168 → `const visibleActions = computed(() => { return actionsWithMetadata.value; });` (passthrough)
  - button render loops `v-for="action in visibleActions"` at :815
GameShell escape-hatch + fallback anchors:
  - GameShell.vue:140 prop decl `suppressActionPanel?: boolean`; :177 default; :2425 turn-strip v-if; :2436 ActionPanel-mount v-if
  - NOTE GameShell already history-guards ActionPanel props at :2439-2447 (isViewingHistory ? [] : ...) — do not disturb; LIBX-04 (plan 04) owns history.
Bridge doc:
  - src/ui/composables/useBoardActionBridge.ts:15 doc-comment references `suppressActionPanel` — update to new name + platform-only note.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Engine per-action suppressFromDock channel (mirror `manual`)</name>
  <read_first>
    - src/engine/action/types.ts:459-463 (manual? field)
    - src/engine/action/action-builder.ts:152-167 (.manual() builder + its JSDoc)
    - src/engine/element/action-metadata.ts:27-76 (buildActionMetadata, manual spread at :75)
    - src/session/types.ts:396-405 (ActionMetadata interface, manual? at :405)
    - src/engine/element/action-metadata.test.ts (existing metadata-channel test patterns)
  </read_first>
  <behavior>
    - An action built with `.suppressFromDock()` has `definition.suppressFromDock === true`.
    - buildActionMetadata emits `suppressFromDock: true` in the metadata for that action, and omits the key entirely for actions that did not call it (matching the `manual` spread convention).
    - The action remains fully present in the action list (suppression is metadata-only, not a registration/availability change).
  </behavior>
  <action>Mirror the `manual` channel for a new `suppressFromDock`. Add `suppressFromDock?: boolean` to ActionDefinition in src/engine/action/types.ts (next to `manual` at :459-463, JSDoc: "Hide this action's dock button in the auto-UI ActionPanel. The action stays fully executable via the board / custom UI (useBoardInteraction) — only the dock button is hidden. Not a security control."). Add `suppressFromDock(): this { this.definition.suppressFromDock = true; return this; }` to src/engine/action/action-builder.ts (mirror .manual() at :164-167, with parallel JSDoc). In src/engine/element/action-metadata.ts add the sibling spread `...(actionDef.suppressFromDock ? { suppressFromDock: true } : {})` next to the manual spread at :75. Add `suppressFromDock?: boolean` to ActionMetadata in src/session/types.ts (next to manual? at :405). Extend src/engine/element/action-metadata.test.ts with a fail-on-pre-fix test asserting buildActionMetadata emits suppressFromDock:true for a `.suppressFromDock()` action and omits it otherwise (PROC-01 engine unit).</action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/engine/element/action-metadata.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "suppressFromDock" src/engine/action/types.ts src/engine/action/action-builder.ts src/engine/element/action-metadata.ts src/session/types.ts` each >= 1.
    - Metadata test green; omission-when-unset asserted.
  </acceptance_criteria>
  <done>Per-action suppression flows engine type → builder → metadata → session type, mirroring `manual`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ActionPanel dock filter (visibleActions)</name>
  <read_first>
    - src/ui/components/auto-ui/ActionPanel.vue:140-168 (actionsWithMetadata + visibleActions passthrough) + :815 (v-for render)
    - src/session/types.ts ActionMetadata (Task 1 output, suppressFromDock?)
    - an existing ActionPanel or auto-ui @vue/test-utils test for mount/props patterns
  </read_first>
  <behavior>
    - Given availableActions including one whose metadata has suppressFromDock:true, visibleActions omits it; the un-suppressed actions still render as dock buttons.
    - The suppressed action is still present in the incoming availableActions/actionMetadata props (the panel filters render only; it does not mutate availability).
  </behavior>
  <action>Change ActionPanel.vue's visibleActions computed at :166-168 from the passthrough `return actionsWithMetadata.value;` to `return actionsWithMetadata.value.filter(a => !a.suppressFromDock);`. This is the entire render-side change — the board substrate (useBoardActionBridge) is fed independently, so a dock-suppressed action stays board-clickable. Create src/ui/components/auto-ui/ActionPanel.dock-suppression.test.ts (@vue/test-utils) asserting: an action with suppressFromDock:true metadata does NOT appear among rendered dock buttons while a sibling un-suppressed action does (fail-on-pre-fix: pre-change the passthrough renders it). Confirm the suppressed action is still passed in via props (executable-elsewhere invariant).</action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/ui/components/auto-ui/ActionPanel.dock-suppression.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "filter.*suppressFromDock" src/ui/components/auto-ui/ActionPanel.vue` matches at :166-168.
    - Component test green; suppressed button absent, sibling present, suppressed action still in props.
  </acceptance_criteria>
  <done>Dock renders per-action; suppressed actions hidden from the dock only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GameShell escape-hatch rename + all-suppressed turn-prompt fallback</name>
  <read_first>
    - src/ui/components/GameShell.vue:140 (suppressActionPanel decl), :177 (default), :2424-2447 (turn-strip + ActionPanel mount v-if gates; note existing history-guard props at :2439-2447), actionMetadata computed (~:400-405/464), availableActions source
    - src/ui/composables/useBoardActionBridge.ts:15 (doc-comment referencing suppressActionPanel)
    - an existing GameShell.*.test.ts for @vue/test-utils mount patterns
  </read_first>
  <behavior>
    - Passing platformActionPanelEscapeHatch:true still fully suppresses the ActionPanel and shows the bare turn-prompt strip (regression of old suppressActionPanel behavior under the new name).
    - When availableActions is non-empty AND every one has metadata.suppressFromDock:true, GameShell renders the bare turn-prompt strip (allDockActionsSuppressed) and does NOT mount the full ActionPanel — a single "no dock buttons to show" code path shared with the escape hatch.
    - When some actions are un-suppressed, the ActionPanel mounts normally.
    - No metadata / empty availableActions → default unsuppressed rendering (allDockActionsSuppressed is false).
  </behavior>
  <action>Rename the prop `suppressActionPanel` → `platformActionPanelEscapeHatch` at GameShell.vue:140 (decl) and :177 (default false), and its two usages at :2425/:2436. Update the JSDoc to: "Platform-only escape hatch that suppresses the entire action dock. Do NOT use from a game's own scaffold/bridge — use per-action `.suppressFromDock()` on the action definition instead. Referenced by the platform client and Phase 166 SKILLDEF-03." Add an `allDockActionsSuppressed` computed: false when no metadata or availableActions is empty, otherwise true iff every name in availableActions has actionMetadata[name]?.suppressFromDock. Change the turn-strip v-if (:2425) to `platformActionPanelEscapeHatch || allDockActionsSuppressed` and the ActionPanel-mount v-if (:2436) to `!platformActionPanelEscapeHatch && !allDockActionsSuppressed` so both produce the same safe fallback (never zero turn indicator). In src/ui/composables/useBoardActionBridge.ts:15 update the doc-comment: replace the `suppressActionPanel` reference with `platformActionPanelEscapeHatch` and mark it platform-only (de-document as an ordinary author prop). Do NOT touch the :2439-2447 history-guard props (owned by plan 04). Create src/ui/components/GameShell.action-panel-suppression.test.ts covering: (a) all-suppressed → turn-strip shown + ActionPanel not mounted, (b) some-un-suppressed → ActionPanel mounts, (c) platformActionPanelEscapeHatch:true regression → full suppression + turn strip.</action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/ui/components/GameShell.action-panel-suppression.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "suppressActionPanel" src/ui/components/GameShell.vue src/ui/composables/useBoardActionBridge.ts` == 0 (old name fully gone).
    - `grep -c "platformActionPanelEscapeHatch" src/ui/components/GameShell.vue` >= 3 (decl, default, both v-if usages).
    - `grep -n "allDockActionsSuppressed" src/ui/components/GameShell.vue` present in both v-if gates + the computed.
    - Component test green for all three cases; a turn indicator is always present in the all-suppressed case.
  </acceptance_criteria>
  <done>Blunt prop fenced + renamed; all-suppressed falls back to the turn-prompt strip; never zero turn indicator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| action author → UI dock render | suppressFromDock is an author-declared UI hint, not an authorization gate. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-01-01 | Elevation of Privilege | suppressFromDock mistaken as an access control | mitigate | JSDoc explicitly states the action stays fully executable via the board and suppression is NOT a security control; suppressed actions remain in availableActions so no authz decision rides on the flag. |
| T-164-01-02 | Spoofing/Denial | all-suppressed dock leaving player with no turn indicator | mitigate | allDockActionsSuppressed shares the escape-hatch fallback path → bare turn-prompt strip always renders; component test (c) asserts a turn indicator is always present. |
</threat_model>

<verification>
- `npx vitest run src/engine/element/action-metadata.test.ts src/ui/components/auto-ui/ActionPanel.dock-suppression.test.ts src/ui/components/GameShell.action-panel-suppression.test.ts` green.
- Awareness (no edit): `grep -rn "suppressActionPanel" ~/BoardSmithGames 2>/dev/null` — flag any sibling-game reference to the old name for a future cross-repo pass (library-only phase; do NOT edit game repos).
- Full suite deferred to phase gate.
</verification>

<success_criteria>
- Per-action dock suppression via `.suppressFromDock()`; suppressed actions still executable via board.
- Blunt prop renamed platformActionPanelEscapeHatch, platform-only, de-documented from author surface.
- All-suppressed → turn-prompt strip; all three tests green.
</success_criteria>

<output>
Create `.planning/phases/164-library-misc-action-panel-loop-visual-debug-view/164-03-SUMMARY.md` when done.
</output>
