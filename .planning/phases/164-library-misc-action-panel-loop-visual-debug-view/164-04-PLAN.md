---
phase: 164-library-misc-action-panel-loop-visual-debug-view
plan: 04
type: execute
wave: 2
depends_on: [164-03]
files_modified:
  - src/ui/components/GameShell.vue
  - src/ui/components/GameShell.time-travel-desync.test.ts
  - src/ui/composables/useBoardActionBridge.ts
autonomous: true
requirements: [LIBX-04, PROC-01]

must_haves:
  truths:
    - "During time-travel the board :state slot reflects the historical (displayed) state, not the always-live state, at BOTH board-render call sites AND the sidebar-extra slot"
    - "A simulated board click while isViewingHistory does NOT call the action controller (no live-engine mutation) — inert no-op commit"
    - "All four mutating bridge functions (startAction, executeAction, setSelectionValue, toggleMultiSelectValue) early-return under isViewingHistory"
    - "Exiting history (handleTimeTravel(null,...)) restores displayedState to live state"
  artifacts:
    - path: "src/ui/components/GameShell.vue"
      provides: "displayedState computed unifying live/historical GameState shape; applied at board + sidebar-extra slots"
      contains: "displayedState"
    - path: "src/ui/composables/useBoardActionBridge.ts"
      provides: "isViewingHistory option + early-return guards in all four mutating functions"
      contains: "isViewingHistory"
  key_links:
    - from: "src/ui/components/GameShell.vue"
      to: "displayedState"
      via: ":state binding at 2152, 2322, 2342 replaced with displayedState"
      pattern: ":state=\"displayedState\""
    - from: "src/ui/components/GameShell.vue"
      to: "src/ui/composables/useBoardActionBridge.ts"
      via: "isViewingHistory passed into useBoardActionBridge({...})"
      pattern: "isViewingHistory"
---

<objective>
LIBX-04 (D31): The time-travel debug view desyncs `#game-board` — the board slot receives the historical `:game-view` but an always-live `:state`, so a board click during history can commit against the LIVE engine. Fix both (defense in depth): (a) unify the board on a single `displayedState` computed (historical during time-travel, live otherwise) at both board-render call sites and the sidebar-extra slot; (b) gate `useBoardActionBridge` so board clicks are inert no-ops while `isViewingHistory`.

Purpose: A single source of truth for the displayed board state and a hard guarantee that viewing history cannot mutate the live game.
Output: `displayedState` computed, unified `:state` bindings, `isViewingHistory` bridge guard, PROC-01 tests including exit-restores-live regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-library-misc-action-panel-loop-visual-debug-view/164-CONTEXT.md
@.planning/phases/164-library-misc-action-panel-loop-visual-debug-view/164-RESEARCH.md

<interfaces>
<!-- Orchestrator resolution 3: fix ALL THREE :state sites (2152 sidebar-extra, 2322 dynamic-component, 2342 slot);
     leave DebugPanel's own :state at 2469 LIVE (it IS the time-travel control surface). -->
Shape mismatch to normalize (RESEARCH Pattern 4):
  - state.value : GameState = { flowState, state: PlayerState, playerSeat, isSpectator }   (client/types.ts:173-185)
  - timeTravelState.value : raw PlayerGameState (ONE level shallower — same shape as state.value.state)
  - existing gameView computed at GameShell.vue:473-478 already normalizes: timeTravelState.value.view vs state.value?.state.view
displayedState must re-wrap the historical PlayerGameState into a GameState-shaped object so no consumer needs a shape-aware branch:
  - when timeTravelState set: `state.value ? { ...state.value, state: timeTravelState.value } : null`
  - else: state.value
:state binding sites (grep ":state=\"state\"" must drop from 4 → 1):
  - GameShell.vue:2152 (#sidebar-extra), :2322 (dynamic-component board), :2342 (slot board) → change to displayedState
  - GameShell.vue:2469 (DebugPanel) → LEAVE as :state="state"
Bridge (useBoardActionBridge.ts) mutating functions to guard (add `if (isViewingHistory.value) return;` at top):
  - startAction :173, executeAction :189, setSelectionValue :204, toggleMultiSelectValue :226
  - BoardActionBridgeOptions interface :33; call site in GameShell.vue :790-797
  - GameShell already exposes isViewingHistory computed at :338
Exit path: handleTimeTravel at GameShell.vue:1786-1794 sets timeTravelState.value = historicalState (null on exit, emitted by DebugPanel :1275/:1288).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: displayedState computed + unify all three board/sidebar :state sites</name>
  <read_first>
    - src/ui/components/GameShell.vue:473-478 (gameView computed — the normalization pattern to mirror), :334-338 (timeTravelState + isViewingHistory), :1786-1794 (handleTimeTravel), :2140-2166 + :2320-2362 (board render sites) + :2469 (DebugPanel — must stay live)
    - src/client/types.ts:173-185 (GameState shape)
    - an existing GameShell.*.test.ts for @vue/test-utils mount + time-travel emit patterns
  </read_first>
  <behavior>
    - When timeTravelState is set, displayedState returns a GameState-shaped object whose `.state` is the historical PlayerGameState (re-wrapped), NOT the live one.
    - When timeTravelState is null (live), displayedState === state.value.
    - After the change, grep of `:state="state"` across GameShell.vue counts exactly 1 (the DebugPanel at :2469); the board sites (2322, 2342) and sidebar-extra (2152) all read displayedState.
    - Exiting history (handleTimeTravel(null,null,null)) restores displayedState to live state.value.
  </behavior>
  <action>Add a `displayedState` computed next to `gameView` (~GameShell.vue:473) typed `ComputedRef<GameState | null>`: when timeTravelState.value is truthy return `state.value ? { ...state.value, state: timeTravelState.value } : null` (re-wrap the shallower historical PlayerGameState into the GameState shape consumers expect), else return `state.value`. Replace `:state="state"` with `:state="displayedState"` at the THREE sites: GameShell.vue:2152 (#sidebar-extra), :2322 (dynamic-component board), :2342 (slot board). LEAVE :2469 (DebugPanel) as `:state="state"` — it is the time-travel control surface and must keep showing live history. Create src/ui/components/GameShell.time-travel-desync.test.ts asserting: (a) during time-travel the board slot's :state prop reflects the historical state at BOTH board-render paths (mount via slot AND via the uis/dynamic-component path per RESEARCH Pitfall 3), (b) exiting history restores :state to live. This is the fail-on-pre-fix PROC-01 board-state test.</action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/ui/components/GameShell.time-travel-desync.test.ts 2>&1 | tail -20 && echo "--- state-site count (expect 1) ---" && grep -c ':state="state"' src/ui/components/GameShell.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ':state="state"' src/ui/components/GameShell.vue` == 1 (only DebugPanel at :2469 remains live).
    - `grep -c ':state="displayedState"' src/ui/components/GameShell.vue` == 3 (2152, 2322, 2342).
    - displayedState computed present next to gameView; re-wraps historical state into GameState shape.
    - Test green: historical during time-travel (both render paths), live after exit.
  </acceptance_criteria>
  <done>Board + sidebar read a single displayedState; historical during history, live otherwise; both render paths covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: isViewingHistory guard on all four mutating bridge functions</name>
  <read_first>
    - src/ui/composables/useBoardActionBridge.ts:33 (BoardActionBridgeOptions), :173 startAction, :189 executeAction, :204 setSelectionValue, :226 toggleMultiSelectValue
    - src/ui/components/GameShell.vue:338 (isViewingHistory computed), :790-797 (useBoardActionBridge call site)
    - RESEARCH Anti-Patterns (do NOT rely on isMyTurn && !history compose — setSelectionValue/toggleMultiSelectValue don't re-check isMyTurn mid-action)
  </read_first>
  <behavior>
    - With isViewingHistory=true, calling any of startAction / executeAction / setSelectionValue / toggleMultiSelectValue is an inert no-op: the underlying action controller (execute/fill) is NOT called; no error thrown.
    - With isViewingHistory=false, the same calls behave exactly as today (no regression).
    - The mid-pick case (start an action, then time-travel, then a selection change) does NOT commit — setSelectionValue and toggleMultiSelectValue must independently guard, not rely on isMyTurn.
  </behavior>
  <action>Add `isViewingHistory: Ref<boolean> | ComputedRef<boolean>` to BoardActionBridgeOptions in src/ui/composables/useBoardActionBridge.ts:33 (JSDoc: "true while the debug panel shows historical state; board clicks must never commit to the live engine — LIBX-04"). Add `if (isViewingHistory.value) return;` as the first line of all four mutating functions: startAction (:173), executeAction (:189), setSelectionValue (:204), toggleMultiSelectValue (:226) — the two selection functions have NO turn/history guard today, so guard them explicitly (do not derive from isMyTurn per RESEARCH Anti-Patterns). Wire GameShell's existing isViewingHistory computed (:338) into the useBoardActionBridge({...}) call at :790-797. Extend src/ui/components/GameShell.time-travel-desync.test.ts (or add to it) with: a simulated board click / selection while isViewingHistory that asserts the action controller's execute/fill is NOT called (mock or spy the controller), plus the mid-pick-then-time-travel no-commit case.</action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/ui/components/GameShell.time-travel-desync.test.ts 2>&1 | tail -20 && grep -c "isViewingHistory.value) return" src/ui/composables/useBoardActionBridge.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isViewingHistory.value) return" src/ui/composables/useBoardActionBridge.ts` == 4 (all four functions guarded).
    - isViewingHistory added to BoardActionBridgeOptions and passed from GameShell:790-797.
    - Test green: no controller execute/fill during history (board click AND mid-pick selection); live behavior unregressed.
  </acceptance_criteria>
  <done>Board clicks during history are inert no-ops; all four mutating paths guarded independently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| debug time-travel view → live engine | A board click while viewing history could otherwise commit a stale/historical selection against the live game state. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-04-01 | Tampering | board click during time-travel → live-engine mutation | mitigate | Defense-in-depth: displayedState feeds historical state to the board (visual truth) AND all four useBoardActionBridge mutating functions early-return under isViewingHistory (no controller.execute/fill). Test asserts no live mutation for both a fresh click and a mid-pick selection. |
| T-164-04-02 | Tampering | exit-history fails to restore live state (stuck historical) | mitigate | Regression test asserts handleTimeTravel(null) restores displayedState to live state.value; the `??`/falsy handling in displayedState is explicitly covered. |
</threat_model>

<verification>
- `npx vitest run src/ui/components/GameShell.time-travel-desync.test.ts` green.
- `grep -c ':state="state"' src/ui/components/GameShell.vue` == 1; `grep -c "isViewingHistory.value) return" src/ui/composables/useBoardActionBridge.ts` == 4.
- Phase gate: full suite `npm run test` green (~2923 baseline) once, after wave 2 merges.
</verification>

<success_criteria>
- Board + sidebar read displayedState (single source of truth); historical during history, live otherwise.
- Board clicks during history are inert no-ops (no live mutation); all four bridge functions guarded.
- Exiting history restores live state (regression-tested).
</success_criteria>

<output>
Create `.planning/phases/164-library-misc-action-panel-loop-visual-debug-view/164-04-SUMMARY.md` when done.
</output>
