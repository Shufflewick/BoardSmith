---
phase: 169-post-fix-game-de-workaround-sweep
plan: 02
type: execute
wave: 2
depends_on: ["169-01"]
files_modified:
  - ~/BoardSmithGames/lanternfall/src/rules/flow.ts
  - ~/BoardSmithGames/lanternfall/src/ui/components/GardenBoard.vue
  - ~/BoardSmithGames/lanternfall/BOARDSMITH-BUGS.md
  - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md
autonomous: true
requirements: [SWEEP-01, PROC-01]

must_haves:
  truths:
    - "lanternfall's now-unneeded workarounds are removed (fix verified + suite green) OR kept-and-noted"
    - "lanternfall `npx vitest run` is green after the sweep"
    - "Each removal cites a PRESENT fix verdict from 169-CROSSWALK.md; each keep cites why"
    - "lanternfall's BSR-12 AI status is recorded honestly (ai.ts + ai-smoke.test.ts are UNTRACKED pre-existing WIP — read/run, do NOT commit) for 169-06 Task 3 to consume"
    - "pre-existing dirty-tree files are recorded and NOT folded into the sweep commit; every sweep commit is file-scoped"
  artifacts:
    - path: "~/BoardSmithGames/lanternfall/BOARDSMITH-BUGS.md"
      provides: "Updated ledger entries for swept/kept workarounds"
  key_links:
    - from: "lanternfall sweep"
      to: "169-CROSSWALK.md fix-present verdicts"
      via: "removal gate"
      pattern: "PRESENT"
    - from: "lanternfall src/rules/ai.ts (untracked WIP)"
      to: "169-CROSSWALK.md BSR-12 per-repo status"
      via: "AI re-verify (build + ai-smoke test)"
      pattern: "BSR-12"
---

<objective>
Conservatively sweep the lanternfall game repo: remove only clearly-dead compensating logic and stale "boardsmith bug" comments whose underlying fix is verified PRESENT in 169-CROSSWALK.md, keeping the suite green. A workaround that cannot be safely removed (fix not PRESENT, or suite goes red) is KEPT + noted — that is a valid, successful outcome, not a failure. Additionally, re-verify lanternfall's deferred AI (BSR-12) and record its per-repo status for 169-06's cross-repo close verdict.

Purpose: Retire dead workaround weight now that the v4.8 library fixes ship live via the symlink; feed lanternfall's AI status into the phase BSR-12 close.
Output: A `sweep/v4.8-dework` branch in `~/BoardSmithGames/lanternfall` with gated removals + refreshed comments + updated ledger + a recorded BSR-12 status; this repo's 169-CROSSWALK.md updated with lanternfall outcomes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md
@.planning/phases/169-post-fix-game-de-workaround-sweep/169-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record pre-existing dirty tree + green baseline + confirm fix-present verdicts for lanternfall targets</name>
  <read_first>
    - ~/BoardSmithGames/lanternfall/src/rules/flow.ts (line ~60 maxIterations)
    - ~/BoardSmithGames/lanternfall/src/ui/components/GardenBoard.vue (lines ~546-548 metadata guard)
    - ~/BoardSmithGames/lanternfall/BOARDSMITH-BUGS.md
    - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md (D29/LIBX-02 and D26/SPACE-05 verdicts)
  </read_first>
  <action>cd into `~/BoardSmithGames/lanternfall`. FIRST, BEFORE branching: run `git -C ~/BoardSmithGames/lanternfall status --porcelain` and RECORD the exact pre-existing dirty state verbatim in the summary (known at plan time: modified `src/rules/index.ts`, untracked `src/rules/ai.ts` + `tests/ai-smoke.test.ts`). This master is DIRTY — do NOT discard, stash, or commit any pre-existing WIP. Branching a dirty tree with `git checkout -b` carries the WIP forward, which is fine; the sweep simply must NOT claim credit for it. If the porcelain output shows dirty files beyond the three known ones, RECORD them too and proceed (do not touch them). Create and checkout branch `sweep/v4.8-dework` off master (do NOT push, do NOT touch master). Run `npx vitest run` and record the exact green baseline (N passed / files) — this is the pre-removal oracle; note explicitly whether `tests/ai-smoke.test.ts` is exercised by the suite. Then confirm in 169-CROSSWALK.md that D29/LIBX-02 (`unbounded` loop valve) and D26/SPACE-05 (availableActions/actionMetadata reconciliation) are marked PRESENT via grep against `node_modules/boardsmith/src`. If either is ABSENT, mark its target as a BLOCKER (keep + record) and do not remove it in Task 2. Make no source edits in this task.</action>
  <verify>
    <automated>cd ~/BoardSmithGames/lanternfall && git rev-parse --abbrev-ref HEAD | grep -q "sweep/v4.8-dework" && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Pre-existing dirty-tree state (`git status --porcelain`) recorded verbatim BEFORE branching; no pre-existing WIP discarded, stashed, or committed
    - Branch `sweep/v4.8-dework` checked out in lanternfall (never master)
    - `npx vitest run` exits 0; the green baseline count is recorded, with a note on whether `ai-smoke.test.ts` runs in the suite
    - D29 and D26 verdicts confirmed from 169-CROSSWALK.md; ABSENT targets flagged BLOCKER
  </acceptance_criteria>
  <done>Pre-existing dirty tree recorded; green baseline captured on the sweep branch; each target's fix-present verdict confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Apply gated removals + comment refresh, re-verify AI (BSR-12), verify green, file-scoped commit + update ledgers</name>
  <read_first>
    - ~/BoardSmithGames/lanternfall/src/rules/flow.ts (maxIterations valve; D29/BUG 6)
    - ~/BoardSmithGames/lanternfall/src/ui/components/GardenBoard.vue (metadata-divergence guard; D26)
    - ~/BoardSmithGames/lanternfall/src/rules/ai.ts (UNTRACKED pre-existing WIP — the BSR-12 AI-status source)
    - ~/BoardSmithGames/lanternfall/tests/ai-smoke.test.ts (UNTRACKED pre-existing WIP — the AI smoke test)
    - ~/BoardSmithGames/lanternfall/BOARDSMITH-BUGS.md
    - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md
  </read_first>
  <action>On the `sweep/v4.8-dework` branch, apply ONLY conservative, fix-verified changes. For `src/rules/flow.ts:~60` `maxIterations: ENGINE_SAFETY_VALVE_ROUNDS` (D29/BUG 6): the maxIterations tripwire is a legit API — do NOT switch to `unbounded:true` unless it is trivially safe AND leaves the suite green; default action is REFRESH the stale "boardsmith bug" comment to note D29's `unbounded` valve now exists upstream (comment-only). For `src/ui/components/GardenBoard.vue:~546-548` metadata guard (D26/SPACE-05): only if D26 is PRESENT, attempt removal of the now-redundant divergence guard; run `npx vitest run` immediately after — if RED, revert that single removal (git checkout the file) and record it as kept-and-noted. Do not remove the two WITHDRAWN lanternfall filings (they were never bugs).

BSR-12 AI RE-VERIFY (Blocker-1 coverage): `src/rules/ai.ts` and `tests/ai-smoke.test.ts` are UNTRACKED pre-existing WIP — read and RUN them but do NOT `git add`/commit them as part of this sweep. Confirm D9/AI-01 (function-valued multiSelect enumeration) and D8/AI-02 (redacted-view MCTS) are PRESENT in 169-CROSSWALK.md. Verify lanternfall's `ai.ts` type-checks/builds (as the repo does) and that `ai-smoke.test.ts` passes (run it directly, e.g. `npx vitest run tests/ai-smoke.test.ts`). Record an honest per-repo BSR-12 status line for 169-06 Task 3 to consume, e.g. "lanternfall BSR-12: AI present as UNTRACKED WIP (not committed by this sweep); ai.ts builds + ai-smoke test PASSES against AI-01/AI-02" — or, if it fails or does not build, state the exact gap. Do NOT commit ai.ts/ai-smoke.test.ts; do NOT add a game-side redaction hack.

After each change run `npx vitest run` and keep it green (revert-on-red per removal). Then update `~/BoardSmithGames/lanternfall/BOARDSMITH-BUGS.md`: mark each swept entry resolved-by-v4.8 (cite the Dxx) and each kept entry with the keep rationale. COMMIT FILE-SCOPED: `git add` ONLY the sweep-touched files (`src/rules/flow.ts`, `src/ui/components/GardenBoard.vue`, `BOARDSMITH-BUGS.md` — plus only those actually edited) and commit — NEVER `git add -A` / `git add .` / `git commit -a`. The pre-existing dirty files (`src/rules/index.ts`, `src/rules/ai.ts`, `tests/ai-smoke.test.ts`, and any others recorded in Task 1) MUST NOT be staged or committed. Separately update this repo's 169-CROSSWALK.md with lanternfall's per-target outcome (removed / kept-noted / blocked) and its BSR-12 status line. In the SUMMARY, list the pre-existing dirty files this sweep deliberately did NOT touch.</action>
  <verify>
    <automated>cd ~/BoardSmithGames/lanternfall && npx vitest run && git log --oneline -1 | grep -iq sweep && ! git show --stat HEAD | grep -Eq "rules/ai\.ts|ai-smoke\.test\.ts|rules/index\.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (matches or exceeds the green baseline) after all changes
    - Any removal that turned the suite red was reverted and recorded as kept-and-noted
    - No removal was made whose fix verdict is ABSENT
    - lanternfall BSR-12 status recorded honestly (untracked-WIP reality stated; ai.ts build + ai-smoke result) into 169-CROSSWALK.md for 169-06 Task 3
    - Commit is FILE-SCOPED: only sweep-touched files staged; pre-existing WIP (index.ts, ai.ts, ai-smoke.test.ts) NOT committed (verified via `git show --stat HEAD`)
    - lanternfall BOARDSMITH-BUGS.md updated per swept/kept entry; 169-CROSSWALK.md records lanternfall outcomes; SUMMARY lists the untouched pre-existing dirty files
    - Commit is on `sweep/v4.8-dework`, not master; nothing pushed
  </acceptance_criteria>
  <done>lanternfall suite green post-sweep; ledger + crosswalk reconciled; BSR-12 status recorded; kept items explicitly justified; commit file-scoped with pre-existing WIP excluded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| removal vs. suite | A removed workaround must not silently break behavior; the suite is the oracle |
| sweep commit vs. pre-existing WIP | The audited sweep commit must contain ONLY sweep-touched files, never pre-existing dirty-tree WIP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-02 | Tampering | gated removal | mitigate | Every removal gated on a PRESENT verdict + green `npx vitest run`; revert-on-red per removal |
| T-169-02b | Repudiation | sweep commit vs. pre-existing WIP | mitigate | Record dirty tree before branching; FILE-SCOPED `git add` only; never `-a`/`-A`; verify HEAD excludes index.ts/ai.ts/ai-smoke.test.ts |
| T-169-SC | Tampering | npm installs | accept | No installs; removes dead game code + edits comments/ledgers only |
</threat_model>

<verification>
- lanternfall `npx vitest run` green on the sweep branch
- Every removal cites a PRESENT verdict; every keep cites a reason
- BSR-12 status recorded; pre-existing WIP excluded from the file-scoped commit
- Ledger + crosswalk updated
</verification>

<success_criteria>
lanternfall is swept conservatively with a green suite, OR its unremovable workarounds are explicitly kept-and-noted — either is success. Its BSR-12 AI status is recorded honestly (untracked-WIP reality) for the phase close. No unverified removal, no red suite traded for cleanliness, no pre-existing WIP folded into the sweep commit.
</success_criteria>

<output>
Create `.planning/phases/169-post-fix-game-de-workaround-sweep/169-02-SUMMARY.md` when done.
</output>
