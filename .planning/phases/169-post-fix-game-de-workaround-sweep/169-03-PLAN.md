---
phase: 169-post-fix-game-de-workaround-sweep
plan: 03
type: execute
wave: 3
depends_on: ["169-01"]
files_modified:
  - ~/BoardSmithGames/seven/src/rules/actions.ts
  - ~/BoardSmithGames/seven/src/rules/elements.ts
  - ~/BoardSmithGames/seven/src/rules/game.ts
  - ~/BoardSmithGames/seven/BOARDSMITH-REQUESTS.md
  - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md
autonomous: true
requirements: [SWEEP-01, PROC-01]

must_haves:
  truths:
    - "seven's redundant `.notUndoable()` re-guards are removed only if UNDO-01 is verified server-side + suite green; else kept-and-noted"
    - "the `concealFromEverySeat` smell is removed/replaced only if D24/SPACE-03 is verified retiring the child-count leak"
    - "seven `npx vitest run` is green after the sweep"
    - "seven's BSR-12 AI status (src/rules/ai.ts builds + AI tests pass against AI-01/AI-02) is recorded for 169-06 Task 3 to consume"
    - "pre-existing dirty-tree files are recorded and NOT folded into the sweep commit; every sweep commit is file-scoped"
  artifacts:
    - path: "~/BoardSmithGames/seven/BOARDSMITH-REQUESTS.md"
      provides: "Updated ledger entries (BSR-5 = D1, D24)"
  key_links:
    - from: "seven sweep"
      to: "169-CROSSWALK.md fix-present verdicts"
      via: "removal gate (D1, D24)"
      pattern: "PRESENT"
    - from: "seven src/rules/ai.ts"
      to: "169-CROSSWALK.md BSR-12 per-repo status"
      via: "AI re-verify (build + AI tests)"
      pattern: "BSR-12"
---

<objective>
Conservatively sweep `~/BoardSmithGames/seven`: remove the now-redundant game-side `.notUndoable()` server re-guards (BSR-5 = D1) and the `concealFromEverySeat` smell (D24/SPACE-03) only where the specific fix is verified PRESENT and the suite stays green; keep multiSelect defensive assertions (legit) unless verified redundant. Re-verify seven's AI (BSR-12) and record its per-repo status for 169-06's cross-repo close verdict. A kept workaround (fix not PRESENT or suite red) is a valid, recorded outcome.

Purpose: Drop dead undo-hole mitigations and the concealment smell now that Phase 155 (UNDO-01) and Phase 163 (SPACE-03) ship live; feed seven's AI status into the phase BSR-12 close.
Output: `sweep/v4.8-dework` branch in seven with gated removals + refreshed comments + updated `BOARDSMITH-REQUESTS.md` + a recorded BSR-12 status; 169-CROSSWALK.md updated with seven outcomes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md
@.planning/phases/169-post-fix-game-de-workaround-sweep/169-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record pre-existing dirty tree + green baseline + confirm D1 and D24 verdicts for seven</name>
  <read_first>
    - ~/BoardSmithGames/seven/src/rules/actions.ts (the four `.notUndoable()` calls + docblocks calling it INERT server-side; BSR-5)
    - ~/BoardSmithGames/seven/src/rules/elements.ts (line ~565 concealFromEverySeat; line ~452 multiSelect assertion)
    - ~/BoardSmithGames/seven/src/rules/game.ts (lines ~141-143 concealFromEverySeat; line ~298 multiSelect assertion)
    - ~/BoardSmithGames/seven/BOARDSMITH-REQUESTS.md
    - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md (D1/UNDO-01, D24/SPACE-03 verdicts)
  </read_first>
  <action>cd into `~/BoardSmithGames/seven`. FIRST, BEFORE branching: run `git -C ~/BoardSmithGames/seven status --porcelain` and RECORD the exact pre-existing dirty state verbatim in the summary. Do NOT discard, stash, or commit any pre-existing WIP — branching a dirty tree with `git checkout -b` carries the WIP forward (fine), but the sweep must NOT claim credit for it. Then create + checkout `sweep/v4.8-dework` off master (never push/touch master). Run `npx vitest run`; record the green baseline (note whether the AI tests are in the suite). Confirm in 169-CROSSWALK.md that D1/UNDO-01 is PRESENT (server undo executor honors `hasNonUndoableAction` via `assertUndoAllowed` in `node_modules/boardsmith/src/session/utils.ts`) and D24/SPACE-03 is PRESENT (hidden-Space child-count suppression in space.ts). Mark any ABSENT verdict as a BLOCKER (its target stays kept-and-noted). No source edits this task.</action>
  <verify>
    <automated>cd ~/BoardSmithGames/seven && git rev-parse --abbrev-ref HEAD | grep -q "sweep/v4.8-dework" && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Pre-existing dirty-tree state (`git status --porcelain`) recorded verbatim BEFORE branching; no pre-existing WIP discarded, stashed, or committed
    - `sweep/v4.8-dework` checked out in seven
    - `npx vitest run` exits 0; baseline recorded, with a note on whether AI tests run in the suite
    - D1 and D24 verdicts confirmed; ABSENT ⇒ BLOCKER flagged
  </acceptance_criteria>
  <done>Pre-existing dirty tree recorded; green baseline on the sweep branch; D1/D24 fix-present verdicts confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Remove redundant undo re-guards + conceal smell (gated), re-verify AI (BSR-12), verify green, file-scoped commit + update ledgers</name>
  <read_first>
    - ~/BoardSmithGames/seven/src/rules/actions.ts
    - ~/BoardSmithGames/seven/src/rules/elements.ts
    - ~/BoardSmithGames/seven/src/rules/game.ts
    - ~/BoardSmithGames/seven/src/rules/ai.ts (the BSR-12 AI-status source)
    - any ~/BoardSmithGames/seven AI test file (grep for ai / mcts under src or tests)
    - ~/BoardSmithGames/seven/BOARDSMITH-REQUESTS.md
    - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md
  </read_first>
  <action>On `sweep/v4.8-dework`: (1) Keep the four `.notUndoable()` CALLS themselves — `.notUndoable()` is a legit API. Remove only the now-redundant game-side server-re-guard LOGIC and the stale docblock prose that claims `.notUndoable()` is inert server-side, IFF D1 is PRESENT; refresh those docblocks to state the library now enforces it server-side (Phase 155/UNDO-01). (2) For `concealFromEverySeat` (elements.ts:~565, game.ts:~141-143), IFF D24 is PRESENT, replace/remove the smell per the fixed serializer behavior (child count no longer leaks). (3) Keep the multiSelect assertions (elements.ts:~452, game.ts:~298) unless the crosswalk marks them verified-redundant. After EACH edit run `npx vitest run`; if RED, `git checkout` that file to revert the single change and record it kept-and-noted.

BSR-12 AI RE-VERIFY (Blocker-1 coverage): confirm D9/AI-01 and D8/AI-02 PRESENT in 169-CROSSWALK.md. Verify `src/rules/ai.ts` builds/type-checks (as the repo does) and its AI tests pass under `npx vitest run` (run them directly if isolatable). If the library now supplies the per-seat redacted view + function-valued multiSelect enumeration, refresh any stale AI comment to cite AI-01/AI-02; do NOT add a game-side redaction/enumeration hack. Record an honest per-repo BSR-12 status line for 169-06 Task 3, e.g. "seven BSR-12: ai.ts builds + AI tests PASS against AI-01/AI-02" — or the exact residual gap if it fails.

Update `~/BoardSmithGames/seven/BOARDSMITH-REQUESTS.md`: mark BSR-5 resolved-by-D1 and the conceal item resolved-by-D24 (or kept + rationale). COMMIT FILE-SCOPED: `git add` ONLY the sweep-touched files (`src/rules/actions.ts`, `src/rules/elements.ts`, `src/rules/game.ts`, `src/rules/ai.ts` if its comment was refreshed, `BOARDSMITH-REQUESTS.md` — only those actually edited) — NEVER `git add -A` / `git add .` / `git commit -a`. Do NOT stage any pre-existing dirty files recorded in Task 1. Update this repo's 169-CROSSWALK.md with seven's per-target outcomes + its BSR-12 status line. In the SUMMARY, list the pre-existing dirty files this sweep deliberately did NOT touch.</action>
  <verify>
    <automated>cd ~/BoardSmithGames/seven && npx vitest run && git log --oneline -1 | grep -iq sweep</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 after all changes (≥ baseline)
    - `.notUndoable()` calls retained; only redundant re-guard logic / stale comments removed, gated on D1 PRESENT
    - conceal smell removed only on D24 PRESENT; any red-causing removal reverted + recorded
    - seven BSR-12 status recorded honestly (ai.ts build + AI-test result) into 169-CROSSWALK.md for 169-06 Task 3; no game-side redaction/enumeration hack added
    - Commit is FILE-SCOPED: only sweep-touched files staged; pre-existing WIP NOT committed; SUMMARY lists untouched dirty files
    - BOARDSMITH-REQUESTS.md + 169-CROSSWALK.md updated; commit on sweep branch, not pushed
  </acceptance_criteria>
  <done>seven suite green post-sweep; ledger + crosswalk reconciled; BSR-12 status recorded; kept items justified; commit file-scoped with pre-existing WIP excluded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| removal vs. suite | Removing an undo re-guard must not reopen the undo hole; the suite + D1 verdict are the gate |
| sweep commit vs. pre-existing WIP | The audited sweep commit must contain ONLY sweep-touched files, never pre-existing dirty-tree WIP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-03 | Tampering | undo re-guard / conceal removal | mitigate | Gated on D1/D24 PRESENT + green `npx vitest run`; `.notUndoable()` calls kept; revert-on-red |
| T-169-03b | Repudiation | sweep commit vs. pre-existing WIP | mitigate | Record dirty tree before branching; FILE-SCOPED `git add` only; never `-a`/`-A` |
| T-169-SC | Tampering | npm installs | accept | No installs; dead-code + comment + ledger edits only |
</threat_model>

<verification>
- seven `npx vitest run` green on the sweep branch
- `.notUndoable()` calls intact; only redundant compensating logic / stale prose removed
- BSR-12 status recorded; pre-existing WIP excluded from the file-scoped commit
- Ledger + crosswalk updated
</verification>

<success_criteria>
seven is swept conservatively with a green suite, or its unremovable re-guards/conceal smell are kept-and-noted — either is success. Its BSR-12 AI status is recorded for the phase close. No unverified removal; no red suite; no pre-existing WIP folded into the sweep commit.
</success_criteria>

<output>
Create `.planning/phases/169-post-fix-game-de-workaround-sweep/169-03-SUMMARY.md` when done.
</output>
