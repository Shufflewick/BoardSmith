---
phase: 169-post-fix-game-de-workaround-sweep
plan: 04
type: execute
wave: 4
depends_on: ["169-01"]
files_modified:
  - ~/BoardSmithGames/one-two-punch/src/rules/game.ts
  - ~/BoardSmithGames/one-two-punch/src/rules/guards.ts
  - ~/BoardSmithGames/one-two-punch/src/rules/ai.ts
  - ~/BoardSmithGames/one-two-punch/BOARDSMITH-BUGS.md
  - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md
autonomous: true
requirements: [SWEEP-01, PROC-01]

must_haves:
  truths:
    - "one-two-punch's reimplemented server-side undo guard (BUG 3 = D1) is removed only if UNDO-01 verified + suite green; else kept-and-noted"
    - "the deferred AI (BUG 8 = D8) is re-verified against AI-02 redacted-view MCTS: ai.ts builds + AI tests pass, or a residual gap is noted"
    - "one-two-punch `npx vitest run` is green after the sweep"
  artifacts:
    - path: "~/BoardSmithGames/one-two-punch/BOARDSMITH-BUGS.md"
      provides: "Updated BUG 3 (D1) + BUG 8 (D8/BSR-12) ledger entries"
  key_links:
    - from: "one-two-punch ai.ts"
      to: "node_modules/boardsmith redacted-view MCTS (AI-02)"
      via: "BSR-12 re-verify"
      pattern: "toJSONForPlayer"
---

<objective>
Conservatively sweep `~/BoardSmithGames/one-two-punch`: remove the reimplemented server-side undo guard (BUG 3 = D1) only if UNDO-01 is verified PRESENT and the suite stays green, and re-verify the deferred AI opponent (BUG 8 = D8) against AI-02 redacted-view MCTS now that Phase 159 shipped — this is the priority BSR-12 close candidate. Kept items (fix not PRESENT / suite red / residual AI gap) are valid recorded outcomes.

Purpose: Retire the hand-reimplemented undo hole guard and re-enable/close the AI that was blocked on D8/D9.
Output: `sweep/v4.8-dework` branch in one-two-punch with gated removal + AI re-verify + updated `BOARDSMITH-BUGS.md`; 169-CROSSWALK.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md
@.planning/phases/169-post-fix-game-de-workaround-sweep/169-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Green baseline + confirm D1 verdict; remove reimplemented undo guard (gated)</name>
  <read_first>
    - ~/BoardSmithGames/one-two-punch/src/rules/game.ts (lines ~308-359 reimplemented server-side undo guard; BUG 3)
    - ~/BoardSmithGames/one-two-punch/src/rules/guards.ts (line ~199)
    - ~/BoardSmithGames/one-two-punch/BOARDSMITH-BUGS.md
    - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md (D1/UNDO-01 verdict)
  </read_first>
  <action>cd into `~/BoardSmithGames/one-two-punch`. Create + checkout `sweep/v4.8-dework` off master (never push/touch master; do NOT stage the repo's `_*_tmp.mjs` / `_dbg*.mjs` scratch files). Run `npx vitest run`; record the green baseline. Confirm D1/UNDO-01 PRESENT in 169-CROSSWALK.md. IFF PRESENT, remove the reimplemented server-side undo guard at game.ts:~308-359 + guards.ts:~199 ("must close the undo hole" — now closed server-side by Phase 155). Run `npx vitest run` immediately; if RED, `git checkout` to revert and record kept-and-noted. If D1 ABSENT, keep + record BLOCKER. Do not touch ai.ts in this task.</action>
  <verify>
    <automated>cd ~/BoardSmithGames/one-two-punch && git rev-parse --abbrev-ref HEAD | grep -q "sweep/v4.8-dework" && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `sweep/v4.8-dework` checked out; scratch `.mjs` files not staged
    - baseline recorded; `npx vitest run` green after the gated removal (or removal reverted + recorded)
    - undo-guard removal made only on D1 PRESENT
  </acceptance_criteria>
  <done>Undo-hole reimplementation removed (or kept-noted); suite green on the sweep branch.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify the deferred AI (BUG 8 = D8) against AI-02 redacted-view MCTS</name>
  <read_first>
    - ~/BoardSmithGames/one-two-punch/src/rules/ai.ts (lines ~12-33: MCTS reads un-redacted state + sequentializes reveals; "does NOT try to work around it")
    - any ~/BoardSmithGames/one-two-punch AI test file (grep for ai / mcts under src)
    - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md (D8/AI-02 verdict — redacted-view MCTS `toJSONForPlayer`)
  </read_first>
  <action>On the sweep branch, confirm D8/AI-02 PRESENT (redacted-view MCTS clone via `toJSONForPlayer` in `node_modules/boardsmith/src/ai`). Verify `src/rules/ai.ts` builds (tsc/vite as the repo does) and its AI tests run under `npx vitest run`. If the library now supplies the per-seat redacted view, refresh ai.ts's stale "NOT worked around" comment to note AI-02 provides it; do not add a game-side redaction workaround. If the AI builds + AI tests pass, this closes BSR-12 for one-two-punch — record that; if a residual gap remains, keep BSR-12 open + note the exact gap. Keep the whole suite green.</action>
  <verify>
    <automated>cd ~/BoardSmithGames/one-two-punch && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - ai.ts builds; AI tests pass under `npx vitest run` (or the residual gap is precisely recorded)
    - No game-side redaction hack added; stale comment refreshed to cite AI-02
    - BSR-12 close/keep-open decision recorded for one-two-punch
  </acceptance_criteria>
  <done>one-two-punch AI re-verified against AI-02; BSR-12 disposition recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile ledgers (BUG 3, BUG 8) + crosswalk</name>
  <read_first>
    - ~/BoardSmithGames/one-two-punch/BOARDSMITH-BUGS.md
    - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md
  </read_first>
  <action>Update `~/BoardSmithGames/one-two-punch/BOARDSMITH-BUGS.md`: mark BUG 3 resolved-by-D1 (or kept + rationale) and BUG 8 resolved-by-D8/AI-02 (or kept-open + residual gap). Commit source + ledger on the sweep branch (excluding scratch `.mjs` files). Update this repo's 169-CROSSWALK.md with one-two-punch's per-target outcomes and its BSR-12 disposition.</action>
  <verify>
    <automated>cd ~/BoardSmithGames/one-two-punch && git log --oneline -1 | grep -iq sweep && grep -qi "BUG 8" BOARDSMITH-BUGS.md</automated>
  </verify>
  <acceptance_criteria>
    - BOARDSMITH-BUGS.md BUG 3 + BUG 8 entries updated
    - 169-CROSSWALK.md records one-two-punch outcomes + BSR-12 disposition
    - Commit on sweep branch, not pushed; scratch files excluded
  </acceptance_criteria>
  <done>Ledger + crosswalk reconciled for one-two-punch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| removal vs. suite | Removing the reimplemented undo guard must not reopen the undo hole |
| AI redaction | MCTS must reason over a redacted per-seat view (AI-02), never un-redacted state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-04a | Tampering | undo-guard removal | mitigate | Gated on D1 PRESENT + green `npx vitest run`; revert-on-red |
| T-169-04b | Information Disclosure | MCTS redacted view | mitigate | Re-verify against AI-02 `toJSONForPlayer`; no game-side redaction hack; residual gap recorded not papered over |
| T-169-SC | Tampering | npm installs | accept | No installs; dead-code + comment + ledger edits only |
</threat_model>

<verification>
- one-two-punch `npx vitest run` green on the sweep branch
- undo guard removed only on D1 PRESENT; AI re-verified against AI-02
- BUG 3 + BUG 8 ledger entries + crosswalk updated
</verification>

<success_criteria>
one-two-punch is swept conservatively with a green suite; the deferred AI is re-verified and BSR-12 closed (or its residual gap precisely noted). Kept items are recorded, not forced.
</success_criteria>

<output>
Create `.planning/phases/169-post-fix-game-de-workaround-sweep/169-04-SUMMARY.md` when done.
</output>
