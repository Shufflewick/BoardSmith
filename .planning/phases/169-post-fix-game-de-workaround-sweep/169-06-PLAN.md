---
phase: 169-post-fix-game-de-workaround-sweep
plan: 06
type: execute
wave: 6
depends_on: ["169-01", "169-02", "169-03", "169-04", "169-05"]
files_modified:
  - ~/BoardSmithGames2/seven/src/rules/actions.ts
  - ~/BoardSmithGames2/seven/src/rules/ai.ts
  - ~/BoardSmithGames2/seven/BOARDSMITH-BUG-02-undo-op-ignores-non-undoable-actions.md
  - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md
autonomous: true
requirements: [SWEEP-01, PROC-01]

must_haves:
  truths:
    - "BoardSmithGames2/seven's redundant `.notUndoable()` re-guards (BOARDSMITH-BUG-02 = D1) are removed only if UNDO-01 verified + suite green; else kept-and-noted"
    - "the `multiSelect:{min:7,max:7}` auto-confirm is kept (legit); no concealFromEverySeat here (recorded divergence)"
    - "BoardSmithGames2/seven `npx vitest run` is green after the sweep"
    - "BSR-12 is closed across all repos with AI (or residual gaps noted); the crosswalk is fully reconciled against all five repos' ledgers"
    - "pre-existing dirty-tree files are recorded and NOT folded into the sweep commit; every sweep commit is file-scoped"
  artifacts:
    - path: "~/BoardSmithGames2/seven/BOARDSMITH-BUG-02-undo-op-ignores-non-undoable-actions.md"
      provides: "Updated D1 ledger entry"
    - path: ".planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md"
      provides: "Final cross-repo reconciliation + BSR-12 close verdict"
  key_links:
    - from: "BoardSmithGames2/seven sweep"
      to: "169-CROSSWALK.md D1 verdict"
      via: "removal gate"
      pattern: "PRESENT"
    - from: "169-02/03/04 recorded BSR-12 per-repo statuses"
      to: "169-06 Task 3 BSR-12 close verdict"
      via: "cross-repo aggregation"
      pattern: "BSR-12"
---

<objective>
Conservatively sweep `~/BoardSmithGames2/seven` (the second seven repo) and finalize the phase: remove the redundant `.notUndoable()` server re-guards (BOARDSMITH-BUG-02 = D1) only if UNDO-01 is verified PRESENT + suite green, keep the legit `multiSelect:{min:7,max:7}` auto-confirm, then perform the close-time cross-repo ledger reconciliation and the final BSR-12 close verdict by AGGREGATING the per-repo AI statuses recorded by 169-02 (lanternfall), 169-03 (seven), 169-04 (one-two-punch), and this plan (BoardSmithGames2/seven). Kept items are valid recorded outcomes.

Purpose: Sweep the last repo and reconcile the paperwork (SKILLAUTO-08 discipline: audit the ledgers, not just the code) so no swept/kept workaround is left mis-recorded, and so the BSR-12 close verdict is grounded in a recorded status for every AI-bearing repo.
Output: `sweep/v4.8-dework` branch in BoardSmithGames2/seven with gated removal + updated bug file; 169-CROSSWALK.md finalized with all five repos' outcomes + the BSR-12 close verdict aggregated from all four AI-bearing repos' recorded statuses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md
@.planning/phases/169-post-fix-game-de-workaround-sweep/169-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record pre-existing dirty tree + green baseline + gated `.notUndoable()` re-guard removal for BoardSmithGames2/seven</name>
  <read_first>
    - ~/BoardSmithGames2/seven/src/rules/actions.ts (lines ~64, 174, 275, 354: `.notUndoable()` ×4 + docblocks citing BOARDSMITH-BUG-02)
    - ~/BoardSmithGames2/seven/BOARDSMITH-BUG-02-undo-op-ignores-non-undoable-actions.md
    - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md (D1/UNDO-01 verdict)
  </read_first>
  <action>cd into `~/BoardSmithGames2/seven`. FIRST, BEFORE branching: run `git -C ~/BoardSmithGames2/seven status --porcelain` and RECORD the exact pre-existing dirty state verbatim in the summary. Do NOT discard, stash, or commit any pre-existing WIP — branching a dirty tree with `git checkout -b` carries the WIP forward (fine), but the sweep must NOT claim credit for it. Create + checkout `sweep/v4.8-dework` off master (never push/touch master). Run `npx vitest run`; record the green baseline. Confirm D1/UNDO-01 PRESENT in 169-CROSSWALK.md. Keep the four `.notUndoable()` CALLS (legit API); IFF D1 PRESENT, remove only the now-redundant game-side re-guard logic and refresh the docblocks that cite BOARDSMITH-BUG-02 to note the library enforces `.notUndoable()` server-side (Phase 155). Keep the `multiSelect:{min:7,max:7}` auto-confirm (legit). Note the divergence that this repo has NO `concealFromEverySeat` (handled differently) — record, don't act. Run `npx vitest run` after each edit; revert-on-red (record kept-and-noted).</action>
  <verify>
    <automated>cd ~/BoardSmithGames2/seven && git rev-parse --abbrev-ref HEAD | grep -q "sweep/v4.8-dework" && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Pre-existing dirty-tree state (`git status --porcelain`) recorded verbatim BEFORE branching; no pre-existing WIP discarded, stashed, or committed
    - `sweep/v4.8-dework` checked out; baseline recorded; `npx vitest run` green
    - `.notUndoable()` calls + `multiSelect:{min:7,max:7}` kept; only redundant re-guard logic / stale docblocks removed, gated on D1 PRESENT
    - concealFromEverySeat divergence recorded (no action)
  </acceptance_criteria>
  <done>Pre-existing dirty tree recorded; BoardSmithGames2/seven swept (or kept-noted); suite green on the sweep branch.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify this repo's AI (BSR-12) + reconcile its ledger (file-scoped commit)</name>
  <read_first>
    - ~/BoardSmithGames2/seven/src/rules/ai.ts (grep for the MCTS/redacted-view path)
    - ~/BoardSmithGames2/seven/BOARDSMITH-BUG-02-*.md
    - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md (D8/AI-02 verdict)
  </read_first>
  <action>On the sweep branch, verify `src/rules/ai.ts` builds and its AI tests pass under `npx vitest run` against AI-02 (redacted-view MCTS). Refresh any stale AI comment to cite AI-02; add no game-side redaction hack. Record an honest per-repo BSR-12 status line for Task 3, e.g. "BoardSmithGames2/seven BSR-12: ai.ts builds + AI tests PASS against AI-01/AI-02" — or the exact residual gap. Update `~/BoardSmithGames2/seven/BOARDSMITH-BUG-02-*.md` (mark resolved-by-D1 or kept + rationale). COMMIT FILE-SCOPED: `git add` ONLY the sweep-touched files (`src/rules/actions.ts`, `src/rules/ai.ts` if its comment was refreshed, `BOARDSMITH-BUG-02-*.md` — only those actually edited) and commit — NEVER `git add -A` / `git add .` / `git commit -a`. Do NOT stage any pre-existing dirty files recorded in Task 1. In the SUMMARY, list the pre-existing dirty files this sweep deliberately did NOT touch.</action>
  <verify>
    <automated>cd ~/BoardSmithGames2/seven && npx vitest run && git log --oneline -1 | grep -iq sweep</automated>
  </verify>
  <acceptance_criteria>
    - ai.ts builds + AI tests pass (or residual gap recorded); BoardSmithGames2/seven BSR-12 status line recorded for Task 3
    - BOARDSMITH-BUG-02 entry updated
    - Commit is FILE-SCOPED: only sweep-touched files staged; pre-existing WIP NOT committed; SUMMARY lists untouched dirty files; commit on sweep branch, not pushed
  </acceptance_criteria>
  <done>BoardSmithGames2/seven AI re-verified; its ledger reconciled; commit file-scoped with pre-existing WIP excluded.</done>
</task>

<task type="auto">
  <name>Task 3: Final cross-repo ledger reconciliation + BSR-12 close verdict (aggregate 4 recorded per-repo statuses)</name>
  <read_first>
    - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md (all five repos' outcomes + the recorded per-repo BSR-12 status lines from plans 02/03/04 + this plan's Task 2)
    - the 169-02..05-SUMMARY.md files for each repo's swept/kept/blocked results AND its recorded BSR-12 status (169-02 lanternfall, 169-03 seven, 169-04 one-two-punch)
  </read_first>
  <action>Finalize 169-CROSSWALK.md as the phase's reconciliation record. Confirm every filing across all five repos' ledgers has a final disposition (removed / kept-and-noted / blocked-fix-absent / deferred / reclassified / no-op / withdrawn / out-of-scope) and that each removed workaround's own-repo ledger entry was updated (SKILLAUTO-08 discipline).

Write the BSR-12 close verdict by AGGREGATING the FOUR recorded per-repo AI statuses — one per AI-bearing repo: (1) lanternfall (from 169-02's recorded status — note its AI is UNTRACKED pre-existing WIP that the sweep deliberately did NOT commit; the status still reports whether ai.ts builds + ai-smoke test passes), (2) seven (from 169-03's recorded status), (3) one-two-punch (from 169-04's recorded status; priority repo), (4) BoardSmithGames2/seven (from this plan's Task 2 status). Verdict: CLOSED if ALL FOUR recorded statuses show ai.ts builds + AI tests pass against AI-01/AI-02; KEPT-OPEN with the exact residual gap(s) and the naming repo(s) otherwise. If any of the four per-repo statuses is missing from the crosswalk/SUMMARYs, DO NOT assert CLOSED — record the missing input as a KEPT-OPEN blocker (PROC-01 adversarial-verification: no verdict without evidence for every repo). Note doom-machine is N/A (no AI). List any BLOCKER (fix-absent) removals that were correctly skipped. This is a THIS-repo doc edit only — no game-repo edits.</action>
  <verify>
    <automated>grep -qi "BSR-12" .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md && grep -Eqi "CLOSED|KEPT-OPEN" .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md && grep -Eqi "lanternfall|BoardSmithGames2" .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md</automated>
  </verify>
  <acceptance_criteria>
    - Every filing across all 5 repos has a final disposition recorded in 169-CROSSWALK.md
    - BSR-12 verdict aggregates a recorded status for ALL FOUR AI-bearing repos (lanternfall, seven, one-two-punch, BoardSmithGames2/seven); a missing per-repo status forces KEPT-OPEN, never an unevidenced CLOSED
    - lanternfall's status honestly reflects its UNTRACKED-WIP AI (not committed by the sweep)
    - doom noted N/A (no AI); skipped BLOCKER removals (fix ABSENT) listed, not silent
  </acceptance_criteria>
  <done>Phase reconciliation complete; BSR-12 verdict recorded (aggregated from all four AI-bearing repos); every workaround has a final, justified disposition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| removal vs. suite | Removing an undo re-guard must not reopen the undo hole |
| paperwork vs. code | A swept workaround must not be left mis-recorded in its own repo ledger |
| verdict vs. evidence | The BSR-12 CLOSED verdict must be backed by a recorded status for EVERY AI-bearing repo |
| sweep commit vs. pre-existing WIP | The audited sweep commit must contain ONLY sweep-touched files, never pre-existing dirty-tree WIP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-06a | Tampering | undo re-guard removal | mitigate | Gated on D1 PRESENT + green `npx vitest run`; `.notUndoable()` calls kept; revert-on-red |
| T-169-06b | Repudiation | ledger reconciliation | mitigate | Every filing gets a final recorded disposition; skipped BLOCKERs listed, not silent (SKILLAUTO-08) |
| T-169-06c | Repudiation | BSR-12 verdict evidence | mitigate | Aggregate a recorded per-repo status for all four AI repos; missing input ⇒ KEPT-OPEN, never unevidenced CLOSED |
| T-169-06d | Repudiation | sweep commit vs. pre-existing WIP | mitigate | Record dirty tree before branching; FILE-SCOPED `git add` only; never `-a`/`-A` |
| T-169-SC | Tampering | npm installs | accept | No installs; dead-code + comment + ledger/doc edits only |
</threat_model>

<verification>
- BoardSmithGames2/seven `npx vitest run` green on the sweep branch
- Every filing across all 5 repos has a final disposition in 169-CROSSWALK.md
- BSR-12 close verdict recorded, aggregated from all four AI-bearing repos' statuses
- Pre-existing WIP excluded from the file-scoped commits
</verification>

<success_criteria>
The last repo is swept conservatively with a green suite, BSR-12 is closed (or its residual gap precisely noted) based on a recorded status for every AI-bearing repo, and every workaround across all five repos has a final, justified disposition in the crosswalk with its own-repo ledger updated. Kept/blocked items are recorded, not forced; no pre-existing WIP folded into the sweep commits.
</success_criteria>

<output>
Create `.planning/phases/169-post-fix-game-de-workaround-sweep/169-06-SUMMARY.md` when done.
</output>
