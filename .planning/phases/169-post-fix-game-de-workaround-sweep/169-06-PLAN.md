---
phase: 169-post-fix-game-de-workaround-sweep
plan: 06
type: execute
wave: 6
depends_on: ["169-01", "169-02", "169-03", "169-04", "169-05"]
files_modified:
  - ~/BoardSmithGames2/seven/src/rules/actions.ts
  - ~/BoardSmithGames2/seven/src/rules/ai.ts
  - ~/BoardSmithGames2/seven/BOARDSMITH-BUG-02-undo-op-ignores-non-undoable-actions.md
  - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md
autonomous: true
requirements: [SWEEP-01, PROC-01]

must_haves:
  truths:
    - "BoardSmithGames2/seven's redundant `.notUndoable()` re-guards (BOARDSMITH-BUG-02 = D1) are removed only if UNDO-01 verified + suite green; else kept-and-noted"
    - "the `multiSelect:{min:7,max:7}` auto-confirm is kept (legit); no concealFromEverySeat here (recorded divergence)"
    - "BoardSmithGames2/seven `npx vitest run` is green after the sweep"
    - "BSR-12 is closed across all repos with AI (or residual gaps noted); the crosswalk is fully reconciled against all five repos' ledgers"
  artifacts:
    - path: "~/BoardSmithGames2/seven/BOARDSMITH-BUG-02-undo-op-ignores-non-undoable-actions.md"
      provides: "Updated D1 ledger entry"
    - path: ".planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md"
      provides: "Final cross-repo reconciliation + BSR-12 close verdict"
  key_links:
    - from: "BoardSmithGames2/seven sweep"
      to: "169-CROSSWALK.md D1 verdict"
      via: "removal gate"
      pattern: "PRESENT"
---

<objective>
Conservatively sweep `~/BoardSmithGames2/seven` (the second seven repo) and finalize the phase: remove the redundant `.notUndoable()` server re-guards (BOARDSMITH-BUG-02 = D1) only if UNDO-01 is verified PRESENT + suite green, keep the legit `multiSelect:{min:7,max:7}` auto-confirm, then perform the close-time cross-repo ledger reconciliation and the final BSR-12 close verdict across every repo with AI. Kept items are valid recorded outcomes.

Purpose: Sweep the last repo and reconcile the paperwork (SKILLAUTO-08 discipline: audit the ledgers, not just the code) so no swept/kept workaround is left mis-recorded.
Output: `sweep/v4.8-dework` branch in BoardSmithGames2/seven with gated removal + updated bug file; 169-CROSSWALK.md finalized with all five repos' outcomes + the BSR-12 close verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md
@.planning/phases/169-post-fix-game-de-workaround-sweep/169-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Green baseline + gated `.notUndoable()` re-guard removal for BoardSmithGames2/seven</name>
  <read_first>
    - ~/BoardSmithGames2/seven/src/rules/actions.ts (lines ~64, 174, 275, 354: `.notUndoable()` ×4 + docblocks citing BOARDSMITH-BUG-02)
    - ~/BoardSmithGames2/seven/BOARDSMITH-BUG-02-undo-op-ignores-non-undoable-actions.md
    - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md (D1/UNDO-01 verdict)
  </read_first>
  <action>cd into `~/BoardSmithGames2/seven`. Create + checkout `sweep/v4.8-dework` off master (never push/touch master). Run `npx vitest run`; record the green baseline. Confirm D1/UNDO-01 PRESENT in 169-CROSSWALK.md. Keep the four `.notUndoable()` CALLS (legit API); IFF D1 PRESENT, remove only the now-redundant game-side re-guard logic and refresh the docblocks that cite BOARDSMITH-BUG-02 to note the library enforces `.notUndoable()` server-side (Phase 155). Keep the `multiSelect:{min:7,max:7}` auto-confirm (legit). Note the divergence that this repo has NO `concealFromEverySeat` (handled differently) — record, don't act. Run `npx vitest run` after each edit; revert-on-red (record kept-and-noted).</action>
  <verify>
    <automated>cd ~/BoardSmithGames2/seven && git rev-parse --abbrev-ref HEAD | grep -q "sweep/v4.8-dework" && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `sweep/v4.8-dework` checked out; baseline recorded; `npx vitest run` green
    - `.notUndoable()` calls + `multiSelect:{min:7,max:7}` kept; only redundant re-guard logic / stale docblocks removed, gated on D1 PRESENT
    - concealFromEverySeat divergence recorded (no action)
  </acceptance_criteria>
  <done>BoardSmithGames2/seven swept (or kept-noted); suite green on the sweep branch.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify this repo's AI + reconcile its ledger</name>
  <read_first>
    - ~/BoardSmithGames2/seven/src/rules/ai.ts (grep for the MCTS/redacted-view path)
    - ~/BoardSmithGames2/seven/BOARDSMITH-BUG-02-*.md
    - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md (D8/AI-02 verdict)
  </read_first>
  <action>On the sweep branch, verify `src/rules/ai.ts` builds and its AI tests pass under `npx vitest run` against AI-02 (redacted-view MCTS). Refresh any stale AI comment to cite AI-02; add no game-side redaction hack. Update `~/BoardSmithGames2/seven/BOARDSMITH-BUG-02-*.md` (mark resolved-by-D1 or kept + rationale). Commit source + bug-file changes on the sweep branch. Record this repo's AI result toward the BSR-12 close verdict.</action>
  <verify>
    <automated>cd ~/BoardSmithGames2/seven && npx vitest run && git log --oneline -1 | grep -iq sweep</automated>
  </verify>
  <acceptance_criteria>
    - ai.ts builds + AI tests pass (or residual gap recorded)
    - BOARDSMITH-BUG-02 entry updated; commit on sweep branch, not pushed
  </acceptance_criteria>
  <done>BoardSmithGames2/seven AI re-verified; its ledger reconciled.</done>
</task>

<task type="auto">
  <name>Task 3: Final cross-repo ledger reconciliation + BSR-12 close verdict</name>
  <read_first>
    - .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md (all five repos' outcomes from plans 02-06 tasks)
    - the 169-02..05-SUMMARY.md files for each repo's swept/kept/blocked results
  </read_first>
  <action>Finalize 169-CROSSWALK.md as the phase's reconciliation record. Confirm every filing across all five repos' ledgers has a final disposition (removed / kept-and-noted / blocked-fix-absent / deferred / reclassified / no-op / withdrawn / out-of-scope) and that each removed workaround's own-repo ledger entry was updated (SKILLAUTO-08 discipline). Write the BSR-12 close verdict: CLOSED if every AI-bearing repo (one-two-punch priority, plus lanternfall/seven/BoardSmithGames2-seven) builds + passes its AI tests against AI-01/AI-02; KEPT-OPEN with the exact residual gap otherwise. Note doom-machine is N/A (no AI). List any BLOCKER (fix-absent) removals that were correctly skipped. This is a THIS-repo doc edit only — no game-repo edits.</action>
  <verify>
    <automated>grep -qi "BSR-12" .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md && grep -Eqi "CLOSED|KEPT-OPEN" .planning/phases/169-post-fix-game-de-workaround-sweep/169-CROSSWALK.md</automated>
  </verify>
  <acceptance_criteria>
    - Every filing across all 5 repos has a final disposition recorded in 169-CROSSWALK.md
    - BSR-12 close verdict written (CLOSED or KEPT-OPEN + residual gap); doom noted N/A
    - Skipped BLOCKER removals (fix ABSENT) are listed, not silent
  </acceptance_criteria>
  <done>Phase reconciliation complete; BSR-12 verdict recorded; every workaround has a final, justified disposition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| removal vs. suite | Removing an undo re-guard must not reopen the undo hole |
| paperwork vs. code | A swept workaround must not be left mis-recorded in its own repo ledger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-06a | Tampering | undo re-guard removal | mitigate | Gated on D1 PRESENT + green `npx vitest run`; `.notUndoable()` calls kept; revert-on-red |
| T-169-06b | Repudiation | ledger reconciliation | mitigate | Every filing gets a final recorded disposition; skipped BLOCKERs listed, not silent (SKILLAUTO-08) |
| T-169-SC | Tampering | npm installs | accept | No installs; dead-code + comment + ledger/doc edits only |
</threat_model>

<verification>
- BoardSmithGames2/seven `npx vitest run` green on the sweep branch
- Every filing across all 5 repos has a final disposition in 169-CROSSWALK.md
- BSR-12 close verdict recorded
</verification>

<success_criteria>
The last repo is swept conservatively with a green suite, BSR-12 is closed (or its residual gap precisely noted), and every workaround across all five repos has a final, justified disposition in the crosswalk with its own-repo ledger updated. Kept/blocked items are recorded, not forced.
</success_criteria>

<output>
Create `.planning/phases/169-post-fix-game-de-workaround-sweep/169-06-SUMMARY.md` when done.
</output>
