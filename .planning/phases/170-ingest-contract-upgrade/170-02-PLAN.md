---
phase: 170-ingest-contract-upgrade
plan: 02
type: execute
wave: 2
depends_on: ["170-01"]
files_modified:
  - src/cli/slash-command/bs/ingest-rules.md
  - src/cli/slash-command/bs/ingest/interview-fallback.md
  - src/cli/slash-command/bs/ingest.test.ts
autonomous: true
requirements: [INGEST-01, INGEST-03, INGEST-04, PROC-02]
must_haves:
  truths:
    - "A freshly-ingested project contains the source rulebook file at `rulebook/source/<original-filename>`, and the designer's original file is untouched wherever it sat."
    - "A designer can read `rulebook/INDEX.md` alone and learn the edition, which source file was transcribed, that file's SHA-256, and when it was transcribed."
    - "`rulebook/INDEX.md` always carries a `## Open Rules Gaps` section — reading `_None._` when nothing was marked — so its absence is a defect rather than an ambiguity."
    - "On the no-rulebook interview path the header block is still emitted, with explicit not-applicable values rather than missing lines."
  artifacts:
    - path: "src/cli/slash-command/bs/ingest-rules.md"
      provides: "Step 3 archive+hash sub-item, the 4-line INDEX header block, and the always-emitted Open Rules Gaps assembly"
      contains: "rulebook/source/"
    - path: "src/cli/slash-command/bs/ingest/interview-fallback.md"
      provides: "interview-path header-line values (never-omit rule)"
      contains: "Source hash:"
    - path: "src/cli/slash-command/bs/ingest.test.ts"
      provides: "v4.9 INGEST-01/03/04 skill-text contract assertions"
      contains: "Source hash:"
  key_links:
    - from: "src/cli/slash-command/bs/ingest/transcription.md"
      to: "src/cli/slash-command/bs/ingest-rules.md Step 3"
      via: "openGaps[] accumulated into ## Open Rules Gaps"
      pattern: "openGaps\\[\\]"
    - from: "src/cli/slash-command/bs/ingest-rules.md Step 3"
      to: "rulebook/INDEX.md header block"
      via: "Source: / Source hash: lines naming the archived path"
      pattern: "rulebook/source/"
---

<objective>
Land the `ingest-rules.md` Step 3 (Synthesis) half of the contract: archive + hash the source
rulebook (INGEST-01), always emit the four-line INDEX header block (INGEST-04), and always emit
`## Open Rules Gaps` assembled from the `openGaps[]` field Plan 01 added (INGEST-03), plus the
interview-path values for the never-omit rule. Lock each with a contract test.

Purpose: Step 3 is the only step that both knows `{rulebookPath}` (bound in Step 2) and writes
`rulebook/INDEX.md`. The `rulebook/source/` path and the exact `Source hash:` spelling become the
contract Phase 171 (provenance recording) and Phase 173 (verify re-transcription) read, so their
literal spelling is load-bearing beyond this phase.

Output: edited `ingest-rules.md` and `interview-fallback.md`; extended `ingest.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/170-ingest-contract-upgrade/170-CONTEXT.md
@.planning/phases/170-ingest-contract-upgrade/170-RESEARCH.md
@.planning/phases/170-ingest-contract-upgrade/170-01-SUMMARY.md

**Sequencing trap — do NOT put archive/hash logic in `scaffold.md`.** CONTEXT.md's
`<code_context>` section calls `scaffold.md` "the natural home" for archiving; 170-RESEARCH.md
Pitfall 1 refutes that empirically. `scaffold.md` is delegated from Step 1, which runs *before*
Step 2 asks whether a rulebook even exists — `{rulebookPath}` does not exist yet at that point.
The archive+hash work belongs in `ingest-rules.md` **Step 3 (Synthesis)**. CONTEXT.md's locked
*decisions* stand; its guess about which file owns the work does not. A test asserting
`scaffold.md` contains `rulebook/source/` would be a defect, not a proof.

**Requirement-ID collision hazard.** `ingest.test.ts`'s existing `INGEST-01`..`INGEST-07`
describe blocks are **v4.6** IDs. Name every new block with a `v4.9 ` prefix. Do not edit the
v4.6 blocks.

**Fold, do not renumber.** 170-RESEARCH.md Open Question 1 recommends adding the archive+hash
work as a sub-item of the existing Step 3 artifact list rather than inserting a new top-level
numbered step — Step 3 is already framed as "assembles the following artifacts," and the archived
file plus two header lines are two more artifacts. Renumbering Steps 4-7 would break every
cross-file "Step N" citation in the skill family. Take the recommendation.

**No installer change is needed** (`SHARED_DIRS` recursively copies `ingest/`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add source archive + SHA-256 and the four-line INDEX header block to ingest-rules.md Step 3</name>
  <files>src/cli/slash-command/bs/ingest-rules.md</files>
  <read_first>
    - src/cli/slash-command/bs/ingest-rules.md (full file — especially the "Context-Economics Hard Rule" section, Step 1's explicit "runs before transcription/interview" paragraph, Step 2's `{rulebookPath}` binding, and Step 3's numbered artifact list items 1-6 including the existing `Edition:` header-line prose in item 2)
    - src/cli/slash-command/bs/ingest/transcription.md (the "Edition" section — the existing `Edition: 2nd edition, 2019 printing` example and the interview-path `Edition: unpublished — designer statement` line, so the new lines match the established prose style)
    - .planning/phases/170-ingest-contract-upgrade/170-CONTEXT.md (locked archive path, hash algorithm, never-move-or-delete rule, never-omit-a-header-line rule)
  </read_first>
  <action>
Edit `ingest-rules.md` Step 3 only. Do not renumber Steps 4-7 and do not add a new top-level step.

(a) **Archive the source (INGEST-01).** Add a new sub-item to Step 3's artifact list — place it
first, before the `rulebook/INDEX.md` item, since the INDEX header lines depend on it. It must
instruct the orchestrator to copy the source file bound at Step 2 to
`rulebook/source/<original-filename>`, preserving the original filename verbatim. State the
non-destructive invariant explicitly and in the imperative: ingest **copies**; it never moves,
renames, deletes, or overwrites the designer's original — a source already sitting at the project
root (as `seven/rules.pdf` does) stays exactly where it is. State that if a file already exists at
the archive destination, the orchestrator stops and asks the designer rather than clobbering it.
State that the copy must be verified before continuing — the archived file must exist and be
byte-identical to the source (same size and same hash).

(b) **Hash it (INGEST-01).** Prescribe computing the SHA-256 of the **archived** file with the
literal command `shasum -a 256 rulebook/source/<original-filename>` , taking the first
whitespace-delimited field as the hash. Name the Linux/CI fallback `sha256sum` explicitly (macOS
ships `shasum`; GNU/Linux ships `sha256sum`) so the instruction is portable. This is a
deterministic shell operation, not agent judgment — say so, and forbid the agent from reporting a
hash it did not compute by running the command.

(c) **The header block (INGEST-04).** Extend the existing `Edition:` header-line prose in Step 3's
INDEX.md item into a fixed four-line prose block written at the top of `rulebook/INDEX.md`, in
this order and with these exact labels: `Edition:`, `Source:`, `Source hash:`, `Transcribed:`.
Prose lines, not YAML frontmatter — this matches the INDEX.md style a designer already reads.
Values: `Source:` names the archived `rulebook/source/<original-filename>` path (the in-project
path, never an external absolute path); `Source hash:` is the bare 64-hex-character SHA-256 from
(b); `Transcribed:` is the ISO date (YYYY-MM-DD) of the ingest session. State the never-omit rule
literally: **all four lines are always written; a line is never dropped.** When a value is
genuinely unknown, write the line with the explicit value `not stated in the rulebook` rather than
omitting it. State that the header block goes above the existing `## Slices` and `## Term → Slice`
tables and must not disturb them — `/bs-check-status` and every chunk's term→slice sweep parse
those tables.

(d) **`## Open Rules Gaps` (INGEST-03).** Add a further Step 3 instruction to write a section
whose heading is exactly `## Open Rules Gaps` (no parenthetical suffix) into `rulebook/INDEX.md`,
assembled **exclusively** from the accumulated `openGaps[]` lists the transcription subagents
return. State, in the same breath, the Context-Economics restatement: never sweep or grep the
slice files to build this section — the orchestrator does not re-read slices. State that the
section is written **always**, and that when no gaps were returned its body is exactly `_None._`,
so an absent section is a defect rather than an ambiguity. Instruct the section to carry a
one-sentence honesty note stating that it reports what transcription *marked* as
named-but-undefined and does not claim to be an exhaustive list of the rulebook's gaps.

State the concatenation behaviour explicitly, in these terms: **list every `openGaps[]` entry
returned, one per subagent report, even if the same rule name recurs across slices — do not
deduplicate.** A recurring name is itself signal (the rulebook names the rule in more than one
place and defines it in none), and leaving dedup unspecified would make the Plan 03 checkpoint's
slice-marker-count reconciliation ambiguous — a working transport could then be read as a broken
one. The raw-count equality check is only meaningful if this is pinned.

(e) **Consumption naming.** Ensure Step 3 names `openGaps[]` by that exact field name, so the
existing return-shape pinning in `ingest.test.ts` sees the orchestrator side too, matching how
`citedTerms[]`/`componentMentions[]`/`visualEvidence[]`/`variants[]` are already named there.

Do not touch Step 1, Step 2, or `ingest/scaffold.md`.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/ingest.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ingest-rules.md` contains the literal strings `rulebook/source/`, `shasum -a 256`, `sha256sum`
    - `ingest-rules.md` contains all four literal header labels: `Edition:`, `Source:`, `Source hash:`, `Transcribed:`
    - `ingest-rules.md` contains the literal heading `## Open Rules Gaps` and the literal empty-state token `_None._`
    - `ingest-rules.md` contains the literal string `not stated in the rulebook`
    - `ingest-rules.md` contains the literal string `openGaps[]`
    - `ingest-rules.md` contains the literal string `do not deduplicate` — pinning the concatenation behaviour the Plan 03 reconciliation depends on
    - `ingest-rules.md` still contains `## Slices`-preserving guidance and does not alter the existing Step 3 items 1-6 semantics beyond the header-block extension
    - `grep -c '^## Step ' src/cli/slash-command/bs/ingest-rules.md` returns the same count as before the edit (8: Steps 0-7) — proving no step was renumbered or added
    - `ingest/scaffold.md` is unmodified: `git diff --name-only` does not list `src/cli/slash-command/bs/ingest/scaffold.md`
    - `npx vitest run src/cli/slash-command/bs/ingest.test.ts` exits 0
  </acceptance_criteria>
  <done>Step 3 archives + hashes the source, writes the four-line header block above the existing tables, and always writes `## Open Rules Gaps` from `openGaps[]`; step numbering and `scaffold.md` are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Give the interview (no-rulebook) path explicit not-applicable header values</name>
  <files>src/cli/slash-command/bs/ingest/interview-fallback.md</files>
  <read_first>
    - src/cli/slash-command/bs/ingest/interview-fallback.md (full file — especially the "Output Re-Target" section and the existing statement that this path produces the SAME `rulebook/NN-topic.md` + `rulebook/INDEX.md` shape as the transcription path)
    - src/cli/slash-command/bs/ingest-rules.md (Step 3 as edited in Task 1 — the header block and Open Rules Gaps prose this file must stay consistent with)
  </read_first>
  <action>
The interview path has no source file to archive, but CONTEXT.md's never-omit rule applies to the
header block regardless (170-RESEARCH.md Open Question 2 recommends explicit not-applicable values
over omission; take the recommendation).

In `interview-fallback.md`'s "Output Re-Target" section — where the shared `rulebook/INDEX.md`
output shape is already declared — state the interview-path values for the same four header lines,
using the same exact labels: `Edition:` keeps its existing `unpublished — designer statement`
value; `Source:` and `Source hash:` are each written with the explicit value
`not applicable — no source rulebook (interview path)`; `Transcribed:` is the ISO date of the
interview session. State that no line is omitted on this path either, and that there is no
`rulebook/source/` directory on this path (nothing to archive) — that absence is expected here and
only here.

Also state that `## Open Rules Gaps` is written on this path too, on the same always/`_None._`
terms, populated from any rule the designer named but could not define during the interview.

Keep it short — this file cites rather than restates `ingest-rules.md`'s prose per the skill
family's citation-not-restatement convention. Do not restate the hashing command here.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/ingest.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `interview-fallback.md` contains all four literal labels `Edition:`, `Source:`, `Source hash:`, `Transcribed:`
    - `interview-fallback.md` contains the literal string `not applicable — no source rulebook (interview path)` (em dash, matching the file's existing `unpublished — designer statement` style)
    - `interview-fallback.md` contains the literal heading `## Open Rules Gaps` and the literal token `_None._`
    - `interview-fallback.md` does NOT contain `shasum -a 256` (no hashing on this path; restating it would be a false instruction)
    - Pre-existing v4.6 assertions on this file stay green: it still contains `rulebook/`, `citedTerms[]`, `componentMentions[]`, and `../aspects/index.md`
    - `npx vitest run src/cli/slash-command/bs/ingest.test.ts` exits 0
  </acceptance_criteria>
  <done>The interview path emits the same four header labels with explicit not-applicable values and the same always-emitted gaps section; nothing about hashing is falsely claimed there.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the Step 3 contract in ingest.test.ts</name>
  <files>src/cli/slash-command/bs/ingest.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/ingest.test.ts (full current file, including the `v4.9 INGEST-02`/`v4.9 INGEST-03` blocks Plan 01 added and the `RETURN_SHAPE_FIELDS` array)
    - src/cli/slash-command/bs/ingest-rules.md (as edited in Task 1)
    - src/cli/slash-command/bs/ingest/interview-fallback.md (as edited in Task 2)
  </read_first>
  <action>
Extend `ingest.test.ts` in its existing idiom (`read()` inside each `it()`, plain
`toContain`/`toMatch`, no parser, no new dependency). All new describe names carry the `v4.9 `
prefix.

Add `v4.9 INGEST-01 — source archive + SHA-256`, asserting against `ingest-rules.md`:
  - contains `rulebook/source/`
  - contains `shasum -a 256` and contains `sha256sum`
  - states the non-destructive invariant — assert a regex requiring both "copy" and a
    never-move/never-delete phrasing (case-insensitive)
  - **negative assertion (Pitfall 1 guard):** `ingest/scaffold.md` does NOT contain
    `rulebook/source/`. Archiving in `scaffold.md` would run before `{rulebookPath}` exists; this
    assertion is what keeps a future reword from quietly moving it there.

Add `v4.9 INGEST-04 — INDEX.md header block`, asserting against `ingest-rules.md`:
  - contains each of `Edition:`, `Source:`, `Source hash:`, `Transcribed:`
  - contains `not stated in the rulebook`
  - and asserting against `ingest/interview-fallback.md`: contains the same four labels plus
    `not applicable — no source rulebook (interview path)`

Extend (or add, adjacent to Plan 01's block) `v4.9 INGEST-03 — ## Open Rules Gaps section`,
asserting against `ingest-rules.md`:
  - contains the exact heading `## Open Rules Gaps` (assert the bare heading with no parenthetical
    suffix on the same line — `seven`'s pre-phase INDEX used
    `## Open Rules Gaps (named-but-undefined in the source)`, and this phase standardizes on the
    bare form)
  - contains `_None._`
  - contains `openGaps[]`
  - and `ingest/interview-fallback.md` contains `## Open Rules Gaps` and `_None._`

Also extend the existing `ingest-rules.md consumes the synthesis-facing fields by the same names`
assertion's inline field list to include `openGaps[]`, so the orchestrator side is pinned the same
way `variants[]` already is.

Every new `it()` must be selectable: verify each new describe returns a non-zero passed count under
its own `-t` selector.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/ingest.test.ts && npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "v4.9 INGEST-01" && npx vitest run src/cli/slash-command/bs/ingest.test.ts -t "v4.9 INGEST-04" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `ingest.test.ts` contains a describe named exactly `v4.9 INGEST-01 — source archive + SHA-256`
    - `ingest.test.ts` contains a describe named exactly `v4.9 INGEST-04 — INDEX.md header block`
    - `ingest.test.ts` contains an assertion that `ingest/scaffold.md` does NOT contain `rulebook/source/`
    - Each of `-t "v4.9 INGEST-01"`, `-t "v4.9 INGEST-03"`, `-t "v4.9 INGEST-04"` exits 0 with a non-zero passed count
    - `npx vitest run src/cli/slash-command/bs/ingest.test.ts` exits 0 with zero failures
    - `npm test` exits 0 (wave-merge gate)
    - Adversarial check (PROC-02): for each of the three new requirement blocks, temporarily remove the corresponding line from the skill text, confirm exactly that block goes RED, restore, confirm GREEN. Record all three probe results in the plan SUMMARY. A block that stays green with its subject removed is a defect and must be tightened before the plan closes.
  </acceptance_criteria>
  <done>All three v4.9 requirement blocks exist, are individually selectable, pass, and were each proven to fail when their subject text is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| designer-supplied file path → skill-prescribed filesystem operation | `{rulebookPath}` is a designer-supplied path the skill instructs an agent to copy from and to shell-hash. This is the only genuine (if low-severity) surface in the phase. |
| skill text → LLM agent | Instruction integrity: a reworded instruction silently changes agent behavior with no compile error. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-170-03 | Tampering | archive destination `rulebook/source/<original-filename>` | mitigate | Step 3 prose instructs stop-and-ask on an existing destination file rather than overwriting (Task 1a); prevents clobbering a previously-archived source on a re-ingest |
| T-170-04 | Tampering | designer's original rulebook file | mitigate | The never-move/never-delete/never-rename invariant is stated imperatively in Step 3 and pinned by a `v4.9 INGEST-01` assertion (Task 3); PROC-01's manual proof (Plan 03) additionally verifies `~/BoardSmithGames/seven` is left unmodified |
| T-170-05 | Tampering | `{rulebookPath}` unexpected value (traversal, absolute path outside the project, non-PDF) | accept | The path is supplied by the designer, on their own machine, to a tool they invoked, pointing at their own file. The skill only *reads* from it and *writes* inside the project's own `rulebook/source/`. No privilege boundary is crossed and no untrusted party supplies the path. The destination is fixed to an in-project relative path (`Source:` explicitly forbids recording an external absolute path), so a traversal-shaped source cannot redirect the write. |
| T-170-06 | Spoofing | `Source hash:` value | mitigate | Step 3 forbids reporting a hash the agent did not compute by running the command; the phase's manual proof (Plan 03) independently re-runs `shasum -a 256` and compares against the recorded line |
| T-170-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan or phase (Markdown + existing vitest). 170-RESEARCH.md records the Package Legitimacy Audit as N/A. |

No high-severity threats. No network input, no authentication or session boundary, no new destructive filesystem operation is introduced.
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/ingest.test.ts` exits 0; `npm test` exits 0.
- `git diff --name-only` for this plan lists exactly `ingest-rules.md`, `ingest/interview-fallback.md`, `ingest.test.ts` — `scaffold.md` must NOT appear (Pitfall 1 guard).
- A RED in `cross-file consistency — every referenced path resolves on disk` means a
  `REFERENCED_PATHS` string was accidentally altered (Pitfall 6); this plan renames nothing.
</verification>

<success_criteria>
- Step 3 of `ingest-rules.md` prescribes the copy to `rulebook/source/<original-filename>`, the `shasum -a 256` (or `sha256sum`) computation, the four always-emitted header lines, and the always-emitted `## Open Rules Gaps` built only from `openGaps[]`.
- The interview path emits the same four labels with explicit not-applicable values.
- Three new `v4.9 `-prefixed test blocks pin all of it, each proven to fail when its subject is removed.
</success_criteria>

<output>
Create `.planning/phases/170-ingest-contract-upgrade/170-02-SUMMARY.md` when done. Record all three
Task 3 adversarial revert-probe results explicitly — they are this plan's PROC-02 evidence.
</output>
