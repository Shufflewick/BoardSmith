---
phase: 170-ingest-contract-upgrade
plan: 03
type: execute
wave: 3
depends_on: ["170-01", "170-02"]
files_modified:
  - .planning/phases/170-ingest-contract-upgrade/170-PROOF-RUN.md
  - .planning/phases/170-ingest-contract-upgrade/170-PROOF-INDEX.md
autonomous: false
requirements: [PROC-01]
must_haves:
  truths:
    - "A real `/bs-ingest-rules` run, following the edited skill text, produced `rulebook/source/rules.pdf` inside a throwaway project."
    - "The `Source hash:` line in that run's `rulebook/INDEX.md` equals the SHA-256 an independent `shasum -a 256` computes over the archived file."
    - "That run's `rulebook/INDEX.md` carries all four header lines and a `## Open Rules Gaps` section."
    - "`~/BoardSmithGames/seven` is unmodified by the proof run."
  artifacts:
    - path: ".planning/phases/170-ingest-contract-upgrade/170-PROOF-INDEX.md"
      provides: "Verbatim capture of the real run's rulebook/INDEX.md — the derivation source for Plan 04's golden fixture"
      contains: "Source hash:"
    - path: ".planning/phases/170-ingest-contract-upgrade/170-PROOF-RUN.md"
      provides: "PROC-01 evidence record: commands run, observed outputs, hash comparison, seven-unmodified proof"
      contains: "5138858e789452d6d366e3ba3a898b5d5417a3561ee23bd53123fd98fe337880"
  key_links:
    - from: "src/cli/slash-command/bs/ingest-rules.md"
      to: "throwaway project rulebook/INDEX.md"
      via: "local skill install + real /bs-ingest-rules session"
      pattern: "rulebook/source/"
---

<objective>
PROC-01: prove the Plan 01 + Plan 02 skill-text edits against a **real ingest run**, not against
skill-text review alone — then capture that run's `rulebook/INDEX.md` verbatim so Plan 04 can
derive its golden fixture from real output rather than hand-authoring it.

Purpose: A skill-text contract test proves only that the *instructions* say the right thing; it
cannot prove an agent following them produces the artifact. Closing that gap is the discipline this
phase exists to establish, and it must close on this phase's own work first.

Output: a captured real `INDEX.md` and a written PROC-01 evidence record.

**Ordering note (deliberate, per 170-RESEARCH.md Pitfall 3):** this plan is sequenced BEFORE the
golden-fixture plan. `seven`'s current on-disk `INDEX.md` predates this contract — it has no header
block at all and uses the pre-standardization heading
`## Open Rules Gaps (named-but-undefined in the source)`. Hand-adapting it would pin a shape the
skill text does not actually produce. The fixture must be derived from this run's output. That is
why Plan 04 sits in its own wave behind this human gate; the sequencing cost is accepted knowingly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/170-ingest-contract-upgrade/170-CONTEXT.md
@.planning/phases/170-ingest-contract-upgrade/170-RESEARCH.md
@.planning/phases/170-ingest-contract-upgrade/170-01-SUMMARY.md
@.planning/phases/170-ingest-contract-upgrade/170-02-SUMMARY.md

**Known-good reference values (verified during research, 2026-07-27):**
- `~/BoardSmithGames/seven/rules.pdf` — 2,194,346 bytes, PDF v1.3, 2 pages
- its SHA-256: `5138858e789452d6d366e3ba3a898b5d5417a3561ee23bd53123fd98fe337880`

`~/BoardSmithGames/seven` is a **read-only reference game** for this phase. Nothing in this plan
writes to it. `one-two-punch` is reserved for later phases and is not touched at all.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage the throwaway project and install the edited skills locally</name>
  <files>—</files>
  <read_first>
    - bin/boardsmith.js (how the dev-repo CLI dispatches non-compiled commands from source, so the install below runs the edited skill text rather than a stale build)
    - src/cli/commands/install-claude-command.ts (the `--local` to `./.claude/skills` destination, `SHARED_DIRS`, and the `bs-shared/` layout the installed tree must have)
    - src/cli/slash-command/bs/ingest-rules.md (the edited Step 3 — you must be able to recognize its instructions in the installed copy)
  </read_first>
  <action>
Create the throwaway parent directory `/tmp/bs-170-ingest-proof`, outside every tracked repo. If it
already exists from a prior attempt, delete and recreate it so the run starts from the Step 0
"empty / fresh directory" case.

Install the EDITED skill text into that directory's project-local skill tree by invoking this
repo's CLI with the throwaway dir as the working directory: run
`node <BoardSmith-repo-root>/bin/boardsmith.js claude --local --force` with cwd
`/tmp/bs-170-ingest-proof`. The `--local` flag targets `./.claude/skills`; the global
`~/.claude/skills/` tree must not be touched by this plan at any point.

Capture the pre-run baseline for the non-mutation proof: `git -C ~/BoardSmithGames/seven status
--porcelain`, `git -C ~/BoardSmithGames/seven rev-parse HEAD`, and
`shasum -a 256 ~/BoardSmithGames/seven/rules.pdf`. Confirm the hash equals the reference value
above. If it does not, STOP — the proof target drifted and the phase's pinned hash must be
re-derived before anything downstream trusts it.

Do not start the ingest run in this task. Task 2 is the human gate that runs it.
  </action>
  <verify>
    <automated>grep -l 'rulebook/source/' /tmp/bs-170-ingest-proof/.claude/skills/bs-ingest-rules/SKILL.md && grep -l 'openGaps' /tmp/bs-170-ingest-proof/.claude/skills/bs-shared/ingest/transcription.md</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/bs-170-ingest-proof/.claude/skills/bs-ingest-rules/SKILL.md` exists and contains `rulebook/source/` — proving the run will follow the EDITED text, not a pre-phase copy
    - `/tmp/bs-170-ingest-proof/.claude/skills/bs-shared/ingest/transcription.md` exists and contains `openGaps[]` and `Visual (p.`
    - `/tmp/bs-170-ingest-proof` contains no `SKETCH.md`, no `PROJECT.md`, no `rulebook/`, no `ASSETS.md` (Step 0 fresh-directory case)
    - `shasum -a 256 ~/BoardSmithGames/seven/rules.pdf` outputs `5138858e789452d6d366e3ba3a898b5d5417a3561ee23bd53123fd98fe337880`
    - `git -C ~/BoardSmithGames/seven status --porcelain` produces empty output (clean baseline recorded)
    - The install output names a destination under `/tmp/bs-170-ingest-proof/.claude/skills`, and `~/.claude/skills` has no new or modified `bs-*` entry
  </acceptance_criteria>
  <done>The throwaway dir is fresh, the edited skill text is installed project-locally in it, the global skill tree is untouched, and the `seven` clean/hash baseline is recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run /bs-ingest-rules for real against seven/rules.pdf and verify the produced artifacts</name>
  <read_first>
    - src/cli/slash-command/bs/ingest-rules.md (Steps 0-3 as edited — so the observed run can be compared against what the text actually instructs)
    - .planning/phases/170-ingest-contract-upgrade/170-VALIDATION.md ("Manual-Only Verifications" — the instruction list this checkpoint executes)
  </read_first>
  <action>
Pause and hand control to the operator. Present the `<what-built>` summary and the `<how-to-verify>`
checklist below verbatim, then wait. Do not run `/bs-ingest-rules` on the operator's behalf and do
not substitute a simulated or partial run — the point of PROC-01 is that a real agent session,
following the installed text with no author present to steer it, produces the artifacts. Resume only
on an explicit "approved" plus the pasted (a)-(g) outputs.
  </action>
  <what-built>
    Plan 01 edited `transcription.md` (the `Derived`/`Visual` decision test and the `openGaps[]`
    return field). Plan 02 edited `ingest-rules.md` Step 3 (source archive to
    `rulebook/source/<original-filename>`, `shasum -a 256`, the four-line INDEX header block, and the
    always-emitted `## Open Rules Gaps`) plus `interview-fallback.md`'s not-applicable header values.
    All are locked by `v4.9 `-prefixed contract tests in `ingest.test.ts`. Task 1 installed that
    exact text into `/tmp/bs-170-ingest-proof/.claude/skills`.
  </what-built>
  <how-to-verify>
    1. Start a Claude Code session whose working directory is `/tmp/bs-170-ingest-proof`.
    2. Invoke `/bs-ingest-rules`. When asked whether a rulebook exists, answer yes and give the
       source path `~/BoardSmithGames/seven/rules.pdf`. Answer the per-section confirmation prompts
       normally.
    3. Let the run reach at least the end of Step 3 (Synthesis). The artifacts under test are
       written there, before the Step 6 approval gate — the sketch itself is not what is being proven.
    4. In the created project directory, run each check below and paste the actual terminal output
       for each into the resume message:
       a. `ls -l rulebook/source/` shows `rules.pdf` at 2,194,346 bytes.
       b. `shasum -a 256 rulebook/source/rules.pdf` outputs
          `5138858e789452d6d366e3ba3a898b5d5417a3561ee23bd53123fd98fe337880`.
       c. `grep '^Source hash:' rulebook/INDEX.md` shows that same 64-hex value. State explicitly in
          the resume message whether (b) and (c) match.
       d. `grep -n '^Edition:' rulebook/INDEX.md`, then the same for `^Source:`, `^Source hash:`,
          `^Transcribed:` — all four present, each with a non-empty value.
       e. `grep -n '^## Open Rules Gaps' rulebook/INDEX.md` shows the heading spelled exactly
          `## Open Rules Gaps`, with no parenthetical suffix. Report whether its body lists entries
          or reads `_None._` — either is a pass; a missing section is a FAIL.
       f. `grep -n '^## Slices' rulebook/INDEX.md` and `grep -n '^## Term' rulebook/INDEX.md` both
          still match — the header addition did not disturb the tables downstream consumers parse.
       g. `git -C ~/BoardSmithGames/seven status --porcelain` produces empty output and
          `git -C ~/BoardSmithGames/seven rev-parse HEAD` matches the Task 1 baseline.
    5. If ANY of (a)-(g) fails, do NOT approve. Report the exact failure. A failure here means the
       skill text is wrong — a green contract test proves only that the instruction exists, not that
       an agent following it produces the artifact. The correct response is to fix the skill text in
       Plan 01/02's files, re-run Task 1's install, and re-run this checkpoint. Do not hand-patch the
       produced artifact and do not weaken the criteria.
  </how-to-verify>
  <acceptance_criteria>
    - Terminal output pasted for each of (a)-(g)
    - (b) and (c) show the identical 64-hex value `5138858e789452d6d366e3ba3a898b5d5417a3561ee23bd53123fd98fe337880`
    - (d) shows all four header lines present with non-empty values
    - (e) shows the bare `## Open Rules Gaps` heading present
    - (f) shows both pre-existing tables still present
    - (g) shows `~/BoardSmithGames/seven` clean and at the same HEAD as the Task 1 baseline
    - The absolute path of the created project directory is stated in the resume message (Task 3 needs it)
  </acceptance_criteria>
  <resume-signal>Paste the (a)-(g) outputs and the created project's absolute path, then type "approved" — or describe the failure.</resume-signal>
  <done>A real ingest run produced an archived + hashed source and a conforming INDEX.md, with the reference game provably unmodified.</done>
</task>

<task type="auto">
  <name>Task 3: Capture the run output and write the PROC-01 evidence record</name>
  <files>.planning/phases/170-ingest-contract-upgrade/170-PROOF-INDEX.md, .planning/phases/170-ingest-contract-upgrade/170-PROOF-RUN.md</files>
  <read_first>
    - The `rulebook/INDEX.md` produced by the Task 2 run, at the project path the checkpoint reported
    - .planning/phases/170-ingest-contract-upgrade/170-VALIDATION.md (the Manual-Only Verifications row this record satisfies)
  </read_first>
  <action>
Copy the produced `rulebook/INDEX.md` verbatim and unedited to
`.planning/phases/170-ingest-contract-upgrade/170-PROOF-INDEX.md`. Do not tidy, reflow, or correct
it — its whole value is that it is real, unretouched output. Prepend nothing; any provenance note
goes in `170-PROOF-RUN.md` instead.

Write `.planning/phases/170-ingest-contract-upgrade/170-PROOF-RUN.md` recording, in this order: the
exact install command and its destination; the exact ingest invocation and source path used; the
created project's absolute path; the pasted (a)-(g) outputs from the checkpoint verbatim; the
explicit hash-match statement; the explicit `seven`-unmodified statement with before/after HEAD; and
a closing "what the run revealed" section naming any divergence between what the skill text
instructs and what the agent actually did. Write "none observed" only if that is true — a divergence
found here is a finding to file, not a blemish to smooth over.

Only after both files exist and `170-PROOF-INDEX.md` is non-empty, remove the throwaway tree
`/tmp/bs-170-ingest-proof`. Deleting before capture destroys the Plan 04 fixture source — this is
exactly the mistake Phase 149 made and Phase 150 had to redo.
  </action>
  <verify>
    <automated>grep -l '^Source hash:' .planning/phases/170-ingest-contract-upgrade/170-PROOF-INDEX.md && grep -c '^## Open Rules Gaps$' .planning/phases/170-ingest-contract-upgrade/170-PROOF-INDEX.md && grep -l '5138858e789452d6d366e3ba3a898b5d5417a3561ee23bd53123fd98fe337880' .planning/phases/170-ingest-contract-upgrade/170-PROOF-RUN.md</automated>
  </verify>
  <acceptance_criteria>
    - `170-PROOF-INDEX.md` exists, is non-empty, and contains a line starting `Source hash:`
    - `170-PROOF-INDEX.md` contains a line exactly equal to `## Open Rules Gaps`
    - `170-PROOF-INDEX.md` contains all four header labels `Edition:`, `Source:`, `Source hash:`, `Transcribed:`
    - `170-PROOF-RUN.md` contains the literal hash `5138858e789452d6d366e3ba3a898b5d5417a3561ee23bd53123fd98fe337880` and an explicit statement that the INDEX line matched the independently computed value
    - `170-PROOF-RUN.md` contains a "what the run revealed" section
    - `/tmp/bs-170-ingest-proof` no longer exists
    - `git -C ~/BoardSmithGames/seven status --porcelain` still produces empty output after cleanup
  </acceptance_criteria>
  <done>The real run's INDEX.md is captured verbatim in the phase directory as Plan 04's fixture source, the PROC-01 evidence record is written, and the throwaway tree is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edited skill text to autonomous agent, running inside a real project dir | The proof run lets an agent perform real filesystem writes while following newly-edited instructions. |
| proof run to reference game `~/BoardSmithGames/seven` | The run reads the designer's rulebook; it must never write to that repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-170-07 | Tampering | `~/BoardSmithGames/seven` (reference game) | mitigate | Task 1 records a clean-tree + HEAD baseline; checkpoint criterion (g) and Task 3's post-cleanup check both re-assert `git status --porcelain` is empty and HEAD unchanged |
| T-170-08 | Tampering | the operator's global `~/.claude/skills/` tree | mitigate | Install is `--local` into the throwaway dir only; Task 1 acceptance requires the destination path be under `/tmp/bs-170-ingest-proof` and the global tree show no new/modified `bs-*` entry |
| T-170-09 | Repudiation | PROC-01 claim ("we proved it") | mitigate | `170-PROOF-RUN.md` records verbatim command output rather than a narrative assertion, and `170-PROOF-INDEX.md` preserves the unedited artifact |
| T-170-SC | Tampering | npm/pip/cargo installs | accept | No package installs; `boardsmith init` inside the throwaway project may install this repo's own already-audited dependency set into a `/tmp` tree that is deleted at Task 3 |

No high-severity threats. The run happens on the operator's own machine against the operator's own file, in a `/tmp` directory that is removed on completion.
</threat_model>

<verification>
- The checkpoint's (a)-(g) outputs are pasted in the SUMMARY, not summarized.
- `170-PROOF-INDEX.md` is byte-identical to the run's `rulebook/INDEX.md`.
- `~/BoardSmithGames/seven` is clean before and after.
</verification>

<success_criteria>
- A real `/bs-ingest-rules` run under the edited skill text archived + hashed `rules.pdf` and produced an INDEX.md carrying all four header lines plus `## Open Rules Gaps`.
- The recorded `Source hash:` matches an independently computed `shasum -a 256`.
- The reference game is provably unmodified.
- The run's INDEX.md is captured in-repo as the derivation source for Plan 04's golden fixture.
</success_criteria>

<output>
Create `.planning/phases/170-ingest-contract-upgrade/170-03-SUMMARY.md` when done. This SUMMARY is
the phase's PROC-01 record — reproduce the (a)-(g) evidence in it rather than pointing elsewhere.
</output>
