---
phase: 171-provenance-recording
plan: 06
type: execute
wave: 5
depends_on: [171-04, 171-05]
files_modified:
  - src/cli/slash-command/bs/templates/CHUNK.template.md
  - src/cli/slash-command/bs/build/close.md
  - src/cli/slash-command/bs/build/playtest.md
  - src/cli/slash-command/bs/state-machine.md
  - src/cli/slash-command/bs/check-status.md
  - src/cli/slash-command/bs/build-chunk.test.ts
  - src/cli/slash-command/bs/status-tools.test.ts
  - src/cli/slash-command/bs/templates.test.ts
autonomous: true
requirements: [PROV-01, PROV-03]

must_haves:
  truths:
    - "A freshly scaffolded CHUNK.md already contains the fenced `## Verified Against` section, so the fence exists before any session could be tempted to author the block by hand."
    - "Both close paths invoke `boardsmith chunk-check <slug>` — the full `close` step AND the light path that runs the Bookkeeping Sequence from inside `playtest`."
    - "`/bs-check-status` reports verification drift by formatting `boardsmith chunk-provenance-status --json`, computing nothing itself."
    - "The count pins that describe the Bookkeeping Sequence agree with its actual length in every file that states it."
  artifacts:
    - path: "src/cli/slash-command/bs/templates/CHUNK.template.md"
      provides: "the `## Verified Against` machine-owned section, fences included"
      contains: "boardsmith:verified-against:begin"
    - path: "src/cli/slash-command/bs/build/close.md"
      provides: "Bookkeeping Sequence item invoking chunk-check, reused by name from the light path"
      contains: "boardsmith chunk-check"
    - path: "src/cli/slash-command/bs/check-status.md"
      provides: "the verification-provenance report item, formatted from --json"
      contains: "chunk-provenance-status --json"
  key_links:
    - from: "build/playtest.md light path"
      to: "close.md's Bookkeeping Sequence"
      via: "cited by name, not duplicated"
      pattern: "Bookkeeping Sequence"
    - from: "check-status.md"
      to: "boardsmith chunk-provenance-status --json"
      via: "the skill formats, never computes"
      pattern: "chunk-provenance-status --json"
---

<objective>
Wire the two commands into the pipeline: scaffold the fence into new chunks, invoke `chunk-check`
from BOTH close paths, and give `/bs-check-status` a provenance item backed by the `--json`
aggregation.

Purpose: skill text's only job in this phase is to invoke a command. This plan writes the smallest
amount of instruction that does that, and nothing else.
Output: template + close + playtest + state-machine + check-status edits, with every count pin and
drift test updated to match.

**Honest statement of what this plan CANNOT prove.** The `close`-invokes-the-command step is skill
text, and Phase 170 established over fourteen live runs that a contract test proves an instruction
EXISTS, never that a live session receives or follows it. `/bs-build-chunk` Step 0's `ingest-check`
call from Phase 170 has still never been exercised by a live session. The load-bearing guarantees in
this phase are therefore NOT the instructions in this plan — they are (1) the machine-owned fence a
session declines to edit, and (2) plan 05's `verifiedWithoutProvenance` flag, which surfaces a
skipped invocation. Every test added here asserts only that the instruction exists. Write that
sentence into the test file, not just into this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/171-provenance-recording/171-CONTEXT.md
@.planning/phases/170-ingest-contract-upgrade/170-MECHANISMS.md

**Both close paths.** `build/close.md`'s Bookkeeping Sequence is ALSO run by the light path from
inside `build/playtest.md`, which cites the section BY NAME rather than duplicating it. Any new
bookkeeping must land on both paths or trivial chunks silently skip provenance.

The count is pinned in four places, verified 2026-07-28:
  src/cli/slash-command/bs/build/close.md:25          "The light path reuses **only** this five-item sequence."
  src/cli/slash-command/bs/build/playtest.md:166      "the exact **five-item** sequence"
  src/cli/slash-command/bs/state-machine.md:41-48     the light-path paragraph enumerating the same duties
  src/cli/slash-command/bs/build-chunk.test.ts:747    expect(close).toMatch(/light path reuses \*\*only\*\* this five-item sequence/i)
Growing the sequence to six items means updating all four. Leaving one stale reproduces exactly the
class of self-contradicting contract Phase 170 spent itself on.

`check-status.md:37` says "Synthesize exactly the following seven items — this is the canonical
contract. Do not add or omit items", and line 108 says "Present all seven items together". Adding an
eighth item means updating both sentences. `status-tools.test.ts:134` pins item 7 by name, which is
additive-safe.

<interfaces>
Commands available from plans 171-04 / 171-05:
  boardsmith chunk-check <slug> [--project <dir>] [--json]
      writes/repairs the block; exits non-zero if it had to
  boardsmith chunk-provenance-status [--project <dir>] [--json]
      read-only aggregation; three states (full | code-conformance-only | unknown) plus
      verifiedWithoutProvenance[]

Constants exported from src/cli/commands/chunk-provenance.ts, to be used VERBATIM in the template so
the writer and the scaffold cannot drift:
  VERIFIED_AGAINST_HEADING  '## Verified Against'
  VERIFIED_AGAINST_BEGIN    '<!-- boardsmith:verified-against:begin -->'
  VERIFIED_AGAINST_END      '<!-- boardsmith:verified-against:end -->'
  VERIFIED_AGAINST_EMPTY    '_Not yet recorded._'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the fenced section into CHUNK.template.md</name>
  <read_first>
    - src/cli/slash-command/bs/templates/CHUNK.template.md lines 170-192 (`## Verified Checklist` and `## Verified Commit Hash` — the new section is their sibling and follows them)
    - src/cli/commands/ingest-archive.ts lines 88-112 (`renderIndex`'s machine-owned explanatory comment — the template's comment must say the same things in the same voice)
    - src/cli/slash-command/bs/templates.test.ts (the existing template pin style)
  </read_first>
  <action>
    Append a `## Verified Against` section to `CHUNK.template.md` immediately after
    `## Verified Commit Hash`. It carries:
    - a comment stating what writes it (`boardsmith chunk-check <slug>`), that it is machine-owned,
      that anything hand-written between the fences is overwritten on the next run, and WHY it is
      fenced rather than politely requested (the 2026-07-28 evidence: a session filled a
      machine-owned section by hand and the result looked entirely healthy while being wrong)
    - the begin fence, the `_Not yet recorded._` placeholder body, the end fence — byte-identical to
      the exported constants

    Add a test to `templates.test.ts` (in the existing requirement-ID describe style, named for
    PROV-01) asserting the template contains the heading and BOTH fence strings, and asserting the
    fence strings are byte-identical to the constants imported from
    `../../commands/chunk-provenance.js`. Importing the constants rather than re-typing them is the
    point: a re-typed marker is the duplication trap `PRESENTATION_LEXICON` already had to be pinned
    against.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/templates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "boardsmith:verified-against:begin" src/cli/slash-command/bs/templates/CHUNK.template.md` returns 1, and the same for `:end`.
    - `npx vitest run src/cli/slash-command/bs/templates.test.ts` passes, including the new byte-identity assertion against the imported constants.
    - The section appears AFTER `## Verified Commit Hash` (assert by comparing `indexOf` positions in the test).
  </acceptance_criteria>
  <done>New chunks arrive with the fence already present, byte-identical to what the writer expects.</done>
</task>

<task type="auto">
  <name>Task 2: Invoke chunk-check from both close paths, and re-pin the sequence length</name>
  <read_first>
    - src/cli/slash-command/bs/build/close.md (whole file — the Bookkeeping Sequence's numbering, its "cite, never restate" discipline, and the light-path reuse paragraph at lines 20-33)
    - src/cli/slash-command/bs/build/playtest.md lines 155-175 (the Light-Path Bookkeeping section that cites close.md by name)
    - src/cli/slash-command/bs/state-machine.md lines 29-49 (the light-path step names and duty enumeration)
    - src/cli/slash-command/bs/build-chunk.test.ts around line 747 (the five-item pin)
  </read_first>
  <action>
    In `build/close.md`, insert a new Bookkeeping Sequence item immediately after the
    verified-commit-hash item (so provenance is recorded against the same commit that item anchors),
    renumbering the following items:

      **Record what this chunk was verified against.** Run `boardsmith chunk-check <slug>`. It
      computes the verification scope, resolves this chunk's cited slices, and writes the
      machine-owned `## Verified Against` block. A NON-ZERO exit means it had to write or repair the
      block: re-read `chunks/<slug>/CHUNK.md` (the copy in context is stale) and re-run the command,
      which will then pass. Do not hand-author anything between the block's fences — that section is
      written by this command and by nothing else.

    Keep it that short. Do not restate the block's field list, the scope rules, or the reason codes —
    the command owns all of that, and restating a mechanical contract in prose is the exact pattern
    Phase 170 disproved twelve times.

    Then update every place that states the sequence's length: close.md's "five-item" line,
    playtest.md's "**five-item**", state-machine.md's light-path duty enumeration (add the provenance
    duty to the list it already gives), and `build-chunk.test.ts:747`'s regex. Before finishing, run
    `grep -rn "five-item\|five item" src` and confirm zero remaining occurrences.

    Add tests to `build-chunk.test.ts` asserting: close.md contains the literal string
    `boardsmith chunk-check`; playtest.md's Light-Path Bookkeeping section still cites
    `close.md`'s `## Bookkeeping Sequence` BY NAME and does not duplicate the new item's text
    (assert playtest.md does NOT contain `boardsmith chunk-check` — reuse by citation is the
    contract, and a duplicated copy is how the two paths drift apart).

    Head the new tests with a comment stating verbatim: these assertions prove the instruction
    EXISTS. Phase 170 established across fourteen live runs that they cannot prove a live session
    follows it. The load-bearing guarantees are the machine-owned fence and
    `chunk-provenance-status`'s `verifiedWithoutProvenance` flag.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "boardsmith chunk-check" src/cli/slash-command/bs/build/close.md` returns >= 1.
    - `grep -c "boardsmith chunk-check" src/cli/slash-command/bs/build/playtest.md` returns 0 (cited by name, never duplicated).
    - `grep -rn "five-item\|five item" src` returns nothing.
    - `grep -c "six-item" src/cli/slash-command/bs/build/close.md src/cli/slash-command/bs/build/playtest.md` returns 1 for each file.
    - state-machine.md's light-path paragraph names the provenance duty.
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts` passes.
  </acceptance_criteria>
  <done>Both close paths route through the same single cited sequence, which now records provenance, and no stale count remains anywhere in src.</done>
</task>

<task type="auto">
  <name>Task 3: /bs-check-status reports verification drift from the --json aggregation</name>
  <read_first>
    - src/cli/slash-command/bs/check-status.md (whole file — the seven-item contract, the read-only posture section, and the Reference Files list)
    - src/cli/slash-command/bs/status-tools.test.ts around line 134 (the per-item pin style)
  </read_first>
  <action>
    Add an eighth item to `check-status.md`'s report, and update BOTH sentences that state the count
    ("exactly the following seven items" at line 37 and "Present all seven items" at line 108).

      **8. Verification provenance and drift.** Run `boardsmith chunk-provenance-status --json` and
      FORMAT its output — do not compute any of it here. Report: how many chunks are `full`,
      `code-conformance-only` (with each one's reason code), and `unknown`; which rulebook edition and
      skills-tree hash each group was verified against, calling out drift when more than one of either
      is present; and, under its own heading, every slug in `verifiedWithoutProvenance` — chunks that
      claim verification with no provenance record behind them. `unknown` means the chunk was verified
      before provenance recording existed; it is not the same as `code-conformance-only` and must not
      be reported as it.

    Explicitly note that this command is read-only, so item 8 does not violate this skill's
    no-writes-of-any-kind posture — and add the same note to the `## Read-Only Posture (explicit)`
    section so the posture stays self-evidently true after this change.

    Add `boardsmith chunk-provenance-status` to the Reference Files list framing (it is a command,
    not a file — list it under the body where the item is described rather than mangling the file
    list).

    Add tests to `status-tools.test.ts` in the existing per-item style: item 8 is enumerated; the
    literal `boardsmith chunk-provenance-status --json` appears; the word `unknown` is described as
    distinct from `code-conformance-only`; `verifiedWithoutProvenance` is named; and the file still
    contains no instruction to write. Carry the same "this proves the instruction exists, not that it
    is followed" comment.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/status-tools.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "chunk-provenance-status --json" src/cli/slash-command/bs/check-status.md` >= 1.
    - `grep -c "seven items" src/cli/slash-command/bs/check-status.md` returns 0; `grep -c "eight items" ...` returns 2.
    - `grep -c "verifiedWithoutProvenance" src/cli/slash-command/bs/check-status.md` >= 1.
    - `npx vitest run src/cli/slash-command/bs/status-tools.test.ts` passes with the new item-8 tests.
    - `npm test` and `npx tsc --noEmit` both exit 0.
  </acceptance_criteria>
  <done>/bs-check-status reports the three states and the flagged chunks by formatting JSON it did not compute.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill text → live session | an instruction may simply not be executed (the Phase 170 finding); this boundary is untrusted by construction |
| scaffolded template → machine writer | a re-typed fence marker in the template would silently diverge from the writer's constant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-21 | Repudiation | `close` skipping `boardsmith chunk-check` | transfer | Cannot be mitigated in skill text — twelve instruction-shaped mechanisms failed in Phase 170. Transferred to `verifiedWithoutProvenance` (plan 05), which surfaces the omission, and to the scaffolded fence, which removes the hand-authoring alternative |
| T-171-22 | Tampering | light path silently skipping provenance | mitigate | The item lives in the single sequence both paths run; playtest.md is asserted NOT to duplicate the text, so the two cannot drift |
| T-171-23 | Tampering | template fence drifting from the writer's constant | mitigate | The template test imports the constants and asserts byte-identity rather than re-typing the markers |
| T-171-24 | Elevation of privilege | `/bs-check-status` gaining a write path | mitigate | Item 8 is backed by a command with no write path (pinned by plan 05's read-only test); the posture section is updated to state this explicitly |
| T-171-25 | Spoofing | a stale count pin describing a six-item sequence as five | mitigate | `grep -rn "five-item" src` returning nothing is an acceptance criterion |
| T-171-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed by this plan |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/` — green (build-chunk, status-tools, templates, ingest)
- `grep -rn "five-item\|five item" src` — no output
- `npm test` + `npx tsc --noEmit` — green
</verification>

<success_criteria>
- The fence ships in the template, byte-identical to the writer's constants.
- Both close paths record provenance via one cited sequence; no count pin is stale.
- `/bs-check-status` formats the aggregation and remains provably read-only.
- The tests state, in the file, that they prove existence and not adherence.
</success_criteria>

<output>
Create `.planning/phases/171-provenance-recording/171-06-SUMMARY.md` when done. It MUST carry a
"What this does not prove" section naming the Phase 170 skip risk and the still-unexercised
`ingest-check` invocation from `/bs-build-chunk` Step 0.
</output>
