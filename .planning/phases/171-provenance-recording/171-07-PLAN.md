---
phase: 171-provenance-recording
plan: 07
type: execute
wave: 6
depends_on: [171-05, 171-06]
files_modified:
  - .planning/phases/171-provenance-recording/171-PROOF.md
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03]

must_haves:
  truths:
    - "The new commands are demonstrated against real bs-built games, not only against temp fixtures."
    - "All 29 chunks across the two reference games report `unknown` before any chunk-check runs, and every one of them is flagged as claiming verification with no provenance record."
    - "Running chunk-check on a real chunk writes a block recording `code-conformance-only` with reason `pre-provenance-project`, exits 1, and exits 0 on the immediate re-run."
    - "`~/BoardSmithGames/seven` is byte-identical and at commit a03f38d4792af9dfc7c798be69686fc3230f54dd before and after this plan."
  artifacts:
    - path: ".planning/phases/171-provenance-recording/171-PROOF.md"
      provides: "the PROC-01 record: real commands, real output, real counts, and an honest limitations section"
      contains: "pre-provenance-project"
  key_links:
    - from: "the new CLI commands"
      to: "~/BoardSmithGames/one-two-punch and seven (copies)"
      via: "node bin/boardsmith.js <cmd> --project <copy>"
      pattern: "--project"
---

<objective>
Satisfy PROC-01 for this phase: every change is proven against a real bs-built game, not only against
the skill text or a temp fixture. Unit tests against fixtures are necessary and not sufficient —
this phase's predecessor shipped green twice on a broken contract.

Purpose: the two reference games are the ready-made proof target. All 29 of their chunks are
`unknown` and none has a provenance block, so the three-state classification, the flag, and the
`pre-provenance-project` reason code can all be demonstrated on real data on the first run.
Output: `171-PROOF.md` carrying real command invocations, verbatim output, real counts, and an
explicit statement of what is still unproven.

**READ-ONLY INVARIANT.** `~/BoardSmithGames/seven` is Phase 170's proof target and must stay clean at
`a03f38d4792af9dfc7c798be69686fc3230f54dd`. Never run a writing command against it. Copy it, work in
the copy, and assert cleanliness before AND after. `~/BoardSmithGames/one-two-punch` is likewise not
to be mutated — copy it too.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/171-provenance-recording/171-CONTEXT.md
@.planning/phases/170-ingest-contract-upgrade/170-MECHANISMS.md

Standing policy from Phase 170, recorded in STATE.md: the ingest harness may inform, and must never
again gate whether a manual pass is run. It certified a broken contract twice. This plan does not use
the harness at all.

<interfaces>
How to run the CLI against a project outside this repo — `bin/boardsmith.js` detects the `.git`
directory and runs `src/cli/cli.ts` through tsx, so no build step is needed:

  node /Users/jtsmith/BoardSmith/bin/boardsmith.js chunk-provenance-status --project <dir> --json
  node /Users/jtsmith/BoardSmith/bin/boardsmith.js chunk-check <slug> --project <dir>

Live target state, verified 2026-07-28 (these are the expected pre-run values):
  ~/BoardSmithGames/one-two-punch  12 chunk directories, every chunk verified or verified (user-waived),
                                   no `## Verified Against` block anywhere,
                                   rulebook/: 00-visual-survey.md, 01-setup-and-round-structure.md,
                                              02-action-cards-and-resolution.md, INDEX.md
                                   INDEX.md has NO `Source hash:` line and NO `## Slices` heading
  ~/BoardSmithGames/seven          17 chunk directories, same status picture, same absent block,
                                   rulebook/: 00-visual-survey.md, 01-definitions-and-components.md,
                                              01-overview-setup-and-play.md, 02-solo-variant.md, INDEX.md
                                   INDEX.md HAS a `## Slices` heading, no `Source hash:` line,
                                   and TWO slices beginning `01-` (so `rulebook/01` shorthand is ambiguous there)
  Both games: no rulebook/source/ directory; rules.pdf sits at the project root.
  Both games: non-canonical Edition: free text (the live F-1 data).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert the read-only invariant, copy both games, and capture the before-state</name>
  <read_first>
    - .planning/phases/170-ingest-contract-upgrade/170-MECHANISMS.md (the closing paragraph: "Never mutate ~/BoardSmithGames/seven")
    - src/cli/commands/chunk-provenance.ts (the two commands' actual flags, as plans 04/05 left them)
  </read_first>
  <action>
    1. Assert `git -C ~/BoardSmithGames/seven rev-parse HEAD` is exactly
       `a03f38d4792af9dfc7c798be69686fc3230f54dd` and `git -C ~/BoardSmithGames/seven status --porcelain`
       is empty. If either fails, STOP and report — do not proceed, and do not attempt to clean it.
       Record both outputs.
    2. Copy both games into the scratch directory with `cp -R` (never `git clone`, so untracked
       working state is reproduced faithfully). Work only in the copies from here on.
    3. Run `chunk-provenance-status --json` against each copy and capture the raw JSON.
    4. Record, for each game: total chunk count, the per-state counts, and the length of
       `verifiedWithoutProvenance`. Assert `unknown` equals the total chunk count and that
       `full` and `codeConformanceOnly` are both 0 — no chunk in either game has a block yet.
    5. Assert `verifiedWithoutProvenance` length equals the number of chunks whose `Status:` starts
       with `verified` (compute that count independently from the copies with grep, and compare —
       do not trust the command's own arithmetic to check itself).
  </action>
  <verify>
    <automated>node /Users/jtsmith/BoardSmith/bin/boardsmith.js chunk-provenance-status --project "$COPY_OTP" --json</automated>
  </verify>
  <acceptance_criteria>
    - `git -C ~/BoardSmithGames/seven rev-parse HEAD` prints `a03f38d4792af9dfc7c798be69686fc3230f54dd`; `git status --porcelain` prints nothing.
    - The one-two-punch copy reports `unknown: 12`, `full: 0`, `codeConformanceOnly: 0`.
    - The seven copy reports `unknown: 17`, `full: 0`, `codeConformanceOnly: 0`.
    - `verifiedWithoutProvenance.length` equals the independently-grepped verified-chunk count for each game.
    - The raw JSON of both runs is captured verbatim for 171-PROOF.md.
  </acceptance_criteria>
  <done>The read-only invariant holds, both copies exist, and the pre-run three-state picture is captured from real data.</done>
</task>

<task type="auto">
  <name>Task 2: Run chunk-check on real chunks in both copies and assert on actual output</name>
  <read_first>
    - src/cli/commands/chunk-provenance.ts (`chunkCheckCommand`'s exact output strings and JSON shape)
    - ~/BoardSmithGames/one-two-punch/chunks/jab/CHUNK.md (a real chunk — know what its `## Interpretation` cites before asserting on the resolved set)
  </read_first>
  <action>
    Working ONLY in the copies:

    1. Run `chunk-check` on every chunk of the one-two-punch copy. For each: capture the exit code
       and the JSON. Assert the first run exits 1 (the block did not exist) and that an immediate
       re-run of the same chunk exits 0 with the file byte-identical.
    2. Assert the recorded scope is `code-conformance-only` with reason `pre-provenance-project` for
       every chunk in both games — this is the live state (no `rulebook/source/`, no `Source hash:`),
       and it is the specific distinction CONTEXT.md decision 10 exists to preserve. If any chunk
       reports `source-missing` instead, that is a defect in the precedence order: STOP and report it
       rather than recording the wrong reason as expected behaviour.
    3. Assert the cited-slice resolution did real work on real prose: at least 11 of the 12
       one-two-punch chunks resolve at least one slice (11 of 12 reference a rulebook slice per the
       live census). Record the resolved and unresolved sets per chunk verbatim — including every
       unresolved citation, which is the visible-failure guarantee from CONTEXT.md decision 8.
    4. Run `chunk-check` on at least three chunks of the seven copy, chosen to include one whose
       prose uses the `rulebook/01` shorthand if any does. Seven has two `01-` slices, so that
       shorthand must land in `unresolved`, never resolve to a guess. Record the outcome either way.
    5. Spot-verify one recorded slice hash by computing the SHA-256 of that slice file independently
       (`shasum -a 256`) and comparing to the value in the block.
    6. Re-run `chunk-provenance-status --json` on both copies. Assert every chunk has moved from
       `unknown` to `code-conformance-only`, that `unknown` is now 0, and that
       `verifiedWithoutProvenance` is now empty — the flag clears exactly when the record exists.
    7. Verify the F-1 read-time normalisation on real data: both games' raw `Edition:` free text
       differs, yet the status report groups all chunks of each game under the single
       `not stated in the rulebook` edition bucket rather than a paraphrase.
  </action>
  <verify>
    <automated>node /Users/jtsmith/BoardSmith/bin/boardsmith.js chunk-provenance-status --project "$COPY_OTP" --json</automated>
  </verify>
  <acceptance_criteria>
    - Every first `chunk-check` run exits 1; every immediate re-run exits 0 and leaves the file byte-identical.
    - Every chunk in both copies records `Scope: code-conformance-only` and `Reason: pre-provenance-project`.
    - At least 11 of 12 one-two-punch chunks resolve >= 1 cited slice.
    - Any `rulebook/01` shorthand encountered in the seven copy appears in `unresolved`, and neither `01-` slice appears in that chunk's `resolved` set on the strength of that token.
    - The independently computed `shasum -a 256` of one slice matches the hash recorded in the block.
    - The post-run status shows `unknown: 0` and `verifiedWithoutProvenance: []` for both copies.
    - Both games' chunks group under exactly one normalised edition bucket each.
  </acceptance_criteria>
  <done>The commands are proven end-to-end against real bs-built games, with the reason code, the citation resolution, the hashes, and the flag all asserted on actual output.</done>
</task>

<task type="auto">
  <name>Task 3: Write 171-PROOF.md, including what is still unproven, and re-assert the invariant</name>
  <read_first>
    - .planning/phases/170-ingest-contract-upgrade/170-PROOF-RUN-2.md (the record shape this file should follow: what was run, what was observed, what failed, what remains unproven)
  </read_first>
  <action>
    Write `.planning/phases/171-provenance-recording/171-PROOF.md` containing:

    - **What was run** — every command, verbatim, with the copy paths.
    - **What was observed** — the before/after three-state counts for both games, the reason code, the
      per-chunk resolved/unresolved citation sets, the spot-checked hash, the exit codes on first and
      second runs, and the edition grouping.
    - **What this proves** — PROV-01 (a block is written with slice hashes, edition, source hash,
      version, skills-tree hash, scope), PROV-02 (scope computed, never declared; the five reason
      codes, with `pre-provenance-project` demonstrated on live data), PROV-03 (three states, drift
      grouping, and the verified-without-provenance flag both raising and clearing).
    - **What this does NOT prove**, stated plainly and without hedging:
      - That a live `/bs-build-chunk` `close` session actually invokes `boardsmith chunk-check`.
        That is skill text and carries the Phase 170 skip risk: twelve instruction-shaped mechanisms
        were each verified present in the installed tree and each skipped on live runs. The
        contract tests in plan 06 prove the instruction exists, never that it is followed.
      - That `/bs-build-chunk` Step 0's `ingest-check` invocation from Phase 170 works on a live
        session — it has still never been exercised by one.
      - Name the two load-bearing guarantees that do NOT depend on a session following an
        instruction: the machine-owned fence, and `verifiedWithoutProvenance`.
      - Recommend the live-session gate as a follow-up, and say explicitly that the ingest harness
        must not be used to substitute for it (STATE.md standing policy).
    - **Read-only invariant** — the before and after `rev-parse` / `status --porcelain` output for
      `~/BoardSmithGames/seven`, and the same for `one-two-punch`.

    Re-run both invariant assertions now, at the end, and record the results. Then delete the copies.
  </action>
  <verify>
    <automated>git -C ~/BoardSmithGames/seven rev-parse HEAD && git -C ~/BoardSmithGames/seven status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - `git -C ~/BoardSmithGames/seven rev-parse HEAD` still prints `a03f38d4792af9dfc7c798be69686fc3230f54dd` and `git status --porcelain` still prints nothing.
    - `git -C ~/BoardSmithGames/one-two-punch status --porcelain` prints nothing.
    - `171-PROOF.md` contains a `## What This Does NOT Prove` section naming both the `close` skip risk and the unexercised Step 0 `ingest-check` call.
    - `171-PROOF.md` records real numeric counts (12, 17, and the flag counts), not placeholders.
    - The scratch copies are removed.
  </acceptance_criteria>
  <done>The PROC-01 record exists, is honest about its limits, and the reference games are provably untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this plan → ~/BoardSmithGames/seven | a read-only artifact belonging to a shipped phase, outside this repo |
| this plan → ~/BoardSmithGames/one-two-punch | a real designer project used as live test data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-26 | Tampering | `~/BoardSmithGames/seven` | mitigate | Every writing command runs against a `cp -R` copy in scratch; cleanliness at `a03f38d4…` is asserted before Task 1 proceeds and again in Task 3, and the copies are deleted |
| T-171-27 | Tampering | `~/BoardSmithGames/one-two-punch` | mitigate | Same copy-and-assert discipline; `git status --porcelain` empty is an acceptance criterion |
| T-171-28 | Repudiation | a proof that overstates what it demonstrated | mitigate | A mandatory "What This Does NOT Prove" section naming the two specific unproven invocations; recording the wrong reason code as expected behaviour is an explicit STOP condition in Task 2 |
| T-171-29 | Spoofing | the command checking its own arithmetic | mitigate | The flag count is compared against an independently grepped count, and one slice hash against an independent `shasum -a 256` |
| T-171-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed; the CLI runs from this repo's working tree through tsx |
</threat_model>

<verification>
- `git -C ~/BoardSmithGames/seven rev-parse HEAD` == `a03f38d4792af9dfc7c798be69686fc3230f54dd`, working tree clean, before and after
- `git -C ~/BoardSmithGames/one-two-punch status --porcelain` empty after
- `171-PROOF.md` exists with real counts and a limitations section
</verification>

<success_criteria>
- Both reference games demonstrated: 29 chunks `unknown` before, 0 after, flag raised then cleared.
- `pre-provenance-project` demonstrated on live data, distinct from `source-missing`.
- Ambiguous shorthand demonstrated as unresolved rather than guessed.
- The proof states its own limits without hedging.
</success_criteria>

<output>
Create `.planning/phases/171-provenance-recording/171-07-SUMMARY.md` when done, and
`.planning/phases/171-provenance-recording/171-PROOF.md` as the durable record. Also file a todo for
F-3 from `170-PROOF-RUN-2.md` (ownership of `boardsmith.json`'s stub `description`/`playtime` after
`init` is unstated — two runs made opposite scope calls) at
`.planning/todos/pending/boardsmith-json-stub-ownership.md`; it is carried in from Phase 170 as a
file-a-todo item and belongs to no PROV requirement.
</output>
