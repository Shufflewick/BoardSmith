---
phase: 172-source-free-conformance-checks
plan: 04
type: execute
wave: 3
depends_on: [172-02, 172-03]
files_modified:
  - src/cli/cli.ts
  - src/cli/cli-conformance-commands.test.ts
autonomous: true
requirements: [CHECK-03, CHECK-05]

must_haves:
  truths:
    - "`boardsmith trace-check` and `boardsmith drift-check` are invokable from the CLI"
    - "Both accept `--project <dir>` and `--json` and nothing else"
    - "A run that reports findings exits 0"
    - "A run against a directory that is not a bs- project exits non-zero with a one-line message"
    - "No stack trace or internal path is ever printed"
  artifacts:
    - path: "src/cli/cli.ts"
      provides: "Two new commander registrations next to chunk-check/chunk-provenance-status"
      contains: "trace-check"
    - path: "src/cli/cli-conformance-commands.test.ts"
      provides: "End-to-end CLI-surface proof of registration and the exit-code contract"
      min_lines: 100
  key_links:
    - from: "src/cli/cli.ts"
      to: "src/cli/commands/trace-check.js"
      via: "import { traceCheckCommand } + program.command('trace-check')"
      pattern: "traceCheckCommand"
    - from: "src/cli/cli.ts"
      to: "src/cli/commands/drift-check.js"
      via: "import { driftCheckCommand } + program.command('drift-check')"
      pattern: "driftCheckCommand"
---

<objective>
Register both commands on the CLI surface and pin the exit-code contract that makes them advisory
sweeps rather than gates.

Purpose: decision 5 makes these two commands the phase's user-facing deliverable, and decision 6
makes "findings exit 0, tool failure exits non-zero" load-bearing — a check that fires non-zero on
correct work gets waived, and 29 existing chunks legitimately have gaps. Until the commands are
registered, plan 172-05's proof (which invokes `node bin/boardsmith.js trace-check --project <copy>`)
cannot run at all.

Output: two `.command()` registrations in `cli.ts`, plus the first test in this repo that exercises
a command through the real CLI entry point.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-source-free-conformance-checks/172-CONTEXT.md
@.planning/phases/172-source-free-conformance-checks/172-PATTERNS.md
@.planning/phases/172-source-free-conformance-checks/172-02-SUMMARY.md
@.planning/phases/172-source-free-conformance-checks/172-03-SUMMARY.md
@src/cli/cli.ts
@src/cli/lib/ingest-hook.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register trace-check and drift-check in cli.ts</name>
  <files>src/cli/cli.ts</files>
  <action>
Add two imports next to `cli.ts:20`'s `chunk-provenance.js` import:
`traceCheckCommand` from `./commands/trace-check.js` and `driftCheckCommand` from
`./commands/drift-check.js`.

Add two `program.command()` blocks immediately after the `chunk-provenance-status` registration
(`cli.ts:174-179`), matching its shape exactly — no positional argument (both operate over the whole
project), `--project <dir>` and `--json` options only, and a description that states read-only-ness
explicitly, reusing the established `(read-only)` phrasing:
  - `trace-check` — "Report traceability gaps between Interpretation claims, rulings, and tests (read-only)"
  - `drift-check` — "Report chunks whose Build Manifest files changed since their verified commit (read-only)"

Precede the pair with a short comment naming CHECK-03/CHECK-05 and 172-CONTEXT.md decision 5–6, in
the style of the existing "Provenance: record or repair..." comment at `cli.ts:165-166`, stating
that unlike `chunk-check` these two never write and never exit non-zero for a finding.

Do NOT add a mode flag, a `--source-free` flag, or a `--fix` flag. Phase 179 assembles the
source-free MODE; these commands are source-free by construction. `--project` already exists
uniformly — do not invent a new path flag.

Change nothing else in `cli.ts`: the existing `parseAsync()` try/catch at lines 195-207 already
gives both commands the "thrown Error becomes a clean one-line message plus exit 1" behaviour they
rely on.
  </action>
  <verify>
    <automated>node bin/boardsmith.js trace-check --help &amp;&amp; node bin/boardsmith.js drift-check --help</automated>
  </verify>
  <done>Both commands appear in `node bin/boardsmith.js --help`; each `--help` lists exactly
  `--project <dir>` and `--json`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the exit-code contract through the real CLI entry point</name>
  <files>src/cli/cli-conformance-commands.test.ts</files>
  <behavior>
    Each case spawns `node bin/boardsmith.js <cmd> --project <fixture> --json` as a child process
    (the only way to observe a real process exit code and prove registration):
    - `trace-check` against a fixture project WITH findings: exit code 0, stdout parses as JSON,
      `findings.length > 0`.
    - `drift-check` against a fixture git project WITH drift: exit code 0, stdout parses as JSON,
      at least one `chunk-code-drifted` finding.
    - `trace-check` against a directory with no `chunks/`: exit code 1, stderr is a single
      actionable line naming the directory and the `--project` flag, and contains no `at ` stack
      frame, no `.ts:` line reference, and no absolute path into this repo's `src/`.
    - `drift-check` against a directory that is not a git repo: exit code 1, same message
      discipline.
    - Neither command's fixture project is modified: whole-project byte-hash identical before and
      after the spawned run.
  </behavior>
  <action>
Create `src/cli/cli-conformance-commands.test.ts` (picked up by `vitest.config.ts`'s
`src/**/*.test.ts` include). Use the `mkdtemp`/`afterEach` scaffold from `chunk-provenance.test.ts`
for fixtures and the real-git-repo helper shape from `src/cli/lib/ingest-hook.test.ts:27-32` and
:71-79 (explicit `-c user.email=t@t -c user.name=t` on the commit — sandbox environments have no
configured identity) for the drift fixture.

Spawn via `execFile(process.execPath, [<repo>/bin/boardsmith.js, ...args])` and read the exit code
from the rejection's `code` property; `bin/boardsmith.js` routes to `src/cli/cli.ts` through `tsx`
in this dev repo, so no build step is needed. Keep it to four spawns — each costs a couple of
seconds, and the phase's fast feedback loop lives in the per-command unit test files.

This test is what proves decision 6 end to end: the unit tests in plans 02/03 can assert a returned
result, but only a real process can prove that a run full of findings still exits 0 and that a tool
failure exits 1 with no leaked internals.
  </action>
  <verify>
    <automated>npx vitest run src/cli/cli-conformance-commands.test.ts</automated>
  </verify>
  <done>Four spawned cases pass; findings runs exit 0; tool-failure runs exit 1 with a single clean
  line; fixture projects are byte-identical after each run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → command options | user-supplied `--project` path reaches `resolve()` and then the filesystem and git |
| thrown Error → stderr | an error message crossing to the terminal must not carry internal paths or stack frames |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-05 | Information disclosure | `cli.ts` top-level catch | mitigate | Already implemented at `cli.ts:195-207` — `parseAsync` is awaited and a thrown Error prints `err.message` only. Task 2 pins it for both new commands with an explicit assertion that stderr carries no `at ` frame, no `.ts:` reference, and no `src/` path. |
| T-172-09 | Elevation of privilege | `--project` pointing outside the user's project | accept | The flag is user-supplied on their own machine and both commands are strictly read-only; restricting it would break the proof harness's `--project <copy-dir>` use, which is the documented pattern. No secret is read and nothing is written. |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase; no install task exists. |
</threat_model>

<verification>
- `node bin/boardsmith.js trace-check --help` and `node bin/boardsmith.js drift-check --help`
- `npx vitest run src/cli/cli-conformance-commands.test.ts`
- `npm test`
</verification>

<success_criteria>
- Both commands are registered with exactly `--project <dir>` and `--json`, no mode flag.
- Findings exit 0; tool failure exits 1 with a one-line actionable message and no leaked internals.
- The CLI surface plan 172-05's proof harness invokes exists and works.
</success_criteria>

<output>
Create `.planning/phases/172-source-free-conformance-checks/172-04-SUMMARY.md` when done.
</output>
