---
phase: 172-source-free-conformance-checks
plan: 05
type: execute
wave: 4
depends_on: [172-04]
files_modified:
  - .planning/phases/172-source-free-conformance-checks/172-PROOF.md
  - .planning/phases/172-source-free-conformance-checks/172-VALIDATION.md
autonomous: true
requirements: [CHECK-03, CHECK-05]

must_haves:
  truths:
    - "Both commands ran against copies of both reference games and produced real, non-zero findings"
    - "Every reported count was cross-checked against an independently computed value"
    - "Neither reference-game original was modified — seven still clean at its pinned HEAD, one-two-punch byte-identical"
    - "The proof records what is still unproven, not only what passed"
  artifacts:
    - path: ".planning/phases/172-source-free-conformance-checks/172-PROOF.md"
      provides: "PROC-01-style real-data proof record: verbatim invocations, verbatim output, independently computed cross-checks, read-only confirmation, and a what-is-still-unproven section"
      min_lines: 120
      contains: "What is still unproven"
  key_links:
    - from: "172-PROOF.md"
      to: "~/BoardSmithGames/{seven,one-two-punch} COPIES"
      via: "node bin/boardsmith.js {trace,drift}-check --project <copy-dir> --json"
      pattern: "--project"
---

<objective>
Prove both commands end-to-end against real data: run them on COPIES of `~/BoardSmithGames/seven`
and `~/BoardSmithGames/one-two-punch`, cross-check every reported count against an independently
computed value, and record the result in `172-PROOF.md`.

Purpose: success criterion 3 — both checks demonstrated against at least one reference game,
surfacing real findings, not a dry no-op run. This is also the phase's de-risking function for
everything downstream: Phases 173–179 build on the assumption that these two checks work on the 29
existing chunks with no migration.

Output: `172-PROOF.md` mirroring `171-PROOF.md`'s shape, plus the filled-in status column of
`172-VALIDATION.md`'s Per-Task Verification Map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-source-free-conformance-checks/172-CONTEXT.md
@.planning/phases/172-source-free-conformance-checks/172-RESEARCH.md
@.planning/phases/172-source-free-conformance-checks/172-VALIDATION.md
@.planning/phases/171-provenance-recording/171-PROOF.md
@.planning/phases/171-provenance-recording/171-07-PLAN.md
@.planning/phases/172-source-free-conformance-checks/172-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read-only preflight, copies, and the real runs</name>
  <files>.planning/phases/172-source-free-conformance-checks/172-PROOF.md</files>
  <action>
**The read-only invariant is non-negotiable and comes first.** Before anything else:

1. `git -C ~/BoardSmithGames/seven rev-parse HEAD` MUST equal
   `a03f38d4792af9dfc7c798be69686fc3230f54dd`, AND `git -C ~/BoardSmithGames/seven status --porcelain`
   MUST be empty. If either fails, STOP the plan and report — do not proceed, do not attempt to
   clean it.
2. `git -C ~/BoardSmithGames/one-two-punch rev-parse HEAD` MUST equal
   `7e69471bd8980a854f3e351f2f486e1fb6f712b9`. Do NOT assert porcelain-empty there: it carries two
   pre-existing unrelated deletions (`.boardsmith/runtime-bundle.mjs`, `.boardsmith/runtime-entry.ts`)
   that predate this phase and predate Phase 171. Asserting clean would spuriously fail on a
   condition nothing in this phase caused.
3. Record a whole-tree byte manifest of BOTH originals (a sorted `find`-plus-`shasum` listing,
   including `.git`-excluded working files) so "unchanged after" is provable rather than asserted.

Then `cp -R` each original into a scratch directory. `cp -R`, never `git clone` — it preserves
untracked and dirty state faithfully AND preserves `.git` intact, so hash-ancestry checks inside the
copy resolve against the same history as the original. Every command from here on runs against the
COPY only; the originals are never passed as `--project` again in this plan.

Run all four invocations, capturing stdout verbatim to files in the scratch directory:
- `node /Users/jtsmith/BoardSmith/bin/boardsmith.js trace-check --project <copy> --json`
- `node /Users/jtsmith/BoardSmith/bin/boardsmith.js drift-check --project <copy> --json`
for each of the two copies. Also run each command WITHOUT `--json` once per game, so the human
report's grouping and count-first summary are exercised on real volume (measured expectation: dozens
to low hundreds of findings) rather than only on unit fixtures.

Record each command's exit code. All four must be 0 — these games have real gaps and real drift, and
decision 6 makes findings exit 0.

Begin `172-PROOF.md` with the preflight results, the copy locations, the verbatim invocations, and
the captured output (truncate long JSON to the counts plus a representative sample per finding kind,
and say explicitly where truncation happened).
  </action>
  <verify>
    <automated>git -C ~/BoardSmithGames/seven rev-parse HEAD | grep -q '^a03f38d4792af9dfc7c798be69686fc3230f54dd$' &amp;&amp; test -z "$(git -C ~/BoardSmithGames/seven status --porcelain)" &amp;&amp; git -C ~/BoardSmithGames/one-two-punch rev-parse HEAD | grep -q '^7e69471bd8980a854f3e351f2f486e1fb6f712b9$'</automated>
  </verify>
  <done>Both originals confirmed at their pinned HEADs; copies made with `cp -R`; six invocations
  (4 JSON + 2 human) captured verbatim with exit codes; all exit 0; all findings non-zero on at
  least one game per command.</done>
</task>

<task type="auto">
  <name>Task 2: Independent cross-check, read-only confirmation, and the proof record</name>
  <files>.planning/phases/172-source-free-conformance-checks/172-PROOF.md, .planning/phases/172-source-free-conformance-checks/172-VALIDATION.md</files>
  <action>
**Never trust the command's own arithmetic to validate itself.** For every count `172-PROOF.md`
records, compute the same number a second way, by a different method, and state both. At minimum:

- Total claims per game: an independent `grep -c '^[0-9]\+\. \*\*'` bounded between
  `## Interpretation` and the NEXT `## ` heading, per chunk, summed. RESEARCH.md measured 165 across
  one-two-punch's 12 chunks and 212 across seven's 17 — treat those as the expected independent
  values and investigate any disagreement rather than adjusting the expectation.
- Total rulings per game: an independent `grep -c '^### Ruling '` on each `RULINGS.md` (26 and 36).
- Drift per chunk: an independent `git diff --name-only <hash> HEAD` in the copy, intersected by
  hand against that chunk's manifest file list, for at least three chunks per game including the
  highest-drift one.
- The decision-12 exhibit: confirm one-two-punch's `jab` chunk manifest lists
  `src/ui/components/GuardCardView.vue`, that the file is absent from disk, and that
  `git log --diff-filter=D -- src/ui/components/GuardCardView.vue` shows the deleting commit — then
  confirm `drift-check` reports it as drift.
- Resolution-ladder outcome: for at least three real citations per game, hand-walk the three rungs
  (owners → live-claim validity → authoring) and confirm the command reached the same conclusion.

**Record ACTUAL post-ladder counts.** RESEARCH.md's predicted numbers (~67 `ambiguous-claim-ref` on
one-two-punch, `claim-untested` approaching 165/212) were computed under the SUPERSEDED pre-amendment
resolution rule and are UPPER BOUNDS, not expectations. If the actual ambiguous count is far lower,
that is the amended ladder working as designed — record it as measured, and say so.

Then confirm the read-only guarantee: re-run the whole-tree byte manifest for BOTH originals and
diff against the pre-run manifest. For `seven` also re-assert `git status --porcelain` empty and HEAD
unchanged. For `one-two-punch` assert byte-identical only (its two pre-existing deletions must still
be exactly as they were).

Finish `172-PROOF.md` mirroring `171-PROOF.md`'s shape, and include an explicit
**"What is still unproven"** section. Candidates known in advance, to be confirmed or corrected
against what the run actually shows:
- All 29 real hashes are valid HEAD-ancestors, so the `drift-unknown`-from-unresolvable-hash and
  not-an-ancestor paths are proven only by unit fixture, not by live data.
- Whichever ladder rung never turned out to be the deciding rung on live data.
- `test-unlinked`'s trigger, if no real file satisfies all three of its conditions.
- Whether the human report's grouping actually reads well at the real volume — a judgement, not a
  measurement, and should be labelled as one.
Report these honestly and specifically, in the register `171-07-SUMMARY.md` used when it recorded
that seven's live prose never exercised the ambiguous-shorthand path.

Finally, fill in `172-VALIDATION.md`'s Per-Task Verification Map status column with the observed
result for each task, and set `nyquist_compliant: true` / `wave_0_complete: true` in its frontmatter
once every row is green.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Every count in `172-PROOF.md` has an independently computed counterpart printed beside it;
  both originals confirmed byte-identical after the run (seven also porcelain-empty at its pinned
  HEAD); the "What is still unproven" section is present and specific; `172-VALIDATION.md`'s map is
  filled and its sign-off boxes are checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BoardSmith commands → reference-game repositories | the phase's central invariant is that these two repos are read, never written; a violation destroys the Phase 170/171 proof baseline too |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-06 | Tampering | reference-game originals | mitigate | Preflight HEAD/porcelain assertions with a hard STOP, `cp -R` copies, every `--project` pointed at a copy, and a whole-tree byte-manifest diff before and after. Tasks 1 and 2. |
| T-172-10 | Repudiation | proof credibility | mitigate | Every count is cross-checked by an independent method and both values are printed; the command's own arithmetic is never its own validator. An explicit "what is still unproven" section prevents the proof from over-claiming. Task 2. |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase; no install task exists. |
</threat_model>

<verification>
- `npm test` — full suite green (baseline 3407 plus this phase's new tests).
- `git -C ~/BoardSmithGames/seven status --porcelain` empty and HEAD unchanged after the run.
- `~/BoardSmithGames/one-two-punch` byte-identical after the run.
- `172-PROOF.md` exists with verbatim invocations, verbatim output, paired independent counts, and a
  "What is still unproven" section.
</verification>

<success_criteria>
- Both commands demonstrated on both reference games with real, non-zero findings — success
  criterion 3 satisfied without contrivance.
- Every reported number independently corroborated.
- Both originals provably untouched.
- `172-VALIDATION.md`'s Per-Task Verification Map fully populated and green.
</success_criteria>

<output>
Create `.planning/phases/172-source-free-conformance-checks/172-05-SUMMARY.md` when done.
</output>
