---
phase: 173-verify-pipeline-core
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/cli/commands/ingest-archive.ts
  - src/cli/commands/ingest-archive.test.ts
  - .planning/phases/173-verify-pipeline-core/173-PROOF.md
autonomous: true
requirements: [VERIFY-01]

must_haves:
  truths:
    - "Running `ingest-archive` on a project whose INDEX.md predates the four-header contract leaves that project computing `full` verification scope, not `pre-provenance-project`."
    - "A real designer-authored `Edition:` value survives an `ingest-archive` run that omits `--edition`."
    - "A wrapped, multi-line `Source:` paragraph is not truncated and its continuation line is not orphaned."
    - "An error raised while repairing an existing INDEX.md can never cause that INDEX.md to be replaced by a fresh scaffold."
    - "Running `ingest-archive` twice in a row produces a byte-identical INDEX.md the second time."
  artifacts:
    - path: "src/cli/commands/ingest-archive.ts"
      provides: "Repaired existing-INDEX branch: insert-if-absent header lines, wrap-safe Source:, Edition: preservation, branch decided outside the try/catch"
      contains: "HEADER_LABELS"
    - path: "src/cli/commands/ingest-archive.test.ts"
      provides: "Failing-first cases pinning all three decision-1b defects plus the scaffold-overwrite hazard"
      contains: "decision 1b"
    - path: ".planning/phases/173-verify-pipeline-core/173-PROOF.md"
      provides: "Wave-1 gate proof section against copies of both reference games"
      contains: "## 1. Wave-1 gate"
  key_links:
    - from: "src/cli/commands/ingest-archive.ts"
      to: "src/cli/commands/chunk-provenance.ts computeVerificationScope"
      via: "the `Source hash:` / `Source:` lines the repair writes are exactly what the scope function reads"
      pattern: "Source hash:"
---

<objective>
Fix `ingestArchiveCommand`'s existing-INDEX branch so that adopting a pre-provenance project
actually establishes provenance, and prove it against copies of BOTH reference games.

Purpose: this is the phase's HARD GATE (CONTEXT.md decision 1b). `ingest-archive` currently
reports `index: rulebook/INDEX.md provenance header updated (existing sections untouched)` while
silently no-op'ing two required inserts, corrupting a wrapped `Source:` line, and clobbering a
real `Edition:` value. Reproduced twice — by `173-RESEARCH.md` and independently by the phase
orchestrator against a `cp -R` copy of `seven`. Until this lands, decision 1's adoption path is
unimplementable and NO other success criterion in this phase can be proven against real data.

Output: a corrected existing-INDEX branch, colocated failing-first tests, and `173-PROOF.md` § 1
carrying the real-data gate evidence.

**Scope fence.** Bound to `ingestArchiveCommand`'s existing-INDEX branch. This is NOT a rewrite of
`ingest-archive`. The broader refuse-on-ambiguity hardening that `173-RESEARCH.md` Pitfall 1 step 2
proposes was considered by the user and DECLINED — do not implement it. If the fix surfaces more of
the false-success class, file a todo in `.planning/STATE.md` rather than expanding scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/173-verify-pipeline-core/173-CONTEXT.md
@.planning/phases/173-verify-pipeline-core/173-RESEARCH.md
@.planning/phases/173-verify-pipeline-core/173-PATTERNS.md
@.planning/phases/173-verify-pipeline-core/173-VALIDATION.md
@src/cli/commands/ingest-archive.ts
@src/cli/commands/ingest-archive.test.ts

<interfaces>
<!-- Contracts this plan writes against. Do not re-derive them by exploring the codebase. -->

From src/cli/commands/ingest-archive.ts (already exported — reuse, do not re-declare):
```typescript
export const INDEX_HEADINGS: readonly ['## Open Rules Gaps', '## Slices', '## Term → Slice'];
export const HEADER_LABELS: readonly ['Edition:', 'Source:', 'Source hash:', 'Transcribed:'];
export const EDITION_UNKNOWN = 'not stated in the rulebook';
export const EDITION_EMPTY_LEXICON: readonly string[];
export const EDITION_NOTE_LABEL = 'Edition note:';
export function normalizeEdition(raw: string | undefined): string;
export function renderIndex(params: {
  gameName: string; edition: string | undefined; archivedPath: string;
  sourceHash: string; transcribed: string;
}): string;
export interface IngestArchiveOptions { project?: string; edition?: string; json?: boolean }
```

From src/cli/commands/chunk-provenance.ts — the payoff check this fix must satisfy:
```typescript
export async function computeVerificationScope(projectDir: string): Promise<VerificationScope>;
// Precedence: no-rulebook-project -> index-missing -> pre-provenance-project (no `Source hash:`
// line at all) -> source-missing (file at `Source:`'s value unreadable) -> source-hash-mismatch
// -> full. It reads `/^Source:\s*(.*)$/m` (FIRST match, trimmed) and joins it to projectDir.
// Therefore the canonical `Source:` line MUST be a bare project-relative path, and it MUST be the
// first `^Source:` match in the file.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin all three decision-1b defects with failing-first tests</name>
  <files>src/cli/commands/ingest-archive.test.ts</files>
  <behavior>
    New describe block: `ingest-archive — existing INDEX.md adoption (decision 1b)`.

    Fixture helper `writePreProvenanceIndex(dir, opts)` builds a synthetic INDEX.md that reproduces
    `seven`'s real shape as captured in 173-RESEARCH.md Pitfall 1's Evidence block: a `# Rulebook
    Index — seven` title, a blank line, an `Edition:` line carrying the real designer-authored value
    `not stated in the rulebook (no edition/printing on cover, title page, or colophon) — pending
    designer confirmation`, a blank line, then a WRAPPED `Source:` paragraph whose first physical
    line is "Source: `rules.pdf` (2 pages). This index is the term → slice cross-reference. It is
    built from the" and whose second physical line is "transcription subagents' returned
    `citedTerms[]` lists.", then the `## Slices` and `## Term → Slice` sections with at least one
    real row each. It deliberately has NO `Source hash:` line, NO `Transcribed:` line, and NO
    gaps fences — matching both reference games.

    Behaviors (each is its own `it`, each currently RED):
    - B1 insert-if-absent (hash): after `ingestArchiveCommand` runs with no `--edition`, the file
      matches `/^Source hash: [0-9a-f]{64}$/m`, and the value equals the sha256 of the archived
      copy computed independently in the test (do not read it back from the command's own JSON).
    - B2 insert-if-absent (transcribed): the file matches `/^Transcribed: \d{4}-\d{2}-\d{2}$/m`.
    - B3 header order/contiguity: the four `HEADER_LABELS` lines appear in HEADER_LABELS order, and
      no non-empty line sits between `Source:` and `Transcribed:`.
    - B4 Edition preserved: with `--edition` omitted, the `Edition:` line is byte-identical to the
      fixture's original value — the designer-pending qualifier is NOT regressed to
      `EDITION_UNKNOWN`.
    - B5 Edition still settable: with `--edition "2nd edition"`, the line becomes
      `Edition: 2nd edition`.
    - B6 F-1 unchanged: with `--edition "none stated in the rulebook"`, the line normalizes to
      `Edition: ${EDITION_UNKNOWN}` and an `Edition note:` line carries the original — the existing
      Phase 170 F-1 behavior must not regress.
    - B7 wrap-safe: after the run, the fixture's wrapped prose is INTACT — the file still contains
      the exact substring "(2 pages). This index is the term → slice cross-reference. It is built
      from the" immediately followed on the next physical line by "transcription subagents'
      returned". No continuation line is orphaned and no prose byte is lost.
    - B8 canonical Source wins: `/^Source:\s*(.*)$/m.exec(file)[1].trim()` equals
      `rulebook/source/rules.pdf` — i.e. the canonical bare-path line is the FIRST `^Source:` match,
      which is what `computeVerificationScope` reads.
    - B9 bare-path in-place: a second fixture whose `Source:` is already a bare single-line path is
      replaced in place with no line inserted and no duplicate `Source:` line (count of
      `/^Source:/mg` matches === 1).
    - B10 payoff check: `computeVerificationScope(projectDir)` (imported from
      `./chunk-provenance.js`) returns `scope === 'full'` for the repaired fixture. This is the
      assertion that makes the whole fix meaningful — an exit code is not evidence.
    - B11 idempotence: running the command twice produces a byte-identical INDEX.md the second
      time (no duplicated header lines, no accumulating `Edition note:`).
    - B12 no scaffold-overwrite: with a fixture INDEX.md carrying a distinctive marker string in its
      `## Slices` table, after the run that marker is still present — the file was repaired, never
      replaced by `renderIndex`'s fresh scaffold. Assert on a forced-failure path too: make the
      repair throw (e.g. by chmod'ing the INDEX.md read-only, or by whatever minimal seam the
      implementation exposes) and assert the on-disk file is byte-identical to before, NOT a fresh
      scaffold.
  </behavior>
  <action>
    Add the block to the END of `src/cli/commands/ingest-archive.test.ts`, following the file's
    existing conventions: real-filesystem `mkdtempSync`/`rmSync` fixtures (no mocks), a real small
    binary written to `<tmp>/rules.pdf` to be archived, and direct calls to `ingestArchiveCommand`
    rather than shelling out. Import `computeVerificationScope` from `./chunk-provenance.js`
    (ESM `.js` extension is required — see CONVENTIONS.md "Path Extensions").

    Every test must be RED before Task 2 begins. Run the suite and record which of B1-B12 fail and
    with what message; a behavior that is unexpectedly GREEN means the fixture does not reproduce
    the real defect and the fixture is wrong, not the code — fix the fixture before proceeding.

    Do NOT write any implementation in this task.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/ingest-archive.test.ts 2>&1 | tail -40</automated>
    <expect>The new `decision 1b` describe block runs and at least B1, B2, B4, B7, B8 and B10 FAIL. Pre-existing cases in the file stay green.</expect>
  </verify>
  <done>The new describe block exists, executes, and is demonstrably RED on the defect behaviors; no production code changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair the existing-INDEX branch</name>
  <files>src/cli/commands/ingest-archive.ts</files>
  <behavior>All of B1-B12 from Task 1 go GREEN with no test edited to accommodate the implementation.</behavior>
  <action>
    Rewrite ONLY the existing-INDEX branch of `ingestArchiveCommand` (currently the
    `try { await fs.access(indexPath); ...repair... } catch { ...scaffold... }` block near the end
    of the file). Four changes, in this order:

    1. **Decide the branch before the try/catch.** Replace the access-inside-try control flow with
       an explicit existence probe whose result selects the branch, so the scaffold path is only
       ever reached when INDEX.md genuinely does not exist. Today ANY throw raised while repairing
       falls into the `catch` and overwrites the designer's INDEX.md with a fresh scaffold — that is
       a silent total-content-loss path and it is the reason B12 exists. This restructure is a
       prerequisite for the rest; do it first.

    2. **Insert-if-absent, per label.** Add a module-local helper that, given the file text and one
       of `HEADER_LABELS`, returns the match index and the line bounds of that label's line, using
       a line-anchored regex (`^Label:` with the `m` flag). For `Source hash:` and `Transcribed:`:
       when the label matches, replace that line; when it does not match, INSERT the line
       immediately after the canonical `Source:` line, preserving `HEADER_LABELS` order so a
       repaired legacy INDEX.md converges on exactly the shape `renderIndex` produces. Never call
       `.replace()` blind — a `.replace()` whose pattern does not match is a silent no-op, which is
       root cause (a).

    3. **Wrap-safe `Source:` handling.** Classify the matched `Source:` line's value:
       - **ALREADY CANONICAL (check this FIRST, before any other test)** — the value already equals
         `relArchivePath` exactly. Leave the line untouched and do nothing further for `Source:`.

         This short-circuit is load-bearing for idempotence and must not be reordered below the
         other cases. After a first repair the file reads:

         ```
         Source: rulebook/source/rules.pdf          ← canonical, inserted by run 1
         `rules.pdf` (2 pages). This index is the…   ← the designer's prose, label stripped
         ```

         The canonical line's value has no internal whitespace, but the next physical line IS
         non-blank continuation prose — so the "bare path" test below FAILS on it and the
         "wrapped prose" branch would match instead, inserting a SECOND canonical line on every
         subsequent run. Caught by the Phase 173 plan-checker before execution; B9/B11 pin it.
       - **bare path** — the value has no internal whitespace AND the next physical line is blank,
         a heading, or another `HEADER_LABELS` line. Replace the line in place with
         `Source: ${relArchivePath}` (today's behavior, which is correct for this shape).
       - **wrapped prose** — anything else (the value contains internal whitespace, or the next
         physical line is non-blank continuation prose). Do NOT truncate and do NOT delete. Strip
         only the literal `Source: ` label prefix from that first physical line, leaving the prose
         verbatim and rejoined with its continuation, and INSERT a new canonical
         `Source: ${relArchivePath}` line immediately ABOVE the (now unlabeled) paragraph.
         Rationale to put in a code comment: `computeVerificationScope` reads the FIRST `^Source:`
         match and joins it to the project dir, so the canonical bare-path line must exist and must
         come first; the legacy prose is the designer's and is not this command's to delete.
       - **absent** — insert `Source: ${relArchivePath}` immediately after the `Edition:` line
         (or at the top of the header region if `Edition:` is also absent).

       Do NOT add a stop-and-ask / refuse-on-ambiguity path here. The user considered and declined
       that hardening (CONTEXT.md decision 1b scope note); a refusal here would also fail the
       wave-1 gate outright, since BOTH reference games carry a wrapped `Source:` paragraph.

    4. **Never clobber a real `Edition:`.** When `options.edition` is undefined or blank AND an
       `Edition:` line already exists, leave that line byte-identical and do not strip or re-emit
       the adjacent `Edition note:` line. Only when `options.edition` is supplied does the existing
       normalize-and-optionally-note behavior run (reusing `normalizeEdition` and
       `EDITION_NOTE_LABEL` exactly as today, including the strip-old-note step that prevents
       duplicate note accumulation). When `--edition` is omitted and NO `Edition:` line exists,
       insert `Edition: ${EDITION_UNKNOWN}`.

    Reuse `normalizeEdition` and `EDITION_EMPTY_LEXICON`; do not introduce a second edition
    normalizer. No `## heading` location is added by this task, so `findHeadingIndex` does not apply
    here — but do not hand-roll one either; if a heading lookup becomes necessary, import
    `findHeadingIndex` from `./build-manifest.js` as `chunk-provenance.ts` does.

    Error messages must name project-relative paths only (never absolute internal paths) and must
    state the next action, per CLAUDE.md.

    The human-facing success line this command prints must stop being unconditional: only print the
    "provenance header updated" line when the header was actually brought to the four-line contract.
    The false-success message is what let this defect survive.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/ingest-archive.test.ts && npx vitest run src/cli/commands/chunk-provenance.test.ts 2>/dev/null; npm test 2>&1 | tail -20</automated>
    <expect>All B1-B12 green, no pre-existing case regressed, full suite at or above the Phase 172 baseline (3510 passing, per 173-VALIDATION.md).</expect>
  </verify>
  <done>All twelve behaviors pass; `git diff --stat src/cli/commands/ingest-archive.ts` shows the change confined to the existing-INDEX branch and its helpers.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-1 gate proof against copies of BOTH reference games</name>
  <files>.planning/phases/173-verify-pipeline-core/173-PROOF.md</files>
  <action>
    Reuse the Phase 171/172 copy-based harness VERBATIM (173-RESEARCH.md § "The proof harness this
    phase reuses"). **All commands run against `cp -R` copies. No command in this task receives a
    path inside `~/BoardSmithGames/`.**

    Set `SCRATCH="${TMPDIR:-/tmp}/173-proof"` so the harness is deterministic and re-runnable.

    Preflight, on the ORIGINALS, before any copy:
    - `git -C ~/BoardSmithGames/seven rev-parse HEAD` must be `a03f38d4792af9dfc7c798be69686fc3230f54dd`; `git -C ~/BoardSmithGames/seven status --porcelain` must be empty.
    - `git -C ~/BoardSmithGames/one-two-punch rev-parse HEAD` must be `7e69471bd8980a854f3e351f2f486e1fb6f712b9`. Do NOT assert porcelain-empty there (pre-existing unrelated deletions).
    - Capture a whole-tree sha256 manifest of each original (`find … -not -path '*/.git/*' | sort | xargs shasum -a 256`) to `$SCRATCH/manifest-{seven,otp}.before`.

    Then `cp -R` both games into `$SCRATCH/` and, against each COPY only:
    1. Record the BEFORE state: `chunk-provenance-status --project <copy> --json`, and the copy's
       `rulebook/INDEX.md` verbatim.
    2. Run `ingest-archive rules.pdf --project <copy> --json` with NO `--edition`.
    3. Record the AFTER state and assert, each one independently of the command's own output:
       - `chunk-provenance-status --json` no longer reports `pre-provenance` — it reports the
         adopted scope. Cross-check by hand: `grep -c '^Source hash:'` on the repaired INDEX.md
         is 1, and `shasum -a 256 <copy>/rulebook/source/rules.pdf` equals that line's value.
       - `Edition:` is byte-identical to the BEFORE capture (the designer-pending qualifier
         survives).
       - The wrapped `Source:` prose is intact: the continuation line is still attached to its
         first line and no prose byte was lost (`diff` the BEFORE/AFTER INDEX.md and show that the
         only changes are INSERTED canonical header lines and the stripped `Source: ` label token).
       - `Source hash:` and `Transcribed:` are both present and well-formed.
       - Running `ingest-archive` a second time leaves the copy's INDEX.md byte-identical.

    Write the gate script that performs these assertions to `$SCRATCH/173-gate.sh`, exiting
    non-zero on the first failed assertion, so the gate is re-runnable rather than a one-shot
    transcript.

    Post-run: re-run the preflight rev-parse/status/manifest block on the ORIGINALS and diff against
    the `.before` captures. Byte-identical, no exceptions. If either original moved, STOP, restore,
    and report — do not continue the phase.

    Create `.planning/phases/173-verify-pipeline-core/173-PROOF.md` with a `# Phase 173 — Proof`
    title and a `## 1. Wave-1 gate — ingest-archive adoption (decision 1b)` section carrying:
    verbatim command output for both games, the BEFORE/AFTER INDEX.md diffs, the independent
    cross-checks, the originals-unchanged re-verification, and an explicit
    `GATE: PASSED` / `GATE: FAILED` line. Add a `## What is still unproven` stub section listing the
    three live-session proofs that plans 173-06 and 173-07 owe.

    **Every count reported must be cross-checked by a second method** (172-PROOF.md discipline) —
    never trust the tool's own output as its own evidence. `ingest-archive` already lied about
    success once; its exit code is not evidence.
  </action>
  <verify>
    <automated>bash "${TMPDIR:-/tmp}/173-proof/173-gate.sh" && grep -c '^GATE: PASSED' .planning/phases/173-verify-pipeline-core/173-PROOF.md</automated>
    <expect>Gate script exits 0; PROOF.md carries at least one `GATE: PASSED` line at line start.</expect>
  </verify>
  <done>Both reference-game copies compute the adopted scope (not `pre-provenance`) with `Edition:` preserved, `Source:` prose intact, and `Source hash:`/`Transcribed:` present; both originals verified byte-identical; `173-PROOF.md` § 1 records it with verbatim output and independent cross-checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → live project filesystem | `--project` and the rulebook path select which real project's `INDEX.md` gets rewritten |
| Existing on-disk `INDEX.md` → repair logic | Legacy, un-contracted designer content parsed by regex and rewritten in place |
| Proof harness → sibling game repos | This plan reads `~/BoardSmithGames/*` and copies them; a mistake there destroys real designer work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-173-01 | Tampering | `ingestArchiveCommand` existing-INDEX branch | mitigate | Decide the existence branch OUTSIDE the try/catch so a repair error can never fall through to the scaffold-overwrite path; pin with test B12 |
| T-173-02 | Tampering | `Source:` / `Edition:` rewrite | mitigate | Wrap-safe insert-above-and-strip-label instead of single-line replace; preserve `Edition:` when `--edition` is omitted; tests B4/B7 |
| T-173-03 | Repudiation | Command success reporting | mitigate | Stop printing the unconditional "provenance header updated" line; only report what was actually brought to contract — the false-success message is what hid this defect |
| T-173-04 | Information disclosure | Thrown error messages | mitigate | Error strings name project-relative paths only, never absolute internal paths (CLAUDE.md) |
| T-173-05 | Tampering | Proof harness vs. `~/BoardSmithGames` originals | mitigate | `cp -R` copies only; pinned-commit + porcelain preflight; whole-tree sha256 manifest diff before and after; no command receives an original path |
| T-173-SC | Tampering | npm/pip/cargo installs | accept | This phase installs no packages; `173-RESEARCH.md` § Package Legitimacy Audit is empty by design. Any install appearing in execution is out of scope and must stop the plan |
</threat_model>

<verification>
- `npx vitest run src/cli/commands/ingest-archive.test.ts` — all new + pre-existing cases green.
- `npm test` — at or above the 3510 baseline. Note `src/session/stateless-ops.*` carries another
  agent's uncommitted work; do not touch or stage it, and report its tests separately if red.
- `npm run lint` — zero errors in `src/cli/` (3 pre-existing errors in `src/engine/`/`src/ui/` are
  not this plan's).
- `bash "${TMPDIR:-/tmp}/173-proof/173-gate.sh"` exits 0.
- Both reference-game originals byte-identical before and after.
</verification>

<success_criteria>
- `computeVerificationScope()` returns `full` for a repaired synthetic fixture (unit) AND for
  `cp -R` copies of both `seven` and `one-two-punch` (real data).
- No designer content is lost: `Edition:` preserved, wrapped `Source:` prose intact, `## Slices` /
  `## Term → Slice` content untouched.
- The fix is confined to the existing-INDEX branch; no refuse-on-ambiguity hardening added.
- `173-PROOF.md` § 1 exists with `GATE: PASSED`.
</success_criteria>

<output>
Create `.planning/phases/173-verify-pipeline-core/173-01-SUMMARY.md` when done. Record explicitly
whether the fix surfaced additional false-success behavior in `ingest-archive` (todo candidate,
not scope).
</output>
