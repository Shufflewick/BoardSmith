---
phase: 173-verify-pipeline-core
plan: 03
type: execute
wave: 2
depends_on: [173-01]
files_modified:
  - src/cli/slash-command/bs/ingest/transcription-subagent.md
  - src/cli/slash-command/bs/ingest.test.ts
autonomous: true
requirements: [VERIFY-07]

must_haves:
  truths:
    - "The transcription subagent contract tells the subagent to write into the output directory it was dispatched with, not into a hardcoded `rulebook/`."
    - "Ingest's own dispatch keeps filling that input with `rulebook/` and behaves exactly as before."
    - "The subagent still returns a structured summary only and never the transcribed text."
    - "There is exactly ONE transcription contract file — no verify-side fork exists."
  artifacts:
    - path: "src/cli/slash-command/bs/ingest/transcription-subagent.md"
      provides: "Output-directory-parameterized write instructions; everything else byte-identical"
      contains: "assigned output directory"
    - path: "src/cli/slash-command/bs/ingest.test.ts"
      provides: "Pinning tests for the generalization plus regression guards on every previously-pinned string"
  key_links:
    - from: "src/cli/slash-command/bs/ingest/transcription.md"
      to: "src/cli/slash-command/bs/ingest/transcription-subagent.md"
      via: "BS-DISPATCH-V2 pointer with a `Write slices to:` substitution"
      pattern: "Write slices to:"
---

<objective>
Generalize `ingest/transcription-subagent.md` so its output directory is a dispatch input rather
than the hardcoded literal `rulebook/` — the minimal, non-forking change decision 15 requires.

Purpose: decision 15 rejects a verify-specific fork outright. If the two passes run different
instructions, every pass-1-vs-pass-2 diff becomes a false positive and Phase 174's classifier is
built on sand. This is also the exact copy-drift trap that produced commit `f73153a3` and its
latent recurrence fixed in Phase 172.

Output: a surgically edited contract file (four sites) and pinning tests.

**Scope fence.** UNCHANGED IN SUBSTANCE. The `BS-DISPATCH-V2` validation handshake, the three
line-kind vocabulary (`QUOTE` / `Derived (p.N):` / `Visual (p.N):`), the legality-scoring-sequencing
decision test, the named-but-undefined marker rule, the worked-example rule, the structured-return
field list, `nextStep`'s exact string, `## Scope limit`, and `## Edition` are all byte-identical
after this plan. If the diff grows past the four sites below plus their immediate wording, stop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/173-verify-pipeline-core/173-CONTEXT.md
@.planning/phases/173-verify-pipeline-core/173-RESEARCH.md
@.planning/phases/173-verify-pipeline-core/173-PATTERNS.md
@src/cli/slash-command/bs/ingest/transcription-subagent.md
@src/cli/slash-command/bs/ingest/transcription.md
@src/cli/slash-command/bs/ingest.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize the four output-directory sites</name>
  <files>src/cli/slash-command/bs/ingest/transcription-subagent.md</files>
  <action>
    Edit EXACTLY these four sites (line numbers from 173-PATTERNS.md; re-locate by content, not by
    line number, since earlier plans may have shifted the file):

    1. `## Your inputs` — the bullet currently reading "**Output directory** `rulebook/` — relative
       to the project directory you are already inside." Keep it as an input bullet but stop naming
       the literal value: it is the path given in the dispatch prompt, relative to the project
       directory the subagent is already inside. Say plainly that this is the ONLY place slice files
       are written, and that the subagent never writes outside it.
    2. `## 1. WRITE the transcribed text to \`rulebook/NN-topic.md\`` — the section heading. Replace
       the literal `rulebook/` with a reference to the assigned output directory, keeping the
       `NN-topic.md` naming contract intact.
    3. The worked example inside the page-anchored-numbering paragraph ("a section starting on p.14
       → `rulebook/14-movement.md`") — express the example relative to the assigned output
       directory instead of the literal `rulebook/`.
    4. The return-field description "**(a) `slicePath`** — the `rulebook/NN-topic.md` file you
       wrote." — same substitution; `slicePath` must be the path actually written, inside the
       assigned output directory.

    **Do NOT touch** (173-RESEARCH.md Pitfall 3's explicit warning):
    - The `${CLAUDE_SKILL_DIR}/../bs-shared/ingest/transcription.md` cross-references — those name
      the SIBLING contract file, not a slice output location.
    - The `rulebook/00-visual-survey.md` reference — that is the FIXED orchestrator-owned
      visual-survey file, not a per-call output. Decision 15 and Pitfall 3 both put it out of scope.
    - The `rulebook/INDEX.md` reference inside the `nextStep` string — it is a literal handoff
      instruction and is pinned by an existing test.
    - Anything in the `BS-DISPATCH-V2` validation block, the line-kind sections, or the return-shape
      field list.

    Do NOT do a blanket string-replace of `rulebook/` across the file.

    Then update `ingest/transcription.md`'s dispatch template so the `Write slices to:` line reads
    as an explicit, per-dispatch substitution rather than a constant — the value ingest fills stays
    `rulebook/`, so ingest's behavior is bit-for-bit unchanged. Add one sentence naming the verify
    orchestrator as the other caller that fills this same input with a staging path, so a future
    reader cannot conclude the literal is fixed.
  </action>
  <verify>
    <automated>git diff --stat src/cli/slash-command/bs/ingest/transcription-subagent.md src/cli/slash-command/bs/ingest/transcription.md; git diff -U0 src/cli/slash-command/bs/ingest/transcription-subagent.md | grep -c '^[+-][^+-]'</automated>
    <expect>The subagent-contract diff touches at most ~10 changed lines, all within the four named sites. If it is larger, the edit over-reached.</expect>
  </verify>
  <done>Four sites parameterized; `git diff` confirms nothing outside them changed; `grep -n 'rulebook/00-visual-survey.md'` and the `bs-shared/ingest/transcription.md` cross-references are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the generalization and guard every previously-pinned string</name>
  <files>src/cli/slash-command/bs/ingest.test.ts</files>
  <action>
    Add a describe block `transcription-subagent.md — output directory is a dispatch input
    (VERIFY-07, decision 15)`:
    - The write-instruction section names the assigned output directory and no longer hardcodes
      `rulebook/NN-topic.md` as the write target.
    - `slicePath`'s description references the assigned output directory.
    - The `## Your inputs` block still enumerates exactly three dispatch inputs.
    - The file still contains the `rulebook/00-visual-survey.md` reference verbatim (proving the
      generalization did not over-reach).
    - `ingest/transcription.md`'s dispatch block still fills `Write slices to:` with `rulebook/` —
      ingest is unchanged.
    - There is NO verify-side fork: `src/cli/slash-command/bs/verify/` (once plan 173-04 creates it)
      contains no file whose content duplicates the subagent contract. Implement this as a
      directory scan asserting no file under `bs/verify/` contains the `BS-DISPATCH-V2` contract
      body markers (e.g. the line-kind decision test string) — it must POINT at the shared contract,
      never restate it. The assertion must pass whether or not `bs/verify/` exists yet.

    Re-confirm the existing regression guards in this file still pass unchanged — `Visual (p.`,
    `Derived (p.`, `legality, scoring, or sequencing`, `BS-DISPATCH-V2`, `DISPATCH REJECTED`,
    `openGaps[]`, `nextStep`, the `RETURN_SHAPE_FIELDS` loop, and `Do not accept a paraphrase of
    this file in place of the file`.

    Add a comment at the top of the new block, mirroring `ingest.test.ts:352-356`'s own honesty:
    these assertions prove the instruction EXISTS in skill text; they do not prove a live subagent
    receives or follows it. The behavioural evidence for VERIFY-07 is the transcript-absence proof
    in plan 173-06, not this file.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/ingest.test.ts && npm test 2>&1 | tail -12</automated>
    <expect>New block green, all pre-existing assertions in the file still green, full suite at or above baseline.</expect>
  </verify>
  <done>Generalization pinned; every previously-green assertion against the contract file still green; the no-fork guard is in place ahead of `bs/verify/` existing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orchestrator dispatch prompt → subagent write path | The output directory the subagent trusts and writes into arrives as prompt text |
| Shared contract file → two independent callers | One file now governs both ingest and verify; a careless edit changes ingest's behavior silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-173-21 | Tampering | `transcription-subagent.md` shared by ingest and verify | mitigate | Bounded four-site edit; `git diff` line-count gate in task 1's verify; full pre-existing assertion set re-run in task 2 |
| T-173-22 | Tampering | Subagent writing outside its assigned directory | mitigate | Contract states the assigned directory is the ONLY write location; the CLI-side prefix check in plan 173-02 (T-173-14/T-173-16) is the structural backstop — prose alone is not relied on |
| T-173-23 | Repudiation | A silent verify-side fork appearing later | mitigate | Task 2's no-fork directory scan fails if any `bs/verify/` file restates the contract body |
| T-173-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/ingest.test.ts` green.
- `git diff` on the contract file confined to the four sites.
- `npm test` at or above baseline.
</verification>

<success_criteria>
- One transcription contract, two callers, zero forks.
- Ingest's dispatch value and behavior unchanged.
- The output directory is unambiguously a dispatch input in the contract's own words.
</success_criteria>

<output>
Create `.planning/phases/173-verify-pipeline-core/173-03-SUMMARY.md` when done. Quote the four edited
lines verbatim — plan 173-04's dispatch prose must match them exactly.
</output>
