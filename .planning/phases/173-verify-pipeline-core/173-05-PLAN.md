---
phase: 173-verify-pipeline-core
plan: 05
type: execute
wave: 4
depends_on: [173-04]
files_modified:
  - src/cli/commands/install-claude-command.ts
  - src/cli/commands/install-claude-command.test.ts
autonomous: true
requirements: [VERIFY-01]

must_haves:
  truths:
    - "After `boardsmith claude`, `bs-verify-game/SKILL.md` exists in the installed skills tree."
    - "After `boardsmith claude`, every file from `bs/verify/` exists under the installed shared tree."
    - "A partially-installed `verify` shared dir is detected as partial, not as already-installed."
    - "`boardsmith claude uninstall` removes `bs-verify-game` and leaves no orphan."
    - "All five `SKILL_NAMES` sites in the installer test assert the same seven-skill set, and it is no longer possible for one to silently drift."
  artifacts:
    - path: "src/cli/commands/install-claude-command.ts"
      provides: "bs-verify-game registered in SKILL_ENTRY_POINTS, 'verify' in SHARED_DIRS, a verify leaf probe, and a post-install summary line"
      contains: "bs-verify-game"
    - path: "src/cli/commands/install-claude-command.test.ts"
      provides: "All five SKILL_NAMES sites updated plus a meta-test that makes future drift impossible"
  key_links:
    - from: "src/cli/commands/install-claude-command.ts"
      to: "src/cli/slash-command/bs/verify-game.md"
      via: "SKILL_ENTRY_POINTS source path"
      pattern: "verify-game.md"
    - from: "src/cli/commands/install-claude-command.ts"
      to: "src/cli/slash-command/bs/verify/"
      via: "SHARED_DIRS entry driving the recursive copy"
      pattern: "'verify'"
---

<objective>
Register `bs-verify-game` and its `bs/verify/` shared directory with the installer — the mechanical
half of VERIFY-01 — and make the five-site `SKILL_NAMES` drift hazard structurally impossible.

Purpose: neither array is directory-scanned. `bs/verify/` is NOT auto-discovered; both lists are
hand-maintained. And `install-claude-command.test.ts` declares `SKILL_NAMES` at FIVE separate sites
(confirmed by grep: lines 100, 242, 306, 444, 495). Updating only the first leaves four describe
blocks silently asserting the old six-skill set forever.

Output: a four-point installer edit, five updated test sites, and one meta-test that ends this
defect class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/173-verify-pipeline-core/173-CONTEXT.md
@.planning/phases/173-verify-pipeline-core/173-RESEARCH.md
@.planning/phases/173-verify-pipeline-core/173-PATTERNS.md
@.planning/phases/173-verify-pipeline-core/173-04-SUMMARY.md
@src/cli/commands/install-claude-command.ts
@src/cli/commands/install-claude-command.test.ts

<interfaces>
<!-- Exact registration points, confirmed by direct line read in 173-PATTERNS.md. -->

install-claude-command.ts:30-37
```typescript
const SKILL_ENTRY_POINTS: Array<{ source: string; skillName: string }> = [
  { source: join('bs', 'create-game.md'), skillName: 'bs-create-game' },
  { source: join('bs', 'ingest-rules.md'), skillName: 'bs-ingest-rules' },
  { source: join('bs', 'build-chunk.md'), skillName: 'bs-build-chunk' },
  { source: join('bs', 'check-status.md'), skillName: 'bs-check-status' },
  { source: join('bs', 'insert-chunk.md'), skillName: 'bs-insert-chunk' },
  { source: 'generate-ai-instructions.md', skillName: 'bs-generate-ai' },
];
```

install-claude-command.ts:50
```typescript
const SHARED_DIRS = ['build', 'ingest', 'templates', 'aspects'];
```

install-claude-command.ts:58-64
```typescript
const SHARED_LEAF_PROBES = [
  join(SHARED_ROOT, 'state-machine.md'),
  join(SHARED_ROOT, 'build', 'build.md'),
  join(SHARED_ROOT, 'ingest', 'transcription.md'),
  join(SHARED_ROOT, 'templates', 'SKETCH.template.md'),
  join(SHARED_ROOT, 'aspects', 'index.md'),
];
```

`ownedPaths()` (:76-81) and the uninstaller (:255-287) both DERIVE from `SKILL_ENTRY_POINTS` +
`SHARED_ROOT` — no separate edit is needed there once the entry point is registered.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Four-point installer registration</name>
  <files>src/cli/commands/install-claude-command.ts</files>
  <action>
    Make exactly these edits, all in this one file:
    1. `SKILL_ENTRY_POINTS` — append
       `{ source: join('bs', 'verify-game.md'), skillName: 'bs-verify-game' }`.
    2. `SHARED_DIRS` — append `'verify'`. This is what makes `bs/verify/*.md` land under the
       installed shared tree; the copy loop does no directory scanning.
    3. `SHARED_LEAF_PROBES` — append a probe naming one real file inside `bs/verify/`, taking the
       name from `173-04-SUMMARY.md` (`source-resolution.md` unless that summary says otherwise).
       Without this, `isFullyInstalled()` (WR-03a) cannot tell a complete install of the new shared
       dir from an empty one, because `fs.cp` creates the destination directory before populating it.
    4. Post-install console summary (around :231-237) — add a `bs-verify-game` line matching the
       existing one-line-per-skill alignment, describing it as re-verifying an existing game's
       rulebook transcription against its archived source.

    Also update the top-of-file doc comment (:4-6), which names the skill family by hand and
    currently lists six — it silently drifts to describing six skills when seven exist.

    Do not add directory scanning. The explicit lists are deliberate; the fix for their fragility is
    task 3's meta-test, not auto-discovery.
  </action>
  <verify>
    <automated>test "$(grep -c 'bs-verify-game' src/cli/commands/install-claude-command.ts)" -ge 3 && grep -q "'verify'" src/cli/commands/install-claude-command.ts && npx vitest run src/cli/commands/install-claude-command.test.ts 2>&1 | tail -20</automated>
    <expect>Registration present at entry point, summary and doc comment; `verify` in SHARED_DIRS. Existing installer tests still pass (they assert a superset install, so they should stay green).</expect>
  </verify>
  <done>Four-point edit complete; a real `boardsmith claude --local` into a temp dir produces `bs-verify-game/SKILL.md` and the shared `verify/` directory.</done>
</task>

<task type="auto">
  <name>Task 2: Update all five SKILL_NAMES sites and add verify shared-dir assertions</name>
  <files>src/cli/commands/install-claude-command.test.ts</files>
  <action>
    Append `'bs-verify-game'` to the `SKILL_NAMES` array at ALL FIVE declaration sites (grep
    `const SKILL_NAMES = \[` — currently lines 100, 242, 306, 444, 495; re-grep rather than trusting
    those numbers). Each site belongs to a different describe block: the real-install DIST-01/02
    block, the handoff-contract block, the WR-01 orphan-free reinstall block, and the two WR-03a
    partial-install blocks. Updating fewer than five leaves the rest asserting the old six-skill set.

    Add assertions for the new shared dir, mirroring how `ingest` is asserted today:
    - after a real install, the shared `verify/` directory exists and contains every file present in
      `src/cli/slash-command/bs/verify/` (compare the two listings, do not hardcode file names);
    - the WR-03a partial-install case works for `verify/` specifically: empty the installed
      `verify/` dir, run a non-force install, and assert it is repopulated;
    - `boardsmith claude uninstall` removes `bs-verify-game` and leaves no orphan under the shared
      tree.

    Also extend the handoff-contract block's existing assertions to `bs-verify-game`: it must not set
    `disable-model-invocation` and must not self-dispatch via the Skill tool.
  </action>
  <verify>
    <automated>test "$(grep -c "'bs-verify-game'," src/cli/commands/install-claude-command.test.ts)" -ge 5 && npx vitest run src/cli/commands/install-claude-command.test.ts 2>&1 | tail -20</automated>
    <expect>At least five occurrences (one per SKILL_NAMES site); the full installer suite green.</expect>
  </verify>
  <done>All five sites carry the seven-skill set; verify shared-dir install, partial-install repair, and uninstall are all asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Make the five-site drift structurally impossible</name>
  <files>src/cli/commands/install-claude-command.test.ts, src/cli/commands/install-claude-command.ts</files>
  <action>
    The five-site hazard is a Pit-of-Success failure: the easy path (update the array you are
    looking at) is the wrong path. Close it.

    Export the skill-name list from the source module (e.g. `export const SKILL_NAMES` derived from
    `SKILL_ENTRY_POINTS.map((e) => e.skillName)`, or export `SKILL_ENTRY_POINTS` itself), then add
    ONE meta-test to the test file that:
    - reads its OWN source file from disk,
    - extracts every `const SKILL_NAMES = [ ... ]` literal in it,
    - asserts each extracted list, sorted, equals the exported list, sorted,
    - and asserts the number of extracted literals matches the number found, so a newly added
      sixth site is covered automatically rather than silently exempt.

    The failure message must name which site diverged and what it is missing — an actionable error,
    per CLAUDE.md.

    Optional and preferred if it does not balloon the diff: replace the five literals with imports of
    the exported constant and keep the meta-test as the guard for any future re-introduction. Choose
    whichever leaves the test file clearer; state the choice in the summary.

    Do NOT weaken any existing assertion to make this pass.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/install-claude-command.test.ts && npm test 2>&1 | tail -12</automated>
    <expect>Meta-test green. Sanity-check it actually bites: temporarily remove `'bs-verify-game'` from ONE site, confirm the meta-test fails naming that site, then restore.</expect>
  </verify>
  <done>A single source of truth for the installed skill set, guarded by a test that fails loudly when any hardcoded site drifts from it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo skill tree → user's `~/.claude/skills` (or project-local) | The installer copies files into a directory outside the repo |
| Hand-maintained arrays → what actually gets installed | A missing entry produces a silently incomplete install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-173-41 | Tampering | `SHARED_DIRS` recursive copy into the user's skills tree | mitigate | Explicit array (no globbing, no scanning); existing WR-01 orphan-removal test re-run; uninstall asserted to leave no orphan |
| T-173-42 | Repudiation | Silently partial install of the new shared dir | mitigate | `SHARED_LEAF_PROBES` entry for `verify/` so `isFullyInstalled()` cannot mistake an empty created dir for a complete install; WR-03a case added |
| T-173-43 | Tampering | Five-site `SKILL_NAMES` drift | mitigate | Single exported source of truth plus a self-reading meta-test that names the diverging site |
| T-173-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `npx vitest run src/cli/commands/install-claude-command.test.ts` green.
- `npm test` at or above baseline; `npm run lint` zero errors in `src/cli/`.
- A real `boardsmith claude --local` into a temp dir yields `bs-verify-game/SKILL.md` plus the
  shared `verify/` tree. (The end-to-end install against a real game copy is plan 173-06's task 1.)
</verification>

<success_criteria>
- `bs-verify-game` installs, uninstalls cleanly, and its shared dir is partial-install aware.
- All five `SKILL_NAMES` sites assert the same seven-skill set, guarded structurally.
</success_criteria>

<output>
Create `.planning/phases/173-verify-pipeline-core/173-05-SUMMARY.md` when done. Record whether the
five literals were replaced by imports or left in place behind the meta-test.
</output>
