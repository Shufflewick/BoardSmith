---
phase: 173-verify-pipeline-core
plan: 06
type: execute
wave: 5
depends_on: [173-01, 173-05]
files_modified:
  - .planning/phases/173-verify-pipeline-core/173-PROOF.md
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-07]

must_haves:
  truths:
    - "bs-verify-game/SKILL.md and the shared verify/ tree are present after a real install into a real project copy."
    - "A live /bs-verify-game pass runs against an existing bs-built project without rebuilding it."
    - "Every live slice under rulebook/ is byte-identical before and after the pass."
    - "Every file the pass wrote lives under rulebook/.verify/RUN-ID/."
    - "The orchestrator's captured transcript contains no slice-body-shaped line anywhere across the pass."
    - "Both reference-game originals are byte-identical before and after every proof run."
  artifacts:
    - path: ".planning/phases/173-verify-pipeline-core/173-PROOF.md"
      provides: "Sections 2 and 3 — the SC-1 install proof and the SC-2/SC-3 live-pass proof, with verbatim output"
      contains: "SC-2 / SC-3"
  key_links:
    - from: "the installed bs-verify-game skill"
      to: "boardsmith verify-run-init and verify-run-record"
      via: "a live session actually invoking the commands — Phase 170's central failure mode"
      pattern: "verify-run-"
---

<objective>
Produce the live-session evidence for SC-1 (installs and runs without rebuilding), SC-2
(non-destructive staging), and SC-3 (the orchestrator never reads a slice).

Purpose: `173-VALIDATION.md` is explicit — no task may claim VERIFY-01/07/08 on `.toContain()`
evidence alone. Every skill-text test in this repo asserts string presence only, and Phase 170
proved twelve instruction-shaped mechanisms were skipped while every automated check stayed green.
This plan is where the phase earns those requirements.

Output: `173-PROOF.md` sections 2 and 3, plus two re-runnable assertion scripts in the scratch dir.

**Read-only invariant.** This plan reads `~/BoardSmithGames/seven` and
`~/BoardSmithGames/one-two-punch` and copies them. Every command under proof runs against a `cp -R`
copy; no command receives a path inside `~/BoardSmithGames/`. Harness proven in Phases 171 and 172 —
reuse verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/173-verify-pipeline-core/173-CONTEXT.md
@.planning/phases/173-verify-pipeline-core/173-RESEARCH.md
@.planning/phases/173-verify-pipeline-core/173-VALIDATION.md
@.planning/phases/173-verify-pipeline-core/173-PROOF.md
@.planning/phases/173-verify-pipeline-core/173-02-SUMMARY.md
@.planning/phases/173-verify-pipeline-core/173-04-SUMMARY.md
@.planning/phases/171-provenance-recording/171-PROOF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harness, real install, and the SC-1 install proof</name>
  <files>.planning/phases/173-verify-pipeline-core/173-PROOF.md</files>
  <action>
    Reuse the copy-based harness verbatim, with SCRATCH set to the value of `${TMPDIR:-/tmp}/173-proof`.

    Preflight on the ORIGINALS, before any copy:
    - `git -C ~/BoardSmithGames/seven rev-parse HEAD` equals
      `a03f38d4792af9dfc7c798be69686fc3230f54dd`, and `git status --porcelain` is empty.
    - `git -C ~/BoardSmithGames/one-two-punch rev-parse HEAD` equals
      `7e69471bd8980a854f3e351f2f486e1fb6f712b9`. Do NOT assert porcelain-empty there.
    - Whole-tree sha256 manifests of both originals captured to `.before` files.

    Remove any copies left over from plan 173-01 and `cp -R` both games in fresh — this pass starts
    from a clean copy.

    Install the skills into the `seven` copy by running the repo's `boardsmith claude --local --force`
    from inside the copy. Then assert, independently of the installer's own console output:
    - a `bs-verify-game/SKILL.md` exists in the installed tree and is byte-identical to
      `src/cli/slash-command/bs/verify-game.md`;
    - every file present in `src/cli/slash-command/bs/verify/` exists under the installed shared
      `verify/` directory (compare listings; do not hardcode names);
    - the other six skills are still present — the install did not regress.

    Capture a whole-tree sha256 manifest of the `seven` copy immediately after install as
    `manifest-seven.post-install`, so task 2 can prove the verify pass changed nothing outside the
    staging tree — and so it is visible that install itself changed nothing in the game.

    Write these assertions into a re-runnable script at `$SCRATCH/173-sc1.sh` that exits non-zero on
    the first failure, then append a `## 2. SC-1 — bs-verify-game installs, project untouched`
    section to `173-PROOF.md` with verbatim command output.
  </action>
  <verify>
    <automated>bash "${TMPDIR:-/tmp}/173-proof/173-sc1.sh"</automated>
    <expect>Exits 0. The installed skill exists in the game copy, matches the repo source byte-for-byte, the shared verify tree is complete, and the other six skills survived.</expect>
  </verify>
  <done>Skills installed into a real game copy; SC-1's mechanical half proven independently of the installer's own reporting; post-install manifest captured; assertions re-runnable.</done>
</task>

<task type="auto">
  <name>Task 2: A real /bs-verify-game pass with transcript capture (SC-2 and SC-3)</name>
  <files>.planning/phases/173-verify-pipeline-core/173-PROOF.md</files>
  <action>
    Run a REAL verify pass against the `seven` copy. "Real" means: read the INSTALLED
    `bs-verify-game/SKILL.md` from the copy as an actual file read and follow it as written,
    including its dispatch to the installed shared `verify/` sub-steps. Do not execute from this
    plan's summary of what the skill says, and do not shortcut any step. The point of this task is to
    find out whether a session actually follows the text — which is exactly what Phase 170 found
    sessions do not do.

    Dispatch REAL subagents per slice-unit, following the dispatch block the skill constructs.
    Adoption (plan 173-01's repaired `ingest-archive`) runs through the skill's own gated Step 1,
    confirmed once, as the skill instructs.

    Capture everything to `$SCRATCH/173-transcript.log`: every command invoked with its full output,
    every dispatch prompt verbatim, and every subagent structured return verbatim. This log IS the
    SC-3 evidence, so it must be complete — an incomplete log makes the absence proof worthless, and
    that must be stated in the proof if it happens.

    Write all assertions below into a re-runnable script at `$SCRATCH/173-sc23.sh` that exits
    non-zero on the first failure.

    SC-2 (non-destructive staging):
    - Re-run the whole-tree sha256 manifest of the `seven` copy and diff it against
      `manifest-seven.post-install`. Every differing path must be under `rulebook/.verify/` (plus
      git metadata). Zero live `rulebook/*.md` slices changed; zero chunk files changed; zero source
      files changed.
    - Cross-check by a second method: `git status --porcelain` inside the copy lists nothing outside
      `rulebook/.verify/`.
    - Count the staged slices two ways — a `find` over the staging dir, and the `count` field from
      `verify-run-status --json` — and confirm they agree. If they disagree, that is a real finding:
      record it, do not reconcile it by picking one.
    - Confirm the staging tree is present, is NOT gitignored (decision 6), and was not deleted on
      close (decision 7).

    SC-3 (an ABSENCE, so use the positive observable from RESEARCH.md):
    - Grep `173-transcript.log` for slice-body-shaped lines: the transcription contract's
      quoted-rule marker at line start, lines matching `Derived (p.`, and lines matching
      `Visual (p.`. These appear only inside written slice files, never inside a structured-summary
      return. FINDING NONE IS THE EVIDENCE. Report the exact grep commands and their zero counts.
    - Confirm positively that every dispatch prompt in the log carries only the three substitutions
      (unit or page range, rulebook path, output directory) and no rulebook prose.
    - Confirm positively that every subagent return in the log carries only the structured-summary
      fields and no transcribed body.
    - If ANY slice-body-shaped line appears, SC-3 is NOT met: record it as a failure with the
      offending excerpt and stop. Do not rationalize it.

    Also record explicitly whether each skill-text-to-command invocation actually ran on this live
    session: `ingest-archive` (via the adoption gate), `chunk-provenance-status` (the independent
    post-adoption re-check), `verify-run-init`, `verify-run-status`, `verify-run-record`. Quote the
    log line for each. Any that did NOT run is a Phase-170-class finding and must be reported as
    such, not smoothed over.

    Post-run: re-run the preflight rev-parse, status and manifest block on BOTH ORIGINALS and diff
    against the `.before` captures. Byte-identical, no exceptions. If either original moved, STOP,
    restore, and report.

    Append a `## 3. SC-2 / SC-3 — a live verify pass` section to `173-PROOF.md` with all of the
    above verbatim, including a subsection naming what this pass did NOT prove.
  </action>
  <verify>
    <automated>bash "${TMPDIR:-/tmp}/173-proof/173-sc23.sh"</automated>
    <expect>Exits 0. Zero slice-body-shaped lines in the transcript; every changed path under rulebook/.verify/; staged-slice counts agree across two methods; both originals byte-identical.</expect>
  </verify>
  <done>SC-2 and SC-3 proven with verbatim evidence and independent cross-checks; every new skill-text-to-command invocation confirmed to have actually run, or reported as skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Proof harness → sibling game repos | This plan reads `~/BoardSmithGames/*`; a mistake there destroys real designer work |
| Installer → the game copy's local skills tree | A real install writes into the copy |
| Live pass → the game copy's `rulebook/` | The whole point is that it writes only into the staging tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-173-51 | Tampering | Proof run vs. `~/BoardSmithGames` originals | mitigate | `cp -R` copies only; pinned-commit and porcelain preflight; whole-tree sha256 manifest diff before and after; no command receives an original path |
| T-173-52 | Tampering | Live pass writing outside the staging tree | mitigate | Whole-tree manifest diff plus an independent `git status --porcelain` cross-check; any path outside `rulebook/.verify/` fails the proof |
| T-173-53 | Repudiation | A proof that asserts compliance instead of observing it | mitigate | SC-3 is proven by a transcript grep returning zero, not by a claim; every count cross-checked by a second method (172-PROOF.md discipline) |
| T-173-54 | Information disclosure | The captured transcript may contain local absolute paths | accept | The transcript stays in the scratch dir; only excerpts go into `173-PROOF.md`, with home-directory paths elided |
| T-173-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `bash "${TMPDIR:-/tmp}/173-proof/173-sc1.sh"` exits 0.
- `bash "${TMPDIR:-/tmp}/173-proof/173-sc23.sh"` exits 0.
- Both reference-game originals byte-identical before and after.
- No process left running; no dev server started.
</verification>

<success_criteria>
- `173-PROOF.md` sections 2 and 3 exist with verbatim output and independent cross-checks.
- SC-1, SC-2 and SC-3 each have observable evidence, not a compliance claim.
- Every new skill-text-to-command invocation is either shown running or reported as skipped.
</success_criteria>

<output>
Create `.planning/phases/173-verify-pipeline-core/173-06-SUMMARY.md` when done. If the live session
diverged from the skill text at any point, say exactly where — that finding is worth more than a
clean pass.
</output>
