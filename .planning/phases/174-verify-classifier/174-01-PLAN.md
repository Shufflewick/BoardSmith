---
phase: 174-verify-classifier
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/174-verify-classifier/174-PROOF.md
  - .planning/phases/174-verify-classifier/174-FIXTURES/MANIFEST.md
  - .planning/phases/174-verify-classifier/174-FIXTURES/seven/
  - .planning/phases/174-verify-classifier/174-FIXTURES/one-two-punch/
autonomous: true
requirements: [VERIFY-03]

must_haves:
  truths:
    - "Real pass-2 staged slices exist for a copy of seven, produced by the Phase 173 pipeline (verify-run-init/-status/-record + real transcription-subagent dispatches), not hand-written."
    - "Real pass-2 staged slices exist for a copy of one-two-punch, same mechanism."
    - "The pass-1 (live) and pass-2 (staged) slice bodies for both games are archived in-repo so later waves have durable real fixtures after scratch is cleaned."
    - "The presentation-marker forms actually present on each side are enumerated with measured counts, per game, per side — the measured basis for decision 12b's constant."
    - "Both reference-game originals under ~/BoardSmithGames are byte-identical before and after the run."
  artifacts:
    - path: ".planning/phases/174-verify-classifier/174-PROOF.md"
      provides: "Section 1 — real pass-1-vs-pass-2 material production, with verbatim command output and measured counts"
      contains: "## 1. Fixture production"
    - path: ".planning/phases/174-verify-classifier/174-FIXTURES/MANIFEST.md"
      provides: "sha256 of every archived fixture file, its origin path, and the exact command sequence that reproduces it"
      contains: "sha256"
  key_links:
    - from: "the archived fixture tree"
      to: "src/cli/commands/verify-classify.test.ts (plans 174-03/174-04)"
      via: "real-slice fixtures copied into a tmpdir by the unit tests"
      pattern: "174-FIXTURES"
---

<objective>
Produce the real pass-1-vs-pass-2 material this whole phase is validated against, and archive it so
it survives scratch cleanup.

Purpose: `174-RESEARCH.md`'s single biggest finding is that **zero** pass-1-vs-pass-2 material exists
anywhere on disk — neither reference game has `rulebook/.verify/`, `rulebook/source/`, or a
`Source hash:` line, and every artifact Phase 173's proof produced lived in `/tmp` and is gone.
CONTEXT.md decision 13 (AMENDED) therefore puts producing that material in scope for this phase's
first wave. No classifier logic is built until real pairs exist. This also front-loads the phase's
highest-risk unknown, which is what SC-2's bar is for.

Output: `174-PROOF.md` section 1, plus a committed `174-FIXTURES/` tree of real live and staged
slices with a hash manifest.

**Read-only invariant.** This plan reads `~/BoardSmithGames/seven` and
`~/BoardSmithGames/one-two-punch` and copies them. Every command under proof runs against a `cp -R`
copy; no command ever receives a path inside `~/BoardSmithGames/`. Harness proven in Phases 171,
172, and 173 — reuse it verbatim rather than inventing a second one.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/174-verify-classifier/174-CONTEXT.md
@.planning/phases/174-verify-classifier/174-RESEARCH.md
@.planning/phases/174-verify-classifier/174-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight, copies, install, and source adoption on both copies</name>
  <files>.planning/phases/174-verify-classifier/174-PROOF.md</files>
  <read_first>
    - `.planning/phases/173-verify-pipeline-core/173-PROOF.md` sections 1 and 2 (the preflight +
      `cp -R` + install harness this task reuses verbatim, including the exact assertion style)
    - `.planning/phases/173-verify-pipeline-core/173-06-PLAN.md` Task 1 (the harness written as a plan)
    - `src/cli/slash-command/bs/verify/source-resolution.md` (the gated adoption flow Step 1 runs)
  </read_first>
  <action>
    Set `SCRATCH="${TMPDIR:-/tmp}/174-proof"`. Remove any leftover copies from a previous attempt so
    this pass starts clean.

    Preflight on the ORIGINALS, before any copy:
    - `git -C ~/BoardSmithGames/seven rev-parse HEAD` MUST equal
      `a03f38d4792af9dfc7c798be69686fc3230f54dd`, and `git -C ~/BoardSmithGames/seven status
      --porcelain` MUST be empty. `seven` is READ-ONLY and pinned.
    - `git -C ~/BoardSmithGames/one-two-punch rev-parse HEAD` — record the observed hash verbatim in
      the proof. Do NOT assert porcelain-empty there; that repo has known pre-existing unrelated
      dirty state (173-06 recorded the same).
    - Capture a whole-tree sha256 manifest of each original to `$SCRATCH/seven.before` and
      `$SCRATCH/otp.before` (`find ... -type f -exec shasum -a 256 {} +`, sorted).

    Then, for each game: `cp -R` the original to `$SCRATCH/seven` and `$SCRATCH/one-two-punch`, and
    install the skills into each copy with a real `npx boardsmith claude --local --force` run from
    inside the copy (the same real install 173-06 used — never a hand-made skills tree). Assert
    `.claude/skills/bs-verify-game/SKILL.md`, `.claude/skills/bs-shared/verify/source-resolution.md`,
    and `.claude/skills/bs-shared/verify/staging-dispatch.md` exist in each copy by reading the
    filesystem, not by trusting the installer's console output.

    Then adopt each copy's source rulebook so an archived, hashed source exists — this is the state
    neither game is in today. Use the same real mechanism Phase 173's wave-1 gate used
    (`boardsmith ingest-archive` against the copy's root rulebook candidate, per
    `173-PROOF.md` section 1), NOT a hand-copied file into `rulebook/source/`. After adoption assert,
    per copy: exactly one file under `rulebook/source/`, and `rulebook/INDEX.md` contains a
    `Source hash:` line whose hex value equals `shasum -a 256` of that archived file.

    Record every command and its verbatim output into `174-PROOF.md` under a new
    `## 1. Fixture production` section, with a `### <game> — preflight/adoption` sub-heading per game.
    Never write "ran clean" — paste the output and the measured hashes.
  </action>
  <verify>
    <automated>test -f "${TMPDIR:-/tmp}/174-proof/seven/rulebook/INDEX.md" && grep -q 'Source hash:' "${TMPDIR:-/tmp}/174-proof/seven/rulebook/INDEX.md" && grep -q 'Source hash:' "${TMPDIR:-/tmp}/174-proof/one-two-punch/rulebook/INDEX.md" && grep -q '## 1. Fixture production' .planning/phases/174-verify-classifier/174-PROOF.md</automated>
  </verify>
  <acceptance_criteria>
    - `git -C ~/BoardSmithGames/seven rev-parse HEAD` prints `a03f38d4792af9dfc7c798be69686fc3230f54dd`
      and `git status --porcelain` prints nothing, both recorded verbatim in the proof.
    - Each copy has exactly one file under `rulebook/source/` and an `INDEX.md` `Source hash:` line
      equal to that file's `shasum -a 256`.
    - Each copy contains `.claude/skills/bs-verify-game/SKILL.md` (asserted by filesystem read).
    - `174-PROOF.md` contains a `## 1. Fixture production` heading and, beneath it, the verbatim
      output of every command run — no summarised or paraphrased output.
  </acceptance_criteria>
  <done>Both copies exist, are skill-installed, and carry a real archived+hashed source; proof section 1 opened with verbatim preflight/adoption evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Real pass-2 re-transcription on both copies (the Phase 173 pipeline, reused unchanged)</name>
  <files>.planning/phases/174-verify-classifier/174-PROOF.md</files>
  <read_first>
    - `src/cli/slash-command/bs/verify/staging-dispatch.md` (the exact Run Allocation → Resume →
      Dispatch → Recording → Close sequence to follow; the `BS-DISPATCH-V2` pointer block is copied
      byte-identical, never composed from memory)
    - `src/cli/slash-command/bs/ingest/transcription-subagent.md` (the contract the dispatched
      subagent reads; do not restate it in the dispatch prompt)
    - `.planning/phases/173-verify-pipeline-core/173-PROOF.md` section 3 (a real, complete run of
      exactly this sequence against `seven`, including the real command JSON shapes)
  </read_first>
  <action>
    For each copy, run a real pass-2 re-transcription using the EXISTING Phase 173 commands and
    contracts — reuse the mechanism, reimplement nothing:

    1. `boardsmith verify-run-init --project <copy> --ranges '["1-1","2-2"]' --json` (use per-page
       ranges sized to the copy's actual page count; take `runId`, `stagingDir`, `ranges` from the
       JSON — never compose a run-id).
    2. `boardsmith verify-run-status --project <copy> --run-id <runId> --json` to get `rangesPending`.
    3. For each pending range, dispatch ONE real Task-tool subagent with the `BS-DISPATCH-V2` pointer
       block copied byte-identical from `staging-dispatch.md`'s Dispatch section, filling
       `Your page range`, `Rulebook path` (the adopted `rulebook/source/<file>`), and
       `Write slices to` (the run's `stagingDir`).
    4. Record every unit the subagent's return names with
       `boardsmith verify-run-record --run-id <runId> --unit <unitId> --slice <slicePath> --range <N>-<M> --project <copy> --json`,
       then `--complete-range <N>-<M>` in the same turn.
    5. Repeat until `verify-run-status --json` reports `rangesPending` empty.

    **The orchestrator does not open a staged slice at any point in this task** — record from the
    subagent's returned `slicePath` field only. This is the same discipline the phase later has to
    prove for classification; violating it here contaminates the transcript evidence.

    Record, per game, in `174-PROOF.md` section 1: the run-id, the manifest ranges, every dispatch
    (with the literal prompt block sent), the recorded unit list, and the final
    `verify-run-status --json` output. Then measure and record: the number of staged files produced,
    their names, and the number of live rule slices under `rulebook/*.md` — `173-PROOF.md` measured
    6 staged vs 3 live for `seven`, and this m:n asymmetry is exactly what plan 174-03's pairing must
    handle, so it is evidence, not trivia.
  </action>
  <verify>
    <automated>for g in seven one-two-punch; do d="${TMPDIR:-/tmp}/174-proof/$g"; ls "$d"/rulebook/.verify/*/slices/*.md >/dev/null || exit 1; done; grep -q 'rangesPending' .planning/phases/174-verify-classifier/174-PROOF.md</automated>
  </verify>
  <acceptance_criteria>
    - Each copy has at least 2 real `.md` files under `rulebook/.verify/<runId>/slices/`.
    - Each copy's `rulebook/.verify/<runId>/RUN.md` ledger has `rangesPending: []` in the final
      `verify-run-status --json` output pasted into the proof.
    - Every live slice under each copy's `rulebook/*.md` is byte-identical to the same file in the
      untouched original (verified by `shasum -a 256` comparison against the `.before` manifest) —
      pass 2 wrote nothing over pass 1.
    - The proof records, per game, the staged filename list and the live rule-slice filename list
      side by side with both counts.
    - No line of the proof's transcript section contains a quoted rule line, a `Derived (p.` line, or
      a `Visual (p.` line originating from the orchestrator's own reads.
  </acceptance_criteria>
  <done>Both copies carry a completed, ledger-recorded pass-2 staging tree produced by real subagent dispatches; counts and filenames recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Archive the fixtures in-repo and measure the presentation-marker inventory</name>
  <files>.planning/phases/174-verify-classifier/174-FIXTURES/MANIFEST.md, .planning/phases/174-verify-classifier/174-PROOF.md</files>
  <read_first>
    - `.planning/phases/174-verify-classifier/174-CONTEXT.md` decisions 12b and 17 (the exclusion set
      must be measured against real slices, and the SC-2 bar is measured on rule-bearing content only)
    - `.planning/phases/174-verify-classifier/174-RESEARCH.md` "Common Pitfalls → Pitfall 2" and
      "Code Examples" (the measured schema-asymmetry numbers this task re-measures against the new
      real pass-2 output)
  </read_first>
  <action>
    Copy the real material out of scratch into the repo so later waves keep working after scratch is
    cleaned. Create, per game, under `.planning/phases/174-verify-classifier/174-FIXTURES/<game>/`:
    `live/` (every `rulebook/*.md` from the copy), `staged/` (every
    `rulebook/.verify/<runId>/slices/*.md`), `RUN.md` (the run ledger verbatim), and `INDEX.md`.
    Copy bytes unmodified — no reformatting, no truncation, no redaction.

    Write `174-FIXTURES/MANIFEST.md` containing: the two source repo HEAD hashes, the two run-ids, the
    exact command sequence that reproduces the tree (so a future phase can regenerate it), and a
    table of every archived file with its `shasum -a 256` and its origin path inside the scratch copy.

    Then measure the presentation-marker inventory on the real data and record it in `174-PROOF.md`
    section 1 under `### Presentation-marker inventory (decision 12b basis)`. Per game, per side
    (live = pass 1, staged = pass 2), report exact counts from these greps run against the archived
    fixtures:
    - `^Visual (p\.` — the post-Phase-170 form.
    - `^Derived (p\.[0-9]+) — diagram description` — the legacy form.
    - `^Derived (p\.[0-9]+) — art` — the legacy form.
    - `^Derived (p\.` total — so the rule-bearing remainder is derivable as total minus the two
      legacy presentation forms.
    Report any OTHER `^Derived (p.N) — <qualifier>:` qualifier that appears in the real data verbatim,
    with its count — if a third legacy presentation qualifier exists, decision 12b's enumerated
    constant must cover it and plan 174-03 needs to know now, not after SC-2 misses its bar.

    Close section 1 with an originals re-verification: recompute the whole-tree sha256 manifests of
    both `~/BoardSmithGames` originals and assert them byte-identical to the `.before` manifests
    captured in Task 1, pasting the `diff` result.
  </action>
  <verify>
    <automated>test -f .planning/phases/174-verify-classifier/174-FIXTURES/MANIFEST.md && ls .planning/phases/174-verify-classifier/174-FIXTURES/seven/staged/*.md >/dev/null && ls .planning/phases/174-verify-classifier/174-FIXTURES/one-two-punch/staged/*.md >/dev/null && grep -q 'Presentation-marker inventory' .planning/phases/174-verify-classifier/174-PROOF.md</automated>
  </verify>
  <acceptance_criteria>
    - `174-FIXTURES/<game>/live/` and `174-FIXTURES/<game>/staged/` both non-empty for both games,
      and every archived file's `shasum -a 256` matches the MANIFEST.md row for it.
    - `174-PROOF.md` section 1 contains a per-game, per-side count table for `^Visual (p\.`,
      `^Derived (p\.N) — diagram description`, `^Derived (p\.N) — art`, and total `^Derived (p\.`,
      with the grep commands shown.
    - Any additional legacy `— <qualifier>:` form found in real data is listed verbatim with a count,
      or the proof states explicitly that a grep for `^Derived \(p\.[0-9]+\) — ` found only the two
      known qualifiers.
    - `diff` of the before/after whole-tree manifests for BOTH originals is empty, pasted in the proof.
  </acceptance_criteria>
  <done>Real fixtures committed in-repo with a hash manifest; the decision-12b exclusion set has a measured basis; originals proven untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this plan → `~/BoardSmithGames/*` | The originals are read-only assets a bug here could destroy |
| dispatched subagent → scratch copy filesystem | The subagent writes files inside the copy's staging dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-01 | Tampering | `~/BoardSmithGames/seven`, `one-two-punch` | mitigate | Every command runs against `$SCRATCH` copies only; whole-tree sha256 manifests captured before and diffed after (Task 1 + Task 3), and `seven`'s pinned HEAD asserted before any work |
| T-174-02 | Tampering | staged slice writes escaping the run dir | mitigate | Reuses `verify-run-record`'s existing `relative()`/`startsWith('..')` staging-path guard (`verify-run.ts:756-763`) unchanged; Task 2 additionally asserts every pre-existing live slice byte-identical |
| T-174-03 | Repudiation | proof claims without evidence | mitigate | Verbatim command output required; "ran clean" is prohibited by acceptance criteria |
| T-174-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (`174-RESEARCH.md` Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `174-PROOF.md` section 1 exists with verbatim evidence for preflight, adoption, dispatch, recording,
  the staged-vs-live count asymmetry, the presentation-marker inventory, and originals re-verification.
- `174-FIXTURES/` holds real live + staged slices for both games with a matching hash manifest.
- Both `~/BoardSmithGames` originals byte-identical before and after.
</verification>

<success_criteria>
Real pass-1-vs-pass-2 material exists, is committed, and its presentation-marker schema asymmetry is
measured — so plans 174-03 onward build against real data instead of invented fixtures.
</success_criteria>

<output>
Create `.planning/phases/174-verify-classifier/174-01-SUMMARY.md` when done
</output>
