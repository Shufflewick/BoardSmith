---
phase: 174-verify-classifier
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/commands/verify-run.ts
  - src/cli/commands/verify-run.test.ts
autonomous: true
requirements: [VERIFY-03]

must_haves:
  truths:
    - "A sibling module can import the ledger's atomic-write and parse helpers from verify-run.ts — there is exactly one atomic ledger write path in the repo."
    - "The ledger's record union recognises a fourth kind, 'classification', in the SAME fence pair, and a malformed classification line lands in malformedLines instead of throwing."
    - "A classification line never appears in verify-run-status's recorded[] unit list, and a unit line never appears in the classification list — the two kinds cannot contaminate each other."
  artifacts:
    - path: "src/cli/commands/verify-run.ts"
      provides: "Exported atomicWriteFile, appendLedgerLine, locateFences, parseLedgerBody, resolveLedgerState, ledgerFilePath, readLedgerOrThrow; exported ParsedLine union widened with ParsedClassificationLine; exported ClassificationRecord"
      contains: "export interface ClassificationRecord"
    - path: "src/cli/commands/verify-run.test.ts"
      provides: "Tests pinning the export surface, the classification parse branch, and kind-isolation"
      contains: "classification"
  key_links:
    - from: "src/cli/commands/verify-classify.ts (plans 174-03/174-04)"
      to: "src/cli/commands/verify-run.ts"
      via: "named imports of the now-exported ledger helpers"
      pattern: "from './verify-run.js'"
---

<objective>
Make genuine reuse of the Phase 173 ledger possible by EXPORTING its helpers and teaching its record
union about a fourth `kind`, so classification inherits crash-safety instead of re-earning it.

Purpose: `174-RESEARCH.md` Pitfall 1 measured that `atomicWriteFile`, `appendLedgerLine`,
`locateFences`, `parseLedgerBody`, `resolveLedgerState`, `ledgerFilePath`, and `readLedgerOrThrow` are
all module-private today. CONTEXT.md decision 5 and the Don't-Hand-Roll table are explicit: a second
atomic-write path is a second chance to lose recorded work — the exact defect class `173-REVIEW.md`
CR-01 caught. This plan is the "Task 0" that Pitfall 1 says must land before any classification logic.

Output: a widened, exported ledger surface in `verify-run.ts` plus tests pinning it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/174-verify-classifier/174-CONTEXT.md
@.planning/phases/174-verify-classifier/174-RESEARCH.md
@.planning/phases/174-verify-classifier/174-PATTERNS.md
@src/cli/commands/verify-run.ts
</context>

<interfaces>
Current export surface of `src/cli/commands/verify-run.ts` (measured, not assumed):
exported — `VerifyRunOptions` (70), `RUN_ID_RE` (83), `RANGE_ID_RE` (91), `RUN_LEDGER_BEGIN/END`
(97-98), `RUN_MANIFEST_BEGIN/END` (104-105), `LedgerRecord` (108), `RangeMarkerRecord` (126),
`stagingSlicesDir` (176), `VerifyRunInitResult` (461), `verifyRunInitCommand` (485),
`VerifyRunRecordResult` (552), `VerifyRunRangeActionResult` (561), `verifyRunRecordCommand` (666),
`VerifyRunStatusResult` (818), `verifyRunStatusCommand` (847).

module-private and needed by `verify-classify.ts` — `ledgerFilePath` (193), `atomicWriteFile` (213),
`locateFences` (255), `appendLedgerLine` (318), `parseLedgerBody` (351), `resolveLedgerState` (422),
`readLedgerOrThrow` (568), plus the `ParsedUnitLine` (327) / `ParsedMarkerLine` (332) / `ParsedLine`
(337) union.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export the seven ledger helpers and the ParsedLine union</name>
  <files>src/cli/commands/verify-run.ts, src/cli/commands/verify-run.test.ts</files>
  <read_first>
    - `src/cli/commands/verify-run.ts` lines 60-460 (the helper bodies, their doc comments, and the
      `ParsedLine` union that is being widened)
    - `src/cli/commands/verify-run.test.ts` lines 1-80 (the import block and fixture helpers this
      task's tests extend)
    - `.planning/phases/174-verify-classifier/174-RESEARCH.md` "Pitfall 1"
  </read_first>
  <behavior>
    - Test `EXPORT-1`: a test file can `import { atomicWriteFile, appendLedgerLine, locateFences,
      parseLedgerBody, resolveLedgerState, ledgerFilePath, readLedgerOrThrow } from './verify-run.js'`
      and every imported binding is a function (`typeof === 'function'`).
    - Test `EXPORT-2`: `ledgerFilePath(projectDir, runId)` returns the path
      `<projectDir>/rulebook/.verify/<runId>/RUN.md` for a run created by `verifyRunInitCommand`, and
      that file exists on disk.
    - Test `EXPORT-3`: `atomicWriteFile` leaves no `*.tmp*` sibling behind after a successful write
      (the atomic rename completed), asserted by reading the directory listing.
  </behavior>
  <action>
    Add the `export` keyword to exactly these seven declarations in `src/cli/commands/verify-run.ts`:
    `ledgerFilePath` (line 193), `atomicWriteFile` (213), `locateFences` (255), `appendLedgerLine`
    (318), `parseLedgerBody` (351), `resolveLedgerState` (422), `readLedgerOrThrow` (568). Also export
    the `ParsedUnitLine`, `ParsedMarkerLine`, and `ParsedLine` declarations (327, 332, 337).

    Do not change any helper's signature, body, or behavior in this task — the atomic-write path is
    the one Phase 173-08 hardened after CR-01 and it is being shared, not revised. Where a helper's
    doc comment says "this is the one place X is computed", extend that sentence to name
    `verify-classify.ts` as the second caller so the intent survives the widening.

    Add a `describe('verify-run.ts — ledger helper export surface (174-02)')` block to
    `verify-run.test.ts` implementing EXPORT-1..3 above. EXPORT-1 is the pin that makes a future
    accidental un-export fail here rather than in a sibling module's build.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-run.test.ts -t "export surface"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^export function ledgerFilePath\|^export async function atomicWriteFile\|^export function locateFences\|^export function appendLedgerLine\|^export function parseLedgerBody\|^export function resolveLedgerState\|^export async function readLedgerOrThrow' src/cli/commands/verify-run.ts` returns 7.
    - `grep -c 'export type ParsedLine' src/cli/commands/verify-run.ts` returns 1.
    - `npx vitest run src/cli/commands/verify-run.test.ts` passes with the three new EXPORT tests.
    - `npx tsc --noEmit -p tsconfig.json` (or the repo's typecheck script) reports no new errors.
    - No diff inside any exported helper's body — verified by `git diff` showing only `export`
      keyword additions and doc-comment additions in those seven regions.
  </acceptance_criteria>
  <done>All seven helpers plus the ParsedLine union are importable from a sibling module, pinned by a test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Teach the ledger union a fourth kind — 'classification'</name>
  <files>src/cli/commands/verify-run.ts, src/cli/commands/verify-run.test.ts</files>
  <read_first>
    - `src/cli/commands/verify-run.ts` lines 327-455 (`ParsedUnitLine`/`ParsedMarkerLine`/`ParsedLine`,
      `parseLedgerBody`'s three existing discriminant branches and its malformed-line discipline,
      `resolveLedgerState`)
    - `.planning/phases/174-verify-classifier/174-PATTERNS.md` § "Core CRUD pattern — new ledger record
      `kind`" (the exact `ClassificationRecord` shape, and the "malformed → malformedLines, never
      thrown" rule to follow)
    - `.planning/phases/174-verify-classifier/174-CONTEXT.md` decisions 2b, 3, 6
  </read_first>
  <behavior>
    - Test `LEDGER-1`: a hand-appended valid classification line inside the ledger fence parses into
      `parseLedgerBody`'s `lines[]` as `{ type: 'classification' }` with every field preserved.
    - Test `LEDGER-2`: a classification line with a missing or non-string required field, and a
      classification line that is torn/unparseable JSON, both land in `malformedLines` and
      `parseLedgerBody` does NOT throw.
    - Test `LEDGER-3`: with one unit line, one `range-complete` marker, and one classification line in
      the same fence, `resolveLedgerState(...).recorded` contains exactly the one unit record and no
      classification record; `verifyRunStatusCommand`'s `recorded[]` is unchanged by the presence of
      the classification line (kind isolation).
    - Test `LEDGER-4`: a `range-reset` marker does NOT supersede a classification record — reset
      semantics are unit-scoped only.
  </behavior>
  <action>
    In `src/cli/commands/verify-run.ts`:

    Add `export interface ClassificationRecord` with exactly these fields: `kind: 'classification'`,
    `pairId: string`, `unit: string`, `liveSlices: string[]`, `stagedSlices: string[]`,
    `provenance: 'source-changed' | 'source-unchanged' | 'unknown'`,
    `ruleDelta: 'cosmetic' | 'sharper' | 'contradictory' | 'unclassified'`, `stale: boolean`,
    `evidence: string`, `recordedAt: string`. Note `liveSlices`/`stagedSlices` are ARRAYS, not single
    strings — CONTEXT.md decision 4 (amended) makes the pair unit an m:n page-overlapping GROUP, and
    `174-RESEARCH.md` Pitfall 4 measured 6 staged files against 3 live slices on real data.

    Add `export interface ParsedClassificationLine { type: 'classification'; index: number; record:
    ClassificationRecord }` and widen `ParsedLine` to include it.

    Add a branch to `parseLedgerBody` discriminating `rec.kind === 'classification'`, placed BEFORE
    the existing unit branch (the unit branch accepts `kind === undefined || kind === 'unit'`, so a
    new kind must be claimed explicitly first). Validate every field's type, including that
    `liveSlices`/`stagedSlices` are arrays of strings, that `provenance` and `ruleDelta` are strings,
    and that `stale` is a boolean; anything failing goes to `malformedLines` — never thrown, exactly
    as the T-173-13 doc comment above `parseLedgerBody` requires. Do not validate enum membership
    here; that is the record command's normalization job (plan 174-04), and a parser that threw on an
    unknown label would defeat decision 8.

    In `resolveLedgerState`, skip `type === 'classification'` lines when building `recorded[]` and do
    NOT let `range-reset` supersede them. Add a second returned field
    `classifications: ClassificationRecord[]` resolved as last-write-wins per `pairId` (a later record
    for the same `pairId` supersedes an earlier one — the append-only ledger's re-classification path),
    keeping the existing `recorded`/`completeRanges` fields untouched so every current caller is
    unaffected.

    Add a `describe('verify-run.ts — classification record kind (174-02)')` block to
    `verify-run.test.ts` implementing LEDGER-1..4, building fixtures with the real
    `verifyRunInitCommand` + a direct ledger append (via the now-exported `appendLedgerLine` +
    `atomicWriteFile`) rather than hand-writing a whole RUN.md.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-run.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export interface ClassificationRecord" src/cli/commands/verify-run.ts` returns 1, and
      the interface declares `liveSlices: string[]` and `stagedSlices: string[]`.
    - `grep -c "'classification'" src/cli/commands/verify-run.ts` is at least 3 (interface, parse
      branch, ParsedClassificationLine).
    - `npx vitest run src/cli/commands/verify-run.test.ts` passes, including LEDGER-1..4.
    - The whole pre-existing `verify-run.test.ts` suite still passes unchanged — no existing
      assertion is edited or deleted to accommodate the new kind.
    - `npm test` green.
  </acceptance_criteria>
  <done>The RUN.md ledger carries classification records in its existing fence, with kind isolation and malformed-line tolerance pinned by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger file on disk → parser | A hand-edited or torn RUN.md is untrusted input to `parseLedgerBody` |
| sibling module → exported helpers | Widening the export surface widens who can write the ledger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-04 | Tampering | `parseLedgerBody` classification branch | mitigate | Every field type-validated; malformed lines routed to `malformedLines`, never thrown, never partially constructed (LEDGER-2) |
| T-174-05 | Denial of Service | a torn classification line halting status | mitigate | The T-173-13 discipline is preserved: an unparseable line reads as NOT recorded rather than an error (LEDGER-2) |
| T-174-06 | Elevation of Privilege | a second atomic-write path bypassing 173-08's crash-safety | mitigate | This plan exports the one hardened path instead of allowing a copy; plan 174-04 imports it (EXPORT-1 pins the import) |
| T-174-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (`174-RESEARCH.md` Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npx vitest run src/cli/commands/verify-run.test.ts` passes with the EXPORT and LEDGER blocks.
- `npm test` green (no regression across the 3611-test suite).
- Typecheck clean.
</verification>

<success_criteria>
`verify-classify.ts` can be written in the next wave using the one existing atomic ledger mechanism,
with a classification record kind the existing parser already understands and isolates.
</success_criteria>

<output>
Create `.planning/phases/174-verify-classifier/174-02-SUMMARY.md` when done
</output>
