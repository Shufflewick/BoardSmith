---
phase: 174-verify-classifier
plan: 03
type: execute
wave: 2
depends_on: [174-01, 174-02]
files_modified:
  - src/cli/commands/verify-classify.ts
  - src/cli/commands/verify-classify.test.ts
autonomous: true
requirements: [VERIFY-03]

must_haves:
  truths:
    - "The enumerated codes for provenance, rule delta, and presentation-marker exclusion are frozen constants pinned by tests, not hand-written unions or inline regexes."
    - "Staleness is a single-input function of the rule delta — provenance is not a parameter, even optionally."
    - "A live slice's page span is derived from its own p.N citation lines; a staged unit's from the ledger's rangeId. Neither reads INDEX.md."
    - "Pairing is a many-to-many page-overlap group join; a group whose two sides have different file counts is normal, and a page span present on only one side is an enumerated unpaired-slice finding."
    - "Presentation-only content is excluded from rule-bearing line counts on BOTH schemas — Visual (p.N): and legacy Derived (p.N) — diagram description:/— art:."
  artifacts:
    - path: "src/cli/commands/verify-classify.ts"
      provides: "PROVENANCE_KINDS, RULE_DELTA_KINDS, PRESENTATION_EXCLUSION_MARKERS, deriveStale, ruleBearingLines, livePageSpan, pairSlices, resolveProvenance"
      contains: "export function deriveStale"
    - path: "src/cli/commands/verify-classify.test.ts"
      provides: "Unit coverage tagged pairing / presentation / staleness / provenance, using the real archived fixtures from 174-FIXTURES"
      contains: "174-FIXTURES"
  key_links:
    - from: "src/cli/commands/verify-classify.ts"
      to: "src/cli/commands/chunk-provenance.ts"
      via: "imported computeVerificationScope / parseVerifiedAgainst / resolveCitedSlices"
      pattern: "from './chunk-provenance.js'"
    - from: "src/cli/commands/verify-classify.test.ts"
      to: ".planning/phases/174-verify-classifier/174-FIXTURES"
      via: "real slice bodies copied into a tmpdir fixture project"
      pattern: "174-FIXTURES"
---

<objective>
Build the mechanical, judgment-free half of the classifier as pure functions: the enumerated codes,
the presentation-exclusion filter, page-span pairing, and the two derived dimensions
(staleness from rule delta, provenance from hashes).

Purpose: CONTEXT.md's sort table puts everything except the rule-delta label in the CLI. Every piece
here is a place a wrong implementation would look like classifier misbehaviour — decision 12b's
filter is called out explicitly as "a correctness landmine, not a detail" because a missing legacy
marker manufactures `sharper` verdicts from schema drift alone and lands on SC-2's bar.

Output: `src/cli/commands/verify-classify.ts` pure-function core plus colocated tests driven by the
REAL slices archived in `174-FIXTURES/` by plan 174-01.

No command surface, no ledger writes, no CLI registration in this plan — that is 174-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/174-verify-classifier/174-CONTEXT.md
@.planning/phases/174-verify-classifier/174-RESEARCH.md
@.planning/phases/174-verify-classifier/174-PATTERNS.md
@.planning/phases/174-verify-classifier/174-PROOF.md
@.planning/phases/174-verify-classifier/174-01-SUMMARY.md
@src/cli/commands/build-manifest.ts
@src/cli/commands/chunk-provenance.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enumerated codes, the presentation-exclusion filter, and the staleness map</name>
  <files>src/cli/commands/verify-classify.ts, src/cli/commands/verify-classify.test.ts</files>
  <read_first>
    - `src/cli/commands/build-manifest.ts` lines 19-32 (`FINDING_KINDS` — the `Object.freeze([...] as
      const)` + derived-type shape to mirror; never a hand-written union)
    - `src/cli/commands/ingest-archive.ts` lines 295-326 (`PRESENTATION_LEXICON` — the shape a
      test-pinned lexicon constant takes in this repo)
    - `src/cli/slash-command/bs/ingest.test.ts` lines 490-505 (how a lexicon constant is pinned)
    - `.planning/phases/174-verify-classifier/174-PROOF.md` § "Presentation-marker inventory" (the
      MEASURED marker forms and counts this constant must cover — including any third qualifier plan
      174-01 found in real data)
    - `.planning/phases/174-verify-classifier/174-CONTEXT.md` decisions 2b, 3, 6, 12, 12b, 17
  </read_first>
  <behavior>
    - Test `presentation-1`: every real live slice archived under
      `174-FIXTURES/one-two-punch/live/` has its `Derived (p.N) — diagram description:` and
      `— art:` lines classified as presentation, and `ruleBearingLines()` on that file returns a count
      equal to (total `Derived (p.` lines) minus (those legacy presentation lines) plus its quote
      lines — asserted against the exact counts recorded in `174-PROOF.md` section 1.
    - Test `presentation-2`: every real staged slice under `174-FIXTURES/seven/staged/` has its
      `Visual (p.N):` lines classified as presentation and excluded from `ruleBearingLines()`.
    - Test `presentation-3`: a `Derived (p.1): Rounds are simultaneous …` line (no presentation
      qualifier) is rule-bearing and IS returned by `ruleBearingLines()`.
    - Test `presentation-4` (pin): `PRESENTATION_EXCLUSION_MARKERS` contains exactly the measured set
      and every member appears verbatim in the classification subagent contract file once plan 174-05
      lands — this test asserts the constant's membership now and is extended by 174-05 for the
      cross-file pin.
    - Test `staleness-1`: `deriveStale` returns false for `cosmetic` and true for `sharper`,
      `contradictory`, and `unclassified` — table-driven over `RULE_DELTA_KINDS` so a future added
      code fails the test until it is given an explicit mapping.
    - Test `staleness-2` (SC-4 structural): `deriveStale.length === 1` — the function takes exactly one
      parameter, so provenance cannot be threaded in, and `PROVENANCE_KINDS` does not appear anywhere
      in the source region defining the staleness map.
  </behavior>
  <action>
    Create `src/cli/commands/verify-classify.ts` with, in this order:

    `export const PROVENANCE_KINDS = Object.freeze(['source-changed', 'source-unchanged', 'unknown']
    as const)` plus `export type Provenance = (typeof PROVENANCE_KINDS)[number]`. Doc-comment it as
    CONTEXT.md decision 2b's three-state enum and state that `unknown` is never collapsed into either
    populated state.

    `export const RULE_DELTA_KINDS = Object.freeze(['cosmetic', 'sharper', 'contradictory',
    'unclassified'] as const)` plus `export type RuleDelta = (typeof RULE_DELTA_KINDS)[number]`.

    `export const PRESENTATION_EXCLUSION_MARKERS` — an `Object.freeze` array of the enumerated line
    prefixes that mark a presentation note on EITHER schema, taken from the measured inventory in
    `174-PROOF.md` section 1: the post-170 `Visual (p.N):` form and the pre-170
    `Derived (p.N) — diagram description:` and `Derived (p.N) — art:` forms, plus any additional
    qualifier plan 174-01 measured. Doc-comment it as test-pinned and measured against real slices,
    with the reason: the two sides of every real pair are on DIFFERENT schemas, and a filter that only
    knows the new prefix reads the old side's diagram notes as rule-bearing and manufactures `sharper`
    verdicts from schema drift alone (decision 12b).

    `export function isPresentationLine(line: string): boolean` — matches a line against the
    enumerated markers, case-insensitively on the qualifier, tolerating the em-dash-qualifier shape
    measured in real data. It must be driven by `PRESENTATION_EXCLUSION_MARKERS`, not by a regex
    literal duplicated inline.

    `export function ruleBearingLines(sliceText: string): string[]` — returns the slice's lines with
    presentation lines removed and blank/heading lines dropped, so a caller can count rule-bearing
    content. This is the function decision 17's "the bar is measured on rule-bearing content only"
    depends on.

    `export function deriveStale(ruleDelta: RuleDelta): boolean` backed by a
    `const STALE_BY_RULE_DELTA: Record<RuleDelta, boolean>` total map. Exactly one parameter.
    Doc-comment: provenance is never an input to this map (decision 3 / SC-4); `source-changed` with a
    `cosmetic` delta is NOT stale and `source-unchanged` with a `sharper` delta IS.

    Create `src/cli/commands/verify-classify.test.ts` following `verify-run.test.ts`'s fixture style —
    real filesystem `fs.mkdtemp(join(tmpdir(), 'bs-verify-classify-'))` temp dirs, no mocks. Add a
    module-level helper that resolves the archived fixture root
    (`.planning/phases/174-verify-classifier/174-FIXTURES`) relative to this test file and copies a
    named game's `live/` and `staged/` trees into the temp dir, so every test in this plan and 174-04
    runs against real slice bodies rather than invented ones. Implement `presentation-1..4` and
    `staleness-1..2` in `describe` blocks whose names contain the words `presentation` and `staleness`
    so `174-VALIDATION.md`'s `-t` commands select them.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-classify.test.ts -t "presentation" && npx vitest run src/cli/commands/verify-classify.test.ts -t "staleness"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Object.freeze" src/cli/commands/verify-classify.ts` is at least 3.
    - `grep -c "export function deriveStale(ruleDelta: RuleDelta): boolean" src/cli/commands/verify-classify.ts` returns 1, and `grep -v '^\s*\*' src/cli/commands/verify-classify.ts | grep -c "deriveStale(.*provenance"` returns 0.
    - The staleness test is table-driven over `RULE_DELTA_KINDS` (asserted by the test iterating the
      constant, not four hardcoded cases).
    - Presentation tests reference real files under `174-FIXTURES/` and assert counts equal to the
      numbers recorded in `174-PROOF.md` section 1.
    - `npx vitest run src/cli/commands/verify-classify.test.ts` passes.
  </acceptance_criteria>
  <done>The three enumerated constants, the dual-schema presentation filter, and the single-input staleness map exist and are pinned against real slice data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Page-span extraction and many-to-many pair grouping</name>
  <files>src/cli/commands/verify-classify.ts, src/cli/commands/verify-classify.test.ts</files>
  <read_first>
    - `.planning/phases/174-verify-classifier/174-RESEARCH.md` "Pattern 2" and "Pitfall 4" (the
      measured basis: `seven` has an INDEX Slices table, `one-two-punch` has none; a real
      re-transcription produced 6 staged files against 3 live slices; live slices carry `p.N,`
      citation prefixes)
    - `.planning/phases/174-verify-classifier/174-CONTEXT.md` decision 4 (AMENDED — page-span overlap,
      m:n groups, `unpaired-slice` findings)
    - `.planning/phases/174-verify-classifier/174-FIXTURES/` (the real live and staged slices this
      function is written against — read at least one live and one staged file from each game)
    - `src/cli/commands/verify-run.ts` lines 108-130 and 422-455 (`LedgerRecord.rangeId`, and
      `resolveLedgerState`'s pure-function style to follow)
    - `src/cli/commands/build-manifest.ts` lines 19-32 (`FINDING_KINDS` shape, for the new
      `unpaired-slice` finding kind)
  </read_first>
  <behavior>
    - Test `pairing-1`: `livePageSpan()` over the real `174-FIXTURES/seven/live/*.md` files returns
      the page spans derivable from their own `p.N,` citation lines, with no read of `INDEX.md`.
    - Test `pairing-2`: `livePageSpan()` works identically on
      `174-FIXTURES/one-two-punch/live/*.md`, which has NO `INDEX.md` Slices table — the case an
      INDEX-keyed implementation would be blind on.
    - Test `pairing-3`: given the real `seven` fixture's 3 live rule slices and 6 staged units,
      `pairSlices()` produces page-overlap groups in which a group holding 1 live file and 3 staged
      files is a normal `paired` group, NOT a finding.
    - Test `pairing-4`: a live slice whose page span overlaps no staged range yields exactly one
      `{ kind: 'unpaired-slice', side: 'staged-missing' }` finding naming that live slice; a staged
      range overlapping no live slice yields `{ kind: 'unpaired-slice', side: 'live-missing' }`.
      Neither is silently dropped.
    - Test `pairing-5`: `pairId` is stable — running `pairSlices()` twice over the same inputs, and
      over the same inputs in shuffled array order, yields identical `pairId` values and identical
      group membership (decision 16 depends on this).
    - Test `pairing-6`: a group whose every side is presentation-only (e.g. `00-visual-survey.md` with
      zero rule-bearing lines on both sides) is reported with `kind: 'presentation-only'` — reported,
      never silently skipped, and excluded from the rule-bearing denominator per decision 17.
  </behavior>
  <action>
    Add to `src/cli/commands/verify-classify.ts`:

    `export interface PageSpan { first: number; last: number }` and
    `export function livePageSpan(sliceText: string): PageSpan | undefined` — scans the slice's own
    content for `p.N` citation prefixes (the `^p\.(\d+),` shape measured on real live slices) and
    returns min/max. Returns `undefined` when the slice carries no citation at all; the caller reports
    that rather than guessing a span. **It must not read `INDEX.md`** — `one-two-punch` has no Slices
    table and an INDEX-keyed implementation is blind there (RESEARCH Pattern 2).

    `export function parseRangeId(rangeId: string): PageSpan` — parses the ledger's `"N-M"` rangeId
    shape into a span. Reject a rangeId that does not match with an actionable error naming the value.

    `export const PAIR_KINDS = Object.freeze(['paired', 'presentation-only', 'unpaired-slice'] as
    const)` with a derived type, following `FINDING_KINDS`' shape.

    `export interface SlicePair { pairId: string; kind: PairKind; span: PageSpan; liveSlices: string[];
    stagedSlices: string[]; stagedUnits: string[]; liveRuleBearingLines: number;
    stagedRuleBearingLines: number; missingSide?: 'live-missing' | 'staged-missing' }`.

    `export function pairSlices(input: { liveSlices: { path: string; text: string }[]; stagedUnits: {
    unit: string; slicePath: string; rangeId?: string; text: string }[] }): SlicePair[]` — a pure
    function, no I/O, in `resolveLedgerState`'s style. Algorithm: compute each live slice's span from
    its own content and each staged unit's span from its `rangeId`; build connected groups over page
    overlap (a live slice and a staged unit share a group when their spans intersect; groups merge
    transitively, so the m:n case RESEARCH measured is handled rather than approximated by a 1:1
    lookup). Derive `pairId` deterministically from the group's merged page span (e.g. the
    `pages-<first>-<last>` shape) so it is stable across runs and independent of input ordering and of
    LLM-chosen filenames. A group with files on only one side gets `kind: 'unpaired-slice'` plus
    `missingSide`. A group where both sides' `ruleBearingLines()` counts are zero gets
    `kind: 'presentation-only'`. Everything else is `paired`.

    A staged unit with no `rangeId` must be reported, not dropped: give it a group of its own with
    `kind: 'unpaired-slice'`, `missingSide: 'live-missing'`, and record the unit id — silent
    under-recording is the defect class decision 4 exists to prevent.

    Implement `pairing-1..6` in a `describe` block whose name contains `pairing`.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-classify.test.ts -t "pairing"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "INDEX.md" src/cli/commands/verify-classify.ts` returns 0 outside doc comments
      explaining why it is not read (verify with `grep -v '^\s*\*' src/cli/commands/verify-classify.ts | grep -c 'INDEX.md'` returning 0).
    - `pairSlices` is a pure function: `grep -v '^\s*\*' src/cli/commands/verify-classify.ts | grep -c "fs\." ` counts zero filesystem calls inside this task's added region (the pairing function receives text, it does not read it).
    - `pairing-3` asserts a 1-live/3-staged group is `kind: 'paired'` using the real `seven` fixture counts.
    - `pairing-5` asserts identical `pairId` sets across two invocations and a shuffled-input invocation.
    - `npx vitest run src/cli/commands/verify-classify.test.ts` passes.
  </acceptance_criteria>
  <done>Pairing is an m:n page-overlap group join over real fixture data, with stable pair ids and every one-sided or presentation-only group enumerated rather than skipped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Provenance resolution — three states, hash-only, never the subagent's opinion</name>
  <files>src/cli/commands/verify-classify.ts, src/cli/commands/verify-classify.test.ts</files>
  <read_first>
    - `src/cli/commands/chunk-provenance.ts` around lines 95, 165, and 572
      (`computeVerificationScope`, `resolveCitedSlices`, `parseVerifiedAgainst` — the exported
      functions this task composes; note the `f73153a3` lesson in that file: locate sections BY LINE,
      never by first substring match)
    - `.planning/phases/174-verify-classifier/174-CONTEXT.md` decisions 2 and 2b
    - `.planning/phases/174-verify-classifier/174-RESEARCH.md` "Open Questions" item 2 (why `unknown`
      exists: neither reference game has a prior recorded hash today)
  </read_first>
  <behavior>
    - Test `provenance-1`: a fixture project where a CHUNK.md citing the pair's live slice records a
      `Source hash:` equal to the current archived source's hash resolves to `source-unchanged`.
    - Test `provenance-2`: the same fixture with a differing recorded hash resolves to
      `source-changed`.
    - Test `provenance-3`: a project where NO chunk citing the pair's live slices records a source hash
      (the actual current state of both reference games) resolves to `unknown` — never
      `source-unchanged` by default.
    - Test `provenance-4`: where two citing chunks record different hashes and at least one differs
      from current, the result is `source-changed` (any disagreement with current is a change the
      designer must see), and the returned detail names every recorded hash found.
    - Test `provenance-5`: a project with no archived source at all resolves to `unknown` with a
      reason string, and does not throw.
    - Test `provenance-6` (SC-4 structural): the provenance result is not accepted by, and cannot be
      passed to, `deriveStale` — asserted by a type-level/arity check plus a source grep that the
      staleness call site passes only a rule delta.
  </behavior>
  <action>
    Add to `src/cli/commands/verify-classify.ts`:

    `export interface ProvenanceResult { provenance: Provenance; currentHash?: string;
    recordedHashes: string[]; reason: string }` and
    `export async function resolveProvenance(projectDir: string, liveSlices: string[]):
    Promise<ProvenanceResult>`.

    Resolution ladder, enumerated and documented in the doc comment:
    1. Current archived-source hash via `computeVerificationScope()` from `chunk-provenance.ts` —
       never a re-implemented sha256-of-archive (Don't-Hand-Roll table: re-deriving risks disagreeing
       with the source-resolution step that already ran in Step 1). No archive → `unknown` with the
       reason naming the missing archive.
    2. Prior recorded hashes: for every CHUNK.md in the project, use `resolveCitedSlices()` to find
       which cite any of this pair's live slices, and `parseVerifiedAgainst()` to read that chunk's
       recorded `Source hash:`. Collect all found hashes.
    3. No recorded hash found → `unknown` (decision 2b: on a first-ever verify pass of a
       pre-provenance project there is no prior hash, and both populated states would be claims the
       tool cannot support). Any recorded hash differing from current → `source-changed`. Otherwise
       (at least one recorded hash, all equal to current) → `source-unchanged`.

    Every branch sets `reason` to a short, enumerated, machine-stable phrase (not free prose) plus the
    measured inputs, so a human report can explain a verdict without re-deriving it. The subagent has
    no access to, and no say in, provenance — do not add any parameter through which a label could
    influence this function.

    Implement `provenance-1..6` in a `describe` block whose name contains `provenance`, building
    fixture projects with the real archived slices from `174-FIXTURES/` plus hand-written CHUNK.md
    `## Verified Against` blocks generated through the existing `chunk-check` path where practical.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-classify.test.ts -t "provenance"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from './chunk-provenance.js'" src/cli/commands/verify-classify.ts` returns 1 and the
      import names `computeVerificationScope`, `parseVerifiedAgainst`, and `resolveCitedSlices`.
    - `grep -v '^\s*\*' src/cli/commands/verify-classify.ts | grep -c "createHash" ` returns 0 in the
      provenance region — the archive hash is not recomputed locally.
    - `provenance-3` asserts the literal string `unknown`, and no branch of `resolveProvenance` can
      return `source-unchanged` when `recordedHashes` is empty (asserted by test, not by review).
    - `npx vitest run src/cli/commands/verify-classify.test.ts` passes; `npm test` green.
  </acceptance_criteria>
  <done>Provenance is a three-state, hash-only computation composed from the existing exported provenance plumbing, with `unknown` never collapsed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slice/CHUNK.md text on disk → parsers | Untrusted, possibly hand-edited, possibly on the old schema |
| caller-supplied paths → `resolveProvenance` | Paths must stay inside the project dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-07 | Spoofing | a presentation note passing as rule-bearing content | mitigate | `PRESENTATION_EXCLUSION_MARKERS` covers BOTH schemas, measured against real slices and test-pinned (decision 12b) |
| T-174-08 | Information Disclosure | `resolveProvenance` reading outside the project | mitigate | Apply `verify-run.ts:756-763`'s `relative()`/`startsWith('..')`/`isAbsolute()` guard to every path parameter before any read |
| T-174-09 | Repudiation | a provenance claim the tool cannot support | mitigate | `unknown` is a first-class state with a `reason`; no branch defaults to `source-unchanged` (provenance-3) |
| T-174-10 | Tampering | a hand-edited CHUNK.md hash steering provenance | accept | Provenance is advisory and never an input to staleness (decision 3); a tampered hash cannot suppress a stale verdict |
| T-174-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (`174-RESEARCH.md` Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npx vitest run src/cli/commands/verify-classify.test.ts` passes with `presentation`, `staleness`,
  `pairing`, and `provenance` blocks all green.
- `npm test` green.
</verification>

<success_criteria>
Every mechanical dimension of VERIFY-03 exists as a tested pure function over REAL slice data, with
provenance structurally unable to reach the staleness map.
</success_criteria>

<output>
Create `.planning/phases/174-verify-classifier/174-03-SUMMARY.md` when done
</output>
