---
phase: 174-verify-classifier
plan: 05
type: execute
wave: 4
depends_on: [174-04]
files_modified:
  - src/cli/slash-command/bs/verify/classification-subagent.md
  - src/cli/slash-command/bs/verify/classification-dispatch.md
  - src/cli/slash-command/bs/verify-game.md
  - src/cli/slash-command/bs/verify.test.ts
  - src/cli/commands/install-claude-command.ts
  - .planning/phases/174-verify-classifier/174-FIXTURES/lexicon/
autonomous: true
requirements: [VERIFY-03, VERIFY-07]

must_haves:
  truths:
    - "A classification subagent contract exists stating one decision procedure (consequence-equivalence), the dual-schema presentation exclusion with a real-data worked example, and one structured RETURN shape."
    - "The contract rejects a dispatch prompt lacking its required token, unread, before opening any slice."
    - "verify-game.md's 'never compares' and 'the pass ends here / no classification' statements are REWRITTEN IN PLACE, not appended around — no sentence anywhere in the skill still says the whole pass performs no classification."
    - "classification-dispatch.md drives resume from verify-classify-status --json, never from a directory scan, and dispatches one subagent per pending pair."
    - "Skill prose never derives staleness and never states a staleness rule — SC-4 lives only in code."
  artifacts:
    - path: "src/cli/slash-command/bs/verify/classification-subagent.md"
      provides: "The one judgment contract: decision procedure, exclusion rule, RETURN shape, scope limit"
      contains: "consequence"
    - path: "src/cli/slash-command/bs/verify/classification-dispatch.md"
      provides: "verify-game.md Step 4's delegate — pairing, resume, per-pair dispatch, recording, close"
      contains: "verify-classify-status"
    - path: "src/cli/slash-command/bs/verify.test.ts"
      provides: "Rewritten drift pins: classification vocabulary now REQUIRED in the right files, staleness vocabulary FORBIDDEN in all skill text, the deleted no-classification sentences pinned ABSENT"
      contains: "classification-dispatch.md"
  key_links:
    - from: "src/cli/slash-command/bs/verify-game.md Step 4"
      to: "src/cli/slash-command/bs/verify/classification-dispatch.md"
      via: "a ${CLAUDE_SKILL_DIR}/../bs-shared/verify/ pointer, same shape as Step 2's"
      pattern: "verify/classification-dispatch.md"
    - from: "classification-dispatch.md"
      to: "boardsmith verify-classify-record"
      via: "the orchestrator recording the subagent's returned label"
      pattern: "verify-classify-record"
---

<objective>
Write the judgment half — the classification subagent contract — and wire it into the skill as
`verify-game.md` Step 4, rewriting the Phase 173 boundary statements in place.

Purpose: CONTEXT.md decision 1 isolates exactly one judgment (the rule-delta label) in a subagent that
is the ONE place a slice is legitimately read, which is what closes VERIFY-07's classification half.
`174-RESEARCH.md` Pitfall 3 enumerates the exact statements that must be rewritten: leaving "there is
no classification" prose beside a new classification step is self-contradicting skill text, and Phase
170 proved one half gets ignored on a live run. `verify.test.ts` currently PINS those statements and
pins classification vocabulary ABSENT from every verify file — those pins are Phase 173's boundary and
this plan replaces them (no deprecation cycle; remove the bad thing, add the good thing).

Output: two new skill files, an in-place rewrite of `verify-game.md`, rewritten drift tests, an
installer leaf probe, and the hand-built lexicon regression pairs decision 15 asks for.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/174-verify-classifier/174-CONTEXT.md
@.planning/phases/174-verify-classifier/174-RESEARCH.md
@.planning/phases/174-verify-classifier/174-PATTERNS.md
@.planning/phases/174-verify-classifier/174-PROOF.md
@src/cli/slash-command/bs/verify-game.md
@src/cli/slash-command/bs/verify/staging-dispatch.md
@src/cli/slash-command/bs/ingest/transcription-subagent.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The classification subagent contract + hand-built lexicon regression pairs</name>
  <files>src/cli/slash-command/bs/verify/classification-subagent.md, .planning/phases/174-verify-classifier/174-FIXTURES/lexicon/</files>
  <read_first>
    - `src/cli/slash-command/bs/ingest/transcription-subagent.md` in full (the four-part shape to
      mirror: handshake at lines 18-49, "Your inputs" at 52-64, the single-decision-test + worked
      examples at 99-119, the structured-RETURN section at 151-186, the scope limit at 198-201)
    - `.planning/phases/174-verify-classifier/174-CONTEXT.md` decisions 9, 10, 11, 12, 12b
    - `.planning/phases/174-verify-classifier/174-PROOF.md` § "Presentation-marker inventory" (the
      measured marker forms this contract must enumerate verbatim)
    - `.planning/phases/174-verify-classifier/174-FIXTURES/one-two-punch/live/` (the real
      `Derived (p.1) — diagram description:` line used as the exclusion worked example — quote it from
      the real file, do not invent one)
    - `src/cli/commands/verify-classify.ts` (`RULE_DELTA_KINDS`, `PRESENTATION_EXCLUSION_MARKERS` — the
      contract's label names and marker list must match these constants exactly; a test pins it)
  </read_first>
  <action>
    Create `src/cli/slash-command/bs/verify/classification-subagent.md` with these sections, in this
    order, mirroring `transcription-subagent.md`'s structure:

    1. **FIRST: validate your dispatch prompt.** Require the exact token `BS-CLASSIFY-V1`. If absent,
       STOP immediately, **read no slice**, and return exactly a `DISPATCH REJECTED — missing
       BS-CLASSIFY-V1 token.` block explaining that the dispatcher composed the prompt from memory
       instead of copying the pointer block from `classification-dispatch.md`. Same discipline, same
       reason as `BS-DISPATCH-V2`: the token cannot be produced from memory, so carrying it is proof the
       block was copied.
    2. **Your inputs** — `Pair id`, `Live slices` (one or more paths), `Staged slices` (one or more
       paths). State plainly that a pair can be many-to-many: a live slice's pages may be covered by
       several staged files and vice versa, because the two passes chose section boundaries
       independently. Read those files and nothing else.
    3. **What is rule-bearing, and what is excluded.** Enumerate the presentation-marker forms verbatim
       from `PRESENTATION_EXCLUSION_MARKERS`: the post-170 `Visual (p.N):` form and the pre-170
       `Derived (p.N) — diagram description:` / `Derived (p.N) — art:` forms. Explain WHY both are
       listed: the two sides of a real pair are usually on different schemas — the live side predates
       the split and writes its diagram notes as a qualified `Derived` line — so a comparison that only
       excluded `Visual (p.N):` would read the old side's diagram notes as rule content and manufacture
       differences out of schema drift alone. Include the real worked example: quote the actual
       `Derived (p.1) — diagram description:` line from
       `174-FIXTURES/one-two-punch/live/01-setup-and-round-structure.md` and state that it is excluded
       identically to a `Visual (p.1):` line, and that an unqualified `Derived (p.N):` line stays
       rule-bearing and IS compared.
    4. **The decision procedure — one test, not a category list** (mirroring the Derived/Visual test's
       shape): *Would the two readings produce identical outcomes in every game situation?*
       - Yes → `cosmetic`.
       - No, and the two are compatible (pass 2 constrains something pass 1 left vague or absent) →
         `sharper`.
       - No, and the two cannot both be true → `contradictory`.
       State explicitly that this is equivalence of CONSEQUENCE, not similarity of WORDING — two
       independently-written sentences saying the same thing are `cosmetic` no matter how differently
       they are phrased. Add two worked examples per label, drawn from the real fixture slices where
       possible.
    5. **Line-level comparison, MAX-severity rollup.** Compare rule-bearing line by rule-bearing line,
       keep the per-line finding, and roll the pair verdict up as the maximum severity present
       (`contradictory` > `sharper` > `cosmetic`). Phase 175's impact map needs the line detail to scope
       repair, so the RETURN carries it.
    6. **RETURN a structured object only** — `{ pairId, label, evidence, quotedPass1, quotedPass2,
       lineFindings[] }`, where `label` is exactly one member of `cosmetic` / `sharper` /
       `contradictory` (never a sentence, never a hedge), `evidence` is the only prose field and nothing
       parses it, and `sharper`/`contradictory` REQUIRE `quotedPass1` and `quotedPass2` populated with
       both readings quoted VERBATIM. Never return the slice bodies. If the pair genuinely cannot be
       judged, say so in `evidence` and omit `label` — the recording command will enumerate that as
       `unclassified`, which is treated as stale; do not guess `cosmetic` to avoid an empty answer.
    7. **Scope limit.** This subagent never computes staleness, never decides provenance, never writes a
       ledger record, never writes any file, and never opens a slice it was not given. Staleness and
       provenance are computed in code from the label and the source hashes; a staleness claim in this
       return would be ignored.

    Then write the hand-built lexicon regression pairs under
    `.planning/phases/174-verify-classifier/174-FIXTURES/lexicon/`: at least six pair directories, each
    holding `live.md` and `staged.md` plus an `EXPECTED.md` naming the expected label and why — two
    expected `cosmetic` (pure rewording, and a reordering with identical consequence), two expected
    `sharper` (pass 2 adds a bound pass 1 left open; pass 2 names a tie-break pass 1 omitted), two
    expected `contradictory` (a changed number that both sides assert; a reversed precedence). Include
    one `cosmetic` case whose live side carries a legacy `Derived (p.N) — diagram description:` line
    differing wildly from the staged side's `Visual (p.N):` line — the schema-asymmetry trap, which must
    still be `cosmetic`. These are decision 15's regression pairs; plan 174-06 runs them through a real
    dispatch.
  </action>
  <verify>
    <automated>test -f src/cli/slash-command/bs/verify/classification-subagent.md && grep -q 'BS-CLASSIFY-V1' src/cli/slash-command/bs/verify/classification-subagent.md && grep -q 'diagram description' src/cli/slash-command/bs/verify/classification-subagent.md && ls .planning/phases/174-verify-classifier/174-FIXTURES/lexicon/*/EXPECTED.md | wc -l | grep -qE '[6-9]|[1-9][0-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `classification-subagent.md` contains the literal token `BS-CLASSIFY-V1` and a
      `DISPATCH REJECTED` block that instructs reading no slice.
    - It contains all three labels `cosmetic`, `sharper`, `contradictory` and every member of
      `PRESENTATION_EXCLUSION_MARKERS` verbatim.
    - It contains the real quoted `Derived (p.1) — diagram description:` line copied byte-identical
      from the archived `one-two-punch` live fixture.
    - It contains the words `RETURN` and the field names `pairId`, `label`, `evidence`, `quotedPass1`,
      `quotedPass2`, `lineFindings`.
    - It contains NO staleness vocabulary: `grep -ci 'stale' src/cli/slash-command/bs/verify/classification-subagent.md`
      returns 0 outside the scope-limit sentence that forbids it (assert with
      `grep -c 'never computes staleness' ...` returning 1 and no other `stale` occurrence).
    - At least 6 lexicon pair directories each contain `live.md`, `staged.md`, and `EXPECTED.md`, and at
      least one `EXPECTED.md` documents the schema-asymmetry `cosmetic` trap.
  </acceptance_criteria>
  <done>The one judgment contract exists with a real-data exclusion example and an enumerated RETURN, plus six hand-built lexicon regression pairs.</done>
</task>

<task type="auto">
  <name>Task 2: classification-dispatch.md delegate, verify-game.md in-place rewrite, installer probe</name>
  <files>src/cli/slash-command/bs/verify/classification-dispatch.md, src/cli/slash-command/bs/verify-game.md, src/cli/commands/install-claude-command.ts</files>
  <read_first>
    - `src/cli/slash-command/bs/verify/staging-dispatch.md` in full (the section shape to mirror:
      Context-Economics Hard Rule → Run Allocation → Resume → Dispatch → Recording → Close; the
      pointer-block discipline at lines 91-119; the ledger-not-filesystem rule at 49-53)
    - `src/cli/slash-command/bs/verify-game.md` lines 15-20, 30-39, 85-98, 100-116 (the exact
      statements to rewrite and the Reference Files list to extend)
    - `.planning/phases/174-verify-classifier/174-RESEARCH.md` "Pitfall 3" (the enumerated rewrite
      locations, with the before-text quoted)
    - `src/cli/commands/install-claude-command.ts` lines 55-105 (`SHARED_DIRS`, `SHARED_LEAF_PROBES`,
      `expectedInstallPaths` — `verify/` already installs recursively; a leaf probe is the added
      completeness check)
  </read_first>
  <action>
    Create `src/cli/slash-command/bs/verify/classification-dispatch.md` as Step 4's delegate, mirroring
    `staging-dispatch.md`'s sections:

    - **Context-Economics Hard Rule (restated for comparison, where the temptation is strongest):** the
      orchestrator never opens a slice, staged OR live. The classification subagent is the ONE place a
      slice is legitimately read. The observable a reviewer can check is unchanged from Step 2's: this
      skill's transcript contains no quoted-rule line, no `Derived (p.` line, no `Visual (p.` line.
    - **Pair enumeration:** `boardsmith verify-classify-pairs --project <dir> --run-id <runId> --json`.
      Take the pair set from its JSON. Never derive pairs by filename, never by reading `INDEX.md`,
      never by eyeballing the staging directory — the two passes chose different filenames and
      different section boundaries, so page-span overlap computed by the command is the only key both
      sides carry.
    - **Resume — before any dispatch:** `boardsmith verify-classify-status --project <dir> --run-id
      <runId> --json`; dispatch only `pendingPairs`. State the same reason `staging-dispatch.md` gives:
      the ledger, not the filesystem, is the only source of truth for what is recorded. Note that
      `unpaired-slice` and `presentation-only` groups are reported by the command and are NOT dispatched
      — they are surfaced to the designer in Step 3's report.
    - **Dispatch:** one Task-tool subagent per pending pair. Do not compose, restate, or summarize the
      classification contract in the prompt — it lives in
      `${CLAUDE_SKILL_DIR}/../bs-shared/verify/classification-subagent.md` and the subagent reads it
      directly. Give the pointer block to copy byte-identical, carrying the `BS-CLASSIFY-V1` token and
      the fields `Pair id`, `Live slices`, `Staged slices`, filled from the pairs JSON. State that the
      token is required and validated, and that a dispatch without it is rejected unread, with the same
      "you cannot produce the token from memory" reasoning `staging-dispatch.md` uses.
    - **Recording:** for each returned label, `boardsmith verify-classify-record --run-id <runId>
      --pair-id <pairId> --label <label> --evidence <text> --quoted-pass1 <text> --quoted-pass2 <text>
      --project <dir> --json`, recorded from the subagent's returned fields — the orchestrator does not
      open a slice to check them. State that staleness and provenance are computed by the command and
      that this file must not compute, restate, or predict either. A missing or non-enum label is passed
      through as received; the command enumerates it as `unclassified`, which is conservative by design
      — never retry with a guessed label to make a pair look classified.
    - **Close:** when `verify-classify-status --json` reports `pendingPairs` empty, this step is done and
      `verify-game.md` Step 3 takes over. This phase records verdicts and nothing else: no marker is
      flipped in CHUNK.md or SKETCH.md, no repair loop is opened, no human adjudication gate is run
      (Phase 175's work), and no staged slice is ever promoted over a live one.

    Then rewrite `src/cli/slash-command/bs/verify-game.md` IN PLACE:
    - Lines 15-20: delete "and never compares the staged output to what already exists. That comparison
      is a later phase's job; this skill's job ends the moment staging closes." Replace with a sentence
      stating it never writes a staged slice over a live one, and that comparison happens in Step 4
      below. Keep the "no flag or path anywhere in this skill writes staged output into a live
      location" sentence — still true.
    - Lines 96-98: delete the entire "**The pass ends here.** There is no comparison … Staging and
      recording is the entire scope of this skill." paragraph. It cannot survive beside a Step 4.
    - Renumber Close to Step 5 (or place the new Step 4 before Close and keep Close's number
      consistent), and add a `## Step 4: Classification (VERIFY-03, VERIFY-07)` section: a short router
      paragraph dispatching to
      `${CLAUDE_SKILL_DIR}/../bs-shared/verify/classification-dispatch.md`, matching Step 2's brevity
      and shape — pair enumeration by command, per-pair subagent dispatch, verdict recording by
      command. State in one sentence that this step records verdicts only: it flips no staleness marker
      and opens no repair loop (Phase 175).
    - Close (formerly Step 3): keep the staging-is-KEPT and lock-release rules, and add that the run's
      classification verdicts are reported to the designer by formatting
      `verify-classify-status --json` — **formatted, never computed** by this skill.
    - Extend the Reference Files list with the two new files and their one-line roles.

    Then add `join(SHARED_ROOT, 'verify', 'classification-subagent.md')` to `SHARED_LEAF_PROBES` in
    `src/cli/commands/install-claude-command.ts`, so a partial install that left the new contract
    behind reads as incomplete rather than as installed.
  </action>
  <verify>
    <automated>grep -q 'Step 4' src/cli/slash-command/bs/verify-game.md && ! grep -q 'The pass ends here' src/cli/slash-command/bs/verify-game.md && ! grep -q 'never compares the staged output' src/cli/slash-command/bs/verify-game.md && grep -q 'classification-dispatch.md' src/cli/slash-command/bs/verify-game.md && grep -q "classification-subagent.md" src/cli/commands/install-claude-command.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'The pass ends here' src/cli/slash-command/bs/verify-game.md` returns 0.
    - `grep -c 'never compares the staged output' src/cli/slash-command/bs/verify-game.md` returns 0.
    - `grep -c 'no classification' src/cli/slash-command/bs/verify-game.md` returns 0.
    - `verify-game.md` contains `## Step 4` naming classification and a
      `${CLAUDE_SKILL_DIR}/../bs-shared/verify/classification-dispatch.md` pointer, plus both new files
      in its Reference Files list.
    - `classification-dispatch.md` contains the literal strings `BS-CLASSIFY-V1`,
      `verify-classify-pairs`, `verify-classify-status`, `verify-classify-record`, and a sentence
      stating the ledger not the filesystem is the source of truth.
    - `classification-dispatch.md` contains no staleness derivation rule:
      `grep -c 'cosmetic → \|marks stale\|is stale' src/cli/slash-command/bs/verify/classification-dispatch.md`
      returns 0.
    - `staging-dispatch.md` is unmodified (`git diff --name-only` does not list it) — its own
      scope-fence sentence remains true for that file.
    - `SHARED_LEAF_PROBES` contains the new contract path.
  </acceptance_criteria>
  <done>Step 4 exists, the contradicting Phase 173 boundary statements are gone from verify-game.md, and the new contract is install-probed.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite verify.test.ts's boundary pins and add the cross-file lexicon pin</name>
  <files>src/cli/slash-command/bs/verify.test.ts</files>
  <read_first>
    - `src/cli/slash-command/bs/verify.test.ts` in full — especially lines 41-45
      (`ALL_VERIFY_FILES`), 228-238 (the staging-dispatch close pin), 240-277 (the VERIFY-07
      pointer-not-restatement block), and 279-306 (`describe('Decision 16 — this phase classifies
      nothing (structural absence)')`, which pins `cosmetic`/`sharper`/`contradictory`/`classify` ABSENT
      from every verify file and pins `no comparison`/`no classification` PRESENT in verify-game.md —
      all four of those assertions are now false and must be replaced)
    - `src/cli/slash-command/bs/ingest.test.ts` lines 490-505 (the `PRESENTATION_LEXICON` cross-file
      pin pattern to mirror for `PRESENTATION_EXCLUSION_MARKERS`)
    - `src/cli/commands/verify-classify.ts` (`PRESENTATION_EXCLUSION_MARKERS`, `RULE_DELTA_KINDS`)
  </read_first>
  <action>
    Rewrite `src/cli/slash-command/bs/verify.test.ts`'s boundary assertions — this is a replacement, not
    an addition. Concretely:

    - Extend `ALL_VERIFY_FILES` with `'verify/classification-dispatch.md'` and
      `'verify/classification-subagent.md'`.
    - DELETE the `describe('Decision 16 — this phase classifies nothing (structural absence)')` block's
      first and third tests (the classification-vocabulary absence gate and the
      `no comparison`/`no classification` presence gate). Keep the `--apply`/`promote`/`cutover`
      absence test and extend it to the two new files — nothing here promotes a staged slice, and that
      is still true.
    - ADD a `describe('verify-game.md — Step 4 classification routing (VERIFY-03)')` block pinning:
      Step 4 exists; it points at `verify/classification-dispatch.md`; the deleted sentences are ABSENT
      (`expect(skill).not.toContain('The pass ends here')`,
      `not.toContain('never compares the staged output')`, `not.toMatch(/no classification/)`); and
      Close states the verdict report is FORMATTED from `verify-classify-status --json`, never computed.
    - ADD a `describe('classification-subagent.md — the one judgment contract')` block pinning: the
      `BS-CLASSIFY-V1` token; the `DISPATCH REJECTED` block; all three labels; the consequence-vs-wording
      sentence; the RETURN field names; and the scope-limit sentence that the subagent never computes
      staleness.
    - ADD a `describe('classification-dispatch.md — pointer, not restatement (VERIFY-07)')` block
      pinning: the pointer to `${CLAUDE_SKILL_DIR}/../bs-shared/verify/classification-subagent.md`; the
      `BS-CLASSIFY-V1` token in the dispatch block; the three `verify-classify-*` command names; and a
      no-fork guard mirroring the existing `CONTRACT_BODY_MARKERS` test — no file under `bs/verify/`
      other than `classification-subagent.md` may contain that contract's own decision-procedure
      marker text.
    - ADD a `describe('SC-4 — no skill file derives staleness')` block: for every file in
      `ALL_VERIFY_FILES`, after `stripComments`, assert the text does NOT contain a staleness
      derivation rule (patterns like `marks stale`, `→ stale`, `is stale`, `not stale`). The one place
      staleness is derived is `deriveStale` in code, with a test; prose that restates the map is prose
      that can drift out of agreement with it.
    - ADD the cross-file lexicon pin, mirroring `ingest.test.ts:490-505`: read
      `PRESENTATION_EXCLUSION_MARKERS`'s declaration out of `src/cli/commands/verify-classify.ts` by
      regex and assert every member appears verbatim in `classification-subagent.md`, and that the
      contract introduces no presentation-marker form the constant lacks. This is the pin that keeps
      decision 12b's exclusion set identical on both sides of the code/prose boundary.

    Preserve the file's existing honesty caveat at lines 10-16 and extend it: these assertions prove the
    instruction EXISTS, not that a live session follows it — the acceptance bar for VERIFY-03 and
    VERIFY-07 is the live proof owed by plans 174-06 and 174-07 in `174-PROOF.md`.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/verify.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "classifies nothing" src/cli/slash-command/bs/verify.test.ts` returns 0.
    - `grep -c "classification-subagent.md" src/cli/slash-command/bs/verify.test.ts` is at least 2.
    - `grep -c "PRESENTATION_EXCLUSION_MARKERS" src/cli/slash-command/bs/verify.test.ts` is at least 1.
    - The SC-4 prose-absence block iterates `ALL_VERIFY_FILES` (asserted by the test using the constant,
      not a hardcoded list).
    - `npx vitest run src/cli/slash-command/bs/verify.test.ts` passes; `npm test` green.
  </acceptance_criteria>
  <done>The drift pins now describe the post-174 boundary: classification vocabulary required where it belongs, staleness prose forbidden everywhere, and the exclusion lexicon pinned across the code/prose boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatcher → classification subagent | The prompt is untrusted; a composed-from-memory prompt is the known failure mode |
| subagent → orchestrator | The return crosses into the orchestrator's context and must not carry slice bodies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-16 | Spoofing | a dispatch prompt composed from memory instead of copied | mitigate | `BS-CLASSIFY-V1` token required and validated by the contract; a dispatch without it is rejected unread, before any slice is opened |
| T-174-17 | Information Disclosure | slice bodies leaking into the orchestrator transcript | mitigate | RETURN shape forbids slice bodies; the transcript-absence observable is grepped for real in plan 174-06 (VERIFY-07) |
| T-174-18 | Tampering | self-contradicting skill text letting a live session skip Step 4 | mitigate | The contradicting sentences are DELETED, and their absence is grep-pinned by a test (Task 3) |
| T-174-19 | Repudiation | skill prose restating the staleness map and drifting from code | mitigate | SC-4 prose-absence gate across every verify file (Task 3) |
| T-174-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (`174-RESEARCH.md` Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/verify.test.ts` green with the rewritten pins.
- `npm test` green.
- `grep -rn 'no classification' src/cli/slash-command/bs/verify-game.md` returns nothing.
</verification>

<success_criteria>
The skill can run a classification step: one judgment contract, one delegate, no sentence anywhere
claiming the pass performs no classification, and no prose copy of the staleness map.
</success_criteria>

<output>
Create `.planning/phases/174-verify-classifier/174-05-SUMMARY.md` when done
</output>
