---
phase: 174-verify-classifier
plan: 06
type: execute
wave: 5
depends_on: [174-05]
files_modified:
  - .planning/phases/174-verify-classifier/174-PROOF.md
autonomous: true
requirements: [VERIFY-03, VERIFY-07]

must_haves:
  truths:
    - "The SC-2 numeric bar (≥90% cosmetic, zero contradictory) is written down BEFORE any verdict is measured, and the actual measured counts are recorded beside it."
    - "Every pair in a real run carries both dimensions independently: a provenance value from the hash compare and a rule-delta label from the subagent (SC-1)."
    - "Two independent good-faith transcriptions of the same real reference-game pages classify overwhelmingly cosmetic, measured with counts, not asserted (SC-2)."
    - "Zero slice-body-shaped lines appear across the orchestrator transcript, the raw dispatch prompt, and the raw subagent return of a real classification pass (VERIFY-07)."
    - "The same pair set classified twice yields identical verdicts, with the comparison recorded (decision 16)."
    - "Both reference-game originals are byte-identical before and after."
  artifacts:
    - path: ".planning/phases/174-verify-classifier/174-PROOF.md"
      provides: "Sections 2 (SC-1/SC-2 real classification pass, bar pre-declared then measured), 3 (VERIFY-07 transcript grep), 4 (determinism + lexicon regression)"
      contains: "## 2. SC-1 / SC-2"
  key_links:
    - from: "a live /bs-verify-game Step 4 session"
      to: "boardsmith verify-classify-pairs / -record / -status"
      via: "the session actually invoking the commands — Phase 170's central failure mode"
      pattern: "verify-classify-"
---

<objective>
Earn SC-1, SC-2, SC-5, VERIFY-07's classification half, and decision 16's determinism check with a
real classification pass over the real pass-1-vs-pass-2 material.

Purpose: `174-VALIDATION.md` is explicit that no task may claim VERIFY-03 or VERIFY-07 on
`.toContain()` evidence alone — Phase 170 proved twelve instruction-shaped mechanisms were skipped on
live runs while every automated check stayed green. CONTEXT.md decision 14 makes SC-2's numeric bar a
phase BLOCKER, declared before it is measured. This is where the phase earns its requirements.

Output: `174-PROOF.md` sections 2, 3, and 4.

**Read-only invariant.** Same as plan 174-01: every command runs against a `cp -R` copy; no command
receives a path inside `~/BoardSmithGames/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/174-verify-classifier/174-CONTEXT.md
@.planning/phases/174-verify-classifier/174-VALIDATION.md
@.planning/phases/174-verify-classifier/174-PROOF.md
@.planning/phases/174-verify-classifier/174-05-SUMMARY.md
@.planning/phases/173-verify-pipeline-core/173-PROOF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare the bar, reconstitute the real material, run a real classification pass</name>
  <files>.planning/phases/174-verify-classifier/174-PROOF.md</files>
  <read_first>
    - `.planning/phases/174-verify-classifier/174-PROOF.md` section 1 and
      `174-FIXTURES/MANIFEST.md` (the run-ids, per-file sha256s, and the reproduce command for the real
      pass-2 material)
    - `.planning/phases/173-verify-pipeline-core/173-PROOF.md` section 3 (the shape a live-pass proof
      takes in this milestone: how the pass was actually run, verbatim per-step evidence, then
      independent verification that never trusts a command's own output)
    - `src/cli/slash-command/bs/verify/classification-dispatch.md` and
      `src/cli/slash-command/bs/verify/classification-subagent.md` (the contracts under proof)
    - `.planning/phases/174-verify-classifier/174-CONTEXT.md` decisions 14, 16, 17
  </read_first>
  <action>
    **First, before any verdict exists, open `174-PROOF.md` section `## 2. SC-1 / SC-2 — a real
    classification pass` with a `### The bar, declared before measuring` block** stating verbatim:
    ≥90% of paired, rule-bearing slice pairs classify `cosmetic`, and zero classify `contradictory`;
    missing the bar is a phase blocker, not a note; the percentage is computed over paired rule-bearing
    pairs only, so presentation-only groups can neither inflate nor deflate it. Commit this block before
    running the pass so the declaration provably precedes the measurement.

    Then reconstitute the real material at `SCRATCH="${TMPDIR:-/tmp}/174-proof"`. If the plan-174-01
    copies still exist there and their staged trees still match `MANIFEST.md`'s sha256s, use them
    as-is. Otherwise rebuild each: `cp -R` the original game, adopt its source with the same real
    `boardsmith ingest-archive` call plan 174-01 used (the same input file yields the same hash), then
    restore the archived staged tree and `RUN.md` from `174-FIXTURES/<game>/staged/` and
    `174-FIXTURES/<game>/RUN.md` into `rulebook/.verify/<runId>/`. Verify every restored file's
    `shasum -a 256` against `MANIFEST.md` and paste the comparison — the classification input must be
    provably the real pass-2 bytes, not a fresh approximation. State explicitly in the proof which of
    the two paths was taken.

    Re-preflight the originals (pinned HEAD for `seven`, observed HEAD for `one-two-punch`, whole-tree
    sha256 `.before` manifests) and install the CURRENT skills tree into each copy with a real
    `npx boardsmith claude --local --force`. Assert
    `.claude/skills/bs-shared/verify/classification-dispatch.md` and
    `.claude/skills/bs-shared/verify/classification-subagent.md` exist in each copy by filesystem read —
    this is SC-1's install half for the new files and the installer-probe change from plan 174-05.

    Then run the real classification pass per copy, following
    `verify/classification-dispatch.md` exactly:
    1. `boardsmith verify-classify-pairs --project <copy> --run-id <runId> --json` — paste the full JSON.
    2. `boardsmith verify-classify-status --project <copy> --run-id <runId> --json` — take `pendingPairs`.
    3. One real Task-tool subagent per pending pair, with the `BS-CLASSIFY-V1` pointer block copied
       byte-identical from `classification-dispatch.md`. Capture the RAW dispatch prompt and the RAW
       subagent return for every dispatch — both are evidence for Task 2's VERIFY-07 grep and must be
       preserved verbatim, not summarised.
    4. `boardsmith verify-classify-record …` per returned label, then a final
       `verify-classify-status --json`.

    Record everything verbatim. Never write "ran clean".
  </action>
  <verify>
    <automated>grep -q '## 2. SC-1 / SC-2' .planning/phases/174-verify-classifier/174-PROOF.md && grep -q 'The bar, declared before measuring' .planning/phases/174-verify-classifier/174-PROOF.md && grep -q 'pendingPairs' .planning/phases/174-verify-classifier/174-PROOF.md</automated>
  </verify>
  <acceptance_criteria>
    - The `### The bar, declared before measuring` block exists and is in an earlier commit than the
      measured counts (shown by `git log --oneline -- .planning/phases/174-verify-classifier/174-PROOF.md`).
    - Every restored fixture file's sha256 matches `174-FIXTURES/MANIFEST.md`, comparison pasted.
    - Both copies contain `.claude/skills/bs-shared/verify/classification-subagent.md` (filesystem read).
    - The proof carries, per copy: the full `verify-classify-pairs --json`, every raw dispatch prompt,
      every raw subagent return, every `verify-classify-record --json`, and the final
      `verify-classify-status --json`.
    - The final `verify-classify-status --json` reports `pendingPairs: []` for each copy.
  </acceptance_criteria>
  <done>The bar is on record before any verdict; a real classification pass over the real pass-2 material has run on both games with raw prompts and returns captured.</done>
</task>

<task type="auto">
  <name>Task 2: Measure SC-1 and SC-2 against the bar, and grep VERIFY-07's transcript observable</name>
  <files>.planning/phases/174-verify-classifier/174-PROOF.md</files>
  <read_first>
    - `.planning/phases/173-verify-pipeline-core/173-PROOF.md` § "SC-3 — the orchestrator never reads a
      slice: independent verification" (the exact grep method and the honesty about what it proves)
    - `174-PROOF.md` sections 1 and 2 as written so far (the counts and raw captures being measured)
    - `.planning/phases/174-verify-classifier/174-CONTEXT.md` decisions 14, 17 and the `<specifics>`
      note on the transcript observable
  </read_first>
  <action>
    **SC-1 measurement.** From the final `verify-classify-status --json` and the ledger's classification
    lines, tabulate every pair: `pairId`, `kind`, live/staged file counts, `provenance`, `ruleDelta`,
    `stale`. Assert in the proof that both dimensions are present and INDEPENDENT — show at least the
    cross-tab of provenance against rule delta, and state which cells are populated. Note explicitly
    that a `cosmetic` verdict with non-`source-unchanged` provenance is NOT stale and that no cell of the
    cross-tab was used to derive staleness (SC-4's structural claim already has a unit test; this is its
    real-data corroboration). Given both reference games are pre-provenance, expect `unknown` provenance
    on the first pass — record it as measured, and do NOT reinterpret it as `source-unchanged`.

    **SC-2 measurement.** Compute, per game and combined: total pairs, paired rule-bearing pairs,
    `cosmetic` / `sharper` / `contradictory` / `unclassified` counts, and the cosmetic percentage over
    paired rule-bearing pairs. Print the numbers beside the pre-declared bar and state PASS or FAIL
    explicitly. If the bar is MISSED: per decision 17, the first thing to check is whether the exclusion
    filter is complete — re-grep both sides of the failing pairs for presentation markers and report
    whether the misses concentrate on pairs whose live side carries legacy `— diagram description` /
    `— art` annotations, BEFORE concluding the classifier over-flags. Record that diagnosis in the
    proof and stop for the developer rather than adjusting the bar.

    **VERIFY-07 measurement.** Grep, independently, three artifacts per dispatch: the orchestrator
    transcript, the RAW dispatch prompt, and the RAW subagent return. Patterns: `Derived (p.`,
    `Visual (p.`, and a quoted-rule-line shape (a `QUOTE`/`p.N, Section:` citation prefix). Report the
    per-artifact match count. The expectation is zero in the orchestrator transcript and in the dispatch
    prompt; the subagent return may legitimately quote rule lines inside `quotedPass1`/`quotedPass2` for
    a `sharper`/`contradictory` verdict, so state that exception explicitly and report how many quoted
    lines appeared and in which field — VERIFY-07 is about the ORCHESTRATOR never reading a slice, and
    a proof that blurs that distinction proves nothing. Include the exact grep commands so the check is
    re-runnable.

    Write all of this into `174-PROOF.md` section 2 (SC-1/SC-2) and a new
    `## 3. VERIFY-07 — the orchestrator never opened a slice` section, and add a short
    "what this pass did NOT prove" list, following 173-PROOF's discipline.
  </action>
  <verify>
    <automated>grep -q '## 3. VERIFY-07' .planning/phases/174-verify-classifier/174-PROOF.md && grep -qE 'cosmetic.*%|cosmeticPct' .planning/phases/174-verify-classifier/174-PROOF.md && grep -q 'what this pass did NOT prove' .planning/phases/174-verify-classifier/174-PROOF.md</automated>
  </verify>
  <acceptance_criteria>
    - The proof contains a per-pair table with both `provenance` and `ruleDelta` columns populated for
      every pair, plus the provenance × rule-delta cross-tab.
    - The proof states the measured cosmetic percentage as a number beside the declared ≥90% bar and the
      literal word PASS or FAIL, plus the `contradictory` count against the zero bar.
    - If FAIL, the proof contains the exclusion-filter diagnosis required by decision 17 and does not
      restate the bar at a lower value.
    - The VERIFY-07 section reports per-artifact grep counts with the commands shown, and states the
      `quotedPass1`/`quotedPass2` exception explicitly rather than silently.
    - A "what this pass did NOT prove" list is present.
  </acceptance_criteria>
  <done>SC-1 and SC-2 are measured with real counts against a pre-declared bar, and VERIFY-07's observable is grepped rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Determinism double-run and the hand-built lexicon regression pairs</name>
  <files>.planning/phases/174-verify-classifier/174-PROOF.md</files>
  <read_first>
    - `.planning/phases/173-verify-pipeline-core/173-PROOF.md` § "Clean-run comparison" (the ad hoc
      two-run diff discipline this task mirrors — RESEARCH Pitfall 5 confirms no reusable command exists)
    - `.planning/phases/174-verify-classifier/174-FIXTURES/lexicon/` (the six-plus hand-built pairs and
      their `EXPECTED.md` labels written by plan 174-05)
    - `.planning/phases/174-verify-classifier/174-CONTEXT.md` decisions 15, 16
  </read_first>
  <action>
    **Determinism (decision 16).** Classify the SAME real pair set a second time — a fresh `--run-id`
    over the same reconstituted material, or a second pass recording into a second run's ledger — with
    fresh subagent dispatches. Then diff the two runs' verdict sets independently: extract
    `(pairId, ruleDelta, stale)` triples from each run's `verify-classify-status --json` and compare them
    with an external diff, never trusting either run's own "matched" claim. Record: the two run-ids, both
    triple sets, the diff output, and the verdict — identical or not. Also confirm `pairId` values are
    identical across the two runs (the stability `pairSlices` promises). If any verdict moved, record it
    as a finding with both labels and both evidence strings; a classifier whose verdicts move between
    runs cannot support a staleness marker anything downstream trusts, so this is reported, never smoothed.

    **Lexicon regression (decision 15's second half).** Dispatch the classification subagent against each
    hand-built pair under `174-FIXTURES/lexicon/`, using the same `BS-CLASSIFY-V1` pointer block, and
    tabulate returned label against `EXPECTED.md`. Report the hit rate and every miss with both the
    expected and returned label plus the returned `evidence`. Call out the schema-asymmetry trap pair
    specifically — if it returns anything but `cosmetic`, the exclusion set or the contract's worked
    example is incomplete, and that is the same failure mode SC-2's bar would surface less directly.
    These are regression evidence ALONGSIDE the real proof, never in place of it.

    Close with the originals re-verification: recompute both `~/BoardSmithGames` whole-tree sha256
    manifests and diff against the `.before` captures, pasting the result.

    Write both into `174-PROOF.md` as `## 4. Determinism and lexicon regression`.
  </action>
  <verify>
    <automated>grep -q '## 4. Determinism and lexicon regression' .planning/phases/174-verify-classifier/174-PROOF.md && grep -qE 'identical|verdict moved' .planning/phases/174-verify-classifier/174-PROOF.md</automated>
  </verify>
  <acceptance_criteria>
    - Two distinct run-ids are recorded with their full `(pairId, ruleDelta, stale)` triple sets and an
      external diff output.
    - The proof states whether pair ids were identical across runs and whether any verdict moved, with
      any mover reported in full.
    - Every `174-FIXTURES/lexicon/` pair appears in the expected-vs-returned table, including the
      schema-asymmetry trap pair called out by name.
    - `diff` of before/after whole-tree manifests for BOTH originals is empty, pasted in the proof.
  </acceptance_criteria>
  <done>Determinism is measured rather than assumed, the lexicon has real regression evidence, and the originals are proven untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this plan → `~/BoardSmithGames/*` | Read-only originals a bug could destroy |
| subagent returns → the proof record | Untrusted content quoted into an evidence document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-20 | Tampering | reference-game originals | mitigate | All work on `$SCRATCH` copies; pinned HEAD asserted; before/after whole-tree sha256 diff pasted (Task 1 + Task 3) |
| T-174-21 | Repudiation | a bar adjusted after the fact to pass | mitigate | The bar is committed in a separate, earlier commit than the measurement, verified via `git log` on the proof file |
| T-174-22 | Spoofing | fixture material silently regenerated and passed off as the original pass-2 output | mitigate | Every restored file's sha256 verified against `MANIFEST.md`, and the proof must state which reconstitution path was taken |
| T-174-23 | Information Disclosure | slice bodies entering the orchestrator context | mitigate | VERIFY-07 grep across transcript + raw prompt + raw return, with the `quotedPass*` exception stated rather than hidden |
| T-174-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (`174-RESEARCH.md` Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `174-PROOF.md` sections 2, 3, and 4 exist with the bar declared before measurement, measured counts,
  per-artifact grep counts, and the determinism diff.
- Both originals byte-identical before and after.
</verification>

<success_criteria>
SC-1, SC-2, SC-5, VERIFY-07's classification half, and decision 16's determinism check all have real
measured evidence — or a recorded, diagnosed failure that stops the phase rather than a lowered bar.
</success_criteria>

<output>
Create `.planning/phases/174-verify-classifier/174-06-SUMMARY.md` when done
</output>
