---
phase: 174-verify-classifier
plan: 07
type: execute
wave: 6
depends_on: [174-06]
files_modified:
  - .planning/phases/174-verify-classifier/174-PROOF.md
  - .planning/phases/174-verify-classifier/174-VALIDATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [VERIFY-01, VERIFY-03, VERIFY-07]

must_haves:
  truths:
    - "A genuine rules change is introduced by MUTATING THE REAL ARCHIVED SOURCE and re-transcribing it through a real dispatch — not by hand-editing a staged slice — and the affected pair classifies sharper or contradictory (SC-3)."
    - "The source mutation flips the provenance dimension to source-changed on the affected pair, and provenance still plays no part in the staleness verdict (SC-4 on real data)."
    - "A real run produces a per-chunk verdict without rebuilding the project (VERIFY-01)."
    - "REQUIREMENTS.md records VERIFY-01, VERIFY-03, and VERIFY-07 with citations to the proof sections that earned them — or records honestly why one stays open."
    - "174-PROOF.md carries a phase-wide 'what is still unproven' list and a 'how to re-run every proof' block."
  artifacts:
    - path: ".planning/phases/174-verify-classifier/174-PROOF.md"
      provides: "Sections 5 (SC-3 real source mutation) and 6 (VERIFY-01 per-chunk verdict + phase closeout)"
      contains: "## 5. SC-3"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VERIFY-03 closed, VERIFY-01 and VERIFY-07 closed with the classification-half citations Phase 173 left owing"
      contains: "VERIFY-03"
  key_links:
    - from: "a mutated rulebook/source/<file> in a copy"
      to: "a sharper or contradictory verdict on the affected pair"
      via: "real re-transcription dispatch then real classification dispatch — the pipeline, not the comparator alone"
      pattern: "sharper|contradictory"
---

<objective>
Prove the pipeline catches a genuine rules change (SC-3), close VERIFY-01's per-chunk-verdict half on
real data, and close the phase's evidence trail.

Purpose: CONTEXT.md decision 15 is explicit — hand-editing a staged slice tests only the comparator;
the requirement is that the *pipeline* catches a real change, so the archived source is mutated and
re-transcribed for real. And VERIFY-01/VERIFY-07 were deliberately left open by Phase 173 with the
reason recorded in `REQUIREMENTS.md` ("Marking this complete now would be the third premature
completion mark this phase has had to catch"); closing them requires the same standard: a real run,
real evidence, and the observable checked rather than asserted.

Output: `174-PROOF.md` sections 5 and 6, `174-VALIDATION.md` signed off, `REQUIREMENTS.md` updated,
`ROADMAP.md` phase result recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/174-verify-classifier/174-CONTEXT.md
@.planning/phases/174-verify-classifier/174-VALIDATION.md
@.planning/phases/174-verify-classifier/174-PROOF.md
@.planning/phases/174-verify-classifier/174-06-SUMMARY.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC-3 — mutate the real archived source, re-transcribe for real, classify</name>
  <files>.planning/phases/174-verify-classifier/174-PROOF.md</files>
  <read_first>
    - `.planning/phases/174-verify-classifier/174-CONTEXT.md` decisions 2, 3, 15
    - `.planning/phases/174-verify-classifier/174-PROOF.md` sections 1-4 (the material, the run-ids, the
      baseline verdicts this task changes exactly one thing against)
    - `src/cli/slash-command/bs/verify/source-resolution.md` (how a hash mismatch against the archive is
      recorded as a signal and never silently overwritten — the mutation must go through this path, not
      around it)
    - `src/cli/slash-command/bs/verify/staging-dispatch.md` (the re-transcription sequence being re-run
      for the affected page range)
  </read_first>
  <action>
    Working on the `$SCRATCH` copy of `seven` (the anchor game), introduce ONE genuine rules change by
    editing the REAL archived source at `rulebook/source/<file>` — not a staged slice, not a live slice.
    Choose a change whose consequence is unambiguous and quotable on both sides, e.g. altering a numeric
    bound or reversing a precedence, and record the exact before/after text and the source file's
    sha256 before and after. Pick a change on a page that already has a paired verdict from plan 174-06,
    so the delta against a known-`cosmetic` baseline is the measurement.

    Then run the pipeline, not the comparator:
    1. `boardsmith verify-run-init --project <copy> --json` to allocate a NEW run against the mutated
       source (a fresh run-id; do not reuse plan 174-06's).
    2. Real Task-tool re-transcription dispatch of the affected page range, `BS-DISPATCH-V2` pointer
       block copied byte-identical, output into the new run's staging dir; record every returned unit
       and mark the range complete.
    3. `boardsmith verify-classify-pairs --run-id <newRunId> --json`, then real classification
       dispatches per pending pair, then `verify-classify-record` per returned label, then
       `verify-classify-status --json`.

    Record in `174-PROOF.md` under `## 5. SC-3 — a genuine rules change, caught by the pipeline`:
    the mutation diff, both source hashes, the new run-id, every raw dispatch prompt and raw return, all
    verdicts, and the explicit comparison against plan 174-06's baseline verdict for the same page span.

    Then state the two SC assertions separately and explicitly:
    - **SC-3:** the pair covering the mutated page classifies `sharper` or `contradictory` (name which,
      and quote the `quotedPass1`/`quotedPass2` evidence the subagent returned), and it is `stale`.
    - **SC-4 on real data:** the mutation moved the source bytes, so the affected pair's provenance
      reads `source-changed` where the baseline read otherwise — and show at least one pair that is
      `source-changed` yet `cosmetic` and therefore NOT stale, demonstrating provenance is reported and
      never a staleness verdict. If no such pair occurs naturally in this run, say so plainly and cite
      the unit test that pins the property instead of implying real-data coverage that does not exist.

    If the affected pair classifies `cosmetic`, SC-3 has FAILED — record it as a failure with the full
    evidence and stop for the developer. Do not retry with a larger mutation to manufacture a pass; a
    change the classifier misses at this size is the finding.
  </action>
  <verify>
    <automated>grep -q '## 5. SC-3' .planning/phases/174-verify-classifier/174-PROOF.md && grep -qE 'sharper|contradictory' .planning/phases/174-verify-classifier/174-PROOF.md && grep -q 'source-changed' .planning/phases/174-verify-classifier/174-PROOF.md</automated>
  </verify>
  <acceptance_criteria>
    - The proof records the archived source's sha256 before and after the mutation, and the literal
      before/after text of the changed rule.
    - The staged slice under comparison was produced by a real re-transcription dispatch of the mutated
      source in a NEW run-id — shown by the run's ledger and the raw dispatch prompt, both pasted.
      No hand-edited staged slice appears anywhere in this section.
    - The affected pair's recorded verdict is `sharper` or `contradictory` with both readings quoted
      verbatim in `evidence`, and `stale: true`.
    - The provenance dimension for the affected pair reads `source-changed`, stated separately from the
      rule-delta verdict.
    - Either a real `source-changed` + `cosmetic` + not-stale pair is shown, or the proof states plainly
      that none occurred and cites the unit test covering it.
    - `~/BoardSmithGames` originals byte-identical before and after (diff pasted).
  </acceptance_criteria>
  <done>SC-3 is proven through the real pipeline against a real source mutation, with SC-4's independence corroborated on real data or its absence stated honestly.</done>
</task>

<task type="auto">
  <name>Task 2: VERIFY-01's per-chunk verdict on real data, and the phase evidence closeout</name>
  <files>.planning/phases/174-verify-classifier/174-PROOF.md, .planning/phases/174-verify-classifier/174-VALIDATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 67-125 (the VERIFY block, including the verbatim reasons
      VERIFY-01 and VERIFY-07 were left open and the citation style used for closed requirements)
    - `.planning/phases/173-verify-pipeline-core/173-PROOF.md` § "What is still unproven (final,
      phase-wide)" and § "How to re-run every proof" (the closeout structure to mirror)
    - `.planning/phases/174-verify-classifier/174-VALIDATION.md` (the sign-off checklist and the
      per-task verification map whose Status column this task fills in)
    - `.planning/ROADMAP.md` lines 127-137 (the Phase 174 block, and Phase 173's `**Result:**`
      paragraph as the format to follow)
  </read_first>
  <action>
    **VERIFY-01's per-chunk verdict half.** Run `boardsmith verify-classify-status --project <copy>
    --run-id <runId> --json` on both copies and record the `chunkVerdicts[]` output verbatim in
    `174-PROOF.md` under `## 6. VERIFY-01 — a per-chunk verdict without rebuilding`. Assert
    independently, not from the command's own claim: (a) every chunk in the project appears exactly once
    in `chunkVerdicts`; (b) each chunk's `pairIds` are pairs whose live slices that chunk actually cites,
    spot-checked against the chunk's own citation lines; (c) no build ran during the pass — no source
    file, test file, CHUNK.md, or SKETCH.md in the copy changed, verified by whole-tree sha256 diff
    against the copy's own state before the pass; and (d) a chunk whose cited slices only landed in
    `unpaired-slice` groups reads `unclassified`, not clean.

    **Closeout.** Add to `174-PROOF.md`:
    - A phase-wide `## What is still unproven` list, written honestly and specifically — carry forward
      anything the earlier sections flagged, including Phase 173's still-open findings that this phase
      did not touch, and any SC whose real-data coverage was partial.
    - A `## How to re-run every proof` block naming every command and script, in order, with the
      fixture paths.

    Update `.planning/phases/174-verify-classifier/174-VALIDATION.md`: fill the Status column for every
    row of the per-task verification map with the real outcome, tick the Wave 0 and Sign-Off checkboxes
    that are genuinely satisfied, set `nyquist_compliant: true` only if every row has an automated
    command or a recorded manual proof, and set `status: complete`.

    Update `.planning/REQUIREMENTS.md`:
    - **VERIFY-03** — mark `[x]` with citations to `174-PROOF.md` sections 2 (SC-1/SC-2 with the
      measured cosmetic percentage), 4 (determinism), and 5 (SC-3), naming the actual numbers rather
      than "proven".
    - **VERIFY-01** — replace the "left OPEN" note with the per-chunk-verdict citation from section 6,
      keeping the existing install-and-run citations to `173-PROOF.md` intact.
    - **VERIFY-07** — replace the "left OPEN" note with the classification-half citation from section 3,
      including the per-artifact grep counts and the explicit `quotedPass1`/`quotedPass2` exception.
    - Update the requirement-to-phase table rows for VERIFY-01, VERIFY-03, and VERIFY-07.
    If ANY of the three did not actually earn closure, leave it open with the reason stated in the same
    voice Phase 173 used — a premature completion mark is the failure mode this milestone keeps catching.

    Update `.planning/ROADMAP.md`: mark the Phase 174 plan checkboxes, set its Plans count, add a
    `**Result:**` paragraph in Phase 173's style naming the measured SC-2 numbers, the SC-3 outcome, the
    determinism outcome, which requirements closed, and every finding reported but not fixed; and set the
    Progress table row for Phase 174 to Complete with the date.
  </action>
  <verify>
    <automated>grep -q '## 6. VERIFY-01' .planning/phases/174-verify-classifier/174-PROOF.md && grep -q 'How to re-run every proof' .planning/phases/174-verify-classifier/174-PROOF.md && grep -q 'nyquist_compliant: true' .planning/phases/174-verify-classifier/174-VALIDATION.md && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `chunkVerdicts[]` output is pasted verbatim for both copies, with the four independent assertions
      (a)-(d) each stated and evidenced.
    - The no-build claim is backed by a whole-tree sha256 diff of the copy, not by the absence of a build
      command in the transcript.
    - `174-PROOF.md` contains `## What is still unproven` and `## How to re-run every proof`.
    - `174-VALIDATION.md` has no `⬜ pending` rows left and its frontmatter reads `status: complete`.
    - `REQUIREMENTS.md`'s VERIFY-01/03/07 entries each either carry a `[x]` with section-and-number
      citations, or remain `[ ]` with an explicit stated reason.
    - `ROADMAP.md`'s Phase 174 block lists all seven plans as complete with a `**Result:**` paragraph
      naming the measured numbers, and the Progress table row reads Complete.
    - `npm test` green.
  </acceptance_criteria>
  <done>VERIFY-01's per-chunk verdict is proven on real data and the phase's evidence trail, validation sign-off, requirements, and roadmap are all closed honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this plan → `~/BoardSmithGames/*` | Read-only originals; the mutation happens only in a copy |
| proof claims → REQUIREMENTS.md closure marks | A closure mark is a durable claim downstream phases rely on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-24 | Tampering | the archived source mutation escaping into an original | mitigate | The mutation is applied only inside `$SCRATCH/seven`; whole-tree sha256 before/after diff on both originals pasted |
| T-174-25 | Repudiation | a premature requirement closure mark | mitigate | Each closure requires section-and-number citations; the plan explicitly permits leaving a requirement open with a stated reason |
| T-174-26 | Spoofing | a hand-edited staged slice passed off as a re-transcription | mitigate | The new run's ledger and the raw `BS-DISPATCH-V2` prompt are both pasted; acceptance criteria forbid any hand-edited staged slice in section 5 |
| T-174-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (`174-RESEARCH.md` Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `174-PROOF.md` sections 5 and 6 exist with the mutation evidence, the SC-3 verdict, the per-chunk
  verdict output, the unproven list, and the re-run block.
- `174-VALIDATION.md` signed off; `REQUIREMENTS.md` and `ROADMAP.md` updated.
- `npm test` green.
</verification>

<success_criteria>
The pipeline is proven to catch a genuine rules change, a per-chunk verdict exists without a rebuild,
and every requirement this phase touched is marked with real evidence or left open with a real reason.
</success_criteria>

<output>
Create `.planning/phases/174-verify-classifier/174-07-SUMMARY.md` when done
</output>
