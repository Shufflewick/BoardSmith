# Expected: `sharper`

Pass 1 is silent on ties. Pass 2 names a tie-break rule pass 1 never mentioned. A tied-score game
situation is undefined under pass 1 and determinate under pass 2 — compatible (pass 2 does not
contradict "highest score wins"; it resolves the one case pass 1 left open), so `sharper`, not
`contradictory`.
