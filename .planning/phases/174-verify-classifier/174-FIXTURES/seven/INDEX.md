# Fixture Index — seven

Real pass-1 (live) and pass-2 (staged) rulebook slices archived from a `cp -R` scratch copy of
`~/BoardSmithGames/seven` (pinned at `a03f38d4792af9dfc7c798be69686fc3230f54dd`), produced by
`174-01-PLAN.md`. See `../MANIFEST.md` for the reproduction command sequence and per-file hashes.

- `live/` — the game's pre-existing pass-1 rulebook slices (`rulebook/*.md`), byte-identical to
  the untouched original — 4 files, one of which (`00-visual-survey.md`) is not a rule slice.
  3 real rule slices: `01-definitions-and-components.md`, `01-overview-setup-and-play.md`,
  `02-solo-variant.md`.
- `staged/` — the pass-2 re-transcription this plan produced, run-id
  `2026-07-29T23-25-24Z`, one dispatch covering page range `1-2` — 6 files.
- `RUN.md` — the run's ledger, verbatim, all 6 units `range-complete`.

m:n asymmetry: 3 live rule slices vs. 6 staged files — matches `173-PROOF.md`'s own 6-vs-3
measurement of this exact game exactly.
