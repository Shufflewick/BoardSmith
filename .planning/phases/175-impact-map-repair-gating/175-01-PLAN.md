---
phase: 175-impact-map-repair-gating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/commands/verify-impact.ts
  - src/cli/commands/verify-impact.test.ts
  - src/cli/slash-command/bs/state-machine.md
  - src/cli/slash-command/bs/templates/CHUNK.template.md
  - src/cli/slash-command/bs/templates/SKETCH.template.md
  - src/cli/slash-command/bs/templates.test.ts
autonomous: true
requirements: [VERIFY-05]
must_haves:
  truths:
    - "A rules-stale chunk carries a machine-owned, fenced `## Rules Staleness` section in CHUNK.md whose Marker value is unmistakably distinct from `stale — re-derive before build`"
    - "A SKETCH.md detailed entry reflects that chunk's rules-staleness as a DERIVED pointer, never as an independent decision"
    - "The new marker is registered in state-machine.md's Cold-Resume Parse Contract / Consistency Check, so no `bs-` skill hard-fails on a project carrying one"
    - "The Status enum and all of its enumerating/pinning sites are byte-for-byte unchanged"
  artifacts:
    - path: "src/cli/commands/verify-impact.ts"
      provides: "Rules-staleness marker constants, renderer, strict parser, and the CHUNK-then-SKETCH writer"
      exports: ["RULES_STALENESS_HEADING", "RULES_STALENESS_BEGIN", "RULES_STALENESS_END", "RULES_STALENESS_VALUES", "RULES_STALENESS_LABELS", "RULES_STALENESS_EMPTY", "SKETCH_RULES_STALENESS_GRAMMAR", "renderRulesStaleness", "parseRulesStaleness", "writeRulesStalenessMarker"]
    - path: "src/cli/commands/verify-impact.test.ts"
      provides: "Unit pins for render/parse/write, write order, and the no-clear-path negative"
    - path: "src/cli/slash-command/bs/state-machine.md"
      provides: "A `## Rules Staleness Marker` section + a NEW Consistency Check item registering the marker"
      contains: "rules-stale — rulebook moved since this chunk was verified"
    - path: "src/cli/slash-command/bs/templates/CHUNK.template.md"
      provides: "The scaffolded `## Rules Staleness` fenced section + its PARSE CONTRACT entry"
    - path: "src/cli/slash-command/bs/templates/SKETCH.template.md"
      provides: "The derived `- Rules Staleness (derived from chunks/<slug>/CHUNK.md):` pointer line"
  key_links:
    - from: "src/cli/slash-command/bs/templates.test.ts"
      to: "src/cli/commands/verify-impact.ts"
      via: "import of the exported marker constants (never a re-typed string)"
      pattern: "from '\\.\\./\\.\\./commands/verify-impact\\.js'"
    - from: "src/cli/slash-command/bs/templates/CHUNK.template.md"
      to: "src/cli/commands/verify-impact.ts"
      via: "scaffolded fence sentinels byte-identical to the exported constants"
      pattern: "boardsmith:rules-staleness:begin"
---

<objective>
Create the rules-staleness marker as a NEW, ORTHOGONAL, machine-owned fenced field — and register it
in the same change, so no project carrying one can hard-fail a `bs-` skill's cold-resume parse.

Purpose: VERIFY-05's visible marker in both CHUNK.md and SKETCH.md. CONTEXT decisions 1, 2, 3, 5,
and 18. Decision 5 makes registration load-bearing: `state-machine.md`'s Consistency Check item 3
turns an unrecognized status into a PARSE FAILURE that stops the skill and asks — shipping the
marker without registering it breaks every `bs-` skill on any project that has one. That is why the
marker and its registration are ONE plan, not two.

Output: `verify-impact.ts` with the marker's constants/renderer/strict-parser/writer, the marker
registered in `state-machine.md`, scaffolded into both templates, and pinned in `templates.test.ts`
— including a NEGATIVE pin that the Status enum's own sites did not move.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/175-impact-map-repair-gating/175-CONTEXT.md
@.planning/phases/175-impact-map-repair-gating/175-RESEARCH.md
@.planning/phases/175-impact-map-repair-gating/175-PATTERNS.md

<interfaces>
Contracts the executor needs. Do NOT re-derive; import these.

From `src/cli/commands/chunk-provenance.ts` — the ONLY existing precedent for a second
machine-owned fenced field alongside `Status:` (this is the shape to copy):
```typescript
export const VERIFIED_AGAINST_HEADING = '## Verified Against';
export const VERIFIED_AGAINST_BEGIN = '<!-- boardsmith:verified-against:begin -->';
export const VERIFIED_AGAINST_END = '<!-- boardsmith:verified-against:end -->';
export const VERIFIED_AGAINST_LABELS = Object.freeze([
  'Scope:', 'Reason:', 'Rulebook edition:', 'Rulebook source hash:',
  'BoardSmith version:', 'Skills tree hash:', 'Cited slices:', 'Unresolved citations:',
] as const);
export const VERIFIED_AGAINST_EMPTY = '_Not yet recorded._';
export function renderVerifiedAgainst(record: VerifiedAgainstRecord): string;  // body BETWEEN fences only, pure
export function parseVerifiedAgainst(chunkText: string): ParsedVerifiedAgainst; // strict: partial never returns valid
```

From `src/cli/commands/build-manifest.ts` — the by-line heading lookup to reuse:
```typescript
export function findHeadingIndex(text: string, heading: string): number;
export function extractSection(text: string, heading: string): string | undefined;
```

From `src/cli/commands/verify-run.ts` — the ONE atomic write path in this repo:
```typescript
export async function atomicWriteFile(filePath: string, content: string): Promise<void>;
```

From `src/cli/commands/verify-classify.ts` — the rule-delta enum whose value the marker records:
```typescript
export const RULE_DELTA_KINDS = Object.freeze(['cosmetic','sharper','contradictory','unclassified'] as const);
export type RuleDelta = (typeof RULE_DELTA_KINDS)[number];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: verify-impact.ts — the marker's constants, renderer, and strict parser</name>
  <files>src/cli/commands/verify-impact.ts, src/cli/commands/verify-impact.test.ts</files>
  <read_first>
    - `src/cli/commands/chunk-provenance.ts` lines 218–332 (the `VERIFIED_AGAINST_*` constant
      block, `VerifiedAgainstRecord`, `renderVerifiedAgainst`, `renderVerifiedAgainstSection`) and
      lines 572–645 (`parseVerifiedAgainst`'s strict-parse discipline)
    - `src/cli/commands/chunk-provenance.ts` lines 388–396 — the `f73153a3` by-LINE heading anchor
      comment and its `/^## Verified Against[ \t]*$/m` regex
    - `src/cli/commands/build-manifest.ts` lines 65–96 (`findHeadingIndex`, `extractSection`)
    - `src/cli/commands/verify-classify.ts` lines 52–68 (`RULE_DELTA_KINDS`, `RuleDelta`)
    - `.planning/phases/175-impact-map-repair-gating/175-PATTERNS.md` § "Machine-owned fenced
      region" and § "Line-anchored section lookup"
    - `src/cli/commands/drift-check.test.ts` (any existing test in `src/cli/commands/`) for the
      colocated-test conventions
  </read_first>
  <behavior>
    - `RULES_STALENESS_VALUES` has exactly two members: `'clear'` and
      `'rules-stale — rulebook moved since this chunk was verified'`.
    - `renderRulesStaleness` emits every label in `RULES_STALENESS_LABELS` order and returns ONLY
      the body between the fences (no heading, no fence sentinels) — mirroring
      `renderVerifiedAgainst`.
    - `parseRulesStaleness(chunkText)` returns `{ state: 'unknown' }` when the heading is absent,
      when either fence is absent, when the body is exactly `RULES_STALENESS_EMPTY`, when the
      `Marker:` value is not a member of `RULES_STALENESS_VALUES`, or when a required label is
      missing — a PARTIALLY parsed block is never returned as valid.
    - `parseRulesStaleness` locates the heading with a line-anchored regex, so a prose mention of
      `## Rules Staleness` inside CHUNK.template.md's PARSE CONTRACT comment is NOT matched
      (regression guard for `f73153a3`).
    - The em-dash in the stale marker value is U+2014, not a hyphen — asserted by codepoint.
    - `RULES_STALE_MARKER !== 'stale — re-derive before build'` and neither string is a substring
      of the other (decision 1's "unmistakably distinct" requirement, as a test).
  </behavior>
  <action>
Create `src/cli/commands/verify-impact.ts` with a file-level doc comment stating that this module
is the Phase 175 impact-map / repair-gating surface, that rules-staleness is ORTHOGONAL to the
`Status:` enum (CONTEXT decision 1), and that it never re-derives staleness (Phase 174's
`deriveStale` is the only authority).

Export these constants, following `chunk-provenance.ts`'s `VERIFIED_AGAINST_*` block shape exactly
and with its own DISTINCT fence pair (never reuse `VERIFIED_AGAINST_BEGIN`/`END`):

- `RULES_STALENESS_HEADING = '## Rules Staleness'`
- `RULES_STALENESS_BEGIN = '<!-- boardsmith:rules-staleness:begin -->'`
- `RULES_STALENESS_END = '<!-- boardsmith:rules-staleness:end -->'`
- `RULES_STALENESS_EMPTY = '_Not rules-stale._'`
- `RULES_STALENESS_VALUES = Object.freeze(['clear', 'rules-stale — rulebook moved since this chunk was verified'] as const)` — frozen array + derived type `RulesStalenessValue`, never a hand-written union (the established enumerated-set pattern: `FINDING_KINDS`, `RULE_DELTA_KINDS`, `PROVENANCE_KINDS`)
- `RULES_STALENESS_CLEAR = RULES_STALENESS_VALUES[0]` and `RULES_STALE_MARKER = RULES_STALENESS_VALUES[1]`
- `RULES_STALENESS_LABELS = Object.freeze(['Marker:', 'Run:', 'Rule delta:', 'Attributed slices:', 'Prior reading:', 'Changed reading:', 'Adjudication:'] as const)`, destructured positionally into `LABEL_MARKER`, `LABEL_RUN`, `LABEL_RULE_DELTA`, `LABEL_ATTRIBUTED`, `LABEL_PRIOR`, `LABEL_CHANGED`, `LABEL_ADJUDICATION` — a simple append pattern so future extension never restructures consumers
- `SKETCH_RULES_STALENESS_GRAMMAR = 'Rules Staleness (derived from chunks/<slug>/CHUNK.md):'`

Export `interface RulesStalenessRecord` with fields: `marker: RulesStalenessValue`, `runId: string`,
`ruleDelta: RuleDelta`, `attributedSlices: string[]`, `priorReading?: string`,
`changedReading?: string`, `adjudication: string`.

Export `renderRulesStaleness(record: RulesStalenessRecord): string` — pure, body-between-fences
only. Render `Attributed slices:` as a `| slice |` markdown table when non-empty and as
`RULES_STALENESS_EMPTY` when empty, mirroring `renderVerifiedAgainst`'s `Cited slices:` handling.
Render `Prior reading:`/`Changed reading:` verbatim (these carry Phase 174's `quotedPass1`/
`quotedPass2`) and omit them when absent, mirroring how `renderVerifiedAgainst` omits `Reason:` on
`full` scope.

Export `renderRulesStalenessSection(record): string` — heading + a MACHINE-OWNED explanatory
comment (same wording pattern as `renderVerifiedAgainstSection`'s, naming
`boardsmith verify-impact-apply` as the sole writer) + the fenced body.

Export `parseRulesStaleness(chunkText: string): ParsedRulesStaleness` where
`ParsedRulesStaleness = { state: 'clear' | 'rules-stale' | 'unknown'; record?: RulesStalenessRecord }`.
Locate the heading with `/^## Rules Staleness[ \t]*$/m` (line-anchored — copy the `f73153a3`
comment's reasoning, never a bare `indexOf`), then locate the fences BELOW that index. Strict by
design: any missing fence, missing required label, `RULES_STALENESS_EMPTY` body, or unrecognized
`Marker:` value yields `state: 'unknown'` with no `record`.

Create `src/cli/commands/verify-impact.test.ts` covering every behavior above, with test names
containing the token `marker` so `-t "marker"` selects them per `175-VALIDATION.md`.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-impact.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/cli/commands/verify-impact.test.ts` passes.
    - `grep -c "boardsmith:rules-staleness:begin" src/cli/commands/verify-impact.ts` ≥ 1 and
      `grep -c "VERIFIED_AGAINST_BEGIN" src/cli/commands/verify-impact.ts` == 0 (no shared fence pair).
    - A test asserts `RULES_STALE_MARKER.includes('—')` (em-dash) and
      `!RULES_STALE_MARKER.includes('stale — re-derive before build')`.
    - A test feeds `parseRulesStaleness` a CHUNK.md text whose ONLY occurrence of the literal
      `## Rules Staleness` is inside an HTML comment and asserts `state === 'unknown'`.
    - `grep -v '^ *[*/]' src/cli/commands/verify-impact.ts | grep -c "indexOf(RULES_STALENESS_HEADING)"` == 0.
  </acceptance_criteria>
  <done>Marker constants, renderer, section renderer, and strict line-anchored parser exist and are unit-pinned; the fence pair is distinct from `## Verified Against`'s.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The CHUNK-first / SKETCH-second marker writer</name>
  <files>src/cli/commands/verify-impact.ts, src/cli/commands/verify-impact.test.ts</files>
  <read_first>
    - `src/cli/commands/verify-impact.ts` (Task 1's output)
    - `src/cli/commands/chunk-provenance.ts` lines 355–511 (`chunkCheckCommand`'s repair-then-fail
      path and its fence-refusal `throw` — the ONE thing that throws)
    - `src/cli/slash-command/bs/state-machine.md` "Authority" (lines 51–59) and "Write Order"
      (lines 61–73)
    - `src/cli/commands/verify-run.ts` lines 250–295 (`atomicWriteFile`)
    - `src/cli/slash-command/bs/templates/SKETCH.template.md` lines 61–95 (the detailed-entry
      block and its `- Status (derived from chunks/<slug>/CHUNK.md):` pointer grammar)
  </read_first>
  <behavior>
    - `writeRulesStalenessMarker` writes CHUNK.md BEFORE SKETCH.md, and a simulated failure of the
      SKETCH.md write leaves CHUNK.md already written (never the reverse ordering).
    - The `Marker:` line is the LAST line written inside the CHUNK.md fenced body (state-machine.md
      "Write Order" — the status-analogous field last).
    - SKETCH.md's derived pointer is repaired to match CHUNK.md; CHUNK.md is NEVER repaired to
      match SKETCH.md (state-machine.md "Authority", TMPL-03).
    - A CHUNK.md whose `## Rules Staleness` section is missing its fences THROWS a one-line
      actionable error naming the sentinels and the repair recipe, and leaves the file untouched.
    - A SKETCH.md detailed entry with no `- Rules Staleness (derived from ...)` line has one
      INSERTED immediately after its `- Status (derived from ...)` line.
    - The writer emits `RULES_STALE_MARKER` only. There is no parameter, flag, option, or code path
      by which it writes `RULES_STALENESS_CLEAR` (decision 4 — only Phase 176's repair close clears
      the marker, and there is no manual clear path).
  </behavior>
  <action>
Add `writeRulesStalenessMarker(projectDir: string, record: RulesStalenessRecord & { slug: string }):
Promise<{ chunkWritten: boolean; sketchWritten: boolean; sketchRepaired: boolean }>` to
`verify-impact.ts`.

Sequence, citing `state-machine.md` "Write Order" and "Authority" in the doc comment rather than
restating them: read `chunks/<slug>/CHUNK.md`; locate the `## Rules Staleness` section by line via
Task 1's parser helpers; if the section exists but a fence is missing, `throw` the fence-refusal
error modelled byte-for-byte on `chunkCheckCommand`'s (name both sentinels, tell the reader to
delete the whole section and re-run `boardsmith verify-impact-apply <slug>`) and touch nothing; if
the section is absent entirely, insert `renderRulesStalenessSection(record)` immediately AFTER the
`## Verified Against` section (its documented position at the file's end); persist with
`atomicWriteFile` from `verify-run.ts` — never a bare `fs.writeFile`.

Order the rendered body so `Marker:` is emitted LAST inside the fences. `RULES_STALENESS_LABELS`
must therefore be reordered from Task 1's draft if needed: `Run:`, `Rule delta:`,
`Attributed slices:`, `Prior reading:`, `Changed reading:`, `Adjudication:`, `Marker:` — and Task
1's positional destructuring updated to match. Record the write-order reason in a comment.

Only after the CHUNK.md write lands: read `SKETCH.md`, find the `### <slug>` detailed entry, and
write/repair its `- Rules Staleness (derived from chunks/<slug>/CHUNK.md): <value>` line using
`SKETCH_RULES_STALENESS_GRAMMAR`, inserting it directly after that entry's
`- Status (derived from chunks/<slug>/CHUNK.md):` line when absent. A sketch-level tail entry (no
`chunks/<slug>/` directory) is skipped and reported, never written — it has no CHUNK.md to derive
from.

Add tests to `verify-impact.test.ts` using temp-directory project fixtures (real CHUNK.md/SKETCH.md
pairs built from the shipped templates), with names containing `marker` and `write-order`.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-impact.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts, on a real temp project, that the written CHUNK.md fenced body's LAST
      non-empty line starts with `Marker:`.
    - A test makes SKETCH.md unwritable (or stubs the sketch write to reject) and asserts CHUNK.md
      on disk already carries the marker — proving CHUNK-first ordering.
    - A test seeds SKETCH.md's derived pointer with the WRONG value and asserts SKETCH.md was
      repaired to match CHUNK.md while CHUNK.md's own bytes are unchanged.
    - A test deletes `RULES_STALENESS_END` from a CHUNK.md and asserts the call rejects, the error
      message contains both sentinel strings, and the file's sha256 is unchanged.
    - `grep -v '^ *[*/]' src/cli/commands/verify-impact.ts | grep -c "fs.writeFile"` == 0.
    - `grep -v '^ *[*/]' src/cli/commands/verify-impact.ts | grep -ci "clear"` shows no occurrence
      inside `writeRulesStalenessMarker`'s body (verified by reading the function; the constant's
      declaration is allowed).
  </acceptance_criteria>
  <done>The marker writer honours CHUNK-first/SKETCH-second, `Marker:`-last, CHUNK.md authority, refuses a broken fence loudly without touching the file, and has no clear path.</done>
</task>

<task type="auto">
  <name>Task 3: Register the marker — state-machine.md, both templates, and the pins (including the Status-enum negative)</name>
  <files>src/cli/slash-command/bs/state-machine.md, src/cli/slash-command/bs/templates/CHUNK.template.md, src/cli/slash-command/bs/templates/SKETCH.template.md, src/cli/slash-command/bs/templates.test.ts</files>
  <read_first>
    - `src/cli/slash-command/bs/state-machine.md` lines 5–21 ("Status Enum (exact)" and the
      existing `stale — re-derive before build` em-dash note), lines 75–85 ("Cold-Resume Parse
      Contract"), lines 87–104 ("Consistency Check", especially item 3's exact three-way wording)
    - `src/cli/slash-command/bs/templates/CHUNK.template.md` lines 1–22 (the Status line, its
      "Valid values" comment, and the PARSE CONTRACT required-heading list) and lines 193–203 (the
      `## Verified Against` section — the shape to copy)
    - `src/cli/slash-command/bs/templates/SKETCH.template.md` lines 61–95 (detailed-entry block +
      the Status-line grammar comment)
    - `src/cli/slash-command/bs/templates.test.ts` lines 30–100 (`STATUS_ENUM_VALUES`,
      `STALE_MARKER`, `STATE_MACHINE_ENUM_LINE`, `TEMPLATE_ENUM_LINE`, `actualHeadings`), lines
      465–490 (the PROV-01 fence describe block whose shape the new pins copy), lines 491–552
      (`EXPECTED_HEADINGS` and the PARSE CONTRACT enumeration test)
    - `.planning/phases/175-impact-map-repair-gating/175-RESEARCH.md` § "Complete Status-Parsing
      Site Inventory" (the 5 sites that must NOT change) and § Open Question 1's recommendation
    - `.planning/phases/175-impact-map-repair-gating/175-CONTEXT.md` decisions 1, 5, 18
  </read_first>
  <action>
**`state-machine.md`** — add a new `## Rules Staleness Marker` section immediately AFTER
"Status Enum (exact)". State that rules-staleness is a SECOND, INDEPENDENT axis from the Status
enum (a `verified` chunk stays `verified` — a human really did playtest it), give the two values
(`clear` and `rules-stale — rulebook moved since this chunk was verified`) with the same em-dash
NOTE the existing stale marker carries, state that it lives in CHUNK.md's own machine-owned
`## Rules Staleness` fenced section and is reflected as a DERIVED pointer in SKETCH.md, state that
`boardsmith verify-impact-apply` is its only writer and that only Phase 176's repair close clears
it (there is no manual clear path), and explicitly distinguish it from
`stale — re-derive before build` ("never built" vs. "built against rules that have since moved").

Then add a NEW item **5** to "Consistency Check" — do NOT edit item 3, whose wording governs the
`Status:` line only and whose three-way recognized set must stay byte-identical (decision 1: the
marker is not a status value). Item 5's wording: the rules-staleness marker, where present, parses
against its own two-value set (see "Rules Staleness Marker"); a malformed marker, a missing fence,
or an unrecognized value there is ALSO a parse failure (stop and ask). Add a matching sentence to
"Cold-Resume Parse Contract" pointing at item 5, so the two sites cannot self-contradict (the
174-05 defect class).

**`CHUNK.template.md`** — append a `## Rules Staleness` section at the END of the file, after
`## Verified Against`, with the exact same shape: heading, a MACHINE-OWNED explanatory comment
(naming `boardsmith verify-impact-apply <slug>` as the sole writer and referencing the 2026-07-28
hand-fill incident the `## Verified Against` comment already documents), the two fence sentinels
byte-identical to `RULES_STALENESS_BEGIN`/`END`, and the empty-state body `_Not rules-stale._`. Add
`"## Rules Staleness"` to the PARSE CONTRACT required-heading list as the final entry. **Leave line
8–9's `Status:` line and its "Valid values" comment byte-for-byte unchanged.**

**`SKETCH.template.md`** — add `- Rules Staleness (derived from chunks/<!-- slug -->/CHUNK.md): <!-- clear | rules-stale — rulebook moved since this chunk was verified -->`
immediately after the detailed entry's `- Status (derived from ...)` line, and extend the
Status-line-grammar comment block above with a sibling paragraph naming this line's grammar and
noting tail entries do not carry it. **Leave line 85's Status enum list byte-for-byte unchanged.**

**`templates.test.ts`** — import `RULES_STALENESS_HEADING`, `RULES_STALENESS_BEGIN`,
`RULES_STALENESS_END`, `RULES_STALENESS_EMPTY`, `RULES_STALE_MARKER`, `RULES_STALENESS_CLEAR`, and
`SKETCH_RULES_STALENESS_GRAMMAR` from `../../commands/verify-impact.js` (the same
import-the-constant discipline the file already uses for `VERIFIED_AGAINST_*` — never a re-typed
string). Add a new `describe('VERIFY-05 — the rules-staleness marker is registered everywhere it must be')`
with assertions that: both templates and `state-machine.md` contain `RULES_STALE_MARKER`;
`CHUNK.template.md` contains the heading and both sentinels and the empty-state body;
`SKETCH.template.md` contains `SKETCH_RULES_STALENESS_GRAMMAR`; `state-machine.md` contains a
`## Rules Staleness Marker` heading and a Consistency Check item 5 mentioning the marker. Add
`'## Rules Staleness'` to `EXPECTED_HEADINGS['templates/CHUNK.template.md']` as the last entry.

Add a separate `describe('decision 1 orthogonality guard — the Status enum did not move')` holding
the NEGATIVE assertions: `STATUS_ENUM_VALUES` still has exactly the five members;
`STATE_MACHINE_ENUM_LINE` and `TEMPLATE_ENUM_LINE` each appear in their files unchanged;
`CHUNK.template.md` and `SKETCH.template.md` each contain `TEMPLATE_ENUM_LINE` exactly once; and
neither `TEMPLATE_ENUM_LINE` nor `STATE_MACHINE_ENUM_LINE` contains `RULES_STALE_MARKER` or the
substring `rules-stale`. Comment the describe block with decision 18's rule: if a future change
needs to edit these, orthogonality was violated upstream — that is the signal, not expected work.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/templates.test.ts src/cli/commands/verify-impact.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/templates.test.ts` passes, including the new
      `EXPECTED_HEADINGS` entry and the PARSE CONTRACT enumeration test.
    - `git diff -U0 src/cli/slash-command/bs/templates/CHUNK.template.md | grep -c '^-'` shows no
      removed line containing `Valid values (exact, case-sensitive)`.
    - `grep -c 'proposed | approved | built | verified | verified (user-waived) | stale — re-derive before build' src/cli/slash-command/bs/templates/CHUNK.template.md` == 1 and the same for `SKETCH.template.md`.
    - `grep -n 'rules-stale' src/cli/slash-command/bs/state-machine.md` returns ≥ 2 lines (the
      marker section and Consistency Check item 5).
    - `grep -c '^3\. All statuses parse against a recognized value' src/cli/slash-command/bs/state-machine.md` == 1 and that line's text is unchanged in `git diff`.
    - `npx vitest run src/cli/slash-command/bs/build-chunk.test.ts src/cli/slash-command/bs/status-tools.test.ts` still passes with no edits to those files.
  </acceptance_criteria>
  <done>The marker is registered in state-machine.md, scaffolded into both templates, pinned from the exported constants, and the Status enum's five sites are asserted unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → project state files (CHUNK.md / SKETCH.md) | The tool writes into files a human also edits; a bad write corrupts designer state |
| skill text → live agent session | Registered-vs-unregistered marker decides whether a `bs-` skill hard-fails on a real project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-01 | Tampering | `writeRulesStalenessMarker` CHUNK.md write | mitigate | Fence-refusal `throw` leaves the file untouched; all writes via `atomicWriteFile` (fsync+rename), never `fs.writeFile` |
| T-175-02 | Denial of Service | Every `bs-` skill's Consistency Check | mitigate | Marker registered in `state-machine.md` item 5 in this SAME change (decision 5) — an unregistered marker is a project-wide skill hard-fail |
| T-175-03 | Tampering | The closed 5-value Status enum | mitigate | Explicit negative pins in `templates.test.ts` assert `STATUS_ENUM_VALUES`/`TEMPLATE_ENUM_LINE`/`STATE_MACHINE_ENUM_LINE` unchanged and free of `rules-stale` |
| T-175-04 | Repudiation | Marker written to SKETCH.md only | mitigate | Writer is CHUNK-first, SKETCH-second, with a test proving CHUNK.md lands even when the SKETCH write fails |
| T-175-SC | Tampering | npm/pip/cargo installs | accept | No packages installed anywhere in this phase (`175-RESEARCH.md` § Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npx vitest run src/cli/commands/verify-impact.test.ts src/cli/slash-command/bs/templates.test.ts`
- `npm test` — full suite; baseline 3706/3706 at Phase 174's close, so any new failure is this
  plan's own regression
- `npx tsc --noEmit` (or the repo's typecheck script) clean
</verification>

<success_criteria>
- `## Rules Staleness` exists as a machine-owned fenced section in `CHUNK.template.md` with its own
  distinct sentinel pair and appears in the PARSE CONTRACT list.
- `SKETCH.template.md` carries a derived `Rules Staleness` pointer line.
- `state-machine.md` has a `## Rules Staleness Marker` section and a NEW Consistency Check item 5;
  item 3's text is byte-identical to before.
- `templates.test.ts` pins the marker from the exported constants AND asserts the Status enum's
  sites unchanged.
- Full suite green.
</success_criteria>

<output>
Create `.planning/phases/175-impact-map-repair-gating/175-01-SUMMARY.md` when done
</output>
