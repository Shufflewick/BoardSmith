---
phase: 175-impact-map-repair-gating
plan: 05
type: execute
wave: 4
depends_on: [175-04]
files_modified:
  - src/cli/slash-command/bs/verify-game.md
  - src/cli/slash-command/bs/verify/adjudication-gate.md
  - src/cli/slash-command/bs/verify.test.ts
  - src/cli/commands/install-claude-command.ts
autonomous: true
requirements: [VERIFY-04, VERIFY-05, VERIFY-06]
must_haves:
  truths:
    - "`verify-game.md` no longer claims Step 3 flips no staleness marker and opens no repair loop — that claim is now false and is gone, not merely contradicted"
    - "A new step between Classification and Close presents every contradiction at once, requires an explicit human answer before any durable write, and only then writes markers"
    - "The gate's prose offers no bypass — no flag, no 'if unattended, proceed' carve-out"
    - "The new step formats CLI `--json` output and never computes a count itself"
  artifacts:
    - path: "src/cli/slash-command/bs/verify/adjudication-gate.md"
      provides: "The hard stop-and-ask prose + the ordered write sequence, modelled on build/ask.md's Gate-Before-Write"
      min_lines: 60
    - path: "src/cli/slash-command/bs/verify-game.md"
      provides: "Six steps: 0 State Detection, 1 Source Resolution, 2 Staging, 3 Classification, 4 Adjudication & Impact, 5 Close"
      contains: "verify/adjudication-gate.md"
    - path: "src/cli/slash-command/bs/verify.test.ts"
      provides: "Drift pins asserting the old boundary text ABSENT and the new step present"
  key_links:
    - from: "src/cli/slash-command/bs/verify-game.md"
      to: "src/cli/slash-command/bs/verify/adjudication-gate.md"
      via: "Step 4's dispatch line, same shape as Steps 1/2/3"
      pattern: "bs-shared/verify/adjudication-gate\\.md"
    - from: "src/cli/slash-command/bs/verify/adjudication-gate.md"
      to: "boardsmith verify-impact-* commands"
      via: "named CLI invocations, formatted never computed"
      pattern: "verify-impact-(gate|adjudicate|apply|status)"
---

<objective>
Rewrite `verify-game.md`'s now-false boundary statement IN PLACE, and add the step that actually
fires the adjudication gate and the staleness write.

Purpose: VERIFY-04's "always stops the pass", VERIFY-05's cross-file marker write, and VERIFY-06's
scoped re-playtest, expressed in the skill text that a live session actually follows. Research's
Pitfall 4 names the exact hazard: `verify-game.md` Step 3 currently asserts it "flips no staleness
marker anywhere and opens no repair loop (that is Phase 175's job)". This phase makes that FALSE.
Phase 170 proved on fourteen live runs that self-contradicting skill text gets half-followed, and
174-05 already had to make this identical fix to this identical file for Phase 173's boundary claims.
The old text must be DELETED, with drift pins asserting its ABSENCE — appending a new step around it
is the failure mode.

Note the existing pins that this change breaks and must be updated deliberately, not worked around:
`verify.test.ts` pins `stepHeadings.length === 5` (becomes 6) and each heading against
`/VERIFY-0[12378]/` (must widen to cover 4/5/6).

Output: a new `verify/adjudication-gate.md` reference file, `verify-game.md` at six steps with the
false sentence gone, and rewritten drift pins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/175-impact-map-repair-gating/175-CONTEXT.md
@.planning/phases/175-impact-map-repair-gating/175-RESEARCH.md
@.planning/phases/175-impact-map-repair-gating/175-PATTERNS.md

<interfaces>
The exact now-false text to DELETE (`verify-game.md` Step 3, final sentence, line 93–94):
```
This step records verdicts only: it flips no staleness
marker anywhere and opens no repair loop (that is Phase 175's job).
```

The hard-gate pattern to model on (`build/ask.md` "Gate-Before-Write", lines 153–190):
```
Do **not** write anything durable — not `Status: approved`, not a
RULINGS.md entry, not an ASSETS.md row — until the user has given explicit approval. Presenting
is not approving; only an explicit yes authorizes the write.

Only after that explicit yes:
1. Write any RULINGS.md `### Ruling N` entries from house-rule/adaptation choices made above.
...
4. Write `Status: approved` to CHUNK.md **last**, after every other write for this gate has
   landed (cite `state-machine.md` "Write Order" ...)
5. Then update this chunk's derived-status pointer in SKETCH.md to match ...
```

The "formatted, never computed" convention to copy (`verify-game.md` Step 4 Close, lines 104–106):
```
- Report the run's classification verdicts to the designer by formatting
  `verify-classify-status --json`'s output — **formatted, never computed** by this skill; every
  number in the report comes from the command's JSON, not from this skill's own arithmetic.
```

The pins in `verify.test.ts` that must change (lines 76–84 and 302–307):
```typescript
it('has exactly five numbered steps, each tagged with a VERIFY requirement ID', () => {
  const stepHeadings = skill.match(/^## Step \d+:.*$/gm) ?? [];
  expect(stepHeadings.length).toBe(5);
  for (const heading of stepHeadings) expect(heading).toMatch(/VERIFY-0[12378]/);
});

it('deletes the Phase 173 no-classification/no-comparison boundary statements', () => {
  expect(skill).not.toContain('The pass ends here');
  expect(skill).not.toContain('never compares the staged output');
  expect(skill).not.toMatch(/no classification/);
});
```

Installer probe list to extend (`install-claude-command.ts` lines 67–75):
```typescript
const SHARED_LEAF_PROBES = [
  join(SHARED_ROOT, 'state-machine.md'),
  ...
  join(SHARED_ROOT, 'verify', 'source-resolution.md'),
  join(SHARED_ROOT, 'verify', 'classification-subagent.md'),
];
```
(`SHARED_DIRS` already copies `verify/` recursively, so the new file ships without further change —
the probe is the install-verification parity addition.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: `verify/adjudication-gate.md` — the hard stop-and-ask and the ordered write sequence</name>
  <files>src/cli/slash-command/bs/verify/adjudication-gate.md</files>
  <read_first>
    - `src/cli/slash-command/bs/build/ask.md` lines 140–195 (the full "Gate-Before-Write" section)
    - `src/cli/slash-command/bs/verify/classification-dispatch.md` (the sibling reference file whose
      structure and dispatch-prose shape this one mirrors)
    - `src/cli/slash-command/bs/state-machine.md` "Write Order" (61–73), "Authority" (51–59),
      "Redteam Escalation" (164–168), and the `## Rules Staleness Marker` section plan 175-01 added
    - `.planning/phases/175-impact-map-repair-gating/175-CONTEXT.md` decisions 6, 7, 8, 9, 14, 15, 16
    - `.planning/phases/175-impact-map-repair-gating/175-PATTERNS.md` § "The Hard Gate pattern" and
      § "Cite, never restate `state-machine.md`"
  </read_first>
  <action>
Create `src/cli/slash-command/bs/verify/adjudication-gate.md`.

Structure it as the sibling `verify/*.md` reference files are structured — a title, a one-paragraph
purpose, then numbered sub-steps — and cite `state-machine.md` sections by name rather than restating
their rules, as every `bs-` skill does.

Content, in order:

1. **When this file runs.** After Step 3's classification of ALL pairs completes — never mid-pass
   (decision 6). Cite `verify-classify-status`'s "every pair classified" condition. State plainly why
   the gate is batched: the designer sees every contradiction at once with full context instead of
   one interruption per finding.

2. **Detect.** Run `boardsmith verify-impact-gate --json` and FORMAT its output — formatted, never
   computed; every number in the presentation comes from that JSON. If `summary.pending` is `0`, skip
   directly to sub-step 5.

3. **Present.** For each pending contradiction, present the pair id, the provenance, both readings
   verbatim under `Reading as built (pass 1):` / `Reading in the fresh transcription (pass 2):`, and
   every affected chunk slug — uncapped (decision 15: report volume is the risk, so group and
   summarise, but never drop). State explicitly that this is ONE question per contradictory FINDING,
   never one per affected chunk, and name the measured reason: 174 found a single finding touching 6+
   chunks, and per-chunk prompting asks the same question six times and trains the designer to click
   through it (decision 14).

4. **Wait.** Copy `build/ask.md`'s exact discipline in this file's own words: do not write anything
   durable — not a `RULINGS.md` entry, not a rules-staleness marker, not an impact record — until the
   designer has answered. Presenting is not answering; only an explicit answer authorizes the write.
   State that there is no flag, option, or unattended-mode carve-out that skips this: an unattended
   run stops, records what it has, and resumes later (decision 9). Then the two terminal answers:
   - **Resolved** — the designer names which reading governs and why. Record with
     `boardsmith verify-impact-adjudicate --outcome resolved --decision ... --citation ... --rationale ...`,
     which appends a `### Ruling N` entry to `RULINGS.md` in the corpus's existing three-field shape.
     Note that the corpus's own informal convention governs and that no structured supersession
     syntax is invented (decision 7).
   - **Deferred / aborted** — record with `--outcome UNADJUDICATED`. The affected chunks are STILL
     marked rules-stale; the pass stays resumable; the lock releases cleanly. Cite
     `state-machine.md` "Redteam Escalation" as the precedent for a named, honest terminal state, and
     state the rule directly: never silently clean (decision 8).

5. **Write.** Run `boardsmith verify-impact-apply`. State what it does and in which order, citing
   `state-machine.md` "Write Order" and "Authority" rather than restating them: the rules-staleness
   marker into each affected chunk's CHUNK.md first and SKETCH.md second, never SKETCH.md alone, with
   the marker value written last; the impact map appended to the run's own ledger. State that the
   command REFUSES to write while any contradiction is still pending, so the gate has structural
   teeth and not only this prose.

6. **Report.** Run `boardsmith verify-impact-status --json` and format it: the
   `"N of M chunks rules-stale"` fraction, every stale slug (uncapped), and each chunk's repair-gate
   disposition — whether repair will re-open its human playtest gate (`reopen-playtest`) or close it
   without re-playtesting (`close-without-replaytest`), or whether its code movement is `unknown-drift`
   and therefore undecided. State that the disposition is computed by the command and only formatted
   here, and that performing the repair itself is Phase 176's job, not this step's.

Do not restate the Status enum, the write order, the authority rule, or the consistency check inline
— link to `${CLAUDE_SKILL_DIR}/../bs-shared/state-machine.md` sections by name.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/verify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The file exists and is at least 60 lines.
    - It contains all four command names: `verify-impact-gate`, `verify-impact-adjudicate`,
      `verify-impact-apply`, `verify-impact-status`.
    - It contains the strings `Presenting is not` and `formatted, never computed`.
    - It contains `UNADJUDICATED` and the phrase `never silently clean`.
    - `grep -viE "flag|option|unattended" src/cli/slash-command/bs/verify/adjudication-gate.md` is
      not the whole file — i.e. the no-bypass paragraph explicitly names flags/options/unattended
      runs and forbids a skip. Assert positively:
      `grep -c "no flag, option, or unattended" src/cli/slash-command/bs/verify/adjudication-gate.md` ≥ 1.
    - It cites `${CLAUDE_SKILL_DIR}/../bs-shared/state-machine.md` at least twice and does NOT
      contain the literal string `Write order is always CHUNK.md first` (restating rather than citing).
  </acceptance_criteria>
  <done>The gate's prose exists as a sibling `verify/*.md` reference file, models `build/ask.md`'s no-bypass discipline, names both terminal answers, and cites `state-machine.md` rather than restating it.</done>
</task>

<task type="auto">
  <name>Task 2: `verify-game.md` — delete the false boundary statement, insert Step 4, renumber Close</name>
  <files>src/cli/slash-command/bs/verify-game.md, src/cli/commands/install-claude-command.ts</files>
  <read_first>
    - `src/cli/slash-command/bs/verify-game.md` (full file — 145 lines; note lines 15–20's
      "comparison happens in Step 3" paragraph, lines 85–94's Step 3, lines 96–111's Step 4 Close,
      and lines 113–132's Reference Files footer)
    - `src/cli/slash-command/bs/verify/adjudication-gate.md` (Task 1's output)
    - `.planning/phases/175-impact-map-repair-gating/175-RESEARCH.md` § Pitfall 4
    - `.planning/phases/174-verify-classifier/174-05-SUMMARY.md` — the in-place-rewrite technique
      174-05 used on this same file for Phase 173's boundary statements
    - `src/cli/commands/install-claude-command.ts` lines 56–80 (`SHARED_DIRS`,
      `SHARED_LEAF_PROBES`)
  </read_first>
  <action>
**Delete** the final sentence of Step 3 in its entirety: `This step records verdicts only: it flips
no staleness marker anywhere and opens no repair loop (that is Phase 175's job).` Replace it with a
sentence that is TRUE after this change: Step 3 records verdicts only; acting on them — the
adjudication gate and the staleness write — is Step 4's job, below. Grep the whole file afterwards for
`Phase 175` and `flips no staleness marker` and remove every remaining occurrence.

**Also audit** the header paragraph at lines 15–20 (`comparison happens in Step 3, below, and even
there the verdict is only ever recorded`) — the "only ever recorded" claim is now scoped to Step 3
alone and must be reworded so it does not read as a whole-skill promise. Keep the genuinely still-true
promise intact and unchanged in substance: no staged slice ever takes a live one's place, and there is
no flag or path anywhere in this skill that writes staged output into a live location.

**Insert** a new `## Step 4: Adjudication Gate and Impact Map (VERIFY-04, VERIFY-05, VERIFY-06)`
between Step 3 and the existing Close step, following Steps 1/2/3's exact shape: one dispatch line to
`${CLAUDE_SKILL_DIR}/../bs-shared/verify/adjudication-gate.md`, then an "In short:" paragraph
summarising the sequence — classification of all pairs completes first, then every `contradictory`
verdict is presented at once and the pass STOPS until the designer answers, then a resolution appends
a `RULINGS.md` entry (or is recorded `UNADJUDICATED`), then the rules-staleness marker is written into
each affected chunk's CHUNK.md and SKETCH.md and the impact map is appended to the run's ledger, and
finally the stale fraction and each chunk's repair-gate disposition are reported. State that
performing the repair is Phase 176's job and that this step decides only which chunks need it. Cite
`state-machine.md` sections by name; restate nothing.

**Renumber** the existing `## Step 4: Close (VERIFY-02)` to `## Step 5: Close (VERIFY-02)` and update
every in-file cross-reference to it (the Step 0 lock paragraph says "A clean close (Step 4) releases
the line to exactly `none`" — that becomes Step 5).

**Extend** the Reference Files footer with a bullet for
`${CLAUDE_SKILL_DIR}/../bs-shared/verify/adjudication-gate.md` describing it as the hard adjudication
gate plus the staleness-write and impact-map sequence.

**Installer parity:** add `join(SHARED_ROOT, 'verify', 'adjudication-gate.md')` to
`SHARED_LEAF_PROBES` in `src/cli/commands/install-claude-command.ts`. `SHARED_DIRS` already copies
`verify/` recursively, so no copy logic changes.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/verify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "flips no staleness marker" src/cli/slash-command/bs/verify-game.md` == 0.
    - `grep -c "Phase 175" src/cli/slash-command/bs/verify-game.md` == 0.
    - `grep -c "^## Step " src/cli/slash-command/bs/verify-game.md` == 6.
    - `grep -c "^## Step 5: Close" src/cli/slash-command/bs/verify-game.md` == 1 and
      `grep -c "^## Step 4: Close" src/cli/slash-command/bs/verify-game.md` == 0.
    - `grep -c "(Step 4) releases" src/cli/slash-command/bs/verify-game.md` == 0.
    - `grep -c "adjudication-gate.md" src/cli/slash-command/bs/verify-game.md` ≥ 2 (Step 4's dispatch
      line and the Reference Files bullet).
    - `grep -c "adjudication-gate.md" src/cli/commands/install-claude-command.ts` == 1.
  </acceptance_criteria>
  <done>`verify-game.md` has six steps, the false boundary claim is gone (not merely contradicted), Close is Step 5 with every cross-reference updated, and the new reference file is install-probed.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the drift pins in `verify.test.ts`</name>
  <files>src/cli/slash-command/bs/verify.test.ts</files>
  <read_first>
    - `src/cli/slash-command/bs/verify.test.ts` lines 25–48 (`read`, `flat`, `stripComments`,
      `ALL_VERIFY_FILES`), lines 76–90 (the step-count and requirement-ID pin), lines 250–310 (the
      copy-drift/forbidden-vocabulary loop and the Phase-173 boundary-deletion pin — the exact
      technique to copy)
    - `src/cli/slash-command/bs/verify-game.md` and `verify/adjudication-gate.md` (this plan's output)
    - `.planning/phases/175-impact-map-repair-gating/175-VALIDATION.md` the cross-cutting row:
      "No self-contradicting boundary statement left in `verify-game.md` ... old text asserted ABSENT"
  </read_first>
  <action>
Update `ALL_VERIFY_FILES` to include `'verify/adjudication-gate.md'`, so the existing
copy-drift/forbidden-vocabulary loop covers it too.

Update the step-shape pin: `expect(stepHeadings.length).toBe(6)` and widen the per-heading regex to
`/VERIFY-0[1-8]/` so Step 4's `VERIFY-04, VERIFY-05, VERIFY-06` tag matches. Rename the test to say
"six numbered steps".

Add a new describe block `VERIFY-04/05/06 — Step 4 exists and the Phase 175 boundary statement is
gone`, mirroring the existing `deletes the Phase 173 ... boundary statements` test's technique:
- Positive: `verify-game.md` matches `/^## Step 4: Adjudication Gate and Impact Map/m` and contains
  the `bs-shared/verify/adjudication-gate.md` dispatch path.
- Positive: `verify-game.md` matches `/^## Step 5: Close/m`.
- **Negative (the load-bearing pins):** `verify-game.md` does NOT contain `flips no staleness marker`,
  does NOT contain `Phase 175`, does NOT contain `opens no repair loop`, and does NOT contain
  `records verdicts only: it flips`. Comment the block naming 174-05's identical fix and Phase 170's
  finding that self-contradicting skill text gets half-followed live — these negatives are what stop
  the same defect recurring.
- `verify-game.md` names all four `verify-impact-*` commands OR delegates them to
  `adjudication-gate.md` — assert that between the two files, all four names appear (the router stays
  lean; the reference file carries the invocations).
- `adjudication-gate.md` contains `Presenting is not`, `UNADJUDICATED`, `formatted, never computed`,
  and the no-bypass sentence; and does NOT contain any of `--force`, `--yes`, `--skip`, `bypass`
  (scan with `stripComments` first so a comment mentioning a forbidden word cannot self-invalidate
  the gate).
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/verify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/verify.test.ts` passes with the new pins.
    - The suite FAILS if `flips no staleness marker` is re-introduced into `verify-game.md` — verify
      by temporarily re-adding the sentence, running the suite, observing the failure, then reverting.
      Record the observed failure message in the plan summary.
    - `grep -c "adjudication-gate.md" src/cli/slash-command/bs/verify.test.ts` ≥ 2.
    - `grep -c "toBe(6)" src/cli/slash-command/bs/verify.test.ts` ≥ 1 and
      `grep -c "toBe(5)" src/cli/slash-command/bs/verify.test.ts` == 0 in the step-count test.
  </acceptance_criteria>
  <done>The drift pins assert the new six-step shape, the new step's presence, and — proven by a temporary re-introduction — the ABSENCE of every now-false boundary phrase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill text → live agent session | The only mechanism enforcing VERIFY-04's "always stops"; self-contradicting text gets half-followed (Phase 170, fourteen live runs) |
| installer → installed skills tree | A reference file that does not ship makes the dispatch line a dangling pointer at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-19 | Tampering | `verify-game.md` Step 3's stale boundary claim | mitigate | Delete in place; negative drift pins (`not.toContain('flips no staleness marker')`, `not.toContain('Phase 175')`) proven to fail on re-introduction |
| T-175-20 | Elevation of Privilege | The gate's prose | mitigate | No-bypass paragraph asserted present; `--force`/`--yes`/`--skip`/`bypass` asserted absent after `stripComments` |
| T-175-21 | Denial of Service | Dangling `adjudication-gate.md` dispatch path | mitigate | Added to `SHARED_LEAF_PROBES`; `SHARED_DIRS` already copies `verify/` recursively |
| T-175-22 | Repudiation | Step renumbering | mitigate | Cross-reference grep (`(Step 4) releases` == 0) plus the step-count pin at 6 |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/verify.test.ts`
- `npx vitest run src/cli/slash-command/bs/` — all five bs skill-text suites
- `grep -n "Phase 175\|flips no staleness marker" src/cli/slash-command/bs/verify-game.md` returns
  nothing
- `npm test` full suite green
</verification>

<success_criteria>
- `verify-game.md` has six steps, Close is Step 5, and no now-false boundary claim remains anywhere
  in the file.
- `verify/adjudication-gate.md` exists, carries the hard-gate discipline and both terminal answers,
  and cites rather than restates `state-machine.md`.
- The negative drift pins demonstrably fail when the deleted sentence is re-introduced.
- The new reference file is install-probed.
</success_criteria>

<output>
Create `.planning/phases/175-impact-map-repair-gating/175-05-SUMMARY.md` when done
</output>
