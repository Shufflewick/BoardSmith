---
phase: 175-impact-map-repair-gating
plan: 06
type: execute
wave: 4
depends_on: [175-04]
files_modified:
  - src/cli/slash-command/bs/check-status.md
  - src/cli/slash-command/bs/status-tools.test.ts
autonomous: true
requirements: [VERIFY-05, VERIFY-06]
must_haves:
  truths:
    - "A designer running `/bs-check-status` sees the rules-stale count and fraction, uncapped, on the surface they actually read"
    - "The new item formats a read-only CLI command's `--json` and computes nothing, preserving the skill's no-writes posture"
    - "The report's own count sentences say nine items, not eight — the file does not contradict itself about how many items it has"
  artifacts:
    - path: "src/cli/slash-command/bs/check-status.md"
      provides: "Item 9 — rules-staleness reporting from `verify-impact-status --json`"
      contains: "verify-impact-status --json"
    - path: "src/cli/slash-command/bs/status-tools.test.ts"
      provides: "Pins for item 9, the nine-item count sentences, and the preserved read-only posture"
  key_links:
    - from: "src/cli/slash-command/bs/check-status.md"
      to: "boardsmith verify-impact-status --json"
      via: "item 9's format-never-compute invocation, mirroring item 8's chunk-provenance-status call"
      pattern: "verify-impact-status --json"
---

<objective>
Close the item Phase 174 deferred with the words "revisit there, not here" — surface rules-staleness
on `/bs-check-status`, the surface a designer actually reads.

Purpose: CONTEXT decision 19, which makes this explicitly in scope for this phase precisely because
deferring it a second time would turn 174's carry-forward note into a permanent excuse. It is also
decision 15's natural reporting home: the stale fraction, prominent and never capped.

`check-status.md` is read-only by documented posture and item 8 already establishes the exact pattern:
run a read-only command's `--json`, format its output, compute nothing, and consume the command's own
severity/summary fields rather than re-deriving them.

Note the self-consistency pin this change must move deliberately: `status-tools.test.ts` currently
asserts the file says `eight items` at least twice and never `seven items`. Adding item 9 makes those
sentences wrong; both the prose and the pin change together.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/175-impact-map-repair-gating/175-CONTEXT.md
@.planning/phases/175-impact-map-repair-gating/175-RESEARCH.md
@.planning/phases/175-impact-map-repair-gating/175-PATTERNS.md

<interfaces>
The `--json` shape item 9 formats (from `verify-impact.ts`, plan 175-04):
```typescript
export interface VerifyImpactStatusResult {
  runId: string;
  entries: Array<{ slug: string; ruleDelta: string; stale: boolean; status: string;
    driftState: 'clean'|'drifted'|'unknown'; changedFiles: string[]; missingFiles: string[];
    attributions: Array<{ pairId: string; liveSlice: string; rung: string; attributed: boolean; reason: string }>;
    gate: { disposition: 'reopen-playtest'|'close-without-replaytest'|'unknown-drift'|'not-applicable';
            nextStatus?: string; clearMarker: boolean; reverifyStamp: boolean; reason: string };
    markerState: 'clear'|'rules-stale'|'unknown' }>;
  staleFraction: { stale: number; total: number };
  staleSlugs: string[];
  dispositionCounts: Record<string, number>;
  contradictionsPending: string[];
  warnings: string[];
}
```

The item-8 pattern to copy (`check-status.md` lines 108–127):
```
**8. Verification provenance and drift.** Run `boardsmith chunk-provenance-status --json` and
FORMAT its output — do not compute any of it here. Report: ...

Consume the command's own `projectProvenanceState` field rather than re-deriving severity from
the raw `verifiedWithoutProvenance` count: ...  This skill formats that distinction; it
does not recompute it.

This command is read-only — item 8 does not violate this skill's no-writes-of-any-kind posture
(see `## Read-Only Posture (explicit)` below).
```

The count sentences that must change (`check-status.md`):
```
- (Note, not a live branch:) the no-`SKETCH.md` case is terminal at Step 0 — it stops and returns
  "no project has been ingested yet" before this eight-item synthesis is ever reached, ...

Present all eight items together as one report, in the order above, followed by the exact next
command on its own line.
```

The pin that must change (`status-tools.test.ts` lines 212–218):
```typescript
it('the count sentences say "eight items", never "seven items"', () => {
  expect(checkStatus).not.toMatch(/seven items/i);
  const matches = checkStatus.match(/eight items/gi) ?? [];
  expect(matches.length).toBeGreaterThanOrEqual(2);
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: `check-status.md` item 9 — rules-staleness reporting, format-never-compute</name>
  <files>src/cli/slash-command/bs/check-status.md</files>
  <read_first>
    - `src/cli/slash-command/bs/check-status.md` (full file — 170 lines; items 1–8, the count
      sentences, the `## Read-Only Posture (explicit)` section, and the Reference Files footer)
    - `src/cli/slash-command/bs/state-machine.md`'s `## Rules Staleness Marker` section (plan 175-01)
    - `.planning/phases/175-impact-map-repair-gating/175-CONTEXT.md` decisions 15, 19
    - `.planning/phases/175-impact-map-repair-gating/175-PATTERNS.md` § `check-status.md`
    - `.planning/phases/174-verify-classifier/174-CONTEXT.md` deferred section (the "revisit there,
      not here" note this item closes)
  </read_first>
  <action>
Add `**9. Rules staleness and the repair gate.**` immediately after item 8, following item 8's
structure sentence-for-sentence in shape:

- Run `boardsmith verify-impact-status --json` and FORMAT its output — do not compute any of it here.
- Report the fraction as `"N of M chunks rules-stale"`, taken directly from the command's
  `staleFraction` field, and list EVERY slug in `staleSlugs` — never capped, never truncated, never
  "and N more". State the reason in the prose: on short, heavily cross-referenced rulebooks the stale
  set is broader than ideal (Phase 174 measured 6 of 16 on `seven` and 6 of 11 on `one-two-punch`),
  and truncating that list would hide exactly the signal the designer needs.
- Group by the command's own `gate.disposition`, consuming `dispositionCounts` rather than
  re-deriving severity — the same discipline item 8 applies to `projectProvenanceState`. Explain each
  group in one clause: `reopen-playtest` (repair will re-open the human playtest gate because the
  chunk's code moved), `close-without-replaytest` (the chunk passes unchanged and closes with no
  re-playtesting), `unknown-drift` (code movement could not be determined — reported as undecided,
  never as clean), `not-applicable` (the chunk never had a playtest gate to re-open).
- Report `contradictionsPending` under its own heading when non-empty, naming
  `/bs-verify-game`'s adjudication gate as where they are answered — `check-status.md` reports them,
  it never answers them.
- State that if no verify run exists yet, the command has nothing to report and this item says so
  plainly rather than inventing a zero (never a fabricated clean).
- Close with the same read-only carve-out sentence item 8 carries: this command is read-only, so
  item 9 does not violate the no-writes-of-any-kind posture.

Distinguish this explicitly from item 8 in one sentence: item 8 reports what a chunk was verified
AGAINST; item 9 reports whether the rulebook underneath it has since MOVED. Also distinguish
`rules-stale` from `stale — re-derive before build` in one clause, citing
`state-machine.md`'s `## Rules Staleness Marker` section rather than restating it.

Then update every count sentence in the file from `eight items` to `nine items` — both the
no-`SKETCH.md` note and the `Present all eight items together` line — and add
`boardsmith verify-impact-status --json` to the Reference Files preamble sentence that already names
item 8's command as a CLI command rather than a file.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/status-tools.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\*\*9\. " src/cli/slash-command/bs/check-status.md` == 1.
    - `grep -c "verify-impact-status --json" src/cli/slash-command/bs/check-status.md` ≥ 2 (the item
      body and the Reference Files preamble).
    - `grep -ci "eight items" src/cli/slash-command/bs/check-status.md` == 0 and
      `grep -ci "nine items" src/cli/slash-command/bs/check-status.md` ≥ 2.
    - The file contains all four disposition strings: `reopen-playtest`,
      `close-without-replaytest`, `unknown-drift`, `not-applicable`.
    - The file contains `staleFraction`, `staleSlugs`, `dispositionCounts`, and
      `contradictionsPending`.
    - `grep -c "never capped" src/cli/slash-command/bs/check-status.md` ≥ 1.
    - `grep -ciE "write .*SKETCH|edit .*CHUNK|mutate" src/cli/slash-command/bs/check-status.md` == 0
      (read-only posture preserved).
  </acceptance_criteria>
  <done>Item 9 reports the uncapped stale fraction, every stale slug, and each chunk's repair-gate disposition by formatting a read-only command's `--json`; the file's own count sentences say nine items.</done>
</task>

<task type="auto">
  <name>Task 2: Pin item 9 and the new count in `status-tools.test.ts`</name>
  <files>src/cli/slash-command/bs/status-tools.test.ts</files>
  <read_first>
    - `src/cli/slash-command/bs/status-tools.test.ts` lines 30–60 (`read`, `STALE_MARKER`,
      `WAIVED_STATUS`, `REFERENCED_PATHS`, the cited-heading guard), lines 110–135 (the item-3
      through item-6 enumeration pins whose shape this copies), lines 205–225 (the
      `verifiedWithoutProvenance` pin, the `eight items` count pin, and the read-only-posture pin)
    - `src/cli/slash-command/bs/check-status.md` (Task 1's output)
    - `src/cli/commands/verify-impact.ts` — the exported marker constants and
      `REPAIR_GATE_DISPOSITIONS`, to import rather than re-type
  </read_first>
  <action>
Import `RULES_STALE_MARKER` and `REPAIR_GATE_DISPOSITIONS` from `../../commands/verify-impact.js`,
following this file's existing discipline of pinning against a real exported constant rather than a
re-typed string (the `STALE_MARKER` const stays as-is — it pins the DIFFERENT, pre-existing marker,
and the two must not be conflated).

Change the count pin: assert the file does NOT match `/eight items/i` and DOES contain
`nine items` at least twice. Rename the test accordingly.

Add a new `describe('decision 19 — item 9 reports rules staleness')` with:
- `check-status.md` matches `/^\*\*9\. /m`.
- It contains `boardsmith verify-impact-status --json`.
- It contains `staleFraction`, `staleSlugs`, `dispositionCounts`, and `contradictionsPending`.
- It contains every member of `REPAIR_GATE_DISPOSITIONS` (loop over the frozen array — never a
  re-typed list, so a disposition rename cannot silently desync the skill text).
- It contains `RULES_STALE_MARKER`'s exact string, AND separately still contains `STALE_MARKER`'s
  exact string, AND the two assertions are in distinct tests so a future conflation is visible.
- Negative: the file contains no instruction to write a state file (re-assert the existing
  read-only-posture regex, so item 9 cannot have smuggled in a write).
- Negative: `RULES_STALE_MARKER` does not appear inside any line of `check-status.md` that also
  contains `stale — re-derive before build` (the two markers are never described as the same thing).

Add `state-machine.md`'s new `## Rules Staleness Marker` heading to this file's existing
cited-heading guard list (the constant that asserts every `state-machine.md` heading
`check-status.md` cites actually exists in `state-machine.md`), so item 9's citation cannot dangle.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/status-tools.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/status-tools.test.ts` passes.
    - The suite FAILS if `check-status.md`'s `nine items` is reverted to `eight items` — verify by a
      temporary edit, observe the failure, revert, and record the observed message in the summary.
    - `grep -c "REPAIR_GATE_DISPOSITIONS" src/cli/slash-command/bs/status-tools.test.ts` ≥ 1.
    - `grep -c "RULES_STALE_MARKER" src/cli/slash-command/bs/status-tools.test.ts` ≥ 2.
    - `grep -c "Rules Staleness Marker" src/cli/slash-command/bs/status-tools.test.ts` ≥ 1 (the
      cited-heading guard entry).
  </acceptance_criteria>
  <done>Item 9, the nine-item count, every disposition string, and the two-markers-are-distinct property are all pinned against exported constants, and the `state-machine.md` citation cannot dangle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill text → designer's mental model | The one surface a designer reads for project state; a capped or wrong count silently misinforms |
| `check-status.md` → project state files | The skill's documented posture is no writes of any kind; a new item must not breach it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-23 | Information Disclosure | Item 9's stale list | mitigate | `never capped` asserted in prose; the uncapped `staleSlugs` field named explicitly (decision 15) |
| T-175-24 | Tampering | `check-status.md`'s read-only posture | mitigate | The existing read-only regex pin is re-asserted after the edit; item 9 calls only a read-only command |
| T-175-25 | Spoofing | Conflating `rules-stale` with `stale — re-derive before build` | mitigate | Two distinct pinned constants, asserted in separate tests, plus a negative that no single line names both |
| T-175-26 | Repudiation | A dangling `state-machine.md` heading citation | mitigate | `## Rules Staleness Marker` added to the existing cited-heading guard list |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/status-tools.test.ts`
- `npx vitest run src/cli/slash-command/bs/` — all bs skill-text suites
- `npm test` full suite green
</verification>

<success_criteria>
- `check-status.md` has an item 9 that formats `verify-impact-status --json`, reports the uncapped
  stale fraction and every stale slug, and groups by disposition.
- The file's count sentences say nine items; no `eight items` remains.
- Pins are against exported constants and demonstrably fail on regression.
- The skill's read-only posture is unchanged.
</success_criteria>

<output>
Create `.planning/phases/175-impact-map-repair-gating/175-06-SUMMARY.md` when done
</output>
