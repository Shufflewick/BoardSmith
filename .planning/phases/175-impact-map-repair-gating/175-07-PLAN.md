---
phase: 175-impact-map-repair-gating
plan: 07
type: execute
wave: 5
depends_on: [175-05, 175-06]
files_modified:
  - .planning/phases/175-impact-map-repair-gating/175-PROOF.md
autonomous: false
requirements: [VERIFY-04]
must_haves:
  truths:
    - "A REAL `contradictory` verdict — the one 174-07 produced by mutating a real PDF — stops a real pass before any staleness write"
    - "Both readings are presented verbatim side by side to a human, who confirms they can actually adjudicate from what they see"
    - "A resolution lands as a real `### Ruling N` entry appended to a real reference game's RULINGS.md copy"
    - "A deferral lands as UNADJUDICATED and the affected chunks are still marked stale"
    - "Both `~/BoardSmithGames` originals are byte-identical before and after"
  artifacts:
    - path: ".planning/phases/175-impact-map-repair-gating/175-PROOF.md"
      provides: "Sections 1-2: the real-material provenance check and the live VERIFY-04 gate proof, with measured counts"
      min_lines: 120
  key_links:
    - from: ".planning/phases/175-impact-map-repair-gating/175-PROOF.md"
      to: "175-FIXTURES/174-07-contradictory/"
      via: "sha256-verified real recorded contradictory ClassificationRecord, cited by path"
      pattern: "174-07-contradictory"
---

<objective>
Prove VERIFY-04 against the REAL `contradictory` verdict, on a real project copy, with a real human
reading the gate.

Purpose: the milestone's proof bar (`173-PROOF.md` / `174-PROOF.md`): real runs against `cp -R`
copies, measured counts never "ran clean", originals confirmed byte-identical before and after. The
hardest input is already solved — 174-07's real `contradictory` finding (a genuine Fight-phase
precedence reversal edited into `one-two-punch`'s image-only `rules.pdf`) is committed at
`175-FIXTURES/174-07-contradictory/` with per-file sha256s, including the raw subagent return with
`lineFindings[]` and the recorded `ClassificationRecord`. No PDF re-mutation is needed.

This milestone has already caught five premature or overstated completion marks. Do not add a sixth:
every claim in this proof names the artifact it rests on, and anything not observed live is labelled
as not observed.

Output: `175-PROOF.md` sections 1–2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/175-impact-map-repair-gating/175-CONTEXT.md
@.planning/phases/175-impact-map-repair-gating/175-RESEARCH.md
@.planning/phases/175-impact-map-repair-gating/175-FIXTURES/174-07-contradictory/MANIFEST.md
@.planning/phases/174-verify-classifier/174-PROOF.md
@.planning/phases/173-verify-pipeline-core/173-PROOF.md

<interfaces>
The real recorded verdict this proof consumes, from
`175-FIXTURES/174-07-contradictory/staged/one-two-punch/RUN.md`:
```
{"kind":"classification","pairId":"pages-1-2", ... ,"provenance":"source-changed",
 "ruleDelta":"contradictory","stale":true, ...
 "quotedPass1":"The player with the lower timing on their card must resolve their action first...",
 "quotedPass2":"The player with the higher timing on their card must resolve their action first...",
 "recordedAt":"2026-07-30T01:41:59.573Z"}
```

Commands under proof (plans 175-03/175-04):
```
boardsmith verify-impact-gate       --project <dir> [--run-id <id>] [--json]
boardsmith verify-impact-adjudicate --project <dir> --run-id <id> --pair-id <id>
                                    --outcome resolved|UNADJUDICATED
                                    [--decision <t>] [--citation <t>] [--rationale <t>] [--json]
boardsmith verify-impact-apply      --project <dir> [--run-id <id>] [--json]
boardsmith verify-impact-status     --project <dir> [--run-id <id>] [--json]
```

Proof-run discipline, unchanged from 171–174:
- Work on `cp -R` copies under a scratch dir; never touch `~/BoardSmithGames/seven` (READ-ONLY,
  pinned at `a03f38d4792af9dfc7c798be69686fc3230f54dd`) or `~/BoardSmithGames/one-two-punch`.
- Snapshot both originals' recursive sha256 manifests BEFORE and AFTER, and record both in the proof.
- Record exact measured counts. Never the phrase "ran clean".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real-material provenance check + the live gate stop (VERIFY-04, first half)</name>
  <files>.planning/phases/175-impact-map-repair-gating/175-PROOF.md</files>
  <read_first>
    - `.planning/phases/175-impact-map-repair-gating/175-FIXTURES/174-07-contradictory/MANIFEST.md`
      (the per-file sha256 list to re-verify)
    - `.planning/phases/175-impact-map-repair-gating/175-FIXTURES/174-07-contradictory/staged/one-two-punch/RUN.md`
      (the real recorded ledger)
    - `.planning/phases/175-impact-map-repair-gating/175-FIXTURES/174-07-contradictory/evidence/subagent-otp-sc3-classify-return.txt`
      (the raw subagent return with `lineFindings[]`)
    - `.planning/phases/174-verify-classifier/174-PROOF.md` §5 (how the mutation was produced, and
      the mutated PDF's sha256)
    - `.planning/phases/173-verify-pipeline-core/173-PROOF.md` §§ "How to re-run every proof" and
      "Both reference-game originals: byte-identical after the entire phase" (the structure to follow)
    - `src/cli/commands/verify-impact.ts` (the commands under proof)
  </read_first>
  <action>
Create `.planning/phases/175-impact-map-repair-gating/175-PROOF.md` following `174-PROOF.md`'s
section structure and voice.

**§1 — Real material provenance.** Re-verify every sha256 in
`175-FIXTURES/174-07-contradictory/MANIFEST.md` against the files on disk and record the exact
match count (`N/N verified`). Record the mutated PDF's recorded sha256 and state plainly that the PDF
itself is NOT archived, that this proof therefore does not re-run the mutation, and that
`174-PROOF.md` §5 documents the regeneration recipe. Quote the real recorded `contradictory`
`ClassificationRecord` line verbatim from the fixture ledger. Record the `lineFindings[]` composition
from the raw subagent return (the real count of contradictory vs. cosmetic line-level findings) and
note research's measured limitation: only the max-severity `quotedPass1`/`quotedPass2` pair is
persisted onto `ClassificationRecord`, so a multi-delta pair's non-max lines are not on the record —
flag this forward to Phase 176 rather than implying it is covered.

**§2 — VERIFY-04: the gate stops a real pass.** Build a scratch copy:
`cp -R ~/BoardSmithGames/one-two-punch <scratch>/otp-175` (record the scratch path). Seed it with the
real run: copy `175-FIXTURES/174-07-contradictory/staged/one-two-punch/RUN.md` and `slices/` into the
copy's `rulebook/.verify/<run-id>/` so the copy carries the REAL recorded contradictory verdict.
Record the run-id used and the sha256 of the seeded ledger.

Then run, in order, capturing full stdout for each into the proof:

1. `boardsmith verify-impact-gate --project <copy> --json` — record the exact
   `summary.contradictory` / `summary.pending` counts and the full `contradictions[0]` object.
2. `boardsmith verify-impact-gate --project <copy>` (human report) — paste the verbatim output block
   showing both readings side by side.
3. `boardsmith verify-impact-apply --project <copy> --json` — record that it returned
   `blocked: true` with the pending pair named. **Immediately before and after this call, compute a
   recursive sha256 manifest of the entire copy and record both**; they must be identical, proving the
   gate stops the pass BEFORE any staleness write. Record the diff count (`0 files changed`) as a
   measured number, not as a claim.
4. `grep -rn "boardsmith:rules-staleness:begin" <copy>/chunks | wc -l` — record the count (expected
   the scaffolded-empty count from the templates, with zero carrying `RULES_STALE_MARKER`); also
   record `grep -rc "rules-stale — rulebook moved" <copy>/chunks | ...` totalling 0.

Record the two `~/BoardSmithGames` originals' recursive sha256 manifest hashes taken BEFORE any of
this ran, in a clearly labelled subsection, for the after-comparison in §2's closing and plan 175-08's
final check.

If any command's real output differs from what the plan expected, record the ACTUAL output and say so
explicitly. Do not smooth it.
  </action>
  <verify>
    <automated>test -f .planning/phases/175-impact-map-repair-gating/175-PROOF.md && grep -c "blocked" .planning/phases/175-impact-map-repair-gating/175-PROOF.md</automated>
  </verify>
  <acceptance_criteria>
    - `175-PROOF.md` §1 records `N/N` sha256 matches against the fixture MANIFEST, with N stated.
    - §1 quotes the real `ClassificationRecord` line containing `"ruleDelta":"contradictory"` and both
      `lower timing` / `higher timing` quotes.
    - §2 contains verbatim `verify-impact-gate` human output showing both readings under distinct
      labels.
    - §2 records `verify-impact-apply` returning `blocked: true` and a before/after whole-copy sha256
      comparison with an explicit `0 files changed`.
    - §2 records a measured count of `rules-stale — rulebook moved` occurrences in the copy's
      `chunks/` as `0`.
    - The document contains no occurrence of the phrase `ran clean`
      (`grep -ci "ran clean" 175-PROOF.md` == 0).
  </acceptance_criteria>
  <done>The real contradictory material is sha256-verified in place, and a real `verify-impact-apply` on a real project copy is measurably blocked with zero bytes written.</done>
</task>

<task type="auto">
  <name>Task 2: Both terminal answers, on real files (VERIFY-04, second half)</name>
  <files>.planning/phases/175-impact-map-repair-gating/175-PROOF.md</files>
  <read_first>
    - `.planning/phases/175-impact-map-repair-gating/175-PROOF.md` (Task 1's output)
    - `~/BoardSmithGames/one-two-punch/RULINGS.md` (the real 26-entry corpus the copy carries)
    - `src/cli/commands/verify-impact.ts` (`appendRuling`, `verifyImpactAdjudicateCommand`)
    - `src/cli/commands/trace-check.ts` (the existing `RULINGS.md` reader that must still parse the
      appended corpus)
    - `.planning/phases/175-impact-map-repair-gating/175-CONTEXT.md` decisions 7, 8, 9
  </read_first>
  <action>
Append **§3 — VERIFY-04: both terminal answers on real files** to `175-PROOF.md`.

**§3a — Deferral (`UNADJUDICATED`).** On a fresh `cp -R` copy seeded exactly as in §2, run
`boardsmith verify-impact-adjudicate --project <copy> --run-id <id> --pair-id <pairId> --outcome UNADJUDICATED --json`.
Record: `RULINGS.md`'s sha256 before and after (must be identical — a deferral writes no ruling); the
appended `AdjudicationRecord` ledger line verbatim; then run `verify-impact-apply` and record that it
now proceeds, the exact count of chunks marked, and the verbatim `## Rules Staleness` fenced body from
ONE affected CHUNK.md showing `Adjudication: UNADJUDICATED`. Then run
`boardsmith verify-impact-gate --json` again and record that the pair is STILL pending — a deferral is
not a resolution.

**§3b — Resolution (`### Ruling N`).** On another fresh `cp -R` copy seeded the same way, record
`nextRulingNumber`'s input state first: `grep -c '^### Ruling ' <copy>/RULINGS.md` (the real corpus
count). Then run `verify-impact-adjudicate ... --outcome resolved --decision "<real designer-voice
text naming which reading governs>" --citation "<the real slice path and page reference>" --rationale
"<why>"`. Record: the assigned ruling number; the appended entry verbatim; the assertion that the new
`RULINGS.md` content `startsWith` the old content byte-for-byte (record the byte length of both); and
`grep -c 'supersede' <appended entry>` == 0. Then run `boardsmith trace-check --project <copy> --json`
and record that the appended corpus still parses — the total ruling count read by the existing reader,
and any findings it emits about the new entry (an untested-ruling finding is EXPECTED and must be
reported, not hidden).

**§3c — No representable bypass.** Record the verbatim `--help` output of all four
`verify-impact-*` commands and state the measured absence of any `--force`, `--yes`, `--skip`,
`--skip-gate`, `--clear`, or `--bypass` option. Record
`grep -rciE "process\.env" src/cli/commands/verify-impact.ts` == 0. State plainly that this is a
structural absence, following `build/ask.md`'s own model, not a documented-but-present flag.

Record every measured number. If `trace-check` emits an unexpected finding about the appended entry,
record it as a real finding of this proof and route it, rather than adjusting the proof to avoid it.
  </action>
  <verify>
    <automated>grep -c "### Ruling" .planning/phases/175-impact-map-repair-gating/175-PROOF.md</automated>
  </verify>
  <acceptance_criteria>
    - §3a records `RULINGS.md` sha256 identical before/after the `UNADJUDICATED` record, and a
      verbatim CHUNK.md fenced body containing `Adjudication: UNADJUDICATED`.
    - §3a records the same pair still `pending` on a re-run of `verify-impact-gate`.
    - §3b records the real pre-append ruling count, the assigned number as that count + 1, the
      appended entry verbatim with all three field labels, and both byte lengths proving append-only.
    - §3b records `boardsmith trace-check --json` output on the appended corpus, including its total
      ruling count and any finding about the new entry.
    - §3c records all four `--help` outputs verbatim and an explicit statement that no bypass option
      exists, with the `process.env` grep count.
    - No occurrence of `ran clean` anywhere in the file.
  </acceptance_criteria>
  <done>Both terminal answers are proven on real files: a deferral writes no ruling and keeps the pair pending; a resolution appends a conforming `### Ruling N` the existing reader still parses.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human confirms the gate is actually adjudicable</name>
  <files>.planning/phases/175-impact-map-repair-gating/175-PROOF.md</files>
  <read_first>
    - `.planning/phases/175-impact-map-repair-gating/175-PROOF.md` §2 and §3 (the verbatim gate output)
  </read_first>
  <action>
Present `175-PROOF.md` §2 and §3's verbatim gate output to the designer, ask the six verification
questions listed under `<how-to-verify>` below, and record their answer verbatim as a new
`§3d — Human adjudicability check` subsection of `175-PROOF.md`. If the designer names a missing
field or missing context, record it as a numbered finding with an explicit disposition — fixed in
this phase, or routed forward with a named destination phase — and never absorb it silently.
  </action>
  <what-built>
    `boardsmith verify-impact-gate` presents every un-adjudicated `contradictory` finding at once, with
    both readings quoted verbatim side by side and every affected chunk named uncapped;
    `verify-impact-apply` measurably refuses to write while any finding is pending; and both terminal
    answers (`resolved` → a real `### Ruling N` append, `UNADJUDICATED` → recorded, chunks still
    marked stale) are proven on real project copies.

    VERIFY-04's requirement is that a contradiction "always stops and asks the human" — whether the
    presentation is genuinely sufficient for a human to DECIDE from is the one property no unit test
    can observe. That is what this checkpoint verifies.
  </what-built>
  <how-to-verify>
    1. Read `175-PROOF.md` §2, step 2 — the verbatim `verify-impact-gate` human report.
    2. Confirm you can tell, from that output alone and without opening a slice: which rule changed,
       what it said before, what it says now, and which chunks are affected.
    3. Confirm the affected-chunk list is complete and not truncated (no "and N more", no ellipsis).
    4. Read §3a's CHUNK.md fenced body and confirm `Adjudication: UNADJUDICATED` reads as an honest
       unresolved state, not as a resolved one.
    5. Read §3b's appended `### Ruling N` entry and confirm it reads like the surrounding
       human-authored entries in `~/BoardSmithGames/one-two-punch/RULINGS.md` — same three fields,
       same voice, no machine-shaped syntax.
    6. If anything is insufficient to adjudicate from, say exactly which field or context is missing.
  </how-to-verify>
  <verify>
    <automated>MISSING — this property is a human's ability to decide from the presentation; §2/§3's verbatim outputs are the automated substrate it rests on</automated>
    <human-check>The designer confirms the gate output is sufficient to adjudicate a real contradiction, the affected-chunk list is uncapped, and the appended ruling reads like the human corpus around it.</human-check>
  </verify>
  <acceptance_criteria>
    - The designer's verdict is recorded verbatim in `175-PROOF.md` §3d, including any named
      insufficiency.
    - If the designer names a missing field or context, it is recorded as a real finding with a
      disposition (fixed in this phase, or routed forward with a named destination) — never
      absorbed silently.
  </acceptance_criteria>
  <resume-signal>Type "approved" or name exactly which field or context is missing from the gate presentation.</resume-signal>
  <done>The designer has confirmed the gate presentation is adjudicable, and their verdict (plus any named gap and its disposition) is recorded in `175-PROOF.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proof run → `~/BoardSmithGames` originals | A proof that mutates its own reference data destroys the milestone's baseline |
| proof narrative → reader's trust | An overstated claim is worse than a stated gap; this milestone has already caught five |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-27 | Tampering | Reference-game originals | mitigate | All work on `cp -R` copies; recursive sha256 manifests of both originals recorded before, and compared in plan 175-08 |
| T-175-28 | Repudiation | Claiming the gate blocked | mitigate | Whole-copy sha256 manifest before/after the blocked `verify-impact-apply`, with an explicit `0 files changed` count |
| T-175-29 | Spoofing | Claiming a live proof that was structural | mitigate | Every §ns claim names its artifact; anything not observed live is labelled as not observed |
| T-175-30 | Tampering | `RULINGS.md` append into a real corpus | mitigate | `startsWith`-the-original byte-length comparison plus a `trace-check` re-parse of the appended corpus |
</threat_model>

<verification>
- `175-PROOF.md` §§1–3 exist with verbatim command output and measured counts
- `grep -ci "ran clean" .planning/phases/175-impact-map-repair-gating/175-PROOF.md` == 0
- Both `~/BoardSmithGames` originals' pre-run sha256 manifests recorded
- `npm test` still green after the proof session (the proof must not have required a source change;
  if it did, that change is a real finding and is recorded)
</verification>

<success_criteria>
- The real `contradictory` fixture material is sha256-verified in place.
- A real `verify-impact-apply` is measurably blocked with zero bytes written.
- Both terminal answers are proven on real project copies, with `trace-check` still parsing the
  appended corpus.
- No bypass option exists in any `--help` output.
- The designer has confirmed the gate is adjudicable and their verdict is recorded.
</success_criteria>

<output>
Create `.planning/phases/175-impact-map-repair-gating/175-07-SUMMARY.md` when done
</output>
