---
phase: 175-impact-map-repair-gating
plan: 08
type: execute
wave: 6
depends_on: [175-07]
files_modified:
  - .planning/phases/175-impact-map-repair-gating/175-PROOF.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [VERIFY-05, VERIFY-06]
must_haves:
  truths:
    - "A real staleness write lands in both CHUNK.md and SKETCH.md on real reference-game copies, and the result still passes a cold-resume parse"
    - "The VERIFY-06 payoff is MEASURED on the real stale sets (6/16 `seven`, 6/11 `one-two-punch`): how many stale chunks close without re-playtesting"
    - "The `verified (user-waived)` + stale + code-changed path is proven, and labelled live or structural according to what actually happened"
    - "Both `~/BoardSmithGames` originals are byte-identical after the entire phase"
  artifacts:
    - path: ".planning/phases/175-impact-map-repair-gating/175-PROOF.md"
      provides: "Sections 4-7: the real cross-file staleness write, the VERIFY-06 payoff measurement, the waived path, and the phase closeout"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VERIFY-04/05/06 closed with section-and-number citations into 175-PROOF.md, or left open with a named reason"
  key_links:
    - from: ".planning/phases/175-impact-map-repair-gating/175-PROOF.md"
      to: ".planning/phases/174-verify-classifier/174-PROOF.md §8"
      via: "the real stale sets this phase's payoff is measured against"
      pattern: "174-PROOF\\.md"
---

<objective>
Prove VERIFY-05 and VERIFY-06 on real reference-game copies — and MEASURE the number that is this
phase's practical payoff.

Purpose: Phase 174 closed with anchor density as an explicit open risk — 6 of 16 `seven` chunks and 6
of 11 `one-two-punch` chunks go rules-stale from ONE finding each, because several chunks genuinely
quote the same foundational rule. VERIFY-06's scoping is what makes that tolerable: if most of those
chunks pass the lenses with NO code change, they close without re-playtesting and the human cost stays
near zero. That relationship is the most valuable number this phase can produce, and it is measured
here on the real stale sets — not asserted.

Decision 13's path (`verified (user-waived)` + rules-stale + code-changed) may not occur naturally in
either reference game. If it does not, prove it structurally and SAY it was structural. This milestone
has caught five premature or overstated completion marks; do not add a sixth.

Output: `175-PROOF.md` sections 4–7, and the requirement/roadmap closeout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/175-impact-map-repair-gating/175-CONTEXT.md
@.planning/phases/175-impact-map-repair-gating/175-PROOF.md
@.planning/phases/174-verify-classifier/174-PROOF.md
@.planning/phases/174-verify-classifier/174-VERIFICATION.md

<interfaces>
The real stale sets to measure against (`174-PROOF.md` §8, after decision 19's attribution ladder):
```
seven:          6 of 16 citing chunks rules-stale (37.5%), ruleDelta `sharper`
one-two-punch:  6 of 11 citing chunks rules-stale (54.5%), carrying the real `contradictory` finding
```
Every clean chunk's cleanliness is nameable from real `attributions[]` output; every stale chunk's
attributing rung is recorded in `174-PROOF.md` §8.

The real evidence available in-repo:
- `175-FIXTURES/174-07-contradictory/evidence/seven-final-status.json` — the real final
  `verify-classify-status` output for `seven`
- `175-FIXTURES/174-07-contradictory/evidence/subagent-seven-chunkcheck-classify-return.txt`
- `175-FIXTURES/174-07-contradictory/staged/{seven,one-two-punch}/RUN.md` — the real run ledgers
- `.planning/phases/174-verify-classifier/174-FIXTURES/` — the real live+staged slice pairs

Commands under proof:
```
boardsmith verify-impact-apply   --project <copy> [--run-id <id>] [--json]
boardsmith verify-impact-status  --project <copy> [--run-id <id>] [--json]
boardsmith drift-check           --project <copy> [--json]
boardsmith chunk-check <slug>    --project <copy> --reverified-no-code-change "<hash>..<head> — 0 manifest files changed"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VERIFY-05 — a real cross-file staleness write that still cold-resume-parses</name>
  <files>.planning/phases/175-impact-map-repair-gating/175-PROOF.md</files>
  <read_first>
    - `.planning/phases/175-impact-map-repair-gating/175-PROOF.md` (plan 07's §§1–3)
    - `.planning/phases/175-impact-map-repair-gating/175-FIXTURES/174-07-contradictory/staged/seven/RUN.md`
      and `.../evidence/seven-final-status.json`
    - `src/cli/slash-command/bs/state-machine.md` "Write Order", "Authority", "Consistency Check"
      (including the item 5 plan 175-01 added)
    - `src/cli/commands/verify-impact.ts` (`writeRulesStalenessMarker`, `parseRulesStaleness`)
    - `.planning/phases/173-verify-pipeline-core/173-PROOF.md` § "Both reference-game originals:
      byte-identical after the entire phase" (the closing-check format)
  </read_first>
  <action>
Append **§4 — VERIFY-05: the real cross-file staleness write** to `175-PROOF.md`.

For EACH reference game (`seven` and `one-two-punch`), on a fresh `cp -R` copy seeded with that game's
real run ledger from `175-FIXTURES/174-07-contradictory/staged/<game>/`:

1. Record the pre-run state: `grep -c 'rules-stale — rulebook moved' <copy>/chunks/*/CHUNK.md` totals
   (expected 0) and `grep -c 'Rules Staleness (derived from' <copy>/SKETCH.md`.
2. Run `boardsmith verify-impact-apply --project <copy> --json` (after adjudicating any contradiction
   per plan 07's §3 procedure — record which outcome was used). Capture the full JSON.
3. Record the MEASURED count of chunks marked, and compare it against the expected stale set from
   `174-PROOF.md` §8 (`seven` 6/16, `one-two-punch` 6/11). **If the measured count differs, record the
   actual number, name every slug in the symmetric difference, and diagnose why** — do not adjust the
   expectation to match.
4. Paste the verbatim `## Rules Staleness` fenced body from at least two affected CHUNK.md files, and
   the corresponding `- Rules Staleness (derived from chunks/<slug>/CHUNK.md):` lines from SKETCH.md.
5. Prove write order and authority on real files: assert (and record the assertion output) that in
   each written CHUNK.md the LAST non-empty line inside the fences begins with `Marker:`; and that
   every SKETCH.md derived pointer matches its CHUNK.md's marker value, with zero mismatches.
6. Prove the Status enum did NOT move on real data: record
   `grep -c 'rules-stale' <copy>/chunks/*/CHUNK.md` restricted to lines that also contain `Status:`
   — must be 0 — and paste one affected chunk's unchanged `Status: verified` line beside its new
   `Marker: rules-stale ...` line, showing the two axes side by side.
7. Prove cold-resume parseability: run `boardsmith chunk-provenance-status --project <copy> --json`
   and `boardsmith drift-check --project <copy> --json` on the written copy and record that both
   parse every chunk without a parse failure (record the per-command chunk counts). Then record the
   verbatim `state-machine.md` Consistency Check item 5 text as the registration this rests on.
8. Confirm both `~/BoardSmithGames` originals' recursive sha256 manifests still match plan 07 §2's
   pre-run snapshot, and record the comparison result as a measured count of differing files.
  </action>
  <verify>
    <automated>grep -c "Rules Staleness" .planning/phases/175-impact-map-repair-gating/175-PROOF.md</automated>
  </verify>
  <acceptance_criteria>
    - §4 records, per game, the measured marked-chunk count against `174-PROOF.md` §8's expected set,
      with any difference named slug-by-slug and diagnosed.
    - §4 pastes at least two verbatim `## Rules Staleness` fenced bodies and their matching SKETCH.md
      derived pointers.
    - §4 records `Marker:`-last verification output and zero CHUNK/SKETCH mismatches.
    - §4 records `grep`-measured 0 occurrences of `rules-stale` on any `Status:` line, plus a
      side-by-side `Status: verified` / `Marker: rules-stale ...` excerpt.
    - §4 records `chunk-provenance-status --json` and `drift-check --json` parsing every chunk of the
      written copy with zero parse failures, with counts.
    - §4 records both originals' sha256 manifest comparison as `0 files differ`.
  </acceptance_criteria>
  <done>The marker really lands in both files on both reference games, `Marker:` is last, SKETCH derives from CHUNK, the Status enum did not move, the result still cold-resume-parses, and the originals are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: VERIFY-06 — measure the payoff on the real stale sets, and settle the waived path</name>
  <files>.planning/phases/175-impact-map-repair-gating/175-PROOF.md</files>
  <read_first>
    - `.planning/phases/175-impact-map-repair-gating/175-PROOF.md` (§§1–4)
    - `.planning/phases/174-verify-classifier/174-PROOF.md` §8 (the stale/total tables, every clean
      chunk's reason, every stale chunk's attributing rung)
    - `.planning/phases/174-verify-classifier/174-VERIFICATION.md` (the carried-forward open risks)
    - `src/cli/commands/drift-check.ts` and `src/cli/commands/verify-impact.ts`
      (`computeRepairGate`, `verifyImpactStatusCommand`)
    - `.planning/phases/175-impact-map-repair-gating/175-CONTEXT.md` decisions 10, 11, 12, 13, 15
    - `~/BoardSmithGames/seven/chunks/*/CHUNK.md` and `~/BoardSmithGames/one-two-punch/chunks/*/CHUNK.md`
      `Status:` lines — to establish whether any real chunk carries `verified (user-waived)`
  </read_first>
  <action>
Append **§5 — VERIFY-06: the measured payoff** to `175-PROOF.md`.

On the written copies from §4, run `boardsmith verify-impact-status --project <copy> --json` for each
game and record:

1. The verbatim `staleFraction` and the full `staleSlugs` list — confirming it is uncapped by
   comparing its length to `staleFraction.stale`.
2. The verbatim `dispositionCounts`, and a per-slug table: slug, `driftState`, `gate.disposition`,
   `gate.nextStatus`, `gate.reverifyStamp`.
3. **The payoff number, stated explicitly for each game:** of the N rules-stale chunks, how many are
   `close-without-replaytest` (close with NO re-playtesting), how many are `reopen-playtest`, and how
   many are `unknown-drift`. Then state the human-effort consequence in one sentence per game, e.g.
   "of `seven`'s 6 rules-stale chunks, K close without re-playtesting and J re-open the gate — the
   designer re-playtests J chunks, not 6."
4. **Interpret honestly against 174's carried risk.** State whether the measured payoff makes 174's
   anchor-density fraction (37.5% / 54.5%) tolerable in practice or not. If most stale chunks land in
   `unknown-drift` rather than `clean` — which is plausible on these games, since research recorded
   both as `pre-provenance` projects with no `## Verified Commit Hash` recorded — say so plainly:
   `unknown-drift` means the payoff was NOT demonstrated on this data, and the honest statement is
   that the mechanism is correct while the data cannot exercise it. Do not convert an `unknown` into
   a `clean` to produce a better number. Record the real per-game `drift-check` `counts` object as the
   evidence for whichever conclusion holds.
5. If `unknown-drift` dominates, ALSO run the measurement on a synthetically-anchored copy: record a
   `## Verified Commit Hash` for each chunk from the copy's own git HEAD before any change, re-run
   `drift-check` and `verify-impact-status`, and record the resulting dispositions — labelling this
   subsection explicitly as an anchored-copy measurement, distinct from the unmodified-real-data
   measurement above. State exactly what was synthesized and why.

Append **§6 — decision 13: the `verified (user-waived)` path.**

Record the measured fact first: `grep -l 'Status: verified (user-waived)'` across both copies'
`chunks/*/CHUNK.md`, with the count.

- If at least one real chunk is `verified (user-waived)` AND rules-stale AND code-changed: run the
  real path and record the resulting disposition verbatim. Label it **LIVE**.
- If not: prove it structurally — record `computeRepairGate`'s table-driven unit-test output for the
  `verified (user-waived)` × `drifted` × `stale` case, and construct one real-file demonstration by
  editing a copy's chunk `Status:` line to `verified (user-waived)` and a manifest file's bytes, then
  running `drift-check` + `verify-impact-status` and recording the disposition. Label this section
  **STRUCTURAL + CONSTRUCTED-FIXTURE, not naturally occurring** and say exactly what was constructed.
  Do not describe it as a live proof.

Also record decision 11's stamp on real data: pick one `close-without-replaytest` (or
constructed-clean) chunk, run
`boardsmith chunk-check <slug> --project <copy> --reverified-no-code-change "<hash>..<head> — 0 manifest files changed"`,
and paste the resulting `## Verified Against` fenced body showing the new
`Re-verified (no code change):` line.
  </action>
  <verify>
    <automated>grep -c "close-without-replaytest" .planning/phases/175-impact-map-repair-gating/175-PROOF.md</automated>
  </verify>
  <acceptance_criteria>
    - §5 records, per game, `staleFraction`, the full uncapped `staleSlugs`, `dispositionCounts`, and
      a per-slug disposition table.
    - §5 states the payoff number explicitly per game in the form "of N rules-stale chunks, K close
      without re-playtesting".
    - §5 states plainly whether the payoff was demonstrated on real data or whether `unknown-drift`
      prevented it, citing the real `drift-check` `counts` object either way.
    - Any anchored-copy measurement is in its own clearly labelled subsection naming what was
      synthesized.
    - §6 records the measured `verified (user-waived)` count and is labelled either **LIVE** or
      **STRUCTURAL + CONSTRUCTED-FIXTURE**, never ambiguously.
    - §6 pastes a real `## Verified Against` body containing `Re-verified (no code change):`.
    - No occurrence of `ran clean` anywhere in the file.
  </acceptance_criteria>
  <done>VERIFY-06's payoff is measured on the real stale sets with an honest verdict about whether the data could exercise it, and decision 13's path is settled with its evidence class named.</done>
</task>

<task type="auto">
  <name>Task 3: Closeout — what is proven, what is not, and the requirement citations</name>
  <files>.planning/phases/175-impact-map-repair-gating/175-PROOF.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/175-impact-map-repair-gating/175-PROOF.md` (§§1–6)
    - `.planning/phases/173-verify-pipeline-core/173-PROOF.md` §§ "What is still unproven (final,
      phase-wide)" and "How to re-run every proof" (the closing sections' exact format)
    - `.planning/REQUIREMENTS.md` VERIFY-04/05/06 entries and the citation style used on
      VERIFY-01/02/03/07/08
    - `.planning/ROADMAP.md` Phase 175 section and the Progress table
    - `.planning/phases/174-verify-classifier/174-VERIFICATION.md` (the carried open risks list to
      extend rather than reset)
  </read_first>
  <action>
Append to `175-PROOF.md`:

**§7 — What is still unproven (final, phase-wide).** Enumerate honestly, at minimum:
- The `lineFindings[]` persistence gap: only the max-severity `quotedPass1`/`quotedPass2` pair is
  retained on `ClassificationRecord`, so a multi-delta pair's non-max line deltas are not available to
  Phase 176's repair scoping. Flag with a named destination.
- Whether a LIVE `/bs-verify-game` session actually follows Step 4's prose — this phase pins skill
  text, and skill-text pins do not prove a live session receives or follows it (the standing caveat
  every plan in this milestone carries; Phase 170 found twelve half-followed instructions).
- No native Task/Agent-tool dispatch: the carried milestone-wide caveat, unchanged.
- Whichever of §5's payoff / §6's waived path was labelled STRUCTURAL or CONSTRUCTED rather than LIVE.
- Anything the designer named at plan 07's human checkpoint that was routed forward rather than fixed.

**§8 — How to re-run every proof.** The exact command sequence, copy paths, and seeding steps, in the
same format `173-PROOF.md` uses, so a later reader can reproduce §§1–6 without reconstructing them.

**§9 — Both reference-game originals: byte-identical after the entire phase.** Record the final
recursive sha256 manifest comparison for `~/BoardSmithGames/seven` and
`~/BoardSmithGames/one-two-punch` against plan 07 §2's pre-run snapshot, as a measured count of
differing files. Record `seven`'s pinned commit
(`a03f38d4792af9dfc7c798be69686fc3230f54dd`) and confirm it is unchanged.

**`.planning/REQUIREMENTS.md`** — for each of VERIFY-04, VERIFY-05, VERIFY-06, either mark `[x]` with
a parenthetical citation naming the exact `175-PROOF.md` section and measured number that closes it
(matching VERIFY-01/03/07's existing citation style), or leave it `[ ]` with an explicit one-paragraph
statement of what is missing and where it is routed. Do not mark a requirement closed on a
STRUCTURAL-only proof without saying so in the citation itself.

**`.planning/ROADMAP.md`** — update Phase 175's `**Plans**:` line and plan checklist to the eight
plans actually shipped, update the Progress table row, and write a `**Result:**` paragraph in the same
voice as Phases 173/174's: what was proven, with numbers; what was measured and found wanting; and
what is carried forward. Mark the phase `[x]` in the Phases list ONLY if all three requirements
closed; otherwise state the phase's status honestly, exactly as Phase 174's entry does.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `175-PROOF.md` has §§7, 8, 9 with the section titles named above.
    - §9 records `0 files differ` for both originals, or names every differing file if not.
    - `.planning/REQUIREMENTS.md` VERIFY-04/05/06 each carry either an `[x]` with a
      `175-PROOF.md §N`-shaped citation and a measured number, or a `[ ]` with a stated gap.
    - `grep -c "175-PROOF.md" .planning/REQUIREMENTS.md` ≥ 3.
    - `.planning/ROADMAP.md` Phase 175 lists eight plans and carries a `**Result:**` paragraph
      containing at least two measured numbers.
    - `npm test` full suite green; the test count is recorded in the summary against the 3706
      baseline.
    - `grep -ci "ran clean" .planning/phases/175-impact-map-repair-gating/175-PROOF.md` == 0.
  </acceptance_criteria>
  <done>The proof states what it proved and what it did not, is reproducible, confirms both originals untouched, and the requirements/roadmap reflect exactly that — with no requirement closed on evidence weaker than its citation claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proof narrative → requirement checkbox | A closed checkbox on structural-only evidence is the exact defect this milestone has hit five times |
| proof run → reference-game originals | The milestone's baseline; a mutated original invalidates every prior phase's proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-31 | Spoofing | The VERIFY-06 payoff number | mitigate | If `unknown-drift` dominates, the proof states the payoff was NOT demonstrated and cites `drift-check`'s real `counts`; any anchored copy is a separately labelled subsection naming what was synthesized |
| T-175-32 | Spoofing | Decision 13's waived path | mitigate | Labelled **LIVE** or **STRUCTURAL + CONSTRUCTED-FIXTURE**, never ambiguously; the measured `verified (user-waived)` count is recorded first |
| T-175-33 | Tampering | Reference-game originals | mitigate | Final recursive sha256 manifest comparison plus `seven`'s pinned commit re-confirmed |
| T-175-34 | Repudiation | Requirement closure | mitigate | Each `[x]` carries a `175-PROOF.md §N` citation with a measured number; STRUCTURAL-only evidence must say so inside the citation |
</threat_model>

<verification>
- `175-PROOF.md` §§1–9 complete, with measured counts throughout and no `ran clean`
- `npm test` full suite green, count recorded against the 3706 baseline
- `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` updated consistently with the proof
- Both `~/BoardSmithGames` originals byte-identical; `seven` still at
  `a03f38d4792af9dfc7c798be69686fc3230f54dd`
</verification>

<success_criteria>
- VERIFY-05's cross-file write is proven on both reference games with write order, authority, and
  cold-resume parse all measured.
- VERIFY-06's payoff is measured on the real stale sets with an honest verdict about the data's
  ability to exercise it.
- Decision 13's path is settled with its evidence class named.
- Requirements and roadmap reflect exactly what was proven, with citations.
</success_criteria>

<output>
Create `.planning/phases/175-impact-map-repair-gating/175-08-SUMMARY.md` when done
</output>
