---
phase: 176-stale-chunk-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/commands/build-manifest.ts
  - src/cli/commands/build-manifest.test.ts
  - src/cli/commands/verify-ruling-recheck.ts
  - src/cli/commands/verify-ruling-recheck.test.ts
autonomous: true
requirements: [CHECK-01]

must_haves:
  truths:
    - "Every RULINGS.md entry can be enumerated with its full Decision/Citation/Rationale body text, from the single existing ruling parser."
    - "A superseded ruling is excluded from re-validation, in both supersession directions, and an unparseable supersede sentence is reported rather than assumed."
    - "The ruling verdict set is exactly four values and `undetermined` is reachable, never collapsed."
    - "A missing or empty fresh staged transcription produces a scope-limited result, never a silent fall back to live rulebook/ slices."
  artifacts:
    - path: "src/cli/commands/build-manifest.ts"
      provides: "parseRulings additively widened with per-ruling body text"
      contains: "body"
    - path: "src/cli/commands/verify-ruling-recheck.ts"
      provides: "RULING_VERDICTS enum, enumeration, verdict record, scope-limited detection, verifyRulingRecheckCommand"
      exports: ["RULING_VERDICTS", "verifyRulingRecheckCommand"]
    - path: "src/cli/commands/verify-ruling-recheck.test.ts"
      provides: "CHECK-01 mechanical coverage"
  key_links:
    - from: "src/cli/commands/verify-ruling-recheck.ts"
      to: "src/cli/commands/build-manifest.ts"
      via: "import { parseRulings }"
      pattern: "parseRulings"
    - from: "src/cli/commands/verify-ruling-recheck.ts"
      to: "src/cli/commands/verify-run.ts"
      via: "import { atomicWriteFile }"
      pattern: "atomicWriteFile"
---

<objective>
Build CHECK-01's mechanical half: enumerate every `RULINGS.md` entry with its full body text (so a
judgment subagent can read it), record one of four enumerated verdicts through the single atomic
ledger write path, and report scope-limited when the fresh staged transcription is absent.

Purpose: the CLI enumerates and records; the subagent judges (176-CONTEXT.md decision 2). This plan
builds only the enumerate-and-record side. No judgment, no absence-detecting regex, no verdict
heuristics live in code (176-RESEARCH.md Pitfall 4).
Output: an additively-widened `parseRulings`, a new `verify-ruling-recheck.ts` command module, and
its test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/176-stale-chunk-repair/176-CONTEXT.md
@.planning/phases/176-stale-chunk-repair/176-RESEARCH.md
@.planning/phases/176-stale-chunk-repair/176-PATTERNS.md
</context>

<interfaces>
<!-- Extracted by the planner. Use these directly; do not go exploring for them. -->

Existing, `src/cli/commands/build-manifest.ts:284-290` — the exact shape to widen:
```typescript
export interface ParsedRuling {
  number: number;
  /** Set only for the explicit supersede verbs, direction-resolved. */
  supersededBy?: number;
  /** Supersede-verb sentences whose target number or direction could not be resolved. */
  unparsedSupersession: string[];
}
export function parseRulings(rulingsText: string): ParsedRuling[]
```
The loop already computes `const body = rulingsText.slice(h.bodyStart, bodyEnd);` at line 346.
The only other caller is `src/cli/commands/trace-check.ts:410-419`, which destructures
`{ supersededBy, unparsedSupersession }` and skips superseded rulings with
`if (ruling.supersededBy !== undefined) continue;`.

Existing enumerated-set precedent, `src/cli/commands/verify-impact.ts:931-938`:
```typescript
export const REPAIR_GATE_DISPOSITIONS = Object.freeze([
  'reopen-playtest', 'close-without-replaytest', 'unknown-drift', 'not-applicable',
] as const);
export type RepairGateDisposition = (typeof REPAIR_GATE_DISPOSITIONS)[number];
```

Existing single atomic write path, `src/cli/commands/verify-run.ts:293-309`:
```typescript
export async function atomicWriteFile(filePath: string, content: string): Promise<void>
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen parseRulings additively with per-ruling body text</name>
  <files>src/cli/commands/build-manifest.ts, src/cli/commands/build-manifest.test.ts</files>
  <read_first>
    - src/cli/commands/build-manifest.ts lines 284-378 (`ParsedRuling`, `SUPERSEDED_BY` regex at
      line 300, the heading-split loop, `const body = ...` at line 346)
    - src/cli/commands/trace-check.ts lines 400-440 (the only other caller; its destructuring and
      its `supersededBy !== undefined` skip)
    - src/cli/commands/build-manifest.test.ts (existing `parseRulings` describe block — match its
      fixture and assertion style)
    - .planning/phases/176-stale-chunk-repair/176-PATTERNS.md §`build-manifest.ts` (decision 18:
      additive widening, zero new regex, zero second scan pass)
  </read_first>
  <behavior>
    - `parseRulings` returns, per entry, a `body` string containing that ruling's full text after
      its `### Ruling N` heading and before the next heading (Decision / Citation / Rationale
      lines all present).
    - The last ruling's `body` runs to end-of-file.
    - Existing fields (`number`, `supersededBy`, `unparsedSupersession`) are unchanged for every
      existing test case — no existing assertion is edited.
    - A ruling whose own entry carries `SUPERSEDED BY RULING 9` still resolves
      `supersededBy === 9` AND now also exposes that sentence inside `body`.
  </behavior>
  <action>
    Add a `body: string` field to `ParsedRuling` and populate it from the `body` local the loop
    already computes — do not re-slice `rulingsText`, do not add a second `### Ruling (\d+)`
    regex (the grep gate 175-03 established stays in force). Confirm `trace-check.ts`'s
    destructuring is unaffected before committing.

    In `build-manifest.test.ts`, extend the existing `parseRulings` describe block with cases
    using the real-shaped fixture text quoted verbatim in `176-PATTERNS.md` for `seven`'s
    Ruling 3 (the direction-reversed `⚠ RATIONALE SUPERSEDED BY RULING 9` marker) plus a
    two-ruling fixture proving last-entry body termination. Add a repo-wide grep assertion (read
    the `src/cli` tree, count matches of a `Ruling\s*\(\\d\+\)`-style regex literal) proving
    exactly ONE such regex declaration still exists.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/cli/commands/build-manifest.test.ts` passes with the new cases and with
      every pre-existing `parseRulings` assertion unmodified (`git diff` shows additions only in
      that describe block).
    - A test asserts `parseRulings(text)[i].body` contains the substrings `Decision:`,
      `Citation interpreted or overridden:`, and `Rationale:` for a real-shaped ruling.
    - A test asserts the last ruling's `body` includes text after the final `Rationale:` line.
    - A grep test asserts exactly one `### Ruling (\d+)` regex declaration exists under `src/cli/`.
    - `npx vitest run src/cli/commands/trace-check.test.ts` passes unmodified.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/commands/build-manifest.test.ts src/cli/commands/trace-check.test.ts</automated>
  </verify>
  <done>`ParsedRuling.body` exists, is populated from the already-computed local, and no caller or existing assertion changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RULING_VERDICTS enum, ruling enumeration, and scope-limited detection</name>
  <files>src/cli/commands/verify-ruling-recheck.ts, src/cli/commands/verify-ruling-recheck.test.ts</files>
  <read_first>
    - src/cli/commands/verify-impact.ts lines 925-1054 (`REPAIR_GATE_DISPOSITIONS` frozen-array +
      derived-type + `ImpactMapEntry` shape)
    - src/cli/commands/verify-classify.ts lines 860-920 (`ChunkVerdict`, `CITATION_ANCHOR_RUNGS` —
      the enumerate-and-record record shape to mirror)
    - src/cli/commands/trace-check.ts lines 410-425 (the exact supersession skip condition to reuse)
    - .planning/phases/176-stale-chunk-repair/176-CONTEXT.md decisions 1, 2, 3, 9, 10
    - .planning/phases/176-stale-chunk-repair/176-RESEARCH.md Pitfall 4 (absence-of-source is
      subagent judgment; no CLI keyword list)
  </read_first>
  <behavior>
    - `RULING_VERDICTS` is exactly `['still-needed','resolved-by-source','contradicted','undetermined']`,
      frozen, with a derived type and a pinning test asserting the exact four-member array and order.
    - `enumerateRulingsForRecheck(rulingsText)` returns one entry per ruling with
      `supersededBy === undefined`, each carrying `number` and `body`; superseded rulings appear in a
      separate `skipped[]` naming the superseding ruling number.
    - Rulings whose supersede sentence could not be resolved are surfaced in an
      `unparsedSupersession[]` on the result and are NOT skipped (report, never assume — decision 3).
    - `resolveFreshTranscription(projectDir, runId?)` returns either resolved staged slice paths or a
      `scopeLimited` result naming the missing path; it never returns a `rulebook/` live-slice path
      under any input (decision 9/10).
    - Constructing a `RulingVerdictRecord` with a verdict outside `RULING_VERDICTS` throws; a record
      with an empty `reasoning` throws (decision 4 — the reasoning is the artifact, not just the label).
  </behavior>
  <action>
    Create `src/cli/commands/verify-ruling-recheck.ts`. Export `RULING_VERDICTS` (frozen array) and
    `RulingVerdict`; `RulingVerdictRecord` as `{ number, verdict, reasoning, supersededBy? }`;
    `enumerateRulingsForRecheck` importing `parseRulings` from `./build-manifest.js` — no second
    ruling parse path; `resolveFreshTranscription` returning a discriminated result whose
    scope-limited arm carries the missing path and a reason string, following PROV-02's
    report-honestly precedent.

    Do NOT implement any absence-phrase list, verdict heuristic, or prose scan that could produce or
    constrain a verdict in code — the verdict arrives from the subagent and is only validated
    against the enum here.

    Create `verify-ruling-recheck.test.ts` covering every behavior above, using inline fixture text
    matching the real ruling shape quoted in `176-PATTERNS.md`, plus a test asserting no string
    literal resembling an absence phrase (`never reproduces`, `entirely silent`, `absent from`)
    appears anywhere in `verify-ruling-recheck.ts`.
  </action>
  <acceptance_criteria>
    - `RULING_VERDICTS` pinning test asserts deep equality with the exact four-value array and that
      `Object.isFrozen` is true.
    - A test asserts `undetermined` is producible and is never rewritten to another value by any
      exported function.
    - A test asserts a ruling with `supersededBy` set is absent from the enumerated set and present
      in `skipped[]`, exercising the direction-reversed `SUPERSEDED BY RULING 9` shape.
    - A test asserts an unparseable supersede sentence yields an entry in `unparsedSupersession[]`
      AND the ruling still enumerates for re-check.
    - A test asserts `resolveFreshTranscription` on a project with no staged run returns
      `scopeLimited` with the missing path named, and that no returned path matches `/(^|\/)rulebook\//`.
    - A source assertion: reading `verify-ruling-recheck.ts` and stripping comments yields zero
      matches for the absence-phrase list.
    - `npx vitest run src/cli/commands/verify-ruling-recheck.test.ts` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-ruling-recheck.test.ts</automated>
  </verify>
  <done>The enum, enumeration, supersession skip/report split, and scope-limited resolution all exist and are pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: verifyRulingRecheckCommand — recording verdicts through the one atomic write path</name>
  <files>src/cli/commands/verify-ruling-recheck.ts, src/cli/commands/verify-ruling-recheck.test.ts</files>
  <read_first>
    - src/cli/commands/verify-run.ts lines 270-315 (`atomicWriteFile` and the comment naming it the
      ONE atomic ledger write path in the repo)
    - src/cli/commands/verify-impact.ts (`verifyImpactStatusCommand`'s `--project`/`--json` option
      handling and its report-only exit-code discipline)
    - .planning/phases/176-stale-chunk-repair/176-CONTEXT.md decisions 14, 16, and the "Claude's
      Discretion" note on report volume
    - 176-PATTERNS.md §Shared Patterns → "Atomic, single-write-path ledger persistence"
  </read_first>
  <behavior>
    - `verifyRulingRecheckCommand({ project?, runId?, json? })` reads `RULINGS.md`, enumerates,
      and reports one row per ruling: number, verdict (or `pending` when not yet judged),
      reasoning, and the skipped/unparsed sets — uncapped, never truncated (decision 15's
      no-implicit-sampling discipline applied to report volume).
    - A recording entry point persists `RulingVerdictRecord`s by calling `atomicWriteFile`; no
      `fs.writeFile`/`writeFileSync` appears anywhere in the module.
    - Findings exit 0. Tool failure (missing project, unreadable `RULINGS.md`) exits non-zero with
      a single actionable line naming the directory and `--project`, no stack frame, no `.ts:`
      reference, no repo `src/` path (172 decision 6's message discipline).
    - The command writes nothing into `RULINGS.md` itself (decision 16 — this phase reports).
  </behavior>
  <action>
    Implement the command and its recording function in the same module. Import `atomicWriteFile`
    from `./verify-run.js`. Structure the `--json` payload so the skill can format without
    recomputing anything (PROV-03's compute/format split): include per-verdict counts as a
    `verdictCounts` map derived in the CLI, so decision 14's measured distribution never has to be
    counted by hand in prose.
  </action>
  <acceptance_criteria>
    - A test asserts the module source contains zero occurrences of `fs.writeFile`,
      `writeFileSync`, or `fsPromises.writeFile`, and at least one call to `atomicWriteFile`.
    - A test runs the command against a temp project fixture and asserts a whole-directory sha256
      map of `RULINGS.md` is byte-identical before and after (no ruling text written).
    - A test asserts `--json` output carries `verdictCounts` whose values sum to the number of
      enumerated (non-skipped) rulings.
    - A test asserts a report with 30+ rulings emits 30+ rows (no cap, no ellipsis).
    - A test asserts a non-bs-project run exits non-zero with a message containing `--project` and
      matching neither `/\.ts:\d/` nor `/\bsrc\//`.
    - `npx vitest run src/cli/commands/verify-ruling-recheck.test.ts` green; `npm test` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-ruling-recheck.test.ts && npm test</automated>
  </verify>
  <done>CHECK-01's enumerate-and-record mechanics are complete, atomic-write-only, and read-only against RULINGS.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → CLI parser | `RULINGS.md` / staged slice text read from a local bs-project |
| CLI → subagent dispatch | ruling body text forwarded into a fresh-context judgment prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-01 | Tampering | `RULINGS.md` writes | mitigate | This module is read-only against `RULINGS.md`; pinned by a byte-identical sha256 test (Task 3). |
| T-176-02 | Information Disclosure | error messages | mitigate | Tool-failure messages carry no stack frame, `.ts:` reference, or repo path (172 decision 6, pinned in Task 3). |
| T-176-03 | Denial of Service | torn/partial verdict record | mitigate | All durable writes route through `atomicWriteFile` (temp + fsync + rename), the single hardened path from 173-08/CR-01. |
| T-176-SC | Tampering | npm/pip/cargo installs | accept | This phase installs no external packages (176-RESEARCH.md § Package Legitimacy Audit — not applicable). |
</threat_model>

<verification>
- `npx vitest run src/cli/commands/build-manifest.test.ts src/cli/commands/trace-check.test.ts src/cli/commands/verify-ruling-recheck.test.ts`
- `npm test` (full suite; baseline 3826 — any new failure is this plan's regression)
- `npx tsc --noEmit` (the pre-existing `docs/seed-to-state.test.ts` rootDir error is the only permitted error)
</verification>

<success_criteria>
- `ParsedRuling.body` exists, populated from the already-computed local, zero new ruling regex.
- `RULING_VERDICTS` is a frozen four-member set with a pinning test; `undetermined` reachable.
- Superseded rulings are skipped in both directions; unparseable chains are reported, not assumed.
- No absence-detection or verdict heuristic exists in CLI code (source-asserted).
- Missing fresh transcription reports scope-limited; no live-slice path can be returned.
- All durable writes route through `atomicWriteFile`; `RULINGS.md` is byte-identical after a run.
</success_criteria>

<output>
Create `.planning/phases/176-stale-chunk-repair/176-01-SUMMARY.md` when done
</output>
