---
phase: 176-stale-chunk-repair
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/commands/verify-repair.ts
  - src/cli/commands/verify-repair.test.ts
  - src/cli/commands/verify-impact.test.ts
autonomous: true
requirements: [CHECK-02]

must_haves:
  truths:
    - "A stale chunk's cited live slices resolve to their FRESH STAGED counterparts, correctly under the real m:n fan-out (seven: 3 live → 6 staged)."
    - "Only chunks with `stale === true` in the impact map enter the lens loop; no other chunk is dispatched."
    - "A verify episode opens a fresh 3-round budget, appends after the chunk's existing build-era rounds without renumbering them, and labels the episode boundary legibly in CHUNK.md."
    - "The repair gate is re-derived from POST-repair code state; a pre-repair gate can never be reported as final."
    - "A missing fresh staged transcription produces a scope-limited result, never a live-slice substitution."
  artifacts:
    - path: "src/cli/commands/verify-repair.ts"
      provides: "staged-slice resolution, episode round bookkeeping, post-repair gate re-derivation"
      exports: ["resolveStagedSlicePaths", "planVerifyEpisodeRound", "recomputeRepairGatePostRepair"]
    - path: "src/cli/commands/verify-repair.test.ts"
      provides: "CHECK-02 mechanical coverage against the committed 175 fixtures"
  key_links:
    - from: "src/cli/commands/verify-repair.ts"
      to: "src/cli/commands/verify-impact.ts"
      via: "import { computeRepairGate }"
      pattern: "computeRepairGate"
    - from: "src/cli/commands/verify-repair.ts"
      to: "src/cli/commands/verify-classify.ts"
      via: "ChunkVerdict.pairIds / attributions consumed, never re-paired"
      pattern: "pairIds"
---

<objective>
Build CHECK-02's mechanical core: turn a stale chunk into the fresh staged slice paths a lens must
read, plan its verify-episode audit round without rewriting build history, and re-derive the repair
gate AFTER repair has had a chance to move code.

Purpose: the lenses and the repair loop already exist as skill text and are reused verbatim
(decisions 6 and 8). The only genuinely new mechanism is the glue: which paths, which round, and
which gate reading is authoritative.
Output: `verify-repair.ts` + its tests, plus post-repair re-invocation coverage in
`verify-impact.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/176-stale-chunk-repair/176-CONTEXT.md
@.planning/phases/176-stale-chunk-repair/176-RESEARCH.md
@.planning/phases/176-stale-chunk-repair/176-PATTERNS.md
@.planning/phases/175-impact-map-repair-gating/175-VERIFICATION.md
</context>

<interfaces>
<!-- Extracted by the planner. Consume these; do not re-derive their facts. -->

`src/cli/commands/verify-impact.ts:1042-1053` — this plan's `--json` input contract:
```typescript
export interface ImpactMapEntry {
  slug: string;
  ruleDelta: string;
  stale: boolean;
  status: string;
  driftState: 'clean' | 'drifted' | 'unknown';
  changedFiles: string[];
  missingFiles: string[];
  attributions: ChunkVerdict['attributions'];
  gate: RepairGate;
  markerState: 'clear' | 'rules-stale' | 'unknown';
}
```

`src/cli/commands/verify-impact.ts:977-1039` — pure, total, safe to call again; branch order is
load-bearing (`unknown` FIRST):
```typescript
export function computeRepairGate(input: {
  status: string; stale: boolean; driftState: 'clean' | 'drifted' | 'unknown';
}): RepairGate
```

`src/cli/commands/verify-classify.ts:881-894` — the pairing already computed by Phase 174:
```typescript
export interface ChunkVerdict {
  slug: string; citedLiveSlices: string[]; pairIds: string[];
  ruleDelta: RuleDelta; stale: boolean;
  attributions: Array<{ pairId: string; liveSlice: string; rung: CitationAnchorRung;
                        attributed: boolean; reason: string }>;
}
```
The run's own `RUN.md` classification records carry `"liveSlices":[...]` / `"stagedSlices":[...]`
keyed by `pairId` — that is the lookup table, not a filename heuristic.

Real committed fixture (decision 19 — no re-transcription in scope):
`.planning/phases/175-impact-map-repair-gating/175-FIXTURES/174-07-contradictory/staged/{seven,one-two-punch}/`
containing `RUN.md` and `slices/` with full-rulebook coverage for both games.

Real `### Audit Round` heading precedents (all three chunks are already AT round 3 from build):
- `~/BoardSmithGames/seven/chunks/table-and-draw/CHUNK.md:319` — ``### Audit Round 3 (final round — `state-machine.md` "Repair Loop Bound": max 3)``
- `~/BoardSmithGames/one-two-punch/chunks/block/CHUNK.md:883` — `### Audit Round 3 (FINAL — the round bound is 3; state-machine.md "Repair Loop Bound")`
- `~/BoardSmithGames/one-two-punch/chunks/jab/CHUNK.md:822` — `### Audit Round 3 (the last permitted — ...)`
All match `### Audit Round \d+` with an optional trailing parenthetical.

Inbound carried-forward item, routed here by `175-08-SUMMARY.md` / `175-PROOF.md` §7: the
`lineFindings[]` multi-delta persistence gap — a `ClassificationRecord` retains only the
max-severity `quotedPass1`/`quotedPass2` pair per pair group, not one pair per line finding. Neither
reference game has a multi-delta pair, so it cannot be exercised live. This plan's input contract
must not silently assume one quote pair per delta; where a resolution depends on the retained quote,
say so in the summary rather than papering over it.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve a stale chunk to its fresh staged slice paths</name>
  <files>src/cli/commands/verify-repair.ts, src/cli/commands/verify-repair.test.ts</files>
  <read_first>
    - src/cli/commands/verify-impact.ts lines 925-1054 (`ImpactMapEntry`, `RepairGate`,
      `REPAIR_GATE_DISPOSITIONS`)
    - src/cli/commands/verify-classify.ts lines 270-300 (`pairSlices` export) and 860-920
      (`ChunkVerdict.attributions`, `CITATION_ANCHOR_RUNGS`)
    - .planning/phases/175-impact-map-repair-gating/175-FIXTURES/174-07-contradictory/staged/seven/RUN.md
      (the real `liveSlices[]`/`stagedSlices[]` classification records)
    - .planning/phases/176-stale-chunk-repair/176-CONTEXT.md decisions 5, 9, 10
    - .planning/phases/176-stale-chunk-repair/176-RESEARCH.md § Don't Hand-Roll (no second pairing
      algorithm) and Pitfall 5 (committed fixture is authoritative; ignore OS scratch)
  </read_first>
  <behavior>
    - `resolveStagedSlicePaths(entry, runRecords)` maps each `entry.pairIds` value to the
      `stagedSlices[]` recorded for that `pairId`, de-duplicated and order-stable.
    - Real m:n fan-out holds: `seven`'s 3 live rule slices resolve to its 6 staged files from the
      committed fixture; `one-two-punch`'s live slices resolve to its staged set.
    - A `pairId` with no matching classification record yields a scope-limited result naming the
      unresolved pairId — never an empty-but-successful path list.
    - No returned path matches `/(^|\/)rulebook\/[^.]/` outside a `.verify/` staging prefix
      (decision 9 — staged only, never live).
    - `selectStaleChunks(entries)` returns only `stale === true` entries (decision 5).
  </behavior>
  <action>
    Create `src/cli/commands/verify-repair.ts`. Implement `selectStaleChunks` and
    `resolveStagedSlicePaths` consuming `ImpactMapEntry` plus the run's parsed classification
    records — import the ledger parse helpers already exported from `verify-run.ts`; do not add a
    second RUN.md reader and do not re-run `pairSlices()` to re-derive grouping.

    Represent scope-limited as a discriminated result arm carrying the reason and the specific
    missing/unresolved identifier, following PROV-02's report-honestly precedent (decision 10) —
    there must be no code path that substitutes a `rulebook/` live path when staging is absent.

    Create `verify-repair.test.ts` and drive these cases from the COMMITTED fixture at
    `175-FIXTURES/174-07-contradictory/staged/`, copied into a temp dir per test; never read
    `${TMPDIR}174-07-proof/`.
  </action>
  <acceptance_criteria>
    - A test asserts `seven`'s staged resolution returns exactly the fixture's 6 staged slice
      filenames (asserted against a `readdir` of the fixture, not a hardcoded list of one).
    - A test asserts an unmatched `pairId` yields the scope-limited arm naming that pairId, and
      that the success arm is not returned.
    - A test asserts every resolved path contains `/.verify/` and none matches
      `/(^|\/)rulebook\/\d/` (live-slice shape).
    - A test asserts `selectStaleChunks` on a mixed entry list returns only the stale ones.
    - `npx vitest run src/cli/commands/verify-repair.test.ts` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-repair.test.ts</automated>
  </verify>
  <done>A stale chunk deterministically resolves to fresh staged paths, or reports scope-limited — never to a live slice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify-episode round bookkeeping (decision 17) — append-only, legibly bounded</name>
  <files>src/cli/commands/verify-repair.ts, src/cli/commands/verify-repair.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/build/repair.md lines 1-94 (the bounded loop, `Maximum 3 audit
      rounds per chunk`, fix/refute-with-citation, round-3 triage's three plain-language options)
    - src/cli/slash-command/bs/build/audit.md lines 126-145 (the persist-before-repair rule and
      the cold-resume rule for a partial `### Audit Round N` entry)
    - src/cli/slash-command/bs/state-machine.md § "Write Order" (`Round entries ... are
      **append-only** — never overwritten or renumbered`) and § "Repair Loop Bound"
    - .planning/phases/176-stale-chunk-repair/176-CONTEXT.md decision 17 (per-verify-episode bound;
      both consequences)
    - .planning/phases/176-stale-chunk-repair/176-PATTERNS.md § decision 17's heading precedents
      (the three real `### Audit Round 3 (...)` headings quoted with line anchors) and the
      `seven/chunks/table-and-draw/CHUNK.md:202-330` round/repair alternation
  </read_first>
  <behavior>
    - `parseAuditRounds(chunkMd)` returns every existing `### Audit Round N` heading with its number
      and its trailing parenthetical, matching all three real precedent shapes.
    - `planVerifyEpisodeRound(chunkMd, episode)` returns the next absolute round number
      (`max(existing) + 1`) AND the episode-relative round number, with a heading of the form
      `### Audit Round {absolute} (verify-repair episode {E}, round {R} of 3)`.
    - A chunk with three build-era rounds gets absolute round 4 / episode round 1 — it is NOT routed
      to triage on its first verify dispatch (decision 17's whole point).
    - The episode budget is exhausted at episode round 3 regardless of absolute number; the fourth
      episode-round request returns a `triage` disposition instead of a heading.
    - Appending is append-only: the produced document's prefix up to the insertion point is
      byte-identical to the input, and every pre-existing round heading string survives unchanged.
  </behavior>
  <action>
    Implement `parseAuditRounds`, `planVerifyEpisodeRound`, and an append function that writes the
    new round entry through `atomicWriteFile` (imported from `./verify-run.js` — the ONE write
    path). Derive the episode number from the count of existing episode-labelled headings already
    present, so a resumed verify pass continues its own episode rather than opening a new one.

    Do not restate `build/repair.md`'s fix/refute/triage semantics in code comments or output
    strings — cite the file by name (decision 8; the skill-text side is plan 176-03's job).

    Test using CHUNK.md fixture text carrying the three real precedent headings verbatim (quoted in
    `176-PATTERNS.md`), plus a zero-round chunk.
  </action>
  <acceptance_criteria>
    - A test asserts `parseAuditRounds` finds 3 rounds in a fixture built from the real
      `table-and-draw` / `block` / `jab` heading shapes, including the trailing parentheticals.
    - A test asserts a 3-build-round chunk's first verify round is absolute 4, episode 1, round 1,
      and its heading matches `/^### Audit Round 4 \(verify-repair episode 1, round 1 of 3\)$/m`.
    - A test asserts the 4th episode-round request returns the `triage` disposition and produces no
      heading.
    - A test asserts append-only: `output.startsWith(input.trimEnd())` (or an explicit prefix
      equality) and that every original heading string is still present exactly once.
    - A test asserts the module contains no `fs.writeFile`/`writeFileSync` and calls
      `atomicWriteFile`.
    - `npx vitest run src/cli/commands/verify-repair.test.ts -t "episode"` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-repair.test.ts</automated>
  </verify>
  <done>A verify episode gets a fresh 3-round budget, is legible in CHUNK.md, and never renumbers build history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-derive the repair gate AFTER repair (Pitfall 1)</name>
  <files>src/cli/commands/verify-repair.ts, src/cli/commands/verify-repair.test.ts, src/cli/commands/verify-impact.test.ts</files>
  <read_first>
    - src/cli/commands/verify-impact.ts lines 977-1039 (`computeRepairGate`, blindness-before-
      narrowing branch order) and its `verifyImpactStatusCommand` composition
    - src/cli/commands/drift-check.ts (the single authority for "did this chunk's code move")
    - .planning/phases/176-stale-chunk-repair/176-RESEARCH.md Pitfall 1 (the pre-repair snapshot is
      stale the instant repair finishes) and Assumption A3 (second invocation safety — untested by
      research; this task tests it)
    - .planning/phases/175-impact-map-repair-gating/175-VERIFICATION.md (VERIFY-06's accepted
      NOT-DEMONSTRATED payoff limitation this plan's gate feeds)
  </read_first>
  <behavior>
    - `recomputeRepairGatePostRepair({ projectDir, slug, stale, status })` re-runs drift detection
      against the CURRENT working tree and calls `computeRepairGate` with that freshly-derived
      `driftState` — it accepts no caller-supplied `driftState` and no pre-computed `RepairGate`.
    - A chunk whose gate read `close-without-replaytest` before repair, and whose manifest files are
      then modified, re-reads as `reopen-playtest`.
    - A chunk left byte-identical by repair keeps its pre-repair disposition on re-derivation
      (proving the re-invocation is not gratuitously destabilising).
    - `driftState === 'unknown'` still short-circuits first on the second invocation.
    - Calling `computeRepairGate` twice with identical input returns deeply-equal results and
      performs no I/O (Assumption A3, now tested).
  </behavior>
  <action>
    Implement `recomputeRepairGatePostRepair` in `verify-repair.ts`, importing `computeRepairGate`
    from `./verify-impact.js` — never reimplementing its ladder. Make the pre-repair gate
    structurally unavailable to it: its parameter type must not include a `gate` or `driftState`
    field, so no caller can pass Step 4's snapshot through.

    In `verify-repair.test.ts`, build a real two-commit git fixture project (mirroring
    `verify-impact.test.ts`'s existing real-git fixture) where a chunk's manifest file is modified
    between the two gate readings, and assert the disposition flip.

    In `verify-impact.test.ts`, add the idempotence/purity cases for a second `computeRepairGate`
    invocation.
  </action>
  <acceptance_criteria>
    - A test asserts the clean→drifted flip: first reading `close-without-replaytest`, second
      reading (after a real file modification in a real git fixture) `reopen-playtest`.
    - A test asserts the unchanged case preserves the disposition.
    - A test asserts `unknown` short-circuits on re-invocation.
    - A type-level or source assertion proves `recomputeRepairGatePostRepair`'s parameter object has
      no `driftState`/`gate` member (e.g. `npx tsc --noEmit` fails on a deliberate fixture, or a
      source regex asserts the parameter destructuring names neither).
    - A test asserts two `computeRepairGate` calls with equal input are deeply equal.
    - `npx vitest run src/cli/commands/verify-repair.test.ts src/cli/commands/verify-impact.test.ts` green; `npm test` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-repair.test.ts src/cli/commands/verify-impact.test.ts && npm test</automated>
  </verify>
  <done>The gate that reaches a report is always derived from post-repair code state; the pre-repair snapshot cannot be passed in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → CLI | staged slice tree, `RUN.md` ledger, and chunk `CHUNK.md` read from a local bs-project |
| CLI → chunk artifact write | verify-episode round entries appended into `CHUNK.md` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-04 | Tampering | `CHUNK.md` round history | mitigate | Append-only assertion (prefix byte-equality + every original heading intact), Task 2. |
| T-176-05 | Repudiation | gate disposition reported from a stale snapshot | mitigate | `recomputeRepairGatePostRepair` structurally rejects a caller-supplied `driftState`/`gate`, Task 3. |
| T-176-06 | Denial of Service | torn `CHUNK.md` on crash mid-append | mitigate | Writes route through `atomicWriteFile` only. |
| T-176-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this phase. |
</threat_model>

<verification>
- `npx vitest run src/cli/commands/verify-repair.test.ts src/cli/commands/verify-impact.test.ts`
- `npm test` (full suite)
- Confirm both `~/BoardSmithGames` originals are byte-identical (whole-tree sha256 diff empty) —
  every fixture in this plan is a temp copy of a COMMITTED fixture, never the live games.
</verification>

<success_criteria>
- Stale-chunk → staged-slice resolution correct under the real m:n fan-out, from committed fixtures.
- Only `stale === true` entries are selectable for dispatch.
- Verify episodes get a fresh 3-round budget with a legible, append-only heading.
- Post-repair gate re-derivation proven with a real disposition flip; pre-repair snapshot unpassable.
- No second pairing algorithm, no second RUN.md reader, no second write path.
</success_criteria>

<output>
Create `.planning/phases/176-stale-chunk-repair/176-02-SUMMARY.md` when done
</output>
