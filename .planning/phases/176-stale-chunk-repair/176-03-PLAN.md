---
phase: 176-stale-chunk-repair
plan: 03
type: execute
wave: 2
depends_on: ["176-01", "176-02"]
files_modified:
  - src/cli/slash-command/bs/verify/ruling-recheck.md
  - src/cli/slash-command/bs/verify/repair-dispatch.md
  - src/cli/cli.ts
  - src/cli/commands/install-claude-command.ts
  - src/cli/commands/install-claude-command.test.ts
autonomous: true
requirements: [CHECK-01, CHECK-02]

must_haves:
  truths:
    - "A live session has a written judgment contract that returns exactly one of four ruling verdicts plus reasoning, and that recognises a source-absence citation."
    - "A live session has a written repair-dispatch route that points at build/audit.md's three lens templates and build/repair.md's bounded loop BY REFERENCE, with no second copy of their prose."
    - "Both new commands are reachable from the real CLI surface with --project/--json, matching the existing verify-* registration shape."
    - "Both new skill files physically ship on a real local install."
  artifacts:
    - path: "src/cli/slash-command/bs/verify/ruling-recheck.md"
      provides: "CHECK-01 judgment contract and dispatch prose"
    - path: "src/cli/slash-command/bs/verify/repair-dispatch.md"
      provides: "CHECK-02 orchestration: lens dispatch by reference, episode rounds, scope-limited, post-repair gate re-invocation"
    - path: "src/cli/cli.ts"
      provides: "verify-ruling-recheck and verify-repair command registration"
  key_links:
    - from: "src/cli/slash-command/bs/verify/repair-dispatch.md"
      to: "src/cli/slash-command/bs/build/audit.md"
      via: "${CLAUDE_SKILL_DIR}/../bs-shared/build/audit.md reference"
      pattern: "build/audit.md"
    - from: "src/cli/slash-command/bs/verify/repair-dispatch.md"
      to: "src/cli/slash-command/bs/build/repair.md"
      via: "${CLAUDE_SKILL_DIR}/../bs-shared/build/repair.md reference"
      pattern: "build/repair.md"
    - from: "src/cli/commands/install-claude-command.ts"
      to: "verify/ruling-recheck.md, verify/repair-dispatch.md"
      via: "SHARED_LEAF_PROBES entries"
      pattern: "SHARED_LEAF_PROBES"
---

<objective>
Write the two skill-text files a live session actually follows, and make both new commands reachable.

Purpose: plans 01 and 02 built mechanics. This plan builds the judgment contract (CHECK-01) and the
orchestration route (CHECK-02) — the latter delegating to `build/audit.md` and `build/repair.md`
BY REFERENCE, because a forked lens is a lens that can drift from the pipeline the phase goal says
it must be identical to (decisions 6 and 8).
Output: `verify/ruling-recheck.md`, `verify/repair-dispatch.md`, CLI registration, installer probes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/176-stale-chunk-repair/176-CONTEXT.md
@.planning/phases/176-stale-chunk-repair/176-RESEARCH.md
@.planning/phases/176-stale-chunk-repair/176-PATTERNS.md
@.planning/phases/176-stale-chunk-repair/176-01-SUMMARY.md
@.planning/phases/176-stale-chunk-repair/176-02-SUMMARY.md
</context>

<interfaces>
<!-- The ONLY substitution points in build/audit.md's three lens templates (176-PATTERNS.md,
     confirmed by direct read of audit.md:54-108). Every other word is fixed prose, reused
     unchanged: -->

    {gameName}, {slug}, {slicePaths}, {codeFilePaths}, {visibilityDeclarationText}

- fidelity lens (audit.md:54-66) — `{gameName}`, `{slug}`, `{slicePaths}`, `{codeFilePaths}`
- visibility lens (audit.md:68-94) — the above plus `{visibilityDeclarationText}`; dispatches
  `diffPlayerViews(testGame, seatA, seatB)` (`src/testing/view-diff.ts`) and
  `assertNoHiddenInfoLeak(...)` (`src/testing/dom-leak.ts`)
- undo lens (audit.md:96-108) — `{gameName}`, `{slug}`, `{codeFilePaths}` only (code-only check)
- 4th design-review lens (audit.md:45-48) — dispatched for `ui: touches|major` chunks via
  `build/design-review.md`; findings land in the SAME `## Findings Ledger` (decision 7: applied
  exactly as written, no new verify-side policy)

For CHECK-02, `{slicePaths}` = the FRESH STAGED slice paths resolved by plan 176-02's
`resolveStagedSlicePaths` (decision 9), never the live `rulebook/` paths.

Existing delegation style to mirror: `src/cli/slash-command/bs/verify/classification-dispatch.md`
and `verify/staging-dispatch.md` (both point at `ingest/transcription-subagent.md` and never paste
its body). Existing judgment-contract style to mirror:
`src/cli/slash-command/bs/verify/classification-subagent.md`.

Existing CLI registration shape (`src/cli/cli.ts:285-345`): `.command(...)` / `.description(...)` /
`.option('--project <dir>', ...)` / `.option('--json', ...)` / `.action(...)`.
Installer probe list: `src/cli/commands/install-claude-command.ts:67-76` (`SHARED_LEAF_PROBES`,
already carrying `verify/classification-subagent.md` and `verify/adjudication-gate.md`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: verify/ruling-recheck.md — CHECK-01's one judgment contract</name>
  <files>src/cli/slash-command/bs/verify/ruling-recheck.md</files>
  <read_first>
    - src/cli/slash-command/bs/verify/classification-subagent.md (the analog: handshake, decision
      procedure with worked examples per label, enumerated RETURN shape)
    - src/cli/slash-command/bs/verify/classification-dispatch.md (enumerate → dispatch → record →
      close, ledger-only resume)
    - src/cli/slash-command/bs/state-machine.md § "Rulings Outrank Rulebook", § "Write Order"
    - .planning/phases/176-stale-chunk-repair/176-CONTEXT.md decisions 1, 2, 3, 4, 9, 10, 16
    - .planning/phases/176-stale-chunk-repair/176-RESEARCH.md Pitfall 4 and its verbatim quote of
      `seven`'s Ruling 1 Citation field
    - .planning/phases/176-stale-chunk-repair/176-01-SUMMARY.md (the exported enum and command
      names this file must cite)
  </read_first>
  <action>
    Create `src/cli/slash-command/bs/verify/ruling-recheck.md` as CHECK-01's dispatch route plus
    judgment contract, in `classification-subagent.md`'s register.

    It must specify: a versioned handshake token (mirroring `BS-CLASSIFY-V1`'s role) so a return
    can be recognised; that the subagent reads (a) the ruling's full body text supplied by the
    orchestrator from `parseRulings` and (b) the FRESH STAGED transcription only — never the live
    `rulebook/` slices (decision 9); that it returns exactly ONE of `still-needed`,
    `resolved-by-source`, `contradicted`, `undetermined` plus a `reasoning` field that is never
    empty; and that the orchestrator records the return through the `verify-ruling-recheck` command
    and never re-reads a ruling or a slice itself (decision 2).

    Include the absence-of-source decision procedure IN THE PROMPT (decision 4, Pitfall 4): when a
    ruling's own Citation field asserts that the source does not contain the material, a
    non-`still-needed` verdict is only available if the fresh transcription now actually contains
    that previously-absent content. Give two worked examples per verdict label, and name the two
    plausible wrong answers for the absence case (`contradicted` — "I could not find it"; and
    `resolved-by-source` — "no gap detected") and why each is wrong.

    State that a superseded ruling is not dispatched (decision 3), that an unparseable supersede
    sentence is reported rather than assumed, and that `undetermined` is the honest answer when the
    comparison genuinely cannot be made — never a fallback for a hard case (decision 1's
    first-class-blindness principle).

    State the scope-limited rule (decision 10): if the fresh transcription is unavailable, report
    scope-limited following PROV-02's pattern; there is no fall back to live slices.

    State decision 16's boundary explicitly: this route REPORTS verdicts; it never edits
    `RULINGS.md` and never fixes reference-game content.

    Cite `state-machine.md` sections by name; restate nothing from them. Do not paste any lens
    template text into this file.
  </action>
  <acceptance_criteria>
    - File exists and contains all four verdict strings verbatim, and a RETURN-shape block naming
      `verdict` and `reasoning`.
    - File contains the phrase identifying staged-only input and does NOT instruct reading
      `rulebook/` live slices (a grep for a live-slice read instruction returns nothing).
    - File names both wrong answers for the absence case and why each is wrong.
    - File contains a `state-machine.md` citation and no restatement of the Status enum.
    - File contains no substring from `build/audit.md`'s lens templates (pinned in plan 176-04).
    - `npx vitest run src/cli/slash-command/bs/verify.test.ts` still green (pre-existing pins,
      including the `--apply`/`promote`/`cutover` forbidden-word guard, must not trip).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/verify.test.ts</automated>
  </verify>
  <done>CHECK-01's judgment contract exists as skill text, staged-only, absence-aware, four verdicts, reasoning mandatory.</done>
</task>

<task type="auto">
  <name>Task 2: verify/repair-dispatch.md — CHECK-02's route, delegating by reference</name>
  <files>src/cli/slash-command/bs/verify/repair-dispatch.md</files>
  <read_first>
    - src/cli/slash-command/bs/build/audit.md (full — the three lens templates at 54-108, the
      4th-lens rule at 45-48, the persist-before-repair and cold-resume rules at 126-145)
    - src/cli/slash-command/bs/build/repair.md (full — fix/refute-with-citation, `Maximum 3 audit
      rounds per chunk`, round-3 triage's three plain-language options at 78-84)
    - src/cli/slash-command/bs/verify/classification-dispatch.md (the pointer-not-restatement style)
    - src/cli/slash-command/bs/state-machine.md § "Write Order", § "Repair Loop Bound",
      § "Rules Staleness Marker", § "Session Handoff Seams"
    - .planning/phases/176-stale-chunk-repair/176-CONTEXT.md decisions 5, 6, 7, 8, 9, 10, 11, 12, 17
    - .planning/phases/176-stale-chunk-repair/176-02-SUMMARY.md (the command/function names this
      file cites: staged-path resolution, episode round planning, post-repair gate re-derivation)
  </read_first>
  <action>
    Create `src/cli/slash-command/bs/verify/repair-dispatch.md`. It orchestrates, per stale chunk
    only (decision 5): resolve staged slice paths → dispatch `build/audit.md`'s three lenses (plus
    the 4th for `ui: touches|major`, decision 7) → append the round entry → dispatch
    `build/repair.md`'s bounded loop → re-derive the repair gate.

    Delegate by REFERENCE using the `${CLAUDE_SKILL_DIR}/../bs-shared/build/audit.md` and
    `.../build/repair.md` paths — the same style `classification-dispatch.md` uses for
    `ingest/transcription-subagent.md`. Do NOT paste any lens template body or any repair-loop
    prose into this file. Name the five substitution points explicitly (`{gameName}`, `{slug}`,
    `{slicePaths}`, `{codeFilePaths}`, `{visibilityDeclarationText}`) and state that `{slicePaths}`
    is bound to the FRESH STAGED paths (decision 9) and that nothing else in the templates changes
    (decision 6).

    Any verify-specific framing belongs HERE, never injected into a lens prompt — say so in the
    file, since that is the exact anti-pattern decision 6 forbids.

    State decision 11: `## Interpretation` remains forbidden to the lenses, and is MORE important
    for a rules-stale chunk because that section was written against rules that have since moved.

    State decision 17's episode rule: a verify pass opens a fresh 3-round budget; rounds are
    appended after the chunk's existing build-era rounds with an episode-labelled heading, never
    renumbering history (cite `state-machine.md` "Write Order" as the authority). Name the four
    real chunks that already carry three build-era rounds as the reason the rule exists.

    State decision 12's seam explicitly: repair MAY change code, and a chunk whose code changed
    during repair re-opens the human playtest gate while a chunk that passes the lenses unchanged
    closes without re-playtesting — and that the gate is RE-DERIVED after the loop closes, never
    read from Step 4's pre-repair snapshot (Pitfall 1).

    State decision 10's scope-limited rule and decision 16's report-don't-fix boundary.

    Cite `build/repair.md`'s round-3 triage rather than restating its three options.
  </action>
  <acceptance_criteria>
    - File contains `${CLAUDE_SKILL_DIR}/../bs-shared/build/audit.md` and
      `${CLAUDE_SKILL_DIR}/../bs-shared/build/repair.md`.
    - File contains all five substitution tokens verbatim.
    - File contains NONE of these marker phrases from the lens templates (pinned in 176-04):
      `you are checking the CODE against the RAW SOURCE`,
      `These raw sources, NOT the Visibility Declaration`.
    - File contains no verbatim copy of `build/repair.md`'s `Maximum 3 audit rounds per chunk`
      sentence as its own policy statement — it cites the file instead.
    - File states the post-repair re-derivation rule and names the pre-repair snapshot as
      non-authoritative.
    - File names the four already-at-3-rounds chunks (`best-seven-selection`, `table-and-draw`,
      `block`, `jab`).
    - `npx vitest run src/cli/slash-command/bs/verify.test.ts` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/verify.test.ts</automated>
  </verify>
  <done>CHECK-02's route exists, delegates by reference, binds staged paths, and encodes the episode and post-repair-gate rules.</done>
</task>

<task type="auto">
  <name>Task 3: Register both commands and ship both skill files</name>
  <files>src/cli/cli.ts, src/cli/commands/install-claude-command.ts, src/cli/commands/install-claude-command.test.ts</files>
  <read_first>
    - src/cli/cli.ts lines 33-38 (verify-impact import block) and 285-345 (the `verify-classify-*`
      registration blocks — the exact chain shape to copy)
    - src/cli/commands/install-claude-command.ts lines 60-110 (`SHARED_LEAF_PROBES`, `SHARED_ROOT`,
      and how probes are joined against `targetDir`)
    - src/cli/commands/install-claude-command.test.ts (existing probe assertions)
    - .planning/phases/176-stale-chunk-repair/176-01-SUMMARY.md and 176-02-SUMMARY.md (exported
      command function names)
  </read_first>
  <action>
    Register `verify-ruling-recheck` and `verify-repair` in `cli.ts` beside the existing
    `verify-impact-*` block, importing from `./commands/verify-ruling-recheck.js` and
    `./commands/verify-repair.js`. Each takes `--project <dir>` and `--json` as baseline options
    plus any run-scoping option the command implements; no `--force`, `--skip`, `--yes`, or any
    bypass option, matching the no-bypass discipline held since Phase 175.

    Add `verify/ruling-recheck.md` and `verify/repair-dispatch.md` to `SHARED_LEAF_PROBES`, and
    extend `install-claude-command.test.ts` with a REAL local install into a scratch directory
    asserting both files physically land under `bs-shared/verify/` (the same proof 175-05 ran for
    `adjudication-gate.md`).
  </action>
  <acceptance_criteria>
    - `node bin/boardsmith.js verify-ruling-recheck --help` and
      `node bin/boardsmith.js verify-repair --help` both exit 0 and list `--project` and `--json`.
    - A source assertion: neither new registration block contains `force`, `skip`, `yes`, or
      `bypass` as an option name.
    - A test performs a real install into a temp dir and asserts both new `.md` files exist at
      their `bs-shared/verify/` paths.
    - `npx vitest run src/cli/commands/install-claude-command.test.ts` green; `npm test` green;
      `npx tsc --noEmit` clean apart from the pre-existing `docs/seed-to-state.test.ts` rootDir error.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/commands/install-claude-command.test.ts && node bin/boardsmith.js verify-repair --help && npm test</automated>
  </verify>
  <done>Both commands are on the real CLI surface and both skill files physically ship.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill text → fresh-context subagent | dispatch prompts carrying ruling bodies and slice paths |
| installer → user's `.claude/skills/` tree | skill files written into a user-level directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-07 | Tampering | forked lens prose drifting from `build/audit.md` | mitigate | Delegation by reference only; forked-copy drift guard pinned in plan 176-04. |
| T-176-08 | Elevation of Privilege | a bypass option skipping the repair gate or the round bound | mitigate | Source assertion: no `force`/`skip`/`yes`/`bypass` option on either new command (Task 3). |
| T-176-09 | Spoofing | an unrecognised subagent return being recorded as a verdict | mitigate | Versioned handshake token in `ruling-recheck.md`; the CLI validates against `RULING_VERDICTS` (plan 176-01). |
| T-176-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this phase. |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/verify.test.ts src/cli/commands/install-claude-command.test.ts`
- `node bin/boardsmith.js verify-ruling-recheck --help`, `node bin/boardsmith.js verify-repair --help`
- `npm test`, `npx tsc --noEmit`
</verification>

<success_criteria>
- Both skill files exist, delegate by reference, and carry no copied lens or repair-loop prose.
- Staged-only input, absence-aware judgment, four verdicts with mandatory reasoning.
- Episode round rule, post-repair gate re-derivation, scope-limited, and report-don't-fix all stated.
- Both commands registered with `--project`/`--json` and no bypass option.
- A real install proves both files ship.
</success_criteria>

<output>
Create `.planning/phases/176-stale-chunk-repair/176-03-SUMMARY.md` when done
</output>
