---
phase: 176-stale-chunk-repair
plan: 04
type: execute
wave: 3
depends_on: ["176-03"]
files_modified:
  - src/cli/slash-command/bs/verify-game.md
  - src/cli/slash-command/bs/verify.test.ts
autonomous: true
requirements: [CHECK-01, CHECK-02]

must_haves:
  truths:
    - "`verify-game.md` contains no statement this phase makes false — including stale claims nobody listed in advance."
    - "A reader of `verify-game.md` can reach the repair route and the ruling re-check route from the router itself."
    - "No file under `bs/verify/` contains a forked copy of `build/audit.md`'s lens templates or `build/repair.md`'s loop prose."
    - "The old boundary sentences are provably ABSENT, not merely contradicted by newer text nearby."
  artifacts:
    - path: "src/cli/slash-command/bs/verify-game.md"
      provides: "router with repair routing and swept boundary claims"
    - path: "src/cli/slash-command/bs/verify.test.ts"
      provides: "no-fork drift guards, absence pins, routing pins"
  key_links:
    - from: "src/cli/slash-command/bs/verify-game.md"
      to: "verify/repair-dispatch.md"
      via: "step dispatch + Reference Files bullet"
      pattern: "verify/repair-dispatch.md"
    - from: "src/cli/slash-command/bs/verify.test.ts"
      to: "src/cli/slash-command/bs/build/audit.md"
      via: "lens-template marker phrases read from the real file"
      pattern: "build/audit.md"
---

<objective>
Rewrite `verify-game.md` in place so nothing in it is false after this phase lands, sweep it for
stale cross-file claims beyond the two already named, and pin the whole result with drift guards —
including the no-fork guard that keeps CHECK-02's lenses identical to the build pipeline's.

Purpose: this is a KNOWN RECURRING DEFECT CLASS. Phases 173, 174, and 175 each had to rewrite this
same file's boundary statements, and 175 additionally found a THIRD stale claim (a hardcoded item
count) that nobody had listed in advance. Fixing only the two named lines would repeat that miss.
Output: a swept, re-pinned `verify-game.md` and an extended `verify.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/176-stale-chunk-repair/176-CONTEXT.md
@.planning/phases/176-stale-chunk-repair/176-PATTERNS.md
@.planning/phases/176-stale-chunk-repair/176-03-SUMMARY.md
</context>

<interfaces>
<!-- The two boundary statements already known to become false (176-PATTERNS.md, verbatim): -->

`verify-game.md` line 15:
> "... It never runs a build and never edits a chunk's design."

`verify-game.md` line 109 (Step 4's closing sentence):
> "Finally the stale fraction and each chunk's repair-gate disposition (`reopen-playtest`,
> `close-without-replaytest`, or `unknown-drift`) are reported. ... **Performing the repair itself
> is Phase 176's job — this step decides only which chunks need it.**"

`verify-game.md` lines 20-21 are STILL TRUE and must be verified to remain true, not changed:
> "... There is no flag or path anywhere in this skill that writes staged output into a live
> location."

Current structure: Steps 0,1,2,3,4,5 (`## Step 5: Close (VERIFY-02)` at line 112), then
`## Reference Files` (line 129) with five `verify/` bullets plus the shared-file bullets.

Known third-claim candidate (176-PATTERNS.md item 3): line 109 enumerates THREE repair-gate
dispositions but `REPAIR_GATE_DISPOSITIONS` has FOUR (`not-applicable` omitted) — verify against
`verifyImpactStatusCommand`'s actual reporting behaviour before rewriting; it may be a deliberate
exclusion or a second stale claim.

Existing drift-guard style to mirror: `verify.test.ts:243-292` (`CONTRACT_BODY_MARKERS` no-fork
loop over every `bs/verify/*.md`) and `:477+` (cross-file lexicon pin reading the real constant
declaration out of the source file rather than re-typing it).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Sweep verify-game.md for ALL stale cross-file claims and add repair routing</name>
  <files>src/cli/slash-command/bs/verify-game.md</files>
  <read_first>
    - src/cli/slash-command/bs/verify-game.md (FULL — every line; this task is a sweep, not a
      two-line patch)
    - src/cli/slash-command/bs/verify/repair-dispatch.md and verify/ruling-recheck.md (created in
      plan 176-03 — the routes this file must dispatch to)
    - src/cli/commands/verify-impact.ts (`REPAIR_GATE_DISPOSITIONS` and
      `verifyImpactStatusCommand`'s actual reporting behaviour — to settle whether line 109's
      three-value enumeration is a deliberate exclusion or a second stale claim)
    - .planning/phases/176-stale-chunk-repair/176-PATTERNS.md § `verify-game.md` (items 1-5, with
      line anchors)
    - .planning/phases/175-impact-map-repair-gating/175-05-SUMMARY.md (how 175 handled the same
      class: delete in place, drop the hardcoded count rather than increment it, pin the negative)
  </read_first>
  <action>
    First SWEEP, then edit. Enumerate every claim in the file that asserts what this skill does not
    do, how many of something there are, or which phase owns a piece of work — including the step
    count, any "five reference files"-style count, every `Phase 17x` forward reference, and the
    repair-gate disposition enumeration. Record the enumeration in the summary so the next phase
    inherits the list rather than rediscovering it.

    Then rewrite IN PLACE (delete the false text, do not append a correction beside it):
    - Line 15: narrow precisely. "Does not run the BUILD pipeline's investigate/redteam/ask/build
      steps for a new chunk" stays true; "never edits a chunk's design" becomes false because
      repair MAY change an existing chunk's already-built code (decision 12). Do not delete the
      "does not rebuild the project" framing wholesale.
    - Line 109: replace the Phase-176 forward reference with a real pointer to the new step. Step 4
      genuinely still only decides WHICH chunks need repair — the NEXT step performs it — so the
      sentence is rewritten, not deleted.
    - Settle the three-vs-four disposition enumeration against the command's real behaviour and fix
      it if it is stale; if the omission of `not-applicable` is deliberate, say so explicitly so the
      next reader does not re-flag it.

    Add the repair routing as a new numbered step dispatching
    `${CLAUDE_SKILL_DIR}/../bs-shared/verify/repair-dispatch.md`, and route CHECK-01's ruling
    re-check to `verify/ruling-recheck.md` (either as its own step or inside the repair step —
    pick one and make the file internally consistent). Renumber Close accordingly and update EVERY
    cross-reference to a renumbered step. Add both new files to `## Reference Files` in the exact
    existing one-line bullet style.

    Verify lines 20-21 remain true after the addition (staged output is lens INPUT, never promoted
    to a live location) and leave them byte-identical.

    Follow 175's discipline: where a hardcoded count would need incrementing, prefer DROPPING the
    count and pinning that no hardcoded count reappears.
  </action>
  <acceptance_criteria>
    - `grep -c "never edits a chunk's design" verify-game.md` returns 0.
    - `grep -c "Performing the repair itself is Phase 176's job" verify-game.md` returns 0.
    - `grep -n "Phase 17" verify-game.md` returns only references that are still true (or nothing).
    - `grep -c "verify/repair-dispatch.md" verify-game.md` >= 2 (step dispatch + Reference Files).
    - `grep -c "verify/ruling-recheck.md" verify-game.md` >= 2.
    - Step headings are contiguous and 0-indexed with no gap or duplicate:
      `grep -oE "^## Step [0-9]+" verify-game.md` yields a strictly increasing sequence from 0.
    - Lines 20-21's "no flag or path ... writes staged output into a live location" sentence is
      byte-identical to its pre-edit form (`git diff` shows it untouched).
    - The summary records the full swept claim inventory, not just the two named lines.
    - `npx vitest run src/cli/slash-command/bs/verify.test.ts` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/verify.test.ts</automated>
  </verify>
  <done>Nothing in verify-game.md is false after this phase, the sweep inventory is recorded, and both new routes are reachable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drift guards — no forked lens or repair prose, and the old boundary text is provably gone</name>
  <files>src/cli/slash-command/bs/verify.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/verify.test.ts lines 243-300 (the `CONTRACT_BODY_MARKERS` no-fork
      loop and the forbidden-word guard) and 465-512 (the SC-4 prose gate and the cross-file
      lexicon pin that reads a real constant declaration out of source)
    - src/cli/slash-command/bs/build/audit.md lines 45-145 (the three lens templates and the
      4th-lens rule — the source the marker phrases are read FROM)
    - src/cli/slash-command/bs/build/repair.md lines 1-94 (the bounded loop prose)
    - .planning/phases/176-stale-chunk-repair/176-PATTERNS.md § Shared Patterns → "Structural drift
      guards (grep-style lexicon pins) over prose trust"
    - .planning/phases/175-impact-map-repair-gating/175-05-SUMMARY.md (the requirement that a
      negative pin be EMPIRICALLY PROVEN to fail before it is trusted)
  </read_first>
  <behavior>
    - No file under `src/cli/slash-command/bs/verify/` contains any lens-template marker phrase.
      The marker phrases are READ OUT OF `build/audit.md` at test time (not re-typed), so the pin
      cannot silently rot if the template is reworded.
    - No file under `bs/verify/` restates `build/repair.md`'s bound sentence or its three triage
      option labels as its own policy.
    - `repair-dispatch.md` DOES contain the two delegation paths (positive pin) — a no-fork guard
      that passes because the delegation was deleted would be worthless.
    - `verify-game.md` does NOT contain either retired boundary sentence (absence pins).
    - `verify-game.md` DOES contain a step dispatching `verify/repair-dispatch.md` and a route to
      `verify/ruling-recheck.md`.
    - No hardcoded step/reference-file count reappears in `verify-game.md`.
    - `## Interpretation` is not instructed to be read anywhere under `bs/verify/` (decision 11).
  </behavior>
  <action>
    Extend `verify.test.ts` with a new describe block per behavior above, mirroring the existing
    `CONTRACT_BODY_MARKERS` loop's structure. Derive the lens marker phrases by reading
    `build/audit.md` and extracting 2-3 stable sentences from the fidelity and visibility templates
    programmatically (e.g. locating the template fence and slicing a known sentence), so the guard
    tracks the real file. If programmatic extraction proves brittle, fall back to literal marker
    constants BUT add a companion assertion that each literal is still present in `build/audit.md`
    — so a reworded template fails loudly instead of silently disarming the guard.

    Then EMPIRICALLY PROVE the negative pins actually fail: temporarily paste a lens template
    sentence into `repair-dispatch.md`, run the suite, observe the no-fork assertion fail; revert.
    Do the same for one retired boundary sentence in `verify-game.md`. Record both proofs in the
    summary — a negative pin that has never been observed to fail is not evidence.
  </action>
  <acceptance_criteria>
    - A test asserts, for every `bs/verify/*.md`, absence of each lens marker phrase; the phrases
      are sourced from `build/audit.md` at test time (or literal + presence-in-audit.md companion
      assertion).
    - A test asserts `repair-dispatch.md` contains both `build/audit.md` and `build/repair.md`
      delegation paths.
    - A test asserts `verify-game.md` does not contain `never edits a chunk's design` nor
      `Performing the repair itself is Phase 176's job`.
    - A test asserts `verify-game.md` contains a `^## Step \d+:` heading whose body dispatches
      `verify/repair-dispatch.md`.
    - A test asserts no `/\b(five|six|seven|eight|nine)[- ]step/i` and no hardcoded reference-file
      count reappears in `verify-game.md`.
    - A test asserts no file under `bs/verify/` instructs reading `## Interpretation`.
    - The summary records the two empirical pin-failure proofs (what was temporarily edited, the
      exact assertion message observed, and confirmation of revert).
    - `npm test` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/verify.test.ts && npm test</automated>
  </verify>
  <done>The no-fork guard, the absence pins, and the routing pins all exist and have been observed to fail when violated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill text → live session behaviour | a false boundary statement silently misroutes a real pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-10 | Repudiation | stale boundary claims in `verify-game.md` | mitigate | In-place rewrite + absence pins proven to fail when violated (Task 2). |
| T-176-11 | Tampering | forked lens prose drifting from the build pipeline | mitigate | No-fork guard sourcing marker phrases from `build/audit.md` itself. |
| T-176-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this phase. |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/verify.test.ts`
- `npm test`
- Manual grep sweep results recorded in the summary (the full stale-claim inventory)
</verification>

<success_criteria>
- Both named boundary statements are gone, rewritten in place, not appended around.
- The sweep inventory covers claims beyond the two named, with the three-vs-four disposition
  enumeration settled against the command's real behaviour.
- Repair and ruling-recheck routes are reachable from the router and listed in Reference Files.
- Step numbering is contiguous; no hardcoded count reintroduced.
- No forked lens or repair-loop prose exists anywhere under `bs/verify/`, pinned and proven.
</success_criteria>

<output>
Create `.planning/phases/176-stale-chunk-repair/176-04-SUMMARY.md` when done
</output>
