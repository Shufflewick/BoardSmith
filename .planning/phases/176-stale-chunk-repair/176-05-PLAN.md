---
phase: 176-stale-chunk-repair
plan: 05
type: execute
wave: 4
depends_on: ["176-03", "176-04"]
files_modified:
  - .planning/phases/176-stale-chunk-repair/176-PROOF.md
autonomous: true
requirements: [CHECK-01]

must_haves:
  truths:
    - "All ~62 rulings across both reference games are re-validated against the fresh staged transcription, with full measured counts per verdict — not a sample."
    - "`seven`'s Ruling 1 verdicts `still-needed`, and its REASONING is recorded, not just the label."
    - "Superseded rulings are demonstrably skipped on real data, including the direction-reversed Ruling 3 case."
    - "Both `~/BoardSmithGames` originals are byte-identical before and after the entire proof run."
    - "The dispatch mechanism actually used is stated honestly."
  artifacts:
    - path: ".planning/phases/176-stale-chunk-repair/176-PROOF.md"
      provides: "CHECK-01's live evidence: full-corpus verdict distribution + SC-3"
      contains: "still-needed"
  key_links:
    - from: "176-PROOF.md"
      to: "verify-ruling-recheck --json"
      via: "measured verdictCounts pasted from real command output"
      pattern: "verdictCounts"
---

<objective>
Run CHECK-01 for real: re-validate every `RULINGS.md` entry in both reference games against the
committed fresh staged transcription, and prove SC-3 on `seven`'s Ruling 1.

Purpose: decision 14 requires the FULL corpus, because a verdict distribution across a real corpus
is the only evidence the classifier is neither over- nor under-flagging — a subset cannot show that.
Decision 4/SC-3 requires the reasoning behind Ruling 1's verdict, because two wrong answers are both
plausible-looking and one of them would invite deleting `seven`'s only scoring authority.
Output: `176-PROOF.md` sections 1-3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/176-stale-chunk-repair/176-CONTEXT.md
@.planning/phases/176-stale-chunk-repair/176-RESEARCH.md
@.planning/phases/176-stale-chunk-repair/176-01-SUMMARY.md
@.planning/phases/176-stale-chunk-repair/176-03-SUMMARY.md
@.planning/phases/173-verify-pipeline-core/173-PROOF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-corpus ruling re-validation on both reference games</name>
  <files>.planning/phases/176-stale-chunk-repair/176-PROOF.md</files>
  <read_first>
    - .planning/phases/173-verify-pipeline-core/173-PROOF.md (the established proof structure:
      numbered sections, real command transcripts, "What is still unproven", "How to re-run every
      proof", and the byte-identical-originals section; also §6, where the native-dispatch caveat
      was closed for SC-3 under the Agent tool)
    - .planning/phases/176-stale-chunk-repair/176-CONTEXT.md decisions 9, 13, 14, 16, 19 and the
      "Claude's Discretion" note on report volume
    - .planning/phases/176-stale-chunk-repair/176-RESEARCH.md Pitfall 5 (committed fixture is
      authoritative; the 323MB OS-scratch copy is not) and § Environment Availability
    - .planning/phases/176-stale-chunk-repair/176-03-SUMMARY.md (the handshake token and RETURN
      shape `ruling-recheck.md` defines)
  </read_first>
  <action>
    Work against `cp -R` copies of `~/BoardSmithGames/seven` and `~/BoardSmithGames/one-two-punch`
    in a scratch directory — never the live games. Record each original's whole-tree sha256 BEFORE
    starting.

    Install the skills locally into each copy (real install, not a symlink assumption). Point the
    fresh-transcription input at the COMMITTED fixture at
    `.planning/phases/175-impact-map-repair-gating/175-FIXTURES/174-07-contradictory/staged/{seven,one-two-punch}/slices/`
    (decision 19 — no re-transcription is in scope). Do not read `${TMPDIR}174-07-proof/`.

    Enumerate rulings with the real `verify-ruling-recheck` command, then dispatch ONE fresh-context
    judgment subagent per non-superseded ruling following `verify/ruling-recheck.md`'s contract, and
    record each returned verdict + reasoning through the real command. Every ruling in both games is
    covered — 36 + 26 = 62 per research's direct count; if the real count differs, report the real
    number and reconcile it, never adjust the claim silently.

    Write `176-PROOF.md` §1 (setup, fixture provenance, sha256 baselines) and §2 (full-corpus
    results): per game, the measured `verdictCounts` pasted from real `--json` output, the count of
    skipped-superseded rulings, and the count and content of any `unparsedSupersession` entries.
    Show the numbers add up: enumerated + skipped = total parsed.

    Report the dispatch mechanism honestly — name whether each dispatch went through a `claude -p`
    OS subprocess or the native Task/Agent tool, and cite `173-PROOF.md` §6 as the precedent for
    how that distinction has been recorded before. Do not imply native dispatch if a subprocess was
    used.

    Per decision 16, REPORT findings; change nothing in either reference game's content.
  </action>
  <acceptance_criteria>
    - `176-PROOF.md` §2 shows, per game, a verdict count for every one of the four verdicts (zeros
      written explicitly, never omitted), and the counts sum to the enumerated ruling total.
    - The total parsed rulings per game is stated and reconciled against research's 36 / 26.
    - Every ruling is accounted for as enumerated or skipped-superseded; no ruling is unlisted.
    - `seven`'s Ruling 3 appears in the skipped set with `supersededBy: 9` (the direction-reversed
      case), demonstrated on real data.
    - Any `unparsedSupersession` sentence is quoted verbatim in the proof, not summarised away.
    - The dispatch mechanism is named explicitly in §1.
    - Both originals' whole-tree sha256 diffs are empty at the end of the task, recorded in the proof.
    - `npm test` still green (this task changes no source).
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
    <human-check>176-PROOF.md §2 shows full per-verdict counts for both games with no ruling unaccounted for</human-check>
  </verify>
  <done>All ~62 rulings re-validated with measured per-verdict counts, supersession demonstrated on real data, originals untouched.</done>
</task>

<task type="auto">
  <name>Task 2: SC-3 — seven's Ruling 1, verdict AND reasoning</name>
  <files>.planning/phases/176-stale-chunk-repair/176-PROOF.md</files>
  <read_first>
    - .planning/phases/176-stale-chunk-repair/176-CONTEXT.md decision 4 (verbatim: why
      `contradicted` and `resolved-by-source` are both plausible and both catastrophic)
    - .planning/phases/176-stale-chunk-repair/176-RESEARCH.md § Code Examples → the verbatim
      Ruling 1 text, and Pitfall 4 (absence detection is subagent judgment, never a CLI rule)
    - `~/BoardSmithGames/seven/RULINGS.md` Ruling 1 and `~/BoardSmithGames/seven/rulebook/INDEX.md`
      § "Open Rules Gaps" item 1 (the prose-to-prose cross-file corroboration — READ ONLY)
    - .planning/phases/174-verify-classifier/174-PROOF.md (how a prior phase declared its bar in a
      commit BEFORE the verdict existed)
  </read_first>
  <action>
    Declare SC-3's bar in `176-PROOF.md` §3 and COMMIT that declaration before running the
    dispatch, following 174's precedent of committing the bar before the result exists: the
    expected verdict is `still-needed`, and the reasoning must independently identify that the
    ruling's own Citation asserts a source absence AND that the fresh staged transcription still
    does not contain the card's face.

    Then run the real dispatch against the committed fresh staged transcription and record, verbatim:
    the returned verdict, the returned reasoning text in full, and the specific staged slice(s) the
    subagent read. Quote the ruling's Citation field verbatim alongside so a reader can check the
    reasoning against the evidence without opening another file.

    If the returned verdict is NOT `still-needed`, report that honestly as a measured failure of the
    bar — do not re-dispatch until a preferred answer appears, and do not tune the prompt after
    seeing the result without recording that the prompt was changed and why.

    Additionally: verify the CLI took no part in producing the verdict. Assert (with a real grep,
    result pasted into the proof) that no absence-phrase list exists in
    `verify-ruling-recheck.ts` — the judgment was the subagent's, per Pitfall 4.
  </action>
  <acceptance_criteria>
    - §3 contains the SC-3 bar declaration with its own commit hash cited, and that commit
      predates the dispatch record.
    - §3 records the verdict verbatim and the FULL reasoning text — not a paraphrase, not a label
      alone.
    - §3 names the staged slice paths the subagent read, and each is under a `.verify/` staging
      path (no live `rulebook/` slice).
    - §3 quotes Ruling 1's Citation field verbatim.
    - §3 pastes the real grep output proving no absence-phrase list exists in the CLI module.
    - §3 states whether the bar was MET or NOT MET in those words.
    - `~/BoardSmithGames/seven` is byte-identical (whole-tree sha256 diff empty) after this task.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
    <human-check>176-PROOF.md §3 records verdict + full reasoning + read slice paths, and states MET or NOT MET</human-check>
  </verify>
  <done>SC-3 is proven or honestly reported as unmet, with the reasoning recorded and the judgment demonstrably not CLI-produced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proof run → reference-game repos | real command runs against `cp -R` copies of read-only pinned repos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-12 | Tampering | `~/BoardSmithGames` originals | mitigate | All runs against `cp -R` copies; whole-tree sha256 diff recorded before and after both tasks. |
| T-176-13 | Repudiation | a tuned or re-rolled verdict presented as a first result | mitigate | Bar declared and committed before dispatch; any prompt change after seeing a result is recorded. |
| T-176-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this phase. |
</threat_model>

<verification>
- `npm test` (unchanged source; confirms no accidental edit)
- Whole-tree `sha256` diff of both `~/BoardSmithGames` originals: empty before and after
- `176-PROOF.md` §1-§3 present with measured counts, not prose summaries
</verification>

<success_criteria>
- Full ~62-ruling corpus re-validated, per-verdict counts measured and reconciled.
- Supersession skip demonstrated on the real direction-reversed case.
- SC-3's verdict AND full reasoning recorded, with the bar declared beforehand in a prior commit.
- Dispatch mechanism stated honestly.
- Both originals byte-identical; nothing in either reference game fixed (decision 16).
</success_criteria>

<output>
Create `.planning/phases/176-stale-chunk-repair/176-05-SUMMARY.md` when done
</output>
