---
phase: 176-stale-chunk-repair
plan: 06
type: execute
wave: 5
depends_on: ["176-05"]
files_modified:
  - .planning/phases/176-stale-chunk-repair/176-PROOF.md
  - .planning/phases/176-stale-chunk-repair/176-VALIDATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CHECK-01, CHECK-02]

must_haves:
  truths:
    - "The three audit lenses run for real against real stale chunks using the fresh staged transcription, and real findings are recorded."
    - "Lens coverage is stated explicitly — exactly which of the 12 stale chunks were audited and which were NOT."
    - "At least one of the four chunks already carrying 3 build-era audit rounds is in the subset, proving decision 17's fresh episode budget on real data."
    - "The repair gate is re-derived POST-repair on a real chunk, and the reading is compared against the pre-repair snapshot."
    - "The phase's requirements are closed (or honestly left open) with section-and-number citations."
  artifacts:
    - path: ".planning/phases/176-stale-chunk-repair/176-PROOF.md"
      provides: "CHECK-02's live evidence + phase-wide unproven list + re-run instructions"
      contains: "coverage"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CHECK-01/CHECK-02 disposition with citations"
  key_links:
    - from: "176-PROOF.md"
      to: "verify-impact-status --json"
      via: "pre-repair vs post-repair gate readings pasted verbatim"
      pattern: "reopen-playtest|close-without-replaytest"
---

<objective>
Run CHECK-02 for real on a measured subset of the 12 stale chunks, prove the post-repair gate
re-derivation on live data, and close the phase.

Purpose: decision 15 accepts a subset (3 lenses × 12 chunks = 36 dispatches is genuinely expensive)
but demands the coverage be STATED — an unstated sample reads as full coverage. Decision 17's
per-episode round budget only becomes real when demonstrated on a chunk that already has three
build-era rounds. Pitfall 1 only becomes real when a pre-repair and a post-repair gate reading are
put side by side.
Output: `176-PROOF.md` §4-§7 and the phase closeout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/176-stale-chunk-repair/176-CONTEXT.md
@.planning/phases/176-stale-chunk-repair/176-RESEARCH.md
@.planning/phases/176-stale-chunk-repair/176-VALIDATION.md
@.planning/phases/176-stale-chunk-repair/176-02-SUMMARY.md
@.planning/phases/176-stale-chunk-repair/176-03-SUMMARY.md
@.planning/phases/176-stale-chunk-repair/176-05-SUMMARY.md
@.planning/phases/175-impact-map-repair-gating/175-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real lens run on a stated subset of the 12 stale chunks</name>
  <files>.planning/phases/176-stale-chunk-repair/176-PROOF.md</files>
  <read_first>
    - .planning/phases/176-stale-chunk-repair/176-CONTEXT.md decisions 5, 6, 7, 9, 10, 11, 15, 16, 17
    - .planning/phases/176-stale-chunk-repair/176-PATTERNS.md § decision 17's heading precedents
      (the four chunks already at 3 rounds, with line anchors) and the `table-and-draw`
      round/repair alternation to model the entry shape on
    - src/cli/slash-command/bs/verify/repair-dispatch.md (created in 176-03 — the route being exercised)
    - src/cli/slash-command/bs/build/audit.md (the templates being dispatched, unchanged)
    - .planning/phases/175-impact-map-repair-gating/175-PROOF.md (the real stale sets: `seven` 6/16,
      `one-two-punch` 6/11)
  </read_first>
  <action>
    Work against fresh `cp -R` copies of both reference games; record whole-tree sha256 of both
    originals before starting.

    Enumerate the real stale set with `verify-impact-status --json` and paste the full stale slug
    list into the proof — all 12, uncapped. THEN choose the audit subset and state it explicitly:
    a table of every stale chunk with an AUDITED / NOT AUDITED column and the reason for each
    exclusion. The subset MUST include at least one of `best-seven-selection`, `table-and-draw`,
    `block`, `jab` (the four already at three build-era rounds).

    For each audited chunk: resolve the fresh staged slice paths (never live slices), dispatch
    `build/audit.md`'s three lens templates with only the five substitution points bound (plus the
    4th design-review lens if and only if the chunk is `ui: touches|major`, decision 7), and append
    the verify-episode round entry to the chunk COPY's `## Findings Ledger` using the
    episode-labelled heading. Record the real finding counts per lens per chunk — a clean run here
    would be more suspicious than a noisy one, and per decision 16 nothing gets fixed in
    reference-game content.

    Record, for the already-at-3-rounds chunk: the pre-existing round headings verbatim, the new
    episode heading, and a diff-shaped demonstration that no existing heading was renumbered.

    State the dispatch mechanism honestly (subprocess vs native Task tool), as in plan 176-05.

    If the fresh staged transcription is missing or insufficient for any chunk, record SCOPE-LIMITED
    for that chunk with the missing path named — never substitute a live slice.
  </action>
  <acceptance_criteria>
    - §4 lists all 12 real stale slugs from real `--json` output, uncapped.
    - §4 contains an AUDITED / NOT AUDITED table covering all 12, each exclusion carrying a reason.
    - §4 states, in words, the coverage fraction (e.g. "N of 12 audited; the remaining M were not").
    - At least one of the four already-at-3-rounds chunks appears as AUDITED.
    - Per audited chunk, a per-lens finding count is recorded (zeros written explicitly).
    - The lens dispatch record shows only the five substitution points varying; the template prose
      is not reproduced in the proof as a modified copy.
    - The already-at-3-rounds chunk's original `### Audit Round 1/2/3` headings are shown intact
      alongside the new episode heading.
    - The 4th lens was dispatched if and only if the chunk's `ui:` value is `touches` or `major` —
      stated per chunk.
    - Both originals' sha256 diffs empty after the task.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
    <human-check>176-PROOF.md §4 states lens coverage explicitly with an AUDITED/NOT AUDITED row for every one of the 12 stale chunks</human-check>
  </verify>
  <done>Real lens findings recorded on a stated subset, with the episode-round rule demonstrated on a chunk that had exhausted its build budget.</done>
</task>

<task type="auto">
  <name>Task 2: Post-repair gate re-derivation on live data (Pitfall 1)</name>
  <files>.planning/phases/176-stale-chunk-repair/176-PROOF.md</files>
  <read_first>
    - .planning/phases/176-stale-chunk-repair/176-RESEARCH.md Pitfall 1 and Assumption A3
    - .planning/phases/176-stale-chunk-repair/176-02-SUMMARY.md
      (`recomputeRepairGatePostRepair`'s real signature and behaviour)
    - .planning/phases/175-impact-map-repair-gating/175-VERIFICATION.md and
      `175-PROOF.md` §5 (VERIFY-06's honestly NOT-DEMONSTRATED payoff: 11 of 12 chunks re-open the
      gate because their code genuinely drifted for unrelated reasons)
  </read_first>
  <action>
    For at least one audited chunk whose repair loop changed code, and one whose repair loop changed
    nothing, record BOTH gate readings side by side in `176-PROOF.md` §5: the pre-repair
    `ImpactMapEntry.gate.disposition` captured before dispatch, and the post-repair reading from a
    fresh re-derivation after the loop closed. Show the disposition for each, and state whether it
    changed.

    Then connect this to VERIFY-06 honestly: state how many audited chunks closed without
    re-playtesting versus re-opened the gate, and compare against 175's measured 1-of-12 baseline.
    Do not present a small saving as a payoff; 175 already recorded that the payoff is NOT
    demonstrated on this data, and this phase inherits that finding rather than overturning it with
    a smaller sample.

    Explicitly note whether the pre-repair snapshot, had it been trusted, would have produced a
    different (wrong) disposition for any chunk — that is the concrete evidence Pitfall 1 is real
    rather than theoretical. If it would NOT have differed for any chunk in the subset, say so; a
    non-difference honestly reported is still evidence.
  </action>
  <acceptance_criteria>
    - §5 shows, for at least two chunks, a pre-repair and a post-repair gate disposition, each
      pasted from real command output.
    - §5 states for each whether the disposition changed.
    - §5 states the audited-subset counts of `reopen-playtest` vs `close-without-replaytest` vs
      `unknown-drift` vs `not-applicable`, with zeros written explicitly.
    - §5 explicitly answers whether trusting the pre-repair snapshot would have been wrong for any
      chunk, including the "no difference observed" case if that is the result.
    - §5 cites `175-PROOF.md` §5's 1-of-12 baseline and does not claim a payoff beyond it.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
    <human-check>176-PROOF.md §5 puts pre-repair and post-repair gate readings side by side and answers whether the snapshot would have misled</human-check>
  </verify>
  <done>Pitfall 1 is answered with real paired readings, and VERIFY-06's inherited limitation is restated, not overturned.</done>
</task>

<task type="auto">
  <name>Task 3: Phase closeout — unproven list, re-run instructions, requirement dispositions</name>
  <files>.planning/phases/176-stale-chunk-repair/176-PROOF.md, .planning/phases/176-stale-chunk-repair/176-VALIDATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/173-verify-pipeline-core/173-PROOF.md § "What is still unproven" and
      § "How to re-run every proof" (the required closing structure)
    - .planning/phases/176-stale-chunk-repair/176-VALIDATION.md (the sign-off checklist to complete)
    - .planning/REQUIREMENTS.md lines 200-210 and the coverage table near line 267 (CHECK-01,
      CHECK-02 rows)
    - .planning/ROADMAP.md § Phase 176 (its three Success Criteria, verbatim)
    - .planning/phases/175-impact-map-repair-gating/175-08-SUMMARY.md (how 175 wrote its
      `**Result:**` paragraph naming every measured number)
  </read_first>
  <action>
    Complete `176-PROOF.md` with § "What is still unproven" (carry forward at minimum: the
    unaudited stale chunks from §4's coverage table, the dispatch-mechanism caveat as actually
    observed, the anchor-density inheritance from 174, VERIFY-06's undemonstrated payoff, and any
    scope-limited chunk) and § "How to re-run every proof" with literal commands.

    Mark ROADMAP Phase 176 with a `**Result:**` paragraph naming every measured number — ruling
    verdict counts per game, lens coverage fraction, per-lens finding counts, and the gate
    dispositions — and update the plan checklist.

    Update `REQUIREMENTS.md`: close CHECK-01 and CHECK-02 with `176-PROOF.md` section-and-number
    citations ONLY if their own text is actually satisfied by measured evidence. If either is not,
    leave it OPEN and say why — this milestone has now caught three premature completion marks, and
    a fourth would be worse than an open requirement.

    Sign off `176-VALIDATION.md`: set `nyquist_compliant` and `status` truthfully and tick each
    checklist item that is genuinely met, leaving unmet ones unticked with a note.

    Confirm and record: both `~/BoardSmithGames` originals byte-identical across the ENTIRE phase
    (not just this plan), and `npm test` green with the count and its baseline.
  </action>
  <acceptance_criteria>
    - `176-PROOF.md` contains both `## What is still unproven` and `## How to re-run every proof`
      sections, the latter with runnable commands.
    - The unproven list names every NOT-AUDITED chunk from §4's coverage table by slug.
    - The unproven list carries forward the `lineFindings[]` multi-delta persistence gap
      (`175-PROOF.md` §7) with its still-unexercisable status stated.
    - ROADMAP Phase 176 carries a `**Result:**` paragraph containing at least four measured numbers.
    - `REQUIREMENTS.md`'s CHECK-01/CHECK-02 rows and coverage table are both updated consistently
      (no row saying Complete while the checklist item is unticked).
    - Every closed requirement cites a specific `176-PROOF.md` section number.
    - `176-VALIDATION.md` frontmatter reflects the real state; no checklist item is ticked without
      evidence.
    - Whole-tree sha256 diff of both originals is empty, recorded in the proof.
    - `npm test` green, count and baseline both stated.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
    <human-check>REQUIREMENTS.md, ROADMAP.md, 176-VALIDATION.md and 176-PROOF.md agree with each other and with the measured evidence</human-check>
  </verify>
  <done>The phase is closed with honest dispositions, a carried-forward unproven list, and re-runnable proof instructions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proof run → reference-game repos | real lens and repair dispatch against `cp -R` copies |
| repair loop → chunk code | repair MAY change code in the copies (decision 12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-14 | Tampering | `~/BoardSmithGames` originals | mitigate | All runs against `cp -R` copies; phase-wide sha256 diff recorded in Task 3. |
| T-176-15 | Repudiation | an unstated lens sample read as full coverage | mitigate | AUDITED / NOT AUDITED table covering all 12 stale chunks, plus a stated coverage fraction. |
| T-176-16 | Repudiation | a premature requirement closure | mitigate | Closure conditioned on measured evidence with section-and-number citations; open is preferred to premature. |
| T-176-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this phase. |
</threat_model>

<verification>
- `npm test` green with count and baseline stated
- Whole-tree `sha256` diff of both `~/BoardSmithGames` originals empty across the whole phase
- `176-PROOF.md` §4-§7 complete; coverage table covers all 12 stale chunks
</verification>

<success_criteria>
- Real lens findings on a stated subset, including one already-at-3-rounds chunk.
- Coverage explicitly stated; no implied full run.
- Paired pre/post-repair gate readings with an explicit answer on whether the snapshot would mislead.
- Requirements closed only where measured evidence supports it, with citations.
- Nothing fixed in reference-game content (decision 16).
</success_criteria>

<output>
Create `.planning/phases/176-stale-chunk-repair/176-06-SUMMARY.md` when done
</output>
