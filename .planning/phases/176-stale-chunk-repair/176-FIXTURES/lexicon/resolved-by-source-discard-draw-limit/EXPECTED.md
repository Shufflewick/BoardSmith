Expected verdict: `resolved-by-source`

Rationale for this expectation: the ruling's own Citation records that the rulebook was SILENT on
the discard-pile draw limit. The constructed fresh transcription now states, in the source itself,
exactly the rule the ruling had to supply (at most one discard-pile draw per turn) — the two are in
full agreement, not conflict. The ruling's own reason for existing (filling a silence) is gone
because the source no longer is silent. This is the "the ruling's own reason for existing (per its
Rationale) is gone" case named in `verify/ruling-recheck.md`'s "The four verdicts" section — not
`contradicted` (the two statements agree) and not `still-needed` (the source is no longer silent).
