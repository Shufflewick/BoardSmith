---
phase: 177-derived-line-re-derivation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/commands/verify-classify.ts
  - src/cli/commands/verify-classify.test.ts
  - src/cli/slash-command/bs/verify/classification-subagent.md
  - src/cli/slash-command/bs/verify.test.ts
  - .planning/phases/174-verify-classifier/174-PROOF.md
autonomous: true
requirements: [CHECK-04]

must_haves:
  truths:
    - "A dash-qualified presentation line carrying a parenthetical (`— diagram description (Plan phase):`) is recognized as a presentation line."
    - "The widened constant excludes nothing it did not exclude before, beyond the qualified-parenthetical forms — every unqualified `Derived (p.N):` line in both reference games stays rule-bearing."
    - "The code constant and its prose expression in `classification-subagent.md` still say the same thing — the cross-file lexicon pin stays green, not disabled."
    - "Phase 174's recorded results are documented as unaffected, without re-running a closed phase's proof."
  artifacts:
    - path: "src/cli/commands/verify-classify.ts"
      provides: "PRESENTATION_EXCLUSION_MARKERS widened to tolerate an optional parenthetical qualifier"
      contains: "PRESENTATION_EXCLUSION_MARKERS"
    - path: "src/cli/commands/verify-classify.test.ts"
      provides: "Positive pins on the 4 REAL slipping lines + a negative no-over-exclusion pin"
      contains: "Plan phase"
    - path: ".planning/phases/174-verify-classifier/174-PROOF.md"
      provides: "Note explaining 174's lineFindings came from the subagent layer, not the mechanical constant"
      contains: "PRESENTATION_EXCLUSION_MARKERS"
  key_links:
    - from: "src/cli/commands/verify-classify.ts"
      to: "src/cli/slash-command/bs/verify/classification-subagent.md"
      via: "cross-file lexicon pin in verify.test.ts"
      pattern: "diagram description"
---

<objective>
Fix decision 13: `PRESENTATION_EXCLUSION_MARKERS` is STRICTER than the rule its own subagent contract
states, and 4 real lines in a committed reference game slip through it today.

Purpose: decision 1 locks CHECK-04 to reusing this single shared definition of "presentation line".
That reuse is only safe if the definition is correct. Right now the same rule has two divergent
expressions — `classification-subagent.md:88` keys on the QUALIFIER's presence (correct), while the
regex requires the colon immediately after `description`/`art` (wrong). That is exactly the
two-definitions drift decision 1 exists to prevent, except the drift is inside the pair decision 1
treats as canonical.
Output: a widened, still-pinned constant; prose and code back in lockstep; a note on 174's proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/177-derived-line-re-derivation/177-CONTEXT.md
@.planning/phases/177-derived-line-re-derivation/177-RESEARCH.md
@.planning/phases/177-derived-line-re-derivation/177-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen PRESENTATION_EXCLUSION_MARKERS, pinned by the 4 real slipping lines and a negative case</name>
  <files>src/cli/commands/verify-classify.ts, src/cli/commands/verify-classify.test.ts</files>
  <read_first>
    - src/cli/commands/verify-classify.ts lines 93-141 — `PRESENTATION_EXCLUSION_MARKERS`,
      `isPresentationLine`, `CITATION_HEADER_RE`, `ruleBearingLines`: the exact current shapes
    - src/cli/commands/verify-classify.test.ts lines 135-176 — the existing `presentation-1`
      through `presentation-4` tests, including the frozen-set pin that asserts the array's exact
      contents (it WILL fail until updated) and the `FIXTURES_ROOT` fixture-reading helpers
    - .planning/phases/177-derived-line-re-derivation/177-RESEARCH.md § "VERIFIED regex gap" — the
      five live-tested input/output pairs
    - .planning/phases/177-derived-line-re-derivation/177-PATTERNS.md § `verify-classify.ts`
      (modified — decision 13's regex fix) — the proposed widened pattern
    - .planning/phases/177-derived-line-re-derivation/177-CONTEXT.md decision 13
  </read_first>
  <behavior>
    - `isPresentationLine("Derived (p.1) — diagram description (Plan phase): ...")` → true
    - `isPresentationLine("Derived (p.1) — diagram description (Fight phase): ...")` → true
    - `isPresentationLine("Derived (p.2) — diagram description (first Punch example): ...")` → true
    - `isPresentationLine("Derived (p.2) — diagram description (second Punch example): ...")` → true
    - `isPresentationLine("Derived (p.1) — diagram description: ...")` → true (unchanged)
    - `isPresentationLine("Derived (p.2) — art: ...")` → true (unchanged)
    - `isPresentationLine("Derived (p.1): The rules page is a single wide, landscape panel...")` → false (unchanged)
    - `isPresentationLine("Visual (p.3): ...")` → true (unchanged)
    - Negative sweep: across every `Derived (p.` line in
      `174-FIXTURES/{seven,one-two-punch}/live/*.md`, the set of lines newly classified presentation
      by the widened constant is EXACTLY the 4 named above — no more.
  </behavior>
  <action>
    Widen the two dash-qualified patterns in `PRESENTATION_EXCLUSION_MARKERS` so an optional
    parenthetical qualifier may sit between `description`/`art` and the colon, keying on the
    QUALIFIER's presence exactly as `classification-subagent.md:88` already states the rule in prose.
    Do not add a fourth marker and do not introduce a 177-local pattern — this is a correction to the
    single shared definition, not a fork. Leave `isPresentationLine`'s body and signature untouched.

    Extend `verify-classify.test.ts`'s existing `presentation-*` block:
    (a) update the `presentation-4` frozen-set pin to the new exact array contents — keep it a
        verbatim exact-contents assertion, never loosen it to a `toContain`/regex-shape check;
    (b) add positive cases for the 4 REAL slipping lines, quoted from
        `174-FIXTURES/one-two-punch/live/*.md` — read them out of the fixture rather than retyping
        them, so the test cannot drift from the real data it claims to pin;
    (c) add the NEGATIVE no-over-exclusion test described in `<behavior>`: walk every `Derived (p.`
        line in both games' committed `live/` fixtures, compute the set the widened constant now
        excludes but the old one did not, and assert that set has exactly 4 members and that each
        carries a `— diagram description`/`— art` qualifier. Assert as a corollary that all 10 of
        `seven`'s `Derived` lines remain non-presentation (`seven` has zero dash-qualified lines).

    `174-FIXTURES/{seven,one-two-punch}/live/*.md` are byte-identical to the two pinned reference
    games' current live slices for every slice file — verified directly during planning; only
    `INDEX.md` differs, and `INDEX.md` is excluded from the slice set by the existing convention. Use
    those committed fixtures; do not read `~/BoardSmithGames` from a unit test.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/cli/commands/verify-classify.test.ts` passes, including the updated exact
      frozen-set pin.
    - The 4 real slipping lines are read from the committed fixture files at test time, not retyped
      as string literals in the test body.
    - The negative test asserts a newly-excluded set of size exactly 4 and fails if a 5th line is
      ever newly excluded.
    - `grep -n "PRESENTATION_EXCLUSION_MARKERS" src/cli/commands/*.ts` still shows exactly one
      declaration site (no second/local copy introduced anywhere).
    - `npm test` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-classify.test.ts</automated>
  </verify>
  <done>The constant matches a dash-qualifier carrying a parenthetical, pinned by the 4 real lines and a negative over-exclusion guard.</done>
</task>

<task type="auto">
  <name>Task 2: Keep the cross-file lexicon pin honest — prose and code back in lockstep</name>
  <files>src/cli/slash-command/bs/verify/classification-subagent.md, src/cli/slash-command/bs/verify.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/verify.test.ts lines 492-526 — the `PRESENTATION_EXCLUSION_MARKERS —
      cross-file lexicon pin (decision 12b)` test: it strips backslashes, strips `^`, maps `d+`→`N`,
      and asserts the resulting literal prefix appears verbatim in `classification-subagent.md`.
      Task 1's widened pattern breaks this normalization.
    - src/cli/slash-command/bs/verify/classification-subagent.md lines 55-92 — the three lexicon
      bullets (lines 60-62), the worked dual-schema example (line 78), and the correct prose rule
      at lines 88-89
    - .planning/phases/177-derived-line-re-derivation/177-CONTEXT.md decision 13 (the two divergent
      expressions of one rule)
  </read_first>
  <action>
    Restore the pin's meaning rather than weakening it. Two coordinated edits:

    (a) In `classification-subagent.md`, extend the lexicon bullet list so the qualified-parenthetical
        form is named explicitly as a recognized presentation form — one new bullet in the existing
        one-line style, using a real qualifier drawn from `one-two-punch` (`(Plan phase)`) rather than
        an invented one. Keep lines 88-89's unqualified-stays-rule-bearing sentence exactly as it is;
        it was already correct and is what the code is being brought into line with.

    (b) In `verify.test.ts`'s lexicon pin, extend the normalization to also erase the optional
        parenthetical group from a marker's regex source before the verbatim-prose check, so the
        widened marker still normalizes to a literal prefix the prose contains. Do NOT skip the
        widened markers, do NOT switch the assertion to a substring-of-a-substring or a regex match,
        and do NOT delete the test — the whole point of decision 13 is that these two expressions
        must be provably the same rule.

    Add one new assertion in the same describe block: the qualified-parenthetical form named in the
    prose is itself matched by `isPresentationLine`, importing the real function — so the prose
    example cannot become a claim the code does not honour.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/verify.test.ts` passes; the `cross-file lexicon pin`
      test is still present, still asserts verbatim prose containment, and is not skipped.
    - `classification-subagent.md` names the qualified-parenthetical form with a real `one-two-punch`
      qualifier; `grep -c "Plan phase" src/cli/slash-command/bs/verify/classification-subagent.md`
      is at least 1.
    - The new assertion drives the real `isPresentationLine` against the prose's own example.
    - `npm test` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/verify.test.ts</automated>
  </verify>
  <done>Code and prose state one rule, and the test that proves it is stronger than before, not looser.</done>
</task>

<task type="auto">
  <name>Task 3: Record in 174-PROOF.md why Phase 174's results stand unchanged</name>
  <files>.planning/phases/174-verify-classifier/174-PROOF.md</files>
  <read_first>
    - .planning/phases/174-verify-classifier/174-PROOF.md — locate the section recording the
      `lineFindings[]` results and the §3 transcript-observable section; append near the findings
      section, matching the file's existing heading depth and tone
    - .planning/phases/177-derived-line-re-derivation/177-CONTEXT.md decision 13, final paragraph
      ("Phase 174's recorded results STAND and do not need re-measuring")
  </read_first>
  <action>
    Append a short, dated note to `174-PROOF.md` stating: Phase 177 widened
    `PRESENTATION_EXCLUSION_MARKERS` to tolerate a parenthetical qualifier; Phase 174's recorded
    `lineFindings[]` came from the SUBAGENT layer, whose prose rule (`classification-subagent.md`'s
    qualifier-presence test) was already correct; the mechanical constant is a second gate downstream
    of that judgment, not the source of those findings; therefore 174's recorded results are not
    invalidated and its proof is not re-run.

    Name the 4 real lines the old constant missed and state which game they are in, so a reader can
    check the claim rather than take it. Do not re-run, re-measure, or edit any of 174's recorded
    numbers. Do not claim the note was verified by re-dispatching anything — it is a reasoning note
    about which layer produced the findings, and it should say so plainly.
  </action>
  <acceptance_criteria>
    - `174-PROOF.md` contains a new dated note naming `PRESENTATION_EXCLUSION_MARKERS`, Phase 177,
      and the 4 real slipping lines.
    - The note states explicitly that no 174 result was re-measured and that the note is a
      layer-attribution argument, not a re-verification.
    - `git diff` on `174-PROOF.md` shows only an addition — zero deletions or modifications to any
      previously recorded number or verdict.
  </acceptance_criteria>
  <verify>
    <automated>git diff --numstat .planning/phases/174-verify-classifier/174-PROOF.md</automated>
    <human-check>The 174-PROOF.md diff is additive only and the note names the 4 real lines</human-check>
  </verify>
  <done>174's closed proof carries an honest note about the constant's fix, with nothing re-measured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared lexicon constant → every consumer (174 classifier, 177 check) | one definition, three consumers; a silent divergence mis-sorts rule-bearing content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-01 | Tampering | `PRESENTATION_EXCLUSION_MARKERS` widening | mitigate | Negative over-exclusion test bounds the newly-excluded set to exactly the 4 measured real lines. |
| T-177-02 | Repudiation | cross-file lexicon pin weakened to make a change pass | mitigate | Test must remain a verbatim-prose containment assertion; loosening it is explicitly forbidden by Task 2's acceptance criteria. |
| T-177-03 | Information disclosure | closed-phase proof edited to look better | mitigate | Task 3's diff must be additive only, checked with `git diff --numstat`. |
| T-177-SC | Tampering | npm/pip/cargo installs | accept | This phase installs no external packages (177-RESEARCH.md § Package Legitimacy Audit). |
</threat_model>

<verification>
- `npx vitest run src/cli/commands/verify-classify.test.ts`
- `npx vitest run src/cli/slash-command/bs/verify.test.ts`
- `npm test`
- `git diff --numstat .planning/phases/174-verify-classifier/174-PROOF.md` shows additions only
</verification>

<success_criteria>
- The 4 real slipping lines are excluded; nothing else newly is.
- One definition of "presentation line" remains, expressed identically in code and prose, pinned.
- 174's results are documented as standing, with no re-measurement.
</success_criteria>

<output>
Create `.planning/phases/177-derived-line-re-derivation/177-01-SUMMARY.md` when done
</output>
