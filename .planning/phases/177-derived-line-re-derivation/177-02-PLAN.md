---
phase: 177-derived-line-re-derivation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/commands/verify-derive-recheck.ts
  - src/cli/commands/verify-derive-recheck.test.ts
autonomous: true
requirements: [CHECK-04]

must_haves:
  truths:
    - "A verdict outside `agrees | disagrees | underivable | not-rule-bearing` cannot be recorded."
    - "`underivable` is a first-class recordable verdict that never collapses into `agrees` or `disagrees`."
    - "A `disagrees` record without BOTH derivations quoted verbatim is rejected at construction."
    - "The blind-derivation payload built for any real slice contains ZERO `Derived (p.` and ZERO `Visual (p.` lines."
    - "The module reads LIVE `rulebook/*.md`, never a staged `.verify/<runId>/slices/` tree."
    - "The module contains no rule-bearingness keyword or phrase list."
  artifacts:
    - path: "src/cli/commands/verify-derive-recheck.ts"
      provides: "CHECK-04's mechanical core: frozen verdict enum, record choke point, quote-line payload filter, live-slice enumeration"
      min_lines: 150
      exports: ["DERIVE_VERDICTS", "createDeriveVerdictRecord", "quoteLinesOnly", "buildBlindDerivePayload", "enumerateDerivedLines", "readLiveSlices"]
    - path: "src/cli/commands/verify-derive-recheck.test.ts"
      provides: "Pinning tests for the enum, the choke point, and the payload filter against real committed slices"
      contains: "buildBlindDerivePayload"
  key_links:
    - from: "src/cli/commands/verify-derive-recheck.ts"
      to: "src/cli/commands/verify-classify.ts"
      via: "import { isPresentationLine }"
      pattern: "from './verify-classify.js'"
---

<objective>
Build CHECK-04's mechanical core: the frozen verdict enum, the single validation choke point, the
live-slice enumeration, and — the one genuinely novel piece in this phase — the quote-lines-only
payload filter that makes decision 5's independence STRUCTURAL.

Purpose: a "re-derivation" that has seen the original `Derived` line is not a second opinion, it is a
confirmation, and it would report high agreement whether or not the originals were sound. The
original must be absent from the payload by construction, not by instruction.
Output: `verify-derive-recheck.ts`'s pure surface plus its colocated pinning tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/177-derived-line-re-derivation/177-CONTEXT.md
@.planning/phases/177-derived-line-re-derivation/177-RESEARCH.md
@.planning/phases/177-derived-line-re-derivation/177-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DERIVE_VERDICTS and the single record-construction choke point</name>
  <files>src/cli/commands/verify-derive-recheck.ts, src/cli/commands/verify-derive-recheck.test.ts</files>
  <read_first>
    - src/cli/commands/verify-ruling-recheck.ts lines 1-105 — the module doc comment (which states
      what the CLI refuses to do as explicitly as what it does), `RULING_VERDICTS`'s frozen-array +
      derived-type + type-guard shape, `RulingVerdictRecord`, and `createRulingVerdictRecord`'s
      throw-on-out-of-enum / throw-on-empty-reasoning choke point at lines 59-84
    - src/cli/commands/verify-ruling-recheck.test.ts — the colocated test file's block-per-export
      organization and its error-message assertions
    - src/cli/commands/build-manifest.ts lines 20-30 — `FINDING_KINDS`, the enumerated-code-set
      convention (frozen array + derived type, never a hand-written union)
    - .planning/phases/177-derived-line-re-derivation/177-CONTEXT.md decisions 2, 6, 8, 14
    - .planning/phases/177-derived-line-re-derivation/177-PATTERNS.md § `verify-derive-recheck.ts`
  </read_first>
  <behavior>
    - `createDeriveVerdictRecord` with verdict `"probably-agrees"` throws, and the message names the
      four legal verdicts.
    - `createDeriveVerdictRecord` with an empty/whitespace `reasoning` throws.
    - A `disagrees` record with an empty `originalReading` throws; with an empty `rederivedReading`
      throws; with both non-empty succeeds.
    - An `underivable` record constructs successfully and round-trips with `verdict === 'underivable'` —
      it is never rewritten to `agrees` or `disagrees` by any code path.
    - A `not-rule-bearing` record constructs successfully with no `rederivedReading` required.
    - `DERIVE_VERDICTS` is frozen and its contents are exactly the four members, in order.
  </behavior>
  <action>
    Create `src/cli/commands/verify-derive-recheck.ts`. Open with a module doc comment in
    `verify-ruling-recheck.ts`'s style that states this module's two refusals explicitly: it contains
    NO rule-bearingness keyword or phrase list (decision 2 — recognizing a rule-bearing line is
    subagent judgment, never a string match here), and its payload construction never leaks a
    `Derived`/`Visual` line into the blind-derivation prompt (decision 5).

    Export `DERIVE_VERDICTS` as `Object.freeze([...] as const)` with exactly
    `agrees`, `disagrees`, `underivable`, `not-rule-bearing`; export the derived `DeriveVerdict` type
    and a module-local `isDeriveVerdict` type guard. Never hand-write the union.

    Export `DeriveVerdictRecord` carrying at least: `slicePath`, `lineNumber`, `originalLine`,
    `verdict`, `reasoning`, `originalReading`, `rederivedReading`, `sourceQuotes`.

    Export `createDeriveVerdictRecord(input)` as the ONE place a verdict string is checked against
    the enum. It throws on an out-of-enum verdict (message naming all four), throws on empty
    `reasoning`, and — decision 8's stricter bar — throws when `verdict === 'disagrees'` and either
    `originalReading` or `rederivedReading` is empty, because SC-2 requires findings that cite BOTH
    derivations verbatim and a summary loses exactly the material a designer needs to adjudicate.
    Model that as an additional `if` block inside the same function; never add a second validator
    elsewhere. Error text is a single actionable line — no stack frame, no `.ts:` reference.

    Write the colocated test file with one describe block per export, covering every case in
    `<behavior>`, plus a pin that `DERIVE_VERDICTS` is frozen with exactly those four members and a
    source assertion (`readFileSync` of the module, mirroring the repo's existing source-assertion
    style) that the module body contains no keyword/phrase-list array for rule-bearingness.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/cli/commands/verify-derive-recheck.test.ts` passes.
    - `grep -c "isDeriveVerdict" src/cli/commands/verify-derive-recheck.ts` shows the guard used only
      inside `createDeriveVerdictRecord` — one validation site.
    - A test named to match `-t "underivable"` exists and asserts `underivable` survives construction
      unchanged.
    - A test named to match `-t "both-derivations"` exists and asserts the `disagrees` field-presence
      throw for each missing field independently.
    - A test named to match `-t "no-phrase-list"` exists and asserts by source inspection that no
      rule-bearingness phrase list exists in the module.
    - `npm test` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-derive-recheck.test.ts</automated>
  </verify>
  <done>The four verdicts are frozen and enforced at exactly one choke point, with `disagrees` requiring both readings verbatim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: quoteLinesOnly, buildBlindDerivePayload, and live-slice enumeration</name>
  <files>src/cli/commands/verify-derive-recheck.ts, src/cli/commands/verify-derive-recheck.test.ts</files>
  <read_first>
    - src/cli/commands/verify-classify.ts lines 93-141 — `isPresentationLine` (import it, never
      redefine it: decision 1), `CITATION_HEADER_RE` (`^p\.\d+,.*:$`), and `ruleBearingLines`, whose
      selection CHECK-04 needs the INVERSE of
    - src/cli/commands/verify-classify.ts lines 585-613 — `computeRunPairs`'s live-slice read: the
      correct definition of "which files are slices" (`rulebook/*.md` excluding `INDEX.md` and
      `00-visual-survey.md`)
    - src/cli/commands/verify-ruling-recheck.ts lines 124-146 (`enumerateRulingsForRecheck`'s
      enumerate-and-report-never-silently-drop posture) and lines 186-251
      (`resolveFreshTranscription` — read it to confirm it resolves the STAGED tree, then do NOT
      reuse it: 177-RESEARCH.md Pitfall 3)
    - .planning/phases/174-verify-classifier/174-FIXTURES/seven/live/ and
      .planning/phases/174-verify-classifier/174-FIXTURES/one-two-punch/live/ — the committed live
      slices this test corpus uses; byte-identical to both pinned games' current live slices for
      every slice file (only `INDEX.md` differs, and `INDEX.md` is not a slice)
    - src/cli/commands/verify-classify.test.ts lines 52-70 — the `FIXTURES_ROOT` / `readFixture`
      helper pattern for reading committed `.planning` fixtures from a unit test
    - .planning/phases/177-derived-line-re-derivation/177-RESEARCH.md Question 1 and Question 2
  </read_first>
  <behavior>
    - `quoteLinesOnly` over every committed live slice in BOTH games returns zero lines starting with
      `Derived (p.` and zero starting with `Visual (p.`.
    - `quoteLinesOnly` retains bare citation headers matching `^p\.\d+,.*:$` and the quoted rulebook
      prose beneath them — it is not so aggressive that it returns an empty array for any real slice.
    - `buildBlindDerivePayload(slice, entry)`'s output string, for EVERY one of the 22 real `Derived`
      lines, contains zero occurrences of the substrings `Derived (p.` and `Visual (p.`.
    - `buildBlindDerivePayload`'s output contains no substring of the entry's own `originalLine`
      beyond what independently appears in a retained quote line.
    - `enumerateDerivedLines` over the two committed games' live fixtures yields exactly 10 entries
      for `seven` and 12 for `one-two-punch` before presentation filtering — 22 total.
    - `readLiveSlices` excludes `INDEX.md` and `00-visual-survey.md`.
  </behavior>
  <action>
    Add to `verify-derive-recheck.ts`:

    `readLiveSlices(projectDir): Promise<{ path: string; text: string }[]>` — reads the LIVE
    `rulebook/*.md` tree directly, excluding `INDEX.md` and `00-visual-survey.md`, mirroring
    `computeRunPairs`'s existing convention. It must NOT call, import, or reimplement
    `resolveFreshTranscription`: CHECK-04 targets live slices with no staged prerequisite (decision
    12), and reusing the staged resolver would make the check falsely report scope-limited on a
    project that has never staged a run.

    `quoteLinesOnly(sliceText): string[]` — selects ONLY directly-quoted rulebook content and its
    citation headers. Compose from the machinery `verify-classify.ts` already exposes rather than
    re-deriving line classification: drop blank lines and markdown headings, and drop every line
    starting with `Derived (p.` or `Visual (p.` ENTIRELY — not merely the presentation-tagged subset
    `isPresentationLine` excludes. This is deliberately the inverse of `ruleBearingLines`, which
    KEEPS unqualified `Derived` lines.

    `enumerateDerivedLines(slices)` — every `Derived (p.` line in the live tree, recorded with its
    slice path, 1-based line number and verbatim text, split into those a mechanical
    `isPresentationLine` call excludes and those surviving for judgment. Report the excluded set
    rather than silently dropping it, mirroring 176's enumerate-and-report posture. Do not decide
    rule-bearingness here — that is the subagent's (decision 2), and 177-RESEARCH.md Pitfall 1
    measured at least 3 `seven` lines that are presentation-shaped with no qualifier at all and
    therefore reach the subagent by design.

    `buildBlindDerivePayload(slice, entry): string` — the dispatch payload for one `Derived` line:
    the `BS-DERIVE-V1` handshake token, the slice's identity, and that slice's `quoteLinesOnly`
    output. It receives the entry only for identity/ordering; the entry's own `originalLine` text is
    never emitted. This function is the single construction site for the blind payload — no caller
    may append to its output before dispatch. Say that in its doc comment, and state plainly that a
    derivation which saw the original would be a confirmation, not a second opinion.

    Test all of `<behavior>` against the committed `174-FIXTURES/*/live` corpus, using a
    `FIXTURES_ROOT` helper in `verify-classify.test.ts`'s style. Include a source assertion that the
    module never references a `.verify/` staging path.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/cli/commands/verify-derive-recheck.test.ts` passes.
    - A test matching `-t "blind"` asserts, over all 22 real `Derived` entries, that
      `buildBlindDerivePayload`'s output contains zero `Derived (p.` and zero `Visual (p.`
      occurrences — a counting assertion (`=== 0`), not a truthiness check.
    - A test matching `-t "live"` asserts by source inspection that the module contains no `.verify/`
      staging path and does not import `resolveFreshTranscription`.
    - A test matching `-t "visual"` asserts `Visual (p.` lines are excluded from both the payload and
      the judgment enumeration.
    - `enumerateDerivedLines` counts assert 10 / 12 / 22 exactly against the committed fixtures; if
      the real counts differ, the test records the real number and the task reports the discrepancy
      rather than adjusting the claim silently.
    - Every real slice yields a non-empty `quoteLinesOnly` result (guards against an
      over-aggressive filter that would trivially satisfy the zero-leak assertion).
    - `npm test` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-derive-recheck.test.ts</automated>
  </verify>
  <done>The blind payload is structurally incapable of carrying the original line, proven against all 22 real lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live slice text → blind-derivation dispatch prompt | the payload crosses into a subagent's context; anything leaked there destroys the check's only guarantee |
| subagent RETURN → ledger record | an untrusted verdict string crosses into durable state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-04 | Spoofing | blind-derive payload carrying the original `Derived` line | mitigate | `buildBlindDerivePayload` is the single construction site; counting test asserts zero `Derived (p.`/`Visual (p.` over all 22 real lines. |
| T-177-05 | Tampering | an out-of-enum or unreasoned verdict entering the ledger | mitigate | `createDeriveVerdictRecord` is the one choke point; throws on out-of-enum, empty reasoning, and `disagrees` without both readings. |
| T-177-06 | Information disclosure | reading the archived source PDF | mitigate | Module reads only `rulebook/*.md`; source assertion pins the absence of any archive path (extended in plan 03). |
| T-177-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this phase. |
</threat_model>

<verification>
- `npx vitest run src/cli/commands/verify-derive-recheck.test.ts`
- `npm test`
- `grep -c "Derived (p\." on a dumped real payload is exercised as a unit assertion here and as a real-prompt grep in plan 07
</verification>

<success_criteria>
- Four verdicts frozen, validated at one choke point, `disagrees` requiring both readings.
- Blind payload proven leak-free against the full real 22-line corpus.
- Live-slice targeting proven by source assertion; staged resolver demonstrably not reused.
- No rule-bearingness phrase list anywhere in the module.
</success_criteria>

<output>
Create `.planning/phases/177-derived-line-re-derivation/177-02-SUMMARY.md` when done
</output>
