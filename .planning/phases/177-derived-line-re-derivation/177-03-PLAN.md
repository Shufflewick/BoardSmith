---
phase: 177-derived-line-re-derivation
plan: 03
type: execute
wave: 2
depends_on: ["177-02"]
files_modified:
  - src/cli/commands/verify-derive-recheck.ts
  - src/cli/commands/verify-derive-recheck.test.ts
  - src/cli/cli.ts
autonomous: true
requirements: [CHECK-04]

must_haves:
  truths:
    - "`boardsmith verify-derive-recheck` runs project-wide with no `--run-id` and no staged run present."
    - "A disagreement finding is reported with both derivations quoted verbatim, and exits 0."
    - "Only tool failure (unreadable project/rulebook) produces a non-zero exit or a throw."
    - "Records are written through the repo's single atomic write path, never a second `fs.writeFile`."
    - "Running the command leaves every non-ledger file in the project byte-identical."
  artifacts:
    - path: "src/cli/commands/verify-derive-recheck.ts"
      provides: "The report command, the project-level ledger write/read, and the --json surface Phase 179 consumes"
      exports: ["verifyDeriveRecheckCommand", "recordDeriveVerdicts", "readDeriveVerdicts"]
    - path: "src/cli/cli.ts"
      provides: "verify-derive-recheck command registration with --project and --json only"
      contains: "verify-derive-recheck"
  key_links:
    - from: "src/cli/commands/verify-derive-recheck.ts"
      to: "src/cli/commands/verify-run.ts"
      via: "import { atomicWriteFile }"
      pattern: "atomicWriteFile"
---

<objective>
Give CHECK-04 its CLI surface: a project-level ledger, a report command, and a `--json` shape Phase
179 can assemble a source-free mode from.

Purpose: decisions 14 and 15 settle the two open research questions — CHECK-04 is project-level with
no run concept (it has nothing to scope to a run: it is source-free by construction and independent
of any run's staleness verdicts), and it REPORTS rather than gating Close (findings exit 0; a check
that fires on correct work gets waived). Both match `trace-check`/`drift-check`'s established shape.
Output: `boardsmith verify-derive-recheck`, registered and pinned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/177-derived-line-re-derivation/177-CONTEXT.md
@.planning/phases/177-derived-line-re-derivation/177-PATTERNS.md
@.planning/phases/177-derived-line-re-derivation/177-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project-level ledger through the one atomic write path</name>
  <files>src/cli/commands/verify-derive-recheck.ts, src/cli/commands/verify-derive-recheck.test.ts</files>
  <read_first>
    - src/cli/commands/verify-ruling-recheck.ts lines 372-388 — `recordRulingVerdicts`: the
      fenced-body JSON-lines ledger shape and the `atomicWriteFile` call that is the ONLY durable
      write in that module
    - src/cli/commands/verify-run.ts — the exported ledger helpers `atomicWriteFile`,
      `parseLedgerBody`, `ledgerFilePath`; confirm `atomicWriteFile`'s exact signature before use
    - .planning/phases/173-verify-pipeline-core/173-REVIEW.md CR-01 — the "exactly one atomic write
      path in the repo" defect class
    - src/cli/commands/trace-check.ts lines 1-15 — the source-free-by-construction doc comment style
    - src/cli/commands/chunk-provenance.ts lines 706-714 — the T-171-19-class before/after
      whole-project byte-hash test this phase mirrors
    - .planning/phases/177-derived-line-re-derivation/177-CONTEXT.md decision 14
  </read_first>
  <behavior>
    - `recordDeriveVerdicts(projectDir, records)` writes `rulebook/.derive-recheck/DERIVE-VERDICTS.md`
      with a begin/end fenced body of JSON lines, one per record, and returns the project-relative
      ledger path.
    - `readDeriveVerdicts(projectDir)` round-trips exactly what `recordDeriveVerdicts` wrote,
      including a record whose verdict is `underivable`.
    - Re-recording replaces the body atomically — no partial file is ever observable.
    - Recording accepts no `runId` parameter and the ledger path contains no run-id segment.
    - Whole-project byte hash before vs. after a full `verify-derive-recheck` run differs only in
      `rulebook/.derive-recheck/` — no slice, no `SKETCH.md`, no archive is touched.
  </behavior>
  <action>
    Add `recordDeriveVerdicts(projectDir, records)` and `readDeriveVerdicts(projectDir)` to
    `verify-derive-recheck.ts`, writing a PROJECT-LEVEL ledger at
    `rulebook/.derive-recheck/DERIVE-VERDICTS.md` — no `.verify/<runId>/` segment, no `--run-id`
    parameter anywhere in the signature (decision 14). Use the same begin/end HTML-comment fence and
    JSON-lines body shape `recordRulingVerdicts` uses, and write exclusively through
    `atomicWriteFile` imported from `verify-run.js`. Never call `fs.writeFile`/`writeFileSync`
    directly and never add a second write function.

    Every record reaching the ledger must have passed through `createDeriveVerdictRecord` — the
    recording function accepts already-validated records and does not re-parse verdict strings.

    Extend the module doc comment with the source-free-by-construction guarantee in `trace-check.ts`'s
    style: this module has no code path that opens the archived source rulebook; source-freeness is
    structural, not a flag (decision 4).

    Add the before/after whole-project byte-hash test in `chunk-provenance.ts:706-714`'s style,
    against a temp-dir project seeded from the committed `174-FIXTURES/*/live` slices plus a decoy
    file at an archive-shaped path — proving the archive is never read and nothing outside
    `rulebook/.derive-recheck/` is written.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/cli/commands/verify-derive-recheck.test.ts` passes.
    - `grep -n "writeFile" src/cli/commands/verify-derive-recheck.ts` shows zero direct
      `fs.writeFile`/`writeFileSync` calls; `atomicWriteFile` is the only durable write.
    - A test matching `-t "project-level"` asserts the ledger path has no run-id segment and that
      neither `recordDeriveVerdicts` nor `readDeriveVerdicts` accepts a `runId`.
    - A test matching `-t "source-free"` is the before/after whole-project byte-hash test, and it
      includes the archive decoy.
    - The round-trip test covers an `underivable` record explicitly.
    - `npm test` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-derive-recheck.test.ts</automated>
  </verify>
  <done>A project-level ledger exists, written through the repo's single atomic path, with source-freeness pinned by a byte-hash test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The report command and its CLI registration</name>
  <files>src/cli/commands/verify-derive-recheck.ts, src/cli/commands/verify-derive-recheck.test.ts, src/cli/cli.ts</files>
  <read_first>
    - src/cli/commands/verify-ruling-recheck.ts lines 257-364 — `RulingRecheckReportRow`,
      `VerifyRulingRecheckResult`, and `verifyRulingRecheckCommand`: the `--json` early return, the
      chalk-yellow scope warning, the chalk-green summary, and the fact that `process.exitCode` is
      never set
    - src/cli/cli.ts lines 39 and 386-401 — the import line and the `verify-ruling-recheck`
      registration block to mirror
    - .planning/phases/172-source-free-conformance-checks/172-CONTEXT.md decision 6 — "advisory
      sweeps a verify pipeline consumes, not gates"; findings exit 0
    - src/cli/commands/trace-check.ts / drift-check.ts — the findings-exit-0 report shape and the
      enumerated finding kinds convention
    - .planning/phases/177-derived-line-re-derivation/177-CONTEXT.md decisions 8, 15
  </read_first>
  <behavior>
    - `verifyDeriveRecheckCommand({ project, json })` on a project with recorded disagreements
      resolves normally and leaves `process.exitCode` unset (findings exit 0).
    - The same command on a project with no `rulebook/` directory throws one actionable line naming
      the missing directory and `--project` — no stack frame, no `.ts:` reference.
    - `--json` emits the result object and nothing else on stdout (no human report interleaved).
    - The `--json` result carries per-verdict counts with zeros written explicitly, plus a
      `findings[]` array where each `disagrees` entry carries `originalReading` and
      `rederivedReading` verbatim.
    - The registered command exposes exactly `--project` and `--json` — no `--run-id`, no bypass
      option of any kind.
  </behavior>
  <action>
    Add `VerifyDeriveRecheckResult` and `verifyDeriveRecheckCommand(options)` to
    `verify-derive-recheck.ts`, mirroring `verifyRulingRecheckCommand`'s shape: `--json` early return
    via `console.log(JSON.stringify(result, null, 2))`, then the human report (per-verdict counts with
    zeros written explicitly, then one row per finding). A `disagrees` finding prints BOTH derivations
    verbatim — the original `Derived` line and the blind re-derivation — because SC-2 requires citing
    both and a summary loses what a designer needs to adjudicate (decision 8).

    Never set `process.exitCode` anywhere in this module. A disagreement is a finding, not a gate
    (decision 15). Only an unreadable project/rulebook throws.

    Design the `--json` shape for Phase 179's source-free-mode assembly: a stable top-level object
    with the project path, the enumerated total, the mechanically-excluded presentation count, the
    per-verdict counts, and `findings[]`. Phase 179 assembles a mode from checks of exactly this
    shape.

    Register the command in `src/cli/cli.ts` alongside the existing `verify-ruling-recheck` block:
    import `verifyDeriveRecheckCommand` from `./commands/verify-derive-recheck.js`, add
    `.command('verify-derive-recheck')` with a description naming the four verdicts and the
    read-only, project-level, source-free posture, and options `--project <dir>` and `--json` ONLY.
    Add a short comment in the registration block, in the neighbouring block's style, stating that
    this command registers no `--run-id` (decision 14) and no bypass option.

    Add tests for every case in `<behavior>`, including a `--json`-purity test asserting stdout parses
    as JSON with nothing prepended, and a source assertion that `process.exitCode` never appears in
    the module.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/cli/commands/verify-derive-recheck.test.ts` passes.
    - A test matching `-t "exit"` asserts `process.exitCode` is unset after a run that produced
      findings, and asserts by source inspection that `process.exitCode` appears zero times in the
      module.
    - `node dist/cli/cli.js verify-derive-recheck --help` (or the repo's equivalent built/`tsx`
      invocation) lists exactly `--project` and `--json`; `grep -c "run-id" ` over the registration
      block is 0.
    - The `--json` output for a seeded project with a `disagrees` record contains both
      `originalReading` and `rederivedReading` non-empty.
    - Per-verdict counts include all four verdicts with zeros written explicitly.
    - `npm test` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-derive-recheck.test.ts</automated>
  </verify>
  <done>`boardsmith verify-derive-recheck` reports project-wide, cites both derivations, and exits 0 on findings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI invocation → project filesystem | a `--project` path selects which tree is read and where the ledger is written |
| command stdout → Phase 179 mode assembler | `--json` is a machine-consumed contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-07 | Tampering | a second durable write path appearing | mitigate | `atomicWriteFile` only; grep assertion for direct `fs.writeFile` in acceptance criteria. |
| T-177-08 | Denial of service | a report-only check blocking a designer's Close | mitigate | `process.exitCode` never set; pinned by source assertion (decision 15). |
| T-177-09 | Information disclosure | `--json` stdout contaminated by human report text | mitigate | `--json` purity test parses stdout as JSON with nothing prepended (176-06's contamination defect class). |
| T-177-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this phase. |
</threat_model>

<verification>
- `npx vitest run src/cli/commands/verify-derive-recheck.test.ts`
- `npm test`
- Real `verify-derive-recheck --help` output shows exactly `--project` and `--json`
</verification>

<success_criteria>
- Project-level, run-id-free ledger through the one atomic write path.
- Findings exit 0; only tool failure throws, with an actionable message.
- `--json` carries per-verdict counts and both derivations on every disagreement.
- Command registered with no bypass option.
</success_criteria>

<output>
Create `.planning/phases/177-derived-line-re-derivation/177-03-SUMMARY.md` when done
</output>
