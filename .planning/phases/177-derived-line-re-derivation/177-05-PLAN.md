---
phase: 177-derived-line-re-derivation
plan: 05
type: execute
wave: 3
depends_on: ["177-03", "177-04"]
files_modified:
  - src/cli/slash-command/bs/verify-game.md
  - src/cli/slash-command/bs/verify.test.ts
  - .planning/phases/177-derived-line-re-derivation/177-SWEEP.md
autonomous: true
requirements: [CHECK-04]

must_haves:
  truths:
    - "`/bs-verify-game` has a CHECK-04 step routing to both new contracts, and Close is renumbered with no gap or duplicate."
    - "The Context-Economics Hard Rule carries an explicit carve-out for the CHECK-04 dispatch prompts, so the rule is not violated by design."
    - "Close's condition is unchanged — CHECK-04 reports and does not gate it."
    - "Every claim in `verify-game.md` this phase could falsify was checked, including the ones found still TRUE, and the check is recorded."
  artifacts:
    - path: "src/cli/slash-command/bs/verify-game.md"
      provides: "The CHECK-04 step, the Context-Economics carve-out, renumbered Close, and Reference Files bullets"
      contains: "BS-DERIVE-V1"
    - path: ".planning/phases/177-derived-line-re-derivation/177-SWEEP.md"
      provides: "The full stale-claim sweep record, listing claims found TRUE as well as claims revised"
      contains: "STILL TRUE"
  key_links:
    - from: "src/cli/slash-command/bs/verify-game.md"
      to: "src/cli/slash-command/bs/verify/derive-recheck.md"
      via: "step prose delegating the judgment contract by reference"
      pattern: "derive-recheck.md"
---

<objective>
Wire CHECK-04 into `/bs-verify-game` — and sweep the whole router for stale cross-file claims, not
just the ones already named.

Purpose: this milestone has hit stale cross-file prose in FIVE places across FOUR phases, and every
single time the survivor was a claim nobody had listed. One claim in particular is falsified BY
DESIGN here: the Context-Economics Hard Rule says this skill's transcript never contains a quote line
or a `Derived (p.` line, while CHECK-04's dispatch prompts must CONTAIN exactly those — that is their
payload. Without an explicit carve-out, the rule is broken the moment the step ships.
Output: the new step, the carve-out, a renumbered Close, and a durable sweep record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/177-derived-line-re-derivation/177-CONTEXT.md
@.planning/phases/177-derived-line-re-derivation/177-RESEARCH.md
@.planning/phases/177-derived-line-re-derivation/177-PATTERNS.md
@.planning/phases/177-derived-line-re-derivation/177-03-SUMMARY.md
@.planning/phases/177-derived-line-re-derivation/177-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the CHECK-04 step, renumber Close, add Reference Files bullets</name>
  <files>src/cli/slash-command/bs/verify-game.md, src/cli/slash-command/bs/verify.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/verify-game.md lines 119-161 — Step 5 (the direct prose template: a
      compressed "In short:" delegation naming the contract file, the handshake token, the inputs,
      the verdict set, and the recording command), Step 6, and Step 7 Close
    - src/cli/slash-command/bs/verify-game.md lines 163-200 — the Reference Files list's one-line
      bullet style, including the `ruling-recheck.md` bullet at 177-178
    - src/cli/slash-command/bs/verify.test.ts lines 341-360 (`has a Step 7 naming Close` — this
      assertion moves), 604-650 (the Reference Files, contiguity, and no-hardcoded-count guards)
    - .planning/phases/177-derived-line-re-derivation/177-CONTEXT.md decisions 3, 5, 7, 8, 15
    - .planning/phases/177-derived-line-re-derivation/177-PATTERNS.md § `verify-game.md`
  </read_first>
  <action>
    Insert a new step between Step 6 (Repair Dispatch) and Close, since CHECK-04 is explicitly
    independent of staleness and repair. Renumber Close to the next number with a full numeric
    renumber — never a spliced insertion leaving a duplicate or a gap.

    Write the new step's prose in Step 5's compressed "In short:" style, delegating to
    `${CLAUDE_SKILL_DIR}/../bs-shared/verify/derive-recheck.md` and
    `${CLAUDE_SKILL_DIR}/../bs-shared/verify/derive-compare.md` for the full judgment contracts, and
    stating: (a) every `Derived` line surviving `isPresentationLine` exclusion is enumerated
    PROJECT-WIDE, all of them, never scoped to stale chunks — this check does not consume staleness
    verdicts; (b) each is dispatched BLIND, carrying `BS-DERIVE-V1` and that slice's quote lines only,
    never the original line — the payload is built by `boardsmith verify-derive-recheck`, not composed
    by this orchestrator; (c) a SEPARATE dispatch carrying `BS-DERIVE-COMPARE-V1`, the original line
    and the recorded re-derivation, returns the verdict; (d) findings citing BOTH derivations verbatim
    are reported and exit 0 — they never gate Close.

    Update `verify.test.ts`: move the `naming Close` assertion to the new number, keep the contiguity
    and no-hardcoded-count guards green, and add a CHECK-04 version of the existing "lists both new
    routes in Reference Files" test for the two new contract bullets. Add a positive pin that the new
    step names both handshake tokens and delegates to both contract files by path.

    Add the two Reference Files bullets in the existing one-line style, describing each contract's
    role — the blind-derivation contract and the comparison contract.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/verify.test.ts` passes, including the pre-existing
      step-contiguity and no-hardcoded-step-count guards.
    - Step headings in `verify-game.md` are contiguous, 0-indexed, with no gap or duplicate — proven
      by the existing test, not by inspection.
    - The new step names `derive-recheck.md`, `derive-compare.md`, `BS-DERIVE-V1`,
      `BS-DERIVE-COMPARE-V1`, and `verify-derive-recheck`.
    - The new step states the project-wide scope and the never-gates-Close property explicitly.
    - Reference Files gains exactly two new bullets, in the existing one-line style.
    - `npm test` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/verify.test.ts</automated>
  </verify>
  <done>The router has a CHECK-04 step, Close is renumbered cleanly, and both new routes are listed and pinned.</done>
</task>

<task type="auto">
  <name>Task 2: The Context-Economics carve-out, with a pin</name>
  <files>src/cli/slash-command/bs/verify-game.md, src/cli/slash-command/bs/verify.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/verify-game.md lines 37-46 — the `## Context-Economics Hard Rule`
      section verbatim, especially the observable sentence: "this skill's own transcript should never
      contain a quoted-rule line, a `Derived (p.` line, or a `Visual (p.` line"
    - .planning/phases/174-verify-classifier/174-PROOF.md §3 — the `quotedPass1`/`quotedPass2`
      exception precedent: how a legitimate slice-body-shaped string in a SUBAGENT return was
      documented as an exception to the same observable, rather than the observable being deleted
    - src/cli/slash-command/bs/verify.test.ts lines 256-306 — the VERIFY-07 structural-enforcement
      and transcript-absence-observable tests, the pinning style to follow
    - .planning/phases/177-derived-line-re-derivation/177-RESEARCH.md Question 3 — why the observable
      must be split into two separate checks, never one blanket check
  </read_first>
  <action>
    Add an explicit carve-out note to the Context-Economics Hard Rule section. Do not delete or
    weaken the existing sentence — state the distinction, exactly as 174 did:

    - The ORCHESTRATOR's own transcript still shows zero quoted-rule lines, zero `Derived (p.` lines
      and zero `Visual (p.` lines. That is unchanged and remains the reviewable observable.
    - The exception belongs to the SUBAGENT dispatch prompts and returns. CHECK-04's blind-derivation
      prompt legitimately carries quote lines (that is its entire payload) and its comparison prompt
      legitimately carries a `Derived (p.` line (decision 8 requires citing both derivations).
    - Those payloads are constructed by `boardsmith verify-derive-recheck` and passed through, never
      composed by the orchestrator reading a slice — which is what keeps the orchestrator's own rule
      true.
    - Name the two distinct observables a reviewer checks, and state they are checked SEPARATELY,
      never as one blanket grep: the blind-derivation prompt must contain ZERO `Derived (p.` and ZERO
      `Visual (p.` (no exception applies — this is stronger than VERIFY-07's own check), while the
      comparison prompt and return are EXPECTED to contain a `Derived (p.` line, accounted for by
      this carve-out.

    Cite `174-PROOF.md` §3's `quotedPass1`/`quotedPass2` precedent by name so a reader can see this is
    the established handling, not a new licence.

    Pin it in `verify.test.ts`: the Context-Economics section must contain the carve-out, must still
    contain the original orchestrator-transcript sentence, and must name both observables separately.
  </action>
  <acceptance_criteria>
    - `verify-game.md`'s Context-Economics section still contains its original observable sentence
      verbatim — asserted by a test, so it cannot be quietly deleted later.
    - The carve-out names the blind prompt's ZERO expectation and the comparison prompt's EXPECTED
      match as two separate checks.
    - The carve-out cites `quotedPass1`/`quotedPass2` and 174's precedent.
    - A new test in `verify.test.ts` asserts all three of the above.
    - `npm test` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/verify.test.ts</automated>
  </verify>
  <done>The hard rule and CHECK-04's payloads coexist by explicit, pinned carve-out rather than by contradiction.</done>
</task>

<task type="auto">
  <name>Task 3: Full stale-claim sweep of verify-game.md, recording TRUE findings too</name>
  <files>.planning/phases/177-derived-line-re-derivation/177-SWEEP.md, src/cli/slash-command/bs/verify-game.md</files>
  <read_first>
    - src/cli/slash-command/bs/verify-game.md — the ENTIRE file, post-Task-1/2 state, top to bottom.
      This is a sweep; a targeted read defeats its purpose
    - .planning/phases/177-derived-line-re-derivation/177-PATTERNS.md § `verify-game.md` → "Full
      stale-claim sweep" — the six claims already identified (lines 25-27, 37-46, 113, 146-149,
      163-200, 190-200) with their expected dispositions
    - .planning/phases/177-derived-line-re-derivation/177-RESEARCH.md § `verify-game.md` — the
      independently-performed sweep, for cross-checking; a claim either sweep found must appear in
      the record
    - src/cli/slash-command/bs/verify.test.ts lines 636-640 — the no-hardcoded-step-count regex,
      which only catches the spelled-out words `five` through `nine` and NOT `four` or digits
    - .planning/phases/176-stale-chunk-repair/176-VERIFICATION.md — 176's disclosed limitations and
      its uniform-verdict lesson, for the record's framing
  </read_first>
  <action>
    Sweep `verify-game.md` end to end for every claim this phase could falsify, and write
    `177-SWEEP.md` recording ALL of them — including the ones checked and found STILL TRUE, because
    recording only the broken ones is indistinguishable from not having swept.

    For each claim: quote it verbatim with its line number, state the disposition as one of
    `STILL TRUE (re-verified)`, `REVISED`, or `CARVE-OUT ADDED`, and give the one-sentence reason.
    At minimum cover the six claims the pattern map and research both identified, and state
    explicitly whether Close's condition sentence stays as-is — it should, because decision 15 makes
    CHECK-04 report-only, and adding a third condition would turn an advisory sweep into a gate.

    Beyond those six, do a first-principles pass: any sentence asserting what the skill never does,
    never reads, never writes, or how many of something exists. Specifically grep the whole file for
    spelled-out and digit step counts and reference-file counts — the existing guard regex only
    catches `five` through `nine`, so `four`, `ten`, and digit forms would slip. Fix any count found,
    by citing structure rather than restating a number, and record it.

    Record any claim you checked and could NOT resolve as `UNRESOLVED` with the reason, rather than
    omitting it. Do not fix reference-game content and do not touch other skill files in this task.
  </action>
  <acceptance_criteria>
    - `177-SWEEP.md` lists every claim with a verbatim quote, a line number, and one of the four
      dispositions; at least two entries are `STILL TRUE (re-verified)`.
    - The six claims named in `177-PATTERNS.md` all appear, with dispositions.
    - The record states explicitly that Close's condition is unchanged and why (decision 15).
    - A real grep for spelled-out counts (`four` through `ten`) and digit step counts across
      `verify-game.md` is pasted into the record with its output, even if empty.
    - `npx vitest run src/cli/slash-command/bs/verify.test.ts` passes, including the
      no-hardcoded-count guard.
    - `npm test` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/verify.test.ts</automated>
    <human-check>177-SWEEP.md records claims found still TRUE as well as claims revised, with verbatim quotes and line numbers</human-check>
  </verify>
  <done>Every falsifiable claim in the router is accounted for in a durable record, TRUE ones included.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| router prose ↔ actual pipeline behavior | a stale claim in the router misleads every future reader and reviewer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-14 | Repudiation | the Context-Economics rule violated by design and unremarked | mitigate | Explicit carve-out naming two separate observables, pinned by a test that also protects the original sentence. |
| T-177-15 | Tampering | step renumbering leaving a gap or duplicate | mitigate | Existing contiguity guard must pass; full numeric renumber, not a splice. |
| T-177-16 | Repudiation | an unlisted stale claim surviving the sweep | mitigate | First-principles pass plus a recorded grep for counts the existing guard regex cannot catch; TRUE findings recorded too. |
| T-177-17 | Elevation of privilege | a report-only check silently becoming a Close gate | mitigate | Sweep record states Close's condition is unchanged; no third condition added (decision 15). |
| T-177-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this phase. |
</threat_model>

<verification>
- `npx vitest run src/cli/slash-command/bs/verify.test.ts`
- `npm test`
- `177-SWEEP.md` present with dispositions for every claim, including `STILL TRUE (re-verified)` entries
</verification>

<success_criteria>
- CHECK-04 has a step; Close renumbered contiguously; both routes listed and pinned.
- The Context-Economics carve-out exists, is pinned, and preserves the original observable.
- The sweep is recorded in full, TRUE findings included, with a real count grep pasted.
</success_criteria>

<output>
Create `.planning/phases/177-derived-line-re-derivation/177-05-SUMMARY.md` when done
</output>
