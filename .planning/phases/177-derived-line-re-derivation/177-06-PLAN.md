---
phase: 177-derived-line-re-derivation
plan: 06
type: execute
wave: 4
depends_on: ["177-05"]
files_modified:
  - .planning/phases/177-derived-line-re-derivation/177-PREDICTION.md
  - .planning/phases/177-derived-line-re-derivation/177-PROOF.md
autonomous: true
requirements: [CHECK-04]

must_haves:
  truths:
    - "A per-line predicted verdict for all 22 `Derived` lines exists in git BEFORE any dispatch runs."
    - "The prediction commits to a substantial `underivable` share, with reasoning naming the Derived-depends-on-Derived chain."
    - "The proof run works on `cp -R` copies; both originals' whole-tree sha256 are recorded before anything runs."
    - "The dispatch mechanism actually used is named — `claude -p` subprocess or native Task/Agent tool."
  artifacts:
    - path: ".planning/phases/177-derived-line-re-derivation/177-PREDICTION.md"
      provides: "The committed-before-measurement distribution prediction, per line and in total"
      contains: "underivable"
    - path: ".planning/phases/177-derived-line-re-derivation/177-PROOF.md"
      provides: "§1 — setup, sha256 baselines, real install, dispatch mechanism"
      contains: "sha256"
  key_links:
    - from: "177-PREDICTION.md"
      to: "177-PROOF.md"
      via: "the prediction's commit hash cited in the proof, predating the measurement commit"
      pattern: "commit"
---

<objective>
Commit the distribution prediction BEFORE any dispatch exists, then stage the proof run.

Purpose: git ordering is the only evidence a prediction was not retrofitted to the result — the
discipline 174-06 and 176-05 both used, and the reason their bars mean anything. A prediction written
after seeing 22 verdicts is not a prediction. Research also warns specifically against under-predicting
`underivable`: it is not only PDF-image derivations that are unreachable, but every `Derived` line
whose supporting evidence is ITSELF a diagram-description `Derived` line — structurally excluded from
the blind payload by construction.
Output: `177-PREDICTION.md` in its own commit, and `177-PROOF.md` §1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/177-derived-line-re-derivation/177-CONTEXT.md
@.planning/phases/177-derived-line-re-derivation/177-RESEARCH.md
@.planning/phases/177-derived-line-re-derivation/177-05-SUMMARY.md
@.planning/phases/173-verify-pipeline-core/173-PROOF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Predict the distribution, per line, and commit it before any dispatch</name>
  <files>.planning/phases/177-derived-line-re-derivation/177-PREDICTION.md</files>
  <read_first>
    - .planning/phases/174-verify-classifier/174-FIXTURES/seven/live/*.md and
      .planning/phases/174-verify-classifier/174-FIXTURES/one-two-punch/live/*.md — read every
      `Derived (p.` line and enough surrounding quote content to predict per line, not in aggregate
    - .planning/phases/177-derived-line-re-derivation/177-RESEARCH.md Question 2 (slice-locality: the
      two representative cases, including `seven`'s deck-math line whose only support is another
      `Derived` line) and Pitfall 2 (under-predicting `underivable`) and Pitfall 1 (at least 3
      unqualified `seven` presentation-shaped lines that must land `not-rule-bearing`)
    - .planning/phases/177-derived-line-re-derivation/177-CONTEXT.md decisions 10, 11
    - .planning/phases/176-stale-chunk-repair/176-PROOF.md §3 "The bar, declared and committed BEFORE
      dispatch" — the precedent format
  </read_first>
  <action>
    Write `177-PREDICTION.md` WITHOUT dispatching anything. State at the top, plainly, that no
    dispatch has been run and no verdict exists yet.

    Enumerate all 22 `Derived` lines (10 in `seven`, 12 in `one-two-punch`) by slice path and line
    number, quoting each verbatim. For each, predict one of `agrees` / `disagrees` / `underivable` /
    `not-rule-bearing` with a one-sentence reason grounded in what that slice's QUOTE LINES actually
    contain — a per-line prediction, because an aggregate percentage cannot be wrong in an
    informative way.

    Then state the predicted totals per verdict, summing to 22.

    Predict a genuinely substantial `underivable` share and say why in prose: the blind payload
    excludes ALL `Derived` lines, so any line whose evidentiary support is another `Derived` line's
    diagram description is underivable BY CONSTRUCTION, not by soft judgment. A prediction under 20%
    `underivable` would look like it was made without sampling the real lines — if that is genuinely
    what the per-line reading supports, say so explicitly and defend it, do not hedge to a
    comfortable number.

    State the interpretation rules BEFORE the result exists, so they cannot be chosen afterward:
    (a) a large `underivable` share is a REAL FINDING about the ingest contract — quote lines do not
    carry what derivations actually used — and will be reported as such, never tuned away by relaxing
    decision 5's independence; (b) a UNIFORM distribution in EITHER direction proves consistency, not
    discrimination, and will be labelled that way — 176's corpus returned 60/60 a single verdict and
    was honestly recorded as such; (c) zero `not-rule-bearing` verdicts would be SUSPICIOUS given at
    least 3 unqualified presentation-shaped lines were measured in `seven` alone.

    Commit this file on its own, with a message naming it as a pre-dispatch prediction. Record the
    commit hash in the file's own summary section is not possible — instead, report the hash in the
    plan SUMMARY so plan 07 can cite it.
  </action>
  <acceptance_criteria>
    - `177-PREDICTION.md` contains 22 per-line predictions with verbatim quotes, slice paths and line
      numbers; the per-verdict totals sum to 22.
    - Every prediction carries a one-sentence reason referencing that slice's quote-line content.
    - The three interpretation rules (mass-underivable, uniform-result, zero-not-rule-bearing) are
      stated before any result exists.
    - The file states explicitly that no dispatch has been run.
    - `git log` shows `177-PREDICTION.md` committed in its own commit, and that commit contains no
      measurement artifact.
    - The commit hash is recorded in the plan SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>git log --oneline -1 -- .planning/phases/177-derived-line-re-derivation/177-PREDICTION.md</automated>
    <human-check>177-PREDICTION.md has 22 per-line predictions and is committed before any dispatch artifact exists</human-check>
  </verify>
  <done>A falsifiable, per-line prediction exists in git ahead of any measurement.</done>
</task>

<task type="auto">
  <name>Task 2: Stage the proof run — copies, baselines, real install, dispatch mechanism</name>
  <files>.planning/phases/177-derived-line-re-derivation/177-PROOF.md</files>
  <read_first>
    - .planning/phases/173-verify-pipeline-core/173-PROOF.md — the established proof structure:
      numbered sections, real command transcripts, preflight-on-the-originals, byte-identical
      originals re-verification, "What is still unproven", and §6's dispatch-mechanism record
    - .planning/phases/176-stale-chunk-repair/176-PROOF.md §1 — "Setup, fixture provenance, sha256
      baselines, dispatch mechanism" and "Real install (never a symlink assumption)"
    - .planning/phases/177-derived-line-re-derivation/177-CONTEXT.md decision 12 and § Specifics
      (proof bar unchanged from 171-176)
    - .planning/phases/177-derived-line-re-derivation/177-RESEARCH.md § Environment Availability and
      Question 5 (the pinned commits, and the pre-existing `one-two-punch` `.boardsmith/` deletions
      that are a previously-documented Phase 173 exception, not this phase's concern)
  </read_first>
  <action>
    Write `177-PROOF.md` §1.

    Preflight ON THE ORIGINALS, before any copy: record `git log -1` and `git status --short` for
    `~/BoardSmithGames/seven` and `~/BoardSmithGames/one-two-punch`, confirm each matches its pinned
    commit, and record each repo's whole-tree sha256 baseline. Note the pre-existing `one-two-punch`
    `.boardsmith/` deletions explicitly as the previously-documented Phase 173 exception — do not
    silently absorb them and do not claim a clean tree that is not clean.

    Make `cp -R` copies into a scratch directory. Every subsequent command in this phase's proof runs
    against the copies; the originals are never written to. Say that in the proof.

    Install the skills into each copy for real — never a symlink assumption — and assert the installed
    contract files exist with a direct filesystem check on the installed paths, not by trusting the
    installer's console output. Include `derive-recheck.md` and `derive-compare.md` in that check.

    Record the dispatch mechanism honestly and in advance: name whether dispatches go through a
    `claude -p` OS subprocess or the native Task/Agent tool, and cite `173-PROOF.md` §6 as the
    precedent for how that distinction has been recorded. Every real dispatch in Phases 173-176 used a
    `claude -p` subprocess; if that is what this phase uses, say so plainly rather than describing
    dispatch generically. Do not imply native dispatch if a subprocess is used.

    Paste real command output for the enumeration step — `boardsmith verify-derive-recheck --json`
    against each copy — showing the enumerated `Derived` line total and the mechanically-excluded
    presentation count per game. Reconcile against the 10 / 12 / 22 counts; if the real numbers
    differ, report the real numbers and reconcile them, never adjust the claim silently.

    Do not dispatch anything in this task. §1 is setup and enumeration only.
  </action>
  <acceptance_criteria>
    - §1 records both originals' pinned commit hashes, `git status --short` output, and whole-tree
      sha256 baselines, taken before any copy.
    - The `one-two-punch` `.boardsmith/` deletions are named as the pre-existing Phase 173 exception.
    - §1 shows the real `cp -R` commands and states that all work runs on copies.
    - The real install is asserted by direct filesystem check on installed paths, and both new
      contract files appear in that check.
    - The dispatch mechanism is named explicitly, with `173-PROOF.md` §6 cited.
    - Real `verify-derive-recheck --json` enumeration output is pasted per game, with counts
      reconciled against 10 / 12 / 22.
    - No verdict, dispatch prompt, or distribution appears in §1.
    - Both originals' sha256 re-verified unchanged at the end of the task, recorded in the proof.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
    <human-check>177-PROOF.md §1 shows real preflight, copies, install assertions, dispatch mechanism, and enumeration counts with no verdicts</human-check>
  </verify>
  <done>The proof run is staged on copies with baselines recorded and the dispatch mechanism stated, before any judgment happens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proof run → reference-game repos | real commands run against `cp -R` copies of read-only pinned repos |
| prediction → measurement | git ordering is the only guarantee the prediction was not retrofitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-18 | Repudiation | a prediction written after the result | mitigate | `177-PREDICTION.md` committed in its own commit containing no measurement artifact; hash cited by plan 07. |
| T-177-19 | Tampering | `~/BoardSmithGames` originals modified by the proof run | mitigate | All work on `cp -R` copies; whole-tree sha256 recorded before and re-verified after. |
| T-177-20 | Spoofing | dispatch mechanism described generically or overstated as native | mitigate | Mechanism named explicitly in §1 with `173-PROOF.md` §6 cited. |
| T-177-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this phase. |
</threat_model>

<verification>
- `git log` shows `177-PREDICTION.md` in its own commit, before any dispatch artifact
- `177-PROOF.md` §1 present with baselines, real install assertions and enumeration counts
- Both originals' whole-tree sha256 diffs empty
- `npm test` green

</verification>

<success_criteria>
- 22 per-line predictions committed before measurement, with interpretation rules fixed in advance.
- Proof staged on copies, originals byte-identical, install proven by filesystem check.
- Dispatch mechanism named honestly.
</success_criteria>

<output>
Create `.planning/phases/177-derived-line-re-derivation/177-06-SUMMARY.md` when done
</output>
