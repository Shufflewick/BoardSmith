---
phase: 177-derived-line-re-derivation
plan: 07
type: execute
wave: 5
depends_on: ["177-06"]
files_modified:
  - .planning/phases/177-derived-line-re-derivation/177-PROOF.md
autonomous: true
requirements: [CHECK-04]

must_haves:
  truths:
    - "A REAL blind-derivation dispatch prompt, saved verbatim, contains ZERO `Derived (p.` and ZERO `Visual (p.` lines — grepped, not asserted."
    - "The comparison dispatch prompt IS shown to contain the original `Derived` line, checked separately and accounted for by the carve-out."
    - "All 22 `Derived` lines are dispatched — full corpus, no sample, no coverage caveat."
    - "The measured distribution is compared against the committed prediction, and a mass-`underivable` or uniform result is reported honestly rather than tuned away."
    - "At least one disagreement or underivable finding is shown with BOTH derivations quoted verbatim."
    - "Both reference-game originals are byte-identical after the entire run; nothing in either game's slice content is fixed."
  artifacts:
    - path: ".planning/phases/177-derived-line-re-derivation/177-PROOF.md"
      provides: "CHECK-04's live evidence: the grepped independence observable, the full 22-line distribution, and the closure section"
      contains: "BS-DERIVE-V1"
  key_links:
    - from: "177-PROOF.md"
      to: "verify-derive-recheck --json"
      via: "measured per-verdict counts pasted from real command output"
      pattern: "verdictCounts"
---

<objective>
Run CHECK-04 for real on all 22 `Derived` lines, and PROVE the blind payload's independence by
grepping an actual dispatch prompt.

Purpose: decision 5 is the whole phase, and it is only worth anything if it is structural. A
re-derivation that saw the original is a confirmation — it would report high agreement whether or not
the originals were sound. The observable is a real prompt with zero `Derived (p.` matches. Grep it;
never assert it.
Output: `177-PROOF.md` §2-§4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/177-derived-line-re-derivation/177-CONTEXT.md
@.planning/phases/177-derived-line-re-derivation/177-RESEARCH.md
@.planning/phases/177-derived-line-re-derivation/177-06-SUMMARY.md
@.planning/phases/173-verify-pipeline-core/173-PROOF.md
@.planning/phases/176-stale-chunk-repair/176-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: §2 — the grepped blind-independence observable</name>
  <files>.planning/phases/177-derived-line-re-derivation/177-PROOF.md</files>
  <read_first>
    - .planning/phases/173-verify-pipeline-core/173-PROOF.md §3 — the transcript-observable method
      this reuses: a real artifact, a real grep, the output pasted
    - .planning/phases/174-verify-classifier/174-PROOF.md §3 — the `quotedPass1`/`quotedPass2`
      exception, and how a legitimate slice-body-shaped string was accounted for separately rather
      than blanketed
    - .planning/phases/175-impact-map-repair-gating/175-FIXTURES/174-07-contradictory/evidence/
      dispatch-prompt-seven-chunkcheck-classify.txt — the precedent for saving a real dispatch prompt
      verbatim as evidence
    - .planning/phases/177-derived-line-re-derivation/177-RESEARCH.md Question 3 — why the observable
      must be split into two separate checks
    - src/cli/slash-command/bs/verify-game.md § Context-Economics Hard Rule (with plan 05's carve-out)
  </read_first>
  <action>
    Take a REAL blind-derivation dispatch prompt — the exact bytes sent for one of the 22 lines, as
    produced by `buildBlindDerivePayload` and dispatched — and save it verbatim as an evidence file
    alongside the proof. Choose the line whose original `Derived` text is the most distinctive, so a
    leak would be unmissable.

    Then run and paste, as real terminal output, TWO SEPARATE checks:
    (a) `grep -c "Derived (p\." <blind-prompt-file>` → expect exactly 0, and
        `grep -c "Visual (p\." <blind-prompt-file>` → expect exactly 0. No exception applies to the
        blind prompt; this is stronger than VERIFY-07's own check.
    (b) `grep -c "Derived (p\." <compare-prompt-file>` on the corresponding comparison dispatch prompt
        → expect a non-zero match, accounted for by the Context-Economics carve-out and the
        `quotedPass1`/`quotedPass2` precedent, which cite by name.

    Never run one blanket check across both prompt kinds — the whole point is that they have opposite
    expectations. State each expectation BEFORE its output.

    Additionally grep the blind prompt for a distinctive substring of the original `Derived` line's
    own text (not the `Derived (p.` prefix), and paste the zero result — a leak could in principle
    arrive without the prefix.

    If ANY of these greps returns an unexpected result, record it as a measured failure of the
    independence guarantee and stop: do not re-run until a clean prompt appears, and do not edit the
    payload builder and re-grep without recording that it was changed and why.
  </action>
  <acceptance_criteria>
    - A real blind-derivation dispatch prompt is saved verbatim as an evidence file, path recorded in
      §2, and it is the actual dispatched bytes — not a reconstruction.
    - §2 pastes real `grep -c` output for `Derived (p\.` and `Visual (p\.` against the blind prompt,
      both `0`, with the expectation stated before the output.
    - §2 pastes real `grep -c` output for `Derived (p\.` against the comparison prompt, non-zero,
      explicitly accounted for by the carve-out and the named precedent.
    - §2 pastes the zero result of the distinctive-substring grep against the blind prompt.
    - §2 states that the two prompt kinds are checked separately and why.
    - No assertion in §2 substitutes for a grep.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Derived (p\." .planning/phases/177-derived-line-re-derivation/177-PROOF.md</automated>
    <human-check>§2 shows two separate greps with opposite, pre-stated expectations, against real saved prompts</human-check>
  </verify>
  <done>Structural independence is proven by a real prompt and a real grep, with the comparison prompt's legitimate match accounted for separately.</done>
</task>

<task type="auto">
  <name>Task 2: §3 — the full 22-line corpus run and the measured distribution</name>
  <files>.planning/phases/177-derived-line-re-derivation/177-PROOF.md</files>
  <read_first>
    - .planning/phases/177-derived-line-re-derivation/177-PREDICTION.md — the committed per-line
      predictions and the three interpretation rules, and its commit hash from 177-06-SUMMARY.md
    - .planning/phases/176-stale-chunk-repair/176-PROOF.md §2 — the full-corpus results format:
      per-game verdict counts with zeros written explicitly, and the arithmetic shown to add up
    - .planning/phases/176-stale-chunk-repair/176-VERIFICATION.md — 176's disclosed limitations,
      including the uniform 60/60 result honestly recorded as consistency rather than discrimination
    - .planning/phases/177-derived-line-re-derivation/177-CONTEXT.md decisions 9, 10, 11
    - src/cli/slash-command/bs/verify/derive-recheck.md and derive-compare.md — the two contracts as
      shipped, so dispatches follow them by reference rather than a composed prompt
  </read_first>
  <action>
    Dispatch all 22 lines: one `BS-DERIVE-V1` blind derivation each, then a `BS-DERIVE-COMPARE-V1`
    comparison for each line the first stage returned a value for. Skip the comparison dispatch only
    for lines the blind stage already resolved as `not-rule-bearing` or `underivable`, and state that
    skip rule and its resulting dispatch count explicitly. Full corpus — no sampling, therefore no
    coverage caveat to carry.

    Record every verdict through the real `verify-derive-recheck` recording path, then paste the real
    `--json` per-verdict counts per game, with zeros written explicitly. Show the arithmetic: enumerated
    + mechanically-excluded-presentation = total `Derived` lines, and the four verdict counts sum to
    the dispatched total.

    Cite `177-PREDICTION.md`'s commit hash and show it predates this section's work. Then compare
    predicted vs. measured, per verdict AND per line — name every line where the prediction was wrong,
    and do not quietly drop the misses.

    Apply the pre-committed interpretation rules honestly:
    - If a large share came back `underivable`, report it as a REAL FINDING about the ingest
      contract — quote lines do not carry what derivations actually used — and name which lines and
      why. Do NOT relax decision 5's independence to make things derive; that would destroy the
      phase's only real guarantee to manufacture a pleasant number.
    - If the distribution is UNIFORM in either direction, label it as proving consistency rather than
      discrimination, citing 176's 60/60 precedent by name.
    - If zero lines came back `not-rule-bearing`, flag that as suspicious given at least 3 unqualified
      presentation-shaped lines were measured in `seven` alone, and investigate rather than accept.

    Show at least one finding in full: the original `Derived` line verbatim and the blind
    re-derivation verbatim, side by side, as SC-2 requires. If the corpus produced no `disagrees`,
    show an `underivable` finding in the same both-derivations form and say plainly that no
    disagreement was observed on real data.

    Report findings only. Fix nothing in either reference game's slice content — that boundary is held
    from Phases 172 and 176.
  </action>
  <acceptance_criteria>
    - §3 accounts for all 22 lines: each appears with its slice path, line number, blind result and
      final verdict. None is unlisted.
    - Per-game and total per-verdict counts are pasted from real `--json` output, zeros written
      explicitly, and the arithmetic is shown to add up.
    - The dispatch count is stated with the skip rule that produced it.
    - `177-PREDICTION.md`'s commit hash is cited and shown to predate this section.
    - Predicted vs. measured is compared per verdict AND per line, with every miss named.
    - Whichever of the three interpretation rules applies is applied in the words the prediction fixed
      in advance; if none applies, that is stated.
    - At least one finding shows BOTH derivations verbatim.
    - Both reference games' slice content is unchanged — no repair performed.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
    <human-check>§3 accounts for all 22 lines with measured counts, cites the prediction commit, and names every prediction miss</human-check>
  </verify>
  <done>The full corpus is measured against a prediction committed beforehand, with the honest-failure branch honoured if it fires.</done>
</task>

<task type="auto">
  <name>Task 3: §4 — closure, limitations, and originals re-verification</name>
  <files>.planning/phases/177-derived-line-re-derivation/177-PROOF.md</files>
  <read_first>
    - .planning/phases/173-verify-pipeline-core/173-PROOF.md — the "What is still unproven", "How to
      re-run every proof", and originals-re-verification sections' structure and candour
    - .planning/phases/176-stale-chunk-repair/176-VERIFICATION.md — how 176 disclosed its own
      limitations (constructed-only discrimination, 2-of-12 lens coverage, a never-dispatched lens)
    - .planning/phases/177-derived-line-re-derivation/177-CONTEXT.md § Deferred / Carried In — the
      open items this phase inherits and must not silently absorb
    - .planning/phases/177-derived-line-re-derivation/177-SWEEP.md — the router sweep record, to
      cross-reference from the proof
  </read_first>
  <action>
    Write `177-PROOF.md` §4.

    Re-verify both originals: whole-tree sha256 diff against the §1 baselines, output pasted, expected
    empty. If not empty, report exactly what changed rather than explaining it away.

    Assess each of CHECK-04's three success criteria against the evidence actually produced, and say
    MET or NOT MET in those words for each — never a comfortable checkbox:
    SC-1 (every rule-bearing `Derived` line re-derived independently, using only current-slice quote
    lines), SC-2 (a disagreement is reported citing both derivations), SC-3 (runs with no source
    rulebook present and ignores `Visual` lines). Note honestly that both reference games contain zero
    `Visual (p.` lines, so SC-3's `Visual` half is proven by unit test against constructed input
    rather than by live data — the same disposition basis Phase 176 used, named as such.

    Write "What is still unproven", carrying forward at minimum: the dispatch mechanism used (if
    `claude -p` subprocess, state that no native Task/Agent-tool dispatch was exercised, consistent
    with the whole milestone); any interpretation-rule branch that did not fire; and whether the
    `underivable` finding, if substantial, was reported to the ingest contract's owner or merely
    recorded here.

    Write "How to re-run every proof in this document" with the real commands, so a reader can
    reproduce rather than trust.

    Cross-reference `177-SWEEP.md` for the router's stale-claim sweep so the two records are linked.
  </action>
  <acceptance_criteria>
    - §4 pastes the real whole-tree sha256 diff for both originals, empty.
    - Each of SC-1, SC-2, SC-3 is assessed with the literal words MET or NOT MET plus the evidence
      cited.
    - The zero-`Visual`-lines limitation is disclosed with its disposition basis named.
    - "What is still unproven" names the dispatch mechanism's limitation explicitly.
    - "How to re-run" lists real, copy-pasteable commands.
    - `177-SWEEP.md` is cross-referenced.
    - `npm test` green.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
    <human-check>§4 assesses all three SCs as MET or NOT MET, discloses limitations, and shows empty sha256 diffs</human-check>
  </verify>
  <done>The phase closes on evidence, with its limitations disclosed rather than absorbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| blind dispatch prompt → subagent context | the single point where the phase's guarantee can leak |
| proof run → reference-game repos | real commands run against `cp -R` copies of read-only pinned repos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-21 | Spoofing | a re-derivation that saw the original, reported as independent | mitigate | Independence proven by grepping a real saved dispatch prompt, plus a distinctive-substring grep; assertion is explicitly not accepted. |
| T-177-22 | Repudiation | a distribution tuned by relaxing independence until lines derive | mitigate | Interpretation rules committed in `177-PREDICTION.md` before measurement; mass-`underivable` reported as a finding, never tuned. |
| T-177-23 | Repudiation | a uniform result presented as discrimination | mitigate | 176's 60/60 precedent cited; uniform results labelled as consistency. |
| T-177-24 | Tampering | `~/BoardSmithGames` originals modified | mitigate | All work on `cp -R` copies; sha256 diff re-verified and pasted in §4. |
| T-177-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this phase. |
</threat_model>

<verification>
- Real `grep -c` output for both prompt kinds, pasted with pre-stated expectations
- Real `verify-derive-recheck --json` per-verdict counts for all 22 lines
- `177-PREDICTION.md` commit hash predating §3
- Whole-tree sha256 diffs of both originals: empty
- `npm test` green

</verification>

<success_criteria>
- Blind independence proven by grep on a real prompt; comparison prompt's match accounted for separately.
- All 22 lines dispatched and accounted for, counts reconciled, predictions compared per line.
- Honest labelling applied for mass-`underivable`, uniform results, and zero `not-rule-bearing`.
- At least one finding cites both derivations verbatim.
- SC-1/2/3 assessed MET or NOT MET; limitations disclosed; originals byte-identical.
</success_criteria>

<output>
Create `.planning/phases/177-derived-line-re-derivation/177-07-SUMMARY.md` when done
</output>
