---
phase: 177-derived-line-re-derivation
plan: 09
type: execute
wave: 2
gap_closure: true
depends_on: ["177-08"]
files_modified:
  - src/cli/commands/verify-derive-recheck.ts
  - src/cli/commands/verify-derive-recheck.test.ts
autonomous: true
requirements: [CHECK-04]

must_haves:
  truths:
    - "A subagent-authored `reasoning` (or either reading) containing the ledger's own fence marker is REJECTED at construction, never written — the ledger cannot be corrupted by model-controlled free text."
    - "A hand-edited or out-of-enum ledger record fails on READ with one actionable line, instead of reaching the report and producing a NaN verdict count."
    - "The durable recording path is append/upsert, matching the sibling ledgers — the documented per-line call pattern can never destroy a previously recorded verdict."
    - "An `agrees`/`disagrees` verdict cannot be recorded citing zero source quotes."
    - "A compare verdict that contradicts a blind-stage `underivable`/`not-rule-bearing` is rejected, not silently accepted."
  artifacts:
    - path: "src/cli/commands/verify-derive-recheck.ts"
      provides: "Fence-safe, evidence-requiring construction choke point; revalidating reader; upsert-append recorder"
      contains: "recordDeriveVerdict"
  key_links:
    - from: "readDeriveVerdicts"
      to: "createDeriveVerdictRecord"
      via: "every parsed line re-enters the single validation choke point"
      pattern: "createDeriveVerdictRecord\\("
    - from: "recordDeriveVerdict"
      to: "readDeriveVerdicts"
      via: "read-merge-write upsert keyed on slicePath:lineNumber"
      pattern: "readDeriveVerdicts\\("
---

<objective>
Close the ledger-integrity half of GAP 3: CR-04 (fence injection), CR-02 (unvalidated read path),
CR-06 (destructive whole-ledger replace), plus WR-05 (evidence-free verdicts) and WR-04 (the
prompt-only pass-through rule with no code cross-check).

Purpose: GAP 3's write surface (plan 177-10) must not be built on an API that guarantees data loss
the moment a caller exists. The module's own header claims `createDeriveVerdictRecord` is the
single validation choke point; today the read path bypasses it and the recorder replaces the whole
ledger. Fix the API before exposing it.

Output: a fence-rejecting, evidence-requiring construction site; a reader that re-enters that same
choke point; and `recordDeriveVerdict` (singular) as the append/upsert callable the skill text names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177-derived-line-re-derivation/177-REVIEW.md
@.planning/phases/177-derived-line-re-derivation/177-VERIFICATION.md
@.planning/phases/177-derived-line-re-derivation/177-CONTEXT.md
@src/cli/commands/verify-derive-recheck.ts
@src/cli/commands/verify-derive-recheck.test.ts

<interfaces>
Current shape (extracted so the executor need not explore):

- `export function createDeriveVerdictRecord(input: { slicePath; lineNumber; originalLine; verdict: string; reasoning; originalReading?; rederivedReading?; sourceQuotes?: string[] }): DeriveVerdictRecord`
- `const DERIVE_LEDGER_BEGIN = '<!-- boardsmith:derive-verdicts:begin -->'`
- `const DERIVE_LEDGER_END = '<!-- boardsmith:derive-verdicts:end -->'`
- `export async function recordDeriveVerdicts(projectDir, records: DeriveVerdictRecord[]): Promise<{ ledgerPath: string }>` — REPLACES the fenced body
- `export async function readDeriveVerdicts(projectDir): Promise<DeriveVerdictRecord[]>` — `JSON.parse(l) as DeriveVerdictRecord`, no validation
- `atomicWriteFile` imported from `./verify-run.js` — the ONE atomic write path in the repo

The sibling append semantics to match live in `src/cli/commands/verify-classify.ts`:
`appendLedgerLine(ledgerText, relLedgerPath, JSON.stringify(record))` at line 848, used by
`verifyClassifyRecordCommand` (line 763). Read that function before designing the upsert — the
requirement is behavioral parity (append, never destroy), not necessarily the same helper, since
CHECK-04's ledger is project-level with no run scoping (177-CONTEXT.md decision 14).

The blind contract (`src/cli/slash-command/bs/verify/derive-recheck.md`) returns
`{ rederivedValue, sourceQuotes }` where `rederivedValue` may itself be the literal string
`underivable` or `not-rule-bearing`; the compare contract
(`src/cli/slash-command/bs/verify/derive-compare.md`) returns
`{ verdict, reasoning, originalReading, rederivedReading }` and states those two blind outcomes are
"passed through unchanged, never re-adjudicated".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden the single construction choke point — reject fence markers, require evidence, and cross-check the blind pass-through</name>
  <files>src/cli/commands/verify-derive-recheck.ts, src/cli/commands/verify-derive-recheck.test.ts</files>
  <read_first>
    - src/cli/commands/verify-derive-recheck.ts (lines 107-177 — createDeriveVerdictRecord and its doc comment; lines 364-406 — the fence constants)
    - src/cli/slash-command/bs/verify/derive-compare.md (the four-verdict section and the pass-through rule, lines ~60-120)
    - src/cli/commands/verify-classify.ts (lines 800-830 — the sibling's refusal to record sharper/contradictory without both verbatim quotes, the error style to mirror)
    - .planning/phases/177-derived-line-re-derivation/177-REVIEW.md (CR-04, WR-04, WR-05)
  </read_first>
  <behavior>
    - Throws when `reasoning`, `originalReading`, or `rederivedReading` contains `DERIVE_LEDGER_BEGIN` or `DERIVE_LEDGER_END`, with a message naming the offending field and instructing a re-dispatch
    - Throws when `verdict` is `agrees` or `disagrees` and `sourceQuotes` is absent, empty, or contains only whitespace entries
    - Throws when `rederivedValue` is `underivable` or `not-rule-bearing` and `verdict` differs from it, quoting both labels
    - Accepts an `underivable` record whose `sourceQuotes` is empty (there was nothing to cite)
    - Still throws on out-of-enum verdicts, empty reasoning, and a `disagrees` missing either reading (no regression on the existing pins)
  </behavior>
  <action>
    Add `rederivedValue: string` as a REQUIRED input to `createDeriveVerdictRecord` — the blind
    stage's own return, verbatim. There are no non-test callers today, so making it required costs
    nothing and closes WR-04's silent-acceptance hole; do not make it optional to avoid touching
    tests. Persist it on `DeriveVerdictRecord` so the pass-through rule is auditable in the ledger
    rather than only enforced in memory.

    Add three validation blocks inside the existing `if`-chain (never a second validator elsewhere
    in the module, per the header's own posture):
    1. Fence rejection across `reasoning`/`originalReading`/`rederivedReading` (CR-04).
    2. `sourceQuotes` non-empty requirement for `agrees`/`disagrees` (WR-05), in the same error
       style as the existing `originalReading` check.
    3. Blind pass-through cross-check on `rederivedValue` vs `verdict` (WR-04).

    Update the function's doc comment's enumerated throw list to name all seven conditions — the
    comment is currently a four-item list and will otherwise drift.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-derive-recheck.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A node one-liner calling `createDeriveVerdictRecord` with `reasoning` containing `<!-- boardsmith:derive-verdicts:end -->` THROWS, and the thrown message names the field `reasoning`.
    - A call with `verdict: 'agrees'` and `sourceQuotes: []` THROWS.
    - A call with `rederivedValue: 'underivable'` and `verdict: 'agrees'` THROWS naming both labels.
    - `grep -v '^ \*' src/cli/commands/verify-derive-recheck.ts | grep -c 'rederivedValue'` returns at least 3.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Model-controlled text can no longer corrupt the ledger, evidence-free confirmations are unrecordable, and the blind stage's terminal verdicts cannot be re-adjudicated by the compare stage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the read path re-enter the same choke point, so the ledger is not a second entry into the type</name>
  <files>src/cli/commands/verify-derive-recheck.ts, src/cli/commands/verify-derive-recheck.test.ts</files>
  <read_first>
    - src/cli/commands/verify-derive-recheck.ts (lines 408-436 — readDeriveVerdicts; lines 7-46 — the header's "single choke point" claim this task makes true)
    - .planning/phases/177-derived-line-re-derivation/177-REVIEW.md (CR-02 — the executed proof showing a NaN/null verdict count reaching --json)
  </read_first>
  <behavior>
    - A ledger line that is not valid JSON throws one actionable message naming the ledger path, the 1-based record index, and the recovery step — never a raw `SyntaxError`
    - A ledger record with `"verdict":"TOTALLY-BOGUS"` throws through `createDeriveVerdictRecord`'s enum check, never reaching `verdictCounts`
    - A ledger whose fenced body is truncated mid-record throws the same actionable shape
    - A valid ledger round-trips byte-identically through write → read → write
    - `readDeriveVerdicts` still returns `[]` (never throws) when no ledger file exists
  </behavior>
  <action>
    Replace the `JSON.parse(l) as DeriveVerdictRecord` map with a per-line parse-then-revalidate
    that wraps `JSON.parse` in a try/catch emitting an actionable message (ledger path relative to
    `projectDir`, record index, and the "delete the file to re-run CHECK-04 from scratch" recovery
    line), then routes the parsed object through `createDeriveVerdictRecord` so the ledger cannot
    be a second entry path into `DeriveVerdictRecord`.

    Do not coerce silently: a missing required field must surface through the choke point's own
    error, not be papered over with a default that manufactures a valid-looking record.

    Update the module header's "single choke point" paragraph to state that BOTH the write and read
    paths route through `createDeriveVerdictRecord`, since the current text claims a property the
    code did not have.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-derive-recheck.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A test seeds a temp project ledger containing `{"verdict":"TOTALLY-BOGUS",...}` and asserts `readDeriveVerdicts` rejects; a second test asserts the resulting message contains the relative ledger path and does NOT contain the substring `SyntaxError`.
    - A test asserts `verdictCounts` from `verifyDeriveRecheckCommand` never gains a key outside `DERIVE_VERDICTS` (previously reachable per CR-02's executed proof).
    - `grep -c 'as DeriveVerdictRecord' src/cli/commands/verify-derive-recheck.ts` returns 0.
  </acceptance_criteria>
  <done>The ledger read path validates through the same construction site the header claims is singular, and a corrupted ledger produces an actionable message rather than a parser's.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace the destructive recorder with an upsert-append callable, matching the sibling ledgers</name>
  <files>src/cli/commands/verify-derive-recheck.ts, src/cli/commands/verify-derive-recheck.test.ts</files>
  <read_first>
    - src/cli/commands/verify-derive-recheck.ts (lines 364-406 — recordDeriveVerdicts and the ledger path helper)
    - src/cli/commands/verify-classify.ts (lines 763-860 — verifyClassifyRecordCommand and appendLedgerLine, the append semantics to match)
    - src/cli/commands/verify-run.ts (atomicWriteFile — the ONE atomic write path that must remain singular)
    - .planning/phases/177-derived-line-re-derivation/177-REVIEW.md (CR-06 — why the documented per-line pattern is destructive)
    - src/cli/commands/verify-derive-recheck.test.ts (line ~428 — the test pinning "re-recording REPLACES the body", which this task must retarget rather than delete outright)
  </read_first>
  <behavior>
    - `recordDeriveVerdict(projectDir, recordA)` then `recordDeriveVerdict(projectDir, recordB)` leaves BOTH records readable — the pattern that previously destroyed A
    - Recording twice for the same `slicePath:lineNumber` upserts: exactly one record for that location, carrying the second call's content
    - Records are written in a stable order (existing order preserved, new record appended) so the ledger diff is reviewable
    - The whole write remains atomic: no partial ledger is observable, and `atomicWriteFile` remains the only durable write path in the module
    - `replaceDeriveVerdicts` (the renamed batch form) still exists for a full rewrite and is documented as the non-default path
  </behavior>
  <action>
    Rename `recordDeriveVerdicts` to `replaceDeriveVerdicts`, keeping its batch semantics and
    documenting plainly that it REPLACES and is not the callable the workflow uses. Add
    `recordDeriveVerdict(projectDir, record)` (singular) that reads the existing ledger, upserts by
    `slicePath:lineNumber`, and writes the merged set through `replaceDeriveVerdicts` so there is
    still exactly one durable write path — the easy path becomes the non-destructive one, which is
    the pit-of-success shape CR-06 says is inverted today.

    Retarget the existing "re-recording REPLACES the body" test onto `replaceDeriveVerdicts` and
    add the append/upsert tests above against `recordDeriveVerdict`. Also move `fs.mkdir` for the
    ledger directory so the test claim that `atomicWriteFile` is the only durable filesystem
    mutation is accurate, or narrow that claim's wording to "the only durable FILE write" —
    whichever is true after the change; WR-11 flagged the current claim as false.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-derive-recheck.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A test performs two `recordDeriveVerdict` calls for different locations and asserts `readDeriveVerdicts` returns 2 records — this test fails against the pre-fix API.
    - A test performs two `recordDeriveVerdict` calls for the SAME location and asserts exactly 1 record, carrying the second call's `reasoning`.
    - `grep -c 'recordDeriveVerdicts' src/cli/commands/verify-derive-recheck.ts` returns 0 (the plural name is gone; `replaceDeriveVerdicts` and `recordDeriveVerdict` are what remain).
    - `grep -v '^ \*' src/cli/commands/verify-derive-recheck.ts | grep -c 'writeFile(' ` returns 0 — no second durable write path was introduced.
  </acceptance_criteria>
  <done>The callable the workflow uses appends and upserts; a full replace requires deliberately naming the replacing function.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subagent free-prose return → durable ledger | Model-controlled text crosses into a fenced file format whose delimiters it can forge |
| on-disk ledger → report/`--json` consumer | Hand-editable file crosses back into typed application state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-09-01 | Tampering | `createDeriveVerdictRecord` | mitigate | Reject any field containing `DERIVE_LEDGER_BEGIN`/`END` at the single construction site |
| T-177-09-02 | Tampering | `readDeriveVerdicts` | mitigate | Re-enter `createDeriveVerdictRecord` on every parsed line; actionable error on malformed JSON |
| T-177-09-03 | Repudiation | destructive `recordDeriveVerdicts` | mitigate | Upsert-append so a recorded verdict cannot vanish without an explicit `replaceDeriveVerdicts` call |
| T-177-09-04 | Spoofing | evidence-free `agrees` | mitigate | Require non-empty `sourceQuotes` for `agrees`/`disagrees` |
| T-177-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies in this plan; nothing to install |
</threat_model>

<verification>
- `npx vitest run src/cli/commands/verify-derive-recheck.test.ts` green.
- `npx tsc --noEmit` clean.
- CR-04's and CR-02's executed proofs from 177-REVIEW.md re-run and now fail to reproduce; record the observed output in the SUMMARY.
</verification>

<success_criteria>
The ledger API is safe to expose: it cannot be corrupted by model text, cannot silently accept an
invalid record from disk, and its default callable does not destroy prior verdicts.
</success_criteria>

<output>
Create `.planning/phases/177-derived-line-re-derivation/177-09-SUMMARY.md` when done
</output>
