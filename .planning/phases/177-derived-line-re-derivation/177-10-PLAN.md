---
phase: 177-derived-line-re-derivation
plan: 10
type: execute
wave: 3
gap_closure: true
depends_on: ["177-09"]
files_modified:
  - src/cli/commands/verify-derive-recheck.ts
  - src/cli/commands/verify-derive-recheck.test.ts
  - src/cli/cli.ts
  - src/cli/slash-command/bs/verify-game.md
  - src/cli/slash-command/bs/verify/derive-recheck.md
  - src/cli/slash-command/bs/verify.test.ts
autonomous: true
requirements: [CHECK-04]

must_haves:
  truths:
    - "A verdict can be recorded end-to-end from a real caller: `boardsmith verify-derive-record ...` writes a verdict that `boardsmith verify-derive-recheck --json` then reports."
    - "A recorded verdict is never reported against text it was not recorded for — a line edited or shifted since recording reports `pending` with a named staleness reason."
    - "A recorded verdict matching no current candidate is reported as an orphan, never silently discarded."
    - "`verify-game.md` Step 7 names a callable command that exists, not a library function with no entry point."
    - "The report prints no literal `undefined` for any finding field."
  artifacts:
    - path: "src/cli/cli.ts"
      provides: "The registered `verify-derive-record` write command, no bypass option of any kind"
      contains: "verify-derive-record"
    - path: "src/cli/commands/verify-derive-recheck.ts"
      provides: "verifyDeriveRecordCommand, originalLine-aware join, staleRecords and orphanedRecords on the result"
      contains: "verifyDeriveRecordCommand"
  key_links:
    - from: "src/cli/cli.ts"
      to: "verifyDeriveRecordCommand"
      via: "commander .command('verify-derive-record').action(...)"
      pattern: "verify-derive-record"
    - from: "verify-game.md Step 7"
      to: "boardsmith verify-derive-record"
      via: "named CLI invocation in the orchestrator's recording instruction"
      pattern: "verify-derive-record"
    - from: "verifyDeriveRecheckCommand"
      to: "record.originalLine"
      via: "join comparison against the current candidate text"
      pattern: "originalLine ==="
---

<objective>
Close the remaining half of GAP 3 — CHECK-04's missing write surface (CR-05) — and the report-side
correctness blockers it would otherwise expose: CR-03 (stale-record false confirmation), WR-03
(orphaned records silently discarded), WR-06 (`undefined` in designer output), WR-02 and WR-10
(duplicated and misleading "no rulebook/" error paths).

Purpose: 177-VERIFICATION.md names CR-05 "the single clearest 'task complete, goal not achieved'
gap in this phase" — the report command works but nothing outside a test can populate it, and
`verify-game.md` Step 7 prescribes a call pattern with no callable entry point. This plan makes
CHECK-04 operable end-to-end.

Output: `boardsmith verify-derive-record`, an originalLine-aware join, orphan and staleness
reporting, and a Step 7 that names commands that exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177-derived-line-re-derivation/177-REVIEW.md
@.planning/phases/177-derived-line-re-derivation/177-VERIFICATION.md
@.planning/phases/177-derived-line-re-derivation/177-CONTEXT.md
@src/cli/commands/verify-derive-recheck.ts
@src/cli/cli.ts

<interfaces>
The sibling write command this one must match in posture (`src/cli/cli.ts:300-321`):

```
program
  .command('verify-classify-record')
  .description('Record one classification verdict for a pair, atomically appended to the run\'s ledger (stale and provenance are derived, never supplied)')
  .option('--project <dir>', 'Project directory (defaults to cwd)')
  .requiredOption('--run-id <id>', 'The run to record against')
  .requiredOption('--pair-id <id>', 'The pair id to classify (see verify-classify-pairs)')
  .option('--label <value>', ...)
  .option('--evidence <text>', ...)
  .option('--quoted-pass1 <text>', ...)
  .option('--quoted-pass2 <text>', ...)
  .option('--json', ...)
  .action(verifyClassifyRecordCommand);
```

CHECK-04's command takes NO `--run-id` (177-CONTEXT.md decision 14 — project-level, no run concept)
and NO bypass option of any kind (172-CONTEXT.md decision 9's posture, already stated in the
existing `verify-derive-recheck` registration comment at `cli.ts:416-420`).

Current result type (`verify-derive-recheck.ts:455-463`):
```
export interface VerifyDeriveRecheckResult {
  project: string;
  enumeratedCount: number;
  presentationExcludedCount: number;
  verdictCounts: Record<DeriveVerdict, number>;
  findings: DeriveRecheckFinding[];
}
```

After plan 177-09, the recording callable is `recordDeriveVerdict(projectDir, record)` (singular,
upsert-append) and `createDeriveVerdictRecord` requires `rederivedValue` and non-empty
`sourceQuotes` for `agrees`/`disagrees`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the report incapable of false confirmation — join on text, surface staleness and orphans, and collapse the duplicated error paths</name>
  <files>src/cli/commands/verify-derive-recheck.ts, src/cli/commands/verify-derive-recheck.test.ts</files>
  <read_first>
    - src/cli/commands/verify-derive-recheck.ts (lines 442-569 — the result types, join, and printer; lines 192-221 — readLiveSlices' catch-all error; lines 488-502 — the redundant fs.access pre-check)
    - .planning/phases/177-derived-line-re-derivation/177-REVIEW.md (CR-03, WR-03, WR-06, WR-02, WR-10 — each with its executed proof or precise line reference)
    - src/cli/commands/verify-derive-recheck.test.ts (the verifyDeriveRecheckCommand describe block, so the new assertions extend rather than duplicate existing ones)
  </read_first>
  <behavior>
    - A ledger record whose `originalLine` differs from the current candidate's text reports `pending`, and the mismatch appears in `staleRecords` naming the location and the recorded text
    - A ledger record whose location matches no current candidate appears in `orphanedRecords` and is printed once in the human output
    - Neither a stale nor an orphaned record contributes to `verdictCounts`
    - A `disagrees` finding missing a reading prints an explicit "(missing — re-run the comparison dispatch)" instead of the literal `undefined`
    - `rulebook/` present but unreadable (EACCES) reports the real condition, not "No rulebook/ directory"
    - `rulebook/` missing produces exactly ONE message, naming `--project`, with no stack frame and no `.ts:` reference
  </behavior>
  <action>
    Add `originalLine` to the join predicate: look the record up by `slicePath:lineNumber`, then
    accept it only when `record.originalLine === entry.text`. On mismatch, push a staleness entry
    naming the location, the recorded text, and the current text, and report the finding `pending`
    — never inherit a verdict for text that changed. `record.originalLine` is currently stored and
    never read; this is the field that makes CR-03's false confirmation detectable.

    Compute `orphanedRecords` as the recorded set minus the current candidate locations, and add
    both `staleRecords` and `orphanedRecords` to `VerifyDeriveRecheckResult` so Phase 179's
    `--json` consumer sees them. Print one line per orphan and per stale record in the human output
    — enumerate-and-report, never silently drop, matching the module's stated posture.

    Guard the disagreement printer against absent readings with an explicit missing-value sentence
    rather than interpolating a possibly-undefined value.

    Delete the `fs.access(rulebookDir)` pre-check in `verifyDeriveRecheckCommand` (it succeeds for a
    regular file named `rulebook`, so it does not establish what its message claims) and move the
    `--project` sentence into `readLiveSlices`' own throw, so exactly one "no rulebook/" message
    exists in the module. In that same catch, inspect `(err as NodeJS.ErrnoException).code` and
    claim "no rulebook/ directory" only for `ENOENT`; otherwise report the real condition including
    the code.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-derive-recheck.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A test records a verdict for a line, rewrites that line's text in the slice, re-runs the report, and asserts the finding is `pending` and `staleRecords` has length 1 — this test fails against the pre-fix join (CR-03's executed proof).
    - A test records a verdict for a slice path that no longer exists and asserts `orphanedRecords` has length 1 and `findings` does not silently absorb it.
    - `grep -v '^ \*' src/cli/commands/verify-derive-recheck.ts | grep -c 'No rulebook/ directory'` returns 1.
    - `grep -c 'fs.access' src/cli/commands/verify-derive-recheck.ts` returns 0.
    - A test asserts the human output for a reading-less `disagrees` contains no occurrence of the literal string `undefined`.
  </acceptance_criteria>
  <done>A verdict can only be reported against the exact text it was recorded for; stale and orphaned records are visible rather than silently satisfying or vanishing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the missing write surface — verifyDeriveRecordCommand and its CLI registration</name>
  <files>src/cli/commands/verify-derive-recheck.ts, src/cli/cli.ts, src/cli/commands/verify-derive-recheck.test.ts</files>
  <read_first>
    - src/cli/cli.ts (lines 285-330 — the verify-classify-record registration to match in posture; lines 414-431 — the existing CHECK-04 registration and its no-bypass comment)
    - src/cli/commands/verify-classify.ts (lines 763-860 — verifyClassifyRecordCommand's shape: option parsing, validation delegation, --json purity, exit-code discipline)
    - src/cli/commands/verify-derive-recheck.ts (createDeriveVerdictRecord and recordDeriveVerdict as they stand after plan 177-09)
    - .planning/phases/177-derived-line-re-derivation/177-REVIEW.md (CR-05 — the fix direction and the sibling precedent)
  </read_first>
  <behavior>
    - `boardsmith verify-derive-record --project <dir> --slice-path rulebook/01-x.md --line-number 4 --original-line '...' --rederived-value '...' --verdict agrees --reasoning '...' --source-quote '...'` writes a record readable by `verify-derive-recheck --json`
    - Recording a second verdict for a different line preserves the first (the upsert-append from 177-09, exercised through the real CLI rather than only in a unit test)
    - An invalid verdict exits non-zero with `createDeriveVerdictRecord`'s message — validation is delegated, never re-implemented in the command
    - `--json` emits the recorded record and the relative ledger path and nothing else on stdout
    - No `--force`, `--skip`, `--overwrite`, or env-var bypass exists on the command
  </behavior>
  <action>
    Add `verifyDeriveRecordCommand(options)` to `verify-derive-recheck.ts`: parse the options, build
    the record through `createDeriveVerdictRecord` (all validation delegated — the command adds no
    second validator), persist through `recordDeriveVerdict`, and report the relative ledger path.
    Follow `verifyClassifyRecordCommand`'s structure for option handling, `--json` purity, and
    error surfacing so the two commands read the same way.

    Register it in `cli.ts` immediately after the existing `verify-derive-recheck` registration,
    inside the same CHECK-04 comment block, with `--project` optional and `--slice-path`,
    `--line-number`, `--original-line`, `--rederived-value`, `--verdict`, `--reasoning` required,
    plus optional `--original-reading`, `--rederived-reading`, repeatable `--source-quote <text...>`,
    and `--json`. Register NO `--run-id` (decision 14) and no bypass option of any kind. Extend the
    existing comment block to state that this command is the ONLY write surface for CHECK-04's
    ledger.

    Add an end-to-end test that spawns or invokes the command function twice against a temp project
    and asserts both verdicts survive and are reported — the observable CR-05 says does not exist
    today.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-derive-recheck.test.ts && node dist/cli/cli.js verify-derive-record --help</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "verify-derive-record'" src/cli/cli.ts` returns 1.
    - `grep -n "force\|--skip\|--overwrite" src/cli/cli.ts | grep -c 'verify-derive'` returns 0.
    - A repo-wide grep for `createDeriveVerdictRecord|recordDeriveVerdict` outside `verify-derive-recheck.{ts,test.ts}` now returns at least one non-prose hit (the CLI registration's imported action) — the zero-non-test-callers condition CR-05 names is gone.
    - The end-to-end test asserts two successive records both appear in `verify-derive-recheck --json` output.
  </acceptance_criteria>
  <done>CHECK-04 has a callable write surface with the same no-bypass posture as every sibling check, exercised end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Correct the skill text that prescribes the non-existent call pattern, and pin it</name>
  <files>src/cli/slash-command/bs/verify-game.md, src/cli/slash-command/bs/verify/derive-recheck.md, src/cli/slash-command/bs/verify.test.ts</files>
  <read_first>
    - src/cli/slash-command/bs/verify-game.md (Step 7, lines ~168-186 — the recording instruction naming `recordDeriveVerdicts`)
    - src/cli/slash-command/bs/verify/derive-recheck.md (lines 44-66 — the "Your inputs" section WR-08 says misdescribes who strips the payload and omits Named-but-undefined)
    - src/cli/slash-command/bs/verify.test.ts (the existing step-contiguity, no-hardcoded-count, and by-reference drift guards this task must keep true)
    - .planning/phases/177-derived-line-re-derivation/177-CONTEXT.md (Integration Points — the standing instruction to SWEEP verify-game.md for stale cross-file claims, not only fix named ones)
  </read_first>
  <behavior>
    - Step 7 names `boardsmith verify-derive-record` as the recording invocation, per line, and no longer names `recordDeriveVerdicts`
    - `derive-recheck.md` attributes the stripping to `buildBlindDerivePayload` (never the orchestrator) and names `Named-but-undefined` in the never-given list
    - A drift guard test fails if any file under `bs/verify/` or `verify-game.md` names a CHECK-04 function with no registered CLI command
    - The existing step-contiguity and no-hardcoded-count guards still pass
  </behavior>
  <action>
    Rewrite Step 7's recording sentence to name the real command and the real per-line pattern (one
    `verify-derive-record` invocation per line, appended, never a whole-ledger rewrite), and keep
    the "formatted, never computed" discipline for the report.

    Fix `derive-recheck.md`'s "Your inputs" per WR-08: restate the stripping as done by
    `buildBlindDerivePayload` (`verify-derive-recheck.ts`) before the prompt was assembled — the
    orchestrator never opens the slice — and add `any Named-but-undefined line` as a third bullet
    in the never-given list.

    Then SWEEP both files for other stale cross-file claims rather than only fixing the two named
    ones: 177-CONTEXT.md records that this milestone hit stale prose in five separate places across
    four phases, and in every case the survivor was a claim nobody had listed. Record what the
    sweep found — including "nothing else" if that is the honest answer — in the SUMMARY.

    Add a guard to `verify.test.ts` asserting that every `boardsmith verify-*` command name
    appearing in `verify-game.md` and in the files under `bs/verify/` is registered in `cli.ts`.
    That guard is what makes CR-05's failure mode — skill text prescribing a non-existent entry
    point — impossible to reintroduce silently.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/verify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'recordDeriveVerdicts' src/cli/slash-command/bs/verify-game.md src/cli/slash-command/bs/verify/derive-compare.md` returns 0 for both files.
    - `grep -c 'verify-derive-record' src/cli/slash-command/bs/verify-game.md` returns at least 1.
    - `grep -c 'Named-but-undefined' src/cli/slash-command/bs/verify/derive-recheck.md` returns at least 1.
    - The new `verify.test.ts` guard is observed FAILING when `verify-derive-record` is temporarily removed from `cli.ts`, then passing after restore; the observed failure output is recorded in the SUMMARY.
    - The SUMMARY names every stale claim the sweep found, or states explicitly that the sweep found none.
  </acceptance_criteria>
  <done>The documented workflow names commands that exist, and a guard prevents that link from breaking again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → verdict record | Orchestrator-supplied values cross into a durable ledger |
| stored verdict → designer-facing report | A record written at one time is reported against a file that may have changed since |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-10-01 | Spoofing | stale verdict reported as live confirmation | mitigate | Join includes `record.originalLine === entry.text`; mismatch reports `pending` with a named staleness reason |
| T-177-10-02 | Elevation of privilege | bypass flags on the new write command | mitigate | Register no `--force`/`--skip`/`--overwrite` and no env-var escape; validation delegated to `createDeriveVerdictRecord` |
| T-177-10-03 | Information disclosure | error messages leaking internal paths/stack frames | mitigate | Single actionable "no rulebook/" message naming `--project`, no stack frame, no `.ts:` reference (pinned by existing test) |
| T-177-10-04 | Repudiation | orphaned records vanishing from the report | mitigate | `orphanedRecords` surfaced on the result and printed |
| T-177-10-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies in this plan; nothing to install |
</threat_model>

<verification>
- `npx vitest run src/cli/` green.
- `npx tsc --noEmit` clean.
- `node dist/cli/cli.js verify-derive-record --help` lists the required options and no bypass flag.
</verification>

<success_criteria>
A verdict is recordable and reportable end-to-end from a real caller; a recorded verdict can never
be reported against text it was not recorded for; and the skill text names only commands that exist.
</success_criteria>

<output>
Create `.planning/phases/177-derived-line-re-derivation/177-10-SUMMARY.md` when done
</output>
