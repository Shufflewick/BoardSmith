---
phase: 177-derived-line-re-derivation
plan: 12
type: execute
wave: 5
gap_closure: true
depends_on: ["177-11"]
files_modified:
  - .planning/phases/177-derived-line-re-derivation/177-TARGETING-PREDICTION.md
  - .planning/phases/177-derived-line-re-derivation/177-PROOF-2.md
autonomous: true
requirements: [CHECK-04]

must_haves:
  truths:
    - "The targeting metric's definition and its predicted values are committed to git BEFORE any dispatch of the re-run — provable by commit ordering, not by assertion."
    - "The re-run uses REAL `claude -p` dispatches against fresh `cp -R` copies, with both `~/BoardSmithGames` originals confirmed byte-identical before and after."
    - "Every verdict in the re-run is written through the real `boardsmith verify-derive-record` CLI command, not by importing the library from a driver script."
    - "The saved blind prompts are grepped for the three annotation families AND for each target's own slice path and line number — all zero-expectation greps run on real artifacts, not asserted."
    - "The measured targeting outcome is reported whatever it says, with no threshold moved after the fact."
  artifacts:
    - path: ".planning/phases/177-derived-line-re-derivation/177-TARGETING-PREDICTION.md"
      provides: "The pre-registered metric definitions and predicted values, committed before measurement"
      min_lines: 60
    - path: ".planning/phases/177-derived-line-re-derivation/177-PROOF-2.md"
      provides: "The gap-closure live proof: setup and hashes, the independence greps, the re-measured distribution and targeting outcome, and closure"
      contains: "BS-DERIVE-V1"
  key_links:
    - from: "177-PROOF-2.md"
      to: "boardsmith verify-derive-record"
      via: "every recorded verdict written through the registered CLI write command"
      pattern: "verify-derive-record"
    - from: "177-PROOF-2.md"
      to: "177-TARGETING-PREDICTION.md"
      via: "predicted-vs-measured comparison citing the prediction's commit hash"
      pattern: "177-TARGETING-PREDICTION"
---

<objective>
Re-run the live proof against the fixed module, so the phase's headline number is measured on code
that no longer produces the artifact it measured last time.

Purpose: 177-PROOF.md's 56% `disagrees` rate is dominated by targeting-collapse artifacts. After
plans 177-08 through 177-11 the payload is decoration-proof, pointer-free, focus-narrowed, and
carries a `factAlignment`. None of that is worth anything until it is measured on real dispatches
against the real corpus — and the measurement is only honest if its metric was fixed before the
result existed.

Output: a committed pre-registration, then `177-PROOF-2.md` following the structure 177-PROOF.md
established.

Binding discipline, from this phase's own convention: `177-PREDICTION.md` is committed and
immutable — do not edit it. Never tune a threshold in response to a result. A worse number than
177-PROOF.md's is a valid outcome and gets reported as-is.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177-derived-line-re-derivation/177-PROOF.md
@.planning/phases/177-derived-line-re-derivation/177-PREDICTION.md
@.planning/phases/177-derived-line-re-derivation/177-CONTEXT.md
@.planning/phases/177-derived-line-re-derivation/177-VERIFICATION.md

<interfaces>
The proof bar this phase already met once and must meet again (177-PROOF.md §1 and §4):

- Preflight sha256 baselines on the ORIGINALS before any copy
- `cp -R` copies; all subsequent work runs against copies only
- A real install of the bs- skills into each copy, never a symlink assumption
- Dispatch mechanism stated honestly in advance — every "real dispatch" in Phases 173-177 used a
  `claude -p` OS subprocess, never native Task-tool dispatch (177-CONTEXT.md carried-in note)
- Enumeration reconciled against the real 10 / 12 / 22 counts, never adjusted silently
- Whole-tree sha256 diff on both originals after the run, expected empty
- A "How to re-run every proof in this document" section carrying the actual commands

Cross-repo proof targets (177-CONTEXT.md):
- `~/BoardSmithGames/seven` — pinned at `a03f38d4792af9dfc7c798be69686fc3230f54dd`, 10 `Derived`, 0 `Visual`
- `~/BoardSmithGames/one-two-punch` — pinned at `7e69471bd8980a854f3e351f2f486e1fb6f712b9`, 12 `Derived`, 0 `Visual`

Prior measured baseline this run is compared against (177-PROOF.md §3): 16 dispatched candidates + 6
mechanically presentation-excluded; `agrees` 4, `disagrees` 9, `underivable` 1, `not-rule-bearing` 2;
29 real dispatches (16 blind + 13 comparison). 8 of the 9 `disagrees` are identified in that proof
as targeting-collapse artifacts; `one-two-punch:52` is identified as the one genuine content
disagreement.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-register the targeting metric and its predicted values, commit before any dispatch, and dry-run the harness</name>
  <files>.planning/phases/177-derived-line-re-derivation/177-TARGETING-PREDICTION.md</files>
  <read_first>
    - .planning/phases/177-derived-line-re-derivation/177-PREDICTION.md (the pre-registration format to follow, including its "Interpretation rules, fixed BEFORE any result exists" section — read for shape, do not edit)
    - .planning/phases/177-derived-line-re-derivation/177-PROOF.md (§3 and its "Interpretation rules — applied honestly" section, where none of the three pre-committed rules fired and a fourth cause had to be reported)
    - src/cli/commands/verify-derive-recheck.ts (the shipped `targetingAmbiguousCount`, `offTargetDisagreements`, `genuineDisagreements`, and `factAlignment` fields after plan 177-11 — the metric must be defined in terms of fields that exist)
  </read_first>
  <action>
    Write `.planning/phases/177-derived-line-re-derivation/177-TARGETING-PREDICTION.md` stating, in
    advance of any dispatch:

    1. Metric definitions expressed in the shipped `--json` field names — at minimum the mechanical
       pre-dispatch payload distinctness rate, `targetingAmbiguousCount`, the split of `disagrees`
       into `offTargetDisagreements` and `genuineDisagreements`, and the phase-goal unit that plan
       177-13 will report.
    2. A predicted value for each metric, per game and in total, with the reasoning behind it.
    3. Interpretation rules fixed before any result exists — including, explicitly, what outcome
       would mean the targeting fix DID NOT work and exactly what would be reported in that case.
       Name the failure outcome as concretely as the success outcome; a pre-registration that only
       describes success is not a pre-registration.
    4. An explicit statement that no threshold in this file may be edited after the run and that
       `177-PREDICTION.md` remains untouched.

    Commit this file alone, with no source or proof changes in the same commit, so `git log` proves
    the prediction predates the measurement. Record the commit hash in the SUMMARY.

    Then dry-run the harness with dispatch DISABLED: build every payload for all 22 real candidates,
    compute and record the mechanical pre-dispatch metrics, dispatch nothing. Any harness defect must
    surface before real dispatches are spent.
  </action>
  <verify>
    <automated>git show --stat --oneline $(git log --format=%H -1 -- .planning/phases/177-derived-line-re-derivation/177-TARGETING-PREDICTION.md)</automated>
  </verify>
  <acceptance_criteria>
    - `git log --format=%H -1 -- .planning/phases/177-derived-line-re-derivation/177-TARGETING-PREDICTION.md` resolves, and `git show --stat` for that commit lists ONLY that file.
    - `git diff HEAD -- .planning/phases/177-derived-line-re-derivation/177-PREDICTION.md` is empty — the original prediction was not edited.
    - The file's interpretation-rules section names a concrete failure outcome, not only a success outcome.
    - The SUMMARY records the dry-run's mechanical pre-dispatch numbers, including the payload distinctness rate across all 22 real candidates, measured with zero model dispatches.
  </acceptance_criteria>
  <done>The metric and its predictions are immutable and provably predate the measurement, and the harness is proven to build correct payloads before any dispatch is spent.</done>
</task>

<task type="auto">
  <name>Task 2: Run the live re-proof — real `claude -p` dispatches on `cp -R` copies, every verdict recorded through the real CLI</name>
  <files>.planning/phases/177-derived-line-re-derivation/177-PROOF-2.md</files>
  <read_first>
    - .planning/phases/177-derived-line-re-derivation/177-PROOF.md (§1 setup/hashes/install/enumeration, §2 the grepped independence observable, §3 the corpus run, and the "How to re-run every proof in this document" section carrying the working driver commands)
    - .planning/phases/177-derived-line-re-derivation/177-TARGETING-PREDICTION.md (as committed in Task 1)
    - src/cli/slash-command/bs/verify/derive-recheck.md and src/cli/slash-command/bs/verify/derive-compare.md (the contracts the dispatches carry, as rewritten in 177-11)
    - src/cli/cli.ts (the `verify-derive-record` registration — the recording path this run must use)
  </read_first>
  <action>
    Follow 177-PROOF.md's §1 protocol exactly: sha256 baselines on both ORIGINALS first, then
    `cp -R` to fresh copies, then a real install of the bs- skills into each copy. All subsequent
    work runs against copies only. State the dispatch mechanism (`claude -p` OS subprocess, not
    native Task-tool dispatch) in the document before any result appears.

    Dispatch every enumerated candidate blind, then every non-terminal blind result through a
    comparison dispatch. Save every prompt verbatim to disk BEFORE dispatch, so the greps run on
    real artifacts rather than on reconstructions.

    Record EVERY verdict by invoking `boardsmith verify-derive-record` — the real CLI surface added
    in plan 177-10 — never by importing `createDeriveVerdictRecord` from the driver script. The
    prior run imported the library directly; that is exactly the gap CR-05 named, so exercising the
    registered command is now part of the evidence. Report through
    `boardsmith verify-derive-recheck --json`.

    Run the independence greps on the SAVED blind prompts as zero-expectation checks: the three
    annotation families, and — new this run — each target's own slice path and its own line number.
    Grep the comparison prompts separately, keeping the existing carve-out that a compare prompt
    legally contains the original line.

    Write `177-PROOF-2.md` §§1-3 with the real numbers: enumeration reconciled against the 10/12/22
    counts, the full verdict distribution, the targeting metrics, and predicted-vs-measured against
    `177-TARGETING-PREDICTION.md` with every miss named individually. If the targeting fix did not
    work, say so in those words and apply the failure interpretation rule the prediction
    pre-committed — do not search for a reading that rescues the number.
  </action>
  <verify>
    <automated>npx boardsmith verify-derive-recheck --project "$PROOF_COPY_DIR/seven" --json > /tmp/177-proof2-seven.json && node -e "const r=require('/tmp/177-proof2-seven.json'); if (r.findings.some(f=>f.verdict==='pending')) throw new Error('unrecorded candidates remain — the CLI write path did not record every dispatch'); console.log('recorded', r.findings.length, 'offTarget', r.offTargetDisagreements, 'genuine', r.genuineDisagreements)"</automated>
  </verify>
  <acceptance_criteria>
    - The proof shows real `claude -p` invocations with their command lines, and a dispatch count reconciled against the candidate count — not a claim that dispatches occurred.
    - Zero blind prompt contains any of `Derived (p.`, `Visual (p.`, `Named-but-undefined (p.`, its own slice path, or its own line number — each shown as real grep output with a zero result.
    - Every recorded verdict's provenance is a `boardsmith verify-derive-record` invocation reproduced in the document; `grep -c 'createDeriveVerdictRecord' 177-PROOF-2.md` returns 0 for the recording path.
    - `verify-derive-recheck --json` for both copies reports zero `pending` findings.
    - Predicted-vs-measured is shown per metric AND per line, with every miss named.
  </acceptance_criteria>
  <done>The full corpus is re-measured on real dispatches through the real write surface, and the result is reported against a prediction that provably predates it.</done>
</task>

<task type="auto">
  <name>Task 3: Close the proof — originals byte-identical, limitations disclosed, re-run instructions</name>
  <files>.planning/phases/177-derived-line-re-derivation/177-PROOF-2.md</files>
  <read_first>
    - .planning/phases/177-derived-line-re-derivation/177-PROOF.md (§4 — closure, the whole-tree sha256 diff, "What is still unproven", and the re-run section, all of which this document must match in rigor)
    - .planning/phases/177-derived-line-re-derivation/177-11-PLAN.md (its threat model's accepted risk T-177-11-05 — the blind subagent's file-read tools are not sandboxed by this harness, which must be disclosed here)
    - .planning/phases/177-derived-line-re-derivation/177-CONTEXT.md (Deferred / Carried In — the standing limitations this proof inherits and must restate rather than quietly drop)
  </read_first>
  <action>
    Re-hash both ORIGINALS and show the whole-tree sha256 diff against the preflight baselines,
    expecting empty output. If either differs, stop and report it — a non-empty diff invalidates the
    run regardless of the numbers.

    Write §4: closure, plus a "What is still unproven" section that names, at minimum: zero real
    `Visual (p.` lines exist in either corpus so `Visual`-ignoring remains proven on constructed
    input only; the blind dispatch's file-read tools are not sandboxed, so CR-07's independence is
    prompt-enforced at the tool layer even though the prompt now carries no resolvable coordinate;
    any residual shared-focus-passage candidates reported `targetingAmbiguous`; and any warning from
    177-REVIEW.md deliberately deferred by this gap-closure set (WR-07's quote allow-list inversion
    is the known one).

    Finish with "How to re-run every proof in this document", carrying the actual commands including
    the `verify-derive-record` invocations.

    Report no repair: neither reference game's slice content may be fixed. This check reports; it
    does not repair — the boundary Phases 172 and 176 held.
  </action>
  <verify>
    <automated>diff <(cd ~/BoardSmithGames/seven && git status --porcelain) /dev/null && diff <(cd ~/BoardSmithGames/one-two-punch && git status --porcelain) /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - The whole-tree sha256 diff for BOTH originals is shown as real command output and is empty.
    - Both originals are still at their pinned commits (`a03f38d4792af9dfc7c798be69686fc3230f54dd` and `7e69471bd8980a854f3e351f2f486e1fb6f712b9`), shown via `git -C ... rev-parse HEAD`.
    - "What is still unproven" names at least the four items listed in the action, each in its own bullet.
    - The re-run section's commands are copy-pasteable and include a `verify-derive-record` invocation.
  </acceptance_criteria>
  <done>The proof closes with both originals provably untouched and every limitation named rather than implied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proof harness → `~/BoardSmithGames` originals | A driver bug could mutate a read-only reference game |
| `claude -p` subagent → filesystem | The dispatched subagent has file-read tools and could open the live slice despite the prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-12-01 | Tampering | reference-game originals | mitigate | sha256 baselines before any copy, whole-tree diff after, `git status --porcelain` empty on both, all shown as real output |
| T-177-12-02 | Repudiation | a result reported against a retrofitted expectation | mitigate | Metric and predictions committed in their own commit before any dispatch; `177-PREDICTION.md` untouched |
| T-177-12-03 | Information disclosure | blind subagent opening the live slice | accept | No resolvable coordinate is in the prompt after 177-11; tool-layer sandboxing is out of scope for this `claude -p` harness and is disclosed in §4 |
| T-177-12-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies; the bs- skill install into each copy uses the repo's own CLI |
</threat_model>

<verification>
- `177-PROOF-2.md` exists with §§1-4 and real command output throughout.
- Both originals byte-identical and at their pinned commits.
- `verify-derive-recheck --json` reports zero `pending` on both copies.
</verification>

<success_criteria>
The corpus is re-measured on real dispatches against the fixed module, recorded through the real CLI
write surface, compared against a provably-prior prediction, and closed with both originals untouched.
</success_criteria>

<output>
Create `.planning/phases/177-derived-line-re-derivation/177-12-SUMMARY.md` when done
</output>
