---
phase: 177-derived-line-re-derivation
plan: 13
type: execute
wave: 6
gap_closure: true
depends_on: ["177-12"]
files_modified:
  - .planning/phases/177-derived-line-re-derivation/177-GOAL-MEASUREMENT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [CHECK-04]

must_haves:
  truths:
    - "The phase goal is measured in its OWN unit — per rule-bearing Derived line, how many actually received an independent second opinion about that line's own fact — not inferred from the success criteria passing."
    - "The measured goal number is reported whatever it says; a passing criteria set with an unmet goal is stated in those words."
    - "CHECK-04 is closed in REQUIREMENTS.md only on evidence, and left OPEN with the residual named if the evidence does not support closure."
    - "Every 177-REVIEW.md finding is accounted for: fixed, or deliberately deferred with the reason recorded."
  artifacts:
    - path: ".planning/phases/177-derived-line-re-derivation/177-GOAL-MEASUREMENT.md"
      provides: "The goal's own unit, computed from 177-PROOF-2.md's recorded artifacts, with the honest verdict"
      contains: "Goal"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CHECK-04's evidence-based disposition"
      contains: "CHECK-04"
  key_links:
    - from: "177-GOAL-MEASUREMENT.md"
      to: "177-PROOF-2.md"
      via: "every number cited from the recorded ledger and --json output, never recomputed by hand"
      pattern: "177-PROOF-2"
    - from: ".planning/REQUIREMENTS.md CHECK-04"
      to: "177-GOAL-MEASUREMENT.md"
      via: "disposition citing the measured goal unit"
      pattern: "177-GOAL-MEASUREMENT"
---

<objective>
Measure the phase goal in its own unit and report it honestly, then dispose of CHECK-04 on that
evidence alone.

Purpose: this project's hard-won convention (MEMORY: "Phase goal vs success criteria" — v4.9 Phase
174 scored 90.9% on SC-2 and still failed its goal at 100% chunks stale) is that a phase can pass
every criterion and still fail its goal. Phase 177 already demonstrated it once: 177-07 shipped
green tests and honestly reported the goal NOT MET. This plan closes the loop with a measurement of
the goal's own unit rather than an inference from the criteria.

Output: `177-GOAL-MEASUREMENT.md`, and evidence-based updates to REQUIREMENTS.md, ROADMAP.md, and
STATE.md — including leaving CHECK-04 OPEN if that is what the evidence says.

Binding: no threshold may be introduced or moved here. The metric definitions come from
`177-TARGETING-PREDICTION.md`, committed before the measurement existed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/177-derived-line-re-derivation/177-PROOF-2.md
@.planning/phases/177-derived-line-re-derivation/177-TARGETING-PREDICTION.md
@.planning/phases/177-derived-line-re-derivation/177-VERIFICATION.md
@.planning/phases/177-derived-line-re-derivation/177-REVIEW.md

<interfaces>
The phase goal, verbatim from ROADMAP.md:355 — "Rule-bearing inferences get an independent second
opinion, separate from the presentation notes the Phase 170 split now keeps out of the way."

Its unit is one rule-bearing `Derived` line. A line got what the goal promises only when all of the
following are true of it, and each is measurable from the recorded artifacts:
1. It was enumerated as a candidate (not silently dropped) — from `enumeratedCount` reconciled
   against the real corpus count.
2. Its blind dispatch was independent — no annotation family, no slice path, no line number in the
   saved prompt (177-PROOF-2.md §2's zero-expectation greps).
3. The second opinion was about THAT line's fact — `factAlignment === 'same-fact'`, or a terminal
   blind outcome (`underivable`/`not-rule-bearing`) legitimately reached, and not
   `targetingAmbiguous`.
4. A verdict was actually recorded through the real write surface — no `pending` finding remains.

CHECK-04's current REQUIREMENTS.md entry (lines 215-235) is a detailed OPEN/PARTIAL disposition with
SC-1 NOT MET, SC-2 MET, SC-3 MET, citing `177-PROOF.md` §§2-4. It is a good model for the shape an
honest re-disposition takes — rewrite it against `177-PROOF-2.md`, do not append to it.

ROADMAP.md Phase 177's entry carries a `**Result:** NOT MET` block and an unchecked phase box.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute and report the goal's own unit, honestly</name>
  <files>.planning/phases/177-derived-line-re-derivation/177-GOAL-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/177-derived-line-re-derivation/177-PROOF-2.md (all four sections — every number in the measurement must be cited from here, never recomputed by hand)
    - .planning/phases/177-derived-line-re-derivation/177-TARGETING-PREDICTION.md (the metric definitions and the pre-committed interpretation rules, including the failure rule)
    - .planning/ROADMAP.md (lines 263-280 — the phase goal, its three success criteria, and the existing NOT MET result block)
    - .planning/phases/177-derived-line-re-derivation/177-VERIFICATION.md (the six observable truths and the 3/6 score this measurement must be comparable against)
  </read_first>
  <action>
    Write `.planning/phases/177-derived-line-re-derivation/177-GOAL-MEASUREMENT.md` computing, per
    rule-bearing `Derived` line across both reference games, how many received an independent second
    opinion about that line's own fact — the four-condition unit defined in this plan's interfaces
    block. Report the count and the rate, per game and in total, with a per-line table naming every
    line that did NOT qualify and the specific condition it failed.

    State separately the three success criteria's status (SC-1/SC-2/SC-3) and the goal's status, and
    if they diverge, say so explicitly in those words — "the criteria pass and the goal is not met"
    is a valid and required sentence when true. Cite the v4.9 Phase 174 precedent by name so the
    convention is visibly being followed rather than reinvented.

    Apply whichever interpretation rule `177-TARGETING-PREDICTION.md` pre-committed for the measured
    outcome — including its failure rule if that is the one that fires. Do not introduce a new rule
    here, do not move a threshold, and do not reframe the metric to land better.

    Close with an explicit residual: what would still have to be true for the goal to be met, if it
    is not met. Name it as concrete work, not as a caveat.
  </action>
  <verify>
    <automated>test -s .planning/phases/177-derived-line-re-derivation/177-GOAL-MEASUREMENT.md && grep -c "177-PROOF-2" .planning/phases/177-derived-line-re-derivation/177-GOAL-MEASUREMENT.md</automated>
  </verify>
  <acceptance_criteria>
    - The document states the goal's own unit, its measured count, and its rate — per game and in total.
    - Every line failing the unit is named individually with the specific condition it failed (enumeration, independence, targeting, or recording).
    - The document states SC-1/SC-2/SC-3 status and the goal status separately, and explicitly names any divergence between them.
    - `git diff HEAD -- .planning/phases/177-derived-line-re-derivation/177-TARGETING-PREDICTION.md` is empty — the pre-registration was not edited to match the result.
    - Every numeric claim cites `177-PROOF-2.md` by section.
  </acceptance_criteria>
  <done>The goal is measured in its own unit and reported as-is, with the residual named as concrete work if it is not met.</done>
</task>

<task type="auto">
  <name>Task 2: Dispose of CHECK-04 on evidence, and account for every review finding</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/177-derived-line-re-derivation/177-GOAL-MEASUREMENT.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 205-240 — CHECK-04's current OPEN/PARTIAL entry, the shape an honest disposition takes, and its sibling entries for comparison)
    - .planning/ROADMAP.md (Phase 177's detail block and its `**Result:**` section)
    - .planning/STATE.md (the current position, decisions, and blockers this phase's closure must update)
    - .planning/phases/177-derived-line-re-derivation/177-REVIEW.md (all 7 criticals and 11 warnings — every one must be accounted for as fixed or deferred)
    - .planning/phases/177-derived-line-re-derivation/177-GOAL-MEASUREMENT.md (Task 1's output — the evidence the disposition rests on)
  </read_first>
  <action>
    Rewrite CHECK-04's REQUIREMENTS.md entry against `177-PROOF-2.md` and
    `177-GOAL-MEASUREMENT.md`. Check the box ONLY if the evidence supports it. If SC-1's targeting
    half is still not met, leave it `[ ]` and say why, in the same specific, cited style the current
    entry uses — CHECK-04 may only be closed on evidence, and an unmet goal recorded honestly is a
    better outcome than a closed box that misleads Phase 179's consumer.

    Update ROADMAP.md Phase 177's `**Result:**` block with the re-measured numbers and the honest
    verdict, and set the phase checkbox to match the disposition. Add the six gap-closure plans to
    the phase's plan list so the roadmap reflects the real plan set.

    Add a findings-ledger section to `177-GOAL-MEASUREMENT.md` accounting for all 18 review findings:
    each CR-01..CR-07 and WR-01..WR-11 marked fixed (naming the plan and the pin) or deliberately
    deferred (naming the reason). WR-07's quote allow-list inversion is expected to be the deferred
    one — record it as a named, dated deferral with its rationale, not as an omission.

    Update STATE.md's position, decisions, and blockers to reflect the phase's real disposition. Per
    the standing project note, hand-edit STATE.md; do not call `gsd-sdk state.update-progress`, which
    reverts frontmatter to stale values while reporting `updated: false`.
  </action>
  <verify>
    <automated>grep -n "CHECK-04" .planning/REQUIREMENTS.md | head -5 && grep -n "Phase 177" .planning/ROADMAP.md | head -3</automated>
  </verify>
  <acceptance_criteria>
    - CHECK-04's REQUIREMENTS.md entry cites `177-PROOF-2.md` and `177-GOAL-MEASUREMENT.md`, and its checkbox state matches the measured evidence (checked only if the goal unit supports it).
    - ROADMAP.md Phase 177 lists all 13 plans and its `**Result:**` block carries the re-measured numbers.
    - The findings ledger accounts for all 18 review findings with no blank rows; every deferral names a reason.
    - `git diff .planning/STATE.md` shows hand edits only — no evidence of `state.update-progress` having run.
  </acceptance_criteria>
  <done>CHECK-04's status reflects measured evidence, and no review finding is left unaccounted for.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured proof artifacts → requirement disposition | A number restated by hand instead of cited can drift into a closure claim the evidence does not support |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-13-01 | Repudiation | requirement closure without evidence | mitigate | Every disposition cites `177-PROOF-2.md` by section and `177-GOAL-MEASUREMENT.md`; the box is checked only if the goal unit supports it |
| T-177-13-02 | Tampering | pre-registration edited to match the result | mitigate | `git diff` on `177-TARGETING-PREDICTION.md` and `177-PREDICTION.md` asserted empty |
| T-177-13-03 | Information disclosure | a deferred defect disappearing from the record | mitigate | Findings ledger covers all 18 review findings; every deferral names a reason and a date |
| T-177-13-SC | Tampering | npm/pip/cargo installs | mitigate | No dependencies; documentation-only plan |
</threat_model>

<verification>
- `177-GOAL-MEASUREMENT.md` exists with the goal unit, the per-line failure table, and the findings ledger.
- REQUIREMENTS.md, ROADMAP.md, and STATE.md agree with each other and with the measurement.
- Neither prediction file was edited.
</verification>

<success_criteria>
The phase goal is measured in its own unit and reported honestly, CHECK-04 is disposed of on that
evidence alone, and every one of the 18 review findings is either fixed or deferred with a recorded
reason.
</success_criteria>

<output>
Create `.planning/phases/177-derived-line-re-derivation/177-13-SUMMARY.md` when done
</output>
