---
phase: 177.1-wire-check04-closed-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/commands/derived-line-pattern.ts
  - src/cli/commands/derived-line-pattern.test.ts
  - src/cli/commands/verify-enumerate.ts
  - src/cli/commands/verify-derive-recheck.ts
  - src/cli/commands/verify-classify.ts
  - src/cli/commands/ingest-archive.ts
autonomous: true
requirements: [CHECK-04]

must_haves:
  truths:
    - "Exactly one definition of the annotation-family citation regex exists in src/, and every consumer imports it"
    - "Exactly one definition of the annotation-family vocabulary regex exists in src/, and every consumer imports it"
    - "buildEnumeratorPayload's two-backstop behavior is byte-for-byte unchanged after the consolidation"
    - "Adding a fourth annotation family requires editing exactly one file"
  artifacts:
    - path: "src/cli/commands/derived-line-pattern.ts"
      provides: "The single shared annotation-family pattern module (leaf, no imports)"
      contains: "ANNOTATION_CITATION_RE"
    - path: "src/cli/commands/derived-line-pattern.test.ts"
      provides: "Single-definition grep gate + behavioral pins for every exported pattern"
  key_links:
    - from: "src/cli/commands/verify-enumerate.ts"
      to: "src/cli/commands/derived-line-pattern.js"
      via: "import of the shared annotation patterns"
      pattern: "from './derived-line-pattern.js'"
    - from: "src/cli/commands/verify-classify.ts"
      to: "src/cli/commands/derived-line-pattern.js"
      via: "PRESENTATION_EXCLUSION_MARKERS derived from the shared family list"
      pattern: "from './derived-line-pattern.js'"
---

<objective>
Collapse the duplicated annotation-family regexes (`Derived (p.` / `Visual (p.` /
`Named-but-undefined (p.`) into ONE exported definition in the existing leaf module
`derived-line-pattern.ts`, and route every consumer through it.

Purpose: CONTEXT decision 8. Five independent spellings of the same vocabulary already produced
one measured near-miss (`verify-enumerate.ts:109`'s own header comment calls itself "the fourth
instance in this milestone"). Phase 178 adds a fourth line-kind (`Example (p.N):`); five copies
means Phase 178 must widen all five in lockstep or silently break one.

Output: one shared pattern module, every site importing it, a grep gate that fails if a sixth
copy is ever hand-spelled, and zero behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177.1-wire-check04-closed-design/177.1-CONTEXT.md
@CLAUDE.md
</context>

<interfaces>
Existing, from `src/cli/commands/derived-line-pattern.ts` (the whole file today):

```typescript
export const DERIVED_LINE_RE = /^Derived \(p\.[^)]*\)/i;
```

Its header comment explains WHY it is a leaf module with no imports: `ingest-archive.ts` importing
from `verify-derive-recheck.ts` would close a cycle
(`ingest-archive` -> `verify-derive-recheck` -> `verify-classify` -> `chunk-provenance` ->
`ingest-archive`). Any new export added here must preserve that no-imports property.

The literals to consolidate, as they exist today:
- `verify-derive-recheck.ts:414` `VISUAL_LINE_RE = /^Visual \(p\.[^)]*\)/i`
- `verify-derive-recheck.ts:423` `NAMED_BUT_UNDEFINED_LINE_RE = /^Named-but-undefined \(p\.[^)]*\)/i`
- `verify-derive-recheck.ts:426` `ANY_ANNOTATION_LINE_RE = /Derived \(p\.|Visual \(p\.|Named-but-undefined \(p\./i`
- `verify-enumerate.ts:116` `ANY_ANNOTATION_LINE_RE` — an independent re-spelling of the same regex
- `verify-enumerate.ts:154` `ANNOTATION_VOCABULARY_RE = /^[^A-Za-z0-9]*(?:Derived|Visual|Named-but-undefined)\b/im`
- `verify-classify.ts:102` `PRESENTATION_EXCLUSION_MARKERS` (a frozen array of regex SOURCE strings,
  consumed by `isPresentationLine` at :114 via `new RegExp(source, 'i')`)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the single annotation-family pattern set in derived-line-pattern.ts</name>
  <read_first>
    - src/cli/commands/derived-line-pattern.ts (the whole file — its header explains the no-imports constraint)
    - src/cli/commands/verify-enumerate.ts lines 100-200 (both re-spellings and the two-backstop comment)
    - src/cli/commands/verify-derive-recheck.ts lines 386-466 (VISUAL_LINE_RE, NAMED_BUT_UNDEFINED_LINE_RE, ANY_ANNOTATION_LINE_RE, isQuoteLine, quoteLinesOnly)
    - src/cli/commands/verify-classify.ts lines 85-120 (PRESENTATION_EXCLUSION_MARKERS and isPresentationLine)
  </read_first>
  <behavior>
    - `ANNOTATION_FAMILIES` equals exactly `['Derived', 'Visual', 'Named-but-undefined']`, frozen, in that order.
    - `ANNOTATION_CITATION_RE` matches `Derived (p.1):`, `Visual (p.2):`, `Named-but-undefined (p.3):` anywhere in a string, and matches a multi-page citation body `Derived (p.1, continues on p.2):`.
    - `ANNOTATION_CITATION_RE` does NOT match a plain quoted rulebook sentence with no annotation marker.
    - `ANNOTATION_VOCABULARY_RE` matches a bare `Derived:` line and a decorated `> - Visual:` line (leading non-alphanumeric decoration tolerated), multiline+case-insensitive, and does NOT match the word "derived" used mid-sentence.
    - `annotationLineStartRe('Visual')` returns a regex equivalent to today's `/^Visual \(p\.[^)]*\)/i`.
    - `DERIVED_LINE_RE` still exports and still equals `/^Derived \(p\.[^)]*\)/i` (existing importers unchanged).
    - The module has zero `import` statements (the cycle guarantee).
  </behavior>
  <action>
Add to `src/cli/commands/derived-line-pattern.ts`, keeping `DERIVED_LINE_RE` exported unchanged and
keeping the file free of any `import` statement:

- `ANNOTATION_FAMILIES` — `Object.freeze(['Derived', 'Visual', 'Named-but-undefined'] as const)`.
  This is the ONE list Phase 178 will widen with `Example`.
- `ANNOTATION_CITATION_RE` — built mechanically from `ANNOTATION_FAMILIES` (escape each family,
  join with `|`, suffix ` \(p\.`), replacing the two hand-spelled `ANY_ANNOTATION_LINE_RE` copies.
  It must be equivalent to `/Derived \(p\.|Visual \(p\.|Named-but-undefined \(p\./i`.
- `ANNOTATION_VOCABULARY_RE` — built mechanically from `ANNOTATION_FAMILIES`, equivalent to
  `/^[^A-Za-z0-9]*(?:Derived|Visual|Named-but-undefined)\b/im`.
- `annotationLineStartRe(family)` — a factory returning `^<family> \(p\.[^)]*\)` case-insensitive,
  so `VISUAL_LINE_RE` and `NAMED_BUT_UNDEFINED_LINE_RE` become call sites rather than literals.
- `ANNOTATION_CITATION_SOURCES` — the per-family regex SOURCE strings in
  `PRESENTATION_EXCLUSION_MARKERS` shape, so `verify-classify.ts` can derive its array rather than
  re-spelling it.

Because `ANNOTATION_CITATION_RE` and `ANNOTATION_VOCABULARY_RE` carry the `g`-free but stateful-safe
concern of shared regex objects, define them as `const` regex literals produced once at module
init (no `lastIndex` state, since neither uses the `g` flag) — document that in a comment.

Write `src/cli/commands/derived-line-pattern.test.ts` covering every bullet in `<behavior>`, plus a
regression pin that `ANNOTATION_CITATION_RE.source` and `ANNOTATION_VOCABULARY_RE.source` are
byte-equal to the two literals they replace (copy those two literals verbatim into the test as the
expected values — that is the proof of zero behavior change).
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith &amp;&amp; npx vitest run src/cli/commands/derived-line-pattern.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/cli/commands/derived-line-pattern.test.ts` passes with at least 8 tests.
    - `grep -c "^import" src/cli/commands/derived-line-pattern.ts` returns 0.
    - The test file asserts `ANNOTATION_CITATION_RE.source === 'Derived \\(p\\.|Visual \\(p\\.|Named-but-undefined \\(p\\.'` and `ANNOTATION_VOCABULARY_RE.source === '^[^A-Za-z0-9]*(?:Derived|Visual|Named-but-undefined)\\b'`.
    - `ANNOTATION_FAMILIES` is exported and frozen (`Object.isFrozen` asserted in the test).
  </acceptance_criteria>
  <done>derived-line-pattern.ts exports the full annotation-family pattern set, proven byte-equal to the literals it replaces, with zero imports.</done>
</task>

<task type="auto">
  <name>Task 2: Route every consumer through the shared module and gate against a sixth copy</name>
  <read_first>
    - src/cli/commands/derived-line-pattern.ts (as written in Task 1)
    - src/cli/commands/verify-enumerate.ts lines 100-200
    - src/cli/commands/verify-derive-recheck.ts lines 386-466
    - src/cli/commands/verify-classify.ts lines 85-120
    - src/cli/commands/ingest-archive.ts lines 460-480 and 780-800 (its DERIVED_LINE_RE usage and its comment naming this duplication pattern)
  </read_first>
  <action>
Delete the local declarations and import from `./derived-line-pattern.js` at every site:

- `verify-enumerate.ts`: delete lines declaring `ANY_ANNOTATION_LINE_RE` and
  `ANNOTATION_VOCABULARY_RE`; import `ANNOTATION_CITATION_RE` and `ANNOTATION_VOCABULARY_RE`.
  Update `buildEnumeratorPayload`'s filter and its two-backstop `if` to use the imported names.
  REWRITE the header comment at lines 105-125 that currently explains and apologizes for the
  duplication — it becomes self-invalidating prose the moment the duplication is gone. Replace it
  with a comment stating the two backstops are deliberately DIFFERENT patterns (citation-keyed vs
  vocabulary-keyed) drawn from one shared family list, and why a single check is not a backstop.
- `verify-derive-recheck.ts`: delete `VISUAL_LINE_RE`, `NAMED_BUT_UNDEFINED_LINE_RE`, and
  `ANY_ANNOTATION_LINE_RE`; import `ANNOTATION_CITATION_RE` and `annotationLineStartRe`, and build
  the two line-start regexes from the factory. `isQuoteLine` and the `payload` backstop at line 707
  keep identical semantics. (This file is deleted in plan 06; it is updated here so the tree stays
  green in the interim and so the delete is a pure removal rather than a removal-plus-fix.)
- `verify-classify.ts`: derive `PRESENTATION_EXCLUSION_MARKERS` from `ANNOTATION_CITATION_SOURCES`
  IF the derived array is element-wise equal to today's frozen literal. If it is NOT equal (the
  qualifier-group shape at line 94 differs), leave `PRESENTATION_EXCLUSION_MARKERS` in place and
  add a comment naming exactly which element differs and why it is a genuinely different pattern —
  then record that in the plan SUMMARY as a finding. Do NOT silently widen or narrow
  `isPresentationLine`'s behavior to force the consolidation.
- `ingest-archive.ts`: update its comment at line 788 which names the
  `ANY_ANNOTATION_LINE_RE`/filter duplication as an open pattern — that claim is now false.

Add to `derived-line-pattern.test.ts` a single-definition grep gate: scan every `.ts` file under
`src/` EXCLUDING `derived-line-pattern.ts` and any `.test.ts`, strip `//` and `/* */` comment text
first (so a comment naming a family cannot self-invalidate the gate), and assert zero matches for
a hand-spelled `Derived \(p\.` / `Visual \(p\.` / `Named-but-undefined \(p\.` regex literal.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith &amp;&amp; npx vitest run src/cli/commands/derived-line-pattern.test.ts src/cli/commands/verify-enumerate.test.ts src/cli/commands/verify-derive-recheck.test.ts src/cli/commands/verify-classify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four test files pass with zero failures — in particular `verify-enumerate.test.ts`'s existing construction-site-backstop tests and `verify-derive-recheck.test.ts`'s `quoteLinesOnly`/`buildBlindDerivePayload` blind tests still pass unchanged (that is the zero-behavior-change proof).
    - The new grep gate test exists, strips comments before counting, and passes.
    - `grep -n "ANY_ANNOTATION_LINE_RE" src/cli/commands/verify-enumerate.ts src/cli/commands/verify-derive-recheck.ts` returns no declaration lines (`const ANY_ANNOTATION_LINE_RE =`).
    - `npx tsc --noEmit -p .` (or the repo's typecheck script) reports zero errors.
    - If `PRESENTATION_EXCLUSION_MARKERS` was NOT consolidated, `verify-classify.ts` carries a comment naming the specific differing element, and the SUMMARY records it as a finding.
  </acceptance_criteria>
  <done>Every annotation-family pattern in src/ resolves to derived-line-pattern.ts; a grep gate fails the build if a sixth copy is hand-spelled; every pre-existing test still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slice markdown -> enumerator payload | Untrusted transcribed text crosses into a dispatch prompt; the annotation-family patterns are the guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177.1-01 | Tampering | consolidated `ANNOTATION_CITATION_RE` | mitigate | Test pins `.source` byte-equal to both literals it replaces; every pre-existing blind/backstop test must pass unchanged |
| T-177.1-02 | Information disclosure | `buildEnumeratorPayload` backstop | mitigate | Two structurally different backstops retained (citation-keyed + vocabulary-keyed); a single-check collapse is explicitly forbidden in the rewritten comment and covered by `verify-enumerate.test.ts` |
| T-177.1-SC | Tampering | npm installs | mitigate | This plan installs no packages; if any install becomes necessary, STOP and escalate |
</threat_model>

<verification>
- `npx vitest run src/cli/commands/` fully green.
- One file defines the annotation families; a grep gate enforces it.
</verification>

<success_criteria>
- Exactly one definition of each annotation-family pattern exists in `src/`, in `derived-line-pattern.ts`.
- Zero behavior change, proven by every pre-existing test passing untouched.
- Adding `Example` in Phase 178 requires editing `ANNOTATION_FAMILIES` only.
</success_criteria>

<output>
Create `.planning/phases/177.1-wire-check04-closed-design/177.1-01-SUMMARY.md` when done.
</output>
