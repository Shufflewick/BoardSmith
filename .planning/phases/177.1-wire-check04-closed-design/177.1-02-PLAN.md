---
phase: 177.1-wire-check04-closed-design
plan: 02
type: execute
wave: 2
depends_on: [177.1-01]
files_modified:
  - src/cli/commands/verify-derive-check.ts
  - src/cli/commands/verify-derive-check.test.ts
autonomous: true
requirements: [CHECK-04]

must_haves:
  truths:
    - "A dual-enumeration classification can be recorded and read back unchanged"
    - "Recording a verdict for one line never destroys a verdict already recorded for another"
    - "A hand-edited ledger carrying an out-of-set verdict is rejected on read with an actionable message, never counted"
    - "A judgment field carrying a ledger fence marker is rejected at construction, never written"
  artifacts:
    - path: "src/cli/commands/verify-derive-check.ts"
      provides: "DERIVE_CHECK_VERDICTS, createDeriveCheckRecord, the atomic upsert-append ledger, and the revalidating read path"
      contains: "DERIVE_CHECK_VERDICTS"
    - path: "src/cli/commands/verify-derive-check.test.ts"
      provides: "Executed proofs for CR-02, CR-04, CR-06, WR-05 against the new verdict set"
  key_links:
    - from: "src/cli/commands/verify-derive-check.ts"
      to: "src/cli/commands/verify-enumerate.js"
      via: "DerivedLineClassification is the source of the verdict set"
      pattern: "from './verify-enumerate.js'"
    - from: "readDeriveCheckVerdicts"
      to: "createDeriveCheckRecord"
      via: "every read-path record is reconstructed through the single construction site"
      pattern: "createDeriveCheckRecord"
---

<objective>
Create `src/cli/commands/verify-derive-check.ts` and MOVE the proven ledger machinery into it from
`verify-derive-recheck.ts` — atomic upsert-append (CR-06), fence-injection rejection (CR-04),
read-path revalidation through the single construction site (CR-02), and the evidence requirement
(WR-05) — retargeted onto the dual-enumeration verdict set.

Purpose: CONTEXT decision 2 — that code survived an 18-finding review across 177-09/177-10 and must
be MOVED, never rewritten. Only the verdict enum and the record's judgment fields change.

Output: the new module's data layer, with the moved invariants proven by executed tests (not
inspection) against the new verdict set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177.1-wire-check04-closed-design/177.1-CONTEXT.md
@.planning/phases/177.1-wire-check04-closed-design/177.1-01-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
The verdict set comes from `verify-enumerate.ts:1119` — do not re-spell it:

```typescript
export type DerivedLineClassification =
  | 'corroborated'
  | 'corroborated-by-composition'
  | 'uncorroborated'
  | 'contradicted'
  | 'quote-unverified'
  | 'absence-corroborated'
  | 'absence-contradicted'
  | 'absence-unverifiable';
```

`classifyDerivedLines` returns, per line (`verify-enumerate.ts:1164`):

```typescript
export interface DerivedLineClassificationResult {
  slicePath: string;
  lineNumber: number;
  derivedLineText: string;
  classification: DerivedLineClassification;
  citedFactIds: string[];
  reason: string;
}
```

Grounded evidence shape (`verify-enumerate.ts:352`):

```typescript
export interface GroundedBothFact {
  id: string; statement: string; quotedFromA: string; quotedFromB: string;
  matchedFactA: EnumeratedFact; matchedFactB: EnumeratedFact;
}
```

The machinery being moved lives in `verify-derive-recheck.ts` at:
`DERIVE_LEDGER_BEGIN`/`DERIVE_LEDGER_END`:782-783, `deriveVerdictsLedgerPath`:789,
`deriveVerdictKey`:793, `replaceDeriveVerdicts`:815, `recordDeriveVerdict`:842,
`readDeriveVerdicts`:968, `createDeriveVerdictRecord`:205.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DERIVE_CHECK_VERDICTS and createDeriveCheckRecord — the single construction site</name>
  <read_first>
    - src/cli/commands/verify-derive-recheck.ts lines 79-325 (DERIVE_VERDICTS, DeriveVerdictRecord, createDeriveVerdictRecord — the fence rejection and WR-05 evidence rules being moved)
    - src/cli/commands/verify-enumerate.ts lines 1100-1180 (DerivedLineClassification, DerivedLineClassificationResult, MissedFact)
    - src/cli/commands/verify-derive-recheck.test.ts lines 67-353 (the executed proofs whose shape the new tests must preserve)
  </read_first>
  <behavior>
    - `DERIVE_CHECK_VERDICTS` is a frozen 8-member array matching `DerivedLineClassification` exactly; a compile-time exhaustiveness check fails if the union gains a member without the array gaining it.
    - `createDeriveCheckRecord` throws, naming all eight legal verdicts, for a verdict outside the set.
    - Throws for an empty/whitespace `reason` — the reason IS the artifact.
    - Throws when `reason`, `derivedLineText`, or any `groundedQuotes` string contains the ledger BEGIN or END fence marker, naming the offending field (CR-04).
    - `corroborated` and `corroborated-by-composition` throw when `citedFactIds` is empty (WR-05 analog: a corroboration with nothing cited is not evidence).
    - `contradicted` throws when `citedFactIds` is empty.
    - `uncorroborated`, `quote-unverified`, `absence-corroborated`, `absence-contradicted`, `absence-unverifiable` construct successfully with empty `citedFactIds` — nothing to cite is the honest state, never an error.
    - A record round-trips through `JSON.parse(JSON.stringify(record))` byte-identically.
  </behavior>
  <action>
Create `src/cli/commands/verify-derive-check.ts`. Define:

- `DERIVE_CHECK_VERDICTS` — frozen array of the eight `DerivedLineClassification` members in the
  order the type declares them. Add a `satisfies readonly DerivedLineClassification[]` and a
  never-assignment exhaustiveness guard so widening the union without widening the array is a
  compile error.
- `DeriveCheckVerdict` = `(typeof DERIVE_CHECK_VERDICTS)[number]`.
- `DeriveCheckRecord` with fields: `slicePath`, `lineNumber`, `derivedLineText`, `verdict`,
  `reason`, `citedFactIds: string[]`, `groundedQuotes: { statement: string; quotedFromA: string;
  quotedFromB: string }[]`, `recordedAt: string` (ISO 8601, UTC).
- `createDeriveCheckRecord(input)` — the ONE construction site, enforcing every rule in
  `<behavior>`. Port the fence-marker scan and the "naming the field" error text from
  `createDeriveVerdictRecord` rather than re-inventing the messages.

Constants `DERIVE_CHECK_LEDGER_BEGIN = '<!-- boardsmith:derive-check-verdicts:begin -->'` and
`DERIVE_CHECK_LEDGER_END = '<!-- boardsmith:derive-check-verdicts:end -->'`.

DELIBERATE, DOCUMENTED SEMANTIC CHANGE (record it in a header comment and in the SUMMARY): the old
record's `rederivedValue`/`originalReading`/`rederivedReading`/`factAlignment` fields, and the
WR-04 pass-through cross-check between `rederivedValue` and `verdict`, have NO analogue under dual
enumeration — there is no second reading of the same line to align. They are dropped, not adapted.
`groundedQuotes` is the dual-enumeration evidence artifact that replaces them.

Write `verify-derive-check.test.ts` covering every `<behavior>` bullet, mirroring the structure of
`verify-derive-recheck.test.ts` lines 67-353 so the moved invariants are visibly the same proofs.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith &amp;&amp; npx vitest run src/cli/commands/verify-derive-check.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/cli/commands/verify-derive-check.test.ts` passes with at least 12 tests.
    - A test asserts `DERIVE_CHECK_VERDICTS.length === 8` and `Object.isFrozen(DERIVE_CHECK_VERDICTS)`.
    - A test asserts a `reason` containing the literal string `boardsmith:derive-check-verdicts:end` throws with a message containing the word `reason`.
    - A test asserts `createDeriveCheckRecord` with `verdict: 'absence-unverifiable'` and `citedFactIds: []` succeeds.
    - `npx tsc --noEmit` reports zero errors, and deleting a member from `DERIVE_CHECK_VERDICTS` produces a compile error (demonstrate once, then restore).
  </acceptance_criteria>
  <done>The new module has one construction site enforcing the moved CR-04 and evidence invariants against the eight-member dual-enumeration verdict set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move the atomic upsert-append ledger and the revalidating read path</name>
  <read_first>
    - src/cli/commands/verify-derive-recheck.ts lines 777-1025 (the whole ledger section: path, key, replace, record, read + revalidation)
    - src/cli/commands/verify-derive-check.ts (as written in Task 1)
    - src/cli/commands/verify-derive-recheck.test.ts lines 1131-1400 (the CR-06 and CR-02 executed proofs to mirror)
  </read_first>
  <behavior>
    - `recordDeriveCheckVerdicts` called for slice A then slice B leaves BOTH slices' records readable (CR-06 — the pattern that previously destroyed one).
    - Called twice for the SAME `slicePath:lineNumber`, exactly one record survives carrying the second call's content, and existing records keep their original order with the updated record appended last.
    - `readDeriveCheckVerdicts` reconstructs every record through `createDeriveCheckRecord`, so a hand-edited out-of-set verdict throws (CR-02) rather than producing a NaN/undefined count.
    - A not-valid-JSON ledger line throws ONE actionable message naming the ledger path relative to the project and the record index — never a raw `SyntaxError`.
    - Returns an empty array (never throws) when no ledger file exists.
    - The ledger path contains no run-id segment and no function accepts a `runId` (project-level by construction).
    - A whole-project byte hash before vs after recording differs ONLY under `rulebook/.derive-check/`; no `.tmp`/partial file survives.
    - A valid ledger round-trips byte-identically through write -> read -> write.
  </behavior>
  <action>
Move into `verify-derive-check.ts`, preserving implementation shape:

- `deriveCheckLedgerPath(projectDir)` -> `rulebook/.derive-check/verdicts.md` (project-level, no
  run-id segment — carry forward the comment explaining why).
- `deriveCheckKey(record)` -> `${slicePath}:${lineNumber}`.
- `replaceDeriveCheckVerdicts(projectDir, records)` — the deliberately-destructive, non-default
  whole-body replace, reusing the SAME atomic write helper `verify-derive-recheck.ts` uses
  (`atomicWriteFile`); do not hand-roll a second write path.
- `recordDeriveCheckVerdicts(projectDir, records)` — upsert-append: read existing, upsert each
  incoming record by key preserving existing order, append new keys last, write atomically. NOTE
  the shape change from the old per-line API: this takes an ARRAY, because the dual-enumeration
  unit of work is a SLICE (all its lines classified from one reconciler return). The upsert-append
  guarantee is unchanged — recording slice B never destroys slice A.
- `readDeriveCheckVerdicts(projectDir)` — parse the fenced body and reconstruct EVERY record
  through `createDeriveCheckRecord`. This is the single choke point; no `as DeriveCheckRecord`
  cast may appear anywhere in the module.

Add tests to `verify-derive-check.test.ts` for every `<behavior>` bullet, mirroring
`verify-derive-recheck.test.ts` lines 1131-1400 including its behavioral (not grep-based)
whole-project-hash and no-partial-file proofs.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith &amp;&amp; npx vitest run src/cli/commands/verify-derive-check.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All `<behavior>` bullets have a passing test; the file totals at least 22 tests.
    - A grep-count test asserts zero occurrences of `as DeriveCheckRecord` in `verify-derive-check.ts` source, counted after stripping comment lines.
    - A test hand-seeds a ledger record with `"verdict": "agrees"` (a RETIRED verdict) and asserts `readDeriveCheckVerdicts` throws naming the eight legal verdicts.
    - A test asserts the ledger path string contains `.derive-check` and does NOT match `/\.verify\//` or any run-id segment.
    - The whole-project-hash test shows differences confined to `rulebook/.derive-check/`.
  </acceptance_criteria>
  <done>The ledger's atomic upsert-append, single-choke-point revalidation, and project-level source-free guarantees are live in the new module, proven by executed tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subagent return -> ledger record | Model-authored text crosses into a persisted file that is later re-read as structured data |
| on-disk ledger -> report | A hand-edited or corrupted ledger crosses back into counting/reporting code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177.1-03 | Tampering | ledger fence markers | mitigate | `createDeriveCheckRecord` rejects any judgment field containing BEGIN/END fence text, naming the field (CR-04, executed test) |
| T-177.1-04 | Tampering | hand-edited ledger | mitigate | `readDeriveCheckVerdicts` reconstructs every record through the single construction site; out-of-set verdicts throw (CR-02, executed test) |
| T-177.1-05 | Denial of service | non-atomic write | mitigate | Reuse the existing `atomicWriteFile`; test asserts no `.tmp`/partial file survives |
| T-177.1-06 | Repudiation | lost verdicts | mitigate | Upsert-append keyed on `slicePath:lineNumber`; test proves recording slice B preserves slice A |
| T-177.1-SC | Tampering | npm installs | mitigate | No packages installed by this plan; escalate if one becomes necessary |
</threat_model>

<verification>
- `npx vitest run src/cli/commands/verify-derive-check.test.ts` green.
- `npx vitest run src/cli/commands/` green (nothing regressed).
</verification>

<success_criteria>
- Every CR-02/CR-04/CR-06/WR-05 invariant that guarded the retired ledger now guards the new one, proven by executed tests rather than inspection.
- The verdict set is the eight-member dual-enumeration set, compile-time tied to `DerivedLineClassification`.
- Any behavior that only made sense for the retired verdict set is DROPPED and recorded as a finding, never quietly adapted.
</success_criteria>

<output>
Create `.planning/phases/177.1-wire-check04-closed-design/177.1-02-SUMMARY.md` when done.
</output>
