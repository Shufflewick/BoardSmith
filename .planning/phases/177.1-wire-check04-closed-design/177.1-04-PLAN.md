---
phase: 177.1-wire-check04-closed-design
plan: 04
type: execute
wave: 4
depends_on: [177.1-03]
files_modified:
  - src/cli/commands/verify-derive-check.ts
  - src/cli/commands/verify-derive-check.test.ts
  - src/cli/cli.ts
  - src/cli/cli.test.ts
autonomous: true
requirements: [CHECK-04]

must_haves:
  truths:
    - "A designer can run `boardsmith verify-derive-check` in a bs-built project and see every Derived line's dual-enumeration status"
    - "The command tells the caller exactly which slices still need dispatching, and hands it the exact payload bytes to dispatch"
    - "The command names the three pinned model ids, so the orchestrator never has to remember them"
    - "The command always exits 0 — a contradicted finding is advisory, never a gate"
  artifacts:
    - path: "src/cli/commands/verify-derive-check.ts"
      provides: "verifyDeriveCheckCommand — the read/report surface"
      contains: "verifyDeriveCheckCommand"
    - path: "src/cli/cli.ts"
      provides: "Registration of verify-derive-check and the retargeted verify-derive-record"
      contains: "verify-derive-check"
  key_links:
    - from: "src/cli/cli.ts"
      to: "src/cli/commands/verify-derive-check.js"
      via: "commander registration of both commands"
      pattern: "verify-derive-check"
    - from: "verifyDeriveCheckCommand"
      to: "buildEnumeratorPayload"
      via: "per-slice payload preparation handed to the orchestrator"
      pattern: "buildEnumeratorPayload"
---

<objective>
Build `verify-derive-check` — the read/report half of the pairing — and register both commands in
`cli.ts`.

Purpose: ROADMAP success criterion 1 and CONTEXT decision 1. `verify-enumerate.ts` has had zero CLI
wiring since it was written; `grep '.command(' src/cli/cli.ts` lists 20 commands and none reaches
it. This is the entry point that makes CHECK-04's closed design reachable by a designer.

Output: a command that enumerates candidates project-wide, prepares payloads, reports every line's
recorded classification, and exits 0 unconditionally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177.1-wire-check04-closed-design/177.1-CONTEXT.md
@.planning/phases/177.1-wire-check04-closed-design/177.1-03-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
Machinery to MOVE from `verify-derive-recheck.ts` (design-agnostic, per CONTEXT decision 6):
- `readLiveSlices(projectDir)`:338 — reads `rulebook/*.md`, excluding `INDEX.md` and
  `00-visual-survey.md`; never constructs a `.verify/` staging path.
- `annotationBody(line)`:402 and `LINE_DECORATION_RE`:392 — decoration strip (CR-01).
- `quoteLinesOnly(sliceText)`:460 — the shared quote filter (WR-07's deny-list stays as-is; the
  allow-list inversion is Phase 178's, per CONTEXT decision 9 — do NOT invert it here).
- `enumerateDerivedLines(...)`:592 and `DerivedLineEntry`:565 / `EnumerateDerivedLinesResult`:573.
- The CR-03 stale-record join (record's `derivedLineText` vs the line's current text) and the WR-03
  orphaned-record report, from `verifyDeriveRecheckCommand`:1129.
- The actionable `No rulebook/ directory` message and the WR-02 EACCES distinction. Exactly ONE
  copy of that message may exist in the module (WR-10).

Machinery to LEAVE BEHIND (deleted in plan 07, per CONTEXT decision 5): `focusQuoteWindow`,
`blindDeriveHandle`, `buildBlindDerivePayload`, `derivePayloadSet`, `DerivePayloadSetEntry`,
`BLIND_DERIVE_TOKEN`, `factAlignment`/`FACT_ALIGNMENTS`, `DERIVE_VERDICTS`, and every
targeting/`sharedFocusWith`/`targetingAmbiguousCount` computation.

`isPresentationLine` lives in `verify-classify.ts`:114 (backed by
`PRESENTATION_EXCLUSION_MARKERS`:102) — import it, never re-derive it.

The three pinned model ids (CONTEXT decision 4, exactly what `177-22-MEASUREMENT/driver.mjs:17-19`
dispatched): enumerator A `claude-opus-5`, enumerator B `claude-haiku-4-5-20251001`, reconciler
`claude-sonnet-5`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: verifyDeriveCheckCommand — candidate enumeration, payload preparation, report</name>
  <read_first>
    - src/cli/commands/verify-derive-recheck.ts lines 325-620 and 1025-1288 (readLiveSlices, annotationBody, quoteLinesOnly, enumerateDerivedLines, and the whole verifyDeriveRecheckCommand report shape incl. CR-03 stale join, WR-03 orphaned records, WR-02 EACCES, WR-10 single message)
    - src/cli/commands/verify-derive-check.ts (plans 02-03)
    - src/cli/commands/verify-classify.ts lines 85-120 (isPresentationLine)
    - src/cli/commands/verify-enumerate.ts lines 100-200 (ENUMERATE_TOKEN, buildEnumeratorPayload)
    - .planning/phases/177-derived-line-re-derivation/177-22-MEASUREMENT/extract-corpus.mjs (the harness's candidate enumeration, incl. its disclosed legend-exclusion fix requiring a digit after `p.`)
  </read_first>
  <behavior>
    - Run against a project with no ledger: every `Derived` line surviving `isPresentationLine` exclusion is reported `pending`, with zero manufactured default verdicts.
    - Candidate enumeration is PROJECT-WIDE — it never scopes to stale chunks and never reads a `rulebook/.verify/` staging path (proven behaviorally: a run against a project with a populated `.verify/<run-id>/slices/` tree and a `rulebook/source/` decoy is deeply equal to the same run with those trees absent).
    - A slice whose only `Derived (p.N):` occurrences are inside an annotation-convention LEGEND (the literal placeholder `Derived (p.N):` with no digit) yields zero candidates — the harness's disclosed 177-21 fix, carried into the product.
    - For each slice with at least one candidate and no complete ledger coverage, the result carries `enumeratorPayload` — the exact `buildEnumeratorPayload` output bytes — and `derivedLines` (lineNumber + verbatim text) for the reconciler.
    - A slice whose `buildEnumeratorPayload` throws is reported with `payloadError` naming the slice, never silently skipped and never dispatched.
    - `models` in the result is exactly `{ enumeratorA: 'claude-opus-5', enumeratorB: 'claude-haiku-4-5-20251001', reconciler: 'claude-sonnet-5' }`.
    - `verdictCounts` has a key for each of the eight verdicts with explicit zeros, and never gains a key outside `DERIVE_CHECK_VERDICTS`.
    - A ledger record whose line text no longer matches the current slice text is reported in `staleRecords` and the line reports `pending` (CR-03).
    - A ledger record whose location matches no current candidate is reported in `orphanedRecords`, never silently absorbed (WR-03).
    - Missing `rulebook/` throws one actionable line naming `--project`, with no stack and no `.ts:` leak; an unreadable `rulebook/` (EACCES) reports the real condition, not "No rulebook/ directory" (WR-02).
    - `--json` emits the result and nothing else on stdout.
    - `process.exitCode` stays unset in every case above, including when the ledger contains `contradicted` records (CONTEXT decision 14).
  </behavior>
  <action>
Move the design-agnostic helpers named in `<interfaces>` into `verify-derive-check.ts` (copy the
implementations and their explanatory comments; do not re-derive them), then add exported
`verifyDeriveCheckCommand(options)` with options `--project` and `--json`.

Its result shape: `{ projectDir, models, slices: [{ slicePath, candidates, enumeratorPayload?,
payloadError?, derivedLines }], findings: [{ slicePath, lineNumber, derivedLineText, verdict,
reason, citedFactIds, status }], verdictCounts, pendingCount, staleRecords, orphanedRecords,
groundingRejections, missedFactCount }`.

`status` per finding is `'recorded' | 'pending'`. Emit `enumeratorPayload` only for slices with
pending candidates — a fully-recorded slice does not need re-dispatching, and emitting its payload
would put quote lines on stdout for no reason.

Human-readable output groups by slice, then by verdict, and states plainly that a non-corroboration
is "worth a human glance", never a verdict — the language CHECK-04's closure note requires. Ensure
`process.exitCode` is never assigned anywhere in the module.

Port the corresponding tests from `verify-derive-recheck.test.ts` (the `readLiveSlices`,
`quoteLinesOnly`, `enumerateDerivedLines`, `annotationBody`, `module source guarantees`, and
`verifyDeriveRecheckCommand` describe blocks), retargeted — including the two behavioral
replacements at lines 969 and 1041 (the `.verify/`-decoy deep-equality proof and the
`process.exitCode` proof), which are the executed-proof forms this milestone requires.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith &amp;&amp; npx vitest run src/cli/commands/verify-derive-check.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every `<behavior>` bullet has a passing test.
    - A test asserts `result.models` deep-equals the three pinned ids as string literals.
    - A test asserts `Object.keys(result.verdictCounts).sort()` deep-equals `[...DERIVE_CHECK_VERDICTS].sort()`.
    - A grep-count test (comments stripped) asserts exactly one `No rulebook/ directory` message exists in the module (WR-10).
    - A test asserts `process.exitCode === undefined` after a run whose ledger contains a `contradicted` record.
    - `npx vitest run src/cli/commands/` fully green.
  </acceptance_criteria>
  <done>The read/report surface enumerates candidates project-wide, hands out exact payload bytes, reports every verdict, and never gates.</done>
</task>

<task type="auto">
  <name>Task 2: Register both commands in cli.ts</name>
  <read_first>
    - src/cli/cli.ts lines 396-475 (the verify-ruling-recheck / verify-repair registration shape and the CHECK-04 comment block at 419-423 explaining the deliberate absence of --run-id and any bypass option)
    - src/cli/commands/verify-derive-check.ts (plans 02-04)
    - src/cli/cli.test.ts (existing registration tests, for the shape to extend)
  </read_first>
  <action>
Replace the `verify-derive-recheck` registration at `cli.ts:424-434` with `verify-derive-check`,
importing `verifyDeriveCheckCommand` from `./commands/verify-derive-check.js`. Retarget the
`verify-derive-record` registration at :436-471 onto the new
`verifyDeriveRecordCommand` from the same module, replacing its flag set with `--project`,
`--slice-path`, `--enumerator-a`, `--enumerator-b`, `--reconciler`, `--json`.

REWRITE the comment block at 419-423. Its current text describes the retired mechanism ("re-derives
every rule-bearing Derived line independently of the original transcription pass, using only the
current slice's quote lines"). Replace it with the dual-enumeration description, and PRESERVE
verbatim the two sentences that are still true and load-bearing: that both commands are
project-level and source-free BY CONSTRUCTION with neither registering `--run-id` or any bypass
option, and that `verify-derive-record` is the ONLY write surface for CHECK-04's ledger.

Descriptions must name the eight-verdict dual-enumeration set rather than the retired four, and
must state the check is advisory.

Leave `verify-derive-recheck.ts` and its command import in place for now ONLY if removing them
would break the build; plan 07 performs the removal. Prefer removing the registration here and
deferring only the module deletion.

Add `cli.test.ts` coverage: `verify-derive-check` is registered; `verify-derive-recheck` is NOT
registered; `verify-derive-record` registers exactly the six flags above and no `--verdict`,
`--force`, `--skip`, `--overwrite`, or `--run-id`.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith &amp;&amp; npx vitest run src/cli/cli.test.ts &amp;&amp; npx tsx src/cli/cli.ts verify-derive-check --help</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx src/cli/cli.ts verify-derive-check --help` prints usage naming `--project` and `--json` and exits 0.
    - `npx tsx src/cli/cli.ts verify-derive-recheck --help` fails with commander's unknown-command error.
    - `npx tsx src/cli/cli.ts verify-derive-record --help` lists exactly `--project`, `--slice-path`, `--enumerator-a`, `--enumerator-b`, `--reconciler`, `--json` (plus `-h`).
    - `cli.test.ts` asserts all three of the above.
    - `npx tsx src/cli/cli.ts verify-derive-check --project ~/BoardSmithGames/seven --json` exits 0 and emits parseable JSON whose `slices` array is non-empty. (Run against a `cp -R` copy, not the original.)
    - `npx tsc --noEmit` reports zero errors.
  </acceptance_criteria>
  <done>A designer can reach the dual-enumeration check from the command line; the retired command name is gone from the CLI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slice markdown -> stdout payload | Quote lines cross into a machine-readable result the orchestrator will dispatch |
| CLI exit code -> pipeline gate | An exit code change would silently convert an advisory check into a build gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177.1-11 | Information disclosure | `enumeratorPayload` on stdout | mitigate | Payload comes from `buildEnumeratorPayload`, whose two-backstop construction site throws rather than emitting an annotation line; emitted only for pending slices |
| T-177.1-12 | Denial of service | advisory check becoming a gate | mitigate | Source-level assertion that `process.exitCode` is never assigned, plus a behavioral test with a `contradicted` record present |
| T-177.1-13 | Tampering | staging-path leakage | mitigate | Behavioral deep-equality proof against a `.verify/` + `rulebook/source/` decoy tree |
| T-177.1-SC | Tampering | npm installs | mitigate | No packages installed by this plan; escalate if one becomes necessary |
</threat_model>

<verification>
- `npx vitest run src/cli/` green.
- The command runs end-to-end against a staged copy of a reference game and exits 0.
</verification>

<success_criteria>
- ROADMAP criterion 1 satisfied: the read-command / single-atomic-write-surface pairing exists for the dual-enumeration design, carrying fence-injection rejection, read-path revalidation, and upsert-append semantics.
- ROADMAP criterion 5 satisfied at the CLI layer: exit 0 unconditionally, pinned by test.
</success_criteria>

<output>
Create `.planning/phases/177.1-wire-check04-closed-design/177.1-04-SUMMARY.md` when done.
</output>
