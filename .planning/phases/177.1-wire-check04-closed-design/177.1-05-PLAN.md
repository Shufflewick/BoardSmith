---
phase: 177.1-wire-check04-closed-design
plan: 05
type: execute
wave: 5
depends_on: [177.1-04]
files_modified:
  - src/cli/slash-command/bs/verify-game.md
  - src/cli/slash-command/bs/verify.test.ts
autonomous: true
requirements: [CHECK-04]

must_haves:
  truths:
    - "Step 7 dispatches enumerate-facts.md and reconcile-facts.md, and names no retired contract"
    - "Step 7 formats verify-derive-check --json and computes nothing itself"
    - "The Context-Economics carve-out's two-observable reviewer check survives, re-aimed at the new dispatches"
    - "A reviewer can grep the enumerator prompt for zero annotation lines and the reconciler prompt for expected fact statements, as two separate checks"
  artifacts:
    - path: "src/cli/slash-command/bs/verify-game.md"
      provides: "Step 7 rewritten onto the dual-enumeration design"
      contains: "BS-ENUMERATE-V1"
  key_links:
    - from: "src/cli/slash-command/bs/verify-game.md"
      to: "src/cli/slash-command/bs/verify/enumerate-facts.md"
      via: "Step 7 dispatch + Reference Files entry"
      pattern: "verify/enumerate-facts.md"
    - from: "src/cli/slash-command/bs/verify-game.md"
      to: "boardsmith verify-derive-check --json"
      via: "formatted, never computed"
      pattern: "verify-derive-check --json"
---

<objective>
Rewrite `verify-game.md`'s Step 7, its router paragraph, its Context-Economics carve-out, and its
Reference Files onto the dual-enumeration design, and pin the rewrite with regression tests.

Purpose: ROADMAP success criterion 2 and CONTEXT decision 7. Step 7 today dispatches the RETIRED
design — `verify/derive-recheck.md`'s `BS-DERIVE-V1`, `verify/derive-compare.md`'s
`BS-DERIVE-COMPARE-V1`, `buildBlindDerivePayload`, `factAlignment`, and
`boardsmith verify-derive-recheck --json`. Every one of those is gone or going.

Output: skill prose that dispatches the contracts CHECK-04 actually closed on, with PROC-01/PROC-02
regression pins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177.1-wire-check04-closed-design/177.1-CONTEXT.md
@.planning/phases/177.1-wire-check04-closed-design/177.1-04-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
Sites in `verify-game.md` that name a retired artifact (verified 2026-07-31):
- lines 11-13 — the router paragraph's dispatch list names `verify/derive-recheck.md` and
  `verify/derive-compare.md`.
- lines 49-68 — the Context-Economics carve-out, naming `derive-recheck.md`, `derive-compare.md`,
  `BS-DERIVE-V1`, `BS-DERIVE-COMPARE-V1`, and `buildBlindDerivePayload`, and ending with the
  TWO-OBSERVABLE reviewer check (lines 64-68) that CONTEXT decision 7 says to PRESERVE, re-aimed.
- lines 168-189 — Step 7 itself.
- lines 226-231 — the two Reference Files entries.

Existing drift tests to update or remove, in `verify.test.ts`:
- line 53-54 region: `ALL_VERIFY_FILES` lists `verify/derive-recheck.md` and
  `verify/derive-compare.md` — those entries go when the files go (plan 07).
- lines 700-780: the `derive-compare.md` / `derive-recheck.md` drift tests, the
  `--fact-alignment` pin at :706, and the installer leaf probes at :725-758.
- lines 760-779: the `enumerate-facts.md` / `reconcile-facts.md` block, including the cross-file
  `ENUMERATE_TOKEN` lexicon pin — KEEP and extend.

The CLI surface Step 7 must name (from plans 03-04): `boardsmith verify-derive-check --json`
(read/report, emits `models`, `slices[].enumeratorPayload`, `slices[].derivedLines`, `findings`,
`verdictCounts`) and `boardsmith verify-derive-record --slice-path ... --enumerator-a ...
--enumerator-b ... --reconciler ...` (sole write surface, one invocation per SLICE).

The three pinned model ids (CONTEXT decision 4): `claude-opus-5`, `claude-haiku-4-5-20251001`,
`claude-sonnet-5`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Step 7 and the router paragraph</name>
  <read_first>
    - src/cli/slash-command/bs/verify-game.md (whole file — 251 lines; note the "In short:" delegation style every other step uses)
    - src/cli/slash-command/bs/verify/enumerate-facts.md (the contract Step 7 dispatches)
    - src/cli/slash-command/bs/verify/reconcile-facts.md (as amended in plan 03)
    - .planning/phases/177.1-wire-check04-closed-design/177.1-04-SUMMARY.md (the exact `--json` result shape and flag names)
    - .planning/phases/177-derived-line-re-derivation/177-22-MEASUREMENT/driver.mjs (the dispatch order and model assignment that was actually measured)
  </read_first>
  <action>
Rewrite `## Step 7: Derived-Line Re-Check (CHECK-04)` to describe the dual-enumeration sequence,
holding this file's established "dispatch and format, never compute" discipline:

1. Run `boardsmith verify-derive-check --json`. It enumerates every `Derived` line surviving
   `isPresentationLine` exclusion PROJECT-WIDE — independent of Step 4's staleness verdicts and not
   scoped to the chunks Step 6 touched. Keep that independence sentence; it is still true and still
   load-bearing.
2. For each slice the command reports as pending, dispatch the SAME `slices[].enumeratorPayload`
   bytes TWICE, unchanged, to two independent cross-family subagents carrying
   `${CLAUDE_SKILL_DIR}/../bs-shared/verify/enumerate-facts.md`'s `BS-ENUMERATE-V1` handshake —
   enumerator A on `claude-opus-5`, enumerator B on `claude-haiku-4-5-20251001`. State that the
   model ids come from the command's own `models` field, so this prose and the code cannot drift,
   and state WHY cross-family independence is load-bearing: two same-family enumerators would
   confirm each other's decomposition rather than independently reproduce the facts.
3. Dispatch a THIRD subagent on `claude-sonnet-5` carrying
   `${CLAUDE_SKILL_DIR}/../bs-shared/verify/reconcile-facts.md`'s `BS-RECONCILE-V1` handshake, the
   two enumerator returns, and `slices[].derivedLines`.
4. Record all three returns through exactly ONE `boardsmith verify-derive-record` invocation per
   SLICE — the CLI's atomic upsert-append, so recording a later slice never destroys a verdict
   already recorded for an earlier one. State plainly that grounding validation, arithmetic
   composition, and classification all happen INSIDE that command, never in this skill.
5. Report by formatting `boardsmith verify-derive-check --json`'s output — **formatted, never
   computed** by this skill, the same discipline Step 8's Close already holds.
6. Findings are reported and exit 0. Include CHECK-04's closure language verbatim in spirit: a
   non-corroboration is worth a human glance, NEVER a verdict, and this check must not be used as a
   build gate.

Remove every mention of `buildBlindDerivePayload`, `factAlignment`, `--fact-alignment`,
`DERIVE_VERDICTS`, `BS-DERIVE-V1`, `BS-DERIVE-COMPARE-V1`, `derive-recheck.md`, `derive-compare.md`,
and `verify-derive-recheck` from this step.

Update the router paragraph at lines 11-13 to name `verify/enumerate-facts.md` and
`verify/reconcile-facts.md` in place of the two retired files.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith &amp;&amp; grep -c "derive-recheck\|derive-compare\|BS-DERIVE\|buildBlindDerivePayload\|fact-alignment" src/cli/slash-command/bs/verify-game.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "derive-recheck\|derive-compare\|BS-DERIVE\|buildBlindDerivePayload\|fact-alignment" src/cli/slash-command/bs/verify-game.md` returns 0.
    - `grep -c "BS-ENUMERATE-V1" src/cli/slash-command/bs/verify-game.md` >= 1 and `grep -c "BS-RECONCILE-V1"` >= 1.
    - `grep -c "verify-derive-check --json" src/cli/slash-command/bs/verify-game.md` >= 1.
    - `grep -c "formatted, never computed" src/cli/slash-command/bs/verify-game.md` >= 2 (Step 7 and Step 8).
    - All three model ids `claude-opus-5`, `claude-haiku-4-5-20251001`, `claude-sonnet-5` appear in Step 7.
    - Step 7 still contains the sentence establishing independence from Step 4 staleness and Step 6 repair scope.
  </acceptance_criteria>
  <done>Step 7 dispatches the closed design and formats the new command; no retired identifier survives in it.</done>
</task>

<task type="auto">
  <name>Task 2: Re-aim the Context-Economics carve-out and Reference Files, and pin the rewrite</name>
  <read_first>
    - src/cli/slash-command/bs/verify-game.md lines 38-68 and 208-251 (the carve-out incl. its two-observable reviewer check, and Reference Files)
    - src/cli/slash-command/bs/verify.test.ts lines 40-60 and 690-800 (ALL_VERIFY_FILES, the retired drift tests, and the enumerate/reconcile block to extend)
    - src/cli/slash-command/bs/verify/enumerate-facts.md and verify/reconcile-facts.md
  </read_first>
  <action>
Rewrite the carve-out at lines 49-68, PRESERVING its structure (CONTEXT decision 7 — the
two-observable reviewer check is the valuable part) and re-aiming it:

- The ORCHESTRATOR's own rule is unchanged: this skill's transcript still shows zero quoted-rule
  lines, zero `Derived (p.` lines, and zero `Visual (p.` lines.
- The exception belongs to the SUBAGENT prompts and returns: the enumerator payload legitimately
  carries QUOTE lines — that is its entire content — and is constructed by `buildEnumeratorPayload`
  (`verify-enumerate.ts`), never composed by this orchestrator reading a slice.
- The reconciler prompt and return legitimately carry `Derived` line text and fact statements,
  because classifying a `Derived` line requires citing it.
- Keep the `174-PROOF.md` §3 `quotedPass1`/`quotedPass2` precedent citation — it is still the exact
  precedent.
- Rewrite the two-observable check with the new aim: the enumerator prompt (`BS-ENUMERATE-V1`) must
  contain ZERO `Derived (p.`, ZERO `Visual (p.`, and ZERO `Named-but-undefined (p.` lines — no
  exception applies to it, which is stronger than this rule's own transcript check — while the
  reconciler prompt and return (`BS-RECONCILE-V1`) are EXPECTED to contain `Derived` line text,
  accounted for by this carve-out. State explicitly that these are TWO separate observables and
  never one blanket grep across both.

Replace the two Reference Files entries at 226-231 with entries for
`verify/enumerate-facts.md` (CHECK-04's enumeration contract: the `BS-ENUMERATE-V1` handshake, the
never-sees list, the no-arithmetic rule) and `verify/reconcile-facts.md` (CHECK-04's reconciliation
contract: the `BS-RECONCILE-V1` handshake, the verbatim-quote grounding rule, the
`arithmeticSpec` pointer, and the `absence` proposal).

Add regression tests to `verify.test.ts` in the `enumerate-facts.md / reconcile-facts.md` describe
block: Step 7 names both contracts by path; Step 7 names `verify-derive-check --json`; Step 7 names
all three model ids; the carve-out names `BS-ENUMERATE-V1` and `BS-RECONCILE-V1` and contains the
phrase `TWO separate observables`; and an ABSENCE gate (after `stripComments`) asserting
`verify-game.md` contains none of `BS-DERIVE-V1`, `BS-DERIVE-COMPARE-V1`, `buildBlindDerivePayload`,
`factAlignment`, `verify-derive-recheck`.

Do NOT remove the retired contracts from `ALL_VERIFY_FILES` or delete the retired drift tests here —
plan 07 does that alongside the file deletions, so the suite never references a file that still
exists or a file that no longer does.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith &amp;&amp; npx vitest run src/cli/slash-command/bs/verify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/cli/slash-command/bs/verify.test.ts` passes with at least 5 new tests.
    - The new absence gate strips comments before scanning and passes.
    - `grep -c "TWO separate observables" src/cli/slash-command/bs/verify-game.md` returns 1.
    - `grep -c "174-PROOF.md" src/cli/slash-command/bs/verify-game.md` >= 1 (the precedent citation survived).
    - Reference Files lists `verify/enumerate-facts.md` and `verify/reconcile-facts.md` and no longer lists `verify/derive-recheck.md` or `verify/derive-compare.md`.
    - `npx vitest run src/cli/` is fully green.
  </acceptance_criteria>
  <done>The carve-out's reviewer check is preserved and re-aimed; the rewrite is locked by PROC-01/PROC-02-style pins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slice quote lines -> enumerator dispatch prompt | The independence guarantee lives at this boundary; the carve-out is how a reviewer audits it |
| skill prose -> installed skill | Skill prose IS source code; it installs to `~/.claude/skills/` via `npx boardsmith claude` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177.1-14 | Information disclosure | enumerator dispatch prompt | mitigate | Carve-out's stronger-than-baseline observable retained; `buildEnumeratorPayload`'s construction-site backstop is the structural enforcement, cited by name in the prose |
| T-177.1-15 | Repudiation | prose/code drift | mitigate | Cross-file pins assert Step 7 names the real command, the real tokens, and the real model ids; an absence gate catches retired-identifier resurrection |
| T-177.1-16 | Tampering | reviewer collapsing two greps into one | mitigate | Prose states the two observables are separate and never one blanket grep; test pins the phrase |
| T-177.1-SC | Tampering | npm installs | mitigate | No packages installed by this plan; escalate if one becomes necessary |
</threat_model>

<verification>
- `npx vitest run src/cli/` green.
- Zero retired identifiers remain in `verify-game.md`.
</verification>

<success_criteria>
- ROADMAP criterion 2 satisfied: Step 7 dispatches `enumerate-facts.md`/`reconcile-facts.md` and formats the new command's `--json`.
- The carve-out's two-observable reviewer check survives, re-aimed, pinned.
</success_criteria>

<output>
Create `.planning/phases/177.1-wire-check04-closed-design/177.1-05-SUMMARY.md` when done.
</output>
