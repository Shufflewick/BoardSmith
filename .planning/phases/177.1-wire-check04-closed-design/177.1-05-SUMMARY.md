---
phase: 177.1-wire-check04-closed-design
plan: 05
subsystem: skill-prose
tags: [verify-pipeline, check-04, dual-enumeration, skill-prose, regression-tests, vitest]

requires:
  - phase: 177.1-wire-check04-closed-design
    provides: "verifyDeriveCheckCommand + boardsmith verify-derive-check/verify-derive-record registered in cli.ts (177.1-04) — the exact --json shape and CLI surface this plan's Step 7 formats"
provides:
  - "verify-game.md Step 7 (CHECK-04), its router paragraph, its Context-Economics carve-out, and its Reference Files, rewritten onto the dual-enumeration design — dispatching enumerate-facts.md/reconcile-facts.md and formatting boardsmith verify-derive-check --json instead of the retired blind-derivation design"
  - "PROC-01/PROC-02-style regression pins in verify.test.ts locking the rewrite: contract paths, model ids, the --json command name, the re-aimed two-observable carve-out check, and an absence gate for every retired CHECK-04 identifier"
affects: [177.1-06, 177.1-07, 177.1-08]

tech-stack:
  added: []
  patterns:
    - "The carve-out's TWO-OBSERVABLE reviewer check structure is preserved unchanged and re-aimed: the enumerator prompt (BS-ENUMERATE-V1) must contain ZERO annotation lines of any of the three families (Derived (p., Visual (p., Named-but-undefined (p.) — stronger than the orchestrator's own transcript rule — while the reconciler prompt/return (BS-RECONCILE-V1) legitimately carries Derived line text and fact statements. Never one blanket grep across both."
    - "Step 7's prose names the three pinned model ids as literal strings (claude-opus-5/claude-haiku-4-5-20251001/claude-sonnet-5) while stating they come from the command's own `models` field, so prose and code cannot drift independently."

key-files:
  created: []
  modified:
    - src/cli/slash-command/bs/verify-game.md
    - src/cli/slash-command/bs/verify.test.ts

key-decisions:
  - "Removed the '--fact-alignment recording flag' test (old line 706) rather than updating it in place — it pinned Step 7's OLD content directly (a flag that no longer exists in the rewritten Step 7), and is now superseded by the new absence gate asserting `factAlignment` is nowhere in verify-game.md. This is distinct from — and does not touch — the retired-contract-file drift tests (installer leaf probes, scope-limit pins, derive-compare.md RETURN-field pins at lines 683-757), which the plan explicitly forbids removing this wave because derive-recheck.md/derive-compare.md still exist and still ship; those stay untouched for plan 07."
  - "Rewrote (rather than deleted) the two describe blocks that pinned Step 7's OLD literal wording directly ('CHECK-04 routing and Reference Files' and 'Context-Economics carve-out for CHECK-04') — these test verify-game.md's own content, which task 1 changed, so leaving them unmodified would have left them permanently red for a reason unrelated to plan 07's scope. Renamed both describe blocks '(177.1-05)' to mark the generation that owns them."
  - "'states findings never gate Close' was rewritten to 'states findings never gate the build', matching CONTEXT decision 14's own language ('this check must not be used as a build gate') rather than the retired-design phrase 'never a Close gate' that Step 7 no longer contains verbatim."

patterns-established:
  - "Slicing verify-game.md between '## Step 7: Derived-Line Re-Check' and '## Step 8: Close' (or between the carve-out heading and '## Step 0:') to scope a regression assertion to exactly the section under test, rather than asserting substring-presence anywhere in the whole file — used by all 5 new tests this plan adds, avoiding a false pass if e.g. a model id or token merely appears elsewhere in the file."

requirements-completed: []

duration: ~1h10min
completed: 2026-07-31
---

# Phase 177.1 Plan 05: verify-game.md Step 7 — the dual-enumeration rewrite Summary

**Rewrote `verify-game.md`'s Step 7, router paragraph, Context-Economics carve-out, and Reference Files onto the dual-enumeration design CHECK-04 actually closed on — dispatching `enumerate-facts.md`/`reconcile-facts.md` and formatting `boardsmith verify-derive-check --json` — and pinned the rewrite with PROC-01/PROC-02-style regression tests, clearing the wave's one in-scope drift-guard failure.**

## Performance

- **Duration:** ~1h10min
- **Started:** 2026-07-31
- **Completed:** 2026-07-31
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Rewrote the router paragraph (naming `verify/enumerate-facts.md` and `verify/reconcile-facts.md` in place of the two retired files) and Step 7 itself: it now runs `boardsmith verify-derive-check --json`, dispatches the SAME `slices[].enumeratorPayload` bytes twice (unchanged) to two cross-family enumerators (`claude-opus-5`/`claude-haiku-4-5-20251001`) carrying `BS-ENUMERATE-V1`, dispatches a third `claude-sonnet-5` reconciler carrying `BS-RECONCILE-V1` plus both enumerator returns and `slices[].derivedLines`, records all three returns through exactly ONE `boardsmith verify-derive-record` invocation per SLICE, and reports by formatting `boardsmith verify-derive-check --json`'s output — **formatted, never computed**, matching Step 8's own discipline. Preserved the independence-from-staleness/repair sentence verbatim and D-14's advisory/exit-0/never-a-build-gate language.
- Re-aimed the Context-Economics carve-out (CONTEXT decision 7): kept its structure and the `174-PROOF.md` §3 `quotedPass1`/`quotedPass2` precedent citation verbatim, re-pointed the exception at `enumerate-facts.md`'s `buildEnumeratorPayload`-constructed prompt and `reconcile-facts.md`'s prompt/return, and rewrote the TWO-OBSERVABLE reviewer check: the enumerator prompt (`BS-ENUMERATE-V1`) must contain ZERO annotation lines across all three families (stronger than the orchestrator's own rule); the reconciler prompt/return (`BS-RECONCILE-V1`) is EXPECTED to carry `Derived` line text — two separate observables, never one blanket grep.
- Replaced the two Reference Files entries with `verify/enumerate-facts.md` (the enumeration contract: `BS-ENUMERATE-V1`, the never-sees list, the no-arithmetic rule) and `verify/reconcile-facts.md` (the reconciliation contract: `BS-RECONCILE-V1`, the verbatim-quote grounding rule, the `arithmeticSpec` pointer, the `absence` proposal).
- Removed every retired identifier from `verify-game.md`: `derive-recheck.md`, `derive-compare.md`, `BS-DERIVE-V1`, `BS-DERIVE-COMPARE-V1`, `buildBlindDerivePayload`, `factAlignment`, `--fact-alignment`, `DERIVE_VERDICTS`, `verify-derive-recheck` — grep-count 0, confirmed by a new absence-gate test.
- Added 5 new pins in `verify.test.ts`'s `enumerate-facts.md / reconcile-facts.md` describe block (contracts named by path in Step 7; `verify-derive-check --json` named in Step 7; all three model ids named in Step 7; carve-out names both tokens plus `TWO separate observables`; the absence gate itself), and rewrote the two describe blocks that pinned Step 7's/the carve-out's OLD literal wording directly so they assert the new content instead of the retired one. Removed the now-false `--fact-alignment` positive pin, superseded by the absence gate.
- Left `ALL_VERIFY_FILES`, the `derive-compare.md`/`derive-recheck.md` installer leaf probes, scope-limit pins, and the `derive-compare.md` RETURN-field content pins (lines ~683-757) completely untouched — those files still exist and still ship; their deletion is plan 07's job, per the plan's explicit prohibition.

## Task Commits

1. **Task 1: Rewrite Step 7 and the router paragraph (+ carve-out + Reference Files)** — `a59cdb14` (feat)
2. **Task 2: Re-aim the Context-Economics carve-out's tests and pin the rewrite** — `c0bbbaf0` (test)

**Plan metadata:** commit pending (this SUMMARY + STATE.md + ROADMAP.md + REQUIREMENTS.md)

## Files Created/Modified

- `src/cli/slash-command/bs/verify-game.md` — Router paragraph, Step 7, Context-Economics carve-out, and Reference Files rewritten onto the dual-enumeration design; zero retired identifiers remain.
- `src/cli/slash-command/bs/verify.test.ts` — 5 new regression pins; two describe blocks rewritten to assert the new Step 7/carve-out content; the stale `--fact-alignment` positive pin removed.

## Decisions Made

See `key-decisions` in frontmatter.

## Deviations from Plan

**None from the plan's own explicit scope.** One judgment call, disclosed: the plan's `<interfaces>` section listed "lines 700-780: the derive-compare.md/derive-recheck.md drift tests, the --fact-alignment pin at :706, and the installer leaf probes at :725-758" together as "existing drift tests to update or remove," while the `<action>` block separately prohibited removing "the retired contracts from ALL_VERIFY_FILES or delete the retired drift tests here — plan 07 does that." I resolved this by treating the two groups differently: the `--fact-alignment` pin (line 706) tests `verify-game.md`'s OWN content and had to change because task 1 changed that content — I removed it (superseded by the new absence gate) rather than leave it permanently red for a reason unrelated to plan 07. The installer leaf probes and RETURN-field pins (711-757) test the STILL-EXISTING `derive-compare.md`/`derive-recheck.md` FILES' own content and ship status — untouched, per the explicit prohibition, and they still pass because those files are untouched.

I also rewrote (did not delete) two describe blocks the plan's interfaces section did not name by line number — `verify-game.md — CHECK-04 routing and Reference Files` and `verify-game.md — Context-Economics carve-out for CHECK-04` (old lines ~928-986) — because they pinned Step 7's/the carve-out's OLD literal wording (`BS-DERIVE-V1`, `derive-recheck.md`, the two-observable regex's old "ZERO Derived and ZERO Visual" two-clause form) directly against `verify-game.md`, which task 1 necessarily changed. Leaving them unmodified would have left the suite red for reasons outside plan 07's stated scope (derive-compare.md deletion), contradicting the wave's target end-state of exactly 2 failures, both `derive-compare.md`-related.

## Verification Results

- `npx vitest run src/cli/slash-command/bs/verify.test.ts`: 126 tests, 124 passing, 2 failing — both are `derive-compare.md`-RETURN-field drift-guard tests (plan 07's job), exactly the target end-state stated in this wave's brief.
- `npx vitest run src/cli/`: 1646 tests / 49 files, 1644 passing, 2 failing (same 2).
- **Full suite: 4230 tests / 245 files, 4228 passing, 2 failing** (same 2). Baseline entering this wave was 4224/245, 4221 passing, 3 failing. This wave: +6 net tests (7 added, 1 removed), and the ONE test this wave was scoped to clear — `drift guard — every verify-* command mentioned in verify-game.md and bs/verify/*.md is registered in cli.ts` — is now green, because Step 7's `boardsmith verify-derive-recheck --json` mention (the only bare-backtick, non-`.ts`-suffixed mention of the deregistered command name) is gone, replaced by `boardsmith verify-derive-check --json`, which IS registered.
- `npx tsc --noEmit`: unchanged before/after (the one pre-existing, unrelated `TS6059` error in `docs/seed-to-state.test.ts`).
- `grep -c "derive-recheck\|derive-compare\|BS-DERIVE\|buildBlindDerivePayload\|fact-alignment" src/cli/slash-command/bs/verify-game.md` → 0.
- `grep -c "BS-ENUMERATE-V1"` → 3, `grep -c "BS-RECONCILE-V1"` → 3, `grep -c "verify-derive-check --json"` → 3, `grep -c "formatted, never computed"` → 2 (Step 7 + Step 8), `grep -c "TWO separate observables"` → 1, `grep -c "174-PROOF.md"` → 1. All three model ids appear in Step 7.

## Issues Encountered

None beyond the documented deviation reasoning above.

## User Setup Required

None — no external service configuration required. No package installs.

## Next Phase Readiness

`verify-game.md`'s Step 7 now dispatches the contracts CHECK-04 actually closed on, and formats a command that is genuinely registered and invocable. Downstream plans need to know:

- **The suite is now RED on exactly 2 tests, both in `src/cli/slash-command/bs/verify.test.ts`, both in the `drift guard — every field named in derive-compare.md's RETURN block corresponds to an accepted verify-derive-record CLI option` describe block** — they assert `derive-compare.md`'s RETURN section still declares `verdict`/`reasoning`/`originalReading`/`rederivedReading`/`factAlignment` as CLI flags on `verify-derive-record`, which no longer carries any of them (177.1-03's deliberate change). **These two tests exist to be deleted, not fixed, when plan 07 deletes `derive-compare.md`/`derive-recheck.md` and their guard describe block.** Do not "fix" them by re-adding retired flags to `verify-derive-record` — that would resurrect the retired design.
- **`derive-recheck.md`/`derive-compare.md` (the files) are completely untouched by this plan** — still installed, still content-pinned by their own drift tests (lines ~547-757 in `verify.test.ts`), still listed in `ALL_VERIFY_FILES`. Plan 07 deletes both files, their two describe blocks (`derive-recheck.md / derive-compare.md — the two CHECK-04 judgment contracts`, and the `drift guard — every field named in derive-compare.md's RETURN block...` block), and their `ALL_VERIFY_FILES` entries, together, in one atomic pass — deleting the files without deleting the tests (or vice versa) will produce exactly the opposite failure mode this plan just resolved.
- **`verify-game.md` Step 7's literal dispatch sequence** (for plan 06's live product-path replay against the `177-22` recorded slice): run `verify-derive-check --json` → for each pending slice, dispatch `enumeratorPayload` twice (once per model, `BS-ENUMERATE-V1`) → dispatch `BS-RECONCILE-V1` with both enumerator returns + `derivedLines` → one `verify-derive-record` call per slice with all three returns as files → re-run `verify-derive-check --json` to report. This is the exact sequence plan 06 needs to replay through the product CLI and compare against `177-22-MEASUREMENT/`'s recorded per-run results, per CONTEXT decisions 10-13 (the split mechanical/model-dependent bar, pre-registered before dispatch).
- **The carve-out's structural enforcement is unchanged**: `buildEnumeratorPayload` (`verify-enumerate.ts`) is still the sole construction site for the enumerator prompt, and `verify-derive-check`'s `slices[].derivedLines` field is still the sole source of the reconciler's `Derived` line text — neither is composed by the orchestrator reading a slice. Nothing in this plan touched that code; it only re-cited it correctly in prose.

---
*Phase: 177.1-wire-check04-closed-design*
*Completed: 2026-07-31*

## Self-Check: PASSED
Both modified files and both task commit hashes verified present.
