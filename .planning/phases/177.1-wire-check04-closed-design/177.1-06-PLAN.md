---
phase: 177.1-wire-check04-closed-design
plan: 06
type: execute
wave: 6
depends_on: [177.1-05]
files_modified:
  - .planning/phases/177.1-wire-check04-closed-design/177.1-PRE-REGISTRATION.md
  - .planning/phases/177.1-wire-check04-closed-design/177.1-REPLAY-PROOF/
autonomous: true
requirements: [CHECK-04]

must_haves:
  truths:
    - "The proof bar is written down and committed BEFORE any comparison is run"
    - "A zero-dispatch dry run predicts every mechanical output exactly, before any model is called"
    - "Replaying 177-22 run1's recorded returns through the product CLI reproduces analysis-run1.json's mechanical outputs and classifications exactly"
    - "The reference game's working tree is byte-identical before and after"
  artifacts:
    - path: ".planning/phases/177.1-wire-check04-closed-design/177.1-PRE-REGISTRATION.md"
      provides: "Committed-alone expectations: exact mechanical outputs plus the permitted label set per line"
      contains: "Satisfiability self-check"
    - path: ".planning/phases/177.1-wire-check04-closed-design/177.1-REPLAY-PROOF/diff-report.md"
      provides: "Item-by-item comparison of prediction vs. product-path result"
  key_links:
    - from: "boardsmith verify-derive-check"
      to: "177-22-MEASUREMENT/manifest.json"
      via: "byte-identical enumerator payload comparison"
      pattern: "enumeratorPayload"
    - from: "boardsmith verify-derive-record"
      to: "177-22-MEASUREMENT/analysis-run1.json"
      via: "classification-by-classification comparison"
      pattern: "classification"
---

<objective>
Prove the product path reproduces the measurement harness with ZERO new model dispatches, and
pre-register the bar before running the comparison.

Purpose: CONTEXT decisions 10, 11, 12, 13. Decision 12 makes the zero-dispatch dry run a
first-class deliverable — the instrument that makes the mechanical half of the bar both EXACT and
provably reachable before any model is called. Because this plan replays RECORDED model output,
even classification is deterministic here; the permitted-label-set half of decision 11's split bar
is exercised by plan 08's live run, not this one.

Comparison target (decision 10, Claude's-discretion slice choice): 177-22 run 1's `seven` corpus —
`rulebook/01-definitions-and-components.md` (L21, single-operation arithmetic) and
`rulebook/01-overview-setup-and-play.md` (L36 chain arithmetic, L38 plain corroboration). It is the
only recorded slice pair exercising grounding, BOTH arithmetic composition kinds, and plain
corroboration, at a size one agent can verify line by line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177.1-wire-check04-closed-design/177.1-CONTEXT.md
@.planning/phases/177.1-wire-check04-closed-design/177.1-05-SUMMARY.md
@.planning/phases/177-derived-line-re-derivation/177-22-MEASUREMENT/PRE-REGISTRATION.md
@CLAUDE.md
</context>

<interfaces>
Recorded comparison data, all committed under
`.planning/phases/177-derived-line-re-derivation/177-22-MEASUREMENT/`:

- `manifest.json` — per-slice `buildEnumeratorPayload` output and the mechanical `Derived` line list.
- `run1/enum/seven__*.A.json`, `*.B.json` — enumerator returns; the model's raw text is under `facts`.
- `run1/reconcile/seven__*.json` — reconciler returns; raw text under `reconcile`.
- `analysis-run1.json` — the record to reproduce. `01-definitions-and-components`: A=13, B=9,
  grounded=9, rejected=0, classifications `[[21,'corroborated-by-composition']]`.
  `01-overview-setup-and-play`: A=25, B=19, grounded=19, rejected=0, classifications
  `[[36,'corroborated-by-composition'],[38,'corroborated']]`.
- `analyze.mjs:113-136` — `ARITHMETIC_LINES`: `seven__01-definitions-and-components:21` is
  single/multiply, operand magnitudes [7,4,4], claimed result 112 "numbered cards", approximate
  false; `seven__01-overview-setup-and-play:36` is a chain — subtract[2,1], subtract[10,3], divide
  stepResult[1]/stepResult[0], claimed result 7 "rounds", approximate false.
- `analyze.mjs:41-104` — `repairUnescapedQuotes`, `tryParseJson`, `loadFacts`.
- `baseline-seven.sha256` — the pre-run tree hash for `~/BoardSmithGames/seven`.

KNOWN, DISCLOSABLE ADAPTATION: the recorded reconciler returns predate plan 03's `arithmeticSpec`
field, so the replay must supply it for the two arithmetic lines, translated from `ARITHMETIC_LINES`
into the new contract shape. This adapts INPUT SHAPE, not judgment — operands, operation, and
claimed result are byte-identical to what the harness used. Disclose it in the pre-registration
BEFORE running, never after.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Zero-dispatch dry run and pre-registration, committed alone</name>
  <read_first>
    - .planning/phases/177-derived-line-re-derivation/177-22-MEASUREMENT/PRE-REGISTRATION.md
    - .planning/phases/177-derived-line-re-derivation/177-22-MEASUREMENT/manifest.json
    - .planning/phases/177-derived-line-re-derivation/177-22-MEASUREMENT/analysis-run1.json
    - .planning/phases/177-derived-line-re-derivation/177-22-MEASUREMENT/analyze.mjs lines 106-202
    - .planning/phases/177.1-wire-check04-closed-design/177.1-04-SUMMARY.md
  </read_first>
  <action>
Stage first per CONTEXT decision 13: `cp -R ~/BoardSmithGames/seven` into this plan's scratch
directory, write a sha256 baseline of the ORIGINAL tree to
`177.1-REPLAY-PROOF/baseline-seven.sha256`, and operate only on the copy.

Run the ZERO-DISPATCH DRY RUN — `boardsmith verify-derive-check --project <staged-seven> --json`
with no ledger present. No model is called. Save to `177.1-REPLAY-PROOF/dry-run.json`.

Write `177.1-PRE-REGISTRATION.md` recording, BEFORE any comparison executes:

MECHANICAL HALF (exact, predicted from the dry run): the sha256 of each slice's
`enumeratorPayload` from `dry-run.json` alongside the sha256 of the corresponding payload in
`manifest.json`, predicted EQUAL because `buildEnumeratorPayload` is the same unmodified function
the harness called; the exact candidate set per slice, predicted equal to `manifest.json`'s
`derivedLines`; predicted replay grounded/rejected counts 9/0 and 19/0; predicted composed-fact ids
for L21 and L36, obtained by running `composeArithmeticClaim`/`composeArithmeticChain` directly on
the recorded grounded facts and quoted as literals; predicted classifications L21
`corroborated-by-composition`, L36 `corroborated-by-composition`, L38 `corroborated`; predicted
exit code 0 for every invocation.

MODEL-DEPENDENT HALF (for plan 08's LIVE run only): for each of `seven`'s three lines, derive
MECHANICALLY — recording the command, never hand-copying — the union of labels observed across
`177-20-MEASUREMENT`, `177-21-MEASUREMENT`, and `177-22-MEASUREMENT` analysis JSON. State the bar:
plan 08's live classification for a line PASSES if it is a member of that line's permitted set. An
exact-label bar is FORBIDDEN — it re-imports the retired determinism gate against a measured ~12.5%
cross-run variance and is unsatisfiable by construction.

DISCLOSURES (stated before running): the `arithmeticSpec` input-shape adaptation, with the
translated spec written out verbatim, plus any other input the replay must supply that the recorded
returns do not carry.

SATISFIABILITY SELF-CHECK (required section): one line per committed criterion stating whether a
CORRECT implementation could fail it for reasons outside its control. Any criterion where the
answer is yes must be rewritten or dropped BEFORE commit.

Commit this file ALONE — no other change in the commit — matching 177-22's discipline.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith &amp;&amp; git show --stat --format= HEAD</automated>
  </verify>
  <acceptance_criteria>
    - `177.1-PRE-REGISTRATION.md` exists and `git show --stat --format= HEAD` lists exactly one changed file.
    - The file contains a `## Satisfiability self-check` section with one line per committed criterion.
    - The file states the permitted-label-set bar, names the ~12.5% measured variance, and explicitly forbids an exact-label bar for the live run.
    - `177.1-REPLAY-PROOF/dry-run.json` parses as JSON and carries a non-empty `enumeratorPayload` for both `seven` slices.
    - `177.1-REPLAY-PROOF/baseline-seven.sha256` is byte-identical to `177-22-MEASUREMENT/baseline-seven.sha256`. If it is NOT, STOP and report the difference rather than proceeding — the comparison target's input would no longer be the measured one.
    - Zero `claude -p` and zero Task-tool dispatches occurred during this task.
  </acceptance_criteria>
  <done>The bar is written down, checked for satisfiability, and committed alone — before any comparison could tune it.</done>
</task>

<task type="auto">
  <name>Task 2: Replay 177-22 run1 through the product CLI and diff against the pre-registration</name>
  <read_first>
    - .planning/phases/177.1-wire-check04-closed-design/177.1-PRE-REGISTRATION.md (as committed in Task 1)
    - .planning/phases/177-derived-line-re-derivation/177-22-MEASUREMENT/run1/enum/seven__01-definitions-and-components.A.json
    - .planning/phases/177-derived-line-re-derivation/177-22-MEASUREMENT/analyze.mjs lines 41-104
    - .planning/phases/177.1-wire-check04-closed-design/177.1-03-SUMMARY.md
    - .planning/phases/177.1-wire-check04-closed-design/177.1-04-SUMMARY.md
  </read_first>
  <action>
Transform the recorded run1 `seven` returns into the three JSON files the product CLI expects:
extract `.facts` from each `*.A.json`/`*.B.json` and `.reconcile` from each reconciler file. Inject
the pre-registered `arithmeticSpec` into the two arithmetic proposals and change nothing else. Save
these inputs under `177.1-REPLAY-PROOF/inputs/` so the transformation is auditable.

If a recorded return needs the unescaped-quote repair, apply the SAME generic repair `analyze.mjs`
describes, log the repair count to `177.1-REPLAY-PROOF/repair-log.json`, and disclose it — never
silently.

Run, against the staged `seven` copy, once per slice:
`boardsmith verify-derive-record --project <staged> --slice-path rulebook/<slice>.md
--enumerator-a <A.json> --enumerator-b <B.json> --reconciler <R.json> --json`

Then `boardsmith verify-derive-check --project <staged> --json`, saved to
`177.1-REPLAY-PROOF/replay-result.json`.

Write `177.1-REPLAY-PROOF/diff-report.md` comparing item by item every mechanical prediction against
what happened: payload sha256s, candidate sets, grounded and rejected counts, composed-fact ids,
per-line classifications, `citedFactIds`, and exit codes.

Report EVERY divergence, including ones that look benign. A divergence is a finding to explain, not
a number to adjust — if a prediction was wrong, say so and say why. NEVER edit
`177.1-PRE-REGISTRATION.md`.

Finally re-hash the ORIGINAL `~/BoardSmithGames/seven` tree, prove it byte-identical to the
baseline (decision 13), and record that in the diff report.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith &amp;&amp; test -f .planning/phases/177.1-wire-check04-closed-design/177.1-REPLAY-PROOF/diff-report.md &amp;&amp; shasum -a 256 -c .planning/phases/177.1-wire-check04-closed-design/177.1-REPLAY-PROOF/baseline-seven.sha256</automated>
  </verify>
  <acceptance_criteria>
    - `diff-report.md`, `replay-result.json`, and `inputs/` exist under `177.1-REPLAY-PROOF/`.
    - `diff-report.md` states, per item, PREDICTED vs ACTUAL for all seven mechanical categories listed in the action, with an explicit MATCH or DIVERGENCE verdict on each.
    - The two `enumeratorPayload` sha256s equal the `manifest.json` payload sha256s for the same slices.
    - Replayed grounded/rejected counts are exactly 9/0 and 19/0; replayed classifications are exactly `[[21,'corroborated-by-composition']]` and `[[36,'corroborated-by-composition'],[38,'corroborated']]`.
    - `shasum -a 256 -c 177.1-REPLAY-PROOF/baseline-seven.sha256` reports OK for every entry (the original reference game is untouched).
    - Zero `claude -p` and zero Task-tool dispatches occurred during this task.
    - If any prediction diverged, `diff-report.md` names the root cause and the SUMMARY records it as a finding; the pre-registration file is unmodified (`git diff HEAD~1 -- 177.1-PRE-REGISTRATION.md` is empty).
  </acceptance_criteria>
  <done>The product path is proven to reproduce the measurement harness's mechanical outputs and classifications exactly, on recorded input, with the reference game untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded harness data -> product CLI | Committed model output crosses into the product's compute path |
| staged copy -> original reference game | Any write escaping the staged copy corrupts the comparison target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177.1-17 | Tampering | post-hoc bar tuning | mitigate | Pre-registration committed alone before comparison; acceptance criterion asserts the file is unmodified afterward |
| T-177.1-18 | Tampering | original reference game mutated | mitigate | `cp -R` staging, sha256 baseline before, `shasum -c` after (decision 13) |
| T-177.1-19 | Repudiation | undisclosed input repair | mitigate | Any JSON repair is generic, counted, logged to `repair-log.json`, and disclosed — matching 177-22's own disclosure |
| T-177.1-20 | Denial of service | unsatisfiable criterion | mitigate | Mandatory satisfiability self-check section before commit (decision 11) |
| T-177.1-SC | Tampering | npm installs | mitigate | No packages installed by this plan; escalate if one becomes necessary |
</threat_model>

<verification>
- `diff-report.md` shows MATCH on every mechanical category.
- The original `~/BoardSmithGames/seven` tree hashes clean against its baseline.
</verification>

<success_criteria>
- The mechanical half of decision 11's bar is committed before the run and met exactly.
- The permitted label set for the live run is derived mechanically and committed.
- Zero model dispatches were needed to prove product/harness agreement on recorded input.
</success_criteria>

<output>
Create `.planning/phases/177.1-wire-check04-closed-design/177.1-06-SUMMARY.md` when done.
</output>
