---
phase: 177.1-wire-check04-closed-design
plan: 06
subsystem: verify-pipeline
tags: [verify-pipeline, check-04, dual-enumeration, product-path-proof, pre-registration, zero-dispatch]

requires:
  - phase: 177.1-wire-check04-closed-design
    provides: "reconcileSlice() + verifyDeriveRecordCommand + verifyDeriveCheckCommand, both registered in cli.ts (177.1-03/177.1-04) — the exact product surface this plan proves against the 177-22 measurement harness"
  - phase: 177-derived-line-re-derivation
    provides: "177-22-MEASUREMENT's committed run1 recorded dispatch data (manifest.json, payloads/, run1/enum, run1/reconcile, analysis-run1.json, baseline-seven.sha256) — this plan's comparison target"
provides:
  - "177.1-PRE-REGISTRATION.md — the split mechanical/model-dependent bar (CONTEXT decision 11), committed alone before any comparison, predicting all 6 mechanical categories from a zero-dispatch dry run plus direct reconcileSlice calls, and deriving a mechanically-computed permitted label set per line for plan 08's live run"
  - "177.1-REPLAY-PROOF/ — dry-run.json, inputs/ (independently-transformed replay inputs), replay-result.json, diff-report.md, repair-log.json, baseline-seven.sha256: proof that the product CLI reproduces analysis-run1.json's mechanical outputs and classifications exactly, on recorded input, with zero model dispatches and the original reference game untouched"
affects: [177.1-07, 177.1-08]

tech-stack:
  added: []
  patterns:
    - "Pre-registration discipline: commit the prediction file ALONE (git show --stat --format= HEAD lists exactly one changed file), then run the comparison as a SEPARATE commit — proven in git history, not just asserted in prose."
    - "Zero-dispatch prediction via direct, in-process calls to the exported pure functions (reconcileSlice, via a standalone tsx script) as a stronger proof step than reading the CLI's own --json output, since verify-derive-check --json's read-only report never exposes groundedBothCount or composed-fact ids directly — only verify-derive-record's citedFactIds does, at record time."

key-files:
  created:
    - .planning/phases/177.1-wire-check04-closed-design/177.1-PRE-REGISTRATION.md
    - .planning/phases/177.1-wire-check04-closed-design/177.1-REPLAY-PROOF/dry-run.json
    - .planning/phases/177.1-wire-check04-closed-design/177.1-REPLAY-PROOF/baseline-seven.sha256
    - .planning/phases/177.1-wire-check04-closed-design/177.1-REPLAY-PROOF/inputs/*.json
    - .planning/phases/177.1-wire-check04-closed-design/177.1-REPLAY-PROOF/repair-log.json
    - .planning/phases/177.1-wire-check04-closed-design/177.1-REPLAY-PROOF/replay-result.json
    - .planning/phases/177.1-wire-check04-closed-design/177.1-REPLAY-PROOF/diff-report.md
  modified: []

key-decisions:
  - "baseline-seven.sha256's exact reproduction command was reverse-engineered from the recorded file's own content (263 markdown/pdf files, including node_modules README/LICENSE/CHANGELOG files) rather than assumed: find \"$(pwd)\" \\( -iname '*.md' -o -iname '*.pdf' \\) -type f, confirmed byte-for-byte identical (diff exit 0, no sort needed) to 177-22-MEASUREMENT/baseline-seven.sha256 before any staging work began — proving the original reference game has not changed since 177-22 measured it."
  - "Replay inputs (inputs/*.json) were transformed INDEPENDENTLY from the raw run1/enum and run1/reconcile recordings (not copied from 177.1-03's fixtures), then cross-checked byte-identical to 177.1-03's own hand-amended fixtures as a second, independent confirmation that the arithmeticSpec disclosure was applied consistently."
  - "Composed-fact ids and groundedBothCount, which verify-derive-record --json's output shape does not directly expose, were confirmed via a standalone zero-dispatch tsx script calling reconcileSlice directly against the transformed inputs — the same function verify-derive-record calls internally — rather than left unverified."

patterns-established:
  - "The mechanical half of decision 11's split bar is checked per-category in a table (PREDICTED vs ACTUAL vs verdict) in diff-report.md, so a future reader can audit each of the 7 committed categories independently rather than trusting one blanket 'it matched' claim."

requirements-completed: [CHECK-04]

duration: ~1h40min
completed: 2026-07-31
---

# Phase 177.1 Plan 06: Zero-dispatch pre-registration + product-path replay of 177-22 run1 Summary

**Pre-registered the mechanical/model-dependent split bar from a zero-dispatch dry run, committed it alone, then replayed 177-22 run1's recorded `seven` corpus through the real product CLI — every one of 7 mechanical categories (payload bytes, candidates, grounded/rejected counts, composed-fact operand ids, classifications, citedFactIds, exit codes) matched exactly, with zero model dispatches and the original reference game proven untouched.**

## Performance

- **Duration:** ~1h40min
- **Started:** 2026-07-31
- **Completed:** 2026-07-31
- **Tasks:** 2
- **Files modified:** 0 (all new files — no existing source touched)

## Accomplishments

- Reverse-engineered and reproduced `177-22-MEASUREMENT/baseline-seven.sha256`'s exact selection command (`find "$(pwd)" \( -iname '*.md' -o -iname '*.pdf' \) -type f`), confirming byte-for-byte identity with the recorded baseline before any staging work — proof the reference game has not drifted since 177-22 measured it.
- Wrote `177.1-PRE-REGISTRATION.md` and committed it ALONE (`be9ce540`, one file changed), predicting all 6 mechanical categories (enumerator payload sha256, candidate-line selection, grounded/rejected counts, composed-fact ids, per-line classifications, exit codes) from a zero-dispatch `verify-derive-check --json` dry run plus direct in-process `reconcileSlice` calls against the recorded, disclosed-and-amended `arithmeticSpec` inputs. Mechanically derived (script-computed, not hand-picked) the permitted-label-set bar for plan 08's live run from the union of classifications across all six `177-20`/`177-21`/`177-22` analysis JSONs, and explicitly named the ~16.7% measured cross-run variance that makes an exact-label bar unsatisfiable by construction.
- Independently transformed run1's raw recorded returns into the product's input shape (Task 2, `22169f5d`) — 0 unescaped-quote repairs needed — and cross-checked the result byte-identical to 177.1-03's own hand-amended fixtures, a second independent confirmation the `arithmeticSpec` disclosure was applied consistently.
- Replayed both `seven` slices through `boardsmith verify-derive-record` then `boardsmith verify-derive-check --json` against the `cp -R` staged copy: zero divergences on all 7 mechanical categories, documented item-by-item in `diff-report.md`. Re-observed (not a new finding) 177.1-03's already-disclosed `validateGrounding` tie-break behavior on L21's composed proposal.
- Proved the ORIGINAL `~/BoardSmithGames/seven` untouched: `shasum -a 256 -c` against the 263-entry baseline reports 263/263 OK, re-run after the full replay.

## Task Commits

1. **Task 1: Zero-dispatch dry run and pre-registration, committed alone** — `be9ce540` (docs)
2. **Task 2: Replay 177-22 run1 through the product CLI and diff against the pre-registration** — `22169f5d` (test)

**Plan metadata:** commit pending (this SUMMARY + STATE.md + ROADMAP.md + REQUIREMENTS.md)

## Files Created/Modified

- `.planning/phases/177.1-wire-check04-closed-design/177.1-PRE-REGISTRATION.md` — The split bar, committed alone before any comparison ran.
- `.planning/phases/177.1-wire-check04-closed-design/177.1-REPLAY-PROOF/dry-run.json` — The zero-dispatch dry run's raw output.
- `.planning/phases/177.1-wire-check04-closed-design/177.1-REPLAY-PROOF/baseline-seven.sha256` — sha256 of the ORIGINAL `~/BoardSmithGames/seven` tree, byte-identical to `177-22-MEASUREMENT/baseline-seven.sha256`.
- `.planning/phases/177.1-wire-check04-closed-design/177.1-REPLAY-PROOF/inputs/*.json` — The 6 independently-transformed replay input files (2 enumerator returns + 1 reconciler return per slice, `arithmeticSpec`-injected).
- `.planning/phases/177.1-wire-check04-closed-design/177.1-REPLAY-PROOF/repair-log.json` — 0 unescaped-quote repairs needed.
- `.planning/phases/177.1-wire-check04-closed-design/177.1-REPLAY-PROOF/replay-result.json` — The final `verify-derive-check --json` report after both slices were recorded.
- `.planning/phases/177.1-wire-check04-closed-design/177.1-REPLAY-PROOF/diff-report.md` — Item-by-item PREDICTED-vs-ACTUAL comparison across all 7 mechanical categories, all MATCH.

## Decisions Made

See `key-decisions` in frontmatter.

## Deviations from Plan

**None.** Both tasks executed exactly as the plan specified. The one judgment call — transforming replay inputs independently from the raw run1 recordings rather than simply reusing 177.1-03's already-committed fixtures — was chosen deliberately to produce a stronger, doubly-independent proof (own transformation cross-checked against 177.1-03's own), not a deviation from any instruction.

## Findings (recorded, not silently resolved)

**No new findings.** This replay re-observed, but did not newly discover, the two properties 177.1-03 already disclosed:

1. **The `arithmeticSpec` scope boundary (177.1-03 Finding 1) still applies, restated here for plan 08's benefit.** This plan's exactness claim covers the MECHANICAL pipeline only — `validateGrounding` → `composeArithmeticClaim`/`composeArithmeticChain` → `classifyDerivedLines`, all deterministic given fixed input. It does **not** cover whether a live Sonnet-5 reconciler dispatch, following the now-fully-wired `reconcile-facts.md` prompt (177.1-05 wired Step 7 to dispatch it), will independently produce a well-formed `arithmeticSpec` that resolves the same way. No live dispatch has ever populated this field for any corpus, including this one. **Plan 08 owes that proof, not this plan.**
2. **`validateGrounding`'s deterministic tie-break (177.1-03 Finding 2) re-observed identically.** L21's replayed `groundedQuotes` again show the same underlying "4 copies" grounded fact cited twice across two nominally-distinct `arithmeticSpec.operandStatements` entries, because both quote the same source sentence. Exactly as predicted in the pre-registration — not a new discovery, and the composed-fact id is unaffected either way (it is a function of resolved MAGNITUDES, never of which fact object supplied them).

## Issues Encountered

None. All predictions matched on the first replay attempt — no debug iteration was needed.

## User Setup Required

None — no external service configuration required. No package installs.

## Next Phase Readiness

**Plan 07 (deletes the retired design) can proceed unblocked.** This plan did not touch `derive-recheck.ts`, `derive-compare.md`, `derive-recheck.md`, or their tests — they remain exactly as 177.1-05 left them (still live, still installed, still content-pinned, still the source of the suite's 2 known-red drift-guard tests). Suite state entering plan 07: **4230 tests / 245 files, 4228 passing, 2 failing** — the same 2 failures baselined at the start of this wave, both in `verify.test.ts`'s `derive-compare.md` RETURN-block drift-guard describe block, both to be **deleted** (not fixed) when plan 07 removes `derive-compare.md`/`derive-recheck.md` and their guard tests together.

**Plan 08 (the live-dispatch proof) needs, from this plan:**
- The exact permitted-label-set bar (`177.1-PRE-REGISTRATION.md`'s "MODEL-DEPENDENT HALF" section): `seven__01-definitions-and-components:21` → `{corroborated-by-composition, uncorroborated}`; `seven__01-overview-setup-and-play:36` → `{corroborated-by-composition}`; `seven__01-overview-setup-and-play:38` → `{corroborated, uncorroborated}`. Membership is the bar; exact-label agreement is forbidden.
- The unresolved `arithmeticSpec` live-dispatch gap (Finding 1 above, carried from 177.1-03): plan 08's live reconciler dispatch is the FIRST time any real model call will attempt to populate `arithmeticSpec` for this corpus — there is no precedent proving it will produce a well-formed, correctly-resolving spec.
- The `validateGrounding` tie-break behavior (Finding 2): if plan 08's live dispatch produces `arithmeticSpec.operandStatements` naming two statements that happen to quote the same source sentence, they will resolve to the same grounded fact — a real, structural property to watch for, not a bug to chase.
- `.planning/phases/177.1-wire-check04-closed-design/177.1-REPLAY-PROOF/` is a complete, self-contained audit trail (dry run, transformed inputs, replay result, diff report) that plan 08 can reference directly rather than re-deriving.

---
*Phase: 177.1-wire-check04-closed-design*
*Completed: 2026-07-31*

## Self-Check: PASSED
Verified below.
