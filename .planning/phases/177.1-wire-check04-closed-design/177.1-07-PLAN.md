---
phase: 177.1-wire-check04-closed-design
plan: 07
type: execute
wave: 7
depends_on: [177.1-06]
files_modified:
  - src/cli/commands/verify-derive-recheck.ts
  - src/cli/commands/verify-derive-recheck.test.ts
  - src/cli/slash-command/bs/verify/derive-recheck.md
  - src/cli/slash-command/bs/verify/derive-compare.md
  - src/cli/slash-command/bs/verify.test.ts
  - src/cli/commands/install-claude-command.ts
  - src/cli/commands/derived-line-pattern.ts
  - src/cli/commands/ingest-archive.ts
autonomous: true
requirements: [CHECK-04]

must_haves:
  truths:
    - "Exactly ONE live derived-line-check path exists — no flag, no shim, no second implementation"
    - "No source file, test, skill contract, or installer manifest references the retired design"
    - "A fresh install ships enumerate-facts.md and reconcile-facts.md and does not ship the retired contracts"
    - "The full test suite is green after the removal"
  artifacts:
    - path: "src/cli/commands/verify-derive-check.ts"
      provides: "The sole remaining derived-line-check implementation"
      contains: "verifyDeriveCheckCommand"
  key_links:
    - from: "src/cli/commands/install-claude-command.ts"
      to: "verify/enumerate-facts.md + verify/reconcile-facts.md"
      via: "skill install manifest"
      pattern: "enumerate-facts"
---

<objective>
DELETE the retired per-line blind re-derivation design — code, tests, contracts, installer entries,
and pins — leaving exactly ONE live path.

Purpose: ROADMAP criterion 3 and CONTEXT decision 5, under the project's No Backward Compatibility
rule: removed, not deprecated. Everything genuinely still needed was MOVED in plans 02-04, so this
is a pure removal; if it turns out not to be, that is a finding that plans 02-04 missed something.

RECONCILIATION TO RECORD: ROADMAP criterion 3 says remove "the `verify-derive-recheck`/
`verify-derive-record` commands." CONTEXT decision 1 (later, locked) keeps `verify-derive-record`
as the retargeted single write surface and renames only the read command. CONTEXT governs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177.1-wire-check04-closed-design/177.1-CONTEXT.md
@.planning/phases/177.1-wire-check04-closed-design/177.1-06-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
Code to delete (`src/cli/commands/verify-derive-recheck.ts`, whole file): `DERIVE_VERDICTS`,
`FACT_ALIGNMENTS`, `DeriveVerdictRecord`, `createDeriveVerdictRecord`, `focusQuoteWindow`,
`blindDeriveHandle`, `BLIND_DERIVE_TOKEN`, `buildBlindDerivePayload`, `derivePayloadSet`,
`DeriveRecheckFinding`, `formatReading`, `verifyDeriveRecheckCommand`, the old
`verifyDeriveRecordCommand`, the `rulebook/.derive-recheck/` ledger, and every
`sharedFocusWith`/`targetingAmbiguousCount` computation.

Tests to delete: `src/cli/commands/verify-derive-recheck.test.ts` (whole file). Its ledger,
`readLiveSlices`, `quoteLinesOnly`, `enumerateDerivedLines`, `annotationBody`, and
module-source-guarantee blocks were PORTED in plans 02 and 04.

Contracts to delete: `src/cli/slash-command/bs/verify/derive-recheck.md`,
`src/cli/slash-command/bs/verify/derive-compare.md`.

Pins to remove from `verify.test.ts`: the `ALL_VERIFY_FILES` entries at lines 53-54; the
`derive-recheck.md`/`derive-compare.md` drift tests around lines 690-760, including the
`--fact-alignment` pin at :706 and the installer leaf probes at :725-758.

Installer: `src/cli/commands/install-claude-command.ts`'s manifest entries for both retired files.

`verify-derive-recheck.ts` re-exports `DERIVED_LINE_RE` at :413 — confirm every importer now takes
it from `derived-line-pattern.js` directly. `derived-line-pattern.ts`'s header cites
`verify-derive-recheck.ts` in its import-cycle rationale; that rationale still holds and must be
REWRITTEN against the surviving modules, never deleted.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Port-coverage audit, then delete the retired code and contracts</name>
  <read_first>
    - src/cli/commands/verify-derive-recheck.ts (whole file — the last read before deletion)
    - src/cli/commands/verify-derive-recheck.test.ts (the describe-block inventory)
    - src/cli/commands/verify-derive-check.ts and src/cli/commands/verify-derive-check.test.ts (the ports)
    - src/cli/commands/derived-line-pattern.ts (its header cites the module being deleted)
    - src/cli/commands/install-claude-command.ts (the skill install manifest)
  </read_first>
  <action>
FIRST produce a port-coverage table in the SUMMARY: every exported symbol and every `describe`
block in the two files being deleted, marked PORTED (naming the new location), DELETED-BY-DESIGN
(naming the CONTEXT decision 5 bullet), or UNPORTED. Any UNPORTED row STOPS this task — port it or
escalate. Do not delete an unported proof.

Then delete `verify-derive-recheck.ts`, `verify-derive-recheck.test.ts`,
`verify/derive-recheck.md`, and `verify/derive-compare.md`, and remove both contracts from
`install-claude-command.ts`'s manifest.

Rewrite `derived-line-pattern.ts`'s header so the import-cycle rationale names the SURVIVING
consumers (`verify-enumerate.ts`, `verify-derive-check.ts`, `ingest-archive.ts`,
`verify-classify.ts`) and the real current cycle risk.

Sweep for orphan references across `src/`, `docs/`, and `README*`: `verify-derive-recheck`,
`derive-recheck`, `derive-compare`, `BS-DERIVE-V1`, `BS-DERIVE-COMPARE-V1`,
`buildBlindDerivePayload`, `blindDeriveHandle`, `focusQuoteWindow`, `factAlignment`,
`FACT_ALIGNMENTS`, `DERIVE_VERDICTS`, `.derive-recheck`. Fix each hit — update prose that describes
the design, delete prose that only existed for it. `.planning/` history is EXEMPT: it is the record
of what happened and must not be rewritten.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The four files no longer exist (`test ! -f` for each).
    - `grep -rn "verify-derive-recheck|BS-DERIVE-V1|BS-DERIVE-COMPARE-V1|buildBlindDerivePayload|blindDeriveHandle|focusQuoteWindow|FACT_ALIGNMENTS|DERIVE_VERDICTS|\.derive-recheck" src/ docs/` returns zero hits.
    - `npx tsc --noEmit` reports zero errors.
    - The SUMMARY contains the port-coverage table with zero UNPORTED rows.
    - `derived-line-pattern.ts`'s header names no deleted module.
  </acceptance_criteria>
  <done>The retired implementation and its contracts are gone from the tree, with every surviving proof accounted for.</done>
</task>

<task type="auto">
  <name>Task 2: Prune the retired pins and prove the install and the full suite</name>
  <read_first>
    - src/cli/slash-command/bs/verify.test.ts lines 40-60 and 680-800 (ALL_VERIFY_FILES and the retired drift tests / installer leaf probes)
    - src/cli/commands/install-claude-command.ts (as edited in Task 1)
    - .planning/phases/177.1-wire-check04-closed-design/177.1-05-SUMMARY.md (the pins added in plan 05, which must survive)
  </read_first>
  <action>
Remove `verify/derive-recheck.md` and `verify/derive-compare.md` from `ALL_VERIFY_FILES` and delete
every drift test and installer leaf probe scoped to them, so the suite never asserts against a file
that no longer exists.

REPLACE — do not merely delete — the installer leaf probes at lines 725-758 with equivalents aimed
at `verify/enumerate-facts.md` and `verify/reconcile-facts.md`: a real `installClaudeCommand`
install lands both files under `.claude/skills/bs-shared/verify/`, and deleting one of them flips a
non-force reinstall from "already installed" to repopulating the missing leaf. That probe is the
only executed proof the new contracts actually ship; losing it would make the whole rewiring
unverified at the install boundary.

Add an ABSENCE probe: after a real install, `.claude/skills/bs-shared/verify/derive-recheck.md` and
`derive-compare.md` do NOT exist.

Run the full suite. Any failure caused by the removal is fixed here; any failure that reveals an
unported behavior is recorded as a finding first, then fixed.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` is fully green with zero failures and zero skipped derive-related tests.
    - `verify.test.ts` contains installer leaf probes naming `enumerate-facts.md` and `reconcile-facts.md`, and an absence probe for the two retired contracts.
    - `grep -c "derive-recheck\|derive-compare" src/cli/slash-command/bs/verify.test.ts` returns 0 except inside the absence probe (which asserts non-existence) — state the exact count and its location in the SUMMARY.
    - `npx tsx src/cli/cli.ts verify-derive-check --help` still exits 0 and `verify-derive-recheck` is still an unknown command.
    - `npm run audit:dead-code` reports no newly-orphaned file or export introduced by this removal.
  </acceptance_criteria>
  <done>The suite is green, the new contracts are proven to install, and the retired ones are proven not to.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deleted module -> surviving importers | A missed re-export or import leaves a broken or silently-dual path |
| repo -> installed skills directory | The installer manifest decides what a designer's machine actually runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177.1-21 | Repudiation | silently lost proofs | mitigate | Mandatory port-coverage table; any UNPORTED row stops the task |
| T-177.1-22 | Tampering | stale installed skills | mitigate | Real-install leaf probes for both new contracts plus an absence probe for both retired ones |
| T-177.1-23 | Elevation of privilege | a second live path surviving | mitigate | Repo-wide orphan-reference grep with zero-hit acceptance criterion; `npm run audit:dead-code` clean |
| T-177.1-SC | Tampering | npm installs | mitigate | No packages installed by this plan; escalate if one becomes necessary |
</threat_model>

<verification>
- `npm test` green.
- `npx tsc --noEmit` clean.
- Repo-wide grep for retired identifiers returns zero hits outside `.planning/`.
</verification>

<success_criteria>
- ROADMAP criterion 3 satisfied: the retired design is REMOVED, not deprecated, and every genuinely-needed part was moved rather than left behind.
- Exactly one live derived-line-check path exists.
</success_criteria>

<output>
Create `.planning/phases/177.1-wire-check04-closed-design/177.1-07-SUMMARY.md` when done.
</output>
