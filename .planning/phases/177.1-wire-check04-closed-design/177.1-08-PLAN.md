---
phase: 177.1-wire-check04-closed-design
plan: 08
type: execute
wave: 8
depends_on: [177.1-07]
files_modified:
  - .planning/phases/177.1-wire-check04-closed-design/177.1-LIVE-PROOF/
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CHECK-04]

must_haves:
  truths:
    - "A real end-to-end run through the /bs-verify-game Step 7 path classifies a reference game's Derived lines"
    - "The live run's mechanical outputs match the pre-registered predictions exactly"
    - "Every live classification is a member of that line's pre-registered permitted label set"
    - "The run exits 0 and gates nothing"
    - "The reference game's working tree is byte-identical before and after"
  artifacts:
    - path: ".planning/phases/177.1-wire-check04-closed-design/177.1-LIVE-PROOF/PROOF.md"
      provides: "The end-to-end transcript evidence, split-bar evaluation, and independence grep"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CHECK-04 closing note recording that the closed design is now the one in the pipeline"
      contains: "177.1"
  key_links:
    - from: "installed .claude/skills/bs-verify-game/SKILL.md Step 7"
      to: "boardsmith verify-derive-check"
      via: "a real dispatch-and-format run"
      pattern: "verify-derive-check"
---

<objective>
Run the rewired Step 7 for real against a reference game, evaluate it against decision 11's SPLIT
bar, and record CHECK-04's closing note.

Purpose: ROADMAP criteria 4 and 5. The closure evidence for CHECK-04 stands and is NOT reopened —
this proves REACHABILITY, not judgment quality. The narrow claim: the product path reproduces what
the `.planning/` harness already produced for the same input.

THE BAR IS SPLIT (CONTEXT decision 11, pre-registered in plan 06):
- MECHANICAL — payload bytes, candidate selection, grounding validation results, arithmetic
  composition — must agree EXACTLY with the pre-registration. These are deterministic functions of
  the input.
- MODEL-DEPENDENT — per-line classification — must land WITHIN that line's pre-registered permitted
  label set. An exact-label bar is FORBIDDEN: it re-imports the retired determinism gate against a
  measured ~12.5% cross-run variance and is unsatisfiable by construction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177.1-wire-check04-closed-design/177.1-CONTEXT.md
@.planning/phases/177.1-wire-check04-closed-design/177.1-PRE-REGISTRATION.md
@.planning/phases/177.1-wire-check04-closed-design/177.1-07-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
The dispatch sequence Step 7 prescribes, and the models it pins (CONTEXT decision 4, exactly what
`177-22-MEASUREMENT/driver.mjs:17-19` used): enumerator A `claude-opus-5`, enumerator B
`claude-haiku-4-5-20251001`, reconciler `claude-sonnet-5`.

Dispatch is via the Task tool under the installed contracts — NOT via `claude -p`. The `claude -p`
shape in `177-22-MEASUREMENT/dispatch-enum.mjs` was the measurement driver's shape; the product's
is a subagent dispatch carrying the contract plus the payload, per CONTEXT decision 3.

Target: `~/BoardSmithGames/seven`, the plan-06 comparison target — 2 slices, 3 `Derived` lines
(L21, L36, L38), exercising grounding, both arithmetic composition kinds, and plain corroboration.

CHECK-04's `REQUIREMENTS.md` entry is at line 215; its `CLOSED 2026-07-31` note begins around line
446 and its "What this requirement now promises" line names "a tool that flags rule-bearing
inferences worth a human's attention." The summary table row is at line 531.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Real end-to-end run through the rewired Step 7</name>
  <read_first>
    - .planning/phases/177.1-wire-check04-closed-design/177.1-PRE-REGISTRATION.md (the committed bar — read BEFORE running, and do not modify it afterward)
    - src/cli/slash-command/bs/verify-game.md Step 7 and the Context-Economics carve-out (the prose being executed)
    - src/cli/slash-command/bs/verify/enumerate-facts.md and verify/reconcile-facts.md (the contracts to dispatch)
    - .planning/phases/177.1-wire-check04-closed-design/177.1-REPLAY-PROOF/diff-report.md (what the mechanical half already proved on recorded input)
  </read_first>
  <action>
Install the skills for real (`npx boardsmith claude`, local install into a scratch project or the
staged copy) so the run executes the INSTALLED prose, not the repo source — that is the only way
this proves what a designer reaches.

Stage per CONTEXT decision 13: `cp -R ~/BoardSmithGames/seven` into scratch, sha256-baseline the
ORIGINAL tree into `177.1-LIVE-PROOF/baseline-seven.sha256`, operate only on the copy.

Execute Step 7's sequence exactly as written, without shortcuts:
1. `boardsmith verify-derive-check --project <staged> --json` -> save `check-before.json`.
2. Per pending slice, dispatch the SAME `enumeratorPayload` bytes twice via the Task tool under
   `enumerate-facts.md`, once on `claude-opus-5` and once on `claude-haiku-4-5-20251001`. Save both
   raw returns.
3. Dispatch the reconciler on `claude-sonnet-5` under `reconcile-facts.md` with both returns and
   the slice's `derivedLines`. Save the raw return.
4. One `boardsmith verify-derive-record` invocation per slice.
5. `boardsmith verify-derive-check --project <staged> --json` -> save `check-after.json`, plus the
   human-readable form.

Save every payload, prompt, and return under `177.1-LIVE-PROOF/`.

Then run the INDEPENDENCE GREP the carve-out prescribes, as TWO SEPARATE observables — never one
blanket grep: (a) every dispatched enumerator prompt contains ZERO `Derived (p.`, ZERO `Visual (p.`,
and ZERO `Named-but-undefined (p.` lines, using the real production regexes imported from
`derived-line-pattern.ts` rather than a hand-typed pattern; (b) the reconciler prompt and return
ARE expected to contain `Derived` line text — record that separately as the expected case, never as
a violation.

Write `177.1-LIVE-PROOF/PROOF.md` evaluating BOTH halves of the bar:
- MECHANICAL: payload sha256s, candidate set, grounded/rejected counts, composed-fact ids — each
  PREDICTED vs ACTUAL with a MATCH/DIVERGENCE verdict against the pre-registration.
- MODEL-DEPENDENT: per line, the live label and the pre-registered permitted set, with an
  IN-SET/OUT-OF-SET verdict. Do NOT compare labels for exact equality anywhere in this document.
- Exit codes for every invocation.
- The two-observable independence result.

Finally re-hash the ORIGINAL `~/BoardSmithGames/seven` and prove it byte-identical to the baseline.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith &amp;&amp; shasum -a 256 -c .planning/phases/177.1-wire-check04-closed-design/177.1-LIVE-PROOF/baseline-seven.sha256</automated>
  </verify>
  <acceptance_criteria>
    - `177.1-LIVE-PROOF/` contains `check-before.json`, `check-after.json`, both enumerator returns and the reconciler return per slice, every dispatched prompt, and `PROOF.md`.
    - `PROOF.md` shows MATCH on every mechanical category against the pre-registration.
    - `PROOF.md` shows IN-SET for all three lines, and contains no exact-label equality assertion anywhere.
    - `check-after.json` has `pendingCount === 0` and three `findings` entries at lines 21, 36, 38.
    - Every CLI invocation exited 0, recorded in `PROOF.md` with the observed codes.
    - The independence grep is reported as two separate observables using the production regexes, with zero annotation matches in every enumerator prompt.
    - `shasum -a 256 -c 177.1-LIVE-PROOF/baseline-seven.sha256` reports OK for every entry.
    - `177.1-PRE-REGISTRATION.md` is unmodified by this task (`git diff -- .../177.1-PRE-REGISTRATION.md` is empty).
    - If any mechanical category DIVERGES or any line is OUT-OF-SET, `PROOF.md` reports it plainly as a failure with a root cause — never adjusted, never re-run until it passes.
  </acceptance_criteria>
  <done>A designer-reachable /bs-verify-game Step 7 run classified a real game's Derived lines, evaluated against a bar committed before the run.</done>
</task>

<task type="auto">
  <name>Task 2: Record CHECK-04's closing note and confirm the phase's advisory and open-item state</name>
  <read_first>
    - .planning/REQUIREMENTS.md lines 210-260 and 425-540 (CHECK-04's entry, its CLOSED 2026-07-31 note, and the summary-table row at 531)
    - .planning/phases/177.1-wire-check04-closed-design/177.1-LIVE-PROOF/PROOF.md
    - .planning/phases/177.1-wire-check04-closed-design/177.1-CONTEXT.md (decisions 9 and 14)
  </read_first>
  <action>
Append to CHECK-04's `REQUIREMENTS.md` entry a Phase 177.1 delivery note. It must state: the
closure itself is NOT reopened and its evidence is unchanged; what changed is REACHABILITY — the
dual-enumeration design now has a CLI surface (`verify-derive-check` + `verify-derive-record`) and
`/bs-verify-game` Step 7 dispatches `enumerate-facts.md`/`reconcile-facts.md`; the retired blind
per-line design is REMOVED; and the product path was proven against the harness on both recorded
input (plan 06 replay, mechanical exact) and a live run (plan 08, split bar). Cite
`177.1-PRE-REGISTRATION.md`, `177.1-REPLAY-PROOF/`, and `177.1-LIVE-PROOF/PROOF.md` by path.
Update the summary-table row at line 531 to point at this note.

Record WR-07 (`quoteLinesOnly` deny-list -> allow-list inversion) as STILL OPEN, dated 2026-07-30,
routed to Phase 178 per CONTEXT decision 9 — it must not silently vanish because this phase touched
neighbouring code.

Record in the SUMMARY every finding surfaced by plans 02-07 that the CONTEXT's `<specifics>` calls
for: behavior that only made sense for the retired verdict set (plan 02), the arithmetic-spec gap
between harness and product (plan 03), the `repairUnescapedQuotes` non-port (plan 03), and any
`PRESENTATION_EXCLUSION_MARKERS` divergence (plan 01).

Then confirm the phase's end state: full suite green, dead-code audit clean, and the advisory
guarantee intact.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith &amp;&amp; npm test &amp;&amp; npm run audit:dead-code</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "177.1" .planning/REQUIREMENTS.md` >= 3 (entry note, cited artifacts, summary row).
    - CHECK-04's entry explicitly states the closure is not reopened and that what changed is reachability.
    - WR-07 appears in `REQUIREMENTS.md` as open, dated 2026-07-30, routed to Phase 178.
    - `npm test` fully green.
    - `npm run audit:dead-code` reports no unused file or export introduced by this phase.
    - `npx tsx src/cli/cli.ts verify-derive-check --project <staged-seven>; echo $?` prints `0` even with `contradicted` records present (construct that state deliberately if the live run produced none).
    - No background process started by this plan is left running.
  </acceptance_criteria>
  <done>CHECK-04's entry records the delivery, WR-07 stays visibly open, and the phase closes with a green suite and an advisory check that gates nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installed skill prose -> live dispatch | The designer-facing path; the only place the rewiring is proven real |
| slice quote lines -> enumerator prompt | The independence guarantee, audited by the two-observable grep |
| staged copy -> original reference game | Any escaping write corrupts the comparison target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177.1-24 | Information disclosure | live enumerator prompts | mitigate | Two-observable independence grep using the production regexes against every saved prompt, zero-match criterion |
| T-177.1-25 | Tampering | post-hoc bar adjustment | mitigate | Pre-registration proven unmodified; divergence reported as failure, never re-run until green |
| T-177.1-26 | Denial of service | advisory check becoming a gate | mitigate | Explicit exit-0 check with a `contradicted` record deliberately present (CONTEXT decision 14) |
| T-177.1-27 | Repudiation | WR-07 silently dropped | mitigate | Acceptance criterion asserts it remains recorded open, dated, and routed |
| T-177.1-SC | Tampering | npm installs | mitigate | No packages installed by this plan; escalate if one becomes necessary |
</threat_model>

<verification>
- `PROOF.md` shows MATCH on every mechanical category and IN-SET on every line.
- `npm test` and `npm run audit:dead-code` green.
- `~/BoardSmithGames/seven` hashes clean against its baseline.
</verification>

<success_criteria>
- ROADMAP criterion 4 satisfied: a real end-to-end `/bs-verify-game`-path run on a reference game reproduces what the harness produced for the same input, under the split bar.
- ROADMAP criterion 5 satisfied: advisory, exit 0, gates nothing — pinned by an executed check.
- CHECK-04's entry records the delivery without reopening its closure; WR-07 stays open for Phase 178.
</success_criteria>

<output>
Create `.planning/phases/177.1-wire-check04-closed-design/177.1-08-SUMMARY.md` when done.
</output>
