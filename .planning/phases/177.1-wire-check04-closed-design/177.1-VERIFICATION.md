---
phase: 177.1-wire-check04-closed-design
verified: 2026-07-31T18:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
---

# Phase 177.1: Wire CHECK-04's Closed Design Into The Pipeline — Verification Report

**Phase Goal:** A designer running `/bs-verify-game` actually reaches the derived-line check
CHECK-04 was closed on (dual enumeration + reconciliation), instead of the per-line blind design
Phase 177 retired.
**Verified:** 2026-07-31
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement — Reachability Trace (independently re-run, not summary-trusted)

1. `npx boardsmith verify-derive-check --help` — **responds live**, prints the real description
   ("Enumerate every rule-bearing Derived line project-wide... read-only, project-level,
   source-free, machine-readable, exits 0 unconditionally").
2. `grep '.command(' src/cli/cli.ts` — `verify-derive-check` and `verify-derive-record` both
   registered (`src/cli/cli.ts:430,442`), imported from `./commands/verify-derive-check.js`
   (both commands folded into one module, a discretion CONTEXT explicitly allowed).
3. `verify-game.md` Step 7 (line 170) dispatches `enumerate-facts.md`'s `BS-ENUMERATE-V1` and
   `reconcile-facts.md`'s `BS-RECONCILE-V1`, records through `verify-derive-record`, formats
   `verify-derive-check --json` — confirmed by direct read of lines 170-238, not summary text.
4. The dual-enumeration library (`verify-enumerate.ts`) is called from `verify-derive-check.ts`
   which is registered in `cli.ts` — the chain is unbroken end to end.

**No break found anywhere in the chain. Goal reached.**

## Observable Truths / ROADMAP Success Criteria

| # | Truth (ROADMAP SC) | Status | Evidence |
|---|---|---|---|
| 1 | CLI surface exposes dual-enumeration end-to-end, read/write pair carrying forward fence-injection rejection, read-path revalidation, upsert-append | ✓ VERIFIED | `verify-derive-check`/`verify-derive-record` registered `src/cli/cli.ts:430,442`; `--help` output confirmed live; ledger machinery moved per `177.1-02-SUMMARY.md`, CR-04/CR-02/CR-06 explicitly carried forward |
| 2 | `/bs-verify-game` Step 7 dispatches `enumerate-facts.md`/`reconcile-facts.md`, formats (never computes) `--json` | ✓ VERIFIED | `verify-game.md:170-238` read directly; "formatted, never computed" discipline intact — Step 7 only formats CLI JSON output |
| 3 | Retired design REMOVED (not deprecated) — files, command, blind-payload machinery gone, exactly one live path | ✓ VERIFIED | `verify-derive-recheck.ts`, `derive-recheck.md`, `derive-compare.md` confirmed absent from disk (`ls` → No such file). `buildBlindDerivePayload`/`focusQuoteWindow`/`blindDeriveHandle`/`factAlignment`(as a field)/four-verdict-enum/`BS-DERIVE-V1`/`BS-DERIVE-COMPARE-V1` grep hits are exclusively absence-assertion tests (`cli.test.ts`, `verify.test.ts`) or prose explaining why the NEW contract has its own distinct token — no orphan implementation found |
| 4 | Proven by real end-to-end run reproducing the `.planning/` harness's classification for the same input | ✓ VERIFIED | `177.1-REPLAY-PROOF/diff-report.md`: 7/7 mechanical categories MATCH against `177-22` run1, replayed through the real product CLI (not simulated). `177.1-LIVE-PROOF/PROOF.md`: real `claude -p` dispatches to the 3 pinned models, all 3 live classifications land IN-SET against pre-registered permitted sets, live reconciler independently produced a well-formed `arithmeticSpec` for both a `single` and `chain` claim, accepted correct by code |
| 5 | Advisory, exit 0, never gates Close | ✓ VERIFIED | `177.1-08-SUMMARY.md`: hand-constructed `contradicted` ledger record, re-ran `verify-derive-check --json` — exit 0 confirmed, `verdictCounts.contradicted: 1` present, non-fatal |

**Score:** 5/5 truths verified.

## Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/cli/commands/verify-derive-check.ts` | CLI command module, both commands | ✓ VERIFIED | exists, exports `verifyDeriveCheckCommand`/`verifyDeriveRecordCommand`-equivalent logic, wired |
| `src/cli/commands/verify-enumerate.ts` | dual-enumeration library, now called | ✓ VERIFIED | imported by `verify-derive-check.ts`; no longer orphaned (previously imported nowhere per CONTEXT measured reality #2) |
| `src/cli/commands/derived-line-pattern.ts` | single consolidated `ANNOTATION_VOCABULARY_RE`/`ANNOTATION_CITATION_RE` export | ✓ VERIFIED | single definition, imported by `verify-enumerate.ts`; prior 5-site duplication (CONTEXT measured reality #6) now reduced to 1 definition + imports |
| `src/cli/slash-command/bs/verify-game.md` | Step 7 + carve-out + Reference Files rewritten | ✓ VERIFIED | lines 49-69, 170-238 all reference the new contracts; zero retired identifiers after comment-stripping (enforced by `verify.test.ts` absence gate) |
| `src/cli/slash-command/bs/verify/enumerate-facts.md`, `reconcile-facts.md` | installed, token-pinned contracts | ✓ VERIFIED | present, confirmed byte-identical to installed copy in `177.1-08-PROOF.md` |
| `verify-derive-recheck.ts`, `verify/derive-recheck.md`, `verify/derive-compare.md` | must NOT exist | ✓ VERIFIED (absence) | `ls` confirms all three absent from disk |

## Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `cli.ts` | `verify-derive-check.ts` | `.command().action()` registration | WIRED | both commands registered, `--help` responds live |
| `verify-game.md` Step 7 | `enumerate-facts.md`/`reconcile-facts.md` | dispatch by reference | WIRED | Step 7 text names both contracts and their tokens |
| `verify-derive-check.ts` | `verify-enumerate.ts` | function import (`buildEnumeratorPayload`, `classifyDerivedLines`, etc.) | WIRED | confirmed no longer an orphan library |
| `verify-derive-record` | ledger (`rulebook/.derive-check/verdicts.md`) | atomic upsert-append | WIRED | `177.1-08` live proof wrote and re-read this ledger through the CLI |

## Pre-Registration Integrity (independently checked via git, not trusted from prose)

- `git log --oneline --follow -- .../177.1-PRE-REGISTRATION.md` → single commit `be9ce540`.
- `git show --stat be9ce540` → single-file commit, 236 insertions, committed BEFORE the replay
  (`177.1-06` Task 2) and live proof (`177.1-08`).
- `git diff be9ce540 HEAD -- .../177.1-PRE-REGISTRATION.md` → **empty** — file never amended after
  commit.
- **VERIFIED**: committed alone, before dispatch, never amended.

## Satisfiability Discipline (D-11)

- `verify-derive-check.test.ts` contains no exact-label assertion against live model output.
- `177.1-08-PLAN.md` (lines 46, 123-124) explicitly forbids exact-label equality and mandates
  IN-SET/OUT-OF-SET permitted-set membership language.
- `177.1-LIVE-PROOF/PROOF.md`'s "MODEL-DEPENDENT HALF" table uses IN-SET verdicts only; explicit
  statement "No exact-label equality assertion is made anywhere in this document."
- **VERIFIED**: the constraint the phase was designed around is intact and independently confirmed
  in the actual proof artifact.

## Honest Reporting — Four Disclosed Findings

| Finding | Durable artifact | Status |
|---|---|---|
| `PRESENTATION_EXCLUSION_MARKERS` not derivable | `177.1-01-PLAN.md`/`177.1-01-SUMMARY.md`, `177.1-04-PLAN.md`, `177.1-08-PLAN.md`/`SUMMARY.md` | ✓ recorded, not just transcript |
| Retired blind-reading fields (`rederivedValue`/`originalReading`/`rederivedReading`/`factAlignment`) dropped, not adapted | `177.1-02-SUMMARY.md` "Findings" section + module header comment in `verify-derive-check.ts` | ✓ recorded in code AND summary |
| `arithmeticSpec` hand-amended fixtures | `177.1-03-SUMMARY.md` Findings 1-2 | ✓ recorded, carried into `177.1-06`/`177.1-08` pre-registration disclosures |
| `validateGrounding` tie-break | `177.1-03-SUMMARY.md` Finding 2; re-observed in `177.1-06` replay; live-only non-reproduction reported honestly in `177.1-08` `PROOF.md` | ✓ recorded across three artifacts, including the live run's honest non-reproduction |

WR-07 confirmed **visibly OPEN** in `.planning/REQUIREMENTS.md` (line 508, delivery note) and
routed to Phase 178, dated 2026-07-30 as CONTEXT decision 9 requires.

## Suite State

`npx vitest run` (re-run independently): **244 test files passed, 4119 tests passed**, zero
failures. Duration ~20s.

## Anti-Patterns Found

None blocking. Scanned all phase-touched files (`verify-derive-check.ts`, `verify-enumerate.ts`,
`derived-line-pattern.ts`, `verify-game.md`, `enumerate-facts.md`, `reconcile-facts.md`) for
TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER/"not yet implemented". One `PLACEHOLDER`-named regex constant
found (`DERIVED_CITATION_LEGEND_PLACEHOLDER_RE`) — this is a descriptive identifier name for a
legend-line-matching regex, not a debt marker; not flagged.

## Requirements Coverage

CHECK-04 (delivery gap in an already-closed requirement): SATISFIED. `.planning/REQUIREMENTS.md`
line 584 and the inline delivery note (lines ~474-510) both record the Phase 177.1 delivery
directly and accurately, matching the codebase evidence gathered independently above.

## Deviation Disclosed (not a gap, but worth surfacing)

`177.1-08`'s live proof used `claude -p --model <pinned-id>` rather than the Task tool, because
the executing agent session had no Task-tool subagent dispatch available. This is a genuine,
explicitly disclosed gap between the proof and what a designer's live Claude Code session does
when `/bs-verify-game` dispatches through the Task tool (subagent sandboxing/context-isolation
mechanics are not reproduced). The contract token handshake, pinned model ids, and payload bytes
are otherwise identical. This is a reasonable proxy and was disclosed plainly rather than silently
substituted — noted here for visibility, not scored as a gap, since the CLI/skill wiring itself
(the actual phase goal) does not depend on which dispatch mechanism a proof harness used.

## Human Verification Required

None. All must-haves were verifiable via direct grep/read/git/test-run evidence; the phase's own
live-dispatch proof already exercised the one component (real model calls) that would otherwise
require human judgment, and that proof's raw evidence files were inspected directly above rather
than trusted from SUMMARY prose.

## Gaps Summary

No gaps found. All 5 ROADMAP success criteria verified independently against the live codebase:
CLI responds, Step 7 dispatches the correct contracts, the retired design is fully absent (not
orphaned), the product path was proven both by recorded-input replay and a real live dispatch, and
the advisory/non-gating property was proven with a deliberately constructed failing record. The
pre-registration's git history confirms it was committed alone and never amended. The
satisfiability discipline (D-11) that the whole phase was designed around is intact in the actual
proof artifact, not just asserted. All four disclosed findings are durably recorded outside of
conversation transcripts. WR-07 remains visibly open and routed to Phase 178. Test suite is fully
green (244/244 files, 4119/4119 tests).

---

_Verified: 2026-07-31_
_Verifier: Claude (gsd-verifier)_
