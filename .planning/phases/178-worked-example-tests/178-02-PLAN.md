---
phase: 178-worked-example-tests
plan: 02
type: execute
wave: 2
depends_on: [178-01]
files_modified:
  - src/cli/commands/example-derivation.ts
  - src/cli/commands/example-derivation.test.ts
autonomous: true
requirements: [CHECK-06, TEST-01]

must_haves:
  truths:
    - "A worked example is represented by a structured spec, never by test code"
    - "Every spec is keyed by an id the CALLER assigns, never by text the model returned"
    - "Two model returns that phrase themselves identically fail closed instead of collapsing onto one entry"
    - "The extraction payload carries the Visual line that records seven's example contradiction"
  artifacts:
    - path: "src/cli/commands/example-derivation.ts"
      provides: "WorkedExample spec type, caller-assigned ids, the extraction and translation payload builders — the ONE module both build and verify call (SC-3)"
      exports: ["WORKED_EXAMPLE_KINDS", "createWorkedExampleSpec", "buildExampleExtractionPayload", "buildExampleTranslationPayload", "workedExampleId"]
    - path: "src/cli/commands/example-derivation.test.ts"
      provides: "Collision fail-closed test, payload-composition tests against real reference-game slice text"
  key_links:
    - from: "src/cli/commands/example-derivation.ts"
      to: "src/cli/commands/verify-derive-check.ts"
      via: "annotationBody / EXAMPLE_LINE_RE / VISUAL_LINE_RE import (no re-spelled regex)"
      pattern: "from './verify-derive-check.js'"
---

<objective>
Build `example-derivation.ts`: the single shared module that turns a slice into an extraction
dispatch payload, validates the `WorkedExample` spec a model returns, and turns a spec into a
translation dispatch payload. Both the build side (TEST-01) and the verify side (CHECK-06) call
THIS module — that shared call is what ROADMAP success criterion 3 requires.

Purpose: 177.1's code review found three criticals sharing one root — lookups keyed by
model-supplied free text. This module is where that hazard would re-enter this phase, so identity
is caller-assigned from the first line of code, not retrofitted.
Output: `example-derivation.ts` + its tests. No CLI, no ledger, no I/O beyond what it is handed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-worked-example-tests/178-CONTEXT.md
@.planning/phases/178-worked-example-tests/178-01-SUMMARY.md
@./CLAUDE.md

<interfaces>
From src/cli/commands/verify-derive-check.ts (available after plan 01):
  export function annotationBody(line: string): string
  export function quoteLinesOnly(sliceText: string): string[]
  export const EXAMPLE_LINE_RE: RegExp        // added by plan 01
  export const VISUAL_LINE_RE: RegExp         // exported by plan 01
  export function parseSubagentJsonInput(text: string, flagLabel: string, filePath: string): unknown

From src/cli/commands/derived-line-pattern.ts:
  export const ANNOTATION_CITATION_RE: RegExp
  export const ANNOTATION_VOCABULARY_RE: RegExp

The fence-injection rejection and choke-point-constructor shape to copy verbatim in structure
(NOT to import): src/cli/commands/verify-derive-check.ts `createDeriveCheckRecord` (line ~390).
</interfaces>

<corpus_reality>
The real fixtures this module must handle, read at plan time:

- `one-two-punch/rulebook/02-action-cards-and-resolution.md:84-91` — `## Punch Examples`, two
  `p.2, Punch Examples (italic):` citation headers, each a quoted sentence
  ("If you are punched and have one ready and two exhausted Guards, you would lose one exhausted
  Guard.") followed by a `Visual (p.2):` diagram line describing the before/after Guard states.
  These are `transition` kind.
- `seven/rulebook/01-definitions-and-components.md:6,12` — `"example: 5, 5, 5"` and
  `"example: 5, 6, 7"` inside quote lines, each followed by a `Visual (p.1):` line. Line 13's
  Visual line reads: "...a red 1, a blue 2, and a red 3 (the printed example text reads 5, 6, 7
  while the accompanying card images show 1, 2, 3)". These are `predicate` kind, and the second is
  the `example-inconsistent` fixture — the contradiction is visible ONLY because the Visual line is
  in the payload.
- `doom-machine/rulebook/01-destroying-a-machine-part.md:13` — a
  `Worked example content (p.1, panel -7-, verbatim from card art):` header over a quoted card-art
  string; `doom-machine/rulebook/02-machine-phase.md:15` — present only as a
  `Diagram description (p.2, panel -8-, the EXAMPLE image):` line.

Consequence, load-bearing: the extraction payload MUST retain `Visual (p.N):` lines and
`Diagram description (...)` lines even though CHECK-04's `quoteLinesOnly` strips them. This is
option B from `178-WR07-DECISION.md` in practice — CHECK-06 gets its OWN inclusion logic.
</corpus_reality>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WorkedExample spec, caller-assigned identity, and the fail-closed choke point</name>
  <read_first>
    - src/cli/commands/verify-derive-check.ts lines 290-480 (DERIVE_CHECK_VERDICTS frozen enum, createDeriveCheckRecord's validation shape, fence-injection rejection)
    - .planning/phases/177.1-wire-check04-closed-design/177.1-REVIEW.md CR-01 and CR-02 (the text-keyed lookup hazard, verbatim)
    - .planning/phases/178-worked-example-tests/178-CONTEXT.md decisions 3, 5, 7 and addendum §5
  </read_first>
  <behavior>
    - `workedExampleId({ slicePath, lineNumber })` returns a stable, deterministic id derived ONLY from caller-known inputs; it never reads any model-returned field.
    - `createWorkedExampleSpec` throws when: `kind` is outside `WORKED_EXAMPLE_KINDS`; any free-prose field is empty; any free-prose field contains a ledger fence marker; `sourceText` is not a verbatim substring of the slice text the caller supplies.
    - Registering two specs that resolve to the SAME id throws with a message naming both `sourcePreview` values — never last-write-wins, never silent merge.
    - Two specs whose model-authored `sourceText`/`setup`/`expected` are byte-identical but whose caller-assigned ids differ are BOTH retained as distinct entries.
    - A spec may be constructed with `kind: 'predicate'` and no action, and with `kind: 'transition'` and an action; the inverse combinations throw.
  </behavior>
  <action>
    Create `src/cli/commands/example-derivation.ts`. Define
    `WORKED_EXAMPLE_KINDS = Object.freeze(['transition','predicate'] as const)` with the
    exhaustiveness-check pattern `verify-derive-check.ts` uses for `DERIVE_CHECK_VERDICTS`.
    Define `interface WorkedExampleSpec` with: `id`, `slicePath`, `lineNumber`, `pageCitation`,
    `kind`, `sourceText` (verbatim), `setup`, `action` (transition only), `expected`, and
    `supportingQuoteLines: string[]` (the verbatim slice lines the extractor cited — plan 04's
    provenance gate consumes these).
    `workedExampleId` composes `slicePath` + `lineNumber` only. `createWorkedExampleSpec` is the ONE
    choke point: it takes `{ id, sliceText, returned }` where `id` is caller-assigned and `returned`
    is the model's raw object, validates every field, and returns a frozen spec. Add
    `collectWorkedExampleSpecs(specs)` which builds a `Map<string, WorkedExampleSpec>` keyed by `id`
    and THROWS on a duplicate id rather than overwriting — the direct inheritance of CR-01/CR-02's
    fix. Reuse `verify-derive-check.ts`'s fence-marker constants for injection rejection; do not
    re-spell them.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/example-derivation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/cli/commands/example-derivation.test.ts` exits 0 with at least 8 assertions across the five behaviors above.
    - A test named to reference "collision" constructs two specs with byte-identical `sourceText` and identical `slicePath`+`lineNumber` and asserts `collectWorkedExampleSpecs` THROWS.
    - A test constructs two specs with byte-identical `sourceText` but different `lineNumber` and asserts both survive in the map with distinct keys.
    - `grep -n "returned\.\(sourceText\|setup\|expected\)" src/cli/commands/example-derivation.ts` shows no occurrence used as a Map key or `.find()` predicate.
    - `createWorkedExampleSpec` rejects a `sourceText` that does not appear verbatim in the supplied `sliceText`, with a message quoting the offending text.
  </acceptance_criteria>
  <done>A worked example has caller-assigned identity, a validated shape, and a fail-closed collision path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildExampleExtractionPayload — CHECK-06's own inclusion logic</name>
  <read_first>
    - src/cli/commands/verify-enumerate.ts lines 100-200 (buildEnumeratorPayload + construction-site backstop — the shape to mirror, with INVERTED inclusion)
    - src/cli/commands/verify-derive-check.ts lines 140-215 (annotationBody, isQuoteLine, quoteLinesOnly)
    - .planning/phases/178-worked-example-tests/178-WR07-DECISION.md
  </read_first>
  <behavior>
    - Given `seven`'s `01-definitions-and-components.md` text, the payload contains BOTH `"example: 5, 6, 7"` AND the `Visual (p.1):` line that records the 1/2/3 card-image discrepancy.
    - Given `one-two-punch`'s `02-action-cards-and-resolution.md` text, the payload contains both `p.2, Punch Examples (italic):` citation headers, both quoted sentences, and both `Visual (p.2):` diagram lines.
    - The payload NEVER contains a `Derived (p.N):` line — a `Derived` line is an ingest-time inference, and letting an extractor read one lets it launder an inference into a "worked example". Assert this as a construction-site backstop that THROWS, not a filter that silently drops.
    - The payload preserves each retained line's 1-based line number so the caller can assign ids without the model supplying them.
  </behavior>
  <action>
    Add `buildExampleExtractionPayload(slice: { path: string; text: string })` returning
    `{ slicePath, lines: Array<{ lineNumber: number; text: string }>, payload: string }`.
    Inclusion is an ALLOW-list, defined positively and commented as such: quoted prose lines,
    `p.N, <label>:` citation headers, `Example (p.N):` lines, `Visual (p.N):` lines, and
    `Diagram description (` / `Worked example content (` headers (the doom-machine forms measured in
    `<corpus_reality>`). Everything else is excluded. Route every prefix test through
    `annotationBody` — never a second decoration-normalization implementation.
    Add the construction-site backstop after assembly: if the assembled payload string matches
    `DERIVED_LINE_RE` at any line start, THROW naming the slice and line, independent of which
    inclusion rule let it through. Write the doom-machine header forms as named constants with the
    real slice paths in a comment, so a future reader can find the fixture they came from.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/example-derivation.test.ts -t extraction</automated>
  </verify>
  <acceptance_criteria>
    - Tests use verbatim slice text copied from the three real reference-game slices named in `<corpus_reality>` (committed as fixtures under `src/cli/commands/__fixtures__/` or inline), not invented prose.
    - A test asserts the `seven` payload contains the substring `the accompanying card images show 1, 2, 3`.
    - A test asserts the `one-two-punch` payload contains both `one ready and two exhausted Guards` and `three ready and no exhausted Guards`.
    - A test asserts a slice text containing a `Derived (p.2):` line makes `buildExampleExtractionPayload` THROW, and the error message names the line number.
    - Every returned line carries a `lineNumber` matching the 1-based position in the input text (asserted for at least three lines).
  </acceptance_criteria>
  <done>The extraction payload carries exactly the evidence an extractor needs to find an example AND to notice it contradicts its own art, and structurally cannot carry a Derived inference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: buildExampleTranslationPayload — the kind-branched second dispatch</name>
  <read_first>
    - .planning/phases/178-worked-example-tests/178-CONTEXT.md decisions 6, 7 (two dispatches, unexecutable with a named reason)
    - .planning/phases/178-worked-example-tests/178-RESEARCH.md Pitfall 4 and Open Question 2
    - ~/BoardSmithGames/seven/src/rules/scoring.ts (exports: `ScoringPattern` interface with a `check` member, `RUN_OF_SEVEN_PATTERN`, `COMBO_SETS_AND_RUNS_PATTERN`, `SET_5_PLUS_SET_2_PATTERN`, `legalScoringPatterns`, `numberCardsOf`)
    - ~/BoardSmithGames/one-two-punch/src/rules/index.ts (the re-export surface: `OneTwoPunchGame`, `guards.js`, `punch.js`, `resolution.js`)
  </read_first>
  <behavior>
    - The translation payload for a `predicate` spec names the game project's exported symbol surface and states that a direct function call is a legitimate target — not only `game.doAction(...)`.
    - The translation payload for a `transition` spec names the action-execution target shape.
    - The payload NEVER contains any part of the generated game's existing test files — a translator that can see an existing test writes agreement with it, not a test of the example (the decision 6 rationale).
    - Given a spec whose `kind` has no viable target in the supplied symbol surface, the payload still dispatches; deciding `unexecutable` is the MODEL's verdict with a named reason, never a payload-builder shortcut.
  </behavior>
  <action>
    Add `buildExampleTranslationPayload(spec: WorkedExampleSpec, api: GameApiSurface)` where
    `GameApiSurface` is `{ projectDir, testDir, exportedSymbols: Array<{ name: string; kind: 'function'|'const'|'class'; module: string }> }`.
    Add `collectGameApiSurface(projectDir)` that produces that surface MECHANICALLY by scanning the
    generated project's `src/rules/index.ts` re-export chain for `export function` / `export const` /
    `export class` declarations — a listing, not an inference. Document in its comment exactly what it
    cannot do (it does not resolve `export *` transitively beyond one level, and it does not know
    whether a symbol is pure) so a caller never over-trusts it.
    Record, in this module's header comment, the plan-time finding that resolves 178-RESEARCH Open
    Question 2: `seven` DOES export callable predicate surface, but not as free `isSet`/`isRun`
    functions — it exposes `ScoringPattern.check` members on exported pattern constants
    (`RUN_OF_SEVEN_PATTERN`, `COMBO_SETS_AND_RUNS_PATTERN`, `SET_5_PLUS_SET_2_PATTERN`) plus
    `legalScoringPatterns(scoredCards)` and `numberCardsOf(cards)`, and these take `SevenCard[]`
    element objects rather than raw numbers. Verify that claim against the real file before writing
    it down; if it has changed, record what is actually there. A `predicate` example therefore
    translates to constructing real cards and calling `check`, and the translation contract (plan 06)
    must say so.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/example-derivation.test.ts -t translation</automated>
  </verify>
  <acceptance_criteria>
    - `collectGameApiSurface('~/BoardSmithGames/seven')` (expanded to a real absolute path in the test) returns a surface including `legalScoringPatterns` and `numberCardsOf`; the test asserts both by name.
    - A test asserts the payload for a `predicate` spec contains the string `predicate` and at least one symbol name from the supplied surface.
    - A test asserts the payload for any spec contains ZERO substrings read from the project's `tests/` directory — implemented by asserting `collectGameApiSurface` never reads a path under `testDir` (assert via a filesystem-read spy or by asserting the returned `exportedSymbols[].module` values all resolve under `src/`).
    - The module header comment names the three `seven` pattern constants and `SevenCard[]`, and states it was verified against `~/BoardSmithGames/seven/src/rules/scoring.ts`.
  </acceptance_criteria>
  <done>Both dispatch payloads exist in one module, kind-branched, with the generated project's real API surface supplied mechanically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model JSON return → spec construction | Untrusted, model-authored free text crosses into a validated record |
| generated game source tree → translation payload | Project files are read and embedded into a dispatch prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-03 | Spoofing | `collectWorkedExampleSpecs` id map | mitigate | Ids are caller-assigned from `slicePath`+`lineNumber`; duplicate id throws (inherits 177.1 CR-01/CR-02 fix) |
| T-178-04 | Tampering | `createWorkedExampleSpec` free-prose fields | mitigate | Fence-marker rejection reusing `verify-derive-check.ts`'s constants; `sourceText` must be a verbatim substring of the supplied slice |
| T-178-05 | Information disclosure | `collectGameApiSurface` | mitigate | Reads only under the project's `src/`; test asserts no `tests/` read |
| T-178-SC | Tampering | npm/pip/cargo installs | mitigate | No installs; any addition requires a blocking human checkpoint |
</threat_model>

<verification>
- `npm test` green.
- `example-derivation.ts` imports `annotationBody`, `EXAMPLE_LINE_RE`, `VISUAL_LINE_RE`, `DERIVED_LINE_RE` rather than re-spelling any of them (`grep -c "new RegExp\|/\^Derived\|/\^Visual" src/cli/commands/example-derivation.ts` shows no annotation-family literal).
</verification>

<success_criteria>
- One shared derivation module exists with both payload builders and the spec choke point.
- Identity is caller-assigned; collision fails closed with a test proving it.
- The extraction payload provably carries seven's contradiction evidence and one-two-punch's two transitions.
</success_criteria>

<output>
Create `.planning/phases/178-worked-example-tests/178-02-SUMMARY.md` when done.
</output>
