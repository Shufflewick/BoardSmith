---
phase: 178-worked-example-tests
plan: 03
type: execute
wave: 3
depends_on: [178-02]
files_modified:
  - src/cli/commands/verify-example-replay.ts
  - src/cli/commands/verify-example-replay.test.ts
autonomous: true
requirements: [CHECK-06, TEST-01]

must_haves:
  truths:
    - "Every worked-example verdict is one of a frozen, test-pinned enumerated set"
    - "`unexecutable` and `example-inconsistent` are first-class verdicts that require a named reason"
    - "Recording a verdict for one example never destroys a verdict already recorded for another"
    - "Reading the ledger back re-validates every record through the same choke point that wrote it"
  artifacts:
    - path: "src/cli/commands/verify-example-replay.ts"
      provides: "The frozen verdict enum, the record choke point, the atomic upsert-append ledger triad, and the read/report command"
      exports: ["EXAMPLE_REPLAY_VERDICTS", "createExampleReplayRecord", "recordExampleReplayVerdicts", "readExampleReplayVerdicts", "replaceExampleReplayVerdicts", "exampleReplayLedgerPath", "verifyExampleReplayCommand"]
  key_links:
    - from: "src/cli/commands/verify-example-replay.ts"
      to: "src/cli/commands/verify-run.ts"
      via: "atomicWriteFile — the repo's ONE atomic write path"
      pattern: "atomicWriteFile"
    - from: "src/cli/commands/verify-example-replay.ts"
      to: "src/cli/commands/example-derivation.ts"
      via: "buildExampleExtractionPayload for pending slices"
      pattern: "buildExampleExtractionPayload"
---

<objective>
Build the CHECK-06 ledger and its read/report command, mirroring the LIVE CHECK-04 pairing
(`verify-derive-check.ts`) in structure — frozen verdict enum, one choke-point constructor, an
atomic upsert-append triad, and a read-only `--json` command that exits 0 unconditionally.

Purpose: the four guarantees 177.1's code review verified intact (atomic upsert-append CR-06,
fence-injection rejection CR-04, read-path revalidation through one choke point CR-02, evidence
requirement WR-05) are the reason that pairing is worth copying. Copy the STRUCTURE; do not import
CHECK-04's blind-derivation judgment machinery, which solves a different problem.
Output: `verify-example-replay.ts` with the ledger + read command. The WRITE command is plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-worked-example-tests/178-CONTEXT.md
@.planning/phases/178-worked-example-tests/178-02-SUMMARY.md
@./CLAUDE.md

<interfaces>
The structural model to mirror — src/cli/commands/verify-derive-check.ts:
  export const DERIVE_CHECK_VERDICTS = Object.freeze([...] as const)        // :299
  export const DERIVE_CHECK_LEDGER_BEGIN / _END = '<!-- boardsmith:...:begin/end -->'  // :333
  export function createDeriveCheckRecord(input): DeriveCheckRecord         // :390 — the ONE choke point
  export function deriveCheckLedgerPath(projectDir): string                 // :481
  function deriveCheckKey(r): string                                        // :485 — upsert key
  export async function replaceDeriveCheckVerdicts(...)                     // :508
  export async function recordDeriveCheckVerdicts(...)                      // :539 — read, filter by key, append, replace
  export async function readDeriveCheckVerdicts(projectDir)                 // :563 — re-enters createDeriveCheckRecord
  export async function verifyDeriveCheckCommand(options)                   // :699 — read/report, never sets process.exitCode
  export async function readLiveSlices(projectDir)                          // :98

From src/cli/commands/example-derivation.ts (plan 02):
  buildExampleExtractionPayload, workedExampleId, WorkedExampleSpec

From src/cli/commands/verify-run.ts:
  atomicWriteFile — the ONE atomic write path in this repo (173-REVIEW CR-01)

From src/cli/commands/chunk-provenance.ts:
  resolveCitedSlices — resolves a chunk's cited slices from its CHUNK.md `## Verified Against` block
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Frozen verdict set and the record choke point</name>
  <read_first>
    - src/cli/commands/verify-derive-check.ts lines 290-480 (verdict enum, exhaustiveness guard, createDeriveCheckRecord)
    - .planning/phases/178-worked-example-tests/178-CONTEXT.md decisions 4, 5, 7
    - src/cli/commands/example-derivation.ts (plan 02's WorkedExampleSpec)
  </read_first>
  <behavior>
    - `EXAMPLE_REPLAY_VERDICTS` is exactly `['agrees','disagrees','example-inconsistent','unexecutable']`, frozen, with a compile-time exhaustiveness guard mirroring `_deriveCheckVerdictsExhaustive`.
    - `createExampleReplayRecord` throws on a verdict outside the set, naming the set in the message.
    - `unexecutable` requires a non-empty `reason` — a record without one throws.
    - `example-inconsistent` requires BOTH contradicting verbatim excerpts (`contradictionA`, `contradictionB`) — a record with only one throws.
    - `disagrees` requires the expected outcome and the observed outcome, both verbatim.
    - Any free-prose field containing `<!-- boardsmith:` throws (fence injection).
    - The record's key is `exampleId` — a caller-assigned value — and `createExampleReplayRecord` throws if `exampleId` does not equal `workedExampleId({ slicePath, lineNumber })` for the record's own fields.
  </behavior>
  <action>
    Create `src/cli/commands/verify-example-replay.ts`. Define `EXAMPLE_REPLAY_VERDICTS`,
    `ExampleReplayVerdict`, `ExampleReplayRecord` (fields: `exampleId`, `slicePath`, `lineNumber`,
    `kind`, `verdict`, `reason`, `expected`, `observed`, `contradictionA`, `contradictionB`,
    `supportingQuoteLines`, `provenance` — a discriminated `'quote-verified' | 'quote-unverified'`
    value plan 04 sets, `recordedAt`, `testFilePath` when one was generated), and
    `createExampleReplayRecord` as the single validating constructor. Mirror
    `createDeriveCheckRecord`'s per-verdict required-field branching exactly in shape.
    Do NOT import `buildBlindDerivePayload`, `focusQuoteWindow`, or any blind-derivation function —
    those solve a different problem (178-RESEARCH Pitfall 3).
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-example-replay.test.ts -t "verdict"</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts `EXAMPLE_REPLAY_VERDICTS` deep-equals the four-element array above and is frozen (`Object.isFrozen` true).
    - One throwing test exists per behavior bullet above (7 total), each asserting on the error message text, not just that it threw.
    - A test constructs a record whose `exampleId` disagrees with its `slicePath`/`lineNumber` and asserts it throws.
    - `grep -c "buildBlindDerivePayload\|focusQuoteWindow\|blindDeriveHandle" src/cli/commands/verify-example-replay.ts` returns 0.
  </acceptance_criteria>
  <done>A verdict cannot enter the system without a named reason and caller-assigned identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The atomic upsert-append ledger triad with read-path revalidation</name>
  <read_first>
    - src/cli/commands/verify-derive-check.ts lines 481-620 (ledgerPath, key, replace, record, read)
    - src/cli/commands/verify-run.ts (atomicWriteFile)
  </read_first>
  <behavior>
    - `exampleReplayLedgerPath(projectDir)` resolves to `rulebook/.example-replay/EXAMPLE-VERDICTS.md` under `projectDir`.
    - Recording a verdict for example B leaves a previously-recorded verdict for example A byte-identical in the ledger.
    - Re-recording example A replaces A's entry in place (upsert), producing exactly one entry for A.
    - `readExampleReplayVerdicts` routes every parsed record back through `createExampleReplayRecord`; a hand-corrupted ledger entry (verdict outside the set, or a missing required field for its verdict) makes the read THROW, naming the offending `exampleId`.
    - A ledger file whose fence markers are absent or unbalanced makes the read throw with an actionable message, never silently return `[]`.
    - Writes go through `atomicWriteFile`; no direct `fs.writeFile` of the ledger exists in the module.
  </behavior>
  <action>
    Implement `exampleReplayLedgerPath`, `exampleReplayKey` (returns `record.exampleId` — the
    caller-assigned identity, never a text field), `replaceExampleReplayVerdicts`,
    `recordExampleReplayVerdicts` (read → filter out matching key → append → replace), and
    `readExampleReplayVerdicts`. Define `EXAMPLE_REPLAY_LEDGER_BEGIN`/`_END` fence constants in the
    `<!-- boardsmith:example-replay-verdicts:begin -->` shape. Serialize records as a fenced JSON
    block inside the markdown ledger, matching the CHECK-04 ledger's own file shape so a reader who
    knows one knows both.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-example-replay.test.ts -t "ledger"</automated>
  </verify>
  <acceptance_criteria>
    - A test records A, records B, reads back, and asserts BOTH are present and A's fields are unchanged (deep-equal to what was written).
    - A test records A twice with different verdicts and asserts exactly one A entry survives, carrying the second verdict.
    - A test writes a ledger by hand containing `"verdict": "banana"` and asserts `readExampleReplayVerdicts` throws with a message containing the offending `exampleId`.
    - A test truncates the closing fence and asserts the read throws (not returns `[]`).
    - `grep -c "fs.writeFile\|writeFileSync" src/cli/commands/verify-example-replay.ts` returns 0.
  </acceptance_criteria>
  <done>The ledger holds CHECK-04's four guarantees, proven by test rather than by resemblance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: verify-example-replay — the read/report command</name>
  <read_first>
    - src/cli/commands/verify-derive-check.ts lines 620-870 (VerifyDeriveCheckResult shape, verdict counts, verifyDeriveCheckCommand, its `.verify/` decoy behavior)
    - src/cli/commands/chunk-provenance.ts (resolveCitedSlices signature and its `## Verified Against` parsing)
    - .planning/phases/178-worked-example-tests/178-CONTEXT.md decisions 11, 16
  </read_first>
  <behavior>
    - `verifyExampleReplayCommand({ json: true })` prints a JSON object with `slices[]` (each with `slicePath`, `extractionPayload`, `pending: boolean`), `verdicts[]`, `counts` (raw per-verdict integers), and `perGameBreakdown`-shaped grouping by slice — and NEVER a percentage field.
    - The command never sets `process.exitCode`, even when every example disagrees.
    - With no `--chunk`, it enumerates PROJECT-WIDE from `readLiveSlices` — independent of any `.verify/<runId>/` staging directory (assert with a staged-decoy fixture, mirroring CHECK-04's own decoy test).
    - With `--chunk <slug>`, it scopes to exactly that chunk's cited slices via `resolveCitedSlices`, and errors actionably when the slug names no chunk.
    - A slice with no ledger entry is reported `pending` WITH its extraction payload attached, so the orchestrator dispatches bytes the CLI built rather than composing its own.
  </behavior>
  <action>
    Implement `VerifyExampleReplayOptions` (`--project`, `--json`, `--chunk`; NO `--run-id`, NO
    bypass/force/skip option of any kind — the command is source-free and run-less by construction),
    `VerifyExampleReplayResult`, and `verifyExampleReplayCommand`. Compose `readLiveSlices` +
    `buildExampleExtractionPayload` + `readExampleReplayVerdicts`. Human-readable output groups by
    slice and reports raw counts with a per-slice breakdown; add an explicit line stating the corpus
    size when it is below 10, in the shape "n = <count> — too small to distinguish the mechanism
    working from luck; read the per-example rows, not the totals" (CONTEXT decision 16).
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-example-replay.test.ts -t "command"</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts `process.exitCode` is `undefined` or `0` after a run in which every recorded verdict is `disagrees`.
    - A test creates a fixture project with `rulebook/.verify/<runId>/slices/decoy.md` and asserts `slices[]` contains no path under `.verify/`.
    - A test with `--chunk` asserts the reported `slices[]` equals the chunk's cited slices from `resolveCitedSlices` and nothing else.
    - `node dist/cli.js verify-example-replay --project <fixture> --json | node -e "const o=JSON.parse(require('fs').readFileSync(0));if(JSON.stringify(o).match(/percent|Percentage|%/))process.exit(1)"` exits 0.
    - A pending slice's `extractionPayload` is byte-equal to `buildExampleExtractionPayload(slice).payload` (asserted directly).
  </acceptance_criteria>
  <done>A read-only, project-level, source-free CHECK-06 report exists that hands out its own dispatch payloads and never gates anything.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-disk ledger → in-memory records | A hand-edited or corrupted ledger crosses back into program state |
| CLI options → filesystem reads | `--project` / `--chunk` select what is read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-06 | Tampering | ledger read path | mitigate | Every read re-enters `createExampleReplayRecord`; unbalanced fences throw rather than returning `[]` (CR-02 inheritance) |
| T-178-07 | Repudiation | verdict overwrite | mitigate | Upsert keyed by caller-assigned `exampleId` via `atomicWriteFile`; per-example record carries `recordedAt` |
| T-178-08 | Elevation of privilege | bypass flags | mitigate | No `--force`/`--skip`/`--overwrite`/`--run-id` registered; a test asserts the option list |
| T-178-SC | Tampering | npm/pip/cargo installs | mitigate | No installs; any addition requires a blocking human checkpoint |
</threat_model>

<verification>
- `npm test` green.
- `npx vitest run src/cli/commands/verify-example-replay.test.ts` exits 0.
</verification>

<success_criteria>
- Frozen verdict set including `unexecutable` and `example-inconsistent`, each requiring a named reason.
- Atomic upsert-append ledger with revalidating read path, proven by test.
- Read-only `--json` command, exits 0 unconditionally, hands out its own extraction payloads, never reports a percentage.
</success_criteria>

<output>
Create `.planning/phases/178-worked-example-tests/178-03-SUMMARY.md` when done.
</output>
