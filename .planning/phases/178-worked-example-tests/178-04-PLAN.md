---
phase: 178-worked-example-tests
plan: 04
type: execute
wave: 4
depends_on: [178-03]
files_modified:
  - src/cli/commands/verify-example-replay.ts
  - src/cli/commands/verify-example-replay.test.ts
  - src/cli/cli.ts
  - src/cli/cli.test.ts
autonomous: true
requirements: [CHECK-06]

must_haves:
  truths:
    - "`verify-example-record` is the only surface that writes the CHECK-06 ledger"
    - "A replay mismatch against a slice whose quotes are not source-verified is reported at explicitly lower confidence, never as a confident accusation against the code"
    - "A `--slice-path` that escapes the project's rulebook/ is rejected, not read"
    - "Both commands are registered and reachable from the real CLI"
  artifacts:
    - path: "src/cli/commands/verify-example-replay.ts"
      provides: "verifyExampleRecordCommand — the sole ledger write surface, with provenance gating and path containment"
      exports: ["verifyExampleRecordCommand"]
    - path: "src/cli/cli.ts"
      provides: "verify-example-replay and verify-example-record registration"
      contains: "verify-example-record"
  key_links:
    - from: "src/cli/commands/verify-example-replay.ts"
      to: "src/cli/commands/verify-enumerate.ts"
      via: "QuoteVerifiedProvenance.obtain(projectDir) / .covers(slicePath)"
      pattern: "QuoteVerifiedProvenance"
    - from: "src/cli/cli.ts"
      to: "src/cli/commands/verify-example-replay.ts"
      via: "command registration"
      pattern: "verifyExampleRecordCommand"
---

<objective>
Add the sole write surface for CHECK-06's ledger, gate every mismatch on quote provenance, and
register both commands in the real CLI.

Purpose: CONTEXT decision 12 is this milestone's hardest-won lesson — 177's `seven:11` correction
proved that judging an inference against an UNVERIFIED quote cannot separate "the code is wrong"
from "the quote is wrong," and a confident false accusation is worse than an honest shrug. This
plan is where that lesson becomes code. 177.1's CR-04 (unbounded `--slice-path` read) is the second
lesson, inherited rather than repeated.
Output: `verifyExampleRecordCommand` + CLI registration for the pair.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-worked-example-tests/178-CONTEXT.md
@.planning/phases/178-worked-example-tests/178-03-SUMMARY.md
@./CLAUDE.md

<interfaces>
From src/cli/commands/verify-enumerate.ts (decision 12's machinery — reuse, never re-implement):
  export class QuoteVerifiedProvenance {
    static async obtain(projectDir: string): Promise<QuoteVerifiedProvenance | null>  // null when scope is not full
    readonly sourceHash: string
    readonly edition: string | undefined
    readonly archivedSourceBasename: string | undefined
    readonly unarchivedSources: readonly string[]
    covers(slicePath: string): boolean        // :1158 — filename-substring heuristic, see its own comment
  }

From src/cli/commands/verify-derive-check.ts:
  export function parseSubagentJsonInput(text, flagLabel, filePath): unknown   // :1206
  async function readRequiredJsonFile(filePath, flagLabel)                     // :1221 (module-private; mirror it)
  export async function verifyDeriveRecordCommand(options)                     // :1268 — the write-command shape

The containment guard to mirror — src/cli/commands/verify-classify.ts:706:
  `--live-slice "${options.liveSlice}" resolves outside ${relative(projectDir, rulebookDir)}.`
  (177.1-REVIEW CR-04 fixed the same gap on verify-derive-record; do not re-open it here)

CLI registration shape — src/cli/cli.ts:427-462 (the verify-derive-check / verify-derive-record block).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: verifyExampleRecordCommand — the sole write surface</name>
  <read_first>
    - src/cli/commands/verify-derive-check.ts lines 1200-1377 (parseSubagentJsonInput, readRequiredJsonFile, verifyDeriveRecordCommand incl. its containment guard)
    - src/cli/commands/verify-classify.ts lines 690-720 (the containment-guard message shape)
    - src/cli/commands/example-derivation.ts (createWorkedExampleSpec, collectWorkedExampleSpecs, workedExampleId)
  </read_first>
  <behavior>
    - `--slice-path ../../../etc/passwd` is REJECTED with a message naming `rulebook/`, and no read of that path occurs.
    - The command reads the extractor return and the translator return from files (`--extraction <file>`, `--translation <file>`), assigns each example's id itself via `workedExampleId` from `slicePath` + the returned `lineNumber`, and NEVER keys anything by returned prose.
    - Two returned examples resolving to the same `slicePath`+`lineNumber` cause the command to FAIL with a message naming both, writing nothing.
    - A returned example whose `sourceText` is not a verbatim substring of the slice on disk is rejected, writing nothing.
    - Recording is one atomic upsert-append per invocation; a mid-run rejection leaves the ledger byte-identical to before the invocation.
    - No `--run-id`, `--force`, `--skip`, or `--overwrite` option exists.
  </behavior>
  <action>
    Implement `VerifyExampleRecordOptions` (`--project`, `--slice-path`, `--extraction`,
    `--translation`, `--json`) and `verifyExampleRecordCommand` in `verify-example-replay.ts`.
    Resolve `sliceFsPath` against `join(projectDir, 'rulebook')` with `resolve` + a
    `startsWith(rulebookDir + sep)` containment check BEFORE any read, mirroring
    `verify-classify.ts`'s guard verbatim in shape and message. Parse both JSON inputs through
    `parseSubagentJsonInput`. Build specs through `createWorkedExampleSpec` and
    `collectWorkedExampleSpecs` (which throws on id collision). Build records through
    `createExampleReplayRecord` and persist with a single `recordExampleReplayVerdicts` call.
    Validate-everything-then-write: no partial write path exists.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-example-replay.test.ts -t "record"</automated>
  </verify>
  <acceptance_criteria>
    - A test passes `--slice-path ../../../../etc/passwd` and asserts a throw whose message contains `rulebook`, and that the ledger file is unchanged.
    - A test supplies an extraction return with two entries sharing `lineNumber` and asserts the command throws naming both and the ledger is byte-identical to its pre-call bytes.
    - A test supplies a `sourceText` absent from the slice and asserts rejection with the offending text quoted, ledger unchanged.
    - `grep -c "run-id\|force\|--skip\|overwrite" src/cli/commands/verify-example-replay.ts` returns 0.
    - A test asserts that after a successful record of two examples, `readExampleReplayVerdicts` returns exactly those two, each with the id `workedExampleId` computes for it.
  </acceptance_criteria>
  <done>The ledger has exactly one write surface, and it validates everything before it writes anything.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate every mismatch on quote provenance (decision 12)</name>
  <read_first>
    - src/cli/commands/verify-enumerate.ts lines 994-1200 (QuoteVerifiedProvenance: obtain, covers, unarchivedSources, and the comments naming what it cannot do)
    - src/cli/commands/verify-enumerate.ts lines 1280-1320 (the existing suspect/downgrade branch — the shape to mirror)
    - .planning/phases/178-worked-example-tests/178-CONTEXT.md decision 12
    - .planning/REQUIREMENTS.md CHECK-04 entry, the `seven:11` correction paragraph
  </read_first>
  <behavior>
    - When `QuoteVerifiedProvenance.obtain(projectDir)` returns `null`, every recorded `disagrees` is stored with `provenance: 'quote-unverified'` and the report labels it explicitly as lower-confidence, never as a code defect.
    - When provenance exists but `.covers(slicePath)` is `false`, the same downgrade applies, and the report names WHY (the slice is not covered by the archived source).
    - When provenance exists and covers the slice, `disagrees` is stored with `provenance: 'quote-verified'` and may be reported as a genuine mismatch.
    - `unarchivedSources` being non-empty is surfaced in the report output, not swallowed.
    - The downgrade NEVER changes the verdict itself — `disagrees` stays `disagrees`; only its confidence label and reporting change. (A downgrade that rewrote the verdict would hide the finding.)
  </behavior>
  <action>
    Add provenance resolution to `verifyExampleRecordCommand` (obtain once per invocation, not per
    example) and thread the resulting `'quote-verified' | 'quote-unverified'` value into every
    `createExampleReplayRecord` call. Make `createExampleReplayRecord` REQUIRE that field — a record
    without it throws, so a future caller cannot forget the gate. Extend
    `verifyExampleReplayCommand`'s report to group mismatches into two explicitly-named buckets
    ("mismatch, quotes source-verified" and "mismatch, quotes NOT source-verified — read as a
    question about the quote, not an accusation against the code") and to print
    `unarchivedSources` when non-empty.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-example-replay.test.ts -t "provenance"</automated>
  </verify>
  <acceptance_criteria>
    - A test with a fixture project having NO archived source asserts every recorded `disagrees` carries `provenance: 'quote-unverified'` and that the human-readable report contains the phrase `not an accusation against the code`.
    - A test with a fixture project whose archived source does not cover the slice asserts the same downgrade and that the report names the slice.
    - A test asserts the verdict string itself is `disagrees` in both cases (the downgrade did not rewrite it).
    - A test asserts `createExampleReplayRecord` throws when `provenance` is omitted.
    - A test with non-empty `unarchivedSources` asserts each basename appears in the report output.
  </acceptance_criteria>
  <done>CHECK-06 cannot make a confident accusation against code on the strength of an unverified quote.</done>
</task>

<task type="auto">
  <name>Task 3: Register both commands in cli.ts</name>
  <read_first>
    - src/cli/cli.ts lines 396-462 (the CHECK-04 registration block, including its explanatory comment)
    - src/cli/cli.test.ts (existing registration assertions)
    - src/cli/slash-command/bs/verify.test.ts lines 985-1030 (the drift guard: every named `boardsmith verify-*` command in skill prose must exist in cli.ts)
  </read_first>
  <action>
    Register `verify-example-replay` (options: `--project`, `--json`, `--chunk`) and
    `verify-example-record` (options: `--project`, required `--slice-path`, required `--extraction`,
    required `--translation`, `--json`) in `src/cli/cli.ts`, immediately after the CHECK-04 block,
    with a leading comment in the same shape naming CHECK-06, the four verdicts, the advisory
    posture on the verify side, the build-blocking posture on the build side, and that
    `verify-example-record` is the ONLY ledger write surface. Add registration assertions to
    `cli.test.ts` mirroring the existing CHECK-04 ones, including a negative assertion that neither
    command registers `--run-id` or any bypass flag.
  </action>
  <verify>
    <automated>npm run build && node dist/cli.js verify-example-replay --help && npx vitest run src/cli/cli.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node dist/cli.js verify-example-replay --help` exits 0 and its output lists `--project`, `--json`, `--chunk` and NOT `--run-id`.
    - `node dist/cli.js verify-example-record --help` exits 0 and lists `--slice-path`, `--extraction`, `--translation` as required.
    - `node dist/cli.js verify-example-record --project /tmp` exits non-zero with a message naming the missing required options.
    - `cli.test.ts` contains an assertion enumerating both commands' registered option names.
    - `npx vitest run src/cli/slash-command/bs/verify.test.ts -t "drift guard"` exits 0.
  </acceptance_criteria>
  <done>Both commands are reachable from the real CLI with the exact option surfaces the skill prose will later name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI `--slice-path` → filesystem | A caller-supplied relative path selects a file to read |
| subagent JSON return file → ledger | Model output crosses into persisted state |
| archived source hashes → finding confidence | Provenance decides whether a finding accuses code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-09 | Information disclosure | `--slice-path` | mitigate | `resolve` + `startsWith(rulebookDir + sep)` containment check before any read (inherits 177.1 CR-04) |
| T-178-10 | Repudiation | mismatch findings | mitigate | `provenance` is a required record field; unverified quotes downgrade the finding's confidence without rewriting the verdict |
| T-178-11 | Tampering | extraction/translation JSON | mitigate | `parseSubagentJsonInput` + `createWorkedExampleSpec` verbatim-substring check + fence rejection; validate-all-then-write |
| T-178-SC | Tampering | npm/pip/cargo installs | mitigate | No installs; any addition requires a blocking human checkpoint |
</threat_model>

<verification>
- `npm test` green.
- `npm run build` succeeds and both commands respond to `--help`.
</verification>

<success_criteria>
- One ledger write surface with containment, collision, and verbatim checks, all validate-before-write.
- Provenance gating implemented as a required field, with the downgrade proven not to rewrite verdicts.
- Both commands registered; the skill-prose drift guard passes.
</success_criteria>

<output>
Create `.planning/phases/178-worked-example-tests/178-04-SUMMARY.md` when done.
</output>
