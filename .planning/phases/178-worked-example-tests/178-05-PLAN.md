---
phase: 178-worked-example-tests
plan: 05
type: execute
wave: 5
depends_on: [178-04]
files_modified:
  - src/cli/commands/verify-example-replay.ts
  - src/cli/commands/verify-example-replay.test.ts
  - src/cli/cli.ts
  - src/cli/cli.test.ts
autonomous: true
requirements: [CHECK-06, TEST-01]

must_haves:
  truths:
    - "The translation dispatch prompt is bytes a registered CLI command produced, never prose an orchestrator composed"
    - "Both pipeline sides obtain those bytes from the same command, so neither restates GameApiSurface"
    - "`buildExampleTranslationPayload` and `collectGameApiSurface` are declared exactly once in the repo"
    - "The command reads, prints, and writes nothing — it cannot touch the ledger or a test file"
  artifacts:
    - path: "src/cli/commands/verify-example-replay.ts"
      provides: "verifyExampleTranslateCommand — the CLI surface that turns an extractor return into per-example translation dispatch payloads"
      exports: ["verifyExampleTranslateCommand"]
    - path: "src/cli/cli.ts"
      provides: "verify-example-translate registration"
      contains: "verify-example-translate"
  key_links:
    - from: "src/cli/commands/verify-example-replay.ts"
      to: "src/cli/commands/example-derivation.ts"
      via: "collectGameApiSurface + buildExampleTranslationPayload (the SC-3 translation half)"
      pattern: "buildExampleTranslationPayload"
    - from: "src/cli/cli.ts"
      to: "src/cli/commands/verify-example-replay.ts"
      via: "command registration"
      pattern: "verifyExampleTranslateCommand"
---

<objective>
Give the translation half of the shared derivation module a CLI surface, so `build/test.md` and
`verify-game.md` can CITE a real command for the second dispatch exactly the way they already cite
`verify-example-replay` for the first.

Purpose: plan 02 builds `buildExampleTranslationPayload` and `collectGameApiSurface`; plans 03-04
give only the EXTRACTION half a command. Without this plan the translation payload is reachable
from no shipped command, and a skill would have to re-derive `GameApiSurface` in prose — the exact
"restate the logic instead of citing the real command" failure `build/test.md` forbids, and the
same build-it-but-never-wire-it gap Phase 177.1 exists to repair. This plan closes it BEFORE any
skill prose is written against it.
Output: `verifyExampleTranslateCommand` in the CHECK-06 command module + its registration, plus a
guard proving there is exactly one implementation of each shared builder in the repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-worked-example-tests/178-CONTEXT.md
@.planning/phases/178-worked-example-tests/178-02-SUMMARY.md
@.planning/phases/178-worked-example-tests/178-04-SUMMARY.md
@./CLAUDE.md

<interfaces>
From src/cli/commands/example-derivation.ts (plan 02) — the module this command exists to expose:
  export interface WorkedExampleSpec { id, slicePath, lineNumber, pageCitation, kind, sourceText,
                                       setup, action?, expected, supportingQuoteLines }
  export function workedExampleId(input: { slicePath: string; lineNumber: number }): string
  export function createWorkedExampleSpec(input: { id, sliceText, returned }): WorkedExampleSpec
  export function collectWorkedExampleSpecs(specs): Map<string, WorkedExampleSpec>  // throws on id collision
  export interface GameApiSurface { projectDir, testDir, exportedSymbols }
  export function collectGameApiSurface(projectDir: string): GameApiSurface
  export function buildExampleTranslationPayload(spec: WorkedExampleSpec, api: GameApiSurface): string

Already in src/cli/commands/verify-example-replay.ts (plans 03-04) — reuse, do not re-implement:
  verifyExampleReplayCommand   // the read/report command; the shape and exit discipline to mirror
  verifyExampleRecordCommand   // the sole ledger WRITE surface, with its `--slice-path` containment
                               // guard and its `parseSubagentJsonInput` handling of `--extraction`

From src/cli/commands/verify-derive-check.ts:
  export function parseSubagentJsonInput(text, flagLabel, filePath): unknown   // :1206

CLI registration shape — src/cli/cli.ts, the CHECK-06 block plan 04 added directly after CHECK-04's.

The pipeline position this command occupies (both sides, identically):
  1. boardsmith verify-example-replay   --project <d> [--chunk <s>] --json   → extraction payloads
  2. dispatch BS-EXTRACT-EXAMPLE-V1                                          → <slice>.extract.json
  3. boardsmith verify-example-translate --project <d> --slice-path <p> --extraction <f> --json
                                                                             → translation payloads
  4. dispatch BS-TRANSLATE-EXAMPLE-V1                                        → <slice>.translate.json
  5. boardsmith verify-example-record   --project <d> --slice-path <p> --extraction <f> --translation <f>
Step 3 is what this plan builds. Steps 1, 2, 4, 5 already exist or are contracts (plan 07).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: verifyExampleTranslateCommand — the second dispatch's byte source</name>
  <read_first>
    - src/cli/commands/verify-example-replay.ts (plans 03-04: `verifyExampleReplayCommand`'s option/result shape and its never-set-exitCode discipline; `verifyExampleRecordCommand`'s `--slice-path` containment guard and `--extraction` parsing — both are reused here, not re-written)
    - src/cli/commands/example-derivation.ts (plan 02: `collectGameApiSurface`, `buildExampleTranslationPayload`, `createWorkedExampleSpec`, `collectWorkedExampleSpecs`, `workedExampleId`)
    - src/cli/commands/verify-classify.ts lines 690-720 (the containment-guard message shape plan 04 mirrored)
    - .planning/phases/178-worked-example-tests/178-CONTEXT.md decisions 6, 7
  </read_first>
  <behavior>
    - `--slice-path ../../../etc/passwd` is REJECTED with a message naming `rulebook/` before any read, using the SAME guard helper plan 04 introduced — not a second copy of it.
    - Given a slice on disk and an extractor return file, the command emits one translation payload per returned example, each carrying the `exampleId` the CLI itself computed via `workedExampleId({ slicePath, lineNumber })` — never an id the model supplied.
    - Two returned examples resolving to the same `slicePath`+`lineNumber` make the command FAIL, naming both, emitting no payloads (the `collectWorkedExampleSpecs` collision path).
    - `collectGameApiSurface(projectDir)` is called exactly ONCE per invocation and the resulting surface is passed to every `buildExampleTranslationPayload` call.
    - An entry the extractor marked `example-inconsistent` is NOT translated: it is reported in a `notTranslated[]` array with its reason, and the command still exits 0. Deciding it is inconsistent already happened; this command never re-judges it.
    - A slice whose extractor return contains zero examples produces zero payloads and exits 0 — a finding, not a failure.
    - Exit discipline: findings (zero payloads, `notTranslated` entries) exit 0; only tool failure exits non-zero — an unreadable or unparseable `--extraction` file, a containment violation, an id collision, or a spec that fails `createWorkedExampleSpec`. The command never sets `process.exitCode` for a finding.
    - The command writes NOTHING: no ledger, no test file, no scratch file. Its entire output is stdout.
  </behavior>
  <action>
    Add `VerifyExampleTranslateOptions` (`--project`, required `--slice-path`, required
    `--extraction`, `--json`; NO `--run-id`, NO bypass/force/skip option) and
    `verifyExampleTranslateCommand` to `src/cli/commands/verify-example-replay.ts`.
    Flow: resolve and containment-check `--slice-path` through the SAME guard helper
    `verifyExampleRecordCommand` uses — extract it into a module-local named helper if plan 04 left
    it inline, so exactly one containment implementation exists in the module. Read the slice text.
    Parse `--extraction` through `parseSubagentJsonInput`. Assign each entry's id with
    `workedExampleId`, build specs through `createWorkedExampleSpec` and `collectWorkedExampleSpecs`,
    call `collectGameApiSurface(projectDir)` once, and map each surviving spec through
    `buildExampleTranslationPayload`.
    Result shape: `{ slicePath, apiSurfaceSymbolCount, payloads: Array<{ exampleId, lineNumber, kind,
    translationPayload }>, notTranslated: Array<{ lineNumber, reason }> }`. `--json` prints it;
    human-readable output prints one payload header per example plus the `notTranslated` reasons.
    Declare in the module header what this command is FOR: it is the only supported way to obtain
    translation dispatch bytes, and a caller that composes its own prompt is re-deriving
    `GameApiSurface` — the thing this command exists to prevent.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-example-replay.test.ts -t "translate"</automated>
  </verify>
  <acceptance_criteria>
    - A test passes `--slice-path ../../../../etc/passwd` and asserts a throw whose message contains `rulebook`, with no payload emitted.
    - A test asserts each emitted `exampleId` deep-equals `workedExampleId({ slicePath, lineNumber })` for its own entry, and that changing the model-supplied text in the return file does not change any id.
    - A test asserts each emitted `translationPayload` is byte-equal to `buildExampleTranslationPayload(spec, api)` called directly with the same spec and `collectGameApiSurface(projectDir)` — proving the command emits the module's bytes, not its own.
    - A test with two returned entries sharing `lineNumber` asserts the command throws naming both and emits nothing.
    - A test with an `example-inconsistent` entry asserts it appears in `notTranslated[]` with its reason, that no payload was built for it, and that `process.exitCode` is `undefined` or `0`.
    - A test with a zero-example extraction return asserts zero payloads and `process.exitCode` `undefined` or `0`.
    - A test asserts the ledger file's bytes and mtime are unchanged across an invocation, and that no file under the project was created.
    - `grep -c "run-id\|--force\|--skip\|overwrite" src/cli/commands/verify-example-replay.ts` returns 0.
  </acceptance_criteria>
  <done>Translation dispatch bytes have a registered producer; no caller ever needs to describe the game's API surface itself.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register verify-example-translate and pin the one-implementation guarantee</name>
  <read_first>
    - src/cli/cli.ts (the CHECK-06 registration block plan 04 added, including its leading comment)
    - src/cli/cli.test.ts (plan 04's registration assertions for `verify-example-replay` / `verify-example-record`)
    - src/cli/slash-command/bs/verify.test.ts lines 985-1030 (the drift guard: every named `boardsmith verify-*` command must resolve to a real `.command('verify-...')` in cli.ts)
  </read_first>
  <behavior>
    - `boardsmith verify-example-translate --help` exits 0 and lists `--project`, `--slice-path`, `--extraction`, `--json`, and no bypass flag.
    - Invoking it without `--slice-path` or `--extraction` exits non-zero naming the missing required option.
    - `buildExampleTranslationPayload`, `buildExampleExtractionPayload`, and `collectGameApiSurface` each have exactly ONE declaration site across `src/**`, and that site is `example-derivation.ts`.
  </behavior>
  <action>
    Register `verify-example-translate` in `src/cli/cli.ts` inside the CHECK-06 block, immediately
    after `verify-example-record`, extending that block's leading comment with one sentence naming
    this command's role: it produces the SECOND dispatch's payload from the FIRST dispatch's return,
    it writes nothing, and it is what skill prose cites instead of describing the game's API surface.
    Add `cli.test.ts` assertions in the shape plan 04 established: the command is registered, its
    option list is exactly the four above, and it registers no `--run-id` or bypass flag.
    Then add a `CHECK-06 — one derivation implementation (SC-3)` describe block in
    `verify-example-replay.test.ts` that reads source text (not runtime behavior) and asserts:
    (i) each of the three shared symbol names above appears as a declaration (`export function <name>`)
    exactly once across all files under `src/`, in `example-derivation.ts`; (ii)
    `verify-example-replay.ts` imports `buildExampleExtractionPayload`, `buildExampleTranslationPayload`,
    and `collectGameApiSurface` from `./example-derivation.js`; (iii) `verify-example-replay.ts`
    declares no function whose name matches `/^build.*Payload$/` or `/^collect.*ApiSurface$/`.
    State in a comment that this block is the falsifier: it fails the moment a second implementation
    of either payload builder is added anywhere under `src/`.
  </action>
  <verify>
    <automated>npm run build && node dist/cli.js verify-example-translate --help && npx vitest run src/cli/cli.test.ts src/cli/commands/verify-example-replay.test.ts src/cli/slash-command/bs/verify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node dist/cli.js verify-example-translate --help` exits 0 and its output lists `--slice-path` and `--extraction` as required and does NOT list `--run-id`.
    - `node dist/cli.js verify-example-translate --project /tmp` exits non-zero naming the missing required options.
    - `cli.test.ts` contains an assertion enumerating `verify-example-translate`'s registered option names.
    - The SC-3 describe block's declaration-count assertion is demonstrated to be a real detector: the test computes the count from a source scan and a sibling assertion shows the count would be 2 (and the test would fail) if a second `export function buildExampleTranslationPayload` string were present in the scanned set — demonstrated against an in-test mutated copy of the scan input, never by editing a real file.
    - `npx vitest run src/cli/slash-command/bs/verify.test.ts -t "drift guard"` exits 0.
  </acceptance_criteria>
  <done>The translation command is reachable from the real CLI, and a second implementation of the derivation logic now fails the suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI `--slice-path` → filesystem | A caller-supplied relative path selects a file to read |
| extractor JSON return file → dispatch payload | Model output crosses into the bytes a second model will be given |
| generated game source tree → dispatch payload | Project files are read and embedded into a prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-28 | Information disclosure | `--slice-path` on the translate command | mitigate | Reuses the SINGLE containment helper (`resolve` + `startsWith(rulebookDir + sep)`) before any read; a second copy of the guard is forbidden by the module-local-helper requirement |
| T-178-29 | Tampering | extractor return → translation prompt | mitigate | `parseSubagentJsonInput` + `createWorkedExampleSpec` verbatim-substring and fence checks run BEFORE any payload is built; ids are caller-assigned via `workedExampleId`, never model-supplied |
| T-178-30 | Elevation of privilege | command scope creep | mitigate | The command writes nothing; a test asserts the ledger bytes/mtime are unchanged and no file was created |
| T-178-SC | Tampering | npm/pip/cargo installs | mitigate | No installs; any addition requires a blocking human checkpoint |
</threat_model>

<verification>
- `npm test` green.
- `npm run build` succeeds and `node dist/cli.js verify-example-translate --help` exits 0.
- `grep -c "GameApiSurface" src/cli/commands/verify-example-replay.ts` shows the type is only imported and passed, never re-declared.
</verification>

<success_criteria>
- A registered `verify-example-translate` command emits `buildExampleTranslationPayload`'s exact bytes.
- Ids are caller-assigned; collisions fail closed; `example-inconsistent` entries are passed through untranslated at exit 0.
- Exactly one declaration site exists for each shared derivation symbol, proven by a source-scanning test that would fail on duplication.
</success_criteria>

<output>
Create `.planning/phases/178-worked-example-tests/178-05-SUMMARY.md` when done.
</output>
