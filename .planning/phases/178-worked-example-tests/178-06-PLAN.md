---
phase: 178-worked-example-tests
plan: 06
type: execute
wave: 6
depends_on: [178-05]
files_modified:
  - src/cli/slash-command/bs/verify/extract-example.md
  - src/cli/slash-command/bs/verify/translate-example.md
  - src/cli/commands/install-claude-command.ts
  - src/cli/slash-command/bs/verify.test.ts
autonomous: true
requirements: [CHECK-06, TEST-01]

must_haves:
  truths:
    - "Extraction and translation are two separately-dispatched contracts with distinct handshake tokens — the independence is structural, not an instruction"
    - "An example that contradicts its own source returns example-inconsistent and is never turned into a test"
    - "A predicate example may be translated into a direct function call, not only a game.doAction call"
    - "Both contracts ship in a real install, proven by a leaf probe"
  artifacts:
    - path: "src/cli/slash-command/bs/verify/extract-example.md"
      provides: "BS-EXTRACT-EXAMPLE-V1 — the identification/extraction judgment contract"
      contains: "BS-EXTRACT-EXAMPLE-V1"
    - path: "src/cli/slash-command/bs/verify/translate-example.md"
      provides: "BS-TRANSLATE-EXAMPLE-V1 — the spec→runnable-test judgment contract"
      contains: "BS-TRANSLATE-EXAMPLE-V1"
  key_links:
    - from: "src/cli/commands/install-claude-command.ts"
      to: "src/cli/slash-command/bs/verify/extract-example.md"
      via: "SHARED_LEAF_PROBES entry"
      pattern: "extract-example.md"
    - from: "src/cli/slash-command/bs/verify.test.ts"
      to: "src/cli/slash-command/bs/verify/translate-example.md"
      via: "prose regression pin (PROC-01)"
      pattern: "BS-TRANSLATE-EXAMPLE-V1"
---

<objective>
Write the two judgment contracts this phase dispatches — extraction and translation — with the
handshake, never-sees, and RETURN-shape discipline `enumerate-facts.md`/`reconcile-facts.md`
established, and pin both with prose regression tests and an installer leaf probe.

Purpose: CONTEXT decision 6 requires TWO dispatches, not one. A single combined pass invites the
model to work backward from code it can already see, producing agreement rather than a test.
Splitting them into separate contracts with distinct tokens and non-overlapping inputs makes that
independence structural.
Output: two contract files, installer probes, and pins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-worked-example-tests/178-CONTEXT.md
@.planning/phases/178-worked-example-tests/178-05-SUMMARY.md
@./CLAUDE.md

<interfaces>
The contract template to follow — src/cli/slash-command/bs/verify/enumerate-facts.md:
  - `## FIRST: validate your dispatch prompt` block: if the prompt lacks the exact token, STOP,
    read nothing, return `DISPATCH REJECTED — missing <TOKEN>. ...`
  - an explicit never-sees list
  - a structured RETURN shape, JSON, with every field named
  - a no-arithmetic / no-inference rule stating what the contract may NOT do

The CLI-side shapes these contracts must return into (plans 02-04):
  WorkedExampleSpec: id (CALLER-ASSIGNED — the contract must NOT invent one), slicePath, lineNumber,
    pageCitation, kind ('transition'|'predicate'), sourceText (verbatim), setup, action, expected,
    supportingQuoteLines[]
  EXAMPLE_REPLAY_VERDICTS: 'agrees' | 'disagrees' | 'example-inconsistent' | 'unexecutable'

Installer — src/cli/commands/install-claude-command.ts:
  const SHARED_LEAF_PROBES = [ ..., join(SHARED_ROOT,'verify','enumerate-facts.md'), join(SHARED_ROOT,'verify','reconcile-facts.md') ]
</interfaces>

<fixture_reality>
The contracts must handle these exact real cases (verified at plan time):

- `seven` predicate surface: NOT free `isSet`/`isRun` functions. `~/BoardSmithGames/seven/src/rules/scoring.ts`
  exports `ScoringPattern` (an interface with a `check` member), the pattern constants
  `RUN_OF_SEVEN_PATTERN`, `COMBO_SETS_AND_RUNS_PATTERN`, `SET_5_PLUS_SET_2_PATTERN`, plus
  `legalScoringPatterns(scoredCards)` and `numberCardsOf(cards)` — and these take `SevenCard[]`
  element objects, not raw numbers. A `predicate` translation must construct real cards.
  (This resolves 178-RESEARCH Open Question 2. Re-verify against the file; if it has moved, record
  what is actually there rather than repeating this paragraph.)
- `seven`'s Run example is DEFECTIVE IN ITS OWN SOURCE: the quoted text reads `"example: 5, 6, 7"`
  while the adjacent `Visual (p.1):` line records that the card images show 1, 2, 3. This is the
  designated adversarial fixture for `example-inconsistent`. The extraction contract must return
  `example-inconsistent` with BOTH contradicting excerpts and must never pick a side.
- `one-two-punch`'s two Punch Examples are the cleanest `transition` fixtures: a before-state of
  three Guards (READY/EXHAUSTED/EXHAUSTED), a punch, and an after-state.
- `doom-machine`'s example is image-derived (`Worked example content (p.1, panel -7-, verbatim from
  card art):`) and may be legitimately `unexecutable` — with a NAMED reason, never a silent drop.
</fixture_reality>
</context>

<tasks>

<task type="auto">
  <name>Task 1: extract-example.md — the identification and extraction contract</name>
  <read_first>
    - src/cli/slash-command/bs/verify/enumerate-facts.md (full — handshake block, never-sees list, RETURN shape, the no-inference rule)
    - src/cli/slash-command/bs/verify/reconcile-facts.md (the verbatim-quote grounding rule)
    - src/cli/commands/example-derivation.ts (the exact spec fields the CLI will validate)
    - .planning/phases/178-worked-example-tests/178-CONTEXT.md decisions 1, 3, 4, 5
  </read_first>
  <action>
    Create `src/cli/slash-command/bs/verify/extract-example.md` carrying the
    `BS-EXTRACT-EXAMPLE-V1` handshake and dispatch-rejection block copied in shape from
    `enumerate-facts.md`. Content:
    - What a worked example IS, stated as judgment with the three real shapes named (a
      state→action→state transition; a definition illustration; an image-derived sample) and an
      explicit statement that no keyword or heading identifies one — the phrasing is ad-hoc and
      per-game, so a heading/keyword rule would work on the lines someone looked at and silently
      misjudge the rest.
    - The never-sees list: this contract is given ONLY the extraction payload the CLI built. It
      never sees the game's source code, never sees any existing test, and never sees a
      `Derived (p.N):` line. It must not infer an example that is not printed.
    - The typed RETURN: an array of examples, each with `lineNumber` (the payload's own 1-based
      number for the line the example starts at — the CLI assigns the id from it; the contract must
      NEVER invent an `id`), `pageCitation`, `kind`, `sourceText` VERBATIM from the payload, `setup`,
      `action` (transition only), `expected`, and `supportingQuoteLines[]` verbatim.
    - The `example-inconsistent` rule, stated as a hard rule with the `seven` Run case as the worked
      illustration: when the printed example contradicts its own accompanying visual or another line
      of the same slice, return `kind`-agnostic `example-inconsistent` with both contradicting
      excerpts verbatim and NEVER choose a side, never reconcile them, never emit a spec for it.
    - A zero-examples return is a legitimate, expected result and must be returned plainly — a slice
      with no worked example is a real finding about the ingest contract, never a reason to stretch
      an ordinary rule statement into an "example".
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/verify.test.ts -t "extract-example"</automated>
  </verify>
  <acceptance_criteria>
    - The file contains the exact token `BS-EXTRACT-EXAMPLE-V1` in both the handshake check and the rejection string.
    - The file names all four RETURN-relevant strings `transition`, `predicate`, `example-inconsistent`, and `supportingQuoteLines`.
    - The file contains an explicit sentence forbidding the contract from inventing an `id`.
    - The file's never-sees list explicitly names game source code, existing tests, and `Derived (p.` lines.
    - A `verify.test.ts` describe block pins: the token, the rejection-string shape, the never-sees entries, the `example-inconsistent` rule, and the "zero examples is a legitimate result" sentence — at least 6 assertions.
  </acceptance_criteria>
  <done>Identification is a judgment contract with a hard rule against inventing examples or resolving contradictions.</done>
</task>

<task type="auto">
  <name>Task 2: translate-example.md — the spec→runnable-test contract, kind-branched</name>
  <read_first>
    - src/cli/slash-command/bs/verify/extract-example.md (Task 1's output — its RETURN is this contract's input)
    - src/cli/commands/example-derivation.ts (buildExampleTranslationPayload, GameApiSurface)
    - ~/BoardSmithGames/seven/src/rules/scoring.ts (verify the predicate surface named in `<fixture_reality>`)
    - ~/BoardSmithGames/one-two-punch/tests/punch.test.ts (the target project's real test idiom — imports, vitest style)
    - .planning/phases/178-worked-example-tests/178-RESEARCH.md Pitfall 4
  </read_first>
  <action>
    Create `src/cli/slash-command/bs/verify/translate-example.md` carrying the
    `BS-TRANSLATE-EXAMPLE-V1` handshake and dispatch-rejection block. Content:
    - Input: exactly ONE `WorkedExampleSpec` plus the mechanically-collected `GameApiSurface`. It
      never sees the slice's other content, never sees the extraction payload, and never sees any
      existing test file — a translator that can read an existing test writes agreement with it
      rather than a test of the example.
    - Branch on `kind`, explicitly and separately:
      `transition` translates to setting up the described state and executing the described action
      through the game's action surface, then asserting the described after-state.
      `predicate` translates to a DIRECT call on an exported symbol from the supplied surface — it
      is NOT required to be an `Action`. State the `seven` shape concretely as the illustration:
      the predicate lives as a `check` member on an exported `ScoringPattern` constant and takes
      constructed card elements, not raw numbers, so the translation constructs real cards first.
    - `unexecutable` is a first-class RETURN with a REQUIRED named reason drawn from a stated,
      enumerated list (no symbol in the surface expresses the example; the example depends on
      component state the chunk does not model yet; the example is image-derived and its content is
      not determinate). Guessing at an API that is not in the supplied surface is forbidden and is
      the single most likely failure mode — say so.
    - The emitted code targets the generated project's own idiom: `vitest` `describe`/`it`/`expect`,
      importing from the project's own module paths, and nothing outside the project's dependency
      tree. Forbid network, filesystem, timer, `eval`, and nondeterministic APIs explicitly, naming
      that the CLI scans the returned code against `boardsmith`'s sandbox rules before writing it
      and REJECTS a violation.
    - The RETURN shape: `{ testCode, imports[], verdictHint, unexecutableReason }` — `verdictHint`
      is advisory only; the actual verdict comes from RUNNING the test, never from the model's
      claim about it.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/verify.test.ts -t "translate-example"</automated>
  </verify>
  <acceptance_criteria>
    - The file contains the exact token `BS-TRANSLATE-EXAMPLE-V1` in both the handshake check and the rejection string.
    - The file contains separately-headed `transition` and `predicate` branches, and the `predicate` branch names `ScoringPattern`, `check`, and states the card-element (not raw number) input shape.
    - The file enumerates at least three named `unexecutable` reasons and states that guessing at an absent API is forbidden.
    - The file's never-sees list explicitly names existing test files.
    - The file states that the verdict comes from running the test, not from `verdictHint`.
    - A `verify.test.ts` describe block pins all of the above — at least 7 assertions — plus a cross-contract assertion that the two tokens are distinct and neither file contains the other's token.
  </acceptance_criteria>
  <done>Translation is a separate contract that can honestly decline, and that knows a predicate is not an action.</done>
</task>

<task type="auto">
  <name>Task 3: Installer leaf probes and the real-install proof</name>
  <read_first>
    - src/cli/commands/install-claude-command.ts lines 40-115 (SHARED_LEAF_PROBES, SHARED_DIRS, expectedInstallPaths)
    - src/cli/slash-command/bs/verify.test.ts lines 738-780 (the existing "installer leaf probes — real install proves both contracts ship" describe block, 177-15's shape)
  </read_first>
  <action>
    Add `join(SHARED_ROOT, 'verify', 'extract-example.md')` and
    `join(SHARED_ROOT, 'verify', 'translate-example.md')` to `SHARED_LEAF_PROBES`. Extend the
    existing leaf-probe describe block in `verify.test.ts` to perform a real install into a temp dir
    and assert both files land with their handshake tokens intact — mirroring exactly what the
    177-15 block does for `enumerate-facts.md`/`reconcile-facts.md`, not a weaker existence check.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/verify.test.ts src/cli/commands/install-claude-command.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "extract-example.md\|translate-example.md" src/cli/commands/install-claude-command.ts` returns 2.
    - A test performs a real install and asserts the installed `bs-shared/verify/extract-example.md` file's contents contain `BS-EXTRACT-EXAMPLE-V1`.
    - The same for `translate-example.md` and `BS-TRANSLATE-EXAMPLE-V1`.
    - `npx vitest run src/cli/commands/install-claude-command.test.ts` exits 0, including its existing self-reading meta-test.
  </acceptance_criteria>
  <done>A real install ships both contracts, proven by probing their content rather than their existence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatch prompt → subagent | An unauthorized or malformed prompt may reach a contract |
| contract prose → generated code | Contract instructions shape what code the model emits into a project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-15 | Spoofing | subagent dispatch | mitigate | `BS-EXTRACT-EXAMPLE-V1` / `BS-TRANSLATE-EXAMPLE-V1` handshake with a read-nothing rejection path, distinct per contract |
| T-178-16 | Tampering | emitted test code | mitigate | Contract forbids network/filesystem/timer/eval/nondeterministic APIs and states the CLI scan rejects violations (enforced in plan 05) |
| T-178-17 | Information disclosure | translation input | mitigate | Never-sees list excludes existing tests and slice content; enforced by `buildExampleTranslationPayload`'s composition (plan 02) |
| T-178-SC | Tampering | npm/pip/cargo installs | mitigate | No installs; any addition requires a blocking human checkpoint |
</threat_model>

<verification>
- `npm test` green.
- Both contracts pinned in `verify.test.ts` and probed by a real install.
</verification>

<success_criteria>
- Two contracts, two distinct tokens, non-overlapping inputs.
- `example-inconsistent` and `unexecutable` are contract-level first-class outcomes with required evidence/reasons.
- The predicate branch reflects `seven`'s real exported surface.
</success_criteria>

<output>
Create `.planning/phases/178-worked-example-tests/178-06-SUMMARY.md` when done.
</output>
