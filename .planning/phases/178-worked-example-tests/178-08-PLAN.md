---
phase: 178-worked-example-tests
plan: 08
type: execute
wave: 8
depends_on: [178-07]
files_modified:
  - src/cli/slash-command/bs/build/test.md
  - src/cli/slash-command/bs/build-chunk.test.ts
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "A newly-built chunk's worked examples become executable tests as a numbered step in build/test.md's ordered sequence"
    - "A worked-example mismatch is build-blocking and routes the chunk back to repair, like every other step in that sequence"
    - "A chunk with zero worked examples names the exemption explicitly rather than silently skipping"
    - "Both dispatch prompts are bytes a named command produced; the step never describes the game's API surface itself"
  artifacts:
    - path: "src/cli/slash-command/bs/build/test.md"
      provides: "The worked-example test-generation step, inserted between the existing steps 3 and 4"
      contains: "verify-example-emit"
  key_links:
    - from: "src/cli/slash-command/bs/build/test.md"
      to: "src/cli/commands/example-test-emit.ts"
      via: "boardsmith verify-example-emit --chunk"
      pattern: "verify-example-emit"
    - from: "src/cli/slash-command/bs/build/test.md"
      to: "src/cli/commands/verify-example-replay.ts"
      via: "boardsmith verify-example-translate — the cited producer of the second dispatch's bytes"
      pattern: "verify-example-translate"
    - from: "src/cli/slash-command/bs/build-chunk.test.ts"
      to: "src/cli/slash-command/bs/build/test.md"
      via: "prose regression pin (PROC-01)"
      pattern: "verify-example-emit"
---

<objective>
Wire TEST-01 into `build/test.md` as a new numbered step between the existing steps 3 and 4, so a
newly-built chunk's worked examples become executable tests as part of the build, and a mismatch
routes the chunk to `repair`.

Purpose: this is the accumulation mechanism. Without it, worked examples remain a one-time seed for
hand-written tests. With it, every new chunk of every new game accrues them systematically.
Output: an edited `build/test.md` and its regression pins. Skill prose is source code here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-worked-example-tests/178-CONTEXT.md
@.planning/phases/178-worked-example-tests/178-07-SUMMARY.md
@./CLAUDE.md

<interfaces>
`build/test.md`'s existing ordered sequence (non-reorderable, stop-on-failure), read at plan time:
  1. Compile gate — `tsc --noEmit`
  2. Sandbox lint — `boardsmith lint` (names the seven rules explicitly)
  3. Chunk unit/integration tests
  4. Full accumulated suite (regression)
  5. Random-sim playthrough — `simulateRandomGames`, four zero-checks + the SKILLAUTO-08
     per-action coverage counter, with the explicit "a chunk with zero new actions is exempt; name
     that exemption explicitly in the test file's comment" pattern
  6. Asset-reachability gate (conditional on `ui: touches|major`) — `scanAssetReachability`
  7. A11y floor (conditional on `ui: touches|major`) — five items
  Then: "## Failures Loop Back to `build`" and "## Downstream Shape (cite, never restate)".

The file's own stated discipline, which the new step must follow:
  "do not reimplement or duplicate this scan; cite it and run the real command."

Commands available (plans 03-06):
  boardsmith verify-example-replay    --project <dir> --chunk <slug> --json
  boardsmith verify-example-translate --project <dir> --slice-path <p> --extraction <f> --json
  boardsmith verify-example-record    --project <dir> --slice-path <p> --extraction <f> --translation <f>
  boardsmith verify-example-emit      --project <dir> --chunk <slug>
Contracts (plan 07): bs-shared/verify/extract-example.md (BS-EXTRACT-EXAMPLE-V1),
                     bs-shared/verify/translate-example.md (BS-TRANSLATE-EXAMPLE-V1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the worked-example step and renumber the sequence</name>
  <read_first>
    - src/cli/slash-command/bs/build/test.md (full)
    - src/cli/slash-command/bs/build/repair.md (what "routes to repair" concretely means, so the new step's failure language matches)
    - .planning/phases/178-worked-example-tests/178-CONTEXT.md decision 10
    - src/cli/commands/example-test-emit.ts (the emit command's real option surface and its zero-example exemption text)
  </read_first>
  <action>
    Insert a new item 4, "Worked-example tests (TEST-01)", between the existing items 3 and 4, and
    renumber the subsequent items 4-7 to 5-8 throughout the file, including every cross-reference to
    them elsewhere in the file (the a11y section's "item 2 above" style references and the
    "## Failures Loop Back" paragraph).
    The new step, in ordered sub-steps, citing the real commands and never restating their logic:
    (a) Run `boardsmith verify-example-replay --chunk <this chunk's slug> --json` to enumerate this
        chunk's cited slices and get each pending slice's extraction payload.
    (b) For each pending slice, dispatch that slice's `extractionPayload` UNCHANGED to a subagent
        carrying `${CLAUDE_SKILL_DIR}/../bs-shared/verify/extract-example.md`'s
        `BS-EXTRACT-EXAMPLE-V1` handshake, and save the return to a file.
    (c) Run `boardsmith verify-example-translate --slice-path <that slice> --extraction <that return
        file> --json` to obtain one translation dispatch payload per extracted example. State
        explicitly that this command is the ONLY source of those bytes: the step must never compose
        a translation prompt itself, because doing so would mean restating the game's exported API
        surface in prose instead of citing the command that collects it.
    (d) Dispatch each returned `payloads[].translationPayload` UNCHANGED and SEPARATELY to a second
        subagent carrying `translate-example.md`'s `BS-TRANSLATE-EXAMPLE-V1` handshake, and save the
        returns to a file. Two dispatches, never one combined pass — state the reason inline (a
        combined pass lets the model work backward from code it can already see, producing agreement
        rather than a test).
    (e) Record both returns through exactly ONE `boardsmith verify-example-record` invocation per
        SLICE — atomic upsert-append, never a whole-ledger rewrite.
    (f) Run `boardsmith verify-example-emit --chunk <slug>` to write the chunk's single generated
        test file, then RUN that file with the project's own test runner. The verdict comes from
        running the test, never from the translator's `verdictHint`.
    (g) A `disagrees` verdict is BUILD-BLOCKING and routes this chunk back to `build`/`repair` in
        the same way every other step in this sequence does — state why the asymmetry with
        `/bs-verify-game` is deliberate: in build, the chunk was JUST written to satisfy those exact
        slices, so a mismatch is precisely the drift this step exists to catch.
    (h) An `unexecutable` or `example-inconsistent` verdict is NOT a build failure. Route
        `example-inconsistent` to the designer via `## Open Rules Gaps`; record `unexecutable` with
        its named reason. Never turn either into a passing test. An entry
        `verify-example-translate` reports under `notTranslated[]` was never dispatched for
        translation at all — record it, never re-judge it here.
    (i) A chunk whose cited slices contain zero worked examples SKIPS the step and names the
        exemption explicitly in the generated test file's comment — the same SKILLAUTO-08 exemption
        discipline item 5's action-coverage counter already uses, never a silent omission.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^[0-9]\." src/cli/slash-command/bs/build/test.md` shows exactly 8 numbered items, in order 1-8, with item 4 being the worked-example step.
    - The file contains all four command names `verify-example-replay`, `verify-example-translate`, `verify-example-record`, `verify-example-emit`, and both handshake tokens.
    - The file contains no restatement of the verdict-classification logic — assert by grepping that the four verdict strings appear only in the routing sentences, and that no sentence describes HOW a verdict is computed.
    - The file states the build-blocking rule and the reason for the asymmetry with the verify side in the same paragraph.
    - The file contains no prose description of the game's exported API surface or of what a translation prompt should say — the translation bytes are obtained only by citing `verify-example-translate` (assert by grepping that no sentence outside the command citation names `GameApiSurface`, `exportedSymbols`, or instructs the orchestrator to describe the project's exports).
    - The file contains the zero-example exemption sentence naming that it goes in the test file's comment.
    - No stale cross-reference remains: `grep -n "item [0-9]" src/cli/slash-command/bs/build/test.md` shows every reference pointing at the correct renumbered item (verify each by reading, not by count alone).
  </acceptance_criteria>
  <done>The ordered sequence has a worked-example step whose failure routes to repair and whose exemption is named.</done>
</task>

<task type="auto">
  <name>Task 2: Regression pins for the new step (PROC-01/PROC-02)</name>
  <read_first>
    - src/cli/slash-command/bs/build-chunk.test.ts (the existing build/test.md pin shape, incl. line ~978's step enumeration)
    - src/cli/slash-command/bs/verify.test.ts lines 985-1030 (the drift guard asserting every named `boardsmith verify-*` command exists in cli.ts — the new commands must satisfy it)
  </read_first>
  <action>
    Add a `build/test.md — worked-example step (TEST-01)` describe block to `build-chunk.test.ts`
    pinning: the step's position (it appears after the chunk-tests item and before the full-suite
    item, asserted by index in the parsed numbered list, not by a substring search); the four
    command names, including `verify-example-translate` as the cited producer of the translation
    dispatch bytes; both handshake tokens; the two-dispatch rule; the build-blocking sentence; the
    zero-example exemption sentence; and the "verdict comes from running the test" sentence.
    Also add a guard asserting the file's numbered items are exactly 1..8 with no duplicate or
    skipped number — so a future edit that inserts a step without renumbering fails loudly.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/build-chunk.test.ts src/cli/slash-command/bs/verify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The new describe block contains at least 7 assertions.
    - The step-position assertion parses the numbered list and compares indices; it does not rely on a raw substring ordering check.
    - The numbering guard fails if a duplicate or missing number is introduced — demonstrate by temporarily mutating a copy of the file in the test fixture and asserting the parse helper reports the defect.
    - `npx vitest run src/cli/slash-command/bs/verify.test.ts -t "drift guard"` exits 0 (all four new command names resolve in cli.ts).
  </acceptance_criteria>
  <done>The prose is pinned; a silent renumber or a dropped command reference fails the suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill prose → build pipeline behavior | Prose is the executable specification the build orchestrator follows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-18 | Tampering | `build/test.md` step order | mitigate | Numbering guard test fails on duplicate/missing item numbers; step position asserted by parsed index |
| T-178-19 | Repudiation | silent skip of the step | mitigate | Zero-example exemption must be named in the generated file's comment (enforced by plan 06's emitter) and pinned in prose |
| T-178-SC | Tampering | npm/pip/cargo installs | mitigate | No installs; any addition requires a blocking human checkpoint |
</threat_model>

<verification>
- `npm test` green.
- `build/test.md` reads as one coherent 8-item sequence end to end.
</verification>

<success_criteria>
- TEST-01 is a numbered, build-blocking step citing all four real commands and both contracts, with both dispatch payloads produced by a command rather than composed in prose.
- Renumbering is complete and guarded by a test.
- The zero-example exemption is explicit, never silent.
</success_criteria>

<output>
Create `.planning/phases/178-worked-example-tests/178-08-SUMMARY.md` when done.
</output>
