---
phase: 178-worked-example-tests
plan: 08
type: execute
wave: 8
depends_on: [178-07]
files_modified:
  - src/cli/slash-command/bs/verify-game.md
  - src/cli/slash-command/bs/verify.test.ts
autonomous: true
requirements: [CHECK-06]

must_haves:
  truths:
    - "/bs-verify-game runs worked-example replay project-wide as a new Step 8 and Close is Step 9"
    - "The check reports and exits 0 — it never gates the Close"
    - "The orchestrator's own transcript still contains no quoted-rule line; the carve-out covers only the subagent payloads"
    - "The two new contracts appear in Reference Files"
  artifacts:
    - path: "src/cli/slash-command/bs/verify-game.md"
      provides: "Step 8 (CHECK-06), Close renumbered to Step 9, two new Reference Files entries, extended Context-Economics carve-out"
      contains: "verify-example-replay"
  key_links:
    - from: "src/cli/slash-command/bs/verify-game.md"
      to: "src/cli/commands/verify-example-replay.ts"
      via: "boardsmith verify-example-replay --json"
      pattern: "verify-example-replay"
    - from: "src/cli/slash-command/bs/verify.test.ts"
      to: "src/cli/slash-command/bs/verify-game.md"
      via: "prose regression pin (PROC-01)"
      pattern: "Step 8"
---

<objective>
Wire CHECK-06 into `/bs-verify-game` as a new Step 8, renumber Close to Step 9, extend the
Context-Economics carve-out to the two new dispatch payloads, and pin all of it.

Purpose: CONTEXT decision 11 — deliberately asymmetric with the build side. On the verify side this
is an advisory sweep a verify pipeline consumes, project-wide and independent of staleness, exactly
the shape Step 7 already has. 177.1 proved a measured design with no pipeline wiring is a
reachability gap, not a delivery.
Output: an edited `verify-game.md` and its pins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-worked-example-tests/178-CONTEXT.md
@.planning/phases/178-worked-example-tests/178-07-SUMMARY.md
@./CLAUDE.md

<interfaces>
`verify-game.md`'s current structure, read at plan time:
  ## Invocation
  ## Context-Economics Hard Rule           (+ the CHECK-04 carve-out at :49-70, re-aimed at
                                            BS-ENUMERATE-V1 / BS-RECONCILE-V1 by 177.1-05)
  ## Step 0: State Detection and Lock      (its text says "A clean close (Step 8, below)" — a
                                            cross-reference that MUST be renumbered to Step 9)
  ## Step 1..6
  ## Step 7: Derived-Line Re-Check (CHECK-04)   — the shape to mirror: project-wide, independent of
                                            staleness, dispatch-then-record-then-format, exit 0
  ## Step 8: Close (VERIFY-02)             — becomes Step 9
  ## Reference Files                       — one entry per delegated contract

Step 7's own closing discipline to mirror verbatim in shape:
  "Report by formatting `boardsmith verify-derive-check --json`'s output — formatted, never
   computed by this skill."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 8 (CHECK-06) and renumber Close to Step 9</name>
  <read_first>
    - src/cli/slash-command/bs/verify-game.md (full)
    - src/cli/commands/verify-example-replay.ts (the real option surface and the report's two mismatch buckets from plan 04)
    - .planning/phases/178-worked-example-tests/178-CONTEXT.md decisions 11, 12, 16, 17
  </read_first>
  <action>
    Insert `## Step 8: Worked-Example Replay (CHECK-06)` between the current Steps 7 and 8, and
    renumber the Close section to `## Step 9: Close (VERIFY-02)`. Update Step 0's cross-reference
    ("A clean close (Step 8, below)") to Step 9, and any other numeric cross-reference to the Close.
    Step 8's content, mirroring Step 7's shape:
    - Independent of staleness and repair: it does not consume Step 4's staleness verdicts and is
      not scoped to the chunks Step 6 touched. Run `boardsmith verify-example-replay --json`
      project-wide.
    - For each slice reported pending, dispatch `slices[].extractionPayload` UNCHANGED to a subagent
      carrying `${CLAUDE_SKILL_DIR}/../bs-shared/verify/extract-example.md`'s
      `BS-EXTRACT-EXAMPLE-V1` handshake; dispatch each returned spec separately to a second subagent
      carrying `translate-example.md`'s `BS-TRANSLATE-EXAMPLE-V1` handshake. Two dispatches, never
      one combined pass.
    - Record through exactly ONE `boardsmith verify-example-record` invocation per SLICE — atomic
      upsert-append, never a whole-ledger rewrite. Provenance gating, spec validation, and
      verdict classification all happen INSIDE that command, never in this skill.
    - Execute each translated test with the project's own test runner; the verdict comes from the
      run, never from the translator's `verdictHint`.
    - Report by formatting `boardsmith verify-example-replay --json`'s output — formatted, never
      computed by this skill. Report raw counts and a per-slice breakdown, never a percentage; when
      the corpus is small, say plainly that it is too small to distinguish the mechanism working
      from luck. Report the two mismatch buckets distinctly: quotes source-verified vs. quotes NOT
      source-verified (the latter is a question about the quote, never an accusation against the
      code).
    - A slice or game with ZERO extractable examples is reported as a real finding about the ingest
      contract — examples were not transcribed — never as a tuning signal and never a reason to
      loosen extraction.
    - Findings are reported and the check exits 0. It must not be used as a gate on the Close.
    Then add two entries to `## Reference Files` for `extract-example.md` and `translate-example.md`,
    in the same one-line shape the CHECK-04 entries use, naming each contract's token and its one
    hard rule (`example-inconsistent` never picks a side; `unexecutable` requires a named reason).
    Finally, extend the Context-Economics carve-out paragraph: the extraction payload legitimately
    carries quote lines AND `Visual (p.` lines (that is its entire payload, and seven's example
    contradiction is visible only through the Visual line), while the ORCHESTRATOR's own transcript
    still shows zero quoted-rule lines. State the reviewer's observable for the new payloads
    explicitly, in the same two-observable style the CHECK-04 carve-out uses: the extraction prompt
    must contain ZERO `Derived (p.` lines — no exception applies to it.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/verify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^## Step " src/cli/slash-command/bs/verify-game.md` lists Steps 0-9 with no duplicate or missing number, Step 8 being Worked-Example Replay and Step 9 being Close.
    - `grep -n "Step 8, below" src/cli/slash-command/bs/verify-game.md` returns nothing; the Step 0 cross-reference names Step 9.
    - Step 8's text contains `verify-example-replay`, `verify-example-record`, `BS-EXTRACT-EXAMPLE-V1`, `BS-TRANSLATE-EXAMPLE-V1`, the phrase `exits 0`, and the phrase `formatted, never computed`.
    - Step 8 contains no percentage-bearing reporting instruction, and contains the zero-examples-is-a-finding sentence.
    - `## Reference Files` contains one line each for `extract-example.md` and `translate-example.md`.
    - The carve-out paragraph names the extraction prompt's zero-`Derived (p.` observable explicitly.
  </acceptance_criteria>
  <done>/bs-verify-game runs CHECK-06 as an advisory Step 8, and the Close is Step 9.</done>
</task>

<task type="auto">
  <name>Task 2: Regression pins for Step 8 and the renumbering</name>
  <read_first>
    - src/cli/slash-command/bs/verify.test.ts lines 780-870 (the 177.1-05 CHECK-04 routing describe block and the Context-Economics carve-out block — the shapes to mirror)
    - src/cli/slash-command/bs/verify.test.ts lines 985-1030 (the drift guard on named boardsmith commands)
  </read_first>
  <action>
    Add a `verify-game.md — CHECK-06 routing and Reference Files` describe block pinning: the Step 8
    heading text and its position immediately before Close; both handshake tokens; both command
    names; the exit-0/advisory sentence; the two-bucket provenance reporting; the
    zero-examples-is-a-finding sentence; the absence of any percentage instruction; and both new
    Reference Files entries. Add a step-numbering guard asserting the `## Step N:` headings parse to
    exactly 0..9 with no duplicates or gaps, and that Close is last. Extend the existing
    Context-Economics carve-out describe block with the new extraction-prompt observable.
  </action>
  <verify>
    <automated>npx vitest run src/cli/slash-command/bs/verify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The new describe block contains at least 8 assertions.
    - The step-numbering guard parses headings and asserts the sequence 0..9 with Close last; it fails if a heading is duplicated or skipped (demonstrated against a mutated fixture copy).
    - `npx vitest run src/cli/slash-command/bs/verify.test.ts -t "drift guard"` exits 0.
    - `npx vitest run src/cli/slash-command/bs/verify.test.ts` exits 0 with zero pre-existing tests removed (assert the file's total test count did not decrease).
  </acceptance_criteria>
  <done>Step 8 and the renumbering are pinned; a future silent renumber fails the suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill prose → verify pipeline behavior | Prose is the executable specification the verify orchestrator follows |
| dispatch payload → orchestrator transcript | Quote lines cross into a model context under a documented carve-out |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-20 | Tampering | step numbering | mitigate | Numbering guard test parses `## Step N:` headings and asserts 0..9 contiguous with Close last |
| T-178-21 | Information disclosure | extraction dispatch payload | mitigate | Carve-out names the zero-`Derived (p.` observable for the extraction prompt; enforced in code by plan 02's construction-site backstop |
| T-178-22 | Elevation of privilege | CHECK-06 used as a gate | mitigate | Prose states exit 0 and never-gates-the-Close; command sets no `process.exitCode` (plan 03 test) |
| T-178-SC | Tampering | npm/pip/cargo installs | mitigate | No installs; any addition requires a blocking human checkpoint |
</threat_model>

<verification>
- `npm test` green.
- `verify-game.md` reads as one coherent Step 0-9 sequence.
</verification>

<success_criteria>
- CHECK-06 is Step 8, advisory, project-wide, provenance-gated in its reporting.
- Close is Step 9 and every cross-reference agrees.
- Both contracts are in Reference Files; the carve-out covers the new payloads with a stated observable.
</success_criteria>

<output>
Create `.planning/phases/178-worked-example-tests/178-08-SUMMARY.md` when done.
</output>
