---
phase: 178-worked-example-tests
plan: 10
type: execute
wave: 10
depends_on: [178-09]
files_modified:
  - .planning/phases/178-worked-example-tests/178-PROOF.md
  - .planning/phases/178-worked-example-tests/178-PROOF/
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [CHECK-06, TEST-01]

must_haves:
  truths:
    - "Real, live model dispatches ran the extract→translate path against all three reference games"
    - "seven's defective Run example came back example-inconsistent with both contradicting excerpts, and never became a test"
    - "At least one generated example test was WRITTEN into a real game project copy and EXECUTED by that project's own vitest"
    - "Both mechanisms provably called the same derivation module (SC-3)"
    - "The reference games' originals are byte-identical after the proof run"
  artifacts:
    - path: ".planning/phases/178-worked-example-tests/178-PROOF.md"
      provides: "Raw per-game counts, per-example named outcomes against the pre-registered permitted sets, the stability result, and the honest limits statement"
    - path: ".planning/phases/178-worked-example-tests/178-PROOF/"
      provides: "The dispatch harness, every raw model return, the generated test files, and the sha256 baselines"
  key_links:
    - from: ".planning/phases/178-worked-example-tests/178-PROOF.md"
      to: ".planning/phases/178-worked-example-tests/178-PRE-REGISTRATION.md"
      via: "per-example comparison against permitted sets"
      pattern: "PRE-REGISTRATION"
---

<objective>
Run the proof: real `claude -p` dispatches of both contracts against `cp -R` copies of all three
reference games, execute the generated tests with each project's own test runner, compare every
outcome against the pre-registered permitted sets, and record the result honestly at n≈6.

Purpose: this is where the phase either works on real data or does not. Decision 17 governs the
temptation this plan will produce: a game with zero extractable examples is a real finding about the
ingest contract, never a reason to loosen extraction.
Output: `178-PROOF.md` + `178-PROOF/` evidence, and the requirement closure notes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-worked-example-tests/178-CONTEXT.md
@.planning/phases/178-worked-example-tests/178-PRE-REGISTRATION.md
@.planning/phases/178-worked-example-tests/178-09-SUMMARY.md
@./CLAUDE.md

<interfaces>
The dispatch mechanism, proven across Phase 177 and 177.1 — reuse it, do not invent a second:
  .planning/phases/177-derived-line-re-derivation/177-22-MEASUREMENT/dispatch-enum.mjs
  execFileSync('claude', ['-p', prompt, '--model', model, '--output-format', 'json'],
               { maxBuffer: 64*1024*1024, timeout: 300000, encoding: 'utf8' })
  prompt = contract file content VERBATIM + `=== DISPATCH PAYLOAD ===` + the CLI-built payload
  (native Task/Agent dispatch is not available in this environment — standing precedent, 173-PROOF §6)

Commands under proof:
  boardsmith verify-example-replay --project <copy> [--chunk <slug>] --json
  boardsmith verify-example-record --project <copy> --slice-path <p> --extraction <f> --translation <f>
  boardsmith verify-example-emit   --project <copy> --chunk <slug>

Target projects (COPIES ONLY):
  ~/BoardSmithGames/{seven,one-two-punch,doom-machine}   — each has `"test": "vitest run"`,
  vitest ^2.0.0, and node_modules/boardsmith symlinked to this repo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage copies, dispatch extraction live, record verdicts</name>
  <read_first>
    - .planning/phases/178-worked-example-tests/178-PRE-REGISTRATION.md (all four sections — the bar this run is measured against)
    - .planning/phases/177-derived-line-re-derivation/177-22-MEASUREMENT/dispatch-enum.mjs (the dispatch harness to adapt)
    - src/cli/slash-command/bs/verify/extract-example.md
    - src/cli/commands/verify-example-replay.ts (the exact record CLI option surface)
  </read_first>
  <action>
    Under `.planning/phases/178-worked-example-tests/178-PROOF/`, take sha256 baselines of all three
    original game trees using the command recorded in the pre-registration's Section 4, then
    `cp -R` each game to a staging directory. All subsequent work touches ONLY the copies.
    Build `178-PROOF/dispatch-example.mjs` by adapting the 177-22 harness: it takes a contract path,
    a payload file, and a model id, and writes the raw return to disk unmodified. Run
    `boardsmith verify-example-replay --project <copy> --json` for each copy, and dispatch every
    pending slice's `extractionPayload` bytes UNCHANGED with the `BS-EXTRACT-EXAMPLE-V1` contract.
    Save every raw return under `178-PROOF/returns/<game>/<slice>.extract.json`.
    Do not hand-edit any return. If a return is malformed, record that as a result — the CLI's
    JSON.parse path either accepts or rejects a return outright; there is no repair utility on the
    product side and none is to be introduced here (177.1-03's recorded decision).
    Then record each slice through exactly one `verify-example-record` invocation. Where the record
    command rejects a return, record the rejection verbatim as a finding rather than adjusting the
    return to fit.
  </action>
  <verify>
    <automated>ls .planning/phases/178-worked-example-tests/178-PROOF/returns/*/*.extract.json | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `178-PROOF/BASELINES.sha256` exists and covers all three original game trees.
    - At least one raw `.extract.json` return exists per game, each containing the model's unmodified output (no hand edits — assert by including the harness's own write path in the file header comment).
    - `boardsmith verify-example-replay --project <seven copy> --json` shows a recorded verdict for `01-definitions-and-components.md` line 12 with verdict `example-inconsistent` AND both `contradictionA` and `contradictionB` non-empty, one quoting the printed `5, 6, 7` text and one quoting the card-image `1, 2, 3` evidence.
    - Every rejection by `verify-example-record`, if any, is captured verbatim in `178-PROOF/REJECTIONS.md`.
    - `git status --porcelain ~/BoardSmithGames` (or an equivalent check against `BASELINES.sha256`) shows the originals untouched at this point.
  </acceptance_criteria>
  <done>Live extraction ran on real slices; the adversarial fixture was caught with both sides of its contradiction recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch translation, emit and EXECUTE the generated tests, measure stability</name>
  <read_first>
    - src/cli/slash-command/bs/verify/translate-example.md
    - src/cli/commands/example-test-emit.ts (generatedTestFilePath, the sandbox gate)
    - .planning/phases/178-worked-example-tests/178-PRE-REGISTRATION.md Section 3 (the stability definition)
    - ~/BoardSmithGames/one-two-punch/tests/punch.test.ts (the target idiom the generated test must match to run at all)
  </read_first>
  <action>
    For every spec recorded in Task 1, dispatch the `BS-TRANSLATE-EXAMPLE-V1` contract with
    `buildExampleTranslationPayload`'s bytes, saving raw returns under
    `178-PROOF/returns/<game>/<example>.translate.json`. Then run
    `boardsmith verify-example-emit --project <copy> --chunk <slug>` and EXECUTE the emitted file
    with the copy's own runner: `npx vitest run tests/examples/<slug>.examples.test.ts` inside the
    copy. Never `eval`, never `new Function`, never in-process execution of returned code — a
    malformed or hostile generation must fail as an ordinary test failure.
    Record each example's PASS/FAIL outcome. Then measure stability per the pre-registration's
    Section 3: repeat the full extract→translate→emit→execute path a SECOND time, independently, for
    every example, and compare the two runs on PASS/FAIL outcome only — not on spec text, not on
    generated code text. Record every outcome pair.
    Capture the byte content of at least one generated test file into `178-PROOF/generated/` so a
    reader can see what the mechanism actually produced.
  </action>
  <verify>
    <automated>ls .planning/phases/178-worked-example-tests/178-PROOF/generated/*.test.ts && grep -c "PASS\|FAIL" .planning/phases/178-worked-example-tests/178-PROOF/OUTCOMES.md</automated>
  </verify>
  <acceptance_criteria>
    - At least one generated test file from `one-two-punch` exists in `178-PROOF/generated/` and its recorded execution output shows a real `vitest` result (the raw runner output is captured, not summarized).
    - `178-PROOF/OUTCOMES.md` lists, per example: game, slice, line, kind, run-1 verdict, run-1 test PASS/FAIL, run-2 verdict, run-2 test PASS/FAIL.
    - Every `unexecutable` outcome carries its named reason verbatim from the model return.
    - The stability comparison is on PASS/FAIL pairs only; the file states explicitly that differing spec text with agreeing outcomes was counted as stable.
    - No generated test was executed by any means other than the copy's own `vitest run` — assert by including the exact commands run.
  </acceptance_criteria>
  <done>Generated tests were written into a real project copy and executed by its own runner, twice, with outcomes recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Write 178-PROOF.md, prove SC-3, restore-verify the originals, close the requirements</name>
  <read_first>
    - .planning/phases/178-worked-example-tests/178-PROOF/OUTCOMES.md (Task 2's output)
    - .planning/phases/178-worked-example-tests/178-PRE-REGISTRATION.md (Sections 1-3)
    - .planning/REQUIREMENTS.md (CHECK-06 line 528, TEST-01 line 533, and the status table rows near line 585)
    - .planning/ROADMAP.md Phase 178 (goal, the three success criteria, the Plans line)
  </read_first>
  <action>
    Write `.planning/phases/178-worked-example-tests/178-PROOF.md` with: the raw per-game counts and
    a per-example outcome table compared row-by-row against the pre-registered permitted sets,
    marking each IN-SET or OUT-OF-SET; the stability result stated on PASS/FAIL outcomes; and an
    explicit limits paragraph stating the corpus size, that no percentage is reported, and — in the
    177 closure note's own register — that three games is real generalization evidence, not a
    general result. Any OUT-OF-SET result is reported as-is with its raw return quoted; it is never
    resolved by loosening the mechanism or by editing the pre-registration.
    Prove SC-3 mechanically: add a source-inspection test (in `example-derivation.test.ts`) asserting
    that both call paths reach the same module — the build path via `example-test-emit.ts` and the
    verify path via `verify-example-replay.ts` both import `buildExampleExtractionPayload` and
    `buildExampleTranslationPayload` from `example-derivation.ts`, and that neither module declares
    its own payload builder. Cite the test name in the proof.
    Re-verify the originals against `BASELINES.sha256` and record the comparison output in the proof.
    Then update `.planning/REQUIREMENTS.md`: mark CHECK-06 and TEST-01 with their closure notes in
    the established shape (what was built, what was measured on real data, what is honestly NOT
    proven at n≈6, and any finding surfaced about the ingest contract per decision 17), and update
    their rows in the status table. Update `.planning/ROADMAP.md` Phase 178's `**Plans:**` line to
    the real plan count and list.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `178-PROOF.md` contains a per-example table with an IN-SET / OUT-OF-SET column for every pre-registered example, and no percentage anywhere.
    - It contains the sha256 re-comparison output showing the three originals unchanged.
    - It names the SC-3 source-inspection test by file and test name, and that test passes.
    - `npm test` reports 0 failing and a test count greater than or equal to the 4125 baseline.
    - `.planning/REQUIREMENTS.md` CHECK-06 and TEST-01 entries carry dated closure notes citing `178-PROOF.md`, and their status-table rows are updated.
    - `.planning/ROADMAP.md` Phase 178's `**Plans:**` line names the real plan files.
  </acceptance_criteria>
  <done>The phase's claim is written down with its evidence and its honest limits, and the requirements carry their closure notes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Worked examples now become executable tests on both sides of the bs pipeline: `build/test.md`
    generates them for a newly-built chunk (build-blocking on mismatch), and `/bs-verify-game`
    Step 8 replays them project-wide (advisory, exit 0). Both call the same derivation module.
    Live dispatches ran against all three reference games; `seven`'s defective Run example was
    caught as `example-inconsistent` rather than becoming a passing test that encodes a
    contradiction.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/178-worked-example-tests/178-PROOF.md` — check the per-example table
       against `178-PRE-REGISTRATION.md`'s permitted sets, and read the limits paragraph.
    2. Open `.planning/phases/178-worked-example-tests/178-PROOF/generated/` and read a real
       generated test file. Does it read like a test of the printed example, or like a restatement
       of the code?
    3. Confirm the `seven` Run example's `example-inconsistent` record quotes BOTH sides of the
       contradiction and picks neither.
    4. Confirm anything reported OUT-OF-SET is reported plainly rather than explained away.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what should change.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live model output → real game project copy | Generated code is written into a project and executed by a test runner |
| proof run → reference game originals | A proof that mutated the originals would corrupt every later measurement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-25 | Tampering | reference game originals | mitigate | sha256 baselines before, `cp -R` staging, byte-identical re-verification after, output recorded in the proof |
| T-178-26 | Tampering | generated test execution | mitigate | Executed only via the copy's own `npx vitest run <path>`; never `eval`/`new Function`; sandbox-scanned at emit time (plan 05) |
| T-178-27 | Repudiation | result interpretation | mitigate | Every outcome compared against a pre-committed permitted set; OUT-OF-SET results quoted verbatim, never resolved by editing the expectation |
| T-178-SC | Tampering | npm/pip/cargo installs | mitigate | No installs; any addition requires a blocking human checkpoint |
</threat_model>

<verification>
- `npm test` green at or above the 4125-test baseline, 0 failing.
- Originals byte-identical against `BASELINES.sha256`.
- No dev server or background process left running (project rule).
</verification>

<success_criteria>
- Live proof on all three reference games with raw returns preserved.
- The `seven` adversarial fixture caught as `example-inconsistent` with both excerpts.
- At least one generated test written into a real project copy and executed by its own runner, twice.
- SC-3 proven by a source-inspection test, not by assertion.
- CHECK-06 and TEST-01 closed with dated notes and honest limits.
</success_criteria>

<output>
Create `.planning/phases/178-worked-example-tests/178-10-SUMMARY.md` when done.
</output>
