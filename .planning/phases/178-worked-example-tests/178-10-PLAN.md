---
phase: 178-worked-example-tests
plan: 10
type: execute
wave: 10
depends_on: [178-09]
files_modified:
  - .planning/phases/178-worked-example-tests/178-PRE-REGISTRATION.md
autonomous: true
requirements: [CHECK-06, TEST-01]

must_haves:
  truths:
    - "The expected extraction is written down, per game, by slice and line, BEFORE any model is dispatched"
    - "Every acceptance criterion has a written answer to 'could a correct implementation ever fail this for reasons outside its control?'"
    - "Stability is defined on the generated test's PASS/FAIL outcome, never on byte-identical spec text"
    - "No criterion is expressed as a percentage or as an N-of-M bar over a single game"
  artifacts:
    - path: ".planning/phases/178-worked-example-tests/178-PRE-REGISTRATION.md"
      provides: "The committed, pre-dispatch expectation and the satisfiability audit"
      contains: "could a correct implementation ever fail this"
  key_links:
    - from: ".planning/phases/178-worked-example-tests/178-PRE-REGISTRATION.md"
      to: ".planning/phases/178-worked-example-tests/178-01-MEASUREMENT/RESULTS.md"
      via: "the mechanical line inventory the expectation is built on"
      pattern: "178-01-MEASUREMENT"
---

<objective>
Write and commit the pre-dispatch expectation for the proof run, ALONE, before any `claude -p`
dispatch happens — so git ordering proves the expectation was not retrofitted to the result.

Purpose: CONTEXT decisions 13-17. CHECK-04 spent four full definitive measurement runs failing a
single criterion that was unsatisfiable by construction. Decision 14 exists to catch that at
pre-registration time rather than after the fourth run. This plan's entire value is that it happens
FIRST and separately.
Output: `178-PRE-REGISTRATION.md`, committed as its own commit with no other file in it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-worked-example-tests/178-CONTEXT.md
@.planning/phases/177.1-wire-check04-closed-design/177.1-PRE-REGISTRATION.md
@./CLAUDE.md

<corpus>
The candidate corpus, from `<measured_reality>` #2-4 and re-verified at plan time — the starting
inventory this pre-registration must confirm or correct by direct read, never inherit on faith:

  seven/rulebook/01-definitions-and-components.md:6   `"example: 5, 5, 5"`      (Set — predicate)
  seven/rulebook/01-definitions-and-components.md:12  `"example: 5, 6, 7"`      (Run — predicate,
      DEFECTIVE IN SOURCE: line 13's Visual line records the card images show 1, 2, 3)
  one-two-punch/rulebook/02-action-cards-and-resolution.md:84-91  two `p.2, Punch Examples (italic):`
      entries (transition)
  doom-machine/rulebook/01-destroying-a-machine-part.md:13  `Worked example content (p.1, panel -7-,
      verbatim from card art):` (image-derived)
  doom-machine/rulebook/02-machine-phase.md:15  the SOUL HARVESTER example, present only as a
      `Diagram description` line

n ≈ 6 across three games. An order of magnitude smaller than CHECK-04's 32 lines, and not the same
kind of thing.
</corpus>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the expected extraction, per game, by slice and line</name>
  <read_first>
    - .planning/phases/178-worked-example-tests/178-01-MEASUREMENT/RESULTS.md
    - ~/BoardSmithGames/seven/rulebook/01-definitions-and-components.md
    - ~/BoardSmithGames/one-two-punch/rulebook/02-action-cards-and-resolution.md
    - ~/BoardSmithGames/doom-machine/rulebook/01-destroying-a-machine-part.md and 02-machine-phase.md
    - ~/BoardSmithGames/seven/rulebook/INDEX.md line 63 (open gap #4 — the recorded Run-example discrepancy)
  </read_first>
  <action>
    Create `.planning/phases/178-worked-example-tests/178-PRE-REGISTRATION.md`. Section 1 is the
    expected extraction: walk EVERY slice of all three reference games (not only the five candidates
    above — the point is to state what the mechanism should find everywhere, including "nothing
    here") and record a table with columns: game, slice path, line number, verbatim first line of
    the example, expected `kind`, and the PERMITTED SET of outcomes for it. A permitted set, never a
    single predicted label — 177.1's pre-registration established this shape because a single
    prediction turns ordinary model variance into a false failure.
    Assign per-example permitted sets explicitly, with the reason:
    - one-two-punch's two Punch Examples: `{agrees, disagrees, unexecutable}` — a `disagrees` here
      would be a genuine finding about the code, and `unexecutable` is legitimate if the chunk does
      not model Guard exhaustion yet.
    - seven's Set example: `{agrees, disagrees, unexecutable}`.
    - seven's Run example: `{example-inconsistent}` ONLY — this is the phase's designated adversarial
      fixture and the one place a single-label expectation IS justified, because the contradiction is
      printed in the slice itself and any other verdict means the mechanism failed to notice it.
      State that explicitly as the reason.
    - doom-machine's two: `{unexecutable, example-inconsistent, agrees, disagrees}` — image-derived,
      genuinely open.
    Record the corpus size as a raw count, and state plainly that at this n a percentage is
    meaningless (decision 16) and that a per-example named outcome table is the report.
    Also state decision 17's rule up front: a game returning zero extractable examples is recorded
    as a real finding about the ingest contract, and extraction is NEVER loosened in response.
  </action>
  <verify>
    <automated>grep -c "one-two-punch\|doom-machine\|seven" .planning/phases/178-worked-example-tests/178-PRE-REGISTRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The expected-extraction table covers every `rulebook/*.md` slice in all three games (excluding `INDEX.md` and `00-visual-survey.md`), including rows asserting "no worked example expected" — the slice count in the table matches the count `readLiveSlices` would return for each game, and that count is stated.
    - Every row carries a permitted SET, not a single label, except seven's Run example, whose single-label expectation carries a written justification.
    - Every candidate line is quoted verbatim with its 1-based line number, verified by direct read of the slice file.
    - The file contains no percentage anywhere and states the raw corpus count.
    - The file states decision 17's zero-examples rule verbatim in substance.
  </acceptance_criteria>
  <done>What the mechanism should find is written down before any model has looked at it.</done>
</task>

<task type="auto">
  <name>Task 2: The satisfiability audit — every criterion checked against "could this ever pass?"</name>
  <read_first>
    - .planning/phases/178-worked-example-tests/178-CONTEXT.md decisions 14, 15, 16
    - .planning/REQUIREMENTS.md the CHECK-04 entry's four amendments (the four runs that failed an unsatisfiable criterion — the cost being avoided here)
    - .planning/phases/178-worked-example-tests/178-RESEARCH.md Pitfall 5
    - .planning/ROADMAP.md Phase 178's three success criteria
  </read_first>
  <action>
    Section 2 of `178-PRE-REGISTRATION.md`: the acceptance bar, one criterion per row, each with
    FOUR columns — the criterion, how it is measured (the exact command or observable), the
    satisfiability audit answer to "could a correct implementation ever fail this for reasons
    outside its control?", and the disposition (KEEP / REWRITE / REJECT). Any criterion whose audit
    answer is anything other than a clear "no" is REWRITTEN or REJECTED in this document, before it
    is committed — not after a measurement run fails it.
    Derive the criteria from the ROADMAP's three success criteria plus the phase's own decisions.
    At minimum, audit these explicitly:
    - "every worked example in a newly-built chunk's cited slices produces an executable test" —
      audit against `unexecutable` being a legitimate outcome; rewrite to "produces either an
      executable test or a recorded `unexecutable` with a named reason."
    - "the seven Run example is never turned into a test" — audit: passes trivially if extraction
      finds nothing at all, so strengthen it to require the `example-inconsistent` verdict WITH both
      contradicting excerpts recorded.
    - Any N-of-M bar over a single game — REJECT per 178-RESEARCH Pitfall 5 (M ≤ 3 makes it fragile
      by construction); replace with per-example named outcomes.
    Section 3: the stability definition. Stability is measured on the generated test's PASS/FAIL
    OUTCOME across two independent extraction+translation runs of the SAME example — never on
    byte-identical spec text. Two runs phrasing an example differently while producing tests that
    agree on pass/fail is a SUCCESS, not a flip. State explicitly that the retired CHECK-04
    determinism gate is NOT re-imported (decision 15), and state what WOULD count as a real
    stability failure: the same example's generated test passing in one run and failing in the
    other, with the code unchanged.
    Section 4: the staging discipline (decision 18) — proof runs on `cp -R` copies with sha256
    baselines taken before and compared after; originals proven byte-identical. Record the exact
    baseline command to be used.
  </action>
  <verify>
    <automated>grep -q "could a correct implementation ever fail this" .planning/phases/178-worked-example-tests/178-PRE-REGISTRATION.md && grep -q "sha256" .planning/phases/178-worked-example-tests/178-PRE-REGISTRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Section 2 is a table where every row has all four columns filled, and at least one row carries disposition REWRITE or REJECT with the rewritten wording shown.
    - No criterion in the final KEEP set is expressed as a percentage or as an N-of-M threshold over fewer than 4 items.
    - Section 3 defines stability on PASS/FAIL outcome, names decision 15 explicitly, and states what a real stability failure looks like.
    - Section 4 records the literal `cp -R` and sha256 baseline commands to be run.
    - The file is committed as its own commit containing no other changed file — verify with `git show --stat HEAD` naming exactly one path.
  </acceptance_criteria>
  <done>Every criterion has been checked for satisfiability before it can cost a measurement run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| expectation document → proof interpretation | A retrofitted expectation would make any result look predicted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-23 | Repudiation | pre-registration timing | mitigate | Committed alone, before plan 10's dispatches; `git show --stat HEAD` asserts a single-file commit |
| T-178-24 | Tampering | acceptance criteria | mitigate | Satisfiability audit column forces every criterion to be checkable before it is adopted |
| T-178-SC | Tampering | npm/pip/cargo installs | mitigate | No installs; any addition requires a blocking human checkpoint |
</threat_model>

<verification>
- `git show --stat HEAD` shows exactly `178-PRE-REGISTRATION.md`.
- No `claude -p` dispatch has occurred in this plan.
</verification>

<success_criteria>
- A committed, pre-dispatch, per-example expectation with permitted sets.
- Every criterion audited for satisfiability, with at least one rewritten or rejected on that basis.
- Stability defined on executable outcome; the retired determinism gate explicitly not re-imported.
</success_criteria>

<output>
Create `.planning/phases/178-worked-example-tests/178-10-SUMMARY.md` when done.
</output>
