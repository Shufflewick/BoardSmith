---
phase: 179-source-free-verification-mode
plan: 02
type: execute
wave: 2
depends_on: [179-01]
files_modified:
  - src/cli/commands/verify-source-free.ts
  - src/cli/commands/verify-source-free.test.ts
  - src/cli/cli.ts
autonomous: true
requirements: [VERIFY-09]

must_haves:
  truths:
    - "A designer can run one read-only command against a source-free project and get the mode, the reduced scope with its reason, and the unchecked defect classes."
    - "That command exits 0 on a source-free project — a reduced pass is a success, not a failure."
    - "Every number and every string in the eventual report comes from this command's --json, never from skill arithmetic."
  artifacts:
    - path: "src/cli/commands/verify-source-free.ts"
      provides: "verifySourceFreeCheckCommand — human + --json renderer over computeSourceFreeReport"
      exports: ["verifySourceFreeCheckCommand"]
    - path: "src/cli/cli.ts"
      provides: "verify-source-free-check registration"
      contains: "verify-source-free-check"
  key_links:
    - from: "src/cli/cli.ts"
      to: "src/cli/commands/verify-source-free.ts"
      via: ".command('verify-source-free-check').action(verifySourceFreeCheckCommand)"
      pattern: "verify-source-free-check"
    - from: "verify-source-free-check --json"
      to: "renderVerifiedAgainst"
      via: "the same code-conformance-only scope + reason PROV-02 already durably records"
      pattern: "code-conformance-only"
---

<objective>
Expose plan 01's core as the one read-only CLI surface the skill will format, and prove — through
the real provenance renderer, not a doc comment — that a source-free project's durable
`## Verified Against` block reads `code-conformance-only` with the unavailable-source reason.

Purpose: SC-3 is a provenance claim. 178's review found three "criticals that were true in prose and
false in the data flow"; this plan asserts the data flow.
Output: `verify-source-free-check` registered and exercised end-to-end through the real entry point.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/179-source-free-verification-mode/179-CONTEXT.md
@.planning/phases/179-source-free-verification-mode/179-01-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
From `src/cli/commands/verify-source-free.ts` (plan 01):
- `computeSourceFreeReport(projectDir: string): Promise<SourceFreeReport>`
- `VERIFY_PIPELINE_STEPS`, `SOURCE_FREE_ADDITIONAL_CHECKS`

From `src/cli/commands/chunk-provenance.ts`:
- `renderVerifiedAgainst(record: VerifiedAgainstRecord): string` (:~350) — emits `Scope:` always and
  `Reason:` only when `scope === SCOPE_CODE_ONLY`
- `VERIFIED_AGAINST_LABELS` — the frozen label tuple (`Scope:`, `Reason:`, …)

House pattern for read commands (`src/cli/cli.ts` :436 `verify-derive-check`, :484
`verify-example-replay`): `--project <dir>`, `--json`, read-only, findings exit 0.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: verify-source-free-check command + registration</name>
  <files>src/cli/commands/verify-source-free.ts, src/cli/cli.ts</files>
  <read_first>
    - `src/cli/commands/verify-derive-check.ts` — the read-command shape (options, `--json` branch, human renderer, exit code discipline)
    - `src/cli/cli.ts` lines 420-500 — the registration block and its no-bypass-option comments
    - `src/cli/commands/verify-source-free.ts` (plan 01)
  </read_first>
  <behavior>
    - `boardsmith verify-source-free-check --project <dir> --json` prints one JSON object containing `sourceFree`, `scope`, `reason`, `stepsRun[]`, `stepsSkipped[]`, `uncheckedDefectClasses[]`, `checksRun[]`.
    - Exit code is 0 on a source-free project, and 0 on a full-scope project. Non-zero is reserved for tool failure (unreadable project dir).
    - Human output on a source-free project names the mode, the reason, and each unchecked defect class with its responsible check on one line each.
    - Human output on a full-scope project says plainly that the pass is full scope and lists no unchecked classes.
  </behavior>
  <action>
    Add `verifySourceFreeCheckCommand(options: { project?: string; json?: boolean })` to
    `verify-source-free.ts`, delegating entirely to `computeSourceFreeReport`. The command renders;
    it computes nothing.

    Register in `src/cli/cli.ts` as `verify-source-free-check` with `--project <dir>` and `--json`,
    following the `verify-derive-check` registration exactly. Add a registration comment stating
    that this command has NO `--source-free`, `--force`, `--assume-full`, or equivalent option —
    decision 1: source-free is entered from disk state, never declared — and that its exit code is
    always 0 absent a tool failure, because a reduced pass is a successful pass.

    Resolve `--project` with `resolve()` before any filesystem access, matching the sibling
    commands, so a relative path is normalised once at the boundary.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-source-free.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "verify-source-free-check" src/cli/cli.ts` returns a `.command(` registration line.
    - `grep -v '^\s*//' src/cli/cli.ts | grep -c -- "--assume-full\|--source-free\|--force-scope"` returns 0.
    - A test invokes `verifySourceFreeCheckCommand({ project: fixtureDir, json: true })` against a source-free fixture and parses stdout as JSON containing `sourceFree: true` and a non-empty `uncheckedDefectClasses`.
    - A test asserts the command does not call `process.exit` with a non-zero code on a source-free fixture.
  </acceptance_criteria>
  <done>The command is reachable through the real CLI entry point and exits 0 on the reduced path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PROV-02 data-flow integration test — the durable block, not the doc comment</name>
  <files>src/cli/commands/verify-source-free.test.ts</files>
  <read_first>
    - `src/cli/commands/chunk-provenance.ts` lines 290-380 — `VERIFIED_AGAINST_LABELS`, `VerifiedAgainstRecord`, `renderVerifiedAgainst`
    - `src/cli/commands/chunk-provenance.test.ts` — existing scope/reason fixture patterns to reuse rather than fork
    - `.planning/phases/178-worked-example-tests/178-PROOF.md` §12 — three guarantees that were true in prose and false in the data flow
  </read_first>
  <behavior>
    - On a fixture project in the exact source-free state (`rulebook/INDEX.md` records a `Source:` path and `Source hash:`, no file at that path, no candidate at project root), `computeVerificationScope` returns `code-conformance-only` + `source-missing`, and `renderVerifiedAgainst` of a record built from it emits BOTH a `Scope: code-conformance-only` line and a `Reason: source-missing` line.
    - The same fixture's `verify-source-free-check --json` reports the SAME `scope` and `reason` values — one project state, one answer, two surfaces that cannot disagree.
    - On a full-scope fixture, the rendered block emits no `Reason:` line at all.
  </behavior>
  <action>
    Add a `describe('PROV-02 data flow — source-free project')` block to
    `verify-source-free.test.ts`. Build the fixture on disk in a temp dir; assert against
    `renderVerifiedAgainst`'s real output string, not against a hand-built expectation of what it
    should contain.

    Cross-assert the two surfaces: read `scope`/`reason` from `computeSourceFreeReport` and from
    `computeVerificationScope` and assert equality, so a future divergence between the run report
    and the durable block fails here rather than in a live proof run.

    Cover the second real source-free reason too: a fixture whose `INDEX.md` has no `Source hash:`
    line at all yields `pre-provenance-project`, and the report still reads `sourceFree: true` —
    both reasons must reach the same mode, since a project that never had provenance is as
    source-free as one that lost it.
  </action>
  <verify>
    <automated>npx vitest run src/cli/commands/verify-source-free.test.ts src/cli/commands/chunk-provenance.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts `renderVerifiedAgainst(...)` output contains the literal substring `code-conformance-only` AND a line beginning `Reason:` for the source-missing fixture.
    - A test asserts the full-scope fixture's rendered output contains no line beginning `Reason:`.
    - A test asserts `computeSourceFreeReport(dir).reason === (await computeVerificationScope(dir)).reason` for both source-free fixtures.
    - Both `source-missing` and `pre-provenance-project` fixtures return `sourceFree: true`.
  </acceptance_criteria>
  <done>SC-3's provenance claim is asserted through the real renderer on a real fixture, not asserted in prose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv -> `--project <dir>` | Untrusted path string crosses into filesystem reads |
| command output -> skill formatting | The skill trusts this JSON entirely; a wrong field silently becomes a wrong report |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-04 | Tampering | `--project` path handling | mitigate | `resolve()` at the boundary before any read, matching sibling read commands; read-only command performs no writes at all |
| T-179-05 | Repudiation | scope/reason disagreement between run report and durable block | mitigate | Task 2 cross-asserts both surfaces derive from the single `computeVerificationScope` call |
| T-179-06 | Elevation of Privilege | a scope-declaring option | mitigate | No `--assume-full`/`--source-free`/`--force-scope` option exists; asserted by task 1's grep criterion |
| T-179-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages |
</threat_model>

<verification>
- `npx vitest run src/cli/commands/verify-source-free.test.ts` green.
- `node dist/cli/cli.js verify-source-free-check --help` (after `npm run build`, or the repo's usual CLI invocation) lists the command with `--project` and `--json` and no scope-declaring option.
- `npm test` shows zero failing against the 4322 / 247 baseline.
</verification>

<success_criteria>
- One read-only command, exit 0, registered through the real entry point.
- SC-3's provenance path proven by assertion on rendered output.
</success_criteria>

<output>
Create `.planning/phases/179-source-free-verification-mode/179-02-SUMMARY.md` when done.
</output>
