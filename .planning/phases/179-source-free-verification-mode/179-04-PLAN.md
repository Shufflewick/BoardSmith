---
phase: 179-source-free-verification-mode
plan: 04
type: execute
wave: 4
depends_on: [179-03]
files_modified:
  - .planning/phases/179-source-free-verification-mode/179-PRE-REGISTRATION.md
autonomous: true
requirements: [VERIFY-09]

must_haves:
  truths:
    - "Every criterion the proof will be judged against is written down BEFORE any measurement, and committed alone."
    - "Each criterion has been audited for satisfiability — could a correct implementation fail it for reasons outside its control?"
    - "No criterion can pass VACUOUSLY (on zero findings, zero examples, or zero dispatches)."
    - "The staging and baseline protocol is whole-tree, and its exact commands are named in advance."
  artifacts:
    - path: ".planning/phases/179-source-free-verification-mode/179-PRE-REGISTRATION.md"
      provides: "the pre-registered bar, the satisfiability audit, and the staging protocol"
      min_lines: 60
  key_links:
    - from: "179-PRE-REGISTRATION.md"
      to: "ROADMAP Phase 179 SC-1/SC-2/SC-3 + CONTEXT decision 11(a)-(d)"
      via: "one audited row per criterion"
      pattern: "satisfiab"
    - from: "179-PRE-REGISTRATION.md"
      to: "git status --short"
      via: "whole-tree baseline protocol, never an enumerated subset"
      pattern: "git status --short"
---

<objective>
Commit the expectation before measuring it. Write the bar for plan 05's live proof — the three
ROADMAP success criteria plus decision 11's four things — and audit every one of them for
satisfiability and for vacuous passage, then commit that document ALONE.

Purpose: Phase 178's identical audit caught two ROADMAP criteria unsatisfiable as literally written
and one that would have passed vacuously on zero extraction. This plan pays that cost again on
purpose.
Output: `179-PRE-REGISTRATION.md`, committed by itself, before any staging or dispatch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/179-source-free-verification-mode/179-CONTEXT.md
@.planning/ROADMAP.md
@.planning/phases/179-source-free-verification-mode/179-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the pre-registration with the satisfiability audit applied to every criterion</name>
  <files>.planning/phases/179-source-free-verification-mode/179-PRE-REGISTRATION.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — "### Phase 179" goal and its three success criteria, verbatim
    - `.planning/phases/179-source-free-verification-mode/179-CONTEXT.md` decisions 9, 10, 11, 12
    - `.planning/phases/178-worked-example-tests/178-10-PLAN.md` and the resulting pre-registration doc — the audit format to reuse rather than reinvent
    - `.planning/phases/178-worked-example-tests/178-PROOF.md` §10 — the enumerated-subset baseline failure this protocol must not inherit
  </read_first>
  <action>
    Create `.planning/phases/179-source-free-verification-mode/179-PRE-REGISTRATION.md`.

    Section 1 — the criteria, verbatim: ROADMAP SC-1, SC-2, SC-3 quoted exactly as written, plus
    decision 11's (a) run completes, (b) unchecked list names real defect classes, (c) provenance
    records `code-conformance-only` with the unavailable-source reason, (d) the checks that DID run
    produced REAL FINDINGS.

    Section 2 — the satisfiability audit, one row per criterion, answering three questions
    explicitly: could a CORRECT implementation fail this for reasons outside its control? Could this
    pass VACUOUSLY (zero findings, zero slices, zero dispatches)? Is the criterion measurable from
    an artifact this run will actually produce? Where a criterion is unsatisfiable or vacuous as
    literally written, say so plainly and record the RESTATED operational bar that will be measured
    instead — do not silently substitute it. State the verdict for each of the three ROADMAP SCs
    explicitly, since the phase brief requires this audit be applied to THIS phase's SCs and said so.

    Section 3 — decision 11(d)'s bar, made non-vacuous and named in advance. State the minimum real
    findings required from the checks that run, per check: `trace-check` (CHECK-03) and
    `drift-check` (CHECK-05) each report at least one real finding on the staged copy, and at least
    one CHECK-06 (`verify-example-replay`) finding is recorded through a REAL live dispatch on the
    staged copy with its provenance downgraded because no archived source exists — the specific
    degradation decision 3 promises. Record what will be reported if a check returns zero findings:
    reported plainly as a zero, never as a pass, and never as a reason to loosen the check.

    Section 4 — the staging and baseline protocol, with exact commands. Name which reference game
    will be staged and why. State: `cp -R` the game to a working copy; delete `rulebook/source/`
    from the COPY; ALSO delete any candidate source file at the copy's project root, because the
    negative case requires no candidate at `rulebook/source/` AND none at the root — a copy that
    still has `rules.pdf` at root lands in Case 2, not the negative case, and would prove nothing.
    Record the exact removals. Baseline the ORIGINAL with `git status --short` run in the original's
    own repo (or a full-tree hash), before and after — WHOLE TREE, never an enumerated path list,
    per decision 10 and 178-PROOF §10. State that "N/N files OK" over an enumerated set is not an
    acceptable substitute and why.

    Section 5 — the expected outcome, stated before measuring: which `ScopeReason` value is
    expected on this staged copy and why, how many `uncheckedDefectClasses` entries are expected
    (from `VERIFY_PIPELINE_STEPS`' skipped steps), and the expected exit code (0).

    Do NOT run any command against a staged copy in this plan. No measurement, no dispatch, no
    staging — those all belong to plan 05, AFTER this document is committed.
  </action>
  <verify>
    <automated>test -f .planning/phases/179-source-free-verification-mode/179-PRE-REGISTRATION.md &amp;&amp; grep -c "git status --short" .planning/phases/179-source-free-verification-mode/179-PRE-REGISTRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The document quotes ROADMAP SC-1, SC-2, SC-3 verbatim and lists decision 11's (a)-(d).
    - Section 2 contains one audit row per criterion (7 total), each answering the unsatisfiable question and the vacuous-pass question with an explicit verdict, and each ROADMAP SC's verdict is stated by name.
    - Section 3 names a per-check minimum-findings bar including at least one REAL live CHECK-06 dispatch with downgraded quote provenance.
    - Section 4 contains the literal string `git status --short` and an explicit statement that an enumerated-path baseline is not acceptable.
    - Section 5 names an expected `ScopeReason` value and an expected exit code of 0.
    - No file under any `~/BoardSmithGames` path was created, modified, or copied by this plan.
  </acceptance_criteria>
  <done>The bar exists in writing, audited, before anything is measured.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the pre-registration ALONE</name>
  <files>.planning/phases/179-source-free-verification-mode/179-PRE-REGISTRATION.md</files>
  <read_first>
    - `.planning/phases/179-source-free-verification-mode/179-PRE-REGISTRATION.md` (task 1's output)
    - Recent git history for the milestone's pre-registration commit-message convention (e.g. `git log --oneline -12`)
  </read_first>
  <action>
    Stage ONLY `.planning/phases/179-source-free-verification-mode/179-PRE-REGISTRATION.md` and
    commit it by itself, with a message stating it is the pre-dispatch expectation for Phase 179's
    source-free proof, committed alone before any staging or measurement.

    Verify the commit contains exactly one file. If the working tree carries other uncommitted
    changes, leave them uncommitted — do not amend, do not bundle. A pre-registration committed
    alongside implementation is not a pre-registration.
  </action>
  <verify>
    <automated>git show --stat --name-only --format= HEAD | grep -v '^$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git show --name-only --format= HEAD` lists exactly one path: `.planning/phases/179-source-free-verification-mode/179-PRE-REGISTRATION.md`.
    - The commit message names it as a pre-dispatch expectation committed alone.
    - `git log -1 --format=%H` recorded in the SUMMARY, so plan 05's proof can cite the commit that predates its measurements.
  </acceptance_criteria>
  <done>The expectation is on the record, alone, and its commit hash is citable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| expectation -> measurement | A bar written after seeing results is not a bar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-10 | Repudiation | post-hoc bar adjustment | mitigate | Committed alone before any staging or dispatch; commit hash recorded and cited by the proof |
| T-179-11 | Information disclosure (false completeness) | vacuous criteria | mitigate | Section 2's explicit vacuous-pass question per criterion; section 3's non-zero minimum-findings bar |
| T-179-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages |
</threat_model>

<verification>
- The pre-registration exists, is audited, and is the sole file in HEAD.
- No reference-game file touched.
</verification>

<success_criteria>
- Seven criteria audited for satisfiability and vacuity, with explicit verdicts.
- One-file commit, hash recorded.
</success_criteria>

<output>
Create `.planning/phases/179-source-free-verification-mode/179-04-SUMMARY.md` when done, recording
the pre-registration commit hash.
</output>
