---
phase: 179-source-free-verification-mode
plan: 06
type: execute
wave: 6
depends_on: [179-05]
files_modified:
  - .planning/phases/179-source-free-verification-mode/179-PROOF.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [VERIFY-09]

must_haves:
  truths:
    - "A real reference-game project, in a real source-free state, completes a verify pass instead of failing."
    - "The report names real defect classes, derived from the code mapping, not composed by the session."
    - "The durable provenance reads code-conformance-only with the unavailable-source reason — read back from a real CHUNK.md on disk AFTER the run, not from the CLI's live recomputation."
    - "That durable write is idempotent and left designer-authored CHUNK.md content untouched, observed on the real staged project."
    - "The checks that DID run produced REAL findings — including one live CHECK-06 dispatch whose quote provenance is downgraded because no source exists."
    - "Both reference-game originals are proven untouched by a WHOLE-TREE check."
  artifacts:
    - path: ".planning/phases/179-source-free-verification-mode/179-PROOF.md"
      provides: "the live evidence, judged against the pre-registered bar, with limits stated plainly"
      min_lines: 80
    - path: ".planning/REQUIREMENTS.md"
      provides: "VERIFY-09 closure note written on the evidence actually obtained"
      contains: "VERIFY-09"
  key_links:
    - from: "179-PROOF.md"
      to: "the staged copy's chunks/<slug>/CHUNK.md"
      via: "the post-run `## Verified Against` block, quoted verbatim"
      pattern: "Verified Against"
    - from: "179-PROOF.md"
      to: "179-PRE-REGISTRATION.md"
      via: "cites the pre-registration commit hash and judges each criterion against it"
      pattern: "PRE-REGISTRATION"
    - from: "179-PROOF.md"
      to: "git status --short"
      via: "whole-tree originals-untouched evidence"
      pattern: "git status --short"
---

<objective>
Run the mode for real, on a real project in a real source-free state, and record what happened —
including anything that does not go the way the pre-registration expected.

Purpose: this closes VERIFY-09 and the milestone. A mode that runs nothing and honestly reports
running nothing is a banner; decision 11(d) is the bar that separates the two.
Output: `179-PROOF.md`, VERIFY-09 closed on the evidence actually obtained, ROADMAP and STATE
updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/179-source-free-verification-mode/179-PRE-REGISTRATION.md
@.planning/phases/179-source-free-verification-mode/179-CONTEXT.md
@.planning/phases/179-source-free-verification-mode/179-04-SUMMARY.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage the source-free project and run the mechanical half of the reduced pass</name>
  <files>.planning/phases/179-source-free-verification-mode/179-PROOF/ (captured outputs)</files>
  <read_first>
    - `.planning/phases/179-source-free-verification-mode/179-PRE-REGISTRATION.md` §4 (staging + baseline protocol) and §5 (expected outcome)
    - `src/cli/slash-command/bs/verify/source-free-mode.md` — the reduced sequence being executed
    - `.planning/phases/178-worked-example-tests/178-PROOF.md` §10 — the baseline failure not to repeat
  </read_first>
  <action>
    Take the whole-tree BEFORE baseline of the chosen reference game's original with
    `git status --short` in the original's own repo (record the exact output, including an empty
    output), per decision 10. Never an enumerated path list, never a scoped sha256 manifest.

    `cp -R` the game to a scratch working copy. From the COPY only, delete `rulebook/source/` and
    any candidate source file at the copy's project root — record exactly what was removed and
    confirm by `ls` that neither location holds a candidate, since the negative case requires both
    to be absent. Never modify the original.

    Run against the copy, capturing full stdout for each: `boardsmith verify-source-free-check
    --project <copy> --json`, `boardsmith trace-check --project <copy> --json`, and
    `boardsmith drift-check --project <copy> --json`. Record each command's exit code explicitly —
    exit 0 on the source-free path is itself SC-1 evidence, not an incidental detail.

    Before running anything against the copy, capture the BEFORE state of the durable record: for
    the chunk named in the pre-registration §5, `cat <copy>/chunks/<slug>/CHUNK.md` and record
    verbatim whether a `## Verified Against` section exists at all and what it contains. Both
    reference games' chunks are `unknown` today (no block), so "absent" is the expected and
    informative BEFORE — capture it either way, since the AFTER comparison is what proves the run
    wrote the record rather than finding one already there.

    Record the `scope`, `reason`, and the full `uncheckedDefectClasses[]` from the first command,
    and the real finding counts from the other two. Compare each against §5's pre-registered
    expectation and state agreement or divergence plainly; a divergence is a result, not a problem
    to smooth over.

    If any command errors, root-cause it before proceeding (Prove Before Fix) — do not work around
    it and do not proceed to task 2 with a broken mechanical half.
  </action>
  <verify>
    <automated>boardsmith verify-source-free-check --project &lt;staged-copy&gt; --json; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `verify-source-free-check --json` on the staged copy reports `sourceFree: true`, a `scope` of `code-conformance-only`, a named `reason`, and a non-empty `uncheckedDefectClasses[]`; exit code 0, captured verbatim.
    - `trace-check --json` and `drift-check --json` each ran against the copy with their exit codes and real finding counts captured verbatim.
    - The BEFORE `git status --short` output for the original is captured verbatim (whole tree, no path filter).
    - `ls <copy>/rulebook/source` fails and `ls <copy>` shows no candidate source file — both outputs captured.
    - The BEFORE state of `<copy>/chunks/<slug>/CHUNK.md`'s `## Verified Against` section is captured verbatim (including its absence).
    - The original reference game has zero modifications at this point, by `git status --short`.
  </acceptance_criteria>
  <done>The mode ran on a real source-free project, exited 0, and its mechanical outputs are captured.</done>
</task>

<task type="auto">
  <name>Task 2: Live CHECK-06 dispatch, then the Close's durable provenance write</name>
  <files>.planning/phases/179-source-free-verification-mode/179-PROOF/ (dispatch prompts and returns)</files>
  <read_first>
    - `src/cli/slash-command/bs/verify-game.md` Step 8 — the exact CHECK-06 dispatch/record sequence, followed unchanged
    - `src/cli/commands/verify-example-replay.ts` — `QuoteVerifiedProvenance` gating and the two mismatch buckets
    - `.planning/phases/179-source-free-verification-mode/179-PRE-REGISTRATION.md` §3 — the minimum-findings bar — and §5's pre-registered durable-record expectation (which chunk, which `ScopeReason`)
    - `src/cli/slash-command/bs/verify/source-free-mode.md` — the Close, whose `verify-close-record` dispatch is executed here exactly as written
  </read_first>
  <action>
    Run `boardsmith verify-example-replay --project <copy> --json` and dispatch at least one pending
    slice's `slices[].extractionPayload` UNCHANGED to a REAL `claude -p` subagent carrying the
    `BS-EXAMPLE-EXTRACT-V1` handshake, then follow Step 8's sequence through
    `verify-example-translate` and record via `verify-example-record` — the product path, never a
    hand-rolled harness. Save every dispatched prompt and every raw return to
    `.planning/phases/179-source-free-verification-mode/179-PROOF/`.

    The observable that matters: the recorded finding's quote provenance is DOWNGRADED
    (`quote-unverified` / not-source-verified bucket) because no archived source exists to verify
    the supporting quote against — decision 3's degradation, live, with the verdict itself not
    rewritten. Capture the recorded ledger entry showing this, not just the report line.

    Also run `boardsmith verify-derive-check --project <copy> --json` to record CHECK-04's
    reachability and pending work on the source-free copy, capturing its exit code. A full
    three-dispatch CHECK-04 round is NOT required by the pre-registered bar; record exactly what was
    and was not dispatched rather than implying more.

    Then execute source-free mode's CLOSE against the copy, exactly as `source-free-mode.md` writes
    it — `boardsmith verify-close-record --project <copy> --json`, with no `--run`, since source-free
    mode allocates no staging run. Capture its full JSON and exit code.

    Read the durable record BACK FROM DISK. `cat <copy>/chunks/<slug>/CHUNK.md` for the chunk named
    in the pre-registration §5 and quote its `## Verified Against` block verbatim, asserting by
    inspection that its `Scope:` line reads `code-conformance-only` and its `Reason:` line carries
    the `ScopeReason` §5 pre-registered. This readback — not `verify-source-free-check`'s live
    recomputation — is SC-3's evidence. The CLI's live answer was always computable and was never the
    defect; the defect was that no run ever recorded it, and a proof that only re-invokes the CLI
    would pass while proving nothing about the durable record.

    Prove the write's two safety properties on the real project, not on a fixture: run
    `verify-close-record` a SECOND time and capture that every `recorded[].changed` is `false` and
    that each written `CHUNK.md`'s SHA-256 is identical across the two runs (idempotency); and
    `git diff` the copy for one written chunk, showing the change is confined between the
    machine-owned fences with designer-authored content outside them untouched.

    Capture `git status --short` in the COPY as positive evidence of exactly which files the write
    touched, and confirm the touched set matches `recorded[]` — a file changed that `recorded[]` does
    not name is a scoping defect and must be reported, not smoothed over.

    Re-take `git status --short` on the ORIGINAL after all dispatches and after the Close — Phase
    178's deletions happened during test execution, not during editing, so the AFTER check is the one
    that catches this class. The original must still be clean: the write is a real side effect and it
    must have landed only on the copy.
  </action>
  <verify>
    <automated>boardsmith verify-example-replay --project &lt;staged-copy&gt; --json; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - At least one real `claude -p` extraction dispatch was made against the staged copy, with the prompt and raw return saved under `179-PROOF/`.
    - At least one recorded CHECK-06 finding has downgraded quote provenance, with the ledger entry captured verbatim showing the downgrade and the unrewritten verdict.
    - `verify-derive-check --json` exit code and pending-slice count captured; what was and was not dispatched is stated explicitly.
    - `verify-close-record --project <copy> --json` ran with no `--run`, its JSON and exit code captured verbatim; exit code is 0.
    - The post-run `## Verified Against` block of the pre-registered chunk is quoted verbatim FROM THE FILE ON DISK, showing a `Scope:` line reading `code-conformance-only` and a `Reason:` line carrying the `ScopeReason` pre-registered in §5. A divergence from §5's value is recorded as a divergence, not reconciled after the fact.
    - A second `verify-close-record` run reports every `recorded[].changed` as `false`, with per-file SHA-256 equality across the two runs captured.
    - A `git diff` of one written chunk in the COPY shows the change confined between the machine-owned fences.
    - `git status --short` in the COPY is captured and its file set matches `recorded[]`; any mismatch is reported as a scoping defect.
    - AFTER `git status --short` on the original is captured verbatim and is clean; any modification found is restored and disclosed in the proof, never silently fixed.
  </acceptance_criteria>
  <done>Decision 11(d) has real evidence, and SC-3 rests on a durable record read back from disk — idempotent, fence-bounded, and scoped to what the run evaluated.</done>
</task>

<task type="auto">
  <name>Task 3: 179-PROOF.md, VERIFY-09 closure, and phase closeout</name>
  <files>.planning/phases/179-source-free-verification-mode/179-PROOF.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/phases/179-source-free-verification-mode/179-PRE-REGISTRATION.md` — every criterion, judged one by one
    - `.planning/REQUIREMENTS.md` — VERIFY-09's entry and the closure-note style used by CHECK-06/VERIFY-05 (section-and-number citations, limits stated)
    - `.planning/ROADMAP.md` — "### Phase 179" and the Progress table
    - `.planning/STATE.md` — current frontmatter, to hand-edit
  </read_first>
  <action>
    Write `.planning/phases/179-source-free-verification-mode/179-PROOF.md`. Cite the
    pre-registration commit hash from `179-05-SUMMARY.md` in the first section, so the bar is
    provably older than the measurements. Then one section per pre-registered criterion — ROADMAP
    SC-1/SC-2/SC-3 and decision 11(a)-(d) — each with an explicit MET / NOT MET verdict and the
    verbatim evidence it rests on. Judge SC-1 against the pre-registration's RESTATED bar, not its
    literal wording, and say so in that section: the literal criterion is satisfiable by a mode that
    runs nothing, and the restatement is what makes the verdict mean something. SC-3's section must
    quote the on-disk `## Verified Against` block; citing `verify-source-free-check --json` alone
    there is not evidence for SC-3 and must not be presented as such. Include a section for the whole-tree originals-untouched
    evidence (BEFORE and AFTER `git status --short`), and a final "Limits, stated plainly" section
    covering at minimum: how much of CHECK-04 was actually dispatched, that the durable write was
    observed on one staged project's chunks and not across a population, that one staged game is not
    generalization evidence (every new game measured in this milestone surfaced a new defect), and
    anything that diverged from §5's expectation.

    Update `.planning/REQUIREMENTS.md`: write VERIFY-09's closure note on the evidence ACTUALLY
    obtained, with section-and-number citations into `179-PROOF.md`. If any criterion came back NOT
    MET, say so in the note and leave the requirement open — the milestone's last requirement is
    exactly the one there is most pressure to round up, and rounding it up would make every other
    closure note in this file less trustworthy. Update the status table row for VERIFY-09 to match
    the note.

    Update `.planning/ROADMAP.md`: Phase 179's `**Plans**:` count and plan list, a `**Result:**`
    paragraph in the same honest register as Phases 175-178 (what was proven, what was not, what is
    carried), the phase checkbox, and the Progress table row.

    Hand-edit `.planning/STATE.md` directly for position and progress. Do NOT run
    `gsd-sdk query state.advance-plan` or `state.update-progress` — both corrupt STATE.md's
    frontmatter while reporting `updated: false`.

    Run `npm test` and record the result against the 4322-test / 247-file baseline. Commit.
  </action>
  <verify>
    <automated>npm test 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `179-PROOF.md` has one section per pre-registered criterion (7), each with an explicit MET/NOT MET verdict and verbatim evidence, plus the originals-untouched section and a limits section.
    - SC-3's section quotes the post-run on-disk `## Verified Against` block verbatim; SC-1's section names the restated bar it was judged against.
    - `179-PROOF.md` cites the pre-registration commit hash.
    - `.planning/REQUIREMENTS.md`'s VERIFY-09 entry carries a closure note with section-and-number citations into `179-PROOF.md`, and its status-table row matches the note's verdict.
    - `.planning/ROADMAP.md` Phase 179 shows 6 plans listed, a `**Result:**` paragraph, and an updated Progress row.
    - `.planning/STATE.md` was edited by hand; no `state.advance-plan` / `state.update-progress` invocation appears in the transcript.
    - `npm test` reports 0 failing, with the test/file counts recorded and compared against 4322 / 247.
  </acceptance_criteria>
  <done>The phase closes on evidence, with its limits on the record and the suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged copy -> original reference game | A proof run that mutates the original destroys the evidence it depends on |
| model return -> recorded verdict | An extraction/translation return is untrusted model output crossing into a ledger |
| proof narrative -> requirement closure | An overstated proof closes a requirement that is not actually met |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-12 | Tampering | reference-game originals | mitigate | `cp -R` staging; whole-tree `git status --short` BEFORE and AFTER every dispatch and test execution; any modification disclosed, not silently reverted |
| T-179-13 | Spoofing | model-supplied fields in CHECK-06 returns | transfer | Recorded only through `verify-example-record`, which already cross-validates `lineNumber` against the retained-line set (178 CR-03 fix); this plan adds no second write path |
| T-179-14 | Repudiation | overstated closure | mitigate | Every criterion judged against a commit that predates measurement; NOT MET verdicts leave the requirement open |
| T-179-20 | Tampering | the Close's write landing on the ORIGINAL rather than the copy | mitigate | `--project <copy>` on every invocation; whole-tree `git status --short` on the original AFTER the Close; the copy's own `git status --short` cross-checked against `recorded[]` |
| T-179-21 | Information disclosure (false completeness) | SC-3 judged on live recomputation | mitigate | SC-3's evidence is the on-disk block read back after the run; the CLI's JSON is explicitly not accepted as SC-3 evidence |
| T-179-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages; live dispatches use the already-present `claude -p` |
</threat_model>

<verification>
- Every pre-registered criterion has an explicit verdict backed by verbatim captured output.
- SC-3 rests on the on-disk `## Verified Against` block, captured BEFORE and AFTER the run.
- Both `git status --short` readings on the original are recorded.
- `npm test` zero failing.
</verification>

<success_criteria>
- SC-1/SC-2/SC-3 and decision 11(a)-(d) each judged, on evidence, against a bar committed earlier.
- VERIFY-09 disposed of honestly — closed on the evidence obtained, or left open with the gap named.
</success_criteria>

<output>
Create `.planning/phases/179-source-free-verification-mode/179-06-SUMMARY.md` when done.
</output>
