# Phase 51-01: Create Aspect Template Files

## Goal

Create separate template files for each aspect (Dice, PlayingCards, HexGrid, SquareGrid) with sample UI components and documentation links.

## Requirements Addressed

- TPL-01: Each aspect lives in a separate template file
- TPL-02: Dice aspect provides Die3D component, DicePool setup, roll action pattern
- TPL-03: PlayingCards aspect provides card fan, deck pile, draw/play patterns
- TPL-04: HexGrid aspect provides hex board layout, cell selection pattern
- TPL-05: SquareGrid aspect provides grid layout, cell selection pattern
- TPL-06: Each template includes links to relevant BoardSmith documentation

## Tasks

1. [x] Create `src/cli/slash-command/aspects/` directory
2. [x] Create `dice.md` template with:
   - Element setup (DicePool, Die)
   - Roll action pattern using `die.roll()`
   - Die3D component usage with findElements
   - No v-if on Die3D, safe defaults with ??
   - Link to `docs/dice-and-scoring.md`

3. [x] Create `playing-cards.md` template with:
   - Element setup (Card, Hand, DrawPile)
   - Draw and play action patterns
   - Card display using getSuitSymbol/getSuitColor
   - Hand visibility with contentsVisibleToOwner
   - Link to `docs/core-concepts.md`

4. [x] Create `hex-grid.md` template with:
   - Element setup (Board, Cell, Stone)
   - Axial coordinate system (q, r)
   - SVG hex rendering with hexToPixel, getHexPolygonPoints
   - Cell click handling for placeStone
   - Link to `docs/ui-components.md`

5. [x] Create `square-grid.md` template with:
   - Element setup (Board, Cell, Piece)
   - Row/col coordinates with chess-style notation
   - CSS grid layout
   - Two-step move pattern (piece then destination)
   - Link to `docs/core-concepts.md`

## Files Created

- `src/cli/slash-command/aspects/dice.md`
- `src/cli/slash-command/aspects/playing-cards.md`
- `src/cli/slash-command/aspects/hex-grid.md`
- `src/cli/slash-command/aspects/square-grid.md`

## Success Criteria

- [x] All four aspect files created
- [x] Each file has element setup, action patterns, and UI component
- [x] Each file links to relevant documentation
- [x] Templates follow existing patterns from instructions.md
