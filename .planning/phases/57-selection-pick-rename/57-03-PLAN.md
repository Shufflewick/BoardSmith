---
phase: 57-selection-pick-rename
plan: 03
type: execute
wave: 3
depends_on: ["57-01", "57-02"]
files_modified:
  - packages/games/checkers/ui/src/components/CheckersBoard.vue
  - packages/games/cribbage/ui/src/components/CribbageBoard.vue
  - packages/games/go-fish/ui/src/components/GoFishBoard.vue
  - packages/games/polyhedral-potions/ui/src/components/DiceShelf.vue
  - packages/games/hex/rules/src/ai.ts
  - packages/games/go-fish/rules/src/ai.ts
  - packages/games/demoAnimation/src/ui/components/GameTable.vue
  - packages/games/demoComplexUiInteractions/src/ui/components/GameTable.vue
  - packages/games/demoComplexUiInteractions/src/rules/game.ts
  - packages/games/demoComplexUiInteractions/src/rules/actions.ts
  - packages/games/demoActionPanel/rules/src/actions.ts
  - packages/games/demoActionPanel/rules/src/flow.ts
  - packages/games/demoActionPanel/rules/src/game.ts
autonomous: true

must_haves:
  truths:
    - "Extracted games use currentPick instead of currentSelection"
    - "All game UIs reference pickStep instead of selectionStep"
    - "All 9 extracted games build successfully"
    - "All game tests pass"
  artifacts:
    - path: "packages/games/checkers/ui/src/components/CheckersBoard.vue"
      provides: "Updated game UI"
      contains: "currentPick"
    - path: "packages/games/go-fish/ui/src/components/GoFishBoard.vue"
      provides: "Updated game UI"
      contains: "currentPick"
  key_links:
    - from: "packages/games/*/ui/src/components/*.vue"
      to: "boardsmith/ui"
      via: "import"
      pattern: "import.*currentPick|pickStep"
---

<objective>
Update all 9 extracted games to use pick terminology instead of selection.

Purpose: Ensure consistency across the entire BoardSmith ecosystem.
Output: All extracted games use currentPick, pickStep, fetchPickChoices APIs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-selection-pick-rename/57-RESEARCH.md
@.planning/phases/57-selection-pick-rename/57-01-SUMMARY.md
@.planning/phases/57-selection-pick-rename/57-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update game UI components (4 games with selection usage)</name>
  <files>packages/games/checkers/ui/src/components/CheckersBoard.vue, packages/games/cribbage/ui/src/components/CribbageBoard.vue, packages/games/go-fish/ui/src/components/GoFishBoard.vue, packages/games/polyhedral-potions/ui/src/components/DiceShelf.vue</files>
  <action>
For each game UI component, grep for "selection" and update:

1. `currentSelection` -> `currentPick` (from useActionController or slot props)
2. `selectionStep` -> `pickStep` (in useActionController options)
3. `fetchSelectionChoices` -> `fetchPickChoices` (in useActionController options)
4. Update any JSDoc comments or template comments that reference "selection"

Note: The deprecated aliases from Plan 02 will still work, but we want games to use the new terminology for consistency.

Expected changes per game:
- CheckersBoard.vue: currentSelection in template conditions
- CribbageBoard.vue: currentSelection usage
- GoFishBoard.vue: currentSelection usage
- DiceShelf.vue: currentSelection usage (Polyhedral Potions)
  </action>
  <verify>
- `grep -r "currentSelection" packages/games/*/ui/` shows no matches
- `grep -r "currentPick" packages/games/*/ui/` shows updated usage
- Each game builds: `npm run build --workspace=@mygames/checkers` etc.
  </verify>
  <done>All 4 game UIs updated to use pick terminology</done>
</task>

<task type="auto">
  <name>Task 2: Update demo games and rules files</name>
  <files>packages/games/demoAnimation/src/ui/components/GameTable.vue, packages/games/demoComplexUiInteractions/src/ui/components/GameTable.vue, packages/games/demoComplexUiInteractions/src/ui/App.vue, packages/games/demoComplexUiInteractions/src/rules/game.ts, packages/games/demoComplexUiInteractions/src/rules/actions.ts, packages/games/demoActionPanel/rules/src/actions.ts, packages/games/demoActionPanel/rules/src/flow.ts, packages/games/demoActionPanel/rules/src/game.ts</files>
  <action>
Update demo games:

1. demoAnimation GameTable.vue - update selection references
2. demoComplexUiInteractions GameTable.vue and App.vue - update selection references
3. demoComplexUiInteractions rules files - update selection references in action definitions
4. demoActionPanel rules files - update selection references

For rules files (actions.ts, flow.ts, game.ts):
- These may contain JSDoc or comments mentioning "selection"
- Update terminology in comments/JSDoc
- Keep the actual action builder API names (chooseElement, chooseFromList) - those are not being renamed

Focus on user-facing terminology and API surface, not internal implementation details.
  </action>
  <verify>
- `grep -r "currentSelection" packages/games/demo*` shows no matches
- Demo games build successfully
  </verify>
  <done>Demo games updated to use pick terminology</done>
</task>

<task type="auto">
  <name>Task 3: Update AI files and verify all games</name>
  <files>packages/games/hex/rules/src/ai.ts, packages/games/go-fish/rules/src/ai.ts, packages/games/cribbage/tests/game.test.ts</files>
  <action>
Update AI files:
1. packages/games/hex/rules/src/ai.ts - update selection references
2. packages/games/go-fish/rules/src/ai.ts - update selection references

Update test files:
1. packages/games/cribbage/tests/game.test.ts - update selection references

Final verification - build and test all 9 extracted games:
- Checkers
- Cribbage
- Go Fish
- Hex
- Polyhedral Potions
- Floss Bitties
- demoAnimation
- demoComplexUiInteractions
- demoActionPanel

Run: `npm test` to verify no regressions.
  </action>
  <verify>
- `grep -r "selection" packages/games/` shows only internal references (Selection type) or none
- All 9 games build: verify no TypeScript errors
- `npm test` passes (full test suite including game tests)
  </verify>
  <done>All 9 extracted games updated and verified, all tests pass</done>
</task>

</tasks>

<verification>
- `grep -rn "currentSelection" packages/games/` returns no matches
- `grep -rn "selectionStep" packages/games/` returns no matches
- All extracted games build without TypeScript errors
- `npm test` passes (full test suite)
</verification>

<success_criteria>
- All 9 extracted games use pick terminology
- No "currentSelection" or "selectionStep" references in packages/games/
- All games build successfully
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/57-selection-pick-rename/57-03-SUMMARY.md`
</output>
