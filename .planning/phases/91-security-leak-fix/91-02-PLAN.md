---
phase: 91-security-leak-fix
plan: 02
type: execute
wave: 1
depends_on: ["91-01"]
files_modified:
  - src/engine/element/game.ts
autonomous: true
requirements: [SEC-01, SEC-02]

must_haves:
  truths:
    - "A module-private redactHiddenElementAttrs(attrs) helper exists above toJSONForPlayer in game.ts"
    - "All three $-whitelist branches (count-only, hidden/count-only zone children, owner-only zone) call the helper instead of looping over key.startsWith('$')"
    - "Hidden elements serialize $images as { back } only — face and any other side are dropped"
    - "Hidden elements never serialize $image (single-sided images dropped unconditionally)"
    - "Only the 18 allowlisted layout $-keys survive; unknown $-keys are dropped (fail-safe)"
    - "The fully-hidden placeholder branch (__hidden only) is unchanged"
    - "image-leak.test.ts passes green and the full suite stays green"
  artifacts:
    - path: "src/engine/element/game.ts"
      provides: "redactHiddenElementAttrs helper + SAFE_LAYOUT_KEYS allowlist + three patched branches"
      contains: "redactHiddenElementAttrs"
  key_links:
    - from: "toJSONForPlayer count-only branch (~game.ts:2205-2218)"
      to: "redactHiddenElementAttrs"
      via: "replaces the key.startsWith('$') loop"
      pattern: "redactHiddenElementAttrs"
    - from: "toJSONForPlayer hidden/count-only zone branch (~game.ts:2244-2258)"
      to: "redactHiddenElementAttrs"
      via: "replaces the key.startsWith('$') loop"
      pattern: "redactHiddenElementAttrs"
    - from: "toJSONForPlayer owner-only branch (~game.ts:2265-2287)"
      to: "redactHiddenElementAttrs"
      via: "replaces the key.startsWith('$') loop"
      pattern: "redactHiddenElementAttrs"
---

<objective>
Close the leak: replace the blanket `key.startsWith('$')` pass in all three hidden-element branches of `toJSONForPlayer` with a single module-private `redactHiddenElementAttrs` helper that allowlists genuine layout `$`-keys, drops `$image`, keeps only `$images.back`, and fail-safe-drops unknown `$`-keys. This turns the red test from Plan 01 green.

Purpose: Secure-by-default serialization (CLAUDE.md "Pit of Success" / secure defaults) — game authors get safe per-player output with zero opt-in, and a future value-bearing `$`-key cannot silently leak.
Output: edited `src/engine/element/game.ts` (one new helper + three one-line call-site swaps).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-security-leak-fix/91-CONTEXT.md
@.planning/phases/91-security-leak-fix/91-RESEARCH.md
@.planning/phases/91-security-leak-fix/91-01-SUMMARY.md

<interfaces>
<!-- The function being modified and the keys involved. Executor edits in place. -->

From src/engine/element/game.ts (system under change):
```typescript
toJSONForPlayer(player: P | number | null): ElementJSON
// Three branches copy json.attributes via `if (key.startsWith('$')) systemAttrs[key] = value`:
//   1. count-only, not visible          (~lines 2205-2218)
//   2. zone hidden/count-only children  (~lines 2244-2258)
//   3. owner-only zone, non-owner       (~lines 2265-2287)
// Branch at ~2222-2228 (fully hidden → { __hidden: true }) is already safe — DO NOT touch.
```

From src/engine/element/types.ts:
```typescript
export type ImageRef = string | { sprite: string; x: number; y: number; width: number; height: number };
export interface ElementJSON { className: string; attributes: Record<string, unknown>; children?: ElementJSON[]; childCount?: number; }
```

The 18 safe layout `$`-keys (from RESEARCH $-Key Catalog — VERIFIED by source grep):
$type, $layout, $direction, $gap, $overlap, $fan, $fanAngle, $align,
$rowLabels, $columnLabels, $rowCoord, $colCoord,
$hexOrientation, $coordSystem, $qCoord, $rCoord, $sCoord, $hexSize
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add redactHiddenElementAttrs helper and wire all three branches</name>
  <files>src/engine/element/game.ts</files>
  <read_first>
    - src/engine/element/game.ts:2195-2320 (toJSONForPlayer and the three branches to patch + the safe branch to leave alone)
    - src/engine/element/game-element.ts:64-76 ($image / $images semantics)
    - .planning/phases/91-security-leak-fix/91-RESEARCH.md ("Pattern 1: redactHiddenElementAttrs helper", "Pattern 2: Three call sites — before and after", "The Correct Redaction Rule", Anti-Patterns)
    - .planning/phases/91-security-leak-fix/91-CONTEXT.md (SEC-01/SEC-02 locked decisions)
    - src/engine/element/image-leak.test.ts (the red contract this must satisfy)
  </read_first>
  <behavior>
    - Hidden deck card: serialized attributes contain `__hidden: true`, `$images.back` kept, no `$images.face`, `$type` kept.
    - Owner-only hand, non-owner viewer: no `$images.face` on any child; `$images.back` kept.
    - Count-only container: no leaked `$image` / `$images.face`.
    - `$image` present on input → absent on output (always).
    - Unknown `$`-key (e.g. `$secretValue`) present on input → absent on output (fail-safe).
    - Allowlisted layout key (`$type`, `$layout`, `$direction`, …) present on input → present on output.
    - `$images` with no `back` key → `$images` omitted entirely (not an empty object).
  </behavior>
  <action>
    Add a module-private (file-level, not a class member) `const SAFE_LAYOUT_KEYS = new Set([...])` containing exactly the 18 keys listed in the interfaces block, and a pure function `redactHiddenElementAttrs(attrs: Record<string, unknown>): Record<string, unknown>` directly ABOVE `toJSONForPlayer` in `src/engine/element/game.ts`. Per RESEARCH "Pattern 1": initialize the result as `{ __hidden: true }`; skip `__hidden`; drop `$image`; for `$images`, keep only the `back` side if present (`{ back }`), otherwise omit; copy a key only if it is in `SAFE_LAYOUT_KEYS`; drop everything else (unknown `$`-keys and non-`$` keys) — fail-safe. The helper performs NO visibility logic (the branches already established the element is hidden — Anti-Pattern: do not call `isVisibleTo` inside it) and does NOT inspect `faceUp`/card state (Anti-Pattern + Pitfall 3).
    Replace the three `key.startsWith('$')` loops with `redactHiddenElementAttrs(json.attributes ?? {})` (branch 1, count-only ~2205-2218) and `redactHiddenElementAttrs(childJson.attributes ?? {})` (branch 2 zone hidden/count-only ~2244-2258, and branch 3 owner-only ~2265-2287), per RESEARCH "Pattern 2 — before/after". Since the helper sets `__hidden: true` itself, remove the now-redundant manual `systemAttrs` construction and `__hidden` assignment in each branch. Leave the fully-hidden branch (~2222-2228) and the normal-children path untouched. Do NOT redact in `GameElement.toJSON()` — `toJSONForPlayer` is the only redaction site (Anti-Pattern).
  </action>
  <verify>
    <automated>npx vitest run src/engine/element/image-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "redactHiddenElementAttrs" src/engine/element/game.ts` shows one definition plus three call sites (4 matches total).
    - `grep -c "key.startsWith('\$')" src/engine/element/game.ts` returns 0 inside the three patched branches (the blanket loops are gone).
    - `npx vitest run src/engine/element/image-leak.test.ts` EXITS 0 (the Plan 01 red test is now green).
    - The helper is file-private (not exported, not a class method) and contains no `isVisibleTo`/`faceUp` reference.
    - The fully-hidden branch (`{ __hidden: true }` only) is byte-for-byte unchanged.
  </acceptance_criteria>
  <done>The helper exists, all three branches call it, the blanket `$` loops are removed, and image-leak.test.ts passes: hidden elements ship `$images.back` only, no `$image`, no unknown `$`-keys, allowlisted layout keys preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Regression gate — existing visibility/serialization suites and full suite green</name>
  <files>src/engine/element/game.ts</files>
  <read_first>
    - src/engine/element/deck-hand-visibility.test.ts (existing F32 visibility regression — must not break)
    - src/engine/element/game-element.test.ts (existing per-player serialization tests — must not break)
    - .planning/phases/91-security-leak-fix/91-VALIDATION.md (Phase Requirements → Test Map, Sampling Rate)
  </read_first>
  <action>
    Run the existing visibility and serialization regression suites, then the full suite, to confirm the redaction did not change any legitimate (visible) output and did not regress the fully-hidden branch. If `deck-hand-visibility.test.ts` or `game-element.test.ts` fail, the redaction is over-broad (likely dropping a key a VISIBLE element needs, or touching the wrong branch) — fix in game.ts (do not weaken the test or the allowlist beyond the verified 18 keys). Confirm no type regressions with the engine typecheck. Do not add fallbacks or backward-compat shims (CLAUDE.md).
  </action>
  <verify>
    <automated>npx vitest run src/engine/element/deck-hand-visibility.test.ts src/engine/element/game-element.test.ts src/engine/element/image-leak.test.ts && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/engine/element/deck-hand-visibility.test.ts` exits 0.
    - `npx vitest run src/engine/element/game-element.test.ts` exits 0.
    - `npx vitest run` (full suite) exits 0 — no regressions anywhere.
    - No change to test files in this task (production-code-only fix); the only edited source remains `src/engine/element/game.ts`.
  </acceptance_criteria>
  <done>All targeted regression suites and the full vitest suite pass with the redaction in place; the leak is closed and no existing behavior regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine state → per-player payload | `toJSONForPlayer` is the single authoritative filter; redaction MUST happen here before the payload leaves the engine |
| game-authored `$`-attrs → serializer | Authors may set arbitrary `$`-keys; the serializer treats only the verified 18 layout keys as safe on hidden elements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01 | Information Disclosure | hidden element identity leaked via `$images.face` / `$image` to unauthorized viewers | mitigate | `redactHiddenElementAttrs` keeps only `$images.back`, drops `$image`, in all three hidden branches; proven by green image-leak.test.ts |
| T-91-02 | Information Disclosure | future value-bearing `$`-key inheriting the `$` prefix (fail-open) | mitigate | Explicit `SAFE_LAYOUT_KEYS` allowlist + fail-safe-drop of any key not on it; unknown `$`-keys never broadcast on hidden elements |
| T-91-03 | Tampering (regression) | the already-safe fully-hidden branch (`{ __hidden: true }`) | mitigate | Branch left untouched; Task 2 runs deck-hand-visibility + full suite to prove no regression |
| T-91-04 | Information Disclosure (residual) | a game using per-card distinct `back` images, or non-identity face filenames | accept | Engine cannot enforce "all backs identical"; keeping `back` is correct at engine level (RESEARCH Assumptions A2). Residual is a game-author concern, lower severity; the fix does not depend on face filenames being non-identifying |
| T-91-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed this phase |
</threat_model>

<verification>
- `redactHiddenElementAttrs` defined once and called from all three branches; no `key.startsWith('$')` loop remains in those branches.
- `npx vitest run src/engine/element/image-leak.test.ts` green (SEC-01 + SEC-02 closed).
- `deck-hand-visibility.test.ts` and `game-element.test.ts` green (no regression to visible output or the fully-hidden branch).
- `npx vitest run` full suite green.
</verification>

<success_criteria>
The per-player serializer is secure by default: any element a viewer cannot fully see ships only `$images.back` (no `face`/other sides), never `$image`, and only the 18 allowlisted layout `$`-keys; all unknown `$`-keys are dropped. The Plan 01 proof is green and the entire suite passes.
</success_criteria>

<output>
Create `.planning/phases/91-security-leak-fix/91-02-SUMMARY.md` when done. Record: helper location, the three patched branches, the redaction rule applied, and full-suite green confirmation.
</output>
