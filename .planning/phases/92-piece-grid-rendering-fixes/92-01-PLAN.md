---
phase: 92-piece-grid-rendering-fixes
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/ui/components/auto-ui/auto-ui-helpers.ts
  - src/ui/components/auto-ui/auto-ui-helpers.test.ts
autonomous: true
requirements: [PIECE-01, PIECE-02, PIECE-03]

must_haves:
  truths:
    - "resolvePieceVisual returns kind:'image' for a bare-string or .face URL $images"
    - "resolvePieceVisual returns kind:'sprite' for a sprite-sheet $images.face object using the object's own width/height (never 238/333)"
    - "resolvePieceVisual returns kind:'token' with player.color (or #888888 when ownerless) when no image is present"
    - "resolveGridSize returns {ok:true,rows,cols} from explicit $rowCoord/$colCoord"
    - "resolveGridSize returns {ok:true,rows,cols} from inferred numeric child coords when no explicit coords"
    - "resolveGridSize returns {ok:false,error} naming the element id and $rowCoord/$colCoord when neither path resolves"
    - "resolveGridSize returns {ok:true,rows:0,cols:0} for an explicitly-declared but empty grid (no spurious error)"
    - "D-01: piece-visual resolution and grid-sizing are built as standalone extractable pure functions (resolvePieceVisual, resolveGridSize) reusable by the Phase 93 renderer, lifting the existing $images extraction rather than duplicating it"
    - "D-02: resolveGridSize honors both inference paths in order — explicit $rowCoord/$colCoord, then inferred child coords, then loud error — with no 8×8 fallback in the helper"
    - "D-03: resolveGridSize's error path returns an actionable message naming the element id and the $rowCoord/$colCoord prop to add, never leaking file paths/line numbers/stack, and never throws"
    - "D-04: piece images reuse the existing $images convention ($images.face or a bare string), resolved by the same extraction logic used for cards — no new $image singular prop"
    - "D-05: imageless pieces resolve to a colored-disc token using owner player.color (neutral #888888 when ownerless), carrying identity by color without relying on label text"
  artifacts:
    - path: "src/ui/components/auto-ui/auto-ui-helpers.ts"
      provides: "resolvePieceVisual + resolveGridSize pure functions and their exported PieceVisual/GridResult types"
      contains: "export function resolvePieceVisual"
    - path: "src/ui/components/auto-ui/auto-ui-helpers.test.ts"
      provides: "Unit coverage of all PIECE-01/02/03 helper cases"
      contains: "resolveGridSize"
  key_links:
    - from: "src/ui/components/auto-ui/auto-ui-helpers.test.ts"
      to: "src/ui/components/auto-ui/auto-ui-helpers.ts"
      via: "import { resolvePieceVisual, resolveGridSize }"
      pattern: "from './auto-ui-helpers"
---

<objective>
Create the two extractable pure-function helpers that all Phase 92 rendering fixes flow
through: `resolvePieceVisual` (PIECE-01/02) and `resolveGridSize` (PIECE-03). Build them
test-first so the testable core of all three requirements is proven before any Vue SFC is
touched. These helpers are designed for reuse by the Phase 93 renderer (D-01) — near-zero throwaway.

Purpose: Lift existing inlined logic out of `AutoElement.vue` into a standalone, unit-tested
module so the renderer rebuild (Phase 93) can import them unchanged, and so the grid/piece
logic gets automated coverage that DOM-rendered SFCs cannot give.
Output: `auto-ui-helpers.ts` + `auto-ui-helpers.test.ts`, full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/92-piece-grid-rendering-fixes/92-CONTEXT.md
@.planning/phases/92-piece-grid-rendering-fixes/92-RESEARCH.md
@.planning/phases/92-piece-grid-rendering-fixes/92-PATTERNS.md
@.planning/phases/92-piece-grid-rendering-fixes/92-UI-SPEC.md

<interfaces>
<!-- Local GameElement interface to mirror (AutoElement.vue lines 23-32). The helper
     re-declares a minimal GameElement inline — do NOT import from the SFC. -->
interface GameElement {
  id: number;
  name?: string;
  className: string;
  attributes?: Record<string, unknown>;
  children?: GameElement[];
  childCount?: number;
}

<!-- Discriminated unions the helpers MUST export (UI-SPEC Helper API Contract, D-01): -->
export type PieceVisual =
  | { kind: 'image'; src: string }
  | { kind: 'sprite'; sprite: string; x: number; y: number; width: number; height: number }
  | { kind: 'token'; color: string; label: string };

export type GridResult =
  | { ok: true; rows: number; cols: number }
  | { ok: false; error: string };

<!-- Serialized player shape (game-element.ts:773-776): attributes.player = { __playerRef, seat, color?, name? } -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for resolvePieceVisual and resolveGridSize</name>
  <files>src/ui/components/auto-ui/auto-ui-helpers.test.ts</files>
  <read_first>
    - src/ui/composables/useGameViewEnrichment.test.ts (exact analog: node-env vitest, inline GameElement factories, console.error spy pattern — replicate its structure)
    - src/ui/composables/useGameViewHelpers.ts (analog for the pure-function module the tests will import)
    - .planning/phases/92-piece-grid-rendering-fixes/92-RESEARCH.md (§"Phase Requirements → Test Map" lists the 8 exact cases; §"Code Examples" gives exact attribute shapes)
    - .planning/phases/92-piece-grid-rendering-fixes/92-PATTERNS.md (§"auto-ui-helpers.test.ts" gives buildPiece/buildBoard/buildCell factory shapes)
    - .planning/phases/92-piece-grid-rendering-fixes/92-UI-SPEC.md (§"Helper API Contract" defines exact return shapes and the error string)
  </read_first>
  <behavior>
    resolvePieceVisual:
    - Test: $images === 'http://x/p.png' (bare string) → { kind:'image', src:'http://x/p.png' }
    - Test: $images === { face: 'http://x/p.png' } → { kind:'image', src:'http://x/p.png' }
    - Test: $images === { face: { sprite:'s.png', x:40, y:80, width:40, height:40 } } → { kind:'sprite', sprite:'s.png', x:40, y:80, width:40, height:40 } — width/height are 40/40, NOT 238/333
    - Test: no $images, attributes.player = { color:'#ff0000' } → { kind:'token', color:'#ff0000', label:'pawn' } (label from element.name, else className)
    - Test: no $images, no player → { kind:'token', color:'#888888', label: <name or className> }
    resolveGridSize:
    - Test: $rowCoord:'row', $colCoord:'col', children at (0,0),(2,3) → { ok:true, rows:3, cols:4 } (max+1)
    - Test: no explicit coords, children whose first two numeric attrs span a range → { ok:true, rows, cols } via inference
    - Test: children present but no resolvable coord system → { ok:false, error } where error === `Grid "<id-or-name>" can't render — declare $rowCoord/$colCoord on the Grid element`
    - Test: $rowCoord/$colCoord declared but children === [] → { ok:true, rows:0, cols:0 } (Pitfall 6: empty-but-declared is valid, not an error)
    - Test: error string names the element and contains the literal substring "$rowCoord/$colCoord"
  </behavior>
  <action>
    Create `src/ui/components/auto-ui/auto-ui-helpers.test.ts`. Import `{ resolvePieceVisual,
    resolveGridSize }` (and the `PieceVisual`/`GridResult` types) from `'./auto-ui-helpers.js'`
    — the module does not exist yet, so the suite MUST fail at import. Mirror the structure of
    `useGameViewEnrichment.test.ts`: top-level `import { describe, it, expect } from 'vitest'`,
    inline factory functions `buildPiece(attributes)`, `buildBoard(attributes, children)`, and
    `buildCell(row, col, rowAttr='row', colAttr='col')` returning minimal `GameElement` objects
    (id/className/name/attributes/children). Write the exact cases enumerated in <behavior>,
    one `describe` block per helper. Cast discriminated results before asserting payload fields
    (e.g. `(result as { kind:'sprite'; width:number }).width`). The error-string assertion MUST
    check the full literal: `Grid "<id>" can't render — declare $rowCoord/$colCoord on the Grid element`.
    Do NOT implement the helpers in this task.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/auto-ui/auto-ui-helpers.test.ts 2>&1 | grep -qiE "cannot find module|failed to resolve import|no test (suite )?found|error" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ui/components/auto-ui/auto-ui-helpers.test.ts` exists and imports from `./auto-ui-helpers.js`.
    - Running the targeted test fails because the helper module does not yet exist (RED state).
    - Test count: at least 5 cases for resolvePieceVisual and at least 4 cases for resolveGridSize.
    - The error-message test asserts the exact literal string including `$rowCoord/$colCoord` and the element id.
    - No implementation of resolvePieceVisual or resolveGridSize is present in this commit.
  </acceptance_criteria>
  <done>Test file exists, imports the not-yet-existing helpers, and the suite fails to load (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement resolvePieceVisual and resolveGridSize to green</name>
  <files>src/ui/components/auto-ui/auto-ui-helpers.ts</files>
  <read_first>
    - src/ui/components/auto-ui/AutoElement.vue (lines 23-32 for the local GameElement interface to mirror; lines 321-341 for getImageInfo to lift; lines 607-657 for the boardSize coord-scan logic to lift)
    - src/ui/composables/useGameViewHelpers.ts (analog: pure-function module structure, `attrs ?? {}` defensive access, early returns)
    - src/engine/element/types.ts (lines 11-24: ImageRef shape — width/height are non-optional for pieces)
    - .planning/phases/92-piece-grid-rendering-fixes/92-RESEARCH.md (§"Pattern 1/Pattern 2" give reference implementations; §"Common Pitfalls" 5 & 6; §"Don't Hand-Roll")
    - .planning/phases/92-piece-grid-rendering-fixes/92-PATTERNS.md (§"auto-ui-helpers.ts" gives file header + the exact extraction logic to lift)
    - src/ui/components/auto-ui/auto-ui-helpers.test.ts (the contract written in Task 1)
  </read_first>
  <action>
    Create `src/ui/components/auto-ui/auto-ui-helpers.ts`. Header comment: pure functions, no Vue
    reactivity, no DOM, reused by current renderer and Phase 93. Re-declare the minimal `GameElement`
    interface inline (mirror AutoElement.vue lines 23-32) — do NOT import from the SFC. Export the
    `PieceVisual` and `GridResult` types exactly as in the <interfaces> block.

    Implement `resolvePieceVisual(element)`: read `attrs = element.attributes ?? {}`; `images = attrs.$images`.
    Bare string → `{ kind:'image', src: images }`. Object with string `.face` → `{ kind:'image', src: face }`.
    Object whose `.face` is a sprite object (`sprite` + numeric `x`/`y` + `width`/`height`) →
    `{ kind:'sprite', sprite, x, y, width, height }` using the object's OWN width/height — do NOT fall
    back to 238/333 (those are card-only; Pitfall 5). Otherwise → token: read
    `(attrs.player as { color?: string } | undefined)?.color ?? '#888888'`, label = `element.name ?? element.className`.

    Implement `resolveGridSize(element)`: read `attrs` and `children = element.children ?? []`. Check
    explicit `$rowCoord`/`$colCoord` FIRST (before any children-length guard — Pitfall 6): if both are
    declared, scan children for the max of each named coord and return `{ ok:true, rows: maxRow+1, cols: maxCol+1 }`
    (when children is empty this yields `{ ok:true, rows:0, cols:0 }`). If no explicit coords, infer:
    take the first child's first two numeric, non-underscore-prefixed attribute keys; if two exist, scan
    all children for their maxes and return `{ ok:true, rows: maxFirst+1, cols: maxSecond+1 }`. If neither
    path resolves, return `{ ok:false, error: 'Grid "' + (element.name ?? element.id) + '" can\'t render — declare $rowCoord/$colCoord on the Grid element' }`.
    The error string must match the test literal exactly (em-dash ` — `, `$rowCoord/$colCoord`). Do not throw.
    Lift the existing logic from AutoElement.vue rather than reinventing it.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/auto-ui/auto-ui-helpers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/components/auto-ui/auto-ui-helpers.ts` exists and exports `resolvePieceVisual`, `resolveGridSize`, `PieceVisual`, `GridResult`.
    - All tests from Task 1 pass (GREEN).
    - Sprite branch returns the sprite object's own width/height — `grep -n "238\|333" src/ui/components/auto-ui/auto-ui-helpers.ts` returns nothing.
    - `resolveGridSize` checks explicit `$rowCoord`/`$colCoord` before any children-length guard (empty declared grid → `{ok:true,rows:0,cols:0}`, asserted by test).
    - No `throw` statement in the file (`grep -c "throw" src/ui/components/auto-ui/auto-ui-helpers.ts` returns 0).
    - Module re-declares GameElement inline and does not import from `AutoElement.vue`.
  </acceptance_criteria>
  <done>Both helpers implemented; targeted suite green; no card-default sprite fallback; no throw.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game-author definition → helper return value | `$images` URLs, sprite paths, and element names authored in the game definition flow through the helper into render-ready data. Same trust level as game code. |
| engine serialized game view → helper input | The helper consumes an already visibility-filtered game view (filtering is Phase 91's responsibility); it adds no new data and does not reach across visibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Information Disclosure | resolveGridSize error string | mitigate | Error string is a fixed literal naming only the element id + the prop to declare. No file paths, line numbers, or stack fragments (D-03). Asserted by the exact-string unit test. |
| T-92-02 | Tampering (XSS surface produced) | resolvePieceVisual `src`/`sprite` | mitigate | Helper returns plain data only — it constructs no markup and performs no DOM writes. Rendering safety (no `v-html`, attribute/text bindings only) is enforced in Plan 02 where the values are rendered. |
| T-92-SC | Supply chain | npm installs | n/a | No package installs in this phase (RESEARCH §"No New Installations"). |
</threat_model>

<verification>
- `npx vitest run src/ui/components/auto-ui/auto-ui-helpers.test.ts` is green.
- `npm test` (full suite) is green — no regression from the new module.
- Helper file contains no `238`/`333` literals and no `throw`.
</verification>

<success_criteria>
- Both pure helpers exist with exported types and full unit coverage for PIECE-01/02/03 logic.
- All 8+ enumerated test cases pass; error-string test asserts the exact D-03 literal.
- Full vitest suite green.
</success_criteria>

<output>
Create `.planning/phases/92-piece-grid-rendering-fixes/92-01-SUMMARY.md` when done.
</output>
