---
phase: 93-renderer-rebuild
plan: 03
type: execute
wave: 2
depends_on: ["93-01"]
files_modified:
  - src/ui/components/auto-ui/renderers/GridBoardRenderer.vue
  - src/ui/components/auto-ui/renderers/HexBoardRenderer.vue
  - src/ui/components/auto-ui/renderers/PieceRenderer.vue
autonomous: true
requirements: [RENDER-01, RENDER-04]

must_haves:
  truths:
    - "A grid board lays out with grid-template-columns repeat(N,1fr) where N comes from resolveGridSize(element).cols; there is no hardcoded 8x8 fallback"
    - "A grid board with missing/undeclared coordinates renders the exact locked error string from auto-ui-helpers.ts, not a degraded grid"
    - "A hex board positions cells via hexToPixel and draws polygons via getHexPolygonPoints from useHexGrid — no hand-rolled axial math"
    - "A piece renders via resolvePieceVisual: image / sprite / labeled token, always circular, with a single box-shadow and a scale(1.05) hover"
  artifacts:
    - path: "src/ui/components/auto-ui/renderers/GridBoardRenderer.vue"
      provides: "CSS-grid board via resolveGridSize + provide gridCoords; loud error panel"
      min_lines: 50
    - path: "src/ui/components/auto-ui/renderers/HexBoardRenderer.vue"
      provides: "SVG hex board via hexToPixel/getHexPolygonPoints closed-form math"
      min_lines: 50
    - path: "src/ui/components/auto-ui/renderers/PieceRenderer.vue"
      provides: "Piece visual via resolvePieceVisual with carry-forward hover + single shadow"
      min_lines: 30
  key_links:
    - from: "src/ui/components/auto-ui/renderers/GridBoardRenderer.vue"
      to: "src/ui/components/auto-ui/auto-ui-helpers.ts"
      via: "resolveGridSize (Phase 92 helper — reuse, not re-implement)"
      pattern: "resolveGridSize"
    - from: "src/ui/components/auto-ui/renderers/HexBoardRenderer.vue"
      to: "src/ui/composables/useHexGrid.ts"
      via: "hexToPixel / getHexPolygonPoints closed-form math"
      pattern: "hexToPixel|getHexPolygonPoints"
    - from: "src/ui/components/auto-ui/renderers/PieceRenderer.vue"
      to: "src/ui/components/auto-ui/auto-ui-helpers.ts"
      via: "resolvePieceVisual (Phase 92 helper)"
      pattern: "resolvePieceVisual"
---

<objective>
Extract the board-family renderers — grid board (CSS grid), hex board (SVG), and piece — from AutoElement.vue, routing ALL layout math through the existing closed-form helpers (resolveGridSize, hexToPixel, getHexPolygonPoints) and Phase 92's resolvePieceVisual. This is the RENDER-04 closed-form-layout slice and the most math-sensitive part of RENDER-01.

Purpose: RENDER-01 + RENDER-04 — bounded, coordinate-addressable layout with no hand-rolled math and no 8x8 fallback; missing grid coordinates fail loud.
Output: GridBoardRenderer.vue, HexBoardRenderer.vue, PieceRenderer.vue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-renderer-rebuild/93-PATTERNS.md
@.planning/phases/93-renderer-rebuild/93-UI-SPEC.md
@.planning/phases/93-renderer-rebuild/93-RESEARCH.md

<interfaces>
<!-- Phase 92 helpers — REUSE, never re-implement (auto-ui-helpers.ts): -->
resolveGridSize(element): GridResult  // { ok: true, rows, cols } | { ok: false, error: string }
resolvePieceVisual(element): PieceVisual  // discriminated union: url | sprite | token
type PieceVisual = ...  // import the type alongside the function

<!-- useHexGrid.ts standalone closed-form functions (no Vue needed inside renderer): -->
hexToPixel(q: number, r: number, hexSize: number, orientation: 'flat'|'pointy'): {x:number,y:number}
getHexPolygonPoints(hexSize: number, orientation: 'flat'|'pointy', scale?: number): string

<!-- Locked grid error copy (auto-ui-helpers.ts:169) — render EXACTLY, do not paraphrase: -->
Grid "<id>" can't render — declare $rowCoord/$colCoord on the Grid element
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GridBoardRenderer.vue (resolveGridSize + provide gridCoords + loud error)</name>
  <files>src/ui/components/auto-ui/renderers/GridBoardRenderer.vue</files>
  <read_first>
    - src/ui/components/auto-ui/AutoElement.vue (lines 76-82 provide gridCoords; 609-611 gridResult computed; 1147-1153 per-cell grid-row/grid-column; board section template + styles)
    - src/ui/components/auto-ui/auto-ui-helpers.ts (resolveGridSize signature + GridResult shape + the exact error string at line ~169)
    - .planning/phases/93-renderer-rebuild/93-PATTERNS.md (section "renderers/GridBoardRenderer.vue")
    - .planning/phases/93-renderer-rebuild/93-UI-SPEC.md (section "GridBoardRenderer" — grid, cell, cell states, coordinate notation, row/col labels; Copywriting Contract grid error)
    - src/ui/components/auto-ui/renderers/ElementRenderer.vue (children/pieces dispatch through this)
  </read_first>
  <action>
    Create `renderers/GridBoardRenderer.vue` with props `{ element: GameElement; depth: number }`. Compute `gridResult = computed(() => resolveGridSize(props.element))` — DO NOT re-implement grid sizing. When `!gridResult.ok`, render the error panel showing `{{ gridResult.error }}` verbatim (the locked string) and DO NOT render any grid; there is NO 8x8 fallback. When ok, set `grid-template-columns: repeat(${gridResult.cols}, 1fr)` and `display:grid; gap:2px`. Provide `gridCoords` `{ rowCoord, colCoord }` (from `$rowCoord`/`$colCoord`) to child cells exactly as AutoElement 76-82, so cells position via `grid-row`/`grid-column` from the parent coord names (AutoElement 1147-1153 logic). Render cells/children through ElementRenderer. Apply cell visual + hover/highlight/selected/action-selectable states and the coordinate-notation corner label from UI-SPEC §GridBoardRenderer. Scoped styles; `.js` imports.
  </action>
  <verify>
    <automated>npx eslint src/ui/components/auto-ui/renderers/GridBoardRenderer.vue && npx vitest run src/ui/components/auto-ui/auto-ui-helpers.test.ts src/ui/</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint src/ui/components/auto-ui/renderers/GridBoardRenderer.vue` reports no errors
    - `grep -c "resolveGridSize" src/ui/components/auto-ui/renderers/GridBoardRenderer.vue` returns >=1 (helper reused)
    - `grep -Ec "8 ?[x×*] ?8|repeat\(8" src/ui/components/auto-ui/renderers/GridBoardRenderer.vue` returns 0 (no hardcoded 8x8)
    - The error branch renders `gridResult.error`; `grep -c "can't render" src/ui/components/auto-ui/renderers/GridBoardRenderer.vue` returns 0 ONLY if the string is taken from the helper (it must NOT be re-typed — render `{{ gridResult.error }}`). Confirm via `grep -c "gridResult.error" ...` returns >=1
    - `npx vitest run src/ui/components/auto-ui/auto-ui-helpers.test.ts` stays GREEN (Phase 92 helper untouched)
  </acceptance_criteria>
  <done>Grid lays out from resolveGridSize, fails loud on missing coords, no 8x8 fallback; helper reused, lints clean.</done>
</task>

<task type="auto">
  <name>Task 2: HexBoardRenderer.vue (closed-form SVG via useHexGrid)</name>
  <files>src/ui/components/auto-ui/renderers/HexBoardRenderer.vue</files>
  <read_first>
    - src/ui/components/auto-ui/AutoElement.vue (lines 659-719: hex position math + bounds — being REPLACED by useHexGrid functions; hex-board section template + styles)
    - src/ui/composables/useHexGrid.ts (hexToPixel + getHexPolygonPoints signatures; HexGridOptions; do NOT edit)
    - .planning/phases/93-renderer-rebuild/93-PATTERNS.md (section "renderers/HexBoardRenderer.vue" — getCellPosition, hexPoints, SVG structure)
    - .planning/phases/93-renderer-rebuild/93-UI-SPEC.md (section "HexBoardRenderer" — container, SVG, polygon states, hex piece circles, labels, player color cycle)
    - src/ui/components/auto-ui/renderers/ElementRenderer.vue
  </read_first>
  <action>
    Create `renderers/HexBoardRenderer.vue` with props `{ element: GameElement; depth: number; hexPieceSize?: number }`. Import `hexToPixel`, `getHexPolygonPoints` from `../../composables/useHexGrid.js` — use the STANDALONE functions (no need for the reactive composable since the element is in hand). Compute `hexSize = $hexSize ?? 50`, `orientation = $hexOrientation ?? 'pointy'`. `getCellPosition(cell)` reads coord-name attrs (`$qCoord ?? 'q'`, `$rCoord ?? 'r'`) off the parent and the q/r values off the cell, then returns `hexToPixel(q, r, hexSize, orientation)`. `hexPoints = getHexPolygonPoints(hexSize, orientation)`. Compute the SVG viewBox from cell bounds (same algorithm as AutoElement 684-719; you MAY use `useHexGrid`'s `hexGridBounds` if a reactive bound is cleaner — both acceptable). Render `<svg>` with a `<g transform translate>` per cell, a `<polygon :points="hexPoints">`, child pieces via ElementRenderer, and SVG `<text>` coordinate labels. Apply all polygon states (hover/action-selectable+pulse/highlighted/selected/has-children) and the hex piece circle styling + player color cycle from UI-SPEC §HexBoardRenderer. Do NOT copy-paste axial formulas (Anti-pattern: hand-rolling hex math). `tryUseBoardInteraction()` for cell states. Scoped styles; `.js` imports.
  </action>
  <verify>
    <automated>npx eslint src/ui/components/auto-ui/renderers/HexBoardRenderer.vue && npx vitest run src/ui/</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint src/ui/components/auto-ui/renderers/HexBoardRenderer.vue` reports no errors
    - `grep -Ec "hexToPixel|getHexPolygonPoints" src/ui/components/auto-ui/renderers/HexBoardRenderer.vue` returns >=2 (both closed-form functions used)
    - `grep -Ec "Math\.sqrt\(3\)|Math\.cos|axial" src/ui/components/auto-ui/renderers/HexBoardRenderer.vue` returns 0 (no re-implemented axial trig)
    - `grep -c "ElementRenderer" src/ui/components/auto-ui/renderers/HexBoardRenderer.vue` returns >=1
    - `npx vitest run src/ui/` stays GREEN
  </acceptance_criteria>
  <done>Hex board renders SVG cells positioned by useHexGrid closed-form math; no hand-rolled trig; lints clean.</done>
</task>

<task type="auto">
  <name>Task 3: PieceRenderer.vue (resolvePieceVisual + carry-forward hover/shadow)</name>
  <files>src/ui/components/auto-ui/renderers/PieceRenderer.vue</files>
  <read_first>
    - src/ui/components/auto-ui/AutoElement.vue (piece section: pieceVisual computed line ~615, piece template + token styles)
    - src/ui/components/auto-ui/auto-ui-helpers.ts (resolvePieceVisual + PieceVisual type)
    - .planning/phases/93-renderer-rebuild/93-PATTERNS.md (section "renderers/PieceRenderer.vue" — carry-forward #1 hover scale(1.05), #2 single shadow)
    - .planning/phases/93-renderer-rebuild/93-UI-SPEC.md (section "PieceRenderer" — sane floor circular token, single shadow rule, piece hover, draggable)
  </read_first>
  <action>
    Create `renderers/PieceRenderer.vue` with props `{ element: GameElement; depth: number }`. Compute `pieceVisual = computed((): PieceVisual => resolvePieceVisual(props.element))` — reuse the Phase 92 helper, do not re-implement. Render the three branches from the discriminated union: `url` → `<img>` 40×40 circular object-fit:cover; `sprite` → background-image/position/size 40×40 circular; `token` → circular wrapper using player `.color` (else #888) with the abbreviated-name label (12px 700 #fff, max-width 36px ellipsis). CARRY-FORWARD (mandatory): (1) include `transform: scale(1.05)` on `:hover` with `transition: transform 0.2s ease, box-shadow 0.2s ease`; (2) put exactly ONE `box-shadow: 0 2px 8px rgba(0,0,0,0.3)` on the OUTERMOST `.piece` element only — never on both wrapper and inner element. `tryUseBoardInteraction()` for selectable/dragging state; draggable uses the drag-drop.css CSS vars. `:data-element-id` + `:data-animatable="true"` for FLIP/fly. Scoped styles; `.js` imports.
  </action>
  <verify>
    <automated>npx eslint src/ui/components/auto-ui/renderers/PieceRenderer.vue && npx vitest run src/ui/components/auto-ui/auto-ui-helpers.test.ts src/ui/</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint src/ui/components/auto-ui/renderers/PieceRenderer.vue` reports no errors
    - `grep -c "resolvePieceVisual" src/ui/components/auto-ui/renderers/PieceRenderer.vue` returns >=1
    - `grep -c "scale(1.05)" src/ui/components/auto-ui/renderers/PieceRenderer.vue` returns >=1 (carry-forward #1)
    - `grep -c "box-shadow" src/ui/components/auto-ui/renderers/PieceRenderer.vue` returns exactly 1 (carry-forward #2: single shadow)
    - `grep -c "v-html" src/ui/components/auto-ui/renderers/PieceRenderer.vue` returns 0
    - `npx vitest run src/ui/components/auto-ui/auto-ui-helpers.test.ts` stays GREEN
  </acceptance_criteria>
  <done>Piece renders image/sprite/token via resolvePieceVisual with scale(1.05) hover and a single box-shadow; lints clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine gameView → board renderer DOM/SVG | Game-authored coordinate attributes, piece labels, and $images URLs cross into CSS grid positions, SVG transforms, and DOM text/images |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-07 | Tampering (XSS) | piece token label, grid/hex labels | mitigate | Author strings rendered via `{{ }}` / SVG `<text>` text content only; no `v-html` (acceptance greps for 0) |
| T-93-08 | Tampering (URL injection) | piece $images src | mitigate | Rendered via `<img :src>` / `background-image`; never injected into `<style>` or `v-html` |
| T-93-09 | Input Validation | grid coordinate attributes | mitigate | resolveGridSize returns a typed ok/error union; the error branch fails loud with the locked actionable copy, never a degraded 8x8 guess (CLAUDE.md fail-loud) |
| T-93-10 | Tampering | useHexGrid / interaction substrate | accept | Standalone closed-form functions are imported read-only; no composable signature changes |
</threat_model>

<verification>
- `npx eslint src/ui/components/auto-ui/renderers/GridBoardRenderer.vue src/ui/components/auto-ui/renderers/HexBoardRenderer.vue src/ui/components/auto-ui/renderers/PieceRenderer.vue` — clean
- `npx vitest run src/ui/components/auto-ui/auto-ui-helpers.test.ts` — green (helpers unchanged)
- `git diff --stat src/ui/composables/useHexGrid.ts src/ui/composables/useGameGrid.ts` — ZERO changes
</verification>

<success_criteria>
- RENDER-04: grid uses resolveGridSize (no 8x8 fallback, loud error on missing coords); hex uses hexToPixel/getHexPolygonPoints (no hand-rolled trig)
- RENDER-01 (board slice): grid/hex/piece are discrete registry-ready renderers reproducing AutoElement layout + the Phase 92 carry-forward piece fixes
- No v-html; helpers reused, not duplicated; substrate/composables untouched
</success_criteria>

<output>
Create `.planning/phases/93-renderer-rebuild/93-03-SUMMARY.md` when done.
</output>
