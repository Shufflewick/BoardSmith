---
phase: 93-renderer-rebuild
plan: 04
type: execute
wave: 2
depends_on: ["93-01"]
files_modified:
  - src/ui/components/auto-ui/renderers/DieRenderer.vue
  - src/ui/components/auto-ui/renderers/SpaceRenderer.vue
autonomous: true
requirements: [RENDER-01]

must_haves:
  truths:
    - "A die element delegates to the existing Die3D component with sides/value/color/rolling/rollCount/faceLabels/faceImages passed through"
    - "A space (generic container) renders its children via ElementRenderer with fan/overlap/direction honored through CSS vars, never blank below 60px min-height"
  artifacts:
    - path: "src/ui/components/auto-ui/renderers/DieRenderer.vue"
      provides: "Die3D delegation renderer"
      min_lines: 20
    - path: "src/ui/components/auto-ui/renderers/SpaceRenderer.vue"
      provides: "Generic container renderer dispatching children via ElementRenderer"
      min_lines: 30
  key_links:
    - from: "src/ui/components/auto-ui/renderers/DieRenderer.vue"
      to: "src/ui/components/dice/index.ts"
      via: "Die3D component delegation"
      pattern: "Die3D"
    - from: "src/ui/components/auto-ui/renderers/SpaceRenderer.vue"
      to: "src/ui/components/auto-ui/renderers/ElementRenderer.vue"
      via: "children dispatch"
      pattern: "ElementRenderer"
---

<objective>
Extract the two leaf/container renderers from AutoElement.vue: DieRenderer (pure delegation to the existing Die3D component) and SpaceRenderer (the generic fallback container that recurses into children via ElementRenderer). Together with plans 93-02 and 93-03 these complete the eight built-in per-element renderers that the registry will dispatch to.

Purpose: RENDER-01 — the remaining per-element renderers, including the never-blank generic container floor.
Output: DieRenderer.vue, SpaceRenderer.vue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-renderer-rebuild/93-PATTERNS.md
@.planning/phases/93-renderer-rebuild/93-UI-SPEC.md

<interfaces>
<!-- Die3D from src/ui/components/dice/index.ts — pass these attributes through: -->
sides, value, color, rolling, rollCount, faceLabels, faceImages

<!-- Space layout CSS vars (from element attributes $direction/$gap/$fan/$overlap/$align): -->
--layout-gap, --layout-direction  (consumed by .space-children)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DieRenderer.vue (delegate to Die3D)</name>
  <files>src/ui/components/auto-ui/renderers/DieRenderer.vue</files>
  <read_first>
    - src/ui/components/auto-ui/AutoElement.vue (die section: how Die3D is currently invoked and which attrs are mapped)
    - src/ui/components/dice/index.ts (Die3D export + prop names)
    - .planning/phases/93-renderer-rebuild/93-PATTERNS.md (section "renderers/DieRenderer.vue" — dieProps mapping)
    - .planning/phases/93-renderer-rebuild/93-UI-SPEC.md (section "DieRenderer" — delegate, no new visual logic)
  </read_first>
  <action>
    Create `renderers/DieRenderer.vue` with props `{ element: GameElement; depth: number }`. Import `Die3D` from `../../dice/index.js`. Compute a `dieProps` object mapping element attributes through: `sides ?? 6`, `value`, `color`, `rolling`, `rollCount`, `faceLabels`, `faceImages`. Render `<Die3D v-bind="dieProps" />`. No new die visual logic — Die3D owns it. `tryUseBoardInteraction()` only if the die is selectable in the current games (mirror AutoElement's die section; if it had no board interaction, omit it). Scoped styles minimal; `.js` imports.
  </action>
  <verify>
    <automated>npx eslint src/ui/components/auto-ui/renderers/DieRenderer.vue && npx vitest run src/ui/</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint src/ui/components/auto-ui/renderers/DieRenderer.vue` reports no errors
    - `grep -c "Die3D" src/ui/components/auto-ui/renderers/DieRenderer.vue` returns >=1 (delegation, not re-implementation)
    - `npx vitest run src/ui/` stays GREEN
  </acceptance_criteria>
  <done>Die delegates to Die3D with all attributes passed through; lints clean.</done>
</task>

<task type="auto">
  <name>Task 2: SpaceRenderer.vue (generic container — never blank)</name>
  <files>src/ui/components/auto-ui/renderers/SpaceRenderer.vue</files>
  <read_first>
    - src/ui/components/auto-ui/AutoElement.vue (space section: $direction/$fan/$fanAngle/$overlap/$gap/$align handling, space-children layout)
    - .planning/phases/93-renderer-rebuild/93-PATTERNS.md (section "renderers/SpaceRenderer.vue" — children via ElementRenderer, CSS vars)
    - .planning/phases/93-renderer-rebuild/93-UI-SPEC.md (section "SpaceRenderer" — container, header, children, empty state min-height 60px)
    - src/ui/components/auto-ui/renderers/ElementRenderer.vue
  </read_first>
  <action>
    Create `renderers/SpaceRenderer.vue` with props `{ element: GameElement; depth: number }`. Render an optional header (name bold #aaa 16px + child-count meta 13px #666 when a name exists). Render children with `v-for="child in element.children ?? []"` through `ElementRenderer` (`:element="child" :depth="depth+1"`) — NEVER recurse to AutoElement. Drive layout via CSS custom props on `.space-children`: `--layout-gap` from `$gap` (default 8px), `--layout-direction` from `$direction` (default row); support fan/overlap via the same CSS-var approach as AutoElement's space-children. Container baseline `rgba(255,255,255,0.03)` bg, border, radius 8px, padding 12px, `min-height: 60px` so an empty space is never a zero-height blank. `tryUseBoardInteraction()` for selectable state. Scoped styles; `.js` imports.
  </action>
  <verify>
    <automated>npx eslint src/ui/components/auto-ui/renderers/SpaceRenderer.vue && npx vitest run src/ui/</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint src/ui/components/auto-ui/renderers/SpaceRenderer.vue` reports no errors
    - `grep -c "ElementRenderer" src/ui/components/auto-ui/renderers/SpaceRenderer.vue` returns >=1
    - `grep -c "AutoElement" src/ui/components/auto-ui/renderers/SpaceRenderer.vue` returns 0
    - `grep -c "min-height" src/ui/components/auto-ui/renderers/SpaceRenderer.vue` returns >=1 (never-blank floor)
    - `npx vitest run src/ui/` stays GREEN
  </acceptance_criteria>
  <done>Space renders children via ElementRenderer with layout vars and a 60px never-blank floor; lints clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine gameView → leaf renderer DOM | Game-authored space/die names and labels rendered into the DOM; die visuals delegated to Die3D |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-11 | Tampering (XSS) | space/die header text | mitigate | Author strings via `{{ }}` text interpolation only; no `v-html` |
| T-93-12 | Tampering | Die3D component / interaction substrate | accept | DieRenderer only delegates to the existing Die3D; no Die3D or substrate file is modified |
</threat_model>

<verification>
- `npx eslint src/ui/components/auto-ui/renderers/DieRenderer.vue src/ui/components/auto-ui/renderers/SpaceRenderer.vue` — clean
- `npx vitest run src/ui/` — green
</verification>

<success_criteria>
- RENDER-01 (leaf slice): die delegates to Die3D; space recurses via ElementRenderer with a never-blank floor
- No AutoElement recursion; no v-html; no substrate/Die3D modification
</success_criteria>

<output>
Create `.planning/phases/93-renderer-rebuild/93-04-SUMMARY.md` when done.
</output>
