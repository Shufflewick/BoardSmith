---
phase: 93-renderer-rebuild
plan: 05
type: execute
wave: 2
depends_on: ["93-01"]
files_modified:
  - src/ui/components/auto-ui/archetypes/GridBoardTemplate.vue
  - src/ui/components/auto-ui/archetypes/CardTemplate.vue
  - src/ui/components/auto-ui/archetypes/TableauTemplate.vue
  - src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue
autonomous: true
requirements: [RENDER-03, RENDER-04]

must_haves:
  truths:
    - "GridBoardTemplate gives the grid/hex board the dominant area (1fr) with a docked hand strip (auto, max 30vh) and a peripheral chrome row — never equal-space subdivision (D-02 archetype hierarchy: focal board, docked hand, peripheral chrome — not democratic equal-space subdivision)"
    - "CardTemplate makes hand(s) the dominant flex:1 area with decks/discards as auto-height peripheral zones"
    - "TableauTemplate renders every zone via its per-element renderer and never produces a blank screen"
    - "UnsupportedTopologyPanel renders the locked amber honest-fail copy with an actionable next step and no implementation details"
  artifacts:
    - path: "src/ui/components/auto-ui/archetypes/GridBoardTemplate.vue"
      provides: "Focal-board / docked-hand / peripheral-chrome grid-template-areas layout"
      min_lines: 40
    - path: "src/ui/components/auto-ui/archetypes/CardTemplate.vue"
      provides: "Hand-dominant flex column layout"
      min_lines: 25
    - path: "src/ui/components/auto-ui/archetypes/TableauTemplate.vue"
      provides: "General flex-wrap container fallback"
      min_lines: 20
    - path: "src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue"
      provides: "Loud honest-fail boundary panel (RENDER-04)"
      min_lines: 15
  key_links:
    - from: "src/ui/components/auto-ui/archetypes/GridBoardTemplate.vue"
      to: "src/ui/components/auto-ui/renderers/ElementRenderer.vue"
      via: "renders each classified child through ElementRenderer"
      pattern: "ElementRenderer"
---

<objective>
Build the three hierarchy-bearing archetype templates and the honest-fail boundary panel. Each template encodes visual hierarchy (focal board / docked hand / peripheral chrome) — explicitly NOT the Boardzilla equal-space subdivision failure mode (§0 C6). The UnsupportedTopologyPanel is the RENDER-04 honest boundary for un-addressable topologies. Templates render children exclusively through ElementRenderer.

Purpose: RENDER-03 (hierarchy templates) + RENDER-04 (honest-fail boundary). AutoRenderer (plan 93-06) selects among these via selectArchetype.
Output: GridBoardTemplate.vue, CardTemplate.vue, TableauTemplate.vue, UnsupportedTopologyPanel.vue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-renderer-rebuild/93-PATTERNS.md
@.planning/phases/93-renderer-rebuild/93-UI-SPEC.md
@.planning/phases/93-renderer-rebuild/93-CONTEXT.md

<interfaces>
<!-- All four templates take: -->
defineProps<{ topLevelChildren: GameElement[]; gameView?: GameElement | null }>()

<!-- Child classification (GridBoardTemplate) — pattern from AutoGameBoard.vue 122-133: -->
boardElements  = children where $layout === 'grid' || 'hex-grid'
handElements   = children where $type === 'hand'
chromeElements = the rest

<!-- LOCKED honest-fail copy (UI-SPEC Copywriting Contract — render EXACTLY): -->
heading: "This layout cannot be auto-generated"
body: "This game uses a layout that is outside the auto-UI's supported set (grid, hex, stack, hand). Build a custom UI component for this game — see the custom UI guide."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GridBoardTemplate.vue (focal board + docked hand + peripheral chrome)</name>
  <files>src/ui/components/auto-ui/archetypes/GridBoardTemplate.vue</files>
  <read_first>
    - src/ui/components/auto-ui/AutoGameBoard.vue (lines 110-134: topLevelChildren + the layoutClass classification being replaced; provide scope)
    - .planning/phases/93-renderer-rebuild/93-PATTERNS.md (section "archetypes/GridBoardTemplate.vue" — CSS grid-template-areas, classification computeds)
    - .planning/phases/93-renderer-rebuild/93-UI-SPEC.md (section "Template 1: GridBoardTemplate" — exact CSS prescription, peripheral placement rule)
    - src/ui/components/auto-ui/renderers/ElementRenderer.vue
  </read_first>
  <action>
    Create archetypes/GridBoardTemplate.vue. Classify topLevelChildren into boardElements ($layout grid/hex-grid), handElements ($type === 'hand'), and chromeElements (everything else) via computeds. Lay out with CSS grid-template-areas "chrome" / "board" / "hand" and grid-template-rows auto 1fr auto so the board area is the dominant 1fr, the hand strip is auto with max-height 30vh and overflow-x auto, and the chrome row is auto (collapses when empty). Render each classified child via ElementRenderer into its named slot. Non-grid peripheral elements (decks/discards) go in the chrome slot — NEVER given equal grid area alongside the board (C6 anti-pattern). When handElements is empty, the hand area collapses entirely. Scoped styles per UI-SPEC Template 1. Use .js import extensions.
  </action>
  <verify>
    <automated>npx eslint src/ui/components/auto-ui/archetypes/GridBoardTemplate.vue && npx vitest run src/ui/</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint src/ui/components/auto-ui/archetypes/GridBoardTemplate.vue` reports no errors
    - `grep -c "grid-template-areas" src/ui/components/auto-ui/archetypes/GridBoardTemplate.vue` returns >=1
    - `grep -c "1fr" src/ui/components/auto-ui/archetypes/GridBoardTemplate.vue` returns >=1 (board is the dominant area, not equal subdivision)
    - `grep -c "ElementRenderer" src/ui/components/auto-ui/archetypes/GridBoardTemplate.vue` returns >=1
    - `npx vitest run src/ui/` stays GREEN
  </acceptance_criteria>
  <done>Grid template lays out focal board (1fr) + docked hand (auto) + peripheral chrome; children via ElementRenderer; lints clean.</done>
</task>

<task type="auto">
  <name>Task 2: CardTemplate.vue + TableauTemplate.vue</name>
  <files>src/ui/components/auto-ui/archetypes/CardTemplate.vue, src/ui/components/auto-ui/archetypes/TableauTemplate.vue</files>
  <read_first>
    - .planning/phases/93-renderer-rebuild/93-PATTERNS.md (sections "archetypes/CardTemplate.vue" and "archetypes/TableauTemplate.vue")
    - .planning/phases/93-renderer-rebuild/93-UI-SPEC.md (sections "Template 2: CardTemplate" and "Template 3: TableauTemplate")
    - src/ui/components/auto-ui/renderers/ElementRenderer.vue
  </read_first>
  <action>
    Create archetypes/CardTemplate.vue: display flex, flex-direction column, height 100%. Hand/deck-dominant layout — hand(s) get the dominant flex: 1 area (centered), decks/discards are auto-height peripheral zones above. Render children via ElementRenderer; classify hands as the dominant zone vs peripheral (hands -> dominant, decks/other -> peripheral). Create archetypes/TableauTemplate.vue: display flex, flex-wrap wrap, gap 32px, height 100%, overflow auto. Render every child via ElementRenderer — this is the general catch-all; it must never produce a blank screen (every zone renders through its per-element renderer). Both scoped styles only; .js imports.
  </action>
  <verify>
    <automated>npx eslint src/ui/components/auto-ui/archetypes/CardTemplate.vue src/ui/components/auto-ui/archetypes/TableauTemplate.vue && npx vitest run src/ui/</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint src/ui/components/auto-ui/archetypes/CardTemplate.vue src/ui/components/auto-ui/archetypes/TableauTemplate.vue` reports no errors
    - `grep -c "flex: 1" src/ui/components/auto-ui/archetypes/CardTemplate.vue` returns >=1 (hand is dominant, not equal subdivision)
    - `grep -c "ElementRenderer" src/ui/components/auto-ui/archetypes/CardTemplate.vue` returns >=1 AND `grep -c "ElementRenderer" src/ui/components/auto-ui/archetypes/TableauTemplate.vue` returns >=1
    - `npx vitest run src/ui/` stays GREEN
  </acceptance_criteria>
  <done>Card template makes hands dominant; tableau wraps all zones via ElementRenderer; both lint clean.</done>
</task>

<task type="auto">
  <name>Task 3: UnsupportedTopologyPanel.vue (loud honest-fail boundary)</name>
  <files>src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue</files>
  <read_first>
    - src/ui/components/auto-ui/AutoUI.vue (lines 34-40, 55-62: the .game-complete prominent-panel approach to mirror)
    - .planning/phases/93-renderer-rebuild/93-PATTERNS.md (section "archetypes/UnsupportedTopologyPanel.vue" — LOCKED copy + amber CSS)
    - .planning/phases/93-renderer-rebuild/93-UI-SPEC.md (section "Template 4: UnsupportedTopologyPanel" + Copywriting Contract design requirements)
  </read_first>
  <action>
    Create archetypes/UnsupportedTopologyPanel.vue (takes no required props). Render a full-width prominent panel with the LOCKED heading "This layout cannot be auto-generated" and the LOCKED body "This game uses a layout that is outside the auto-UI's supported set (grid, hex, stack, hand). Build a custom UI component for this game — see the custom UI guide." — render both verbatim via text interpolation, do not paraphrase. Amber/warning treatment: heading color #f59e0b 20px 700; body white 16px; panel border-radius 8px, padding 24px, border 1px solid rgba(245,158,11,0.4), background rgba(245,158,11,0.1). NEVER show stack traces, element IDs, attribute names, or TypeScript types (CLAUDE.md: never leak implementation details). Must look LOUD — not a subtle gray placeholder. Scoped styles; .js imports.
  </action>
  <verify>
    <automated>npx eslint src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue && npx vitest run src/ui/</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue` reports no errors
    - `grep -c "This layout cannot be auto-generated" src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue` returns 1 (locked heading verbatim)
    - `grep -c "see the custom UI guide" src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue` returns 1 (locked actionable next step)
    - `grep -c "v-html" src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue` returns 0
    - `npx vitest run src/ui/` stays GREEN
  </acceptance_criteria>
  <done>Honest-fail panel renders the locked amber copy with an actionable next step and no implementation leakage; lints clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine gameView → archetype template | Top-level element classification reads game-authored $layout/$type attributes; templates render no raw author HTML themselves (children go through per-element renderers) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-13 | Information Disclosure | UnsupportedTopologyPanel copy | mitigate | Panel renders only the locked static copy — no element IDs, attribute names, stack traces, or types (CLAUDE.md never-leak rule; acceptance verifies fixed copy) |
| T-93-14 | Tampering (XSS) | template text/markup | mitigate | Templates render structure only; all author content flows through per-element renderers (XSS-guarded in 93-02/03/04); no `v-html` in templates |
| T-93-15 | Availability | TableauTemplate fallback | mitigate | Tableau is the never-blank catch-all so an addressable-but-unspecialized topology still renders every zone rather than a blank screen |
</threat_model>

<verification>
- `npx eslint src/ui/components/auto-ui/archetypes/*.vue` — clean
- `npx vitest run src/ui/` — green
- Locked honest-fail copy present verbatim (grep checks in acceptance criteria)
</verification>

<success_criteria>
- RENDER-03: three templates encode focal/docked/peripheral hierarchy, never equal-space subdivision; children dispatch via ElementRenderer
- RENDER-04: UnsupportedTopologyPanel is the loud, actionable honest-fail boundary with no implementation leakage
</success_criteria>

<output>
Create `.planning/phases/93-renderer-rebuild/93-05-SUMMARY.md` when done.
</output>
