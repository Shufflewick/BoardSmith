---
phase: 93-renderer-rebuild
plan: 06
type: execute
wave: 3
depends_on: ["93-01", "93-02", "93-03", "93-04", "93-05"]
files_modified:
  - src/ui/components/auto-ui/builtin-renderers.ts
  - src/ui/components/auto-ui/AutoRenderer.vue
  - src/ui/components/auto-ui/useAutoRendererAnimations.ts
  - src/ui/components/auto-ui/useAutoRendererAnimations.test.ts
  - src/ui/components/auto-ui/AutoUI.vue
  - src/ui/components/auto-ui/index.ts
  - src/ui/index.ts
  - src/ui/components/auto-ui/AutoElement.vue
  - src/ui/components/auto-ui/AutoGameBoard.vue
autonomous: true
requirements: [RENDER-01, RENDER-05]

must_haves:
  truths:
    - "builtin-renderers.ts registers all eight per-element renderers (card/hand/deck/die/grid/hex/piece/space) at module load with priority band 1-10 and test() functions derived from the old elementType detection"
    - "AutoRenderer.vue selects the archetype via selectArchetype and renders the matching template, providing playerSeat/selectableElements/selectedElements/defaultBackImage to the tree"
    - "AutoRenderer consumes useAnimationEvents (inject only — never createAnimationEvents) and registers deal/flip/reveal handlers via useAutoRendererAnimations; when the injection is undefined nothing throws"
    - "AutoUI.vue renders AutoRenderer (not AutoGameBoard); AutoElement.vue and AutoGameBoard.vue are deleted; registerRenderer is exported from boardsmith/ui"
    - "All substrate test files pass unchanged (git diff shows zero changes to them) and the full suite is green"
  artifacts:
    - path: "src/ui/components/auto-ui/builtin-renderers.ts"
      provides: "Module-load registration of the 8 built-in renderers"
    - path: "src/ui/components/auto-ui/AutoRenderer.vue"
      provides: "Renderer host: provides, archetype dispatch, animation wiring, FlyingCardsOverlay"
      min_lines: 60
    - path: "src/ui/components/auto-ui/useAutoRendererAnimations.ts"
      provides: "Testable animation-event wiring composable (RENDER-05)"
    - path: "src/ui/components/auto-ui/useAutoRendererAnimations.test.ts"
      provides: "RENDER-05 automated proof (handlers register/fire; undefined graceful)"
  key_links:
    - from: "src/ui/components/auto-ui/AutoUI.vue"
      to: "src/ui/components/auto-ui/AutoRenderer.vue"
      via: "import + render (replaces AutoGameBoard)"
      pattern: "AutoRenderer"
    - from: "src/ui/components/auto-ui/AutoRenderer.vue"
      to: "src/ui/composables/useAnimationEvents.ts"
      via: "useAnimationEvents inject (RENDER-05)"
      pattern: "useAnimationEvents"
    - from: "src/ui/index.ts"
      to: "src/ui/components/auto-ui/renderer-registry.ts"
      via: "public registerRenderer export"
      pattern: "registerRenderer"
---

<objective>
The integration point of Phase 93: register the eight built-in renderers, build the AutoRenderer host (archetype dispatch + provides + animation wiring), wire it into AutoUI.vue, export registerRenderer from boardsmith/ui, and DELETE AutoElement.vue + AutoGameBoard.vue — establishing a single source of truth with no dual path (D-01). RENDER-05 animation wiring is extracted into a testable composable so it has a real automated proof despite the absence of @vue/test-utils. RENDER-01 is proven by the substrate test files passing unchanged.

Purpose: RENDER-01 (replace + reuse substrate unchanged) and RENDER-05 (consume useAnimationEvents).
Output: builtin-renderers.ts, AutoRenderer.vue, useAutoRendererAnimations.ts (+test), AutoUI.vue swap, both index.ts updates, two old files deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-renderer-rebuild/93-PATTERNS.md
@.planning/phases/93-renderer-rebuild/93-RESEARCH.md
@.planning/phases/93-renderer-rebuild/93-UI-SPEC.md

<interfaces>
<!-- useAnimationEvents.ts (inject-only consumer; createAnimationEvents is GameShell-only): -->
useAnimationEvents(): UseAnimationEventsReturn | undefined   // undefined when not provided
registerHandler(eventType: string, handler: (event, { signal }) => Promise<void>|void, options: { skip: 'run' | 'drop' }): () => void
// AnimationEvent shape: { id: number; type: string; data: Record<string, unknown>; timestamp: number }

<!-- useFlyingElements.ts: -->
useFlyingElements(): { flyingElements, fly(...) }

<!-- engine animate API (src/engine/element/game.ts:2187): -->
game.animate(type: string, data: Record<string, unknown>, callback?: () => void): void
// IMPORTANT: event `type` is an arbitrary game-defined string. The gate games (hex, go-fish,
// checkers) do NOT emit 'deal'/'flip'/'reveal' from rules; demo-animation emits game.animate('demo').
// Verify actual emitted types by reading game source before finalizing handler set.

<!-- Provide keys AutoRenderer must provide (consumed by every per-element renderer): -->
'playerSeat' (number), 'selectableElements' (Ref<Set>), 'selectedElements' (Ref<Set>), 'defaultBackImage' (Ref<ImageInfo|null>)
DIE_ANIMATION_CONTEXT_KEY (from ../dice/die3d-state.js)

<!-- Registry (from 93-01): -->
registerRenderer({ test, component }), resolveRenderer(element)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: builtin-renderers.ts + AutoRenderer.vue host</name>
  <files>src/ui/components/auto-ui/builtin-renderers.ts, src/ui/components/auto-ui/AutoRenderer.vue</files>
  <read_first>
    - src/ui/components/auto-ui/AutoGameBoard.vue (lines 23-33 imports + die animation context provide; 63-65,108 provides; 74-105 findDefaultBackImage; 110-134 topLevelChildren + layoutClass; template + FlyingCardsOverlay usage)
    - src/ui/components/auto-ui/AutoElement.vue (lines 109-138: the elementType computed — its branch conditions become the built-in test() functions)
    - src/ui/components/auto-ui/renderer-registry.ts (registerRenderer signature; priority bands 1-10)
    - src/ui/components/auto-ui/archetype-selector.ts (selectArchetype)
    - .planning/phases/93-renderer-rebuild/93-PATTERNS.md (section "AutoRenderer.vue" — imports, provides, findDefaultBackImage, archetype computed, template; "renderer-registry.ts" built-in registration note)
    - src/ui/components/dice/die3d-state.js (DIE_ANIMATION_CONTEXT_KEY, createDieAnimationContext)
  </read_first>
  <action>
    Create builtin-renderers.ts: import registerRenderer and all eight renderer components (CardRenderer, HandRenderer, DeckRenderer, DieRenderer, GridBoardRenderer, HexBoardRenderer, PieceRenderer, SpaceRenderer). At module top-level (NOT inside onMounted — Pitfall 5) call registerRenderer once per component with a test(element) derived from the AutoElement elementType conditions (card -> $type==='card'; hand -> $type==='hand'; deck -> $type==='deck'; die -> $type==='die'; grid -> $layout==='grid'; hex -> $layout==='hex-grid'; piece -> leaf/implicit per AutoElement; space -> fallback). Assign priorities in band 1-10 with the generic Space at the lowest so specific types win; piece/space ordering must match AutoElement's detection so leaves resolve correctly. Each test returns -1 when not applicable.
    Create AutoRenderer.vue (replaces AutoGameBoard.vue): props { gameView: GameElement | null | undefined; playerSeat: number } (interaction-array props removed — useBoardInteraction provides those). Side-effect import './builtin-renderers.js' so registration runs at module load. provide DIE_ANIMATION_CONTEXT_KEY via createDieAnimationContext(); provide 'playerSeat'/'selectableElements'/'selectedElements'/'defaultBackImage' (copy findDefaultBackImage verbatim from AutoGameBoard 74-105). Compute topLevelChildren = gameView?.children ?? []. Compute archetype = selectArchetype(topLevelChildren). Template: render FlyingCardsOverlay, then v-if archetype==='grid-board' GridBoardTemplate, v-else-if 'card' CardTemplate, v-else-if 'unsupported' UnsupportedTopologyPanel, v-else TableauTemplate — passing :top-level-children and :game-view. Use .js imports. (Animation wiring is added in Task 2.)
  </action>
  <verify>
    <automated>npx eslint src/ui/components/auto-ui/builtin-renderers.ts src/ui/components/auto-ui/AutoRenderer.vue && npx vitest run src/ui/</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint src/ui/components/auto-ui/builtin-renderers.ts src/ui/components/auto-ui/AutoRenderer.vue` reports no errors
    - `grep -c "registerRenderer" src/ui/components/auto-ui/builtin-renderers.ts` returns >=8 (all eight built-ins registered)
    - `grep -c "onMounted" src/ui/components/auto-ui/builtin-renderers.ts` returns 0 (module-load registration — Pitfall 5)
    - `grep -c "selectArchetype" src/ui/components/auto-ui/AutoRenderer.vue` returns >=1
    - `grep -c "createAnimationEvents" src/ui/components/auto-ui/AutoRenderer.vue` returns 0 (inject-only — Pitfall 2)
    - `grep -Ec "GridBoardTemplate|CardTemplate|TableauTemplate|UnsupportedTopologyPanel" src/ui/components/auto-ui/AutoRenderer.vue` returns >=4
    - `npx vitest run src/ui/` stays GREEN
  </acceptance_criteria>
  <done>All eight renderers register at module load; AutoRenderer dispatches the four archetypes and provides the context chain; lints clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useAutoRendererAnimations.ts wiring composable + test (RENDER-05)</name>
  <files>src/ui/components/auto-ui/useAutoRendererAnimations.ts, src/ui/components/auto-ui/useAutoRendererAnimations.test.ts, src/ui/components/auto-ui/AutoRenderer.vue</files>
  <read_first>
    - src/ui/composables/useAnimationEvents.ts (registerHandler signature, skip semantics 'run'|'drop', AnimationEvent shape, createAnimationEvents for the test)
    - src/ui/composables/useAnimationEvents.test.ts (analog test structure — createAnimationEvents, enqueue, assert handler ran; no @vue/test-utils available)
    - src/ui/composables/useFlyingElements.ts (fly signature for the deal/reveal handler bodies)
    - src/ui/components/auto-ui/AutoGameBoard.vue (lines 148-438: the OLD FLIP/fly watching being replaced by semantic event handlers — read to understand intent, do NOT copy the heuristic DOM-querying)
    - ~/BoardSmithGames/demo-animation/src/rules/actions.ts (game.animate('demo', ...) — a real semantic emit to confirm event/data shape)
    - ~/BoardSmithGames/go-fish/src, ~/BoardSmithGames/hex/src, ~/BoardSmithGames/checkers/src (confirm whether rules emit any game.animate types; A1-A2 verification)
    - .planning/phases/93-renderer-rebuild/93-PATTERNS.md (Shared Patterns: Animation Event Inject; section "AutoRenderer.test.ts")
  </read_first>
  <behavior>
    - Given a created animation-events instance passed in, useAutoRendererAnimations registers handlers for 'deal', 'flip', and 'reveal'
    - When a 'deal' event is enqueued/processed, the registered deal handler runs (assert via a spy on the injected fly or a captured side-effect)
    - When the animationEvents argument is undefined, useAutoRendererAnimations returns without throwing and registers nothing
    - Handlers read event.data defensively (missing expected keys → skip silently, never throw) — assert a 'deal' with empty data does not throw
  </behavior>
  <action>
    Create useAutoRendererAnimations.ts exporting a function useAutoRendererAnimations(animationEvents: UseAnimationEventsReturn | undefined, flyApi: { fly: ... }): void. If animationEvents is undefined, return immediately (graceful no-op — RENDER-05 robustness; A3). Otherwise registerHandler for 'deal' (skip:'drop') driving flyApi.fly from source to destination using defensive event.data access (data?.sourceId ?? data?.elementId etc.); 'flip' (skip:'run') driving the CSS flip-by-id; 'reveal' (skip:'drop') as the fly+flip combo. Each handler checks signal.aborted early and tolerates missing data keys without throwing. Then update AutoRenderer.vue to call useAutoRendererAnimations(useAnimationEvents(), { fly }) where fly comes from useFlyingElements() — inject only, never createAnimationEvents. If reading the gate/demo game source shows additional semantic event types are emitted, register those too; document the actual emitted types in the SUMMARY so the browser plan (93-07) knows what to look for. Write useAutoRendererAnimations.test.ts covering the four behavior cases using createAnimationEvents (mirrors useAnimationEvents.test.ts) and a stub fly spy — no component mounting required.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/auto-ui/useAutoRendererAnimations.test.ts && npx eslint src/ui/components/auto-ui/useAutoRendererAnimations.ts src/ui/components/auto-ui/AutoRenderer.vue</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/components/auto-ui/useAutoRendererAnimations.test.ts` is GREEN with the four behavior cases
    - A test passes `undefined` as animationEvents and asserts no throw (graceful)
    - A test enqueues a 'deal' event and asserts the deal handler ran (fly spy called)
    - `grep -c "createAnimationEvents" src/ui/components/auto-ui/useAutoRendererAnimations.ts` returns 0 (consumer-only)
    - `grep -c "useAutoRendererAnimations" src/ui/components/auto-ui/AutoRenderer.vue` returns >=1 (host calls it)
    - SUMMARY records the actual game.animate event types emitted by the gate/demo games (A1-A2 finding)
  </acceptance_criteria>
  <done>Animation wiring is a tested composable: deal/flip/reveal handlers register and fire, undefined is graceful, AutoRenderer consumes it.</done>
</task>

<task type="auto">
  <name>Task 3: Wire AutoUI.vue, update exports, DELETE old renderer, prove substrate unchanged</name>
  <files>src/ui/components/auto-ui/AutoUI.vue, src/ui/components/auto-ui/index.ts, src/ui/index.ts, src/ui/components/auto-ui/AutoElement.vue, src/ui/components/auto-ui/AutoGameBoard.vue</files>
  <read_first>
    - src/ui/components/auto-ui/AutoUI.vue (lines 14,42-46: the AutoGameBoard import + usage to swap)
    - src/ui/components/auto-ui/index.ts (current AutoElement/AutoGameBoard exports to replace)
    - src/ui/index.ts (lines 33-42 auto-ui export block; 117-128 animation-events export block as the model for the new registerRenderer export)
    - .planning/phases/93-renderer-rebuild/93-PATTERNS.md (sections "AutoUI.vue", "auto-ui/index.ts", "src/ui/index.ts" — exact diffs)
    - .planning/phases/93-renderer-rebuild/93-RESEARCH.md (Substrate Preservation: exact substrate test file list; Export Changes Required)
  </read_first>
  <action>
    Edit AutoUI.vue: replace the AutoGameBoard import with AutoRenderer and the <AutoGameBoard> tag with <AutoRenderer> (same :game-view and :player-seat props); leave the flowState banner and game-complete slot unchanged. Edit auto-ui/index.ts: remove the AutoElement and AutoGameBoard exports; add `export { default as AutoRenderer } from './AutoRenderer.vue'` and `export { registerRenderer, resolveRenderer } from './renderer-registry.js'`; keep AutoUI/ActionPanel/type exports. Edit src/ui/index.ts: in the auto-ui export block replace AutoGameBoard/AutoElement with AutoRenderer, and add a public `export { registerRenderer } from './components/auto-ui/renderer-registry.js'` (resolveRenderer stays internal — not exported from src/ui/index.ts) following the animation-events export-block pattern. DELETE src/ui/components/auto-ui/AutoElement.vue and src/ui/components/auto-ui/AutoGameBoard.vue (D-01 single source of truth, no dual path, no feature flag). Then grep the whole src/ tree for any lingering references to AutoElement/AutoGameBoard and fix them. Finally run the full suite and confirm the substrate test files are unmodified.
  </action>
  <verify>
    <automated>npm test && git diff --stat src/ui/composables/useActionController.test.ts src/ui/composables/useActionController.picks.test.ts src/ui/composables/useBoardInteraction.test.ts src/ui/composables/useDragDropTargets.test.ts src/ui/composables/useAnimationEvents.test.ts src/ui/composables/useGameViewEnrichment.test.ts src/ui/components/auto-ui/auto-ui-helpers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` is GREEN (full suite) — RENDER-01 substrate proof
    - `git diff --stat` over the seven substrate/helper test files (useActionController.test.ts, useActionController.picks.test.ts, useBoardInteraction.test.ts, useDragDropTargets.test.ts, useAnimationEvents.test.ts, useGameViewEnrichment.test.ts, auto-ui-helpers.test.ts) shows ZERO changed lines
    - `git diff --stat src/ui/composables/useActionController.ts src/ui/composables/useBoardInteraction.ts src/ui/composables/useDragDropTargets.ts src/ui/composables/useFLIP.ts src/ui/composables/useFlyingElements.ts` shows ZERO changes (substrate source untouched)
    - `test ! -e src/ui/components/auto-ui/AutoElement.vue && test ! -e src/ui/components/auto-ui/AutoGameBoard.vue` succeeds (both deleted)
    - `grep -rn "AutoElement\|AutoGameBoard" src/` returns 0 matches (no stragglers; no dual path)
    - `grep -c "registerRenderer" src/ui/index.ts` returns >=1 (public export per D-03); `grep -c "resolveRenderer" src/ui/index.ts` returns 0 (kept internal)
    - `grep -c "AutoRenderer" src/ui/components/auto-ui/AutoUI.vue` returns >=1
  </acceptance_criteria>
  <done>AutoRenderer is THE auto-UI renderer; old files deleted with no stragglers; registerRenderer public; full suite green with substrate provably unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine animation events → renderer handlers | game.animate() event data (Record<string,unknown>, no schema) reaches the deal/flip/reveal handlers |
| game author code → boardsmith/ui registerRenderer | Public registerRenderer export is authored TypeScript, same trust as game code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-16 | Denial of Service | animation handlers | mitigate | Handlers read event.data defensively (missing keys → silent skip), check signal.aborted, and never throw on malformed data; useAnimationEvents already isolates handler errors from the queue |
| T-93-17 | Tampering | useAnimationEvents queue ownership | mitigate | AutoRenderer is inject-only (useAnimationEvents); never calls createAnimationEvents (Pitfall 2) — a single queue owned by GameShell |
| T-93-18 | Information Disclosure | animation event data | accept | Event data originates from the engine which has already applied toJSONForPlayer visibility filtering; the renderer adds no new exposure path |
| T-93-19 | Tampering | substrate composables | mitigate | RENDER-01 proof: substrate source and test files are not modified; verified by `git diff --stat` returning zero and the full suite staying green |
| T-93-SC | Tampering | npm installs | n/a | No package installs (RESEARCH Package Legitimacy Audit: zero new dependencies) |
</threat_model>

<verification>
- `npm test` — full suite green
- `git diff --stat` over substrate source + test files — zero changes (RENDER-01)
- `grep -rn "AutoElement\|AutoGameBoard" src/` — zero matches (D-01 single source of truth)
- `npx vitest run src/ui/components/auto-ui/useAutoRendererAnimations.test.ts` — green (RENDER-05)
</verification>

<success_criteria>
- RENDER-01: AutoRenderer replaces AutoElement+AutoGameBoard (deleted), wired into AutoUI; substrate provably unchanged (zero diff + suite green)
- RENDER-05: useAnimationEvents consumed inject-only; deal/flip/reveal handlers tested; undefined graceful
- registerRenderer exported from boardsmith/ui; resolveRenderer internal
- No dual path, no feature flag, no straggler references (D-01)
</success_criteria>

<output>
Create `.planning/phases/93-renderer-rebuild/93-06-SUMMARY.md` when done. Record the actual game.animate event types emitted by the gate/demo games (A1-A2 finding) so plan 93-07 knows what to verify in the browser.
</output>
