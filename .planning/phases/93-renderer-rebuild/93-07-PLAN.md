---
phase: 93-renderer-rebuild
plan: 07
type: execute
wave: 4
depends_on: ["93-06"]
files_modified: []
autonomous: false
requirements: [RENDER-03, RENDER-05]

must_haves:
  truths:
    - "Each of the three gate games (Hex, Go Fish, Checkers) renders in the auto-UI pane without console errors using the new AutoRenderer"
    - "The selected archetype reads as visual hierarchy (focal board / docked hand / peripheral chrome for grid games; hand-dominant for card games) — not equal-space subdivision"
    - "A game that emits game.animate() semantic events plays the auto-UI animation choreography (deal/flip/reveal or the actual emitted type) — the old auto path's silence is gone"
  artifacts: []
  key_links: []
---

<objective>
Prove the rebuilt renderer works in a live browser: the three gate games render under the new AutoRenderer with readable archetype hierarchy (RENDER-03), and a game that emits semantic animation events plays the auto-UI choreography (RENDER-05). This is the real-behavior gate the VALIDATION strategy requires before /gsd:verify-work. Interaction still flows through the existing footer ActionPanel (playability migration is Phase 94) — this plan verifies RENDER + ANIMATION only.

Purpose: RENDER-03 (visual hierarchy judgment) + RENDER-05 (visible animation) — both are manual-only verifications per 93-VALIDATION.md.
Output: human-verified confirmation (no source files changed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-renderer-rebuild/93-VALIDATION.md
@.planning/phases/93-renderer-rebuild/93-06-SUMMARY.md
@.planning/phases/93-renderer-rebuild/93-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight regression gate</name>
  <files>(no files modified — runs npm test and greps src/)</files>
  <read_first>
    - .planning/phases/93-renderer-rebuild/93-06-SUMMARY.md (which game.animate event types the gate/demo games actually emit — the animation vehicle for Task 2)
    - .planning/phases/93-renderer-rebuild/93-RESEARCH.md (Substrate Preservation test list)
  </read_first>
  <action>
    Run the full suite green as the precondition for the browser checkpoint, and re-confirm the substrate is unchanged. From the 93-06 SUMMARY, identify the game (gate or demo-animation) that emits a semantic game.animate event — that is the animation vehicle for Task 2. If the SUMMARY records that NO available game emits a semantic animation event, note it here: RENDER-05's automated proof (useAutoRendererAnimations.test.ts) stands, and Task 2 will use demo-animation (game.animate('demo')) or surface the gap to the user rather than silently passing.
  </action>
  <verify>
    <automated>npm test && grep -rn "AutoElement\|AutoGameBoard" src/ | wc -l | grep -qx 0 && echo CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` is GREEN
    - `grep -rn "AutoElement\|AutoGameBoard" src/` returns 0 matches
    - The animation vehicle game (the one emitting a semantic game.animate type) is named for Task 2
  </acceptance_criteria>
  <done>Full suite green, no old-renderer stragglers, animation vehicle identified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser smoke-test gate games (render + animation)</name>
  <files>(verification only — no files modified)</files>
  <action>
    Start a local dev session for each gate game pointed at this BoardSmith working copy and present the Auto-Generated UI pane for human verification (steps in how-to-verify). Kill every dev server you start before resuming.
  </action>
  <what-built>
    AutoElement.vue + AutoGameBoard.vue were replaced by AutoRenderer.vue, which selects a hierarchy-bearing archetype template (grid-board / card / tableau) by introspection, dispatches each element through the ranked-tester registry to the eight built-in renderers, lays out grid/hex via closed-form math, and wires deal/flip/reveal animation events via useAnimationEvents. Interaction is unchanged (existing footer ActionPanel).
  </what-built>
  <how-to-verify>
    Point each gate game at THIS BoardSmith working copy using the project's existing local-dev mechanism (local tarball / npm link — the same workflow used in prior phases). If a gate game cannot be pointed at the working tree without the Phase 96 re-vendor, STOP and report that as a blocker rather than skipping verification.

    Use the Claude Code Chrome extension to open and observe each game's Auto-Generated UI pane:

    1. HEX (grid-board archetype): run `boardsmith dev` for ~/BoardSmithGames/hex. In the Auto-Generated pane confirm: the hex board renders as the FOCAL (dominant) area via SVG hex polygons; no console errors; player stones appear on cells. Confirm the layout is NOT an equal-space grid of boxes.
    2. GO FISH (card archetype): run the go-fish game. Confirm: hands are the dominant area, decks/discard sit as peripheral zones above; face-down cards show the back (never the face image); no console errors.
    3. CHECKERS (grid-board archetype): run checkers. Confirm: the 8x8 board renders from declared coordinates as the focal area with pieces on correct cells; no console errors.
    4. ANIMATION (RENDER-05): run the animation-vehicle game named in Task 1 (demo-animation emits game.animate('demo'); or a gate game if one emits a semantic type). In the Auto-Generated pane, trigger the action that emits the animation event and confirm the auto-UI plays visible choreography (movement/flip), proving the new renderer consumes useAnimationEvents — the old auto path was silent here.

    For each: confirm no "unsupported topology" panel appears for these four supported games, and the honest-fail panel DOES appear only if you point at a free-form-layout game (optional spot check).

    Kill any dev server you started before resuming (project rule: never leave a dev server running).
  </how-to-verify>
  <verify>
    <human-check>Human confirms all three gate games render in the Auto-Generated pane with no console errors, hierarchy reads correctly, and the animation-vehicle game plays visible choreography.</human-check>
  </verify>
  <acceptance_criteria>
    - Hex, Go Fish, and Checkers each render in the Auto-Generated pane with no console errors
    - Grid games show focal-board hierarchy; Go Fish shows hand-dominant hierarchy (not equal-space subdivision)
    - The animation-vehicle game plays visible auto-UI choreography when its semantic event fires
    - No dev server is left running after the checkpoint
  </acceptance_criteria>
  <done>Three gate games render under the new renderer with readable hierarchy and visible animation; human approved.</done>
  <resume-signal>Type "approved" if all four render correctly and animation plays, or describe the specific game + symptom (e.g. "go-fish auto pane: blank hand area / console error X") so a gap-closure plan can target it.</resume-signal>
</task>

</tasks>

<threat_model>
This plan changes no source files — it is a verification-only checkpoint. No new trust boundary or threat surface is introduced. The only operational note: the dev server started for verification must be killed afterward (project rule), which is an availability/hygiene concern, not a security threat. The XSS / image-URL / animation-data threats relevant to this phase are mitigated in the implementation plans (T-93-03/05/07/08/16) and are not re-introduced here.
</threat_model>

<verification>
- `npm test` green (Task 1)
- Human-verified browser render of all three gate games + animation playback (Task 2)
- No old-renderer references remain in src/
</verification>

<success_criteria>
- RENDER-03: archetype hierarchy reads correctly in the browser for grid and card games (manual sign-off)
- RENDER-05: animation choreography visibly plays in the auto-UI for a semantic-event-emitting game (manual sign-off)
- All three gate games render under the new renderer with no console errors; no dev server left running
</success_criteria>

<output>
Create `.planning/phases/93-renderer-rebuild/93-07-SUMMARY.md` when done, recording the human verification result per game and any gaps to feed /gsd:verify-work or a gap-closure plan.
</output>
