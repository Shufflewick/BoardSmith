---
phase: 94-interaction-presentation-playability
plan: 02
type: execute
wave: 2
depends_on: [94-01]
files_modified:
  - src/ui/composables/useActionController.ts
  - src/ui/composables/useActionControllerTypes.ts
  - src/ui/components/GameShell.vue
  - src/ui/components/auto-ui/ActionPanel.vue
  - src/ui/composables/useActionController.test.ts
  - src/ui/components/auto-ui/ActionPanel.test.ts
autonomous: true
requirements: [INTERACT-01, INTERACT-02]
must_haves:
  truths:
    - "When no action is in progress (currentPick === null) the footer ActionPanel is visible (player can start an action)"
    - "When every active choice is board-anchored the footer is ABSENT from the DOM (not merely hidden)"
    - "A board UI can force the footer absent via :suppress-action-panel and replace it via the #action-panel slot"
    - "For a mixed-anchor choice pick, the panel shows ONLY the unanchored choices (anchored choices never duplicated)"
  artifacts:
    - path: "src/ui/composables/useActionController.ts"
      provides: "allCurrentChoicesAnchored computed (false when no pick; true for element/elements picks)"
      contains: "allCurrentChoicesAnchored"
    - path: "src/ui/components/GameShell.vue"
      provides: "footer v-if guard + suppressActionPanel prop + #action-panel slot"
      contains: "suppressActionPanel"
  key_links:
    - from: "src/ui/components/GameShell.vue"
      to: "actionController.allCurrentChoicesAnchored"
      via: "v-if on the <footer> element (NOT v-show, NOT on <ActionPanel>)"
      pattern: "v-if=.*allCurrentChoicesAnchored"
    - from: "src/ui/components/auto-ui/ActionPanel.vue"
      to: "filteredChoices"
      via: "exclude choices with refs.length > 0 when pick.type === 'choice' and some are anchored"
      pattern: "refs ?? \\[\\]"
---

<objective>
Make the footer ActionPanel auto-absent and suppressible (D-02 / INTERACT-02) and apply the
hybrid-layout filter so anchored choices are never duplicated in the panel (D-03 / INTERACT-01).
Board-centric is the zero-config default; the panel is the fallback.

Purpose: Board games get a board-only surface by default — the footer stops wasting space and
stops duplicating affordances that already live on the board.
Output: `allCurrentChoicesAnchored` computed + GameShell footer `v-if` + `suppress-action-panel`
prop/`#action-panel` slot + ActionPanel `filteredChoices` anchored-exclusion filter, all tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-interaction-presentation-playability/94-RESEARCH.md
@.planning/phases/94-interaction-presentation-playability/94-PATTERNS.md
@.planning/phases/94-interaction-presentation-playability/94-UI-SPEC.md

<interfaces>
<!-- Exact insertion bodies in 94-PATTERNS.md "D-02: useActionController.ts" and
     "D-02: GameShell.vue", and 94-UI-SPEC.md "ActionPanel Suppression Contract". -->

New controller export:
  allCurrentChoicesAnchored: ComputedRef<boolean>
  // false when currentPick === null  (Pitfall 4 — player must be able to start an action)
  // true  when pick.type is 'element' | 'elements'
  // false when pick.type is 'number' | 'text'
  // for 'choice': true iff choices.length > 0 && every choice has (refs ?? []).length > 0

GameShell prop surface (add):
  suppressActionPanel?: boolean   (withDefaults: false)
  Footer: <footer v-if="!props.suppressActionPanel && !actionController.allCurrentChoicesAnchored.value">
            <slot name="action-panel"><ActionPanel .../></slot> ... </footer>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add allCurrentChoicesAnchored computed + controller return type</name>
  <files>src/ui/composables/useActionController.ts, src/ui/composables/useActionControllerTypes.ts, src/ui/composables/useActionController.test.ts</files>
  <read_first>
    - 94-PATTERNS.md "D-02: src/ui/composables/useActionController.ts" (computed body + where to insert + return-object + UseActionControllerReturn type addition)
    - src/ui/composables/useActionController.ts:599-662 (currentPick / isReady / validElements computed pattern, getCurrentChoices)
    - 94-RESEARCH.md "Common Pitfalls" Pitfall 4 (null pick must return false)
    - 94-UI-SPEC.md "ActionPanel Suppression Contract" Auto-Absent Rule (the full truth table)
    - existing src/ui/composables/useActionController.test.ts (test harness analog)
  </read_first>
  <behavior>
    - allCurrentChoicesAnchored === false when currentPick is null.
    - allCurrentChoicesAnchored === true when pick.type === 'element'.
    - allCurrentChoicesAnchored === true when pick.type === 'elements'.
    - allCurrentChoicesAnchored === false when pick.type === 'choice' and no choice has refs.
    - allCurrentChoicesAnchored === true when pick.type === 'choice' and every choice has refs.length > 0.
  </behavior>
  <action>
    Add an `allCurrentChoicesAnchored` computed in useActionController.ts immediately after `isReady`
    (per the PATTERNS.md insertion point), following the existing computed style that reads
    `currentPick.value` and `getCurrentChoices()`. Implement the exact branch order: null → false;
    'element'/'elements' → true; not 'choice' → false; 'choice' → choices.length > 0 && every choice
    has `(refs ?? []).length > 0`. Add it to the controller's returned object. In
    useActionControllerTypes.ts add `allCurrentChoicesAnchored: ComputedRef<boolean>` to
    `UseActionControllerReturn` (after `pendingOnServer`). Add the five behavior cases above to
    useActionController.test.ts. No fenced code in this action — use the PATTERNS.md body.
  </action>
  <verify>
    <automated>npx vitest run src/ui/composables/useActionController.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The five behavior cases above are present and passing in useActionController.test.ts.
    - `grep -n "allCurrentChoicesAnchored" src/ui/composables/useActionController.ts src/ui/composables/useActionControllerTypes.ts` matches in both files (computed + return + type).
    - The computed returns `false` for a null pick (explicit Pitfall-4 guard test).
  </acceptance_criteria>
  <done>The controller exposes a typed `allCurrentChoicesAnchored` that is false at rest and true only when every active choice is board-anchored; five cases green.</done>
</task>

<task type="auto">
  <name>Task 2: GameShell footer v-if + suppress prop/slot, and ActionPanel hybrid filter</name>
  <files>src/ui/components/GameShell.vue, src/ui/components/auto-ui/ActionPanel.vue, src/ui/components/auto-ui/ActionPanel.test.ts</files>
  <read_first>
    - 94-PATTERNS.md "D-02: src/ui/components/GameShell.vue" (GameShellProps add, withDefaults add, footer v-if + #action-panel slot)
    - src/ui/components/GameShell.vue:75-98 (GameShellProps + withDefaults) and :1279-1301 (current unconditional footer)
    - 94-PATTERNS.md "D-01/D-03: ActionPanel.vue" the "D-03 new filter" block (filteredChoices addition at ~224-251, AFTER dedup)
    - 94-RESEARCH.md "Common Pitfalls" Pitfall 2 (v-if not v-show; at <footer> level) and Pitfall 5 (only filter for 'choice' picks)
    - 94-UI-SPEC.md "Hybrid Layout (D-03 / INTERACT-01)" table + "Empty panel fallback" copy
  </read_first>
  <action>
    GameShell.vue: add an optional `suppressActionPanel?: boolean` to GameShellProps and
    `suppressActionPanel: false` to withDefaults. Wrap the existing footer with
    `v-if="!props.suppressActionPanel && !actionController.allCurrentChoicesAnchored.value"` placed on
    the `<footer>` element itself (NEVER v-show, NEVER on <ActionPanel> — Pitfall 2: the whole footer
    chrome must vanish). Wrap the existing <ActionPanel> in a `<slot name="action-panel">` so a board UI
    can override the entire panel while keeping footer chrome (D-02 escape hatch). The actionController
    binding already exists in GameShell's setup; reuse it.
    ActionPanel.vue: in the `filteredChoices` computed, AFTER the existing deduplication block, add the
    D-03 anchored-exclusion: only when `currentPick.value?.type === 'choice'` and at least one choice has
    `(refs ?? []).length > 0`, filter OUT every choice whose `(refs ?? []).length > 0` (keep only the
    unanchored remainder). Do not filter for element picks. Preserve the existing empty-state copy "Tap a
    highlighted element to continue." for the defensive empty-remainder case.
    Create ActionPanel.test.ts covering: anchored choices excluded when some are anchored (choice pick);
    all choices kept when none anchored; filter does not apply for element picks.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/auto-ui/ActionPanel.test.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "GameShell" | grep -q '^0$' && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "v-if=" src/ui/components/GameShell.vue | grep "allCurrentChoicesAnchored"` matches and it is on the `<footer>` line (the literal `game-shell__action-bar` footer), not on `<ActionPanel>`.
    - `grep -c "v-show" src/ui/components/GameShell.vue` on the action-bar footer region is 0 (Pitfall 2).
    - `grep -n "suppressActionPanel" src/ui/components/GameShell.vue` shows both the prop and the withDefaults entry; `grep -n "name=\"action-panel\"" src/ui/components/GameShell.vue` matches (slot present).
    - ActionPanel.test.ts proves: mixed-anchor choice pick yields only unanchored choices in filteredChoices; all-unanchored yields all; element pick is unaffected.
    - `npm run test` green.
  </acceptance_criteria>
  <done>Footer is absent (DOM-removed) when all choices are anchored or when suppressed; the panel shows only the unanchored remainder for mixed picks; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI state → rendered affordances | the computed decides whether the action surface (panel) is reachable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-02-01 | Denial of Service | allCurrentChoicesAnchored mis-returns true at rest, hiding the panel so the player can never act | mitigate | explicit null-pick → false guard (Pitfall 4) with a dedicated unit test; failure is detectable in the gate |
| T-94-02-02 | Elevation of Privilege | suppress-action-panel exposes no privileged action; it only hides UI | accept | suppression is presentational; server-side pick validation is unchanged and authoritative |
| T-94-02-SC | Tampering | npm/pip/cargo installs | n/a | Phase installs zero packages — no package-manager tasks in this plan |
</threat_model>

<verification>
- `npm run test` green; new ActionPanel.test.ts + 5 controller cases passing.
- Manual DOM check deferred to the playability gate (94-06): footer is removed from the DOM (not display:none) when all choices anchored.
</verification>

<success_criteria>
- ROADMAP criterion 1 (board-anchored choices actioned on board; footer ABSENT when all anchored) — auto-absent computed + footer v-if delivered.
- ROADMAP criterion 2 (footer suppressible; GameShell no longer mounts it unconditionally) — prop + slot + v-if delivered.
</success_criteria>

<output>
Create `.planning/phases/94-interaction-presentation-playability/94-02-SUMMARY.md` when done.
</output>
