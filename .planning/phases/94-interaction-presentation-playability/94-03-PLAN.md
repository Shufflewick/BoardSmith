---
phase: 94-interaction-presentation-playability
plan: 03
type: execute
wave: 2
depends_on: [94-01]
files_modified:
  - src/ui/components/auto-ui/renderers/GridBoardRenderer.vue
  - src/ui/components/auto-ui/renderers/HexBoardRenderer.vue
  - src/ui/components/auto-ui/renderers/PieceRenderer.vue
  - src/ui/components/auto-ui/renderers/CardRenderer.vue
  - src/ui/components/auto-ui/renderers/HandRenderer.vue
  - src/ui/components/auto-ui/renderers/SpaceRenderer.vue
  - src/ui/components/auto-ui/renderers/DeckRenderer.vue
  - src/ui/components/auto-ui/renderers/DieRenderer.vue
autonomous: true
requirements: [INTERACT-01]
must_haves:
  truths:
    - "Clicking a selectable board cell/piece/card dispatches the action via the shared useBoardInteraction substrate"
    - "Every board-interactive element expresses the six required interaction states (default/hover/selectable/selected/highlighted/disabled)"
    - "A multi-ref action highlights every cell in the refs array simultaneously (source + target + intermediate)"
    - "Notation-keyed elements match refs even when notation lives in element.attributes (Pitfall 6)"
  artifacts:
    - path: "src/ui/components/auto-ui/renderers/HexBoardRenderer.vue"
      provides: "hex cell click + highlight/selected/selectable states via tryUseBoardInteraction"
      contains: "tryUseBoardInteraction"
    - path: "src/ui/components/auto-ui/renderers/PieceRenderer.vue"
      provides: "piece click handler + isHighlighted computed"
      contains: "triggerElementSelect"
  key_links:
    - from: "renderers/*.vue"
      to: "useBoardInteraction"
      via: "tryUseBoardInteraction().isSelectableElement / isHighlighted / triggerElementSelect"
      pattern: "tryUseBoardInteraction"
    - from: "renderers/*.vue interaction calls"
      to: "matchesRef"
      via: "elementIdentity { id, name, notation } passed to every interaction method"
      pattern: "notation: .*attributes"
---

<objective>
Wire board-centric click/highlight affordances into the eight Phase-93 per-element renderers using
the EXISTING `useBoardInteraction` substrate (D-03 / INTERACT-01). Clicking the board IS the action;
the renderers surface the six interaction states and the simultaneous multi-ref highlight from D-01.

Purpose: Make the auto-UI playable by direct board manipulation — the precondition for the three
playability gates. The interaction substrate is reused unchanged; only the renderers gain affordances.
Output: All 8 renderers expose selectable/highlight/selected/disabled states + click dispatch, using
a shared notation-aware element-identity helper (Pitfall 6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-interaction-presentation-playability/94-PATTERNS.md
@.planning/phases/94-interaction-presentation-playability/94-UI-SPEC.md

<interfaces>
<!-- GridBoardRenderer.vue ALREADY contains the complete click+highlight template (analog = itself).
     PATTERNS.md "D-03 Interaction: GridBoardRenderer.vue" is the canonical pattern to copy. -->

Substrate methods (existing, do NOT modify the substrate):
  tryUseBoardInteraction(): returns undefined outside GameShell (use the try-variant)
  .isSelectableElement({id,name,notation}) / .isHighlighted(..) / .isSelected(..)
  .isDisabledElement(..) / .isDropTarget(..) / .triggerElementSelect(..) / .selectElement(..)
  .isDraggedElement(..) / .startDrag(..) / .triggerDrop(..)

Element identity helper (apply in every renderer — Pitfall 6):
  elementIdentity(el) => { id: el.id, name: el.name, notation: el.attributes?.notation }

Six required states + CSS classes: 94-UI-SPEC.md "Interaction Affordance Contract" table
  (.action-selectable, .is-board-selected, .is-board-highlighted, .is-drop-target, .is-disabled).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire gate-critical board renderers (Grid, Hex, Piece)</name>
  <files>src/ui/components/auto-ui/renderers/GridBoardRenderer.vue, src/ui/components/auto-ui/renderers/HexBoardRenderer.vue, src/ui/components/auto-ui/renderers/PieceRenderer.vue</files>
  <read_first>
    - 94-PATTERNS.md "D-03 Interaction: GridBoardRenderer.vue" (the complete reference template, lines 83-126 + CSS class bindings) and "D-03 Interaction: PieceRenderer.vue" (add click handler + isHighlighted)
    - src/ui/components/auto-ui/renderers/GridBoardRenderer.vue (current interaction code — the template), HexBoardRenderer.vue (boardInteraction already at ~line 37; SVG polygon context), PieceRenderer.vue:44-74 (existing isActionSelectable + drag)
    - 94-RESEARCH.md "Common Pitfalls" Pitfall 6 (notation in attributes) + 94-UI-SPEC.md "Multi-ref Simultaneous Highlight"
    - 94-UI-SPEC.md "Interaction Affordance Contract" (six states + exact color/class spec)
  </read_first>
  <action>
    Confirm GridBoardRenderer already implements the full isCellHighlighted/isCellSelected/
    isCellActionSelectable/isCellDropTarget/handleCellClick set with the CSS class bindings; if any of
    the six states (default/hover/selectable/selected/highlighted/disabled) or the disabled guard is
    missing, complete it per the UI-SPEC contract. Introduce a shared `elementIdentity(el)` helper that
    returns `{ id, name, notation: el.attributes?.notation }` and route ALL interaction calls through it
    (Pitfall 6) — used by every cell/piece interaction method so notation-keyed refs (Checkers squares,
    Hex coords) match. In HexBoardRenderer, add the same isCellHighlighted/isCellSelected/
    isCellActionSelectable/handleCellClick functions (boardInteraction already injected) and bind them to
    each hex `<polygon>` with the SVG fill/stroke highlight treatment from the UI-SPEC color table. In
    PieceRenderer, add the missing `handleClick` (guard on isActionSelectable, call triggerElementSelect)
    and the missing `isHighlighted` computed; bind `@click` and `:class="{ 'is-board-highlighted': isHighlighted }"`.
    Each renderer calls `boardInteraction.isHighlighted(elementIdentity(el))` per element so a multi-ref
    action highlights all matching cells at once (no special multi-ref logic needed — the substrate tracks
    all highlights). No fenced code in this action — copy the PATTERNS.md GridBoardRenderer template verbatim.
  </action>
  <verify>
    <automated>npx vitest run src/ui/composables/useBoardInteraction.test.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -Ec "GridBoardRenderer|HexBoardRenderer|PieceRenderer" | grep -q '^0$' && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "tryUseBoardInteraction" src/ui/components/auto-ui/renderers/{GridBoardRenderer,HexBoardRenderer,PieceRenderer}.vue` lists all three.
    - HexBoardRenderer.vue and PieceRenderer.vue each contain a `triggerElementSelect` call and an `isHighlighted`/`isCellHighlighted` computed/function.
    - All three pass an identity object including `notation: ...attributes?.notation` (grep `notation:` near attributes) — Pitfall 6 satisfied.
    - The six interaction CSS classes from the UI-SPEC contract are bound in each renderer's template.
    - `useBoardInteraction.test.ts` is green (substrate unchanged) and tsc reports no errors in the three files.
  </acceptance_criteria>
  <done>Grid, Hex, and Piece renderers dispatch clicks and render all six states via the shared substrate; multi-ref highlight surfaces per cell; behavioral proof lands in the 94-06 gate.</done>
</task>

<task type="auto">
  <name>Task 2: Wire card-family, zone, and leaf renderers (Card/Hand/Space/Deck/Die)</name>
  <files>src/ui/components/auto-ui/renderers/CardRenderer.vue, src/ui/components/auto-ui/renderers/HandRenderer.vue, src/ui/components/auto-ui/renderers/SpaceRenderer.vue, src/ui/components/auto-ui/renderers/DeckRenderer.vue, src/ui/components/auto-ui/renderers/DieRenderer.vue</files>
  <read_first>
    - 94-PATTERNS.md "D-03 Interaction: CardRenderer, HandRenderer, SpaceRenderer, DeckRenderer, DieRenderer" (which analog each follows: PieceRenderer for click-selectable; GridBoardRenderer for zones)
    - src/ui/components/auto-ui/renderers/PieceRenderer.vue (click-selectable analog after Task 1) and GridBoardRenderer.vue (zone/drop-target analog)
    - 94-UI-SPEC.md "Interaction Affordance Contract" (states) + "Go Fish — Playability Acceptance" (hand-zone click, deck draw)
    - 94-RESEARCH.md Pitfall 6 (reuse the elementIdentity helper from Task 1)
  </read_first>
  <action>
    For the click-selectable leaf renderers (CardRenderer, DeckRenderer, DieRenderer): import
    tryUseBoardInteraction, add `isActionSelectable` and `isHighlighted` computeds keyed by
    elementIdentity(element), add a guarded `handleClick` that calls triggerElementSelect, and bind
    `@click` + the selectable/highlighted CSS classes (PieceRenderer analog). For the zone renderers
    (HandRenderer, SpaceRenderer): follow the GridBoardRenderer analog — highlight/selected/selectable per
    the zone element plus drop-target support (isDropTarget + handleDragOver + handleDrop via
    boardInteraction.triggerDrop) so a hand zone can be clicked (Go Fish "ask" target) and a space can be
    a drop zone. Reuse the same elementIdentity helper (extract it to a shared local util if convenient,
    but do NOT modify the substrate). No fenced code — follow the PATTERNS.md per-renderer analog mapping.
  </action>
  <verify>
    <automated>npx vitest run src/ui/composables/ && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -Ec "CardRenderer|HandRenderer|SpaceRenderer|DeckRenderer|DieRenderer" | grep -q '^0$' && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `grep -L "tryUseBoardInteraction" src/ui/components/auto-ui/renderers/{CardRenderer,HandRenderer,SpaceRenderer,DeckRenderer,DieRenderer}.vue` is EMPTY (every file imports the substrate).
    - Click-selectable renderers (Card/Deck/Die) each contain a `triggerElementSelect` call guarded by `isActionSelectable`.
    - Zone renderers (Hand/Space) each contain `isDropTarget` and a drop handler calling `triggerDrop`.
    - All five files typecheck (tsc reports 0 errors for them) and the composables suite is green.
  </acceptance_criteria>
  <done>All remaining renderers expose click/highlight/drop affordances via the shared substrate; the full renderer set is interaction-wired for the gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| click event → action dispatch | a board click triggers triggerElementSelect, which crosses into the action pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-03-01 | Tampering | a click dispatches selection for an element not in validElements | mitigate | triggerElementSelect validates against validElements before invoking onElementSelect (existing useBoardInteraction guard); handleClick also guards on isActionSelectable/!isDisabled before dispatch |
| T-94-03-02 | Information Disclosure | a renderer highlights a hidden element, leaking its presence | accept | hidden elements appear as `{ __hidden: true }` with no valid selection/ref; isSelectableElement returns false for them so no selectable affordance is shown |
| T-94-03-SC | Tampering | npm/pip/cargo installs | n/a | Phase installs zero packages — no package-manager tasks in this plan |
</threat_model>

<verification>
- `useBoardInteraction.test.ts` + composables suite green (substrate provably unchanged).
- `tsc --noEmit` clean for all 8 renderers.
- Behavioral interaction proof (click → move, multi-jump highlight) is the human gate in 94-06.
</verification>

<success_criteria>
- ROADMAP criterion 1 (board-anchored choices actioned directly on the board) — click affordances delivered across all renderers.
- ROADMAP criterion 3 surfacing half (multi-ref highlights ALL relevant cells simultaneously) — every renderer calls isHighlighted per element; exercised by Checkers multi-jump in 94-05/94-06.
</success_criteria>

<output>
Create `.planning/phases/94-interaction-presentation-playability/94-03-SUMMARY.md` when done.
</output>
