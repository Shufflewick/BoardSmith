---
phase: 94-interaction-presentation-playability
plan: 05
type: execute
wave: 4
depends_on: [94-01, 94-04]
files_modified:
  - ~/BoardSmithGames/hex/src/ui/App.vue
  - ~/BoardSmithGames/hex/src/ui/presentation.ts
  - ~/BoardSmithGames/go-fish/src/rules/actions.ts
  - ~/BoardSmithGames/go-fish/src/ui/App.vue
  - ~/BoardSmithGames/go-fish/src/ui/presentation.ts
  - ~/BoardSmithGames/checkers/src/rules/actions.ts
  - ~/BoardSmithGames/checkers/src/ui/App.vue
  - ~/BoardSmithGames/checkers/src/ui/presentation.ts
autonomous: true
requirements: [INTERACT-01, INTERACT-03, PRESENT-01, PRESENT-03]
must_haves:
  truths:
    - "Hex, Go Fish, and Checkers each slot ONLY <AutoUI> into the #game-board slot (no custom UI code in the gate path)"
    - "Go Fish and Checkers action callbacks emit the refs array shape ({ refs: [{ ref, role }] }); no singular sourceRef/targetRef remain"
    - "Checkers multi-jump emits intermediate captured squares as role:'highlight' refs so all squares highlight at once"
    - "Each game declares a PresentationOverlay sibling file and passes it as :presentation"
  artifacts:
    - path: "~/BoardSmithGames/checkers/src/rules/actions.ts"
      provides: "boardRefs returning refs array incl. multi-jump highlight refs"
      contains: "role:"
    - path: "~/BoardSmithGames/hex/src/ui/presentation.ts"
      provides: "hexPresentationOverlay export"
      contains: "PresentationOverlay"
  key_links:
    - from: "game App.vue #game-board slot"
      to: "<AutoUI :presentation=... :game-view=... :player-seat=...>"
      via: "split-screen custom panel removed"
      pattern: "<AutoUI"
    - from: "checkers boardRefs"
      to: "refs array with source/target/highlight roles"
      via: "capturedNotations mapped to role:'highlight'"
      pattern: "capturedNotations"
---

<objective>
Prepare the three gate games for auto-UI-only playability (INTERACT-01/03, PRESENT-01/03): migrate
their action callbacks to the D-01 `refs` array, collapse each `App.vue` to slot ONLY `<AutoUI>` into
`#game-board`, declare a per-game `PresentationOverlay` sibling file, and emit Checkers multi-jump
highlight refs (the D-01 capability proof).

Purpose: Without these cross-repo edits the gate games still use the old singular-ref API and the
split-screen scaffold — they cannot exercise the new board-centric path. This plan makes them ready.
Output: 3 games migrated + 3 overlay files + multi-jump refs in Checkers.

PRECONDITION (every task): the game must consume the CURRENT local BoardSmith build (which exports the
`refs` API and `PresentationOverlay` from `boardsmith/ui`). Rebuild BoardSmith and refresh the game's
local link/vendored copy using the project's standard local-tarball workflow (v1.2). Failure signal: if
`import type { PresentationOverlay } from 'boardsmith/ui'` does not resolve in the game, the link is stale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-interaction-presentation-playability/94-PATTERNS.md
@.planning/phases/94-interaction-presentation-playability/94-UI-SPEC.md
@.planning/phases/94-interaction-presentation-playability/94-RESEARCH.md

<interfaces>
<!-- Exact game-side before/after in 94-PATTERNS.md "Playability Gate: Game App.vue files",
     "Playability Gate: Game src/rules/actions.ts — D-01 boardRefs migration", and
     "New Presentation Overlay Files". Per-game overlay minimums in 94-UI-SPEC.md tables. -->

boardRefs new shape (choice picks): (choice, ctx) => ({ refs: [{ ref: {...}, role: 'source'|'target'|'highlight' }] })
Hex boardRef (element pick): UNCHANGED — returns BoardElementRef; pick-handler wraps it as role:'highlight'.
App.vue gate shape: <GameShell :presentation="<overlay>"> ... <template #game-board="{ gameView, playerSeat, state }">
                      <AutoUI :game-view="gameView || null" :player-seat="playerSeat" :flow-state="state?.flowState" /> </template>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hex — AutoUI-only App.vue + presentation overlay</name>
  <files>~/BoardSmithGames/hex/src/ui/App.vue, ~/BoardSmithGames/hex/src/ui/presentation.ts</files>
  <read_first>
    - 94-PATTERNS.md "Playability Gate: Game App.vue files" (split-screen → AutoUI-only) and "New Presentation Overlay Files" (hex overlay)
    - ~/BoardSmithGames/hex/src/ui/App.vue (current split-screen) and ~/BoardSmithGames/hex/src/rules/actions.ts:22-27 (boardRef — confirm unchanged)
    - 94-UI-SPEC.md "Hex — Playability Acceptance" + per-game overlay table (HexCell/Cell label suppression)
  </read_first>
  <action>
    Confirm Hex's element-selection `boardRef` needs NO change (pick-handler wraps it as role:'highlight').
    Create ~/BoardSmithGames/hex/src/ui/presentation.ts exporting `hexPresentationOverlay: PresentationOverlay`
    (import type from 'boardsmith/ui') with a minimal byClass entry suppressing the cell name label (stones
    inherit player color). Edit App.vue: remove the split-screen "Custom UI / Auto-Generated UI" comparison;
    the `#game-board` slot must contain ONLY `<AutoUI :game-view="gameView || null" :player-seat="playerSeat"
    :flow-state="state?.flowState as any" />`; pass `:presentation="hexPresentationOverlay"` on `<GameShell>`;
    drop the now-unused slot destructuring (players/isMyTurn/availableActions) and the HexBoard import. No fenced
    code — follow the PATTERNS.md gate App.vue shape.
  </action>
  <verify>
    <automated>set -o pipefail; cd ~/BoardSmithGames/hex && (npm run build 2>&1 | tail -5)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "board-comparison\|Custom UI\|Auto-Generated" ~/BoardSmithGames/hex/src/ui/App.vue` is 0 (split-screen removed).
    - `grep -n "<AutoUI" ~/BoardSmithGames/hex/src/ui/App.vue` matches and `grep -n ":presentation" ~/BoardSmithGames/hex/src/ui/App.vue` matches.
    - ~/BoardSmithGames/hex/src/ui/presentation.ts exports `hexPresentationOverlay` typed `PresentationOverlay`.
    - `npm run build` in the hex game completes without type errors.
  </acceptance_criteria>
  <done>Hex renders through AutoUI only, with its overlay applied; build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Go Fish — refs migration + AutoUI-only App.vue + overlay</name>
  <files>~/BoardSmithGames/go-fish/src/rules/actions.ts, ~/BoardSmithGames/go-fish/src/ui/App.vue, ~/BoardSmithGames/go-fish/src/ui/presentation.ts</files>
  <read_first>
    - ~/BoardSmithGames/go-fish/src/rules/actions.ts:32-75 (the `ask` action: `target` boardRefs and `rank` boardRefs both currently return `targetRef`)
    - 94-PATTERNS.md "Playability Gate: Game src/rules/actions.ts" (go-fish boardRefs migration) + "New Presentation Overlay Files" (go-fish overlay)
    - 94-UI-SPEC.md "Go Fish — Playability Acceptance" + per-game overlay table (PlayerHand: { label: 'Hand' })
  </read_first>
  <action>
    Migrate BOTH boardRefs callbacks in the `ask` action to the refs array shape: the `target` pick returns
    `{ refs: [{ ref: { id: hand.id }, role: 'target' }] }`; the `rank` pick currently links to the first card
    of the rank — migrate it FAITHFULLY to `{ refs: [{ ref: { id: cards[0].id }, role: 'target' }] }` and keep
    the empty-`{ refs: [] }` branch when no card matches. Do NOT change Go Fish game logic beyond the refs
    shape (Prove Before Fix — preserve existing anchor semantics; any hybrid-panel UX observation is recorded
    in the 94-06 gate, not patched here). Create ~/BoardSmithGames/go-fish/src/ui/presentation.ts exporting
    `goFishPresentationOverlay` with `byClass.PlayerHand: { label: 'Hand' }`. Collapse App.vue to AutoUI-only
    in `#game-board` with `:presentation="goFishPresentationOverlay"`, same shape as Task 1. No fenced code.
  </action>
  <verify>
    <automated>set -o pipefail; cd ~/BoardSmithGames/go-fish && npm test 2>&1 | tail -8 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sourceRef\|targetRef" ~/BoardSmithGames/go-fish/src/rules/actions.ts` is 0 (singular fields gone); `grep -c "role:" ~/BoardSmithGames/go-fish/src/rules/actions.ts` is ≥ 2.
    - `grep -c "board-comparison\|Custom UI\|Auto-Generated" ~/BoardSmithGames/go-fish/src/ui/App.vue` is 0; `<AutoUI` and `:presentation` both present.
    - go-fish presentation.ts exports `goFishPresentationOverlay` typed `PresentationOverlay`.
    - Go Fish test suite + build complete without type errors.
  </acceptance_criteria>
  <done>Go Fish uses the refs API, renders through AutoUI only with its overlay, and its suite/build is green.</done>
</task>

<task type="auto">
  <name>Task 3: Checkers — refs migration incl. multi-jump highlight + AutoUI-only App.vue + overlay</name>
  <files>~/BoardSmithGames/checkers/src/rules/actions.ts, ~/BoardSmithGames/checkers/src/ui/App.vue, ~/BoardSmithGames/checkers/src/ui/presentation.ts</files>
  <read_first>
    - ~/BoardSmithGames/checkers/src/rules/actions.ts:36-44 (DestinationChoice interface) + :139-155 (boardRefs returning sourceRef/targetRef)
    - 94-PATTERNS.md "Playability Gate: Game src/rules/actions.ts" (checkers boardRefs migration) + "New Presentation Overlay Files" (checkers overlay)
    - 94-RESEARCH.md "Code Examples → Refs array in a choice pick" (multi-jump capturedNotations) + Open Questions #3 (extend DestinationChoice)
    - 94-UI-SPEC.md "Checkers — Playability Acceptance" (multi-jump highlight; King promotion label option)
  </read_first>
  <action>
    Extend the `DestinationChoice` interface with `capturedNotations?: string[]` and populate it from the
    move's capture path when building destination choices (the squares jumped over in a single or multi-jump).
    Migrate the `boardRefs` callback to return `{ refs: [ { ref: { notation: choice.fromNotation }, role: 'source' },
    { ref: { notation: choice.toNotation }, role: 'target' }, ...choice.capturedNotations.map(n => ({ ref: { notation: n },
    role: 'highlight' })) ] }` — so a multi-jump highlights source + destination + every intermediate captured
    square simultaneously (the D-01 capability proof). Create ~/BoardSmithGames/checkers/src/ui/presentation.ts
    exporting `checkersPresentationOverlay` with `byClass.CheckerPiece: { label: '' }`, `byClass.Square: { label: '' }`,
    and optionally `byAttribute.isKing.true: { label: 'K' }` for king status. Collapse App.vue to AutoUI-only with
    `:presentation="checkersPresentationOverlay"`, same shape as Task 1. No fenced code — follow the PATTERNS.md maps.
  </action>
  <verify>
    <automated>set -o pipefail; cd ~/BoardSmithGames/checkers && npm test 2>&1 | tail -8 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sourceRef\|targetRef" ~/BoardSmithGames/checkers/src/rules/actions.ts` is 0; the boardRefs return contains role 'source', 'target', and 'highlight' entries.
    - `grep -n "capturedNotations" ~/BoardSmithGames/checkers/src/rules/actions.ts` matches in both the DestinationChoice interface and the boardRefs mapping.
    - `grep -c "board-comparison\|Custom UI\|Auto-Generated" ~/BoardSmithGames/checkers/src/ui/App.vue` is 0; `<AutoUI` and `:presentation` present.
    - checkers presentation.ts exports `checkersPresentationOverlay` typed `PresentationOverlay`.
    - Checkers test suite + build complete without type errors.
  </acceptance_criteria>
  <done>Checkers uses the refs API with multi-jump highlight refs, renders through AutoUI only with its overlay, and its suite/build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game author callbacks → session | boardRefs() emits the refs that become highlight metadata |
| game overlay file → auto-UI | per-game PresentationOverlay paints visuals on the filtered gameView |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-05-01 | Information Disclosure | a game overlay declares an image for a hidden card class (e.g. Go Fish face-down), re-exposing identity | mitigate | the auto-UI resolver strips image/stats for __hidden elements (94-04 PRESENT-02 guard); overlays here declare only labels/hand text, no face images |
| T-94-05-02 | Tampering | multi-jump highlight refs reference squares not on the board | accept | notation refs are highlight-only metadata; matchesRef simply finds-or-skips; no selection/authorization derives from them |
| T-94-05-SC | Tampering | npm/pip/cargo installs | mitigate | no new packages added; the only dependency action is refreshing the existing local BoardSmith link/tarball (v1.2 workflow), not a registry install |
</threat_model>

<verification>
- Each game's `npm run build` (and `npm test` where present) is green against the current local BoardSmith.
- `grep -rc "sourceRef\|targetRef" ~/BoardSmithGames/{go-fish,checkers}/src/rules/actions.ts` is 0.
- Each game's App.vue `#game-board` slot contains only `<AutoUI>` and passes `:presentation`.
</verification>

<success_criteria>
- ROADMAP criterion 3 (multi-step action highlights all relevant source/target cells simultaneously) — Checkers multi-jump refs deliver it end-to-end.
- Gate games are auto-UI-only and overlay-equipped, unblocking criteria 6/7/8 (proved in 94-06).
</success_criteria>

<output>
Create `.planning/phases/94-interaction-presentation-playability/94-05-SUMMARY.md` when done.
</output>
