---
phase: 94.1-auto-ui-interaction-completion
plan: "01"
subsystem: ui/testing
tags: [test-infra, vue-component-tests, jsdom, devdependencies]
dependency_graph:
  requires: []
  provides: [component-test-harness, vue-sfc-transform, jsdom-env]
  affects: [src/ui/components/auto-ui/ActionPanel.vue]
tech_stack:
  added: ["@vue/test-utils@^2.4.11", "jsdom@^29.1.1", "@vitejs/plugin-vue@^5.2.4"]
  patterns: [per-file-jsdom-pragma, vue-sfc-vitest-transform]
key_files:
  created:
    - src/ui/components/auto-ui/ActionPanel.smoke.test.ts
  modified:
    - vitest.config.ts
    - package.json
    - package-lock.json
decisions:
  - "jsdom chosen over happy-dom for SVG class/attribute support (HexBoardRenderer)"
  - "Per-file @vitest-environment jsdom pragma preserves global node env for all other tests"
  - "plugins: [vue()] at config root (sibling of test:) is required for SFC .vue imports to compile"
metrics:
  duration: "~10 minutes"
  completed: "2026-06-21"
  tasks_completed: 2
  files_changed: 4
---

# Phase 94.1 Plan 01: Vue Component Test Infrastructure Summary

**One-liner:** Vue SFC component test harness using @vue/test-utils + jsdom + @vitejs/plugin-vue with per-file jsdom pragma, proving ActionPanel mounts in vitest.

## What Was Built

Stood up the Vue component/interaction test infrastructure (locked decision D-01) that was missing from the project. Three approved devDependencies were installed, vitest was configured to transform `.vue` SFCs, and a smoke test proves a real `src/ui/components/auto-ui` SFC mounts under jsdom in `vitest run`.

This is the structural mechanism that prevents the Phase-94 non-convergence: controller-unit tests passed while the browser failed because no test ever mounted a real Vue component. NOTHING else in Phase 94.1 may land before this plan.

## Tasks Completed

| Task | Name | Commit | Key Files |
|------|------|--------|-----------|
| 1 | Dependency approval gate (SATISFIED BY HUMAN BEFORE EXECUTION) | — | — |
| 2 | Install deps, register Vue SFC transform, prove mount works | 2475d2f | package.json, vitest.config.ts, ActionPanel.smoke.test.ts |

**Task 1 note:** The human explicitly approved all three devDependencies before this execution agent was spawned (`<dependency_approval_RESOLVED>` in execution context). Legitimacy gate satisfied: all three packages verified on npm registry by human.

## Verification Results

### Smoke test
```
npx vitest run src/ui/components/auto-ui/ActionPanel.smoke.test.ts
 ✓ ActionPanel smoke test > mounts a real SFC in jsdom and produces DOM  (1 test) 10ms
```

### Full suite
```
Tests  1 failed | 893 passed (894)
```
- 893 = 892 baseline (e46d450) + 1 new smoke test
- 1 failure = pre-existing known `followUp+skip+auto-execute` fail (unchanged from baseline)
- No new failures introduced

### Grep checks (acceptance criteria)
- `grep -c "plugins" vitest.config.ts` → 1 (plugin registered)
- `grep -c "environment: 'node'" vitest.config.ts` → 1 (global node env preserved)
- First line of smoke test: `// @vitest-environment jsdom` ✓

## Deviations from Plan

None — plan executed exactly as written.

**Authentication gates:** None encountered.

## Known Stubs

None. The smoke test intentionally uses a minimal controller to prove mounting works. It is not a stub — it tests exactly what it claims (harness functionality), not interaction behavior. The interaction behavior tests land in Plans 94.1-02 through 94.1-04.

## Threat Flags

None. This plan adds no new network endpoints, auth paths, file access patterns, or schema changes. The only new supply-chain surface (three devDependencies) was verified by human on npm registry before install (legitimacy gate T-94.1-SC satisfied).

## Self-Check: PASSED

- `src/ui/components/auto-ui/ActionPanel.smoke.test.ts` — FOUND
- `vitest.config.ts` — FOUND (modified)
- `package.json` — FOUND (modified)
- Commit 2475d2f — VERIFIED (git log)
