---
phase: 94.1-auto-ui-interaction-completion
plan: 03
type: execute
wave: 3
depends_on: ["94.1-02"]
files_modified:
  - src/ui/composables/interaction-integration.test.ts
  - src/ui/composables/useBoardActionBridge.ts
autonomous: true
requirements: [R3, R4]
must_haves:
  truths:
    - "Board-selecting a Checkers piece (first element pick) triggers the destination choice fetch so the next step has populated, clickable choices in BOTH the footer and on the board (parity)"
    - "A multi-jump destination highlights source + target + captured cells simultaneously via refs[] roles"
    - "Hex still places a stone via a single board click (regression guard intact)"
  artifacts:
    - path: "src/ui/composables/interaction-integration.test.ts"
      provides: "Real controller+bridge+board integration tests for Checkers move/multi-jump and the Hex regression guard"
      min_lines: 60
    - path: "src/ui/composables/useBoardActionBridge.ts"
      provides: "Board-select of a multi-step source advances the pick AND triggers next-step choice fetch/population"
      contains: "fetchChoicesForPick"
  key_links:
    - from: "src/ui/composables/useBoardActionBridge.ts"
      to: "src/ui/composables/useActionController.ts fetchChoicesForPick"
      via: "board-select of the source pick fills the arg and the next pick's choices are fetched"
      pattern: "fetchChoicesForPick|fill\\("
    - from: "Checkers destination choices"
      to: "board cells + footer"
      via: "choicesForBoard (reactive currentChoices) and filteredChoices both populate"
      pattern: "currentChoices\\.value"
---

<objective>
Close Bug A: a Checkers move cannot complete via the auto-UI. Selecting a red piece (board click)
highlights its square, but the destination `chooseFrom` step surfaces no selectable destination cells
and (pre-D-02) the footer was suppressed → stuck. With plan 02's foundation (reactive panel + D-02
footer) the footer now reappears; this plan proves — and, if a residual gap remains in the bridge,
fixes — that board-selecting the multi-step SOURCE actually triggers the next step's choice
fetch/population so destinations are clickable on the board AND listed in the footer (parity). Also
verify multi-jump highlights source+target+captured cells simultaneously via refs[] roles, and guard
the Hex single-click regression.

Per RESEARCH Pitfalls 2/3: `startAction` in useBoardActionBridge.ts calls `board.clear()` after
`controller.start()`, and may call `board.setCurrentAction` with the already-filled selection index;
the watcher re-populates on the next `snapshotVersion` tick. The integration test must drive the real
controller+bridge (not a mock) and `await nextTick()` enough times to flush the clear→repopulate cycle.

Purpose: Prove the Checkers playability gate at the component-integration level before the browser run.
Output: Integration tests (RED→GREEN) for Checkers move + multi-jump + Hex guard, and any bridge fix
needed to trigger next-step choice population on board-select.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/94.1-auto-ui-interaction-completion/94.1-CONTEXT.md
@.planning/phases/94.1-auto-ui-interaction-completion/94.1-RESEARCH.md

<interfaces>
useBoardActionBridge.ts (verified anchors):
- `choicesForBoard` computed (~line 122-143): reads the REACTIVE `controller.currentChoices.value` —
  this path is already correct; the gap is whether the NEXT pick's choices get FETCHED after a
  board-select of the source.
- `startAction` (~line 181-183): calls `controller.start()` (which may pre-fill the first arg), then
  `board.clear()`, then `board.setCurrentAction(firstSel.name)`. Pitfalls 2/3: clear wipes just-set
  validElements; setCurrentAction may use the already-filled index. The watcher corrects on the next tick.
- The watcher `watch([currentPick, filteredValidElements, choicesForBoard], ...)` repopulates board
  selectability when `snapshotVersion` changes.

Checkers move action (~/BoardSmithGames/checkers/src/rules/actions.ts, for fixture shape only — DO NOT
import it): `chooseElement('piece')` then `chooseFrom('destination', { boardRefs: choice => ({ refs: [
{ ref:{notation: fromNotation}, role:'source' }, { ref:{notation: toNotation}, role:'target' },
...capturedNotations.map(n => ({ ref:{notation:n}, role:'highlight' })) ] }), filterBy:{ key:'pieceId',
selectionName:'piece' } })`. Replicate this SHAPE as an in-repo fixture (test-local constants).

useActionController.fetchChoicesForPick stores choices via Map.set + `snapshotVersion++` (see plan 02).
Board-select must reach this so the destination step populates.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Checkers move/multi-jump + Hex regression integration tests</name>
  <read_first>
    - src/ui/composables/useBoardActionBridge.ts (full — startAction, choicesForBoard, the watcher)
    - src/ui/composables/useBoardInteraction.ts (isSelectableElement, matchesRef precedence "F22": id-bearing ref matches by id only)
    - src/ui/composables/useActionController.ts:530-700 (fetchChoicesForPick, currentChoices, validElements)
    - src/ui/components/auto-ui/ActionPanel.interaction.test.ts (the harness + provide pattern from plan 02)
    - ~/BoardSmithGames/checkers/src/rules/actions.ts (move action shape — for the fixture only, do not import)
    - ~/BoardSmithGames/hex (Hex single-click placement — for the fixture only)
  </read_first>
  <behavior>
    - Test A1 (Checkers destination populates after source board-select): real useActionController +
      useBoardActionBridge + useBoardInteraction, two-step move fixture (element piece + choice destination
      with notation refs + filterBy pieceId), `fetchPickChoices` returns validElements for the piece pick
      and notation destination choices (e.g. a5/c5) for the destination pick. Simulate a board click on a
      piece (drive the bridge's board-select path, not controller.fill directly). After flushing ticks,
      assert: (a) controller.currentChoices.value has the destination choices; (b) the board exposes those
      destination cells as selectable (isSelectableElement / board.validElements reflects notation cells);
      (c) parity — the footer (filteredChoices) also lists them. EXPECTED RED if board-select does not
      trigger the destination fetch.
    - Test A2 (multi-jump highlight): a destination choice whose boardRefs carry source(role:source) +
      target(role:target) + 2 captured(role:highlight). Assert all three roles' cells are highlighted
      simultaneously (the bridge surfaces all refs[], not just target).
    - Test A3 (Hex regression guard): single-step `chooseElement` placement fixture; one board click on an
      empty hex selects+executes the placement (board.validElements present, click triggers the place). Must
      be GREEN before and after this plan — it is the regression guard, not a bug repro.
    - All fixtures are test-local constants. Do NOT import from ~/BoardSmithGames.
  </behavior>
  <action>
    Create src/ui/composables/interaction-integration.test.ts with the `// @vitest-environment jsdom`
    pragma. Wire the REAL useActionController + useBoardActionBridge + useBoardInteraction together (no
    mock controller — RESEARCH anti-pattern: mock controller misses the fill→fetchChoicesForPick→
    snapshotVersion chain). Define the Checkers move fixture and Hex placement fixture as test-local
    ActionMetadata. Write Tests A1, A2, A3 per <behavior>, awaiting nextTick enough times to flush the
    board.clear()→repopulate cycle (Pitfalls 2/3). Run the file: A1 (and A2 if the gap affects highlights)
    EXPECTED RED; A3 EXPECTED GREEN. Do NOT modify useBoardActionBridge.ts in this task.
  </action>
  <verify>
    <automated>npx vitest run src/ui/composables/interaction-integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File src/ui/composables/interaction-integration.test.ts exists, first line `// @vitest-environment jsdom`.
    - Uses the REAL controller+bridge+interaction (grep: contains `useBoardActionBridge(` and `useActionController(`; does NOT import from `BoardSmithGames`).
    - A1 is RED if board-select does not populate the destination step; A3 (Hex) is GREEN.
    - The RED assertion text (e.g. empty currentChoices or no selectable destination cells) is captured in task notes.
    - No production source modified in this task.
  </acceptance_criteria>
  <done>Integration tests reproduce the Checkers destination-population gap and assert multi-jump highlight + the Hex regression guard; RED/GREEN baseline captured.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — board-select of a multi-step source triggers next-step choice fetch</name>
  <read_first>
    - src/ui/composables/useBoardActionBridge.ts (startAction + the watcher — the fix site)
    - src/ui/composables/useActionController.ts (start/fill paths that call fetchChoicesForPick — match the bridge to whichever path populates the next pick)
    - src/ui/composables/interaction-integration.test.ts (A1/A2 must go GREEN)
  </read_first>
  <action>
    Make Test A1 (and A2 if needed) GREEN by ensuring the bridge's board-select of the multi-step SOURCE
    advances to the next pick AND triggers that pick's choice fetch — the same `fetchChoicesForPick(nextSel.name)`
    the panel-driven `fill()`/`start()` path runs. Root-cause first (Prove Before Fix): trace why the
    panel-driven flow fetches the destination choices but the board-select path does not (likely `startAction`
    fills the source via `start({args})` but does not reach the next-pick fetch, and/or `board.clear()` wipes
    state before the watcher re-fetches per Pitfalls 2/3). Apply the minimal correct fix in
    useBoardActionBridge.ts so board-select reaches the next-step fetch and the destination choices populate in
    both `choicesForBoard` and the footer. Verify the `filterBy:{key:'pieceId',selectionName:'piece'}` filter
    matches the board-selected piece arg (Pitfall 4: currentArgs vs collectedPicks must agree — if they
    diverge, fix the source so both carry the selected pieceId). Do NOT introduce dummy/hardcoded choices to
    force the test green — the choices must come from the real fetch. Do NOT remove existing debug logging
    unless the bug is confirmed fixed.
  </action>
  <verify>
    <automated>npx vitest run src/ui/composables/interaction-integration.test.ts src/ui/</automated>
  </verify>
  <acceptance_criteria>
    - Tests A1 and A2 are GREEN; A3 (Hex) remains GREEN.
    - useBoardActionBridge.ts board-select path reaches the next-step choice fetch (grep: `fetchChoicesForPick` or the controller fill/advance call is present on the board-select path).
    - Parity holds: the same destination choices appear in `choicesForBoard` AND `filteredChoices` (both read reactive currentChoices) — asserted in A1.
    - No new failures across `npx vitest run src/ui/`; full `npm test` green at baseline (no new failures beyond the known pre-existing one).
    - No dummy/fallback data introduced; choices originate from `fetchPickChoices`.
  </acceptance_criteria>
  <done>Board-selecting a Checkers piece populates clickable destinations on the board and in the footer; multi-jump highlights all refs[] roles; Hex single-click still works; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-side interaction wiring only; no network/auth/persistence/input surface added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.1-03 | Tampering | board-select → choice fetch path | accept | Choices are fetched from the controller (server-filtered) on board-select exactly as on panel-select; no new authority is granted to the client. Filter parity (Pitfall 4) is verified so the client cannot act on a piece it did not select. |
| T-94.1-01 | Information disclosure | Checkers board (full-information game) | accept | Checkers has no hidden state; highlighting source/target/captured cells leaks nothing. PRESENT-02 (hidden-card) property is N/A to Checkers and untouched. |
</threat_model>

<verification>
- `npx vitest run src/ui/composables/interaction-integration.test.ts` green (A1/A2/A3).
- `npm test` green at the e46d450 baseline with no new failures.
</verification>

<success_criteria>
- Checkers move completes via board-select (destinations populate + clickable + footer parity).
- Multi-jump highlights source+target+captured simultaneously.
- Hex single-click regression guard remains green.
</success_criteria>

<output>
Create `.planning/phases/94.1-auto-ui-interaction-completion/94.1-03-SUMMARY.md` when done.
</output>
