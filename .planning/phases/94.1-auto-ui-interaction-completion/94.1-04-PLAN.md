---
phase: 94.1-auto-ui-interaction-completion
plan: 04
type: execute
wave: 4
depends_on: ["94.1-03"]
files_modified:
  - src/ui/components/auto-ui/GoFishAsk.interaction.test.ts
  - src/ui/composables/useActionController.ts
  - src/ui/composables/useBoardActionBridge.ts
autonomous: true
requirements: [R2]
must_haves:
  truths:
    - "On a Go Fish player's turn the 'ask' action surfaces an interaction surface: the footer is present with populated target/rank choices (id-only refs keep the footer per D-02)"
    - "In a 2-player game the single target auto-fills and the rank pick's choices populate reactively (no 'No options available')"
    - "The Go Fish opponent-hand visibility property is not regressed by the test: an opponent's face-down hand exposes only back.svg with anonymized ids (PRESENT-02)"
  artifacts:
    - path: "src/ui/components/auto-ui/GoFishAsk.interaction.test.ts"
      provides: "Mounted-component test reproducing the Go Fish ask 'no affordance/panel' failure + the root-cause assertion + PRESENT-02 no-regress guard"
      min_lines: 50
  key_links:
    - from: "Go Fish ask action (target + rank, id-only refs)"
      to: "footer ActionPanel"
      via: "allCurrentChoicesAnchored=false keeps footer; reactive currentChoices populates rank choices"
      pattern: "currentChoices\\.value|allCurrentChoicesAnchored"
---

<objective>
Close Bug B: on a Go Fish player's turn, the 'ask' action shows 0 `.action-selectable` and a
suppressed footer — no affordance and no panel — so the turn cannot complete. RESEARCH Open Question 2
flags a CONTRADICTION that must be RESOLVED BY TRACING before any fix: id-only refs SHOULD yield
`allCurrentChoicesAnchored=false` (footer present), yet the browser shows the footer suppressed. Two
candidate causes: (1) a second action available simultaneously (e.g. an implicit draw/goFish) prevents
`tryAutoStartSingleAction` from firing, leaving `currentPick=null` → no footer for a different reason
than anchoring; (2) the rank pick's choices never populate reactively (the Bug C gap, fixed in plan 02).
Plans 02+03 likely already resolve Bug B; this plan TRACES to confirm the real cause, writes a
reproducing component test, and applies a fix only if a residual gap remains.

Per CLAUDE.md "Prove Before Fix" and the locked sequencing constraint: do NOT write a blind fix —
trace the Go Fish available-actions/flow first.

Purpose: Prove the Go Fish playability gate at the component level before the browser run, without
regressing the PRESENT-02 hidden-card property.
Output: A root-cause finding, a reproducing+passing Go-Fish component test, a PRESENT-02 no-regress
guard, and any minimal residual fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/94.1-auto-ui-interaction-completion/94.1-CONTEXT.md
@.planning/phases/94.1-auto-ui-interaction-completion/94.1-RESEARCH.md

<interfaces>
Go Fish 'ask' action (~/BoardSmithGames/go-fish/src/rules/actions.ts, for fixture shape — DO NOT import):
`Action.create('ask').chooseFrom('target', { choices: playerChoices(excludeSelf), boardRefs: choice =>
({ refs:[{ ref:{ id: opponentHand.id }, role:'target' }] }) }).chooseFrom('rank', { choices:
getPlayerRanks(player), boardRefs: rank => ({ refs:[{ ref:{ id: firstCardOfRank.id }, role:'target' }] }) })`.
BOTH picks carry id-only refs (no notation) → under D-02 `allCurrentChoicesAnchored=false` → footer present.

Go Fish flow (~/BoardSmithGames/go-fish/src/rules/flow.ts): the turn uses a single `actionStep({ name:
'ask-step', actions: ['ask'] })` inside a turn-loop. So 'ask' should be the ONLY available action during
a normal turn (relevant to candidate cause (1) — confirm by tracing).

useActionController:
- `tryAutoStartSingleAction` auto-starts when exactly one action is available; if >1 available,
  currentPick stays null until the user picks an action.
- After plan 02: `currentChoices`/`validElements` are read reactively by ActionPanel; `allCurrentChoicesAnchored
  = validElements.value.length > 0`.
- 2-player: `target` chooseFrom has a single opponent → auto-fills, advancing to `rank`, whose choices are
  fetched (Bug C path — must populate reactively after plan 02).

PRESENT-02 (Phase 94, VERIFIED in browser): a Go Fish opponent's face-down hand renders ONLY
`/cards/back.svg` with anonymized NEGATIVE element ids; zero face URLs leak. This test must NOT regress it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TRACE Go Fish ask, then RED reproducing component test (+ PRESENT-02 guard)</name>
  <read_first>
    - ~/BoardSmithGames/go-fish/src/rules/flow.ts (confirm 'ask' is the only available action during a turn; resolve candidate cause (1))
    - ~/BoardSmithGames/go-fish/src/rules/actions.ts (ask: target + rank, id-only refs — fixture shape)
    - src/ui/composables/useActionController.ts (tryAutoStartSingleAction, target auto-fill, rank fetch, allCurrentChoicesAnchored after plan 02)
    - src/ui/components/auto-ui/ActionPanel.interaction.test.ts (harness + provide pattern from plan 02)
    - .planning/phases/94-interaction-presentation-playability/ SUMMARY for PRESENT-02 (how opponent hand anonymization is asserted)
  </read_first>
  <action>
    1. TRACE first (Prove Before Fix). Determine the ACTUAL Bug B root cause by reading the Go Fish flow +
       available-actions handling: confirm whether during a normal turn exactly one action ('ask') is
       available (so tryAutoStartSingleAction fires and currentPick advances), versus a second simultaneous
       action leaving currentPick=null. Document which candidate cause (1 or 2, or another) is real, with
       file:line evidence, in the SUMMARY. This trace gates the fix decision in Task 2.
    2. Create src/ui/components/auto-ui/GoFishAsk.interaction.test.ts with the `// @vitest-environment jsdom`
       pragma and a test-local Go-Fish 'ask' fixture (target + rank chooseFrom, id-only refs; 2-player so a
       single target auto-fills). Wire the REAL useActionController (+ bridge if the trace shows the bridge is
       involved). Write:
       - Test B1 (reproduce): start the ask turn; assert an interaction surface exists — footer present
         (allCurrentChoicesAnchored=false) AND the rank choices populate (panel text shows rank labels, NOT
         "No options available"). EXPECTED RED against pre-plan-02 behavior; document its state against
         current (post-02/03) code — if already GREEN, that confirms plans 02/03 fixed Bug B and Task 2 adds
         no production change.
       - Test B2 (PRESENT-02 no-regress): using the overlay/visibility path, assert an opponent's face-down
         hand exposes only back.svg with anonymized (negative) ids — zero face URLs. This guards the Phase 94
         security property while exercising Go Fish in tests.
       - Fixtures are test-local; do NOT import from ~/BoardSmithGames.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/auto-ui/GoFishAsk.interaction.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Bug B root cause is documented with file:line evidence (Open Question 2 resolved): which of currentPick=null vs non-reactive rank choices vs other.
    - File src/ui/components/auto-ui/GoFishAsk.interaction.test.ts exists, first line `// @vitest-environment jsdom`, uses the REAL controller, no `BoardSmithGames` import.
    - Test B1 documents RED-vs-GREEN status against current code (if GREEN, the SUMMARY states plans 02/03 closed Bug B).
    - Test B2 (PRESENT-02) asserts opponent hand → only back.svg + anonymized ids; it passes (no regression).
  </acceptance_criteria>
  <done>The real Bug B cause is proven by tracing; a reproducing Go Fish component test plus a PRESENT-02 no-regress guard exist.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — close any residual Go Fish ask gap (fix only what the trace proved)</name>
  <read_first>
    - src/ui/components/auto-ui/GoFishAsk.interaction.test.ts (B1 must be GREEN)
    - src/ui/composables/useActionController.ts (tryAutoStartSingleAction / rank fetch — only the path the trace identified)
    - src/ui/composables/useBoardActionBridge.ts (only if the trace implicates the bridge)
  </read_first>
  <action>
    Apply the minimal fix the Task-1 trace proved is needed to make Test B1 GREEN. If the trace shows
    plans 02/03 already closed Bug B (B1 already GREEN), make NO production change — record that finding and
    keep the test as a permanent guard. If a residual gap remains (e.g. rank choices not auto-fetched after
    the single-target auto-fill, or currentPick left null by an available-actions edge case), fix it at the
    proven site in useActionController.ts (or useBoardActionBridge.ts). Do NOT introduce dummy/fallback
    choices to force green — choices must come from the real fetch. Do NOT remove existing debug logging
    unless Bug B is confirmed resolved. Maintain Custom-UI/Action-Panel parity (useBoardInteraction): any
    fix must keep both the footer and board paths consistent.
  </action>
  <verify>
    <automated>npx vitest run src/ui/components/auto-ui/GoFishAsk.interaction.test.ts src/ui/</automated>
  </verify>
  <acceptance_criteria>
    - Test B1 is GREEN (Go Fish ask surfaces a footer with populated target/rank choices on a 2-player turn).
    - Test B2 (PRESENT-02) remains GREEN — no opponent face URL leak introduced.
    - If a production fix was applied, it is at the file:line the trace proved (no speculative edits elsewhere).
    - No dummy/fallback data introduced.
    - `npx vitest run src/ui/` and full `npm test` green at baseline (no new failures beyond the known pre-existing one).
  </acceptance_criteria>
  <done>The Go Fish ask completes via the footer at the component level; the fix (if any) is grounded in the trace; PRESENT-02 visibility is intact; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner player ↔ opponent hidden hand | Go Fish has hidden state (opponent cards); the auto-UI must never render an opponent's face cards to an unauthorized viewer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94.1-04 | Information disclosure | Go Fish opponent face-down hand (PRESENT-02) | mitigate | Test B2 asserts an opponent's hand renders ONLY back.svg with anonymized negative ids and zero face URLs; this guards the Phase-94 PRESENT-02 property against regression while exercising the ask flow. The fix path (controller/bridge) touches choice population only, not the visibility-filtered payload. |
| T-94.1-SC | Tampering | npm installs | mitigate | No installs in this plan; supply-chain gate handled in 94.1-01. |
</threat_model>

<verification>
- `npx vitest run src/ui/components/auto-ui/GoFishAsk.interaction.test.ts` green (B1 + B2).
- `npm test` green at baseline with no new failures.
</verification>

<success_criteria>
- Go Fish ask surfaces a working footer (populated target/rank) at the component level.
- Bug B root cause documented; fix (if any) grounded in the trace.
- PRESENT-02 opponent-hand visibility not regressed.
</success_criteria>

<output>
Create `.planning/phases/94.1-auto-ui-interaction-completion/94.1-04-SUMMARY.md` when done.
</output>
