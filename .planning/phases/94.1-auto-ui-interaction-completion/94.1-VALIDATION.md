---
phase: 94.1
slug: auto-ui-interaction-completion
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-21
---

# Phase 94.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> NOTE: This phase's entire premise (D-01) is that controller-unit tests passed while the
> browser failed. The validation bar is therefore **Vue component/interaction tests that
> reproduce the browser failures (Bugs A/B/C)** — not unit tests alone.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (existing) + `@vue/test-utils` + `jsdom` (Wave 0 installs — gated by user dep approval per D-01 / CLAUDE.md) |
| **Config file** | `vitest.config.*` (existing — currently `environment: 'node'`; component tests opt in per-file via `// @vitest-environment jsdom`) |
| **Quick run command** | `npm test -- <changed test file>` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~existing suite + new component tests (TBD by planner) |

---

## Sampling Rate

- **After every task commit:** Run the changed/added test file
- **After every plan wave:** Run the full suite
- **Before `/gsd:verify-work`:** Full suite must be green (baseline: `e46d450` = 892 pass / 1 known pre-existing fail)
- **Max feedback latency:** seconds (component tests must stay fast — no real dev server)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | — | 0 | D-01 test infra | — | N/A | component | `npm test` | ❌ W0 | ⬜ pending |

*Planner/executor populates this map. Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `@vue/test-utils` + `jsdom` installed (gated by user dependency approval — D-01)
- [ ] First component test mounts a real `src/ui/components/auto-ui` renderer in jsdom and drives a board click
- [ ] Component test reproduces Bug A (Checkers destination), Bug B (Go Fish ask), Bug C (choice population "No options available") — RED before the fix

*Detailed Wave 0 file list is the planner's responsibility.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| End-to-end browser playthrough of all 3 gate games | Playability gate | Final confirmation per CLAUDE.md "verify in the browser" | Repro steps in 94.1-CONTEXT.md (checkers/go-fish/hex `npx boardsmith dev`) |

*Component tests are the primary gate; the browser run is the final confirmation, not the substitute.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < a few seconds
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
