---
phase: 95-ship-reframe
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ui-components.md
  - docs/component-showcase.md
  - docs/nomenclature.md
  - docs/llm-overview.md
  - src/cli/slash-command/instructions.md
autonomous: true
requirements: [SHIP-01]
must_haves:
  truths:
    - "No doc/CLI/slash-command location frames the auto-UI as merely a debug aid, reference implementation, or prototyping-only tool"
    - "Docs frame the auto-UI as a valid production choice for simple games"
    - "The /design-game slash command no longer mandates replacing GameTable.vue — it presents AutoUI as a valid shipping default and the custom UI as an optional on-ramp"
    - "instructions.md guidance is consistent with the single-UI scaffold from Plan 01 (no split-screen, no 'Custom UI panel')"
  artifacts:
    - path: "docs/ui-components.md"
      provides: "Production-peer framing for AutoUI"
      contains: "AutoUI"
    - path: "docs/component-showcase.md"
      provides: "Production-ready framing; debug/reference bullets removed"
      contains: "AutoUI"
    - path: "docs/nomenclature.md"
      provides: "Shippable-peer definition for AutoUI"
      contains: "AutoUI"
    - path: "docs/llm-overview.md"
      provides: "AutoUI can ship as production UI for simple games"
      contains: "AutoUI"
    - path: "src/cli/slash-command/instructions.md"
      provides: "Single-UI design-game flow; AutoUI as valid ship default"
      contains: "AutoUI"
  key_links:
    - from: "instructions.md design-game flow"
      to: "Plan 01 single-UI scaffold output"
      via: "consistent App.vue framing (no split-screen, no mandatory GameTable)"
      pattern: "GameTable"
---

<objective>
Reframe every doc/CLI/slash-command location that describes the auto-UI as a "debug aid", "reference implementation", or "prototyping-only" tool. Make the auto-UI a valid production choice for simple games (SHIP-01), and align the `/design-game` slash command with the single-UI scaffold (no mandatory GameTable replacement, no split-screen language).

Purpose: With the scaffold reframed (Plan 01), the docs and the embedded design-game skill must match — otherwise the LLM flow keeps force-replacing App.vue with a GameTable reference and re-introducing the split-screen mental model (Pitfall 1 in RESEARCH).
Output: Edited framing strings across 5 files; consistent single-UI narrative.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-ship-reframe/95-CONTEXT.md
@.planning/phases/95-ship-reframe/95-RESEARCH.md
@.planning/phases/95-ship-reframe/95-PATTERNS.md

<reframe_surface>
<!-- Exact grep-confirmed locations. Reframe = "valid production choice for simple games", NOT delete-everything. -->
docs/ui-components.md L139: "Automatic UI generation from game state. Useful for prototyping or as a reference implementation." → production-peer framing
docs/ui-components.md L155–161: bullet list — add a "ships as your production UI for simple games" bullet
docs/component-showcase.md L103: "Automatic UI generation for prototyping." → "Production-ready automatic game UI."
docs/component-showcase.md L115–118: "- Rapid prototyping" (keep), "- Debugging game state" / "- Reference implementation" (remove or demote to secondary)
docs/nomenclature.md L416: "...Useful for prototyping and as a reference implementation." → add shippable-peer framing
docs/nomenclature.md L421: "Use AutoUI to test rules before building custom UI" → "...to ship simple games or build rules before investing in a custom UI"
docs/llm-overview.md L13: "AutoUI can render any game without custom components" → append "and can ship as the production UI for simple games"
src/cli/slash-command/instructions.md L702: "You MUST customize GameTable.vue..." → mandatory only when building a custom UI; AutoUI is a valid ship choice
src/cli/slash-command/instructions.md L1029: "DO NOT SKIP THIS STEP. The scaffold creates a placeholder UI. You MUST replace it..." → reframe: AutoUI ships; GameTable is an optional on-ramp
src/cli/slash-command/instructions.md L1035: "After customizing, the Custom UI panel should show:" → update; no "Custom UI panel" (single-UI, no split-screen)
</reframe_surface>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reframe the four markdown docs</name>
  <read_first>
    - docs/ui-components.md (around L139 and L155–161)
    - docs/component-showcase.md (around L100–120)
    - docs/nomenclature.md (around L416 and L421)
    - docs/llm-overview.md (L13)
    - .planning/phases/95-ship-reframe/95-RESEARCH.md (Reframe Surface §, lines 175–225 — exact fixes)
  </read_first>
  <action>Apply the targeted text replacements in <reframe_surface> for ui-components.md, component-showcase.md, nomenclature.md, and llm-overview.md. Replace "Useful for prototyping or as a reference implementation" framing with production-peer framing ("a production-ready UI; a fine choice for simple games or a starting point"). In component-showcase.md remove the "Debugging game state" and "Reference implementation" bullets (keep "Rapid prototyping" as a legitimate secondary use). Add a production-framing line where RESEARCH specifies. Keep edits surgical — do not rewrite surrounding unrelated prose.</action>
  <verify>
    <automated>grep -rniE "reference implementation|for prototyping|debugging game state" docs/ui-components.md docs/component-showcase.md docs/nomenclature.md docs/llm-overview.md | grep -v "^#" ; test $(grep -rniE "reference implementation|debugging game state" docs/ui-components.md docs/component-showcase.md docs/nomenclature.md docs/llm-overview.md | grep -ci "auto") -eq 0 && echo "PASS: no debug/reference framing of auto-UI"</automated>
  </verify>
  <acceptance_criteria>
    - No "reference implementation" or "Debugging game state" framing remains for AutoUI in the four docs.
    - Each of the four docs contains explicit production/shippable framing for AutoUI.
    - Edits are confined to the catalogued lines; surrounding content unchanged.
  </acceptance_criteria>
  <done>The four markdown docs frame AutoUI as a valid production UI for simple games.</done>
</task>

<task type="auto">
  <name>Task 2: Align /design-game slash command with single-UI scaffold</name>
  <read_first>
    - src/cli/slash-command/instructions.md (L698–706, L1027–1040 — the mandatory-GameTable section; and any embedded GameShell/App.vue template the design flow generates)
    - .planning/phases/95-ship-reframe/95-RESEARCH.md (Pitfall 1, lines 240–248; Reframe Surface item 5, lines 209–217)
    - .planning/phases/95-ship-reframe/95-01-PLAN.md (the single-UI App.vue contract this file must stay consistent with)
  </read_first>
  <action>Update instructions.md so the design-game flow matches Plan 01's single-UI scaffold (D-01/D-03). At L702 and L1029, remove the "DO NOT SKIP / You MUST replace the placeholder" mandate: state that a fresh scaffold opens in AutoUI and is immediately playable, and that filling GameTable.vue is an OPTIONAL on-ramp taken only when the author wants a bespoke UI (then they set boardsmith.json "ui" to it). At L1035, remove "Custom UI panel" split-screen language — there is one UI now. If instructions.md embeds an App.vue / GameShell template that still imports GameTable alongside AutoUI or renders a board-comparison, update it to the single-import shape defined in 95-01-PLAN.md (AutoUI in the #game-board slot, no GameTable import). Keep the GameTable.vue authoring guidance (prop names etc.) intact for the optional custom-UI path — only the MANDATE and the split-screen framing change.</action>
  <verify>
    <automated>grep -niE "DO NOT SKIP|You MUST customize GameTable|You MUST replace it|Custom UI panel|board-comparison|Auto-Generated UI" src/cli/slash-command/instructions.md ; test $(grep -ciE "DO NOT SKIP THIS STEP|board-comparison|Auto-Generated UI" src/cli/slash-command/instructions.md) -eq 0 && echo "PASS: mandatory/split-screen framing removed"</automated>
  </verify>
  <acceptance_criteria>
    - instructions.md no longer instructs the LLM that customizing GameTable.vue is mandatory ("DO NOT SKIP" / "You MUST replace") — it is presented as optional.
    - No "Custom UI panel" / split-screen / "Auto-Generated UI" framing remains.
    - Any embedded App.vue/GameShell template matches the single-import shape from 95-01-PLAN.md (no simultaneous GameTable + AutoUI import, no board-comparison).
    - Custom-UI authoring guidance (prop names, structure) for the optional path is preserved.
  </acceptance_criteria>
  <done>The design-game slash command describes the single-UI scaffold: AutoUI ships by default, custom UI is an optional on-ramp.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation/text-only edits; no runtime input, no data crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-02 | Information disclosure | docs/instructions text | accept | Text-only reframe; no secrets, no executable surface, no package installs (no T-95-SC). |
</threat_model>

<verification>
- `grep -rniE "reference implementation|debugging game state" docs/*.md` shows no auto-UI debug framing.
- `grep -niE "DO NOT SKIP THIS STEP|board-comparison|Auto-Generated UI|Custom UI panel" src/cli/slash-command/instructions.md` returns nothing.
- instructions.md flow is consistent with 95-01-PLAN.md single-UI scaffold.
</verification>

<success_criteria>
- All 6 SHIP-01 framing locations reframe AutoUI as a valid production choice.
- The /design-game flow no longer mandates GameTable customization or references a split-screen.
</success_criteria>

<output>
Create `.planning/phases/95-ship-reframe/95-02-SUMMARY.md` when done
</output>
