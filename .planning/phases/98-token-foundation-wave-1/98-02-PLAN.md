---
phase: 98-token-foundation-wave-1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .stylelintrc.cjs
  - package.json
  - scripts/check-no-hex.test.mjs
autonomous: false
requirements: [TOKEN-06]
must_haves:
  truths:
    - "A `lint:css` (color-no-hex) npm script exists and runs stylelint against chrome/renderer .vue files"
    - "Introducing a raw hex literal into a clean / non-ignored .vue file fails the script (the wrong path fails CI)"
    - "theme.ts is exempt — it is the one place literals are allowed to live"
    - "The not-yet-swept renderer + chrome .vue files are in a temporary ignoreFiles list, documented as emptied by Phase 99"
  artifacts:
    - path: ".stylelintrc.cjs"
      provides: "color-no-hex rule scoped to .vue <style> blocks with documented temporary ignore list"
      contains: "color-no-hex"
    - path: "package.json"
      provides: "lint:css npm script + stylelint devDependencies"
      contains: "lint:css"
  key_links:
    - from: "package.json"
      to: ".stylelintrc.cjs"
      via: "stylelint --config invocation in the lint:css script"
      pattern: "stylelint"
---

<objective>
Add a stylelint `color-no-hex` guard scoped to the chrome/renderer `.vue` files plus an npm script, so that introducing a raw hex literal into a clean or new file fails CI. Because those `.vue` files are still full of neon hex until the Phase 99 sweep, a blocking rule would fail immediately — so the rule is enabled with a temporary `ignoreFiles` list covering the not-yet-swept components, documented (in the config and this plan) as emptied by Phase 99, ending with zero exclusions.

Purpose: TOKEN-06 — make the wrong path (hand-writing a hex instead of a `--bsg-*` token) fail CI, while keeping `main` green during the staged 98→99 landing.

Output: `.stylelintrc.cjs`, a `lint:css` npm script + devDependencies, and a tiny test proving the rule bites on a new hex.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/98-token-foundation-wave-1/98-CONTEXT.md

<interfaces>
<!-- The .vue files that still contain neon hex until Phase 99 (verified by grep) -->
Chrome/renderer .vue files that MUST be in the temporary ignoreFiles list (they still
hold raw hex pending the Phase 99 sweep):
  src/ui/components/auto-ui/renderers/*.vue  (Card, Hand, Grid, Space, Hex, Piece, etc.)
  src/ui/components/auto-ui/ActionPanel.vue
  src/ui/components/GameShell.vue
  src/ui/components/WaitingRoom.vue
  src/ui/components/DebugPanel.vue
theme.ts is NOT linted by this rule (it is .ts, and it is the sanctioned home for literals).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify stylelint + postcss-html package legitimacy</name>
  <read_first>
    - package.json (devDependencies block — confirm neither is already present)
  </read_first>
  <action>
    BLOCKING legitimacy gate — do NOT install yet. No RESEARCH.md Package Legitimacy Audit exists for this phase, so `stylelint` and `postcss-html` default to [ASSUMED]; the project rule "Don't add dependencies without discussing" also applies. Present the two packages to the user for confirmation, verifying each on its npm registry page before any install. This is a verification action only — the actual install happens in Task 2 after approval.
  </action>
  <what-built>Nothing yet — this gate verifies the two new devDependencies BEFORE install, per the Package Legitimacy Gate (no RESEARCH.md audit exists for this phase, so packages default to [ASSUMED]) and the project rule "Don't add dependencies without discussing".</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/stylelint — confirm it is the official stylelint linter (millions of weekly downloads, repo github.com/stylelint/stylelint).
    2. Open https://www.npmjs.com/package/postcss-html — confirm it is the official PostCSS syntax for parsing `<style>` blocks in HTML/Vue/Svelte (repo github.com/ota-meshi/postcss-html), required for stylelint to read `.vue` SFC styles.
    3. Confirm both are devDependencies only (build/CI tooling, never shipped in the library bundle).
  </how-to-verify>
  <verify>Manual: both packages confirmed on npmjs.com as the official, widely-used packages.</verify>
  <acceptance_criteria>
    - Both packages confirmed legitimate on npmjs.com and approved for devDependencies-only install.
  </acceptance_criteria>
  <done>User has approved installing stylelint + postcss-html as devDependencies, or named an alternative.</done>
  <resume-signal>Type "approved" to allow the stylelint + postcss-html devDependency install, or name an alternative.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add stylelint config, ignore list, and lint:css script</name>
  <files>.stylelintrc.cjs, package.json</files>
  <read_first>
    - package.json (scripts + devDependencies blocks)
    - .planning/phases/98-token-foundation-wave-1/98-CONTEXT.md (`<decisions>` color-no-hex section)
  </read_first>
  <action>
    Install `stylelint` and `postcss-html` as devDependencies (after the Task 1 checkpoint). Create `.stylelintrc.cjs` at repo root:
    - `customSyntax: 'postcss-html'` so stylelint parses `<style>` blocks inside `.vue` SFCs.
    - `rules: { 'color-no-hex': true }`.
    - `files`/`overrides` targeting `src/ui/**/*.vue` only (do NOT lint `.ts` — theme.ts is the sanctioned literal home and must stay exempt).
    - `ignoreFiles`: the not-yet-swept renderer + chrome `.vue` files listed in `<interfaces>` (use globs covering `src/ui/components/auto-ui/renderers/*.vue`, `src/ui/components/auto-ui/ActionPanel.vue`, `src/ui/components/GameShell.vue`, `src/ui/components/WaitingRoom.vue`, `src/ui/components/DebugPanel.vue`). Include a top-of-file comment: "TEMPORARY ignore list — Phase 99 (Theming Swap) empties this as it sweeps each file's neon literals into `--bsg-*` tokens; this list MUST end at zero exclusions. Do not add files here to silence new violations."
    Add a `"lint:css": "stylelint 'src/ui/**/*.vue'"` script to package.json. Do NOT wire it into the default `lint` script or CI blocking config beyond adding the script itself — it must exist and bite when run; gating policy escalation is left explicit. Verify the script passes today (all current violators are ignored, so it must exit 0 on a clean tree).
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npm run lint:css 2>&1 | tail -5; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `.stylelintrc.cjs` exists with `color-no-hex: true`, `customSyntax: 'postcss-html'`, and the documented temporary `ignoreFiles` list.
    - `npm run lint:css` exits 0 on the current tree (all current hex-bearing files are ignored; theme.ts is not in scope).
    - The ignore-list comment names Phase 99 as the emptier and forbids adding files to silence violations.
  </acceptance_criteria>
  <done>stylelint is installed, configured with color-no-hex scoped to .vue (theme.ts exempt), a documented temporary ignore list is in place, and `npm run lint:css` is green on the current tree.</done>
</task>

<task type="auto">
  <name>Task 3: Prove color-no-hex bites on a new hex</name>
  <files>scripts/check-no-hex.test.mjs</files>
  <read_first>
    - .stylelintrc.cjs (the config just created)
    - src/ui/composables/useBoardInteraction.test.ts (vitest conventions in this package)
  </read_first>
  <behavior>
    - A non-ignored `.vue` `<style>` block containing a raw hex (e.g. `color: #ff0000;`) makes stylelint report a `color-no-hex` violation (non-zero exit / warning array non-empty).
    - The same content using a `var(--bsg-*)` token reports zero violations.
  </behavior>
  <action>
    Create `scripts/check-no-hex.test.mjs` (vitest) that invokes stylelint programmatically (import `stylelint` from node_modules) against an in-memory or temp `.vue` snippet that is NOT in the ignore list (use a path under a non-ignored location, e.g. a temp file in the repo's `src/ui/__hexprobe__/probe.vue`, created and deleted within the test). Assert: a hex literal yields at least one `color-no-hex` warning; the `var(--bsg-accent)` variant yields none. Clean up the temp file in an afterEach/finally so no probe file is left behind. This is the TOKEN-06 "the guard exists and bites" proof.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run scripts/check-no-hex.test.mjs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/check-no-hex.test.mjs` passes: hex → ≥1 color-no-hex warning, token → 0 warnings.
    - No probe `.vue` file remains in the tree after the test (cleanup verified).
  </acceptance_criteria>
  <done>An automated test proves the color-no-hex guard reports a violation when a raw hex is introduced into a non-ignored file and passes when a `--bsg-*` token is used instead.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → npm registry | New devDependencies pulled during install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-SC | Tampering | npm installs (stylelint, postcss-html) | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifies both packages on npmjs.com before install; no RESEARCH.md audit exists so packages default to [ASSUMED]; devDependencies only, never shipped |
</threat_model>

<verification>
- `npm run lint:css` exits 0 on the current tree.
- `npx vitest run scripts/check-no-hex.test.mjs` green.
- `.stylelintrc.cjs` ignore-list comment references Phase 99 as the emptier.
</verification>

<success_criteria>
- TOKEN-06: a color-no-hex stylelint rule scoped to chrome/renderer `.vue` files exists, fails when a raw hex is introduced into a clean/new file, exempts theme.ts, and keeps CI green via a documented temporary ignore list that Phase 99 empties.
</success_criteria>

<output>
Create `.planning/phases/98-token-foundation-wave-1/98-02-SUMMARY.md` when done.
</output>
