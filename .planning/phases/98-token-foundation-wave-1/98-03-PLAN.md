---
phase: 98-token-foundation-wave-1
plan: 03
type: execute
wave: 2
depends_on: ["98-01"]
files_modified:
  - src/ui/animation/drag-drop.css
  - src/ui/components/auto-ui/renderers/HandRenderer.vue
  - src/ui/components/auto-ui/renderers/CardRenderer.vue
  - src/ui/components/auto-ui/renderers/GridBoardRenderer.vue
  - src/ui/components/auto-ui/renderers/SpaceRenderer.vue
  - src/ui/components/auto-ui/renderers/HexBoardRenderer.vue
autonomous: true
requirements: [TOKEN-01, TOKEN-02]
must_haves:
  truths:
    - "No `--bs-*` custom-property definition or reference remains anywhere in src/ui"
    - "The drag/drop interaction vars resolve from the `--bsg-*` tokens registered in theme.ts (no neon literal fallbacks remain on them)"
    - "HexBoardRenderer's seat colors come from the `--bsg-seat-1..6` tokens, not a hardcoded Flat-UI cycle"
    - "drag-drop.css no longer defines its own `:root` token block — theme.ts owns those defaults"
  artifacts:
    - path: "src/ui/animation/drag-drop.css"
      provides: "Drag/drop class rules referencing --bsg-* tokens (no local :root definitions)"
      contains: "--bsg-droptarget"
    - path: "src/ui/components/auto-ui/renderers/HexBoardRenderer.vue"
      provides: "Seat coloring via var(--bsg-seat-N)"
      contains: "--bsg-seat-"
  key_links:
    - from: "src/ui/components/auto-ui/renderers/HexBoardRenderer.vue"
      to: "--bsg-seat-1..6 tokens (theme.ts)"
      via: "var(--bsg-seat-N) in piece fill"
      pattern: "--bsg-seat-"
    - from: "src/ui/animation/drag-drop.css"
      to: "--bsg-droptarget tokens (theme.ts)"
      via: "var(--bsg-droptarget...) in .bs-drop-target rules"
      pattern: "var\\(--bsg-droptarget"
---

<objective>
Collapse the `--bs-*` namespace into `--bsg-*` (TOKEN-01) across the drag/drop CSS and the five renderers that reference it, and replace HexBoardRenderer's hardcoded Flat-UI seat cycle with the `--bsg-seat-1..6` tokens (TOKEN-02). This is a mechanical rename — NOT the Phase 99 neon→token sweep. The drag/drop vars' defaults now live in theme.ts (Plan 98-01), so the local `:root` definition block in drag-drop.css is removed and the inline neon fallbacks on each reference are dropped (theme.ts provides the value). A handful of drop-target swatches will begin reading Slate accent values as a result — that is the accepted, never-shipped transient change documented in CONTEXT; do NOT chase the rest of the neon sweep here.

Purpose: After this plan, grepping src/ui finds no `--bs-*` custom property, and seat colors flow from one token source.

Output: Renamed drag tokens across 6 files; HexBoardRenderer seat coloring via tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-token-foundation-wave-1/98-CONTEXT.md
@.planning/phases/98-token-foundation-wave-1/98-01-PLAN.md

<interfaces>
<!-- Exact rename map. Verify CURRENT line numbers by grep before editing (anchors may drift). -->
`--bs-*` to `--bsg-*` token rename (defaults already registered in theme.ts by Plan 98-01):
  --bs-draggable-cursor         -> --bsg-draggable-cursor
  --bs-dragging-cursor          -> --bsg-dragging-cursor
  --bs-dragging-opacity         -> --bsg-dragging-opacity
  --bs-dragging-scale           -> --bsg-dragging-scale
  --bs-drag-transition          -> --bsg-drag-transition
  --bs-drop-target-bg           -> --bsg-droptarget
  --bs-drop-target-border-color -> --bsg-droptarget-border
  --bs-drop-target-shadow       -> --bsg-droptarget-shadow
  --bs-drop-hover-bg            -> --bsg-droptarget-hover
  --bs-drop-hover-border-color  -> --bsg-droptarget-hover-border
  --bs-drop-hover-shadow        -> --bsg-droptarget-hover-shadow
  --bs-drop-hover-scale         -> --bsg-droptarget-hover-scale

Reference sites (grep-verified; confirm before editing):
  src/ui/animation/drag-drop.css         : :root DEFINITION block (delete) + .bs-* rule refs + doc comment
  HandRenderer.vue   ~431,436            : --bs-drop-target-bg / --bs-drop-hover-bg
  CardRenderer.vue   ~488,489,493,497    : --bs-dragging-opacity/scale, --bs-drop-target-bg, --bs-drop-hover-bg
  GridBoardRenderer.vue ~324,329,338,339 : drop-target/hover bg (incl @keyframes)
  SpaceRenderer.vue  ~221,226            : drop-target/hover bg
  HexBoardRenderer.vue ~325 (fill) + ~108-115 PLAYER_COLOR_CYCLE (seat hardcode)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename --bs-* drag tokens in drag-drop.css and delete its local :root block</name>
  <read_first>
    - src/ui/animation/drag-drop.css (read in full)
    - src/ui/theme.ts (confirm the --bsg-droptarget* / --bsg-dragging-* / --bsg-drag-transition defaults are registered there by Plan 98-01)
  </read_first>
  <action>
    In drag-drop.css: DELETE the entire `:root { ... }` token-definition block (the neon `--bs-*` defaults) — theme.ts now owns these defaults (No Backward Compatibility, single source). Rename every `var(--bs-...)` reference in the `.bs-draggable` / `.bs-dragging` / `.bs-drop-target` / `.bs-drop-hover` rule blocks to its `--bsg-*` equivalent per the `<interfaces>` map. Update the documentation header comment so its example snippet uses `--bsg-*` names. Do NOT rename the CSS CLASS selectors (`.bs-draggable` etc.) — those are class names applied by useDragDrop, not custom properties, and are out of scope for TOKEN-01 (the requirement targets custom-property namespaces). Drop any inline literal fallback on the renamed references (theme.ts provides the value).
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && (grep -n -- '--bs-' src/ui/animation/drag-drop.css | grep -v -- '--bsg-' && echo "FAIL --bs- remains") || echo "OK no --bs- props"; grep -c 'var(--bsg-droptarget' src/ui/animation/drag-drop.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -- '--bs-' src/ui/animation/drag-drop.css | grep -v -- '--bsg-'` returns nothing (no `--bs-` custom properties remain; class selectors `.bs-*` are allowed).
    - The local `:root { ... }` definition block is gone from drag-drop.css.
    - `.bs-drop-target` / `.bs-drop-hover` rules reference `var(--bsg-droptarget...)` with no inline neon fallback.
  </acceptance_criteria>
  <done>drag-drop.css references only `--bsg-*` custom properties, owns no `:root` token block, and its doc comment shows the `--bsg-*` names.</done>
</task>

<task type="auto">
  <name>Task 2: Rename --bs-* references in the five renderers</name>
  <read_first>
    - src/ui/components/auto-ui/renderers/HandRenderer.vue (grep `--bs-` to find current lines)
    - src/ui/components/auto-ui/renderers/CardRenderer.vue
    - src/ui/components/auto-ui/renderers/GridBoardRenderer.vue
    - src/ui/components/auto-ui/renderers/SpaceRenderer.vue
    - src/ui/components/auto-ui/renderers/HexBoardRenderer.vue (the `--bs-drop-target-bg` fill only — seat cycle is Task 3)
  </read_first>
  <action>
    In each renderer `.vue`, rename every `var(--bs-...)` reference to its `--bsg-*` equivalent per the `<interfaces>` map (this includes the GridBoardRenderer `@keyframes` blocks and the HexBoardRenderer SVG `fill`). DROP the inline neon-literal fallback argument from each renamed `var()` (theme.ts now supplies the default — keeping the rgba fallback would be dead code and would re-introduce hex). Prove Before Fix: re-grep each file for the current line numbers before editing; the `~NNN` anchors may have drifted. Do NOT touch any other hardcoded color in these files — that broader neon sweep is Phase 99.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && (grep -rn -- '--bs-' src/ui/components/auto-ui/renderers/ | grep -v -- '--bsg-' && echo "FAIL --bs- remains") || echo "OK renderers clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn -- '--bs-' src/ui/components/auto-ui/renderers/ | grep -v -- '--bsg-'` returns nothing.
    - No renamed `var(--bsg-droptarget...)` carries an inline `rgba(0, 255, 136, ...)` fallback.
    - `npx vitest run` for any renderer-touching suites still passes (no behavior regression).
  </acceptance_criteria>
  <done>All five renderers reference only `--bsg-*` drag tokens with no neon fallbacks; no other colors changed.</done>
</task>

<task type="auto">
  <name>Task 3: Replace HexBoardRenderer's hardcoded seat cycle with --bsg-seat tokens</name>
  <read_first>
    - src/ui/components/auto-ui/renderers/HexBoardRenderer.vue (the `PLAYER_COLOR_CYCLE` const ~108-115 and `getPieceColor` below it)
    - src/ui/theme.ts (confirm `--bsg-seat-1..6` are emitted and `SEAT_PALETTE` is exported by Plan 98-01)
  </read_first>
  <action>
    Remove the hardcoded `PLAYER_COLOR_CYCLE` Flat-UI hex array. Rewrite `getPieceColor` so that when a player has a `seat` (and no explicit `color`), it returns `var(--bsg-seat-${(seat % 6) + 1})` — consuming the single token source emitted by theme.ts (so a host `applyTheme` override re-skins seats too). Keep the existing `player.color` short-circuit (explicit per-player color still wins) and keep the neutral fallback for seatless pieces, but express that neutral as `var(--bsg-ink-3)` rather than a raw `#888` (TOKEN-01 hygiene — do not introduce a new hex). Do NOT import `SEAT_PALETTE` for inline JS hex; the `var()` token reference is the pit-of-success path and keeps the value overridable.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && grep -c -- '--bsg-seat-' src/ui/components/auto-ui/renderers/HexBoardRenderer.vue; (grep -nE '#e74c3c|#3498db|#2ecc71|PLAYER_COLOR_CYCLE' src/ui/components/auto-ui/renderers/HexBoardRenderer.vue && echo "FAIL hardcode remains") || echo "OK seat cycle removed"</automated>
  </verify>
  <acceptance_criteria>
    - `PLAYER_COLOR_CYCLE` and its Flat-UI hex literals (#e74c3c, #3498db, ...) are gone from HexBoardRenderer.vue.
    - `getPieceColor` returns `var(--bsg-seat-N)` for seated pieces and `var(--bsg-ink-3)` for the neutral fallback; the `player.color` short-circuit is preserved.
    - `npx vitest run` passes for any HexBoardRenderer suite.
  </acceptance_criteria>
  <done>HexBoardRenderer colors seats from the single `--bsg-seat-1..6` token source; no hardcoded seat palette or `#888` remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure CSS-variable rename; no new untrusted input crosses a boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-03 | Tampering | renamed drag/seat tokens | accept | Values resolve from theme.ts tokens already guarded by applyTheme's `--bsg-*` key allowlist (T-98-01); this plan only renames references |
</threat_model>

<verification>
- `grep -rn -- '--bs-' src/ui | grep -v -- '--bsg-'` returns nothing (no `--bs-*` custom properties anywhere; `.bs-*` class selectors are allowed and out of scope).
- `grep -- '--bsg-seat-' src/ui/components/auto-ui/renderers/HexBoardRenderer.vue` matches.
- `npx vitest run` stays green.
</verification>

<success_criteria>
- TOKEN-01 (`--bs-*` half): the `--bs-*` custom-property namespace no longer exists in src/ui; all references resolve from `--bsg-*`.
- TOKEN-02 (seat half): seat colors come from the single `--bsg-seat-1..6` token set.
</success_criteria>

<output>
Create `.planning/phases/98-token-foundation-wave-1/98-03-SUMMARY.md` when done.
</output>
