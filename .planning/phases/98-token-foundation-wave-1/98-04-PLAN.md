---
phase: 98-token-foundation-wave-1
plan: 04
type: execute
wave: 2
depends_on: ["98-01"]
files_modified:
  - src/ui/components/auto-ui/ActionPanel.vue
  - src/ui/components/WaitingRoom.vue
  - src/ui/components/GameShell.vue
  - src/ui/components/GameShell.theme.test.ts
autonomous: true
requirements: [TOKEN-01, TOKEN-05]
must_haves:
  truths:
    - "No `--bg-*`, `--text-*`, or `--border-*` custom-property definition or reference remains anywhere in src/ui"
    - "The ActionPanel pending/skip block resolves background AND text from --bsg-* tokens together (no light-on-light or dark-on-dark invisible-text)"
    - "GameShell installs the Slate token base on mount via applyTheme()"
    - "A theme override delivered in the iframe `init` postMessage is consumed via applyTheme(data.theme)"
  artifacts:
    - path: "src/ui/components/GameShell.vue"
      provides: "applyTheme() base install on mount + init-message theme override consumption"
      contains: "applyTheme"
    - path: "src/ui/components/GameShell.theme.test.ts"
      provides: "Test that an init message with a theme override calls applyTheme and updates the resolved token"
      exports: []
  key_links:
    - from: "src/ui/components/GameShell.vue"
      to: "applyTheme (theme.ts)"
      via: "onMounted install + init-handler override"
      pattern: "applyTheme\\("
    - from: "src/ui/components/auto-ui/ActionPanel.vue"
      to: "--bsg-surface / --bsg-ink / --bsg-line tokens"
      via: "var(--bsg-*) in pending/skip styles"
      pattern: "var\\(--bsg-(surface|ink|line)"
---

<objective>
Collapse the remaining legacy namespaces (`--bg-*`, `--text-*`, `--border-*`) into `--bsg-*` (TOKEN-01) in ActionPanel and WaitingRoom, and wire the TOKEN-05 receiver: GameShell installs the Slate token base via `applyTheme()` on mount and consumes a host-supplied theme override delivered in the iframe `init` postMessage. The host-side call that SENDS the override lives in ShufflewickPub (HOST-02) and is OUT OF SCOPE — only the BoardSmith receiver + plumbing is built and tested here.

Purpose: After this plan, grepping src/ui finds zero `--bg-*`/`--text-*`/`--border-*` custom properties, and `applyTheme` is a working, tested, host-overridable knob reachable at iframe init.

Output: Renamed ActionPanel/WaitingRoom vars; GameShell theme install + init override receiver; an integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-token-foundation-wave-1/98-CONTEXT.md
@.planning/phases/98-token-foundation-wave-1/98-01-PLAN.md

<interfaces>
<!-- Rename map (grep-verified; confirm current lines before editing). -->
ActionPanel.vue pending/skip block (~1515-1540):
  --bg-secondary  -> --bsg-surface-2
  --text-secondary-> --bsg-ink-2
  --border-color  -> --bsg-line
  --bg-primary    -> --bsg-surface
  --bg-hover      -> --bsg-surface-3
WaitingRoom.vue (~1222):
  --bg-color      -> --bsg-bg

GameShell init receiver site (src/ui/components/GameShell.vue ~610):
  platformMessageHandler, branch `if (data.type === 'init') { playerSeat.value = data.seat; currentScreen.value = 'game'; }`
  -> ADD theme-override consumption here.

applyTheme signature (from theme.ts, Plan 98-01):
  applyTheme(overrides?: Record<string,string>, options?: { scheme?: 'light'|'dark'|'auto' }): void
  // idempotently injects the base token <style>, sets data-theme on scheme, writes only --bsg-* override keys.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse --bg-*/--text-*/--border-* in ActionPanel and WaitingRoom</name>
  <read_first>
    - src/ui/components/auto-ui/ActionPanel.vue (grep `--bg-`/`--text-`/`--border-` to find current lines; the pending/skip `<style>` block)
    - src/ui/components/WaitingRoom.vue (grep `--bg-color`)
    - src/ui/theme.ts (confirm `--bsg-surface`, `--bsg-surface-2`, `--bsg-surface-3`, `--bsg-ink-2`, `--bsg-line`, `--bsg-bg` are emitted by Plan 98-01)
  </read_first>
  <action>
    Rename per the `<interfaces>` map. In the ActionPanel pending/skip block, rename ALL of `--bg-secondary`/`--bg-primary`/`--bg-hover`/`--text-secondary`/`--border-color` together (they sit in one small block), and DROP each inline hex fallback (#f5f5f5/#666/#ccc/#fff/#eee) — theme.ts supplies the default. Renaming the background AND text in the same block in one pass is required: these vars were previously undefined so the light fallbacks were active (light-gray bg + dark text); after the rename they resolve to Slate `--bsg-surface-2` (dark bg) + `--bsg-ink-2` (light ink), which stays readable. Do NOT rename only background or only text — that would create the invisible-text trap. In WaitingRoom, rename `--bg-color` to `--bsg-bg` and drop the `#1a1a2e` fallback (graphite #121417 is also dark, so any existing light text on this container stays readable; confirm by reading the surrounding template that no dark-on-dark text results). Touch no other colors (Phase 99 owns the full sweep).
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && (grep -rnE -- '--(bg|text|border)-' src/ui | grep -v -- '--bsg-' && echo "FAIL legacy ns remains") || echo "OK no --bg/text/border props"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE -- '--(bg|text|border)-' src/ui | grep -v -- '--bsg-'` returns nothing across all of src/ui.
    - ActionPanel pending/skip block references `--bsg-surface*`/`--bsg-ink-2`/`--bsg-line` with no hex fallbacks.
    - WaitingRoom container background references `var(--bsg-bg)` with no `#1a1a2e` fallback.
  </acceptance_criteria>
  <done>The `--bg-*`/`--text-*`/`--border-*` namespaces no longer exist in src/ui; ActionPanel bg+text were renamed together so no invisible-text results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire GameShell theme base-install + init override receiver, with a test</name>
  <read_first>
    - src/ui/components/GameShell.vue (the `platformMessageHandler` block ~599-667 and the component's `onMounted`/setup region; grep `applyTheme` import absence)
    - src/ui/theme.ts (applyTheme signature + behavior)
    - src/ui/components/platformRequestClone.test.ts (an existing GameShell-adjacent vitest for harness conventions)
  </read_first>
  <behavior>
    - On GameShell mount, `applyTheme()` is called once so the Slate token base `<style id="bsg-tokens">` is present in the document.
    - When an `init` postMessage arrives with a `theme` field (a partial `--bsg-*` map), `applyTheme(data.theme)` is invoked and the resolved inline token value updates (e.g. `data.theme = { '--bsg-accent': '#abc' }` -> `documentElement.style.getPropertyValue('--bsg-accent') === '#abc'`).
    - An `init` message WITHOUT a `theme` field still sets seat/screen as before and does not throw.
  </behavior>
  <action>
    Import `applyTheme` from the theme module into GameShell. In the component setup/`onMounted`, call `applyTheme()` once (no overrides) to install the base token stylesheet — this is the single install point for the hosted chrome; it is idempotent so repeated mounts are safe. In the `platformMessageHandler` `if (data.type === 'init')` branch, after the existing `playerSeat`/`currentScreen` assignments, add: if `data.theme` is a non-null object, call `applyTheme(data.theme)` (applyTheme itself enforces the `--bsg-*` key allowlist, so no extra validation here). Optionally honor `data.scheme` ('light'|'dark') by passing `{ scheme: data.scheme }` if present. Do NOT add a second theming path and do NOT touch ShufflewickPub — this is receiver-only.

    Create `src/ui/components/GameShell.theme.test.ts` (vitest + jsdom) that drives the init path. If mounting the full GameShell is too heavy, factor the init-handling into a small exported pure helper (e.g. `consumeInitMessage(data, { applyTheme })`) inside or alongside GameShell and unit-test that helper directly — but prefer dispatching a real `window` `message` event of shape `{ source:'shufflewick', type:'init', seat:0, theme:{ '--bsg-accent':'#abc' } }` if the existing test harness already mounts GameShell in platform mode. Assert the three `<behavior>` cases.
  </action>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/ui/components/GameShell.theme.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/components/GameShell.theme.test.ts` passes all three behavior cases.
    - The override-consumption case asserts the `#abc` resolved value after an init message (TOKEN-05 receiver proof).
    - GameShell imports and calls `applyTheme` exactly at mount + in the init branch; no second theming mechanism is introduced.
  </acceptance_criteria>
  <done>GameShell installs the Slate base on mount and consumes an init-delivered theme override via applyTheme; an integration test proves the override resolves and the no-theme init still works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host page -> iframe (postMessage `init.theme`) | Untrusted parent supplies a theme override consumed by GameShell -> applyTheme |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-04 | Tampering/Injection | GameShell init theme receiver | mitigate | Pass `data.theme` straight to applyTheme, which enforces the `^--bsg-[a-z0-9-]+$` key allowlist + string values + setProperty (no innerHTML); the existing `data.source !== 'shufflewick'` guard already filters foreign messages |
</threat_model>

<verification>
- `grep -rnE -- '--(bg|text|border)-' src/ui | grep -v -- '--bsg-'` returns nothing.
- `npx vitest run src/ui/components/GameShell.theme.test.ts` green.
- Full `npx vitest run` stays green (≥939 passing; update any test that asserted an old token default to the Slate value).
</verification>

<success_criteria>
- TOKEN-01 (`--bg/text/border` half): those namespaces no longer exist in src/ui.
- TOKEN-05 (receiver half): an init-delivered host theme override re-skins the chrome via applyTheme without touching component code, proven by an integration test.
</success_criteria>

<output>
Create `.planning/phases/98-token-foundation-wave-1/98-04-SUMMARY.md` when done.
</output>
