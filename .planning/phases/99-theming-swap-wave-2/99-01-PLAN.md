---
phase: 99-theming-swap-wave-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/theme.ts
  - src/ui/theme.test.ts
autonomous: true
requirements: [THEME-04, THEME-05]
must_haves:
  truths:
    - "Renderers can reference var(--bsg-card-back) for the default card back"
    - "Headings can reference a display-font token without a missing-var fallback"
  artifacts:
    - path: "src/ui/theme.ts"
      provides: "--bsg-card-back and --bsg-display token definitions in the static (theme-independent) block"
      contains: "--bsg-card-back"
  key_links:
    - from: "src/ui/theme.ts"
      to: "CardRenderer.vue / HandRenderer.vue"
      via: "shared --bsg-card-back token"
      pattern: "--bsg-card-back"
---

<objective>
Add the two NEW tokens the Phase 99 sweep needs to `theme.ts` so downstream renderer/chrome plans can reference them instead of inlining literals. This is the only plan in Wave 1 — every sweep plan depends on it.

Purpose: `--bsg-card-back` and `--bsg-display` do not exist yet (Phase 98 shipped accent/surface/ink/line/etc. but neither of these). Per CLAUDE.md "no fallbacks" and CONTEXT.md "If a needed token is missing, ADD it to theme.ts — NEVER inline a literal", these must be registered centrally before any consumer references them.
Output: Updated `theme.ts` with both tokens; updated `theme.test.ts` asserting they are emitted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-theming-swap-wave-2/99-CONTEXT.md
@src/ui/theme.ts
@src/ui/theme.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register --bsg-card-back and --bsg-display in theme.ts</name>
  <files>src/ui/theme.ts</files>
  <read_first>
    - src/ui/theme.ts (study STATIC_TOKENS — theme-independent block ~lines 96-156 — and the DARK/LIGHT color blocks; --bsg-card-back is a surface gradient built from existing tokens so it can live in STATIC_TOKENS and auto-adapt per scheme via the nested vars)
    - .planning/phases/99-theming-swap-wave-2/99-CONTEXT.md (card back + type decisions)
  </read_first>
  <action>
    In STATIC_TOKENS (the theme-independent block, so it resolves correctly in both schemes via its nested color tokens), add:
    - `--bsg-card-back: linear-gradient(180deg, var(--bsg-surface-2), var(--bsg-bg-2));` — the Slate graphite card-back surface. The thin rule (1px solid var(--bsg-line-2)) is applied by consumers as a border, not baked into this token.
    - `--bsg-display: 'Hanken Grotesk', system-ui, sans-serif;` — the display/heading font family token (distinct name so headings can opt into display type without a missing-var fallback). Place it next to the existing `--bsg-font`/`--bsg-mono` font tokens.
    Do NOT add any raw color hex — both tokens are composed from existing --bsg-* tokens / font stacks only. Do NOT alter any existing token values.
  </action>
  <acceptance_criteria>
    - `themeCSS` contains `--bsg-card-back:` and `--bsg-display:` exactly once each.
    - `--bsg-card-back` value references only existing --bsg-* tokens (no raw hex); grep: `grep -n 'bsg-card-back' src/ui/theme.ts` shows the linear-gradient built from var(--bsg-*) tokens.
    - No existing token value changed (the 969-green Phase 98 token set is intact).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && grep -c 'bsg-card-back\|bsg-display' src/ui/theme.ts | grep -qx 2 && echo OK</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assert both new tokens are emitted</name>
  <files>src/ui/theme.test.ts</files>
  <read_first>
    - src/ui/theme.test.ts (match the existing assertion style — it checks themeCSS substrings)
    - src/ui/theme.ts (the just-added tokens)
  </read_first>
  <behavior>
    - themeCSS includes a `--bsg-card-back:` declaration
    - themeCSS includes a `--bsg-display:` declaration
    - --bsg-card-back resolves through var() chains (no literal hex in its value)
  </behavior>
  <action>
    Add a test block asserting `themeCSS` contains both `--bsg-card-back` and `--bsg-display`, and that the `--bsg-card-back` declaration line contains `var(--bsg-` and no `#` hex literal. Follow the file's existing `expect(themeCSS).toContain(...)` pattern.
  </action>
  <acceptance_criteria>
    - New assertions exist and pass.
    - `npm run test -- src/ui/theme.test.ts` is green.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx vitest run src/ui/theme.test.ts</automated>
  </verify>
</task>

</tasks>

<verification>
- `npx vitest run src/ui/theme.test.ts` passes.
- `grep -n 'bsg-card-back\|bsg-display' src/ui/theme.ts` shows both, composed from existing tokens (no new hex).
</verification>

<success_criteria>
Both tokens are registered centrally and test-locked, so every Wave 2 consumer references a token instead of inlining a literal.
</success_criteria>

<output>
Create `.planning/phases/99-theming-swap-wave-2/99-01-SUMMARY.md` when done.
</output>
