---
phase: 99-theming-swap-wave-2
plan: 02
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - src/ui/components/auto-ui/renderers/CardRenderer.vue
  - src/ui/components/auto-ui/renderers/HandRenderer.vue
autonomous: true
requirements: [THEME-01, THEME-02, THEME-05]
must_haves:
  truths:
    - "Card and hand selection/drop states render in Slate accent tokens, not neon"
    - "The default card back is the shared --bsg-card-back surface in both renderers"
    - "HandRenderer's whole-hand selectable uses outline, not border (no layout reflow)"
    - "Card and hand surfaces have both background AND ink as tokens (no invisible-text)"
  artifacts:
    - path: "src/ui/components/auto-ui/renderers/CardRenderer.vue"
      provides: "tokenized card surface, card-back, interaction states"
      contains: "--bsg-card-back"
    - path: "src/ui/components/auto-ui/renderers/HandRenderer.vue"
      provides: "tokenized hand surface, card-back-small, outline selection"
      contains: "--bsg-card-back"
  key_links:
    - from: "CardRenderer.vue / HandRenderer.vue"
      to: "theme.ts"
      via: "var(--bsg-card-back) shared token"
      pattern: "--bsg-card-back"
---

<objective>
Atomically sweep the two card renderers to Slate: tokenize interaction states, replace the corporate-blue card back with the shared `--bsg-card-back`, remove colored glow shadows, and fix HandRenderer's `border` outlier to `outline`.

Purpose: THEME-01 (interaction tokens, no glow), THEME-02 (outline never border), THEME-05 (shared card back). These two files are paired because they share the card-back token and the same card visual language.
Output: Both renderers fully tokenized (bg AND ink), removed from the neon worklist by the Wave 4 gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theming-swap-wave-2/99-CONTEXT.md
@src/ui/theme.ts
@planning/mockups/boardsmith-chrome.html
@src/ui/components/auto-ui/renderers/CardRenderer.vue
@src/ui/components/auto-ui/renderers/HandRenderer.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep CardRenderer.vue to Slate (atomic bg + ink)</name>
  <files>src/ui/components/auto-ui/renderers/CardRenderer.vue</files>
  <read_first>
    - src/ui/components/auto-ui/renderers/CardRenderer.vue (full <style>; note .card-back ~543-556 corporate-blue gradient `#1a365d`/`#2c5282`/`#4a6fa5`, the glow box-shadow ~429, and the interaction-state classes)
    - src/ui/theme.ts (token names: accent, accent-2, selectable, selected, droptarget, ring, shadow-sm, elevation, surface, surface-2, ink, ink-2, line, line-2, card-back)
    - planning/mockups/boardsmith-chrome.html (.cell.selectable/.selected/.drop — dashed selectable / solid selected ring + small scale / dotted drop + stripe)
  </read_first>
  <action>
    Replace every color literal in CardRenderer's <style> with --bsg-* tokens in a single atomic pass — whenever a rule's background becomes a token, its text/ink in the same rule MUST also become a token:
    - selectable → dashed `var(--bsg-accent)` outline + `var(--bsg-selectable)` fill; selected → solid `var(--bsg-accent)` outline ring + `var(--bsg-selected)`/`color-mix` fill + small scale + `var(--bsg-ring)`; drop → dotted `var(--bsg-accent-2)` + `var(--bsg-droptarget)` stripe. Replace `#00d9ff`/`#00ff88`/`rgba(46,204,113…)`.
    - `.card-back` default gradient → `background: var(--bsg-card-back);` with `border: 1px solid var(--bsg-line-2);` (delete `#1a365d`/`#2c5282`/`#4a6fa5`). Leave `.card-back.has-image` transparent/no-border.
    - Remove the colored glow `box-shadow` (~429) → `var(--bsg-shadow-sm)`/`var(--bsg-elevation)` + optional `var(--bsg-ring)`.
    - Card face surfaces → `var(--bsg-surface)`/`var(--bsg-surface-2)`; every `color:#fff` → `var(--bsg-ink)`; hairlines `rgba(0,0,0,x)`/`rgba(255,255,255,x)` → `var(--bsg-line)`/`var(--bsg-line-2)`; neutral shadows `rgba(0,0,0,x)` may stay only if they are inside an already-tokenized shadow token — prefer `var(--bsg-shadow*)`.
    Selection must use `outline`, never `border`. Per CLAUDE.md No Backward Compatibility: delete the old literals, no shims.
  </action>
  <acceptance_criteria>
    - `grep -nE '#[0-9a-fA-F]{3,8}' src/ui/components/auto-ui/renderers/CardRenderer.vue` returns nothing inside <style> color contexts (no hex literals remain).
    - No `#00d9ff`/`#00ff88`/`rgba(46,204,113` remain.
    - `.card-back` uses `var(--bsg-card-back)`; selection states use `outline` (no `border` on selection states); no colored glow box-shadow remains.
    - Every swept surface that sets a background token also sets its color/ink to a --bsg-* token (no raw color:#fff).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/auto-ui/renderers/CardRenderer.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -nE '#00d9ff|#00ff88|rgba\(46, ?204, ?113' src/ui/components/auto-ui/renderers/CardRenderer.vue && grep -q 'bsg-card-back' src/ui/components/auto-ui/renderers/CardRenderer.vue && echo OK</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Sweep HandRenderer.vue to Slate + fix border→outline outlier</name>
  <files>src/ui/components/auto-ui/renderers/HandRenderer.vue</files>
  <read_first>
    - src/ui/components/auto-ui/renderers/HandRenderer.vue (full <style>; .hand-container.is-selected ~392 already uses outline #00d9ff; the OUTLIER .hand-container.action-selectable ~398 uses `border: 2px solid rgba(0,255,136,.6)` + `pulse-hand` keyframes; .card-back-small ~550-563 default back; glow box-shadow ~388/408)
    - src/ui/theme.ts (same tokens as Task 1)
    - planning/mockups/boardsmith-chrome.html (interaction language)
  </read_first>
  <action>
    Atomic sweep, same rules as Task 1, PLUS the outlier fix:
    - `.hand-container.action-selectable` MUST switch from `border: 2px solid …` to `outline: 2px dashed var(--bsg-accent); outline-offset: …;` with `background: var(--bsg-selectable);` so selection never reflows layout (matches the other five renderers). Recolor the `pulse-hand`/hover rules to accent tokens (opacity/outline-color only — no neon, no layout-shifting border).
    - `.hand-container.is-selected` outline `#00d9ff` → solid `var(--bsg-accent)` + `var(--bsg-ring)`.
    - `.card-back-small` default → `background: var(--bsg-card-back); border: 1px solid var(--bsg-line-2);` (shared token, parity with CardRenderer). Keep `.card-back-small.has-image` transparent.
    - Remove colored glow box-shadows → `var(--bsg-shadow-sm)`/`var(--bsg-elevation)`. Surfaces → `var(--bsg-surface*)`; `color:#fff` → `var(--bsg-ink)`; hairlines → `var(--bsg-line)`/`var(--bsg-line-2)`. Replace all `#00d9ff`/`#00ff88`/`rgba(46,204,113…)`.
  </action>
  <acceptance_criteria>
    - `.hand-container.action-selectable` uses `outline` (NOT `border`) for its selection ring; grep confirms no `border:` on any `*-selectable`/`*-selected` rule.
    - No hex literals remain in <style>; no `#00d9ff`/`#00ff88`/`rgba(46,204,113`.
    - `.card-back-small` uses `var(--bsg-card-back)`.
    - Atomic: every backgrounded surface also tokenizes its ink.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/auto-ui/renderers/HandRenderer.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -nE '#00d9ff|#00ff88|rgba\(46, ?204, ?113' src/ui/components/auto-ui/renderers/HandRenderer.vue && ! grep -nE 'action-selectable[^}]*border:' src/ui/components/auto-ui/renderers/HandRenderer.vue && grep -q 'bsg-card-back' src/ui/components/auto-ui/renderers/HandRenderer.vue && echo OK</automated>
  </verify>
</task>

</tasks>

<verification>
- Both files pass inline stylelint color-no-hex; no neon literals; both consume `--bsg-card-back`; HandRenderer selection uses outline.
- `npx vitest run src/ui/components/auto-ui` stays green (update any test asserting an old literal to the token/Slate expectation).
</verification>

<success_criteria>
Card and hand renderers are fully Slate, share the card-back token, use outline-only selection, and carry no neon literal.
</success_criteria>

<output>
Create `.planning/phases/99-theming-swap-wave-2/99-02-SUMMARY.md` when done.
</output>
