---
phase: 99-theming-swap-wave-2
plan: 05
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - src/ui/components/GameShell.vue
  - src/ui/components/HamburgerMenu.vue
autonomous: true
requirements: [THEME-04, THEME-07]
must_haves:
  truths:
    - "In platform mode the shell and drawer backdrops are transparent so the host shows through"
    - "In dev/standalone the backdrops fall back to var(--bsg-bg)"
    - "The HamburgerMenu gradient clip-text title is replaced by solid display type"
    - "Sidebar/action-bar/drawer surfaces are tokenized (bg AND ink)"
  artifacts:
    - path: "src/ui/components/GameShell.vue"
      provides: "platform-aware transparent backdrop + tokenized chrome surfaces"
      contains: "platformMode"
    - path: "src/ui/components/HamburgerMenu.vue"
      provides: "solid display title (no background-clip:text) + tokenized drawer"
  key_links:
    - from: "GameShell.vue / HamburgerMenu.vue backdrop"
      to: "platformMode flag"
      via: "transparent in platform mode, var(--bsg-bg) fallback in dev"
      pattern: "platformMode"
---

<objective>
Sweep the two shell-chrome files: make the hardcoded background gradients platform-aware (transparent in platform mode so the host shows through, `var(--bsg-bg)` in dev/standalone), tokenize the sidebar/action-bar/drawer surfaces, and delete HamburgerMenu's gradient clip-text title.

Purpose: THEME-07 (surfaces + platform transparency), THEME-04 (delete clip-text, solid display title). Paired because both own a hardcoded backdrop gradient gated by the same `platformMode` flag.
Output: Both files Slate, backdrop branches on `platformMode`, no clip-text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theming-swap-wave-2/99-CONTEXT.md
@src/ui/theme.ts
@src/ui/components/GameShell.vue
@src/ui/components/HamburgerMenu.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Platform-aware backdrop + tokenized surfaces in GameShell.vue</name>
  <files>src/ui/components/GameShell.vue</files>
  <read_first>
    - src/ui/components/GameShell.vue (the `platformMode` ref ~138 and its template usages; the hardcoded background gradient ~1443; sidebar + sticky action-bar surface CSS; `color:#fff` and `rgba(0,0,0,x)`/`rgba(255,255,255,.1)` glass/hairlines)
    - src/ui/theme.ts (bg, surface*, ink*, line, line-2)
  </read_first>
  <action>
    Atomic sweep:
    - Backdrop (~1443): branch on the existing `platformMode` flag — bind a class/style so platform mode renders `background: transparent` (host shows through) and dev/standalone renders `background: var(--bsg-bg)`. Prefer a `:class="{ 'platform-mode': platformMode }"` toggle on the root with CSS rules, or a computed style binding. Delete the hardcoded gradient literal.
    - Sidebar + sticky action-bar surfaces → `var(--bsg-surface)` with `var(--bsg-line)`/`var(--bsg-line-2)` hairlines, replacing `rgba(0,0,0,x)` glass and `rgba(255,255,255,.1)` white borders.
    - Every `color:#fff` → `var(--bsg-ink)`. Replace all remaining hex (7 in this file).
    Atomic rule: any surface whose background becomes a token gets its ink tokenized in the same edit.
  </action>
  <acceptance_criteria>
    - No hex in <style>; backdrop is `transparent` when `platformMode` is true and `var(--bsg-bg)` otherwise (grep shows both the platform branch and the var fallback).
    - Sidebar/action-bar surfaces use `var(--bsg-surface)` + `var(--bsg-line*)`; no `color:#fff` remains; bg+ink tokenized together.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/GameShell.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && grep -q 'platformMode\|platform-mode' src/ui/components/GameShell.vue && ! grep -nE 'color: ?#fff' src/ui/components/GameShell.vue && echo OK</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Delete clip-text + platform-aware drawer backdrop in HamburgerMenu.vue</name>
  <files>src/ui/components/HamburgerMenu.vue</files>
  <read_first>
    - src/ui/components/HamburgerMenu.vue (the gradient clip-text block ~218-221 `-webkit-background-clip: text; -webkit-text-fill-color: transparent;`; the hardcoded drawer backdrop gradient ~193; drawer surfaces; 16 hex)
    - src/ui/theme.ts (display, font, accent, surface*, ink*, line*, bg)
    - planning/phases note: in platform mode the drawer backdrop becomes transparent; dev falls back to var(--bsg-bg)
  </read_first>
  <action>
    Atomic sweep:
    - DELETE the gradient clip-text block (`background: linear-gradient(...)`, `-webkit-background-clip: text`, `-webkit-text-fill-color: transparent`). Replace with solid `color: var(--bsg-ink)` (or accent for emphasis) in `var(--bsg-display)` font with a faint `text-shadow`. No `background-clip:text` may remain.
    - Drawer backdrop (~193): branch on platform mode — transparent in platform mode (host shows through), `var(--bsg-bg)` fallback in dev/standalone. Use the platform signal available to this component (prop/inject or the same mechanism GameShell exposes); if none is wired, accept the `var(--bsg-bg)` surface and gate transparency via a `platform-mode` class toggled from the parent. Do NOT invent a new flag — reuse the existing platform signal.
    - Drawer surfaces → `var(--bsg-surface)` + `var(--bsg-line*)`; `color:#fff` → `var(--bsg-ink)`. Replace all 16 hex + any neon.
  </action>
  <acceptance_criteria>
    - No `background-clip` / `-webkit-text-fill-color: transparent` remains; title is solid `var(--bsg-display)`/`var(--bsg-ink|accent)` with text-shadow.
    - No hex in <style>; drawer surfaces tokenized; bg+ink tokenized together.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/HamburgerMenu.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -nE 'background-clip|text-fill-color: ?transparent' src/ui/components/HamburgerMenu.vue && echo OK</automated>
  </verify>
</task>

</tasks>

<verification>
- Both files pass inline color-no-hex; clip-text gone; backdrops branch on platform mode.
- `npx vitest run src/ui/components/HamburgerMenu.test.ts src/ui/components/GameShell.theme.test.ts` stays green (update any test asserting an old literal).
</verification>

<success_criteria>
Shell + drawer are Slate, backdrops are host-transparent in platform mode, and the clip-text title is solid display type.
</success_criteria>

<output>
Create `.planning/phases/99-theming-swap-wave-2/99-05-SUMMARY.md` when done.
</output>
