---
phase: 99-theming-swap-wave-2
plan: 08
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - src/ui/components/GameHistory.vue
  - src/ui/components/GameLobby.vue
autonomous: true
requirements: [THEME-07]
must_haves:
  truths:
    - "Game history surfaces, bullets, and 'you' badge are tokenized (bg AND ink)"
    - "The lobby surfaces and role chips are tokenized, not raw hex"
    - "No neon literal remains in either file"
  artifacts:
    - path: "src/ui/components/GameHistory.vue"
      provides: "tokenized history log surface"
      contains: "var(--bsg-"
    - path: "src/ui/components/GameLobby.vue"
      provides: "tokenized lobby surface + role chips"
      contains: "var(--bsg-"
  key_links:
    - from: "GameHistory.vue / GameLobby.vue"
      to: "theme.ts"
      via: "var(--bsg-surface|ink|accent|line)"
      pattern: "--bsg-(surface|ink|accent|line)"
---

<objective>
Sweep the two list-surface chrome files — GameHistory (the sidebar log, a KEY surface for the contrast test) and GameLobby (the in-repo lobby screen) — to Slate.

Purpose: THEME-07 (surfaces). Paired as two independent surface sweeps with the same pattern.
Output: Both fully tokenized, no neon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theming-swap-wave-2/99-CONTEXT.md
@src/ui/theme.ts
@src/ui/components/GameHistory.vue
@src/ui/components/GameLobby.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep GameHistory.vue (sidebar log)</name>
  <files>src/ui/components/GameHistory.vue</files>
  <read_first>
    - src/ui/components/GameHistory.vue (<style>; the log surface, history bullets, the `#00d9ff` "you"/accent badge; 18 hex; `rgba(0,0,0,x)` glass + `rgba(255,255,255,.1)` hairlines)
    - src/ui/theme.ts (surface*, ink*, accent, line*, field)
  </read_first>
  <action>
    Atomic sweep: log surface → `var(--bsg-surface)` + `var(--bsg-line*)` hairlines; bullets/markers/"you" badge `#00d9ff` → `var(--bsg-accent)`; entry text → `var(--bsg-ink)`/`var(--bsg-ink-2)`; `color:#fff` → `var(--bsg-ink)`. Replace all 18 hex + any neon. Any backgrounded row also tokenizes its ink in the same rule.
  </action>
  <acceptance_criteria>
    - No hex in <style>; no `#00d9ff`; surface uses `var(--bsg-surface)`; bg+ink tokenized together.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/GameHistory.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -nE '#00d9ff|color: ?#fff' src/ui/components/GameHistory.vue && echo OK</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Sweep GameLobby.vue (in-repo lobby)</name>
  <files>src/ui/components/GameLobby.vue</files>
  <read_first>
    - src/ui/components/GameLobby.vue (<style>; lobby surfaces, role/status chips with inline hex, the `#00d9ff` accent; 15 hex)
    - src/ui/theme.ts (surface*, ink*, accent, accent-2, line*, seat-1..6)
  </read_first>
  <action>
    Atomic sweep: lobby surfaces → `var(--bsg-surface)` + `var(--bsg-line*)`; role/status chips → token fills (`var(--bsg-surface-2)`/`var(--bsg-field)` or `var(--bsg-seat-*)` where they encode seat color) with `var(--bsg-ink)`/`var(--bsg-ink-2)` labels; `#00d9ff` → `var(--bsg-accent)`; `color:#fff` → `var(--bsg-ink)`. Replace all 15 hex. (This is the in-repo lobby only — the host PrimeVue lobby preset is OUT OF SCOPE; do not touch host files.)
  </action>
  <acceptance_criteria>
    - No hex in <style>; chips + surfaces tokenized; bg+ink tokenized together.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/GameLobby.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -nE '#00d9ff|color: ?#fff' src/ui/components/GameLobby.vue && echo OK</automated>
  </verify>
</task>

</tasks>

<verification>
- Both pass inline color-no-hex; no neon; surfaces tokenized.
- `npx vitest run src/ui/components` stays green.
</verification>

<success_criteria>
History log and in-repo lobby are fully Slate surfaces.
</success_criteria>

<output>
Create `.planning/phases/99-theming-swap-wave-2/99-08-SUMMARY.md` when done.
</output>
