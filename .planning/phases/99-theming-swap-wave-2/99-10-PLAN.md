---
phase: 99-theming-swap-wave-2
plan: 10
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - src/ui/components/Toast.vue
  - src/ui/components/auto-ui/DoneButton.vue
  - src/ui/components/auto-ui/AutoRenderer.vue
  - src/ui/components/auto-ui/AutoUI.vue
  - src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue
autonomous: true
requirements: [THEME-07]
must_haves:
  truths:
    - "Toast, DoneButton, AutoRenderer, AutoUI, and UnsupportedTopologyPanel carry no raw color literals"
    - "Each swept surface tokenizes bg AND ink together"
  artifacts:
    - path: "src/ui/components/Toast.vue"
      provides: "tokenized toast surface"
      contains: "var(--bsg-"
  key_links:
    - from: "these small components"
      to: "theme.ts"
      via: "var(--bsg-surface|ink|accent|danger|ok)"
      pattern: "--bsg-(surface|ink|accent|danger|ok)"
---

<objective>
Sweep the five small-literal components in one plan: Toast (4 hex), DoneButton (3), AutoRenderer (1), AutoUI (2), UnsupportedTopologyPanel (2). Each is a trivial mechanical tokenization; grouped because none warrants its own plan.

Purpose: THEME-07 (surfaces). These must all leave the ignoreFiles worklist.
Output: All five tokenized, no literals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theming-swap-wave-2/99-CONTEXT.md
@src/ui/theme.ts
@src/ui/components/Toast.vue
@src/ui/components/auto-ui/DoneButton.vue
@src/ui/components/auto-ui/AutoRenderer.vue
@src/ui/components/auto-ui/AutoUI.vue
@src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep Toast.vue + DoneButton.vue</name>
  <files>src/ui/components/Toast.vue, src/ui/components/auto-ui/DoneButton.vue</files>
  <read_first>
    - src/ui/components/Toast.vue (<style>; toast variants error/success/info; 4 hex + `rgba(46,204,113…)`; the `#00d9ff` info accent)
    - src/ui/components/auto-ui/DoneButton.vue (<style>; 3 hex incl. neon)
    - src/ui/theme.ts (accent, accent-ink, danger, ok, warn, surface*, ink*, line*, shadow-sm)
  </read_first>
  <action>
    Atomic sweep both: Toast error → `var(--bsg-danger)`, success → `var(--bsg-ok)`, info → `var(--bsg-accent)` with appropriate token ink; DoneButton primary → `var(--bsg-accent)` + `var(--bsg-accent-ink)` + neutral `var(--bsg-shadow-sm)` (no glow). Replace all hex + `#00d9ff`/`#00ff88`/`rgba(46,204,113…)`; `color:#fff` → token ink. bg+ink tokenized together.
  </action>
  <acceptance_criteria>
    - Neither file has hex; no neon; toast variants + button tokenized; bg+ink together.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/Toast.vue src/ui/components/auto-ui/DoneButton.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -rnE '#00d9ff|#00ff88|rgba\(46, ?204, ?113' src/ui/components/Toast.vue src/ui/components/auto-ui/DoneButton.vue && echo OK</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Sweep AutoRenderer.vue + AutoUI.vue + UnsupportedTopologyPanel.vue</name>
  <files>src/ui/components/auto-ui/AutoRenderer.vue, src/ui/components/auto-ui/AutoUI.vue, src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue</files>
  <read_first>
    - src/ui/components/auto-ui/AutoRenderer.vue (<style>; 1 hex)
    - src/ui/components/auto-ui/AutoUI.vue (<style>; 2 hex incl. neon)
    - src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue (<style>; 2 hex)
    - src/ui/theme.ts (surface*, ink*, accent, line*, warn)
  </read_first>
  <action>
    Atomic sweep all three: container/panel surfaces → `var(--bsg-surface*)` + `var(--bsg-line*)`; message text → `var(--bsg-ink)`/`var(--bsg-ink-2)`; any `#00d9ff`/neon → `var(--bsg-accent)`; `color:#fff` → `var(--bsg-ink)`. Replace all hex. (Color sweep only — do NOT restructure the UnsupportedTopologyPanel content; the DEV-05 message split is Phase 102.)
  </action>
  <acceptance_criteria>
    - None of the three has hex; no neon; surfaces/text tokenized; bg+ink together.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/auto-ui/AutoRenderer.vue src/ui/components/auto-ui/AutoUI.vue src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -rnE '#00d9ff|#00ff88|rgba\(46, ?204, ?113' src/ui/components/auto-ui/AutoRenderer.vue src/ui/components/auto-ui/AutoUI.vue src/ui/components/auto-ui/archetypes/UnsupportedTopologyPanel.vue && echo OK</automated>
  </verify>
</task>

</tasks>

<verification>
- All five pass inline color-no-hex; no neon.
- `npx vitest run src/ui/components` stays green.
</verification>

<success_criteria>
All five small components are literal-free and tokenized.
</success_criteria>

<output>
Create `.planning/phases/99-theming-swap-wave-2/99-10-SUMMARY.md` when done.
</output>
