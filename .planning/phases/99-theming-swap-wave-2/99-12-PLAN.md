---
phase: 99-theming-swap-wave-2
plan: 12
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - src/ui/components/DebugPanel.vue
autonomous: true
requirements: [THEME-07]
must_haves:
  truths:
    - "DebugPanel carries no raw color literal — all 155 hex / 113 rgba are tokens"
    - "Every swept surface tokenizes bg AND ink together (no invisible-text)"
    - "Color sweep ONLY — the DebugPanel reskin/structure is untouched (Phase 102)"
  artifacts:
    - path: "src/ui/components/DebugPanel.vue"
      provides: "fully tokenized debug-panel colors (no structural change)"
      contains: "var(--bsg-"
  key_links:
    - from: "DebugPanel.vue"
      to: "theme.ts"
      via: "var(--bsg-surface|ink|accent|line|mono)"
      pattern: "--bsg-(surface|ink|accent|line|mono)"
---

<objective>
Color-sweep DebugPanel — by far the most literal-dense file (155 hex / 113 rgba) — to Slate tokens, with NO structural/reskin change. Its size demands an isolated, purely mechanical plan.

Purpose: THEME-07 (surfaces). The file must leave the ignoreFiles worklist so the Wave 4 lint gate can pass. The DebugPanel reskin (real `<button aria-expanded>`, ARIA tabs, gated `D` shortcut) is DEV-01/Phase 102 — explicitly NOT in scope here.
Output: DebugPanel fully tokenized; structure and behavior unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theming-swap-wave-2/99-CONTEXT.md
@src/ui/theme.ts
@src/ui/components/DebugPanel.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mechanical color-only tokenization of DebugPanel.vue</name>
  <files>src/ui/components/DebugPanel.vue</files>
  <read_first>
    - src/ui/components/DebugPanel.vue (full <style>; panel chrome, tabs, log rows, JSON/code blocks, status colors; 155 hex incl. `#00d9ff`/`#00ff88`, 113 rgba glass/glow/`rgba(46,204,113…)`)
    - src/ui/theme.ts (surface*, ink*, accent, accent-2, danger, ok, warn, line*, field, mono, shadow, shadow-sm)
  </read_first>
  <action>
    Pure mechanical color sweep — change ONLY color values, never structure/markup/behavior. Rule-by-rule, atomic bg+ink:
    - Panel/tab/row surfaces → `var(--bsg-surface)`/`var(--bsg-surface-2)`/`var(--bsg-surface-3)` + `var(--bsg-line*)` hairlines, replacing `rgba(0,0,0,x)` glass and `rgba(255,255,255,x)` borders.
    - Code/JSON blocks → `var(--bsg-field)` bg + `var(--bsg-mono)` font + `var(--bsg-ink)`/`var(--bsg-ink-2)` text; syntax/status accents `#00d9ff`/`#00ff88`/`rgba(46,204,113…)` → `var(--bsg-accent)`/`var(--bsg-ok)`; errors → `var(--bsg-danger)`; warnings → `var(--bsg-warn)`.
    - Every `color:#fff` → `var(--bsg-ink)`. Remove every colored glow box-shadow → `var(--bsg-shadow*)`. Replace ALL 155 hex.
    Do NOT change any template structure, toggle behavior, keyboard shortcut, or tab logic — that is Phase 102.
  </action>
  <acceptance_criteria>
    - No hex anywhere in <style>; no `#00d9ff`/`#00ff88`/`rgba(46,204,113`; no `color:#fff`.
    - Every backgrounded surface tokenizes its ink in the same rule.
    - Template/script structure is unchanged (color-only diff): the diff touches only <style> color values (and inline style color bindings if any), not markup or logic.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && npx stylelint src/ui/components/DebugPanel.vue --config '{"customSyntax":"postcss-html","rules":{"color-no-hex":true}}' && ! grep -nE '#00d9ff|#00ff88|rgba\(46, ?204, ?113|color: ?#fff' src/ui/components/DebugPanel.vue && echo OK</automated>
  </verify>
</task>

</tasks>

<verification>
- DebugPanel passes inline color-no-hex; no neon; no colored glow; structure unchanged.
- `npx vitest run src/ui/components` stays green.
</verification>

<success_criteria>
DebugPanel colors are fully Slate via a pure color-only diff; the reskin remains deferred to Phase 102.
</success_criteria>

<output>
Create `.planning/phases/99-theming-swap-wave-2/99-12-SUMMARY.md` when done.
</output>
