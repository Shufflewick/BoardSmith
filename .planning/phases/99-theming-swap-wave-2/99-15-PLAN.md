---
phase: 99-theming-swap-wave-2
plan: 15
type: execute
wave: 4
depends_on: ["99-02", "99-03", "99-04", "99-05", "99-06", "99-07", "99-08", "99-09", "99-10", "99-11", "99-12", "99-13", "99-14"]
files_modified:
  - .stylelintrc.cjs
autonomous: true
requirements: [THEME-01, THEME-02, THEME-03, THEME-04, THEME-05, THEME-06, THEME-07, THEME-08]
must_haves:
  truths:
    - "The stylelint ignoreFiles list is EMPTY"
    - "npm run lint:css passes with color-no-hex fully blocking across all chrome/renderer .vue files"
    - "A residual-neon grep over src/ui and src/cli/dev-host returns nothing"
    - "The full test suite stays green"
  artifacts:
    - path: ".stylelintrc.cjs"
      provides: "ignoreFiles: [] — zero exclusions"
      contains: "ignoreFiles"
  key_links:
    - from: ".stylelintrc.cjs"
      to: "all swept .vue files"
      via: "color-no-hex enforced with zero ignores"
      pattern: "color-no-hex"
---

<objective>
The completion gate (correctness rule #3). Empty the `.stylelintrc.cjs` `ignoreFiles` list, run `npm run lint:css` with `color-no-hex` fully blocking, and prove no residual neon survives anywhere. This is the LAST wave — it depends on every sweep plan plus the contrast test.

Purpose: The phase is DONE only when `ignoreFiles` is empty AND `lint:css` is green AND the residual-neon grep returns nothing. This single serial gate owns `.stylelintrc.cjs` so the Wave 2 sweep plans never touch the shared config (keeping them parallel).
Output: `.stylelintrc.cjs` with `ignoreFiles: []`; green `lint:css`; clean residual grep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theming-swap-wave-2/99-CONTEXT.md
@.stylelintrc.cjs
@src/ui/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Empty ignoreFiles and prove lint:css is green</name>
  <files>.stylelintrc.cjs</files>
  <read_first>
    - .stylelintrc.cjs (the 18-entry ignoreFiles worklist + the explanatory header that says it MUST end at zero exclusions)
    - package.json (the `lint:css` script: `stylelint 'src/ui/**/*.vue'`)
  </read_first>
  <action>
    Set `ignoreFiles: []` (empty array) — remove all 18 entries. Also remove the now-obsolete "TEMPORARY ignore list" header comments that describe the worklist (keep the `color-no-hex` rule and the `customSyntax` config). Then run `npm run lint:css`.
    If lint:css reports ANY violation, that file was missed by a Wave 2 sweep — STOP and report the offending file(s) back rather than re-adding them to ignoreFiles (No Backward Compatibility: the list must end empty). Per CLAUDE.md, do not silence violations to "get it green" — the offending file must be swept.
  </action>
  <acceptance_criteria>
    - `.stylelintrc.cjs` has `ignoreFiles: []` (zero entries).
    - `npm run lint:css` exits 0 with `color-no-hex` active.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && node -e "const c=require('./.stylelintrc.cjs'); if((c.ignoreFiles||[]).length!==0){console.error('ignoreFiles not empty');process.exit(1)} if(c.rules['color-no-hex']!==true){console.error('color-no-hex not blocking');process.exit(1)} console.log('config OK')" && npm run lint:css</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Residual-neon grep + full suite green</name>
  <files>.stylelintrc.cjs</files>
  <read_first>
    - the residual targets: all of src/ui plus src/cli/dev-host (DevHost is not covered by lint:css, so the grep is its only gate)
  </read_first>
  <action>
    Run a residual-neon grep over `src/ui` and `src/cli/dev-host` for `#00d9ff`, `#00ff88`, and `rgba(46,204,113…)` — it MUST return nothing. Then run the full test suite; it MUST stay green (969+ from Phase 98). If any test asserts an old neon literal, it should already have been updated by the sweep plan that owned that component — if one slipped through, update the assertion to the token/Slate expectation. This task makes no source edits beyond fixing a stray test assertion if needed.
  </action>
  <acceptance_criteria>
    - Residual grep over src/ui + src/cli/dev-host returns zero matches for the three neon patterns.
    - `npm run test` is green.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jtsmith/BoardSmith && ! grep -rnE '#00d9ff|#00ff88|rgba\(46, ?204, ?113' src/ui src/cli/dev-host && npm run test</automated>
  </verify>
</task>

</tasks>

<verification>
- `.stylelintrc.cjs` ignoreFiles is `[]`; `npm run lint:css` green; residual-neon grep clean; full suite green.
</verification>

<success_criteria>
The phase is complete: zero stylelint exclusions, color-no-hex fully blocking, no neon literal anywhere, and the suite is green — the Slate swap is locked in.
</success_criteria>

<output>
Create `.planning/phases/99-theming-swap-wave-2/99-15-SUMMARY.md` when done.
</output>
