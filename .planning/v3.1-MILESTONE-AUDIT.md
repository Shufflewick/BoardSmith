---
milestone: v3.1
milestone_name: Dynamic Auto-UI
audited: 2026-06-22
status: passed
scores:
  requirements: 23/23
  phases: 7/7
  integration: 5/5 seams wired
  flows: 1/1 complete
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 95-ship-reframe
    items:
      - "Scaffold demo game: Game History panel stays empty + score stat reads 0 — pre-existing projection quirks in the placeholder rules template (not the scaffold/UI structure). Advisory follow-up."
  - phase: 93-renderer-rebuild
    items:
      - "RENDER-05: useAutoRendererAnimations registers deal/flip/reveal handlers (wired, 6 tests green) but the gate games don't emit these animation events; demo-animation exercises emit/move animations in-browser. Forward-ready wiring, accepted boundary — not a broken seam."
  - phase: 96-migration-cleanup
    items:
      - "MIGRATE-04: npm run audit (fallow) reports ~411 findings that are documented false-positives per CLAUDE.md (public API consumed by external game projects). No NEW dead code introduced by the migration; old renderer already deleted."
---

# v3.1 Dynamic Auto-UI — Milestone Audit

**Status: PASSED.** All 23 requirements satisfied, all 7 phases verified, all
cross-phase seams wired, the core E2E flow demonstrated. main suite 927/927.

## Definition of Done (ROADMAP) → met

> Replace the old auto-UI renderer with a dynamically-generated UI good enough to
> *play* a basic game before any custom UI is written; reuse the interaction
> substrate; ship via archetype templates; keep presentation out of the engine;
> migrate every game; delete the old paths.

- ✅ Old renderer (`AutoElement.vue` + `AutoGameBoard.vue`) **deleted** (Phase 93); grep-confirmed absent in code and across all game repos.
- ✅ New renderer: `AutoRenderer` (introspection-selected archetype templates) + ranked-tester renderer registry (`registerRenderer`) + closed-form grid/hex.
- ✅ Interaction substrate (`useActionController`/`useBoardInteraction`/drag/FLIP) reused unchanged; `refs[]` multi-ref protocol added across 7 layers.
- ✅ Presentation kept out of the engine: per-UI overlay sibling file, resolved AFTER engine visibility filtering (PRESENT-02 defense-in-depth at two enforcement points).
- ✅ Single-UI scaffold + production export via tree-shaking (proven by a real Vite build).
- ✅ All 8 `~/BoardSmithGames/` games migrated to single-UI and browser-verified playable; MERC canary 738/7 green.

## Phase verification roll-up

| Phase | Name | VERIFICATION |
|-------|------|--------------|
| 91 | Security Leak Fix | passed |
| 92 | Piece & Grid Rendering Fixes | passed |
| 93 | Renderer Rebuild | passed |
| 94 | Interaction, Presentation & Playability Gate | passed (reconciled; gates closed via 94.1) |
| 94.1 | Auto-UI Interaction Completion (inserted) | passed |
| 95 | Ship & Reframe | passed |
| 96 | Migration & Cleanup | passed |

## Requirements coverage: 23/23 satisfied

SEC-01/02 · PIECE-01/02/03 · RENDER-01/02/03/04/05 · INTERACT-01/02/03 ·
PRESENT-01/02/03 · SHIP-01/02/03 · MIGRATE-01/02/03/04 — all `[x]`, all
traceability rows Complete, each with verified cross-phase integration
touchpoints (see integration map below).

## Cross-phase integration: 5/5 seams wired

1. **Renderer ← substrate (93↔94):** `AutoRenderer`/registry consume the same `useActionController`/`useBoardInteraction`/`refs[]` substrate; no old-renderer imports remain.
2. **Interaction → presentation (94):** overlay resolved after `toJSONForPlayer` sets `__hidden`; `resolvePresentation` strips image/stats for hidden elements (two enforcement points).
3. **Scaffold → build (95):** `boardsmith.json "ui"` → exactly one static import; tree-shaking proven by real build; no `rollupOptions.input`.
4. **Library → games (96):** shared-plumbing changes broke no game; all consume the new public API; MERC 738/7.
5. **E2E:** scaffold → opens single-UI → playable via auto-UI — demonstrated by browser playthroughs (94.1 gate games + 96 all 8 games).

## Notable: net improvements beyond the original scope

- Fixed a **pre-existing scaffold-construction crash** (every freshly scaffolded game crashed with `_t` undefined) — found by the Phase 95 playability gate, fixed at the template source + regression tests.
- Fixed **2 pre-existing `src/ui` test failures** (useActionController followUp-skip for sendAction-only controllers; useBoardActionBridge deferred auto-start) — main went from 925/927 to **927/927** green.

## Verdict

Ship it. Tech-debt items above are accepted boundaries / documented false-positives, not blockers.
