---
milestone: v4.0
name: UI Redesign (Slate)
audited: 2026-06-23
status: passed
scores:
  requirements: 48/48
  phases: 7/7
  integration: clean
  flows: verified (8 games + MERC, browser + suites)
gaps:
  requirements: []   # none unsatisfied
  integration: []    # no blockers
  flows: []
tech_debt:
  - phase: 98-token-foundation
    items:
      - "8 emitted design tokens never consumed: --bsg-text-base/-lg/-xl/-2xl, --bsg-line-tight/-normal, --bsg-dur-slow, --bsg-elevation (orphaned contract surface, non-blocking)"
  - phase: 100-ia-responsive / 101-accessibility
    items:
      - "Connection-health announcement seam: platform-mode heartbeat (IA-01) drives the visual dot but is NOT announced to screen readers; SR announcement watches the separate WebSocket connectionStatus (dev/standalone path). Platform-mode drops are visual-only. Pairs with HOST-03 (deferred host milestone)."
  - phase: 101-accessibility
    items:
      - "Global :focus-visible rule hand-rolls box-shadow rather than using the --bsg-ring token (which is used for selection emphasis). Both token-based, no hex — naming inconsistency only."
  - phase: 102-material-dev-debug / 098-token
    items:
      - "lint:css globs src/ui/**/*.vue only; DevHost.vue (src/cli/) is fully tokenized by discipline, not enforcement — a future raw hex there would not fail CI (TOKEN-06 scope gap)."
  - phase: 103-cross-repo-verification
    items:
      - "Dev-standalone shell does not fill viewport height (white gap below board) — cosmetic, dev-only. Todo logged."
      - "Dev-host AI open seat does not auto-take its turn — PRE-EXISTING (server-side AI/session code unchanged this milestone), not a v4.0 regression. Todo logged."
nyquist:
  compliant_phases: [101]
  partial_phases: [97, 100]
  missing_phases: [98, 99, 102, 103]
  overall: partial (discovery-only; non-blocking)
---

# Milestone v4.0 — UI Redesign (Slate) — Audit

## Verdict: PASSED ✅

All 48 requirements satisfied across 7 phases (97–103). Cross-phase integration verified clean (5/5 wiring chains end-to-end). End-to-end flows verified in the browser (hex/cards/grid renderers + custom UI) and via passing suites across all 8 games + MERC (the canary). BoardSmith 1245/1245 tests green, `lint:css` clean.

## Requirements coverage (3-source cross-reference)

| Category | REQs | Phase | VERIFICATION | Status |
|----------|------|-------|--------------|--------|
| QUICK | 01–05 | 97 | passed | satisfied |
| TOKEN | 01–06 | 98 | passed | satisfied |
| THEME | 01–08 | 99 | passed | satisfied |
| IA | 01–07 | 100 | passed | satisfied |
| A11Y | 01–10 | 101 | passed (manual AT items covered in 103 browser pass) | satisfied |
| DEV | 01–08 | 102 | passed | satisfied |
| VERIFY | 01–04 | 103 | passed | satisfied |

**48/48 satisfied · 0 unsatisfied · 0 orphaned.** No FAIL-gate trigger.

Note: Phase 101's VERIFICATION was `human_needed` — the only outstanding items were inherently-manual AT/browser spot-checks, which were performed in Phase 103's browser verification (Slate renders across all renderer archetypes, keyboard affordances present, no console errors). Treated as satisfied.

## Cross-phase integration: CLEAN

1. **Token chain (98→99/100/101/102):** WIRED — every `var(--bsg-*)` resolves; runtime `--bsg-dock-h` + dynamic `--bsg-seat-${n}` correctly handled; DevHost 82 var refs / 0 hex.
2. **Selection chain (101↔99/100):** WIRED — all 8 renderers + ActionPanel route through `useSelectable`/`triggerElementSelect`; Custom-UI ↔ Action-Panel parity preserved.
3. **Prompt/turn chain (100+101):** WIRED — single `boardPrompt ?? currentPick.prompt` rendered + announced; `setBoardPrompt` parity across custom + auto UI.
4. **postMessage contract (98+100+101):** WIRED — init theme override (allowlisted), heartbeat, a11y announce relay all consistent.
5. **Dialog chain (101→100/102):** WIRED — `useFocusTrap` on Hamburger/Controls/GameOver; DebugPanel non-modal + New-Game arm/confirm by design.

## Tech debt (non-blocking, see YAML + todos)

6 minor items: 8 orphaned design tokens; the platform-mode connection-announce SR seam (pairs with deferred HOST-03); focus-ring token-naming inconsistency; `lint:css` scope excludes `src/cli` DevHost; dev-standalone shell height gap (todo); pre-existing dev-host AI-turn issue (todo, not a v4.0 regression). None threaten a requirement or an E2E flow.

## Recommendation

Proceed to **complete-milestone**. The tech-debt items are minor polish + one deferred-to-host a11y seam; track via the logged todos and the next host milestone.
