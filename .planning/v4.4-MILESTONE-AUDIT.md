---
milestone: v4.4
audited: 2026-07-02
status: passed
scores:
  requirements: 23/23
  phases: 8/8
  integration: 8/8
  flows: 3/3
gaps: []
tech_debt:
  - phase: 128-animation-drag-drop-test-story
    items:
      - "2 pre-existing eslint no-shadow errors in useFlyingElements.ts (documented backlog, predates v4.4)"
  - phase: repo-wide
    items:
      - "Pre-existing tsc test-file looseness in some BoardSmith src/ui test files (documented backlog, predates v4.4)"
      - "Pending todo: dev-host Debug-toggle panel not visibly opening (pre-existing, tracked in .planning/todos/pending/dev-host-debug-toggle-panel-not-opening.md)"
nyquist:
  compliant_phases: 8
  partial_phases: 0
  missing_phases: 0
  overall: compliant
---

# v4.4 Milestone Audit — Agent-Ergonomics Gaps (Audit Fixes)

**Status: PASSED** — 23/23 requirements satisfied, 8/8 phases verified `passed`, cross-phase integration wired, all E2E agent flows proven.

## Requirements Coverage (3-source cross-reference)

All 23 v1 requirements are: checked `[x]` in REQUIREMENTS.md, marked Complete in the traceability table, listed in phase SUMMARY frontmatter, and covered by a phase VERIFICATION.md with `status: passed`. Zero orphans, zero unsatisfied.

| Category | REQ-IDs | Phase | Status |
|----------|---------|-------|--------|
| FLOW | 01–04 | 123 | satisfied |
| VIS | 01–03 | 124 | satisfied |
| SIM | 01–02 | 125 | satisfied |
| ERR | 01–04 | 126 | satisfied |
| DRIVE | 01–03 | 127 | satisfied |
| ANIM | 01–03 | 128 | satisfied |
| MIG | 03–04 | 129 | satisfied |
| DOC | 05–06 | 130 | satisfied (DOC-06 delivered as `docs/migration-guide.md` `## v4.4` per recorded convention decision) |

## Phase Verifications

8/8 phases: VERIFICATION.md `status: passed`; VALIDATION.md `nyquist_compliant: true`.

## Cross-Phase Integration (integration-checker + gap closure)

All 7 wiring points WIRED with file:line evidence: shared `serializeFlowDebugInfo()` single producer; visibility utils on final-tree semantics consumed cross-repo via the `boardsmith/testing` barrel; simulate CLI on Phase 123 seeding (byte-identical same-seed runs); warnings/errorCode through the bridge wire; Vue-free trace module dual-exported; migration proven in 8 games + MERC.

The one gap found (Phase 127's acceptance test not asserting cross-phase payload content) was closed during the audit: commit `3b01ac5` adds 4 assertions (debug:flow-state description, seat-scoped pendingAction isolation, wire errorCode on failing action, debug:logs retrieval through the Node client). Full suite green after: 159 files / 2081 tests. Note: `flowDebugInfo` rides the `debug:flow-state` op by design (T-123-10), not `getState` — the checker's initial read of that leg was a design misread, now documented by the test itself.

## E2E Agent Flows

1. **Headless simulation:** `boardsmith simulate` — seeded, deterministic (byte-identical repeat), non-zero exit on failure; smoked on hex + checkers + go-fish. ✓
2. **Scriptable dev host:** `createDevHostClient` drives connect → join → getLobby → getState → action → control ops → debug:flow-state/debug:logs against a real WS host, browserless, with payload assertions. ✓
3. **Game-level guarantees:** go-fish DOM-leak (dual positive controls) + visibility assertions; cribbage visibility; demo-animation trace test. ✓

## Adversarial catches during the milestone (fixed, worth remembering)

- SnapshotSessionHost dev-host parity gap (browser checkpoint, Phase 123)
- Live mutable PendingActionState on the production wire (review, 123)
- `GameClass.playerView` bypass in visibility derivation (plan checker, 124)
- aria-label/alt/title leak-scan gap (review, 124)
- Bridge dropping `errorCode` on the wire (review, 126)
- 3 SDK promise-lifecycle criticals in dev-host-client (review, 127)
- Unlabeled-env crash risk in fail-loud throws → `isDevThrowEnabled()` (review, 128)
- Eager `@vue/test-utils` import breaking dep-less `boardsmith/testing` consumers (MERC re-vendor, 129)

## Tech Debt (non-blocking, carried)

See frontmatter. All items pre-existing or tracked as pending todos.
