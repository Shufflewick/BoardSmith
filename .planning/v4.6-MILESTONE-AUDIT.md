---
status: passed
milestone: v4.6
milestone_name: BS Skills (Rulebook-Driven Game Building)
audited: 2026-07-05
phase_count: 10
requirements_total: 36
requirements_complete: 36
outstanding_human_verification: 0
reopened_scope: "Phases 150–151 (GEN-01, PLAY-01) — closed VAL-01's deferred human gate 2026-07-05"
---

# v4.6 Milestone Audit — BS Skills (Rulebook-Driven Game Building)

**Status:** PASSED (human gate now CLOSED — see Reopened Scope section)
**Audited:** 2026-07-05 (reopened-scope audit appended 2026-07-05)

## Definition of Done

Replace `/design-game` with a family of `bs-` Claude Code skills that turn a game rulebook into a working BoardSmith game through small, adversarially-vetted, human-playtested chunks. Prove the pipeline end-to-end before pointing it at a real designer.

## Phase Verification Roll-Up

| Phase | Name | Status |
|-------|------|--------|
| 140 | Library Prerequisite — useAnnouncer() | passed |
| 141 | File Templates & State-Machine Authority | passed |
| 142 | /bs-ingest-rules | passed |
| 143 | /bs-build-chunk — Interpretation & Ask Gate | passed |
| 144 | /bs-build-chunk — Build & Test with UI Floor | passed |
| 145 | /bs-build-chunk — Audit & Repair with Design Review | passed |
| 146 | /bs-build-chunk — Playtest, Revise, Close & Final Acceptance | passed |
| 147 | /bs-check-status & /bs-insert-chunk | passed |
| 148 | Distribution — Installer & /bs-generate-ai | passed |
| 149 | End-to-End Dry-Run Validation | human_needed (deferred playtest) |

## Requirements Coverage — 34/34 Complete

3-source cross-reference (REQUIREMENTS.md traceability + phase VERIFICATION.md + SUMMARY frontmatter) confirms all 34 requirements Complete: LIB-01; TMPL-01..03; INGEST-01..07; BUILD-01..13; UIQ-01..05; STAT-01/02; DIST-01/02; VAL-01. Zero Pending.

## Cross-Phase Integration — Clean

The gsd-integration-checker traced the full skill family end-to-end: **0 orphaned, 0 missing, 0 blocking breaks.**

- **Installer ↔ skill-family paths:** every `${CLAUDE_SKILL_DIR}/../bs-shared/…` reference in the 5 entry points resolves to a file the Phase-148 installer actually copies under `bs-shared/`. No dangling references. `interview-fallback.md`'s relative `../aspects/index.md` verified correct within the `bs-shared/` root.
- **Templates/state-machine ↔ build-chunk/ingest:** the status enum (`proposed | approved | built | verified | verified (user-waived)` + the `stale — re-derive before build` and sketch-level tail markers) and the 10 step names are single-sourced in `state-machine.md` and cited by exact string — pinned by the 237-test drift suite (build-chunk 112, templates 44, ingest 40, status-tools 41).
- **A11y floor chains library → scaffold → skill:** Phase 140's `useAnnouncer()` (exported from `boardsmith/ui`) + the scaffold's `axe-core ^4.12.1` devDependency (project-scaffold.ts) → `bs/build/test.md` axe scan + `final-acceptance.md` announce-call check. Confirmed present in a real scaffolded project by the Phase 149 dry-run.
- **check-status/insert-chunk ↔ shared state:** read the same SKETCH.md/CHUNK.md structure ingest writes and build-chunk maintains.

## End-to-End Proof (VAL-01)

The Phase 149 dry-run is genuine execution evidence, not a paper walkthrough: `npx boardsmith init` → `npm install` → interview-fallback ingest of Go Fish → chunk-1 build through the real machine steps → generated code compiles (`tsc` clean), passes 16 tests + `boardsmith lint`, `simulateRandomGames` reaches terminal state without deadlock, and the **hidden-hand leak check (`diffPlayerViews`/`assertNoHiddenInfoLeak`) proves opponent hands do not leak** (with a non-vacuous control). Comparison vs hand-built go-fish: 3/4 axes match, 1 acceptable data-model divergence documented.

The dry-run surfaced and **fixed 2 real defects at source** (confirmed present in current source, not just the scratch): scaffold tsconfig missing `types:["vite/client"]` (every generated game would have failed `tsc`), and `boardsmith init` never running `git init` (making build-chunk's Git Protocol unimplementable on a fresh scaffold). Both fixes ship with load-bearing regression tests.

## Test Health

- BoardSmith full suite: **2652/2652 passing** across 184 files.
- bs- drift suite: **237/237** — the automated guard against cross-file/state-machine drift.
- Zero new npm dependencies added to BoardSmith's own manifest across the whole milestone (axe-core/jsdom/@vue/test-utils confined to the generated-game scaffold template).

## Adversarial Review Value

Every phase ran the discuss → research → plan → plan-check → execute → code-review (up to 3 fix iterations) → verify loop. The adversarial code-review loop caught defects the passing drift/unit tests could not — notably: Phase 144's two blocker-level bugs in the *generated-game* a11y harness (detached-node axe crash + missing jsdom devDep), Phase 146's router-ordering bug where the mandated final-acceptance chunk would have been silently run as an ordinary chunk, and Phase 148's data-loss edge where a `--force` reinstall could have wiped a user's unrelated `~/.claude/skills/templates` (fixed by namespacing under `bs-shared/`). These are exactly the class of pipeline defects the milestone existed to prevent shipping to a designer.

## Outstanding (Tracked, Deferred by Design)

- **Human browser-playtest of the dry-run's chunk-1** (`149-HUMAN-UAT.md`) — the pipeline's own designed human-only gate. Per the user's locked completion decision, the milestone ships with this one manual gate outstanding; it surfaces in `/gsd:audit-uat` and can be run against the scratch dry-run or the hand-built go-fish whenever convenient. This is the correct terminal state for VAL-01, not a milestone gap.

## Verdict

Milestone v4.6 achieved its definition of done. The `bs-` skill family is complete, self-consistent end-to-end, installable in the Agent Skills layout, `/design-game` is fully removed, and the pipeline is proven against a real rulebook. One inherently-human playtest gate is tracked and deferred per the user's explicit decision.

---

# Reopened Scope Audit — Playtest Follow-Up (Phases 150–151)

**Status: PASSED** — the deferred human gate is now **closed**.
**Audited:** 2026-07-05

v4.6 was reopened with two phases to close VAL-01's deferred human browser-playtest honestly (the Phase-149 dry-run built its game into a throwaway `/tmp` dir that cleanup deleted, so there was no pipeline-built game left to playtest).

## Reopened Requirements (2/2 satisfied)

| REQ-ID | Phase | VERIFICATION | Traceability | Final |
|--------|-------|--------------|--------------|-------|
| GEN-01 | 150 | passed | `[x]` | **satisfied** |
| PLAY-01 | 151 | passed | `[x]` | **satisfied** |

Milestone total is now **36/36** requirements (34 original + GEN-01 + PLAY-01). Zero unsatisfied, zero orphaned.

## Reopened Phases (2/2 passed)

| Phase | Status | Evidence |
|-------|--------|----------|
| 150 Regenerate pipeline-built Go Fish | passed (3/3) | Durable `~/BoardSmithGames/go-fish-dryrun/` outside /tmp; tsc clean; 38/38 tests incl. random-sim + dual hidden-info leak proofs; serves; UAT repointed; go-fish/ read-only held |
| 151 Human playtest | passed (human gate) | User playtested; gate caught DEF-A/DEF-B/DEF-C; all resolved (below); rules/UI/hidden-info/turn-handling verified correct in clean play |

## The Human Gate Did Its Job

The playtest caught three browser-level issues the automated bar (38/38, incl. random-sim + both leak checks) is structurally blind to:

- **DEF-A** (broken card images) — generated game referenced card art it never shipped. **Fixed**: generated the 52+1 card SVGs (`go-fish-dryrun 98d3d15`).
- **DEF-B** (ask "lockup" vs AI seat) — **a real, pre-existing BoardSmith dev-host lost-update race** (`SnapshotSessionHost.handleOp`/`runAITurns` unserialized). **Fixed at source**: per-host op-queue serialization (`281e8155`) + 2 regression tests. **Full suite 2653/2653.** This is a cross-cutting library win the milestone's automated discipline never surfaced — precisely the value of the human gate.
- **DEF-C** (2-browser "not your turn", one occurrence) — investigated with client+host instrumentation and guided repros; the UI action pipeline and turn handling (incl. AI-seat takeover) proved correct; **not reproducible post-fix**. Watch item, not a blocker.

## Tech Debt (non-blocking, tracked)

- `bs-build-chunk` skill gap: emit card assets (or CSS-fallback-guard the UI) so future generated games don't ship broken `<img>`s.
- DEF-C reconnect-storm watch item (on-file instrumentation approach to re-open if it recurs).
- MERC re-vendor to pick up the DEF-B session fix (`281e8155`).

## Reopened Verdict

The reopened scope achieved its purpose: the pipeline-built Go Fish was regenerated durably and **a human playtested it**, closing VAL-01's gate honestly. The gate earned its keep — it caught a genuine pre-existing dev-host concurrency bug that shipped green through every automated check. v4.6 is complete with **no outstanding human-verification items**.
