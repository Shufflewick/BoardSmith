---
milestone: v4.7
audited: 2026-07-06
status: passed
scores:
  requirements: 5/5
  phases: 3/3
  integration: 1/1
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 152-asset-completeness-in-bs-build-chunk
    items:
      - "IN-02 (pre-existing, out of scope): CardRenderer.vue's .card-image-back class has no CSS rule — dead class predating this phase; the new overlay <img> now also carries it. Follow-up cleanup only."
      - "Deferred (REQUIREMENTS Future): a first-class `boardsmith lint`/`build` CLI wiring for scanAssetReachability — today the gate is invoked via the bs-build-chunk test-step prose only (WR-04, docstring softened to reflect this)."
  - phase: 153-dev-host-multi-client-turn-desync-fix
    items:
      - "IN-01 (info, accepted): a fixed 20ms sleep in the stale-close integration test — de-risked by the subsequent polling waitFor; not flaky in practice."
  - phase: 154-merc-re-vendor
    items: []
nyquist:
  compliant_phases: []
  partial_phases: [152, 153]
  missing_phases: [154]
  overall: "advisory — all tests exist and pass; VALIDATION.md frontmatter flags (nyquist_compliant/wave_0_complete) were left false and not flipped. Non-blocking: real coverage is green, only the bookkeeping flags are stale."
---

# v4.7 Playtest Follow-Up Fixes — Milestone Audit

**Audited:** 2026-07-06
**Status:** ✅ PASSED
**Definition of done:** Close the three tracked follow-ups from v4.6's human playtest — DEF-A (asset completeness), DEF-C (dev-host reconnect turn-desync), and propagate DEF-B to MERC — with hardened `bs-` pipeline output, a fixed dev-host, and a green vendored consumer.

## Requirements Coverage (3-source cross-reference)

| REQ-ID | Phase | VERIFICATION | SUMMARY frontmatter | Traceability | Final |
|--------|-------|--------------|---------------------|--------------|-------|
| ASSET-01 | 152 | passed | listed | Complete | ✅ satisfied |
| ASSET-02 | 152 | passed | listed | Complete | ✅ satisfied |
| DEVHOST-01 | 153 | passed | listed | Complete | ✅ satisfied |
| DEVHOST-02 | 153 | passed | listed | Complete | ✅ satisfied |
| VENDOR-01 | 154 | passed | listed | Complete | ✅ satisfied |

**5/5 satisfied. No unsatisfied, no partial, no orphaned requirements.**

## Phase Verifications

| Phase | Status | Score | Evidence |
|-------|--------|-------|----------|
| 152 Asset Completeness | passed | 8/8 must-haves | Scaffold `AssetImage.vue` (always-drawn fallback + reveal-on-load), AutoUI CardRenderer/PieceRenderer guards, `asset-scan.ts` build-blocking gate, skill prose; full suite 2675; real-browser Playwright proof (zero visible broken `<img>`, no hand-added assets). Code review CR-01 (critical: missing src-change reset) fixed + regression-tested. |
| 153 Dev-Host Turn-Desync | passed | 5/5 must-haves | Root cause empirically reproduced (`dev.ts` stale-close orphaning a reconnected seat); one-line socket-identity guard, TDD RED→GREEN real-`ws` regression test; full suite 2677; Playwright 5× reload-storm + reconnect + AI-handoff proof (never orphaned). Code review WR-01 (handler duplication) fixed by extracting a shared `connection-handler.ts` both dev.ts and the test import (teeth verified). |
| 154 MERC Re-Vendor | passed | 6/6 must-haves | Re-vendored to `boardsmith-0.0.1-20260706164142.tgz` (carries DEF-B + 152 + 153, verified in installed copy); MERC suite independently re-run **738 passed / 7 skipped (28 files)** = exact baseline; MERC commit `38ac286`; no MERC-side source changes needed. |

## Cross-Phase Integration

**1/1 — verified.** The three phases live in disjoint subsystems (the `bs-build-chunk` scaffold/skill layer, the dev-host WebSocket transport, and MERC's vendor propagation) with no shared runtime wiring that could break across them. The milestone's cross-phase integration test IS Phase 154: the MERC re-vendor bundles DEF-B (`281e8155`) + Phase 152 + Phase 153 into one vendored `boardsmith` package and MERC's full 738-test suite passes green against it — the independent re-execution confirms every fix reaches the vendored consumer and the most complex game absorbs them with zero regressions and zero MERC-side changes.

## End-to-End Flows

| Flow | Status | Evidence |
|------|--------|----------|
| Generated game renders card/piece art with zero broken images (no hand-added assets) | ✅ | 152 real-browser proof: missing/no-src cards show drawn fallback, valid reveals on load |
| Dev-host client stays turn-consistent across reload/reconnect/AI-takeover | ✅ | 153 real-browser proof: 5× reload storm + reconnect + AI-handoff, `never_orphaned` |
| v4.7 fixes reach the vendored MERC consumer, suite stays green | ✅ | 154 MERC 738/7 independently re-run |

## Tech Debt (non-blocking)

- **152:** IN-02 dead `.card-image-back` CSS class (pre-existing); deferred first-class CLI wiring for `scanAssetReachability` (REQUIREMENTS Future).
- **153:** IN-01 fixed 20ms sleep in one integration test (de-risked by a following `waitFor`).
- **154:** none.

## Nyquist Coverage (advisory, discovery-only)

| Phase | VALIDATION.md | nyquist_compliant flag | Note |
|-------|---------------|------------------------|------|
| 152 | exists | false | Bookkeeping flag not flipped; actual per-req tests exist and pass |
| 153 | exists | false | Same — real coverage green |
| 154 | missing | — | Infrastructure phase (re-vendor); no VALIDATION.md by design |

All requirement tests physically exist and pass under `npm test` (BoardSmith 2677 green; MERC 738/7); only the `nyquist_compliant`/`wave_0_complete` frontmatter flags in 152/153's VALIDATION.md were left `false`. Non-blocking — this is a documentation-flag gap, not a coverage gap.

## Verdict

All 5 requirements satisfied, all 3 phases verified passed, cross-phase integration proven by the green MERC re-vendor, all 3 E2E flows confirmed (two by real-browser Playwright proofs). No critical gaps. Minor tech debt tracked. **v4.7 is ready to complete.**
</content>
