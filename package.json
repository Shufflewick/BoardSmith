{
  "name": "boardsmith",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "packages/games/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "cli": "npm run cli --workspace=@boardsmith/cli",
    "regen:llms": "npx repomix"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^22.0.0",
    "playwright": "^1.57.0",
    "puppeteer": "^24.32.1",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
