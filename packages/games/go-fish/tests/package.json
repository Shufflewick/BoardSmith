{
  "name": "@boardsmith/gofish-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --exclude '**/e2e-game.test.ts'",
    "test:watch": "vitest --exclude '**/e2e-game.test.ts'",
    "test:e2e": "vitest run --include '**/e2e-game.test.ts'"
  },
  "dependencies": {
    "@boardsmith/gofish-rules": "workspace:*",
    "@boardsmith/testing": "workspace:*",
    "@boardsmith/runtime": "workspace:*",
    "@boardsmith/engine": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "typescript": "^5.7.0",
    "playwright": "^1.40.0"
  }
}
