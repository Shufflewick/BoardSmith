{
  "revision": 8,
  "bundleProtocol": 1,
  "surfaceHash": "62514e31d9da2b2a",
  "payloadHash": "bf22b8c168ab229d",
  "history": [
    {
      "revision": 1,
      "date": "2026-08-03",
      "bundleProtocol": 1,
      "surfaceHash": "601ab669855fe0d5",
      "payloadHash": "39a105d8db41ec25",
      "summary": "Baseline, taken at the state following the UI-registry work: Deck defaults to count-only so draw piles report their size again, AutoUI and Die3D moved off the boardsmith/ui barrel, and the board-side ValidElement became BoardTarget. Also introduces the contract itself — ENGINE_CONTRACT and ENGINE_REVISION are exported from the boardsmith root so the platform can read the vendored engine's revision at runtime."
    },
    {
      "revision": 2,
      "date": "2026-08-04",
      "bundleProtocol": 1,
      "surfaceHash": "966f014fca003487",
      "payloadHash": "4e482261ddc4a6e9",
      "summary": "Contract fingerprints widened after an adversarial review found them narrower than claimed: surfaceHash now includes prototype members (games call methods, and renaming one used to move neither hash), and the payload fixture now boots through GameRunner with a simultaneous opening step so flow state, isMyTurn and availableActions are covered — the blank-action-bar layer, previously invisible."
    },
    {
      "revision": 3,
      "date": "2026-08-05",
      "bundleProtocol": 1,
      "surfaceHash": "a88e1e1ac6b840fc",
      "payloadHash": "4e482261ddc4a6e9",
      "summary": "Checkpoint retention: games can bound per-action undo checkpoints with checkpoints:{max|enabled}; snapshot.actionCheckpoints is now a {baseIndex,entries} window, and a pruned undo target is refused with a message naming the policy"
    },
    {
      "revision": 4,
      "date": "2026-08-05",
      "bundleProtocol": 1,
      "surfaceHash": "a88e1e1ac6b840fc",
      "payloadHash": "b6cc2b20433e3a3e",
      "summary": "Player-facing state changes: the default player palette now has 16 colors instead of 8 (the first 8 are unchanged, so existing games keep their colors) so a game can seat up to the platform's 16-player maximum without shipping its own palette; separately, a typed array (Uint8Array etc.) in game state now serializes as a compact {__typedArray,data} base64 shape and restores as its real type instead of silently inflating ~7x into a plain object, and an ArrayBuffer or DataView in state is now refused at serialization time rather than silently becoming {}."
    },
    {
      "revision": 5,
      "date": "2026-08-05",
      "bundleProtocol": 1,
      "surfaceHash": "db988455880ea279",
      "payloadHash": "bf22b8c168ab229d",
      "summary": "Player gains a first-class status field ('active' | 'dormant' | 'eliminated', default 'active') plus an isActive getter — it serializes into the tree and is visible in every seat's player view, so hosts can read seat liveness directly, and TurnOrder.ACTIVE_ONLY now filters on it instead of an undeclared 'eliminated' property. Also: every enterText selection is now length-bounded (DEFAULT_TEXT_MAX_LENGTH = 256 when a game sets no maxLength), and CheckpointPolicy is exported from the boardsmith root and boardsmith/session so a published bundle can name the type for GameDefinition.checkpoints."
    },
    {
      "revision": 6,
      "date": "2026-08-06",
      "bundleProtocol": 1,
      "surfaceHash": "aa0e6cb3e2aac241",
      "payloadHash": "bf22b8c168ab229d",
      "summary": "Action builder gains .validate(fn): a submit-time, whole-submission gate returning true | false | a message string, enforced on both the one-shot and incremental execute paths."
    },
    {
      "revision": 7,
      "date": "2026-08-06",
      "bundleProtocol": 1,
      "surfaceHash": "dfee85770a7d895d",
      "payloadHash": "bf22b8c168ab229d",
      "summary": "Engine exports turnSequence() and orderSeatsByTurn(): recover the running order eachPlayer already resolved, and arrange seats for display without dropping or duplicating any."
    },
    {
      "revision": 8,
      "date": "2026-08-06",
      "bundleProtocol": 1,
      "surfaceHash": "62514e31d9da2b2a",
      "payloadHash": "bf22b8c168ab229d",
      "summary": "Renamed ActionBuilder.suppressFromDock() to .suppressFromActionPanel() and its actionMetadata field to match: 'Action Panel' is now the single name for the control surface (the 'dock' synonym is retired everywhere, including the --dock-h CSS custom property, now --action-panel-h)."
    }
  ]
}
