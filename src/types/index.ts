/**
 * @module types
 *
 * Shared types for BoardSmith.
 *
 * ```typescript
 * import type { LobbyState, WebSocketMessage, ElementRef } from 'boardsmith/types';
 * ```
 */

export * from './protocol.js';
